(function(){const t=document.createElement("link").relList;if(t&&t.supports&&t.supports("modulepreload"))return;for(const r of document.querySelectorAll('link[rel="modulepreload"]'))a(r);new MutationObserver(r=>{for(const s of r)if(s.type==="childList")for(const i of s.addedNodes)i.tagName==="LINK"&&i.rel==="modulepreload"&&a(i)}).observe(document,{childList:!0,subtree:!0});function n(r){const s={};return r.integrity&&(s.integrity=r.integrity),r.referrerPolicy&&(s.referrerPolicy=r.referrerPolicy),r.crossOrigin==="use-credentials"?s.credentials="include":r.crossOrigin==="anonymous"?s.credentials="omit":s.credentials="same-origin",s}function a(r){if(r.ep)return;r.ep=!0;const s=n(r);fetch(r.href,s)}})();function cd(e){return e&&e.__esModule&&Object.prototype.hasOwnProperty.call(e,"default")?e.default:e}var Qo={exports:{}},nr={},Xo={exports:{}},I={};/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Xn=Symbol.for("react.element"),dd=Symbol.for("react.portal"),ud=Symbol.for("react.fragment"),md=Symbol.for("react.strict_mode"),pd=Symbol.for("react.profiler"),hd=Symbol.for("react.provider"),yd=Symbol.for("react.context"),fd=Symbol.for("react.forward_ref"),gd=Symbol.for("react.suspense"),bd=Symbol.for("react.memo"),vd=Symbol.for("react.lazy"),Li=Symbol.iterator;function wd(e){return e===null||typeof e!="object"?null:(e=Li&&e[Li]||e["@@iterator"],typeof e=="function"?e:null)}var Yo={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},$o=Object.assign,Zo={};function sn(e,t,n){this.props=e,this.context=t,this.refs=Zo,this.updater=n||Yo}sn.prototype.isReactComponent={};sn.prototype.setState=function(e,t){if(typeof e!="object"&&typeof e!="function"&&e!=null)throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");this.updater.enqueueSetState(this,e,t,"setState")};sn.prototype.forceUpdate=function(e){this.updater.enqueueForceUpdate(this,e,"forceUpdate")};function el(){}el.prototype=sn.prototype;function Bs(e,t,n){this.props=e,this.context=t,this.refs=Zo,this.updater=n||Yo}var Ds=Bs.prototype=new el;Ds.constructor=Bs;$o(Ds,sn.prototype);Ds.isPureReactComponent=!0;var Mi=Array.isArray,tl=Object.prototype.hasOwnProperty,Fs={current:null},nl={key:!0,ref:!0,__self:!0,__source:!0};function al(e,t,n){var a,r={},s=null,i=null;if(t!=null)for(a in t.ref!==void 0&&(i=t.ref),t.key!==void 0&&(s=""+t.key),t)tl.call(t,a)&&!nl.hasOwnProperty(a)&&(r[a]=t[a]);var o=arguments.length-2;if(o===1)r.children=n;else if(1<o){for(var l=Array(o),u=0;u<o;u++)l[u]=arguments[u+2];r.children=l}if(e&&e.defaultProps)for(a in o=e.defaultProps,o)r[a]===void 0&&(r[a]=o[a]);return{$$typeof:Xn,type:e,key:s,ref:i,props:r,_owner:Fs.current}}function Sd(e,t){return{$$typeof:Xn,type:e.type,key:t,ref:e.ref,props:e.props,_owner:e._owner}}function Us(e){return typeof e=="object"&&e!==null&&e.$$typeof===Xn}function kd(e){var t={"=":"=0",":":"=2"};return"$"+e.replace(/[=:]/g,function(n){return t[n]})}var Wi=/\/+/g;function Sr(e,t){return typeof e=="object"&&e!==null&&e.key!=null?kd(""+e.key):t.toString(36)}function wa(e,t,n,a,r){var s=typeof e;(s==="undefined"||s==="boolean")&&(e=null);var i=!1;if(e===null)i=!0;else switch(s){case"string":case"number":i=!0;break;case"object":switch(e.$$typeof){case Xn:case dd:i=!0}}if(i)return i=e,r=r(i),e=a===""?"."+Sr(i,0):a,Mi(r)?(n="",e!=null&&(n=e.replace(Wi,"$&/")+"/"),wa(r,t,n,"",function(u){return u})):r!=null&&(Us(r)&&(r=Sd(r,n+(!r.key||i&&i.key===r.key?"":(""+r.key).replace(Wi,"$&/")+"/")+e)),t.push(r)),1;if(i=0,a=a===""?".":a+":",Mi(e))for(var o=0;o<e.length;o++){s=e[o];var l=a+Sr(s,o);i+=wa(s,t,n,l,r)}else if(l=wd(e),typeof l=="function")for(e=l.call(e),o=0;!(s=e.next()).done;)s=s.value,l=a+Sr(s,o++),i+=wa(s,t,n,l,r);else if(s==="object")throw t=String(e),Error("Objects are not valid as a React child (found: "+(t==="[object Object]"?"object with keys {"+Object.keys(e).join(", ")+"}":t)+"). If you meant to render a collection of children, use an array instead.");return i}function aa(e,t,n){if(e==null)return e;var a=[],r=0;return wa(e,a,"","",function(s){return t.call(n,s,r++)}),a}function xd(e){if(e._status===-1){var t=e._result;t=t(),t.then(function(n){(e._status===0||e._status===-1)&&(e._status=1,e._result=n)},function(n){(e._status===0||e._status===-1)&&(e._status=2,e._result=n)}),e._status===-1&&(e._status=0,e._result=t)}if(e._status===1)return e._result.default;throw e._result}var le={current:null},Sa={transition:null},Cd={ReactCurrentDispatcher:le,ReactCurrentBatchConfig:Sa,ReactCurrentOwner:Fs};function rl(){throw Error("act(...) is not supported in production builds of React.")}I.Children={map:aa,forEach:function(e,t,n){aa(e,function(){t.apply(this,arguments)},n)},count:function(e){var t=0;return aa(e,function(){t++}),t},toArray:function(e){return aa(e,function(t){return t})||[]},only:function(e){if(!Us(e))throw Error("React.Children.only expected to receive a single React element child.");return e}};I.Component=sn;I.Fragment=ud;I.Profiler=pd;I.PureComponent=Bs;I.StrictMode=md;I.Suspense=gd;I.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Cd;I.act=rl;I.cloneElement=function(e,t,n){if(e==null)throw Error("React.cloneElement(...): The argument must be a React element, but you passed "+e+".");var a=$o({},e.props),r=e.key,s=e.ref,i=e._owner;if(t!=null){if(t.ref!==void 0&&(s=t.ref,i=Fs.current),t.key!==void 0&&(r=""+t.key),e.type&&e.type.defaultProps)var o=e.type.defaultProps;for(l in t)tl.call(t,l)&&!nl.hasOwnProperty(l)&&(a[l]=t[l]===void 0&&o!==void 0?o[l]:t[l])}var l=arguments.length-2;if(l===1)a.children=n;else if(1<l){o=Array(l);for(var u=0;u<l;u++)o[u]=arguments[u+2];a.children=o}return{$$typeof:Xn,type:e.type,key:r,ref:s,props:a,_owner:i}};I.createContext=function(e){return e={$$typeof:yd,_currentValue:e,_currentValue2:e,_threadCount:0,Provider:null,Consumer:null,_defaultValue:null,_globalName:null},e.Provider={$$typeof:hd,_context:e},e.Consumer=e};I.createElement=al;I.createFactory=function(e){var t=al.bind(null,e);return t.type=e,t};I.createRef=function(){return{current:null}};I.forwardRef=function(e){return{$$typeof:fd,render:e}};I.isValidElement=Us;I.lazy=function(e){return{$$typeof:vd,_payload:{_status:-1,_result:e},_init:xd}};I.memo=function(e,t){return{$$typeof:bd,type:e,compare:t===void 0?null:t}};I.startTransition=function(e){var t=Sa.transition;Sa.transition={};try{e()}finally{Sa.transition=t}};I.unstable_act=rl;I.useCallback=function(e,t){return le.current.useCallback(e,t)};I.useContext=function(e){return le.current.useContext(e)};I.useDebugValue=function(){};I.useDeferredValue=function(e){return le.current.useDeferredValue(e)};I.useEffect=function(e,t){return le.current.useEffect(e,t)};I.useId=function(){return le.current.useId()};I.useImperativeHandle=function(e,t,n){return le.current.useImperativeHandle(e,t,n)};I.useInsertionEffect=function(e,t){return le.current.useInsertionEffect(e,t)};I.useLayoutEffect=function(e,t){return le.current.useLayoutEffect(e,t)};I.useMemo=function(e,t){return le.current.useMemo(e,t)};I.useReducer=function(e,t,n){return le.current.useReducer(e,t,n)};I.useRef=function(e){return le.current.useRef(e)};I.useState=function(e){return le.current.useState(e)};I.useSyncExternalStore=function(e,t,n){return le.current.useSyncExternalStore(e,t,n)};I.useTransition=function(){return le.current.useTransition()};I.version="18.3.1";Xo.exports=I;var $=Xo.exports;const jd=cd($);/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Ed=$,Rd=Symbol.for("react.element"),Nd=Symbol.for("react.fragment"),Td=Object.prototype.hasOwnProperty,qd=Ed.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,Ad={key:!0,ref:!0,__self:!0,__source:!0};function sl(e,t,n){var a,r={},s=null,i=null;n!==void 0&&(s=""+n),t.key!==void 0&&(s=""+t.key),t.ref!==void 0&&(i=t.ref);for(a in t)Td.call(t,a)&&!Ad.hasOwnProperty(a)&&(r[a]=t[a]);if(e&&e.defaultProps)for(a in t=e.defaultProps,t)r[a]===void 0&&(r[a]=t[a]);return{$$typeof:Rd,type:e,key:s,ref:i,props:r,_owner:qd.current}}nr.Fragment=Nd;nr.jsx=sl;nr.jsxs=sl;Qo.exports=nr;var p=Qo.exports,_r={},il={exports:{}},ve={},ol={exports:{}},ll={};/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */(function(e){function t(j,N){var q=j.length;j.push(N);e:for(;0<q;){var z=q-1>>>1,Q=j[z];if(0<r(Q,N))j[z]=N,j[q]=Q,q=z;else break e}}function n(j){return j.length===0?null:j[0]}function a(j){if(j.length===0)return null;var N=j[0],q=j.pop();if(q!==N){j[0]=q;e:for(var z=0,Q=j.length,ta=Q>>>1;z<ta;){var ft=2*(z+1)-1,wr=j[ft],gt=ft+1,na=j[gt];if(0>r(wr,q))gt<Q&&0>r(na,wr)?(j[z]=na,j[gt]=q,z=gt):(j[z]=wr,j[ft]=q,z=ft);else if(gt<Q&&0>r(na,q))j[z]=na,j[gt]=q,z=gt;else break e}}return N}function r(j,N){var q=j.sortIndex-N.sortIndex;return q!==0?q:j.id-N.id}if(typeof performance=="object"&&typeof performance.now=="function"){var s=performance;e.unstable_now=function(){return s.now()}}else{var i=Date,o=i.now();e.unstable_now=function(){return i.now()-o}}var l=[],u=[],f=1,y=null,h=3,v=!1,w=!1,k=!1,T=typeof setTimeout=="function"?setTimeout:null,d=typeof clearTimeout=="function"?clearTimeout:null,c=typeof setImmediate<"u"?setImmediate:null;typeof navigator<"u"&&navigator.scheduling!==void 0&&navigator.scheduling.isInputPending!==void 0&&navigator.scheduling.isInputPending.bind(navigator.scheduling);function m(j){for(var N=n(u);N!==null;){if(N.callback===null)a(u);else if(N.startTime<=j)a(u),N.sortIndex=N.expirationTime,t(l,N);else break;N=n(u)}}function g(j){if(k=!1,m(j),!w)if(n(l)!==null)w=!0,br(S);else{var N=n(u);N!==null&&vr(g,N.startTime-j)}}function S(j,N){w=!1,k&&(k=!1,d(R),R=-1),v=!0;var q=h;try{for(m(N),y=n(l);y!==null&&(!(y.expirationTime>N)||j&&!Re());){var z=y.callback;if(typeof z=="function"){y.callback=null,h=y.priorityLevel;var Q=z(y.expirationTime<=N);N=e.unstable_now(),typeof Q=="function"?y.callback=Q:y===n(l)&&a(l),m(N)}else a(l);y=n(l)}if(y!==null)var ta=!0;else{var ft=n(u);ft!==null&&vr(g,ft.startTime-N),ta=!1}return ta}finally{y=null,h=q,v=!1}}var x=!1,E=null,R=-1,K=5,A=-1;function Re(){return!(e.unstable_now()-A<K)}function cn(){if(E!==null){var j=e.unstable_now();A=j;var N=!0;try{N=E(!0,j)}finally{N?dn():(x=!1,E=null)}}else x=!1}var dn;if(typeof c=="function")dn=function(){c(cn)};else if(typeof MessageChannel<"u"){var Oi=new MessageChannel,ld=Oi.port2;Oi.port1.onmessage=cn,dn=function(){ld.postMessage(null)}}else dn=function(){T(cn,0)};function br(j){E=j,x||(x=!0,dn())}function vr(j,N){R=T(function(){j(e.unstable_now())},N)}e.unstable_IdlePriority=5,e.unstable_ImmediatePriority=1,e.unstable_LowPriority=4,e.unstable_NormalPriority=3,e.unstable_Profiling=null,e.unstable_UserBlockingPriority=2,e.unstable_cancelCallback=function(j){j.callback=null},e.unstable_continueExecution=function(){w||v||(w=!0,br(S))},e.unstable_forceFrameRate=function(j){0>j||125<j?console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"):K=0<j?Math.floor(1e3/j):5},e.unstable_getCurrentPriorityLevel=function(){return h},e.unstable_getFirstCallbackNode=function(){return n(l)},e.unstable_next=function(j){switch(h){case 1:case 2:case 3:var N=3;break;default:N=h}var q=h;h=N;try{return j()}finally{h=q}},e.unstable_pauseExecution=function(){},e.unstable_requestPaint=function(){},e.unstable_runWithPriority=function(j,N){switch(j){case 1:case 2:case 3:case 4:case 5:break;default:j=3}var q=h;h=j;try{return N()}finally{h=q}},e.unstable_scheduleCallback=function(j,N,q){var z=e.unstable_now();switch(typeof q=="object"&&q!==null?(q=q.delay,q=typeof q=="number"&&0<q?z+q:z):q=z,j){case 1:var Q=-1;break;case 2:Q=250;break;case 5:Q=1073741823;break;case 4:Q=1e4;break;default:Q=5e3}return Q=q+Q,j={id:f++,callback:N,priorityLevel:j,startTime:q,expirationTime:Q,sortIndex:-1},q>z?(j.sortIndex=q,t(u,j),n(l)===null&&j===n(u)&&(k?(d(R),R=-1):k=!0,vr(g,q-z))):(j.sortIndex=Q,t(l,j),w||v||(w=!0,br(S))),j},e.unstable_shouldYield=Re,e.unstable_wrapCallback=function(j){var N=h;return function(){var q=h;h=N;try{return j.apply(this,arguments)}finally{h=q}}}})(ll);ol.exports=ll;var Id=ol.exports;/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Pd=$,be=Id;function b(e){for(var t="https://reactjs.org/docs/error-decoder.html?invariant="+e,n=1;n<arguments.length;n++)t+="&args[]="+encodeURIComponent(arguments[n]);return"Minified React error #"+e+"; visit "+t+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings."}var cl=new Set,Pn={};function qt(e,t){$t(e,t),$t(e+"Capture",t)}function $t(e,t){for(Pn[e]=t,e=0;e<t.length;e++)cl.add(t[e])}var Je=!(typeof window>"u"||typeof window.document>"u"||typeof window.document.createElement>"u"),Vr=Object.prototype.hasOwnProperty,Od=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/,Hi={},Ki={};function Ld(e){return Vr.call(Ki,e)?!0:Vr.call(Hi,e)?!1:Od.test(e)?Ki[e]=!0:(Hi[e]=!0,!1)}function Md(e,t,n,a){if(n!==null&&n.type===0)return!1;switch(typeof t){case"function":case"symbol":return!0;case"boolean":return a?!1:n!==null?!n.acceptsBooleans:(e=e.toLowerCase().slice(0,5),e!=="data-"&&e!=="aria-");default:return!1}}function Wd(e,t,n,a){if(t===null||typeof t>"u"||Md(e,t,n,a))return!0;if(a)return!1;if(n!==null)switch(n.type){case 3:return!t;case 4:return t===!1;case 5:return isNaN(t);case 6:return isNaN(t)||1>t}return!1}function ce(e,t,n,a,r,s,i){this.acceptsBooleans=t===2||t===3||t===4,this.attributeName=a,this.attributeNamespace=r,this.mustUseProperty=n,this.propertyName=e,this.type=t,this.sanitizeURL=s,this.removeEmptyString=i}var te={};"children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(e){te[e]=new ce(e,0,!1,e,null,!1,!1)});[["acceptCharset","accept-charset"],["className","class"],["htmlFor","for"],["httpEquiv","http-equiv"]].forEach(function(e){var t=e[0];te[t]=new ce(t,1,!1,e[1],null,!1,!1)});["contentEditable","draggable","spellCheck","value"].forEach(function(e){te[e]=new ce(e,2,!1,e.toLowerCase(),null,!1,!1)});["autoReverse","externalResourcesRequired","focusable","preserveAlpha"].forEach(function(e){te[e]=new ce(e,2,!1,e,null,!1,!1)});"allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(e){te[e]=new ce(e,3,!1,e.toLowerCase(),null,!1,!1)});["checked","multiple","muted","selected"].forEach(function(e){te[e]=new ce(e,3,!0,e,null,!1,!1)});["capture","download"].forEach(function(e){te[e]=new ce(e,4,!1,e,null,!1,!1)});["cols","rows","size","span"].forEach(function(e){te[e]=new ce(e,6,!1,e,null,!1,!1)});["rowSpan","start"].forEach(function(e){te[e]=new ce(e,5,!1,e.toLowerCase(),null,!1,!1)});var zs=/[\-:]([a-z])/g;function Js(e){return e[1].toUpperCase()}"accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(e){var t=e.replace(zs,Js);te[t]=new ce(t,1,!1,e,null,!1,!1)});"xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(e){var t=e.replace(zs,Js);te[t]=new ce(t,1,!1,e,"http://www.w3.org/1999/xlink",!1,!1)});["xml:base","xml:lang","xml:space"].forEach(function(e){var t=e.replace(zs,Js);te[t]=new ce(t,1,!1,e,"http://www.w3.org/XML/1998/namespace",!1,!1)});["tabIndex","crossOrigin"].forEach(function(e){te[e]=new ce(e,1,!1,e.toLowerCase(),null,!1,!1)});te.xlinkHref=new ce("xlinkHref",1,!1,"xlink:href","http://www.w3.org/1999/xlink",!0,!1);["src","href","action","formAction"].forEach(function(e){te[e]=new ce(e,1,!1,e.toLowerCase(),null,!0,!0)});function Gs(e,t,n,a){var r=te.hasOwnProperty(t)?te[t]:null;(r!==null?r.type!==0:a||!(2<t.length)||t[0]!=="o"&&t[0]!=="O"||t[1]!=="n"&&t[1]!=="N")&&(Wd(t,n,r,a)&&(n=null),a||r===null?Ld(t)&&(n===null?e.removeAttribute(t):e.setAttribute(t,""+n)):r.mustUseProperty?e[r.propertyName]=n===null?r.type===3?!1:"":n:(t=r.attributeName,a=r.attributeNamespace,n===null?e.removeAttribute(t):(r=r.type,n=r===3||r===4&&n===!0?"":""+n,a?e.setAttributeNS(a,t,n):e.setAttribute(t,n))))}var Qe=Pd.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,ra=Symbol.for("react.element"),Ot=Symbol.for("react.portal"),Lt=Symbol.for("react.fragment"),_s=Symbol.for("react.strict_mode"),Qr=Symbol.for("react.profiler"),dl=Symbol.for("react.provider"),ul=Symbol.for("react.context"),Vs=Symbol.for("react.forward_ref"),Xr=Symbol.for("react.suspense"),Yr=Symbol.for("react.suspense_list"),Qs=Symbol.for("react.memo"),Ye=Symbol.for("react.lazy"),ml=Symbol.for("react.offscreen"),Bi=Symbol.iterator;function un(e){return e===null||typeof e!="object"?null:(e=Bi&&e[Bi]||e["@@iterator"],typeof e=="function"?e:null)}var F=Object.assign,kr;function wn(e){if(kr===void 0)try{throw Error()}catch(n){var t=n.stack.trim().match(/\n( *(at )?)/);kr=t&&t[1]||""}return`
`+kr+e}var xr=!1;function Cr(e,t){if(!e||xr)return"";xr=!0;var n=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{if(t)if(t=function(){throw Error()},Object.defineProperty(t.prototype,"props",{set:function(){throw Error()}}),typeof Reflect=="object"&&Reflect.construct){try{Reflect.construct(t,[])}catch(u){var a=u}Reflect.construct(e,[],t)}else{try{t.call()}catch(u){a=u}e.call(t.prototype)}else{try{throw Error()}catch(u){a=u}e()}}catch(u){if(u&&a&&typeof u.stack=="string"){for(var r=u.stack.split(`
`),s=a.stack.split(`
`),i=r.length-1,o=s.length-1;1<=i&&0<=o&&r[i]!==s[o];)o--;for(;1<=i&&0<=o;i--,o--)if(r[i]!==s[o]){if(i!==1||o!==1)do if(i--,o--,0>o||r[i]!==s[o]){var l=`
`+r[i].replace(" at new "," at ");return e.displayName&&l.includes("<anonymous>")&&(l=l.replace("<anonymous>",e.displayName)),l}while(1<=i&&0<=o);break}}}finally{xr=!1,Error.prepareStackTrace=n}return(e=e?e.displayName||e.name:"")?wn(e):""}function Hd(e){switch(e.tag){case 5:return wn(e.type);case 16:return wn("Lazy");case 13:return wn("Suspense");case 19:return wn("SuspenseList");case 0:case 2:case 15:return e=Cr(e.type,!1),e;case 11:return e=Cr(e.type.render,!1),e;case 1:return e=Cr(e.type,!0),e;default:return""}}function $r(e){if(e==null)return null;if(typeof e=="function")return e.displayName||e.name||null;if(typeof e=="string")return e;switch(e){case Lt:return"Fragment";case Ot:return"Portal";case Qr:return"Profiler";case _s:return"StrictMode";case Xr:return"Suspense";case Yr:return"SuspenseList"}if(typeof e=="object")switch(e.$$typeof){case ul:return(e.displayName||"Context")+".Consumer";case dl:return(e._context.displayName||"Context")+".Provider";case Vs:var t=e.render;return e=e.displayName,e||(e=t.displayName||t.name||"",e=e!==""?"ForwardRef("+e+")":"ForwardRef"),e;case Qs:return t=e.displayName||null,t!==null?t:$r(e.type)||"Memo";case Ye:t=e._payload,e=e._init;try{return $r(e(t))}catch{}}return null}function Kd(e){var t=e.type;switch(e.tag){case 24:return"Cache";case 9:return(t.displayName||"Context")+".Consumer";case 10:return(t._context.displayName||"Context")+".Provider";case 18:return"DehydratedFragment";case 11:return e=t.render,e=e.displayName||e.name||"",t.displayName||(e!==""?"ForwardRef("+e+")":"ForwardRef");case 7:return"Fragment";case 5:return t;case 4:return"Portal";case 3:return"Root";case 6:return"Text";case 16:return $r(t);case 8:return t===_s?"StrictMode":"Mode";case 22:return"Offscreen";case 12:return"Profiler";case 21:return"Scope";case 13:return"Suspense";case 19:return"SuspenseList";case 25:return"TracingMarker";case 1:case 0:case 17:case 2:case 14:case 15:if(typeof t=="function")return t.displayName||t.name||null;if(typeof t=="string")return t}return null}function ut(e){switch(typeof e){case"boolean":case"number":case"string":case"undefined":return e;case"object":return e;default:return""}}function pl(e){var t=e.type;return(e=e.nodeName)&&e.toLowerCase()==="input"&&(t==="checkbox"||t==="radio")}function Bd(e){var t=pl(e)?"checked":"value",n=Object.getOwnPropertyDescriptor(e.constructor.prototype,t),a=""+e[t];if(!e.hasOwnProperty(t)&&typeof n<"u"&&typeof n.get=="function"&&typeof n.set=="function"){var r=n.get,s=n.set;return Object.defineProperty(e,t,{configurable:!0,get:function(){return r.call(this)},set:function(i){a=""+i,s.call(this,i)}}),Object.defineProperty(e,t,{enumerable:n.enumerable}),{getValue:function(){return a},setValue:function(i){a=""+i},stopTracking:function(){e._valueTracker=null,delete e[t]}}}}function sa(e){e._valueTracker||(e._valueTracker=Bd(e))}function hl(e){if(!e)return!1;var t=e._valueTracker;if(!t)return!0;var n=t.getValue(),a="";return e&&(a=pl(e)?e.checked?"true":"false":e.value),e=a,e!==n?(t.setValue(e),!0):!1}function Ia(e){if(e=e||(typeof document<"u"?document:void 0),typeof e>"u")return null;try{return e.activeElement||e.body}catch{return e.body}}function Zr(e,t){var n=t.checked;return F({},t,{defaultChecked:void 0,defaultValue:void 0,value:void 0,checked:n??e._wrapperState.initialChecked})}function Di(e,t){var n=t.defaultValue==null?"":t.defaultValue,a=t.checked!=null?t.checked:t.defaultChecked;n=ut(t.value!=null?t.value:n),e._wrapperState={initialChecked:a,initialValue:n,controlled:t.type==="checkbox"||t.type==="radio"?t.checked!=null:t.value!=null}}function yl(e,t){t=t.checked,t!=null&&Gs(e,"checked",t,!1)}function es(e,t){yl(e,t);var n=ut(t.value),a=t.type;if(n!=null)a==="number"?(n===0&&e.value===""||e.value!=n)&&(e.value=""+n):e.value!==""+n&&(e.value=""+n);else if(a==="submit"||a==="reset"){e.removeAttribute("value");return}t.hasOwnProperty("value")?ts(e,t.type,n):t.hasOwnProperty("defaultValue")&&ts(e,t.type,ut(t.defaultValue)),t.checked==null&&t.defaultChecked!=null&&(e.defaultChecked=!!t.defaultChecked)}function Fi(e,t,n){if(t.hasOwnProperty("value")||t.hasOwnProperty("defaultValue")){var a=t.type;if(!(a!=="submit"&&a!=="reset"||t.value!==void 0&&t.value!==null))return;t=""+e._wrapperState.initialValue,n||t===e.value||(e.value=t),e.defaultValue=t}n=e.name,n!==""&&(e.name=""),e.defaultChecked=!!e._wrapperState.initialChecked,n!==""&&(e.name=n)}function ts(e,t,n){(t!=="number"||Ia(e.ownerDocument)!==e)&&(n==null?e.defaultValue=""+e._wrapperState.initialValue:e.defaultValue!==""+n&&(e.defaultValue=""+n))}var Sn=Array.isArray;function Gt(e,t,n,a){if(e=e.options,t){t={};for(var r=0;r<n.length;r++)t["$"+n[r]]=!0;for(n=0;n<e.length;n++)r=t.hasOwnProperty("$"+e[n].value),e[n].selected!==r&&(e[n].selected=r),r&&a&&(e[n].defaultSelected=!0)}else{for(n=""+ut(n),t=null,r=0;r<e.length;r++){if(e[r].value===n){e[r].selected=!0,a&&(e[r].defaultSelected=!0);return}t!==null||e[r].disabled||(t=e[r])}t!==null&&(t.selected=!0)}}function ns(e,t){if(t.dangerouslySetInnerHTML!=null)throw Error(b(91));return F({},t,{value:void 0,defaultValue:void 0,children:""+e._wrapperState.initialValue})}function Ui(e,t){var n=t.value;if(n==null){if(n=t.children,t=t.defaultValue,n!=null){if(t!=null)throw Error(b(92));if(Sn(n)){if(1<n.length)throw Error(b(93));n=n[0]}t=n}t==null&&(t=""),n=t}e._wrapperState={initialValue:ut(n)}}function fl(e,t){var n=ut(t.value),a=ut(t.defaultValue);n!=null&&(n=""+n,n!==e.value&&(e.value=n),t.defaultValue==null&&e.defaultValue!==n&&(e.defaultValue=n)),a!=null&&(e.defaultValue=""+a)}function zi(e){var t=e.textContent;t===e._wrapperState.initialValue&&t!==""&&t!==null&&(e.value=t)}function gl(e){switch(e){case"svg":return"http://www.w3.org/2000/svg";case"math":return"http://www.w3.org/1998/Math/MathML";default:return"http://www.w3.org/1999/xhtml"}}function as(e,t){return e==null||e==="http://www.w3.org/1999/xhtml"?gl(t):e==="http://www.w3.org/2000/svg"&&t==="foreignObject"?"http://www.w3.org/1999/xhtml":e}var ia,bl=function(e){return typeof MSApp<"u"&&MSApp.execUnsafeLocalFunction?function(t,n,a,r){MSApp.execUnsafeLocalFunction(function(){return e(t,n,a,r)})}:e}(function(e,t){if(e.namespaceURI!=="http://www.w3.org/2000/svg"||"innerHTML"in e)e.innerHTML=t;else{for(ia=ia||document.createElement("div"),ia.innerHTML="<svg>"+t.valueOf().toString()+"</svg>",t=ia.firstChild;e.firstChild;)e.removeChild(e.firstChild);for(;t.firstChild;)e.appendChild(t.firstChild)}});function On(e,t){if(t){var n=e.firstChild;if(n&&n===e.lastChild&&n.nodeType===3){n.nodeValue=t;return}}e.textContent=t}var Cn={animationIterationCount:!0,aspectRatio:!0,borderImageOutset:!0,borderImageSlice:!0,borderImageWidth:!0,boxFlex:!0,boxFlexGroup:!0,boxOrdinalGroup:!0,columnCount:!0,columns:!0,flex:!0,flexGrow:!0,flexPositive:!0,flexShrink:!0,flexNegative:!0,flexOrder:!0,gridArea:!0,gridRow:!0,gridRowEnd:!0,gridRowSpan:!0,gridRowStart:!0,gridColumn:!0,gridColumnEnd:!0,gridColumnSpan:!0,gridColumnStart:!0,fontWeight:!0,lineClamp:!0,lineHeight:!0,opacity:!0,order:!0,orphans:!0,tabSize:!0,widows:!0,zIndex:!0,zoom:!0,fillOpacity:!0,floodOpacity:!0,stopOpacity:!0,strokeDasharray:!0,strokeDashoffset:!0,strokeMiterlimit:!0,strokeOpacity:!0,strokeWidth:!0},Dd=["Webkit","ms","Moz","O"];Object.keys(Cn).forEach(function(e){Dd.forEach(function(t){t=t+e.charAt(0).toUpperCase()+e.substring(1),Cn[t]=Cn[e]})});function vl(e,t,n){return t==null||typeof t=="boolean"||t===""?"":n||typeof t!="number"||t===0||Cn.hasOwnProperty(e)&&Cn[e]?(""+t).trim():t+"px"}function wl(e,t){e=e.style;for(var n in t)if(t.hasOwnProperty(n)){var a=n.indexOf("--")===0,r=vl(n,t[n],a);n==="float"&&(n="cssFloat"),a?e.setProperty(n,r):e[n]=r}}var Fd=F({menuitem:!0},{area:!0,base:!0,br:!0,col:!0,embed:!0,hr:!0,img:!0,input:!0,keygen:!0,link:!0,meta:!0,param:!0,source:!0,track:!0,wbr:!0});function rs(e,t){if(t){if(Fd[e]&&(t.children!=null||t.dangerouslySetInnerHTML!=null))throw Error(b(137,e));if(t.dangerouslySetInnerHTML!=null){if(t.children!=null)throw Error(b(60));if(typeof t.dangerouslySetInnerHTML!="object"||!("__html"in t.dangerouslySetInnerHTML))throw Error(b(61))}if(t.style!=null&&typeof t.style!="object")throw Error(b(62))}}function ss(e,t){if(e.indexOf("-")===-1)return typeof t.is=="string";switch(e){case"annotation-xml":case"color-profile":case"font-face":case"font-face-src":case"font-face-uri":case"font-face-format":case"font-face-name":case"missing-glyph":return!1;default:return!0}}var is=null;function Xs(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var os=null,_t=null,Vt=null;function Ji(e){if(e=Zn(e)){if(typeof os!="function")throw Error(b(280));var t=e.stateNode;t&&(t=or(t),os(e.stateNode,e.type,t))}}function Sl(e){_t?Vt?Vt.push(e):Vt=[e]:_t=e}function kl(){if(_t){var e=_t,t=Vt;if(Vt=_t=null,Ji(e),t)for(e=0;e<t.length;e++)Ji(t[e])}}function xl(e,t){return e(t)}function Cl(){}var jr=!1;function jl(e,t,n){if(jr)return e(t,n);jr=!0;try{return xl(e,t,n)}finally{jr=!1,(_t!==null||Vt!==null)&&(Cl(),kl())}}function Ln(e,t){var n=e.stateNode;if(n===null)return null;var a=or(n);if(a===null)return null;n=a[t];e:switch(t){case"onClick":case"onClickCapture":case"onDoubleClick":case"onDoubleClickCapture":case"onMouseDown":case"onMouseDownCapture":case"onMouseMove":case"onMouseMoveCapture":case"onMouseUp":case"onMouseUpCapture":case"onMouseEnter":(a=!a.disabled)||(e=e.type,a=!(e==="button"||e==="input"||e==="select"||e==="textarea")),e=!a;break e;default:e=!1}if(e)return null;if(n&&typeof n!="function")throw Error(b(231,t,typeof n));return n}var ls=!1;if(Je)try{var mn={};Object.defineProperty(mn,"passive",{get:function(){ls=!0}}),window.addEventListener("test",mn,mn),window.removeEventListener("test",mn,mn)}catch{ls=!1}function Ud(e,t,n,a,r,s,i,o,l){var u=Array.prototype.slice.call(arguments,3);try{t.apply(n,u)}catch(f){this.onError(f)}}var jn=!1,Pa=null,Oa=!1,cs=null,zd={onError:function(e){jn=!0,Pa=e}};function Jd(e,t,n,a,r,s,i,o,l){jn=!1,Pa=null,Ud.apply(zd,arguments)}function Gd(e,t,n,a,r,s,i,o,l){if(Jd.apply(this,arguments),jn){if(jn){var u=Pa;jn=!1,Pa=null}else throw Error(b(198));Oa||(Oa=!0,cs=u)}}function At(e){var t=e,n=e;if(e.alternate)for(;t.return;)t=t.return;else{e=t;do t=e,t.flags&4098&&(n=t.return),e=t.return;while(e)}return t.tag===3?n:null}function El(e){if(e.tag===13){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function Gi(e){if(At(e)!==e)throw Error(b(188))}function _d(e){var t=e.alternate;if(!t){if(t=At(e),t===null)throw Error(b(188));return t!==e?null:e}for(var n=e,a=t;;){var r=n.return;if(r===null)break;var s=r.alternate;if(s===null){if(a=r.return,a!==null){n=a;continue}break}if(r.child===s.child){for(s=r.child;s;){if(s===n)return Gi(r),e;if(s===a)return Gi(r),t;s=s.sibling}throw Error(b(188))}if(n.return!==a.return)n=r,a=s;else{for(var i=!1,o=r.child;o;){if(o===n){i=!0,n=r,a=s;break}if(o===a){i=!0,a=r,n=s;break}o=o.sibling}if(!i){for(o=s.child;o;){if(o===n){i=!0,n=s,a=r;break}if(o===a){i=!0,a=s,n=r;break}o=o.sibling}if(!i)throw Error(b(189))}}if(n.alternate!==a)throw Error(b(190))}if(n.tag!==3)throw Error(b(188));return n.stateNode.current===n?e:t}function Rl(e){return e=_d(e),e!==null?Nl(e):null}function Nl(e){if(e.tag===5||e.tag===6)return e;for(e=e.child;e!==null;){var t=Nl(e);if(t!==null)return t;e=e.sibling}return null}var Tl=be.unstable_scheduleCallback,_i=be.unstable_cancelCallback,Vd=be.unstable_shouldYield,Qd=be.unstable_requestPaint,J=be.unstable_now,Xd=be.unstable_getCurrentPriorityLevel,Ys=be.unstable_ImmediatePriority,ql=be.unstable_UserBlockingPriority,La=be.unstable_NormalPriority,Yd=be.unstable_LowPriority,Al=be.unstable_IdlePriority,ar=null,He=null;function $d(e){if(He&&typeof He.onCommitFiberRoot=="function")try{He.onCommitFiberRoot(ar,e,void 0,(e.current.flags&128)===128)}catch{}}var Ie=Math.clz32?Math.clz32:tu,Zd=Math.log,eu=Math.LN2;function tu(e){return e>>>=0,e===0?32:31-(Zd(e)/eu|0)|0}var oa=64,la=4194304;function kn(e){switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return e&4194240;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return e&130023424;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 1073741824;default:return e}}function Ma(e,t){var n=e.pendingLanes;if(n===0)return 0;var a=0,r=e.suspendedLanes,s=e.pingedLanes,i=n&268435455;if(i!==0){var o=i&~r;o!==0?a=kn(o):(s&=i,s!==0&&(a=kn(s)))}else i=n&~r,i!==0?a=kn(i):s!==0&&(a=kn(s));if(a===0)return 0;if(t!==0&&t!==a&&!(t&r)&&(r=a&-a,s=t&-t,r>=s||r===16&&(s&4194240)!==0))return t;if(a&4&&(a|=n&16),t=e.entangledLanes,t!==0)for(e=e.entanglements,t&=a;0<t;)n=31-Ie(t),r=1<<n,a|=e[n],t&=~r;return a}function nu(e,t){switch(e){case 1:case 2:case 4:return t+250;case 8:case 16:case 32:case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return t+5e3;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return-1;case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function au(e,t){for(var n=e.suspendedLanes,a=e.pingedLanes,r=e.expirationTimes,s=e.pendingLanes;0<s;){var i=31-Ie(s),o=1<<i,l=r[i];l===-1?(!(o&n)||o&a)&&(r[i]=nu(o,t)):l<=t&&(e.expiredLanes|=o),s&=~o}}function ds(e){return e=e.pendingLanes&-1073741825,e!==0?e:e&1073741824?1073741824:0}function Il(){var e=oa;return oa<<=1,!(oa&4194240)&&(oa=64),e}function Er(e){for(var t=[],n=0;31>n;n++)t.push(e);return t}function Yn(e,t,n){e.pendingLanes|=t,t!==536870912&&(e.suspendedLanes=0,e.pingedLanes=0),e=e.eventTimes,t=31-Ie(t),e[t]=n}function ru(e,t){var n=e.pendingLanes&~t;e.pendingLanes=t,e.suspendedLanes=0,e.pingedLanes=0,e.expiredLanes&=t,e.mutableReadLanes&=t,e.entangledLanes&=t,t=e.entanglements;var a=e.eventTimes;for(e=e.expirationTimes;0<n;){var r=31-Ie(n),s=1<<r;t[r]=0,a[r]=-1,e[r]=-1,n&=~s}}function $s(e,t){var n=e.entangledLanes|=t;for(e=e.entanglements;n;){var a=31-Ie(n),r=1<<a;r&t|e[a]&t&&(e[a]|=t),n&=~r}}var O=0;function Pl(e){return e&=-e,1<e?4<e?e&268435455?16:536870912:4:1}var Ol,Zs,Ll,Ml,Wl,us=!1,ca=[],at=null,rt=null,st=null,Mn=new Map,Wn=new Map,Ze=[],su="mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");function Vi(e,t){switch(e){case"focusin":case"focusout":at=null;break;case"dragenter":case"dragleave":rt=null;break;case"mouseover":case"mouseout":st=null;break;case"pointerover":case"pointerout":Mn.delete(t.pointerId);break;case"gotpointercapture":case"lostpointercapture":Wn.delete(t.pointerId)}}function pn(e,t,n,a,r,s){return e===null||e.nativeEvent!==s?(e={blockedOn:t,domEventName:n,eventSystemFlags:a,nativeEvent:s,targetContainers:[r]},t!==null&&(t=Zn(t),t!==null&&Zs(t)),e):(e.eventSystemFlags|=a,t=e.targetContainers,r!==null&&t.indexOf(r)===-1&&t.push(r),e)}function iu(e,t,n,a,r){switch(t){case"focusin":return at=pn(at,e,t,n,a,r),!0;case"dragenter":return rt=pn(rt,e,t,n,a,r),!0;case"mouseover":return st=pn(st,e,t,n,a,r),!0;case"pointerover":var s=r.pointerId;return Mn.set(s,pn(Mn.get(s)||null,e,t,n,a,r)),!0;case"gotpointercapture":return s=r.pointerId,Wn.set(s,pn(Wn.get(s)||null,e,t,n,a,r)),!0}return!1}function Hl(e){var t=wt(e.target);if(t!==null){var n=At(t);if(n!==null){if(t=n.tag,t===13){if(t=El(n),t!==null){e.blockedOn=t,Wl(e.priority,function(){Ll(n)});return}}else if(t===3&&n.stateNode.current.memoizedState.isDehydrated){e.blockedOn=n.tag===3?n.stateNode.containerInfo:null;return}}}e.blockedOn=null}function ka(e){if(e.blockedOn!==null)return!1;for(var t=e.targetContainers;0<t.length;){var n=ms(e.domEventName,e.eventSystemFlags,t[0],e.nativeEvent);if(n===null){n=e.nativeEvent;var a=new n.constructor(n.type,n);is=a,n.target.dispatchEvent(a),is=null}else return t=Zn(n),t!==null&&Zs(t),e.blockedOn=n,!1;t.shift()}return!0}function Qi(e,t,n){ka(e)&&n.delete(t)}function ou(){us=!1,at!==null&&ka(at)&&(at=null),rt!==null&&ka(rt)&&(rt=null),st!==null&&ka(st)&&(st=null),Mn.forEach(Qi),Wn.forEach(Qi)}function hn(e,t){e.blockedOn===t&&(e.blockedOn=null,us||(us=!0,be.unstable_scheduleCallback(be.unstable_NormalPriority,ou)))}function Hn(e){function t(r){return hn(r,e)}if(0<ca.length){hn(ca[0],e);for(var n=1;n<ca.length;n++){var a=ca[n];a.blockedOn===e&&(a.blockedOn=null)}}for(at!==null&&hn(at,e),rt!==null&&hn(rt,e),st!==null&&hn(st,e),Mn.forEach(t),Wn.forEach(t),n=0;n<Ze.length;n++)a=Ze[n],a.blockedOn===e&&(a.blockedOn=null);for(;0<Ze.length&&(n=Ze[0],n.blockedOn===null);)Hl(n),n.blockedOn===null&&Ze.shift()}var Qt=Qe.ReactCurrentBatchConfig,Wa=!0;function lu(e,t,n,a){var r=O,s=Qt.transition;Qt.transition=null;try{O=1,ei(e,t,n,a)}finally{O=r,Qt.transition=s}}function cu(e,t,n,a){var r=O,s=Qt.transition;Qt.transition=null;try{O=4,ei(e,t,n,a)}finally{O=r,Qt.transition=s}}function ei(e,t,n,a){if(Wa){var r=ms(e,t,n,a);if(r===null)Mr(e,t,a,Ha,n),Vi(e,a);else if(iu(r,e,t,n,a))a.stopPropagation();else if(Vi(e,a),t&4&&-1<su.indexOf(e)){for(;r!==null;){var s=Zn(r);if(s!==null&&Ol(s),s=ms(e,t,n,a),s===null&&Mr(e,t,a,Ha,n),s===r)break;r=s}r!==null&&a.stopPropagation()}else Mr(e,t,a,null,n)}}var Ha=null;function ms(e,t,n,a){if(Ha=null,e=Xs(a),e=wt(e),e!==null)if(t=At(e),t===null)e=null;else if(n=t.tag,n===13){if(e=El(t),e!==null)return e;e=null}else if(n===3){if(t.stateNode.current.memoizedState.isDehydrated)return t.tag===3?t.stateNode.containerInfo:null;e=null}else t!==e&&(e=null);return Ha=e,null}function Kl(e){switch(e){case"cancel":case"click":case"close":case"contextmenu":case"copy":case"cut":case"auxclick":case"dblclick":case"dragend":case"dragstart":case"drop":case"focusin":case"focusout":case"input":case"invalid":case"keydown":case"keypress":case"keyup":case"mousedown":case"mouseup":case"paste":case"pause":case"play":case"pointercancel":case"pointerdown":case"pointerup":case"ratechange":case"reset":case"resize":case"seeked":case"submit":case"touchcancel":case"touchend":case"touchstart":case"volumechange":case"change":case"selectionchange":case"textInput":case"compositionstart":case"compositionend":case"compositionupdate":case"beforeblur":case"afterblur":case"beforeinput":case"blur":case"fullscreenchange":case"focus":case"hashchange":case"popstate":case"select":case"selectstart":return 1;case"drag":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"mousemove":case"mouseout":case"mouseover":case"pointermove":case"pointerout":case"pointerover":case"scroll":case"toggle":case"touchmove":case"wheel":case"mouseenter":case"mouseleave":case"pointerenter":case"pointerleave":return 4;case"message":switch(Xd()){case Ys:return 1;case ql:return 4;case La:case Yd:return 16;case Al:return 536870912;default:return 16}default:return 16}}var tt=null,ti=null,xa=null;function Bl(){if(xa)return xa;var e,t=ti,n=t.length,a,r="value"in tt?tt.value:tt.textContent,s=r.length;for(e=0;e<n&&t[e]===r[e];e++);var i=n-e;for(a=1;a<=i&&t[n-a]===r[s-a];a++);return xa=r.slice(e,1<a?1-a:void 0)}function Ca(e){var t=e.keyCode;return"charCode"in e?(e=e.charCode,e===0&&t===13&&(e=13)):e=t,e===10&&(e=13),32<=e||e===13?e:0}function da(){return!0}function Xi(){return!1}function we(e){function t(n,a,r,s,i){this._reactName=n,this._targetInst=r,this.type=a,this.nativeEvent=s,this.target=i,this.currentTarget=null;for(var o in e)e.hasOwnProperty(o)&&(n=e[o],this[o]=n?n(s):s[o]);return this.isDefaultPrevented=(s.defaultPrevented!=null?s.defaultPrevented:s.returnValue===!1)?da:Xi,this.isPropagationStopped=Xi,this}return F(t.prototype,{preventDefault:function(){this.defaultPrevented=!0;var n=this.nativeEvent;n&&(n.preventDefault?n.preventDefault():typeof n.returnValue!="unknown"&&(n.returnValue=!1),this.isDefaultPrevented=da)},stopPropagation:function(){var n=this.nativeEvent;n&&(n.stopPropagation?n.stopPropagation():typeof n.cancelBubble!="unknown"&&(n.cancelBubble=!0),this.isPropagationStopped=da)},persist:function(){},isPersistent:da}),t}var on={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},ni=we(on),$n=F({},on,{view:0,detail:0}),du=we($n),Rr,Nr,yn,rr=F({},$n,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:ai,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return"movementX"in e?e.movementX:(e!==yn&&(yn&&e.type==="mousemove"?(Rr=e.screenX-yn.screenX,Nr=e.screenY-yn.screenY):Nr=Rr=0,yn=e),Rr)},movementY:function(e){return"movementY"in e?e.movementY:Nr}}),Yi=we(rr),uu=F({},rr,{dataTransfer:0}),mu=we(uu),pu=F({},$n,{relatedTarget:0}),Tr=we(pu),hu=F({},on,{animationName:0,elapsedTime:0,pseudoElement:0}),yu=we(hu),fu=F({},on,{clipboardData:function(e){return"clipboardData"in e?e.clipboardData:window.clipboardData}}),gu=we(fu),bu=F({},on,{data:0}),$i=we(bu),vu={Esc:"Escape",Spacebar:" ",Left:"ArrowLeft",Up:"ArrowUp",Right:"ArrowRight",Down:"ArrowDown",Del:"Delete",Win:"OS",Menu:"ContextMenu",Apps:"ContextMenu",Scroll:"ScrollLock",MozPrintableKey:"Unidentified"},wu={8:"Backspace",9:"Tab",12:"Clear",13:"Enter",16:"Shift",17:"Control",18:"Alt",19:"Pause",20:"CapsLock",27:"Escape",32:" ",33:"PageUp",34:"PageDown",35:"End",36:"Home",37:"ArrowLeft",38:"ArrowUp",39:"ArrowRight",40:"ArrowDown",45:"Insert",46:"Delete",112:"F1",113:"F2",114:"F3",115:"F4",116:"F5",117:"F6",118:"F7",119:"F8",120:"F9",121:"F10",122:"F11",123:"F12",144:"NumLock",145:"ScrollLock",224:"Meta"},Su={Alt:"altKey",Control:"ctrlKey",Meta:"metaKey",Shift:"shiftKey"};function ku(e){var t=this.nativeEvent;return t.getModifierState?t.getModifierState(e):(e=Su[e])?!!t[e]:!1}function ai(){return ku}var xu=F({},$n,{key:function(e){if(e.key){var t=vu[e.key]||e.key;if(t!=="Unidentified")return t}return e.type==="keypress"?(e=Ca(e),e===13?"Enter":String.fromCharCode(e)):e.type==="keydown"||e.type==="keyup"?wu[e.keyCode]||"Unidentified":""},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:ai,charCode:function(e){return e.type==="keypress"?Ca(e):0},keyCode:function(e){return e.type==="keydown"||e.type==="keyup"?e.keyCode:0},which:function(e){return e.type==="keypress"?Ca(e):e.type==="keydown"||e.type==="keyup"?e.keyCode:0}}),Cu=we(xu),ju=F({},rr,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0}),Zi=we(ju),Eu=F({},$n,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:ai}),Ru=we(Eu),Nu=F({},on,{propertyName:0,elapsedTime:0,pseudoElement:0}),Tu=we(Nu),qu=F({},rr,{deltaX:function(e){return"deltaX"in e?e.deltaX:"wheelDeltaX"in e?-e.wheelDeltaX:0},deltaY:function(e){return"deltaY"in e?e.deltaY:"wheelDeltaY"in e?-e.wheelDeltaY:"wheelDelta"in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0}),Au=we(qu),Iu=[9,13,27,32],ri=Je&&"CompositionEvent"in window,En=null;Je&&"documentMode"in document&&(En=document.documentMode);var Pu=Je&&"TextEvent"in window&&!En,Dl=Je&&(!ri||En&&8<En&&11>=En),eo=" ",to=!1;function Fl(e,t){switch(e){case"keyup":return Iu.indexOf(t.keyCode)!==-1;case"keydown":return t.keyCode!==229;case"keypress":case"mousedown":case"focusout":return!0;default:return!1}}function Ul(e){return e=e.detail,typeof e=="object"&&"data"in e?e.data:null}var Mt=!1;function Ou(e,t){switch(e){case"compositionend":return Ul(t);case"keypress":return t.which!==32?null:(to=!0,eo);case"textInput":return e=t.data,e===eo&&to?null:e;default:return null}}function Lu(e,t){if(Mt)return e==="compositionend"||!ri&&Fl(e,t)?(e=Bl(),xa=ti=tt=null,Mt=!1,e):null;switch(e){case"paste":return null;case"keypress":if(!(t.ctrlKey||t.altKey||t.metaKey)||t.ctrlKey&&t.altKey){if(t.char&&1<t.char.length)return t.char;if(t.which)return String.fromCharCode(t.which)}return null;case"compositionend":return Dl&&t.locale!=="ko"?null:t.data;default:return null}}var Mu={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function no(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t==="input"?!!Mu[e.type]:t==="textarea"}function zl(e,t,n,a){Sl(a),t=Ka(t,"onChange"),0<t.length&&(n=new ni("onChange","change",null,n,a),e.push({event:n,listeners:t}))}var Rn=null,Kn=null;function Wu(e){tc(e,0)}function sr(e){var t=Kt(e);if(hl(t))return e}function Hu(e,t){if(e==="change")return t}var Jl=!1;if(Je){var qr;if(Je){var Ar="oninput"in document;if(!Ar){var ao=document.createElement("div");ao.setAttribute("oninput","return;"),Ar=typeof ao.oninput=="function"}qr=Ar}else qr=!1;Jl=qr&&(!document.documentMode||9<document.documentMode)}function ro(){Rn&&(Rn.detachEvent("onpropertychange",Gl),Kn=Rn=null)}function Gl(e){if(e.propertyName==="value"&&sr(Kn)){var t=[];zl(t,Kn,e,Xs(e)),jl(Wu,t)}}function Ku(e,t,n){e==="focusin"?(ro(),Rn=t,Kn=n,Rn.attachEvent("onpropertychange",Gl)):e==="focusout"&&ro()}function Bu(e){if(e==="selectionchange"||e==="keyup"||e==="keydown")return sr(Kn)}function Du(e,t){if(e==="click")return sr(t)}function Fu(e,t){if(e==="input"||e==="change")return sr(t)}function Uu(e,t){return e===t&&(e!==0||1/e===1/t)||e!==e&&t!==t}var Oe=typeof Object.is=="function"?Object.is:Uu;function Bn(e,t){if(Oe(e,t))return!0;if(typeof e!="object"||e===null||typeof t!="object"||t===null)return!1;var n=Object.keys(e),a=Object.keys(t);if(n.length!==a.length)return!1;for(a=0;a<n.length;a++){var r=n[a];if(!Vr.call(t,r)||!Oe(e[r],t[r]))return!1}return!0}function so(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function io(e,t){var n=so(e);e=0;for(var a;n;){if(n.nodeType===3){if(a=e+n.textContent.length,e<=t&&a>=t)return{node:n,offset:t-e};e=a}e:{for(;n;){if(n.nextSibling){n=n.nextSibling;break e}n=n.parentNode}n=void 0}n=so(n)}}function _l(e,t){return e&&t?e===t?!0:e&&e.nodeType===3?!1:t&&t.nodeType===3?_l(e,t.parentNode):"contains"in e?e.contains(t):e.compareDocumentPosition?!!(e.compareDocumentPosition(t)&16):!1:!1}function Vl(){for(var e=window,t=Ia();t instanceof e.HTMLIFrameElement;){try{var n=typeof t.contentWindow.location.href=="string"}catch{n=!1}if(n)e=t.contentWindow;else break;t=Ia(e.document)}return t}function si(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t&&(t==="input"&&(e.type==="text"||e.type==="search"||e.type==="tel"||e.type==="url"||e.type==="password")||t==="textarea"||e.contentEditable==="true")}function zu(e){var t=Vl(),n=e.focusedElem,a=e.selectionRange;if(t!==n&&n&&n.ownerDocument&&_l(n.ownerDocument.documentElement,n)){if(a!==null&&si(n)){if(t=a.start,e=a.end,e===void 0&&(e=t),"selectionStart"in n)n.selectionStart=t,n.selectionEnd=Math.min(e,n.value.length);else if(e=(t=n.ownerDocument||document)&&t.defaultView||window,e.getSelection){e=e.getSelection();var r=n.textContent.length,s=Math.min(a.start,r);a=a.end===void 0?s:Math.min(a.end,r),!e.extend&&s>a&&(r=a,a=s,s=r),r=io(n,s);var i=io(n,a);r&&i&&(e.rangeCount!==1||e.anchorNode!==r.node||e.anchorOffset!==r.offset||e.focusNode!==i.node||e.focusOffset!==i.offset)&&(t=t.createRange(),t.setStart(r.node,r.offset),e.removeAllRanges(),s>a?(e.addRange(t),e.extend(i.node,i.offset)):(t.setEnd(i.node,i.offset),e.addRange(t)))}}for(t=[],e=n;e=e.parentNode;)e.nodeType===1&&t.push({element:e,left:e.scrollLeft,top:e.scrollTop});for(typeof n.focus=="function"&&n.focus(),n=0;n<t.length;n++)e=t[n],e.element.scrollLeft=e.left,e.element.scrollTop=e.top}}var Ju=Je&&"documentMode"in document&&11>=document.documentMode,Wt=null,ps=null,Nn=null,hs=!1;function oo(e,t,n){var a=n.window===n?n.document:n.nodeType===9?n:n.ownerDocument;hs||Wt==null||Wt!==Ia(a)||(a=Wt,"selectionStart"in a&&si(a)?a={start:a.selectionStart,end:a.selectionEnd}:(a=(a.ownerDocument&&a.ownerDocument.defaultView||window).getSelection(),a={anchorNode:a.anchorNode,anchorOffset:a.anchorOffset,focusNode:a.focusNode,focusOffset:a.focusOffset}),Nn&&Bn(Nn,a)||(Nn=a,a=Ka(ps,"onSelect"),0<a.length&&(t=new ni("onSelect","select",null,t,n),e.push({event:t,listeners:a}),t.target=Wt)))}function ua(e,t){var n={};return n[e.toLowerCase()]=t.toLowerCase(),n["Webkit"+e]="webkit"+t,n["Moz"+e]="moz"+t,n}var Ht={animationend:ua("Animation","AnimationEnd"),animationiteration:ua("Animation","AnimationIteration"),animationstart:ua("Animation","AnimationStart"),transitionend:ua("Transition","TransitionEnd")},Ir={},Ql={};Je&&(Ql=document.createElement("div").style,"AnimationEvent"in window||(delete Ht.animationend.animation,delete Ht.animationiteration.animation,delete Ht.animationstart.animation),"TransitionEvent"in window||delete Ht.transitionend.transition);function ir(e){if(Ir[e])return Ir[e];if(!Ht[e])return e;var t=Ht[e],n;for(n in t)if(t.hasOwnProperty(n)&&n in Ql)return Ir[e]=t[n];return e}var Xl=ir("animationend"),Yl=ir("animationiteration"),$l=ir("animationstart"),Zl=ir("transitionend"),ec=new Map,lo="abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");function pt(e,t){ec.set(e,t),qt(t,[e])}for(var Pr=0;Pr<lo.length;Pr++){var Or=lo[Pr],Gu=Or.toLowerCase(),_u=Or[0].toUpperCase()+Or.slice(1);pt(Gu,"on"+_u)}pt(Xl,"onAnimationEnd");pt(Yl,"onAnimationIteration");pt($l,"onAnimationStart");pt("dblclick","onDoubleClick");pt("focusin","onFocus");pt("focusout","onBlur");pt(Zl,"onTransitionEnd");$t("onMouseEnter",["mouseout","mouseover"]);$t("onMouseLeave",["mouseout","mouseover"]);$t("onPointerEnter",["pointerout","pointerover"]);$t("onPointerLeave",["pointerout","pointerover"]);qt("onChange","change click focusin focusout input keydown keyup selectionchange".split(" "));qt("onSelect","focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));qt("onBeforeInput",["compositionend","keypress","textInput","paste"]);qt("onCompositionEnd","compositionend focusout keydown keypress keyup mousedown".split(" "));qt("onCompositionStart","compositionstart focusout keydown keypress keyup mousedown".split(" "));qt("onCompositionUpdate","compositionupdate focusout keydown keypress keyup mousedown".split(" "));var xn="abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "),Vu=new Set("cancel close invalid load scroll toggle".split(" ").concat(xn));function co(e,t,n){var a=e.type||"unknown-event";e.currentTarget=n,Gd(a,t,void 0,e),e.currentTarget=null}function tc(e,t){t=(t&4)!==0;for(var n=0;n<e.length;n++){var a=e[n],r=a.event;a=a.listeners;e:{var s=void 0;if(t)for(var i=a.length-1;0<=i;i--){var o=a[i],l=o.instance,u=o.currentTarget;if(o=o.listener,l!==s&&r.isPropagationStopped())break e;co(r,o,u),s=l}else for(i=0;i<a.length;i++){if(o=a[i],l=o.instance,u=o.currentTarget,o=o.listener,l!==s&&r.isPropagationStopped())break e;co(r,o,u),s=l}}}if(Oa)throw e=cs,Oa=!1,cs=null,e}function M(e,t){var n=t[vs];n===void 0&&(n=t[vs]=new Set);var a=e+"__bubble";n.has(a)||(nc(t,e,2,!1),n.add(a))}function Lr(e,t,n){var a=0;t&&(a|=4),nc(n,e,a,t)}var ma="_reactListening"+Math.random().toString(36).slice(2);function Dn(e){if(!e[ma]){e[ma]=!0,cl.forEach(function(n){n!=="selectionchange"&&(Vu.has(n)||Lr(n,!1,e),Lr(n,!0,e))});var t=e.nodeType===9?e:e.ownerDocument;t===null||t[ma]||(t[ma]=!0,Lr("selectionchange",!1,t))}}function nc(e,t,n,a){switch(Kl(t)){case 1:var r=lu;break;case 4:r=cu;break;default:r=ei}n=r.bind(null,t,n,e),r=void 0,!ls||t!=="touchstart"&&t!=="touchmove"&&t!=="wheel"||(r=!0),a?r!==void 0?e.addEventListener(t,n,{capture:!0,passive:r}):e.addEventListener(t,n,!0):r!==void 0?e.addEventListener(t,n,{passive:r}):e.addEventListener(t,n,!1)}function Mr(e,t,n,a,r){var s=a;if(!(t&1)&&!(t&2)&&a!==null)e:for(;;){if(a===null)return;var i=a.tag;if(i===3||i===4){var o=a.stateNode.containerInfo;if(o===r||o.nodeType===8&&o.parentNode===r)break;if(i===4)for(i=a.return;i!==null;){var l=i.tag;if((l===3||l===4)&&(l=i.stateNode.containerInfo,l===r||l.nodeType===8&&l.parentNode===r))return;i=i.return}for(;o!==null;){if(i=wt(o),i===null)return;if(l=i.tag,l===5||l===6){a=s=i;continue e}o=o.parentNode}}a=a.return}jl(function(){var u=s,f=Xs(n),y=[];e:{var h=ec.get(e);if(h!==void 0){var v=ni,w=e;switch(e){case"keypress":if(Ca(n)===0)break e;case"keydown":case"keyup":v=Cu;break;case"focusin":w="focus",v=Tr;break;case"focusout":w="blur",v=Tr;break;case"beforeblur":case"afterblur":v=Tr;break;case"click":if(n.button===2)break e;case"auxclick":case"dblclick":case"mousedown":case"mousemove":case"mouseup":case"mouseout":case"mouseover":case"contextmenu":v=Yi;break;case"drag":case"dragend":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"dragstart":case"drop":v=mu;break;case"touchcancel":case"touchend":case"touchmove":case"touchstart":v=Ru;break;case Xl:case Yl:case $l:v=yu;break;case Zl:v=Tu;break;case"scroll":v=du;break;case"wheel":v=Au;break;case"copy":case"cut":case"paste":v=gu;break;case"gotpointercapture":case"lostpointercapture":case"pointercancel":case"pointerdown":case"pointermove":case"pointerout":case"pointerover":case"pointerup":v=Zi}var k=(t&4)!==0,T=!k&&e==="scroll",d=k?h!==null?h+"Capture":null:h;k=[];for(var c=u,m;c!==null;){m=c;var g=m.stateNode;if(m.tag===5&&g!==null&&(m=g,d!==null&&(g=Ln(c,d),g!=null&&k.push(Fn(c,g,m)))),T)break;c=c.return}0<k.length&&(h=new v(h,w,null,n,f),y.push({event:h,listeners:k}))}}if(!(t&7)){e:{if(h=e==="mouseover"||e==="pointerover",v=e==="mouseout"||e==="pointerout",h&&n!==is&&(w=n.relatedTarget||n.fromElement)&&(wt(w)||w[Ge]))break e;if((v||h)&&(h=f.window===f?f:(h=f.ownerDocument)?h.defaultView||h.parentWindow:window,v?(w=n.relatedTarget||n.toElement,v=u,w=w?wt(w):null,w!==null&&(T=At(w),w!==T||w.tag!==5&&w.tag!==6)&&(w=null)):(v=null,w=u),v!==w)){if(k=Yi,g="onMouseLeave",d="onMouseEnter",c="mouse",(e==="pointerout"||e==="pointerover")&&(k=Zi,g="onPointerLeave",d="onPointerEnter",c="pointer"),T=v==null?h:Kt(v),m=w==null?h:Kt(w),h=new k(g,c+"leave",v,n,f),h.target=T,h.relatedTarget=m,g=null,wt(f)===u&&(k=new k(d,c+"enter",w,n,f),k.target=m,k.relatedTarget=T,g=k),T=g,v&&w)t:{for(k=v,d=w,c=0,m=k;m;m=It(m))c++;for(m=0,g=d;g;g=It(g))m++;for(;0<c-m;)k=It(k),c--;for(;0<m-c;)d=It(d),m--;for(;c--;){if(k===d||d!==null&&k===d.alternate)break t;k=It(k),d=It(d)}k=null}else k=null;v!==null&&uo(y,h,v,k,!1),w!==null&&T!==null&&uo(y,T,w,k,!0)}}e:{if(h=u?Kt(u):window,v=h.nodeName&&h.nodeName.toLowerCase(),v==="select"||v==="input"&&h.type==="file")var S=Hu;else if(no(h))if(Jl)S=Fu;else{S=Bu;var x=Ku}else(v=h.nodeName)&&v.toLowerCase()==="input"&&(h.type==="checkbox"||h.type==="radio")&&(S=Du);if(S&&(S=S(e,u))){zl(y,S,n,f);break e}x&&x(e,h,u),e==="focusout"&&(x=h._wrapperState)&&x.controlled&&h.type==="number"&&ts(h,"number",h.value)}switch(x=u?Kt(u):window,e){case"focusin":(no(x)||x.contentEditable==="true")&&(Wt=x,ps=u,Nn=null);break;case"focusout":Nn=ps=Wt=null;break;case"mousedown":hs=!0;break;case"contextmenu":case"mouseup":case"dragend":hs=!1,oo(y,n,f);break;case"selectionchange":if(Ju)break;case"keydown":case"keyup":oo(y,n,f)}var E;if(ri)e:{switch(e){case"compositionstart":var R="onCompositionStart";break e;case"compositionend":R="onCompositionEnd";break e;case"compositionupdate":R="onCompositionUpdate";break e}R=void 0}else Mt?Fl(e,n)&&(R="onCompositionEnd"):e==="keydown"&&n.keyCode===229&&(R="onCompositionStart");R&&(Dl&&n.locale!=="ko"&&(Mt||R!=="onCompositionStart"?R==="onCompositionEnd"&&Mt&&(E=Bl()):(tt=f,ti="value"in tt?tt.value:tt.textContent,Mt=!0)),x=Ka(u,R),0<x.length&&(R=new $i(R,e,null,n,f),y.push({event:R,listeners:x}),E?R.data=E:(E=Ul(n),E!==null&&(R.data=E)))),(E=Pu?Ou(e,n):Lu(e,n))&&(u=Ka(u,"onBeforeInput"),0<u.length&&(f=new $i("onBeforeInput","beforeinput",null,n,f),y.push({event:f,listeners:u}),f.data=E))}tc(y,t)})}function Fn(e,t,n){return{instance:e,listener:t,currentTarget:n}}function Ka(e,t){for(var n=t+"Capture",a=[];e!==null;){var r=e,s=r.stateNode;r.tag===5&&s!==null&&(r=s,s=Ln(e,n),s!=null&&a.unshift(Fn(e,s,r)),s=Ln(e,t),s!=null&&a.push(Fn(e,s,r))),e=e.return}return a}function It(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5);return e||null}function uo(e,t,n,a,r){for(var s=t._reactName,i=[];n!==null&&n!==a;){var o=n,l=o.alternate,u=o.stateNode;if(l!==null&&l===a)break;o.tag===5&&u!==null&&(o=u,r?(l=Ln(n,s),l!=null&&i.unshift(Fn(n,l,o))):r||(l=Ln(n,s),l!=null&&i.push(Fn(n,l,o)))),n=n.return}i.length!==0&&e.push({event:t,listeners:i})}var Qu=/\r\n?/g,Xu=/\u0000|\uFFFD/g;function mo(e){return(typeof e=="string"?e:""+e).replace(Qu,`
`).replace(Xu,"")}function pa(e,t,n){if(t=mo(t),mo(e)!==t&&n)throw Error(b(425))}function Ba(){}var ys=null,fs=null;function gs(e,t){return e==="textarea"||e==="noscript"||typeof t.children=="string"||typeof t.children=="number"||typeof t.dangerouslySetInnerHTML=="object"&&t.dangerouslySetInnerHTML!==null&&t.dangerouslySetInnerHTML.__html!=null}var bs=typeof setTimeout=="function"?setTimeout:void 0,Yu=typeof clearTimeout=="function"?clearTimeout:void 0,po=typeof Promise=="function"?Promise:void 0,$u=typeof queueMicrotask=="function"?queueMicrotask:typeof po<"u"?function(e){return po.resolve(null).then(e).catch(Zu)}:bs;function Zu(e){setTimeout(function(){throw e})}function Wr(e,t){var n=t,a=0;do{var r=n.nextSibling;if(e.removeChild(n),r&&r.nodeType===8)if(n=r.data,n==="/$"){if(a===0){e.removeChild(r),Hn(t);return}a--}else n!=="$"&&n!=="$?"&&n!=="$!"||a++;n=r}while(n);Hn(t)}function it(e){for(;e!=null;e=e.nextSibling){var t=e.nodeType;if(t===1||t===3)break;if(t===8){if(t=e.data,t==="$"||t==="$!"||t==="$?")break;if(t==="/$")return null}}return e}function ho(e){e=e.previousSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n==="$"||n==="$!"||n==="$?"){if(t===0)return e;t--}else n==="/$"&&t++}e=e.previousSibling}return null}var ln=Math.random().toString(36).slice(2),We="__reactFiber$"+ln,Un="__reactProps$"+ln,Ge="__reactContainer$"+ln,vs="__reactEvents$"+ln,em="__reactListeners$"+ln,tm="__reactHandles$"+ln;function wt(e){var t=e[We];if(t)return t;for(var n=e.parentNode;n;){if(t=n[Ge]||n[We]){if(n=t.alternate,t.child!==null||n!==null&&n.child!==null)for(e=ho(e);e!==null;){if(n=e[We])return n;e=ho(e)}return t}e=n,n=e.parentNode}return null}function Zn(e){return e=e[We]||e[Ge],!e||e.tag!==5&&e.tag!==6&&e.tag!==13&&e.tag!==3?null:e}function Kt(e){if(e.tag===5||e.tag===6)return e.stateNode;throw Error(b(33))}function or(e){return e[Un]||null}var ws=[],Bt=-1;function ht(e){return{current:e}}function W(e){0>Bt||(e.current=ws[Bt],ws[Bt]=null,Bt--)}function L(e,t){Bt++,ws[Bt]=e.current,e.current=t}var mt={},se=ht(mt),me=ht(!1),jt=mt;function Zt(e,t){var n=e.type.contextTypes;if(!n)return mt;var a=e.stateNode;if(a&&a.__reactInternalMemoizedUnmaskedChildContext===t)return a.__reactInternalMemoizedMaskedChildContext;var r={},s;for(s in n)r[s]=t[s];return a&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=t,e.__reactInternalMemoizedMaskedChildContext=r),r}function pe(e){return e=e.childContextTypes,e!=null}function Da(){W(me),W(se)}function yo(e,t,n){if(se.current!==mt)throw Error(b(168));L(se,t),L(me,n)}function ac(e,t,n){var a=e.stateNode;if(t=t.childContextTypes,typeof a.getChildContext!="function")return n;a=a.getChildContext();for(var r in a)if(!(r in t))throw Error(b(108,Kd(e)||"Unknown",r));return F({},n,a)}function Fa(e){return e=(e=e.stateNode)&&e.__reactInternalMemoizedMergedChildContext||mt,jt=se.current,L(se,e),L(me,me.current),!0}function fo(e,t,n){var a=e.stateNode;if(!a)throw Error(b(169));n?(e=ac(e,t,jt),a.__reactInternalMemoizedMergedChildContext=e,W(me),W(se),L(se,e)):W(me),L(me,n)}var De=null,lr=!1,Hr=!1;function rc(e){De===null?De=[e]:De.push(e)}function nm(e){lr=!0,rc(e)}function yt(){if(!Hr&&De!==null){Hr=!0;var e=0,t=O;try{var n=De;for(O=1;e<n.length;e++){var a=n[e];do a=a(!0);while(a!==null)}De=null,lr=!1}catch(r){throw De!==null&&(De=De.slice(e+1)),Tl(Ys,yt),r}finally{O=t,Hr=!1}}return null}var Dt=[],Ft=0,Ua=null,za=0,Se=[],ke=0,Et=null,Fe=1,Ue="";function bt(e,t){Dt[Ft++]=za,Dt[Ft++]=Ua,Ua=e,za=t}function sc(e,t,n){Se[ke++]=Fe,Se[ke++]=Ue,Se[ke++]=Et,Et=e;var a=Fe;e=Ue;var r=32-Ie(a)-1;a&=~(1<<r),n+=1;var s=32-Ie(t)+r;if(30<s){var i=r-r%5;s=(a&(1<<i)-1).toString(32),a>>=i,r-=i,Fe=1<<32-Ie(t)+r|n<<r|a,Ue=s+e}else Fe=1<<s|n<<r|a,Ue=e}function ii(e){e.return!==null&&(bt(e,1),sc(e,1,0))}function oi(e){for(;e===Ua;)Ua=Dt[--Ft],Dt[Ft]=null,za=Dt[--Ft],Dt[Ft]=null;for(;e===Et;)Et=Se[--ke],Se[ke]=null,Ue=Se[--ke],Se[ke]=null,Fe=Se[--ke],Se[ke]=null}var ge=null,fe=null,H=!1,Ae=null;function ic(e,t){var n=xe(5,null,null,0);n.elementType="DELETED",n.stateNode=t,n.return=e,t=e.deletions,t===null?(e.deletions=[n],e.flags|=16):t.push(n)}function go(e,t){switch(e.tag){case 5:var n=e.type;return t=t.nodeType!==1||n.toLowerCase()!==t.nodeName.toLowerCase()?null:t,t!==null?(e.stateNode=t,ge=e,fe=it(t.firstChild),!0):!1;case 6:return t=e.pendingProps===""||t.nodeType!==3?null:t,t!==null?(e.stateNode=t,ge=e,fe=null,!0):!1;case 13:return t=t.nodeType!==8?null:t,t!==null?(n=Et!==null?{id:Fe,overflow:Ue}:null,e.memoizedState={dehydrated:t,treeContext:n,retryLane:1073741824},n=xe(18,null,null,0),n.stateNode=t,n.return=e,e.child=n,ge=e,fe=null,!0):!1;default:return!1}}function Ss(e){return(e.mode&1)!==0&&(e.flags&128)===0}function ks(e){if(H){var t=fe;if(t){var n=t;if(!go(e,t)){if(Ss(e))throw Error(b(418));t=it(n.nextSibling);var a=ge;t&&go(e,t)?ic(a,n):(e.flags=e.flags&-4097|2,H=!1,ge=e)}}else{if(Ss(e))throw Error(b(418));e.flags=e.flags&-4097|2,H=!1,ge=e}}}function bo(e){for(e=e.return;e!==null&&e.tag!==5&&e.tag!==3&&e.tag!==13;)e=e.return;ge=e}function ha(e){if(e!==ge)return!1;if(!H)return bo(e),H=!0,!1;var t;if((t=e.tag!==3)&&!(t=e.tag!==5)&&(t=e.type,t=t!=="head"&&t!=="body"&&!gs(e.type,e.memoizedProps)),t&&(t=fe)){if(Ss(e))throw oc(),Error(b(418));for(;t;)ic(e,t),t=it(t.nextSibling)}if(bo(e),e.tag===13){if(e=e.memoizedState,e=e!==null?e.dehydrated:null,!e)throw Error(b(317));e:{for(e=e.nextSibling,t=0;e;){if(e.nodeType===8){var n=e.data;if(n==="/$"){if(t===0){fe=it(e.nextSibling);break e}t--}else n!=="$"&&n!=="$!"&&n!=="$?"||t++}e=e.nextSibling}fe=null}}else fe=ge?it(e.stateNode.nextSibling):null;return!0}function oc(){for(var e=fe;e;)e=it(e.nextSibling)}function en(){fe=ge=null,H=!1}function li(e){Ae===null?Ae=[e]:Ae.push(e)}var am=Qe.ReactCurrentBatchConfig;function fn(e,t,n){if(e=n.ref,e!==null&&typeof e!="function"&&typeof e!="object"){if(n._owner){if(n=n._owner,n){if(n.tag!==1)throw Error(b(309));var a=n.stateNode}if(!a)throw Error(b(147,e));var r=a,s=""+e;return t!==null&&t.ref!==null&&typeof t.ref=="function"&&t.ref._stringRef===s?t.ref:(t=function(i){var o=r.refs;i===null?delete o[s]:o[s]=i},t._stringRef=s,t)}if(typeof e!="string")throw Error(b(284));if(!n._owner)throw Error(b(290,e))}return e}function ya(e,t){throw e=Object.prototype.toString.call(t),Error(b(31,e==="[object Object]"?"object with keys {"+Object.keys(t).join(", ")+"}":e))}function vo(e){var t=e._init;return t(e._payload)}function lc(e){function t(d,c){if(e){var m=d.deletions;m===null?(d.deletions=[c],d.flags|=16):m.push(c)}}function n(d,c){if(!e)return null;for(;c!==null;)t(d,c),c=c.sibling;return null}function a(d,c){for(d=new Map;c!==null;)c.key!==null?d.set(c.key,c):d.set(c.index,c),c=c.sibling;return d}function r(d,c){return d=dt(d,c),d.index=0,d.sibling=null,d}function s(d,c,m){return d.index=m,e?(m=d.alternate,m!==null?(m=m.index,m<c?(d.flags|=2,c):m):(d.flags|=2,c)):(d.flags|=1048576,c)}function i(d){return e&&d.alternate===null&&(d.flags|=2),d}function o(d,c,m,g){return c===null||c.tag!==6?(c=Jr(m,d.mode,g),c.return=d,c):(c=r(c,m),c.return=d,c)}function l(d,c,m,g){var S=m.type;return S===Lt?f(d,c,m.props.children,g,m.key):c!==null&&(c.elementType===S||typeof S=="object"&&S!==null&&S.$$typeof===Ye&&vo(S)===c.type)?(g=r(c,m.props),g.ref=fn(d,c,m),g.return=d,g):(g=Aa(m.type,m.key,m.props,null,d.mode,g),g.ref=fn(d,c,m),g.return=d,g)}function u(d,c,m,g){return c===null||c.tag!==4||c.stateNode.containerInfo!==m.containerInfo||c.stateNode.implementation!==m.implementation?(c=Gr(m,d.mode,g),c.return=d,c):(c=r(c,m.children||[]),c.return=d,c)}function f(d,c,m,g,S){return c===null||c.tag!==7?(c=Ct(m,d.mode,g,S),c.return=d,c):(c=r(c,m),c.return=d,c)}function y(d,c,m){if(typeof c=="string"&&c!==""||typeof c=="number")return c=Jr(""+c,d.mode,m),c.return=d,c;if(typeof c=="object"&&c!==null){switch(c.$$typeof){case ra:return m=Aa(c.type,c.key,c.props,null,d.mode,m),m.ref=fn(d,null,c),m.return=d,m;case Ot:return c=Gr(c,d.mode,m),c.return=d,c;case Ye:var g=c._init;return y(d,g(c._payload),m)}if(Sn(c)||un(c))return c=Ct(c,d.mode,m,null),c.return=d,c;ya(d,c)}return null}function h(d,c,m,g){var S=c!==null?c.key:null;if(typeof m=="string"&&m!==""||typeof m=="number")return S!==null?null:o(d,c,""+m,g);if(typeof m=="object"&&m!==null){switch(m.$$typeof){case ra:return m.key===S?l(d,c,m,g):null;case Ot:return m.key===S?u(d,c,m,g):null;case Ye:return S=m._init,h(d,c,S(m._payload),g)}if(Sn(m)||un(m))return S!==null?null:f(d,c,m,g,null);ya(d,m)}return null}function v(d,c,m,g,S){if(typeof g=="string"&&g!==""||typeof g=="number")return d=d.get(m)||null,o(c,d,""+g,S);if(typeof g=="object"&&g!==null){switch(g.$$typeof){case ra:return d=d.get(g.key===null?m:g.key)||null,l(c,d,g,S);case Ot:return d=d.get(g.key===null?m:g.key)||null,u(c,d,g,S);case Ye:var x=g._init;return v(d,c,m,x(g._payload),S)}if(Sn(g)||un(g))return d=d.get(m)||null,f(c,d,g,S,null);ya(c,g)}return null}function w(d,c,m,g){for(var S=null,x=null,E=c,R=c=0,K=null;E!==null&&R<m.length;R++){E.index>R?(K=E,E=null):K=E.sibling;var A=h(d,E,m[R],g);if(A===null){E===null&&(E=K);break}e&&E&&A.alternate===null&&t(d,E),c=s(A,c,R),x===null?S=A:x.sibling=A,x=A,E=K}if(R===m.length)return n(d,E),H&&bt(d,R),S;if(E===null){for(;R<m.length;R++)E=y(d,m[R],g),E!==null&&(c=s(E,c,R),x===null?S=E:x.sibling=E,x=E);return H&&bt(d,R),S}for(E=a(d,E);R<m.length;R++)K=v(E,d,R,m[R],g),K!==null&&(e&&K.alternate!==null&&E.delete(K.key===null?R:K.key),c=s(K,c,R),x===null?S=K:x.sibling=K,x=K);return e&&E.forEach(function(Re){return t(d,Re)}),H&&bt(d,R),S}function k(d,c,m,g){var S=un(m);if(typeof S!="function")throw Error(b(150));if(m=S.call(m),m==null)throw Error(b(151));for(var x=S=null,E=c,R=c=0,K=null,A=m.next();E!==null&&!A.done;R++,A=m.next()){E.index>R?(K=E,E=null):K=E.sibling;var Re=h(d,E,A.value,g);if(Re===null){E===null&&(E=K);break}e&&E&&Re.alternate===null&&t(d,E),c=s(Re,c,R),x===null?S=Re:x.sibling=Re,x=Re,E=K}if(A.done)return n(d,E),H&&bt(d,R),S;if(E===null){for(;!A.done;R++,A=m.next())A=y(d,A.value,g),A!==null&&(c=s(A,c,R),x===null?S=A:x.sibling=A,x=A);return H&&bt(d,R),S}for(E=a(d,E);!A.done;R++,A=m.next())A=v(E,d,R,A.value,g),A!==null&&(e&&A.alternate!==null&&E.delete(A.key===null?R:A.key),c=s(A,c,R),x===null?S=A:x.sibling=A,x=A);return e&&E.forEach(function(cn){return t(d,cn)}),H&&bt(d,R),S}function T(d,c,m,g){if(typeof m=="object"&&m!==null&&m.type===Lt&&m.key===null&&(m=m.props.children),typeof m=="object"&&m!==null){switch(m.$$typeof){case ra:e:{for(var S=m.key,x=c;x!==null;){if(x.key===S){if(S=m.type,S===Lt){if(x.tag===7){n(d,x.sibling),c=r(x,m.props.children),c.return=d,d=c;break e}}else if(x.elementType===S||typeof S=="object"&&S!==null&&S.$$typeof===Ye&&vo(S)===x.type){n(d,x.sibling),c=r(x,m.props),c.ref=fn(d,x,m),c.return=d,d=c;break e}n(d,x);break}else t(d,x);x=x.sibling}m.type===Lt?(c=Ct(m.props.children,d.mode,g,m.key),c.return=d,d=c):(g=Aa(m.type,m.key,m.props,null,d.mode,g),g.ref=fn(d,c,m),g.return=d,d=g)}return i(d);case Ot:e:{for(x=m.key;c!==null;){if(c.key===x)if(c.tag===4&&c.stateNode.containerInfo===m.containerInfo&&c.stateNode.implementation===m.implementation){n(d,c.sibling),c=r(c,m.children||[]),c.return=d,d=c;break e}else{n(d,c);break}else t(d,c);c=c.sibling}c=Gr(m,d.mode,g),c.return=d,d=c}return i(d);case Ye:return x=m._init,T(d,c,x(m._payload),g)}if(Sn(m))return w(d,c,m,g);if(un(m))return k(d,c,m,g);ya(d,m)}return typeof m=="string"&&m!==""||typeof m=="number"?(m=""+m,c!==null&&c.tag===6?(n(d,c.sibling),c=r(c,m),c.return=d,d=c):(n(d,c),c=Jr(m,d.mode,g),c.return=d,d=c),i(d)):n(d,c)}return T}var tn=lc(!0),cc=lc(!1),Ja=ht(null),Ga=null,Ut=null,ci=null;function di(){ci=Ut=Ga=null}function ui(e){var t=Ja.current;W(Ja),e._currentValue=t}function xs(e,t,n){for(;e!==null;){var a=e.alternate;if((e.childLanes&t)!==t?(e.childLanes|=t,a!==null&&(a.childLanes|=t)):a!==null&&(a.childLanes&t)!==t&&(a.childLanes|=t),e===n)break;e=e.return}}function Xt(e,t){Ga=e,ci=Ut=null,e=e.dependencies,e!==null&&e.firstContext!==null&&(e.lanes&t&&(ue=!0),e.firstContext=null)}function je(e){var t=e._currentValue;if(ci!==e)if(e={context:e,memoizedValue:t,next:null},Ut===null){if(Ga===null)throw Error(b(308));Ut=e,Ga.dependencies={lanes:0,firstContext:e}}else Ut=Ut.next=e;return t}var St=null;function mi(e){St===null?St=[e]:St.push(e)}function dc(e,t,n,a){var r=t.interleaved;return r===null?(n.next=n,mi(t)):(n.next=r.next,r.next=n),t.interleaved=n,_e(e,a)}function _e(e,t){e.lanes|=t;var n=e.alternate;for(n!==null&&(n.lanes|=t),n=e,e=e.return;e!==null;)e.childLanes|=t,n=e.alternate,n!==null&&(n.childLanes|=t),n=e,e=e.return;return n.tag===3?n.stateNode:null}var $e=!1;function pi(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,interleaved:null,lanes:0},effects:null}}function uc(e,t){e=e.updateQueue,t.updateQueue===e&&(t.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,effects:e.effects})}function ze(e,t){return{eventTime:e,lane:t,tag:0,payload:null,callback:null,next:null}}function ot(e,t,n){var a=e.updateQueue;if(a===null)return null;if(a=a.shared,P&2){var r=a.pending;return r===null?t.next=t:(t.next=r.next,r.next=t),a.pending=t,_e(e,n)}return r=a.interleaved,r===null?(t.next=t,mi(a)):(t.next=r.next,r.next=t),a.interleaved=t,_e(e,n)}function ja(e,t,n){if(t=t.updateQueue,t!==null&&(t=t.shared,(n&4194240)!==0)){var a=t.lanes;a&=e.pendingLanes,n|=a,t.lanes=n,$s(e,n)}}function wo(e,t){var n=e.updateQueue,a=e.alternate;if(a!==null&&(a=a.updateQueue,n===a)){var r=null,s=null;if(n=n.firstBaseUpdate,n!==null){do{var i={eventTime:n.eventTime,lane:n.lane,tag:n.tag,payload:n.payload,callback:n.callback,next:null};s===null?r=s=i:s=s.next=i,n=n.next}while(n!==null);s===null?r=s=t:s=s.next=t}else r=s=t;n={baseState:a.baseState,firstBaseUpdate:r,lastBaseUpdate:s,shared:a.shared,effects:a.effects},e.updateQueue=n;return}e=n.lastBaseUpdate,e===null?n.firstBaseUpdate=t:e.next=t,n.lastBaseUpdate=t}function _a(e,t,n,a){var r=e.updateQueue;$e=!1;var s=r.firstBaseUpdate,i=r.lastBaseUpdate,o=r.shared.pending;if(o!==null){r.shared.pending=null;var l=o,u=l.next;l.next=null,i===null?s=u:i.next=u,i=l;var f=e.alternate;f!==null&&(f=f.updateQueue,o=f.lastBaseUpdate,o!==i&&(o===null?f.firstBaseUpdate=u:o.next=u,f.lastBaseUpdate=l))}if(s!==null){var y=r.baseState;i=0,f=u=l=null,o=s;do{var h=o.lane,v=o.eventTime;if((a&h)===h){f!==null&&(f=f.next={eventTime:v,lane:0,tag:o.tag,payload:o.payload,callback:o.callback,next:null});e:{var w=e,k=o;switch(h=t,v=n,k.tag){case 1:if(w=k.payload,typeof w=="function"){y=w.call(v,y,h);break e}y=w;break e;case 3:w.flags=w.flags&-65537|128;case 0:if(w=k.payload,h=typeof w=="function"?w.call(v,y,h):w,h==null)break e;y=F({},y,h);break e;case 2:$e=!0}}o.callback!==null&&o.lane!==0&&(e.flags|=64,h=r.effects,h===null?r.effects=[o]:h.push(o))}else v={eventTime:v,lane:h,tag:o.tag,payload:o.payload,callback:o.callback,next:null},f===null?(u=f=v,l=y):f=f.next=v,i|=h;if(o=o.next,o===null){if(o=r.shared.pending,o===null)break;h=o,o=h.next,h.next=null,r.lastBaseUpdate=h,r.shared.pending=null}}while(!0);if(f===null&&(l=y),r.baseState=l,r.firstBaseUpdate=u,r.lastBaseUpdate=f,t=r.shared.interleaved,t!==null){r=t;do i|=r.lane,r=r.next;while(r!==t)}else s===null&&(r.shared.lanes=0);Nt|=i,e.lanes=i,e.memoizedState=y}}function So(e,t,n){if(e=t.effects,t.effects=null,e!==null)for(t=0;t<e.length;t++){var a=e[t],r=a.callback;if(r!==null){if(a.callback=null,a=n,typeof r!="function")throw Error(b(191,r));r.call(a)}}}var ea={},Ke=ht(ea),zn=ht(ea),Jn=ht(ea);function kt(e){if(e===ea)throw Error(b(174));return e}function hi(e,t){switch(L(Jn,t),L(zn,e),L(Ke,ea),e=t.nodeType,e){case 9:case 11:t=(t=t.documentElement)?t.namespaceURI:as(null,"");break;default:e=e===8?t.parentNode:t,t=e.namespaceURI||null,e=e.tagName,t=as(t,e)}W(Ke),L(Ke,t)}function nn(){W(Ke),W(zn),W(Jn)}function mc(e){kt(Jn.current);var t=kt(Ke.current),n=as(t,e.type);t!==n&&(L(zn,e),L(Ke,n))}function yi(e){zn.current===e&&(W(Ke),W(zn))}var B=ht(0);function Va(e){for(var t=e;t!==null;){if(t.tag===13){var n=t.memoizedState;if(n!==null&&(n=n.dehydrated,n===null||n.data==="$?"||n.data==="$!"))return t}else if(t.tag===19&&t.memoizedProps.revealOrder!==void 0){if(t.flags&128)return t}else if(t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return null;t=t.return}t.sibling.return=t.return,t=t.sibling}return null}var Kr=[];function fi(){for(var e=0;e<Kr.length;e++)Kr[e]._workInProgressVersionPrimary=null;Kr.length=0}var Ea=Qe.ReactCurrentDispatcher,Br=Qe.ReactCurrentBatchConfig,Rt=0,D=null,_=null,X=null,Qa=!1,Tn=!1,Gn=0,rm=0;function ne(){throw Error(b(321))}function gi(e,t){if(t===null)return!1;for(var n=0;n<t.length&&n<e.length;n++)if(!Oe(e[n],t[n]))return!1;return!0}function bi(e,t,n,a,r,s){if(Rt=s,D=t,t.memoizedState=null,t.updateQueue=null,t.lanes=0,Ea.current=e===null||e.memoizedState===null?lm:cm,e=n(a,r),Tn){s=0;do{if(Tn=!1,Gn=0,25<=s)throw Error(b(301));s+=1,X=_=null,t.updateQueue=null,Ea.current=dm,e=n(a,r)}while(Tn)}if(Ea.current=Xa,t=_!==null&&_.next!==null,Rt=0,X=_=D=null,Qa=!1,t)throw Error(b(300));return e}function vi(){var e=Gn!==0;return Gn=0,e}function Me(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return X===null?D.memoizedState=X=e:X=X.next=e,X}function Ee(){if(_===null){var e=D.alternate;e=e!==null?e.memoizedState:null}else e=_.next;var t=X===null?D.memoizedState:X.next;if(t!==null)X=t,_=e;else{if(e===null)throw Error(b(310));_=e,e={memoizedState:_.memoizedState,baseState:_.baseState,baseQueue:_.baseQueue,queue:_.queue,next:null},X===null?D.memoizedState=X=e:X=X.next=e}return X}function _n(e,t){return typeof t=="function"?t(e):t}function Dr(e){var t=Ee(),n=t.queue;if(n===null)throw Error(b(311));n.lastRenderedReducer=e;var a=_,r=a.baseQueue,s=n.pending;if(s!==null){if(r!==null){var i=r.next;r.next=s.next,s.next=i}a.baseQueue=r=s,n.pending=null}if(r!==null){s=r.next,a=a.baseState;var o=i=null,l=null,u=s;do{var f=u.lane;if((Rt&f)===f)l!==null&&(l=l.next={lane:0,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null}),a=u.hasEagerState?u.eagerState:e(a,u.action);else{var y={lane:f,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null};l===null?(o=l=y,i=a):l=l.next=y,D.lanes|=f,Nt|=f}u=u.next}while(u!==null&&u!==s);l===null?i=a:l.next=o,Oe(a,t.memoizedState)||(ue=!0),t.memoizedState=a,t.baseState=i,t.baseQueue=l,n.lastRenderedState=a}if(e=n.interleaved,e!==null){r=e;do s=r.lane,D.lanes|=s,Nt|=s,r=r.next;while(r!==e)}else r===null&&(n.lanes=0);return[t.memoizedState,n.dispatch]}function Fr(e){var t=Ee(),n=t.queue;if(n===null)throw Error(b(311));n.lastRenderedReducer=e;var a=n.dispatch,r=n.pending,s=t.memoizedState;if(r!==null){n.pending=null;var i=r=r.next;do s=e(s,i.action),i=i.next;while(i!==r);Oe(s,t.memoizedState)||(ue=!0),t.memoizedState=s,t.baseQueue===null&&(t.baseState=s),n.lastRenderedState=s}return[s,a]}function pc(){}function hc(e,t){var n=D,a=Ee(),r=t(),s=!Oe(a.memoizedState,r);if(s&&(a.memoizedState=r,ue=!0),a=a.queue,wi(gc.bind(null,n,a,e),[e]),a.getSnapshot!==t||s||X!==null&&X.memoizedState.tag&1){if(n.flags|=2048,Vn(9,fc.bind(null,n,a,r,t),void 0,null),Y===null)throw Error(b(349));Rt&30||yc(n,t,r)}return r}function yc(e,t,n){e.flags|=16384,e={getSnapshot:t,value:n},t=D.updateQueue,t===null?(t={lastEffect:null,stores:null},D.updateQueue=t,t.stores=[e]):(n=t.stores,n===null?t.stores=[e]:n.push(e))}function fc(e,t,n,a){t.value=n,t.getSnapshot=a,bc(t)&&vc(e)}function gc(e,t,n){return n(function(){bc(t)&&vc(e)})}function bc(e){var t=e.getSnapshot;e=e.value;try{var n=t();return!Oe(e,n)}catch{return!0}}function vc(e){var t=_e(e,1);t!==null&&Pe(t,e,1,-1)}function ko(e){var t=Me();return typeof e=="function"&&(e=e()),t.memoizedState=t.baseState=e,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:_n,lastRenderedState:e},t.queue=e,e=e.dispatch=om.bind(null,D,e),[t.memoizedState,e]}function Vn(e,t,n,a){return e={tag:e,create:t,destroy:n,deps:a,next:null},t=D.updateQueue,t===null?(t={lastEffect:null,stores:null},D.updateQueue=t,t.lastEffect=e.next=e):(n=t.lastEffect,n===null?t.lastEffect=e.next=e:(a=n.next,n.next=e,e.next=a,t.lastEffect=e)),e}function wc(){return Ee().memoizedState}function Ra(e,t,n,a){var r=Me();D.flags|=e,r.memoizedState=Vn(1|t,n,void 0,a===void 0?null:a)}function cr(e,t,n,a){var r=Ee();a=a===void 0?null:a;var s=void 0;if(_!==null){var i=_.memoizedState;if(s=i.destroy,a!==null&&gi(a,i.deps)){r.memoizedState=Vn(t,n,s,a);return}}D.flags|=e,r.memoizedState=Vn(1|t,n,s,a)}function xo(e,t){return Ra(8390656,8,e,t)}function wi(e,t){return cr(2048,8,e,t)}function Sc(e,t){return cr(4,2,e,t)}function kc(e,t){return cr(4,4,e,t)}function xc(e,t){if(typeof t=="function")return e=e(),t(e),function(){t(null)};if(t!=null)return e=e(),t.current=e,function(){t.current=null}}function Cc(e,t,n){return n=n!=null?n.concat([e]):null,cr(4,4,xc.bind(null,t,e),n)}function Si(){}function jc(e,t){var n=Ee();t=t===void 0?null:t;var a=n.memoizedState;return a!==null&&t!==null&&gi(t,a[1])?a[0]:(n.memoizedState=[e,t],e)}function Ec(e,t){var n=Ee();t=t===void 0?null:t;var a=n.memoizedState;return a!==null&&t!==null&&gi(t,a[1])?a[0]:(e=e(),n.memoizedState=[e,t],e)}function Rc(e,t,n){return Rt&21?(Oe(n,t)||(n=Il(),D.lanes|=n,Nt|=n,e.baseState=!0),t):(e.baseState&&(e.baseState=!1,ue=!0),e.memoizedState=n)}function sm(e,t){var n=O;O=n!==0&&4>n?n:4,e(!0);var a=Br.transition;Br.transition={};try{e(!1),t()}finally{O=n,Br.transition=a}}function Nc(){return Ee().memoizedState}function im(e,t,n){var a=ct(e);if(n={lane:a,action:n,hasEagerState:!1,eagerState:null,next:null},Tc(e))qc(t,n);else if(n=dc(e,t,n,a),n!==null){var r=oe();Pe(n,e,a,r),Ac(n,t,a)}}function om(e,t,n){var a=ct(e),r={lane:a,action:n,hasEagerState:!1,eagerState:null,next:null};if(Tc(e))qc(t,r);else{var s=e.alternate;if(e.lanes===0&&(s===null||s.lanes===0)&&(s=t.lastRenderedReducer,s!==null))try{var i=t.lastRenderedState,o=s(i,n);if(r.hasEagerState=!0,r.eagerState=o,Oe(o,i)){var l=t.interleaved;l===null?(r.next=r,mi(t)):(r.next=l.next,l.next=r),t.interleaved=r;return}}catch{}finally{}n=dc(e,t,r,a),n!==null&&(r=oe(),Pe(n,e,a,r),Ac(n,t,a))}}function Tc(e){var t=e.alternate;return e===D||t!==null&&t===D}function qc(e,t){Tn=Qa=!0;var n=e.pending;n===null?t.next=t:(t.next=n.next,n.next=t),e.pending=t}function Ac(e,t,n){if(n&4194240){var a=t.lanes;a&=e.pendingLanes,n|=a,t.lanes=n,$s(e,n)}}var Xa={readContext:je,useCallback:ne,useContext:ne,useEffect:ne,useImperativeHandle:ne,useInsertionEffect:ne,useLayoutEffect:ne,useMemo:ne,useReducer:ne,useRef:ne,useState:ne,useDebugValue:ne,useDeferredValue:ne,useTransition:ne,useMutableSource:ne,useSyncExternalStore:ne,useId:ne,unstable_isNewReconciler:!1},lm={readContext:je,useCallback:function(e,t){return Me().memoizedState=[e,t===void 0?null:t],e},useContext:je,useEffect:xo,useImperativeHandle:function(e,t,n){return n=n!=null?n.concat([e]):null,Ra(4194308,4,xc.bind(null,t,e),n)},useLayoutEffect:function(e,t){return Ra(4194308,4,e,t)},useInsertionEffect:function(e,t){return Ra(4,2,e,t)},useMemo:function(e,t){var n=Me();return t=t===void 0?null:t,e=e(),n.memoizedState=[e,t],e},useReducer:function(e,t,n){var a=Me();return t=n!==void 0?n(t):t,a.memoizedState=a.baseState=t,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:t},a.queue=e,e=e.dispatch=im.bind(null,D,e),[a.memoizedState,e]},useRef:function(e){var t=Me();return e={current:e},t.memoizedState=e},useState:ko,useDebugValue:Si,useDeferredValue:function(e){return Me().memoizedState=e},useTransition:function(){var e=ko(!1),t=e[0];return e=sm.bind(null,e[1]),Me().memoizedState=e,[t,e]},useMutableSource:function(){},useSyncExternalStore:function(e,t,n){var a=D,r=Me();if(H){if(n===void 0)throw Error(b(407));n=n()}else{if(n=t(),Y===null)throw Error(b(349));Rt&30||yc(a,t,n)}r.memoizedState=n;var s={value:n,getSnapshot:t};return r.queue=s,xo(gc.bind(null,a,s,e),[e]),a.flags|=2048,Vn(9,fc.bind(null,a,s,n,t),void 0,null),n},useId:function(){var e=Me(),t=Y.identifierPrefix;if(H){var n=Ue,a=Fe;n=(a&~(1<<32-Ie(a)-1)).toString(32)+n,t=":"+t+"R"+n,n=Gn++,0<n&&(t+="H"+n.toString(32)),t+=":"}else n=rm++,t=":"+t+"r"+n.toString(32)+":";return e.memoizedState=t},unstable_isNewReconciler:!1},cm={readContext:je,useCallback:jc,useContext:je,useEffect:wi,useImperativeHandle:Cc,useInsertionEffect:Sc,useLayoutEffect:kc,useMemo:Ec,useReducer:Dr,useRef:wc,useState:function(){return Dr(_n)},useDebugValue:Si,useDeferredValue:function(e){var t=Ee();return Rc(t,_.memoizedState,e)},useTransition:function(){var e=Dr(_n)[0],t=Ee().memoizedState;return[e,t]},useMutableSource:pc,useSyncExternalStore:hc,useId:Nc,unstable_isNewReconciler:!1},dm={readContext:je,useCallback:jc,useContext:je,useEffect:wi,useImperativeHandle:Cc,useInsertionEffect:Sc,useLayoutEffect:kc,useMemo:Ec,useReducer:Fr,useRef:wc,useState:function(){return Fr(_n)},useDebugValue:Si,useDeferredValue:function(e){var t=Ee();return _===null?t.memoizedState=e:Rc(t,_.memoizedState,e)},useTransition:function(){var e=Fr(_n)[0],t=Ee().memoizedState;return[e,t]},useMutableSource:pc,useSyncExternalStore:hc,useId:Nc,unstable_isNewReconciler:!1};function Te(e,t){if(e&&e.defaultProps){t=F({},t),e=e.defaultProps;for(var n in e)t[n]===void 0&&(t[n]=e[n]);return t}return t}function Cs(e,t,n,a){t=e.memoizedState,n=n(a,t),n=n==null?t:F({},t,n),e.memoizedState=n,e.lanes===0&&(e.updateQueue.baseState=n)}var dr={isMounted:function(e){return(e=e._reactInternals)?At(e)===e:!1},enqueueSetState:function(e,t,n){e=e._reactInternals;var a=oe(),r=ct(e),s=ze(a,r);s.payload=t,n!=null&&(s.callback=n),t=ot(e,s,r),t!==null&&(Pe(t,e,r,a),ja(t,e,r))},enqueueReplaceState:function(e,t,n){e=e._reactInternals;var a=oe(),r=ct(e),s=ze(a,r);s.tag=1,s.payload=t,n!=null&&(s.callback=n),t=ot(e,s,r),t!==null&&(Pe(t,e,r,a),ja(t,e,r))},enqueueForceUpdate:function(e,t){e=e._reactInternals;var n=oe(),a=ct(e),r=ze(n,a);r.tag=2,t!=null&&(r.callback=t),t=ot(e,r,a),t!==null&&(Pe(t,e,a,n),ja(t,e,a))}};function Co(e,t,n,a,r,s,i){return e=e.stateNode,typeof e.shouldComponentUpdate=="function"?e.shouldComponentUpdate(a,s,i):t.prototype&&t.prototype.isPureReactComponent?!Bn(n,a)||!Bn(r,s):!0}function Ic(e,t,n){var a=!1,r=mt,s=t.contextType;return typeof s=="object"&&s!==null?s=je(s):(r=pe(t)?jt:se.current,a=t.contextTypes,s=(a=a!=null)?Zt(e,r):mt),t=new t(n,s),e.memoizedState=t.state!==null&&t.state!==void 0?t.state:null,t.updater=dr,e.stateNode=t,t._reactInternals=e,a&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=r,e.__reactInternalMemoizedMaskedChildContext=s),t}function jo(e,t,n,a){e=t.state,typeof t.componentWillReceiveProps=="function"&&t.componentWillReceiveProps(n,a),typeof t.UNSAFE_componentWillReceiveProps=="function"&&t.UNSAFE_componentWillReceiveProps(n,a),t.state!==e&&dr.enqueueReplaceState(t,t.state,null)}function js(e,t,n,a){var r=e.stateNode;r.props=n,r.state=e.memoizedState,r.refs={},pi(e);var s=t.contextType;typeof s=="object"&&s!==null?r.context=je(s):(s=pe(t)?jt:se.current,r.context=Zt(e,s)),r.state=e.memoizedState,s=t.getDerivedStateFromProps,typeof s=="function"&&(Cs(e,t,s,n),r.state=e.memoizedState),typeof t.getDerivedStateFromProps=="function"||typeof r.getSnapshotBeforeUpdate=="function"||typeof r.UNSAFE_componentWillMount!="function"&&typeof r.componentWillMount!="function"||(t=r.state,typeof r.componentWillMount=="function"&&r.componentWillMount(),typeof r.UNSAFE_componentWillMount=="function"&&r.UNSAFE_componentWillMount(),t!==r.state&&dr.enqueueReplaceState(r,r.state,null),_a(e,n,r,a),r.state=e.memoizedState),typeof r.componentDidMount=="function"&&(e.flags|=4194308)}function an(e,t){try{var n="",a=t;do n+=Hd(a),a=a.return;while(a);var r=n}catch(s){r=`
Error generating stack: `+s.message+`
`+s.stack}return{value:e,source:t,stack:r,digest:null}}function Ur(e,t,n){return{value:e,source:null,stack:n??null,digest:t??null}}function Es(e,t){try{console.error(t.value)}catch(n){setTimeout(function(){throw n})}}var um=typeof WeakMap=="function"?WeakMap:Map;function Pc(e,t,n){n=ze(-1,n),n.tag=3,n.payload={element:null};var a=t.value;return n.callback=function(){$a||($a=!0,Ms=a),Es(e,t)},n}function Oc(e,t,n){n=ze(-1,n),n.tag=3;var a=e.type.getDerivedStateFromError;if(typeof a=="function"){var r=t.value;n.payload=function(){return a(r)},n.callback=function(){Es(e,t)}}var s=e.stateNode;return s!==null&&typeof s.componentDidCatch=="function"&&(n.callback=function(){Es(e,t),typeof a!="function"&&(lt===null?lt=new Set([this]):lt.add(this));var i=t.stack;this.componentDidCatch(t.value,{componentStack:i!==null?i:""})}),n}function Eo(e,t,n){var a=e.pingCache;if(a===null){a=e.pingCache=new um;var r=new Set;a.set(t,r)}else r=a.get(t),r===void 0&&(r=new Set,a.set(t,r));r.has(n)||(r.add(n),e=jm.bind(null,e,t,n),t.then(e,e))}function Ro(e){do{var t;if((t=e.tag===13)&&(t=e.memoizedState,t=t!==null?t.dehydrated!==null:!0),t)return e;e=e.return}while(e!==null);return null}function No(e,t,n,a,r){return e.mode&1?(e.flags|=65536,e.lanes=r,e):(e===t?e.flags|=65536:(e.flags|=128,n.flags|=131072,n.flags&=-52805,n.tag===1&&(n.alternate===null?n.tag=17:(t=ze(-1,1),t.tag=2,ot(n,t,1))),n.lanes|=1),e)}var mm=Qe.ReactCurrentOwner,ue=!1;function ie(e,t,n,a){t.child=e===null?cc(t,null,n,a):tn(t,e.child,n,a)}function To(e,t,n,a,r){n=n.render;var s=t.ref;return Xt(t,r),a=bi(e,t,n,a,s,r),n=vi(),e!==null&&!ue?(t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~r,Ve(e,t,r)):(H&&n&&ii(t),t.flags|=1,ie(e,t,a,r),t.child)}function qo(e,t,n,a,r){if(e===null){var s=n.type;return typeof s=="function"&&!Ti(s)&&s.defaultProps===void 0&&n.compare===null&&n.defaultProps===void 0?(t.tag=15,t.type=s,Lc(e,t,s,a,r)):(e=Aa(n.type,null,a,t,t.mode,r),e.ref=t.ref,e.return=t,t.child=e)}if(s=e.child,!(e.lanes&r)){var i=s.memoizedProps;if(n=n.compare,n=n!==null?n:Bn,n(i,a)&&e.ref===t.ref)return Ve(e,t,r)}return t.flags|=1,e=dt(s,a),e.ref=t.ref,e.return=t,t.child=e}function Lc(e,t,n,a,r){if(e!==null){var s=e.memoizedProps;if(Bn(s,a)&&e.ref===t.ref)if(ue=!1,t.pendingProps=a=s,(e.lanes&r)!==0)e.flags&131072&&(ue=!0);else return t.lanes=e.lanes,Ve(e,t,r)}return Rs(e,t,n,a,r)}function Mc(e,t,n){var a=t.pendingProps,r=a.children,s=e!==null?e.memoizedState:null;if(a.mode==="hidden")if(!(t.mode&1))t.memoizedState={baseLanes:0,cachePool:null,transitions:null},L(Jt,ye),ye|=n;else{if(!(n&1073741824))return e=s!==null?s.baseLanes|n:n,t.lanes=t.childLanes=1073741824,t.memoizedState={baseLanes:e,cachePool:null,transitions:null},t.updateQueue=null,L(Jt,ye),ye|=e,null;t.memoizedState={baseLanes:0,cachePool:null,transitions:null},a=s!==null?s.baseLanes:n,L(Jt,ye),ye|=a}else s!==null?(a=s.baseLanes|n,t.memoizedState=null):a=n,L(Jt,ye),ye|=a;return ie(e,t,r,n),t.child}function Wc(e,t){var n=t.ref;(e===null&&n!==null||e!==null&&e.ref!==n)&&(t.flags|=512,t.flags|=2097152)}function Rs(e,t,n,a,r){var s=pe(n)?jt:se.current;return s=Zt(t,s),Xt(t,r),n=bi(e,t,n,a,s,r),a=vi(),e!==null&&!ue?(t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~r,Ve(e,t,r)):(H&&a&&ii(t),t.flags|=1,ie(e,t,n,r),t.child)}function Ao(e,t,n,a,r){if(pe(n)){var s=!0;Fa(t)}else s=!1;if(Xt(t,r),t.stateNode===null)Na(e,t),Ic(t,n,a),js(t,n,a,r),a=!0;else if(e===null){var i=t.stateNode,o=t.memoizedProps;i.props=o;var l=i.context,u=n.contextType;typeof u=="object"&&u!==null?u=je(u):(u=pe(n)?jt:se.current,u=Zt(t,u));var f=n.getDerivedStateFromProps,y=typeof f=="function"||typeof i.getSnapshotBeforeUpdate=="function";y||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==a||l!==u)&&jo(t,i,a,u),$e=!1;var h=t.memoizedState;i.state=h,_a(t,a,i,r),l=t.memoizedState,o!==a||h!==l||me.current||$e?(typeof f=="function"&&(Cs(t,n,f,a),l=t.memoizedState),(o=$e||Co(t,n,o,a,h,l,u))?(y||typeof i.UNSAFE_componentWillMount!="function"&&typeof i.componentWillMount!="function"||(typeof i.componentWillMount=="function"&&i.componentWillMount(),typeof i.UNSAFE_componentWillMount=="function"&&i.UNSAFE_componentWillMount()),typeof i.componentDidMount=="function"&&(t.flags|=4194308)):(typeof i.componentDidMount=="function"&&(t.flags|=4194308),t.memoizedProps=a,t.memoizedState=l),i.props=a,i.state=l,i.context=u,a=o):(typeof i.componentDidMount=="function"&&(t.flags|=4194308),a=!1)}else{i=t.stateNode,uc(e,t),o=t.memoizedProps,u=t.type===t.elementType?o:Te(t.type,o),i.props=u,y=t.pendingProps,h=i.context,l=n.contextType,typeof l=="object"&&l!==null?l=je(l):(l=pe(n)?jt:se.current,l=Zt(t,l));var v=n.getDerivedStateFromProps;(f=typeof v=="function"||typeof i.getSnapshotBeforeUpdate=="function")||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==y||h!==l)&&jo(t,i,a,l),$e=!1,h=t.memoizedState,i.state=h,_a(t,a,i,r);var w=t.memoizedState;o!==y||h!==w||me.current||$e?(typeof v=="function"&&(Cs(t,n,v,a),w=t.memoizedState),(u=$e||Co(t,n,u,a,h,w,l)||!1)?(f||typeof i.UNSAFE_componentWillUpdate!="function"&&typeof i.componentWillUpdate!="function"||(typeof i.componentWillUpdate=="function"&&i.componentWillUpdate(a,w,l),typeof i.UNSAFE_componentWillUpdate=="function"&&i.UNSAFE_componentWillUpdate(a,w,l)),typeof i.componentDidUpdate=="function"&&(t.flags|=4),typeof i.getSnapshotBeforeUpdate=="function"&&(t.flags|=1024)):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=1024),t.memoizedProps=a,t.memoizedState=w),i.props=a,i.state=w,i.context=l,a=u):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=1024),a=!1)}return Ns(e,t,n,a,s,r)}function Ns(e,t,n,a,r,s){Wc(e,t);var i=(t.flags&128)!==0;if(!a&&!i)return r&&fo(t,n,!1),Ve(e,t,s);a=t.stateNode,mm.current=t;var o=i&&typeof n.getDerivedStateFromError!="function"?null:a.render();return t.flags|=1,e!==null&&i?(t.child=tn(t,e.child,null,s),t.child=tn(t,null,o,s)):ie(e,t,o,s),t.memoizedState=a.state,r&&fo(t,n,!0),t.child}function Hc(e){var t=e.stateNode;t.pendingContext?yo(e,t.pendingContext,t.pendingContext!==t.context):t.context&&yo(e,t.context,!1),hi(e,t.containerInfo)}function Io(e,t,n,a,r){return en(),li(r),t.flags|=256,ie(e,t,n,a),t.child}var Ts={dehydrated:null,treeContext:null,retryLane:0};function qs(e){return{baseLanes:e,cachePool:null,transitions:null}}function Kc(e,t,n){var a=t.pendingProps,r=B.current,s=!1,i=(t.flags&128)!==0,o;if((o=i)||(o=e!==null&&e.memoizedState===null?!1:(r&2)!==0),o?(s=!0,t.flags&=-129):(e===null||e.memoizedState!==null)&&(r|=1),L(B,r&1),e===null)return ks(t),e=t.memoizedState,e!==null&&(e=e.dehydrated,e!==null)?(t.mode&1?e.data==="$!"?t.lanes=8:t.lanes=1073741824:t.lanes=1,null):(i=a.children,e=a.fallback,s?(a=t.mode,s=t.child,i={mode:"hidden",children:i},!(a&1)&&s!==null?(s.childLanes=0,s.pendingProps=i):s=pr(i,a,0,null),e=Ct(e,a,n,null),s.return=t,e.return=t,s.sibling=e,t.child=s,t.child.memoizedState=qs(n),t.memoizedState=Ts,e):ki(t,i));if(r=e.memoizedState,r!==null&&(o=r.dehydrated,o!==null))return pm(e,t,i,a,o,r,n);if(s){s=a.fallback,i=t.mode,r=e.child,o=r.sibling;var l={mode:"hidden",children:a.children};return!(i&1)&&t.child!==r?(a=t.child,a.childLanes=0,a.pendingProps=l,t.deletions=null):(a=dt(r,l),a.subtreeFlags=r.subtreeFlags&14680064),o!==null?s=dt(o,s):(s=Ct(s,i,n,null),s.flags|=2),s.return=t,a.return=t,a.sibling=s,t.child=a,a=s,s=t.child,i=e.child.memoizedState,i=i===null?qs(n):{baseLanes:i.baseLanes|n,cachePool:null,transitions:i.transitions},s.memoizedState=i,s.childLanes=e.childLanes&~n,t.memoizedState=Ts,a}return s=e.child,e=s.sibling,a=dt(s,{mode:"visible",children:a.children}),!(t.mode&1)&&(a.lanes=n),a.return=t,a.sibling=null,e!==null&&(n=t.deletions,n===null?(t.deletions=[e],t.flags|=16):n.push(e)),t.child=a,t.memoizedState=null,a}function ki(e,t){return t=pr({mode:"visible",children:t},e.mode,0,null),t.return=e,e.child=t}function fa(e,t,n,a){return a!==null&&li(a),tn(t,e.child,null,n),e=ki(t,t.pendingProps.children),e.flags|=2,t.memoizedState=null,e}function pm(e,t,n,a,r,s,i){if(n)return t.flags&256?(t.flags&=-257,a=Ur(Error(b(422))),fa(e,t,i,a)):t.memoizedState!==null?(t.child=e.child,t.flags|=128,null):(s=a.fallback,r=t.mode,a=pr({mode:"visible",children:a.children},r,0,null),s=Ct(s,r,i,null),s.flags|=2,a.return=t,s.return=t,a.sibling=s,t.child=a,t.mode&1&&tn(t,e.child,null,i),t.child.memoizedState=qs(i),t.memoizedState=Ts,s);if(!(t.mode&1))return fa(e,t,i,null);if(r.data==="$!"){if(a=r.nextSibling&&r.nextSibling.dataset,a)var o=a.dgst;return a=o,s=Error(b(419)),a=Ur(s,a,void 0),fa(e,t,i,a)}if(o=(i&e.childLanes)!==0,ue||o){if(a=Y,a!==null){switch(i&-i){case 4:r=2;break;case 16:r=8;break;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:r=32;break;case 536870912:r=268435456;break;default:r=0}r=r&(a.suspendedLanes|i)?0:r,r!==0&&r!==s.retryLane&&(s.retryLane=r,_e(e,r),Pe(a,e,r,-1))}return Ni(),a=Ur(Error(b(421))),fa(e,t,i,a)}return r.data==="$?"?(t.flags|=128,t.child=e.child,t=Em.bind(null,e),r._reactRetry=t,null):(e=s.treeContext,fe=it(r.nextSibling),ge=t,H=!0,Ae=null,e!==null&&(Se[ke++]=Fe,Se[ke++]=Ue,Se[ke++]=Et,Fe=e.id,Ue=e.overflow,Et=t),t=ki(t,a.children),t.flags|=4096,t)}function Po(e,t,n){e.lanes|=t;var a=e.alternate;a!==null&&(a.lanes|=t),xs(e.return,t,n)}function zr(e,t,n,a,r){var s=e.memoizedState;s===null?e.memoizedState={isBackwards:t,rendering:null,renderingStartTime:0,last:a,tail:n,tailMode:r}:(s.isBackwards=t,s.rendering=null,s.renderingStartTime=0,s.last=a,s.tail=n,s.tailMode=r)}function Bc(e,t,n){var a=t.pendingProps,r=a.revealOrder,s=a.tail;if(ie(e,t,a.children,n),a=B.current,a&2)a=a&1|2,t.flags|=128;else{if(e!==null&&e.flags&128)e:for(e=t.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&Po(e,n,t);else if(e.tag===19)Po(e,n,t);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===t)break e;for(;e.sibling===null;){if(e.return===null||e.return===t)break e;e=e.return}e.sibling.return=e.return,e=e.sibling}a&=1}if(L(B,a),!(t.mode&1))t.memoizedState=null;else switch(r){case"forwards":for(n=t.child,r=null;n!==null;)e=n.alternate,e!==null&&Va(e)===null&&(r=n),n=n.sibling;n=r,n===null?(r=t.child,t.child=null):(r=n.sibling,n.sibling=null),zr(t,!1,r,n,s);break;case"backwards":for(n=null,r=t.child,t.child=null;r!==null;){if(e=r.alternate,e!==null&&Va(e)===null){t.child=r;break}e=r.sibling,r.sibling=n,n=r,r=e}zr(t,!0,n,null,s);break;case"together":zr(t,!1,null,null,void 0);break;default:t.memoizedState=null}return t.child}function Na(e,t){!(t.mode&1)&&e!==null&&(e.alternate=null,t.alternate=null,t.flags|=2)}function Ve(e,t,n){if(e!==null&&(t.dependencies=e.dependencies),Nt|=t.lanes,!(n&t.childLanes))return null;if(e!==null&&t.child!==e.child)throw Error(b(153));if(t.child!==null){for(e=t.child,n=dt(e,e.pendingProps),t.child=n,n.return=t;e.sibling!==null;)e=e.sibling,n=n.sibling=dt(e,e.pendingProps),n.return=t;n.sibling=null}return t.child}function hm(e,t,n){switch(t.tag){case 3:Hc(t),en();break;case 5:mc(t);break;case 1:pe(t.type)&&Fa(t);break;case 4:hi(t,t.stateNode.containerInfo);break;case 10:var a=t.type._context,r=t.memoizedProps.value;L(Ja,a._currentValue),a._currentValue=r;break;case 13:if(a=t.memoizedState,a!==null)return a.dehydrated!==null?(L(B,B.current&1),t.flags|=128,null):n&t.child.childLanes?Kc(e,t,n):(L(B,B.current&1),e=Ve(e,t,n),e!==null?e.sibling:null);L(B,B.current&1);break;case 19:if(a=(n&t.childLanes)!==0,e.flags&128){if(a)return Bc(e,t,n);t.flags|=128}if(r=t.memoizedState,r!==null&&(r.rendering=null,r.tail=null,r.lastEffect=null),L(B,B.current),a)break;return null;case 22:case 23:return t.lanes=0,Mc(e,t,n)}return Ve(e,t,n)}var Dc,As,Fc,Uc;Dc=function(e,t){for(var n=t.child;n!==null;){if(n.tag===5||n.tag===6)e.appendChild(n.stateNode);else if(n.tag!==4&&n.child!==null){n.child.return=n,n=n.child;continue}if(n===t)break;for(;n.sibling===null;){if(n.return===null||n.return===t)return;n=n.return}n.sibling.return=n.return,n=n.sibling}};As=function(){};Fc=function(e,t,n,a){var r=e.memoizedProps;if(r!==a){e=t.stateNode,kt(Ke.current);var s=null;switch(n){case"input":r=Zr(e,r),a=Zr(e,a),s=[];break;case"select":r=F({},r,{value:void 0}),a=F({},a,{value:void 0}),s=[];break;case"textarea":r=ns(e,r),a=ns(e,a),s=[];break;default:typeof r.onClick!="function"&&typeof a.onClick=="function"&&(e.onclick=Ba)}rs(n,a);var i;n=null;for(u in r)if(!a.hasOwnProperty(u)&&r.hasOwnProperty(u)&&r[u]!=null)if(u==="style"){var o=r[u];for(i in o)o.hasOwnProperty(i)&&(n||(n={}),n[i]="")}else u!=="dangerouslySetInnerHTML"&&u!=="children"&&u!=="suppressContentEditableWarning"&&u!=="suppressHydrationWarning"&&u!=="autoFocus"&&(Pn.hasOwnProperty(u)?s||(s=[]):(s=s||[]).push(u,null));for(u in a){var l=a[u];if(o=r!=null?r[u]:void 0,a.hasOwnProperty(u)&&l!==o&&(l!=null||o!=null))if(u==="style")if(o){for(i in o)!o.hasOwnProperty(i)||l&&l.hasOwnProperty(i)||(n||(n={}),n[i]="");for(i in l)l.hasOwnProperty(i)&&o[i]!==l[i]&&(n||(n={}),n[i]=l[i])}else n||(s||(s=[]),s.push(u,n)),n=l;else u==="dangerouslySetInnerHTML"?(l=l?l.__html:void 0,o=o?o.__html:void 0,l!=null&&o!==l&&(s=s||[]).push(u,l)):u==="children"?typeof l!="string"&&typeof l!="number"||(s=s||[]).push(u,""+l):u!=="suppressContentEditableWarning"&&u!=="suppressHydrationWarning"&&(Pn.hasOwnProperty(u)?(l!=null&&u==="onScroll"&&M("scroll",e),s||o===l||(s=[])):(s=s||[]).push(u,l))}n&&(s=s||[]).push("style",n);var u=s;(t.updateQueue=u)&&(t.flags|=4)}};Uc=function(e,t,n,a){n!==a&&(t.flags|=4)};function gn(e,t){if(!H)switch(e.tailMode){case"hidden":t=e.tail;for(var n=null;t!==null;)t.alternate!==null&&(n=t),t=t.sibling;n===null?e.tail=null:n.sibling=null;break;case"collapsed":n=e.tail;for(var a=null;n!==null;)n.alternate!==null&&(a=n),n=n.sibling;a===null?t||e.tail===null?e.tail=null:e.tail.sibling=null:a.sibling=null}}function ae(e){var t=e.alternate!==null&&e.alternate.child===e.child,n=0,a=0;if(t)for(var r=e.child;r!==null;)n|=r.lanes|r.childLanes,a|=r.subtreeFlags&14680064,a|=r.flags&14680064,r.return=e,r=r.sibling;else for(r=e.child;r!==null;)n|=r.lanes|r.childLanes,a|=r.subtreeFlags,a|=r.flags,r.return=e,r=r.sibling;return e.subtreeFlags|=a,e.childLanes=n,t}function ym(e,t,n){var a=t.pendingProps;switch(oi(t),t.tag){case 2:case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return ae(t),null;case 1:return pe(t.type)&&Da(),ae(t),null;case 3:return a=t.stateNode,nn(),W(me),W(se),fi(),a.pendingContext&&(a.context=a.pendingContext,a.pendingContext=null),(e===null||e.child===null)&&(ha(t)?t.flags|=4:e===null||e.memoizedState.isDehydrated&&!(t.flags&256)||(t.flags|=1024,Ae!==null&&(Ks(Ae),Ae=null))),As(e,t),ae(t),null;case 5:yi(t);var r=kt(Jn.current);if(n=t.type,e!==null&&t.stateNode!=null)Fc(e,t,n,a,r),e.ref!==t.ref&&(t.flags|=512,t.flags|=2097152);else{if(!a){if(t.stateNode===null)throw Error(b(166));return ae(t),null}if(e=kt(Ke.current),ha(t)){a=t.stateNode,n=t.type;var s=t.memoizedProps;switch(a[We]=t,a[Un]=s,e=(t.mode&1)!==0,n){case"dialog":M("cancel",a),M("close",a);break;case"iframe":case"object":case"embed":M("load",a);break;case"video":case"audio":for(r=0;r<xn.length;r++)M(xn[r],a);break;case"source":M("error",a);break;case"img":case"image":case"link":M("error",a),M("load",a);break;case"details":M("toggle",a);break;case"input":Di(a,s),M("invalid",a);break;case"select":a._wrapperState={wasMultiple:!!s.multiple},M("invalid",a);break;case"textarea":Ui(a,s),M("invalid",a)}rs(n,s),r=null;for(var i in s)if(s.hasOwnProperty(i)){var o=s[i];i==="children"?typeof o=="string"?a.textContent!==o&&(s.suppressHydrationWarning!==!0&&pa(a.textContent,o,e),r=["children",o]):typeof o=="number"&&a.textContent!==""+o&&(s.suppressHydrationWarning!==!0&&pa(a.textContent,o,e),r=["children",""+o]):Pn.hasOwnProperty(i)&&o!=null&&i==="onScroll"&&M("scroll",a)}switch(n){case"input":sa(a),Fi(a,s,!0);break;case"textarea":sa(a),zi(a);break;case"select":case"option":break;default:typeof s.onClick=="function"&&(a.onclick=Ba)}a=r,t.updateQueue=a,a!==null&&(t.flags|=4)}else{i=r.nodeType===9?r:r.ownerDocument,e==="http://www.w3.org/1999/xhtml"&&(e=gl(n)),e==="http://www.w3.org/1999/xhtml"?n==="script"?(e=i.createElement("div"),e.innerHTML="<script><\/script>",e=e.removeChild(e.firstChild)):typeof a.is=="string"?e=i.createElement(n,{is:a.is}):(e=i.createElement(n),n==="select"&&(i=e,a.multiple?i.multiple=!0:a.size&&(i.size=a.size))):e=i.createElementNS(e,n),e[We]=t,e[Un]=a,Dc(e,t,!1,!1),t.stateNode=e;e:{switch(i=ss(n,a),n){case"dialog":M("cancel",e),M("close",e),r=a;break;case"iframe":case"object":case"embed":M("load",e),r=a;break;case"video":case"audio":for(r=0;r<xn.length;r++)M(xn[r],e);r=a;break;case"source":M("error",e),r=a;break;case"img":case"image":case"link":M("error",e),M("load",e),r=a;break;case"details":M("toggle",e),r=a;break;case"input":Di(e,a),r=Zr(e,a),M("invalid",e);break;case"option":r=a;break;case"select":e._wrapperState={wasMultiple:!!a.multiple},r=F({},a,{value:void 0}),M("invalid",e);break;case"textarea":Ui(e,a),r=ns(e,a),M("invalid",e);break;default:r=a}rs(n,r),o=r;for(s in o)if(o.hasOwnProperty(s)){var l=o[s];s==="style"?wl(e,l):s==="dangerouslySetInnerHTML"?(l=l?l.__html:void 0,l!=null&&bl(e,l)):s==="children"?typeof l=="string"?(n!=="textarea"||l!=="")&&On(e,l):typeof l=="number"&&On(e,""+l):s!=="suppressContentEditableWarning"&&s!=="suppressHydrationWarning"&&s!=="autoFocus"&&(Pn.hasOwnProperty(s)?l!=null&&s==="onScroll"&&M("scroll",e):l!=null&&Gs(e,s,l,i))}switch(n){case"input":sa(e),Fi(e,a,!1);break;case"textarea":sa(e),zi(e);break;case"option":a.value!=null&&e.setAttribute("value",""+ut(a.value));break;case"select":e.multiple=!!a.multiple,s=a.value,s!=null?Gt(e,!!a.multiple,s,!1):a.defaultValue!=null&&Gt(e,!!a.multiple,a.defaultValue,!0);break;default:typeof r.onClick=="function"&&(e.onclick=Ba)}switch(n){case"button":case"input":case"select":case"textarea":a=!!a.autoFocus;break e;case"img":a=!0;break e;default:a=!1}}a&&(t.flags|=4)}t.ref!==null&&(t.flags|=512,t.flags|=2097152)}return ae(t),null;case 6:if(e&&t.stateNode!=null)Uc(e,t,e.memoizedProps,a);else{if(typeof a!="string"&&t.stateNode===null)throw Error(b(166));if(n=kt(Jn.current),kt(Ke.current),ha(t)){if(a=t.stateNode,n=t.memoizedProps,a[We]=t,(s=a.nodeValue!==n)&&(e=ge,e!==null))switch(e.tag){case 3:pa(a.nodeValue,n,(e.mode&1)!==0);break;case 5:e.memoizedProps.suppressHydrationWarning!==!0&&pa(a.nodeValue,n,(e.mode&1)!==0)}s&&(t.flags|=4)}else a=(n.nodeType===9?n:n.ownerDocument).createTextNode(a),a[We]=t,t.stateNode=a}return ae(t),null;case 13:if(W(B),a=t.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(H&&fe!==null&&t.mode&1&&!(t.flags&128))oc(),en(),t.flags|=98560,s=!1;else if(s=ha(t),a!==null&&a.dehydrated!==null){if(e===null){if(!s)throw Error(b(318));if(s=t.memoizedState,s=s!==null?s.dehydrated:null,!s)throw Error(b(317));s[We]=t}else en(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;ae(t),s=!1}else Ae!==null&&(Ks(Ae),Ae=null),s=!0;if(!s)return t.flags&65536?t:null}return t.flags&128?(t.lanes=n,t):(a=a!==null,a!==(e!==null&&e.memoizedState!==null)&&a&&(t.child.flags|=8192,t.mode&1&&(e===null||B.current&1?V===0&&(V=3):Ni())),t.updateQueue!==null&&(t.flags|=4),ae(t),null);case 4:return nn(),As(e,t),e===null&&Dn(t.stateNode.containerInfo),ae(t),null;case 10:return ui(t.type._context),ae(t),null;case 17:return pe(t.type)&&Da(),ae(t),null;case 19:if(W(B),s=t.memoizedState,s===null)return ae(t),null;if(a=(t.flags&128)!==0,i=s.rendering,i===null)if(a)gn(s,!1);else{if(V!==0||e!==null&&e.flags&128)for(e=t.child;e!==null;){if(i=Va(e),i!==null){for(t.flags|=128,gn(s,!1),a=i.updateQueue,a!==null&&(t.updateQueue=a,t.flags|=4),t.subtreeFlags=0,a=n,n=t.child;n!==null;)s=n,e=a,s.flags&=14680066,i=s.alternate,i===null?(s.childLanes=0,s.lanes=e,s.child=null,s.subtreeFlags=0,s.memoizedProps=null,s.memoizedState=null,s.updateQueue=null,s.dependencies=null,s.stateNode=null):(s.childLanes=i.childLanes,s.lanes=i.lanes,s.child=i.child,s.subtreeFlags=0,s.deletions=null,s.memoizedProps=i.memoizedProps,s.memoizedState=i.memoizedState,s.updateQueue=i.updateQueue,s.type=i.type,e=i.dependencies,s.dependencies=e===null?null:{lanes:e.lanes,firstContext:e.firstContext}),n=n.sibling;return L(B,B.current&1|2),t.child}e=e.sibling}s.tail!==null&&J()>rn&&(t.flags|=128,a=!0,gn(s,!1),t.lanes=4194304)}else{if(!a)if(e=Va(i),e!==null){if(t.flags|=128,a=!0,n=e.updateQueue,n!==null&&(t.updateQueue=n,t.flags|=4),gn(s,!0),s.tail===null&&s.tailMode==="hidden"&&!i.alternate&&!H)return ae(t),null}else 2*J()-s.renderingStartTime>rn&&n!==1073741824&&(t.flags|=128,a=!0,gn(s,!1),t.lanes=4194304);s.isBackwards?(i.sibling=t.child,t.child=i):(n=s.last,n!==null?n.sibling=i:t.child=i,s.last=i)}return s.tail!==null?(t=s.tail,s.rendering=t,s.tail=t.sibling,s.renderingStartTime=J(),t.sibling=null,n=B.current,L(B,a?n&1|2:n&1),t):(ae(t),null);case 22:case 23:return Ri(),a=t.memoizedState!==null,e!==null&&e.memoizedState!==null!==a&&(t.flags|=8192),a&&t.mode&1?ye&1073741824&&(ae(t),t.subtreeFlags&6&&(t.flags|=8192)):ae(t),null;case 24:return null;case 25:return null}throw Error(b(156,t.tag))}function fm(e,t){switch(oi(t),t.tag){case 1:return pe(t.type)&&Da(),e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 3:return nn(),W(me),W(se),fi(),e=t.flags,e&65536&&!(e&128)?(t.flags=e&-65537|128,t):null;case 5:return yi(t),null;case 13:if(W(B),e=t.memoizedState,e!==null&&e.dehydrated!==null){if(t.alternate===null)throw Error(b(340));en()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 19:return W(B),null;case 4:return nn(),null;case 10:return ui(t.type._context),null;case 22:case 23:return Ri(),null;case 24:return null;default:return null}}var ga=!1,re=!1,gm=typeof WeakSet=="function"?WeakSet:Set,C=null;function zt(e,t){var n=e.ref;if(n!==null)if(typeof n=="function")try{n(null)}catch(a){U(e,t,a)}else n.current=null}function Is(e,t,n){try{n()}catch(a){U(e,t,a)}}var Oo=!1;function bm(e,t){if(ys=Wa,e=Vl(),si(e)){if("selectionStart"in e)var n={start:e.selectionStart,end:e.selectionEnd};else e:{n=(n=e.ownerDocument)&&n.defaultView||window;var a=n.getSelection&&n.getSelection();if(a&&a.rangeCount!==0){n=a.anchorNode;var r=a.anchorOffset,s=a.focusNode;a=a.focusOffset;try{n.nodeType,s.nodeType}catch{n=null;break e}var i=0,o=-1,l=-1,u=0,f=0,y=e,h=null;t:for(;;){for(var v;y!==n||r!==0&&y.nodeType!==3||(o=i+r),y!==s||a!==0&&y.nodeType!==3||(l=i+a),y.nodeType===3&&(i+=y.nodeValue.length),(v=y.firstChild)!==null;)h=y,y=v;for(;;){if(y===e)break t;if(h===n&&++u===r&&(o=i),h===s&&++f===a&&(l=i),(v=y.nextSibling)!==null)break;y=h,h=y.parentNode}y=v}n=o===-1||l===-1?null:{start:o,end:l}}else n=null}n=n||{start:0,end:0}}else n=null;for(fs={focusedElem:e,selectionRange:n},Wa=!1,C=t;C!==null;)if(t=C,e=t.child,(t.subtreeFlags&1028)!==0&&e!==null)e.return=t,C=e;else for(;C!==null;){t=C;try{var w=t.alternate;if(t.flags&1024)switch(t.tag){case 0:case 11:case 15:break;case 1:if(w!==null){var k=w.memoizedProps,T=w.memoizedState,d=t.stateNode,c=d.getSnapshotBeforeUpdate(t.elementType===t.type?k:Te(t.type,k),T);d.__reactInternalSnapshotBeforeUpdate=c}break;case 3:var m=t.stateNode.containerInfo;m.nodeType===1?m.textContent="":m.nodeType===9&&m.documentElement&&m.removeChild(m.documentElement);break;case 5:case 6:case 4:case 17:break;default:throw Error(b(163))}}catch(g){U(t,t.return,g)}if(e=t.sibling,e!==null){e.return=t.return,C=e;break}C=t.return}return w=Oo,Oo=!1,w}function qn(e,t,n){var a=t.updateQueue;if(a=a!==null?a.lastEffect:null,a!==null){var r=a=a.next;do{if((r.tag&e)===e){var s=r.destroy;r.destroy=void 0,s!==void 0&&Is(t,n,s)}r=r.next}while(r!==a)}}function ur(e,t){if(t=t.updateQueue,t=t!==null?t.lastEffect:null,t!==null){var n=t=t.next;do{if((n.tag&e)===e){var a=n.create;n.destroy=a()}n=n.next}while(n!==t)}}function Ps(e){var t=e.ref;if(t!==null){var n=e.stateNode;switch(e.tag){case 5:e=n;break;default:e=n}typeof t=="function"?t(e):t.current=e}}function zc(e){var t=e.alternate;t!==null&&(e.alternate=null,zc(t)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(t=e.stateNode,t!==null&&(delete t[We],delete t[Un],delete t[vs],delete t[em],delete t[tm])),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}function Jc(e){return e.tag===5||e.tag===3||e.tag===4}function Lo(e){e:for(;;){for(;e.sibling===null;){if(e.return===null||Jc(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.flags&2||e.child===null||e.tag===4)continue e;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Os(e,t,n){var a=e.tag;if(a===5||a===6)e=e.stateNode,t?n.nodeType===8?n.parentNode.insertBefore(e,t):n.insertBefore(e,t):(n.nodeType===8?(t=n.parentNode,t.insertBefore(e,n)):(t=n,t.appendChild(e)),n=n._reactRootContainer,n!=null||t.onclick!==null||(t.onclick=Ba));else if(a!==4&&(e=e.child,e!==null))for(Os(e,t,n),e=e.sibling;e!==null;)Os(e,t,n),e=e.sibling}function Ls(e,t,n){var a=e.tag;if(a===5||a===6)e=e.stateNode,t?n.insertBefore(e,t):n.appendChild(e);else if(a!==4&&(e=e.child,e!==null))for(Ls(e,t,n),e=e.sibling;e!==null;)Ls(e,t,n),e=e.sibling}var Z=null,qe=!1;function Xe(e,t,n){for(n=n.child;n!==null;)Gc(e,t,n),n=n.sibling}function Gc(e,t,n){if(He&&typeof He.onCommitFiberUnmount=="function")try{He.onCommitFiberUnmount(ar,n)}catch{}switch(n.tag){case 5:re||zt(n,t);case 6:var a=Z,r=qe;Z=null,Xe(e,t,n),Z=a,qe=r,Z!==null&&(qe?(e=Z,n=n.stateNode,e.nodeType===8?e.parentNode.removeChild(n):e.removeChild(n)):Z.removeChild(n.stateNode));break;case 18:Z!==null&&(qe?(e=Z,n=n.stateNode,e.nodeType===8?Wr(e.parentNode,n):e.nodeType===1&&Wr(e,n),Hn(e)):Wr(Z,n.stateNode));break;case 4:a=Z,r=qe,Z=n.stateNode.containerInfo,qe=!0,Xe(e,t,n),Z=a,qe=r;break;case 0:case 11:case 14:case 15:if(!re&&(a=n.updateQueue,a!==null&&(a=a.lastEffect,a!==null))){r=a=a.next;do{var s=r,i=s.destroy;s=s.tag,i!==void 0&&(s&2||s&4)&&Is(n,t,i),r=r.next}while(r!==a)}Xe(e,t,n);break;case 1:if(!re&&(zt(n,t),a=n.stateNode,typeof a.componentWillUnmount=="function"))try{a.props=n.memoizedProps,a.state=n.memoizedState,a.componentWillUnmount()}catch(o){U(n,t,o)}Xe(e,t,n);break;case 21:Xe(e,t,n);break;case 22:n.mode&1?(re=(a=re)||n.memoizedState!==null,Xe(e,t,n),re=a):Xe(e,t,n);break;default:Xe(e,t,n)}}function Mo(e){var t=e.updateQueue;if(t!==null){e.updateQueue=null;var n=e.stateNode;n===null&&(n=e.stateNode=new gm),t.forEach(function(a){var r=Rm.bind(null,e,a);n.has(a)||(n.add(a),a.then(r,r))})}}function Ne(e,t){var n=t.deletions;if(n!==null)for(var a=0;a<n.length;a++){var r=n[a];try{var s=e,i=t,o=i;e:for(;o!==null;){switch(o.tag){case 5:Z=o.stateNode,qe=!1;break e;case 3:Z=o.stateNode.containerInfo,qe=!0;break e;case 4:Z=o.stateNode.containerInfo,qe=!0;break e}o=o.return}if(Z===null)throw Error(b(160));Gc(s,i,r),Z=null,qe=!1;var l=r.alternate;l!==null&&(l.return=null),r.return=null}catch(u){U(r,t,u)}}if(t.subtreeFlags&12854)for(t=t.child;t!==null;)_c(t,e),t=t.sibling}function _c(e,t){var n=e.alternate,a=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:if(Ne(t,e),Le(e),a&4){try{qn(3,e,e.return),ur(3,e)}catch(k){U(e,e.return,k)}try{qn(5,e,e.return)}catch(k){U(e,e.return,k)}}break;case 1:Ne(t,e),Le(e),a&512&&n!==null&&zt(n,n.return);break;case 5:if(Ne(t,e),Le(e),a&512&&n!==null&&zt(n,n.return),e.flags&32){var r=e.stateNode;try{On(r,"")}catch(k){U(e,e.return,k)}}if(a&4&&(r=e.stateNode,r!=null)){var s=e.memoizedProps,i=n!==null?n.memoizedProps:s,o=e.type,l=e.updateQueue;if(e.updateQueue=null,l!==null)try{o==="input"&&s.type==="radio"&&s.name!=null&&yl(r,s),ss(o,i);var u=ss(o,s);for(i=0;i<l.length;i+=2){var f=l[i],y=l[i+1];f==="style"?wl(r,y):f==="dangerouslySetInnerHTML"?bl(r,y):f==="children"?On(r,y):Gs(r,f,y,u)}switch(o){case"input":es(r,s);break;case"textarea":fl(r,s);break;case"select":var h=r._wrapperState.wasMultiple;r._wrapperState.wasMultiple=!!s.multiple;var v=s.value;v!=null?Gt(r,!!s.multiple,v,!1):h!==!!s.multiple&&(s.defaultValue!=null?Gt(r,!!s.multiple,s.defaultValue,!0):Gt(r,!!s.multiple,s.multiple?[]:"",!1))}r[Un]=s}catch(k){U(e,e.return,k)}}break;case 6:if(Ne(t,e),Le(e),a&4){if(e.stateNode===null)throw Error(b(162));r=e.stateNode,s=e.memoizedProps;try{r.nodeValue=s}catch(k){U(e,e.return,k)}}break;case 3:if(Ne(t,e),Le(e),a&4&&n!==null&&n.memoizedState.isDehydrated)try{Hn(t.containerInfo)}catch(k){U(e,e.return,k)}break;case 4:Ne(t,e),Le(e);break;case 13:Ne(t,e),Le(e),r=e.child,r.flags&8192&&(s=r.memoizedState!==null,r.stateNode.isHidden=s,!s||r.alternate!==null&&r.alternate.memoizedState!==null||(ji=J())),a&4&&Mo(e);break;case 22:if(f=n!==null&&n.memoizedState!==null,e.mode&1?(re=(u=re)||f,Ne(t,e),re=u):Ne(t,e),Le(e),a&8192){if(u=e.memoizedState!==null,(e.stateNode.isHidden=u)&&!f&&e.mode&1)for(C=e,f=e.child;f!==null;){for(y=C=f;C!==null;){switch(h=C,v=h.child,h.tag){case 0:case 11:case 14:case 15:qn(4,h,h.return);break;case 1:zt(h,h.return);var w=h.stateNode;if(typeof w.componentWillUnmount=="function"){a=h,n=h.return;try{t=a,w.props=t.memoizedProps,w.state=t.memoizedState,w.componentWillUnmount()}catch(k){U(a,n,k)}}break;case 5:zt(h,h.return);break;case 22:if(h.memoizedState!==null){Ho(y);continue}}v!==null?(v.return=h,C=v):Ho(y)}f=f.sibling}e:for(f=null,y=e;;){if(y.tag===5){if(f===null){f=y;try{r=y.stateNode,u?(s=r.style,typeof s.setProperty=="function"?s.setProperty("display","none","important"):s.display="none"):(o=y.stateNode,l=y.memoizedProps.style,i=l!=null&&l.hasOwnProperty("display")?l.display:null,o.style.display=vl("display",i))}catch(k){U(e,e.return,k)}}}else if(y.tag===6){if(f===null)try{y.stateNode.nodeValue=u?"":y.memoizedProps}catch(k){U(e,e.return,k)}}else if((y.tag!==22&&y.tag!==23||y.memoizedState===null||y===e)&&y.child!==null){y.child.return=y,y=y.child;continue}if(y===e)break e;for(;y.sibling===null;){if(y.return===null||y.return===e)break e;f===y&&(f=null),y=y.return}f===y&&(f=null),y.sibling.return=y.return,y=y.sibling}}break;case 19:Ne(t,e),Le(e),a&4&&Mo(e);break;case 21:break;default:Ne(t,e),Le(e)}}function Le(e){var t=e.flags;if(t&2){try{e:{for(var n=e.return;n!==null;){if(Jc(n)){var a=n;break e}n=n.return}throw Error(b(160))}switch(a.tag){case 5:var r=a.stateNode;a.flags&32&&(On(r,""),a.flags&=-33);var s=Lo(e);Ls(e,s,r);break;case 3:case 4:var i=a.stateNode.containerInfo,o=Lo(e);Os(e,o,i);break;default:throw Error(b(161))}}catch(l){U(e,e.return,l)}e.flags&=-3}t&4096&&(e.flags&=-4097)}function vm(e,t,n){C=e,Vc(e)}function Vc(e,t,n){for(var a=(e.mode&1)!==0;C!==null;){var r=C,s=r.child;if(r.tag===22&&a){var i=r.memoizedState!==null||ga;if(!i){var o=r.alternate,l=o!==null&&o.memoizedState!==null||re;o=ga;var u=re;if(ga=i,(re=l)&&!u)for(C=r;C!==null;)i=C,l=i.child,i.tag===22&&i.memoizedState!==null?Ko(r):l!==null?(l.return=i,C=l):Ko(r);for(;s!==null;)C=s,Vc(s),s=s.sibling;C=r,ga=o,re=u}Wo(e)}else r.subtreeFlags&8772&&s!==null?(s.return=r,C=s):Wo(e)}}function Wo(e){for(;C!==null;){var t=C;if(t.flags&8772){var n=t.alternate;try{if(t.flags&8772)switch(t.tag){case 0:case 11:case 15:re||ur(5,t);break;case 1:var a=t.stateNode;if(t.flags&4&&!re)if(n===null)a.componentDidMount();else{var r=t.elementType===t.type?n.memoizedProps:Te(t.type,n.memoizedProps);a.componentDidUpdate(r,n.memoizedState,a.__reactInternalSnapshotBeforeUpdate)}var s=t.updateQueue;s!==null&&So(t,s,a);break;case 3:var i=t.updateQueue;if(i!==null){if(n=null,t.child!==null)switch(t.child.tag){case 5:n=t.child.stateNode;break;case 1:n=t.child.stateNode}So(t,i,n)}break;case 5:var o=t.stateNode;if(n===null&&t.flags&4){n=o;var l=t.memoizedProps;switch(t.type){case"button":case"input":case"select":case"textarea":l.autoFocus&&n.focus();break;case"img":l.src&&(n.src=l.src)}}break;case 6:break;case 4:break;case 12:break;case 13:if(t.memoizedState===null){var u=t.alternate;if(u!==null){var f=u.memoizedState;if(f!==null){var y=f.dehydrated;y!==null&&Hn(y)}}}break;case 19:case 17:case 21:case 22:case 23:case 25:break;default:throw Error(b(163))}re||t.flags&512&&Ps(t)}catch(h){U(t,t.return,h)}}if(t===e){C=null;break}if(n=t.sibling,n!==null){n.return=t.return,C=n;break}C=t.return}}function Ho(e){for(;C!==null;){var t=C;if(t===e){C=null;break}var n=t.sibling;if(n!==null){n.return=t.return,C=n;break}C=t.return}}function Ko(e){for(;C!==null;){var t=C;try{switch(t.tag){case 0:case 11:case 15:var n=t.return;try{ur(4,t)}catch(l){U(t,n,l)}break;case 1:var a=t.stateNode;if(typeof a.componentDidMount=="function"){var r=t.return;try{a.componentDidMount()}catch(l){U(t,r,l)}}var s=t.return;try{Ps(t)}catch(l){U(t,s,l)}break;case 5:var i=t.return;try{Ps(t)}catch(l){U(t,i,l)}}}catch(l){U(t,t.return,l)}if(t===e){C=null;break}var o=t.sibling;if(o!==null){o.return=t.return,C=o;break}C=t.return}}var wm=Math.ceil,Ya=Qe.ReactCurrentDispatcher,xi=Qe.ReactCurrentOwner,Ce=Qe.ReactCurrentBatchConfig,P=0,Y=null,G=null,ee=0,ye=0,Jt=ht(0),V=0,Qn=null,Nt=0,mr=0,Ci=0,An=null,de=null,ji=0,rn=1/0,Be=null,$a=!1,Ms=null,lt=null,ba=!1,nt=null,Za=0,In=0,Ws=null,Ta=-1,qa=0;function oe(){return P&6?J():Ta!==-1?Ta:Ta=J()}function ct(e){return e.mode&1?P&2&&ee!==0?ee&-ee:am.transition!==null?(qa===0&&(qa=Il()),qa):(e=O,e!==0||(e=window.event,e=e===void 0?16:Kl(e.type)),e):1}function Pe(e,t,n,a){if(50<In)throw In=0,Ws=null,Error(b(185));Yn(e,n,a),(!(P&2)||e!==Y)&&(e===Y&&(!(P&2)&&(mr|=n),V===4&&et(e,ee)),he(e,a),n===1&&P===0&&!(t.mode&1)&&(rn=J()+500,lr&&yt()))}function he(e,t){var n=e.callbackNode;au(e,t);var a=Ma(e,e===Y?ee:0);if(a===0)n!==null&&_i(n),e.callbackNode=null,e.callbackPriority=0;else if(t=a&-a,e.callbackPriority!==t){if(n!=null&&_i(n),t===1)e.tag===0?nm(Bo.bind(null,e)):rc(Bo.bind(null,e)),$u(function(){!(P&6)&&yt()}),n=null;else{switch(Pl(a)){case 1:n=Ys;break;case 4:n=ql;break;case 16:n=La;break;case 536870912:n=Al;break;default:n=La}n=nd(n,Qc.bind(null,e))}e.callbackPriority=t,e.callbackNode=n}}function Qc(e,t){if(Ta=-1,qa=0,P&6)throw Error(b(327));var n=e.callbackNode;if(Yt()&&e.callbackNode!==n)return null;var a=Ma(e,e===Y?ee:0);if(a===0)return null;if(a&30||a&e.expiredLanes||t)t=er(e,a);else{t=a;var r=P;P|=2;var s=Yc();(Y!==e||ee!==t)&&(Be=null,rn=J()+500,xt(e,t));do try{xm();break}catch(o){Xc(e,o)}while(!0);di(),Ya.current=s,P=r,G!==null?t=0:(Y=null,ee=0,t=V)}if(t!==0){if(t===2&&(r=ds(e),r!==0&&(a=r,t=Hs(e,r))),t===1)throw n=Qn,xt(e,0),et(e,a),he(e,J()),n;if(t===6)et(e,a);else{if(r=e.current.alternate,!(a&30)&&!Sm(r)&&(t=er(e,a),t===2&&(s=ds(e),s!==0&&(a=s,t=Hs(e,s))),t===1))throw n=Qn,xt(e,0),et(e,a),he(e,J()),n;switch(e.finishedWork=r,e.finishedLanes=a,t){case 0:case 1:throw Error(b(345));case 2:vt(e,de,Be);break;case 3:if(et(e,a),(a&130023424)===a&&(t=ji+500-J(),10<t)){if(Ma(e,0)!==0)break;if(r=e.suspendedLanes,(r&a)!==a){oe(),e.pingedLanes|=e.suspendedLanes&r;break}e.timeoutHandle=bs(vt.bind(null,e,de,Be),t);break}vt(e,de,Be);break;case 4:if(et(e,a),(a&4194240)===a)break;for(t=e.eventTimes,r=-1;0<a;){var i=31-Ie(a);s=1<<i,i=t[i],i>r&&(r=i),a&=~s}if(a=r,a=J()-a,a=(120>a?120:480>a?480:1080>a?1080:1920>a?1920:3e3>a?3e3:4320>a?4320:1960*wm(a/1960))-a,10<a){e.timeoutHandle=bs(vt.bind(null,e,de,Be),a);break}vt(e,de,Be);break;case 5:vt(e,de,Be);break;default:throw Error(b(329))}}}return he(e,J()),e.callbackNode===n?Qc.bind(null,e):null}function Hs(e,t){var n=An;return e.current.memoizedState.isDehydrated&&(xt(e,t).flags|=256),e=er(e,t),e!==2&&(t=de,de=n,t!==null&&Ks(t)),e}function Ks(e){de===null?de=e:de.push.apply(de,e)}function Sm(e){for(var t=e;;){if(t.flags&16384){var n=t.updateQueue;if(n!==null&&(n=n.stores,n!==null))for(var a=0;a<n.length;a++){var r=n[a],s=r.getSnapshot;r=r.value;try{if(!Oe(s(),r))return!1}catch{return!1}}}if(n=t.child,t.subtreeFlags&16384&&n!==null)n.return=t,t=n;else{if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return!0;t=t.return}t.sibling.return=t.return,t=t.sibling}}return!0}function et(e,t){for(t&=~Ci,t&=~mr,e.suspendedLanes|=t,e.pingedLanes&=~t,e=e.expirationTimes;0<t;){var n=31-Ie(t),a=1<<n;e[n]=-1,t&=~a}}function Bo(e){if(P&6)throw Error(b(327));Yt();var t=Ma(e,0);if(!(t&1))return he(e,J()),null;var n=er(e,t);if(e.tag!==0&&n===2){var a=ds(e);a!==0&&(t=a,n=Hs(e,a))}if(n===1)throw n=Qn,xt(e,0),et(e,t),he(e,J()),n;if(n===6)throw Error(b(345));return e.finishedWork=e.current.alternate,e.finishedLanes=t,vt(e,de,Be),he(e,J()),null}function Ei(e,t){var n=P;P|=1;try{return e(t)}finally{P=n,P===0&&(rn=J()+500,lr&&yt())}}function Tt(e){nt!==null&&nt.tag===0&&!(P&6)&&Yt();var t=P;P|=1;var n=Ce.transition,a=O;try{if(Ce.transition=null,O=1,e)return e()}finally{O=a,Ce.transition=n,P=t,!(P&6)&&yt()}}function Ri(){ye=Jt.current,W(Jt)}function xt(e,t){e.finishedWork=null,e.finishedLanes=0;var n=e.timeoutHandle;if(n!==-1&&(e.timeoutHandle=-1,Yu(n)),G!==null)for(n=G.return;n!==null;){var a=n;switch(oi(a),a.tag){case 1:a=a.type.childContextTypes,a!=null&&Da();break;case 3:nn(),W(me),W(se),fi();break;case 5:yi(a);break;case 4:nn();break;case 13:W(B);break;case 19:W(B);break;case 10:ui(a.type._context);break;case 22:case 23:Ri()}n=n.return}if(Y=e,G=e=dt(e.current,null),ee=ye=t,V=0,Qn=null,Ci=mr=Nt=0,de=An=null,St!==null){for(t=0;t<St.length;t++)if(n=St[t],a=n.interleaved,a!==null){n.interleaved=null;var r=a.next,s=n.pending;if(s!==null){var i=s.next;s.next=r,a.next=i}n.pending=a}St=null}return e}function Xc(e,t){do{var n=G;try{if(di(),Ea.current=Xa,Qa){for(var a=D.memoizedState;a!==null;){var r=a.queue;r!==null&&(r.pending=null),a=a.next}Qa=!1}if(Rt=0,X=_=D=null,Tn=!1,Gn=0,xi.current=null,n===null||n.return===null){V=1,Qn=t,G=null;break}e:{var s=e,i=n.return,o=n,l=t;if(t=ee,o.flags|=32768,l!==null&&typeof l=="object"&&typeof l.then=="function"){var u=l,f=o,y=f.tag;if(!(f.mode&1)&&(y===0||y===11||y===15)){var h=f.alternate;h?(f.updateQueue=h.updateQueue,f.memoizedState=h.memoizedState,f.lanes=h.lanes):(f.updateQueue=null,f.memoizedState=null)}var v=Ro(i);if(v!==null){v.flags&=-257,No(v,i,o,s,t),v.mode&1&&Eo(s,u,t),t=v,l=u;var w=t.updateQueue;if(w===null){var k=new Set;k.add(l),t.updateQueue=k}else w.add(l);break e}else{if(!(t&1)){Eo(s,u,t),Ni();break e}l=Error(b(426))}}else if(H&&o.mode&1){var T=Ro(i);if(T!==null){!(T.flags&65536)&&(T.flags|=256),No(T,i,o,s,t),li(an(l,o));break e}}s=l=an(l,o),V!==4&&(V=2),An===null?An=[s]:An.push(s),s=i;do{switch(s.tag){case 3:s.flags|=65536,t&=-t,s.lanes|=t;var d=Pc(s,l,t);wo(s,d);break e;case 1:o=l;var c=s.type,m=s.stateNode;if(!(s.flags&128)&&(typeof c.getDerivedStateFromError=="function"||m!==null&&typeof m.componentDidCatch=="function"&&(lt===null||!lt.has(m)))){s.flags|=65536,t&=-t,s.lanes|=t;var g=Oc(s,o,t);wo(s,g);break e}}s=s.return}while(s!==null)}Zc(n)}catch(S){t=S,G===n&&n!==null&&(G=n=n.return);continue}break}while(!0)}function Yc(){var e=Ya.current;return Ya.current=Xa,e===null?Xa:e}function Ni(){(V===0||V===3||V===2)&&(V=4),Y===null||!(Nt&268435455)&&!(mr&268435455)||et(Y,ee)}function er(e,t){var n=P;P|=2;var a=Yc();(Y!==e||ee!==t)&&(Be=null,xt(e,t));do try{km();break}catch(r){Xc(e,r)}while(!0);if(di(),P=n,Ya.current=a,G!==null)throw Error(b(261));return Y=null,ee=0,V}function km(){for(;G!==null;)$c(G)}function xm(){for(;G!==null&&!Vd();)$c(G)}function $c(e){var t=td(e.alternate,e,ye);e.memoizedProps=e.pendingProps,t===null?Zc(e):G=t,xi.current=null}function Zc(e){var t=e;do{var n=t.alternate;if(e=t.return,t.flags&32768){if(n=fm(n,t),n!==null){n.flags&=32767,G=n;return}if(e!==null)e.flags|=32768,e.subtreeFlags=0,e.deletions=null;else{V=6,G=null;return}}else if(n=ym(n,t,ye),n!==null){G=n;return}if(t=t.sibling,t!==null){G=t;return}G=t=e}while(t!==null);V===0&&(V=5)}function vt(e,t,n){var a=O,r=Ce.transition;try{Ce.transition=null,O=1,Cm(e,t,n,a)}finally{Ce.transition=r,O=a}return null}function Cm(e,t,n,a){do Yt();while(nt!==null);if(P&6)throw Error(b(327));n=e.finishedWork;var r=e.finishedLanes;if(n===null)return null;if(e.finishedWork=null,e.finishedLanes=0,n===e.current)throw Error(b(177));e.callbackNode=null,e.callbackPriority=0;var s=n.lanes|n.childLanes;if(ru(e,s),e===Y&&(G=Y=null,ee=0),!(n.subtreeFlags&2064)&&!(n.flags&2064)||ba||(ba=!0,nd(La,function(){return Yt(),null})),s=(n.flags&15990)!==0,n.subtreeFlags&15990||s){s=Ce.transition,Ce.transition=null;var i=O;O=1;var o=P;P|=4,xi.current=null,bm(e,n),_c(n,e),zu(fs),Wa=!!ys,fs=ys=null,e.current=n,vm(n),Qd(),P=o,O=i,Ce.transition=s}else e.current=n;if(ba&&(ba=!1,nt=e,Za=r),s=e.pendingLanes,s===0&&(lt=null),$d(n.stateNode),he(e,J()),t!==null)for(a=e.onRecoverableError,n=0;n<t.length;n++)r=t[n],a(r.value,{componentStack:r.stack,digest:r.digest});if($a)throw $a=!1,e=Ms,Ms=null,e;return Za&1&&e.tag!==0&&Yt(),s=e.pendingLanes,s&1?e===Ws?In++:(In=0,Ws=e):In=0,yt(),null}function Yt(){if(nt!==null){var e=Pl(Za),t=Ce.transition,n=O;try{if(Ce.transition=null,O=16>e?16:e,nt===null)var a=!1;else{if(e=nt,nt=null,Za=0,P&6)throw Error(b(331));var r=P;for(P|=4,C=e.current;C!==null;){var s=C,i=s.child;if(C.flags&16){var o=s.deletions;if(o!==null){for(var l=0;l<o.length;l++){var u=o[l];for(C=u;C!==null;){var f=C;switch(f.tag){case 0:case 11:case 15:qn(8,f,s)}var y=f.child;if(y!==null)y.return=f,C=y;else for(;C!==null;){f=C;var h=f.sibling,v=f.return;if(zc(f),f===u){C=null;break}if(h!==null){h.return=v,C=h;break}C=v}}}var w=s.alternate;if(w!==null){var k=w.child;if(k!==null){w.child=null;do{var T=k.sibling;k.sibling=null,k=T}while(k!==null)}}C=s}}if(s.subtreeFlags&2064&&i!==null)i.return=s,C=i;else e:for(;C!==null;){if(s=C,s.flags&2048)switch(s.tag){case 0:case 11:case 15:qn(9,s,s.return)}var d=s.sibling;if(d!==null){d.return=s.return,C=d;break e}C=s.return}}var c=e.current;for(C=c;C!==null;){i=C;var m=i.child;if(i.subtreeFlags&2064&&m!==null)m.return=i,C=m;else e:for(i=c;C!==null;){if(o=C,o.flags&2048)try{switch(o.tag){case 0:case 11:case 15:ur(9,o)}}catch(S){U(o,o.return,S)}if(o===i){C=null;break e}var g=o.sibling;if(g!==null){g.return=o.return,C=g;break e}C=o.return}}if(P=r,yt(),He&&typeof He.onPostCommitFiberRoot=="function")try{He.onPostCommitFiberRoot(ar,e)}catch{}a=!0}return a}finally{O=n,Ce.transition=t}}return!1}function Do(e,t,n){t=an(n,t),t=Pc(e,t,1),e=ot(e,t,1),t=oe(),e!==null&&(Yn(e,1,t),he(e,t))}function U(e,t,n){if(e.tag===3)Do(e,e,n);else for(;t!==null;){if(t.tag===3){Do(t,e,n);break}else if(t.tag===1){var a=t.stateNode;if(typeof t.type.getDerivedStateFromError=="function"||typeof a.componentDidCatch=="function"&&(lt===null||!lt.has(a))){e=an(n,e),e=Oc(t,e,1),t=ot(t,e,1),e=oe(),t!==null&&(Yn(t,1,e),he(t,e));break}}t=t.return}}function jm(e,t,n){var a=e.pingCache;a!==null&&a.delete(t),t=oe(),e.pingedLanes|=e.suspendedLanes&n,Y===e&&(ee&n)===n&&(V===4||V===3&&(ee&130023424)===ee&&500>J()-ji?xt(e,0):Ci|=n),he(e,t)}function ed(e,t){t===0&&(e.mode&1?(t=la,la<<=1,!(la&130023424)&&(la=4194304)):t=1);var n=oe();e=_e(e,t),e!==null&&(Yn(e,t,n),he(e,n))}function Em(e){var t=e.memoizedState,n=0;t!==null&&(n=t.retryLane),ed(e,n)}function Rm(e,t){var n=0;switch(e.tag){case 13:var a=e.stateNode,r=e.memoizedState;r!==null&&(n=r.retryLane);break;case 19:a=e.stateNode;break;default:throw Error(b(314))}a!==null&&a.delete(t),ed(e,n)}var td;td=function(e,t,n){if(e!==null)if(e.memoizedProps!==t.pendingProps||me.current)ue=!0;else{if(!(e.lanes&n)&&!(t.flags&128))return ue=!1,hm(e,t,n);ue=!!(e.flags&131072)}else ue=!1,H&&t.flags&1048576&&sc(t,za,t.index);switch(t.lanes=0,t.tag){case 2:var a=t.type;Na(e,t),e=t.pendingProps;var r=Zt(t,se.current);Xt(t,n),r=bi(null,t,a,e,r,n);var s=vi();return t.flags|=1,typeof r=="object"&&r!==null&&typeof r.render=="function"&&r.$$typeof===void 0?(t.tag=1,t.memoizedState=null,t.updateQueue=null,pe(a)?(s=!0,Fa(t)):s=!1,t.memoizedState=r.state!==null&&r.state!==void 0?r.state:null,pi(t),r.updater=dr,t.stateNode=r,r._reactInternals=t,js(t,a,e,n),t=Ns(null,t,a,!0,s,n)):(t.tag=0,H&&s&&ii(t),ie(null,t,r,n),t=t.child),t;case 16:a=t.elementType;e:{switch(Na(e,t),e=t.pendingProps,r=a._init,a=r(a._payload),t.type=a,r=t.tag=Tm(a),e=Te(a,e),r){case 0:t=Rs(null,t,a,e,n);break e;case 1:t=Ao(null,t,a,e,n);break e;case 11:t=To(null,t,a,e,n);break e;case 14:t=qo(null,t,a,Te(a.type,e),n);break e}throw Error(b(306,a,""))}return t;case 0:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Te(a,r),Rs(e,t,a,r,n);case 1:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Te(a,r),Ao(e,t,a,r,n);case 3:e:{if(Hc(t),e===null)throw Error(b(387));a=t.pendingProps,s=t.memoizedState,r=s.element,uc(e,t),_a(t,a,null,n);var i=t.memoizedState;if(a=i.element,s.isDehydrated)if(s={element:a,isDehydrated:!1,cache:i.cache,pendingSuspenseBoundaries:i.pendingSuspenseBoundaries,transitions:i.transitions},t.updateQueue.baseState=s,t.memoizedState=s,t.flags&256){r=an(Error(b(423)),t),t=Io(e,t,a,n,r);break e}else if(a!==r){r=an(Error(b(424)),t),t=Io(e,t,a,n,r);break e}else for(fe=it(t.stateNode.containerInfo.firstChild),ge=t,H=!0,Ae=null,n=cc(t,null,a,n),t.child=n;n;)n.flags=n.flags&-3|4096,n=n.sibling;else{if(en(),a===r){t=Ve(e,t,n);break e}ie(e,t,a,n)}t=t.child}return t;case 5:return mc(t),e===null&&ks(t),a=t.type,r=t.pendingProps,s=e!==null?e.memoizedProps:null,i=r.children,gs(a,r)?i=null:s!==null&&gs(a,s)&&(t.flags|=32),Wc(e,t),ie(e,t,i,n),t.child;case 6:return e===null&&ks(t),null;case 13:return Kc(e,t,n);case 4:return hi(t,t.stateNode.containerInfo),a=t.pendingProps,e===null?t.child=tn(t,null,a,n):ie(e,t,a,n),t.child;case 11:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Te(a,r),To(e,t,a,r,n);case 7:return ie(e,t,t.pendingProps,n),t.child;case 8:return ie(e,t,t.pendingProps.children,n),t.child;case 12:return ie(e,t,t.pendingProps.children,n),t.child;case 10:e:{if(a=t.type._context,r=t.pendingProps,s=t.memoizedProps,i=r.value,L(Ja,a._currentValue),a._currentValue=i,s!==null)if(Oe(s.value,i)){if(s.children===r.children&&!me.current){t=Ve(e,t,n);break e}}else for(s=t.child,s!==null&&(s.return=t);s!==null;){var o=s.dependencies;if(o!==null){i=s.child;for(var l=o.firstContext;l!==null;){if(l.context===a){if(s.tag===1){l=ze(-1,n&-n),l.tag=2;var u=s.updateQueue;if(u!==null){u=u.shared;var f=u.pending;f===null?l.next=l:(l.next=f.next,f.next=l),u.pending=l}}s.lanes|=n,l=s.alternate,l!==null&&(l.lanes|=n),xs(s.return,n,t),o.lanes|=n;break}l=l.next}}else if(s.tag===10)i=s.type===t.type?null:s.child;else if(s.tag===18){if(i=s.return,i===null)throw Error(b(341));i.lanes|=n,o=i.alternate,o!==null&&(o.lanes|=n),xs(i,n,t),i=s.sibling}else i=s.child;if(i!==null)i.return=s;else for(i=s;i!==null;){if(i===t){i=null;break}if(s=i.sibling,s!==null){s.return=i.return,i=s;break}i=i.return}s=i}ie(e,t,r.children,n),t=t.child}return t;case 9:return r=t.type,a=t.pendingProps.children,Xt(t,n),r=je(r),a=a(r),t.flags|=1,ie(e,t,a,n),t.child;case 14:return a=t.type,r=Te(a,t.pendingProps),r=Te(a.type,r),qo(e,t,a,r,n);case 15:return Lc(e,t,t.type,t.pendingProps,n);case 17:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Te(a,r),Na(e,t),t.tag=1,pe(a)?(e=!0,Fa(t)):e=!1,Xt(t,n),Ic(t,a,r),js(t,a,r,n),Ns(null,t,a,!0,e,n);case 19:return Bc(e,t,n);case 22:return Mc(e,t,n)}throw Error(b(156,t.tag))};function nd(e,t){return Tl(e,t)}function Nm(e,t,n,a){this.tag=e,this.key=n,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.ref=null,this.pendingProps=t,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=a,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function xe(e,t,n,a){return new Nm(e,t,n,a)}function Ti(e){return e=e.prototype,!(!e||!e.isReactComponent)}function Tm(e){if(typeof e=="function")return Ti(e)?1:0;if(e!=null){if(e=e.$$typeof,e===Vs)return 11;if(e===Qs)return 14}return 2}function dt(e,t){var n=e.alternate;return n===null?(n=xe(e.tag,t,e.key,e.mode),n.elementType=e.elementType,n.type=e.type,n.stateNode=e.stateNode,n.alternate=e,e.alternate=n):(n.pendingProps=t,n.type=e.type,n.flags=0,n.subtreeFlags=0,n.deletions=null),n.flags=e.flags&14680064,n.childLanes=e.childLanes,n.lanes=e.lanes,n.child=e.child,n.memoizedProps=e.memoizedProps,n.memoizedState=e.memoizedState,n.updateQueue=e.updateQueue,t=e.dependencies,n.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext},n.sibling=e.sibling,n.index=e.index,n.ref=e.ref,n}function Aa(e,t,n,a,r,s){var i=2;if(a=e,typeof e=="function")Ti(e)&&(i=1);else if(typeof e=="string")i=5;else e:switch(e){case Lt:return Ct(n.children,r,s,t);case _s:i=8,r|=8;break;case Qr:return e=xe(12,n,t,r|2),e.elementType=Qr,e.lanes=s,e;case Xr:return e=xe(13,n,t,r),e.elementType=Xr,e.lanes=s,e;case Yr:return e=xe(19,n,t,r),e.elementType=Yr,e.lanes=s,e;case ml:return pr(n,r,s,t);default:if(typeof e=="object"&&e!==null)switch(e.$$typeof){case dl:i=10;break e;case ul:i=9;break e;case Vs:i=11;break e;case Qs:i=14;break e;case Ye:i=16,a=null;break e}throw Error(b(130,e==null?e:typeof e,""))}return t=xe(i,n,t,r),t.elementType=e,t.type=a,t.lanes=s,t}function Ct(e,t,n,a){return e=xe(7,e,a,t),e.lanes=n,e}function pr(e,t,n,a){return e=xe(22,e,a,t),e.elementType=ml,e.lanes=n,e.stateNode={isHidden:!1},e}function Jr(e,t,n){return e=xe(6,e,null,t),e.lanes=n,e}function Gr(e,t,n){return t=xe(4,e.children!==null?e.children:[],e.key,t),t.lanes=n,t.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},t}function qm(e,t,n,a,r){this.tag=t,this.containerInfo=e,this.finishedWork=this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.pendingContext=this.context=null,this.callbackPriority=0,this.eventTimes=Er(0),this.expirationTimes=Er(-1),this.entangledLanes=this.finishedLanes=this.mutableReadLanes=this.expiredLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Er(0),this.identifierPrefix=a,this.onRecoverableError=r,this.mutableSourceEagerHydrationData=null}function qi(e,t,n,a,r,s,i,o,l){return e=new qm(e,t,n,o,l),t===1?(t=1,s===!0&&(t|=8)):t=0,s=xe(3,null,null,t),e.current=s,s.stateNode=e,s.memoizedState={element:a,isDehydrated:n,cache:null,transitions:null,pendingSuspenseBoundaries:null},pi(s),e}function Am(e,t,n){var a=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:Ot,key:a==null?null:""+a,children:e,containerInfo:t,implementation:n}}function ad(e){if(!e)return mt;e=e._reactInternals;e:{if(At(e)!==e||e.tag!==1)throw Error(b(170));var t=e;do{switch(t.tag){case 3:t=t.stateNode.context;break e;case 1:if(pe(t.type)){t=t.stateNode.__reactInternalMemoizedMergedChildContext;break e}}t=t.return}while(t!==null);throw Error(b(171))}if(e.tag===1){var n=e.type;if(pe(n))return ac(e,n,t)}return t}function rd(e,t,n,a,r,s,i,o,l){return e=qi(n,a,!0,e,r,s,i,o,l),e.context=ad(null),n=e.current,a=oe(),r=ct(n),s=ze(a,r),s.callback=t??null,ot(n,s,r),e.current.lanes=r,Yn(e,r,a),he(e,a),e}function hr(e,t,n,a){var r=t.current,s=oe(),i=ct(r);return n=ad(n),t.context===null?t.context=n:t.pendingContext=n,t=ze(s,i),t.payload={element:e},a=a===void 0?null:a,a!==null&&(t.callback=a),e=ot(r,t,i),e!==null&&(Pe(e,r,i,s),ja(e,r,i)),i}function tr(e){if(e=e.current,!e.child)return null;switch(e.child.tag){case 5:return e.child.stateNode;default:return e.child.stateNode}}function Fo(e,t){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var n=e.retryLane;e.retryLane=n!==0&&n<t?n:t}}function Ai(e,t){Fo(e,t),(e=e.alternate)&&Fo(e,t)}function Im(){return null}var sd=typeof reportError=="function"?reportError:function(e){console.error(e)};function Ii(e){this._internalRoot=e}yr.prototype.render=Ii.prototype.render=function(e){var t=this._internalRoot;if(t===null)throw Error(b(409));hr(e,t,null,null)};yr.prototype.unmount=Ii.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var t=e.containerInfo;Tt(function(){hr(null,e,null,null)}),t[Ge]=null}};function yr(e){this._internalRoot=e}yr.prototype.unstable_scheduleHydration=function(e){if(e){var t=Ml();e={blockedOn:null,target:e,priority:t};for(var n=0;n<Ze.length&&t!==0&&t<Ze[n].priority;n++);Ze.splice(n,0,e),n===0&&Hl(e)}};function Pi(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function fr(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11&&(e.nodeType!==8||e.nodeValue!==" react-mount-point-unstable "))}function Uo(){}function Pm(e,t,n,a,r){if(r){if(typeof a=="function"){var s=a;a=function(){var u=tr(i);s.call(u)}}var i=rd(t,a,e,0,null,!1,!1,"",Uo);return e._reactRootContainer=i,e[Ge]=i.current,Dn(e.nodeType===8?e.parentNode:e),Tt(),i}for(;r=e.lastChild;)e.removeChild(r);if(typeof a=="function"){var o=a;a=function(){var u=tr(l);o.call(u)}}var l=qi(e,0,!1,null,null,!1,!1,"",Uo);return e._reactRootContainer=l,e[Ge]=l.current,Dn(e.nodeType===8?e.parentNode:e),Tt(function(){hr(t,l,n,a)}),l}function gr(e,t,n,a,r){var s=n._reactRootContainer;if(s){var i=s;if(typeof r=="function"){var o=r;r=function(){var l=tr(i);o.call(l)}}hr(t,i,e,r)}else i=Pm(n,t,e,r,a);return tr(i)}Ol=function(e){switch(e.tag){case 3:var t=e.stateNode;if(t.current.memoizedState.isDehydrated){var n=kn(t.pendingLanes);n!==0&&($s(t,n|1),he(t,J()),!(P&6)&&(rn=J()+500,yt()))}break;case 13:Tt(function(){var a=_e(e,1);if(a!==null){var r=oe();Pe(a,e,1,r)}}),Ai(e,1)}};Zs=function(e){if(e.tag===13){var t=_e(e,134217728);if(t!==null){var n=oe();Pe(t,e,134217728,n)}Ai(e,134217728)}};Ll=function(e){if(e.tag===13){var t=ct(e),n=_e(e,t);if(n!==null){var a=oe();Pe(n,e,t,a)}Ai(e,t)}};Ml=function(){return O};Wl=function(e,t){var n=O;try{return O=e,t()}finally{O=n}};os=function(e,t,n){switch(t){case"input":if(es(e,n),t=n.name,n.type==="radio"&&t!=null){for(n=e;n.parentNode;)n=n.parentNode;for(n=n.querySelectorAll("input[name="+JSON.stringify(""+t)+'][type="radio"]'),t=0;t<n.length;t++){var a=n[t];if(a!==e&&a.form===e.form){var r=or(a);if(!r)throw Error(b(90));hl(a),es(a,r)}}}break;case"textarea":fl(e,n);break;case"select":t=n.value,t!=null&&Gt(e,!!n.multiple,t,!1)}};xl=Ei;Cl=Tt;var Om={usingClientEntryPoint:!1,Events:[Zn,Kt,or,Sl,kl,Ei]},bn={findFiberByHostInstance:wt,bundleType:0,version:"18.3.1",rendererPackageName:"react-dom"},Lm={bundleType:bn.bundleType,version:bn.version,rendererPackageName:bn.rendererPackageName,rendererConfig:bn.rendererConfig,overrideHookState:null,overrideHookStateDeletePath:null,overrideHookStateRenamePath:null,overrideProps:null,overridePropsDeletePath:null,overridePropsRenamePath:null,setErrorHandler:null,setSuspenseHandler:null,scheduleUpdate:null,currentDispatcherRef:Qe.ReactCurrentDispatcher,findHostInstanceByFiber:function(e){return e=Rl(e),e===null?null:e.stateNode},findFiberByHostInstance:bn.findFiberByHostInstance||Im,findHostInstancesForRefresh:null,scheduleRefresh:null,scheduleRoot:null,setRefreshHandler:null,getCurrentFiber:null,reconcilerVersion:"18.3.1-next-f1338f8080-20240426"};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<"u"){var va=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!va.isDisabled&&va.supportsFiber)try{ar=va.inject(Lm),He=va}catch{}}ve.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Om;ve.createPortal=function(e,t){var n=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!Pi(t))throw Error(b(200));return Am(e,t,null,n)};ve.createRoot=function(e,t){if(!Pi(e))throw Error(b(299));var n=!1,a="",r=sd;return t!=null&&(t.unstable_strictMode===!0&&(n=!0),t.identifierPrefix!==void 0&&(a=t.identifierPrefix),t.onRecoverableError!==void 0&&(r=t.onRecoverableError)),t=qi(e,1,!1,null,null,n,!1,a,r),e[Ge]=t.current,Dn(e.nodeType===8?e.parentNode:e),new Ii(t)};ve.findDOMNode=function(e){if(e==null)return null;if(e.nodeType===1)return e;var t=e._reactInternals;if(t===void 0)throw typeof e.render=="function"?Error(b(188)):(e=Object.keys(e).join(","),Error(b(268,e)));return e=Rl(t),e=e===null?null:e.stateNode,e};ve.flushSync=function(e){return Tt(e)};ve.hydrate=function(e,t,n){if(!fr(t))throw Error(b(200));return gr(null,e,t,!0,n)};ve.hydrateRoot=function(e,t,n){if(!Pi(e))throw Error(b(405));var a=n!=null&&n.hydratedSources||null,r=!1,s="",i=sd;if(n!=null&&(n.unstable_strictMode===!0&&(r=!0),n.identifierPrefix!==void 0&&(s=n.identifierPrefix),n.onRecoverableError!==void 0&&(i=n.onRecoverableError)),t=rd(t,null,e,1,n??null,r,!1,s,i),e[Ge]=t.current,Dn(e),a)for(e=0;e<a.length;e++)n=a[e],r=n._getVersion,r=r(n._source),t.mutableSourceEagerHydrationData==null?t.mutableSourceEagerHydrationData=[n,r]:t.mutableSourceEagerHydrationData.push(n,r);return new yr(t)};ve.render=function(e,t,n){if(!fr(t))throw Error(b(200));return gr(null,e,t,!1,n)};ve.unmountComponentAtNode=function(e){if(!fr(e))throw Error(b(40));return e._reactRootContainer?(Tt(function(){gr(null,null,e,!1,function(){e._reactRootContainer=null,e[Ge]=null})}),!0):!1};ve.unstable_batchedUpdates=Ei;ve.unstable_renderSubtreeIntoContainer=function(e,t,n,a){if(!fr(n))throw Error(b(200));if(e==null||e._reactInternals===void 0)throw Error(b(38));return gr(e,t,n,!1,a)};ve.version="18.3.1-next-f1338f8080-20240426";function id(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>"u"||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!="function"))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(id)}catch(e){console.error(e)}}id(),il.exports=ve;var Mm=il.exports,zo=Mm;_r.createRoot=zo.createRoot,_r.hydrateRoot=zo.hydrateRoot;const Pt=[{id:"java-1",number:1,title:"What are the four fundamental OOP principles?",question:"What are the four fundamental OOP principles?",difficulty:"Basic",category:"Core Java & OOP",answer:`The four fundamental Object-Oriented Programming principles are:
1. **Encapsulation** - Bundling data and methods within a class and restricting direct access via access modifiers.
2. **Abstraction** - Hiding complex implementation details and showing only essential features.
3. **Inheritance** - A subclass acquires properties/behaviors from a superclass.
4. **Polymorphism** - One interface, many implementations (overloading at compile-time, overriding at runtime).


\`\`\`java
public class BankAccount {
    private double balance; // encapsulated
    public void deposit(double amount) { if (amount > 0) balance += amount; }
    public double getBalance() { return balance; }
}

abstract class Animal { abstract void sound(); }
class Dog extends Animal {
    @Override void sound() { System.out.println("Dog barks"); } // polymorphism
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:1,sortKey:1},{id:"java-2",number:2,title:"Abstract class vs interface in Java?",question:"Abstract class vs interface in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`| Feature | Abstract Class | Interface |
|---------|---------------|-----------|
| Inheritance | Single class only | Multiple interfaces |
| Constructors | Allowed | Not allowed |
| Fields | Instance variables | \`public static final\` only |
| Methods | Abstract + concrete | Abstract (pre-Java8), default/static (Java8+) |


\`\`\`java
abstract class Vehicle { abstract void drive(); void stop() {} }
interface Flyable { void fly(); default void land() {} }
class Drone extends Vehicle implements Flyable {
    void drive() {}
    public void fly() {}
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:2,sortKey:2},{id:"java-3",number:3,title:"What is the String pool in Java?",question:"What is the String pool in Java?",difficulty:"Basic",category:"Core Java & OOP",answer:'The String pool is a special heap memory region storing unique string literals. When you create a literal, the JVM reuses an existing identical string if present, saving memory. `new String()` always creates a new object; `intern()` adds it to the pool.\n\n\n```java\nString a = "hello";\nString b = "hello";\nString c = new String("hello");\nSystem.out.println(a == b); // true (same pool object)\nSystem.out.println(a == c); // false (different objects)\n```\n```\n',tech:"java",displayNumber:3,sortKey:3},{id:"java-4",number:4,title:"Explain method overloading vs overriding.",question:"Explain method overloading vs overriding.",difficulty:"Basic",category:"Core Java & OOP",answer:`**Overloading** is compile-time polymorphism: multiple methods with same name but different parameter lists within the same class. **Overriding** is runtime polymorphism: a subclass redefines a superclass method with identical signature.


\`\`\`java
class Calc {
    int add(int a, int b) { return a + b; }       // overload
    int add(int a, int b, int c) { return a + b + c; } // overload
}
class Animal { void sound() { System.out.println("..."); } }
class Cat extends Animal { @Override void sound() { System.out.println("Meow"); } } // override
\`\`\`
\`\`\`
`,tech:"java",displayNumber:4,sortKey:4},{id:"java-5",number:5,title:"What is the difference between `==` and `.equals()`?",question:"What is the difference between `==` and `.equals()`?",difficulty:"Basic",category:"Core Java & OOP",answer:'`==` compares object references (memory addresses). `.equals()` compares logical equality (content), which can be overridden. For Strings, always use `.equals()` to compare values.\n\n\n```java\nString s1 = new String("x");\nString s2 = new String("x");\nSystem.out.println(s1 == s2);      // false\nSystem.out.println(s1.equals(s2)); // true\n```\n```\n',tech:"java",displayNumber:5,sortKey:5},{id:"java-6",number:6,title:"How does Java achieve platform independence?",question:"How does Java achieve platform independence?",difficulty:"Basic",category:"Core Java & OOP",answer:'Java source code compiles to bytecode (.class), which runs on the JVM. The JVM is platform-specific but interprets the same bytecode everywhere, so "write once, run anywhere."\n\n\n```java\n.java (source) -> javac -> .class (bytecode) -> JVM -> native machine code\n```\n```\n',tech:"java",displayNumber:6,sortKey:6},{id:"java-7",number:7,title:"What are access modifiers and their scopes?",question:"What are access modifiers and their scopes?",difficulty:"Basic",category:"Core Java & OOP",answer:"- `private`: accessible only within the same class\n- `default` (no modifier): accessible within the same package\n- `protected`: same package + subclasses\n- `public`: accessible everywhere\n\n\n```java\npublic class A {\n    private int x;\n    protected int y;\n    public int z;\n    int w; // default\n}\n```\n```\n",tech:"java",displayNumber:7,sortKey:7},{id:"java-8",number:8,title:"Explain constructor chaining.",question:"Explain constructor chaining.",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Constructor chaining is calling one constructor from another using `this()` (same class) or `super()` (parent class). It must be the first statement in the constructor. It promotes code reuse for initialization.\n\n\n```java\nclass Person {\n    String name;\n    Person() { this("Unknown"); }   // chaining\n    Person(String name) { this.name = name; }\n}\n```\n```\n',tech:"java",displayNumber:8,sortKey:8},{id:"java-9",number:9,title:"What is the `final` keyword used for?",question:"What is the `final` keyword used for?",difficulty:"Basic",category:"Core Java & OOP",answer:'`final` means "cannot change":\n- `final` variable: constant, must be initialized once\n- `final` method: cannot be overridden\n- `final` class: cannot be subclassed (e.g., `String`)\n\n\n```java\nfinal double PI = 3.14159;\nfinal class Immutable {}\n// class Sub extends Immutable {} // compile error\n```\n```\n',tech:"java",displayNumber:9,sortKey:9},{id:"java-10",number:10,title:"What is `this` and `super`?",question:"What is `this` and `super`?",difficulty:"Basic",category:"Core Java & OOP",answer:`\`this\` refers to the current instance; \`super\` refers to the parent class instance. Both can access fields, methods, and constructors.


\`\`\`java
class Parent { int val = 10; }
class Child extends Parent {
    int val = 20;
    void show() {
        System.out.println(val);      // 20
        System.out.println(this.val);  // 20
        System.out.println(super.val); // 10
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:10,sortKey:10},{id:"java-11",number:11,title:"What is immutability and how to create an immutable class?",question:"What is immutability and how to create an immutable class?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"An immutable object cannot be modified after creation. Steps: declare class `final`, make fields `private final`, no setters, return defensive copies of mutable fields, initialize via constructor.\n\n\n```java\npublic final class ImmutablePoint {\n    private final int x, y;\n    public ImmutablePoint(int x, int y) { this.x = x; this.y = y; }\n    public int getX() { return x; }\n    public int getY() { return y; }\n}\n```\n```\n",tech:"java",displayNumber:11,sortKey:11},{id:"java-12",number:12,title:"Difference between checked and unchecked exceptions?",question:"Difference between checked and unchecked exceptions?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'**Checked exceptions** (extend `Exception`) are verified at compile-time and must be handled or declared (`IOException`). **Unchecked exceptions** (extend `RuntimeException`) occur at runtime (`NullPointerException`, `ArrayIndexOutOfBoundsException`) and need not be declared.\n\n\n```java\nvoid read() throws IOException { // checked - must declare\n    Files.readAllBytes(Path.of("x"));\n}\nvoid div(int a, int b) {\n    int r = a / b; // ArithmeticException - unchecked, no need to declare\n}\n```\n```\n',tech:"java",displayNumber:12,sortKey:12},{id:"java-13",number:13,title:"What is the `try-with-resources` statement?",question:"What is the `try-with-resources` statement?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Introduced in Java 7, `try-with-resources` automatically closes resources (those implementing `AutoCloseable`) at the end of the block, eliminating boilerplate `finally` blocks and preventing resource leaks.\n\n\n```java\ntry (BufferedReader br = new BufferedReader(new FileReader("file.txt"))) {\n    System.out.println(br.readLine());\n} catch (IOException e) {\n    e.printStackTrace();\n} // br auto-closed here\n```\n```\n',tech:"java",displayNumber:13,sortKey:13},{id:"java-14",number:14,title:"Explain the `Exception` hierarchy in Java.",question:"Explain the `Exception` hierarchy in Java.",difficulty:"Basic",category:"Core Java & OOP",answer:"`Throwable` is the root. It has two subclasses: `Error` (serious JVM problems like `OutOfMemoryError`, typically unrecoverable) and `Exception` (recoverable conditions). `Exception` splits into checked and unchecked (`RuntimeException`).\n\n\n```java\nThrowable\n├── Error (OutOfMemoryError)\n└── Exception\n    ├── IOException (checked)\n    └── RuntimeException (unchecked)\n```\n```\n",tech:"java",displayNumber:14,sortKey:14},{id:"java-15",number:15,title:"What is method hiding vs method overriding?",question:"What is method hiding vs method overriding?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Static methods are not overridden — they are **hidden**. If a subclass defines a static method with the same signature, the parent's is hidden, and resolution is based on the reference type (compile-time), not the object type.


\`\`\`java
class A { static void m() { System.out.println("A"); } }
class B extends A { static void m() { System.out.println("B"); } }
A a = new B();
a.m(); // prints "A" (static binding on reference type A)
\`\`\`
\`\`\`
`,tech:"java",displayNumber:15,sortKey:15},{id:"java-16",number:16,title:"What is a singleton class and how to implement it?",question:"What is a singleton class and how to implement it?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`A singleton ensures only one instance exists. Common approaches: eager initialization, lazy initialization with double-checked locking, or the enum approach (Joshua Bloch's preferred, serialization-safe).


\`\`\`java
public enum Singleton { INSTANCE; }
// or
public class Singleton {
    private static volatile Singleton instance;
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) instance = new Singleton();
            }
        }
        return instance;
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:16,sortKey:16},{id:"java-17",number:17,title:"What is the difference between `StringBuilder` and `StringBuffer`?",question:"What is the difference between `StringBuilder` and `StringBuffer`?",difficulty:"Basic",category:"Core Java & OOP",answer:'Both build mutable strings. `StringBuilder` is not thread-safe (faster, Java 5+) and `StringBuffer` is thread-safe (synchronized methods, legacy). For single-threaded use, prefer `StringBuilder`.\n\n\n```java\nStringBuilder sb = new StringBuilder();\nsb.append("Hello").append(" ").append("World");\nString result = sb.toString();\n```\n```\n',tech:"java",displayNumber:17,sortKey:17},{id:"java-18",number:18,title:"How does autoboxing and unboxing work?",question:"How does autoboxing and unboxing work?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Autoboxing automatically converts primitives to wrapper objects (e.g., `int` → `Integer`). Unboxing does the reverse. Added in Java 5. Be careful with `null` unboxing (throws `NullPointerException`).\n\n\n```java\nInteger i = 10;        // autoboxing (int -> Integer)\nint j = i;             // unboxing (Integer -> int)\nList<Integer> list = new ArrayList<>();\nlist.add(5);           // autoboxing\n```\n```\n",tech:"java",displayNumber:18,sortKey:18},{id:"java-19",number:19,title:"What is the difference between `HashMap` and `Hashtable`?",question:"What is the difference between `HashMap` and `Hashtable`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`HashMap` is non-synchronized, allows one null key and multiple null values, and is faster. `Hashtable` is thread-safe (synchronized), does not allow null keys/values, and is legacy.\n\n\n```java\nMap<String, Integer> map = new HashMap<>();\nmap.put(null, 1); // OK\n// Hashtable: map.put(null, 1); // throws NullPointerException\n```\n```\n",tech:"java",displayNumber:19,sortKey:19},{id:"java-20",number:20,title:"What is a marker interface?",question:"What is a marker interface?",difficulty:"Basic",category:"Core Java & OOP",answer:"A marker interface has no methods or fields; it marks a class for special treatment by the JVM or frameworks. Examples: `Serializable`, `Cloneable`, `Remote`.\n\n\n```java\npublic class Person implements Serializable {\n    private String name;\n} // marks Person as serializable\n```\n```\n",tech:"java",displayNumber:20,sortKey:20},{id:"java-21",number:21,title:"Explain the `Object` class methods.",question:"Explain the `Object` class methods.",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Every Java class inherits from `Object`. Key methods: `equals()`, `hashCode()`, `toString()`, `clone()`, `finalize()` (deprecated), `getClass()`, `notify()`, `wait()`, `notifyAll()`.\n\n\n```java\n@Override\npublic boolean equals(Object o) {\n    if (this == o) return true;\n    if (!(o instanceof Person p)) return false;\n    return name.equals(p.name);\n}\n@Override\npublic int hashCode() { return Objects.hash(name); }\n```\n```\n",tech:"java",displayNumber:21,sortKey:21},{id:"java-22",number:22,title:"What is the difference between `equals()` and `hashCode()` contract?",question:"What is the difference between `equals()` and `hashCode()` contract?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"If two objects are equal via `equals()`, they must have the same `hashCode()`. The reverse is not required. Violating this breaks hash-based collections like `HashMap` and `HashSet`.\n\n\n```java\n// Always override both together\n@Override public boolean equals(Object o) { /* ... */ }\n@Override public int hashCode() { return Objects.hash(id); }\n```\n```\n",tech:"java",displayNumber:22,sortKey:22},{id:"java-23",number:23,title:"What is method reference in Java?",question:"What is method reference in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Method references (`::`) are shorthand for lambda expressions that call a single method. Forms: static (`Class::method`), instance (`obj::method`), constructor (`Class::new`), arbitrary instance (`Class::method`).\n\n\n```java\nList<String> names = Arrays.asList("a", "b");\nnames.forEach(System.out::println); // method reference\n```\n```\n',tech:"java",displayNumber:23,sortKey:23},{id:"java-24",number:24,title:"What is the `instanceof` operator?",question:"What is the `instanceof` operator?",difficulty:"Basic",category:"Core Java & OOP",answer:'`instanceof` checks whether an object is an instance of a specific class or interface (including subclasses). Returns boolean. Useful before casting to avoid `ClassCastException`.\n\n\n```java\nObject o = "text";\nif (o instanceof String s) {\n    System.out.println(s.length()); // pattern matching (Java 16+)\n}\n```\n```\n',tech:"java",displayNumber:24,sortKey:24},{id:"java-25",number:25,title:"Explain the difference between `throw` and `throws`.",question:"Explain the difference between `throw` and `throws`.",difficulty:"Basic",category:"Core Java & OOP",answer:'`throw` is used to explicitly throw an exception instance. `throws` is used in a method signature to declare that the method may throw checked exceptions.\n\n\n```java\nvoid validate(int age) throws IllegalArgumentException {\n    if (age < 0) throw new IllegalArgumentException("negative age");\n}\n```\n```\n',tech:"java",displayNumber:25,sortKey:25},{id:"java-26",number:26,title:"What is the difference between `final`, `finally`, and `finalize`?",question:"What is the difference between `final`, `finally`, and `finalize`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'- `final`: restricts modification (variable/method/class)\n- `finally`: block that always executes after try/catch (cleanup)\n- `finalize()`: deprecated Object method called by GC before reclaiming memory\n\n\n```java\ntry { /* code */ }\nfinally { System.out.println("always runs"); }\n```\n```\n',tech:"java",displayNumber:26,sortKey:26},{id:"java-27",number:27,title:"What is a static block and when is it used?",question:"What is a static block and when is it used?",difficulty:"Basic",category:"Core Java & OOP",answer:`A static block (\`static {}\`) runs once when the class is loaded by the JVM. Used for static initialization that cannot fit in a single expression.


\`\`\`java
class Config {
    static Properties props;
    static {
        props = new Properties();
        props.setProperty("env", "prod");
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:27,sortKey:27},{id:"java-28",number:28,title:"Can a constructor be `private`? Why?",question:"Can a constructor be `private`? Why?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Yes. A private constructor prevents external instantiation. Used in singletons, factory methods, and utility classes (where all methods are static).


\`\`\`java
public class Utility {
    private Utility() {} // no instantiation
    public static int add(int a, int b) { return a + b; }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:28,sortKey:28},{id:"java-29",number:29,title:"What is the difference between `int` and `Integer`?",question:"What is the difference between `int` and `Integer`?",difficulty:"Basic",category:"Core Java & OOP",answer:'`int` is a primitive (stores value directly, default 0, no methods). `Integer` is a wrapper class (object, default null, has methods like `parseInt`). Autoboxing bridges them.\n\n\n```java\nint a = 5;\nInteger b = Integer.valueOf(5);\nint c = Integer.parseInt("10");\n```\n```\n',tech:"java",displayNumber:29,sortKey:29},{id:"java-30",number:30,title:"What is a `record` in Java (Java 16+)?",question:"What is a `record` in Java (Java 16+)?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'A `record` is a concise immutable data carrier. The compiler auto-generates private final fields, a canonical constructor, `equals()`, `hashCode()`, and `toString()`. Great for DTOs.\n\n\n```java\npublic record User(String name, int age) {}\nUser u = new User("Alice", 30);\nSystem.out.println(u.name()); // accessor, not getter\n```\n```\n',tech:"java",displayNumber:30,sortKey:30},{id:"java-31",number:31,title:"What is shadowing in Java?",question:"What is shadowing in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Shadowing occurs when a local variable or parameter has the same name as a field, hiding it within that scope. Use `this.` to access the shadowed field.\n\n\n```java\nclass Example {\n    int x = 10;\n    void setX(int x) { this.x = x; } // parameter shadows field\n}\n```\n```\n",tech:"java",displayNumber:31,sortKey:31},{id:"java-32",number:32,title:"Explain covariance and contravariance in Java.",question:"Explain covariance and contravariance in Java.",difficulty:"Advanced",category:"Core Java & OOP",answer:'Covariance: a method can return a subtype of the declared return type (e.g., `Object` → `String`). Contravariance: a parameter can accept a supertype. Arrays are covariant; generics are invariant.\n\n\n```java\nclass A { Object get() { return null; } }\nclass B extends A { @Override String get() { return ""; } } // covariant return\n```\n```\n',tech:"java",displayNumber:32,sortKey:32},{id:"java-33",number:33,title:"What is the difference between deep copy and shallow copy?",question:"What is the difference between deep copy and shallow copy?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`**Shallow copy** duplicates the object but shared referenced objects (both point to same nested objects). **Deep copy** recursively duplicates all nested objects, creating fully independent copies.


\`\`\`java
// Shallow: clone() default
// Deep: manually copy nested objects or use serialization
class Address implements Cloneable {
    String city;
    protected Object clone() throws CloneNotSupportedException {
        return super.clone();
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:33,sortKey:33},{id:"java-34",number:34,title:"What is the `transient` keyword?",question:"What is the `transient` keyword?",difficulty:"Basic",category:"Core Java & OOP",answer:"`transient` marks a field to be skipped during serialization. Useful for sensitive data (passwords) or derived/computed fields.\n\n\n```java\nclass User implements Serializable {\n    String name;\n    transient String password; // not serialized\n}\n```\n```\n",tech:"java",displayNumber:34,sortKey:34},{id:"java-35",number:35,title:"What is the difference between `Comparable` and `Comparator`?",question:"What is the difference between `Comparable` and `Comparator`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`Comparable` defines natural ordering of a class (one method: `compareTo`). `Comparator` is external sorting logic (separate class, multiple orderings possible).\n\n\n```java\nclass Person implements Comparable<Person> {\n    int age;\n    public int compareTo(Person o) { return Integer.compare(age, o.age); }\n}\nComparator<Person> byName = (p1, p2) -> p1.name.compareTo(p2.name);\n```\n```\n",tech:"java",displayNumber:35,sortKey:35},{id:"java-36",number:36,title:"What is the purpose of the `volatile` keyword?",question:"What is the purpose of the `volatile` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`volatile` ensures a variable is read/written directly from main memory (not CPU cache), guaranteeing visibility across threads. It does NOT provide atomicity (use `Atomic` classes for that).\n\n\n```java\nclass Flag {\n    volatile boolean running = true;\n    void stop() { running = false; }\n}\n```\n```\n",tech:"java",displayNumber:36,sortKey:36},{id:"java-37",number:37,title:"What is the difference between `Stack` and `Heap` memory?",question:"What is the difference between `Stack` and `Heap` memory?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`**Stack** stores local variables and method call frames; thread-private, fast, automatically cleaned on method exit. **Heap** stores objects; shared across threads, managed by GC.


\`\`\`java
void method() {
    int x = 5;        // stack
    Object o = new Object(); // reference on stack, object on heap
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:37,sortKey:37},{id:"java-38",number:38,title:"What are enums and when to use them?",question:"What are enums and when to use them?",difficulty:"Basic",category:"Core Java & OOP",answer:"Enums define a fixed set of constants with type safety. They can have fields, methods, and implement interfaces. Prefer enums over `int` constants for readability and safety.\n\n\n```java\nenum Day { MONDAY, TUESDAY, WEDNESDAY }\nDay today = Day.MONDAY;\nswitch (today) { case MONDAY: break; }\n```\n```\n",tech:"java",displayNumber:38,sortKey:38},{id:"java-39",number:39,title:"What is reflection in Java?",question:"What is reflection in Java?",difficulty:"Advanced",category:"Core Java & OOP",answer:'Reflection allows inspecting/modifying runtime behavior of classes (fields, methods, constructors) at runtime. Used by frameworks (Spring, JUnit). It has performance overhead and breaks encapsulation.\n\n\n```java\nClass<?> clazz = Class.forName("java.lang.String");\nMethod[] methods = clazz.getDeclaredMethods();\n```\n```\n',tech:"java",displayNumber:39,sortKey:39},{id:"java-40",number:40,title:"What is the difference between `public static void main(String[] args)` components?",question:"What is the difference between `public static void main(String[] args)` components?",difficulty:"Basic",category:"Core Java & OOP",answer:"`public`: accessible from JVM. `static`: no instance needed. `void`: no return value. `main`: entry point name. `String[] args`: command-line arguments.\n\n\n```java\npublic static void main(String[] args) {\n    for (String arg : args) System.out.println(arg);\n}\n```\n```\n",tech:"java",displayNumber:40,sortKey:40},{id:"java-41",number:41,title:"What is a nested class vs inner class?",question:"What is a nested class vs inner class?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`A **static nested class** doesn't need an outer instance. A **non-static inner class** has an implicit reference to the outer instance, can access its private members.


\`\`\`java
class Outer {
    static class Nested {}    // no outer ref
    class Inner {}            // has outer ref
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:41,sortKey:41},{id:"java-42",number:42,title:"What is the difference between `null` and empty string?",question:"What is the difference between `null` and empty string?",difficulty:"Basic",category:"Core Java & OOP",answer:'`null` means no reference (points to nothing; calling methods on it throws NPE). Empty string `""` is a valid String object with zero length.\n\n\n```java\nString a = null;    // no object\nString b = "";      // valid object, length 0\n// a.length(); // NullPointerException\n```\n```\n',tech:"java",displayNumber:42,sortKey:42},{id:"java-43",number:43,title:"What is the diamond problem and how does Java avoid it?",question:"What is the diamond problem and how does Java avoid it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"The diamond problem occurs with multiple inheritance of classes (ambiguity over which parent method to call). Java avoids it by allowing only single class inheritance but permitting multiple interface implementation. With default methods, conflicts are resolved explicitly via `Interface.super.method()`.\n\n\n```java\ninterface A { default void foo() {} }\ninterface B { default void foo() {} }\nclass C implements A, B {\n    public void foo() { A.super.foo(); } // must resolve explicitly\n}\n```\n```\n",tech:"java",displayNumber:43,sortKey:43},{id:"java-44",number:44,title:"What is a lambda expression?",question:"What is a lambda expression?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"A lambda is a concise way to express an anonymous function (a block of code passed as an argument). It implements a functional interface. Syntax: `(params) -> expression`.\n\n\n```java\nList<Integer> nums = Arrays.asList(1, 2, 3);\nnums.forEach(n -> System.out.println(n * 2));\n```\n```\n",tech:"java",displayNumber:44,sortKey:44},{id:"java-45",number:45,title:"What is a functional interface?",question:"What is a functional interface?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'An interface with exactly one abstract method (SAM). Can have default/static methods. `@FunctionalInterface` annotation enforces this. Examples: `Runnable`, `Callable`, `Comparator`, `Function`.\n\n\n```java\n@FunctionalInterface\ninterface Greeting { void say(String name); }\nGreeting g = name -> System.out.println("Hi " + name);\n```\n```\n',tech:"java",displayNumber:45,sortKey:45},{id:"java-46",number:46,title:"What is the difference between `sleep()` and `wait()`?",question:"What is the difference between `sleep()` and `wait()`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`sleep()` (Thread class) pauses the current thread for a specified time without releasing the lock. `wait()` (Object class) releases the lock and waits for `notify()`/`notifyAll()`.\n\n\n```java\nThread.sleep(1000); // keeps lock\nsynchronized (obj) { obj.wait(); } // releases lock\n```\n```\n",tech:"java",displayNumber:46,sortKey:46},{id:"java-47",number:47,title:"What is the difference between `ArrayList` and `LinkedList`?",question:"What is the difference between `ArrayList` and `LinkedList`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`ArrayList` uses a dynamic array (fast random access O(1), slow insertion in middle O(n)). `LinkedList` uses nodes (fast insert/delete O(1), slow access O(n)).\n\n\n```java\nList<String> arr = new ArrayList<>();  // random access heavy\nList<String> link = new LinkedList<>(); // frequent insert/delete\n```\n```\n",tech:"java",displayNumber:47,sortKey:47},{id:"java-48",number:48,title:"What is the `super` keyword used for in constructors?",question:"What is the `super` keyword used for in constructors?",difficulty:"Basic",category:"Core Java & OOP",answer:'`super()` calls the parent class constructor. It must be the first statement in a subclass constructor. If omitted, the compiler inserts an implicit `super()` (no-arg) call.\n\n\n```java\nclass Animal { Animal(String n) {} }\nclass Dog extends Animal { Dog() { super("Dog"); } }\n```\n```\n',tech:"java",displayNumber:48,sortKey:48},{id:"java-49",number:49,title:"What is the difference between compile-time and runtime polymorphism?",question:"What is the difference between compile-time and runtime polymorphism?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"**Compile-time** (static) polymorphism: method overloading resolved at compile time. **Runtime** (dynamic) polymorphism: method overriding resolved at runtime based on the actual object type via vtable dispatch.\n\n\n```java\n// Overloading - compile time\n// Overriding - runtime (JVM picks actual object's method)\n```\n```\n",tech:"java",displayNumber:49,sortKey:49},{id:"java-50",number:50,title:"What is the purpose of `@Override` annotation?",question:"What is the purpose of `@Override` annotation?",difficulty:"Basic",category:"Core Java & OOP",answer:'`@Override` tells the compiler you intend to override a superclass/interface method. If the signature doesn\'t match, the compiler errors — catching typos at compile time.\n\n\n```java\n@Override\npublic String toString() { return "MyClass"; }\n```\n```\n',tech:"java",displayNumber:50,sortKey:50},{id:"java-51",number:51,title:"What is a deadlock and how to avoid it?",question:"What is a deadlock and how to avoid it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"A deadlock is when two or more threads block forever, each waiting for a lock held by another. Avoid by: locking in consistent order, using `tryLock` with timeout, minimizing lock scope, and using higher-level concurrency utilities.\n\n\n```java\n// Deadlock: Thread1 locks A then B, Thread2 locks B then A\n// Solution: always acquire locks in same order (A then B)\n```\n```\n",tech:"java",displayNumber:51,sortKey:51},{id:"java-52",number:52,title:"What is the difference between `Error` and `Exception`?",question:"What is the difference between `Error` and `Exception`?",difficulty:"Basic",category:"Core Java & OOP",answer:"Both extend `Throwable`. `Error` represents serious JVM failures (`OutOfMemoryError`, `StackOverflowError`) — typically unrecoverable. `Exception` represents conditions a program might handle.\n\n\n```java\ntry {\n    recursiveCall(); // StackOverflowError - can't really handle\n} catch (Exception e) { }\n```\n```\n",tech:"java",displayNumber:52,sortKey:52},{id:"java-53",number:53,title:"What is the `assert` keyword?",question:"What is the `assert` keyword?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`assert` validates assumptions during development. `assert condition;` throws `AssertionError` if false. Disabled by default; enable with `-ea` JVM flag. Not for production error handling.\n\n\n```java\nassert x > 0 : "x must be positive"; // custom message\n```\n```\n',tech:"java",displayNumber:53,sortKey:53},{id:"java-54",number:54,title:"What is the difference between `var` (Java 10+) and explicit type?",question:"What is the difference between `var` (Java 10+) and explicit type?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`var` uses local type inference — the compiler determines the type from the initializer. Type is still static (not dynamic). Only allowed for local variables with initializers.\n\n\n```java\nvar list = new ArrayList<String>(); // inferred as ArrayList<String>\nvar name = "Java";                   // inferred as String\n```\n```\n',tech:"java",displayNumber:54,sortKey:54},{id:"java-55",number:55,title:"What is a strong reference vs weak reference?",question:"What is a strong reference vs weak reference?",difficulty:"Advanced",category:"Core Java & OOP",answer:"**Strong**: normal reference, GC won't collect while reachable. **Weak** (`WeakReference`): collected on next GC even if referenced. Used in caches (`WeakHashMap`). **Soft**: collected only when memory is low. **Phantom**: for pre-cleanup notifications.\n\n\n```java\nWeakReference<ExpensiveObj> ref = new WeakReference<>(new ExpensiveObj());\n// GC may reclaim the object even though ref exists\n```\n```\n",tech:"java",displayNumber:55,sortKey:55},{id:"java-56",number:56,title:"What is the `strictfp` keyword?",question:"What is the `strictfp` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`strictfp` ensures floating-point calculations follow IEEE 754 exactly across platforms (no platform-specific extended precision). Rarely needed today since Java 17 made strict FP the default.\n\n\n```java\npublic strictfp class MathUtils { }\n```\n```\n",tech:"java",displayNumber:56,sortKey:56},{id:"java-57",number:57,title:"What is the difference between `import` and `static import`?",question:"What is the difference between `import` and `static import`?",difficulty:"Basic",category:"Core Java & OOP",answer:"`import` brings a class into scope. `import static` brings static members (methods/fields) directly, allowing unqualified use. Overuse reduces readability.\n\n\n```java\nimport static java.lang.Math.PI;\nimport static java.lang.Math.sqrt;\ndouble r = sqrt(PI);\n```\n```\n",tech:"java",displayNumber:57,sortKey:57},{id:"java-58",number:58,title:"What is a sealed class (Java 17)?",question:"What is a sealed class (Java 17)?",difficulty:"Advanced",category:"Core Java & OOP",answer:"A sealed class restricts which classes can extend it via the `permits` clause. Enables exhaustive pattern matching and controlled hierarchies. Combines open inheritance with safety.\n\n\n```java\npublic sealed class Shape permits Circle, Square {}\nfinal class Circle extends Shape {}\nfinal class Square extends Shape {}\n```\n```\n",tech:"java",displayNumber:58,sortKey:58},{id:"java-59",number:59,title:"What is the difference between `ExecutionException` and `InterruptedException`?",question:"What is the difference between `ExecutionException` and `InterruptedException`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`InterruptedException` is thrown when a thread is interrupted while waiting (sleep/wait). `ExecutionException` wraps an exception thrown by a task submitted to an executor.\n\n\n```java\ntry { future.get(); }\ncatch (InterruptedException e) { Thread.currentThread().interrupt(); }\ncatch (ExecutionException e) { Throwable cause = e.getCause(); }\n```\n```\n",tech:"java",displayNumber:59,sortKey:59},{id:"java-60",number:60,title:"What is the difference between a mutable and immutable object?",question:"What is the difference between a mutable and immutable object?",difficulty:"Basic",category:"Core Java & OOP",answer:'Mutable objects can change state after creation (`StringBuilder`, arrays). Immutable objects cannot (`String`, `Integer`, `LocalDate`). Immutability aids thread-safety and caching.\n\n\n```java\nString s = "a";\ns = s + "b"; // new object created, original unchanged\n```\n```\n',tech:"java",displayNumber:60,sortKey:60},{id:"java-61",number:61,title:"What is the purpose of `Math.random()` vs `Random` class?",question:"What is the purpose of `Math.random()` vs `Random` class?",difficulty:"Basic",category:"Core Java & OOP",answer:"`Math.random()` returns a double in [0,1) using an internal `Random`. The `Random` class offers more control (seeds, `nextInt`, `nextGaussian`, streams). For cryptography, use `SecureRandom`.\n\n\n```java\ndouble d = Math.random();\nRandom r = new Random(42); // seeded\nint n = r.nextInt(100);\n```\n```\n",tech:"java",displayNumber:61,sortKey:61},{id:"java-62",number:62,title:"What is a static factory method?",question:"What is a static factory method?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'A static method that returns an instance of the class (e.g., `LocalDate.now()`, `List.of()`). Advantages: meaningful names, can return cached/subtype, not required to create new object each call.\n\n\n```java\npublic static User createAdmin() { return new User("admin", Role.ADMIN); }\nList<String> list = List.of("a", "b"); // static factory\n```\n```\n',tech:"java",displayNumber:62,sortKey:62},{id:"java-63",number:63,title:"What is the difference between `String`, `StringBuffer`, and `StringBuilder` thread-safety?",question:"What is the difference between `String`, `StringBuffer`, and `StringBuilder` thread-safety?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`String` is immutable (thread-safe by nature). `StringBuffer` is synchronized (thread-safe but slower). `StringBuilder` is not synchronized (fastest, use in single-threaded code).\n\n\n```java\nString s = "immutable";\nStringBuilder sb = new StringBuilder(); // fast\nStringBuffer sbf = new StringBuffer();   // synchronized\n```\n```\n',tech:"java",displayNumber:63,sortKey:63},{id:"java-64",number:64,title:'What is the concept of "fail-fast" in Java?',question:'What is the concept of "fail-fast" in Java?',difficulty:"Advanced",category:"Core Java & OOP",answer:"Fail-fast iterators (e.g., `ArrayList`'s) throw `ConcurrentModificationException` immediately if the collection is structurally modified during iteration (except via the iterator's own methods).\n\n\n```java\nfor (String s : list) {\n    list.add(\"x\"); // ConcurrentModificationException\n}\n```\n```\n",tech:"java",displayNumber:64,sortKey:64},{id:"java-65",number:65,title:"What is the difference between `clone()` and copy constructor?",question:"What is the difference between `clone()` and copy constructor?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`clone()` (from `Cloneable`) is fragile, needs casting, doesn't call constructors. A copy constructor (e.g., `Person(Person other)`) is explicit, type-safe, and easier to maintain.\n\n\n```java\nclass Person {\n    Person(Person other) { this.name = other.name; } // copy constructor\n}\n```\n```\n",tech:"java",displayNumber:65,sortKey:65},{id:"java-66",number:66,title:"What is the difference between a class and an object?",question:"What is the difference between a class and an object?",difficulty:"Basic",category:"Core Java & OOP",answer:"A **class** is a blueprint/template defining structure and behavior. An **object** is a concrete instance of a class occupying memory with its own state.\n\n\n```java\nclass Car {}          // blueprint\nCar myCar = new Car(); // object/instance\n```\n```\n",tech:"java",displayNumber:66,sortKey:66},{id:"java-67",number:67,title:"What is the `default` method in interfaces?",question:"What is the `default` method in interfaces?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Introduced in Java 8, `default` methods provide implementation in interfaces without breaking existing implementers. Enables interface evolution and multiple inheritance of behavior.\n\n\n```java\ninterface Vehicle {\n    default void start() { System.out.println("Starting..."); }\n}\n```\n```\n',tech:"java",displayNumber:67,sortKey:67},{id:"java-68",number:68,title:"What is the difference between `Class.forName()` and `ClassLoader.loadClass()`?",question:"What is the difference between `Class.forName()` and `ClassLoader.loadClass()`?",difficulty:"Advanced",category:"Core Java & OOP",answer:'`Class.forName()` initializes the class (runs static blocks) by default. `ClassLoader.loadClass()` only loads it without initialization. JDBC uses `forName()` to register drivers.\n\n\n```java\nClass<?> c1 = Class.forName("com.mysql.Driver"); // initializes\nClass<?> c2 = getClass().getClassLoader().loadClass("com.mysql.Driver");\n```\n```\n',tech:"java",displayNumber:68,sortKey:68},{id:"java-69",number:69,title:"What is the purpose of the `native` keyword?",question:"What is the purpose of the `native` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`native` declares a method implemented in platform-specific C/C++ code (JNI). Used for low-level OS interactions (e.g., `Object.hashCode()` in hotspot JVM).\n\n\n```java\npublic native int hashCode(); // implemented in C\n```\n```\n",tech:"java",displayNumber:69,sortKey:69},{id:"java-70",number:70,title:"What is the difference between `abstract` method and `default` method?",question:"What is the difference between `abstract` method and `default` method?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"An `abstract` method has no body and must be implemented by subclasses. A `default` method has a body and is optional to override.\n\n\n```java\ninterface I {\n    void mustImplement();     // abstract\n    default void optional() {} // default\n}\n```\n```\n",tech:"java",displayNumber:70,sortKey:70},{id:"java-71",number:71,title:'What is the concept of "composition over inheritance"?',question:'What is the concept of "composition over inheritance"?',difficulty:"Intermediate",category:"Core Java & OOP",answer:"Favoring composition (has-a relationship) over inheritance (is-a) promotes flexibility and reduces tight coupling. Inheritance breaks encapsulation; composition allows runtime behavior changes.\n\n\n```java\nclass Engine {}\nclass Car { private Engine engine; } // composition (has-a)\n```\n```\n",tech:"java",displayNumber:71,sortKey:71},{id:"java-72",number:72,title:"What is the difference between a pass-by-value and pass-by-reference?",question:"What is the difference between a pass-by-value and pass-by-reference?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Java is **always pass-by-value**. For objects, the value is the reference (memory address), so changes to the object's fields are visible, but reassigning the parameter doesn't affect the caller's reference.


\`\`\`java
void modify(List<String> l) { l.add("x"); } // visible
void reassign(List<String> l) { l = new ArrayList<>(); } // not visible to caller
\`\`\`
\`\`\`
`,tech:"java",displayNumber:72,sortKey:72},{id:"java-73",number:73,title:"What is the `switch` expression (Java 14+)?",question:"What is the `switch` expression (Java 14+)?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Switch expressions (vs statements) return a value using `->` syntax and `yield`, are exhaustive with `default` or enum coverage, and can be assigned. Arrow case doesn't fall through.\n\n\n```java\nint days = switch (month) {\n    case 1, 3, 5, 7, 8, 10, 12 -> 31;\n    case 2 -> 28;\n    default -> 30;\n};\n```\n```\n",tech:"java",displayNumber:73,sortKey:73},{id:"java-74",number:74,title:"What is the difference between `getClass()` and `instanceof`?",question:"What is the difference between `getClass()` and `instanceof`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`getClass()` returns the exact runtime class (no subclass match). `instanceof` returns true for the class and all its subclasses.\n\n\n```java\nObject o = new ArrayList<>();\nSystem.out.println(o instanceof List);    // true\nSystem.out.println(o.getClass() == List.class); // false (it's ArrayList)\n```\n```\n",tech:"java",displayNumber:74,sortKey:74},{id:"java-75",number:75,title:"What is a memory leak in Java and how to prevent it?",question:"What is a memory leak in Java and how to prevent it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"Even with GC, leaks occur when objects are unintentionally retained (static collections, unclosed resources, listeners not removed, thread-local not cleaned). Prevent via weak references, try-with-resources, and careful lifecycle management.\n\n\n```java\n// Leak: static Map holds references forever\nstatic Map<String, Object> cache = new HashMap<>();\n// Fix: use WeakHashMap or bounded cache with eviction\n```\n```\n",tech:"java",displayNumber:75,sortKey:75},{id:"java-76",number:76,title:"What is the Java Collections Framework?",question:"What is the Java Collections Framework?",difficulty:"Basic",category:"Collections & Generics",answer:'The Java Collections Framework is the standard set of interfaces, implementations, and algorithms for storing and processing groups of objects. Core interfaces include Collection, List, Set, Queue, Deque, and Map. Implementations such as ArrayList, HashSet, HashMap, PriorityQueue, and TreeMap choose different trade-offs for ordering, uniqueness, lookup speed, memory use, and concurrency. Prefer coding to interfaces so the implementation can change without rewriting callers.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(); names.add("Ava"); names.add("Ben"); System.out.println(names); } }\n```\n```\n',tech:"java",displayNumber:76,sortKey:76},{id:"java-77",number:77,title:"How does ArrayList work internally?",question:"How does ArrayList work internally?",difficulty:"Basic",category:"Collections & Generics",answer:"ArrayList is a resizable array. It stores elements in a contiguous Object[] and grows when capacity is exceeded, usually by allocating a larger array and copying existing elements. Random access by index is O(1), appending is amortized O(1), and insertion or removal in the middle is O(n) because elements must shift. It allows duplicates and null values.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<Integer> list = new ArrayList<>(); list.add(10); list.add(20); list.add(1, 15); System.out.println(list.get(1)); } }\n```\n```\n",tech:"java",displayNumber:77,sortKey:77},{id:"java-78",number:78,title:"When should you use LinkedList instead of ArrayList?",question:"When should you use LinkedList instead of ArrayList?",difficulty:"Intermediate",category:"Collections & Generics",answer:'LinkedList is a doubly linked list implementing List and Deque. It can add or remove at the ends in O(1), but finding an index is O(n). In practice, ArrayList is usually faster for most list workloads due to cache locality and lower memory overhead. Use LinkedList mainly when you specifically need Deque operations and do frequent additions/removals at both ends, though ArrayDeque is often better for that.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<String> deque = new LinkedList<>(); deque.addFirst("front"); deque.addLast("back"); System.out.println(deque.removeFirst()); } }\n```\n```\n',tech:"java",displayNumber:78,sortKey:78},{id:"java-79",number:79,title:"Compare ArrayList and LinkedList.",question:"Compare ArrayList and LinkedList.",difficulty:"Basic",category:"Collections & Generics",answer:'ArrayList is backed by an array; LinkedList is backed by nodes. ArrayList gives O(1) random reads and compact memory usage, but middle inserts/removes shift elements. LinkedList gives O(1) insert/remove only when you already have a node position or operate at ends, but random access is O(n) and each element costs extra node references. For interview answers, default to ArrayList unless queue/deque behavior is required.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> fastReads = new ArrayList<>(); Deque<String> ends = new LinkedList<>(); fastReads.add("x"); ends.addFirst("y"); System.out.println(fastReads.get(0) + ends.peekFirst()); } }\n```\n```\n',tech:"java",displayNumber:79,sortKey:79},{id:"java-80",number:80,title:"What is Vector and why is it legacy?",question:"What is Vector and why is it legacy?",difficulty:"Basic",category:"Collections & Generics",answer:'Vector is a synchronized, resizable array from early Java. Its methods are synchronized, which makes individual operations thread-safe but often slower and not enough for compound actions such as check-then-act. Modern code usually uses ArrayList for non-concurrent lists, Collections.synchronizedList for a synchronized wrapper, or CopyOnWriteArrayList/concurrent queues for specific concurrent patterns.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Vector<String> v = new Vector<>(); v.add("legacy"); System.out.println(v.firstElement()); } }\n```\n```\n',tech:"java",displayNumber:80,sortKey:80},{id:"java-81",number:81,title:"What is CopyOnWriteArrayList?",question:"What is CopyOnWriteArrayList?",difficulty:"Intermediate",category:"Collections & Generics",answer:'CopyOnWriteArrayList is a thread-safe List optimized for many reads and few writes. Every mutating operation copies the underlying array, so iterators see a stable snapshot and never throw ConcurrentModificationException. Reads are fast and lock-free, but writes are expensive and memory-heavy. It is useful for listener lists, routing tables, and configuration snapshots.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<String> listeners = new CopyOnWriteArrayList<>(); listeners.add("A"); for (String s : listeners) { listeners.add("B"); System.out.println(s); } System.out.println(listeners); } }\n```\n```\n',tech:"java",displayNumber:81,sortKey:81},{id:"java-82",number:82,title:"Why should ArrayList initial capacity matter?",question:"Why should ArrayList initial capacity matter?",difficulty:"Intermediate",category:"Collections & Generics",answer:"ArrayList grows automatically, but each growth allocates a new array and copies elements. If you know the approximate size, passing an initial capacity reduces reallocations and copies. Capacity is not the same as size: capacity is internal storage; size is the number of elements actually present.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<Integer> ids = new ArrayList<>(1000); for (int i = 0; i < 3; i++) ids.add(i); System.out.println(ids.size()); } }\n```\n```\n",tech:"java",displayNumber:82,sortKey:82},{id:"java-83",number:83,title:"What is fail-fast iteration in lists?",question:"What is fail-fast iteration in lists?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Most non-concurrent collection iterators are fail-fast. If the collection is structurally modified outside the iterator after iterator creation, the iterator may throw ConcurrentModificationException. This is a bug detector, not a concurrency guarantee. Use Iterator.remove, collect changes separately, or use concurrent/snapshot collections when mutation during iteration is required.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = new ArrayList<>(List.of("a", "b")); Iterator<String> it = xs.iterator(); while (it.hasNext()) if (it.next().equals("a")) it.remove(); System.out.println(xs); } }\n```\n```\n',tech:"java",displayNumber:83,sortKey:83},{id:"java-84",number:84,title:"How do you create unmodifiable and immutable lists?",question:"How do you create unmodifiable and immutable lists?",difficulty:"Basic",category:"Collections & Generics",answer:'Collections.unmodifiableList returns a read-only view of an existing list; changes to the backing list are still visible. List.of and List.copyOf create unmodifiable lists that reject nulls and are not backed by the original mutable list. For defensive APIs, prefer List.copyOf so callers cannot mutate state through an alias.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> mutable = new ArrayList<>(List.of("a")); List<String> copy = List.copyOf(mutable); mutable.add("b"); System.out.println(copy); } }\n```\n```\n',tech:"java",displayNumber:84,sortKey:84},{id:"java-85",number:85,title:"How do you safely sort a List?",question:"How do you safely sort a List?",difficulty:"Basic",category:"Collections & Generics",answer:'Use List.sort or Collections.sort with a Comparator. Sorting mutates the list, so copy first if callers expect the original order to remain unchanged. Comparators should be consistent, handle nulls if nulls are possible, and avoid subtraction for numeric comparison because it can overflow.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(List.of("Bo", "Alice")); names.sort(Comparator.comparingInt(String::length)); System.out.println(names); } }\n```\n```\n',tech:"java",displayNumber:85,sortKey:85},{id:"java-86",number:86,title:"What makes a Set different from a List?",question:"What makes a Set different from a List?",difficulty:"Basic",category:"Collections & Generics",answer:'A Set stores unique elements and does not expose positional access. Uniqueness is determined either by equals/hashCode for hash-based sets or by comparison for sorted sets. Choose a Set when membership and duplicate prevention matter more than index order.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new HashSet<>(); s.add("a"); s.add("a"); System.out.println(s.size()); } }\n```\n```\n',tech:"java",displayNumber:86,sortKey:86},{id:"java-87",number:87,title:"How does HashSet work internally?",question:"How does HashSet work internally?",difficulty:"Intermediate",category:"Collections & Generics",answer:"HashSet is backed by a HashMap where each set element is stored as a map key. add, contains, and remove are O(1) on average when hashCode distributes values well. It does not preserve insertion order and allows one null element.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<Integer> ids = new HashSet<>(); ids.add(3); ids.add(1); System.out.println(ids.contains(1)); } }\n```\n```\n",tech:"java",displayNumber:87,sortKey:87},{id:"java-88",number:88,title:"Why must equals and hashCode agree for HashSet?",question:"Why must equals and hashCode agree for HashSet?",difficulty:"Intermediate",category:"Collections & Generics",answer:'HashSet first uses hashCode to find a bucket, then equals to check logical equality inside that bucket. If equal objects have different hash codes, duplicates can appear and lookups can fail. The contract is: if a.equals(b) is true, a.hashCode() must equal b.hashCode().\n\n\n```java\nimport java.util.*;\nrecord User(int id, String name) {}\nclass Demo { public static void main(String[] args) { Set<User> users = new HashSet<>(); users.add(new User(1,"A")); users.add(new User(1,"A")); System.out.println(users.size()); } }\n```\n```\n',tech:"java",displayNumber:88,sortKey:88},{id:"java-89",number:89,title:"What is LinkedHashSet used for?",question:"What is LinkedHashSet used for?",difficulty:"Basic",category:"Collections & Generics",answer:'LinkedHashSet combines HashSet uniqueness with predictable iteration order. It keeps a linked list across entries, normally in insertion order. It costs slightly more memory than HashSet but is useful when stable output or predictable tests matter.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new LinkedHashSet<>(); s.add("b"); s.add("a"); System.out.println(s); } }\n```\n```\n',tech:"java",displayNumber:89,sortKey:89},{id:"java-90",number:90,title:"How does TreeSet order elements?",question:"How does TreeSet order elements?",difficulty:"Intermediate",category:"Collections & Generics",answer:'TreeSet is a NavigableSet backed by a balanced tree. It keeps elements sorted by natural ordering or a Comparator. add, contains, and remove are O(log n). Its equality for uniqueness follows comparison: if compare returns 0, the elements are considered duplicates.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new TreeSet<>(String.CASE_INSENSITIVE_ORDER); s.add("java"); s.add("JAVA"); System.out.println(s); } }\n```\n```\n',tech:"java",displayNumber:90,sortKey:90},{id:"java-91",number:91,title:"What is EnumSet?",question:"What is EnumSet?",difficulty:"Intermediate",category:"Collections & Generics",answer:"EnumSet is a high-performance Set specialized for enum values. Internally it uses bit vectors, making it compact and fast. It rejects nulls and can only contain values from a single enum type. Use it instead of HashSet for enum flags.\n\n\n```java\nimport java.util.*;\nenum Role { USER, ADMIN }\nclass Demo { public static void main(String[] args) { EnumSet<Role> roles = EnumSet.of(Role.USER, Role.ADMIN); System.out.println(roles.contains(Role.ADMIN)); } }\n```\n```\n",tech:"java",displayNumber:91,sortKey:91},{id:"java-92",number:92,title:"How do HashSet, LinkedHashSet, and TreeSet compare?",question:"How do HashSet, LinkedHashSet, and TreeSet compare?",difficulty:"Basic",category:"Collections & Generics",answer:"HashSet is fastest on average but unordered. LinkedHashSet preserves insertion order with a little extra memory. TreeSet keeps elements sorted and supports range queries, but operations are O(log n). Pick by ordering requirement first, then performance.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { System.out.println(new HashSet<>(List.of(3,1,2))); System.out.println(new LinkedHashSet<>(List.of(3,1,2))); System.out.println(new TreeSet<>(List.of(3,1,2))); } }\n```\n```\n",tech:"java",displayNumber:92,sortKey:92},{id:"java-93",number:93,title:"What are NavigableSet operations?",question:"What are NavigableSet operations?",difficulty:"Intermediate",category:"Collections & Generics",answer:"NavigableSet adds search methods such as lower, floor, ceiling, higher, pollFirst, pollLast, descendingSet, and range views. TreeSet implements it. These operations are ideal for ordered lookups like nearest value, leaderboard ranges, or time windows.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { NavigableSet<Integer> s = new TreeSet<>(List.of(10,20,30)); System.out.println(s.floor(25)); System.out.println(s.higher(20)); } }\n```\n```\n",tech:"java",displayNumber:93,sortKey:93},{id:"java-94",number:94,title:"Can Sets contain mutable objects?",question:"Can Sets contain mutable objects?",difficulty:"Advanced",category:"Collections & Generics",answer:"They can, but mutating fields used by equals, hashCode, or compareTo while the object is inside a Set breaks lookup and uniqueness. The object may become stored in the wrong hash bucket or tree position. Prefer immutable keys/elements or remove, mutate, and re-add.\n\n\n```java\nimport java.util.*;\nclass Demo { static class Box { int id; Box(int id){this.id=id;} public boolean equals(Object o){return o instanceof Box b && b.id==id;} public int hashCode(){return id;} } public static void main(String[] args) { Box b=new Box(1); Set<Box> s=new HashSet<>(); s.add(b); b.id=2; System.out.println(s.contains(b)); } }\n```\n```\n",tech:"java",displayNumber:94,sortKey:94},{id:"java-95",number:95,title:"How do you remove duplicates while preserving order?",question:"How do you remove duplicates while preserving order?",difficulty:"Basic",category:"Collections & Generics",answer:'Use LinkedHashSet when you need uniqueness and original encounter order. Construct it from the list, then optionally create a new ArrayList. This avoids manual contains checks and is usually clearer and faster.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = List.of("a","b","a"); List<String> unique = new ArrayList<>(new LinkedHashSet<>(xs)); System.out.println(unique); } }\n```\n```\n',tech:"java",displayNumber:95,sortKey:95},{id:"java-96",number:96,title:"What is a Map?",question:"What is a Map?",difficulty:"Basic",category:"Collections & Generics",answer:'A Map stores key-value pairs and is not a subtype of Collection. Keys are unique; values may be duplicated. Common implementations include HashMap for general lookup, LinkedHashMap for predictable order, TreeMap for sorted keys, ConcurrentHashMap for concurrency, and EnumMap for enum keys.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> ages = new HashMap<>(); ages.put("Ana", 30); System.out.println(ages.get("Ana")); } }\n```\n```\n',tech:"java",displayNumber:96,sortKey:96},{id:"java-97",number:97,title:"How does HashMap work internally?",question:"How does HashMap work internally?",difficulty:"Intermediate",category:"Collections & Generics",answer:'HashMap stores entries in an array of buckets. It hashes the key, maps the hash to a bucket, and resolves collisions within that bucket. Since Java 8, heavily-collided buckets can become balanced trees when thresholds are met, improving worst-case lookup from O(n) to O(log n). Average get and put are O(1).\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new HashMap<>(); m.put("one", 1); m.put("two", 2); System.out.println(m.get("two")); } }\n```\n```\n',tech:"java",displayNumber:97,sortKey:97},{id:"java-98",number:98,title:"What are HashMap capacity and load factor?",question:"What are HashMap capacity and load factor?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Capacity is the number of buckets; load factor controls when resizing happens. The default load factor is 0.75, balancing memory and collisions. When size exceeds capacity times load factor, HashMap resizes and rehashes entries. If expected size is known, initialize capacity to reduce resizing.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Integer,String> m = new HashMap<>(128, 0.75f); m.put(1, "a"); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:98,sortKey:98},{id:"java-99",number:99,title:"Can HashMap use null keys and values?",question:"Can HashMap use null keys and values?",difficulty:"Basic",category:"Collections & Generics",answer:'HashMap permits one null key and multiple null values. This differs from Hashtable and ConcurrentHashMap, which reject null. Null can make missing-value logic ambiguous because get returns null both for absent keys and keys mapped to null; use containsKey when needed.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,String> m = new HashMap<>(); m.put(null, "root"); m.put("x", null); System.out.println(m.containsKey("x")); } }\n```\n```\n',tech:"java",displayNumber:99,sortKey:99},{id:"java-100",number:100,title:"Why are mutable keys dangerous in HashMap?",question:"Why are mutable keys dangerous in HashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'HashMap lookup depends on the key hash and equality at insertion time. If fields used by hashCode or equals change while the key is in the map, the entry may become unreachable even though it still exists. Use immutable key classes such as records, String, Integer, UUID, or remove and reinsert after mutation.\n\n\n```java\nimport java.util.*;\nclass Demo { record Key(int id) {} public static void main(String[] args) { Map<Key,String> m = new HashMap<>(); m.put(new Key(1), "ok"); System.out.println(m.get(new Key(1))); } }\n```\n```\n',tech:"java",displayNumber:100,sortKey:100},{id:"java-101",number:101,title:"What is LinkedHashMap?",question:"What is LinkedHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'LinkedHashMap extends hash-table lookup with a doubly linked list over entries. It preserves insertion order by default and can also maintain access order. Access-order LinkedHashMap is commonly used for simple LRU caches by overriding removeEldestEntry.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new LinkedHashMap<>(); m.put("b",2); m.put("a",1); System.out.println(m.keySet()); } }\n```\n```\n',tech:"java",displayNumber:101,sortKey:101},{id:"java-102",number:102,title:"How do you build a simple LRU cache with LinkedHashMap?",question:"How do you build a simple LRU cache with LinkedHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'Create a LinkedHashMap with accessOrder=true and override removeEldestEntry to evict when size exceeds the limit. This is simple and works for single-threaded or externally synchronized use. For high-concurrency or production caching features, use a real cache library.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Integer,String> cache = new LinkedHashMap<>(16, .75f, true) { protected boolean removeEldestEntry(Map.Entry<Integer,String> e) { return size() > 2; } }; cache.put(1,"a"); cache.put(2,"b"); cache.get(1); cache.put(3,"c"); System.out.println(cache.keySet()); } }\n```\n```\n',tech:"java",displayNumber:102,sortKey:102},{id:"java-103",number:103,title:"What is TreeMap?",question:"What is TreeMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'TreeMap is a NavigableMap backed by a red-black tree. It stores keys sorted by natural order or a Comparator. get, put, and remove are O(log n). It is useful for sorted iteration and range queries such as subMap, headMap, and tailMap.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { NavigableMap<Integer,String> m = new TreeMap<>(); m.put(20,"b"); m.put(10,"a"); System.out.println(m.firstEntry()); } }\n```\n```\n',tech:"java",displayNumber:103,sortKey:103},{id:"java-104",number:104,title:"How does ConcurrentHashMap differ from HashMap?",question:"How does ConcurrentHashMap differ from HashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'ConcurrentHashMap supports safe concurrent access without synchronizing the whole map for every operation. It rejects null keys and values to avoid ambiguity in concurrent reads. Retrievals are highly concurrent, and updates lock only affected internal regions/nodes. Iterators are weakly consistent, not fail-fast.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> m = new ConcurrentHashMap<>(); m.put("a", 1); m.compute("a", (k,v) -> v + 1); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:104,sortKey:104},{id:"java-105",number:105,title:"What is EnumMap?",question:"What is EnumMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'EnumMap is a Map optimized for enum keys. It stores values in an array indexed by enum ordinal, so it is fast, compact, and preserves enum declaration order. It rejects null keys but allows null values. Use it instead of HashMap whenever all keys are from one enum type.\n\n\n```java\nimport java.util.*;\nenum Day { MON, TUE }\nclass Demo { public static void main(String[] args) { EnumMap<Day,String> m = new EnumMap<>(Day.class); m.put(Day.MON, "work"); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:105,sortKey:105},{id:"java-106",number:106,title:"How do getOrDefault, putIfAbsent, and computeIfAbsent differ?",question:"How do getOrDefault, putIfAbsent, and computeIfAbsent differ?",difficulty:"Intermediate",category:"Collections & Generics",answer:'getOrDefault reads a value or returns a fallback without mutating. putIfAbsent writes a value only when no mapping exists or the current value is null for maps that allow null. computeIfAbsent lazily computes and stores a value only when absent, which is ideal for grouping or memoizing.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,List<Integer>> m = new HashMap<>(); m.computeIfAbsent("x", k -> new ArrayList<>()).add(1); System.out.println(m.getOrDefault("y", List.of())); } }\n```\n```\n',tech:"java",displayNumber:106,sortKey:106},{id:"java-107",number:107,title:"How do you iterate over a Map efficiently?",question:"How do you iterate over a Map efficiently?",difficulty:"Basic",category:"Collections & Generics",answer:'Use entrySet when both key and value are needed; it avoids a second lookup for each key. Use keySet when only keys are needed and values when only values are needed. Do not assume HashMap iteration order.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = Map.of("a",1,"b",2); for (Map.Entry<String,Integer> e : m.entrySet()) System.out.println(e.getKey()+"="+e.getValue()); } }\n```\n```\n',tech:"java",displayNumber:107,sortKey:107},{id:"java-108",number:108,title:"What are Map views?",question:"What are Map views?",difficulty:"Intermediate",category:"Collections & Generics",answer:'keySet, values, and entrySet are backed views of the map, not independent copies. Removing from a mutable view removes from the map. Adding is generally unsupported through views except by putting into the map itself. Copy the view if you need a snapshot.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new HashMap<>(Map.of("a",1,"b",2)); m.keySet().remove("a"); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:108,sortKey:108},{id:"java-109",number:109,title:"What is hash collision handling in HashMap?",question:"What is hash collision handling in HashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'A collision occurs when different keys map to the same bucket. HashMap stores colliding entries in that bucket and checks equality to find the correct key. With enough collisions and sufficient table size, Java can treeify the bucket to reduce lookup cost. Good hashCode implementations keep collisions rare.\n\n\n```java\nimport java.util.*;\nclass Demo { static class K { int id; K(int id){this.id=id;} public int hashCode(){return 1;} public boolean equals(Object o){return o instanceof K k && k.id==id;} } public static void main(String[] args) { Map<K,String> m=new HashMap<>(); m.put(new K(1),"a"); System.out.println(m.get(new K(1))); } }\n```\n```\n',tech:"java",displayNumber:109,sortKey:109},{id:"java-110",number:110,title:"What is IdentityHashMap?",question:"What is IdentityHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'IdentityHashMap compares keys with == instead of equals and uses identity-based hashing. It is not a general-purpose Map. It is useful for object graph traversal, serialization bookkeeping, or cycle detection where distinct object instances must remain distinct even if equals says they are equal.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new IdentityHashMap<>(); m.put(new String("x"), 1); m.put(new String("x"), 2); System.out.println(m.size()); } }\n```\n```\n',tech:"java",displayNumber:110,sortKey:110},{id:"java-111",number:111,title:"What is WeakHashMap?",question:"What is WeakHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'WeakHashMap stores keys as weak references. If a key is no longer strongly reachable elsewhere, the garbage collector may remove its entry. It is useful for metadata associated with objects without preventing their collection. It should not be used when entries must remain reliably present.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Object,String> m = new WeakHashMap<>(); Object key = new Object(); m.put(key, "metadata"); System.out.println(m.containsKey(key)); } }\n```\n```\n',tech:"java",displayNumber:111,sortKey:111},{id:"java-112",number:112,title:"How do equals and compareTo affect TreeMap keys?",question:"How do equals and compareTo affect TreeMap keys?",difficulty:"Advanced",category:"Collections & Generics",answer:'TreeMap uses compareTo or Comparator to decide both order and key uniqueness. If compare returns 0, TreeMap treats keys as the same even if equals would return false. A comparator inconsistent with equals can be valid but surprising, so document it and use it deliberately.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new TreeMap<>(String.CASE_INSENSITIVE_ORDER); m.put("java",1); m.put("JAVA",2); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:112,sortKey:112},{id:"java-113",number:113,title:"How do you merge counts in a Map?",question:"How do you merge counts in a Map?",difficulty:"Basic",category:"Collections & Generics",answer:'Use merge for frequency maps and accumulation. It inserts the initial value when absent and combines old and new values when present. This avoids verbose containsKey checks and is safe for concise counting logic.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> counts = new HashMap<>(); for (String w : List.of("a","b","a")) counts.merge(w, 1, Integer::sum); System.out.println(counts); } }\n```\n```\n',tech:"java",displayNumber:113,sortKey:113},{id:"java-114",number:114,title:"What makes Hashtable legacy?",question:"What makes Hashtable legacy?",difficulty:"Basic",category:"Collections & Generics",answer:'Hashtable is synchronized and does not allow null keys or values. Like Vector, it is a legacy class whose synchronized methods rarely provide the right concurrency model for compound operations. Prefer HashMap, ConcurrentHashMap, or explicit synchronization depending on the requirement.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Hashtable<String,Integer> h = new Hashtable<>(); h.put("a",1); System.out.println(h.get("a")); } }\n```\n```\n',tech:"java",displayNumber:114,sortKey:114},{id:"java-115",number:115,title:"How do immutable Map factories work?",question:"How do immutable Map factories work?",difficulty:"Basic",category:"Collections & Generics",answer:'Map.of and Map.ofEntries create unmodifiable maps that reject null keys and values and duplicate keys. They are compact and clear for constants and small maps. Iteration order is unspecified unless documented by a specific implementation, so do not rely on it.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = Map.of("a",1,"b",2); System.out.println(m.get("a")); } }\n```\n```\n',tech:"java",displayNumber:115,sortKey:115},{id:"java-116",number:116,title:"What is the Queue interface?",question:"What is the Queue interface?",difficulty:"Basic",category:"Collections & Generics",answer:'Queue represents elements waiting to be processed, usually FIFO. offer/poll/peek are preferred because they return special values instead of throwing when an operation cannot be completed. add/remove/element throw exceptions on failure.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<String> q = new ArrayDeque<>(); q.offer("job"); System.out.println(q.poll()); } }\n```\n```\n',tech:"java",displayNumber:116,sortKey:116},{id:"java-117",number:117,title:"How does PriorityQueue work?",question:"How does PriorityQueue work?",difficulty:"Intermediate",category:"Collections & Generics",answer:"PriorityQueue orders elements by natural order or Comparator, not insertion order. The head is the least element according to that ordering by default. It is backed by a binary heap, so offer and poll are O(log n), peek is O(1), and iteration is not sorted.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<Integer> pq = new PriorityQueue<>(); pq.addAll(List.of(5,1,3)); System.out.println(pq.poll()); } }\n```\n```\n",tech:"java",displayNumber:117,sortKey:117},{id:"java-118",number:118,title:"How do you create a max-heap PriorityQueue?",question:"How do you create a max-heap PriorityQueue?",difficulty:"Basic",category:"Collections & Generics",answer:"PriorityQueue is a min-heap by default. Provide Comparator.reverseOrder for a max-heap. Avoid subtraction comparators like (a,b)->b-a because integer overflow can break ordering.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder()); pq.addAll(List.of(5,1,3)); System.out.println(pq.poll()); } }\n```\n```\n",tech:"java",displayNumber:118,sortKey:118},{id:"java-119",number:119,title:"What is ArrayDeque?",question:"What is ArrayDeque?",difficulty:"Basic",category:"Collections & Generics",answer:'ArrayDeque is a resizable-array implementation of Deque. It is usually faster than Stack and LinkedList for stack or queue behavior. It does not allow null elements because null is used by methods such as poll to mean no element.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<String> stack = new ArrayDeque<>(); stack.push("a"); stack.push("b"); System.out.println(stack.pop()); } }\n```\n```\n',tech:"java",displayNumber:119,sortKey:119},{id:"java-120",number:120,title:"Why prefer ArrayDeque over Stack?",question:"Why prefer ArrayDeque over Stack?",difficulty:"Basic",category:"Collections & Generics",answer:"Stack is a legacy synchronized Vector subclass. ArrayDeque provides cleaner Deque methods and better performance for single-threaded stack usage. Use push, pop, and peek for stack behavior or offer/poll for queue behavior.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<Integer> stack = new ArrayDeque<>(); stack.push(1); stack.push(2); System.out.println(stack.pop()); } }\n```\n```\n",tech:"java",displayNumber:120,sortKey:120},{id:"java-121",number:121,title:"What is BlockingQueue?",question:"What is BlockingQueue?",difficulty:"Intermediate",category:"Collections & Generics",answer:'BlockingQueue is a concurrent queue whose operations can wait for space or elements. put blocks until space is available; take blocks until an element is available. It is commonly used for producer-consumer designs and avoids manual wait/notify code.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { BlockingQueue<String> q = new ArrayBlockingQueue<>(1); q.put("job"); System.out.println(q.take()); } }\n```\n```\n',tech:"java",displayNumber:121,sortKey:121},{id:"java-122",number:122,title:"Compare ArrayBlockingQueue and LinkedBlockingQueue.",question:"Compare ArrayBlockingQueue and LinkedBlockingQueue.",difficulty:"Intermediate",category:"Collections & Generics",answer:"ArrayBlockingQueue is bounded and backed by an array; its capacity is fixed. LinkedBlockingQueue is node-based and optionally bounded, but the no-arg constructor creates a very large capacity that can hide backpressure. Prefer explicit bounds in server code to avoid memory growth.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { BlockingQueue<Integer> q = new ArrayBlockingQueue<>(2); q.offer(1); q.offer(2); System.out.println(q.offer(3)); } }\n```\n```\n",tech:"java",displayNumber:122,sortKey:122},{id:"java-123",number:123,title:"What is PriorityBlockingQueue?",question:"What is PriorityBlockingQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:"PriorityBlockingQueue is an unbounded blocking queue that orders elements by priority. take blocks when empty, but put does not block for capacity because it is unbounded. Since equal-priority ordering is not guaranteed, include a sequence number if stable ordering matters.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { PriorityBlockingQueue<Integer> q = new PriorityBlockingQueue<>(); q.put(3); q.put(1); System.out.println(q.take()); } }\n```\n```\n",tech:"java",displayNumber:123,sortKey:123},{id:"java-124",number:124,title:"What is SynchronousQueue?",question:"What is SynchronousQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:'SynchronousQueue has no internal capacity. Each put must rendezvous with a take, so it directly hands off work between threads. It is useful in executor designs where tasks should not queue internally and producers should block until a consumer is ready.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { SynchronousQueue<String> q = new SynchronousQueue<>(); new Thread(() -> { try { q.put("handoff"); } catch (InterruptedException ignored) {} }).start(); System.out.println(q.take()); } }\n```\n```\n',tech:"java",displayNumber:124,sortKey:124},{id:"java-125",number:125,title:"What is DelayQueue?",question:"What is DelayQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:"DelayQueue stores Delayed elements and only releases each element after its delay expires. It is unbounded and useful for scheduled retries, timeout handling, and expiring items. Elements must implement getDelay and compareTo consistently.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { static class Task implements Delayed { public long getDelay(TimeUnit u){ return 0; } public int compareTo(Delayed d){ return 0; } } public static void main(String[] args) throws Exception { DelayQueue<Task> q = new DelayQueue<>(); q.put(new Task()); System.out.println(q.take().getClass().getSimpleName()); } }\n```\n```\n",tech:"java",displayNumber:125,sortKey:125},{id:"java-126",number:126,title:"Why use generics?",question:"Why use generics?",difficulty:"Basic",category:"Collections & Generics",answer:'Generics provide compile-time type safety and remove most casts. A List<String> can only accept strings, so mistakes are caught before runtime. Generics also make APIs self-documenting because type relationships are visible in method signatures.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(); names.add("Ada"); String first = names.get(0); System.out.println(first.toUpperCase()); } }\n```\n```\n',tech:"java",displayNumber:126,sortKey:126},{id:"java-127",number:127,title:"What is a bounded type parameter?",question:"What is a bounded type parameter?",difficulty:"Intermediate",category:"Collections & Generics",answer:"A bounded type parameter restricts acceptable types. <T extends Number> means T must be Number or a subclass. Bounds let generic code call methods from the bound while preserving specific type information.\n\n\n```java\nclass Demo { static <T extends Number> double twice(T n) { return n.doubleValue() * 2; } public static void main(String[] args) { System.out.println(twice(21)); } }\n```\n```\n",tech:"java",displayNumber:127,sortKey:127},{id:"java-128",number:128,title:"What are multiple bounds in generics?",question:"What are multiple bounds in generics?",difficulty:"Advanced",category:"Collections & Generics",answer:"A type parameter can have multiple bounds, with at most one class bound first and then interfaces. For example, <T extends Number & Comparable<T>> requires both numeric behavior and comparability. This lets methods rely on several capabilities without accepting unrelated types.\n\n\n```java\nclass Demo { static <T extends Number & Comparable<T>> T max(T a, T b) { return a.compareTo(b) >= 0 ? a : b; } public static void main(String[] args) { System.out.println(max(3, 7)); } }\n```\n```\n",tech:"java",displayNumber:128,sortKey:128},{id:"java-129",number:129,title:"What is an unbounded wildcard?",question:"What is an unbounded wildcard?",difficulty:"Basic",category:"Collections & Generics",answer:"The wildcard ? means an unknown type. List<?> is useful when code only needs to read objects or use size/iteration without adding typed values. You cannot add arbitrary elements except null because the compiler does not know the actual element type.\n\n\n```java\nimport java.util.*;\nclass Demo { static void printSize(List<?> xs) { System.out.println(xs.size()); } public static void main(String[] args) { printSize(List.of(1,2,3)); } }\n```\n```\n",tech:"java",displayNumber:129,sortKey:129},{id:"java-130",number:130,title:"What is an upper-bounded wildcard?",question:"What is an upper-bounded wildcard?",difficulty:"Intermediate",category:"Collections & Generics",answer:"? extends T accepts T or any subtype of T. It is best for producers: you can read values as T, but cannot safely add T values because the actual list might be a more specific subtype. This is the extends part of PECS.\n\n\n```java\nimport java.util.*;\nclass Demo { static double sum(List<? extends Number> nums) { double s=0; for (Number n: nums) s+=n.doubleValue(); return s; } public static void main(String[] args) { System.out.println(sum(List.of(1,2,3))); } }\n```\n```\n",tech:"java",displayNumber:130,sortKey:130},{id:"java-131",number:131,title:"What is a lower-bounded wildcard?",question:"What is a lower-bounded wildcard?",difficulty:"Intermediate",category:"Collections & Generics",answer:"? super T accepts T or any supertype of T. It is best for consumers: you can add T values safely, but reads come back only as Object. This is the super part of PECS.\n\n\n```java\nimport java.util.*;\nclass Demo { static void addInts(List<? super Integer> out) { out.add(1); out.add(2); } public static void main(String[] args) { List<Number> nums = new ArrayList<>(); addInts(nums); System.out.println(nums); } }\n```\n```\n",tech:"java",displayNumber:131,sortKey:131},{id:"java-132",number:132,title:"What does PECS mean?",question:"What does PECS mean?",difficulty:"Intermediate",category:"Collections & Generics",answer:"PECS means Producer Extends, Consumer Super. If a parameter produces values for your method to read, use ? extends T. If it consumes values your method writes, use ? super T. If both reading and writing exact type values are needed, use a concrete generic type such as List<T>.\n\n\n```java\nimport java.util.*;\nclass Demo { static <T> void copy(List<? extends T> src, List<? super T> dst) { for (T x : src) dst.add(x); } public static void main(String[] args) { List<Number> out = new ArrayList<>(); copy(List.of(1,2), out); System.out.println(out); } }\n```\n```\n",tech:"java",displayNumber:132,sortKey:132},{id:"java-133",number:133,title:"What is type erasure?",question:"What is type erasure?",difficulty:"Advanced",category:"Collections & Generics",answer:"Java implements generics mostly by type erasure. Generic type information is checked at compile time and erased to raw types or bounds in bytecode. This preserves backward compatibility but means you cannot directly create new T(), use primitive type arguments, or check instanceof List<String>.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> a = new ArrayList<>(); List<Integer> b = new ArrayList<>(); System.out.println(a.getClass() == b.getClass()); } }\n```\n```\n",tech:"java",displayNumber:133,sortKey:133},{id:"java-134",number:134,title:"What are raw types and why avoid them?",question:"What are raw types and why avoid them?",difficulty:"Intermediate",category:"Collections & Generics",answer:'A raw type is a generic type used without type arguments, such as List instead of List<String>. Raw types disable generic type checking and can cause ClassCastException later. They exist for backward compatibility with pre-generics code; modern code should avoid them except at legacy boundaries.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> safe = new ArrayList<>(); safe.add("x"); System.out.println(safe.get(0)); } }\n```\n```\n',tech:"java",displayNumber:134,sortKey:134},{id:"java-135",number:135,title:"What is a generic method?",question:"What is a generic method?",difficulty:"Basic",category:"Collections & Generics",answer:'A generic method declares its own type parameter before the return type. It can infer types from arguments and is useful when the type relationship belongs to one method rather than the whole class.\n\n\n```java\nclass Demo { static <T> T first(T a, T b) { return a; } public static void main(String[] args) { String s = first("a", "b"); System.out.println(s); } }\n```\n```\n',tech:"java",displayNumber:135,sortKey:135},{id:"java-136",number:136,title:"How are ConcurrentHashMap iterators different?",question:"How are ConcurrentHashMap iterators different?",difficulty:"Advanced",category:"Collections & Generics",answer:'ConcurrentHashMap iterators are weakly consistent. They do not throw ConcurrentModificationException and may reflect some, all, or none of the updates made after iterator creation. This is useful for concurrent monitoring and traversal, but not for exact snapshots.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> m = new ConcurrentHashMap<>(); m.put("a",1); for (String k : m.keySet()) { m.put("b",2); System.out.println(k); } } }\n```\n```\n',tech:"java",displayNumber:136,sortKey:136},{id:"java-137",number:137,title:"How do atomic updates work in ConcurrentHashMap?",question:"How do atomic updates work in ConcurrentHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Use compute, computeIfAbsent, merge, or replace to perform per-key atomic updates. Avoid get-then-put because another thread can modify the value between calls. The mapping function should be short and should not depend on external locks.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> counts = new ConcurrentHashMap<>(); counts.merge("x", 1, Integer::sum); counts.merge("x", 1, Integer::sum); System.out.println(counts); } }\n```\n```\n',tech:"java",displayNumber:137,sortKey:137},{id:"java-138",number:138,title:"When is CopyOnWriteArrayList a good concurrent choice?",question:"When is CopyOnWriteArrayList a good concurrent choice?",difficulty:"Intermediate",category:"Collections & Generics",answer:'It is good when reads and iteration dominate and writes are rare. Iterators are snapshot-based, so readers avoid locking and are insulated from concurrent modifications. It is bad for high write volume because every mutation copies the array.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<String> xs = new CopyOnWriteArrayList<>(); xs.add("v1"); for (String x : xs) xs.add("v2"); System.out.println(xs); } }\n```\n```\n',tech:"java",displayNumber:138,sortKey:138},{id:"java-139",number:139,title:"What is ConcurrentLinkedQueue?",question:"What is ConcurrentLinkedQueue?",difficulty:"Intermediate",category:"Collections & Generics",answer:'ConcurrentLinkedQueue is an unbounded, non-blocking FIFO queue based on linked nodes. It is safe for multiple producers and consumers and uses lock-free algorithms. poll returns null when empty; use BlockingQueue if consumers should wait.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentLinkedQueue<String> q = new ConcurrentLinkedQueue<>(); q.offer("a"); System.out.println(q.poll()); } }\n```\n```\n',tech:"java",displayNumber:139,sortKey:139},{id:"java-140",number:140,title:"What is ConcurrentSkipListMap?",question:"What is ConcurrentSkipListMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'ConcurrentSkipListMap is a concurrent, sorted NavigableMap implemented with a skip-list. It gives expected O(log n) operations and supports range queries under concurrent access. Use it when you need both ordering and concurrency; otherwise ConcurrentHashMap is usually faster.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentSkipListMap<Integer,String> m = new ConcurrentSkipListMap<>(); m.put(2,"b"); m.put(1,"a"); System.out.println(m.firstKey()); } }\n```\n```\n',tech:"java",displayNumber:140,sortKey:140},{id:"java-141",number:141,title:"What is ConcurrentSkipListSet?",question:"What is ConcurrentSkipListSet?",difficulty:"Advanced",category:"Collections & Generics",answer:"ConcurrentSkipListSet is a concurrent sorted set backed by ConcurrentSkipListMap. It supports NavigableSet operations such as ceiling and subSet while allowing concurrent access. It is useful for sorted registries, time windows, and leaderboards with concurrent updates.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentSkipListSet<Integer> s = new ConcurrentSkipListSet<>(); s.add(10); s.add(5); System.out.println(s.ceiling(6)); } }\n```\n```\n",tech:"java",displayNumber:141,sortKey:141},{id:"java-142",number:142,title:"When should you choose blocking vs non-blocking concurrent collections?",question:"When should you choose blocking vs non-blocking concurrent collections?",difficulty:"Advanced",category:"Collections & Generics",answer:'Choose BlockingQueue when threads should wait for work or backpressure. Choose non-blocking collections such as ConcurrentLinkedQueue when threads should keep running and handle empty results themselves. Blocking designs are simpler for producer-consumer pipelines; non-blocking designs can fit event loops and low-latency systems.\n\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { BlockingQueue<String> blocking = new LinkedBlockingQueue<>(10); ConcurrentLinkedQueue<String> nonBlocking = new ConcurrentLinkedQueue<>(); System.out.println(blocking.offer("a") && nonBlocking.offer("b")); } }\n```\n```\n',tech:"java",displayNumber:142,sortKey:142},{id:"java-143",number:143,title:"Vector vs ArrayList: which should modern code use?",question:"Vector vs ArrayList: which should modern code use?",difficulty:"Basic",category:"Collections & Generics",answer:'Use ArrayList for normal single-threaded lists. Vector synchronizes individual methods, which adds overhead and does not automatically make compound workflows correct. If sharing across threads, choose a collection designed for the access pattern or synchronize externally around the full operation.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> list = new ArrayList<>(); list.add("modern"); System.out.println(list); } }\n```\n```\n',tech:"java",displayNumber:143,sortKey:143},{id:"java-144",number:144,title:"Hashtable vs HashMap vs ConcurrentHashMap?",question:"Hashtable vs HashMap vs ConcurrentHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Hashtable is legacy, synchronized, and rejects null. HashMap is unsynchronized and permits null. ConcurrentHashMap is designed for concurrent access, rejects null, and scales better than synchronizing an entire map. Pick HashMap by default; pick ConcurrentHashMap for shared mutable maps.\n\n\n```java\nimport java.util.*;\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> local = new HashMap<>(); Map<String,Integer> shared = new ConcurrentHashMap<>(); local.put("a",1); shared.put("b",2); System.out.println(local.size()+shared.size()); } }\n```\n```\n',tech:"java",displayNumber:144,sortKey:144},{id:"java-145",number:145,title:"What do Collections.synchronizedXxx wrappers do?",question:"What do Collections.synchronizedXxx wrappers do?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collections.synchronizedList, synchronizedSet, and synchronizedMap return wrappers that synchronize each method on a common lock. Iteration still requires manually synchronizing on the wrapper during the whole traversal. These wrappers are useful for simple compatibility but concurrent collections are often better.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> list = Collections.synchronizedList(new ArrayList<>()); list.add("x"); synchronized (list) { for (String s : list) System.out.println(s); } } }\n```\n```\n',tech:"java",displayNumber:145,sortKey:145},{id:"java-146",number:146,title:"What is the difference between fail-fast and fail-safe terminology?",question:"What is the difference between fail-fast and fail-safe terminology?",difficulty:"Intermediate",category:"Collections & Generics",answer:"Java documentation usually says fail-fast for ordinary iterators and weakly consistent or snapshot for concurrent iterators. Fail-safe is an informal term, not a precise guarantee. CopyOnWriteArrayList iterates over a snapshot; ConcurrentHashMap iterators are weakly consistent.\n\n\n```java\nimport java.util.*;\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<Integer> xs = new CopyOnWriteArrayList<>(List.of(1)); for (int x : xs) xs.add(2); System.out.println(xs); } }\n```\n```\n",tech:"java",displayNumber:146,sortKey:146},{id:"java-147",number:147,title:"How do streams integrate with collections?",question:"How do streams integrate with collections?",difficulty:"Basic",category:"Collections & Generics",answer:'Collections expose stream() and parallelStream() for functional-style processing. Streams do not store data; they process elements from the source through intermediate operations and a terminal operation. Avoid mutating the source while streaming unless the source is concurrent and the operation is designed for it.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = List.of("Ana", "Bob"); List<String> upper = names.stream().map(String::toUpperCase).toList(); System.out.println(upper); } }\n```\n```\n',tech:"java",displayNumber:147,sortKey:147},{id:"java-148",number:148,title:"How do Collectors create collections?",question:"How do Collectors create collections?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collectors turn stream results into lists, sets, maps, groups, or custom collections. toList does not promise a mutable list in modern Java Stream.toList; Collectors.toCollection lets you choose the exact implementation when mutability or ordering matters.\n\n\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Demo { public static void main(String[] args) { Set<String> set = Stream.of("b","a","b").collect(Collectors.toCollection(LinkedHashSet::new)); System.out.println(set); } }\n```\n```\n',tech:"java",displayNumber:148,sortKey:148},{id:"java-149",number:149,title:"How do you collect a stream into a Map safely?",question:"How do you collect a stream into a Map safely?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collectors.toMap needs key mapper, value mapper, and often a merge function for duplicate keys. Without a merge function, duplicate keys throw IllegalStateException. Provide a map supplier when the result order or implementation matters.\n\n\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Demo { public static void main(String[] args) { Map<Character,String> m = Stream.of("ape","ant").collect(Collectors.toMap(s -> s.charAt(0), s -> s, (a,b) -> a + "," + b, LinkedHashMap::new)); System.out.println(m); } }\n```\n```\n',tech:"java",displayNumber:149,sortKey:149},{id:"java-150",number:150,title:"What immutable collection options exist in modern Java?",question:"What immutable collection options exist in modern Java?",difficulty:"Basic",category:"Collections & Generics",answer:'Modern Java provides List.of, Set.of, Map.of, copyOf methods, and stream terminal toList for unmodifiable results. These collections reject mutation and usually reject nulls. Use them for constants, defensive copies, and API return values that callers should not modify.\n\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = List.of("a", "b"); Set<String> ys = Set.copyOf(xs); Map<String,Integer> zs = Map.of("size", xs.size()); System.out.println(ys + " " + zs); } }\n```\n```\n',tech:"java",displayNumber:150,sortKey:150},{id:"java-151",number:151,title:"What is a Thread in Java and how does it differ from a Process?",question:"What is a Thread in Java and how does it differ from a Process?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:`A thread is the smallest unit of execution scheduled by the operating system, living inside a process. A process is an independent program with its own memory space (heap, code, data), whereas threads within the same process share the heap and method area but have private program counters, stacks, and local variables. Threads are lightweight: creating/spawning a thread consumes far fewer resources than a process, and inter-thread communication is cheap (shared memory) but requires synchronization. A JVM runs as a single process with at least one main thread plus background threads (GC, finalizer, etc.).


\`\`\`java
// Threads share the heap but have isolated stacks.
Thread t = Thread.currentThread();
System.out.println("Name: " + t.getName());
System.out.println("Priority: " + t.getPriority());
System.out.println("State: " + t.getState());
System.out.println("Is daemon: " + t.isDaemon());
\`\`\`
\`\`\`
`,tech:"java",displayNumber:151,sortKey:151},{id:"java-152",number:152,title:"What are the different ways to create a Thread in Java?",question:"What are the different ways to create a Thread in Java?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'Two classic approaches: (1) extend `Thread` and override `run()`, then call `start()`; (2) implement `Runnable` and pass it to a `Thread` constructor. The `Runnable` approach is preferred because it decouples the task from the threading mechanism and lets the class extend another class. Java 8+ lets you use lambda expressions for `Runnable`. A third, modern approach is `Callable` (returns a value, can throw checked exceptions) executed via an `ExecutorService`. Always call `start()`, not `run()` — calling `run()` executes synchronously on the current thread.\n\n\n```java\n// 1. Extending Thread\nclass MyThread extends Thread {\n    public void run() { System.out.println("extends Thread"); }\n}\nnew MyThread().start();\n\n// 2. Implementing Runnable (preferred)\nRunnable r = () -> System.out.println("implements Runnable");\nnew Thread(r).start();\n\n// 3. Callable via ExecutorService (covered later)\n```\n```\n',tech:"java",displayNumber:152,sortKey:152},{id:"java-153",number:153,title:"What is the difference between calling `start()` and `run()`?",question:"What is the difference between calling `start()` and `run()`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'`start()` registers the thread with the JVM/OS scheduler and causes a new OS thread to be spawned; the scheduler then invokes `run()` asynchronously on that new thread. Calling `run()` directly simply executes the method body on the *current* thread, with no new thread created. So `run()` called directly is plain sequential code, while `start()` is the only way to achieve true concurrency. A thread\'s `start()` can be called only once — a second call throws `IllegalThreadStateException`.\n\n\n```java\nRunnable r = () -> System.out.println("Running in: " + Thread.currentThread().getName());\nThread t = new Thread(r);\nt.run();   // prints "Running in: main" — no new thread\nt.start(); // prints "Running in: Thread-0" — new thread (throws if called twice)\n```\n```\n',tech:"java",displayNumber:153,sortKey:153},{id:"java-154",number:154,title:"Explain the life cycle / states of a Thread.",question:"Explain the life cycle / states of a Thread.",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'A Java thread moves through states defined in `Thread.State`: `NEW` (created, not started), `RUNNABLE` (ready to run or running — JVM does not distinguish), `BLOCKED` (waiting for a monitor lock), `WAITING` (waiting indefinitely via `wait()`, `join()`, `park()`), `TIMED_WAITING` (waiting with a timeout via `sleep()`, `wait(timeout)`, `join(timeout)`), and `TERMINATED` (completed). Note that OS "running" vs "ready" is collapsed into `RUNNABLE`; the `BLOCKED` state is specifically for synchronized-lock contention, not for waiting on conditions.\n\n\n```java\nThread t = new Thread(() -> {});\nSystem.out.println(t.getState()); // NEW\nt.start();\nSystem.out.println(t.getState()); // RUNNABLE (or TERMINATED if already done)\n// BLOCKED: waiting for a synchronized lock\n// WAITING: Object.wait() / Thread.join() / LockSupport.park()\n// TIMED_WAITING: Thread.sleep() / wait(timeout)\n```\n```\n',tech:"java",displayNumber:154,sortKey:154},{id:"java-155",number:155,title:"What is the difference between `Runnable` and `Callable`?",question:"What is the difference between `Runnable` and `Callable`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'Both represent tasks, but `Runnable.run()` returns `void` and cannot throw checked exceptions, while `Callable.call()` returns a result of type `V` and may throw checked exceptions. `Callable` is designed for use with `ExecutorService`/`Future`/`CompletableFuture` where you need a result or need to propagate exceptions. `Runnable` is simpler, used when no result is needed (fire-and-forget). To run a `Callable`, you submit it to an `ExecutorService` which wraps it in a `FutureTask`.\n\n\n```java\nRunnable r = () -> System.out.println("no result");\nCallable<Integer> c = () -> { return 42; };\n\nExecutorService es = Executors.newSingleThreadExecutor();\nFuture<?> f1 = es.submit(r);        // Future<?> — no useful result\nFuture<Integer> f2 = es.submit(c);  // Future<Integer> — f2.get() returns 42\nes.shutdown();\n```\n```\n',tech:"java",displayNumber:155,sortKey:155},{id:"java-156",number:156,title:"What is the purpose of the `volatile` keyword?",question:"What is the purpose of the `volatile` keyword?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`volatile` guarantees **visibility**: a write to a `volatile` variable happens-before every subsequent read of that variable by any thread, ensuring all threads see the latest value without caching in thread-local registers. It also prevents compiler/JIT reordering of instructions around the volatile access. However, `volatile` does **not** provide atomicity for compound operations (e.g., `volatile int i; i++` is still a race). Use `AtomicInteger` or `synchronized` for atomic compound actions. `volatile` is suitable for simple flags, status indicators, or safely publishing an object reference (if the object is immutable after construction).\n\n\n```java\nclass Flag { volatile boolean stopped = false; }\nFlag f = new Flag();\nnew Thread(() -> { while (!f.stopped) {} }).start();\nf.stopped = true; // visible to the loop thread immediately\n\n// volatile prevents reordering of this write with the construction of Helper\nclass Helper { int n = 1; }\nclass Publisher { volatile Helper h; }\n```\n```\n",tech:"java",displayNumber:156,sortKey:156},{id:"java-157",number:157,title:"What is the difference between `synchronized` and `volatile`?",question:"What is the difference between `synchronized` and `volatile`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`synchronized` provides both **mutual exclusion** (only one thread holds the monitor at a time) and **visibility** (a thread exiting a synchronized block flushes writes, and entering one reloads them). `volatile` provides **visibility only**, with no mutual exclusion — multiple threads can read/write concurrently, so compound actions still race. Use `synchronized` when you need to make a *block* of operations atomic; use `volatile` for single-variable status flags where no atomicity is needed. Overuse of `synchronized` hurts throughput; `volatile` is cheaper but limited.\n\n\n```java\n// volatile: safe for a single read/write flag\nvolatile boolean ready = false;\n\n// synchronized: makes the compound increment atomic + visible\nint count = 0;\nsynchronized void increment() { count++; } // atomic read-modify-write\n```\n```\n",tech:"java",displayNumber:157,sortKey:157},{id:"java-158",number:158,title:"What is a `synchronized` method and a `synchronized` block?",question:"What is a `synchronized` method and a `synchronized` block?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:`A \`synchronized\` method acquires the monitor (lock) of \`this\` for instance methods, or the Class object for static methods, for the method's duration. A \`synchronized\` block acquires the lock of an explicitly specified object only for the statements inside the block, allowing finer-grained locking and better concurrency. Prefer synchronized blocks with a private, dedicated lock object to avoid exposing the lock (e.g., locking on \`this\` or a public class can be hijacked by external code). The same monitor must be used by all threads that need to be mutually excluded.


\`\`\`java
// synchronized method (locks on 'this')
public synchronized void foo() { /* critical section */ }

// synchronized static method (locks on Class object)
public static synchronized void bar() { /* ... */ }

// synchronized block with dedicated lock (finer-grained, safer)
private final Object lock = new Object();
public void baz() {
    // non-critical work here (concurrent)
    synchronized (lock) {
        // critical section only
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:158,sortKey:158},{id:"java-159",number:159,title:"What is the difference between a static and an instance `synchronized` method?",question:"What is the difference between a static and an instance `synchronized` method?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"An instance `synchronized` method locks on the specific object instance (`this`), so two threads on *different* instances can run simultaneously. A static `synchronized` method locks on the `Class` object (`MyClass.class`), which is shared across all instances — so it serializes access for all instances of that class. They use different monitors and do **not** block each other: a thread in a static synchronized method and a thread in an instance synchronized method on the same instance can run concurrently. Choose based on whether the shared state being protected is per-instance or class-level.\n\n\n```java\nclass Counter {\n    private static int global = 0;\n    private int local = 0;\n\n    public static synchronized void incGlobal() { global++; } // locks Counter.class\n    public synchronized void incLocal() { local++; }           // locks 'this'\n}\n// Thread A: Counter.incGlobal()  -> locks Class\n// Thread B: new Counter().incLocal() -> locks instance; runs concurrently with A\n```\n```\n",tech:"java",displayNumber:159,sortKey:159},{id:"java-160",number:160,title:"What are atomic classes in `java.util.concurrent.atomic`?",question:"What are atomic classes in `java.util.concurrent.atomic`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Atomic classes (`AtomicInteger`, `AtomicLong`, `AtomicReference`, `AtomicBoolean`, and array/field updaters) provide lock-free, thread-safe operations on single variables using Compare-And-Swap (CAS) hardware instructions. They support atomic read-modify-write patterns like `incrementAndGet()`, `compareAndSet(expect, update)`, `getAndUpdate(fn)`, etc. They are faster than `synchronized` under low-to-moderate contention because they avoid kernel context switches, but can suffer under high contention due to CAS retry loops. `LongAdder`/`DoubleAdder` and `LongAccumulator`/`DoubleAccumulator` (Java 8) use striped cells for better high-contention scalability.\n\n\n```java\nAtomicInteger count = new AtomicInteger(0);\n// thread-safe increment without locks\nint newVal = count.incrementAndGet(); // returns updated value\n// CAS pattern\nboolean updated = count.compareAndSet(5, 10); // true if value was 5, now 10\n\n// For high-contention counters, prefer LongAdder (Java 8)\nimport java.util.concurrent.atomic.LongAdder;\nLongAdder adder = new LongAdder();\nadder.increment(); // no contention on single CAS\nlong sum = adder.sum();\n```\n```\n",tech:"java",displayNumber:160,sortKey:160},{id:"java-161",number:161,title:"How does `AtomicInteger` work internally?",question:"How does `AtomicInteger` work internally?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`\`AtomicInteger\` wraps a \`volatile int value\` and uses \`Unsafe.compareAndSwapInt\` (CAS) to perform atomic updates. CAS is a hardware instruction: it atomically checks if the current value equals an expected value, and if so, swaps it to the new value; otherwise it fails and the caller retries. The JVM intrinsic compiles this to a single CPU instruction (\`LOCK CMPXCHG\` on x86). Methods like \`incrementAndGet()\` loop: read current, compute next, CAS; on failure, retry. This is lock-free (no thread ever blocks indefinitely waiting for a lock), but not wait-free (a thread may retry many times under contention).


\`\`\`java
// Simplified conceptual implementation
public class AtomicInteger {
    private volatile int value;
    public final int getAndIncrement() {
        for (;;) {
            int current = value;
            int next = current + 1;
            if (compareAndSet(current, next)) return current;
        }
    }
    public final boolean compareAndSet(int expect, int update) {
        return unsafe.compareAndSwapInt(this, valueOffset, expect, update);
    }
}
// Under the hood: JVM intrinsic -> CPU LOCK CMPXCHG
\`\`\`
\`\`\`
`,tech:"java",displayNumber:161,sortKey:161},{id:"java-162",number:162,title:"What is ABA problem and how do you solve it?",question:"What is ABA problem and how do you solve it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"The ABA problem occurs in CAS-based algorithms when a value changes from A to B and back to A; a thread doing CAS(A→C) succeeds because it still sees A, unaware of the intermediate change. This corrupts algorithms that attach meaning to value identity (e.g., lock-free stack pop). Solutions: (1) `AtomicStampedReference` pairs the value with an integer stamp that increments on every update, so A→B→A becomes (A,0)→(B,1)→(A,2) — CAS also checks the stamp. (2) `AtomicMarkableReference` uses a boolean mark instead of a stamp. (3) Avoid mutable references — use immutable value objects so identity change implies value change.\n\n\n```java\nAtomicStampedReference<Integer> ref = new AtomicStampedReference<>(100, 0);\nint[] stamp = new int[1];\nInteger cur = ref.get(stamp);      // cur=100, stamp[0]=0\n// Another thread: 100 -> 200 -> 100 (stamp becomes 2)\nboolean ok = ref.compareAndSet(cur, 101, stamp[0], stamp[0] + 1);\n// CAS fails because stamp[0]=0 != expected 2, preventing the ABA bug.\n```\n```\n",tech:"java",displayNumber:162,sortKey:162},{id:"java-163",number:163,title:"What is `ReentrantLock` and how does it differ from `synchronized`?",question:"What is `ReentrantLock` and how does it differ from `synchronized`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ReentrantLock` is an explicit lock from `java.util.concurrent.locks` that can be acquired/released via `lock()`/`unlock()` (always in a `try-finally`). Advantages over `synchronized`: (1) **fairness** option (`new ReentrantLock(true)`) for FIFO ordering; (2) **try-lock with timeout** (`tryLock(timeout)`) to avoid deadlock; (3) **interruptible** locking (`lockInterruptibly()`); (4) `tryLock()` non-blocking attempt; (5) multiple `Condition` objects via `newCondition()` for fine-grained wait/notify. Downsides: you must remember to `unlock()` in `finally` (easy to leak), and it's more verbose. `synchronized` is simpler, auto-released on exceptions/block exit, and JVM-optimized.\n\n\n```java\nReentrantLock lock = new ReentrantLock(true); // fair\nlock.lock();\ntry {\n    // critical section\n} finally {\n    lock.unlock(); // MUST release, or lock leaks\n}\n\n// Differences from synchronized:\n// - lock.tryLock(1, TimeUnit.SECONDS) // timeout, avoids deadlock\n// - lock.lockInterruptibly()          // can be interrupted\n// - lock.newCondition()               // multiple wait sets\n```\n```\n",tech:"java",displayNumber:163,sortKey:163},{id:"java-164",number:164,title:'What does "reentrant" mean in `ReentrantLock`?',question:'What does "reentrant" mean in `ReentrantLock`?',difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Reentrant means a thread that already holds the lock can acquire it again without deadlocking on itself; the lock keeps a **hold count** that increments on each re-acquisition and decrements on each `unlock()`. The lock is fully released only when the count returns to zero. This lets synchronized methods call each other and nested `synchronized` blocks on the same monitor work. `synchronized` monitors are also reentrant. If a thread not holding the lock calls `unlock()`, `ReentrantLock` throws `IllegalMonitorStateException`.\n\n\n```java\nReentrantLock lock = new ReentrantLock();\nlock.lock();\ntry {\n    lock.lock(); // re-acquire: hold count -> 2 (allowed)\n    try {\n        // nested critical section\n    } finally {\n        lock.unlock(); // count -> 1\n    }\n} finally {\n    lock.unlock(); // count -> 0, fully released\n}\n```\n```\n",tech:"java",displayNumber:164,sortKey:164},{id:"java-165",number:165,title:"What is a `ReadWriteLock`?",question:"What is a `ReadWriteLock`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:`\`ReadWriteLock\` (\`ReentrantReadWriteLock\` is the standard impl) maintains a pair of associated locks: a read lock (shared) and a write lock (exclusive). Multiple threads can hold the read lock simultaneously, but the write lock is exclusive — no reads or other writes occur while a write is held. This boosts concurrency for read-heavy data structures (caches, configuration) where writes are rare. Caveats: by default it is unfair and can starve writers; prefer fairness or \`ReentrantReadWriteLock(true)\` for write-priority if needed. The write lock is reentrant, and it can be downgraded to a read lock but not upgraded.


\`\`\`java
ReadWriteLock rw = new ReentrantReadWriteLock();
Map<String, String> cache = new HashMap<>();

// Read - many threads allowed concurrently
rw.readLock().lock();
try { return cache.get(key); } finally { rw.readLock().unlock(); }

// Write - exclusive
rw.writeLock().lock();
try { cache.put(key, value); } finally { rw.writeLock().unlock(); }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:165,sortKey:165},{id:"java-166",number:166,title:"What is a `Semaphore`?",question:"What is a `Semaphore`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"A `Semaphore` maintains a set of permits. Threads call `acquire()` to take a permit (blocking if none available) and `release()` to return one. It controls access to a resource pool of fixed size, not mutual exclusion per se. A `Semaphore` with 1 permit acts like a mutex (binary semaphore); with N permits it bounds concurrency (e.g., limit to 10 simultaneous connections). Unlike a lock, a semaphore has no notion of ownership — any thread may `release()` a permit even if it didn't `acquire()` it. Constructors support fairness; `tryAcquire()` offers non-blocking/timeout variants.\n\n\n```java\nSemaphore sem = new Semaphore(3); // max 3 concurrent accesses\nvoid access() throws InterruptedException {\n    sem.acquire();\n    try {\n        // use limited resource\n    } finally {\n        sem.release(); // always release\n    }\n}\n// Binary semaphore (1 permit) ~ mutex\nSemaphore mutex = new Semaphore(1);\n```\n```\n",tech:"java",displayNumber:166,sortKey:166},{id:"java-167",number:167,title:"What is a `CountDownLatch`?",question:"What is a `CountDownLatch`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`CountDownLatch` is a one-shot synchronizer initialized with a count. Threads call `countDown()` to decrement the counter; other threads call `await()` to block until the count reaches zero (or a timeout elapses). It is **not reusable** — once it hits zero, all subsequent `await()` calls return immediately and it cannot be reset. Typical uses: (1) start gate where N worker threads wait for a signal to begin together; (2) end gate where a master thread waits for N tasks to finish. For a reusable barrier, use `CyclicBarrier`.\n\n\n```java\nint n = 5;\nCountDownLatch start = new CountDownLatch(1); // start gate\nCountDownLatch done  = new CountDownLatch(n); // end gate\n\nfor (int i = 0; i < n; i++) {\n    new Thread(() -> {\n        try { start.await(); /* wait for go */ runTask(); }\n        finally { done.countDown(); }\n    }).start();\n}\nstart.countDown(); // release all workers\ndone.await();      // wait for all to finish (one-shot, not reusable)\n```\n```\n",tech:"java",displayNumber:167,sortKey:167},{id:"java-168",number:168,title:"What is a `CyclicBarrier`?",question:"What is a `CyclicBarrier`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CyclicBarrier` is a reusable barrier where a fixed number of threads call `await()` and all block until the last one arrives; then all are released simultaneously, and the barrier resets for the next cycle. It supports an optional barrier action (`Runnable`) executed once per cycle by the last arriving thread — useful for merging partial results. Unlike `CountDownLatch`, it is reusable, but a broken barrier (thread times out or is interrupted) throws `BrokenBarrierException` for all waiting threads, requiring barrier.reset() to reuse. Use for phased computations (map-reduce, game loop steps).\n\n\n```java\n// 4 threads, barrier action runs after each phase\nCyclicBarrier barrier = new CyclicBarrier(4, () -> System.out.println("All done, next phase"));\nfor (int i = 0; i < 4; i++) {\n    new Thread(() -> {\n        for (int phase = 0; phase < 3; phase++) {\n            doPhaseWork();\n            try { barrier.await(); } // all wait, then proceed\n            catch (InterruptedException | BrokenBarrierException e) { Thread.currentThread().interrupt(); }\n        }\n    }).start();\n}\n```\n```\n',tech:"java",displayNumber:168,sortKey:168},{id:"java-169",number:169,title:"What is the difference between `CountDownLatch` and `CyclicBarrier`?",question:"What is the difference between `CountDownLatch` and `CyclicBarrier`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Key differences: (1) **Reusability**: `CountDownLatch` is one-shot; `CyclicBarrier` resets automatically after each trip. (2) **Waiting style**: Latch has two roles — threads call `countDown()` (producers) and others call `await()` (consumers). Barrier: all threads call `await()` symmetrically. (3) **Barrier action**: `CyclicBarrier` supports an optional `Runnable` executed per cycle; `CountDownLatch` does not. (4) **Failure modes**: `CyclicBarrier.await()` can throw `BrokenBarrierException` if a thread interrupts or times out, invalidating the barrier for all; `CountDownLatch` never breaks — threads just continue once count hits zero.\n\n\n```java\n// Use CountDownLatch when: one group signals completion, another waits (fire-once)\n// Use CyclicBarrier when: all threads must meet repeatedly (phased algorithms)\n```\n```\n",tech:"java",displayNumber:169,sortKey:169},{id:"java-170",number:170,title:"What is an `ExecutorService` and why would you use it instead of raw threads?",question:"What is an `ExecutorService` and why would you use it instead of raw threads?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ExecutorService` is an interface in `java.util.concurrent` that abstracts thread management: you submit `Runnable`/`Callable` tasks and it schedules them on a managed pool of worker threads. Benefits over creating threads manually: (1) thread reuse avoids the high cost of creating/destroying threads per task; (2) bounded pools prevent resource exhaustion; (3) it returns `Future` results for `Callable`; (4) rich lifecycle control (`shutdown()`/`shutdownNow()`); (5) built-in scheduling (`ScheduledExecutorService`). Raw `new Thread()` per task scales poorly and leaks resources under load. Always prefer an executor for production code.\n\n\n```java\nExecutorService pool = Executors.newFixedThreadPool(10);\nFuture<Integer> f = pool.submit(() -> compute());\nInteger result = f.get(); // blocks for result\npool.shutdown();          // no new tasks, finish submitted ones\npool.awaitTermination(1, TimeUnit.MINUTES);\n```\n```\n",tech:"java",displayNumber:170,sortKey:170},{id:"java-171",number:171,title:"What are the common factory methods in `Executors`?",question:"What are the common factory methods in `Executors`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`Executors` provides static factory methods for common executor configurations:\n- `newFixedThreadPool(n)` — exactly `n` threads; excess tasks queue unbounded (OOM risk).\n- `newCachedThreadPool()` — creates threads on demand, reuses idle ones (60s keep-alive); unbounded pool size (OOM risk under burst).\n- `newSingleThreadExecutor()` — one thread, FIFO queue; guarantees sequential execution.\n- `newWorkStealingPool()` — `ForkJoinPool` for parallel tasks (uses all processors).\n- `newScheduledThreadPool(n)` — for delayed/periodic tasks.\nPrefer `ThreadPoolExecutor` directly for production to control queue bounds and rejection policies.\n\n\n```java\nExecutorService fixed = Executors.newFixedThreadPool(4);\nExecutorService cached = Executors.newCachedThreadPool();\nExecutorService single = Executors.newSingleThreadExecutor();\nScheduledExecutorService scheduled = Executors.newScheduledThreadPool(2);\n// WARNING: Fixed & Single use unbounded LinkedBlockingQueue — can OOM.\n// Cached creates unlimited threads — can OOM.\n// In production: use ThreadPoolExecutor with bounded queue.\n```\n```\n",tech:"java",displayNumber:171,sortKey:171},{id:"java-172",number:172,title:"Explain the `ThreadPoolExecutor` parameters.",question:"Explain the `ThreadPoolExecutor` parameters.",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ThreadPoolExecutor(corePoolSize, maxPoolSize, keepAliveTime, unit, workQueue, threadFactory, handler)`:\n- **corePoolSize**: target number of always-kept-alive threads (even idle).\n- **maxPoolSize**: upper bound on threads when queue is full.\n- **keepAliveTime/unit**: idle timeout for *non-core* threads before termination.\n- **workQueue**: `BlockingQueue` for tasks when all core threads busy (`SynchronousQueue` bypasses queuing; `LinkedBlockingQueue` is unbounded; `ArrayBlockingQueue` is bounded).\n- **threadFactory**: creates threads (naming, daemon, priority).\n- **handler** (RejectedExecutionHandler): what happens when queue is full and pool maxed: `AbortPolicy` (throws, default), `CallerRunsPolicy` (caller runs task), `DiscardPolicy`, `DiscardOldestPolicy`.\nBehavior: tasks use core threads → then queue → then grow to max → then reject.\n\n\n```java\nThreadPoolExecutor ex = new ThreadPoolExecutor(\n    4,                       // corePoolSize\n    16,                      // maxPoolSize\n    60, TimeUnit.SECONDS,    // keepAlive for non-core\n    new ArrayBlockingQueue<>(100), // bounded queue\n    Executors.defaultThreadFactory(),\n    new ThreadPoolExecutor.CallerRunsPolicy()); // backpressure\n```\n```\n",tech:"java",displayNumber:172,sortKey:172},{id:"java-173",number:173,title:"What is a `Future`?",question:"What is a `Future`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"A `Future<V>` represents the result of an asynchronous computation. After submitting a `Callable`/`Runnable` to an `ExecutorService`, you get a `Future` to: `get()` (blocks until done, possibly throwing `ExecutionException` wrapping the task's exception), `get(timeout, unit)` (throws `TimeoutException`), `cancel(mayInterruptIfRunning)`, `isDone()`, and `isCancelled()`. The base `Future` is limited: blocking `get()`, no chaining, no combining. `CompletableFuture` (covered later) addresses these gaps.\n\n\n```java\nExecutorService es = Executors.newSingleThreadExecutor();\nFuture<String> f = es.submit(() -> \"result\");\ntry {\n    String r = f.get(5, TimeUnit.SECONDS); // blocks, with timeout\n} catch (TimeoutException e) {\n    f.cancel(true); // attempt cancellation\n} catch (ExecutionException e) {\n    Throwable cause = e.getCause(); // the task's real exception\n}\n```\n```\n",tech:"java",displayNumber:173,sortKey:173},{id:"java-174",number:174,title:"What is `CompletableFuture`?",question:"What is `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletableFuture` (Java 8) extends `Future` and implements `CompletionStage`, providing a fluent, non-blocking API for asynchronous programming. You can chain dependent stages (`thenApply`, `thenAccept`, `thenRun`), combine independent futures (`thenCombine`, `thenCompose`, `allOf`, `anyOf`), handle exceptions (`exceptionally`, `handle`), and complete manually (`complete`, `completeExceptionally`). It runs callbacks on the thread that completes the stage (or a supplied executor), and supports both `Runnable` (void) and functional (value) chains. It replaces callback hell and `Future.get()` blocking.\n\n\n```java\nCompletableFuture<String> f = CompletableFuture.supplyAsync(() -> fetchData())\n    .thenApply(data -> transform(data))\n    .thenAccept(System.out::println)\n    .exceptionally(ex -> { log(ex); return "fallback"; });\n\n// Combine two independent futures\nCompletableFuture<Integer> a = CompletableFuture.supplyAsync(() -> 1);\nCompletableFuture<Integer> b = CompletableFuture.supplyAsync(() -> 2);\nCompletableFuture<Integer> sum = a.thenCombine(b, (x, y) -> x + y);\n// sum.get() -> 3 (non-blocking composition)\n\n// allOf / anyOf for multiple futures\nCompletableFuture.allOf(f1, f2, f3).join();\n```\n```\n',tech:"java",displayNumber:174,sortKey:174},{id:"java-175",number:175,title:"How does `thenApply`, `thenAccept`, `thenRun` differ?",question:"How does `thenApply`, `thenAccept`, `thenRun` differ?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'All three chain an action after a stage completes, but differ in input/output:\n- `thenApply(Function<T,R>)` — consumes the result, returns a transformed value (maps `T` → `R`).\n- `thenAccept(Consumer<T>)` — consumes the result, returns nothing (`CompletableFuture<Void>`); terminal side-effect.\n- `thenRun(Runnable)` — ignores the result, just runs after completion; for pure side-effects like logging or cleanup.\nThere are also async variants (`thenApplyAsync`, etc.) that run on a supplied or `ForkJoinPool.commonPool()` executor rather than the completing thread.\n\n\n```java\nCompletableFuture.supplyAsync(() -> "abc")\n    .thenApply(s -> s.toUpperCase())  // Function: "ABC"\n    .thenAccept(s -> System.out.println(s)) // Consumer: prints ABC\n    .thenRun(() -> System.out.println("done")); // Runnable: prints done\n```\n```\n',tech:"java",displayNumber:175,sortKey:175},{id:"java-176",number:176,title:"What is the difference between `thenCompose` and `thenCombine`?",question:"What is the difference between `thenCompose` and `thenCombine`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`thenCompose(Function<T, CompletionStage<U>>)` flattens a dependent future — the result of the first stage is fed into a function that *returns another future*, yielding a single flat future (`Future<U>`). It's like `flatMap` for futures (sequential dependency: B needs A's result).\n`thenCombine(CompletionStage<U>, BiFunction<T,U,V>)` joins two *independent* futures and combines their results when both complete; the two futures run in parallel and combine (`Future<V>`). It's like `zip`/`map2`.\nUse `thenCompose` for chaining dependent steps; `thenCombine`/`thenAcceptBoth` for merging concurrent results.\n\n\n```java\n// thenCompose: sequential dependency (flatMap)\nCompletableFuture<User> f = fetchUserId()\n    .thenCompose(id -> fetchUser(id));   // Future<Id> -> Future<User>\n\n// thenCombine: parallel, merge results\nCompletableFuture<Double> price = fetchPrice();\nCompletableFuture<Integer> qty = fetchQty();\nCompletableFuture<Double> total = price.thenCombine(qty, (p, q) -> p * q);\n```\n```\n",tech:"java",displayNumber:176,sortKey:176},{id:"java-177",number:177,title:"What is deadlock and how do you prevent it?",question:"What is deadlock and how do you prevent it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Deadlock occurs when two or more threads are blocked forever, each waiting for a lock held by another. Four Coffman conditions must hold simultaneously: mutual exclusion, hold-and-wait, no preemption, circular wait. Prevention strategies: (1) **Lock ordering** — always acquire locks in a consistent global order; (2) **Timeout-based locks** — use `tryLock(timeout)` to avoid indefinite wait; (3) **Avoid nested locks** — design to hold at most one lock; (4) **Lock coarsening/granularity** — sometimes fewer, broader locks reduce nesting; (5) **Detect & recover** — JVM has no built-in deadlock detection, but `jstack` / `ThreadMXBean` can diagnose; design for `LockSupport.parkNanos` instead of indefinite blocking.\n\n\n```java\n// Deadlock example\nObject a = new Object(), b = new Object();\nThread t1 = new Thread(() -> { synchronized(a) { synchronized(b) {} } });\nThread t2 = new Thread(() -> { synchronized(b) { synchronized(a) {} } });\n// t1 holds a, wants b; t2 holds b, wants a -> deadlock\n\n// Prevention: consistent lock ordering\n// Always lock a before b (or compare hash codes for arbitrary objects)\nsynchronized (a) { synchronized (b) { } }\n// Both threads use same order -> no circular wait\n```\n```\n",tech:"java",displayNumber:177,sortKey:177},{id:"java-178",number:178,title:"What is the difference between deadlock, livelock, and starvation?",question:"What is the difference between deadlock, livelock, and starvation?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:`- **Deadlock**: threads are permanently blocked waiting on each other's locks; none make progress and none are "active" (they're parked/blocked).
- **Livelock**: threads are *active* and keep responding to each other but make no real progress — like two people stepping aside in the same direction repeatedly. Often from over-eager retry/back-off logic where each thread gives up its lock to avoid deadlock and immediately retries.
- **Starvation**: a thread is perpetually denied access to a resource because higher-priority or luckier threads always win (e.g., unfair lock, \`synchronized\` favoring some threads). The thread is runnable but never scheduled. Fair locks and \`ReentrantLock(true)\` mitigate starvation.


\`\`\`java
// Livelock: both yield endlessly
while (conflict) { /* give up resource */ Thread.yield(); } // neither progresses

// Starvation example: many high-priority writers starve a low-priority reader
// under an unfair lock. Fix: ReentrantLock(true) or priority adjustment.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:178,sortKey:178},{id:"java-179",number:179,title:"What is `wait()`, `notify()`, and `notifyAll()` and how are they used?",question:"What is `wait()`, `notify()`, and `notifyAll()` and how are they used?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"These are `Object` methods for inter-thread coordination and **must be called from within a `synchronized` block or method on the same monitor**. `wait()` releases the monitor and blocks the thread until another thread calls `notify()`/`notifyAll()` on the same object. `notify()` wakes one (arbitrary) waiting thread; `notifyAll()` wakes all. Always call `wait()` in a **loop** checking the condition (guards against spurious wakeups and missed signals). Prefer `notifyAll()` unless you have a single specific waiter. Modern code usually prefers `Condition`, `BlockingQueue`, or higher-level concurrent collections instead.\n\n\n```java\nfinal Object lock = new Object();\nboolean ready = false;\n\n// Consumer\nsynchronized (lock) {\n    while (!ready) lock.wait(); // ALWAYS loop, not if\n    // proceed\n}\n\n// Producer\nsynchronized (lock) {\n    ready = true;\n    lock.notifyAll(); // wake all waiters\n}\n```\n```\n",tech:"java",displayNumber:179,sortKey:179},{id:"java-180",number:180,title:"What is a `Condition` and how does it differ from `wait/notify`?",question:"What is a `Condition` and how does it differ from `wait/notify`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`Condition` (from `Lock.newCondition()`) is the `Lock` API\'s analog of `Object.wait/notify`. Differences: (1) a `Lock` can have multiple `Condition` objects (separate wait-sets) for different predicates — e.g., `notEmpty` and `notFull` for a bounded buffer — instead of one shared monitor wait-set; (2) `Condition` methods are `await()`, `signal()`, `signalAll()` (clearer names); (3) `await()` throws `InterruptedException` which you must handle; (4) `Condition` requires explicit `Lock` ownership, making locking discipline visible. Use `Condition` when you need multiple wait queues per lock.\n\n\n```java\nReentrantLock lock = new ReentrantLock();\nCondition notEmpty = lock.newCondition();\nCondition notFull  = lock.newCondition();\n\nvoid put(E e) {\n    lock.lock();\n    try {\n        while (full) notFull.await();\n        add(e);\n        notEmpty.signal();\n    } finally { lock.unlock(); }\n}\n// wait-set per condition avoids "signal to wrong thread" issue with notify()\n```\n```\n',tech:"java",displayNumber:180,sortKey:180},{id:"java-181",number:181,title:"What is the `ForkJoinPool` and `ForkJoinTask`?",question:"What is the `ForkJoinPool` and `ForkJoinTask`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ForkJoinPool` (Java 7) is an `ExecutorService` specialized for divide-and-conquer tasks that **fork** (split into subtasks) and **join** (wait for results). Its worker threads use **work-stealing**: each worker has its own deque; idle workers steal tasks from busy workers' deques, keeping CPUs saturated and reducing contention. Tasks extend `RecursiveTask<V>` (returns result) or `RecursiveAction` (void) and override `compute()`, recursively splitting when the problem is large (`threshold`) and solving directly when small. `ForkJoinPool.commonPool()` is the shared default used by parallel streams and `CompletableFuture` async without explicit executor.\n\n\n```java\nclass SumTask extends RecursiveTask<Long> {\n    final long[] a; final int lo, hi;\n    SumTask(long[] a, int lo, int hi) { this.a=a; this.lo=lo; this.hi=hi; }\n    protected Long compute() {\n        if (hi - lo <= 1000) { // threshold\n            long s = 0; for (int i=lo; i<hi; i++) s += a[i]; return s;\n        }\n        int mid = (lo+hi)/2;\n        SumTask left = new SumTask(a, lo, mid);\n        SumTask right = new SumTask(a, mid, hi);\n        left.fork();               // async execute\n        long r = right.compute();  // compute current thread\n        long l = left.join();      // wait for forked\n        return l + r;\n    }\n}\nLong total = new ForkJoinPool().invoke(new SumTask(arr, 0, arr.length));\n```\n```\n",tech:"java",displayNumber:181,sortKey:181},{id:"java-182",number:182,title:"What is work-stealing in `ForkJoinPool`?",question:"What is work-stealing in `ForkJoinPool`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Work-stealing is the scheduling algorithm of `ForkJoinPool`: each worker thread maintains a double-ended queue (deque) of tasks. A worker pushes new forked subtasks to the *top* of its own deque and pops from the top (LIFO, cache-friendly). When a worker runs out of work, it *steals* a task from the *bottom* of another worker's deque (FIFO). This minimizes contention (owners use one end, thieves the other) and balances load automatically. It's why `ForkJoinPool` scales well for many small tasks, and is the backbone of parallel streams. Caveat: tasks must be independent (no shared mutable state) and ideally of balanced size for good stealing.\n\n\n```java\n// Owner pushes/pops from TOP (LIFO); thieves steal from BOTTOM (FIFO).\n// Idle worker scans other workers' deques to \"steal\" work.\nForkJoinPool pool = ForkJoinPool.commonPool();\n// parallel stream uses commonPool (work-stealing) under the hood\nList<Integer> result = list.parallelStream().map(x -> x*2).toList();\n```\n```\n",tech:"java",displayNumber:182,sortKey:182},{id:"java-183",number:183,title:"What is `ThreadLocal` and when would you use it?",question:"What is `ThreadLocal` and when would you use it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ThreadLocal<T>` provides thread-confined variables: each thread accessing the `ThreadLocal` gets its own independent copy, so there's no sharing and thus no synchronization needed. Typical uses: per-thread state like user session, transaction ID, `SimpleDateFormat` (not thread-safe), database connection/source binding (Spring's `TransactionSynchronizationManager`), and profiling/tracing context. Caution: in thread pools, a `ThreadLocal` set on a worker thread persists across tasks and can leak memory or leak data between unrelated tasks — always `remove()` in a `finally` block. `InheritableThreadLocal` propagates values from parent to child threads (but not across executor thread handoffs).\n\n\n```java\nprivate static final ThreadLocal<SimpleDateFormat> FMT =\n    ThreadLocal.withInitial(() -> new SimpleDateFormat(\"yyyy-MM-dd\"));\n\nString format(Date d) {\n    return FMT.get().format(d); // each thread has its own formatter\n}\n// ALWAYS clean up in thread pools:\ntry {\n    context.set(value);\n    doWork();\n} finally {\n    context.remove(); // prevent leak / cross-task data bleed\n}\n```\n```\n",tech:"java",displayNumber:183,sortKey:183},{id:"java-184",number:184,title:"What is `InheritableThreadLocal`?",question:"What is `InheritableThreadLocal`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'`InheritableThreadLocal` extends `ThreadLocal` so that when a new thread is created from a parent thread, the child receives a *copy* of the parent\'s values (via `childValue(parentValue)` override). This works for plain `new Thread()` but **does not propagate across thread pool hand-offs** — if a task is submitted to an `ExecutorService`, the worker thread is not a child of the submitting thread. For thread-pool-safe context propagation, use `TransmittableThreadLocal` (third-party) or Java 21\'s `ScopedValue` (preview).\n\n\n```java\nInheritableThreadLocal<String> traceId = new InheritableThreadLocal<>() {\n    @Override protected String childValue(String parent) { return parent + "-child"; }\n};\ntraceId.set("req-1");\nnew Thread(() -> System.out.println(traceId.get())).start(); // prints "req-1-child"\n// But: Executors.newSingleThreadExecutor().submit(() -> traceId.get()) -> null!\n```\n```\n',tech:"java",displayNumber:184,sortKey:184},{id:"java-185",number:185,title:"What are Virtual Threads in Java 21?",question:"What are Virtual Threads in Java 21?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`Virtual threads (Project Loom, JEP 444, Java 21) are lightweight, JVM-managed threads that are not 1:1 mapped to OS threads. A virtual thread is cheap to create (millions can exist) because its stack is stored on the Java heap and is *unmounted* from its carrier (platform) thread when it blocks on I/O, allowing the carrier to run other virtual threads. This makes the "one thread per request" model scale to huge concurrency without the OS-thread bottleneck. Create via \`Thread.ofVirtual().start(runnable)\` or \`Executors.newVirtualThreadPerTaskExecutor()\`. They are not a replacement for CPU-bound work — they shine for I/O-bound, blocking-style code.


\`\`\`java
// Java 21
try (var exec = Executors.newVirtualThreadPerTaskExecutor()) {
    for (int i = 0; i < 1_000_000; i++) {
        exec.submit(() -> {
            Thread.sleep(Duration.ofSeconds(1)); // cheap: unmounts carrier
            return "ok";
        });
    }
}
// Thread.ofVirtual().name("vt-", 0).start(() -> doWork());
\`\`\`
\`\`\`
`,tech:"java",displayNumber:185,sortKey:185},{id:"java-186",number:186,title:"How do virtual threads differ from platform threads?",question:"How do virtual threads differ from platform threads?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'Platform threads (the traditional `Thread`) are 1:1 with OS threads: each has a large native stack (typically 1–2 MB), limited by OS/kernel resources (a few thousand typically). Virtual threads are many-to-one: many virtual threads share a small pool of carrier (platform) threads. Virtual threads have tiny, growable Java-heap stacks and can be created in the millions. When a virtual thread blocks on I/O (socket, file, `LockSupport.park`), it unmounts from its carrier, freeing it to run another virtual thread. CPU-bound work still occupies a carrier, so virtual threads do not magically parallelize CPU-heavy tasks. Pinning (holding a `synchronized` monitor or native frame) prevents unmounting and can stall the carrier.\n\n\n```java\n// Platform: expensive, limited\nThread.ofPlatform().name("p-", 0).start(() -> {});\n// Virtual: cheap, many\nThread.ofVirtual().name("v-", 0).start(() -> {});\n// Mixed: virtual threads run ON platform carrier threads internally\n```\n```\n',tech:"java",displayNumber:186,sortKey:186},{id:"java-187",number:187,title:"What is pinning in virtual threads and why does it matter?",question:"What is pinning in virtual threads and why does it matter?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"A virtual thread is *pinned* to its carrier when it cannot unmount — typically because it holds a `synchronized` monitor or is executing a native (JNI) method. While pinned, blocking I/O keeps the carrier occupied, reducing the pool of available carriers and potentially starving other virtual threads. Java 21's `synchronized` pins; Java 24+ (JEP 491) unpins `synchronized`. Workarounds: (1) replace `synchronized` with `ReentrantLock` (does not pin); (2) keep synchronized blocks short and CPU-only; (3) avoid blocking I/O inside `synchronized`. JDK Flight Recorder (`jdk.VirtualThreadPinned` event) can detect pinning.\n\n\n```java\n// BAD: synchronized + blocking I/O = pinned virtual thread\nsynchronized (this) {\n    socket.read(); // carrier cannot unmount\n}\n\n// GOOD: ReentrantLock does not pin\nlock.lock();\ntry { socket.read(); } finally { lock.unlock(); }\n```\n```\n",tech:"java",displayNumber:187,sortKey:187},{id:"java-188",number:188,title:"What is `happens-before` in the Java Memory Model?",question:"What is `happens-before` in the Java Memory Model?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"The Java Memory Model (JMM, JLS 17) defines *happens-before* as a partial order guaranteeing visibility: if action A happens-before action B, then B is guaranteed to see the effects of A. Key happens-before rules: (1) program order in a single thread; (2) unlock of a monitor happens-before subsequent lock of the same monitor; (3) write to `volatile` happens-before subsequent read of that `volatile`; (4) `Thread.start()` happens-before any action in the started thread; (5) last action in a thread happens-before `join()` returns in another; (6) transitivity. Without a happens-before edge, the compiler/CPU may reorder, and a thread may see stale values.\n\n\n```java\nint x = 0; volatile boolean ready = false;\n// Thread A:\nx = 42;\nready = true; // volatile write happens-before subsequent volatile read\n// Thread B:\nif (ready) { // volatile read\n    System.out.println(x); // guaranteed to see 42\n}\n```\n```\n",tech:"java",displayNumber:188,sortKey:188},{id:"java-189",number:189,title:"What is a race condition vs data race?",question:"What is a race condition vs data race?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"A **data race** is a JMM term: two threads access the same memory location concurrently, at least one is a write, and there is no happens-before relationship. Data races cause stale or inconsistent visibility. A **race condition** is a higher-level logic bug: correctness depends on the relative timing of events, even if individual operations are thread-safe (for example, a check-then-act sequence using separate `containsKey` and `put` calls). You can have a race condition without a data race (TOCTOU). Fix data races with `volatile`/`synchronized`/atomics; fix race conditions with atomic compound operations (`putIfAbsent`, `computeIfAbsent`, `AtomicInteger.incrementAndGet`) or by locking the entire compound sequence.\n\n\n```java\n// Data race: unsynchronized write + read of shared field\nint x; // Thread A writes, Thread B reads — no happens-before\n\n// Race condition: check-then-act with individually thread-safe operations\nif (!map.containsKey(k)) map.put(k, v); // another thread may insert between calls\n// Fix: map.putIfAbsent(k, v) — atomic compound\n// Or synchronize the whole check-and-put block on one shared lock\n```\n```\n",tech:"java",displayNumber:189,sortKey:189},{id:"java-190",number:190,title:"What is `Thread.sleep()` vs `Object.wait()` vs `LockSupport.park()`?",question:"What is `Thread.sleep()` vs `Object.wait()` vs `LockSupport.park()`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`Thread.sleep(ms)` pauses the *current* thread for a duration; it does **not** release any locks, cannot be woken by `notify()`, and is interruptible (`InterruptedException`). `Object.wait()` must be called while holding the object's monitor; it **releases** the monitor, waits until notified or timeout, then re-acquires the monitor. `LockSupport.park()` is a low-level primitive used by `Lock`/`Condition`/`Future`; it parks the thread until `unpark(thread)` or interrupt, does not require a monitor, and is not tied to a timeout unless `parkNanos`. Use `sleep` for delays, `wait`/`notify` for monitor-based coordination, `park` for building synchronizers.\n\n\n```java\nThread.sleep(1000);          // does not release locks; interruptible\nsynchronized (obj) { obj.wait(); } // releases monitor, waits for notify\nLockSupport.park();          // parks until unpark or interrupt\nLockSupport.unpark(otherThread);\n```\n```\n",tech:"java",displayNumber:190,sortKey:190},{id:"java-191",number:191,title:"How does interruption work in Java?",question:"How does interruption work in Java?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Interruption is cooperative: `Thread.interrupt()` sets the interrupt status flag. Blocking methods (`sleep`, `wait`, `join`, `BlockingQueue.take`, `Lock.lockInterruptibly`) throw `InterruptedException` and *clear* the flag. Non-blocking code must poll `Thread.interrupted()` (clears flag) or `isInterrupted()` (does not). Best practice: catch `InterruptedException`, restore the interrupt status (`Thread.currentThread().interrupt()`), and exit — never swallow it. Cancellation of `Future` with `cancel(true)` interrupts the worker. Virtual threads honor interruption the same way.\n\n\n```java\nvoid work() {\n    try {\n        while (!Thread.currentThread().isInterrupted()) {\n            BlockingQueue.take(); // throws InterruptedException if interrupted\n        }\n    } catch (InterruptedException e) {\n        Thread.currentThread().interrupt(); // restore flag\n        return; // exit cooperatively\n    }\n}\n```\n```\n",tech:"java",displayNumber:191,sortKey:191},{id:"java-192",number:192,title:"What is `join()` and how does it work?",question:"What is `join()` and how does it work?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`t.join()` blocks the current thread until thread `t` terminates (or a timeout elapses). Internally it waits on `t` as a monitor (`wait()`) until `t` finishes and the JVM `notifyAll`s. `join()` is interruptible. Overloads: `join()`, `join(millis)`, `join(millis, nanos)`. Use it to wait for spawned threads to complete before proceeding — but prefer `ExecutorService.invokeAll`/`CountDownLatch`/`CompletableFuture.allOf` for production. Calling `join()` on a thread that hasn't started or has already terminated returns immediately.\n\n\n```java\nThread t = new Thread(() -> doWork());\nt.start();\nt.join(); // current thread waits until t terminates\nt.join(1000); // wait at most 1 second\n```\n```\n",tech:"java",displayNumber:192,sortKey:192},{id:"java-193",number:193,title:"What are daemon vs user threads?",question:"What are daemon vs user threads?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"User (non-daemon) threads keep the JVM alive; the JVM exits only after all user threads have terminated. Daemon threads are background workers (GC, finalizer, `Timer` if `setDaemon(true)`) that the JVM will kill when no user threads remain — they do not prevent JVM shutdown. Set daemon status with `t.setDaemon(true)` *before* `start()`; after start it throws `IllegalThreadStateException`. A thread inherits the daemon status of its creator. Executor threads are typically user threads; if you want a pool that doesn't keep the JVM alive, wrap the `ThreadFactory` to set daemon.\n\n\n```java\nThread t = new Thread(() -> { while (true) { /* background */ } });\nt.setDaemon(true); // MUST before start\nt.start();\n// JVM can exit even if t is still looping\n```\n```\n",tech:"java",displayNumber:193,sortKey:193},{id:"java-194",number:194,title:"What is `Phaser` and when would you use it?",question:"What is `Phaser` and when would you use it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`Phaser` is a more flexible, reusable barrier that supports a **dynamic number of parties** (register/deregister at runtime) and multiple phases. Threads call `arriveAndAwaitAdvance()` to wait for the current phase to complete, then all advance together. Unlike `CyclicBarrier`, parties can be added (`register()`) or removed (`arriveAndDeregister()`) mid-run, making it suitable for tree-structured or dynamically sized parallel algorithms. `onAdvance(phase, registeredParties)` can terminate the phaser when it returns `true`. Use Phaser when party count is not known up front or changes over time.\n\n\n```java\nPhaser phaser = new Phaser(1); // register self\nfor (int i = 0; i < n; i++) {\n    phaser.register();\n    new Thread(() -> {\n        doWork();\n        phaser.arriveAndDeregister();\n    }).start();\n}\nphaser.arriveAndAwaitAdvance(); // wait for all workers\n```\n```\n",tech:"java",displayNumber:194,sortKey:194},{id:"java-195",number:195,title:"What is `Exchanger`?",question:"What is `Exchanger`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`Exchanger<V>` is a two-party synchronizer that lets two threads swap objects at a rendezvous point. Thread A calls `exchange(a)` and blocks until thread B calls `exchange(b)`; then A receives `b` and B receives `a`. Typical use: producer/consumer pipelines with double-buffering — producer fills a buffer, exchanges it with consumer's empty buffer, and they swap roles without extra copying. Timeout and interruptible variants exist. Only two parties — for N-way, use `CyclicBarrier` or a queue.\n\n\n```java\nExchanger<List<Integer>> ex = new Exchanger<>();\n// Producer\nList<Integer> buf = new ArrayList<>();\nwhile (true) {\n    fill(buf);\n    buf = ex.exchange(buf); // swap full for empty\n}\n// Consumer\nList<Integer> empty = new ArrayList<>();\nwhile (true) {\n    empty = ex.exchange(empty); // swap empty for full\n    drain(empty);\n}\n```\n```\n",tech:"java",displayNumber:195,sortKey:195},{id:"java-196",number:196,title:"What are concurrent collections (`ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`)?",question:"What are concurrent collections (`ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`)?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`java.util.concurrent` collections are thread-safe without wrapping in `Collections.synchronizedXxx`:\n- **`ConcurrentHashMap`**: lock-striping (Java 7) / CAS + synchronized bins (Java 8+); allows concurrent reads and a bounded number of concurrent writes. Iterators are weakly consistent (no `ConcurrentModificationException`).\n- **`CopyOnWriteArrayList`/`CopyOnWriteArraySet`**: every mutation copies the entire array; reads are lock-free. Great for rare writes, frequent reads (listeners).\n- **`BlockingQueue`** (`ArrayBlockingQueue`, `LinkedBlockingQueue`, `SynchronousQueue`, `PriorityBlockingQueue`, `DelayQueue`): producer-consumer queues with blocking `put`/`take`.\nNever wrap a concurrent collection in extra `synchronized` unless you need a compound atomic operation not provided by the API.\n\n\n```java\nConcurrentHashMap<String, Integer> map = new ConcurrentHashMap<>();\nmap.putIfAbsent("a", 1);\nmap.compute("a", (k, v) -> v == null ? 1 : v + 1); // atomic compound\nmap.merge("a", 1, Integer::sum);\n\nCopyOnWriteArrayList<Listener> listeners = new CopyOnWriteArrayList<>();\nlisteners.forEach(Listener::onEvent); // snapshot, no CME\n\nBlockingQueue<Task> q = new ArrayBlockingQueue<>(100);\nq.put(task);   // blocks if full\nTask t = q.take(); // blocks if empty\n```\n```\n',tech:"java",displayNumber:196,sortKey:196},{id:"java-197",number:197,title:"How does `ConcurrentHashMap` work internally (Java 8+)?",question:"How does `ConcurrentHashMap` work internally (Java 8+)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Java 8+ `ConcurrentHashMap` uses a table of bins. Empty bins are filled with CAS. Colliding keys form a linked list; if a bin grows past `TREEIFY_THRESHOLD` (8) and table is large enough, it treeifies into a red-black tree (`TreeBin`) for O(log n) lookup. Writes lock the *bin* (`synchronized` on the first node) rather than the whole map. Size is computed via a `CounterCell` array (similar to `LongAdder`) to avoid a global lock. Iterators are weakly consistent — they may or may not see concurrent updates, but they never throw `ConcurrentModificationException` and never return the same key twice.\n\n\n```java\n// Java 8+ ConcurrentHashMap\n// - CAS for empty bins\n// - synchronized(bin-head) for colliding bins\n// - treeify at 8, untreeify at 6\n// - size via striped CounterCells (LongAdder-like)\nConcurrentHashMap<K,V> m = new ConcurrentHashMap<>();\nm.computeIfAbsent(k, this::load); // lock-free-ish compound\n```\n```\n",tech:"java",displayNumber:197,sortKey:197},{id:"java-198",number:198,title:"What is the difference between `HashMap`, `Hashtable`, `Collections.synchronizedMap`, and `ConcurrentHashMap`?",question:"What is the difference between `HashMap`, `Hashtable`, `Collections.synchronizedMap`, and `ConcurrentHashMap`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"- **`HashMap`**: not thread-safe; concurrent mutation can cause infinite loops (pre-Java 8) or lost updates. Fine for single-thread.\n- **`Hashtable`**: fully synchronized (legacy); every method locks the entire table — correct but slow; `null` keys/values forbidden.\n- **`Collections.synchronizedMap(map)`**: wraps any map with a mutex on every method; compound operations (`if (!contains) put`) still race unless you lock the wrapper yourself.\n- **`ConcurrentHashMap`**: designed for concurrency — finer-grained locking, compound atomic methods (`putIfAbsent`, `compute`), no `null`s, weakly consistent iterators. Default choice for concurrent maps.\n\n\n```java\n// Hashtable / synchronizedMap: whole-map lock, compound ops still racy\nMap<K,V> sync = Collections.synchronizedMap(new HashMap<>());\nsynchronized (sync) { if (!sync.containsKey(k)) sync.put(k, v); } // extra lock needed\n\n// ConcurrentHashMap: built-in atomic compounds\nConcurrentHashMap<K,V> chm = new ConcurrentHashMap<>();\nchm.putIfAbsent(k, v);\n```\n```\n",tech:"java",displayNumber:198,sortKey:198},{id:"java-199",number:199,title:"What is `StampedLock`?",question:"What is `StampedLock`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`StampedLock` (Java 8) is an optimistic read-write lock that returns a `long` stamp on acquire. Modes: (1) **write lock** (exclusive); (2) **read lock** (pessimistic shared); (3) **optimistic read** (`tryOptimisticRead`) — no lock, just a stamp; after reading, `validate(stamp)` checks if a write occurred in between. If invalid, fall back to a pessimistic read. Optimistic reads are extremely cheap for read-heavy, write-rare data. Caveats: not reentrant, no `Condition`, deadlock-prone if you mix modes incorrectly, and a thread holding a read lock cannot upgrade without deadlock (must convert via `tryConvertToWriteLock`).\n\n\n```java\nStampedLock sl = new StampedLock();\ndouble x, y;\ndouble distance() {\n    long stamp = sl.tryOptimisticRead();\n    double cx = x, cy = y;\n    if (!sl.validate(stamp)) { // write happened — fall back\n        stamp = sl.readLock();\n        try { cx = x; cy = y; } finally { sl.unlockRead(stamp); }\n    }\n    return Math.hypot(cx, cy);\n}\n```\n```\n",tech:"java",displayNumber:199,sortKey:199},{id:"java-200",number:200,title:"What is `ThreadFactory` and why customize it?",question:"What is `ThreadFactory` and why customize it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`ThreadFactory` is an interface (`newThread(Runnable)`) used by executors to create worker threads. The default factory produces unnamed, non-daemon, normal-priority threads (`pool-N-thread-M`). Custom factories let you: (1) set meaningful names for debugging (`jstack`/`jconsole`); (2) set daemon status; (3) set priority; (4) set `UncaughtExceptionHandler`; (5) wrap `Runnable` to restore `ThreadLocal`s. Always name threads in production — unnamed threads make dumps unreadable.\n\n\n```java\nThreadFactory named = r -> {\n    Thread t = new Thread(r);\n    t.setName("worker-" + t.getId());\n    t.setUncaughtExceptionHandler((th, ex) -> log.error(th.getName(), ex));\n    t.setDaemon(false);\n    return t;\n};\nExecutorService es = Executors.newFixedThreadPool(8, named);\n```\n```\n',tech:"java",displayNumber:200,sortKey:200},{id:"java-201",number:201,title:"What is `RejectedExecutionHandler`?",question:"What is `RejectedExecutionHandler`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"When a `ThreadPoolExecutor` cannot accept a task (queue full and pool at `maxPoolSize`, or after `shutdown()`), it invokes the `RejectedExecutionHandler`. Built-in policies:\n- **`AbortPolicy`** (default): throws `RejectedExecutionException`.\n- **`CallerRunsPolicy`**: the *calling* thread runs the task — natural backpressure.\n- **`DiscardPolicy`**: silently drop the task.\n- **`DiscardOldestPolicy`**: drop the oldest queued task and retry.\nChoose based on whether losing work is acceptable. `CallerRunsPolicy` is a good default for bounded work that must not be lost.\n\n\n```java\nnew ThreadPoolExecutor(4, 8, 60, TimeUnit.SECONDS,\n    new ArrayBlockingQueue<>(100),\n    new ThreadPoolExecutor.CallerRunsPolicy()); // backpressure: caller runs\n```\n```\n",tech:"java",displayNumber:201,sortKey:201},{id:"java-202",number:202,title:"How do you shut down an `ExecutorService` correctly?",question:"How do you shut down an `ExecutorService` correctly?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Never just abandon a pool — threads will keep the JVM alive. Pattern: (1) `shutdown()` — stop accepting new tasks, finish queued ones; (2) `awaitTermination(timeout)` — wait for completion; (3) if timeout, `shutdownNow()` — interrupt running tasks and return unexecuted ones; (4) `awaitTermination` again. Java 19+ `ExecutorService` is `AutoCloseable` — `try-with-resources` calls `close()` which does this dance. Always restore interrupt status if `awaitTermination` is interrupted.\n\n\n```java\nExecutorService es = Executors.newFixedThreadPool(4);\ntry {\n    // submit work\n} finally {\n    es.shutdown();\n    try {\n        if (!es.awaitTermination(30, TimeUnit.SECONDS)) {\n            es.shutdownNow();\n            es.awaitTermination(10, TimeUnit.SECONDS);\n        }\n    } catch (InterruptedException e) {\n        es.shutdownNow();\n        Thread.currentThread().interrupt();\n    }\n}\n// Java 19+: try (var es = Executors.newVirtualThreadPerTaskExecutor()) { ... }\n```\n```\n",tech:"java",displayNumber:202,sortKey:202},{id:"java-203",number:203,title:"What is `ScheduledExecutorService`?",question:"What is `ScheduledExecutorService`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`ScheduledExecutorService` (`Executors.newScheduledThreadPool(n)`) schedules tasks with delays or periodically. Methods: `schedule(task, delay, unit)` (one-shot delayed), `scheduleAtFixedRate` (period measured from *start* of previous run — can overlap if a run exceeds the period), `scheduleWithFixedDelay` (period measured from *end* of previous run — never overlaps). Prefer `ScheduledThreadPoolExecutor` over `java.util.Timer` (Timer is single-threaded and a thrown exception kills it). Catch exceptions inside the task — an uncaught exception silently cancels subsequent periodic runs.\n\n\n```java\nScheduledExecutorService sch = Executors.newScheduledThreadPool(2);\nsch.schedule(() -> log("once"), 5, TimeUnit.SECONDS);\nsch.scheduleAtFixedRate(this::tick, 0, 1, TimeUnit.SECONDS); // every 1s from start\nsch.scheduleWithFixedDelay(this::tick, 0, 1, TimeUnit.SECONDS); // 1s after previous ends\n```\n```\n',tech:"java",displayNumber:203,sortKey:203},{id:"java-204",number:204,title:"What is `invokeAll` vs `invokeAny`?",question:"What is `invokeAll` vs `invokeAny`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Both take a collection of `Callable`s. `invokeAll` submits all, waits for *all* to complete (or timeout), and returns a list of `Future`s in the same order — some may have failed (`ExecutionException` on `get()`). `invokeAny` returns the result of the *first successful* task and cancels the rest; if all fail, it throws `ExecutionException`. Use `invokeAll` for fan-out-and-gather; `invokeAny` for racing redundant sources (first successful DNS lookup, first healthy replica).\n\n\n```java\nList<Callable<String>> tasks = List.of(() -> "a", () -> "b");\nList<Future<String>> all = es.invokeAll(tasks); // wait for both\nString first = es.invokeAny(tasks);             // first success, cancel rest\n```\n```\n',tech:"java",displayNumber:204,sortKey:204},{id:"java-205",number:205,title:"What is `CompletionService`?",question:"What is `CompletionService`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ExecutorCompletionService` wraps an `Executor` and a `BlockingQueue` of completed `Future`s. As each submitted task finishes, its `Future` is placed on the completion queue, so you can `take()` results in *completion order* rather than submission order. Useful when you want to process results as they arrive (don't wait for the slowest). Contrast with `invokeAll`, which returns in submission order after all complete.\n\n\n```java\nCompletionService<String> cs = new ExecutorCompletionService<>(es);\nint n = 10;\nfor (int i = 0; i < n; i++) cs.submit(() -> fetch(i));\nfor (int i = 0; i < n; i++) {\n    String r = cs.take().get(); // next completed, not next submitted\n    process(r);\n}\n```\n```\n",tech:"java",displayNumber:205,sortKey:205},{id:"java-206",number:206,title:"What is double-checked locking and is it safe in Java?",question:"What is double-checked locking and is it safe in Java?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`Double-checked locking (DCL) is a singleton idiom: check without lock, if null then lock and check again, then initialize. Pre-Java 5 it was broken because the constructor write could be reordered with the reference write, so another thread could see a non-null reference to a partially constructed object. Java 5+ JMM makes it safe **if the instance field is \`volatile\`**, because a volatile write happens-before subsequent volatile reads, publishing the fully constructed object. Prefer enum singleton or holder-class (lazy, thread-safe without explicit locks) unless you have a measured need for DCL.


\`\`\`java
class Dcl {
    private static volatile Dcl instance; // volatile is REQUIRED
    static Dcl get() {
        if (instance == null) {
            synchronized (Dcl.class) {
                if (instance == null) instance = new Dcl();
            }
        }
        return instance;
    }
}
// Safer: holder class
class Holder {
    private static class H { static final Holder I = new Holder(); }
    static Holder get() { return H.I; }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:206,sortKey:206},{id:"java-207",number:207,title:"What is `AtomicReference` and when is it useful?",question:"What is `AtomicReference` and when is it useful?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`AtomicReference<V>` holds a single object reference that can be updated atomically via CAS (`compareAndSet`, `getAndSet`, `updateAndGet`). Use it to atomically swap immutable snapshots (config objects, caches) without locks. Combined with immutable data, it implements lock-free publish: writers construct a new object, CAS it in; readers always see a consistent snapshot. For ABA, use `AtomicStampedReference`. `AtomicReferenceArray` is the array analog.\n\n\n```java\nrecord Config(int timeout, String url) {}\nAtomicReference<Config> cfg = new AtomicReference<>(new Config(30, "http://x"));\n// lock-free update\ncfg.updateAndGet(old -> new Config(old.timeout() + 10, old.url()));\nConfig snap = cfg.get(); // readers never see a torn Config\n```\n```\n',tech:"java",displayNumber:207,sortKey:207},{id:"java-208",number:208,title:"What is `LongAdder` and when should you prefer it over `AtomicLong`?",question:"What is `LongAdder` and when should you prefer it over `AtomicLong`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`LongAdder` (Java 8) is a scalable counter that internally uses a `base` plus a striped array of `Cell`s. Under contention, threads increment different cells (via probing), avoiding a single hot CAS location. `sum()` aggregates all cells (may miss concurrent updates — weakly consistent). Prefer `LongAdder` for high-contention, write-heavy counters (request counts, metrics) where you mostly increment and occasionally read the total. Prefer `AtomicLong` when you need a precise current value often, or CAS-based algorithms (`compareAndSet`). `DoubleAdder`/`LongAccumulator` follow the same idea.\n\n\n```java\nLongAdder hits = new LongAdder();\nhits.increment();        // cheap under contention\nlong total = hits.sum(); // approximate under concurrent writes\n// vs AtomicLong: one CAS location, retries under contention\n```\n```\n",tech:"java",displayNumber:208,sortKey:208},{id:"java-209",number:209,title:"What is `Thread.yield()` and `Thread.priority`?",question:"What is `Thread.yield()` and `Thread.priority`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`Thread.yield()` is a hint to the scheduler that the current thread is willing to yield its remaining time slice. The JVM/OS may ignore it; never use it for correctness (not a lock, not a wait). Thread priority (`MIN_PRIORITY=1`, `NORM=5`, `MAX=10`) is also a hint; many OSes ignore or coarsen it, and relying on it for correctness is a bug. Starvation can occur if you mix high and low priorities under a non-fair scheduler. Prefer proper synchronization and fair locks over priority tricks.\n\n\n```java\nThread.currentThread().setPriority(Thread.MAX_PRIORITY); // hint only\nThread.yield(); // hint only — never for correctness\n```\n```\n",tech:"java",displayNumber:209,sortKey:209},{id:"java-210",number:210,title:"What is `LockSupport`?",question:"What is `LockSupport`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`LockSupport` is the lowest-level parking primitive used to implement locks, `Future`, `CompletableFuture`, `ForkJoinPool`, etc. `park()` parks the current thread; `unpark(t)` makes a permit available so the next `park()` of `t` returns immediately (or wakes it if already parked). Unlike `wait/notify`, `unpark` is *not* lost if it happens before `park` — a permit is stored. No monitor is required. Interrupt unparks and sets the interrupt flag without throwing. Building custom synchronizers on `LockSupport` + `AtomicInteger` is how AQS (`AbstractQueuedSynchronizer`) works.\n\n\n```java\nThread worker = Thread.currentThread();\nLockSupport.park();          // wait for permit\nLockSupport.unpark(worker);  // grant permit (safe if called first)\n// AQS: FIFO queue of waiters + park/unpark\n```\n```\n",tech:"java",displayNumber:210,sortKey:210},{id:"java-211",number:211,title:"What is `AbstractQueuedSynchronizer` (AQS)?",question:"What is `AbstractQueuedSynchronizer` (AQS)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"AQS is the framework behind `ReentrantLock`, `Semaphore`, `CountDownLatch`, `ReentrantReadWriteLock`, `FutureTask`, etc. It maintains a FIFO wait queue of threads (CLH-style) and an `int state` that subclasses interpret (e.g., hold count, permit count). Subclasses implement `tryAcquire`/`tryRelease` (exclusive) or `tryAcquireShared`/`tryReleaseShared` (shared). AQS handles queuing, parking (`LockSupport`), fairness, and interruption. Understanding AQS explains why `ReentrantLock` can have conditions, why `Semaphore` has no owner, and why these primitives share similar performance characteristics.\n\n\n```java\n// Conceptual: ReentrantLock is an AQS subclass\n// state = hold count; exclusive owner thread\n// tryAcquire: if state==0 CAS 0->1 and set owner; if owner==current, state++\n// tryRelease: state--; if 0, clear owner, unpark successor\nclass Mutex extends AbstractQueuedSynchronizer {\n    protected boolean tryAcquire(int a) { return compareAndSetState(0, 1); }\n    protected boolean tryRelease(int a) { setState(0); return true; }\n}\n```\n```\n",tech:"java",displayNumber:211,sortKey:211},{id:"java-212",number:212,title:"What is `CompletionStage` vs `CompletableFuture`?",question:"What is `CompletionStage` vs `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletionStage` is the interface defining the async chaining contract (`thenApply`, `thenCombine`, `exceptionally`, etc.). `CompletableFuture` is the standard concrete implementation that also implements `Future` (blocking `get()`). You can write code that accepts `CompletionStage` for flexibility (other impls like `CompletionStage` from reactive libs). `CompletableFuture` adds manual completion (`complete`, `completeExceptionally`) and `Future` methods. Prefer `CompletionStage` in APIs to decouple from the implementation.\n\n\n```java\n// API takes CompletionStage — caller can pass CompletableFuture or other impl\nCompletionStage<String> fetch() { return CompletableFuture.supplyAsync(() -> "x"); }\n// CompletableFuture = CompletionStage + Future (blocking get, cancel)\n```\n```\n',tech:"java",displayNumber:212,sortKey:212},{id:"java-213",number:213,title:"How do you handle exceptions in `CompletableFuture` chains?",question:"How do you handle exceptions in `CompletableFuture` chains?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Exceptions in a stage propagate to the next stage that has an exception handler. Handlers: `exceptionally(Function<Throwable, T>)` returns a recovery value (stage completes normally with it); `handle(BiFunction<T, Throwable, U>)` receives both value and exception (exactly one is null) and returns a new value; `whenComplete(BiConsumer<T, Throwable>)` is a terminal side-effect that propagates the result/exception unchanged. If no handler is present, the final `Future.get()` throws `ExecutionException` wrapping the cause. `get()` on a completed exceptionally future rethrows the cause wrapped in `CompletionException` (subclass of `RuntimeException`).\n\n\n```java\nCompletableFuture.supplyAsync(() -> { throw new IOException("fail"); })\n    .exceptionally(ex -> "fallback")          // recover value\n    .thenAccept(System.out::println);         // prints "fallback"\n\n// handle: both value and ex available\nCompletableFuture.supplyAsync(() -> 1/0)\n    .handle((v, ex) -> ex == null ? v : -1);\n\n// whenComplete: terminal logging, propagates unchanged\nCompletableFuture.supplyAsync(() -> "ok")\n    .whenComplete((v, ex) -> log.info("done: " + v));\n```\n```\n',tech:"java",displayNumber:213,sortKey:213},{id:"java-214",number:214,title:"What is `allOf` and `anyOf` in `CompletableFuture`?",question:"What is `allOf` and `anyOf` in `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletableFuture.allOf(f1, f2, ...)` returns a `CompletableFuture<Void>` that completes when *all* given futures complete (or any fails). Use `allOf(...).join()` to wait for a batch, then inspect individual futures for results. `anyOf(f1, f2, ...)` returns a `CompletableFuture<Object>` that completes with the result of the *first* to complete (success or failure); use for timeout races or redundant calls. Note: `allOf` does not cancel others on failure; `anyOf` does not cancel the rest on completion — you may want to cancel manually.\n\n\n```java\nCompletableFuture<String> a = CompletableFuture.supplyAsync(() -> "a");\nCompletableFuture<String> b = CompletableFuture.supplyAsync(() -> "b");\nCompletableFuture<Void> both = CompletableFuture.allOf(a, b);\nboth.join(); // wait for both\nList<String> results = List.of(a.join(), b.join());\n\nCompletableFuture<Object> first = CompletableFuture.anyOf(a, b);\nObject fast = first.join(); // first result\n```\n```\n',tech:"java",displayNumber:214,sortKey:214},{id:"java-215",number:215,title:"What is `StructuredTaskScope` (Java 21/24)?",question:"What is `StructuredTaskScope` (Java 21/24)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`StructuredTaskScope` (JEP 453, preview Java 21, finalized Java 24) implements **structured concurrency**: a scope that owns a set of subtasks and guarantees all complete (or are cancelled) before the scope exits. `try (var scope = new StructuredTaskScope.ShutdownOnFailure()) { Future<A> a = scope.fork(() -> fetchA()); Future<B> b = scope.fork(() -> fetchB()); scope.join(); }` — on any failure, `ShutdownOnFailure` cancels the others; `ShutdownOnSuccess` cancels remaining on first success. This eliminates orphaned threads and resource leaks. Scopes are nestable and enforce that children outlive parents.\n\n\n```java\n// Java 24+ finalized\ntry (var scope = new StructuredTaskScope.ShutdownOnFailure()) {\n    Future<String> user = scope.fork(() -> fetchUser(id));\n    Future<List<Order>> orders = scope.fork(() -> fetchOrders(id));\n    scope.join();            // wait for all, propagate exceptions\n    scope.throwIfFailed();   // throw first exception if any\n    return new Profile(user.get(), orders.get());\n}\n```\n```\n",tech:"java",displayNumber:215,sortKey:215},{id:"java-216",number:216,title:"What is `ScopedValue` (Java 21+)?",question:"What is `ScopedValue` (Java 21+)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'`ScopedValue` (JEP 446, preview Java 21, finalized Java 24) provides **implicit context propagation** across virtual threads without `ThreadLocal`. It is a write-once (per scope) immutable key-value pair that is captured when a `ScopedValue.where(key, value).run(() -> ...)` scope is entered, and automatically visible to all virtual threads created within that scope (including those in `StructuredTaskScope`). It avoids the memory leak and cross-task contamination of `ThreadLocal` in thread pools. Use for request IDs, auth tokens, DB sessions.\n\n\n```java\n// Java 24+ finalized\nstatic final ScopedValue<String> TRACE_ID = ScopedValue.newInstance();\n\nvoid handle(Request req) {\n    ScopedValue.where(TRACE_ID, req.traceId()).run(() -> {\n        // any virtual thread spawned here sees TRACE_ID\n        StructuredTaskScope.ShutdownOnFailure scope = new StructuredTaskScope.ShutdownOnFailure();\n        scope.fork(() -> log(TRACE_ID.get())); // "abc-123"\n        scope.join();\n    });\n}\n```\n```\n',tech:"java",displayNumber:216,sortKey:216},{id:"java-217",number:217,title:"What is the Memory Consistency Error and how to avoid it?",question:"What is the Memory Consistency Error and how to avoid it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'A memory consistency error occurs when different threads have inconsistent views of shared data due to lack of a happens-before relationship (reordering, caching). Avoiding it means establishing happens-before edges: use `synchronized`, `volatile`, atomic classes, or concurrent collections; follow the safe-publication rules (final fields, `volatile`, thread-confined). The key is not just "atomicity" but "visibility" — without a happens-before edge, there is no guarantee a thread sees another\'s writes.\n\n\n```java\n// Inconsistent view without happens-before\nboolean ready = false; int value = 0;\n// Writer: value = 42; ready = true;\n// Reader: if (ready) System.out.println(value); // may print 0 or 42!\n// Fix: make ready volatile (write happens-before read) -> reader sees 42.\n```\n```\n',tech:"java",displayNumber:217,sortKey:217},{id:"java-218",number:218,title:"What is safe publication in Java?",question:"What is safe publication in Java?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Safe publication means ensuring that when one thread makes an object visible to another, the other sees the object in a fully constructed, consistent state. Ways to safely publish: (1) store in a `static final` field (JMM guarantees final-field semantics + class-init lock); (2) store in a `volatile` field or `AtomicReference`; (3) store in a properly locked field; (4) put in a thread-safe collection (`ConcurrentHashMap`, `BlockingQueue`); (5) publish through a thread's `start()` or `join()`. Danger: letting `this` escape during construction (e.g., registering `this` in the constructor) exposes a partially constructed object.\n\n\n```java\n// Safe: static final (class init lock + final semantics)\nstatic final Config CFG = new Config();\n// Safe: volatile publish\nvolatile Resource r;\nr = new Resource(); // any thread reading r sees full object\n// UNSAFE: this escapes constructor -> partial construction visible\nclass Bad { Bad() { registry.register(this); } }\n```\n```\n",tech:"java",displayNumber:218,sortKey:218},{id:"java-219",number:219,title:"What is `final` field semantics in the JMM?",question:"What is `final` field semantics in the JMM?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`The JMM guarantees that a \`final\` field initialized in the constructor is visible to other threads *without further synchronization*, provided the object reference does not escape during construction. The write of the final field in the constructor happens-before the first read of that field by another thread, and the constructor's actions happen-before the object becomes visible. This is why immutable objects with all fields \`final\` are inherently thread-safe after safe publication.


\`\`\`java
class Immutable {
    final int x;
    final String s;
    Immutable(int x, String s) {
        this.x = x;
        this.s = s;
    }
}
// Thread A: Immutable i = new Immutable(1, "a");
// Thread B: reads i.x, i.s — guaranteed to see 1 and "a" without volatile/sync
\`\`\`
\`\`\`
`,tech:"java",displayNumber:219,sortKey:219},{id:"java-220",number:220,title:"What is false sharing and how do you avoid it?",question:"What is false sharing and how do you avoid it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"False sharing occurs when independent variables updated by different threads reside on the same CPU cache line (typically 64 bytes). Even though the variables are logically independent, writes invalidate the shared cache line, causing heavy cache-coherence traffic and poor performance. Java 8 introduced `@Contended` (internal annotation) to pad fields, but it requires `-XX:-RestrictContended`. `LongAdder` avoids this via padded striped cells. Avoid by grouping read-mostly fields, padding hot counters, using `LongAdder`, and measuring with JMH — false sharing is a performance issue, not a correctness bug.\n\n\n```java\n// False sharing: x and y likely same cache line; two threads update separately\nclass Counters { volatile long x, y; }\n// Avoid: LongAdder has padded cells (better for hot counters)\nLongAdder requests = new LongAdder();\nrequests.increment();\n// Or @jdk.internal.vm.annotation.Contended with JVM flag (not portable API)\n```\n```\n",tech:"java",displayNumber:220,sortKey:220},{id:"java-221",number:221,title:"How do you detect deadlocks in production?",question:"How do you detect deadlocks in production?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'No automatic runtime deadlock detection in JVM, but tooling exists: (1) `jstack <pid>` — thread dump shows "BLOCKED" on locks; look for cycles. (2) `ThreadMXBean.findDeadlockedThreads()` / `findMonitorDeadlockedThreads()` — programmatic; returns thread IDs in a deadlock cycle. (3) VisualVM / JConsole — "Detect Deadlock" button. (4) JVM flags: `-XX:+UnlockDiagnosticVMOptions -XX:+LogVMOutput` for deadlock logging (experimental). Add a monitoring thread that periodically checks `ThreadMXBean` and alerts/logs.\n\n\n```java\nThreadMXBean mx = ManagementFactory.getThreadMXBean();\nlong[] deadlocked = mx.findDeadlockedThreads();\nif (deadlocked != null) {\n    ThreadInfo[] infos = mx.getThreadInfo(deadlocked);\n    for (ThreadInfo ti : infos) System.err.println(ti);\n}\n```\n```\n',tech:"java",displayNumber:221,sortKey:221},{id:"java-222",number:222,title:"What is `CompletableFuture` `thenComposeAsync` vs `thenCompose`?",question:"What is `CompletableFuture` `thenComposeAsync` vs `thenCompose`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`thenCompose` executes the dependent function on the *same thread* that completes the previous stage (which may be a caller thread, a ForkJoinPool worker, or a custom executor's thread). `thenComposeAsync` submits the dependent function to an `Executor` (default: `ForkJoinPool.commonPool()`) — ensuring it runs asynchronously and avoiding \"blocking\" the completing thread. Use `thenComposeAsync` when the next stage may block or is CPU-intensive, to keep the upstream thread (or common pool) free. Overloads let you supply a custom executor.\n\n\n```java\n// runs on same thread that completed fetchId\nfetchId().thenCompose(id -> fetchUser(id));\n\n// runs on commonPool (or supplied executor), not on fetchId's thread\nfetchId().thenComposeAsync(id -> fetchUser(id));\nfetchId().thenComposeAsync(id -> fetchUser(id), myExecutor);\n```\n```\n",tech:"java",displayNumber:222,sortKey:222},{id:"java-223",number:223,title:"What is the difference between `Thread.startVirtualThread` and `Thread.ofVirtual().start`?",question:"What is the difference between `Thread.startVirtualThread` and `Thread.ofVirtual().start`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'Java 21 introduced two ways: `Thread.startVirtualThread(Runnable)` (static factory, creates and starts in one call, unnamed) and `Thread.ofVirtual().name("vt-", 0).start(Runnable)` (builder API for naming, inheriting thread-local behavior, `ThreadGroup`, etc.). `startVirtualThread` is a shortcut; `ofVirtual()` returns a `Thread.Builder.OfVirtual` for configuration. Both schedule on the virtual-thread scheduler. The builder also supports `start(() -> ...)` and `unstarted(() -> ...)` if you need a reference before starting.\n\n\n```java\n// Java 21+\nThread.startVirtualThread(() -> System.out.println("hi")); // quick one-off\nThread vt = Thread.ofVirtual().name("worker-", 0).unstarted(() -> work());\nvt.start(); // later\n```\n```\n',tech:"java",displayNumber:223,sortKey:223},{id:"java-224",number:224,title:"When should you NOT use virtual threads?",question:"When should you NOT use virtual threads?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Virtual threads are for I/O-bound, blocking-style code with many concurrent tasks. Avoid or use platform threads when: (1) **CPU-bound** workloads — virtual threads don't add parallelism beyond core count; use a bounded `ForkJoinPool`/fixed pool. (2) **Pinning** — code that holds `synchronized` or native frames while doing blocking I/O stalls carriers (Java 21). (3) **Thread-local heavy usage** — millions of virtual threads each with large `ThreadLocal`s can blow heap; clean up with `remove()`. (4) **Tight loops without blocking** — retains carrier, no benefit. (5) **Synchronized thread pools** — `Executors.newVirtualThreadPerTaskExecutor()` is the right fit, not a fixed-size virtual pool.\n\n\n```java\n// WRONG: CPU-bound, no blocking — virtual threads add nothing\ntry (var e = Executors.newVirtualThreadPerTaskExecutor()) {\n    for (int i = 0; i < 1000; i++) e.submit(() -> heavyCpuLoop()); // use fixed pool\n}\n// RIGHT: I/O-bound blocking\ne.submit(() -> { db.query(); http.get(); }); // virtual threads shine\n```\n```\n",tech:"java",displayNumber:224,sortKey:224},{id:"java-225",number:225,title:"What are best practices for writing concurrent Java code?",question:"What are best practices for writing concurrent Java code?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Key principles: (1) **Prefer immutability** — final fields, no setters; safe publication eliminates synchronization. (2) **Use thread-safe libraries** — concurrent collections, `CompletableFuture`, `ExecutorService` — don\'t roll your own locks unless necessary. (3) **Minimize shared mutable state** — thread confinement, `ThreadLocal`, or copy-on-write. (4) **Use the right synchronizer** — `CountDownLatch` for one-shot wait, `CyclicBarrier` for phases, `Semaphore` for resource limits, `Phaser` for dynamic parties. (5) **Avoid premature optimization** — `synchronized` is fast enough for most; profile first. (6) **Always clean up** — `shutdown()`, `ThreadLocal.remove()`, close resources. (7) **Test under concurrency** — JMH for performance, `jcstress` for correctness, ThreadSanitizer for data races.\n\n\n```java\n// Example: immutable config + thread-safe map + CompletableFuture\nrecord Config(String url, int timeout) {}\nConfig cfg = new Config("http://api", 30);\n\nConcurrentHashMap<String, Object> cache = new ConcurrentHashMap<>();\ncache.computeIfAbsent(key, k -> load(k)); // atomic\n\nCompletableFuture.supplyAsync(() -> fetch(cfg))\n    .thenApply(this::transform)\n    .exceptionally(ex -> fallback)\n    .thenAccept(this::store);\n```\n```\n',tech:"java",displayNumber:225,sortKey:225},{id:"java-226",number:226,title:"What are Lambda Expressions in Java 8 and how do they work?",question:"What are Lambda Expressions in Java 8 and how do they work?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Lambda expressions introduce functional programming to Java, allowing you to treat functionality as a method argument or code as data. They provide a concise way to represent anonymous functions. A lambda consists of a parameter list, an arrow token (\`->\`), and a body. They reduce boilerplate from anonymous inner classes and are only usable with functional interfaces (interfaces with exactly one abstract method). The compiler infers parameter types, and lambdas can capture variables from the enclosing scope provided those variables are effectively final.


\`\`\`java
// Anonymous inner class vs lambda
Runnable r1 = new Runnable() {
    @Override public void run() { System.out.println("Hello"); }
};
Runnable r2 = () -> System.out.println("Hello");

// With parameters and a block body
BiFunction<Integer, Integer, Integer> add = (a, b) -> {
    int sum = a + b;
    return sum;
};

// Capturing effectively final variable
int factor = 2;
Function<Integer, Integer> times = x -> x * factor; // factor must not be reassigned
\`\`\`
\`\`\`
`,tech:"java",displayNumber:226,sortKey:226},{id:"java-227",number:227,title:"What is a Functional Interface and how does it differ from a regular interface?",question:"What is a Functional Interface and how does it differ from a regular interface?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`A functional interface has exactly one abstract method (a "single abstract method" or SAM type). It is the target type for lambda expressions and method references. While it may declare any number of default, static, or \`java.lang.Object\` methods, only one method may be abstract. The \`@FunctionalInterface\` annotation is optional but recommended: it signals intent and causes a compile error if the interface is later given a second abstract method.


\`\`\`java
@FunctionalInterface
interface StringFormatter {
    String format(String input); // the single abstract method

    default String formatUpper(String s) { return s.toUpperCase(); } // allowed
    static String empty() { return ""; }                            // allowed
    boolean equals(Object obj);                                      // allowed: from Object
}

StringFormatter f = s -> s.trim();
\`\`\`
\`\`\`
`,tech:"java",displayNumber:227,sortKey:227},{id:"java-228",number:228,title:"Describe the built-in `Function<T, R>` interface and its default methods.",question:"Describe the built-in `Function<T, R>` interface and its default methods.",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Function<T, R>` represents a function that accepts one argument of type `T` and produces a result of type `R`. Its single abstract method is `R apply(T t)`. It provides two useful default methods: `andThen(Function)` composes so that the current function runs first then the other, and `compose(Function)` runs the other function first. A static `identity()` returns a function that returns its input unchanged.\n\n\n```java\nFunction<Integer, Integer> times2 = x -> x * 2;\nFunction<Integer, Integer> plus1  = x -> x + 1;\n\nFunction<Integer, Integer> composed = times2.andThen(plus1); // (x*2)+1\nSystem.out.println(composed.apply(5)); // 11\n\nFunction<String, String> id = Function.identity();\nSystem.out.println(id.apply("hi")); // hi\n```\n```\n',tech:"java",displayNumber:228,sortKey:228},{id:"java-229",number:229,title:"How does the `Predicate<T>` interface work and what default methods does it provide?",question:"How does the `Predicate<T>` interface work and what default methods does it provide?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Predicate<T>` represents a boolean-valued test on a single argument via `boolean test(T t)`. It offers default/static composition methods: `and(Predicate)` (logical AND), `or(Predicate)` (logical OR), `negate()` (logical NOT), and the static `Predicate.isEqual(Object)`. Predicates are heavily used with `Stream.filter` to select elements.\n\n\n```java\nPredicate<Integer> positive = n -> n > 0;\nPredicate<Integer> even     = n -> n % 2 == 0;\n\nPredicate<Integer> posEven = positive.and(even);\nPredicate<Integer> notPos  = positive.negate();\n\nList<Integer> nums = List.of(-2, -1, 0, 1, 2, 3, 4);\nnums.stream().filter(posEven).forEach(System.out::println); // 2, 4\n```\n```\n",tech:"java",displayNumber:229,sortKey:229},{id:"java-230",number:230,title:"What is the `Consumer<T>` interface used for?",question:"What is the `Consumer<T>` interface used for?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Consumer<T>` represents an operation that accepts a single input argument and returns no result. Its abstract method is `void accept(T t)`. It is commonly used for side effects such as printing or collecting. The default method `andThen(Consumer)` returns a composed consumer that performs the current action then the passed one.\n\n\n```java\nConsumer<String> shout = s -> System.out.println(s.toUpperCase());\nConsumer<String> greet = s -> System.out.println("Hi " + s);\n\nList.of("alice", "bob").forEach(shout);\n\nConsumer<String> both = shout.andThen(greet);\nboth.accept("carol"); // CAROL then Hi carol\n```\n```\n',tech:"java",displayNumber:230,sortKey:230},{id:"java-231",number:231,title:"When would you use the `Supplier<T>` interface?",question:"When would you use the `Supplier<T>` interface?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Supplier<T>` represents a supplier of results; it takes no arguments and returns a value via `T get()`. It is useful for deferred or lazy computation, generating values on demand, and providing factory methods. Because it accepts no input, it is the natural choice for lazy initialization and for factories passed to constructors or methods.\n\n\n```java\nSupplier<Double> random = () -> Math.random();\nSystem.out.println(random.get());\n\nSupplier<List<String>> listFactory = () -> new ArrayList<>();\nList<String> a = listFactory.get();\nList<String> b = listFactory.get(); // fresh list each call\n```\n```\n",tech:"java",displayNumber:231,sortKey:231},{id:"java-232",number:232,title:"What is the difference between `Function`, `BiFunction`, and `UnaryOperator`?",question:"What is the difference between `Function`, `BiFunction`, and `UnaryOperator`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Function<T,R>` takes one argument and returns a possibly different type. `BiFunction<T,U,R>` takes two arguments and returns a result. `UnaryOperator<T>` is a specialization of `Function<T,T>` where the argument and result are the same type, providing `andThen`/`compose`. Similarly `BinaryOperator<T>` extends `BiFunction<T,T,T>` and adds `minBy`/`maxBy` static helpers. Choosing the right type communicates intent and avoids unsafe casting.\n\n\n```java\nFunction<String, Integer> length = String::length;\nBiFunction<Integer, Integer, Integer> sum = Integer::sum;\nUnaryOperator<Integer> square = x -> x * x;\nBinaryOperator<Integer> max = BinaryOperator.maxBy(Integer::compare);\n```\n```\n",tech:"java",displayNumber:232,sortKey:232},{id:"java-233",number:233,title:"What are Default Methods in interfaces and why were they introduced?",question:"What are Default Methods in interfaces and why were they introduced?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Default methods, declared with the \`default\` keyword, let interfaces provide a concrete implementation for a method. They were introduced primarily to allow the Collections API to be evolved with lambda-friendly methods (like \`forEach\`, \`stream\`) without breaking every existing implementation. A class implementing the interface can use, override, or ignore the default. If a class implements two interfaces with conflicting default methods, the class must override the method to resolve the ambiguity.


\`\`\`java
interface Greeter {
    default void greet() { System.out.println("Hello"); }
}
class EnglishGreeter implements Greeter { } // inherits greet()

interface A { default void m() { System.out.println("A"); } }
interface B { default void m() { System.out.println("B"); } }
class C implements A, B {
    public void m() { A.super.m(); } // must resolve conflict explicitly
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:233,sortKey:233},{id:"java-234",number:234,title:"What are Static Methods in interfaces?",question:"What are Static Methods in interfaces?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'Static interface methods are utility methods associated with the interface itself, not with instances. They cannot be overridden by implementing classes and are invoked through the interface name (e.g., `Comparator.comparing(...)`). They were added in Java 8 so interfaces could carry factory and helper logic that previously lived in separate utility classes such as `Collections` or `Paths`.\n\n\n```java\nComparator<String> byLength = Comparator.comparing(String::length);\nList<String> list = List.of("a", "ccc", "bb");\nlist.stream().sorted(byLength).forEach(System.out::println);\n```\n```\n',tech:"java",displayNumber:234,sortKey:234},{id:"java-235",number:235,title:"How does Java resolve method invocation when multiple interfaces provide the same default method?",question:"How does Java resolve method invocation when multiple interfaces provide the same default method?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:`Java uses a strict set of rules: (1) Most specific class wins over interfaces, so an overriding class method takes precedence. (2) Among interfaces, a default method in a more specific (sub)interface wins over a less specific (super)interface. (3) If two independent interfaces declare the same default and neither is more specific, the compiler forces the implementing class to override the method; otherwise it is a compile-time error. The \`InterfaceName.super.method()\` syntax explicitly invokes a specific interface's default.


\`\`\`java
interface Animal { default void sound() { System.out.println("..."); } }
interface Pet extends Animal { default void sound() { System.out.println("pet"); } }
interface Robot { default void sound() { System.out.println("beep"); } }

class Cat implements Pet, Robot {
    public void sound() { Pet.super.sound(); } // must override: Pet and Robot conflict
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:235,sortKey:235},{id:"java-236",number:236,title:"What is the Stream API and what problems does it solve?",question:"What is the Stream API and what problems does it solve?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'The Stream API (in `java.util.stream`) provides a declarative, functional approach to processing sequences of data. A stream is not a data structure but a pipeline of operations over a source (collection, array, generator). It solves the verbosity of iterative loops by expressing what to compute rather than how, and it enables easy parallelization via `parallelStream()`. Streams are lazy (intermediate operations are not executed until a terminal operation is invoked) and consume-once (cannot be reused after a terminal operation).\n\n\n```java\nList<String> names = List.of("Alice", "Bob", "Charlie");\nlong count = names.stream()\n                  .filter(n -> n.length() > 3)\n                  .count();\nSystem.out.println(count); // 2\n```\n```\n',tech:"java",displayNumber:236,sortKey:236},{id:"java-237",number:237,title:"Explain the difference between intermediate and terminal stream operations.",question:"Explain the difference between intermediate and terminal stream operations.",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, `distinct`, `peek`, `limit`, `skip`) return a new stream and are always lazy—they do not process data until a terminal operation runs. Terminal operations (`forEach`, `collect`, `reduce`, `count`, `findFirst`, `anyMatch`, `toArray`) trigger the pipeline and produce a non-stream result (or side effect). After a terminal operation, the stream is considered consumed and can no longer be used.\n\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4);\nnums.stream()\n    .filter(n -> n % 2 == 0)   // intermediate (lazy)\n    .map(n -> n * 10)          // intermediate (lazy)\n    .forEach(System.out::println); // terminal (executes pipeline)\n```\n```\n",tech:"java",displayNumber:237,sortKey:237},{id:"java-238",number:238,title:"How does the `map` operation work in the Stream API?",question:"How does the `map` operation work in the Stream API?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`map(Function)` transforms each element of a stream into another object by applying a function, producing a new stream of the same size. It performs a one-to-one transformation. It is commonly used to extract fields, convert types, or compute derived values. The element type of the resulting stream may differ from the source.\n\n\n```java\nList<String> words = List.of("hello", "world");\nList<Integer> lengths = words.stream()\n                             .map(String::length)\n                             .toList();\nSystem.out.println(lengths); // [5, 5]\n```\n```\n',tech:"java",displayNumber:238,sortKey:238},{id:"java-239",number:239,title:"How does `flatMap` differ from `map`?",question:"How does `flatMap` differ from `map`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:`\`flatMap(Function)\` maps each element to a stream and then flattens all the resulting streams into a single stream. It is used for one-to-many transformations—for example, splitting sentences into words or extracting elements from nested collections. \`map\` would produce a stream of streams, whereas \`flatMap\` produces a single merged stream.


\`\`\`java
List<List<Integer>> matrix = List.of(List.of(1, 2), List.of(3, 4));
List<Integer> flat = matrix.stream()
                           .flatMap(List::stream)
                           .toList();
System.out.println(flat); // [1, 2, 3, 4]

List<String> sentences = List.of("hello world", "foo bar");
List<String> words = sentences.stream()
                              .flatMap(s -> Arrays.stream(s.split(" ")))
                              .toList();
\`\`\`
\`\`\`
`,tech:"java",displayNumber:239,sortKey:239},{id:"java-240",number:240,title:"What is the `filter` operation and how is it used with a Predicate?",question:"What is the `filter` operation and how is it used with a Predicate?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`filter(Predicate)` retains only the elements of a stream for which the predicate evaluates to `true`, producing a new stream of the same or smaller size. It does not modify the source; instead it lazily selects elements that satisfy the condition. Combined with other intermediate operations, `filter` is the primary mechanism for selecting data within a pipeline.\n\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4, 5, 6);\nList<Integer> evens = nums.stream()\n                          .filter(n -> n % 2 == 0)\n                          .toList();\nSystem.out.println(evens); // [2, 4, 6]\n```\n```\n",tech:"java",displayNumber:240,sortKey:240},{id:"java-241",number:241,title:"Explain the `reduce` operation with its three overloads.",question:"Explain the `reduce` operation with its three overloads.",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'`reduce` performs a reduction (fold) that combines stream elements into a single result using an associative accumulator. The three overloads are: (1) `reduce(identity, accumulator)` returns the accumulated value with an identity that is both the initial value and the result for an empty stream; (2) `reduce(accumulator)` returns an `Optional<T>` (no identity, useful when the stream may be empty); (3) `reduce(identity, accumulator, combiner)` for parallel streams where the combiner merges partial results from different threads. The accumulator must be associative and the identity must satisfy `accumulator.apply(identity, x) == x`.\n\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4);\nint sum1 = nums.stream().reduce(0, Integer::sum);            // identity 0\nOptional<Integer> sum2 = nums.stream().reduce(Integer::sum); // Optional[10]\nString joined = nums.stream()\n                    .map(String::valueOf)\n                    .reduce("", (a, b) -> a + b);            // "1234"\nSystem.out.println(sum1 + " " + sum2 + " " + joined);\n```\n```\n',tech:"java",displayNumber:241,sortKey:241},{id:"java-242",number:242,title:"How does the `collect` operation work, and what are Collectors?",question:"How does the `collect` operation work, and what are Collectors?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`collect` is a terminal operation that mutably accumulates stream elements into a result container (such as a `List`, `Set`, `Map`, or a summary). A `Collector` (from `java.util.stream.Collectors`) describes the reduction: a supplier for the container, an accumulator, a combiner for parallel execution, and a finisher. Common collectors include `toList()`, `toSet()`, `toMap()`, `joining()`, `groupingBy()`, `partitioningBy()`, and `summarizingInt()`.\n\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann");\nList<String> upper = names.stream().collect(Collectors.toList());\nSet<String> unique = names.stream().collect(Collectors.toSet());\nString csv = names.stream().collect(Collectors.joining(", "));\nSystem.out.println(csv); // Alice, Bob, Ann\n```\n```\n',tech:"java",displayNumber:242,sortKey:242},{id:"java-243",number:243,title:"What does `Collectors.groupingBy` do and what variations exist?",question:"What does `Collectors.groupingBy` do and what variations exist?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`groupingBy(classifier)` groups elements by a key derived from each element, returning a `Map<K, List<T>>`. Variations include `groupingBy(classifier, downstream)` to perform a downstream reduction on each group (e.g., `counting()`, `mapping()`, `summingInt()`), and `groupingBy(classifier, mapFactory, downstream)` to control the returned map type. `partitioningBy(Predicate)` is a specialized two-bucket grouping (true/false).\n\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann", "Brian");\nMap<Character, List<String>> byFirst =\n    names.stream().collect(Collectors.groupingBy(s -> s.charAt(0)));\nSystem.out.println(byFirst); // {A=[Alice, Ann], B=[Bob, Brian]}\n\nMap<Boolean, Long> countByLength =\n    names.stream().collect(Collectors.partitioningBy(s -> s.length() > 3, Collectors.counting()));\n```\n```\n',tech:"java",displayNumber:243,sortKey:243},{id:"java-244",number:244,title:"What is the difference between `collect` and `reduce` for building a Collection?",question:"What is the difference between `collect` and `reduce` for building a Collection?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"`reduce` is meant for immutable reduction producing a single value and is general-purpose but awkward for mutable containers because it typically creates a new accumulator at each step, which is inefficient and not parallel-friendly. `collect` is designed specifically for mutable reduction: it provides a mutable container via a supplier and folds elements into it, making it both efficient and naturally parallel. Generally prefer `collect`/`Collectors.toList()` over `reduce` for gathering into collections.\n\n\n```java\n// Inefficient: creates a new list at every step\nList<Integer> bad = List.of(1,2,3).stream()\n    .reduce(new ArrayList<Integer>(),\n            (list, e) -> { list.add(e); return list; },\n            (l1, l2) -> { l1.addAll(l2); return l1; });\n\n// Idiomatic and efficient\nList<Integer> good = List.of(1,2,3).stream().collect(Collectors.toList());\n```\n```\n",tech:"java",displayNumber:244,sortKey:244},{id:"java-245",number:245,title:"What is the `Optional` class and why was it introduced?",question:"What is the `Optional` class and why was it introduced?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Optional<T>` is a container object that may or may not hold a non-null value. It was introduced to represent the absence of a value explicitly, reducing `NullPointerException`s and eliminating ambiguous `null` returns. Instead of returning `null`, APIs can return `Optional.empty()` to signal "no value." It encourages the caller to handle the absent case explicitly. Note that `Optional` is intended for return types, not for fields, method parameters, or collection elements.\n\n\n```java\nOptional<String> name = Optional.ofNullable(findName(42));\nname.ifPresent(System.out::println);\nString result = name.orElse("default");\n```\n```\n',tech:"java",displayNumber:245,sortKey:245},{id:"java-246",number:246,title:"What are the common methods of the `Optional` class?",question:"What are the common methods of the `Optional` class?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Key methods include: `of(value)` (throws if null), `ofNullable(value)` (allows null → empty), `empty()`, `isPresent()`, `ifPresent(Consumer)`, `get()` (throws if empty—avoid), `orElse(default)`, `orElseGet(Supplier)` (lazy default), `orElseThrow()` / `orElseThrow(Supplier)`, `map(Function)`, `flatMap(Function)`, `filter(Predicate)`, and `stream()` (since Java 9, turns Optional into a 0/1 element stream). `orElseGet` is preferred over `orElse` when computing the default is expensive.\n\n\n```java\nOptional<User> user = findUser(1);\nString email = user\n    .map(User::email)\n    .filter(e -> e.contains("@"))\n    .orElseGet(() -> generateTempEmail()); // lazy\n```\n```\n',tech:"java",displayNumber:246,sortKey:246},{id:"java-247",number:247,title:"Why should you avoid using `Optional.get()` and what are safer alternatives?",question:"Why should you avoid using `Optional.get()` and what are safer alternatives?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.get()` throws `NoSuchElementException` if the Optional is empty, which is little better than a `NullPointerException` and defeats the purpose of `Optional`. Safer alternatives express intent: `orElse(default)` or `orElseGet(Supplier)` for a fallback, `ifPresent(Consumer)` for side effects, `orElseThrow(Supplier)` when absence is genuinely exceptional, and `isPresent()` checks only when necessary. Since Java 10, `orElseThrow()` with no args exists for the exceptional case.\n\n\n```java\nOptional<String> opt = Optional.empty();\n// String v = opt.get(); // NoSuchElementException - avoid\n\nString v = opt.orElse("fallback");\nopt.ifPresent(System.out::println);\nString must = opt.orElseThrow(() -> new IllegalStateException("missing"));\n```\n```\n',tech:"java",displayNumber:247,sortKey:247},{id:"java-248",number:248,title:"What are Method References and what are the four kinds?",question:"What are Method References and what are the four kinds?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Method references are a shorthand notation for lambdas that simply call an existing method. They improve readability. The four kinds are: (1) Static method reference: `ClassName::staticMethod`; (2) Instance method of a particular object: `instance::method`; (3) Instance method of an arbitrary object of a given type: `ClassName::instanceMethod` (the object becomes the first argument); (4) Constructor reference: `ClassName::new`. The target type must be a functional interface compatible with the method signature.\n\n\n```java\nList<String> names = List.of("a", "b");\nnames.forEach(System.out::println);        // instance method of particular object\n\nList<String> upper = names.stream()\n                          .map(String::toUpperCase)   // instance method of arbitrary object\n                          .toList();\nList<Integer> nums = names.stream().map(Integer::parseInt).toList(); // static\nSupplier<List<String>> factory = ArrayList::new;                     // constructor\n```\n```\n',tech:"java",displayNumber:248,sortKey:248},{id:"java-249",number:249,title:"How does `String::toUpperCase` as a method reference differ from `s -> s.toUpperCase()`?",question:"How does `String::toUpperCase` as a method reference differ from `s -> s.toUpperCase()`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'They are functionally equivalent and the compiler produces essentially the same bytecode. `String::toUpperCase` is a method reference of the "instance method of an arbitrary receiver of type String" kind: the stream element (the `String`) becomes the implicit `this` on which `toUpperCase()` is invoked. The lambda `s -> s.toUpperCase()` makes this explicit. Method references are preferred when they make the code clearer; lambdas are used when more complex logic is needed.\n\n\n```java\nStream<String> s = Stream.of("a", "b");\ns.map(String::toUpperCase).forEach(System.out::println); // cleaner\n// Equivalent lambda:\nStream.of("a","b").map(s2 -> s2.toUpperCase()).forEach(System.out::println);\n```\n```\n',tech:"java",displayNumber:249,sortKey:249},{id:"java-250",number:250,title:"What are Records in Java and what problem do they solve?",question:"What are Records in Java and what problem do they solve?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Records (preview in Java 14, final in Java 16) are a concise way to model immutable data carriers. A record automatically derives the canonical constructor, private final fields, accessor methods (named after the component, not \`getX\`), \`equals\`, \`hashCode\`, and \`toString\`. They eliminate the boilerplate of traditional POJOs used purely to hold data. Records are implicitly final and cannot extend other classes (but can implement interfaces). They are ideal for DTOs, value objects, and stream aggregate results.


\`\`\`java
// Before: lots of boilerplate
public class Person {
    private final String name;
    private final int age;
    public Person(String name, int age) { this.name = name; this.age = age; }
    public String getName() { return name; }
    public int getAge() { return age; }
    // equals, hashCode, toString...
}

// After: one line, same behavior
public record Person(String name, int age) { }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:250,sortKey:250},{id:"java-251",number:251,title:"How do you define a Record and what is auto-generated?",question:"How do you define a Record and what is auto-generated?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"A record is declared with the `record` keyword followed by a name and a parenthesized list of components. For `record Point(int x, int y)`, the compiler generates: a private final field for each component, a public accessor `x()` and `y()` (not `getX`), a canonical constructor `Point(int x, int y)`, plus `equals`, `hashCode`, and `toString`. You may add additional constructors, static fields/methods, and instance methods, and you may override the auto-generated members.\n\n\n```java\npublic record Point(int x, int y) { }\n\nPoint p = new Point(1, 2);\nSystem.out.println(p.x());        // 1\nSystem.out.println(p);            // Point[x=1, y=2]\n```\n```\n",tech:"java",displayNumber:251,sortKey:251},{id:"java-252",number:252,title:"Can a Record have custom constructors or additional methods?",question:"Can a Record have custom constructors or additional methods?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Yes. A record may declare an additional (non-canonical) constructor, but any additional constructor must eventually delegate to the canonical constructor via `this(...)`. You can use a compact canonical constructor to validate or normalize components without redeclaring parameters. You can also add static methods, static fields, and instance methods, and override `equals`/`hashCode`/`toString` if needed. You cannot add instance fields that are not components.\n\n\n```java\npublic record Range(int lo, int hi) {\n    public Range {                       // compact canonical constructor\n        if (lo > hi) throw new IllegalArgumentException("lo > hi");\n    }\n    public int size() { return hi - lo; } // additional method\n}\n```\n```\n',tech:"java",displayNumber:252,sortKey:252},{id:"java-253",number:253,title:"How are Records used effectively with the Stream API?",question:"How are Records used effectively with the Stream API?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Records make excellent intermediate or final results in stream pipelines because they are immutable and come with `equals`/`hashCode`/`toString` for free. They are commonly used as keys in `groupingBy`, as the element type after a `map`, or to bundle multiple computed values for downstream collectors like `summarizing` or `toMap`. Their value-based equality also makes them handy as `Set`/map keys for deduplication.\n\n\n```java\nrecord Person(String name, int age) { }\nList<Person> people = List.of(new Person("Ann", 30), new Person("Bob", 25));\nMap<String, Integer> nameToAge = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age));\n```\n```\n',tech:"java",displayNumber:253,sortKey:253},{id:"java-254",number:254,title:"What is Pattern Matching for `instanceof` (Java 16+) and how does it improve code?",question:"What is Pattern Matching for `instanceof` (Java 16+) and how does it improve code?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'Pattern matching for `instanceof` (standard in Java 16, preview earlier) lets you combine a type test with a binding of a typed variable in a single expression: `if (obj instanceof String s) { ... use s ... }`. The bound variable `s` is in scope only when the test succeeds and does not need an explicit cast. This removes the boilerplate `if (obj instanceof String) { String s = (String) obj; }` and reduces casting errors.\n\n\n```java\nObject obj = "hello";\nif (obj instanceof String s) {\n    System.out.println(s.length()); // no cast needed\n}\n```\n```\n',tech:"java",displayNumber:254,sortKey:254},{id:"java-255",number:255,title:"How does Pattern Matching for `switch` work in Java 17/21?",question:"How does Pattern Matching for `switch` work in Java 17/21?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Pattern matching for `switch` (preview in Java 17, finalized in Java 21) allows `case` labels to be type patterns and guards. You write `case String s -> ...`, `case Integer i -> ...`, and use `when` guards for refinements like `case Integer i when i > 0 -> ...`. The switch can mix type patterns, constants, and `default`/`case null` handling. It removes the need for chained `if (x instanceof ...)` blocks and provides exhaustiveness checking for sealed type hierarchies.\n\n\n```java\nstatic String classify(Object o) {\n    return switch (o) {\n        case Integer i when i > 0 -> "positive int";\n        case Integer i            -> "non-positive int";\n        case String s             -> "string: " + s;\n        case null                 -> "null";\n        default                   -> "other";\n    };\n}\n```\n```\n',tech:"java",displayNumber:255,sortKey:255},{id:"java-256",number:256,title:"What is the `case null` handling in modern switch expressions?",question:"What is the `case null` handling in modern switch expressions?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Traditional `switch` on a reference would throw `NullPointerException` if the selector was null and there was no null case. Modern pattern switch requires you to explicitly handle null: you can add `case null -> ...`, or, if you omit it, the compiler inserts an implicit null check that throws `NullPointerException` (to preserve the legacy behavior) unless a `case null` is present. This makes null handling explicit and avoids accidental NPEs.\n\n\n```java\nString result = switch (input) {     // input may be null\n    case null  -> "was null";\n    case "x"   -> "X";\n    default    -> "other";\n};\n```\n```\n',tech:"java",displayNumber:256,sortKey:256},{id:"java-257",number:257,title:"What are Sealed Classes (Java 17) and what problem do they solve?",question:"What are Sealed Classes (Java 17) and what problem do they solve?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Sealed classes (finalized in Java 17) restrict which other classes or interfaces may extend or implement them, using the `sealed` modifier and a `permits` clause. They solve the problem of wanting inheritance for a known, closed set of subtypes (e.g., an algebraic data type) while preventing arbitrary third-party subclasses. This enables exhaustive pattern matching in `switch` because the compiler knows all permitted subtypes. Subtypes must be `final`, `sealed`, or `non-sealed`.\n\n\n```java\npublic sealed interface Shape permits Circle, Rectangle, Square { }\npublic final class Circle    extends Shape { }\npublic final class Rectangle extends Shape { }\npublic non-sealed class Square extends Shape { }\n```\n```\n",tech:"java",displayNumber:257,sortKey:257},{id:"java-258",number:258,title:"What are the rules for permitted subclasses of a sealed type?",question:"What are the rules for permitted subclasses of a sealed type?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Each permitted subclass must be accessible to the sealed class and must declare itself as `final`, `sealed`, or `non-sealed`. A `final` subclass cannot be further extended; a `sealed` subclass must itself declare its own `permits`; a `non-sealed` subclass opens the hierarchy to arbitrary extension (breaking closure). All permitted subclasses must reside in the same module (or same package if unnamed module) as the sealed parent, and the `permits` list must name exactly the direct subclasses.\n\n\n```java\npublic sealed class Expr permits Const, Add, Var { }\npublic final class Const extends Expr { }\npublic sealed class Add extends Expr permits AddInt, AddDbl { }\npublic non-sealed class Var extends Expr { } // open to extension\n```\n```\n",tech:"java",displayNumber:258,sortKey:258},{id:"java-259",number:259,title:"How do Sealed Classes enable exhaustive `switch` pattern matching?",question:"How do Sealed Classes enable exhaustive `switch` pattern matching?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Because a sealed type's permitted subclasses are known at compile time, a `switch` over that type with a `case` for each permitted subtype (plus optionally `default`) is provably exhaustive. When all subtypes are covered, you can omit `default` and the compiler will error if a new subtype is later added without handling it—turning a runtime bug into a compile-time error. This is the Java equivalent of algebraic data type matching.\n\n\n```java\ndouble area(Shape s) {\n    return switch (s) {                       // exhaustive over permits\n        case Circle c    -> Math.PI * c.r() * c.r();\n        case Rectangle r -> r.w() * r.h();\n        case Square sq   -> sq.side() * sq.side();\n        // no default needed; compiler knows all cases\n    };\n}\n```\n```\n",tech:"java",displayNumber:259,sortKey:259},{id:"java-260",number:260,title:"What are Text Blocks (Java 15) and how do they improve multi-line strings?",question:"What are Text Blocks (Java 15) and how do they improve multi-line strings?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Text blocks, finalized in Java 15, let you write multi-line string literals using triple double-quotes (\`"""\`). They automatically handle newlines and reduce the need for concatenation and escape sequences. Indentation is managed by incidentally removing common leading whitespace based on the closing delimiter's position. Text blocks also simplify embedding JSON, SQL, HTML, or XML in code.


\`\`\`java
String json = """
    {
      "name": "Alice",
      "age": 30
    }
    """;
System.out.println(json);
\`\`\`
\`\`\`
`,tech:"java",displayNumber:260,sortKey:260},{id:"java-261",number:261,title:"How does Java handle indentation and escaping in Text Blocks?",question:"How does Java handle indentation and escaping in Text Blocks?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'For indentation, Java computes the minimum indentation of all non-blank content lines (relative to the closing `"""`) and strips that amount from every line—a process called incidental whitespace removal. You can shift the block by positioning the closing delimiter. Escape sequences like `\\n` and `\\"` still work but are usually unnecessary. The `\\` at the end of a line suppresses the line terminator (for a single logical line), and `String.translateEscapes()` (Java 15) handles escapes. The result is a normal `String`.\n\n\n```java\nString sql = """\n    SELECT id, name \\\n    FROM users \\\n    WHERE active = true\n    """;\n// produced String has no newlines between clauses (line continuation via \\)\n```\n```\n',tech:"java",displayNumber:261,sortKey:261},{id:"java-262",number:262,title:"What is the `var` keyword (Java 10) and what are its limitations?",question:"What is the `var` keyword (Java 10) and what are its limitations?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`var` is local-variable type inference: the compiler infers the type from the initializer, so you write `var list = new ArrayList<String>();` instead of spelling out the type. It is limited to local variables with initializers, indexes in enhanced `for` loops, and locals in `try-with-resources`. You cannot use `var` for fields, method parameters, return types, or without an initializer. The type is still static and strongly typed—`var` is not `dynamic` or a `Object` wildcard.\n\n\n```java\nvar name = "Alice";            // String\nvar numbers = List.of(1, 2, 3);// List<Integer>\nfor (var n : numbers) { }      // int\n// var x; // error: needs initializer\n```\n```\n',tech:"java",displayNumber:262,sortKey:262},{id:"java-263",number:263,title:"When should you avoid using `var`?",question:"When should you avoid using `var`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Avoid `var` when the inferred type is not obvious from the initializer, which hurts readability—e.g., `var result = someMethod();` where the method's return type is unclear. Also avoid it when the code relies on a specific type for overloading or when mixing numeric types where the literal type (`var x = 5L` vs `var x = 5`) matters. A good rule: use `var` when it makes code cleaner, keep explicit types when the type carries meaning or aids comprehension.\n\n\n```java\n// Less clear\nvar data = fetch(); // what type? avoid\n// Clearer\nList<User> users = fetchUsers(); // explicit type helps here\n```\n```\n",tech:"java",displayNumber:263,sortKey:263},{id:"java-264",number:264,title:"What are Sequenced Collections (Java 21) and which interfaces were added?",question:"What are Sequenced Collections (Java 21) and which interfaces were added?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Sequenced Collections (Java 21) fill a gap where the collections framework lacked a universal type for collections with a defined encounter order. Three interfaces were added: `SequencedCollection` (extends `Collection`, has `reversed()`, `addFirst`, `addLast`, `getFirst`, `getLast`, `removeFirst`, `removeLast`), `SequencedSet` (extends `SequencedCollection` and `Set`), and `SequencedMap` (with `reversed()`, `firstEntry`, `lastEntry`, `putFirst`, `putLast`). `List`, `Deque`, `LinkedHashSet`, and `SortedMap` now implement these, providing uniform first/last and reverse-view access.\n\n\n```java\n// Uniform first/last access across List and LinkedHashSet\nSequencedCollection<String> list = new ArrayList<>(List.of("a", "b", "c"));\nSequencedSet<String> set = new LinkedHashSet<>(List.of("a", "b", "c"));\n\nlist.getFirst();          // "a"\nlist.getLast();           // "c"\nset.reversed();           // reverse view: [c, b, a]\n```\n```\n',tech:"java",displayNumber:264,sortKey:264},{id:"java-265",number:265,title:"How does the `reversed()` view work in Sequenced Collections?",question:"How does the `reversed()` view work in Sequenced Collections?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`reversed()` returns a reverse-ordered *view* of the collection, not a copy. Mutations to the original are reflected in the reversed view and vice versa. This is efficient and uniform across `List`, `SequencedSet`, and `SequencedMap`. It replaces ad-hoc reverse iteration and makes encounter-order operations consistent. Because it is a view, calling `reversed().reversed()` yields a view back to the original orientation.\n\n\n```java\nSequencedCollection<String> seq = new ArrayList<>(List.of("a", "b", "c"));\nSequencedCollection<String> rev = seq.reversed();\nSystem.out.println(rev);      // [c, b, a]\nrev.addFirst("z");            // affects the original list too\nSystem.out.println(seq);      // [z, a, b, c]\n```\n```\n',tech:"java",displayNumber:265,sortKey:265},{id:"java-266",number:266,title:"What are Virtual Threads (Java 21) and how do they differ from platform threads?",question:"What are Virtual Threads (Java 21) and how do they differ from platform threads?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:`Virtual threads (Project Loom, finalized in Java 21) are lightweight, user-mode threads managed by the JVM rather than the operating system. They are cheap to create (millions can exist) and are scheduled on a small pool of carrier OS threads (platform threads) via the ForkJoinPool. When a virtual thread blocks on I/O, the JVM unmounts it from its carrier, allowing the carrier to run other virtual threads—so blocking is cheap. Platform threads remain for CPU-bound or native-code-heavy tasks. Virtual threads dramatically improve throughput for I/O-bound concurrent code.


\`\`\`java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    for (int i = 0; i < 10_000; i++) {
        executor.submit(() -> {
            Thread.sleep(Duration.ofMillis(100)); // cheap blocking
            return "done";
        });
    }
} // all tasks complete; executor closed
\`\`\`
\`\`\`
`,tech:"java",displayNumber:266,sortKey:266},{id:"java-267",number:267,title:"How do you create and run a Virtual Thread?",question:"How do you create and run a Virtual Thread?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'You can create a virtual thread in three main ways: (1) `Thread.startVirtualThread(Runnable)`, (2) `Thread.ofVirtual().start(Runnable)`, or (3) via `Executors.newVirtualThreadPerTaskExecutor()` which creates a new virtual thread per submitted task. Virtual threads are daemon threads by default and cannot be pooled like platform threads—you create a fresh one per task. `Thread.isVirtual()` distinguishes them at runtime.\n\n\n```java\nThread vt = Thread.startVirtualThread(() -> System.out.println("running"));\nThread.ofVirtual().name("worker").start(() -> {});\n\ntry (var ex = Executors.newVirtualThreadPerTaskExecutor()) {\n    ex.submit(() -> "task");\n}\n```\n```\n',tech:"java",displayNumber:267,sortKey:267},{id:"java-268",number:268,title:"What are the best practices and pitfalls when using Virtual Threads?",question:"What are the best practices and pitfalls when using Virtual Threads?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Best practices: use one virtual thread per task (do not pool them), avoid `synchronized` blocks on shared monitors inside hot paths (they pin the carrier thread—use `ReentrantLock` instead if long-held), and be cautious with thread-local usage (millions of virtual threads make thread-locals expensive). Don't use virtual threads for CPU-bound work where they offer no benefit. Avoid `Thread.sleep` in tight loops for signaling; prefer `LockSupport`. Virtual threads are not faster for computation, only for concurrency under blocking.\n\n\n```java\n// Pinning problem: synchronized pins carrier thread\nsynchronized (lock) { doBlockingIo(); } // avoid in virtual threads\n\n// Prefer ReentrantLock\nlock.lock();\ntry { doBlockingIo(); } finally { lock.unlock(); }\n```\n```\n",tech:"java",displayNumber:268,sortKey:268},{id:"java-269",number:269,title:"How do Virtual Threads relate to the existing `ExecutorService` API?",question:"How do Virtual Threads relate to the existing `ExecutorService` API?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Virtual threads integrate cleanly with the existing concurrency API: `Executors.newVirtualThreadPerTaskExecutor()` returns an `ExecutorService` so existing code using `submit`/`invokeAll` works unchanged. The executor creates a new virtual thread per task rather than reusing pool threads. You should call `close()` (it is `AutoCloseable`) to wait for all tasks to finish. This backward compatibility lets you adopt virtual threads without rewriting concurrency logic.\n\n\n```java\ntry (ExecutorService es = Executors.newVirtualThreadPerTaskExecutor()) {\n    List<Future<String>> futures = IntStream.range(0, 100)\n        .mapToObj(i -> es.submit(() -> "r" + i))\n        .toList();\n    for (Future<String> f : futures) System.out.println(f.get());\n}\n```\n```\n',tech:"java",displayNumber:269,sortKey:269},{id:"java-270",number:270,title:"What is the `Stream.toList()` method (Java 16) and how does it differ from `Collectors.toList()`?",question:"What is the `Stream.toList()` method (Java 16) and how does it differ from `Collectors.toList()`?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Stream.toList()` (Java 16) is a convenience terminal operation that returns an immutable `List`. It is simpler than `Collectors.toList()`, which returns a mutable `List` (mutable per spec). Because `toList()` returns an unmodifiable list, attempting to add/remove throws `UnsupportedOperationException`. Use `toList()` when you want a result you won't mutate; use `Collectors.toList()` or `Collectors.toCollection(...)` when you need a mutable list.\n\n\n```java\nList<Integer> imm = Stream.of(1, 2, 3).toList();   // unmodifiable\nList<Integer> mut = Stream.of(1,2,3).collect(Collectors.toList()); // mutable\n// imm.add(4); // throws UnsupportedOperationException\n```\n```\n",tech:"java",displayNumber:270,sortKey:270},{id:"java-271",number:271,title:"What is the `Stream.takeWhile` and `dropWhile` (Java 9) operations?",question:"What is the `Stream.takeWhile` and `dropWhile` (Java 9) operations?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`takeWhile(predicate)` returns the longest prefix of elements that satisfy the predicate, stopping at the first element that fails (best for ordered streams). `dropWhile(predicate)` drops the longest prefix that satisfies the predicate and returns the remainder. They are order-sensitive unlike `filter`, which evaluates every element. They are useful when processing streams where a condition holds for an initial segment.\n\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4, 1);\nSystem.out.println(nums.stream().takeWhile(n -> n < 3).toList()); // [1, 2]\nSystem.out.println(nums.stream().dropWhile(n -> n < 3).toList()); // [3, 4, 1]\n```\n```\n",tech:"java",displayNumber:271,sortKey:271},{id:"java-272",number:272,title:"What are the new `Optional` methods added in Java 9/10/11?",question:"What are the new `Optional` methods added in Java 9/10/11?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Several enhancements: `Optional.stream()` (Java 9) converts empty to an empty stream and present to a single-element stream, making it easy to flatMap optionals. `Optional.ifPresentOrElse(action, emptyAction)` (Java 9) runs one of two actions. `Optional.or(Supplier)` (Java 9) returns this optional or a fallback supplier. `Optional.orElseThrow()` with no args (Java 10) throws `NoSuchElementException` if empty. These reduce branching when working with optional values.\n\n\n```java\nOptional<String> o = Optional.empty();\no.ifPresentOrElse(System.out::println, () -> System.out.println("none"));\nOptional<String> fallback = o.or(() -> Optional.of("def"));\nStream<String> s = o.stream(); // empty stream\n```\n```\n',tech:"java",displayNumber:272,sortKey:272},{id:"java-273",number:273,title:"How does `Collectors.toMap` handle duplicate keys and how can you control it?",question:"How does `Collectors.toMap` handle duplicate keys and how can you control it?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"The two-arg `toMap(keyMapper, valueMapper)` throws `IllegalStateException` on duplicate keys. To control this, use the three-arg overload `toMap(keyMapper, valueMapper, mergeFunction)`, where the merge function decides how to combine values for the same key. A four-arg overload also accepts a `mapFactory` to choose the returned map type (e.g., `LinkedHashMap::new` for ordering). The merge function receives the existing and new value and returns the value to keep.\n\n\n```java\nMap<String, Integer> byName = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age, (a, b) -> a)); // keep first\nMap<String, Integer> lhm = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age, (a,b)->b, LinkedHashMap::new));\n```\n```\n",tech:"java",displayNumber:273,sortKey:273},{id:"java-274",number:274,title:"What is the difference between `map` and `peek` in streams?",question:"What is the difference between `map` and `peek` in streams?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`peek(Consumer)` is an intermediate operation intended for observing elements as they flow through the pipeline (e.g., logging or debugging) without modifying them; it returns the same stream of unchanged elements. `map(Function)` is for transforming elements into new values and changing the stream type. Using `peek` to mutate state is discouraged; use `map` when you want to change data and `forEach` (terminal) when you only want a side effect at the end.\n\n\n```java\nList<Integer> result = Stream.of(1, 2, 3)\n    .peek(n -> System.out.println("before: " + n))\n    .map(n -> n * 2)\n    .peek(n -> System.out.println("after: " + n))\n    .toList();\n```\n```\n',tech:"java",displayNumber:274,sortKey:274},{id:"java-275",number:275,title:"How do you use `Comparator` with method references for sorting streams?",question:"How do you use `Comparator` with method references for sorting streams?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Comparator.comparing(keyExtractor)` builds a comparator from a function that extracts a comparable key, and you can pass a method reference as the extractor. Chain with `thenComparing` for secondary sort. `Comparator.reverseOrder()` and `reversed()` flip ordering. This is far cleaner than writing anonymous comparators and integrates directly with `Stream.sorted`.\n\n\n```java\nList<Person> sorted = people.stream()\n    .sorted(Comparator.comparing(Person::age).thenComparing(Person::name))\n    .toList();\nList<String> desc = words.stream()\n    .sorted(Comparator.reverseOrder())\n    .toList();\n```\n```\n",tech:"java",displayNumber:275,sortKey:275},{id:"java-276",number:276,title:"What are some useful `Collectors` for numeric aggregation?",question:"What are some useful `Collectors` for numeric aggregation?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'For numeric aggregation: `summingInt/Long/Double`, `averagingInt/...`, `summarizingInt/...` (returns `IntSummaryStatistics` with count/sum/avg/min/max), `counting()`, and `reducing(...)`. These are useful as downstream collectors in `groupingBy`. For grouping with aggregation, combine them: `groupingBy(Person::city, summingInt(Person::age))`.\n\n\n```java\nIntSummaryStatistics stats = people.stream()\n    .collect(Collectors.summarizingInt(Person::age));\nSystem.out.println(stats.getAverage() + " " + stats.getMax());\nMap<String, Double> avgByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.averagingInt(Person::age)));\n```\n```\n',tech:"java",displayNumber:276,sortKey:276},{id:"java-277",number:277,title:"How does `Collectors.partitioningBy` differ from `groupingBy`?",question:"How does `Collectors.partitioningBy` differ from `groupingBy`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`partitioningBy(Predicate)` is a specialized form of grouping that always produces a `Map<Boolean, List<T>>` with exactly two keys: `true` and `false`. It is slightly more efficient and clearer when you simply want to split elements into "matching" vs "non-matching". `groupingBy(classifier)` produces a `Map<K, List<T>>` keyed by arbitrary classifier results (any type), and works for more than two buckets. `partitioningBy` also accepts a downstream collector.\n\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann", "Brian");\n\n// partitioningBy: exactly two buckets\nMap<Boolean, List<String>> p = names.stream()\n    .collect(Collectors.partitioningBy(s -> s.startsWith("A")));\n// {false=[Bob, Brian], true=[Alice, Ann]}\n\n// groupingBy: arbitrary keys\nMap<Character, List<String>> g = names.stream()\n    .collect(Collectors.groupingBy(s -> s.charAt(0)));\n// {A=[Alice, Ann], B=[Bob, Brian]}\n```\n```\n',tech:"java",displayNumber:277,sortKey:277},{id:"java-278",number:278,title:"What is `flatMap` commonly used for with `Optional` and `Stream`?",question:"What is `flatMap` commonly used for with `Optional` and `Stream`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.flatMap(Function)` chains another `Optional` returning function: it returns the inner `Optional` rather than wrapping it, avoiding `Optional<Optional<T>>`. In streams, `flatMap` converts each element to a stream and concatenates them. Together, `Optional::stream` (Java 9) and `flatMap` let you flatten a `Stream<Optional<T>>` into a `Stream<T>`, filtering out empties: `streamOfOptionals.flatMap(Optional::stream)`.\n\n\n```java\nList<Optional<String>> opts = List.of(Optional.of("a"), Optional.empty(), Optional.of("b"));\nList<String> present = opts.stream()\n    .flatMap(Optional::stream)\n    .toList(); // ["a", "b"]\n```\n```\n',tech:"java",displayNumber:278,sortKey:278},{id:"java-279",number:279,title:"How do you handle checked exceptions in lambda expressions?",question:"How do you handle checked exceptions in lambda expressions?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Lambdas can only throw checked exceptions if the target functional interface's method declares them. Since standard interfaces like `Runnable`, `Function`, `Consumer` do not declare checked exceptions, you cannot directly throw them. Workarounds: (1) Wrap the checked exception in an unchecked one (e.g., `throw new RuntimeException(e)`), (2) Use a wrapper method that catches and wraps, (3) Define your own functional interface that declares the checked exception, (4) Use try-catch inside the lambda body. There is no built-in mechanism in the standard functional interfaces to pass checked exceptions through.\n\n\n```java\nFunction<String, Integer> parse = s -> {\n    try { return Integer.parseInt(s); }\n    catch (NumberFormatException e) { throw new RuntimeException(e); }\n};\n\n// Or define your own\n@FunctionalInterface interface CheckedFunction<T,R,E extends Exception> {\n    R apply(T t) throws E;\n}\n```\n```\n",tech:"java",displayNumber:279,sortKey:279},{id:"java-280",number:280,title:"What is the difference between `Stream.of` and `Arrays.stream` for creating streams?",question:"What is the difference between `Stream.of` and `Arrays.stream` for creating streams?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Stream.of(T...)` works for object arrays and varargs of objects. `Arrays.stream(array)` has overloads for primitive arrays (`int[]`, `long[]`, `double[]`) producing `IntStream`/`LongStream`/`DoubleStream`, and an `Object[]` overload that returns `Stream<T>`. For primitives, `Arrays.stream` is the direct way to get a primitive stream; `Stream.of(int[])` would treat the array as a single element. For object varargs, `Stream.of` is more convenient.\n\n\n```java\nint[] arr = {1, 2, 3};\nIntStream is = Arrays.stream(arr);   // correct for primitives\n// Stream.of(arr) would be Stream<int[]>, not IntStream\n\nString[] objs = {"a", "b"};\nStream<String> s1 = Stream.of(objs);\nStream<String> s2 = Arrays.stream(objs);\n```\n```\n',tech:"java",displayNumber:280,sortKey:280},{id:"java-281",number:281,title:"What are Primitive Streams (`IntStream`, `LongStream`, `DoubleStream`) and why use them?",question:"What are Primitive Streams (`IntStream`, `LongStream`, `DoubleStream`) and why use them?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Primitive streams avoid boxing/unboxing overhead for `int`, `long`, `double` values. They provide specialized methods: `sum()`, `average()`, `min()`, `max()`, `range()`, `rangeClosed()`, and `toArray()`. Converting between object and primitive streams uses `mapToInt`, `boxed()`, etc. Primitive streams are significantly faster and use less memory for numeric-heavy workloads.\n\n\n```java\nIntStream.rangeClosed(1, 100).sum();          // 5050\nIntStream.of(1, 2, 3).average().orElse(0);    // 2.0\nint[] squares = IntStream.range(0, 5).map(i -> i*i).toArray();\n```\n```\n",tech:"java",displayNumber:281,sortKey:281},{id:"java-282",number:282,title:"How do you convert between object streams and primitive streams?",question:"How do you convert between object streams and primitive streams?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"To go from object to primitive: `mapToInt(ToIntFunction)`, `mapToLong`, `mapToDouble`. To go from primitive to object: `boxed()` (e.g., `IntStream.boxed()` returns `Stream<Integer>`). The `mapToObj` methods on primitive streams convert to object streams. These conversions are necessary when mixing numeric pipelines with generic collections.\n\n\n```java\nList<Integer> nums = List.of(1, 2, 3);\nint sum = nums.stream().mapToInt(Integer::intValue).sum(); // object -> primitive\nStream<Integer> back = IntStream.of(1,2,3).boxed();        // primitive -> object\n```\n```\n",tech:"java",displayNumber:282,sortKey:282},{id:"java-283",number:283,title:"What is the `Collectors.joining` method and how is it used?",question:"What is the `Collectors.joining` method and how is it used?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Collectors.joining()` concatenates the string representations of stream elements. The no-arg version joins with no delimiter. Overloads accept a delimiter, and a delimiter + prefix + suffix. It is the idiomatic replacement for `StringBuilder` loops and is more readable and parallel-friendly.\n\n\n```java\nList<String> parts = List.of("a", "b", "c");\nString csv = parts.stream().collect(Collectors.joining(", "));\nString wrapped = parts.stream().collect(Collectors.joining(", ", "[", "]"));\nSystem.out.println(csv);     // a, b, c\nSystem.out.println(wrapped); // [a, b, c]\n```\n```\n',tech:"java",displayNumber:283,sortKey:283},{id:"java-284",number:284,title:"What are the `minBy` and `maxBy` collectors and how are they used?",question:"What are the `minBy` and `maxBy` collectors and how are they used?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.minBy(Comparator)` and `Collectors.maxBy(Comparator)` are reduction collectors that return an `Optional` containing the minimum or maximum element according to the comparator. They are useful as downstream collectors in `groupingBy` to find the min/max per group. For the whole stream, `Stream.min/max` is simpler; `minBy/maxBy` shine when you need the extremum per group.\n\n\n```java\nMap<String, Optional<Person>> oldestByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.maxBy(Comparator.comparing(Person::age))));\n```\n```\n",tech:"java",displayNumber:284,sortKey:284},{id:"java-285",number:285,title:"How does `Stream.distinct()` work and what determines equality?",question:"How does `Stream.distinct()` work and what determines equality?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`distinct()` returns a stream of unique elements by removing duplicates based on `equals()`. It is a stateful intermediate operation (it must remember seen elements). For ordered streams, the first occurrence is kept; for unordered streams (like those from `Set`), the choice is nondeterministic but duplicates are removed. For records, `equals` compares component values, making `distinct` effective for deduplication.\n\n\n```java\nList<String> unique = Stream.of("a", "b", "a", "c").distinct().toList(); // [a, b, c]\n```\n```\n',tech:"java",displayNumber:285,sortKey:285},{id:"java-286",number:286,title:"What is the difference between `sorted()` and `sorted(Comparator)`?",question:"What is the difference between `sorted()` and `sorted(Comparator)`?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`sorted()` sorts elements using their natural order (requires elements to implement `Comparable`). `sorted(Comparator)` sorts using the provided comparator, which allows custom ordering or sorting types that don\'t implement `Comparable`. Both are stateful intermediate operations; `sorted()` on an unordered source may still produce a deterministic order.\n\n\n```java\nList.of(3, 1, 2).stream().sorted().toList();                    // [1, 2, 3]\nList.of("b", "a").stream().sorted(Comparator.reverseOrder()).toList(); // [b, a]\n```\n```\n',tech:"java",displayNumber:286,sortKey:286},{id:"java-287",number:287,title:"What is the purpose of `Stream.iterate` and `Stream.generate`?",question:"What is the purpose of `Stream.iterate` and `Stream.generate`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Stream.iterate(seed, unaryOperator)` produces an infinite sequential stream by repeatedly applying the function to the previous result (e.g., `iterate(0, n -> n + 2)` yields even numbers). `Stream.generate(supplier)` produces an infinite stream by repeatedly invoking a `Supplier` (e.g., `generate(Math::random)`). Both are unordered unless limited. Java 9 added `iterate(seed, predicate, unaryOperator)` which stops when the predicate fails, giving a finite stream.\n\n\n```java\nStream.iterate(0, n -> n + 2).limit(5).toList();      // [0, 2, 4, 6, 8]\nStream.iterate(0, n -> n < 10, n -> n + 2).toList();  // Java 9: [0, 2, 4, 6, 8]\nStream.generate(() -> (int)(Math.random()*10)).limit(5).toList(); // random\n```\n```\n",tech:"java",displayNumber:287,sortKey:287},{id:"java-288",number:288,title:"How do you make a Stream parallel and when is it beneficial?",question:"How do you make a Stream parallel and when is it beneficial?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Call `parallelStream()` on a collection or `.parallel()` on an existing stream. Parallel streams use a common `ForkJoinPool` and split the work into chunks processed by multiple threads. They are beneficial for CPU-intensive operations on large datasets with minimal synchronization (e.g., numeric reductions, complex mappings). They add overhead, so they can be slower for small streams or I/O-bound work where virtual threads (Java 21) are better suited.\n\n\n```java\nlong sum = LongStream.rangeClosed(1, 10_000_000).parallel().sum(); // CPU-bound, large data\n// For I/O-bound: use virtual threads instead of parallel streams\n```\n```\n",tech:"java",displayNumber:288,sortKey:288},{id:"java-289",number:289,title:"What is the common `ForkJoinPool` used by parallel streams and how can you isolate workloads?",question:"What is the common `ForkJoinPool` used by parallel streams and how can you isolate workloads?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"All parallel streams share the common `ForkJoinPool.commonPool()` (configured via system properties like `java.util.concurrent.ForkJoinPool.common.parallelism`). This means a long-running parallel stream can starve other parallel work. To isolate, submit the parallel stream task to a custom `ForkJoinPool`: `new ForkJoinPool(n).submit(() -> stream.parallel()...).join()`. This prevents thread starvation in mixed workloads.\n\n\n```java\nForkJoinPool pool = new ForkJoinPool(4);\npool.submit(() -> bigData.parallelStream().map(heavy).toList()).join();\n```\n```\n",tech:"java",displayNumber:289,sortKey:289},{id:"java-290",number:290,title:"What is the `Record Patterns` feature in Java 21 and how does it work?",question:"What is the `Record Patterns` feature in Java 21 and how does it work?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'Record patterns (Java 21) allow deconstructing a record directly in a pattern: `case Point(int x, int y) -> ...`. The components are bound to variables matching their types. You can also use nested record patterns: `case Pair(Point(int x, int y), String label) -> ...`. Record patterns compose with type patterns, `switch` expressions, and `instanceof`. They eliminate boilerplate accessor calls for destructuring.\n\n\n```java\nrecord Point(int x, int y) {}\nstatic String quadrant(Point p) {\n    return switch (p) {\n        case Point(var x, var y) when x > 0 && y > 0 -> "I";\n        case Point(var x, var y) when x < 0 && y > 0 -> "II";\n        // ...\n    };\n}\n```\n```\n',tech:"java",displayNumber:290,sortKey:290},{id:"java-291",number:291,title:"What are `switch` expressions with `yield` and how do they differ from statements?",question:"What are `switch` expressions with `yield` and how do they differ from statements?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Switch expressions (Java 12/14) return a value and use `->` for single expressions or `{ ... yield value; }` for blocks. They must be exhaustive (cover all cases) and cannot fall through. A `yield` statement provides the value for a block case. This replaces the old switch statement with `break` and separate assignment. The expression form is preferred for assignments and ternary-like logic.\n\n\n```java\nString result = switch (day) {\n    case MONDAY, FRIDAY -> "busy";\n    case SATURDAY, SUNDAY -> "weekend";\n    default -> { yield "midweek"; } // block with yield\n};\n```\n```\n',tech:"java",displayNumber:291,sortKey:291},{id:"java-292",number:292,title:"What is the difference between `yield`, `return`, and `break` in modern switch?",question:"What is the difference between `yield`, `return`, and `break` in modern switch?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'In a `switch` expression: `yield` returns a value from a block case and exits the switch expression (required for block cases). `break` with a value (`break value`) is not allowed in expressions; it was a preview syntax replaced by `yield`. `return` exits the *enclosing method*, not just the switch. In a traditional switch statement, `break` exits the switch; `yield` is not allowed there.\n\n\n```java\n// Switch expression - must yield\nint x = switch (v) { case 1 -> { yield 10; } default -> 0; };\n\n// Switch statement - uses break\nswitch (v) { case 1: System.out.println("one"); break; }\n```\n```\n',tech:"java",displayNumber:292,sortKey:292},{id:"java-293",number:293,title:"How do you use `Collectors.mapping` as a downstream collector?",question:"How do you use `Collectors.mapping` as a downstream collector?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.mapping(mapper, downstream)` transforms each element before passing it to the downstream collector. It is commonly used with `groupingBy` when you want to group by one property but collect a different property. It allows decoupling the key from the collected value. For example, `groupingBy(Person::city, mapping(Person::name, toList()))` gives `Map<String, List<String>>`.\n\n\n```java\nMap<String, List<String>> namesByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.mapping(Person::name, Collectors.toList())));\n```\n```\n",tech:"java",displayNumber:293,sortKey:293},{id:"java-294",number:294,title:"What is the `Optional.or` method and how does it differ from `orElse`/`orElseGet`?",question:"What is the `Optional.or` method and how does it differ from `orElse`/`orElseGet`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.or(Supplier<Optional<T>>)` returns this Optional if present, otherwise returns the Optional produced by the supplier. Unlike `orElse`/`orElseGet` which unwrap and return the contained value `T`, `or` returns an `Optional<T>`, preserving the Optional container. This enables chaining multiple optional fallbacks: `opt1.or(() -> opt2).or(() -> opt3)`.\n\n\n```java\nOptional<String> a = Optional.empty();\nOptional<String> b = Optional.of("b");\nOptional<String> c = a.or(() -> b); // Optional["b"]\n```\n```\n',tech:"java",displayNumber:294,sortKey:294},{id:"java-295",number:295,title:"How do you use `Collectors.collectingAndThen` for post-processing?",question:"How do you use `Collectors.collectingAndThen` for post-processing?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.collectingAndThen(downstream, finisher)` applies a finisher function to the result of the downstream collector. It is useful for transforming the collector's output, e.g., making an immutable copy: `collectingAndThen(toList(), Collections::unmodifiableList)`. It can also adapt to different return types or enforce invariants after collection. It runs after the downstream reduction completes.\n\n\n```java\nList<String> unmod = stream.collect(Collectors.collectingAndThen(Collectors.toList(), Collections::unmodifiableList));\n```\n```\n",tech:"java",displayNumber:295,sortKey:295},{id:"java-296",number:296,title:"What are the rules for `switch` exhaustiveness with sealed types?",question:"What are the rules for `switch` exhaustiveness with sealed types?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:`A switch on a sealed type is exhaustive if every permitted subtype is covered by a case. If the switch is exhaustive, no \`default\` is required. If a new permitted subtype is added to the sealed hierarchy and the switch is not updated, it becomes a compile-time error—ensuring no case is missed. For non-sealed types, \`default\` is required unless all possible values are covered (e.g., \`enum\` switches are exhaustive by default).


\`\`\`java
sealed interface Shape permits Circle, Square {}
record Circle(int r) implements Shape {}
record Square(int s) implements Shape {}

double area(Shape sh) {
    return switch (sh) {
        case Circle c -> Math.PI * c.r() * c.r();
        case Square s -> s.s() * s.s();
        // no default needed; exhaustive
    };
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:296,sortKey:296},{id:"java-297",number:297,title:"How does `Stream.concat` work and what are its constraints?",question:"How does `Stream.concat` work and what are its constraints?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Stream.concat(a, b)` creates a lazily concatenated stream whose elements are all elements of `a` followed by all elements of `b`. It is an ordered stream if both inputs are ordered. The resulting stream is a sequential stream even if inputs are parallel; you must call `.parallel()` again if needed. The spliterator is composed, so it may have poorer performance characteristics than a single source.\n\n\n```java\nStream<Integer> combined = Stream.concat(Stream.of(1, 2), Stream.of(3, 4));\ncombined.forEach(System.out::print); // 1 2 3 4\n```\n```\n",tech:"java",displayNumber:297,sortKey:297},{id:"java-298",number:298,title:"What is the `java.time` API and how does it relate to functional programming?",question:"What is the `java.time` API and how does it relate to functional programming?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"The `java.time` API (Java 8) is a modern, immutable, thread-safe date/time API. While not part of the Stream/Functional interfaces, it integrates well: `LocalDate`, `LocalDateTime`, `Instant`, `Duration`, `Period` are all immutable value types. They work naturally with lambdas (e.g., `Stream.iterate(LocalDate.now(), d -> d.plusDays(1))`) and streams. It replaces the flawed `java.util.Date`/`Calendar`.\n\n\n```java\nLocalDate start = LocalDate.of(2024, 1, 1);\nStream.iterate(start, d -> d.plusWeeks(1))\n      .limit(4)\n      .forEach(System.out::println);\n```\n```\n",tech:"java",displayNumber:298,sortKey:298},{id:"java-299",number:299,title:"What are some common stream anti-patterns to avoid?",question:"What are some common stream anti-patterns to avoid?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'Common anti-patterns: (1) Using streams for simple loops where `for` is clearer; (2) Modifying external state inside `peek`/`forEach` (side effects break parallelism); (3) Using `parallelStream()` without measuring—overhead can outweigh benefit; (4) Calling `stream()` on a collection while mutating it (concurrent modification); (5) Using `findFirst` on unordered parallel streams (nondeterministic); (6) Collecting to a list then mutating it—use `toList()` or a concurrent collector; (7) Ignoring that streams are consume-once.\n\n\n```java\n// Bad: side effect in peek\nList<String> bad = new ArrayList<>();\nStream.of("a", "b").peek(bad::add).count();\n\n// Good: collect directly\nList<String> good = Stream.of("a", "b").toList();\n```\n```\n',tech:"java",displayNumber:299,sortKey:299},{id:"java-300",number:300,title:"What is the `SequencedMap` interface and its new methods in Java 21?",question:"What is the `SequencedMap` interface and its new methods in Java 21?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`SequencedMap` (Java 21) extends `Map` and adds encounter-order semantics plus first/last access. New methods: `sequencedKeySet()` (returns `SequencedSet<K>`), `sequencedValues()` (returns `SequencedCollection<V>`), `sequencedEntrySet()` (returns `SequencedSet<Entry<K,V>>`), `firstEntry()`, `lastEntry()`, `pollFirstEntry()`, `pollLastEntry()`, `putFirst(K, V)`, `putLast(K, V)`, and `reversed()` (returns a reversed view). Implementations include `LinkedHashMap`, `TreeMap`, `IdentityHashMap`. It enables uniform first/last access regardless of underlying implementation.\n\n\n```java\nSequencedMap<String, Integer> map = new LinkedHashMap<>();\nmap.put("a", 1); map.put("b", 2);\nmap.putFirst("z", 0);          // z, a, b\nMap.Entry<String, Integer> first = map.firstEntry(); // z=0\nSequencedMap<String, Integer> rev = map.reversed();  // b, a, z\n```\n```\n',tech:"java",displayNumber:300,sortKey:300},{id:"java-301",number:301,title:"What are the main memory areas of the JVM?",question:"What are the main memory areas of the JVM?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`The JVM divides memory into several runtime data areas. The most important are:
- **Heap**: shared across all threads; holds all object instances and arrays. This is where GC happens.
- **Stack (Java Virtual Machine Stack)**: per-thread; holds frames with local variables, operand stack, and return info.
- **Metaspace** (since Java 8, replacing PermGen): stores class metadata (methods, bytecode, symbols).
- **Code Cache**: holds native code produced by the JIT compiler.
- **Program Counter (PC) Register**: per-thread; points to the current instruction.
- **Native Method Stack**: for native (JNI) method calls.

Thread-private areas (PC, stack, native stack) are reclaimed when the thread ends. Heap and metaspace are shared and managed by GC.


\`\`\`java
// Inspect memory areas using JVM flags at startup:
// java -Xmx512m -Xms512m -XX:MaxMetaspaceSize=128m -XX:+PrintGCDetails MyApp
//
// Programmatic peek at the runtime memory:
public class MemoryAreas {
    public static void main(String[] args) {
        Runtime rt = Runtime.getRuntime();
        System.out.println("Max heap:  " + rt.maxMemory()  / 1024 / 1024 + " MB");
        System.out.println("Total heap:" + rt.totalMemory()/ 1024 / 1024 + " MB");
        System.out.println("Free heap: " + rt.freeMemory() / 1024 / 1024 + " MB");
        long threadId = Thread.currentThread().getId(); // own PC + stack
        System.out.println("Thread id: " + threadId);
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:301,sortKey:301},{id:"java-302",number:302,title:"What is the difference between the heap and the stack?",question:"What is the difference between the heap and the stack?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`- **Heap** is shared by all threads and holds objects/arrays. Allocation is dynamic; lifetime is managed by GC. Access is slower and it can throw \`OutOfMemoryError\`.
- **Stack** is per-thread; each method call pushes a frame holding local primitives and object references (not the objects themselves). It is LIFO, fast, and fixed-size per thread (\`-Xss\`). Excessive recursion throws \`StackOverflowError\`.

A common confusion: a \`List<String>\` reference lives on the stack; the \`ArrayList\` object and its elements live on the heap.


\`\`\`java
public class HeapVsStack {
    public static void main(String[] args) {
        int x = 10;                 // primitive 'x' -> stack
        StringBuilder sb = new StringBuilder(); // reference on stack, object on heap
        sb.append("hello");
    }
    // Deep recursion exhausts the STACK, not the heap:
    static void recurse(int n) { recurse(n + 1); } // -> StackOverflowError
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:302,sortKey:302},{id:"java-303",number:303,title:"What is Metaspace and how does it differ from PermGen?",question:"What is Metaspace and how does it differ from PermGen?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"Before Java 8, class metadata lived in a fixed-size **PermGen** (part of the heap), causing `OutOfMemoryError: PermGen space`. Since Java 8, **Metaspace** uses native memory (outside the heap), auto-growing by default up to the OS limit (or `-XX:MaxMetaspaceSize`). This removed most PermGen OOMs. Metaspace is still collected by GC, but only when classes are unloaded (e.g., when their classloader is collected).\n\nKey flags: `-XX:MetaspaceSize` (initial threshold that triggers GC), `-XX:MaxMetaspaceSize`, `-XX:MinMetaspaceFreeRatio`.\n\n\n```java\n// Java 7 (PermGen):                      Java 8+ (Metaspace):\n// -XX:PermSize=64m                        -XX:MetaspaceSize=64m\n// -XX:MaxPermSize=256m                    -XX:MaxMetaspaceSize=256m\n// String pool + class meta in heap        Class meta in native memory\n// -> OutOfMemoryError: PermGen space      -> auto-grows, bounded by MaxMetaspaceSize\n```\n```\n",tech:"java",displayNumber:303,sortKey:303},{id:"java-304",number:304,title:"What is the Code Cache and what fills it?",question:"What is the Code Cache and what fills it?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'The **Code Cache** is a native-memory region holding machine code compiled by the JIT compilers (C1/C2). When a method is "hot" enough, the JIT compiles its bytecode into optimized native code, which is stored here and executed instead of interpreted bytecode. If the code cache fills (`-XX:ReservedCodeCacheSize`, default ~240 MB), the JIT stops compiling and the JVM falls back to the slower interpreter — a subtle performance cliff.\n\nFlags: `-XX:ReservedCodeCacheSize`, `-XX:+UseCodeCacheFlushing`.\n\n\n```java\n// Tune the code cache when you have MANY hot methods (meta-frameworks, big apps):\n// java -XX:ReservedCodeCacheSize=512m -XX:+UseCodeCacheFlushing MyApp\n//\n// Watch with JIT logging:\n// java -XX:+PrintCompilation -XX:+UnlockDiagnosticVMOptions -XX:+PrintCodeCache MyApp\n```\n```\n',tech:"java",displayNumber:304,sortKey:304},{id:"java-305",number:305,title:"What are Compressed OOPs and why do they matter?",question:"What are Compressed OOPs and why do they matter?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**Compressed Ordinary Object Pointers (Compressed OOPs)** encode 64-bit heap pointers into 32-bit values by assuming all objects are 8-byte aligned. This shifts the pointer left by 3 bits, so 32 bits can address up to 32 GB (2³² × 8). Benefits: smaller object headers and references → better CPU cache density and throughput. Enabled by default on 64-bit JVMs when heap < ~32 GB. Beyond 32 GB the JVM disables them, making each reference 8 bytes — so a 32 GB heap can actually hold *fewer* objects than a 31 GB one. This is the famous "heap looks bigger but holds less" effect.

Flag: \`-XX:+UseCompressedOops\` (default on).


\`\`\`java
// Sweet spot: keep heap under 32 GB to keep compressed oops.
// java -Xmx31g -XX:+UseCompressedOops MyApp   // oops remain 32-bit
// java -Xmx40g                                // compressed oops auto-disabled
//
// Verify it is on:
// java -Xmx31g -XX:+PrintFlagsFinal -version | grep UseCompressedOops
// -> bool UseCompressedOops = true
\`\`\`
\`\`\`
`,tech:"java",displayNumber:305,sortKey:305},{id:"java-306",number:306,title:"What is the object header and how does it affect memory?",question:"What is the object header and how does it affect memory?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Every Java object has a header: a **mark word** (8 bytes: hashcode, GC age, locking state, biased-lock info) and a **klass pointer** (8 bytes, or 4 bytes with compressed oops). So an empty \`new Object()\` is 16 bytes (8 + 4 + 4 padding) with compressed oops, or 16 bytes (8 + 8) without. Arrays add a 4-byte length field. The header overhead matters a lot for small objects (e.g., an \`Integer\` is 16 bytes overhead for 4 bytes of data). This is why object churn and tiny wrappers are expensive.


\`\`\`java
import java.lang.instrument.Instrumentation;

public class ObjectSize {
    // Add: java -javaagent:sizeof.jar -XX:+UseCompressedOops MyApp
    static Instrumentation inst;
    public static void premain(String a, Instrumentation i) { inst = i; }
    public static void main(String[] args) {
        System.out.println("Object:    " + inst.getObjectSize(new Object()));    // 16
        System.out.println("Integer:   " + inst.getObjectSize(Integer.valueOf(1)));// 16
        System.out.println("int[0]:    " + inst.getObjectSize(new int[0]));        // 16 (header+len+pad)
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:306,sortKey:306},{id:"java-307",number:307,title:"What is the difference between a Java process's native memory and the Java heap?",question:"What is the difference between a Java process's native memory and the Java heap?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`The **Java heap** is only one consumer of a process's total memory. The OS sees the whole process, which also includes: thread stacks (\`-Xss\` each, ×number of threads), Metaspace, Code Cache, GC control structures, direct ByteBuffers (\`-XX:MaxDirectMemorySize\`), JIT, and the JVM native libraries. A process can OOM on the OS even when the heap is not full — the classic "why is my 4 GB heap using 8 GB RSS?" question. Tools: \`jcmd <pid> VM.native_memory summary\` (Native Memory Tracking).


\`\`\`java
// Enable Native Memory Tracking and inspect:
// java -XX:NativeMemoryTracking=detail -XX:MaxDirectMemorySize=256m MyApp
// jcmd <pid> VM.native_memory summary
//
// Sample output categories:
//   Java Heap    (reserved/committed)
//   Class        (metaspace)
//   Thread       (stacks)
//   Code         (code cache)
//   Internal     (direct buffers, GC)
\`\`\`
\`\`\`
`,tech:"java",displayNumber:307,sortKey:307},{id:"java-308",number:308,title:"What are escape analysis and scalar replacement?",question:"What are escape analysis and scalar replacement?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**Escape analysis** (C2, enabled via \`-XX:+DoEscapeAnalysis\`, default on) determines whether an object's reference escapes the method/thread. If a newly-created object does not escape, the JIT can: (1) **stack-allocate** it instead of heap-allocating, and (2) **scalar-replace** it — break it into its individual fields (scalars) held in registers/locals, eliminating the object entirely. This removes allocation and GC pressure. If the object *does* escape (returned, stored in a field, passed to another thread), it must be heap-allocated.


\`\`\`java
public class EscapeAnalysis {
    // 'p' does not escape -> scalar replacement / stack allocation possible.
    static int sum(int a, int b) {
        Point p = new Point(a, b); // may be eliminated entirely
        return p.x + p.y;
    }
    static Point leak(int a, int b) {
        return new Point(a, b);    // escapes -> heap allocated
    }
    static class Point { int x, y; Point(int x,int y){this.x=x;this.y=y;} }
}
// Demonstrate allocation elimination:
// java -XX:+PrintGCDetails -XX:-DoEscapeAnalysis EscapeAnalysis   // more GC
// java -XX:+PrintGCDetails -XX:+DoEscapeAnalysis EscapeAnalysis   // fewer/faster
\`\`\`
\`\`\`
`,tech:"java",displayNumber:308,sortKey:308},{id:"java-309",number:309,title:"How is memory laid out for a multithreaded program in the JVM?",question:"How is memory laid out for a multithreaded program in the JVM?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Each thread gets its own **PC register** and **JVM stack** (with one frame per nested method call). Threads share the **heap**, **metaspace**, and **code cache**. The stack size is set by \`-Xss\` (default ~1 MB on Linux). Too many threads × stack size → native \`OutOfMemoryError: unable to create new native thread\`. Work-stealing / GC threads also consume native memory. Local variables of primitive type are thread-private; object references on the stack point into the shared heap.


\`\`\`java
public class ThreadMemory {
    static int shared = 0; // in heap (class static field)
    public static void main(String[] args) throws Exception {
        Runnable r = () -> {
            int local = 42;          // on THIS thread's stack
            System.out.println(local);
        };
        Thread t = new Thread(r);
        t.start(); t.join();
        // -Xss controls per-thread stack: java -Xss256k MyApp
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:309,sortKey:309},{id:"java-310",number:310,title:"What is a memory barrier / happens-before and how does it relate to the JVM memory model?",question:"What is a memory barrier / happens-before and how does it relate to the JVM memory model?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`The **JVM Memory Model (JMM, JSR-133)** defines **happens-before** rules guaranteeing one thread's writes are visible to another. It is implemented with **memory barriers** (CPU/fence instructions) inserted by the JIT around volatile fields, synchronized blocks, and \`final\` fields. Key rules: program order (within a thread), monitor lock (unlock happens-before subsequent lock), volatile write happens-before volatile read, thread start/join, and \`final\` field safe publication. Without a happens-before edge, the JIT/CPU may reorder or cache reads, causing subtle bugs. This is about *visibility and ordering*, not just heap/stack layout.


\`\`\`java
public class HappensBefore {
    private volatile boolean ready = false;
    private int value = 0;

    void writer() {
        value = 42;          // write
        ready = true;        // volatile write -> barrier: flushes 'value'
    }
    void reader() {
        if (ready) {         // volatile read -> sees prior writes
            System.out.println(value); // always 42
        }
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:310,sortKey:310},{id:"java-311",number:311,title:"What is object reachability and how does GC determine live objects?",question:"What is object reachability and how does GC determine live objects?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`An object is **reachable** if it can be accessed through a chain of references starting from **GC roots**. Unreachable objects are considered garbage and eligible for collection. Reachability is computed by a **mark** phase that traverses the object graph from roots; anything not marked is dead. Note "eligible" ≠ "collected" — collection happens later during a GC cycle. \`System.gc()\` is only a *suggestion*.


\`\`\`java
public class Reachability {
    public static void main(String[] args) {
        Object a = new Object(); // 'a' is a root reference -> reachable
        Object b = a;            // b points to same object
        a = null;
        // object still reachable via 'b'
        b = null;
        // now unreachable -> eligible for GC on next cycle
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:311,sortKey:311},{id:"java-312",number:312,title:"What are GC roots?",question:"What are GC roots?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`**GC roots** are the starting points of the reachability graph. They include:
- Local variables and parameters on active thread stacks.
- Active Java threads themselves.
- Static fields of loaded classes.
- JNI (native) references / global JNI handles.
- Synchronized monitors (objects held as locks).
- Objects referenced by the JVM itself (e.g., classloaders, some internal structures).

Anything reachable by following references from these roots is live; everything else is garbage.


\`\`\`java
public class GCRoots {
    static Object staticRoot = new Object(); // GC root: static field (class-level)
    public static void main(String[] args) {
        Object localRoot = new Object();     // GC root: local var on stack
        synchronized (localRoot) {           // monitor held -> also a root
            // while here, localRoot is strongly reachable
        }
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:312,sortKey:312},{id:"java-313",number:313,title:"Describe the Mark-Sweep-Compact algorithm.",question:"Describe the Mark-Sweep-Compact algorithm.",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Three classic phases:
- **Mark**: traverse from GC roots, mark every reachable object.
- **Sweep**: walk the heap, reclaim memory of unmarked (dead) objects, adding them to a free list.
- **Compact**: move live objects together to eliminate fragmentation, updating references.

Mark-Sweep alone leaves fragmentation; Compact fixes it but is expensive (must update all pointers). This trio underlies most collectors' old-generation handling.


\`\`\`java
Mark-Sweep-Compact lifecycle:
  Heap before: [L][D][L][D][D][L]   (L=live, D=dead)
  Mark:        [L][ ][L][ ][ ][L]
  Sweep:       [L][_][L][_][_][L]   (free list grows)
  Compact:     [L][L][L][_][_][_]   (no fragmentation, refs updated)
\`\`\`
\`\`\`
`,tech:"java",displayNumber:313,sortKey:313},{id:"java-314",number:314,title:"What is the difference between strong, soft, weak, and phantom references?",question:"What is the difference between strong, soft, weak, and phantom references?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Java has four reference strengths, giving you varying control over GC eligibility:
- **Strong** (\`new Object()\`): never collected while reachable.
- **Soft** (\`SoftReference\`): collected only when the JVM *needs* memory; great for caches.
- **Weak** (\`WeakReference\`): collected on the next GC regardless; basis of \`WeakHashMap\`.
- **Phantom** (\`PhantomReference\`): enqueued after finalization for post-mortem cleanup; must go through a \`ReferenceQueue\` and doesn't give access to the object.


\`\`\`java
import java.lang.ref.*;
import java.util.WeakHashMap;

public class References {
    public static void main(String[] args) {
        Object strong = new Object();
        SoftReference<Object> soft = new SoftReference<>(new Object());
        WeakReference<Object> weak = new WeakReference<>(new Object());
        PhantomReference<Object> phantom = new PhantomReference<>(
            new Object(), new ReferenceQueue<>());

        System.gc();
        System.out.println(soft.get());  // maybe non-null
        System.out.println(weak.get());  // almost certainly null now
        System.out.println(phantom.get()); // ALWAYS null

        WeakHashMap<Object,String> cache = new WeakHashMap<>();
        cache.put(new Object(), "v"); // entry vanishes when key is GC'd
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:314,sortKey:314},{id:"java-315",number:315,title:"What is finalization and why is it discouraged?",question:"What is finalization and why is it discouraged?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`\`finalize()\` was a hook called by the GC before reclaiming an object. It is discouraged because: (1) calls are unpredictable and may never run (fast exit, no GC); (2) objects with a non-trivial \`finalize()\` are promoted to a finalization queue, surviving an extra GC cycle (hurts performance); (3) it can resurrect objects; (4) it serializes cleanup on a single thread. Modern Java uses **try-with-resources** / \`AutoCloseable\` and \`java.lang.ref.Cleaner\` / \`PhantomReference\` instead. \`finalize()\` is deprecated since Java 9 and will be removed.


\`\`\`java
import java.lang.ref.Cleaner;

public class NoFinalize {
    private static final Cleaner cleaner = Cleaner.create();
    private final Cleaner.Cleanable cleanable;
    private final Resource res;

    NoFinalize() {
        res = new Resource();
        cleanable = cleaner.register(this, res::close); // runs when GC'd
    }
    // Prefer explicit close(); Cleaner is a fallback only.
    public void close() { cleanable.clean(); }

    static class Resource { void close() { System.out.println("cleaned"); } }

    // DON'T: protected void finalize() { ... } // deprecated, slow, unreliable
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:315,sortKey:315},{id:"java-316",number:316,title:"What is the generational hypothesis?",question:"What is the generational hypothesis?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"The **weak generational hypothesis** observes that most objects die young (short-lived), and few references point from old objects to young ones. The JVM exploits this by splitting the heap into **young** and **old** generations, collecting the young generation frequently and cheaply (most objects die there), and the old generation rarely. This makes GC far more efficient than a single monolithic heap scan.\n\n\n```java\nGenerational hypothesis (empirically ~98% of objects die young):\n  Young Gen: collected often, fast, cheap (most garbage here)\n  Old Gen:   collected rarely, slow, expensive (long-lived survivors)\n```\n```\n",tech:"java",displayNumber:316,sortKey:316},{id:"java-317",number:317,title:"Describe the young generation layout: Eden and Survivor spaces.",question:"Describe the young generation layout: Eden and Survivor spaces.",difficulty:"Basic",category:"JVM Architecture & GC",answer:`The **young generation** is split into:
- **Eden**: where new objects are allocated.
- **Survivor spaces (S0, S1)**: hold objects that survived a young GC. Only one survivor space is active at a time; a young GC copies live Eden + active survivor objects into the inactive survivor space (or promotes them to old gen if too old/full). This is a **copying collector** — Eden is emptied each young GC. Default ratio: young ≈ 1/3 of heap, Eden : survivors ≈ 8 : 1 : 1.


\`\`\`java
Young Gen layout (default):
  +---------+-------+-------+
  |  Eden   |  S0   |  S1   |
  | 80%     | 10%   | 10%   |
  +---------+-------+-------+
After a Minor GC: live objects copied S0->S1 (or promoted to Old), Eden+S0 cleared.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:317,sortKey:317},{id:"java-318",number:318,title:"What is a Minor GC, Major GC, and Full GC?",question:"What is a Minor GC, Major GC, and Full GC?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Minor GC**: collects the **young generation** only. Frequent, fast, stop-the-world.
- **Major GC**: collects the **old generation** only (terminology varies by collector).
- **Full GC**: collects **entire heap + metaspace**. Rare, slow, long pauses.
In HotSpot, "Major GC" and "Full GC" are often conflated; the logs (\`-Xlog:gc*\`) are the source of truth. A Full GC is what you want to avoid in latency-sensitive apps.


\`\`\`java
// Modern GC logging (Java 9+ unified logging):
// java -Xlog:gc*:file=gc.log:time,uptime,level -jar MyApp.jar
//
// Sample:
// [0.3s] GC(0) Pause Young (Allocation Failure) 25M->5M(100M) 12ms   <- Minor
// [1.2s] GC(3) Pause Full (Ergonomics)           90M->40M(512M) 220ms <- Full
\`\`\`
\`\`\`
`,tech:"java",displayNumber:318,sortKey:318},{id:"java-319",number:319,title:"What is object tenuring and the tenuring threshold?",question:"What is object tenuring and the tenuring threshold?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Objects that survive multiple young GCs are eventually **tenured** (promoted) to the old generation. The **tenuring threshold** is the number of young-GC survivals before promotion (tracked as an object's "age" in its header, max 15). \`-XX:MaxTenuringThreshold\` sets the cap. Premature promotion floods the old gen; overly high thresholds keep objects bouncing between survivor spaces. Adaptive sizing (\`-XX:+UseAdaptiveSizePolicy\`, default) tunes this automatically.


\`\`\`java
// java -XX:MaxTenuringThreshold=15 -XX:+PrintTenuringDistribution MyApp
//
// Log shows age histogram each minor GC:
// Desired survivor size 5M, new threshold 15 (max 15)
// - age   1:   2M bytes,   2M total
// - age   2: 500K bytes, 2.5M total
// Objects reaching threshold -> promoted to Old Gen.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:319,sortKey:319},{id:"java-320",number:320,title:"What is a promotion failure and how does it cause a Full GC?",question:"What is a promotion failure and how does it cause a Full GC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`During a Minor GC, surviving young objects must be promoted into the old generation. If the old gen lacks contiguous space (fragmentation) or is nearly full, a **promotion failure** occurs. The JVM then triggers a **Full GC** to compact the old gen and make room — turning a cheap minor pause into a long full pause. This is a classic cause of unpredictable latency spikes. Mitigations: larger old gen, lower promotion rate, or a collector with concurrent old-gen collection (G1/ZGC/Shenandoah).


\`\`\`java
// Promotion failure symptom in logs:
// [2.1s] GC(5) Pause Young (Allocation Failure)
// [2.1s] GC(5) Promotion failed
// [2.1s] GC(6) Pause Full (Allocation Failure) ... long pause
//
// Fix paths:
//  - increase old gen (lower -XX:NewRatio)
//  - reduce allocation rate (object reuse, pooling)
//  - switch to G1/ZGC with concurrent old-gen collection
\`\`\`
\`\`\`
`,tech:"java",displayNumber:320,sortKey:320},{id:"java-321",number:321,title:"Compare Serial, Parallel, CMS, G1, ZGC, and Shenandoah collectors.",question:"Compare Serial, Parallel, CMS, G1, ZGC, and Shenandoah collectors.",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"- **Serial** (`-XX:+UseSerialGC`): single-threaded; young + old. Simplest, lowest overhead for tiny heaps/CLI tools.\n- **Parallel (Throughput)** (`-XX:+UseParallelGC`): multi-threaded young + old, focuses on max throughput, longer STW pauses.\n- **CMS** (`-XX:+UseConcMarkSweepGC`, removed in Java 14): concurrent old-gen marking to reduce pause, but fragments and can fall back to Full GC. Deprecated.\n- **G1** (`-XX:+UseG1GC`, default since Java 9): region-based, incremental, predictable pause goals via `-XX:MaxGCPauseMillis`.\n- **ZGC** (`-XX:+UseZGC`, production since Java 15): concurrent, sub-millisecond pauses regardless of heap size (TB-scale), uses colored pointers + load barriers.\n- **Shenandoah** (OpenJDK, `-XX:+UseShenandoahGC`): concurrent compaction, low pauses, region-based like G1 but with concurrent move.\n\n\n```java\n// Select a collector:\n// java -XX:+UseSerialGC        MyApp   // tiny apps, single core\n// java -XX:+UseParallelGC      MyApp   // batch/throughput\n// java -XX:+UseG1GC            MyApp   // balanced default (Java 9+)\n// java -XX:+UseZGC             MyApp   // low-latency, large heaps\n// java -XX:+UseShenandoahGC    MyApp   // low-latency (Red Hat/OpenJDK)\n//\n// CMS removed in JDK 14 -> do not use in modern Java.\n```\n```\n",tech:"java",displayNumber:321,sortKey:321},{id:"java-322",number:322,title:"How does the Serial GC work?",question:"How does the Serial GC work?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"Serial GC uses a **single thread** for both young (copying) and old (mark-sweep-compact) collections, pausing all application threads (STW) during each. It has the smallest footprint and simplest implementation, making it ideal for small heaps (< 100 MB), single-processor machines, or short-lived CLI tools. It is a poor choice for multi-core servers where parallel collectors shine.\n\n\n```java\n// Enable Serial GC:  java -XX:+UseSerialGC -Xmx64m MyApp\n// Best for: embedded, CLI utilities, constrained containers.\n// Avoid for: web servers, multi-core, latency-sensitive services.\n```\n```\n",tech:"java",displayNumber:322,sortKey:322},{id:"java-323",number:323,title:"How does the Parallel (Throughput) GC work?",question:"How does the Parallel (Throughput) GC work?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'Parallel GC (a.k.a. "Throughput Collector") uses **multiple threads** for both minor and full GCs, maximizing throughput (work done per unit time) at the cost of longer stop-the-world pauses. It was the default in Java 8. Key tunables: `-XX:ParallelGCThreads`, and adaptive sizing goals `-XX:MaxGCPauseMillis` and `-XX:GCTimeRatio` (e.g., 99 = 1% time in GC).\n\n\n```java\n// java -XX:+UseParallelGC -XX:ParallelGCThreads=8 -XX:GCTimeRatio=99 MyApp\n//   GCTimeRatio=99 -> GC time <= 1% of total (throughput goal)\n//   MaxGCPauseMillis=N -> hint for pause goal (best-effort)\n// Goal: maximize throughput, accept longer STW pauses.\n```\n```\n',tech:"java",displayNumber:323,sortKey:323},{id:"java-324",number:324,title:"How does the Concurrent Mark Sweep (CMS) collector work and why was it removed?",question:"How does the Concurrent Mark Sweep (CMS) collector work and why was it removed?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`CMS collected the **old generation concurrently** with the application to minimize pause times. Phases: initial mark (STW, short) → concurrent mark → remark (STW, short) → concurrent sweep. It did **not** compact, so the old gen fragmented over time, eventually forcing a long stop-the-world **Full GC** to defragment. Combined with its complexity and bugginess, it was deprecated in Java 9 and **removed in Java 14**, superseded by G1/ZGC/Shenandoah.


\`\`\`java
// Deprecated/removed:  java -XX:+UseConcMarkSweepGC MyApp
//   error: 'CMS' is deprecated and will likely be removed in a future release
//   removed entirely in JDK 14.
// Problems: old-gen fragmentation -> promotion failures -> Full GC.
// Modern replacement: G1 (default) or ZGC/Shenandoah for low latency.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:324,sortKey:324},{id:"java-325",number:325,title:"How does G1 GC organize the heap and why is it the default?",question:"How does G1 GC organize the heap and why is it the default?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:'**G1 (Garbage-First)** splits the heap into ~2048 equal **regions** (each 1–32 MB), each dynamically tagged as Eden, Survivor, Old, or Humongous (for large objects > half a region). Instead of collecting generations wholesale, G1 tracks region **liveness** and collects the regions with the most garbage first ("garbage-first"). It can meet a **pause-time goal** (`-XX:MaxGCPauseMillis`, default 200 ms) by limiting how many regions it evacuates per cycle, using a **mixed collection** that reclaims both young and some old regions. It compacts concurrently-ish (evacuation), avoiding fragmentation. Default since Java 9.\n\n\n```java\n// G1 region view:\n// [E][E][S][O][H][E][O][O][E][S]...   each ~1-32 MB\n// Collector picks high-garbage regions first to hit pause goal.\n// java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -XX:G1HeapRegionSize=4m MyApp\n```\n```\n',tech:"java",displayNumber:325,sortKey:325},{id:"java-326",number:326,title:"What is a humongous object in G1 and how is it handled?",question:"What is a humongous object in G1 and how is it handled?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`In G1, an object larger than **half a region** is a **humongous object**, allocated directly into one or more contiguous **Humongous regions** (outside young/old). Humongous objects are expensive: they are collected only during a Full GC or a **concurrent marking / mixed cycle**, and can fragment the heap quickly. Allocating many short-lived large arrays is a known G1 anti-pattern. Mitigation: increase region size (\`-XX:G1HeapRegionSize\`) or reduce large allocations.


\`\`\`java
public class Humongous {
    public static void main(String[] args) {
        // A 3 MB array in a 4 MB region -> humongous (needs > 2 MB)
        byte[] big = new byte[3 * 1024 * 1024]; // allocated in humongous regions
        // Many of these churn -> G1 pressure; consider pooling or bigger regions.
    }
}
// java -Xmx1g -XX:+UseG1GC -XX:G1HeapRegionSize=8m Humongous
// Logs: "Humongous regions: N" with -Xlog:gc*=info
\`\`\`
\`\`\`
`,tech:"java",displayNumber:326,sortKey:326},{id:"java-327",number:327,title:"How does ZGC achieve sub-millisecond pauses?",question:"How does ZGC achieve sub-millisecond pauses?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`ZGC's hallmark is **concurrent** collection: almost all work (marking, relocation/compaction, reference remapping) happens *while the app runs*, with only tiny (sub-ms) STW pauses for root scanning. Two key mechanisms:
- **Colored pointers**: 64-bit pointers embed metadata (mark, remap, relocated bits) in unused high bits, so the GC can track object state without modifying the object.
- **Load barriers**: every object load is intercepted to fix up references pointing to relocated objects (self-healing pointers).
This keeps pause times independent of heap size — from MBs to multi-TB.


\`\`\`java
// java -XX:+UseZGC -Xmx16g -XX:+UnlockExperimentalVMOptions MyApp  (JDK 11-15 exp)
// java -XX:+UseZGC -Xmx16g MyApp                                (JDK 15+ prod)
//
// ZGC phases (all concurrent except tiny pauses):
//   - Concurrent Mark     (colored pointers + load barriers)
//   - Concurrent Relocate (move objects, leave forwarding info)
//   - Load barrier fixes stale refs on access (self-healing)
// Result: typical pauses < 1-2 ms regardless of heap size.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:327,sortKey:327},{id:"java-328",number:328,title:"How does Shenandoah GC differ from G1 and ZGC?",question:"How does Shenandoah GC differ from G1 and ZGC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Shenandoah is a **low-pause, concurrent compaction** collector (originally Red Hat, now in OpenJDK). Like G1 it uses regions, but its differentiator is **concurrent evacuation** of old regions (G1 only evacuates young + selected old in mixed GCs; Shenandoah moves live objects concurrently with the app). It uses a **Brooks pointer** (a forwarding pointer field in each object) plus read/write barriers to make concurrent moves safe. ZGC achieves similar goals with colored pointers + load barriers instead. Both target < 10 ms pauses; Shenandoah historically ran on a wider range of JDK versions and heaps.


\`\`\`java
// java -XX:+UseShenandoahGC -Xmx8g MyApp
//
// Shenandoah phases (concurrent):
//   Init Mark (STW) -> Concurrent Mark -> Final Mark (STW)
//   Concurrent Compaction (evacuate regions concurrently)
//   Brooks pointer + barriers handle in-flight refs.
// vs ZGC: colored-pointer load barrier vs Brooks-pointer R/W barrier.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:328,sortKey:328},{id:"java-329",number:329,title:"What is the String Deduplication feature in G1?",question:"What is the String Deduplication feature in G1?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"**String deduplication** (`-XX:+UseStringDeduplication`, G1-only) scans the heap for equal `String` contents and makes them share the same underlying `char[]`/`byte[]` (the strings themselves remain distinct objects, only the backing array is merged). This can significantly cut heap usage for apps with many duplicate strings (e.g., XML/JSON parsers). It adds minor overhead during GC and only applies to strings that have survived a young GC (to avoid deduplicating short-lived strings).\n\n\n```java\n// java -XX:+UseG1GC -XX:+UseStringDeduplication -XX:+PrintStringDeduplicationStatistics MyApp\n//\n// Trade-off: small GC CPU overhead for potentially large memory savings.\n// Only with G1 (and Shenandoah has a similar feature).\n// Not a substitute for interning when you need identity equality.\n```\n```\n",tech:"java",displayNumber:329,sortKey:329},{id:"java-330",number:330,title:"What are the trade-offs between throughput-oriented and latency-oriented collectors?",question:"What are the trade-offs between throughput-oriented and latency-oriented collectors?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Throughput-oriented** (Parallel): maximize application work per second; accept longer STW pauses. Best for batch jobs, offline processing, where total runtime matters more than individual pause.
- **Latency-oriented** (G1, ZGC, Shenandoah): minimize individual pause times to keep response times predictable; accept some throughput overhead (concurrent work, barriers). Best for interactive services, trading systems, user-facing apps.
Choose based on SLAs: if p99 latency matters → ZGC/G1; if batch throughput matters → Parallel.


\`\`\`java
Throughput vs latency decision:
  Batch / ETL / CI        -> Parallel GC (max work/sec)
  Web/API with SLA p99    -> G1 (default, ~100-200ms) or ZGC (<2ms)
  HFT / real-time service -> ZGC or Shenandoah

Tune intent, not just flags:
  Parallel: -XX:GCTimeRatio=99
  G1/ZGC:   -XX:MaxGCPauseMillis=50
\`\`\`
\`\`\`
`,tech:"java",displayNumber:330,sortKey:330},{id:"java-331",number:331,title:"How do you size the heap with -Xms and -Xmx?",question:"How do you size the heap with -Xms and -Xmx?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"- `-Xms` sets the **initial** heap size; `-Xmx` the **maximum**. The JVM starts at `-Xms` and grows up to `-Xmx` as needed.\n- For server apps, set **`-Xms` = `-Xmx`** to avoid runtime resizing pauses and to commit all memory up front (more predictable). For containers, don't exceed the container memory limit or the OOM killer will terminate the process.\n- Also consider `-XX:MaxRAMPercentage` (Java 10+) to size the heap relative to the container's RAM automatically.\n\n\n```java\n// Fixed heap (avoids resize pauses):\n// java -Xms4g -Xmx4g MyApp\n//\n// Container-aware (Java 10+): use a % of container RAM\n// java -XX:MaxRAMPercentage=75.0 -XX:InitialRAMPercentage=50.0 MyApp\n//\n// NEVER: -Xmx larger than the container limit -> OOM kill by the OS.\n```\n```\n",tech:"java",displayNumber:331,sortKey:331},{id:"java-332",number:332,title:"How do you set a GC pause-time goal?",question:"How do you set a GC pause-time goal?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Pause-time goals tell the collector to prefer shorter pauses, often by doing less work per cycle:
- **G1**: \`-XX:MaxGCPauseMillis=200\` (default 200 ms) — a *soft* goal; G1 shrinks young gen to meet it.
- **ZGC**: \`-XX:MaxGCPauseMillis=10\` (default ~none hard, inherently low).
- **Parallel**: also accepts the flag but it's a hint; throughput collector prioritizes overall time.
The goal is best-effort: the JVM will trade throughput/heap headroom to approach it but won't violate correctness. Setting it unrealistically low forces very frequent, tiny collections.


\`\`\`java
// G1 targeting 100 ms pauses:
// java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -Xmx8g MyApp
//
// Watch realized pauses in logs:
// java -Xlog:gc*:MyApp  -> "Pause Young ... 45ms" vs goal 100ms
// If real pauses >> goal, either lower allocation rate or accept the goal.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:332,sortKey:332},{id:"java-333",number:333,title:"What is the NewRatio and how does it affect GC?",question:"What is the NewRatio and how does it affect GC?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"`-XX:NewRatio=N` sets the ratio of **old generation to young generation** (old : young = N : 1). Default is 2 (old is 2× young, so young ≈ 1/3 of heap). A **larger NewRatio** shrinks the young gen → fewer but longer minor GCs, more promotion. A **smaller NewRatio** enlarges young gen → more frequent but cheaper minor GCs, with more room for objects to die young. Tuned for allocation-heavy vs long-lived workloads. (G1 also accepts `-XX:NewSize`/`-XX:MaxNewSize` but manages regions adaptively.)\n\n\n```java\n// -XX:NewRatio=2  -> old:young = 2:1  (young = 1/3 heap, default)\n// -XX:NewRatio=1  -> old:young = 1:1  (young = 1/2 heap, more room to die young)\n// -XX:NewRatio=3  -> young smaller, more promotion to old\n//\n// java -Xmx6g -XX:NewRatio=2 -XX:+UseParallelGC MyApp\n```\n```\n",tech:"java",displayNumber:333,sortKey:333},{id:"java-334",number:334,title:"What GC logging options should you enable in production?",question:"What GC logging options should you enable in production?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"Use **unified JVM logging** (`-Xlog`) introduced in Java 9 (retire the old `-XX:+PrintGCDetails`, removed in Java 9+):\n- `-Xlog:gc*:file=gc.log:time,uptime,level:filecount=5,filesize=10M` — full GC detail, rotated logs.\n- For overhead-sensitive prod, log at `info` (not `debug`/`trace`).\n- Add `-Xlog:safepoint*` to see STW pause sources beyond GC.\nAlways capture GC logs in production — they are the primary forensic tool for OOMs and latency spikes, with negligible overhead at `info`.\n\n\n```java\n// Recommended production GC logging:\n// java -Xlog:gc*,gc+heap=info,safepoint:file=/var/log/myapp/gc-%t.log:time,tid,level:filecount=10,filesize=20M -jar MyApp.jar\n//\n// Parse with: GCViewer, GCEasy (gceasy.io), or Prometheus + jmx_exporter.\n// Key metrics: pause time, frequency, heap before/after, promotion rate.\n```\n```\n",tech:"java",displayNumber:334,sortKey:334},{id:"java-335",number:335,title:"How do you read a GC log line (G1 example)?",question:"How do you read a GC log line (G1 example)?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`A typical G1 \`info\` line:
\`[2026-08-20T10:00:01.234+0000][gc,start] GC(12) Pause Young (Normal) (G1 Evacuation Pause)\`
\`[gc ] GC(12) Eden: 200.0M(200.0M)->0.0B(220.0M) Survivors: 0.0B->20.0M Heap: 600.0M(1.0G)->420.0M(1.0G)\`
\`[gc ] GC(12) Pause Young (Normal) (G1 Evacuation Pause) 12.345ms\`
Reading it: a **young evacuation pause** collected Eden (200M→0) and reduced total heap (600M→420M) in **12.3 ms**. "Heap: before->after (capacity)" tells you live set and headroom. Mixed collections show Old reclaimed too.


\`\`\`java
Fields to watch:
  Pause Young / Mixed / Full  -> which collection
  Eden: A->B (C)              -> used before->after (capacity)
  Heap: A->B (C)              -> total used before->after (max)
  final "Xms" number          -> STW pause duration (tune against goal)
//
// High 'after' Heap with frequent Full GCs => memory leak / heap too small.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:335,sortKey:335},{id:"java-336",number:336,title:"What is survivor space sizing and the TargetSurvivorRatio?",question:"What is survivor space sizing and the TargetSurvivorRatio?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"The **survivor spaces** hold age-1+ objects between young GCs. `-XX:TargetSurvivorRatio` (default 50) is the desired occupancy of a survivor space; if live objects exceed it after a young GC, the **tenuring threshold is lowered** so more objects promote to old gen early, preventing survivor overflow. This is part of G1/Parallel adaptive sizing. If survivors overflow, objects get promoted prematurely even if young — a promotion-rate problem. Tuning is usually left to the adaptive policy unless you see unwanted early promotions.\n\n\n```java\n// java -XX:TargetSurvivorRatio=50 -XX:MaxTenuringThreshold=15 MyApp\n//\n// If logs show tenuring threshold dropping to 1 quickly:\n//   -> survivor spaces too small OR objects live too long\n//   -> raise young gen (lower -XX:NewRatio) to give them room to die young.\n```\n```\n",tech:"java",displayNumber:336,sortKey:336},{id:"java-337",number:337,title:"How does -XX:+UseAdaptiveSizePolicy work?",question:"How does -XX:+UseAdaptiveSizePolicy work?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"Enabled by default with Parallel and G1, **UseAdaptiveSizePolicy** lets the JVM continuously retune generation sizes, tenuring threshold, and survivor ratios at runtime to meet the pause-time (`MaxGCPauseMillis`) and throughput (`GCTimeRatio`) goals. It reacts to live-set and allocation-rate measurements after each GC. Practical implication: manual `-Xmn`/survivor tuning is often counterproductive because the policy will override it; set high-level goals and let it adapt, only pinning sizes when you have strong evidence.\n\n\n```java\n// Let the JVM adapt (default):\n// java -XX:+UseParallelGC -XX:+UseAdaptiveSizePolicy -XX:GCTimeRatio=99 MyApp\n//\n// Disable to pin your own sizes (experts only):\n// java -XX:-UseAdaptiveSizePolicy -Xmn2g -XX:SurvivorRatio=8 MyApp\n//   (-Xmn sets fixed young gen = Eden+Survivors)\n```\n```\n",tech:"java",displayNumber:337,sortKey:337},{id:"java-338",number:338,title:"What is GC throughput vs latency and how do you measure them?",question:"What is GC throughput vs latency and how do you measure them?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Throughput** = (time doing app work) / (total time). High throughput = GC overhead low (e.g., 99% app time). Measured via \`-XX:GCTimeRatio\` target or from logs (total GC time / uptime).
- **Latency** = length of individual STW pauses (and their frequency). Measured as p50/p95/p99 pause times from GC logs.
A collector optimizing throughput (Parallel) may have long pauses; one optimizing latency (ZGC) sacrifices some throughput to keep pauses tiny. You cannot maximize both — pick per SLA. Measure with GC logs + Prometheus/Grafana or GCEasy.


\`\`\`java
// Throughput target:
//   -XX:GCTimeRatio=99  => GC <= ~1% of time
// Latency target:
//   -XX:MaxGCPauseMillis=50
//
// From logs compute:
//   throughput = 100 * (1 - totalGCTime/uptime)%
//   p99 latency = 99th percentile of individual pause durations
\`\`\`
\`\`\`
`,tech:"java",displayNumber:338,sortKey:338},{id:"java-339",number:339,title:"What is the class loading process (loading, linking, initialization)?",question:"What is the class loading process (loading, linking, initialization)?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`Class loading has three phases:
1. **Loading**: find the \`.class\` bytes (via classloader) and create a \`Class\` object in metaspace.
2. **Linking**: (a) **verification** — bytecode is valid/safe; (b) **preparation** — static fields get default zero values and memory; (c) **resolution** — symbolic references resolved to direct (can be lazy).
3. **Initialization**: run static initializers and static field assignments (\`<clinit>\`), under a lock so only one thread initializes a class.
A class is usable only after initialization completes.


\`\`\`java
public class LoadingPhases {
    static int x = compute();          // runs during INITIALIZATION
    static int compute() { return 42; }
    static { System.out.println("initialized"); } // <clinit>

    public static void main(String[] args) throws Exception {
        // Loading + linking happen lazily on first active use:
        Class<?> c = Class.forName("LoadingPhases"); // triggers init
        System.out.println(c.getSimpleName());
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:339,sortKey:339},{id:"java-340",number:340,title:"What are the built-in class loaders and their hierarchy?",question:"What are the built-in class loaders and their hierarchy?",difficulty:"Basic",category:"JVM Architecture & GC",answer:'Three built-in loaders, parent-child by delegation:\n- **Bootstrap (Primordial)**: loads core JDK classes from `jmods`/`rt.jar` (C++ implemented, `null` from Java).\n- **Platform/Extension** (`PlatformClassLoader` in Java 9+, formerly `ExtClassLoader`): loads JDK modules/platform classes.\n- **Application/System** (`AppClassLoader`): loads your app classes and classpath entries.\nCustom loaders extend `ClassLoader` and delegate to a parent. The hierarchy is **not** inheritance but a `parent` reference chain.\n\n\n```java\npublic class Loaders {\n    public static void main(String[] args) {\n        ClassLoader app = Loaders.class.getClassLoader();\n        System.out.println("App:      " + app);                     // AppClassLoader\n        System.out.println("Platform: " + app.getParent());         // PlatformClassLoader\n        System.out.println("Bootstrap:" + app.getParent().getParent()); // null\n    }\n}\n```\n```\n',tech:"java",displayNumber:340,sortKey:340},{id:"java-341",number:341,title:"Explain the delegation model (parent-first) of class loading.",question:"Explain the delegation model (parent-first) of class loading.",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'The **parent-delegation model**: when asked to load class `C`, a loader first delegates to its **parent**; only if the parent cannot find `C` does the loader attempt to load it itself. This prevents duplication and protects core classes (you cannot replace `java.lang.String` because Bootstrap loads it first). It enforces namespace isolation and security. Violating it (e.g., loading a core class in a child) throws `SecurityException` / `LinkageError` ("attempted duplicate class definition").\n\n\n```java\n// Pseudocode of loadClass:\nprotected Class<?> loadClass(String name, boolean resolve) {\n    Class<?> c = findLoadedClass(name);          // 1. already loaded?\n    if (c == null) {\n        try { c = parent.loadClass(name); }      // 2. delegate UP first\n        catch (ClassNotFoundException e) {\n            c = findClass(name);                 // 3. only now, load self\n        }\n    }\n    if (resolve) resolveClass(c);\n    return c;\n}\n```\n```\n',tech:"java",displayNumber:341,sortKey:341},{id:"java-342",number:342,title:"How do you write a custom ClassLoader?",question:"How do you write a custom ClassLoader?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Extend \`ClassLoader\` and override \`findClass(String name)\` to locate and define bytes (never break delegation by overriding \`loadClass\` unless you intend to). Read the \`.class\` bytes, call \`defineClass(...)\`, and the JVM handles linking/initialization. Common uses: hot-reload, plugin isolation, loading from non-file sources (network, encrypted jars), and per-tenant isolation. Each classloader defines its own namespace, so the same class loaded by two loaders are distinct types.


\`\`\`java
import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Path;

public class CustomLoader extends ClassLoader {
    private final Path dir;
    CustomLoader(ClassLoader parent, Path dir) { super(parent); this.dir = dir; }

    @Override
    protected Class<?> findClass(String name) throws ClassNotFoundException {
        try {
            byte[] bytes = Files.readAllBytes(dir.resolve(name.replace('.', '/') + ".class"));
            return defineClass(name, bytes, 0, bytes.length); // links + prepares
        } catch (IOException e) { throw new ClassNotFoundException(name, e); }
    }
}
// Usage: new CustomLoader(getClass().getClassLoader(), Path.of("/plugins")).loadClass("com.x.Plugin");
\`\`\`
\`\`\`
`,tech:"java",displayNumber:342,sortKey:342},{id:"java-343",number:343,title:"What is a ClassNotFoundException vs NoClassDefFoundError?",question:"What is a ClassNotFoundException vs NoClassDefFoundError?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **ClassNotFoundException** is a **checked exception** thrown when an *explicit* load is attempted at runtime (e.g., \`Class.forName\`, \`loadClass\`, reflection, deserialization) and the class is not found.
- **NoClassDefFoundError** is an **Error** (unchecked) thrown when the JVM *expected* a class to already be linked/initialized (e.g., it was present at compile time) but cannot find its definition at runtime — often because a static initializer threw, or the class vanished from the classpath after compile.
Roughly: explicit dynamic load fails → exception; implicit runtime reference fails → error.


\`\`\`java
public class CNFvsNCD {
    public static void main(String[] args) throws Exception {
        try {
            Class.forName("DoesNotExist"); // explicit -> ClassNotFoundException
        } catch (ClassNotFoundException e) { System.out.println("checked: " + e); }

        Missing m = new Missing(); // if Missing failed to link/init -> NoClassDefFoundError
    }
}
// NoClassDefFoundError commonly caused by: missing JAR at runtime,
// a thrown exception in a static initializer of the class.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:343,sortKey:343},{id:"java-344",number:344,title:"What is the danger of classloader leaks in long-running apps?",question:"What is the danger of classloader leaks in long-running apps?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`A **classloader leak** happens when a long-lived object (e.g., a static field in a core classloader, a thread, or a JNDI/registry entry) holds a reference to a class or instance loaded by a child (e.g., webapp) classloader. That keeps the entire child classloader — and all its loaded classes and their static state — reachable, so metaspace never gets collected even after the webapp is "undeployed." Repeated redeploys then exhaust Metaspace (\`OutOfMemoryError: Metaspace\`). Common culprits: unclosed threads, \`ThreadLocal\` not removed, JDBC drivers registered in a parent, static caches. Fix: remove refs on shutdown, use \`ThreadLocal.remove()\`, clean up registries.


\`\`\`java
public class Leak {
    // Anti-pattern: static ThreadLocal in a shared (parent) classloader
    // holding a value from a child (webapp) classloader -> leak on redeploy.
    private static final ThreadLocal<byte[]> CACHE = ThreadLocal.withInitial(() -> new byte[1024*1024]);

    void use() {
        CACHE.get();
        // MUST: CACHE.remove() when done, or the webapp classloader is pinned.
    }
}
// Detect via: jmap -clstats <pid>  (classes per loader) or Eclipse MAT on a heap dump.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:344,sortKey:344},{id:"java-345",number:345,title:"What are the C1 and C2 JIT compilers?",question:"What are the C1 and C2 JIT compilers?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`The HotSpot JVM uses two JIT compilers:
- **C1 (Client)**: fast to compile, performs light optimizations. Used for quick startup and less-hot methods.
- **C2 (Server)**: slower to compile but applies aggressive, heavy optimizations (inlining, loop unrolling, escape analysis, vectorization). Used for hot methods where the compile cost pays off.
The JVM starts in **interpreted** mode and compiles methods to native code once they're "hot" (called enough times, tracked by invocation/branch counters). C2 produces much faster code than C1.


\`\`\`java
// Force a single compiler if needed:
// java -Xint            MyApp   // interpreter only (slow, for debugging)
// java -Xcomp           MyApp   // compile everything eagerly (slow startup)
// java -client / -server (legacy selects C1/C2 bias;modern JDKs ignore)
//
// Normal: tiered (C1 then C2) is the default and best for most apps.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:345,sortKey:345},{id:"java-346",number:346,title:"What is tiered compilation?",question:"What is tiered compilation?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`**Tiered compilation** (\`-XX:+TieredCompilation\`, default on since Java 8) combines C1 and C2: a method is first compiled quickly by **C1** at low optimization levels (so it runs fast without long warmup), then, if it stays hot, recompiled by **C2** at high optimization. This gives fast startup (C1) plus peak performance (C2) without waiting for full C2 compilation. Levels: 0 = interpreter, 1-3 = C1 (increasing opts), 4 = C2. Watch with \`-XX:+PrintCompilation\`.


\`\`\`java
public class Tiered {
    public static void main(String[] args) {
        long sum = 0;
        for (int i = 0; i < 1_000_000; i++) {       // loop gets C1 then C2
            sum += compute(i);
        }
        System.out.println(sum);
    }
    static long compute(long x) { return x * x + 1; } // inlined by C2 when hot
}
// java -XX:+TieredCompilation -XX:+PrintCompilation Tiered
\`\`\`
\`\`\`
`,tech:"java",displayNumber:346,sortKey:346},{id:"java-347",number:347,title:"How does the JVM decide what to compile (hot methods, counters)?",question:"How does the JVM decide what to compile (hot methods, counters)?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Each method/branch has **invocation counters** and **back-edge (loop) counters**. When a counter crosses a threshold (\`-XX:CompileThreshold\`, default 10,000 for C2 under non-tiered; tiered uses different level-trigger points), the method is queued for JIT compilation. Because counters are **decayed** (halved periodically), bursty methods don't stay permanently "hot." Once compiled, future calls use the native code; the interpreter still runs until the on-stack replacement (OSR) or next invocation. This is why there's a **warmup period** before peak performance in benchmarks.


\`\`\`java
// Tune how "hot" a method must be (non-tiered baseline):
// java -XX:CompileThreshold=5000 MyApp   // compile sooner
//
// Watch compilation decisions:
// java -XX:+PrintCompilation -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining MyApp
//   Output: "123  3  %      Tiered.run @ 2 (MyApp)"  (level 3 C1, OSR %)
//
// Key lesson: measure AFTER warmup, or you benchmark the interpreter.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:347,sortKey:347},{id:"java-348",number:348,title:"What is On-Stack Replacement (OSR)?",question:"What is On-Stack Replacement (OSR)?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**On-Stack Replacement** lets the JVM switch a *long-running method already on the stack* from interpreted to compiled code **in the middle of its execution** — most importantly for hot loops. Without OSR, a method already running a tight loop would keep interpreting until it returned and was called again. OSR compiles the loop body (with a special entry point) and transfers control mid-execution. This is why a single long-running loop still gets optimized without needing to restart.


\`\`\`java
public class OSR {
    public static void main(String[] args) {
        long sum = 0;
        // This loop may be interpreted first, then OSR-swapped to compiled
        // mid-loop once the back-edge counter trips:
        for (long i = 0; i < 2_000_000_000L; i++) {
            sum += i;
        }
        System.out.println(sum);
    }
}
// java -XX:+PrintCompilation OSR   -> look for '%' (OSR) compile entries.
\`\`\`
\`\`\`
`,tech:"java",displayNumber:348,sortKey:348},{id:"java-349",number:349,title:"What are the common types of OutOfMemoryError and their causes?",question:"What are the common types of OutOfMemoryError and their causes?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"- `java.lang.OutOfMemoryError: Java heap space` — live objects exceed `-Xmx`; true leak or heap too small.\n- `: GC overhead limit exceeded` — >98% of time in GC but <2% heap reclaimed; thrashing.\n- `: Metaspace` — too many classes / classloader leak; raise `-XX:MaxMetaspaceSize`.\n- `: Unable to create new native thread` — too many threads or `-Xss` too big vs OS limits.\n- `: Direct buffer memory` — off-heap `ByteBuffer.allocateDirect` exceeds `-XX:MaxDirectMemorySize`.\n- `: Requested array size exceeds VM limit` — attempted huge array (`> Integer.MAX_VALUE-headers`).\n- `: Out of swap space` / `: heap dump` — native/OS memory exhaustion.\n\n\n```java\n// Reproduce a few:\n// java -Xmx32m OOMJavaHeap      -> Java heap space\n// java -XX:MaxMetaspaceSize=8m ManyClasses -> Metaspace\n// java -XX:MaxDirectMemorySize=10m BigDirect -> Direct buffer memory\n//\n// Diagnose: capture heap dump on OOM and analyze:\n// java -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/dump.hprof MyApp\n```\n```\n",tech:"java",displayNumber:349,sortKey:349},{id:"java-350",number:350,title:"What profiling and diagnostic tools are available for JVM memory/GC?",question:"What profiling and diagnostic tools are available for JVM memory/GC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Built-in (JDK/bin):
- **jstat** — GC/heap/compilation stats live (\`jstat -gcutil <pid> 1s\`).
- **jmap** — heap histogram, \`jmap -dump:live,format=b,file=d.hprof <pid>\`, \`-clstats\`.
- **jcmd** — Swiss army knife: \`VM.native_memory\`, \`GC.heap_dump\`, \`Thread.print\`, \`VM.flags\`.
- **jstack** — thread dumps / deadlock detection.
- **jconsole / jvisualvm / Java Mission Control (JMC)** — GUI monitoring, flight recordings (JFR).
- **jfr** — low-overhead Java Flight Recorder for production profiling.
Third-party: **Eclipse MAT** (heap dump analysis, leak suspects), **async-profiler** (CPU/alloc/lock), **GCViewer/GCEasy** (GC logs), **Prometheus + jmx_exporter** (metrics).


\`\`\`java
// Live GC view:
// jstat -gcutil <pid> 1000
//   S0  S1  E   O   M    YGC YGCT FGC FGCT GCT
//   0  12  45  60  92    120 3.4  5  2.1 5.5
//
// Heap dump + analyze:
// jmap -dump:live,format=b,file=/tmp/d.hprof <pid>
//   -> open in Eclipse MAT, run "Leak Suspects Report"
//
// Continuous low-overhead profiling:
// java -XX:StartFlightRecording=duration=60s,filename=rec.jfr MyApp
//   -> open in JMC
//
// Allocation profiling (no GC needed):
// asprof -e alloc -d 30 -f alloc.html <pid>
\`\`\`
\`\`\`
`,tech:"java",displayNumber:350,sortKey:350},{id:"java-351",number:351,title:"What is Inversion of Control (IoC) in the Spring Framework?",question:"What is Inversion of Control (IoC) in the Spring Framework?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Inversion of Control (IoC) is a design principle where the responsibility of creating and managing objects is handed over from the application code to the Spring container. Instead of classes instantiating their own dependencies with \`new\`, the container creates them and "wires" them together. This reduces coupling, improves testability, and centralizes object lifecycle management. The Spring IoC container reads configuration metadata (annotations, Java config, or XML) to know which beans to create and how to inject dependencies.


\`\`\`java
// Tight coupling (no IoC)
class OrderService {
    private PaymentService payment = new PaymentService(); // manual creation
}

// Loose coupling with IoC
@Component
class OrderService {
    private final PaymentService payment;
    public OrderService(PaymentService payment) { this.payment = payment; }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:351,sortKey:351},{id:"java-352",number:352,title:"What is Dependency Injection (DI) and what are its types?",question:"What is Dependency Injection (DI) and what are its types?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Dependency Injection is the implementation mechanism of IoC: the container supplies an object's dependencies rather than the object constructing them. Spring supports three injection styles:

1. **Constructor Injection** – dependencies passed via the constructor. Best for required, immutable dependencies.
2. **Setter Injection** – dependencies set via setter methods. Best for optional or reconfigurable dependencies.
3. **Field Injection** – dependencies injected directly into fields via \`@Autowired\`. Convenient but discouraged (harder to test, hides dependencies).

Spring recommends constructor injection for mandatory dependencies.


\`\`\`java
@Component
class UserService {
    private final UserRepository repo;          // constructor
    private EmailService email;                 // setter

    public UserService(UserRepository repo) { this.repo = repo; }

    @Autowired
    public void setEmailService(EmailService email) { this.email = email; }

    @Autowired private AuditService audit;      // field (discouraged)
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:352,sortKey:352},{id:"java-353",number:353,title:"What is the difference between Constructor, Setter and Field injection?",question:"What is the difference between Constructor, Setter and Field injection?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **Constructor**: Dependencies are final/immutable, injected at creation, ideal for required beans, enables easy unit testing without a Spring context, and prevents partially-initialized objects. This is the recommended approach.
- **Setter**: Allows optional dependencies and post-construction reconfiguration; useful when a dependency is optional or circular references exist.
- **Field**: Shortest syntax but cannot be used for \`final\` fields, makes unit testing without reflection difficult, and hides the class's dependencies. Spring itself discourages it.


\`\`\`java
// Preferred: constructor injection
@Service
public class AccountService {
    private final AccountRepository repo;
    public AccountService(AccountRepository repo) { this.repo = repo; }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:353,sortKey:353},{id:"java-354",number:354,title:"What is the Spring IoC container and the difference between BeanFactory and ApplicationContext?",question:"What is the Spring IoC container and the difference between BeanFactory and ApplicationContext?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"The **IoC container** is the core of Spring and is responsible for instantiating, configuring, and assembling beans. There are two main interfaces:\n\n- **BeanFactory**: The basic container; lazy-instantiates beans on first request. Lightweight, suitable for resource-constrained environments.\n- **ApplicationContext**: A superset of BeanFactory that adds enterprise features: event propagation, internationalization (MessageSource), AOP integration, and eager singleton instantiation by default. This is what most applications use (e.g., `AnnotationConfigApplicationContext`, `ClassPathXmlApplicationContext`).\n\n\n```java\nApplicationContext ctx =\n    new AnnotationConfigApplicationContext(AppConfig.class);\nUserService svc = ctx.getBean(UserService.class);\n```\n```\n",tech:"java",displayNumber:354,sortKey:354},{id:"java-355",number:355,title:"How does Spring resolve which bean to inject when multiple candidates exist?",question:"How does Spring resolve which bean to inject when multiple candidates exist?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`When multiple beans of the same type exist, Spring throws a \`NoUniqueBeanDefinitionException\`. You resolve ambiguity with:
- \`@Primary\` – marks one bean as the default choice.
- \`@Qualifier("name")\` – explicitly names the bean to inject.
- Custom qualifier annotations for type-safe selection.


\`\`\`java
@Primary @Component
class MySQLRepo implements UserRepository {}

@Component @Qualifier("mongo")
class MongoRepo implements UserRepository {}

@Autowired
public UserService(@Qualifier("mongo") UserRepository repo) { ... }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:355,sortKey:355},{id:"java-356",number:356,title:"What is `@Component` and how does component scanning work?",question:"What is `@Component` and how does component scanning work?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Component` is a generic stereotype annotation that marks a class as a Spring-managed bean. Component scanning (enabled by `@ComponentScan` or `@SpringBootApplication`) automatically detects classes annotated with `@Component` (and its specializations) on the classpath and registers them as beans. The default scan starts from the package of the configuration class and recurses downward.\n\n\n```java\n@Configuration\n@ComponentScan("com.example.service")\npublic class AppConfig {}\n\n@Component\npublic class NotificationService {}\n```\n```\n',tech:"java",displayNumber:356,sortKey:356},{id:"java-357",number:357,title:"What is the difference between `@Component`, `@Service`, `@Repository` and `@Controller`?",question:"What is the difference between `@Component`, `@Service`, `@Repository` and `@Controller`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"All four are stereotypes that register a class as a bean. The difference is semantic and behavioral:\n- `@Component`: generic stereotype.\n- `@Service`: marks a service-layer/business-logic class.\n- `@Repository`: marks a persistence-layer class; additionally enables automatic translation of persistence exceptions into Spring's `DataAccessException` hierarchy.\n- `@Controller`: marks a web MVC controller that returns views; `@RestController` is its REST variant returning data directly.\n\nUsing the specific annotations conveys intent and unlocks targeted features (e.g., AOP pointcuts by stereotype, exception translation for repositories).\n\n\n```java\n@Repository public class UserRepository { /* DAO */ }\n@Service public class UserService { /* business logic */ }\n@RestController public class UserController { /* REST endpoints */ }\n```\n```\n",tech:"java",displayNumber:357,sortKey:357},{id:"java-358",number:358,title:"What is `@Configuration` and `@Bean`?",question:"What is `@Configuration` and `@Bean`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@Configuration` marks a class as a source of bean definitions. Methods annotated with `@Bean` inside it return objects that Spring registers as beans in the container. This is Java-based configuration (an alternative to XML). It is used for third-party classes you cannot annotate, or when you need explicit control over bean construction. `@Configuration` classes use CGLIB proxying so that calling `@Bean` methods returns the same singleton instance.\n\n\n```java\n@Configuration\npublic class AppConfig {\n    @Bean\n    public DataSource dataSource() {\n        return new HikariDataSource();\n    }\n}\n```\n```\n",tech:"java",displayNumber:358,sortKey:358},{id:"java-359",number:359,title:"Explain the Spring Bean Lifecycle.",question:"Explain the Spring Bean Lifecycle.",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"The bean lifecycle is:\n1. Container reads configuration and instantiates the bean.\n2. Dependencies are injected (populate properties).\n3. If `BeanNameAware`, `BeanFactoryAware`, etc., callbacks fire.\n4. `BeanPostProcessor.postProcessBeforeInitialization` runs.\n5. `@PostConstruct` / `InitializingBean.afterPropertiesSet` runs.\n6. `BeanPostProcessor.postProcessAfterInitialization` runs (AOP proxies are applied here).\n7. Bean is ready for use.\n8. On container shutdown: `@PreDestroy` / `DisposableBean.destroy` runs.\n\n\n```java\n@Component\npublic class CacheManager {\n    @PostConstruct public void init() { loadCache(); }\n    @PreDestroy public void cleanup() { flushCache(); }\n}\n```\n```\n",tech:"java",displayNumber:359,sortKey:359},{id:"java-360",number:360,title:"How do you customize bean initialization and destruction?",question:"How do you customize bean initialization and destruction?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Three ways:\n1. JSR-250 annotations `@PostConstruct` and `@PreDestroy` (recommended, least coupled).\n2. `InitializingBean` / `DisposableBean` interfaces (couples to Spring).\n3. `initMethod` / `destroyMethod` attributes on `@Bean` (useful for third-party classes).\n\nOrder: interface methods run before annotation methods, which run before `initMethod`.\n\n\n```java\n@Bean(initMethod = "start", destroyMethod = "stop")\npublic Server server() { return new Server(); }\n```\n```\n',tech:"java",displayNumber:360,sortKey:360},{id:"java-361",number:361,title:"What are the different Spring Bean scopes?",question:"What are the different Spring Bean scopes?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **singleton** (default): one shared instance per Spring container.
- **prototype**: a new instance is created every time it is requested.
- **request**: one instance per HTTP request (web only).
- **session**: one instance per HTTP session (web only).
- **application**: one instance per \`ServletContext\` (web only).
- **websocket**: one instance per WebSocket session.

Use prototype for stateful beans; singleton for stateless services.


\`\`\`java
@Component @Scope("prototype")
public class Command { /* stateful per use */ }

@Component @Scope("singleton") // default
public class ConfigService {}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:361,sortKey:361},{id:"java-362",number:362,title:"What is the difference between singleton and prototype scope?",question:"What is the difference between singleton and prototype scope?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"A **singleton** bean is instantiated once and the same instance is injected everywhere — ideal for stateless services. A **prototype** bean is created fresh on every lookup/injection — ideal for stateful objects. Caveat: when a singleton depends on a prototype, the prototype is injected only once (at the singleton's creation). To get a new prototype per call, use `ObjectFactory`/`Provider` or lookup method injection.\n\n\n```java\n@Autowired private ObjectFactory<Command> commandFactory;\npublic void run() { Command c = commandFactory.getObject(); } // new each time\n```\n```\n",tech:"java",displayNumber:362,sortKey:362},{id:"java-363",number:363,title:"What are request and session scopes in Spring?",question:"What are request and session scopes in Spring?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"**request** scope creates a new bean instance for each HTTP request; the bean lives for the duration of that request. **session** scope creates one instance per user HTTP session, shared across requests from that user. Both require a web-aware `ApplicationContext` and are commonly used for storing per-user or per-request state (e.g., a shopping cart as a session bean). You often must use `ObjectFactory`/`Provider` when injecting these into singletons.\n\n\n```java\n@Component @Scope(value = WebApplicationContext.SCOPE_SESSION, proxyMode = ScopedProxyMode.TARGET_CLASS)\npublic class ShoppingCart {}\n```\n```\n",tech:"java",displayNumber:363,sortKey:363},{id:"java-364",number:364,title:"What is `@Lazy` and when would you use it?",question:"What is `@Lazy` and when would you use it?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@Lazy` defers bean instantiation until it is first requested rather than at container startup. It is used to:\n- Reduce startup time for large applications.\n- Resolve circular dependency issues in some cases.\n- Avoid initializing expensive resources until needed.\n\nYou can apply it on `@Bean` definitions, `@Component`, or on injection points.\n\n\n```java\n@Bean @Lazy\npublic HeavyReportService heavyReportService() { return new HeavyReportService(); }\n```\n```\n",tech:"java",displayNumber:364,sortKey:364},{id:"java-365",number:365,title:"What is `@Primary` and `@Qualifier`?",question:"What is `@Primary` and `@Qualifier`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'Both resolve ambiguity when multiple beans of the same type exist. `@Primary` designates a default bean to use when no specific qualifier is given. `@Qualifier` names the exact bean to inject. `@Qualifier` takes precedence over `@Primary` and is more explicit. You can also create custom qualifier annotations for type-safe selection.\n\n\n```java\n@Primary @Bean public PaymentGateway paypal() { return new Paypal(); }\n@Bean @Qualifier("stripe") public PaymentGateway stripe() { return new Stripe(); }\n\n@Autowired @Qualifier("stripe") PaymentGateway gateway;\n```\n```\n',tech:"java",displayNumber:365,sortKey:365},{id:"java-366",number:366,title:"Explain `@Autowired`, `@Resource` and `@Inject`.",question:"Explain `@Autowired`, `@Resource` and `@Inject`.",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'- `@Autowired` (Spring-specific): default by type; falls back to name; supports `required=false`.\n- `@Resource` (JSR-250): default by name, then by type; comes from Java EE.\n- `@Inject` (JSR-330): like `@Autowired` but no `required` attribute; portable across DI frameworks.\n\nFor portability, prefer `@Inject`; for Spring-only apps, `@Autowired` is fine.\n\n\n```java\n@Autowired private OrderRepository repo;   // by type\n@Resource(name="userRepo") private UserRepository ur; // by name\n@Inject private AuditService audit;        // JSR-330\n```\n```\n',tech:"java",displayNumber:366,sortKey:366},{id:"java-367",number:367,title:"What is `@Value` and how is it used?",question:"What is `@Value` and how is it used?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Value` injects values into fields, constructor params, or method params from properties files, environment variables, system properties, or SpEL expressions. It supports `${property:default}` placeholder syntax and `#{spEL}` expressions.\n\n\n```java\n@Value("${app.timeout:30}")\nprivate int timeout;\n\n@Value("#{systemProperties[\'user.home\']}")\nprivate String homeDir;\n\n@Value("${app.name}")\nprivate String appName;\n```\n```\n',tech:"java",displayNumber:367,sortKey:367},{id:"java-368",number:368,title:"What is Spring Boot and how does it differ from Spring?",question:"What is Spring Boot and how does it differ from Spring?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Spring is the core framework providing IoC, DI, AOP, etc. **Spring Boot** is a layer on top that simplifies setup: it offers opinionated defaults, embedded servers (Tomcat/Netty), starter dependencies, auto-configuration, and production-ready features (metrics, health checks). It reduces boilerplate and lets you create a runnable app with minimal configuration.


\`\`\`java
@SpringBootApplication
public class MyApp {
    public static void main(String[] args) {
        SpringApplication.run(MyApp.class, args);
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:368,sortKey:368},{id:"java-369",number:369,title:"Explain `@SpringBootApplication`.",question:"Explain `@SpringBootApplication`.",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@SpringBootApplication` is a convenience annotation that combines three annotations:\n- `@Configuration` – marks the class as a bean source.\n- `@EnableAutoConfiguration` – enables Spring Boot's auto-configuration.\n- `@ComponentScan` – scans the package and sub-packages for components.\n\nIt is typically placed on the main application class. Component scanning starts from its package, so the main class should live in a base package above all others.\n\n\n```java\n@SpringBootApplication // = @Configuration + @EnableAutoConfiguration + @ComponentScan\npublic class Application { public static void main(String[] a){ SpringApplication.run(Application.class,a);} }\n```\n```\n",tech:"java",displayNumber:369,sortKey:369},{id:"java-370",number:370,title:"How does Spring Boot Auto-Configuration work?",question:"How does Spring Boot Auto-Configuration work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"Auto-configuration uses `@EnableAutoConfiguration`, which imports `AutoConfigurationImportSelector`. This selector reads `META-INF/spring.factories` (or `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` in newer versions) to find candidate auto-configuration classes. Each class is conditionally applied using `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`, etc. So a bean is auto-configured only if its required classes are on the classpath and the user hasn't defined their own.\n\n\n```java\n@Configuration\n@ConditionalOnClass(DataSource.class)\n@ConditionalOnMissingBean(DataSource.class)\npublic class DataSourceAutoConfiguration { /* configures HikariCP if none provided */ }\n```\n```\n",tech:"java",displayNumber:370,sortKey:370},{id:"java-371",number:371,title:"What is the role of `spring.factories` / `AutoConfiguration.imports`?",question:"What is the role of `spring.factories` / `AutoConfiguration.imports`?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"These files register auto-configuration classes so Boot can discover them without component scanning. `META-INF/spring.factories` was the legacy mechanism (keyed by `org.springframework.boot.autoconfigure.EnableAutoConfiguration`). Boot 2.7+ deprecates it in favor of `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`, which simply lists fully-qualified class names, one per line. This improves startup performance and clarity.",tech:"java",displayNumber:371,sortKey:371},{id:"java-372",number:372,title:"What are Spring Boot Starters?",question:"What are Spring Boot Starters?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Starters are curated dependency descriptors that bundle a group of libraries needed for a particular functionality, with compatible versions managed by the parent `spring-boot-starter-parent`. Examples: `spring-boot-starter-web` (web + Tomcat + Jackson), `spring-boot-starter-data-jpa`, `spring-boot-starter-security`, `spring-boot-starter-test`. They eliminate version conflicts and reduce `pom.xml`/`build.gradle` complexity.\n\n\n```java\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n</dependency>\n```\n```\n",tech:"java",displayNumber:372,sortKey:372},{id:"java-373",number:373,title:"How do you externalize configuration in Spring Boot?",question:"How do you externalize configuration in Spring Boot?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Spring Boot externalizes config via properties/yaml files, environment variables, command-line arguments, and more. The precedence order (low to high) includes: `application.properties`, profile-specific files, environment variables, and command-line arguments. You can use `@Value` or inject an `Environment`/`@ConfigurationProperties` object. Externalized config lets the same artifact run in different environments.",tech:"java",displayNumber:373,sortKey:373},{id:"java-374",number:374,title:"What is `@ConfigurationProperties`?",question:"What is `@ConfigurationProperties`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@ConfigurationProperties` binds a hierarchical group of properties to a strongly-typed Java bean, supporting relaxed binding, validation (`@Validated`), and type conversion. It is superior to multiple `@Value` annotations for grouped, structured configuration. Enable it with `@EnableConfigurationProperties` or by annotating the class with `@Component`.\n\n\n```java\n@ConfigurationProperties(prefix = "app.mail")\n@Validated\npublic class MailProperties {\n    @NotBlank private String host;\n    private int port = 25;\n    // getters/setters\n}\n```\n```\n',tech:"java",displayNumber:374,sortKey:374},{id:"java-375",number:375,title:"What is the difference between `@Value` and `@ConfigurationProperties`?",question:"What is the difference between `@Value` and `@ConfigurationProperties`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@Value` injects a single scalar value with SpEL support but no validation or structured binding. `@ConfigurationProperties` binds a whole subtree of properties to a POJO with type-safe access, validation, and relaxed binding (`app.mail-host` vs `app.mail.host`). Use `@Value` for a quick single value; use `@ConfigurationProperties` for feature/domain configuration.\n\n\n```java\n@Value("${app.mail.host}") private String host;        // single value\n@ConfigurationProperties("app.mail") MailProps props;  // structured\n```\n```\n',tech:"java",displayNumber:375,sortKey:375},{id:"java-376",number:376,title:"What is the `application.properties` vs `application.yml` difference?",question:"What is the `application.properties` vs `application.yml` difference?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Both configure the application. `application.properties` uses `key=value` flat syntax; `application.yml` uses YAML hierarchical indentation, which is more readable for nested structures. YAML requires correct indentation (2 spaces) and does not allow tabs. YAML support is provided by the `snakeyaml` dependency bundled in Boot.",tech:"java",displayNumber:376,sortKey:376},{id:"java-377",number:377,title:"How does Spring Boot handle profiles?",question:"How does Spring Boot handle profiles?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Profiles isolate environment-specific configuration and beans. You activate a profile via `spring.profiles.active` property, `SPRING_PROFILES_ACTIVE` env var, or `--spring.profiles.active=dev` argument. Configuration can live in `application-{profile}.yml`, and beans can be annotated with `@Profile("dev")` to load conditionally.\n\n\n```java\n@Bean @Profile("dev") public DataSource devDs() { return new H2(); }\n@Bean @Profile("prod") public DataSource prodDs() { return new Mysql(); }\n```\n```yaml\n```\n',tech:"java",displayNumber:377,sortKey:377},{id:"java-378",number:378,title:"What is `@Profile` and how is it used?",question:"What is `@Profile` and how is it used?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Profile` designates that a component, `@Bean` method, or `@Configuration` class should only be registered when the specified profile(s) are active. It accepts a single profile or an array, and supports negation (`!prod`). It is the mechanism for environment-specific wiring.\n\n\n```java\n@Configuration\n@Profile("test")\npublic class TestConfig {\n    @Bean public EmailService email() { return new NoOpEmailService(); }\n}\n```\n```\n',tech:"java",displayNumber:378,sortKey:378},{id:"java-379",number:379,title:"What are the embedded servers in Spring Boot?",question:"What are the embedded servers in Spring Boot?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot embeds a servlet container so the app runs as a standalone JAR with no external server. For servlet stacks, **Tomcat** is the default (via `spring-boot-starter-web`); **Jetty** and **Undertow** can replace it. For reactive stacks (`spring-boot-starter-webflux`), **Netty** is the default. This enables fat-jar deployment and simpler operations.\n\n\n```java\n<!-- swap Tomcat for Jetty -->\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n  <exclusions><exclusion><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-tomcat</artifactId></exclusion></exclusions>\n</dependency>\n<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jetty</artifactId></dependency>\n```\n```\n",tech:"java",displayNumber:379,sortKey:379},{id:"java-380",number:380,title:"How do you change the embedded server port?",question:"How do you change the embedded server port?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Set `server.port` in `application.properties`/`yml`. Use `server.port=0` to pick a random available port (useful in integration tests). You can also configure it programmatically via `WebServerFactoryCustomizer` or by setting the `SERVER_PORT` environment variable.\n\n\n```java\nserver.port=9090\n```\n",tech:"java",displayNumber:380,sortKey:380},{id:"java-381",number:381,title:"What is `@RestController`?",question:"What is `@RestController`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@RestController` is a convenience stereotype combining `@Controller` and `@ResponseBody`. It indicates that every handler method returns the serialized response body (typically JSON via Jackson) directly, rather than a view name. It is the primary annotation for building REST APIs in Spring Boot.\n\n\n```java\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    @GetMapping("/{id}")\n    public User getUser(@PathVariable Long id) { return service.findById(id); }\n}\n```\n```\n',tech:"java",displayNumber:381,sortKey:381},{id:"java-382",number:382,title:"Explain `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`.",question:"Explain `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`.",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'These are composed mapping annotations (shortcuts over `@RequestMapping(method=...)`) that bind handler methods to specific HTTP methods:\n- `@GetMapping` → HTTP GET (read)\n- `@PostMapping` → HTTP POST (create)\n- `@PutMapping` / `@PatchMapping` → HTTP PUT/PATCH (update)\n- `@DeleteMapping` → HTTP DELETE (remove)\n\nThey support path patterns, consumes/produces media types, and headers.\n\n\n```java\n@PostMapping(consumes = "application/json", produces = "application/json")\npublic ResponseEntity<User> create(@RequestBody User user) { ... }\n```\n```\n',tech:"java",displayNumber:382,sortKey:382},{id:"java-383",number:383,title:"What is `@PathVariable` and `@RequestParam`?",question:"What is `@PathVariable` and `@RequestParam`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@PathVariable` extracts a value from the URI template (e.g., `/users/{id}`). `@RequestParam` extracts query parameters (e.g., `/users?page=2`). `@PathVariable` is for resource identification in the path; `@RequestParam` is for optional filters, pagination, or sorting. Both support `required`, `defaultValue`, and type conversion.\n\n\n```java\n@GetMapping("/{id}")\npublic User get(@PathVariable Long id,\n                @RequestParam(defaultValue = "0") int page) { ... }\n```\n```\n',tech:"java",displayNumber:383,sortKey:383},{id:"java-384",number:384,title:"What is `@RequestBody` and `@ResponseBody`?",question:"What is `@RequestBody` and `@ResponseBody`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@RequestBody` binds the HTTP request body (typically JSON) to a method argument using `HttpMessageConverter`s (Jackson by default). `@ResponseBody` writes the return value directly into the response body instead of a view. With `@RestController`, `@ResponseBody` is implicit on every method. These drive JSON serialization in REST APIs.\n\n\n```java\n@PostMapping\npublic User create(@RequestBody UserDto dto) {\n    return service.create(dto); // serialized to JSON automatically\n}\n```\n```\n",tech:"java",displayNumber:384,sortKey:384},{id:"java-385",number:385,title:"What is `ResponseEntity` and when should you use it?",question:"What is `ResponseEntity` and when should you use it?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`\`ResponseEntity<T>\` represents the full HTTP response: status code, headers, and body. Use it when you need fine-grained control over the response — setting custom headers, cookies, status codes (201 Created, 204 No Content, 409 Conflict), or conditional responses. For simple cases, returning the POJO directly suffices.


\`\`\`java
@PostMapping
public ResponseEntity<User> create(@RequestBody User u) {
    User saved = service.save(u);
    URI loc = URI.create("/users/" + saved.getId());
    return ResponseEntity.created(loc).body(saved);
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:385,sortKey:385},{id:"java-386",number:386,title:"How do you handle exceptions in Spring REST with `@ExceptionHandler`?",question:"How do you handle exceptions in Spring REST with `@ExceptionHandler`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@ExceptionHandler` annotated methods in a controller handle specific exceptions thrown by that controller's methods. For global handling, use `@ControllerAdvice`/`@RestControllerAdvice` with `@ExceptionHandler` to centralize error responses across all controllers. Return `ResponseEntity` with an appropriate status and a structured error body.\n\n\n```java\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(UserNotFoundException.class)\n    public ResponseEntity<ErrorDto> handle(UserNotFoundException ex) {\n        return ResponseEntity.status(404).body(new ErrorDto(ex.getMessage()));\n    }\n}\n```\n```\n",tech:"java",displayNumber:386,sortKey:386},{id:"java-387",number:387,title:"What is `@ControllerAdvice` / `@RestControllerAdvice`?",question:"What is `@ControllerAdvice` / `@RestControllerAdvice`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@ControllerAdvice` is a specialization of `@Component` that allows you to apply `@ExceptionHandler`, `@InitBinder`, and `@ModelAttribute` methods globally across many controllers. `@RestControllerAdvice` adds `@ResponseBody`, making it ideal for REST APIs (returns JSON error bodies). It centralizes cross-cutting controller concerns.\n\n\n```java\n@RestControllerAdvice\npublic class ApiAdvice {\n    @ExceptionHandler(IllegalArgumentException.class)\n    public ResponseEntity<Map<String,String>> badReq(IllegalArgumentException ex) {\n        return ResponseEntity.badRequest().body(Map.of("error", ex.getMessage()));\n    }\n}\n```\n```\n',tech:"java",displayNumber:387,sortKey:387},{id:"java-388",number:388,title:"What is `@ResponseStatus`?",question:"What is `@ResponseStatus`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@ResponseStatus` maps an exception (or handler method) to a specific HTTP status code and optional reason. When placed on a custom exception class, Spring returns the given status whenever that exception is thrown. It is simpler than `@ExceptionHandler` for straightforward status mapping but offers no control over the response body.\n\n\n```java\n@ResponseStatus(value = HttpStatus.NOT_FOUND, reason = "User not found")\npublic class UserNotFoundException extends RuntimeException {}\n```\n```\n',tech:"java",displayNumber:388,sortKey:388},{id:"java-389",number:389,title:"How do you validate request bodies in Spring?",question:"How do you validate request bodies in Spring?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Use Bean Validation (Jakarta Validation / Hibernate Validator). Annotate the DTO with constraints (`@NotNull`, `@Size`, `@Email`) and mark the controller parameter with `@Valid` (or `@Validated`). If validation fails, Spring throws `MethodArgumentNotValidException`, which you can catch in an `@ExceptionHandler` to return a 400 with field errors.\n\n\n```java\npublic record CreateUser(@NotBlank @Email String email, @Size(min=6) String password) {}\n\n@PostMapping\npublic ResponseEntity<Void> create(@Valid @RequestBody CreateUser req) { ... }\n```\n```\n",tech:"java",displayNumber:389,sortKey:389},{id:"java-390",number:390,title:"What is Spring AOP and what problem does it solve?",question:"What is Spring AOP and what problem does it solve?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`Aspect-Oriented Programming (AOP) modularizes cross-cutting concerns (logging, security, transactions, monitoring) that cut across many classes. Instead of scattering this code, AOP lets you define an "aspect" that runs at defined points. Spring AOP uses dynamic proxies (JDK or CGLIB) and supports method-execution join points only.


\`\`\`java
@Aspect
@Component
public class LoggingAspect {
    @Before("execution(* com.example.service.*.*(..))")
    public void log(JoinPoint jp) { System.out.println("call " + jp.getSignature()); }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:390,sortKey:390},{id:"java-391",number:391,title:"What are Aspect, JoinPoint, Advice, and Pointcut?",question:"What are Aspect, JoinPoint, Advice, and Pointcut?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'- **Aspect**: a modularization of a cross-cutting concern (a class annotated `@Aspect`).\n- **Join point**: a point in program execution (in Spring, a method execution) where an aspect can be applied.\n- **Advice**: the action taken at a join point (`@Before`, `@After`, `@AfterReturning`, `@AfterThrowing`, `@Around`).\n- **Pointcut**: an expression that matches a set of join points (where the advice should run).\n\n\n```java\n@Aspect @Component\npublic class PerfAspect {\n    @Around("execution(* com.example.service.*.*(..))")  // pointcut\n    public Object time(ProceedingJoinPoint pjp) throws Throwable { // advice\n        long t = System.nanoTime();\n        Object r = pjp.proceed();                          // join point\n        System.out.println((System.nanoTime()-t)/1e6 + "ms");\n        return r;\n    }\n}\n```\n```\n',tech:"java",displayNumber:391,sortKey:391},{id:"java-392",number:392,title:"What are the different types of Advice?",question:"What are the different types of Advice?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **@Before** – runs before the join point.
- **@After** – runs after the join point completes (always, like \`finally\`).
- **@AfterReturning** – runs after normal return; can access the return value.
- **@AfterThrowing** – runs if the method throws an exception.
- **@Around** – wraps the join point; can control whether it proceeds, modify arguments/return, and measure time. Most powerful.


\`\`\`java
@AfterReturning(pointcut="...", returning="result")
public void after(Object result) { /* use result */ }

@AfterThrowing(pointcut="...", throwing="ex")
public void onError(Exception ex) { /* handle */ }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:392,sortKey:392},{id:"java-393",number:393,title:"What is a Pointcut expression and what is the `execution` designator?",question:"What is a Pointcut expression and what is the `execution` designator?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A pointcut expression selects join points. The `execution` designator is the most common: `execution(modifiers? return-type declaring-type? method-name(params) throws?)`. Wildcards (`*`, `..`) help. Other designators: `within`, `this`, `target`, `args`, `annotation`, `@annotation`. Example: `execution(* com.example.service..*(..))` matches all methods in `service` and sub-packages.\n\n\n```java\n@Pointcut("execution(public * com.example..*Service.*(..))")\npublic void serviceMethods() {}\n\n@Before("serviceMethods()")\npublic void before() {}\n```\n```\n',tech:"java",displayNumber:393,sortKey:393},{id:"java-394",number:394,title:"What is the difference between Spring AOP and AspectJ?",question:"What is the difference between Spring AOP and AspectJ?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`- **Spring AOP**: runtime weaving via proxies; only supports method-execution join points on Spring beans; simpler, no separate compilation step.
- **AspectJ**: full AOP with compile-time or load-time weaving; supports field access, constructor, static init join points; works on non-Spring objects. More powerful but more complex.

Use Spring AOP for typical enterprise cross-cutting concerns; use AspectJ when you need non-method join points or to advise objects not managed by Spring.


\`\`\`java
// Spring AOP: proxy-based, method join points only
@EnableAspectJAutoProxy
@Configuration
public class AopConfig {}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:394,sortKey:394},{id:"java-395",number:395,title:"What is `@EnableAspectJAutoProxy` and proxying (JDK vs CGLIB)?",question:"What is `@EnableAspectJAutoProxy` and proxying (JDK vs CGLIB)?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"`@EnableAspectJAutoProxy` enables Spring AOP. Spring creates proxies: **JDK dynamic proxies** (default when the bean implements an interface) and **CGLIB** (when no interface, or to proxy concrete classes). Since Boot 2.0, proxying defaults to CGLIB (`spring.aop.proxy-target-class=true`). CGLIB subclasses the class, so `final` methods cannot be advised.\n\n\n```java\n@EnableAspectJAutoProxy(proxyTargetClass = true) // force CGLIB\n@Configuration\npublic class AopConfig {}\n```\n```\n",tech:"java",displayNumber:395,sortKey:395},{id:"java-396",number:396,title:"What is `@Transactional` and how does Spring manage transactions?",question:"What is `@Transactional` and how does Spring manage transactions?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@Transactional` demarcates a method (or class) as a single transaction. Spring uses AOP to create a proxy that begins a transaction before the method, commits on success, and rolls back on a runtime exception (by default; checked exceptions do not roll back unless configured). It relies on a `PlatformTransactionManager`. Key attributes: `propagation`, `isolation`, `readOnly`, `rollbackFor`, `timeout`.\n\n\n```java\n@Service\npublic class TransferService {\n    @Transactional(propagation = Propagation.REQUIRED, isolation = Isolation.READ_COMMITTED)\n    public void transfer(Long from, Long to, BigDecimal amt) { ... }\n}\n```\n```\n",tech:"java",displayNumber:396,sortKey:396},{id:"java-397",number:397,title:"What are transaction propagation levels?",question:"What are transaction propagation levels?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Propagation defines how a transaction relates to an existing one:
- **REQUIRED** (default): join existing or create new.
- **REQUIRES_NEW**: suspend existing, always create new.
- **NESTED**: nested transaction with savepoint.
- **SUPPORTS**: use existing, else non-transactional.
- **NOT_SUPPORTED**: suspend existing, run non-transactional.
- **MANDATORY**: must exist, else throw.
- **NEVER**: must not exist, else throw.


\`\`\`java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public void audit(String msg) { /* independent tx */ }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:397,sortKey:397},{id:"java-398",number:398,title:"What are transaction isolation levels?",question:"What are transaction isolation levels?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Isolation controls visibility of changes between concurrent transactions, mapped to standard SQL levels:
- **DEFAULT**: DB default.
- **READ_UNCOMMITTED**: may read uncommitted (dirty reads possible).
- **READ_COMMITTED**: no dirty reads.
- **REPEATABLE_READ**: no dirty/non-repeatable reads.
- **SERIALIZABLE**: full isolation, slowest.

Higher isolation reduces anomaly risk but lowers concurrency.


\`\`\`java
@Transactional(isolation = Isolation.REPEATABLE_READ)
public Account read(Long id) { ... }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:398,sortKey:398},{id:"java-399",number:399,title:"What is the difference between `@Component` and `@Bean`?",question:"What is the difference between `@Component` and `@Bean`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@Component` is used on a class you own so Spring auto-detects it via component scanning. `@Bean` is used inside a `@Configuration` class to explicitly instantiate and register a bean — typically for third-party classes you cannot annotate (e.g., `DataSource`, `RestTemplate`). `@Bean` gives full control over construction logic.\n\n\n```java\n@Component              // you own this class\npublic class MyService {}\n\n@Bean                   // explicit, for external class\npublic RestTemplate restTemplate() { return new RestTemplate(); }\n```\n```\n",tech:"java",displayNumber:399,sortKey:399},{id:"java-400",number:400,title:"How do you create a custom auto-configuration?",question:"How do you create a custom auto-configuration?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"To provide auto-configuration (e.g., for a library):\n1. Create a `@Configuration` class guarded by `@ConditionalOnClass` / `@ConditionalOnMissingBean`.\n2. Register it in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`.\n3. Optionally use `@EnableConfigurationProperties` to bind config.\nThis lets users get beans automatically only when conditions are met, while still allowing override.\n\n\n```java\n@Configuration\n@ConditionalOnClass(MyClient.class)\n@EnableConfigurationProperties(MyProps.class)\npublic class MyAutoConfig {\n    @Bean @ConditionalOnMissingBean\n    public MyClient client(MyProps p) { return new MyClient(p.getUrl()); }\n}\n```\n```\n",tech:"java",displayNumber:400,sortKey:400},{id:"java-401",number:401,title:"What is `@Conditional` and conditional annotations?",question:"What is `@Conditional` and conditional annotations?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'`@Conditional` registers a bean only when a specified `Condition` matches. Spring Boot extends this with ready-made annotations: `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`, `@ConditionalOnResource`, `@ConditionalOnWebApplication`, `@ConditionalOnExpression`. These power auto-configuration logic.\n\n\n```java\n@Bean\n@ConditionalOnProperty(name = "feature.cache.enabled", havingValue = "true")\npublic CacheService cacheService() { return new CacheService(); }\n```\n```\n',tech:"java",displayNumber:401,sortKey:401},{id:"java-402",number:402,title:"What is a circular dependency and how does Spring handle it?",question:"What is a circular dependency and how does Spring handle it?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A circular dependency occurs when two or more beans depend on each other (A → B → A). Spring resolves singleton circular references via "early references" (exposing a partially-constructed bean through a singleton factory). This works for constructor injection only if you use `@Lazy` on one side, since constructor cycles cannot be resolved normally and will throw `BeanCurrentlyInCreationException`. Setter/field injection can be resolved. The best fix is to redesign to remove the cycle.\n\n\n```java\n@Component\npublic class A {\n    public A(@Lazy B b) {} // break constructor cycle with @Lazy\n}\n```\n```\n',tech:"java",displayNumber:402,sortKey:402},{id:"java-403",number:403,title:"How do you test a Spring Boot application?",question:"How do you test a Spring Boot application?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Spring Boot testing uses `@SpringBootTest` to load the full application context (or a sliced context). Slices load only relevant layers: `@WebMvcTest` (controllers), `@DataJpaTest` (JPA), `@JsonTest`, `@RestClientTest`. Use `MockMvc` for HTTP testing, `@MockBean` to mock dependencies, and `@TestPropertySource`/`@DynamicPropertySource` for test config. `spring-boot-starter-test` bundles JUnit 5, Mockito, and AssertJ.\n\n\n```java\n@WebMvcTest(UserController.class)\nclass UserControllerTest {\n    @Autowired MockMvc mvc;\n    @MockBean UserService service;\n\n    @Test void get_ok() throws Exception {\n        when(service.findById(1L)).thenReturn(new User(1L,"a"));\n        mvc.perform(get("/api/users/1")).andExpect(status().isOk());\n    }\n}\n```\n```\n',tech:"java",displayNumber:403,sortKey:403},{id:"java-404",number:404,title:"What is `@DataJpaTest` and `@WebMvcTest`?",question:"What is `@DataJpaTest` and `@WebMvcTest`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"These are test slices that load only a subset of the Spring context for speed:\n- `@DataJpaTest`: configures an in-memory JPA setup (H2), repositories, `TestEntityManager`; good for repository tests.\n- `@WebMvcTest`: loads only web layer (controllers, filters, MVC infrastructure), not services; use `@MockBean` for dependencies.\n\nSlices dramatically reduce startup time versus `@SpringBootTest`.\n\n\n```java\n@DataJpaTest\nclass UserRepoTest {\n    @Autowired TestEntityManager em;\n    @Autowired UserRepository repo;\n}\n```\n```\n",tech:"java",displayNumber:404,sortKey:404},{id:"java-405",number:405,title:"What is Actuator and what endpoints does it provide?",question:"What is Actuator and what endpoints does it provide?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Spring Boot Actuator exposes operational endpoints for monitoring and management. Key endpoints: `/health` (app status), `/info` (build info), `/metrics` (counters/gauges), `/env`, `/beans`, `/mappings`, `/loggers`, `/shutdown` (disabled by default). In Boot 2+, most are disabled except `health` and `info` by default; enable with `management.endpoints.web.exposure.include=*`.\n\n\n```java\nmanagement.endpoints.web.exposure.include=health,info,metrics\nmanagement.endpoint.health.show-details=always\n```\n```\n",tech:"java",displayNumber:405,sortKey:405},{id:"java-406",number:406,title:"What is the difference between `@SpringBootTest` and test slices?",question:"What is the difference between `@SpringBootTest` and test slices?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@SpringBootTest` starts the full application context (optionally with a real embedded server via `webEnvironment`), making it closest to production but slowest. Test slices (`@WebMvcTest`, `@DataJpaTest`, etc.) start a minimal, targeted context for speed and isolation. Use slices for unit/integration tests of specific layers; use `@SpringBootTest` for end-to-end/context-load tests.\n\n\n```java\n@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)\nclass AppE2E { @LocalServerPort int port; }\n```\n```\n",tech:"java",displayNumber:406,sortKey:406},{id:"java-407",number:407,title:"What is Microservices Architecture?",question:"What is Microservices Architecture?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Microservices is an architectural style where an application is composed of small, independently deployable services, each owning its own data and communicating over lightweight protocols (HTTP/REST, messaging). Benefits: independent scaling, technology diversity, fault isolation, and team autonomy. Trade-offs: distributed complexity, network latency, data consistency challenges, and operational overhead.\n\n\n```java\nMonolith ──> [Auth Svc] [Order Svc] [Payment Svc] [Inventory Svc]\n             each: own DB, own deploy, talk via REST/gRPC\n```\n```\n",tech:"java",displayNumber:407,sortKey:407},{id:"java-408",number:408,title:"What is Service Discovery and how does Eureka work?",question:"What is Service Discovery and how does Eureka work?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Service Discovery lets services locate each other by logical name instead of hardcoded host/port. **Netflix Eureka** is a registry: services register themselves with a Eureka Server on startup and send heartbeats. Clients query the registry to resolve service instances. Spring Cloud provides `@EnableEurekaServer` (registry) and `@EnableDiscoveryClient` (client) with `spring-cloud-starter-netflix-eureka-*`.\n\n\n```java\n@EnableEurekaServer\n@SpringBootApplication\npublic class DiscoveryServer { public static void main(String[] a){ SpringApplication.run(DiscoveryServer.class,a);} }\n```\n```yaml\n```\n",tech:"java",displayNumber:408,sortKey:408},{id:"java-409",number:409,title:"What is an API Gateway and what does it do?",question:"What is an API Gateway and what does it do?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`An API Gateway is a single entry point that routes requests to downstream microservices, handling cross-cutting concerns: routing, load balancing, authentication/authorization, rate limiting, request/response transformation, caching, and logging. Spring Cloud Gateway (reactive, built on WebFlux/Netty) is the modern choice, replacing the legacy Zuul. It uses Route/Predicate/Filter model.


\`\`\`java
spring:
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**
          filters:
            - name: CircuitBreaker
              args:
                name: orderCB
\`\`\`
\`\`\`
`,tech:"java",displayNumber:409,sortKey:409},{id:"java-410",number:410,title:"What is Spring Cloud Config Server?",question:"What is Spring Cloud Config Server?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Config Server provides externalized, centralized configuration for microservices. It serves config from a Git repository (or Vault/file) to clients over HTTP. Clients (`@EnableConfigServer` on the server; `spring-cloud-config-client` on clients) fetch their config at startup. It supports profiles, encryption, and refresh via Spring Cloud Bus. This keeps configuration out of individual service artifacts.\n\n\n```java\n@EnableConfigServer\n@SpringBootApplication\npublic class ConfigServer { public static void main(String[] a){ SpringApplication.run(ConfigServer.class,a);} }\n```\n```yaml\n```\n",tech:"java",displayNumber:410,sortKey:410},{id:"java-411",number:411,title:"What is a Circuit Breaker and how does Resilience4j work?",question:"What is a Circuit Breaker and how does Resilience4j work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A circuit breaker prevents a service from repeatedly calling a failing dependency, failing fast and allowing recovery. **Resilience4j** provides `CircuitBreaker` with states CLOSED, OPEN, HALF_OPEN, plus `Retry`, `Bulkhead`, `RateLimiter`, and `TimeLimiter`. When failures exceed a threshold, the breaker opens; after a wait, it goes half-open to test recovery. Integrate via `@CircuitBreaker(name="svc")` or the Spring Cloud Circuit Breaker abstraction.\n\n\n```java\n@CircuitBreaker(name = "paymentService", fallbackMethod = "fallback")\npublic String charge() { return paymentClient.charge(); }\n\npublic String fallback(Throwable t) { return "default"; }\n```\n```\n',tech:"java",displayNumber:411,sortKey:411},{id:"java-412",number:412,title:"What are the Circuit Breaker states?",question:"What are the Circuit Breaker states?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`- **CLOSED**: normal operation; calls pass through; failures are counted.
- **OPEN**: failure threshold exceeded; calls fail fast without invoking the dependency. After a configured \`waitDuration\`, transitions to HALF_OPEN.
- **HALF_OPEN**: a limited number of test calls are allowed; if they succeed, back to CLOSED; if they fail, back to OPEN.

This protects the system and gives the downstream time to recover.


\`\`\`java
resilience4j.circuitbreaker:
  instances:
    paymentService:
      failureRateThreshold: 50
      waitDurationInOpenState: 5s
      slidingWindowType: COUNT_BASED
      slidingWindowSize: 10
\`\`\`
\`\`\`
`,tech:"java",displayNumber:412,sortKey:412},{id:"java-413",number:413,title:"What is Distributed Tracing and how do Sleuth/Zipkin work?",question:"What is Distributed Tracing and how do Sleuth/Zipkin work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"Distributed tracing tracks a request as it flows across multiple microservices, helping diagnose latency and failures. **Spring Cloud Sleuth** (Micrometer Tracing in newer versions) adds trace/span IDs to logs and propagates them via headers across service calls. **Zipkin** is a backend that collects and visualizes these spans. Together, you see the full call tree and per-service timing.",tech:"java",displayNumber:413,sortKey:413},{id:"java-414",number:414,title:"What is an API Gateway vs Load Balancer?",question:"What is an API Gateway vs Load Balancer?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"A **Load Balancer** (e.g., client-side Ribbon/Spring Cloud LoadBalancer, or hardware/cloud LB) distributes traffic across instances of a single service for scalability/availability. An **API Gateway** is an application-layer facade that routes to many different services and applies cross-cutting policies (auth, throttling, transformation). The gateway often uses a load balancer internally (note `lb://service-name` in routes) to pick an instance.",tech:"java",displayNumber:414,sortKey:414},{id:"java-415",number:415,title:"What is `@LoadBalanced` RestTemplate?",question:"What is `@LoadBalanced` RestTemplate?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Annotating a `RestTemplate` bean with `@LoadBalanced` enables client-side load balancing: instead of a concrete URL, you call by service name (e.g., `http://order-service/api/orders`), and Spring Cloud LoadBalancer resolves it to a registered instance and balances across replicas. It works by intercepting requests via `LoadBalancerInterceptor`.\n\n\n```java\n@Bean @LoadBalanced\npublic RestTemplate restTemplate() { return new RestTemplate(); }\n\n// usage\nrestTemplate.getForObject("http://order-service/api/orders", List.class);\n```\n```\n',tech:"java",displayNumber:415,sortKey:415},{id:"java-416",number:416,title:"What is Spring Cloud and how does it relate to microservices?",question:"What is Spring Cloud and how does it relate to microservices?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Spring Cloud is a collection of tools that implement common distributed-system patterns on top of Spring Boot: service discovery (Eureka), config (Config Server), gateway (Spring Cloud Gateway), circuit breakers (Resilience4j), load balancing, distributed tracing (Sleuth/Zipkin), and messaging. It standardizes microservices building blocks so teams don't reinvent them.


\`\`\`java
<dependency>
  <groupId>org.springframework.cloud</groupId>
  <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
</dependency>
\`\`\`
\`\`\`
`,tech:"java",displayNumber:416,sortKey:416},{id:"java-417",number:417,title:"What is the difference between monolith and microservices?",question:"What is the difference between monolith and microservices?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"A **monolith** is a single deployable unit with one codebase and one database — simpler to develop, test, and deploy, but harder to scale parts independently and riskier to change. **Microservices** split functionality into independent services with separate data stores and deployments — better scalability, isolation, and team autonomy, but add distributed-system complexity (networking, data consistency, observability). Choose based on team size, scale, and domain complexity.\n\n\n```java\nMonolith: 1 app + 1 DB, scale all-or-nothing\nMicroservices: N apps + N DBs, scale per service\n```\n```\n",tech:"java",displayNumber:417,sortKey:417},{id:"java-418",number:418,title:"How do microservices communicate?",question:"How do microservices communicate?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Two main styles:\n- **Synchronous (HTTP/REST, gRPC)**: request/response, easy to use, but creates runtime coupling and latency chains.\n- **Asynchronous (messaging: Kafka, RabbitMQ, Spring AMQP)**: event-driven, decoupled, resilient, but adds eventual consistency and broker complexity.\n\nSpring supports both via `RestTemplate`/`WebClient` (sync) and `Spring Kafka`/`Spring AMQP` (async). Choose sync for request/response needs; async for high-throughput, decoupled workflows.\n\n\n```java\n// Async with Spring Kafka\n@KafkaListener(topics = "orders")\npublic void onOrder(OrderEvent e) { process(e); }\n```\n```\n',tech:"java",displayNumber:418,sortKey:418},{id:"java-419",number:419,title:"What is eventual consistency in microservices?",question:"What is eventual consistency in microservices?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Since each microservice owns its own database, cross-service transactions cannot use a single ACID transaction. Instead, systems become *eventually consistent*: an update propagates asynchronously (via events/messaging) and all data stores converge to a consistent state after a short delay. Patterns include Saga (choreography or orchestration), outbox, and CDC (Change Data Capture). This trades immediate consistency for availability and resilience.


\`\`\`java
@Transactional
public void placeOrder(Order o) {
    repo.save(o);
    outbox.save(new OutboxEvent("OrderPlaced", o)); // published later
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:419,sortKey:419},{id:"java-420",number:420,title:"What is the Saga pattern?",question:"What is the Saga pattern?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"The Saga pattern manages a distributed transaction spanning multiple services without a global lock. It sequences local transactions; each step publishes an event that triggers the next. If a step fails, compensating transactions undo prior steps. Two styles: **Choreography** (services react to events, decentralized) and **Orchestration** (a central orchestrator directs steps). Spring integrates via event buses (Spring Cloud Stream/Kafka) or orchestrators.\n\n\n```java\nChoreography: OrderPlaced -> [Inventory] reserved -> [Payment] charged\nFailure: PaymentFailed -> Inventory.compensate()\n```\n```\n",tech:"java",displayNumber:420,sortKey:420},{id:"java-421",number:421,title:"How do you secure microservices with Spring Security / OAuth2?",question:"How do you secure microservices with Spring Security / OAuth2?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'Microservices typically use **OAuth2 / JWT**: an Authorization Server issues tokens; the API Gateway validates them and forwards user context. Each service uses `spring-boot-starter-security` + `spring-boot-starter-oauth2-resource-server` to validate JWTs (`spring.security.oauth2.resourceserver.jwt.issuer-uri`). Centralized auth at the gateway plus per-service method security (`@PreAuthorize`) enforces fine-grained access.\n\n\n```java\nspring:\n  security:\n    oauth2:\n      resourceserver:\n        jwt:\n          issuer-uri: http://auth-server:9000\n```\n```java\n@PreAuthorize("hasRole(\'ADMIN\')")\n@GetMapping("/admin")\npublic List<User> all() { ... }\n```\n```\n',tech:"java",displayNumber:421,sortKey:421},{id:"java-422",number:422,title:"What is CQRS and Event Sourcing?",question:"What is CQRS and Event Sourcing?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"**CQRS** (Command Query Responsibility Segregation) separates write (command) and read (query) models, often using different datastores optimized for each, improving performance and scalability. **Event Sourcing** stores state as a sequence of immutable events rather than current state; the current state is replayed from events. They complement each other: commands emit events (sourced), and a read model is built from them. Spring supports via Axon Framework or custom event handlers.\n\n\n```java\n@EventHandler\npublic void on(OrderPlaced e) { readModel.insert(e.toView()); }\n```\n```\n",tech:"java",displayNumber:422,sortKey:422},{id:"java-423",number:423,title:"What is Spring Boot DevTools?",question:"What is Spring Boot DevTools?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot DevTools improves developer experience: automatic restart on classpath changes, LiveReload of browser, sensible defaults for development (disabled caching for templates), and a `RemoteDebug` option. It is intended only for development — exclude it from production builds. Add `spring-boot-devtools` as an `optional` dependency.\n\n\n```java\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-devtools</artifactId>\n  <optional>true</optional>\n</dependency>\n```\n```\n",tech:"java",displayNumber:423,sortKey:423},{id:"java-424",number:424,title:"How do you package and run a Spring Boot application?",question:"How do you package and run a Spring Boot application?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot produces a fat/uber JAR via `spring-boot-maven-plugin` (or Gradle plugin) that includes the embedded server and all dependencies. Run with `java -jar app.jar`. Profiles and config can be overridden at runtime (`--spring.profiles.active=prod`, `--server.port=8080`). Containers (Docker) wrap the JAR for cloud deployment; Buildpacks are supported out of the box with `mvn spring-boot:build-image`.\n\n\n```java\n./mvnw package\njava -jar target/app.jar --spring.profiles.active=prod\n```\n```\n",tech:"java",displayNumber:424,sortKey:424},{id:"java-425",number:425,title:"What are the 12-factor app principles relevant to Spring Boot microservices?",question:"What are the 12-factor app principles relevant to Spring Boot microservices?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Key 12-factor principles Spring Boot naturally supports:
1. **Codebase**: one repo per service.
2. **Dependencies**: declared in Maven/Gradle.
3. **Config**: externalized via properties/env (Spring Boot excels here).
4. **Backing services**: treated as attached resources (datasource beans).
5. **Build/Release/Run**: separated; fat JAR + profile.
6. **Processes**: stateless; use request/session scopes carefully.
7. **Port binding**: embedded server exposes via port.
8. **Concurrency**: scale horizontally.
9. **Disposability**: fast startup/shutdown (important for containers).
10. **Dev/prod parity**: same artifact across environments.

Spring Boot's externalized config, actuators, and embedded servers make most of these trivial.`,tech:"java",displayNumber:425,sortKey:425},{id:"java-426",number:426,title:"What is JPA and how does it relate to Hibernate?",question:"What is JPA and how does it relate to Hibernate?",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`JPA (Java Persistence API) is a specification (JSR 338) that defines a standard for ORM in Java. It provides annotations and interfaces for mapping Java objects to relational databases. JPA is just the API — it doesn't provide implementation.

Hibernate is one of the most popular JPA implementations. It was the inspiration for JPA and provides the actual persistence logic. Other implementations include EclipseLink and OpenJPA.

Think of JPA as the interface and Hibernate as the concrete class that implements it.


\`\`\`java
// JPA is just annotations/interfaces — no implementation
@Entity
public class User {
    @Id @GeneratedValue
    private Long id;
    private String name;
}

// Hibernate provides the implementation
// In persistence.xml or application.yml:
// spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

// Spring Boot auto-configures Hibernate as default JPA provider
\`\`\`
\`\`\`
`,tech:"java",displayNumber:426,sortKey:426},{id:"java-427",number:427,title:"What is ORM and what problem does it solve?",question:"What is ORM and what problem does it solve?",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`ORM (Object-Relational Mapping) is a technique that maps Java objects to database tables, bridging the gap between object-oriented programming and relational databases (the impedance mismatch).

Without ORM, developers write tedious JDBC code for CRUD operations. ORM handles:
- Mapping objects to tables and fields to columns
- Automatic SQL generation
- Transaction management
- Caching for performance
- Lazy loading of relationships


\`\`\`java
// Without ORM - verbose JDBC
String sql = "SELECT id, name FROM users WHERE id = ?";
PreparedStatement ps = conn.prepareStatement(sql);
ps.setLong(1, userId);
ResultSet rs = ps.executeQuery();

// With ORM - clean and type-safe
User user = entityManager.find(User.class, userId);
// or Spring Data JPA
Optional<User> user = userRepository.findById(userId);
\`\`\`
\`\`\`
`,tech:"java",displayNumber:427,sortKey:427},{id:"java-428",number:428,title:"Entity and table mapping",question:"Entity and table mapping",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Entity marks a persistent class, while @Table controls its physical table name and constraints. The class needs a no-argument constructor and an identity field.


\`\`\`java
@Entity
@Table(name = "customers")
class Customer {
    @Id Long id;
    protected Customer() {}
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:428,sortKey:428},{id:"java-429",number:429,title:"Primary key with @Id",question:"Primary key with @Id",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Id identifies the column that uniquely identifies each entity. It may be placed on a field or getter, but the choice determines access strategy for all mappings.


\`\`\`java
@Entity class Account {
    @Id private String iban;
    protected Account() {}
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:429,sortKey:429},{id:"java-430",number:430,title:"IDENTITY key generation",question:"IDENTITY key generation",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"IDENTITY delegates key creation to an auto-increment/identity column. Hibernate usually must insert before it knows the identifier, so batching can be less efficient.\n\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.IDENTITY) Long id;\n```\n```\n",tech:"java",displayNumber:430,sortKey:430},{id:"java-431",number:431,title:"SEQUENCE key generation",question:"SEQUENCE key generation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'SEQUENCE obtains identifiers from a database sequence and can allocate blocks with allocationSize, improving batching. It is commonly preferred on PostgreSQL and Oracle.\n\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.SEQUENCE, generator="user_seq")\n@SequenceGenerator(name="user_seq", sequenceName="user_seq", allocationSize=50)\nLong id;\n```\n```\n',tech:"java",displayNumber:431,sortKey:431},{id:"java-432",number:432,title:"TABLE key generation",question:"TABLE key generation",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'TABLE stores the next identifier in a dedicated table. It is portable but requires row locking and is generally slower than native sequences or identity columns.\n\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.TABLE, generator="ids")\n@TableGenerator(name="ids", table="id_gen", pkColumnName="name", valueColumnName="value", pkColumnValue="customer")\nLong id;\n```\n```\n',tech:"java",displayNumber:432,sortKey:432},{id:"java-433",number:433,title:"AUTO key generation",question:"AUTO key generation",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"AUTO lets the provider select a strategy from the dialect. It is convenient for portable examples, but production schemas should choose and migrate an explicit strategy.\n\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.AUTO) Long id;\n```\n```\n",tech:"java",displayNumber:433,sortKey:433},{id:"java-434",number:434,title:"@Column options",question:"@Column options",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'@Column customizes name, nullability, uniqueness, length, and precision. These are mapping hints and should be backed by actual database constraints and migrations.\n\n\n```java\n@Column(name="email_address", nullable=false, unique=true, length=320)\nString email;\n```\n```\n',tech:"java",displayNumber:434,sortKey:434},{id:"java-435",number:435,title:"@GeneratedValue composite keys",question:"@GeneratedValue composite keys",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Composite keys use an @EmbeddableId class or multiple @Id fields with @IdClass. The key class must implement equals/hashCode and be serializable. @GeneratedValue applies only to simple keys.


\`\`\`java
@Embeddable class OrderItemId implements Serializable {
    Long orderId; Long productId;
    // equals, hashCode
}
@Entity @IdClass(OrderItemId.class) class OrderItem {
    @Id Long orderId; @Id Long productId;
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:435,sortKey:435},{id:"java-436",number:436,title:"One-to-one relationship",question:"One-to-one relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'One-to-one maps one row to one related row. Put the foreign key on the owning side with @JoinColumn; make the inverse side mappedBy to avoid a second join column.\n\n\n```java\n@OneToOne @JoinColumn(name="profile_id", nullable=false)\nProfile profile;\n```\n```\n',tech:"java",displayNumber:436,sortKey:436},{id:"java-437",number:437,title:"One-to-many relationship",question:"One-to-many relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'A one-to-many collection is usually inverse and paired with a many-to-one foreign key on the child. mappedBy identifies that child association as owner.\n\n\n```java\n@OneToMany(mappedBy="order")\nList<Line> lines = new ArrayList<>();\n```\n```\n',tech:"java",displayNumber:437,sortKey:437},{id:"java-438",number:438,title:"Many-to-one relationship",question:"Many-to-one relationship",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'Many child entities can reference one parent. It is the owning side and normally uses a foreign-key column; prefer LAZY loading for large associations.\n\n\n```java\n@ManyToOne(fetch=FetchType.LAZY, optional=false)\n@JoinColumn(name="customer_id")\nCustomer customer;\n```\n```\n',tech:"java",displayNumber:438,sortKey:438},{id:"java-439",number:439,title:"Many-to-many relationship",question:"Many-to-many relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Many-to-many uses a join table and is convenient for simple links. For attributes on the link, model the join table as an entity instead.


\`\`\`java
@ManyToMany
@JoinTable(name="user_role",
    joinColumns=@JoinColumn(name="user_id"),
    inverseJoinColumns=@JoinColumn(name="role_id"))
Set<Role> roles;
\`\`\`
\`\`\`
`,tech:"java",displayNumber:439,sortKey:439},{id:"java-440",number:440,title:"Cascade operations",question:"Cascade operations",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Cascade propagates persistence operations from parent to child; ALL includes remove, which can delete shared data accidentally. Use only the operations the aggregate owns.\n\n\n```java\n@OneToMany(mappedBy="invoice", cascade=CascadeType.ALL, orphanRemoval=true)\nList<Item> items;\n```\n```\n',tech:"java",displayNumber:440,sortKey:440},{id:"java-441",number:441,title:"orphanRemoval",question:"orphanRemoval",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"orphanRemoval deletes a child removed from an owned collection or whose one-to-one reference is cleared. It is not a replacement for cascade remove and should not be used for shared children.\n\n\n```java\nitems.remove(item); // owned orphan is deleted on flush\n```\n```\n",tech:"java",displayNumber:441,sortKey:441},{id:"java-442",number:442,title:"LAZY versus EAGER fetch",question:"LAZY versus EAGER fetch",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"LAZY loads an association when accessed, whereas EAGER loads it immediately. LAZY reduces unnecessary work but requires an open persistence context or an explicit fetch query.\n\n\n```java\n@ManyToOne(fetch=FetchType.LAZY) Customer customer;\n@OneToMany(fetch=FetchType.LAZY) List<Line> lines;\n```\n```\n",tech:"java",displayNumber:442,sortKey:442},{id:"java-443",number:443,title:"Owning side and mappedBy",question:"Owning side and mappedBy",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"The owning side writes the foreign key or join table. mappedBy points to the Java property on that owner; setting only the inverse side does not update the database.\n\n\n```java\nparent.children.add(child);\nchild.parent = parent; // keep both sides synchronized\n```\n```\n",tech:"java",displayNumber:443,sortKey:443},{id:"java-444",number:444,title:"Bidirectional relationship sync",question:"Bidirectional relationship sync",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Always update both sides of a bidirectional association in code; the persistence provider only synchronizes from the owning side. Helper methods on the parent prevent mistakes.


\`\`\`java
@Entity class Order {
    @OneToMany(mappedBy="order") List<Line> lines = new ArrayList<>();
    void addLine(Line l) { lines.add(l); l.setOrder(this); }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:444,sortKey:444},{id:"java-445",number:445,title:"Join table customization",question:"Join table customization",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`@JoinTable customizes the join table name, columns, and constraints for many-to-many or unidirectional one-to-many. Prefer explicit names for clarity in migrations.


\`\`\`java
@ManyToMany
@JoinTable(name="order_product",
    joinColumns=@JoinColumn(name="order_id"),
    inverseJoinColumns=@JoinColumn(name="product_id"))
Set<Product> products;
\`\`\`
\`\`\`
`,tech:"java",displayNumber:445,sortKey:445},{id:"java-446",number:446,title:"Embeddable value object",question:"Embeddable value object",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Embeddable maps a value type into the owner table rather than a separate table. It has no independent identity and is persisted with its owner.


\`\`\`java
@Embeddable class Address {
    String city; String country;
}
@Embedded Address address;
\`\`\`
\`\`\`
`,tech:"java",displayNumber:446,sortKey:446},{id:"java-447",number:447,title:"Element collection",question:"Element collection",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@ElementCollection persists basic values or embeddables in a collection table. Elements have no identity and the collection is commonly replaced as a unit.\n\n\n```java\n@ElementCollection\n@CollectionTable(name="user_phone", joinColumns=@JoinColumn(name="user_id"))\nSet<String> phones;\n```\n```\n',tech:"java",displayNumber:447,sortKey:447},{id:"java-448",number:448,title:"AttributeOverride",question:"AttributeOverride",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@AttributeOverride renames or changes a field of an embedded value for a particular owner, useful when embedding the same type twice.\n\n\n```java\n@Embedded\n@AttributeOverrides(@AttributeOverride(name="city", column=@Column(name="billing_city")))\nAddress billing;\n```\n```\n',tech:"java",displayNumber:448,sortKey:448},{id:"java-449",number:449,title:"Nested embeddables",question:"Nested embeddables",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Embeddables can contain other embeddables, letting domain value objects remain cohesive while columns stay in the owner table. Override nested paths when names collide.


\`\`\`java
@Embeddable class Contact {
    @Embedded Address address;
}
@Embedded Contact contact;
\`\`\`
\`\`\`
`,tech:"java",displayNumber:449,sortKey:449},{id:"java-450",number:450,title:"JPQL basics",question:"JPQL basics",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'JPQL queries entities and their attributes, not table names and columns. Parameters should be named or positional; binding avoids injection and type conversion errors.\n\n\n```java\nList<Customer> cs = em.createQuery(\n    "select c from Customer c where c.email=:e", Customer.class)\n    .setParameter("e", email).getResultList();\n```\n```\n',tech:"java",displayNumber:450,sortKey:450},{id:"java-451",number:451,title:"JPQL joins",question:"JPQL joins",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"JPQL joins navigate mapped associations and can filter or select related data. join fetch also initializes the association, but collection fetch joins can duplicate root rows.\n\n\n```java\nselect o from Order o join o.customer c where c.id = :id\n```\n```\n",tech:"java",displayNumber:451,sortKey:451},{id:"java-452",number:452,title:"JPQL bulk update",question:"JPQL bulk update",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Bulk UPDATE and DELETE operate directly in the database and bypass entity callbacks, dirty checking, and managed state. Clear or refresh the persistence context afterward.


\`\`\`java
int n = em.createQuery(
    "update Account a set a.active=false where a.lastLogin < :d")
    .setParameter("d", date).executeUpdate();
em.clear();
\`\`\`
\`\`\`
`,tech:"java",displayNumber:452,sortKey:452},{id:"java-453",number:453,title:"HQL extensions",question:"HQL extensions",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Hibernate Query Language is JPQL-compatible but offers provider-specific features. Use JPQL for portability and HQL only when a Hibernate capability is intentional.\n\n\n```java\nsession.createQuery("from Customer c where c.email like :prefix", Customer.class)\n    .setParameter("prefix", "a%").list();\n```\n```\n',tech:"java",displayNumber:453,sortKey:453},{id:"java-454",number:454,title:"Criteria API",question:"Criteria API",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Criteria builds type-safe dynamic queries without string concatenation. It is verbose but useful when predicates and joins are assembled conditionally.


\`\`\`java
CriteriaBuilder cb = em.getCriteriaBuilder();
CriteriaQuery<Customer> q = cb.createQuery(Customer.class);
Root<Customer> c = q.from(Customer.class);
q.where(cb.equal(c.get("active"), true));
List<Customer> r = em.createQuery(q).getResultList();
\`\`\`
\`\`\`
`,tech:"java",displayNumber:454,sortKey:454},{id:"java-455",number:455,title:"Static metamodel",question:"Static metamodel",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"The generated JPA metamodel replaces string property names in Criteria queries with compile-time symbols. Configure annotation processing to generate Customer_.\n\n\n```java\nRoot<Customer> c = q.from(Customer.class);\nq.select(c).where(cb.equal(c.get(Customer_.active), true));\n```\n```\n",tech:"java",displayNumber:455,sortKey:455},{id:"java-456",number:456,title:"Native SQL query",question:"Native SQL query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Native queries use database SQL directly and are appropriate for vendor features, complex reporting, or tuned statements. Map the result carefully and avoid database-specific lock-in without need.\n\n\n```java\nList<Customer> r = em.createNativeQuery(\n    "select * from customers where status=?", Customer.class)\n    .setParameter(1, "ACTIVE").getResultList();\n```\n```\n',tech:"java",displayNumber:456,sortKey:456},{id:"java-457",number:457,title:"Named query",question:"Named query",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'Named queries are defined once and validated or parsed early by the provider. They centralize frequently reused statements and can improve query plan reuse.\n\n\n```java\n@NamedQuery(name="Customer.active", query="select c from Customer c where c.active=true")\n```\n```\n',tech:"java",displayNumber:457,sortKey:457},{id:"java-458",number:458,title:"N+1 select problem",question:"N+1 select problem",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"N+1 occurs when one query loads N parents and each lazy association triggers another query. Detect it with SQL logging and fix the access pattern rather than making everything eager.\n\n\n```java\nList<Order> orders = repo.findAll();\norders.forEach(o -> o.getLines().size()); // potentially N+1\n```\n```\n",tech:"java",displayNumber:458,sortKey:458},{id:"java-459",number:459,title:"Fetch join solution",question:"Fetch join solution",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'A fetch join loads an association in the original query. Use distinct for collection joins and paginate cautiously because SQL row multiplication breaks many pagination strategies.\n\n\n```java\n@Query("select distinct o from Order o join fetch o.lines")\nList<Order> withLines();\n```\n```\n',tech:"java",displayNumber:459,sortKey:459},{id:"java-460",number:460,title:"EntityGraph",question:"EntityGraph",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@EntityGraph declares an attribute loading plan independently of JPQL. It is readable for repository methods and can override default fetch behavior for a use case.\n\n\n```java\n@EntityGraph(attributePaths="lines")\nOptional<Order> findById(Long id);\n```\n```\n',tech:"java",displayNumber:460,sortKey:460},{id:"java-461",number:461,title:"Batch fetching",question:"Batch fetching",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Hibernate batch fetching groups lazy association loads into IN queries, reducing round trips without one huge join. Tune batch size and verify SQL for your database.\n\n\n```java\n@BatchSize(size=25)\n@OneToMany(mappedBy="order")\nList<Line> lines;\n```\n```\n',tech:"java",displayNumber:461,sortKey:461},{id:"java-462",number:462,title:"DTO projections for reads",question:"DTO projections for reads",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Selecting a DTO avoids managing full entities and avoids accidental lazy loads. Constructor expressions are portable JPQL and ideal for read-only API responses.\n\n\n```java\nselect new com.acme.OrderView(o.id, c.name)\nfrom Order o join o.customer c\n```\n```\n",tech:"java",displayNumber:462,sortKey:462},{id:"java-463",number:463,title:"Subselect fetching",question:"Subselect fetching",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'@Fetch(FetchMode.SUBSELECT) loads a collection with a single additional query using a subselect derived from the original query. It works well for larger collections when the parent query has no join fetch.\n\n\n```java\n@OneToMany(mappedBy="order")\n@Fetch(FetchMode.SUBSELECT)\nList<Line> lines;\n```\n```\n',tech:"java",displayNumber:463,sortKey:463},{id:"java-464",number:464,title:"Persistence context L1 cache",question:"Persistence context L1 cache",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Each EntityManager has a first-level cache. Repeated find calls for the same identity return the managed instance; it is mandatory, scoped to the persistence context, and not shared between requests.\n\n\n```java\nCustomer a = em.find(Customer.class, 1L);\nCustomer b = em.find(Customer.class, 1L);\nassert a == b;\n```\n```\n",tech:"java",displayNumber:464,sortKey:464},{id:"java-465",number:465,title:"Second-level cache",question:"Second-level cache",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"L2 cache is shared across persistence contexts and must be explicitly configured. Cache entities only when their change rate and access pattern justify invalidation complexity.\n\n\n```java\n@Cacheable\n@org.hibernate.annotations.Cache(usage=CacheConcurrencyStrategy.READ_WRITE)\nclass Country {}\n```\n```\n",tech:"java",displayNumber:465,sortKey:465},{id:"java-466",number:466,title:"Ehcache L2 provider",question:"Ehcache L2 provider",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Ehcache can provide a durable, configurable Hibernate second-level cache through JCache or a native integration. Configure regions, expiry, and provider properties; it is not a substitute for database correctness.\n\n\n```java\nspring.jpa.properties.hibernate.cache.use_second_level_cache=true\nspring.jpa.properties.hibernate.javax.cache.provider=org.ehcache.jsr107.EhcacheCachingProvider\n```\n```\n",tech:"java",displayNumber:466,sortKey:466},{id:"java-467",number:467,title:"Caffeine caching",question:"Caffeine caching",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Caffeine is an in-memory high-performance cache often used through Spring Cache. It is generally application-local; use a distributed cache when multiple nodes must share entries.


\`\`\`java
@Cacheable(cacheNames="countries", key="#id")
Country get(Long id) {
    return repo.findById(id).orElseThrow();
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:467,sortKey:467},{id:"java-468",number:468,title:"Query cache",question:"Query cache",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'Hibernate query cache stores query result identifiers, not complete rows, and depends on L2 entity cache for objects. Updates invalidate regions, so use it only for stable, repeated queries.\n\n\n```java\nquery.setHint("org.hibernate.cacheable", true);\n```\n```\n',tech:"java",displayNumber:468,sortKey:468},{id:"java-469",number:469,title:"Cache concurrency strategies",question:"Cache concurrency strategies",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"READ_ONLY suits immutable data; NONSTRICT_READ_WRITE tolerates brief staleness; READ_WRITE adds coordination; TRANSACTIONAL requires a compatible transactional cache. Choose based on consistency needs.\n\n\n```java\n@Cache(usage=CacheConcurrencyStrategy.READ_ONLY)\nclass Currency {}\n```\n```\n",tech:"java",displayNumber:469,sortKey:469},{id:"java-470",number:470,title:"@Transactional boundary",question:"@Transactional boundary",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Transactional binds an EntityManager and transaction to a service method. Keep the boundary around a business operation, not a controller, and remember self-invocation bypasses proxies.


\`\`\`java
@Transactional
public void transfer(long from, long to, BigDecimal amount) {
    debit(from, amount);
    credit(to, amount);
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:470,sortKey:470},{id:"java-471",number:471,title:"Transaction propagation",question:"Transaction propagation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"REQUIRED joins or creates a transaction; REQUIRES_NEW suspends the current one; SUPPORTS is optional; MANDATORY requires one; NEVER rejects one. Pick semantics deliberately for side effects.\n\n\n```java\n@Transactional(propagation=Propagation.REQUIRES_NEW)\nvoid writeAudit(Audit a) {}\n```\n```\n",tech:"java",displayNumber:471,sortKey:471},{id:"java-472",number:472,title:"Transaction isolation",question:"Transaction isolation",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Isolation controls visibility of concurrent changes: READ_COMMITTED is a common default, REPEATABLE_READ prevents non-repeatable reads, and SERIALIZABLE is strongest but costly. Database support varies.


\`\`\`java
@Transactional(isolation=Isolation.READ_COMMITTED)
void reserve() {
    // atomic DB update
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:472,sortKey:472},{id:"java-473",number:473,title:"Rollback rules",question:"Rollback rules",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Spring rolls back unchecked exceptions by default. Configure rollbackFor for checked exceptions, and avoid catching failures unless you rethrow or mark rollback-only.\n\n\n```java\n@Transactional(rollbackFor=IOException.class)\nvoid importFile() throws IOException {}\n```\n```\n",tech:"java",displayNumber:473,sortKey:473},{id:"java-474",number:474,title:"Read-only transactions",question:"Read-only transactions",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`readOnly=true communicates intent and may enable provider optimizations; it does not universally enforce that no writes occur. Do not rely on it as authorization or data protection.


\`\`\`java
@Transactional(readOnly=true)
List<Customer> search(String text) {
    return repo.findByNameContaining(text);
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:474,sortKey:474},{id:"java-475",number:475,title:"Transaction timeout",question:"Transaction timeout",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"A timeout limits transaction duration and helps release locks under slow work. It is a guardrail, not a performance fix; keep remote calls outside database transactions.\n\n\n```java\n@Transactional(timeout=5)\nvoid updateInventory() {}\n```\n```\n",tech:"java",displayNumber:475,sortKey:475},{id:"java-476",number:476,title:"Declarative vs programmatic transactions",question:"Declarative vs programmatic transactions",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"Declarative @Transactional is the default and sufficient for most cases. Programmatic TransactionTemplate gives fine-grained control when business logic needs multiple commit boundaries in one method.\n\n\n```java\n@Autowired PlatformTransactionManager tx;\nTransactionTemplate tt = new TransactionTemplate(tx);\ntt.execute(status -> { /* work */ return result; });\n```\n```\n",tech:"java",displayNumber:476,sortKey:476},{id:"java-477",number:477,title:"Repository abstraction",question:"Repository abstraction",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"JpaRepository supplies CRUD, paging, sorting, and flush operations for an entity and identifier. Spring creates the implementation from the interface.\n\n\n```java\ninterface CustomerRepository extends JpaRepository<Customer, Long> {}\n```\n```\n",tech:"java",displayNumber:477,sortKey:477},{id:"java-478",number:478,title:"Derived query methods",question:"Derived query methods",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Spring Data parses method names into predicates. Use clear names for simple queries; switch to @Query when names become unreadable.\n\n\n```java\nList<Customer> findByActiveTrueAndNameContainingIgnoreCase(String name);\n```\n```\n",tech:"java",displayNumber:478,sortKey:478},{id:"java-479",number:479,title:"@Query repository query",question:"@Query repository query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@Query defines JPQL on a repository method and supports named parameters. Use a modifying query for updates and manage its persistence context explicitly.\n\n\n```java\n@Query("select c from Customer c where c.email=:email")\nOptional<Customer> findByEmail(@Param("email") String email);\n```\n```\n',tech:"java",displayNumber:479,sortKey:479},{id:"java-480",number:480,title:"Modifying repository query",question:"Modifying repository query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@Modifying marks UPDATE or DELETE queries so Spring executes them as writes. clearAutomatically prevents stale managed entities after bulk SQL.\n\n\n```java\n@Modifying(clearAutomatically=true)\n@Query("delete from Customer c where c.active=false")\nint deleteInactive();\n```\n```\n',tech:"java",displayNumber:480,sortKey:480},{id:"java-481",number:481,title:"Paging and sorting",question:"Paging and sorting",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Pageable applies limit/offset and sorting while Page includes a count query. Slice avoids the count when callers only need to know whether another page exists.\n\n\n```java\nPage<Customer> findByActiveTrue(Pageable page);\n```\n```\n",tech:"java",displayNumber:481,sortKey:481},{id:"java-482",number:482,title:"Specifications",question:"Specifications",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'JpaSpecificationExecutor composes reusable Criteria predicates for optional filters. It keeps dynamic search logic out of string-built JPQL.\n\n\n```java\nSpecification<Customer> active = (r, q, cb) -> cb.isTrue(r.get("active"));\nrepo.findAll(active);\n```\n```\n',tech:"java",displayNumber:482,sortKey:482},{id:"java-483",number:483,title:"Interface projections",question:"Interface projections",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Interface projections select only requested properties and expose a lightweight view. Nested projections can traverse associations but may still trigger joins or loads.\n\n\n```java\ninterface NameView { String getName(); }\nList<NameView> findByActiveTrue();\n```\n```\n",tech:"java",displayNumber:483,sortKey:483},{id:"java-484",number:484,title:"Class and record projections",question:"Class and record projections",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Class projections use constructor expressions or repository support to create immutable result objects. They are useful at service boundaries where entities should not escape.\n\n\n```java\nrecord CustomerView(Long id, String name) {}\n@Query("select new com.acme.CustomerView(c.id, c.name) from Customer c")\nList<CustomerView> views();\n```\n```\n',tech:"java",displayNumber:484,sortKey:484},{id:"java-485",number:485,title:"Query by example",question:"Query by example",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Query by Example builds a dynamic predicate from a probe instance with matching rules. It is convenient for filter forms but less explicit than specifications for complex queries.\n\n\n```java\nExample<Customer> ex = Example.of(new Customer(null, "A%", true));\nrepo.findAll(ex);\n```\n```\n',tech:"java",displayNumber:485,sortKey:485},{id:"java-486",number:486,title:"Auditing timestamps",question:"Auditing timestamps",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Spring Data auditing populates creation and modification timestamps when auditing is enabled. Use an auditable base class or annotations and ensure the entity is managed through Spring Data.\n\n\n```java\n@CreatedDate Instant createdAt;\n@LastModifiedDate Instant updatedAt;\n```\n```\n",tech:"java",displayNumber:486,sortKey:486},{id:"java-487",number:487,title:"AuditorAware",question:"AuditorAware",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`AuditorAware supplies the current principal for @CreatedBy and @LastModifiedBy. Return empty for system jobs or unauthenticated operations rather than inventing a user.


\`\`\`java
class CurrentAuditor implements AuditorAware<String> {
    public Optional<String> getCurrentAuditor() {
        return Optional.of("system");
    }
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:487,sortKey:487},{id:"java-488",number:488,title:"Enable JPA auditing",question:"Enable JPA auditing",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"@EnableJpaAuditing activates entity listeners that fill audit fields. Register AuditorAware when actor identity is required.\n\n\n```java\n@Configuration @EnableJpaAuditing\nclass AuditConfig {}\n```\n```\n",tech:"java",displayNumber:488,sortKey:488},{id:"java-489",number:489,title:"Auditing entity listener",question:"Auditing entity listener",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`AuditingEntityListener connects lifecycle callbacks to Spring Data's auditing handler. Add it to entities or a mapped superclass, and do not confuse it with database triggers.


\`\`\`java
@EntityListeners(AuditingEntityListener.class)
@MappedSuperclass class Audited {
    @CreatedDate Instant created;
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:489,sortKey:489},{id:"java-490",number:490,title:"Authentication versus authorization",question:"Authentication versus authorization",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Authentication establishes who the caller is; authorization decides what that principal may do. Spring Security stores the authenticated principal in SecurityContext for downstream checks.\n\n\n```java\nSecurityContextHolder.getContext().getAuthentication().getName();\n```\n```\n",tech:"java",displayNumber:490,sortKey:490},{id:"java-491",number:491,title:"Security filter chain",question:"Security filter chain",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`The filter chain intercepts requests before controllers, extracts credentials, establishes security context, applies CSRF/session rules, and enforces authorization. Order matters.


\`\`\`java
@Bean SecurityFilterChain api(HttpSecurity http) throws Exception {
    return http.authorizeHttpRequests(a -> a
        .requestMatchers("/public/**").permitAll()
        .anyRequest().authenticated()).build();
}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:491,sortKey:491},{id:"java-492",number:492,title:"Password encoding",question:"Password encoding",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Passwords must be stored as slow, salted one-way hashes, never plaintext or reversible encryption. DelegatingPasswordEncoder supports an algorithm id and future migration.\n\n\n```java\nPasswordEncoder encoder = PasswordEncoderFactories.createDelegatingPasswordEncoder();\nString hash = encoder.encode(rawPassword);\n```\n```\n",tech:"java",displayNumber:492,sortKey:492},{id:"java-493",number:493,title:"Method security",question:"Method security",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Method security enforces authorization at service methods, including calls not originating from HTTP. Enable it and express rules with roles or authorities.


\`\`\`java
@EnableMethodSecurity
@PreAuthorize("hasRole('ADMIN')")
void deleteUser(long id) {}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:493,sortKey:493},{id:"java-494",number:494,title:"CSRF and stateless APIs",question:"CSRF and stateless APIs",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"CSRF protects browser sessions because browsers attach cookies automatically. A stateless bearer-token API commonly disables CSRF only when it does not authenticate through cookies.\n\n\n```java\nhttp.csrf(csrf -> csrf.disable())\n    .oauth2ResourceServer(o -> o.jwt());\n```\n```\n",tech:"java",displayNumber:494,sortKey:494},{id:"java-495",number:495,title:"JWT structure",question:"JWT structure",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'A JWT has base64url header, claims payload, and signature separated by dots. The signature authenticates integrity; claims such as exp and iss still require validation.\n\n\n```java\nString[] parts = token.split("\\\\.");\nassert parts.length == 3; // header.payload.signature\n```\n```\n',tech:"java",displayNumber:495,sortKey:495},{id:"java-496",number:496,title:"JWT validation",question:"JWT validation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Validate signature, issuer, audience, expiration, and required scopes using a trusted key source. Never trust decoded payload claims before signature verification.\n\n\n```java\nJwtDecoder decoder = JwtDecoders.fromIssuerLocation("https://issuer.example");\n```\n```\n',tech:"java",displayNumber:496,sortKey:496},{id:"java-497",number:497,title:"OAuth2 resource server",question:"OAuth2 resource server",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"A resource server accepts access tokens issued by an authorization server and maps validated scopes/claims to authorities. It does not issue tokens.\n\n\n```java\nhttp.oauth2ResourceServer(o -> o\n    .jwt(jwt -> jwt.jwtAuthenticationConverter(converter)));\n```\n```\n",tech:"java",displayNumber:497,sortKey:497},{id:"java-498",number:498,title:"OAuth2 scopes and authorities",question:"OAuth2 scopes and authorities",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Scopes represent delegated permissions and are commonly mapped to SCOPE_x authorities. Keep endpoint requirements least-privilege and distinguish roles from scopes.


\`\`\`java
@PreAuthorize("hasAuthority('SCOPE_orders.read')")
List<Order> orders() {}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:498,sortKey:498},{id:"java-499",number:499,title:"JWT method security",question:"JWT method security",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Combine request and method checks so defense in depth protects service calls. Use a stable claim-to-authority converter and test missing, expired, and insufficient scopes.


\`\`\`java
@PreAuthorize("#owner == authentication.name or hasAuthority('SCOPE_admin')")
Order read(String owner) {}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:499,sortKey:499},{id:"java-500",number:500,title:"Spring Security test support",question:"Spring Security test support",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Spring Security test annotations like @WithMockUser and @WithUserDetails simulate authentication in integration tests without a full login flow. Use them to verify authorization logic.


\`\`\`java
@Test @WithMockUser(roles="ADMIN")
void adminCanDelete() { ... }

@Test @WithMockUser(username="alice")
void userSeesOwnData() { ... }
\`\`\`
\`\`\`
`,tech:"java",displayNumber:500,sortKey:500},{id:"java-501",number:1,title:"Find the sum of all numbers greater than 10 using stream",question:"Find the sum of all numbers greater than 10 using stream",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `filter` to keep elements matching the predicate, then collect (or `mapToInt().sum()` for an actual sum). `filter` keeps only the elements where the lambda returns true.\n\n\n```java\nList<Integer> numbers = List.of(10, 11, 12, 15);\nList<Integer> list = numbers.stream()\n        .filter(i -> i > 10)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 11 12 15\n```\n```\n",tech:"java",displayNumber:1,sortKey:501},{id:"java-502",number:2,title:"Convert List<Integer> to List<String>",question:"Convert List<Integer> to List<String>",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `map` with `String::valueOf` to transform each element, then collect. `map` applies a function to every element and returns a new stream of results.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nList<String> list = numbers.stream()\n        .map(String::valueOf)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 2 3 4 5\n```\n```\n",tech:"java",displayNumber:2,sortKey:502},{id:"java-503",number:3,title:"Find all even numbers using Streams",question:"Find all even numbers using Streams",difficulty:"Basic",category:"Java Streams & Lambdas",answer:`Filter elements where \`i % 2 == 0\`. The modulo predicate keeps only even values.


\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);
List<Integer> list = numbers.stream()
        .filter(i -> i % 2 == 0)
        .collect(Collectors.toList());
list.forEach(System.out::println);
// Output: 2 4
\`\`\`
\`\`\`
`,tech:"java",displayNumber:3,sortKey:503},{id:"java-504",number:4,title:"Find all odd numbers using stream",question:"Find all odd numbers using stream",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Filter with `(i & 1) == 1` (bitwise AND) or `i % 2 != 0`. The lowest bit is 1 for odd numbers.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nList<Integer> list = numbers.stream()\n        .filter(i -> (i & 1) == 1)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 3 5\n```\n```\n",tech:"java",displayNumber:4,sortKey:504},{id:"java-505",number:5,title:"Find duplicate elements",question:"Find duplicate elements",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Use a `HashSet`'s `add` return value: `add` returns false when the element already exists, so `!seen.add(i)` is true only for duplicates.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nSet<Integer> seen = new HashSet<>();\nSet<Integer> list = numbers.stream()\n        .filter(i -> !seen.add(i))\n        .collect(Collectors.toSet());\nlist.forEach(System.out::println);\n// Output: 3\n```\n```\n",tech:"java",displayNumber:5,sortKey:505},{id:"java-506",number:6,title:"Find unique elements while preserving order",question:"Find unique elements while preserving order",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"`distinct()` removes duplicates while preserving encounter order. A `HashSet` with `seen.add(i)` also works but a Set doesn't preserve order — prefer `distinct()` into a List.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nList<Integer> list = numbers.stream()\n        .distinct()\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 2 3 5\n```\n```\n",tech:"java",displayNumber:6,sortKey:506},{id:"java-507",number:7,title:"Find the second-highest number",question:"Find the second-highest number",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Remove duplicates, sort in reverse order, `skip(1)` to drop the highest, then take `findFirst`. Returns an `Optional` in case the stream is too small.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> res = numbers.stream()\n        .distinct()\n        .sorted(Comparator.reverseOrder())\n        .skip(1)\n        .findFirst();\nSystem.out.println(res.get());\n// Output: 3\n```\n```\n",tech:"java",displayNumber:7,sortKey:507},{id:"java-508",number:8,title:"Find the second lowest number",question:"Find the second lowest number",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`Same idea as second-highest but sort ascending (natural order), skip the lowest, then \`findFirst\`.


\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 3, 5);
Optional<Integer> res = numbers.stream()
        .distinct()
        .sorted()
        .skip(1)
        .findFirst();
System.out.println(res.get());
// Output: 2
\`\`\`
\`\`\`
`,tech:"java",displayNumber:8,sortKey:508},{id:"java-509",number:9,title:"Find maximum and minimum",question:"Find maximum and minimum",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `max`/`min` with a comparator (returns `Optional`), or `mapToInt` to an `IntStream` and call `.max().orElseThrow()` for a primitive result.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> maximum = numbers.stream().max(Integer::compareTo);\nOptional<Integer> minimum = numbers.stream().min(Integer::compareTo);\nSystem.out.println(maximum.get()); // 5\nSystem.out.println(minimum.get()); // 1\n\nint max = numbers.stream().mapToInt(i -> i).max().orElseThrow();\nSystem.out.println(max); // 5\n```\n```\n",tech:"java",displayNumber:9,sortKey:509},{id:"java-510",number:10,title:"Count numbers greater than a value",question:"Count numbers greater than a value",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Filter by the condition, then `count()` returns the number of matching elements as a `long`.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nlong count = numbers.stream()\n        .filter(i -> i > 2)\n        .count();\nSystem.out.println(count); // 3\n```\n```\n",tech:"java",displayNumber:10,sortKey:510},{id:"java-511",number:11,title:"Find first element satisfying a condition",question:"Find first element satisfying a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`filter` then `findFirst` returns an `Optional` with the first matching element in encounter order (short-circuits once found).\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> res = numbers.stream()\n        .filter(i -> i > 2)\n        .findFirst();\nSystem.out.println(res.get()); // 3\n```\n```\n",tech:"java",displayNumber:11,sortKey:511},{id:"java-512",number:12,title:"Check whether all elements satisfy a condition",question:"Check whether all elements satisfy a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`allMatch(predicate)` returns true if every element matches (short-circuits on the first mismatch). For an empty stream it returns true.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().allMatch(n -> n > 0);\nSystem.out.println(res); // true\n```\n```\n",tech:"java",displayNumber:12,sortKey:512},{id:"java-513",number:13,title:"Check whether any element satisfies a condition",question:"Check whether any element satisfies a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`anyMatch(predicate)` returns true if at least one element matches, short-circuiting on the first success.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().anyMatch(n -> n % 2 == 0);\nSystem.out.println(res); // true\n```\n```\n",tech:"java",displayNumber:13,sortKey:513},{id:"java-514",number:14,title:"Check whether no element satisfies a condition",question:"Check whether no element satisfies a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`noneMatch(predicate)` returns true if no element matches. Useful for validating that a collection contains no invalid values.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().noneMatch(n -> n < 0);\nSystem.out.println(res); // true\n```\n```\n",tech:"java",displayNumber:14,sortKey:514},{id:"java-515",number:15,title:"Convert strings to uppercase",question:"Convert strings to uppercase",difficulty:"Basic",category:"Java Streams & Lambdas",answer:`Map each string with the \`String::toUpperCase\` method reference and collect into a list.


\`\`\`java
List<String> numbers = List.of("a", "b", "c");
List<String> res = numbers.stream()
        .map(String::toUpperCase)
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: A B C
\`\`\`
\`\`\`
`,tech:"java",displayNumber:15,sortKey:515},{id:"java-516",number:16,title:"Sort strings by length",question:"Sort strings by length",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Use `sorted` with `Comparator.comparingInt(String::length)` to order elements by their length ascending.\n\n\n```java\nList<String> numbers = List.of("aA", "bBBB", "cA", "M");\nList<String> res = numbers.stream()\n        .sorted(Comparator.comparingInt(String::length))\n        .collect(Collectors.toList());\nres.forEach(System.out::println);\n// Output: M aA cA bBBB\n```\n```\n',tech:"java",displayNumber:16,sortKey:516},{id:"java-517",number:17,title:"Sort strings by length in descending order",question:"Sort strings by length in descending order",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`Chain \`.reversed()\` onto the comparator to flip the ordering to descending.


\`\`\`java
List<String> numbers = List.of("aA", "bBBB", "cA", "m");
List<String> res = numbers.stream()
        .sorted(Comparator.comparingInt(String::length).reversed())
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: bBBB cA aA m
\`\`\`
\`\`\`
`,tech:"java",displayNumber:17,sortKey:517},{id:"java-518",number:18,title:"Flatten List<List<Integer>>",question:"Flatten List<List<Integer>>",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`\`flatMap(List::stream)\` turns each inner list into a stream and concatenates them into one flat stream of elements.


\`\`\`java
List<List<Integer>> numbers = Arrays.asList(
        Arrays.asList(1, 2),
        Arrays.asList(3, 4),
        Arrays.asList(5, 6));
List<Integer> res = numbers.stream()
        .flatMap(List::stream)
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: 1 2 3 4 5 6
\`\`\`
\`\`\`
`,tech:"java",displayNumber:18,sortKey:518},{id:"java-519",number:19,title:"Find unique words from sentences",question:"Find unique words from sentences",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`flatMap` each sentence into a stream of words (split on whitespace), then collect into a `Set` to deduplicate.\n\n\n```java\nList<String> sentences = Arrays.asList("Java is powerful", "Java is popular");\nSet<String> res = sentences.stream()\n        .flatMap(sentence -> Arrays.stream(sentence.split("\\\\s+")))\n        .collect(Collectors.toSet());\nres.forEach(System.out::println);\n// Output: Java, is, powerful, popular\n```\n```\n',tech:"java",displayNumber:19,sortKey:519},{id:"java-520",number:20,title:"Character frequency using stream",question:"Character frequency using stream",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`chars()` gives an `IntStream` of code points; box to `Character`, then group by identity counting occurrences with `Collectors.groupingBy` + `Collectors.counting`.\n\n\n```java\nString word = "kaushik";\nMap<Character, Long> freq = word.chars()\n        .mapToObj(c -> (char) c)\n        .collect(Collectors.groupingBy(\n                Function.identity(), Collectors.counting()));\nfreq.forEach((ch, count) -> System.out.println(ch + " : " + count));\n```\n```\n',tech:"java",displayNumber:20,sortKey:520},{id:"java-521",number:21,title:"Word frequency across sentences",question:"Word frequency across sentences",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'Split each sentence into words with `flatMap`, then `groupingBy(identity, counting)` builds a word→count map.\n\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nMap<String, Long> freq = sentences.stream()\n        .flatMap(s -> Arrays.stream(s.split("\\\\s+")))\n        .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));\nfreq.forEach((word, count) -> System.out.println(word + " : " + count));\n// Output: java:3 is:3 good:1 powerful:1 easy:1\n```\n```\n',tech:"java",displayNumber:21,sortKey:521},{id:"java-522",number:22,title:"Find the longest string",question:"Find the longest string",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Use `max` with `Comparator.comparingInt(String::length)` to get the element with the greatest length.\n\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nOptional<String> longest = sentences.stream()\n        .max(Comparator.comparingInt(String::length));\nSystem.out.println(longest.get()); // java is powerful\n```\n```\n',tech:"java",displayNumber:22,sortKey:522},{id:"java-523",number:23,title:"Find the shortest string",question:"Find the shortest string",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Same comparator but use `min` to get the element with the smallest length.\n\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nOptional<String> shortest = sentences.stream()\n        .min(Comparator.comparingInt(String::length));\nSystem.out.println(shortest.get()); // java is good\n```\n```\n',tech:"java",displayNumber:23,sortKey:523},{id:"java-524",number:24,title:"Sort employees by salary descending",question:"Sort employees by salary descending",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Use `sorted` with `Comparator.comparingDouble(Employee::getSalary).reversed()` to order objects by a numeric field, highest first.\n\n\n```java\nclass Employee { String name; String department; double salary; }\n\nList<Employee> result = employees.stream()\n        .sorted(Comparator.comparingDouble(Employee::getSalary).reversed())\n        .collect(Collectors.toList());\n```\n```\n",tech:"java",displayNumber:24,sortKey:524},{id:"java-525",number:25,title:"Group, aggregate and transform employees with collectors",question:"Group, aggregate and transform employees with collectors",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`\`Collectors\` power common aggregations: \`toMap\` builds id→name, \`groupingBy\` buckets by department, and downstream collectors (\`averagingDouble\`, \`summingDouble\`, \`counting\`) compute per-group stats. Combine \`groupingBy\` with a downstream collector for one-pass aggregation.


\`\`\`java
// id -> name
Map<Integer, String> idToName = employees.stream()
        .collect(Collectors.toMap(Employee::getId, Employee::getName));

// department -> employees
Map<String, List<Employee>> byDept = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment));

// department -> average salary
Map<String, Double> avgSalary = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment,
                Collectors.averagingDouble(Employee::getSalary)));

// department -> total salary
Map<String, Double> sumSalary = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment,
                Collectors.summingDouble(Employee::getSalary)));

// department -> headcount
Map<String, Long> deptCount = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment, Collectors.counting()));

// names appearing more than once
Map<String, Long> nameFreq = employees.stream()
        .collect(Collectors.groupingBy(Employee::getName, Collectors.counting()));
nameFreq.entrySet().stream()
        .filter(e -> e.getValue() > 1)
        .forEach(System.out::println);
\`\`\`
\`\`\`
`,tech:"java",displayNumber:25,sortKey:525},{id:"java-526",number:26,title:"Partition numbers into even and odd",question:"Partition numbers into even and odd",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.partitioningBy(predicate)` splits a stream into a `Map<Boolean, List<T>>` with exactly two keys (`true`/`false`). It's more efficient than `groupingBy` when there are only two buckets.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5, 6);\nMap<Boolean, List<Integer>> parts = numbers.stream()\n        .collect(Collectors.partitioningBy(n -> n % 2 == 0));\nSystem.out.println(parts.get(true));  // [2, 4, 6]\nSystem.out.println(parts.get(false)); // [1, 3, 5]\n```\n```\n",tech:"java",displayNumber:26,sortKey:526},{id:"java-527",number:27,title:"Sum a numeric field with reduce",question:"Sum a numeric field with reduce",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`\`reduce(identity, accumulator)\` folds the stream into a single value. The identity is the start/empty value; the accumulator combines the running result with each element.


\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);
int sum = numbers.stream().reduce(0, Integer::sum);
System.out.println(sum); // 15

// product via reduce
int product = numbers.stream().reduce(1, (a, b) -> a * b);
System.out.println(product); // 120
\`\`\`
\`\`\`
`,tech:"java",displayNumber:27,sortKey:527},{id:"java-528",number:28,title:"Join strings with a delimiter, prefix and suffix",question:"Join strings with a delimiter, prefix and suffix",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'`Collectors.joining(delimiter, prefix, suffix)` concatenates the stream of strings into one string with separators and optional wrapping.\n\n\n```java\nList<String> names = List.of("Rahul", "Amit", "Priya");\nString csv = names.stream()\n        .collect(Collectors.joining(", ", "[", "]"));\nSystem.out.println(csv); // [Rahul, Amit, Priya]\n```\n```\n',tech:"java",displayNumber:28,sortKey:528},{id:"java-529",number:29,title:"Get statistics (count, sum, min, max, avg) in one pass",question:"Get statistics (count, sum, min, max, avg) in one pass",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.summarizingInt/Double` (or `IntStream.summaryStatistics()`) computes count, sum, min, max, and average in a single traversal, returning an `IntSummaryStatistics`.\n\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nIntSummaryStatistics stats = numbers.stream()\n        .collect(Collectors.summarizingInt(i -> i));\nSystem.out.println(stats.getSum());     // 15\nSystem.out.println(stats.getAverage()); // 3.0\nSystem.out.println(stats.getMax());     // 5\n```\n```\n",tech:"java",displayNumber:29,sortKey:529},{id:"java-530",number:30,title:"Build a Map handling duplicate keys (merge function)",question:"Build a Map handling duplicate keys (merge function)",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`Collectors.toMap` throws `IllegalStateException` on duplicate keys unless you supply a merge function to resolve collisions (e.g. keep the first, sum values).\n\n\n```java\nList<String> words = List.of("apple", "banana", "avocado", "cherry");\nMap<Character, String> byFirst = words.stream()\n        .collect(Collectors.toMap(\n                w -> w.charAt(0),\n                w -> w,\n                (existing, replacement) -> existing)); // keep first on clash\nSystem.out.println(byFirst); // {a=apple, b=banana, c=cherry}\n```\n```\n',tech:"java",displayNumber:30,sortKey:530},{id:"java-531",number:31,title:"Group by a field and map to another (downstream mapping)",question:"Group by a field and map to another (downstream mapping)",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`groupingBy(classifier, Collectors.mapping(mapper, toList()))` groups then transforms each group's elements, e.g. department → list of employee names.\n\n\n```java\nMap<String, List<String>> deptNames = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.mapping(Employee::getName, Collectors.toList())));\nSystem.out.println(deptNames);\n```\n```\n",tech:"java",displayNumber:31,sortKey:531},{id:"java-532",number:32,title:"Find the highest-paid employee per department",question:"Find the highest-paid employee per department",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'Use `groupingBy` with the downstream `Collectors.maxBy(comparator)`, which yields an `Optional` per group. Wrap with `collectingAndThen` to unwrap the Optional if desired.\n\n\n```java\nMap<String, Optional<Employee>> topPaid = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.maxBy(Comparator.comparingDouble(Employee::getSalary))));\ntopPaid.forEach((dept, emp) ->\n        System.out.println(dept + " -> " + emp.map(Employee::getName).orElse("none")));\n```\n```\n',tech:"java",displayNumber:32,sortKey:532},{id:"java-533",number:33,title:"Two-level (nested) grouping",question:"Two-level (nested) grouping",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:"Nest `groupingBy` inside `groupingBy` to bucket by two keys, producing a `Map<K1, Map<K2, List<T>>>` — e.g. department then gender.\n\n\n```java\nMap<String, Map<String, List<Employee>>> byDeptThenGender = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.groupingBy(Employee::getGender)));\nSystem.out.println(byDeptThenGender);\n```\n```\n",tech:"java",displayNumber:33,sortKey:533},{id:"java-534",number:34,title:"Count occurrences and sort the result by frequency",question:"Count occurrences and sort the result by frequency",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`Group-count into a map, then stream its entries, sort by value descending, and collect back into an ordered \`LinkedHashMap\` to preserve the sorted order.


\`\`\`java
List<String> items = List.of("a", "b", "a", "c", "b", "a");
Map<String, Long> sorted = items.stream()
        .collect(Collectors.groupingBy(s -> s, Collectors.counting()))
        .entrySet().stream()
        .sorted(Map.Entry.<String, Long>comparingByValue().reversed())
        .collect(Collectors.toMap(
                Map.Entry::getKey, Map.Entry::getValue,
                (a, b) -> a, LinkedHashMap::new));
System.out.println(sorted); // {a=3, b=2, c=1}
\`\`\`
\`\`\`
`,tech:"java",displayNumber:34,sortKey:534},{id:"java-535",number:35,title:"Multi-field sorting with thenComparing",question:"Multi-field sorting with thenComparing",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"Chain `Comparator.comparing(...).thenComparing(...)` to sort by a primary key, breaking ties with a secondary key — e.g. by department, then salary descending.\n\n\n```java\nList<Employee> sorted = employees.stream()\n        .sorted(Comparator.comparing(Employee::getDepartment)\n                .thenComparing(Comparator.comparingDouble(Employee::getSalary).reversed()))\n        .collect(Collectors.toList());\nsorted.forEach(System.out::println);\n```\n```\n",tech:"java",displayNumber:35,sortKey:535},{id:"java-536",number:36,title:"flatMap over map entries to invert a multimap",question:"flatMap over map entries to invert a multimap",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'Stream `entrySet()`, `flatMap` each value list into (value, key) pairs, then collect — a common technique to invert a `Map<K, List<V>>` into `Map<V, K>`.\n\n\n```java\nMap<String, List<Integer>> deptIds = Map.of(\n        "HR", List.of(1, 2), "IT", List.of(3, 4));\nMap<Integer, String> idToDept = deptIds.entrySet().stream()\n        .flatMap(e -> e.getValue().stream()\n                .map(id -> Map.entry(id, e.getKey())))\n        .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue));\nSystem.out.println(idToDept); // {1=HR, 2=HR, 3=IT, 4=IT}\n```\n```\n',tech:"java",displayNumber:36,sortKey:536},{id:"java-537",number:37,title:"takeWhile and dropWhile",question:"takeWhile and dropWhile",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`takeWhile` keeps a prefix while the predicate holds (stops at the first failure); `dropWhile` discards that prefix and keeps the rest. Both are for ordered streams (Java 9+).\n\n\n```java\nList<Integer> numbers = List.of(2, 4, 6, 7, 8, 10);\nSystem.out.println(numbers.stream().takeWhile(n -> n % 2 == 0).toList()); // [2, 4, 6]\nSystem.out.println(numbers.stream().dropWhile(n -> n % 2 == 0).toList()); // [7, 8, 10]\n```\n```\n",tech:"java",displayNumber:37,sortKey:537},{id:"java-538",number:38,title:"Generate an infinite stream with iterate and limit",question:"Generate an infinite stream with iterate and limit",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Stream.iterate(seed, next)` produces an infinite sequence; bound it with `limit`. The 3-arg `iterate(seed, hasNext, next)` (Java 9+) adds a built-in stop condition.\n\n\n```java\n// first 5 powers of 2\nList<Integer> powers = Stream.iterate(1, n -> n * 2)\n        .limit(5)\n        .toList();\nSystem.out.println(powers); // [1, 2, 4, 8, 16]\n\n// iterate with predicate (Java 9+)\nStream.iterate(1, n -> n <= 20, n -> n + 5).forEach(System.out::println); // 1 6 11 16\n```\n```\n",tech:"java",displayNumber:38,sortKey:538},{id:"java-539",number:39,title:"Three-argument reduce for parallel-safe accumulation",question:"Three-argument reduce for parallel-safe accumulation",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'`reduce(identity, accumulator, combiner)` supports a different result type and parallel execution: the accumulator folds elements into partial results, the combiner merges partials. The combiner must be associative and consistent with the accumulator.\n\n\n```java\nList<String> words = List.of("java", "stream", "api");\nint totalChars = words.stream()\n        .reduce(0, (sum, w) -> sum + w.length(), Integer::sum);\nSystem.out.println(totalChars); // 13\n```\n```\n',tech:"java",displayNumber:39,sortKey:539},{id:"java-540",number:40,title:"Write a custom Collector",question:"Write a custom Collector",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:`\`Collector.of(supplier, accumulator, combiner, finisher)\` builds a bespoke reduction. The supplier creates the mutable container, accumulator adds elements, combiner merges containers (parallel), finisher produces the final result.


\`\`\`java
Collector<Integer, ?, String> toRange = Collector.of(
        () -> new int[]{Integer.MAX_VALUE, Integer.MIN_VALUE},
        (acc, i) -> { acc[0] = Math.min(acc[0], i); acc[1] = Math.max(acc[1], i); },
        (a, b) -> new int[]{Math.min(a[0], b[0]), Math.max(a[1], b[1])},
        acc -> acc[0] + ".." + acc[1]);
String range = List.of(4, 1, 9, 3).stream().collect(toRange);
System.out.println(range); // 1..9
\`\`\`
\`\`\`
`,tech:"java",displayNumber:40,sortKey:540},{id:"java-541",number:41,title:"collectingAndThen to produce an immutable result",question:"collectingAndThen to produce an immutable result",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.collectingAndThen(downstream, finisher)` post-processes a collector's result — e.g. wrap the collected list in `Collections.unmodifiableList` for an immutable view.\n\n\n```java\nList<Integer> immutable = List.of(1, 2, 3, 4).stream()\n        .filter(n -> n % 2 == 0)\n        .collect(Collectors.collectingAndThen(\n                Collectors.toList(), Collections::unmodifiableList));\nSystem.out.println(immutable); // [2, 4]\n```\n```\n",tech:"java",displayNumber:41,sortKey:541},{id:"java-542",number:42,title:"Parallel stream and when it helps (or hurts)",question:"Parallel stream and when it helps (or hurts)",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:"`parallelStream()` splits work across the common ForkJoinPool for CPU-bound, large, easily-splittable data with stateless, associative operations. For small data, ordered/stateful ops, or I/O it often hurts due to overhead and contention. Never mutate shared state inside it.\n\n\n```java\nlong count = LongStream.rangeClosed(1, 10_000_000)\n        .parallel()\n        .filter(n -> n % 2 == 0)\n        .count();\nSystem.out.println(count); // 5000000\n```\n```\n",tech:"java",displayNumber:42,sortKey:542},{id:"java-543",number:43,title:"Why peek should not be used for logic",question:"Why peek should not be used for logic",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'`peek` is intended for debugging. It may be skipped entirely when the pipeline can compute results without traversing (e.g. `count()` on a sized stream), and its ordering/execution isn\'t guaranteed under parallelism — so never rely on it for side-effecting logic.\n\n\n```java\nlong c = Stream.of(1, 2, 3)\n        .peek(x -> System.out.println("peeked " + x)) // may NOT print in some JDKs\n        .count();\nSystem.out.println(c); // 3\n```\n```\n',tech:"java",displayNumber:43,sortKey:543},{id:"java-544",number:44,title:"Collectors.teeing to compute two aggregates at once",question:"Collectors.teeing to compute two aggregates at once",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:`\`Collectors.teeing(c1, c2, merger)\` (Java 12+) runs two collectors over the same stream in one pass and merges their results — e.g. compute sum and count together to derive an average.


\`\`\`java
double avg = Stream.of(2, 4, 6, 8)
        .collect(Collectors.teeing(
                Collectors.summingInt(i -> i),
                Collectors.counting(),
                (sum, cnt) -> (double) sum / cnt));
System.out.println(avg); // 5.0
\`\`\`
\`\`\`
`,tech:"java",displayNumber:44,sortKey:544},{id:"java-545",number:45,title:"Safely chain nullable lookups with Optional",question:"Safely chain nullable lookups with Optional",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`Optional.map`/`flatMap`/`orElseGet` chain transformations that short-circuit on absence, avoiding nested null checks. `filter` can also drop values not matching a condition.\n\n\n```java\nOptional<Employee> emp = employees.stream()\n        .filter(e -> e.getDepartment().equals("Finance"))\n        .max(Comparator.comparingDouble(Employee::getSalary));\n\nString name = emp.map(Employee::getName)\n        .filter(n -> !n.isBlank())\n        .orElse("No employee");\nSystem.out.println(name);\n```\n```\n',tech:"java",displayNumber:45,sortKey:545},{id:"java-546",number:1,title:"What are the major features introduced in Java 8?",question:"What are the major features introduced in Java 8?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Lambda expressions, functional interfaces, the Stream API, method references, default and static methods in interfaces, the `Optional` class, a new Date/Time API (`java.time`), and `CompletableFuture`. Together they brought functional-style programming to Java.\n\n\n```java\nList.of(1, 2, 3).stream().map(n -> n * 2).forEach(System.out::println);\n```\n```\n",tech:"java",displayNumber:1,sortKey:546},{id:"java-547",number:2,title:"Why was Java 8 considered a major Java release?",question:"Why was Java 8 considered a major Java release?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"It introduced a functional programming paradigm alongside OOP, enabling concise, declarative code with lambdas and streams. It also modernized long-standing pain points (dates, nulls, parallelism), making it the most impactful release since generics.",tech:"java",displayNumber:2,sortKey:547},{id:"java-548",number:3,title:"What is a functional interface?",question:"What is a functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"An interface with exactly one abstract method (SAM — Single Abstract Method). It can be the target type of a lambda expression or method reference. It may also have default/static methods.\n\n\n```java\n@FunctionalInterface\ninterface Calculator { int apply(int a, int b); }\nCalculator add = (a, b) -> a + b;\n```\n```\n",tech:"java",displayNumber:3,sortKey:548},{id:"java-549",number:4,title:"What is the `@FunctionalInterface` annotation?",question:"What is the `@FunctionalInterface` annotation?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A marker annotation that tells the compiler to enforce the single-abstract-method rule. It's optional but recommended: if someone adds a second abstract method, compilation fails.\n\n\n```java\n@FunctionalInterface\ninterface Greeting { void say(String name); }\n```\n```\n",tech:"java",displayNumber:4,sortKey:549},{id:"java-550",number:5,title:"Can a functional interface have multiple default methods?",question:"Can a functional interface have multiple default methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. A functional interface can have any number of default methods; only the count of abstract methods is restricted to one.\n\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); default int twice(int x){ return apply(apply(x)); } default int id(int x){ return x; } }\n```\n```\n",tech:"java",displayNumber:5,sortKey:550},{id:"java-551",number:6,title:"Can a functional interface have static methods?",question:"Can a functional interface have static methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. Static methods don't count as abstract methods, so a functional interface can declare any number of them (e.g. factory helpers like `Function.identity()`).\n\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); static Fn identity(){ return x -> x; } }\n```\n```\n",tech:"java",displayNumber:6,sortKey:551},{id:"java-552",number:7,title:"Can a functional interface extend another interface?",question:"Can a functional interface extend another interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes, as long as the combined set still has exactly one abstract method. If the parent already declares the single abstract method and the child adds none, it remains functional.\n\n\n```java\ninterface A { int run(); }\n@FunctionalInterface\ninterface B extends A {} // still one abstract method\n```\n```\n",tech:"java",displayNumber:7,sortKey:552},{id:"java-553",number:8,title:"What is a lambda expression?",question:"What is a lambda expression?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A lambda is an anonymous function — a concise way to implement a functional interface\'s single method inline, without a named class. It captures behavior as data you can pass around.\n\n\n```java\nRunnable r = () -> System.out.println("running");\n```\n```\n',tech:"java",displayNumber:8,sortKey:553},{id:"java-554",number:9,title:"Why were lambda expressions introduced?",question:"Why were lambda expressions introduced?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"To reduce boilerplate of anonymous inner classes, enable passing behavior as arguments (higher-order functions), and support the Stream API's declarative, functional style. They make code shorter and more readable.",tech:"java",displayNumber:9,sortKey:554},{id:"java-555",number:10,title:"What is the syntax of a lambda expression?",question:"What is the syntax of a lambda expression?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'`(parameters) -> expression` or `(parameters) -> { statements; }`. Parameter types are usually inferred; single parameters need no parentheses; a single expression needs no braces or `return`.\n\n\n```java\nx -> x * x                     // one param, expression\n(a, b) -> a + b                // two params\n() -> System.out.println("hi") // no params\n(int x) -> { return x + 1; }   // explicit type + block\n```\n```\n',tech:"java",displayNumber:10,sortKey:555},{id:"java-556",number:11,title:"What is a method reference?",question:"What is a method reference?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A shorthand for a lambda that just calls an existing method. `System.out::println` is equivalent to `x -> System.out.println(x)`. It uses the `::` operator.\n\n\n```java\nList.of("a", "b").forEach(System.out::println);\n```\n```\n',tech:"java",displayNumber:11,sortKey:556},{id:"java-557",number:12,title:"What are the different types of method references?",question:"What are the different types of method references?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Four kinds: static (`ClassName::staticMethod`), bound instance (`object::method`), unbound instance (`ClassName::instanceMethod`), and constructor (`ClassName::new`).\n\n\n```java\nInteger::parseInt        // static\nSystem.out::println      // bound instance\nString::toUpperCase      // unbound instance\nArrayList::new           // constructor\n```\n```\n",tech:"java",displayNumber:12,sortKey:557},{id:"java-558",number:13,title:"Lambda vs anonymous inner class?",question:"Lambda vs anonymous inner class?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A lambda is more concise, has no separate `.class` file, and `this` refers to the enclosing instance. An anonymous class creates its own scope, can implement multiple methods/have state, and its `this` refers to the anonymous instance.\n\n\n```java\nRunnable a = () -> System.out.println(this);       // enclosing 'this'\nRunnable b = new Runnable(){ public void run(){ System.out.println(this); } }; // its own 'this'\n```\n```\n",tech:"java",displayNumber:13,sortKey:558},{id:"java-559",number:14,title:"What is a closure in Java?",question:"What is a closure in Java?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:`A closure is a function that captures variables from its enclosing scope. Java lambdas are closures: they can reference effectively-final local variables, "closing over" their values.


\`\`\`java
int factor = 3;
Function<Integer,Integer> multiply = n -> n * factor; // captures 'factor'
\`\`\`
\`\`\`
`,tech:"java",displayNumber:14,sortKey:559},{id:"java-560",number:15,title:"What is effectively final?",question:"What is effectively final?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A local variable that is never reassigned after initialization, even without the `final` keyword. Lambdas and anonymous classes may only capture effectively-final locals.\n\n\n```java\nint x = 10;            // never reassigned -> effectively final\nRunnable r = () -> System.out.println(x);\n```\n```\n",tech:"java",displayNumber:15,sortKey:560},{id:"java-561",number:16,title:"Why can a lambda access only final/effectively-final local variables?",question:"Why can a lambda access only final/effectively-final local variables?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:'Captured locals are copied into the lambda; the local variable lives on the stack and may be gone when the lambda runs later. Requiring immutability avoids the ambiguity of which value/copy is "current" and prevents subtle concurrency bugs.',tech:"java",displayNumber:16,sortKey:561},{id:"java-562",number:17,title:"What is a target type?",question:"What is a target type?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'The expected functional-interface type of a lambda in a given context (assignment, method argument, return). The compiler infers the lambda\'s type from this target, since a lambda has no type on its own.\n\n\n```java\nRunnable r = () -> {};          // target type: Runnable\nCallable<String> c = () -> "x"; // same-shaped lambda, different target\n```\n```\n',tech:"java",displayNumber:17,sortKey:562},{id:"java-563",number:18,title:"Can a lambda exist without a functional interface?",question:"Can a lambda exist without a functional interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. A lambda has no intrinsic type; it must be assigned to (or inferred as) a functional interface. Without a target functional type, it won't compile.",tech:"java",displayNumber:18,sortKey:563},{id:"java-564",number:19,title:"What is a default method?",question:"What is a default method?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A method in an interface with a body, declared with the `default` keyword. Implementing classes inherit it without overriding, enabling interfaces to gain behavior.\n\n\n```java\ninterface Vehicle { default void start(){ System.out.println("starting"); } }\n```\n```\n',tech:"java",displayNumber:19,sortKey:564},{id:"java-565",number:20,title:"Why were default methods introduced?",question:"Why were default methods introduced?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"To allow evolving interfaces without breaking existing implementations. Adding `default` methods (e.g. `List.forEach`, `Collection.stream`) let the JDK enhance interfaces while keeping backward compatibility.",tech:"java",displayNumber:20,sortKey:565},{id:"java-566",number:21,title:"What is a static method inside an interface?",question:"What is a static method inside an interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A method with a body belonging to the interface itself (not instances), called via the interface name. Useful for utility/factory methods related to the interface.\n\n\n```java\ninterface Util { static int square(int x){ return x * x; } }\nint r = Util.square(5);\n```\n```\n",tech:"java",displayNumber:21,sortKey:566},{id:"java-567",number:22,title:"Can an interface have both default and static methods?",question:"Can an interface have both default and static methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. An interface can mix abstract, default, and static methods. `Function`, for example, has an abstract `apply`, default `andThen`/`compose`, and static `identity`.",tech:"java",displayNumber:22,sortKey:567},{id:"java-568",number:23,title:"Can a default method be overridden?",question:"Can a default method be overridden?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Yes. An implementing class or sub-interface can override a default method to provide its own behavior; the override wins over the inherited default.\n\n\n```java\nclass Car implements Vehicle { public void start(){ System.out.println("car starts"); } }\n```\n```\n',tech:"java",displayNumber:23,sortKey:568},{id:"java-569",number:24,title:"Can a static interface method be overridden?",question:"Can a static interface method be overridden?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. Static methods are not inherited or polymorphic; they belong to the interface. A class can declare its own static method with the same name, but that's hiding, not overriding.",tech:"java",displayNumber:24,sortKey:569},{id:"java-570",number:25,title:"What happens when two interfaces contain the same default method?",question:"What happens when two interfaces contain the same default method?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:'The "diamond problem": the class won\'t compile until it overrides the method. It can delegate to a specific one via `InterfaceName.super.method()`.\n\n\n```java\nclass C implements A, B {\n    public void hello(){ A.super.hello(); } // resolve ambiguity explicitly\n}\n```\n```\n',tech:"java",displayNumber:25,sortKey:570},{id:"java-571",number:26,title:"What is `Predicate<T>`?",question:"What is `Predicate<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A functional interface representing a boolean-valued condition: `boolean test(T t)`. Widely used in `filter`.\n\n\n```java\nPredicate<Integer> isEven = n -> n % 2 == 0;\nSystem.out.println(isEven.test(4)); // true\n```\n```\n",tech:"java",displayNumber:26,sortKey:571},{id:"java-572",number:27,title:"What is `Function<T,R>`?",question:"What is `Function<T,R>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'Represents a transformation from an input of type T to a result of type R: `R apply(T t)`. Used in `map`.\n\n\n```java\nFunction<String,Integer> length = String::length;\nSystem.out.println(length.apply("java")); // 4\n```\n```\n',tech:"java",displayNumber:27,sortKey:572},{id:"java-573",number:28,title:"What is `Consumer<T>`?",question:"What is `Consumer<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'Represents an operation that accepts one input and returns nothing (side effect only): `void accept(T t)`. Used in `forEach`.\n\n\n```java\nConsumer<String> print = System.out::println;\nprint.accept("hi");\n```\n```\n',tech:"java",displayNumber:28,sortKey:573},{id:"java-574",number:29,title:"What is `Supplier<T>`?",question:"What is `Supplier<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Represents a source of values, taking no input and returning a result: `T get()`. Used for lazy generation/defaults.\n\n\n```java\nSupplier<Double> random = Math::random;\nSystem.out.println(random.get());\n```\n```\n",tech:"java",displayNumber:29,sortKey:574},{id:"java-575",number:30,title:"Difference between Predicate and Function?",question:"Difference between Predicate and Function?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"`Predicate<T>` always returns a `boolean` (`test`), used for conditions. `Function<T,R>` returns any type R (`apply`), used for transformations. A Predicate is essentially a `Function<T, Boolean>` specialized for filtering.",tech:"java",displayNumber:30,sortKey:575},{id:"java-576",number:31,title:"Difference between Consumer and Supplier?",question:"Difference between Consumer and Supplier?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"`Consumer<T>` takes input and returns nothing (consumes/uses a value). `Supplier<T>` takes nothing and produces a value. They are opposites: one is a sink, the other a source.",tech:"java",displayNumber:31,sortKey:576},{id:"java-577",number:32,title:"What is `UnaryOperator<T>`?",question:"What is `UnaryOperator<T>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'A specialization of `Function<T,T>` where input and output are the same type. Used in operations like `List.replaceAll`.\n\n\n```java\nUnaryOperator<String> upper = String::toUpperCase;\nSystem.out.println(upper.apply("java")); // JAVA\n```\n```\n',tech:"java",displayNumber:32,sortKey:577},{id:"java-578",number:33,title:"What is `BinaryOperator<T>`?",question:"What is `BinaryOperator<T>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A specialization of `BiFunction<T,T,T>` where both inputs and the result share one type. Used in `reduce`.\n\n\n```java\nBinaryOperator<Integer> sum = Integer::sum;\nSystem.out.println(sum.apply(3, 4)); // 7\n```\n```\n",tech:"java",displayNumber:33,sortKey:578},{id:"java-579",number:34,title:"What is `BiFunction<T,U,R>`?",question:"What is `BiFunction<T,U,R>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Represents a function taking two inputs (types T and U) and producing a result R: `R apply(T t, U u)`.\n\n\n```java\nBiFunction<Integer,Integer,String> combine = (a, b) -> (a + b) + "";\nSystem.out.println(combine.apply(2, 3)); // "5"\n```\n```\n',tech:"java",displayNumber:34,sortKey:579},{id:"java-580",number:35,title:"What is `BiPredicate<T,U>`?",question:"What is `BiPredicate<T,U>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'A predicate of two arguments: `boolean test(T t, U u)`. Useful for conditions involving two values.\n\n\n```java\nBiPredicate<String,Integer> longerThan = (s, n) -> s.length() > n;\nSystem.out.println(longerThan.test("java", 2)); // true\n```\n```\n',tech:"java",displayNumber:35,sortKey:580},{id:"java-581",number:36,title:"What is `BiConsumer<T,U>`?",question:"What is `BiConsumer<T,U>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Accepts two inputs and returns nothing: `void accept(T t, U u)`. Used by `Map.forEach` to consume key-value pairs.\n\n\n```java\nBiConsumer<String,Integer> print = (k, v) -> System.out.println(k + "=" + v);\nMap.of("a", 1).forEach(print);\n```\n```\n',tech:"java",displayNumber:36,sortKey:581},{id:"java-582",number:37,title:"Difference between `Function` and `UnaryOperator`?",question:"Difference between `Function` and `UnaryOperator`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`Function<T,R>` maps T to a possibly different type R. `UnaryOperator<T>` extends `Function<T,T>`, constraining input and output to the same type — a convenience for same-type transformations.",tech:"java",displayNumber:37,sortKey:582},{id:"java-583",number:38,title:"Difference between `BiFunction` and `BinaryOperator`?",question:"Difference between `BiFunction` and `BinaryOperator`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`BiFunction<T,U,R>` takes two inputs of any types and returns any type. `BinaryOperator<T>` extends `BiFunction<T,T,T>` — both inputs and the output are the same type, used for combining like values (e.g. reduce).",tech:"java",displayNumber:38,sortKey:583},{id:"java-584",number:39,title:"How does `Predicate.and()` work?",question:"How does `Predicate.and()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`and` returns a composed predicate that is true only if both predicates are true, short-circuiting (the second isn't evaluated if the first is false).\n\n\n```java\nPredicate<Integer> positive = n -> n > 0;\nPredicate<Integer> even = n -> n % 2 == 0;\nSystem.out.println(positive.and(even).test(4)); // true\n```\n```\n",tech:"java",displayNumber:39,sortKey:584},{id:"java-585",number:40,title:"How does `Predicate.or()` work?",question:"How does `Predicate.or()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`or` returns a composed predicate that is true if either predicate is true, short-circuiting when the first is true.\n\n\n```java\nPredicate<Integer> neg = n -> n < 0;\nPredicate<Integer> big = n -> n > 100;\nSystem.out.println(neg.or(big).test(150)); // true\n```\n```\n",tech:"java",displayNumber:40,sortKey:585},{id:"java-586",number:41,title:"How does `Predicate.negate()` work?",question:"How does `Predicate.negate()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`negate` returns a predicate representing the logical inverse of the original.\n\n\n```java\nPredicate<Integer> even = n -> n % 2 == 0;\nSystem.out.println(even.negate().test(3)); // true (3 is odd)\n```\n```\n",tech:"java",displayNumber:41,sortKey:586},{id:"java-587",number:42,title:"What are primitive functional interfaces?",question:"What are primitive functional interfaces?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Specialized functional interfaces for primitive types (`IntPredicate`, `IntFunction`, `IntUnaryOperator`, `ToIntFunction`, `IntConsumer`, `IntSupplier`, etc.) that avoid autoboxing overhead by working directly with `int`/`long`/`double`.\n\n\n```java\nIntPredicate isPositive = n -> n > 0; // no Integer boxing\n```\n```\n",tech:"java",displayNumber:42,sortKey:587},{id:"java-588",number:43,title:"Why do `IntPredicate`, `IntFunction`, `IntConsumer`, etc. exist?",question:"Why do `IntPredicate`, `IntFunction`, `IntConsumer`, etc. exist?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"To eliminate the performance cost of autoboxing/unboxing between primitives and wrapper objects in hot code paths (like large streams), reducing memory churn and improving speed.",tech:"java",displayNumber:43,sortKey:588},{id:"java-589",number:44,title:"How do you create your own functional interface?",question:"How do you create your own functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Declare an interface with a single abstract method and (optionally) annotate it `@FunctionalInterface`. Then it can be targeted by lambdas.\n\n\n```java\n@FunctionalInterface\ninterface TriFunction<A,B,C,R> { R apply(A a, B b, C c); }\nTriFunction<Integer,Integer,Integer,Integer> sum3 = (a, b, c) -> a + b + c;\n```\n```\n",tech:"java",displayNumber:44,sortKey:589},{id:"java-590",number:45,title:"Can a functional interface have methods inherited from Object?",question:"Can a functional interface have methods inherited from Object?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Yes. Abstract declarations that override `Object`'s public methods (`equals`, `hashCode`, `toString`) don't count toward the single-abstract-method rule, so the interface stays functional.\n\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); boolean equals(Object o); } // still functional\n```\n```\n",tech:"java",displayNumber:45,sortKey:590},{id:"java-591",number:46,title:"Why is `Comparator` considered a functional interface?",question:"Why is `Comparator` considered a functional interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"It has a single abstract method `int compare(T a, T b)` (its other methods are default/static), so it can be implemented with a lambda.\n\n\n```java\nComparator<String> byLen = (a, b) -> a.length() - b.length();\n```\n```\n",tech:"java",displayNumber:46,sortKey:591},{id:"java-592",number:47,title:"Why is `Runnable` a functional interface?",question:"Why is `Runnable` a functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'It declares one abstract method `void run()` and no others, making it a valid target for a lambda or method reference.\n\n\n```java\nRunnable r = () -> System.out.println("task");\nnew Thread(r).start();\n```\n```\n',tech:"java",displayNumber:47,sortKey:592},{id:"java-593",number:48,title:"Write a lambda to sort employees by salary.",question:"Write a lambda to sort employees by salary.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Pass a `Comparator` lambda (or `Comparator.comparing`) to `sort`. Prefer `comparingDouble` for numeric fields to avoid boxing.\n\n\n```java\nemployees.sort(Comparator.comparingDouble(Employee::getSalary));\n// or: employees.sort((a, b) -> Double.compare(a.getSalary(), b.getSalary()));\n```\n```\n",tech:"java",displayNumber:48,sortKey:593},{id:"java-594",number:49,title:"Write a lambda to filter employees by age.",question:"Write a lambda to filter employees by age.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Use a `Predicate` lambda in `stream().filter(...)`.\n\n\n```java\nList<Employee> adults = employees.stream()\n        .filter(e -> e.getAge() >= 18)\n        .collect(Collectors.toList());\n```\n```\n",tech:"java",displayNumber:49,sortKey:594},{id:"java-595",number:50,title:"Write a lambda to calculate the square of a number.",question:"Write a lambda to calculate the square of a number.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `Function<Integer,Integer>` (or `IntUnaryOperator`) that returns `n * n`.\n\n\n```java\nFunction<Integer,Integer> square = n -> n * n;\nSystem.out.println(square.apply(5)); // 25\n```\n```\n",tech:"java",displayNumber:50,sortKey:595},{id:"java-596",number:51,title:"Write a lambda to check whether a number is even.",question:"Write a lambda to check whether a number is even.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `Predicate<Integer>` returning `n % 2 == 0`.\n\n\n```java\nPredicate<Integer> isEven = n -> n % 2 == 0;\nSystem.out.println(isEven.test(6)); // true\n```\n```\n",tech:"java",displayNumber:51,sortKey:596},{id:"java-597",number:52,title:"Write a lambda to find the maximum of two numbers.",question:"Write a lambda to find the maximum of two numbers.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `BinaryOperator<Integer>` using `Math.max`.\n\n\n```java\nBinaryOperator<Integer> max = Math::max;\nSystem.out.println(max.apply(3, 9)); // 9\n```\n```\n",tech:"java",displayNumber:52,sortKey:597},{id:"java-598",number:53,title:"Write a lambda that accepts two Strings and concatenates them.",question:"Write a lambda that accepts two Strings and concatenates them.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A `BinaryOperator<String>` or `BiFunction<String,String,String>` that joins the two inputs.\n\n\n```java\nBinaryOperator<String> concat = (a, b) -> a + b;\nSystem.out.println(concat.apply("Java", "8")); // Java8\n```\n```\n',tech:"java",displayNumber:53,sortKey:598},{id:"java-599",number:54,title:"Can lambda parameters have explicit types?",question:"Can lambda parameters have explicit types?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. You may declare types explicitly `(int x, int y) -> x + y`, or use `var` (Java 11+). But you must be consistent — either all parameters are typed or all rely on inference.\n\n\n```java\nBinaryOperator<Integer> add = (Integer a, Integer b) -> a + b;\n```\n```\n",tech:"java",displayNumber:54,sortKey:599},{id:"java-600",number:55,title:"Can lambda have multiple statements?",question:"Can lambda have multiple statements?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes, using a block body with braces and an explicit `return` (if it returns a value).\n\n\n```java\nFunction<Integer,Integer> f = n -> { int r = n * 2; return r + 1; };\n```\n```\n",tech:"java",displayNumber:55,sortKey:600},{id:"java-601",number:56,title:"Can lambda throw checked exceptions?",question:"Can lambda throw checked exceptions?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Only if the target functional interface's method declares that checked exception. Standard interfaces like `Function` don't declare any, so lambdas targeting them can't throw checked exceptions directly.\n\n\n```java\nCallable<String> c = () -> readFile(); // Callable declares 'throws Exception' -> OK\n```\n```\n",tech:"java",displayNumber:56,sortKey:601},{id:"java-602",number:57,title:"How do you handle checked exceptions inside lambda?",question:"How do you handle checked exceptions inside lambda?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:`Wrap the checked exception in an unchecked one inside a try/catch, or use a custom "throwing" functional interface that declares the exception, or a utility wrapper.


\`\`\`java
Function<String,String> read = path -> {
    try { return Files.readString(Path.of(path)); }
    catch (IOException e) { throw new UncheckedIOException(e); }
};
\`\`\`
\`\`\`
`,tech:"java",displayNumber:57,sortKey:602},{id:"java-603",number:58,title:"What happens when a lambda captures a local variable?",question:"What happens when a lambda captures a local variable?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"The variable's value is copied into the lambda at capture time, and it must be effectively final. The lambda uses that captured snapshot even after the enclosing method returns.\n\n\n```java\nint base = 10;\nSupplier<Integer> s = () -> base; // captures value 10\n```\n```\n",tech:"java",displayNumber:58,sortKey:603},{id:"java-604",number:59,title:"Can a lambda modify an instance variable?",question:"Can a lambda modify an instance variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. Instance (and static) fields are not subject to the effectively-final rule; a lambda can read and mutate them through the enclosing object.\n\n\n```java\nclass Counter { int count; Runnable inc = () -> count++; }\n```\n```\n",tech:"java",displayNumber:59,sortKey:604},{id:"java-605",number:60,title:"Can a lambda modify a static variable?",question:"Can a lambda modify a static variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. Static fields belong to the class, not the stack, so lambdas may read and modify them freely (mind thread-safety).\n\n\n```java\nclass C { static int total; Runnable add = () -> total += 5; }\n```\n```\n",tech:"java",displayNumber:60,sortKey:605},{id:"java-606",number:61,title:"Can a lambda modify a local variable?",question:"Can a lambda modify a local variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. Captured local variables must be effectively final, so a lambda can read but not reassign them. A common workaround is a single-element array or an `AtomicInteger`.\n\n\n```java\nAtomicInteger sum = new AtomicInteger();\nList.of(1, 2, 3).forEach(n -> sum.addAndGet(n)); // mutate via holder\n```\n```\n",tech:"java",displayNumber:61,sortKey:606},{id:"java-607",number:62,title:"Lambda variable scope vs anonymous class variable scope?",question:"Lambda variable scope vs anonymous class variable scope?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"A lambda shares the enclosing method's scope — its variables live in the same scope, so you can't redeclare an enclosing variable name. An anonymous class introduces a new scope and can shadow enclosing names.\n\n\n```java\nint x = 1;\nRunnable r = () -> { /* int x = 2; would be a compile error (same scope) */ };\n```\n```\n",tech:"java",displayNumber:62,sortKey:607},{id:"java-608",number:63,title:"What does `this` refer to inside a lambda?",question:"What does `this` refer to inside a lambda?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"`this` refers to the enclosing instance (the class where the lambda is defined), because a lambda does not create a new scope.\n\n\n```java\nclass Outer { Runnable r = () -> System.out.println(this.getClass()); } // Outer\n```\n```\n",tech:"java",displayNumber:63,sortKey:608},{id:"java-609",number:64,title:"What does `this` refer to inside an anonymous inner class?",question:"What does `this` refer to inside an anonymous inner class?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"`this` refers to the anonymous class instance itself, not the enclosing object. To reach the outer instance you use `Outer.this`.\n\n\n```java\nRunnable r = new Runnable(){ public void run(){ System.out.println(this.getClass()); } }; // the anonymous class\n```\n```\n",tech:"java",displayNumber:64,sortKey:609},{id:"java-610",number:65,title:"What is a method reference? (recap)",question:"What is a method reference? (recap)",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A compact, readable alternative to a lambda that simply invokes an existing method or constructor, written with `::`. The compiler matches the referenced method's signature to the target functional interface.\n\n\n```java\nFunction<String,Integer> len = String::length; // instead of s -> s.length()\n```\n```\n",tech:"java",displayNumber:65,sortKey:610},{id:"java-611",number:66,title:"Explain `ClassName::staticMethod`.",question:"Explain `ClassName::staticMethod`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A reference to a static method. The functional interface's arguments become the static method's arguments.\n\n\n```java\nFunction<String,Integer> parse = Integer::parseInt; // s -> Integer.parseInt(s)\n```\n```\n",tech:"java",displayNumber:66,sortKey:611},{id:"java-612",number:67,title:"Explain `object::instanceMethod`.",question:"Explain `object::instanceMethod`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A bound instance reference: the method is called on a specific, already-known object. The lambda's arguments are passed to that method.\n\n\n```java\nConsumer<String> printer = System.out::println; // s -> System.out.println(s)\n```\n```\n",tech:"java",displayNumber:67,sortKey:612},{id:"java-613",number:68,title:"Explain `ClassName::instanceMethod`.",question:"Explain `ClassName::instanceMethod`.",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"An unbound instance reference: the first argument becomes the receiver on which the method is invoked, and remaining arguments are passed along.\n\n\n```java\nFunction<String,String> up = String::toUpperCase; // s -> s.toUpperCase()\nComparator<String> c = String::compareTo;         // (a, b) -> a.compareTo(b)\n```\n```\n",tech:"java",displayNumber:68,sortKey:613},{id:"java-614",number:69,title:"Explain `ClassName::new`.",question:"Explain `ClassName::new`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A constructor reference: the target interface's method arguments are forwarded to a matching constructor, producing a new instance.\n\n\n```java\nSupplier<List<String>> list = ArrayList::new;   // () -> new ArrayList<>()\nFunction<Integer,int[]> arr = int[]::new;         // n -> new int[n]\n```\n```\n",tech:"java",displayNumber:69,sortKey:614},{id:"java-615",number:70,title:"Convert a lambda into a method reference.",question:"Convert a lambda into a method reference.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:`When a lambda merely forwards its parameters to a single method call, replace it with a method reference for clarity.


\`\`\`java
// lambda
Function<String,Integer> a = s -> Integer.parseInt(s);
// method reference
Function<String,Integer> b = Integer::parseInt;
\`\`\`
\`\`\`
`,tech:"java",displayNumber:70,sortKey:615},{id:"java-616",number:71,title:"When should you prefer method references?",question:"When should you prefer method references?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Prefer them when a lambda does nothing but call an existing method with the same arguments — they\'re shorter and more expressive. Keep a lambda when you transform arguments or add logic.\n\n\n```java\nnames.forEach(System.out::println);          // prefer reference\nnames.forEach(n -> System.out.println("* " + n)); // lambda needed (extra logic)\n```\n```\n',tech:"java",displayNumber:71,sortKey:616},{id:"java-617",number:72,title:"Can constructors be referenced?",question:"Can constructors be referenced?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes, via `ClassName::new`. The compiler selects the constructor whose signature matches the functional interface's method.\n\n\n```java\nFunction<String,StringBuilder> sb = StringBuilder::new; // s -> new StringBuilder(s)\n```\n```\n",tech:"java",displayNumber:72,sortKey:617},{id:"java-618",number:73,title:"What is a constructor reference?",question:"What is a constructor reference?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A method reference of the form `Type::new` that refers to a constructor, letting a factory-style functional interface (`Supplier`, `Function`) create objects without an explicit lambda.\n\n\n```java\nSupplier<Employee> factory = Employee::new;\nEmployee e = factory.get();\n```\n```\n",tech:"java",displayNumber:73,sortKey:618},{id:"java-619",number:74,title:"Difference between a lambda expression and a method reference?",question:"Difference between a lambda expression and a method reference?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Both implement a functional interface. A lambda defines an inline body (and can transform arguments or add logic); a method reference is a shorthand that delegates directly to an existing method/constructor with matching arguments. Every method reference can be written as a lambda, but not every lambda simplifies to a method reference.\n\n\n```java\nFunction<String,Integer> lambda = s -> s.length(); // inline body\nFunction<String,Integer> ref    = String::length;   // delegates to existing method\n```\n```\n",tech:"java",displayNumber:74,sortKey:619},{id:"react-620",number:1,title:"What is React and why was it created?",question:"What is React and why was it created?",difficulty:"Basic",category:"React Basics & JSX",answer:`React is an open-source JavaScript library for building user interfaces, created by Facebook (Meta) and released in 2013. Jordan Walke developed it to address the pain points of building complex, data-driven UIs at scale.

Before React, developers dealt with two major problems: (1) the DOM was slow to manipulate directly, and (2) keeping the UI in sync with application state was error-prone and verbose. React introduced a declarative model where developers describe *what* the UI should look like for any given state, and React handles *how* to update the DOM efficiently.

Key design decisions:
- **Component-based architecture**: UIs built from small, reusable, self-contained pieces.
- **Virtual DOM**: An in-memory representation of the real DOM enabling efficient updates.
- **One-way data flow**: Data flows downward through props, making the data flow predictable.
- **JSX**: A syntax extension letting you write HTML-like code in JavaScript.

React is a view layer library. It does not enforce a full framework structure and can be paired with routing, state management, and other libraries as needed.


\`\`\`jsx
// React is declarative: describe WHAT the UI should look like
function Greeting({ userName }) {
  return <h1>Welcome, {userName}!</h1>;
}

// You never manually call document.getElementById or element.innerHTML
// React figures out the minimal DOM changes needed
function App() {
  const [name, setName] = React.useState('Alice');
  return (
    <div>
      <Greeting userName={name} />
      <button onClick={() => setName('Bob')}>Change Name</button>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:1,sortKey:620},{id:"react-621",number:2,title:"What is the Virtual DOM and how does it work?",question:"What is the Virtual DOM and how does it work?",difficulty:"Basic",category:"React Basics & JSX",answer:`The Virtual DOM (VDOM) is a lightweight JavaScript object representation of the real DOM. When React renders a component, it builds a tree of plain JS objects (React elements) describing the UI. On a state or prop change, React creates a new VDOM tree and compares it to the previous one via **diffing** (reconciliation). It then computes the minimal set of changes and applies only those to the real DOM in a batch.

This matters because direct DOM operations are expensive (they trigger layout, reflow, and repaint). By batching and minimizing real DOM writes, React keeps UIs fast even under frequent updates.

Important nuance: the VDOM is *not* faster than the real DOM per se; the real win is avoiding unnecessary DOM mutations and batching updates intelligently.


\`\`\`jsx
// A "Virtual DOM" node is just a plain JS object
const vdomNode = {
  type: 'div',
  props: { className: 'greeting', children: 'Hello' },
};

// React.createElement produces plain objects (the VDOM)
const element = React.createElement('div', { className: 'greeting' }, 'Hello');
console.log(element);
// { type: 'div', props: { className: 'greeting', children: 'Hello' }, ... }
\`\`\`
\`\`\`
`,tech:"react",displayNumber:2,sortKey:621},{id:"react-622",number:3,title:"What are the advantages of the Virtual DOM?",question:"What are the advantages of the Virtual DOM?",difficulty:"Basic",category:"React Basics & JSX",answer:`The Virtual DOM provides several concrete advantages:
1. **Performance via batching**: React groups multiple state updates and flushes them together, reducing DOM operations.
2. **Minimal updates**: The diffing algorithm only patches changed nodes rather than re-rendering the whole tree.
3. **Declarative programming**: You write what the UI should be; React handles the imperative DOM updates.
4. **Cross-platform rendering**: The same React element tree can render to the DOM (ReactDOM), Native (React Native), or even canvas — the VDOM is platform-agnostic.
5. **Predictability**: Diffing is deterministic, making UI behavior easier to reason about and test.

A common misconception is that VDOM makes every app automatically fast. In reality, unnecessary re-renders, large component trees, and heavy computations still cause slowness; the VDOM only optimizes the DOM-write layer.


\`\`\`jsx
// Multiple state updates are batched in React 18 (automatic batching)
function Counter() {
  const [a, setA] = React.useState(0);
  const [b, setB] = React.useState(0);
  function handleClick() {
    setA(a + 1);
    setB(b + 1); // Only ONE re-render of the DOM, not two
  }
  return <button onClick={handleClick}>{a} {b}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:3,sortKey:622},{id:"react-623",number:4,title:"What is React Fiber and how does it relate to reconciliation?",question:"What is React Fiber and how does it relate to reconciliation?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React Fiber is React's **reconciliation algorithm rewrite** (introduced in React 16). The old "stack reconciler" was synchronous and recursive — once it started, it couldn't be interrupted. This caused dropped frames on complex UIs.

Fiber reimagines reconciliation as **incremental work** that can be paused, resumed, and prioritized. A "fiber" is a JavaScript object representing a unit of work (roughly one component). Each fiber has:
- \`type\`, \`stateNode\`, \`return\`, \`child\`, \`sibling\` (tree links)
- \`alternate\` (the current vs. work-in-progress fiber)
- \`effectTag\` (what DOM mutations to perform)
- \`lanes\` / \`priority\` (when this work should run)

The Fiber architecture enables:
- **Concurrent features** (React 18): \`useTransition\`, \`useDeferredValue\`, Suspense
- **Time-slicing**: Low-priority work yields to high-priority (user input)
- **Error boundaries**: Errors can be caught at any fiber level
- **Return points**: Components can return arrays, strings, or null without wrappers


\`\`\`jsx
// Fiber work loop (simplified conceptual view)
function workLoop(deadline) {
  let shouldYield = false;
  while (nextUnitOfWork && !shouldYield) {
    nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
    shouldYield = deadline.timeRemaining() < 1;
  }
  if (!nextUnitOfWork && finishedWork) {
    commitRoot(); // Apply DOM changes
  }
}

// Fiber priority lanes (conceptual)
const SyncLane = 1;        // High: click, typing
const DefaultLane = 2;     // Normal: data fetching
const IdleLane = 4;        // Low: background analytics
\`\`\`
\`\`\`
`,tech:"react",displayNumber:4,sortKey:623},{id:"react-624",number:5,title:"Explain the reconciliation (diffing) algorithm in React.",question:"Explain the reconciliation (diffing) algorithm in React.",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Reconciliation is the process React uses to update the DOM efficiently. When a component re-renders, React produces a new element tree and reconciles it with the previous one. The diffing algorithm is based on two key assumptions for performance:

1. **Different element types produce different trees**: If the root element type changes (e.g., \`<div>\` to \`<span>\`), React tears down the old subtree entirely and builds a new one. No diffing of children occurs.

2. **Lists use \`key\` props for identity**: Children of the same type are diffed by their \`key\`. React matches elements with the same key across renders to preserve state and DOM nodes; without stable keys it falls back to positional matching, which causes bugs (see Q on keys).

The algorithm is **O(n)** (not O(n³) of a general tree-diff algorithm) because it never compares two trees of different branches — it assumes components at the same level produce similar structure.

Three sub-steps during reconciliation:
- **Element type changed** → unmount old, mount new (state lost).
- **Element type same (DOM)** → keep node, update only changed attributes.
- **Element type same (component)** → update props, re-render children, keep state.


\`\`\`jsx
// Case 1: Different types -> full remount (state of <Counter/> is lost)
function App({ useButton }) {
  return useButton ? <button>Click</button> : <div>Click</div>;
}

// Case 2: Same type -> only attributes update, node reused
function App({ title }) {
  return <h1 className="title">{title}</h1>; // DOM node reused, text updated
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:5,sortKey:624},{id:"react-625",number:6,title:"What is the difference between the Virtual DOM and the Shadow DOM?",question:"What is the difference between the Virtual DOM and the Shadow DOM?",difficulty:"Basic",category:"React Basics & JSX",answer:`These are completely unrelated concepts that happen to share the word "DOM":

| Aspect | Virtual DOM | Shadow DOM |
|--------|-------------|------------|
| Origin | React library concept | Web standard (W3C) |
| Purpose | Optimize UI re-rendering | Encapsulate styles/markup in components |
| Scope | A JS object tree of the UI | A real DOM subtree with style isolation |
| Who uses it | React, Vue, etc. | Native web components, Lit |

The **Shadow DOM** is a browser feature that lets you attach a hidden, encapsulated DOM tree to an element so that CSS and JS don't leak in or out. The **Virtual DOM** is a purely JS-level abstraction with no browser involvement — it never touches rendering isolation.


\`\`\`jsx
// Virtual DOM: a JS object, not real DOM
const vdom = React.createElement('div', null, 'Hi');

// Shadow DOM: a real browser feature (works in plain JS, no React)
const host = document.createElement('div');
const shadow = host.attachShadow({ mode: 'open' });
shadow.innerHTML = '<style>h1 { color: red; }</style><h1>Scoped</h1>';
// Styles here do NOT affect the rest of the page
\`\`\`
\`\`\`
`,tech:"react",displayNumber:6,sortKey:625},{id:"react-626",number:7,title:"What is JSX and why is it used in React?",question:"What is JSX and why is it used in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`JSX (JavaScript XML) is a syntax extension for JavaScript that lets you write HTML-like markup directly inside JS files. It's not required to use React — you could call \`React.createElement\` directly — but it makes component structure far more readable and visually matches the output.

JSX provides:
- **Familiarity**: UI structure looks like HTML.
- **Composition**: Nesting components reads naturally.
- **Expressiveness**: You can embed any JS expression in \`{ }\`.
- **Compile-time checks**: Static analysis tools can catch typos in element names.

JSX is NOT valid JavaScript — it must be transformed (by Babel, SWC, or the TypeScript compiler) into \`React.createElement()\` calls (or the newer automatic JSX runtime) before the browser can run it.


\`\`\`jsx
// JSX - what you write
const element = <h1 className="greeting">Hello, world!</h1>;

// What it compiles to (classic transform):
const element = React.createElement(
  'h1',
  { className: 'greeting' },
  'Hello, world!'
);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:7,sortKey:626},{id:"react-627",number:8,title:"How does JSX get transformed under the hood? (createElement / JSX transform)",question:"How does JSX get transformed under the hood? (createElement / JSX transform)",difficulty:"Intermediate",category:"React Basics & JSX",answer:`JSX is syntax sugar. A compiler (Babel/SWC/TS) transforms it. There are two transforms:

**Classic Transform** (React 16 and earlier default):
\`\`\`jsx
const el = <div className="a">Hi</div>;
// becomes:
const el = React.createElement('div', { className: 'a' }, 'Hi');
\`\`\`
This requires \`React\` in scope.

**Automatic Runtime** (React 17+, the default now): The compiler imports a function from \`react/jsx-runtime\` automatically, so you no longer need to \`import React\` to use JSX.
\`\`\`jsx
// <div className="a">Hi</div>
import { jsx as _jsx } from "react/jsx-runtime";
const el = _jsx("div", { className: "a", children: "Hi" });
\`\`\`

\`React.createElement(type, props, ...children)\` returns a plain object:
\`\`\`
{ $$typeof: Symbol(react.element), type, props: {children, ...}, key, ref }
\`\`\`
This object is a **React element** — the smallest building block of the VDOM.


\`\`\`jsx
// Children can be passed variadically:
React.createElement('ul', null,
  React.createElement('li', null, 'A'),
  React.createElement('li', null, 'B')
);
// JSX equivalent:
// <ul><li>A</li><li>B</li></ul>

// Elements are just objects describing what to render
const el = <span>x</span>;
console.log(el.$$typeof); // Symbol(react.element)
\`\`\`
\`\`\`
`,tech:"react",displayNumber:8,sortKey:627},{id:"react-628",number:9,title:"What is the difference between React.createElement and JSX?",question:"What is the difference between React.createElement and JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:`There is **no functional difference** at runtime — JSX compiles to \`React.createElement\` (or the automatic runtime). JSX is purely a developer-experience improvement. Both produce identical React element objects.

Trade-offs:
- **JSX**: Readable, visual, matches HTML structure, easier to maintain for complex trees. But requires a build step and can hide what's happening.
- **createElement**: Verbose, nested function calls become unreadable, but explicit and needs no special syntax (useful in environments without JSX support, or for programmatic/dynamic element creation).

Modern best practice is almost always JSX, except for very dynamic trees where you build elements in loops/conditionals in plain JS.


\`\`\`jsx
// Equivalent representations

// 1. JSX
const a = (
  <div className="box" onClick={handleClick}>
    <span>Text</span>
  </div>
);

// 2. createElement
const b = React.createElement(
  'div',
  { className: 'box', onClick: handleClick },
  React.createElement('span', null, 'Text')
);
// a and b describe the exact same UI
\`\`\`
\`\`\`
`,tech:"react",displayNumber:9,sortKey:628},{id:"react-629",number:10,title:"What is a React element vs a React component vs an instance?",question:"What is a React element vs a React component vs an instance?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`These three terms are often confused:

- **React Element**: A plain object describing what to render (\`{ type, props, key, ref }\`). It is immutable and is what \`createElement\`/\`JSX\` return. Elements are not components.
- **React Component**: A function or class that, given props, returns a React element (or tree of them). It's a blueprint, not a rendered thing.
- **Component Instance**: The internal Fiber/state object React creates to track a mounted component. For function components, you don't get a direct handle to the instance; for class components, \`this\` refers to it. The DOM node is also a separate concept.

Think: element = instruction, component = function, instance = the running record React keeps.


\`\`\`jsx
// Component (a function/blueprint)
function Button({ label }) {
  return <button>{label}</button>; // returns an element
}

// Element (an instruction object created when you write JSX)
const el = <Button label="Save" />; // equals createElement(Button, {label:'Save'})

// Instance: React internally creates one when <Button/> mounts (no direct handle)
\`\`\`
\`\`\`
`,tech:"react",displayNumber:10,sortKey:629},{id:"react-630",number:11,title:"What are Functional Components and Class Components in React?",question:"What are Functional Components and Class Components in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`React supports two ways to define components:

**Functional Components** (modern, recommended):
- Plain JavaScript functions that accept \`props\` and return JSX.
- Can use Hooks (\`useState\`, \`useEffect\`, etc.) for state and side effects.
- Simpler, easier to test, no \`this\` binding issues.
- Smaller bundle size after minification (no class boilerplate).

**Class Components** (legacy):
- ES6 classes extending \`React.Component\`.
- Have \`this.props\`, \`this.state\`, lifecycle methods (\`componentDidMount\`, etc.).
- \`this\` binding required in constructors or via class fields.
- Still supported but not recommended for new code.

As of React 18, both are officially supported. Function components + Hooks are the present and future.


\`\`\`jsx
// Functional component (modern)
function Greeting({ name }) {
  const [count, setCount] = React.useState(0);
  return <div>Hello {name}, clicked {count} times</div>;
}

// Class component (legacy)
class Greeting extends React.Component {
  state = { count: 0 };
  render() {
    return <div>Hello {this.props.name}, clicked {this.state.count} times</div>;
  }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:11,sortKey:630},{id:"react-631",number:12,title:"What is the difference between Functional and Class Components?",question:"What is the difference between Functional and Class Components?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Beyond syntax, key differences:

| Aspect | Functional | Class |
|--------|-----------|-------|
| State | \`useState\` (multiple) | single \`this.state\` object |
| Side effects | \`useEffect\` | lifecycle (\`componentDidMount\`, etc.) |
| \`this\` | none | required, binding headaches |
| State updates | functional updater \`setX(prev => prev+1)\` | \`this.setState({x: this.state.x+1})\` |
| Performance | No instance allocation overhead | instances created |
| Code reuse | Custom Hooks | HOCs / render props |
| Error handling | Error boundaries still need classes | \`componentDidCatch\` |

Functional components can now do everything class components can (including error boundaries via libraries, though React's built-in error boundary still requires a class). They are the recommended default.


\`\`\`jsx
// Side effect comparison
function Func() {
  React.useEffect(() => {
    const id = setInterval(() => {}, 1000);
    return () => clearInterval(id); // cleanup
  }, []);
  return null;
}

class Cls extends React.Component {
  componentDidMount() { this.id = setInterval(() => {}, 1000); }
  componentWillUnmount() { clearInterval(this.id); }
  render() { return null; }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:12,sortKey:631},{id:"react-632",number:13,title:"Can a functional component fully replace a class component? Are there exceptions?",question:"Can a functional component fully replace a class component? Are there exceptions?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"For 99% of cases, yes. Hooks cover state, side effects, context, refs, memoization, and even `getSnapshotBeforeUpdate`/`componentDidUpdate`-style logic. The one notable exception historically is **Error Boundaries**: React's `componentDidCatch` / `getDerivedStateFromError` can ONLY be implemented in a class component. There is no `useErrorBoundary` hook built into React.\n\nThat said, you typically need just one tiny class-based error boundary in an entire app, and community libraries wrap it for you. All other lifecycle behaviors map cleanly to Hooks:\n- `componentDidMount` → `useEffect(fn, [])`\n- `componentDidUpdate` → `useEffect(fn, [deps])`\n- `shouldComponentUpdate` → `React.memo` / `useMemo`\n- `getDerivedStateFromProps` → `useState` + `useEffect` or derived state directly in render\n\n\n```jsx\n// The one thing still needing a class: Error Boundary\nclass ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  static getDerivedStateFromError() { return { hasError: true }; }\n  componentDidCatch(error, info) { logError(error, info); }\n  render() {\n    return this.state.hasError ? <Fallback /> : this.props.children;\n  }\n}\n// Wrap any tree: <ErrorBoundary><App/></ErrorBoundary>\n```\n```\n",tech:"react",displayNumber:13,sortKey:632},{id:"react-633",number:14,title:"What are Props in React?",question:"What are Props in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`Props (short for "properties") are read-only inputs passed from a parent component to a child. They flow **downward** (one-way data flow). A child can never modify its own props; it can only use them to render. If a child needs to influence the parent's data, the parent passes down a callback function as a prop.

Props can be of any type: strings, numbers, objects, arrays, functions, even other React elements (children). Because props are immutable from the child's perspective, React can optimize rendering and treat components as pure functions of their props.


\`\`\`jsx
// Parent passes props down
function Parent() {
  const user = { name: 'Ada', age: 36 };
  return <Profile user={user} onEdit={() => save(user)} />;
}

// Child receives and uses props (read-only!)
function Profile({ user, onEdit }) {
  return (
    <div>
      <h2>{user.name}</h2>
      <button onClick={onEdit}>Edit</button>
      {/* user.name = 'Bob' would throw or be ignored; never mutate props */}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:14,sortKey:633},{id:"react-634",number:15,title:"What is State in React?",question:"What is State in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`State is a component's private, mutable data that affects what is rendered. When state changes (via a setter from \`useState\`), React re-renders that component (and its children, unless memoized). Unlike props, state is owned and controlled by the component itself.

Key rules:
- Never mutate state directly (\`state.count++\` is wrong). Always use the setter.
- Treat state as immutable; replace it with a new object/array.
- State updates may be asynchronous/batched, so don't rely on the value right after calling the setter.
- State is local: only the component that owns it (and its descendants via props/callbacks) can read or change it.


\`\`\`jsx
function Counter() {
  const [count, setCount] = React.useState(0); // initial state

  function increment() {
    // CORRECT: use the setter (functional form for latest value)
    setCount(prev => prev + 1);

    // WRONG: mutating state directly does not trigger re-render
    // count++; // never do this
  }
  return <button onClick={increment}>{count}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:15,sortKey:634},{id:"react-635",number:16,title:"What is the difference between Props and State?",question:"What is the difference between Props and State?",difficulty:"Basic",category:"React Basics & JSX",answer:`| Aspect | Props | State |
|--------|-------|-------|
| Ownership | Parent owns it; child receives it | Component owns it itself |
| Mutability | Immutable from child's view | Mutable via setter |
| Purpose | Configure/feed data into a component | Track changing data internal to component |
| Trigger re-render | When parent re-renders | When setter called |
| Source of truth | Parent decides | Component decides |

A helpful mental model: **props are like function arguments; state is like a variable declared inside the function**. Both are inputs to render; re-render happens when either changes.

Another nuance: you can "lift state up" so that what was state in a child becomes a prop passed from the parent — this is how sibling components share data.


\`\`\`jsx
// Props: passed in, read-only
function Child({ value }) { return <p>{value}</p>; }

// State: owned, mutable
function Parent() {
  const [value, setValue] = React.useState(0);
  return <Child value={value} />; // state of Parent becomes prop of Child
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:16,sortKey:635},{id:"react-636",number:17,title:"Why is immutability important in React?",question:"Why is immutability important in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React relies on reference equality to detect changes. When you call a state setter, React compares the new state to the old. If you mutate an object/array in place, its reference stays the same, so React may **skip the re-render** because \`prevState === nextState\`.

Immutability guarantees:
1. **Correct change detection**: A new reference always signals a change.
2. **Time-travel debugging**: Libraries like Redux DevTools rely on snapshotting previous states; mutation breaks that.
3. **Predictable re-renders**: Memoized components (\`React.memo\`, \`useMemo\`) only re-render when inputs change by reference.
4. **Concurrency safety**: React 18 concurrent features may read the same state at multiple points; mutation risks tearing.

Rule: always create a new object/array when updating state.


\`\`\`jsx
function TodoList() {
  const [todos, setTodos] = React.useState([{ id: 1, text: 'a' }]);

  // WRONG - mutates, reference unchanged, may not re-render
  function badAdd() {
    todos.push({ id: 2, text: 'b' });
    setTodos(todos);
  }

  // CORRECT - new array reference
  function goodAdd() {
    setTodos(prev => [...prev, { id: 2, text: 'b' }]);
  }

  // CORRECT - new object for nested update
  function updateFirst() {
    setTodos(prev => prev.map((t, i) =>
      i === 0 ? { ...t, text: 'updated' } : t
    ));
  }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:17,sortKey:636},{id:"react-637",number:18,title:"What are Controlled and Uncontrolled Components?",question:"What are Controlled and Uncontrolled Components?",difficulty:"Basic",category:"React Basics & JSX",answer:`**Controlled Components**: Form inputs whose value is driven by React state. The input's \`value\` (or \`checked\`) prop is set from state, and every change updates that state via \`onChange\`. React is the single source of truth.

**Uncontrolled Components**: Form inputs that manage their own state internally via the DOM. You access their value using a \`ref\` when needed (e.g., on submit). The DOM is the source of truth.

Controlled components are preferred for most cases because they enable validation, formatting, disabling, and conditional behavior in React logic. Uncontrolled are simpler for simple forms, file inputs (\`<input type="file">\`), or integrating non-React libraries.


\`\`\`jsx
// Controlled: React owns the value
function Controlled() {
  const [name, setName] = React.useState('');
  return <input value={name} onChange={e => setName(e.target.value)} />;
}

// Uncontrolled: DOM owns the value, read via ref on submit
function Uncontrolled() {
  const inputRef = React.useRef(null);
  function handleSubmit(e) {
    e.preventDefault();
    console.log(inputRef.current.value);
  }
  return <form onSubmit={handleSubmit}><input ref={inputRef} /></form>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:18,sortKey:637},{id:"react-638",number:19,title:"When should you use Controlled vs Uncontrolled components?",question:"When should you use Controlled vs Uncontrolled components?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Use **Controlled** when:
- You need instant validation/feedback as the user types.
- The input value affects other parts of the UI (e.g., a live preview, disabling a submit button).
- You need to enforce formatting (uppercase, mask digits).
- You want a single predictable source of truth and easier testing.

Use **Uncontrolled** when:
- You have a simple form with a few fields and no live cross-field logic.
- You need a \`<input type="file">\` (always uncontrolled — its value can't be set programmatically for security).
- You're integrating a third-party DOM library (e.g., a datepicker) that manages its own input.
- You want to reduce re-renders for very high-frequency input (though debouncing a controlled input is usually better).

A middle ground: \`defaultValue\` / \`defaultChecked\` give uncontrolled inputs an initial value set by React without taking over updates.


\`\`\`jsx
// Hybrid: uncontrolled with a sensible initial value via defaultValue
function Form() {
  return <input defaultValue="starter" />; // DOM controls updates
}

// Controlled with validation
function EmailField() {
  const [email, setEmail] = React.useState('');
  const valid = /^[^@]+@[^@]+$/.test(email);
  return (
    <>
      <input value={email} onChange={e => setEmail(e.target.value)} />
      {email && !valid && <span>Invalid email</span>}
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:19,sortKey:638},{id:"react-639",number:20,title:"Why are keys important in React lists, and why is using the array index dangerous?",question:"Why are keys important in React lists, and why is using the array index dangerous?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Keys help React identify which items in a list have changed, been added, or removed across renders. During reconciliation, React matches children by key rather than by position. With a stable, unique key, React can move/reuse DOM nodes correctly and preserve component state.

Using the **array index as a key is dangerous** when the list is reordered, filtered, or items are inserted/removed. Because the index changes as the list changes, items get mismatched: a component that should keep its state gets a different key, causing:
- Loss of local component state (input values, scroll position).
- Incorrect association between DOM and state.
- Subtle UI bugs (a checkbox checked on item A suddenly appears on item B).

Index keys are acceptable **only** for static lists that never reorder, filter, or add/remove items. Otherwise use a stable unique id.


\`\`\`jsx
const todos = [{ id: 'a', text: 'Buy milk' }, { id: 'b', text: 'Walk dog' }];

// GOOD: stable unique id
todos.map(t => <li key={t.id}>{t.text}</li>);

// BAD: index changes when list mutates
todos.map((t, i) => <li key={i}>{t.text}</li>);
// If you delete index 0, the old index-1 item now has key 0 -> state mismatch
\`\`\`
\`\`\`
`,tech:"react",displayNumber:20,sortKey:639},{id:"react-640",number:21,title:"What happens if you don't provide a key to a list item?",question:"What happens if you don't provide a key to a list item?",difficulty:"Basic",category:"React Basics & JSX",answer:`React will still render the list but will emit a warning: "Each child in a list should have a unique 'key' prop." Without keys, React falls back to positional (index) matching. As explained in Q20, positional matching causes state mismatches when the list changes. The key must be on the **innermost element returned by the map**, not on the \`<div>\` wrapping the loop.

Keys only need to be unique among siblings — they don't need to be globally unique across the whole app. Never use \`Math.random()\` as a key (it changes every render, defeating the purpose).


\`\`\`jsx
// WRONG - warning, and no key on the mapped element
{items.map(item => (
  <div key={item.id}> {/* key here is ignored for the list item itself */}
    {item.name}
  </div>
))}

// CORRECT - key on the element returned by map
{items.map(item => (
  <li key={item.id}>{item.name}</li>
))}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:21,sortKey:640},{id:"react-641",number:22,title:"What is a React Fragment and when should you use it?",question:"What is a React Fragment and when should you use it?",difficulty:"Basic",category:"React Basics & JSX",answer:`A **Fragment** (\`<>...</>\` or \`<React.Fragment>...</React.Fragment>\`) lets a component return multiple sibling elements without wrapping them in an extra DOM node. This keeps the DOM shallow and avoids invalid HTML (e.g., a \`<div>\` inside a \`<table>\` or \`<ul>\`).

Use fragments when:
- You need to return multiple elements from a component.
- A wrapper \`<div>\` would break semantics or CSS.
- You want to avoid unnecessary depth in the DOM tree.

The shorthand \`<>...</>\` is syntactic sugar for \`<React.Fragment>...</React.Fragment>\`. It does not create a DOM node.


\`\`\`jsx
// Without fragment: adds extra <div> to DOM
function Bad() { return <div><td>1</td><td>2</td></div>; }

// With fragment: no extra node
function Good() {
  return (
    <>
      <td>1</td>
      <td>2</td>
    </>
  );
}

// Explicit form needed when you need a key (see Q23)
function GoodWithKey({ items }) {
  return (
    <React.Fragment>
      {items.map(item => <React.Fragment key={item.id}><td>{item.a}</td><td>{item.b}</td></React.Fragment>)}
    </React.Fragment>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:22,sortKey:641},{id:"react-642",number:23,title:"How do you use a Fragment with a key?",question:"How do you use a Fragment with a key?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`The shorthand \`<>...</>\` syntax **cannot** accept a \`key\` prop. To use a key on a fragment — which you need when mapping fragments in a list — you must use the **explicit** \`<React.Fragment key={...}>\` form. This is extremely common in tables where each row must render multiple \`<td>\`s without a wrapping \`<div>\` but still needs a key.

Under the hood, \`React.Fragment\` is just another element type; the reconciler treats it as a no-op DOM node, but it still carries a \`key\` used for list diffing.


\`\`\`jsx
// Shorthand cannot take a key -> this would warn/break
// {items.map(item => <>{item.a}{item.b}</>)} // no key allowed

// Explicit form with key:
function TableBody({ rows }) {
  return (
    <tbody>
      {rows.map(row => (
        <React.Fragment key={row.id}>
          <td>{row.name}</td>
          <td>{row.age}</td>
        </React.Fragment>
      ))}
    </tbody>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:23,sortKey:642},{id:"react-643",number:24,title:"What are the different patterns for conditional rendering in React?",question:"What are the different patterns for conditional rendering in React?",difficulty:"Basic",category:"React Basics & JSX",answer:'Common patterns, from most to least common:\n\n1. **Inline ternary**: `{condition ? <A /> : <B />}`\n2. **Logical &&**: `{condition && <A />}` (renders nothing if falsy)\n3. **Early return**: `if (!condition) return null; return <A />;` (at top of component)\n4. **Switch/ternary chain**: for multiple mutually exclusive states\n5. **Higher-order / wrapper components**: e.g., `<AuthGate><Dashboard/></AuthGate>`\n6. **Render props / children as function**: `<Wrapper>{isLoading ? <Spinner/> : <Content/>}</Wrapper>`\n\nNotes:\n- `&&` can render `0` as `0` if the left side is `0` (a number). Use `condition > 0 && ...` or `Boolean(condition) && ...`.\n- `null`, `undefined`, `false`, `true` render nothing (useful for `&&`).\n- Prefer early returns for complex conditionals — they avoid nested JSX and improve readability.\n\n\n```jsx\nfunction Status({ user }) {\n  if (!user) return <Login />;\n  if (user.isAdmin) return <AdminPanel />;\n  return <Dashboard />;\n}\n\n// Gotcha: 0 renders as "0"\n{count && <span>{count}</span>} // if count=0, renders "0"!\n{count > 0 && <span>{count}</span>} // safe\n```\n```\n',tech:"react",displayNumber:24,sortKey:643},{id:"react-644",number:25,title:"What is the difference between `&&` and ternary conditional rendering?",question:"What is the difference between `&&` and ternary conditional rendering?",difficulty:"Basic",category:"React Basics & JSX",answer:'- **`condition && <X />`**: Renders `<X />` when truthy; when falsy renders the falsy value (`false`, `null`, `undefined` render nothing, but `0` or `\'\'` render literally). Best for "show or hide" with no else branch.\n- **`condition ? <X /> : <Y />`**: Always renders exactly one branch — ideal when you have both an "if" and "else" path (including an explicit `: null`).\n\nChoose `&&` when there is no else. Choose ternary when you need an explicit else (even `: null`). Avoid deeply nested ternaries; extract to a variable or early return for readability.\n\n\n```jsx\n// Ternary: always one branch\n{isLoggedIn ? <LogoutButton /> : <LoginButton />}\n\n// && : only-if, else renders nothing (unless 0/\'\' )\n{showBanner && <Banner />}\n\n// Bad: nested ternary hard to read\n{isLoading ? <Spinner/> : error ? <Error/> : <Data/>}\n// Better: extract or early-return\n```\n```\n',tech:"react",displayNumber:25,sortKey:644},{id:"react-645",number:26,title:"How do you handle multiple conditions (more than two branches) in JSX?",question:"How do you handle multiple conditions (more than two branches) in JSX?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`For more than two branches, several clean approaches exist:

1. **Extract to a variable** (most readable): compute \`const content = ...\` before \`return\`, then render \`{content}\`.
2. **Lookup object/map**: \`const views = { loading: <Spinner/>, error: <Error/>, ready: <Data/> }; return views[state];\`
3. **Early returns** in the component body.
4. **Switch statement** in a helper function that returns JSX.

Avoid chaining multiple ternaries inline; it becomes unreadable and error-prone. The object-map approach is especially clean when branches are independent of order.


\`\`\`jsx
function Panel({ status }) {
  const views = {
    loading: <Spinner />,
    error: <ErrorBox />,
    empty: <EmptyState />,
    ready: <Data />,
  };
  return <section>{views[status] ?? <ErrorBox />}</section>;
}

// Or early returns:
function Panel2({ status }) {
  if (status === 'loading') return <Spinner />;
  if (status === 'error') return <ErrorBox />;
  return <Data />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:26,sortKey:645},{id:"react-646",number:27,title:"How are events handled in React compared to plain HTML/JavaScript?",question:"How are events handled in React compared to plain HTML/JavaScript?",difficulty:"Basic",category:"React Basics & JSX",answer:`React implements a **synthetic event system**. Differences from native DOM events:

1. **Naming**: React uses **camelCase** (\`onClick\`, \`onSubmit\`) vs HTML's lowercase (\`onclick\`).
2. **Handler value**: You pass a **function** as the handler, not a string (\`onClick={handleClick}\` vs \`onclick="handleClick()"\`).
3. **SyntheticEvent**: React wraps native events in a cross-browser \`SyntheticEvent\` object that normalizes behavior across browsers. Events are pooled in older versions (React 16 and earlier pooled objects for performance; React 17+ no longer pools).
4. **Delegation**: React 17+ attaches listeners to the root container, not \`document\`. This makes event handling more predictable and allows multiple React versions on one page.
5. **\`this\` binding**: In class components you must bind handlers (or use class fields / arrow functions).


\`\`\`jsx
// HTML
// <button onclick="handleClick()">Click</button>

// React
function Button() {
  function handleClick(e) {
    e.preventDefault(); // SyntheticEvent, cross-browser
    console.log('clicked');
  }
  return <button onClick={handleClick}>Click</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:27,sortKey:646},{id:"react-647",number:28,title:"What is the SyntheticEvent in React?",question:"What is the SyntheticEvent in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`SyntheticEvent` is React's cross-browser wrapper around the native browser event. It provides a consistent API regardless of browser quirks, exposing standard methods like `preventDefault()`, `stopPropagation()`, `target`, `currentTarget`, `nativeEvent`, and `type`.\n\nKey points:\n- It is a **normalized** object — same interface in all browsers.\n- **Event pooling** (React <17): event objects were reused for performance; you had to call `e.persist()` to access them asynchronously. **React 17+ removed pooling**, so async access works normally.\n- `e.nativeEvent` gives the underlying browser event when you need low-level access.\n- React 17+ attaches root listeners at the React root container, not `document`, fixing issues with event ordering across React trees.\n\n\n```jsx\nfunction Form() {\n  function onSubmit(e) {\n    e.preventDefault();          // works in all browsers\n    const value = e.target.value; // normalized target\n    // e.nativeEvent gives the raw DOM event if needed\n    console.log(e.nativeEvent);\n  }\n  return <form onSubmit={onSubmit}><input /></form>;\n}\n```\n```\n",tech:"react",displayNumber:28,sortKey:647},{id:"react-648",number:29,title:"How does event delegation work in React?",question:"How does event delegation work in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React uses **event delegation**: instead of attaching a listener to every element, it attaches a single listener per event type at the root (in React 17+, the root container; previously \`document\`). When an event fires, it bubbles up to the root, React looks up the internal fiber tree to find which component owns the handler, and dispatches the synthetic event.

Benefits:
- **Performance**: Fewer actual DOM listeners (memory savings).
- **New components**: No need to re-attach listeners when components mount — they just register their handlers with React's internal system.
- **Consistent behavior**: Works uniformly across all components.

This is why \`e.stopPropagation()\` in React doesn't stop native handlers on non-React elements outside the React root, and why \`e.nativeEvent.stopImmediatePropagation()\` is needed for that.


\`\`\`jsx
// One listener on root handles ALL onClick in the tree
// React internally maps: fiber -> handlers
function App() {
  return (
    <div> {/* root container */}
      <Button /> {/* no real DOM listener added here */}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:29,sortKey:648},{id:"react-649",number:30,title:"What is the difference between passing a function vs a string as an event handler?",question:"What is the difference between passing a function vs a string as an event handler?",difficulty:"Basic",category:"React Basics & JSX",answer:"In HTML you pass a **string** of code: `onclick=\"alert('hi')\"`. In JSX you pass a **function reference**: `onClick={handleClick}`. Passing a string in JSX would just set the prop to a string and never be called. Passing a function means React will invoke it with a `SyntheticEvent` when the event fires.\n\nA common mistake: `onClick={handleClick()}` (with parentheses) calls the function **immediately during render** and passes its *return value* as the handler — usually `undefined` or an unwanted result. Always pass the reference: `onClick={handleClick}`. If you need arguments, use an arrow: `onClick={() => handleClick(id)}`.\n\n\n```jsx\nconst handleClick = (id) => console.log(id);\n\n// WRONG: invoked during render\n<button onClick={handleClick(id)}>X</button>\n\n// CORRECT: arrow preserves lazy invocation\n<button onClick={() => handleClick(id)}>X</button>\n\n// CORRECT: no-arg reference\n<button onClick={handleClick}>X</button>\n```\n```\n",tech:"react",displayNumber:30,sortKey:649},{id:"react-650",number:31,title:"How do you pass arguments to event handlers in React?",question:"How do you pass arguments to event handlers in React?",difficulty:"Basic",category:"React Basics & JSX",answer:"You wrap the handler in an arrow function so it is called with both the event and your custom argument: `onClick={(e) => handle(id, e)}`. Two patterns:\n\n1. **Inline arrow** (most common): `onClick={(e) => deleteItem(id, e)}`.\n2. **`bind`**: `onClick={deleteItem.bind(this, id)}` (used historically with class components; arrow is cleaner now).\n\nAvoid defining a new function on every render inside a large list if performance is critical — but for typical UIs the cost is negligible. If profiling shows issues, use a data attribute: read `e.target.dataset.id` inside one stable handler.\n\n\n```jsx\nfunction List({ items, onDelete }) {\n  return items.map(item => (\n    <li key={item.id}>\n      {item.name}\n      <button onClick={(e) => onDelete(item.id, e)}>Delete</button>\n    </li>\n  ));\n}\n```\n```\n",tech:"react",displayNumber:31,sortKey:650},{id:"react-651",number:32,title:"Why does React use camelCase event names like `onClick` instead of `onclick`?",question:"Why does React use camelCase event names like `onClick` instead of `onclick`?",difficulty:"Basic",category:"React Basics & JSX",answer:"React props are JavaScript identifiers, and JavaScript convention (and JSX) uses camelCase. Since JSX is essentially JavaScript, `onClick` must be camelCase to be valid and consistent with the rest of the JS ecosystem (`onMouseEnter`, `onKeyDown`). HTML attributes are case-insensitive and use lowercase (`onclick`), but in JSX you're writing JS expressions, not HTML strings — so the DOM `onclick` lowercase convention doesn't apply. React's event system maps these camelCase props to the correct native event types internally.\n\n\n```jsx\n// Valid JSX (camelCase)\n<button onClick={fn} onMouseEnter={fn} onKeyDown={fn}>X</button>\n\n// INVALID in JSX (this would be treated as unknown prop / not an event)\n// <button onclick={fn}>X</button>\n```\n```\n",tech:"react",displayNumber:32,sortKey:651},{id:"react-652",number:33,title:"What is `React.createElement` signature and return value?",question:"What is `React.createElement` signature and return value?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"Signature: `React.createElement(type, props, ...children)`.\n\n- `type`: a string (`'div'`) for host elements, or a function/class component, or another React element type (Fragment, etc.).\n- `props`: an object of attributes; `null` or `undefined` is allowed (treated as empty). `key` and `ref` are special — extracted out, not in `props`.\n- `children`: any number of child elements, strings, or arrays, flattened into `props.children`.\n\nReturn value: a **React element** object: `{ $$typeof: Symbol.for('react.element'), type, key, ref, props, _owner }`. It is a plain description of the UI — not a rendered instance. React uses it to build the fiber tree during render.\n\n\n```jsx\nconst el = React.createElement('button', { className: 'btn' }, 'Click');\n// = JSX: <button className=\"btn\">Click</button>\n// el = {\n//   $$typeof: Symbol(react.element),\n//   type: 'button',\n//   key: null, ref: null,\n//   props: { className: 'btn', children: 'Click' }\n// }\n```\n```\n",tech:"react",displayNumber:33,sortKey:652},{id:"react-653",number:34,title:"What is `$$typeof` on a React element and why does it exist?",question:"What is `$$typeof` on a React element and why does it exist?",difficulty:"Advanced",category:"React Basics & JSX",answer:`\`$$typeof\` is a property on every React element set to \`Symbol.for('react.element')\`. Its purpose is **security/XSS protection**. 

When React renders, it trusts the structure you give it. If an attacker could inject a plain JSON object that *looks* like a React element (e.g., \`{ type: 'div', props: {...} }\`), React might treat it as a valid element and render it — a classic XSS vector via \`JSON.parse\` from an untrusted API.

Because \`Symbol.for('react.element')\` **cannot be recreated via JSON** (Symbols don't survive serialization), a malicious object from \`JSON.parse\` will never have a valid \`$$typeof\`, and React will throw instead of rendering it. This is a deliberate guard against server-injected fake elements.


\`\`\`jsx
const el = <div>hi</div>;
console.log(el.$$typeof); // Symbol(react.element)
console.log(el.$$typeof === Symbol.for('react.element')); // true

// Attacker JSON cannot forge this:
const fake = JSON.parse('{"type":"script","props":{"dangerouslySetInnerHTML":{"__html":"<script>evil<\/script>"}}}');
// fake.$$typeof === undefined -> React rejects it
\`\`\`
\`\`\`
`,tech:"react",displayNumber:34,sortKey:653},{id:"react-654",number:35,title:"Explain the difference between the classic and automatic JSX runtime.",question:"Explain the difference between the classic and automatic JSX runtime.",difficulty:"Intermediate",category:"React Basics & JSX",answer:'**Classic runtime** (pre-React 17 default): JSX compiles to `React.createElement(...)`, so `React` must be in scope in every file using JSX. This led to the ubiquitous `import React from \'react\'` even when you never referenced `React` directly.\n\n**Automatic runtime** (React 17+ default in modern toolchains): The compiler auto-imports from `react/jsx-runtime` (functions `jsx`, `jsxs`, `Fragment`). You no longer need to import `React` just to use JSX. This reduces bundle size slightly and removes a common source of "React is not defined" errors.\n\nModern tools (Babel `preset-react` with `runtime: \'automatic\'`, Next.js, Vite, Create React App) use automatic by default. The produced elements are functionally identical.\n\n\n```jsx\n// Automatic runtime: NO need to import React\nexport function Hi() {\n  return <h1>Hello</h1>;\n}\n// compiles to: import { jsx } from "react/jsx-runtime";\n//   jsx("h1", { children: "Hello" });\n\n// Classic runtime: requires React in scope\nimport React from \'react\';\nexport function Hi() { return <h1>Hello</h1>; }\n```\n```\n',tech:"react",displayNumber:35,sortKey:654},{id:"react-655",number:36,title:"How does React render a component for the first time (mount)?",question:"How does React render a component for the first time (mount)?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`On mount:
1. **Render phase**: React calls the component function (or \`render()\` for class), producing a tree of React elements. It recursively reconciles these against "nothing" (the empty tree) and builds the fiber tree, marking each fiber with an effect (e.g., \`Placement\`).
2. **Commit phase**: React takes the finished work and applies it to the real DOM in one synchronous pass — inserting new nodes, running \`useLayoutEffect\`/\`componentDidMount\`.
3. **Browser paint**: The browser paints the updated DOM.
4. **Passive effects**: \`useEffect\` callbacks run asynchronously after paint.

The render phase is pure and may be interrupted (concurrent mode); the commit phase is synchronous and not interruptible.


\`\`\`jsx
function App() {
  React.useEffect(() => {
    console.log('passive effect (after paint)');
  }, []);
  React.useLayoutEffect(() => {
    console.log('layout effect (before paint)');
  }, []);
  console.log('render');
  return <h1>Hi</h1>;
}
// Order on mount: render -> layout effect -> paint -> passive effect
\`\`\`
\`\`\`
`,tech:"react",displayNumber:36,sortKey:655},{id:"react-656",number:37,title:"What is the difference between the render phase and the commit phase?",question:"What is the difference between the render phase and the commit phase?",difficulty:"Advanced",category:"React Basics & JSX",answer:`- **Render phase** (reconciliation): React calls component bodies and \`render()\`, diffs the new element tree against the previous, and decides what changes are needed. It is **pure and can be paused, abandoned, or restarted** (especially in concurrent mode). No DOM mutations happen here.
- **Commit phase**: React applies the computed changes to the real DOM (insertions, updates, deletions), then runs layout effects and, after paint, passive effects. It is **synchronous and not interruptible** — once a commit starts, it must finish to keep the UI consistent.

Implication: never put side effects (network calls, subscriptions) in the render phase (component body). Use \`useEffect\`/\`useLayoutEffect\`. Side effects in render can run multiple times due to interruptions.


\`\`\`jsx
function Bad() {
  // SIDE EFFECT IN RENDER - runs twice in StrictMode, may be interrupted
  fetch('/api').then(r => r.json()); // never do this
  return <div />;
}

function Good() {
  React.useEffect(() => {            // side effect in commit phase
    fetch('/api').then(r => r.json());
  }, []);
  return <div />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:37,sortKey:656},{id:"react-657",number:38,title:'What does "React re-renders a component" actually mean?',question:'What does "React re-renders a component" actually mean?',difficulty:"Intermediate",category:"React Basics & JSX",answer:`"Re-render" means React **calls the component function again** (or \`render()\`), producing a new React element tree. It does NOT mean the DOM is updated directly. After the function returns, React reconciles the new tree against the previous one and applies only the *differences* to the real DOM.

Key clarifications:
- Re-render != DOM update. A re-render may produce identical output and result in zero DOM mutations (React bails out where possible, although it still ran the function).
- A component re-renders when its state changes, its parent re-renders (unless memoized), or its context value changes.
- "Render" is cheap; the expensive part is the reconeneration + DOM writes, which React optimizes.


\`\`\`jsx
function Child({ value }) {
  console.log('Child rendered'); // logs on every parent render
  return <p>{value}</p>;
}

function Parent() {
  const [n, setN] = React.useState(0);
  return (
    <div>
      <button onClick={() => setN(n + 1)}>inc</button>
      <Child value="static" /> {/* re-renders too, though value unchanged */}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:38,sortKey:657},{id:"react-658",number:39,title:"Why can't you use `if` statements or loops directly inside JSX?",question:"Why can't you use `if` statements or loops directly inside JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"JSX is an expression that compiles to a function call returning an element. Inside JSX, the `{ }` braces accept a **JavaScript expression** (something that evaluates to a value), not a statement. `if`, `for`, `while` are statements and cannot appear directly inside `{ }`. \n\nTo use conditional logic, use expressions: ternaries, `&&`, or compute a variable beforehand. To repeat, use `.map()` (an expression returning an array) rather than `for`. You *can* use `if`/`for` in the **component body** outside JSX — that's the correct place for statements.\n\n\n```jsx\nfunction List({ items }) {\n  // OK: statements go in the body\n  const rows = [];\n  for (const item of items) {\n    rows.push(<li key={item.id}>{item.text}</li>);\n  }\n\n  // OK inside JSX: .map is an expression\n  return <ul>{items.map(i => <li key={i.id}>{i.text}</li>)}</ul>;\n\n  // WRONG inside JSX: if/for are statements\n  // return <ul>{ for (...) {} }</ul>;\n}\n```\n```\n",tech:"react",displayNumber:39,sortKey:658},{id:"react-659",number:40,title:"What can you put inside `{ }` in JSX?",question:"What can you put inside `{ }` in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"Inside `{ }` you can place **any JavaScript expression** that evaluates to a value:\n- Variables: `{name}`\n- Function calls: `{formatDate(date)}`\n- Ternaries: `{isLoading ? <Spinner /> : <Content />}`\n- `&&` / `||`: `{items.length && <List items={items} />}`\n- Objects/arrays: `{style}` (as a prop), `{children}` (nested)\n- Template literals: `{'Hello ' + name}`\n\nYou CANNOT place statements (`if`, `for`, `const x = 1`), but you can wrap statements in an IIFE if desperate (not recommended). Also `undefined`, `null`, `true`, `false` render nothing — useful for conditional rendering.\n\n\n```jsx\nconst user = { name: 'Ada', roles: ['admin', 'editor'] };\n\nfunction Card() {\n  return (\n    <div className=\"card\">\n      <h2>{user.name}</h2>                    {/* variable */}\n      <p>{user.name.toUpperCase()}</p>        {/* method call */}\n      <p>{`Hello, ${user.name}`}</p>          {/* template literal */}\n      {user.roles.length > 0 && <Badge />}   {/* logical && */}\n      {user.roles.map(r => <span key={r}>{r}</span>)} {/* array from map */}\n    </div>\n  );\n}\n```\n```\n",tech:"react",displayNumber:40,sortKey:659},{id:"react-660",number:41,title:"What are the rules of JSX?",question:"What are the rules of JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:'1. **Single root element**: A component must return exactly one parent element (or Fragment). JSX cannot return multiple siblings without a wrapper.\n2. **Close all tags**: Self-close (`<img />`) or have a closing tag (`<div></div>`). No unclosed tags.\n3. **camelCase for attributes**: `className`, `htmlFor`, `onClick`, `tabIndex`, not `class`, `for`, `onclick`.\n4. **Expressions in `{ }`**: Only JavaScript expressions, no statements.\n5. **Keys for lists**: Each item in a list needs a unique `key` prop on the top-level element.\n6. **HTML entities**: Use Unicode or numbers (`©` or `&copy;`) not `&copy;` directly in text (JSX is not HTML).\n7. **Comments**: Use `{/* comment */}` inside JSX children, not `<!-- -->`.\n\n\n```jsx\nfunction App() {\n  return (\n    <>           // Fragment: single root without extra DOM node\n      <input className="input" htmlFor="name" />\n      <button onClick={fn} tabIndex={0}>Go</button>\n      {items.map(i => <li key={i.id}>{i.name}</li>)}\n    </>\n  );\n}\n```\n```\n',tech:"react",displayNumber:41,sortKey:660},{id:"react-661",number:42,title:"What is the difference between `className` and `class` in JSX?",question:"What is the difference between `className` and `class` in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:'`class` is a reserved keyword in JavaScript (ES6 classes). Since JSX compiles to JavaScript, you cannot use `class` as a prop name. React uses `className` instead, which maps directly to the DOM `class` attribute. Similarly, `for` becomes `htmlFor` (because `for` is a loop keyword). This is a JSX-to-JS compilation constraint, not a React runtime constraint.\n\n\n```jsx\n// WRONG: \'class\' is a reserved word in JS\n// <div class="box">Hi</div>\n\n// CORRECT:\n<div className="box">Hi</div>\n\n// CORRECT for <label>:\n<label htmlFor="input-id">Label</label>\n```\n```\n',tech:"react",displayNumber:42,sortKey:661},{id:"react-662",number:43,title:"What is `children` in React and how does it work?",question:"What is `children` in React and how does it work?",difficulty:"Basic",category:"React Basics & JSX",answer:`\`children\` is a special prop automatically populated with whatever content you place between a component's opening and closing tags. It enables **component composition** — a parent passes arbitrary content to a child component, which decides where to render it.

\`children\` can be a single element, an array of elements, a string, number, or even a function (render props pattern). React provides \`React.Children\` utilities to iterate/manipulate it safely without assuming its type.


\`\`\`jsx
function Card({ children, title }) {
  return (
    <div className="card">
      <h3>{title}</h3>
      <div className="content">{children}</div>
    </div>
  );
}

// Usage: content between tags becomes children
<Card title="Welcome">
  <p>This is the children!</p>
  <button>Click</button>
</Card>

// Inside Card, children = [<p>..., <button>...]
\`\`\`
\`\`\`
`,tech:"react",displayNumber:43,sortKey:662},{id:"react-663",number:44,title:"What is prop drilling and why can it be a problem?",question:"What is prop drilling and why can it be a problem?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Prop drilling means passing data through multiple layers of components just so a deeply nested component can use it. The intermediate components don't need the data; they merely forward it.

Problems:
- Components become tightly coupled to data they don't use.
- Refactoring becomes harder because many component signatures change.
- Reusable components become less reusable because they accept irrelevant props.

Solutions depend on scale:
- For 1-2 levels: props are fine — don't over-engineer.
- For many levels or global-ish data: use Context, state management, or component composition (pass the ready-made child instead of raw props).


\`\`\`jsx
// Prop drilling: Layout and Sidebar don't care about user, but must forward it
function App() { return <Layout user={user} />; }
function Layout({ user }) { return <Sidebar user={user} />; }
function Sidebar({ user }) { return <UserAvatar user={user} />; }

// Composition alternative:
function App() {
  return <Layout sidebar={<UserAvatar user={user} />} />;
}
function Layout({ sidebar }) { return <aside>{sidebar}</aside>; }
\`\`\`
\`\`\`
`,tech:"react",displayNumber:44,sortKey:663},{id:"react-664",number:45,title:"What is lifting state up in React?",question:"What is lifting state up in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`Lifting state up means moving state from a child component to the closest common parent so multiple sibling components can share it. Since React data flows down through props, siblings cannot directly share state. The parent owns the state and passes values/callbacks down.

Use it when two or more components need to read or update the same data. Don't lift state higher than necessary; putting everything at the top causes unnecessary re-renders and makes data flow harder to follow.


\`\`\`jsx
function Parent() {
  const [text, setText] = React.useState('');
  return (
    <>
      <Input value={text} onChange={setText} />
      <Preview value={text} />
    </>
  );
}
function Input({ value, onChange }) {
  return <input value={value} onChange={e => onChange(e.target.value)} />;
}
function Preview({ value }) { return <p>{value}</p>; }
\`\`\`
\`\`\`
`,tech:"react",displayNumber:45,sortKey:664},{id:"react-665",number:46,title:"What is one-way data flow in React?",question:"What is one-way data flow in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`One-way data flow means data moves from parent to child via props, never upward directly. A child can request a change by calling a function prop, but the parent decides whether and how to update state. After the parent state changes, new props flow downward again.

This model makes UIs predictable: for any state, the rendered UI is deterministic. It also makes debugging easier because you can trace data from its source downward through the component tree.


\`\`\`jsx
function Parent() {
  const [count, setCount] = React.useState(0);
  return <Child count={count} onIncrement={() => setCount(c => c + 1)} />;
}
function Child({ count, onIncrement }) {
  return <button onClick={onIncrement}>{count}</button>;
}
// Child does not mutate Parent's state; it asks Parent to update via callback
\`\`\`
\`\`\`
`,tech:"react",displayNumber:46,sortKey:665},{id:"react-666",number:47,title:"What does it mean that React components should be pure?",question:"What does it mean that React components should be pure?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`A pure component returns the same JSX for the same props/state and does not cause side effects during render. Rendering should be a calculation, not an action. This matters because React may render components multiple times, skip commits, or restart renders under Strict Mode and concurrent rendering.

Avoid in render:
- Mutating props/state/global variables
- Network requests
- Subscriptions
- Timers
- DOM manipulation
- Random IDs without stable hooks

Side effects belong in event handlers or effects (\`useEffect\`, \`useLayoutEffect\`).


\`\`\`jsx
// Bad: mutation in render
function Bad({ items }) {
  items.push('extra');
  return <ul>{items.map(x => <li key={x}>{x}</li>)}</ul>;
}

// Good: derive new values without mutating input
function Good({ items }) {
  const displayItems = [...items, 'extra'];
  return <ul>{displayItems.map(x => <li key={x}>{x}</li>)}</ul>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:47,sortKey:666},{id:"react-667",number:48,title:"What is Strict Mode in React?",question:"What is Strict Mode in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`\`React.StrictMode\` is a development-only wrapper that helps detect unsafe patterns. It does not render UI and has no production effect. In React 18, Strict Mode intentionally double-invokes certain functions (component render, state initializer, effect setup/cleanup) in development to expose accidental side effects.

It helps detect:
- Impure rendering
- Missing effect cleanup
- Deprecated APIs
- Unsafe lifecycle methods
- Legacy string refs

If code breaks under Strict Mode, the code likely has side effects in the wrong place or missing cleanup.


\`\`\`jsx
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// In dev, effects may run setup -> cleanup -> setup to verify cleanup logic
\`\`\`
\`\`\`
`,tech:"react",displayNumber:48,sortKey:667},{id:"react-668",number:49,title:"What is the difference between `null`, `false`, `undefined`, and an empty string in JSX rendering?",question:"What is the difference between `null`, `false`, `undefined`, and an empty string in JSX rendering?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"React renders `null`, `undefined`, `true`, and `false` as nothing. Numbers and strings render as text nodes. This is why conditional rendering often returns `null` to show nothing.\n\nGotchas:\n- `0` renders visibly as `0`, so `count && <Badge />` can accidentally render `0`.\n- Empty string `''` technically renders an empty text node, usually invisible.\n- Returning `undefined` from a component is allowed in modern React, but returning `null` is clearer and intentional.\n\n\n```jsx\nfunction Demo({ count }) {\n  return (\n    <div>\n      {false}{null}{undefined}{true} {/* renders nothing */}\n      {0}                            {/* renders 0 */}\n      {count > 0 && <span>{count}</span>} {/* safe */}\n    </div>\n  );\n}\n```\n```\n",tech:"react",displayNumber:49,sortKey:668},{id:"react-669",number:50,title:"How do you render lists in React?",question:"How do you render lists in React?",difficulty:"Basic",category:"React Basics & JSX",answer:"Lists are rendered by transforming an array of data into an array of React elements, usually using `.map()`. Each rendered item must have a stable `key` prop so React can track identity across re-renders.\n\nAvoid mutating the source array in render (`sort`, `reverse`, `splice`) unless you copy it first, because props/state should be immutable. Use `filter`, `map`, and `slice` which return new arrays.\n\n\n```jsx\nfunction TodoList({ todos }) {\n  return (\n    <ul>\n      {todos.map(todo => (\n        <li key={todo.id}>{todo.text}</li>\n      ))}\n    </ul>\n  );\n}\n\n// Sort safely: copy first\nconst sorted = [...todos].sort((a, b) => a.text.localeCompare(b.text));\n```\n```\n",tech:"react",displayNumber:50,sortKey:669},{id:"react-670",number:51,title:"What is the purpose of `key` beyond removing warnings?",question:"What is the purpose of `key` beyond removing warnings?",difficulty:"Advanced",category:"React Basics & JSX",answer:"A key defines a component's identity among siblings. React uses it to decide whether to preserve an existing fiber and its state or unmount it and mount a new one. Therefore changing a key intentionally resets all state in that subtree; keeping a key preserves it. Keys are reconciliation hints, not props delivered to the component.\n\n\n```jsx\nfunction Form({ userId }) {\n  return <UserForm key={userId} userId={userId} />; // reset form when user changes\n}\n```\n```\n",tech:"react",displayNumber:51,sortKey:670},{id:"react-671",number:52,title:"Why does React not pass `key` and `ref` through props?",question:"Why does React not pass `key` and `ref` through props?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`key` and `ref` are special fields consumed by React's reconciler and ref system. They are metadata about an element, not ordinary component input, so React removes them before constructing `props`. Reading `props.key` or `props.ref` produces a warning. If a component needs the value, pass a separately named prop.\n\n\n```jsx\nfunction Row({ id }) { return <div>{id}</div>; }\n<Row key={item.id} id={item.id} />; // key identifies; id is component data\n```\n```\n",tech:"react",displayNumber:52,sortKey:671},{id:"react-672",number:53,title:"How can changing a key reset component state?",question:"How can changing a key reset component state?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"State belongs to a component's position and identity in the rendered tree. A different key gives React a different identity, so it unmounts the old component and mounts a fresh one. This is useful for clearing forms or restarting animations, but accidental key changes lose state and effects.\n\n\n```jsx\nfunction App({ userId }) {\n  return <ChatInput key={userId} />; // fresh draft for each user\n}\n```\n```\n",tech:"react",displayNumber:53,sortKey:672},{id:"react-673",number:54,title:"What is conditional component mounting versus hiding?",question:"What is conditional component mounting versus hiding?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`{open && <Panel />}` unmounts Panel when false: its state is destroyed and effects clean up. Rendering `<Panel hidden={!open} />` keeps it mounted, preserving state and effects while CSS/HTML hides it. Choose unmounting for expensive or inactive content; choose hiding when preserving user input or subscriptions is required.\n\n\n```jsx\n{open ? <Editor /> : null}       // unmounts\n<Editor hidden={!open} />        // remains mounted\n```\n```\n",tech:"react",displayNumber:54,sortKey:673},{id:"react-674",number:55,title:"What is the difference between an element type and an element instance?",question:"What is the difference between an element type and an element instance?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"A type is the component function/class or host tag such as `'div'`; an element is the immutable description created by JSX. React creates and tracks runtime fibers internally when elements mount. You normally work with elements and components, not instances.\n\n\n```jsx\nfunction Button() { return <button>OK</button>; }\nconst element = <Button />; // description\n// Button is the type; React creates its internal mounted fiber\n```\n```\n",tech:"react",displayNumber:55,sortKey:674},{id:"react-675",number:56,title:"Why must component names start with an uppercase letter?",question:"Why must component names start with an uppercase letter?",difficulty:"Basic",category:"React Basics & JSX",answer:"JSX uses capitalization to distinguish user components from intrinsic DOM tags. `<button>` compiles with the string type `'button'`, while `<Button>` compiles with the variable `Button`. Lowercase custom names are treated as unknown HTML tags and won't invoke the component function.\n\n\n```jsx\nfunction Profile() { return <p>Profile</p>; }\n<Profile />; // component\n<profile />; // treated as an intrinsic tag, not Profile\n```\n```\n",tech:"react",displayNumber:56,sortKey:675},{id:"react-676",number:57,title:"How do you spread props in JSX?",question:"How do you spread props in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"`<Component {...obj} />` copies enumerable properties from `obj` into props. Explicit props after the spread override earlier values; therefore spread order matters. Avoid blindly forwarding unknown props to DOM elements because React may warn or create invalid attributes. Destructure and forward only intended values at component boundaries.\n\n\n```jsx\nconst common = { type: 'button', disabled: false };\n<button {...common} disabled={isBusy}>Save</button> // explicit wins\n```\n```\n",tech:"react",displayNumber:57,sortKey:676},{id:"react-677",number:58,title:"What is the difference between `defaultValue` and `value`?",question:"What is the difference between `defaultValue` and `value`?",difficulty:"Basic",category:"React Basics & JSX",answer:'`value` makes an input controlled: React remains the source of truth and must update it through `onChange`. `defaultValue` sets only the initial DOM value; afterward the browser owns it, making the input uncontrolled. Changing `defaultValue` after mount does not update the current value.\n\n\n```jsx\n<input value={name} onChange={e => setName(e.target.value)} />\n<input defaultValue="Initial" ref={inputRef} />\n```\n```\n',tech:"react",displayNumber:58,sortKey:677},{id:"react-678",number:59,title:"Why is a file input always uncontrolled?",question:"Why is a file input always uncontrolled?",difficulty:"Intermediate",category:"React Basics & JSX",answer:'Browsers prohibit scripts from setting a file input\'s selected path for security and privacy. React therefore cannot control its `value`; use a ref and read `input.files` after a change or submit event. You can reset it by changing the DOM value to an empty string or remounting with a new key.\n\n\n```jsx\nfunction Upload() {\n  const ref = React.useRef(null);\n  return <input type="file" ref={ref} onChange={() => console.log(ref.current.files)} />;\n}\n```\n```\n',tech:"react",displayNumber:59,sortKey:678},{id:"react-679",number:60,title:"How does React handle boolean and custom attributes?",question:"How does React handle boolean and custom attributes?",difficulty:"Basic",category:"React Basics & JSX",answer:'Boolean DOM properties use JSX booleans: `<button disabled={true}>`. For a boolean attribute, `disabled` is present when true and omitted when false. Custom data and accessibility attributes retain lowercase hyphenated spelling (`data-id`, `aria-label`). Unknown ordinary attributes are supported in modern React but should represent valid HTML or data.\n\n\n```jsx\n<button disabled={loading} aria-label="Save" data-testid="save">Save</button>\n```\n```\n',tech:"react",displayNumber:60,sortKey:679},{id:"react-680",number:61,title:"What is the difference between `onChange` in React and HTML?",question:"What is the difference between `onChange` in React and HTML?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"React's `onChange` is normalized to provide immediate updates for text inputs as the user types, unlike traditional HTML's change event which historically fires on blur for text fields. For checkboxes, use `e.target.checked`; for text controls, use `e.target.value`. React still exposes a synthetic event with standard cancellation and propagation methods.\n\n\n```jsx\n<input value={text} onChange={e => setText(e.target.value)} />\n<input type=\"checkbox\" checked={ok} onChange={e => setOk(e.target.checked)} />\n```\n```\n",tech:"react",displayNumber:61,sortKey:680},{id:"react-681",number:62,title:"How do `preventDefault` and `stopPropagation` differ?",question:"How do `preventDefault` and `stopPropagation` differ?",difficulty:"Basic",category:"React Basics & JSX",answer:"`preventDefault()` cancels the browser's default action, such as following a link or submitting a form. `stopPropagation()` prevents the event from traveling to ancestor handlers during capture/bubble phases. They solve different problems and can be used together. Neither stops other handlers on the same element in every native scenario; `stopImmediatePropagation` is a lower-level native API.\n\n\n```jsx\n<form onSubmit={e => { e.preventDefault(); save(); }}>\n  <button type=\"submit\">Save</button>\n</form>\n<div onClick={() => console.log('parent')}>\n  <button onClick={e => { e.stopPropagation(); }}>Child</button>\n</div>\n```\n```\n",tech:"react",displayNumber:62,sortKey:681},{id:"react-682",number:63,title:"What are capture and bubble phases in React events?",question:"What are capture and bubble phases in React events?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"An event travels down the tree in the capture phase, reaches its target, then travels upward in the bubble phase. React's normal `onClick` handlers run during bubbling. Appending `Capture` (`onClickCapture`) runs during capture, before the target and bubble handlers. Capture is useful for global logging or policies that must observe events before children stop propagation.\n\n\n```jsx\n<div onClickCapture={() => console.log('capture')} onClick={() => console.log('bubble')}>\n  <button onClick={() => console.log('target')}>Go</button>\n</div>\n// capture -> target -> bubble\n```\n```\n",tech:"react",displayNumber:63,sortKey:682},{id:"react-683",number:64,title:"How do you handle keyboard events accessibly?",question:"How do you handle keyboard events accessibly?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"Prefer native interactive elements (`button`, `a`, `input`) because browsers provide keyboard and accessibility behavior automatically. Use `onKeyDown`/`onKeyUp` only when needed, inspect `event.key`, and avoid replacing a button with a clickable `div`. If a custom widget is unavoidable, provide focusability, appropriate ARIA roles, and equivalent keyboard behavior.\n\n\n```jsx\n<button onKeyDown={e => e.key === 'Escape' && close()} onClick={close}>Close</button>\n```\n```\n",tech:"react",displayNumber:64,sortKey:683},{id:"react-684",number:65,title:"What is a ref and how does it differ from state?",question:"What is a ref and how does it differ from state?",difficulty:"Basic",category:"React Basics & JSX",answer:"A ref (`useRef`) stores a mutable value whose changes do not trigger re-renders. Its `.current` survives renders and is commonly used for DOM nodes, timers, previous values, and imperative APIs. State is for data that affects displayed output; refs are for values React doesn't need to render.\n\n\n```jsx\nfunction Focus() {\n  const input = React.useRef(null);\n  return <><input ref={input} /><button onClick={() => input.current.focus()}>Focus</button></>;\n}\n```\n```\n",tech:"react",displayNumber:65,sortKey:684},{id:"react-685",number:66,title:"What is `React.memo` and when should it be used?",question:"What is `React.memo` and when should it be used?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`React.memo` wraps a function component and skips rendering it when its props are shallowly equal to the previous props. It is a performance optimization, not a correctness feature. It does not prevent re-renders from the component's own state or consumed context, and a new object/function prop each render defeats shallow equality.\n\nUse it after profiling shows a frequently rendered, expensive child receives unchanged props. Do not wrap every component by default; the comparison itself and code complexity have costs.\n\n\n```jsx\nconst ListItem = React.memo(function ListItem({ item, onSelect }) {\n  return <li onClick={() => onSelect(item.id)}>{item.name}</li>;\n});\n```\n```\n",tech:"react",displayNumber:66,sortKey:685},{id:"react-686",number:67,title:"What is the difference between `useMemo` and `React.memo`?",question:"What is the difference between `useMemo` and `React.memo`?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`React.memo(Component)` memoizes a **component's render** based on its props. `useMemo(factory, deps)` memoizes a **computed value inside a component** until dependencies change. Neither should be used to make code correct; both are optimizations and may be removed if the value is not expensive or stable identity is unnecessary.\n\n\n```jsx\nconst Chart = React.memo(function Chart({ points }) { return <svg />; });\nfunction Dashboard({ data }) {\n  const sorted = React.useMemo(() => [...data].sort(byDate), [data]);\n  return <Chart points={sorted} />;\n}\n```\n```\n",tech:"react",displayNumber:67,sortKey:686},{id:"react-687",number:68,title:"How can unnecessary re-renders happen in a React tree?",question:"How can unnecessary re-renders happen in a React tree?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`A component re-renders when its state changes, its parent renders, or a consumed context value changes. Common avoidable causes include creating new object/array/function props on every parent render, putting state too high in the tree, changing keys, and broad context values that update frequently. A re-render is not automatically a bug; measure with the Profiler before optimizing.


\`\`\`jsx
function Parent({ theme }) {
  const [count, setCount] = React.useState(0);
  const options = React.useMemo(() => ({ theme }), [theme]);
  const onSave = React.useCallback(() => save(), []);
  return <ExpensiveChild options={options} onSave={onSave} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:68,sortKey:687},{id:"react-688",number:69,title:"What is `ReactDOM.createRoot` and how does it differ from `ReactDOM.render`?",question:"What is `ReactDOM.createRoot` and how does it differ from `ReactDOM.render`?",difficulty:"Basic",category:"React Basics & JSX",answer:"`createRoot` is the React 18 client entry point and enables the concurrent renderer and React 18 features such as automatic batching and transitions. `ReactDOM.render` is the legacy React 17 API; it remains for compatibility but opts the tree into legacy behavior and is deprecated for new applications. A root should normally be created once per DOM container.\n\n\n```jsx\nconst root = ReactDOM.createRoot(document.getElementById('root'));\nroot.render(<App />);\n// Later: root.unmount();\n```\n```\n",tech:"react",displayNumber:69,sortKey:688},{id:"react-689",number:70,title:"What is automatic batching in React 18?",question:"What is automatic batching in React 18?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Batching groups multiple state updates into one render. React 18 with \`createRoot\` extends automatic batching beyond React event handlers to timeouts, promises, and native events. This reduces work and avoids intermediate UI states. If an update must be committed synchronously (rare), \`flushSync\` can opt out, but it should not be used casually because it blocks scheduling.


\`\`\`jsx
function Example() {
  const [a, setA] = React.useState(0);
  const [b, setB] = React.useState(0);
  function later() {
    setTimeout(() => {
      setA(x => x + 1);
      setB(x => x + 1); // one render in React 18 createRoot
    }, 0);
  }
  return <button onClick={later}>{a + b}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:70,sortKey:689},{id:"react-690",number:71,title:"What is `useTransition` and how does it affect rendering priority?",question:"What is `useTransition` and how does it affect rendering priority?",difficulty:"Advanced",category:"React Basics & JSX",answer:`\`useTransition\` marks state updates as non-urgent. Urgent updates such as typing remain responsive while React works on the transition in the background. It returns \`isPending\` and \`startTransition\`. Transition updates can be interrupted and restarted; they are not a replacement for debouncing network requests or for showing a loading state.


\`\`\`jsx
function Search({ items }) {
  const [query, setQuery] = React.useState('');
  const [results, setResults] = React.useState(items);
  const [isPending, startTransition] = React.useTransition();
  function change(e) {
    const value = e.target.value;
    setQuery(value); // urgent: keep input responsive
    startTransition(() => setResults(filter(items, value))); // non-urgent
  }
  return <><input value={query} onChange={change} />{isPending && <span>Updating...</span>}</>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:71,sortKey:690},{id:"react-691",number:72,title:"What is `useDeferredValue` and when might it help?",question:"What is `useDeferredValue` and when might it help?",difficulty:"Advanced",category:"React Basics & JSX",answer:"`useDeferredValue(value)` returns a deferred version of a value that may lag behind the current one while urgent work (such as typing) stays responsive. React first renders with the old deferred value, then updates the expensive consumer at lower priority. It is useful when you don't control the state update that produces the value; use `useTransition` when you do control the update.\n\n\n```jsx\nfunction SearchResults({ query }) {\n  const deferredQuery = React.useDeferredValue(query);\n  const results = useMemo(() => expensiveFilter(deferredQuery), [deferredQuery]);\n  return <Results items={results} />;\n}\n```\n```\n",tech:"react",displayNumber:72,sortKey:691},{id:"react-692",number:73,title:"What is an Error Boundary and why is it relevant to component trees?",question:"What is an Error Boundary and why is it relevant to component trees?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`An Error Boundary is a class component that catches JavaScript errors during rendering, lifecycle methods, and constructors in its descendant tree, then displays fallback UI and can log the error. It does not catch errors in event handlers, asynchronous callbacks, server rendering, or errors thrown by the boundary itself; handle those separately.

Boundaries isolate failures so one broken panel does not blank the whole application. Place them around meaningful independent UI regions.


\`\`\`jsx
class Boundary extends React.Component {
  state = { error: null };
  static getDerivedStateFromError(error) { return { error }; }
  componentDidCatch(error, info) { report(error, info); }
  render() { return this.state.error ? <p>Failed to load.</p> : this.props.children; }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:73,sortKey:692},{id:"react-693",number:74,title:"How does React handle an element type change during reconciliation?",question:"How does React handle an element type change during reconciliation?",difficulty:"Advanced",category:"React Basics & JSX",answer:"If an element's type changes at the same tree position — for example, from `<Counter />` to `<Other />`, or `<div>` to `<span>` — React treats the old subtree and new subtree as unrelated. It unmounts the old tree (running cleanup and losing state) and mounts the new one. If the type stays the same, React updates props and reconciles descendants, preserving state where identity/keys match.\n\n\n```jsx\nfunction Screen({ compact }) {\n  // toggling type remounts the subtree\n  return compact ? <CompactPanel /> : <FullPanel />;\n}\n```\n```\n",tech:"react",displayNumber:74,sortKey:693},{id:"react-694",number:75,title:"What are the most important Core React and JSX principles to remember?",question:"What are the most important Core React and JSX principles to remember?",difficulty:"Advanced",category:"React Basics & JSX",answer:`A strong interview summary is:
- Describe UI declaratively; React reconciles element trees and commits minimal DOM changes.
- JSX is syntax that compiles to \`createElement\` or the automatic JSX runtime.
- Components should be pure; put side effects in event handlers/effects.
- Props flow down and are read-only; state is owned and updated through setters.
- Treat state and props as immutable so reference equality and memoization remain reliable.
- Use stable, data-derived keys for lists; indexes are unsafe for mutable lists.
- Use controlled inputs when React needs to own form state; refs/uncontrolled inputs when the DOM should own it.
- Fragments group siblings without extra DOM; explicit fragments are required for keyed fragment lists.
- React events use camelCase handlers and SyntheticEvents, with capture and bubbling behavior.
- Optimize only after measuring; Fiber, batching, transitions, and memoization help, but correctness and clear data flow come first.


\`\`\`jsx
function TodoApp({ initialTodos }) {
  const [todos, setTodos] = React.useState(initialTodos);
  const [text, setText] = React.useState('');

  function add(e) {
    e.preventDefault();
    if (!text.trim()) return;
    setTodos(prev => [...prev, { id: crypto.randomUUID(), text }]);
    setText('');
  }

  return (
    <>
      <form onSubmit={add}>
        <input value={text} onChange={e => setText(e.target.value)} />
        <button type="submit">Add</button>
      </form>
      <ul>
        {todos.map(todo => <li key={todo.id}>{todo.text}</li>)}
      </ul>
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:75,sortKey:694},{id:"react-695",number:76,title:"What is `useState` and how do you use it?",question:"What is `useState` and how do you use it?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`\`useState\` is a Hook that lets you add local state to a function component. It returns a pair: the current state value and a setter function that updates it and re-renders the component. Calling the setter with a new value (different from the current by \`Object.is\` comparison for primitives) schedules a re-render. State is isolated per component instance, so two \`<Counter />\` components never share state.


\`\`\`jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0); // initial value 0

  return (
    <button onClick={() => setCount(count + 1)}>
      Clicked {count} times
    </button>
  );
}
// ponytail: useState returns [value, setter]; setter triggers re-render
\`\`\`
\`\`\`
`,tech:"react",displayNumber:76,sortKey:695},{id:"react-696",number:77,title:"What is lazy initialization in `useState`?",question:"What is lazy initialization in `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`\`useState\` accepts either a direct initial value or an initializer function. The function form is only run on the first render, which avoids expensive recomputation on every render. Use it when computing the initial state is costly (e.g., reading from localStorage, parsing JSON, deriving from props). Passing the result directly runs the computation on every render even though it is discarded.


\`\`\`jsx
import { useState } from 'react';

// Direct value: runs expensiveCalc() on EVERY render
const [a] = useState(expensiveCalc());

// Lazy init: expensiveCalc() runs ONCE
const [b] = useState(() => {
  const saved = localStorage.getItem('todos');
  return saved ? JSON.parse(saved) : [];
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:77,sortKey:696},{id:"react-697",number:78,title:"What are functional updates in `useState`?",question:"What are functional updates in `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"When the next state depends on the previous state, pass a function to the setter: `setState(prev => next)`. React guarantees `prev` is the latest committed state. This avoids stale-closure bugs, especially inside event handlers, async callbacks, intervals, or when multiple state updates are batched in the same render cycle. Using `setCount(count + 1)` twice in the same handler results in only one increment if `count` is stale.\n\n\n```jsx\nconst [count, setCount] = useState(0);\n\n// Bug: both use the same stale `count` => only +1\nconst bad = () => { setCount(count + 1); setCount(count + 1); };\n\n// Correct: each update sees the prior result => +2\nconst good = () => { setCount(c => c + 1); setCount(c => c + 1); };\n```\n```\n",tech:"react",displayNumber:78,sortKey:697},{id:"react-698",number:79,title:"How does state batching work in React 18?",question:"How does state batching work in React 18?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`React batches multiple state updates into a single re-render for performance. In React 17 and earlier, batching happened only inside React event handlers, not in promises, setTimeout, or native event callbacks. React 18 extends automatic batching to those async contexts too, using the same reconciler consistently. If you truly need to flush updates synchronously, use \`flushSync\` from \`react-dom\` (use sparingly).


\`\`\`jsx
import { flushSync } from 'react-dom';

setTimeout(() => {
  // React 18: still batched into one re-render
  setA(1);
  setB(2);
}, 1000);

// Escape hatch: forces synchronous flush (rarely needed)
flushSync(() => { setA(1); });
\`\`\`
\`\`\`
`,tech:"react",displayNumber:79,sortKey:698},{id:"react-699",number:80,title:"Can you update multiple state variables independently?",question:"Can you update multiple state variables independently?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Yes. You can call multiple `useState` hooks in one component and update them independently. Each is a separate state slot. React re-renders once after the event handler finishes, applying all queued updates. Keeping state split into the smallest reasonable pieces (rather than one giant object) is usually clearer, though for tightly coupled data a `useReducer` may be better.\n\n\n```jsx\nconst [name, setName] = useState('');\nconst [age, setAge] = useState(0);\n\nfunction handleSubmit() {\n  setName('Ada');\n  setAge(36); // both applied, single re-render\n}\n```\n```\n",tech:"react",displayNumber:80,sortKey:699},{id:"react-700",number:81,title:"Why shouldn't you mutate state directly?",question:"Why shouldn't you mutate state directly?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"React compares state with `Object.is` to decide whether to re-render and to preserve referential immutability. Mutating an object in place keeps the same reference, so React may skip the update or render stale UI. Always create a new object/array with the change. For arrays use `map`, `filter`, spread; for objects use spread with overrides.\n\n\n```jsx\nconst [user, setUser] = useState({ name: 'Ada', age: 36 });\n\n// Wrong: mutate in place, same reference\nuser.age = 37; setUser(user);\n\n// Right: new object reference\nsetUser(prev => ({ ...prev, age: 37 }));\n```\n```\n",tech:"react",displayNumber:81,sortKey:700},{id:"react-701",number:82,title:"What happens when state is set to the same value?",question:"What happens when state is set to the same value?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"If you set state to a value that is `Object.is`-equal to the current state, React bails out and does not re-render that component (and its children won't re-render from this update either). For objects/arrays this means a new reference with identical contents still triggers a re-render because the reference differs. This bail-out is a performance optimization; you don't need to guard updates manually.\n\n\n```jsx\nsetCount(0);   // if already 0 -> no re-render (primitive, equal)\nsetObj({ a: 1 }); // new reference -> re-render even if contents same\n```\n```\n",tech:"react",displayNumber:82,sortKey:701},{id:"react-702",number:83,title:"How do you reset state when a key changes?",question:"How do you reset state when a key changes?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"The cleanest way to reset a component's internal state is to change its `key` prop. When React sees a different key, it treats it as a new instance and unmounts the old one, discarding its state, then mounts fresh. This is simpler and more reliable than calling setters in effects. It is especially useful for forms keyed by item id.\n\n\n```jsx\nfunction Profile({ userId }) {\n  // remounts when userId changes -> state resets automatically\n  return <UserForm key={userId} userId={userId} />;\n}\n```\n```\n",tech:"react",displayNumber:83,sortKey:702},{id:"react-703",number:84,title:"When should you use `useReducer` instead of `useState`?",question:"When should you use `useReducer` instead of `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Prefer `useReducer` when state transitions are complex, depend on previous state in non-trivial ways, involve multiple sub-values that change together, or when the next state logic is hard to express inline. It centralizes the "how state changes" into a reducer function, making updates predictable and testable, and it scales better than many `useState` calls whose setters must coordinate.\n\n\n```jsx\nconst [state, dispatch] = useReducer(reducer, initialState);\n// Use when: multiple related values, branching logic, or many event types\n```\n```\n',tech:"react",displayNumber:84,sortKey:703},{id:"react-704",number:85,title:"How do you write a reducer and dispatch actions?",question:"How do you write a reducer and dispatch actions?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"A reducer is a pure function `(state, action) => newState`. It must not mutate; return a new state based on `action.type`. `dispatch(action)` sends an action object to the reducer. Actions typically have a `type` string and an optional `payload`. Because the reducer is pure, you can unit test it without rendering.\n\n\n```jsx\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment': return { count: state.count + 1 };\n    case 'set':       return { count: action.payload };\n    default:          return state;\n  }\n}\n\nconst [state, dispatch] = useReducer(reducer, { count: 0 });\ndispatch({ type: 'increment' });\ndispatch({ type: 'set', payload: 10 });\n```\n```\n",tech:"react",displayNumber:85,sortKey:704},{id:"react-705",number:86,title:"What is lazy initialization in `useReducer`?",question:"What is lazy initialization in `useReducer`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useReducer(reducer, initialArg, init)` accepts an optional third argument `init`, an initializer function. React calls `init(initialArg)` once to compute the initial state. This lets you derive initial state from props or do expensive setup only on first mount, keeping the reducer itself pure. Without it you pass the initial state object directly.\n\n\n```jsx\nfunction init(initialCount) {\n  return { count: initialCount, history: [] };\n}\nconst [state, dispatch] = useReducer(reducer, 0, init); // count from props\n```\n```\n",tech:"react",displayNumber:86,sortKey:705},{id:"react-706",number:87,title:"How does dispatch behave — is it stable and can it run async?",question:"How does dispatch behave — is it stable and can it run async?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`dispatch` from `useReducer` is stable across renders (its identity never changes), so it's safe to omit from `useEffect` dependency arrays or pass to memoized children without breaking `React.memo`. You can dispatch inside async callbacks (e.g., after a fetch resolves). The dispatch enqueues an action; React processes the queue and re-renders. Because dispatch is stable, stale-closure problems with state are avoided.\n\n\n```jsx\nconst [state, dispatch] = useReducer(reducer, initial);\n\nuseEffect(() => {\n  fetchData().then(data => dispatch({ type: 'loaded', data }));\n}, []); // dispatch is stable, no need to include it\n```\n```\n",tech:"react",displayNumber:87,sortKey:706},{id:"react-707",number:88,title:"How do you handle side effects (like fetching) with useReducer?",question:"How do you handle side effects (like fetching) with useReducer?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Reducers must stay pure — no fetching, timers, or randomness inside them. Perform side effects in event handlers or `useEffect`, then `dispatch` an action with the result (or error). Common pattern: dispatch a `pending` action before the request, then `fulfilled`/`rejected` after. This keeps data-fetching logic out of the reducer while the reducer only describes state transitions.\n\n\n```jsx\nfunction loadUser(id) {\n  return async (dispatch) => {            // thunk-like helper\n    dispatch({ type: 'load/pending' });\n    try {\n      const u = await api.getUser(id);\n      dispatch({ type: 'load/fulfilled', user: u });\n    } catch (e) {\n      dispatch({ type: 'load/rejected', error: e.message });\n    }\n  };\n}\n```\n```\n",tech:"react",displayNumber:88,sortKey:707},{id:"react-708",number:89,title:"What is the `children` prop and how do you use it?",question:"What is the `children` prop and how do you use it?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`\`children\` is a special prop automatically populated with whatever is passed between a component's opening and closing tags. It lets you create composable layout/container components. You can render it directly, or use functions-as-children (render props) to pass data down. It is just a regular prop — it can be a node, array, string, or function.


\`\`\`jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}

<Card>
  <h1>Title</h1>
  <p>Body content</p>
</Card>;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:89,sortKey:708},{id:"react-709",number:90,title:"How do you set default values for props?",question:"How do you set default values for props?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"In function components, set defaults by destructuring with a fallback: `function Greet({ name = 'Guest' }) {}`. The legacy `Component.defaultProps` works for class components but is discouraged for function components in modern React (and ignored for destructured defaults). Default parameters only apply when the prop is `undefined`, not when it's `null` or `false`.\n\n\n```jsx\nfunction Greet({ name = 'Guest', age = 18 }) {\n  return <p>{name} is {age}</p>;\n}\n// Greet({}) -> \"Guest is 18\"; Greet({ name: null }) -> \"null is 18\"\n```\n```\n",tech:"react",displayNumber:90,sortKey:709},{id:"react-710",number:91,title:"What is prop drilling and what problems does it cause?",question:"What is prop drilling and what problems does it cause?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Prop drilling is passing data through many intermediate components that don't need it, just to reach a deeply nested child. It couples components, makes refactoring painful, and clutters intermediate signatures. It's fine for one or two levels; beyond that, prefer Context, a state library, or composition (passing elements via `children`/`slots`) to avoid threading props.\n\n\n```jsx\n// Drilling: App -> A -> B -> C all forward `user`\n<A user={user} />   // A doesn't use user, just forwards\n// Better: Context or render the consumer where data lives\n```\n```\n",tech:"react",displayNumber:91,sortKey:710},{id:"react-711",number:92,title:"What is the difference between prop drilling and Context?",question:"What is the difference between prop drilling and Context?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Prop drilling passes values explicitly through the component tree (explicit, easy to trace, re-renders only where passed). Context provides values implicitly to any descendant without intermediate props (less boilerplate, but every consumer re-renders when the value changes unless split/memoized). Use props for local, explicit data; use Context for truly global or widely-shared state (theme, auth, locale).


\`\`\`jsx
// Prop drilling: explicit, traceable
<Nav user={user} />

// Context: implicit, avoids drilling but widens coupling
<AuthContext.Provider value={user}><Nav /></AuthContext.Provider>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:92,sortKey:711},{id:"react-712",number:93,title:"Can props be mutated? Why not?",question:"Can props be mutated? Why not?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`Props are read-only from the child's perspective. React enforces a one-way data flow: a parent owns the data and passes it down; a child should never mutate received props because that would break predictability and cause subtle bugs (and in strict mode React may warn). To change data, the parent updates its own state and passes new props. Treat props as immutable inputs.


\`\`\`jsx
function Child({ user }) {
  // user.name = 'x'; // WRONG: props are read-only
  return <p>{user.name}</p>; // read only
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:93,sortKey:712},{id:"react-713",number:94,title:"How do you type/validate props (PropTypes) in React?",question:"How do you type/validate props (PropTypes) in React?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`prop-types` (a separate package since React 15.5) lets you declare expected types; React logs console warnings in development when props mismatch. It's runtime validation, less powerful than TypeScript but useful in plain JS codebases. Common validators: `PropTypes.string`, `.number`, `.func`, `.arrayOf`, `.shape`, `.oneOf`, and `isRequired`. In TS projects, prefer static types instead.\n\n\n```jsx\nimport PropTypes from 'prop-types';\n\nButton.propTypes = {\n  label: PropTypes.string.isRequired,\n  onClick: PropTypes.func,\n  size: PropTypes.oneOf(['sm', 'md', 'lg']),\n};\n```\n```\n",tech:"react",displayNumber:94,sortKey:713},{id:"react-714",number:95,title:"What is `useEffect` and when does it run?",question:"What is `useEffect` and when does it run?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`useEffect(callback, deps)` runs side effects after render is committed to the screen. With no dependency array it runs after every render; with an empty array `[]` it runs once after the initial mount; with a deps array it runs after mounts and whenever any dependency changes (and after the first render). The callback runs asynchronously after paint, so it's good for data fetching, subscriptions, and DOM measurements that don't block paint.\n\n\n```jsx\nuseEffect(() => {\n  document.title = `Count: ${count}`;\n}, [count]); // runs after mount and whenever count changes\n```\n```\n",tech:"react",displayNumber:95,sortKey:714},{id:"react-715",number:96,title:"How do you run an effect only on mount?",question:"How do you run an effect only on mount?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Pass an empty dependency array `[]`. React then runs the effect once after the first commit and never re-runs it (ignoring dependency changes, because there are none). Be careful: values captured inside are from the first render; if you reference props/state, add them to deps or accept the initial snapshot intentionally.\n\n\n```jsx\nuseEffect(() => {\n  console.log('mounted once');\n  // one-time setup (e.g., init third-party lib)\n}, []); // mount-only\n```\n```\n",tech:"react",displayNumber:96,sortKey:715},{id:"react-716",number:97,title:"How do you clean up in `useEffect`?",question:"How do you clean up in `useEffect`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Return a cleanup function from the effect callback. React runs it before re-running the effect (when deps change) and before unmounting. Use it to unsubscribe, clear timers, remove event listeners, or cancel requests — anything that would otherwise leak or cause errors on stale components. Forgetting cleanup is a common source of memory leaks and "setState on unmounted" warnings.


\`\`\`jsx
useEffect(() => {
  const id = setInterval(() => setNow(Date.now()), 1000);
  return () => clearInterval(id); // cleanup on unmount / dep change
}, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:97,sortKey:716},{id:"react-717",number:98,title:"How do you fetch data in `useEffect` safely?",question:"How do you fetch data in `useEffect` safely?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Fetch inside an effect with an empty (or param-based) deps array, and guard against setting state after unmount using an `ignore` flag or `AbortController`. Otherwise a late response can call `setState` on an unmounted component (React warns, and it's a leak). Modern approach: use `AbortController` to cancel the request on cleanup.\n\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController();\n  fetch(`/api/user/${id}`, { signal: controller.signal })\n    .then(r => r.json())\n    .then(setUser)\n    .catch(e => { if (e.name !== 'AbortError') setError(e); });\n  return () => controller.abort(); // cancel on unmount / id change\n}, [id]);\n```\n```\n",tech:"react",displayNumber:98,sortKey:717},{id:"react-718",number:99,title:"What happens when dependencies are missing or wrong?",question:"What happens when dependencies are missing or wrong?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"If you omit a dependency that the effect uses, the effect closes over a stale value and won't re-run when that value changes, causing bugs. The exhaustive-deps ESLint rule flags this. Conversely, including unstable references (new objects/functions each render) causes the effect to run every render. Fix by adding real deps, or memoizing unstable values with `useMemo`/`useCallback`.\n\n\n```jsx\n// Bug: `id` used but not in deps -> never refetches on id change\nuseEffect(() => { fetchUser(id); }, []); // exhaustive-deps warns\n// Fix: add id, or guard intentionally and document why\nuseEffect(() => { fetchUser(id); }, [id]);\n```\n```\n",tech:"react",displayNumber:99,sortKey:718},{id:"react-719",number:100,title:"How do you skip effect runs on initial mount?",question:"How do you skip effect runs on initial mount?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`There's no built-in "skip first render" flag, but you can use a ref to track whether it's the first run and return early. This is useful when you want to react only to updates, not the initial mount. Alternatively, initialize state from the value so the first effect run is a no-op.


\`\`\`jsx
const isFirst = useRef(true);
useEffect(() => {
  if (isFirst.current) { isFirst.current = false; return; }
  console.log('updated, not mounted');
}, [value]);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:100,sortKey:719},{id:"react-720",number:101,title:"How do multiple effects interact and in what order do they run?",question:"How do multiple effects interact and in what order do they run?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Effects run in the order they are declared, after the render is committed, top to bottom. Cleanups also run in declaration order, before the next set of effects when deps change, and on unmount. You can split concerns into multiple `useEffect` calls (one per logical side effect) rather than cramming everything into one — this is encouraged for clarity.\n\n\n```jsx\nuseEffect(() => { /* A: subscribe */ return () => {}; }, []);\nuseEffect(() => { /* B: sync title */ }, [count]);\n// Order: A runs, B runs; on count change: B cleanup -> B re-run\n```\n```\n",tech:"react",displayNumber:101,sortKey:720},{id:"react-721",number:102,title:"What is the purpose of the dependency array, precisely?",question:"What is the purpose of the dependency array, precisely?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"The dependency array tells React when to re-run the effect: it compares each entry with the previous render's values using `Object.is`. Three cases: no array → every render; `[]` → only after first render; `[a, b]` → after first render and whenever `a` or `b` changes by `Object.is`. Omitting the array when you meant `[]` is a frequent mistake that causes infinite loops with state updates.\n\n\n```jsx\nuseEffect(fn);          // every render\nuseEffect(fn, []);      // mount only\nuseEffect(fn, [a, b]);  // when a or b change\n```\n```\n",tech:"react",displayNumber:102,sortKey:721},{id:"react-722",number:103,title:"How do you handle subscriptions in effects?",question:"How do you handle subscriptions in effects?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Subscribe in an effect and return a cleanup that unsubscribes. If the subscription source depends on a prop/state value, include it in deps so React re-subscribes when it changes, and the cleanup prevents overlapping subscriptions. This pattern applies to WebSocket, EventTarget, store subscriptions, etc.\n\n\n```jsx\nuseEffect(() => {\n  const socket = new WebSocket(`/ws/${room}`);\n  socket.onmessage = e => setMessages(m => [...m, e.data]);\n  return () => socket.close(); // unsubscribe on room change/unmount\n}, [room]);\n```\n```\n",tech:"react",displayNumber:103,sortKey:722},{id:"react-723",number:104,title:'What is the "infinite loop" trap with useEffect and objects?',question:'What is the "infinite loop" trap with useEffect and objects?',difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`If an effect sets state and the dependency is an object/array recreated every render (new reference), the effect runs every render, sets state, triggers re-render, and loops. Also, calling a state setter whose dep is the same object reference but you spread into a new one each time loops. Fix: depend on primitive fields, memoize the object, or compute inside the effect without depending on the whole object.


\`\`\`jsx
const opts = { limit: 10 }; // new ref every render
useEffect(() => { fetchData(opts); }, [opts]); // loops!
// Fix: useState/useMemo opts, or depend on opts.limit
useEffect(() => { fetchData(opts); }, [opts.limit]);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:104,sortKey:723},{id:"react-724",number:105,title:"Can you use effects to sync external stores with React state?",question:"Can you use effects to sync external stores with React state?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes — subscribe in an effect and push external changes into state via setter, with cleanup to unsubscribe. For external stores (Redux, Zustand, browser APIs), React 18 also offers `useSyncExternalStore` which is purpose-built: it reads a snapshot and subscribes safely, handling concurrent rendering and avoiding tearing. Prefer `useSyncExternalStore` over hand-rolled effect subscriptions for stores.\n\n\n```jsx\nimport { useSyncExternalStore } from 'react';\n\nconst snapshot = useSyncExternalStore(store.subscribe, store.getSnapshot);\n// safer than useEffect + setState for external stores\n```\n```\n",tech:"react",displayNumber:105,sortKey:724},{id:"react-725",number:106,title:"What is the difference between `useEffect` and `useLayoutEffect`?",question:"What is the difference between `useEffect` and `useLayoutEffect`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`useEffect` runs asynchronously after the browser paints; `useLayoutEffect` runs synchronously after DOM mutations but before the browser paints. Layout effects are for reading layout (e.g., `getBoundingClientRect`) and synchronously re-styling to avoid a visible flash. Because layout effects block paint, overuse hurts performance. For most side effects (fetching, subscriptions) `useEffect` is correct.\n\n\n```jsx\nuseLayoutEffect(() => {\n  // measure then set position BEFORE paint (no flicker)\n  const rect = ref.current.getBoundingClientRect();\n  setPos(rect.top);\n}, []);\n```\n```\n",tech:"react",displayNumber:106,sortKey:725},{id:"react-726",number:107,title:"When would you choose `useLayoutEffect` over `useEffect`?",question:"When would you choose `useLayoutEffect` over `useEffect`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Choose `useLayoutEffect` only when you must read computed layout and make DOM changes (or state changes that affect layout) before the user sees the painted frame — e.g., tooltip positioning, animations that start from a measured size, or avoiding a flash of wrong styles. Otherwise prefer `useEffect` to keep paint fast. On the server, `useLayoutEffect` warns; guard or use `useEffect` there.\n\n\n```jsx\n// Tooltip: measure target, set coordinates, all before paint\nuseLayoutEffect(() => {\n  const { top } = anchorRef.current.getBoundingClientRect();\n  tipRef.current.style.top = `${top}px`;\n}, [anchor]);\n```\n```\n",tech:"react",displayNumber:107,sortKey:726},{id:"react-727",number:108,title:"Why does `useLayoutEffect` warn during SSR?",question:"Why does `useLayoutEffect` warn during SSR?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useLayoutEffect` cannot run on the server because there is no DOM to measure or mutate before paint. React emits a warning during SSR if it's used. The common fix is `useIsomorphicLayoutEffect`: a variable that is `useLayoutEffect` in the browser and `useEffect` on the server. This avoids the warning while keeping layout-sync behavior client-side.\n\n\n```jsx\nimport { useEffect, useLayoutEffect } from 'react';\n\nconst useIsomorphicLayoutEffect =\n  typeof window !== 'undefined' ? useLayoutEffect : useEffect;\n```\n```\n",tech:"react",displayNumber:108,sortKey:727},{id:"react-728",number:109,title:"What are synthetic events in React?",question:"What are synthetic events in React?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"React wraps native browser events in a cross-browser `SyntheticEvent` object, normalizing properties (e.g., `event.target`, `event.preventDefault`, `event.stopPropagation`) across browsers. Handlers receive this synthetic event. It behaves like the native event but is pooled in legacy React (see next). In React 17+, events are attached at the root container, not document, and synthetic events are no longer pooled.\n\n\n```jsx\nfunction Form() {\n  return (\n    <input\n      onChange={e => console.log(e.target.value, e.type)} // SyntheticEvent\n    />\n  );\n}\n```\n```\n",tech:"react",displayNumber:109,sortKey:728},{id:"react-729",number:110,title:"What was event pooling and is it still a thing?",question:"What was event pooling and is it still a thing?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:'In React 16 and earlier, synthetic events were "pooled": after the handler ran, the event object\'s properties were nullified and the object reused for performance. You had to call `e.persist()` to read it asynchronously. React 17 removed pooling entirely — you can freely access `e` in `setTimeout` or `async` code. Interviewers still ask; the correct modern answer is "pooling was removed in React 17."\n\n\n```jsx\n// React 16: e would be nulled after handler -> needed e.persist()\n// React 17+: this just works\nonClick={e => setTimeout(() => console.log(e.target), 1000)};\n```\n```\n',tech:"react",displayNumber:110,sortKey:729},{id:"react-730",number:111,title:"How do you prevent default behavior in React events?",question:"How do you prevent default behavior in React events?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `event.preventDefault()` inside the handler — you cannot return `false` to prevent default as in some frameworks. This is essential for forms: `onSubmit` handlers should `preventDefault()` to stop the browser's full-page reload. Likewise `onClick` on links. Note React attaches listeners at the root, but `preventDefault` still works as expected.\n\n\n```jsx\nfunction Form() {\n  const onSubmit = e => {\n    e.preventDefault(); // stop page reload\n    console.log('handled via JS');\n  };\n  return <form onSubmit={onSubmit}><button>Go</button></form>;\n}\n```\n```\n",tech:"react",displayNumber:111,sortKey:730},{id:"react-731",number:112,title:"How does event delegation work in React 17+?",question:"How does event delegation work in React 17+?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Before React 17, React attached a single set of listeners at the `document`. From React 17, it attaches them to the root DOM container where `ReactDOM.render` mounts, so multiple React versions can coexist and events don't leak to the document. Your `onClick` etc. are still dispatched through React's synthetic system; native listeners you add with `addEventListener` on the same element can interleave but React's delegation root differs.\n\n\n```jsx\n// React 17+: delegation root is the app container, not document\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n```\n```\n",tech:"react",displayNumber:112,sortKey:731},{id:"react-732",number:113,title:"How do you pass arguments to event handlers?",question:"How do you pass arguments to event handlers?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Two common ways: wrap in an arrow function `onClick={() => handle(id)}`, or use `bind` `onClick={handle.bind(null, id)}`. The arrow form is idiomatic in modern code. Be mindful that creating a new function each render can hurt memoized children; if needed, use a data attribute or curry the handler and read `e.currentTarget.dataset` instead.\n\n\n```jsx\n<button onClick={() => deleteItem(id)}>Delete</button>\n// or read from dataset to avoid new closures:\n<button data-id={id} onClick={e => deleteItem(e.currentTarget.dataset.id)} />\n```\n```\n",tech:"react",displayNumber:113,sortKey:732},{id:"react-733",number:114,title:"What is `useRef` and what is it used for?",question:"What is `useRef` and what is it used for?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`useRef(initialValue)` returns a mutable object `{ current }` whose identity is stable across renders. Unlike state, changing `ref.current` does NOT trigger a re-render. Use it to hold DOM nodes, timer IDs, previous values, or any mutable value you don't want to cause re-renders. It persists for the component's lifetime and resets only on unmount/remount.\n\n\n```jsx\nconst inputRef = useRef(null);\nuseEffect(() => { inputRef.current?.focus(); }, []);\n<input ref={inputRef} />;\n```\n```\n",tech:"react",displayNumber:114,sortKey:733},{id:"react-734",number:115,title:"How do you access a DOM node via a ref?",question:"How do you access a DOM node via a ref?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'Attach a ref object to a built-in element\'s `ref` attribute; React assigns the DOM node to `ref.current` after mount and sets it to `null` on unmount. You can then read/measure/manipulate the node in effects or handlers. This is the supported imperative escape hatch — prefer declarative rendering, but refs are fine for focus, media playback, measurements.\n\n\n```jsx\nconst videoRef = useRef(null);\nconst play = () => videoRef.current.play();\n<video ref={videoRef} src="a.mp4" />;\n```\n```\n',tech:"react",displayNumber:115,sortKey:734},{id:"react-735",number:116,title:"How do you track a previous value with a ref?",question:"How do you track a previous value with a ref?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Store the value in a ref and update it after render (in an effect or during render with care). A common custom hook \`usePrevious\` saves the previous render's value. Use this when you need to compare "before vs after" without adding state that triggers extra renders.


\`\`\`jsx
function usePrevious(value) {
  const ref = useRef();
  useEffect(() => { ref.current = value; }); // runs after commit
  return ref.current;
}
const prev = usePrevious(count);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:116,sortKey:735},{id:"react-736",number:117,title:"What is `forwardRef` and when do you need it?",question:"What is `forwardRef` and when do you need it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Function components don't receive `ref` as a normal prop (it's special). `forwardRef` lets a component accept a ref from its parent and forward it to a child DOM node or component. You need it when building reusable wrappers around inputs, buttons, or libraries where the parent must directly access the underlying node. Class components accept `ref` natively, so only function components need `forwardRef`.\n\n\n```jsx\nconst FancyInput = forwardRef((props, ref) => (\n  <input ref={ref} className=\"fancy\" {...props} />\n));\n\nconst ref = useRef();\n<FancyInput ref={ref} />; // ref points to the <input>\n```\n```\n",tech:"react",displayNumber:117,sortKey:736},{id:"react-737",number:118,title:"What is `useImperativeHandle`?",question:"What is `useImperativeHandle`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`useImperativeHandle(ref, createHandle, deps)` customizes the instance value that a parent receives via `ref` when using `forwardRef`. Instead of exposing the whole DOM node, you expose a limited API (e.g., `{ focus, reset }`). Pair it with `forwardRef`. Use sparingly — declarative props are usually better, but imperative handles are handy for focus management or animations.\n\n\n```jsx\nconst FancyInput = forwardRef((props, ref) => {\n  const inputRef = useRef();\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n    clear: () => { inputRef.current.value = ''; },\n  }), []);\n  return <input ref={inputRef} />;\n});\n```\n```\n",tech:"react",displayNumber:118,sortKey:737},{id:"react-738",number:119,title:"What's the difference between a ref and state?",question:"What's the difference between a ref and state?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'State changes trigger re-renders and are immutable-ish (set via setter); refs are mutable via `.current`, do NOT trigger re-renders, and persist across renders. Use state for anything rendered in the UI; use refs for values you read/write imperatively (DOM nodes, timers, caches, "latest" values in callbacks). Don\'t use refs to store data that should be reflected in the view.\n\n\n```jsx\nconst [open, setOpen] = useState(false); // UI reacts\nconst timer = useRef();                  // no re-render on change\n```\n```\n',tech:"react",displayNumber:119,sortKey:738},{id:"react-739",number:120,title:"How do you create and provide context?",question:"How do you create and provide context?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `createContext(defaultValue)` to make a context object. Wrap descendants in `<Context.Provider value={...}>`. Any component inside can read the value with `useContext`. The `defaultValue` is only used when there is no matching Provider above. Wrap the value in `useMemo` when it's an object to avoid unnecessary consumer re-renders.\n\n\n```jsx\nconst ThemeContext = createContext('light');\n\n<ThemeContext.Provider value=\"dark\">\n  <App />\n</ThemeContext.Provider>;\n```\n```\n",tech:"react",displayNumber:120,sortKey:739},{id:"react-740",number:121,title:"How do you consume context with `useContext`?",question:"How do you consume context with `useContext`?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `useContext(MyContext)` inside a component to get the current value from the nearest Provider above it. It re-renders the component whenever the context value changes. You can consume multiple contexts. Note: `useContext` must be called at the top level of a component, not conditionally.\n\n\n```jsx\nfunction Toolbar() {\n  const theme = useContext(ThemeContext); // nearest Provider value\n  return <div className={theme}>...</div>;\n}\n```\n```\n",tech:"react",displayNumber:121,sortKey:740},{id:"react-741",number:122,title:"Does a context value change re-render all consumers?",question:"Does a context value change re-render all consumers?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes — every component calling `useContext` for that context re-renders whenever the Provider's `value` changes (by `Object.is`). If you pass a new object/array each render, all consumers re-render even if their slice is unchanged. Mitigate by: memoizing the value with `useMemo`, splitting contexts by concern, or using external stores (`useSyncExternalStore`) for fine-grained updates.\n\n\n```jsx\n// Bad: new object each render -> all consumers re-render\n<Ctx.Provider value={{ user, theme }}>\n\n// Better: memoize\nconst value = useMemo(() => ({ user, theme }), [user, theme]);\n<Ctx.Provider value={value}>\n```\n```\n",tech:"react",displayNumber:122,sortKey:741},{id:"react-742",number:123,title:"How do you avoid unnecessary context re-renders?",question:"How do you avoid unnecessary context re-renders?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Strategies: (1) Memoize the `value` with `useMemo` so identity is stable when contents are equal. (2) Split into multiple smaller contexts (e.g., `UserContext` vs `ThemeContext`) so a theme change doesn't re-render user consumers. (3) Separate the state setter from the value — provide the setter via a stable context and the value via another. (4) For high-frequency updates, use an external store instead of context.\n\n\n```jsx\nconst UserCtx = createContext(null);\nconst UserDispatch = createContext(null); // stable dispatch\n// split value (changes) from dispatch (stable) to limit re-renders\n```\n```\n",tech:"react",displayNumber:123,sortKey:742},{id:"react-743",number:124,title:"Can you have a default value for context without a Provider?",question:"Can you have a default value for context without a Provider?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Yes. `createContext(defaultValue)` sets a fallback used when a component consumes the context but no Provider is above it in the tree. It's only a fallback — once a Provider exists, its `value` always wins, even if `undefined`. This is useful for testing components in isolation or for optional contexts.\n\n\n```jsx\nconst Ctx = createContext({ lang: 'en' });\n// No Provider -> component sees { lang: 'en' }\n```\n```\n",tech:"react",displayNumber:124,sortKey:743},{id:"react-744",number:125,title:"How do you update context from a consumer?",question:"How do you update context from a consumer?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Context itself is just data; to let consumers update it, lift state to the Provider and pass the setter (or a dispatch) through context too. The provider holds `useState`/`useReducer` and supplies both value and an updater. Consumers call the updater, which changes the Provider\'s state, which re-renders all consumers. This is the standard "context + reducer" pattern.\n\n\n```jsx\nconst [user, setUser] = useState(null);\n<AuthContext.Provider value={{ user, setUser }}>\n// Consumer: const { setUser } = useContext(AuthContext);\n```\n```\n',tech:"react",displayNumber:125,sortKey:744},{id:"react-745",number:126,title:"What is the difference between Context and Redux?",question:"What is the difference between Context and Redux?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Context is a built-in mechanism for passing values down without props; it re-renders all consumers on value change and has no built-in devtools, middleware, or time-travel. Redux is an external state container with a single store, reducers, middleware, selectors, and optimized subscriptions that avoid re-rendering unrelated components. For simple shared state, Context suffices; for complex, high-frequency, or multi-consumer app state, a store with fine-grained subscriptions scales better.\n\n\n```jsx\n// Context: re-renders consumers on any value change\n// Redux + useSelector: only re-renders when selected slice changes\n```\n```\n",tech:"react",displayNumber:126,sortKey:745},{id:"react-746",number:127,title:"How do you provide multiple contexts cleanly?",question:"How do you provide multiple contexts cleanly?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Nest Providers, but deep nesting is ugly. You can compose them into a single \`AppProviders\` component that stacks them in a readable order, or use a context-splitting utility. Keep related contexts separate so updates don't cascade. A common pattern: a \`Providers\` wrapper component used once at the root.


\`\`\`jsx
function AppProviders({ children }) {
  return (
    <ThemeProvider>
      <AuthProvider>
        <I18nProvider>{children}</I18nProvider>
      </AuthProvider>
    </ThemeProvider>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:127,sortKey:746},{id:"react-747",number:128,title:"Can you consume context conditionally or in loops?",question:"Can you consume context conditionally or in loops?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Hooks, including `useContext`, must be called unconditionally at the top level — never inside `if`, loops, or nested functions, or React's hook-order tracking breaks and you'll get errors. To vary behavior, always call `useContext` first, then branch on the resulting value. The \"don't call hooks in conditions\" rule is strict and non-negotiable.\n\n\n```jsx\nfunction C() {\n  const ctx = useContext(MyCtx); // always called\n  if (ctx.enabled) return <On />; // branch AFTER the hook\n  return <Off />;\n}\n```\n```\n",tech:"react",displayNumber:128,sortKey:747},{id:"react-748",number:129,title:"What is `React.memo` and how does it work?",question:"What is `React.memo` and how does it work?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`React.memo(Component)` is a higher-order component that memoizes the rendered output, skipping re-renders when the component's props are shallowly equal (`Object.is` per prop) to the previous render. It's a performance optimization for components that re-render often with the same props. It does a shallow comparison; deeply nested objects still trigger re-renders unless their references are stable (via `useMemo`/`useCallback`). It does NOT help if the component uses context or internal state that changes.\n\n\n```jsx\nconst Row = React.memo(function Row({ item }) {\n  return <li>{item.name}</li>;\n});\n// Re-renders only when `item` reference changes\n```\n```\n",tech:"react",displayNumber:129,sortKey:748},{id:"react-749",number:130,title:"What is `useMemo` and when should you use it?",question:"What is `useMemo` and when should you use it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useMemo(fn, deps)` memoizes the result of a computation, recomputing only when deps change. Use it for expensive calculations (sorting, filtering large arrays, heavy derivations) or to produce stable object/array references passed to memoized children / context. Don't use it for every trivial calculation — the overhead and complexity usually outweigh the gain. Its identity guarantee also helps avoid re-renders.\n\n\n```jsx\nconst sorted = useMemo(() => items.slice().sort(byName), [items]);\n// recomputes only when `items` changes\n```\n```\n",tech:"react",displayNumber:130,sortKey:749},{id:"react-750",number:131,title:"What is `useCallback` and how is it different from `useMemo`?",question:"What is `useCallback` and how is it different from `useMemo`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useCallback(fn, deps)` returns a memoized version of the function itself (stable identity across renders unless deps change). `useMemo` memoizes a value; `useCallback` memoizes a function — in fact `useCallback(fn, deps)` is roughly `useMemo(() => fn, deps)`. Use it to keep stable function identities so memoized children (via `React.memo`) don't re-render, and to keep stable deps in effects.\n\n\n```jsx\nconst handleClick = useCallback(() => doThing(id), [id]);\n// stable identity unless id changes -> safe for memoized children\n```\n```\n",tech:"react",displayNumber:131,sortKey:750},{id:"react-751",number:132,title:"How do refs, memo, and callbacks interact to prevent re-renders?",question:"How do refs, memo, and callbacks interact to prevent re-renders?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"A memoized child (`React.memo`) re-renders only when its props change by shallow compare. If a parent passes a new inline function or new object each render, the child re-renders despite memoization. Wrap those functions in `useCallback` and objects in `useMemo` to keep references stable. Refs are naturally stable (no re-render on change) but don't trigger child updates. Combine: memo child + stable callbacks + stable value props = minimal re-renders.\n\n\n```jsx\nconst onSelect = useCallback(id => setSel(id), []);\nconst data = useMemo(() => ({ items }), [items]);\n<Row onSelect={onSelect} data={data} />; // memoized Row stays put\n```\n```\n",tech:"react",displayNumber:132,sortKey:751},{id:"react-752",number:133,title:"Are `useMemo`/`useCallback` guarantees or optimizations?",question:"Are `useMemo`/`useCallback` guarantees or optimizations?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"They are optimizations, not semantic guarantees. React may discard memoized values (e.g., on low-memory situations or in future concurrent features) and recompute. Never rely on them for correctness — only for performance. If a computation must run exactly once or have a guaranteed identity, use `useState` lazy init or refs. Treat memoization as a hint.\n\n\n```jsx\n// WRONG: relying on useMemo for correctness\nconst list = useMemo(() => fetchList(), []); // may recompute\n// RIGHT: side effect in effect + state\n```\n```\n",tech:"react",displayNumber:133,sortKey:752},{id:"react-753",number:134,title:"How do you profile and find unnecessary re-renders?",question:"How do you profile and find unnecessary re-renders?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Use the React DevTools "Profiler" to record renders and see which components re-rendered and why (it highlights commits and per-component wasted renders). The "Highlight updates" option flashes re-rendering components. For automated detection, the `why-did-you-render` library warns when a component re-renders with equal props. Always measure before optimizing — premature memoization adds complexity.\n\n\n```jsx\n// React DevTools Profiler -> Record -> inspect "re-rendered by"\n// why-did-you-render: patches components to log needless renders\n```\n```\n',tech:"react",displayNumber:134,sortKey:753},{id:"react-754",number:135,title:"When should you NOT use `React.memo`?",question:"When should you NOT use `React.memo`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Don't memoize when the component is cheap to render (the memo comparison costs more than re-rendering), when it always receives new props anyway (e.g., new inline objects), when it reads context that changes often (memo won't help), or at the very top of a tree that re-renders entirely. Memoization without a stable-prop situation just adds overhead. Measure first.\n\n\n```jsx\n// Pointless: parent always passes new object -> memo never skips\n<Row item={{ id, name }} />; // React.memo can't help here\n```\n```\n",tech:"react",displayNumber:135,sortKey:754},{id:"react-755",number:136,title:"How do you create an error boundary?",question:"How do you create an error boundary?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Error boundaries are class components implementing \`static getDerivedStateFromError(error)\` (to render a fallback UI) and/or \`componentDidCatch(error, info)\` (for logging). They catch errors during rendering, in lifecycle methods, and in constructors of the whole tree below them — but NOT in event handlers, async code, or SSR. Function components cannot be error boundaries (no equivalent Hook), so you still need a class for this.


\`\`\`jsx
class ErrorBoundary extends React.Component {
  state = { hasError: false };
  static getDerivedStateFromError() { return { hasError: true }; }
  componentDidCatch(error, info) { logError(error, info); }
  render() {
    return this.state.hasError ? <Fallback /> : this.props.children;
  }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:136,sortKey:755},{id:"react-756",number:137,title:"What can't error boundaries catch?",question:"What can't error boundaries catch?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Error boundaries do NOT catch: errors in event handlers, asynchronous code (setTimeout, promises, async/await), server-side rendering, or errors thrown in the boundary itself. For those, use `try/catch` in event handlers/async functions, or error states. This is a common interview trap — boundaries only cover render/lifecycle/constructor phases of descendants.\n\n\n```jsx\n// Event handler error: handle manually, boundary won't catch\nconst onClick = () => {\n  try { risky(); } catch (e) { setError(e); }\n};\n```\n```\n",tech:"react",displayNumber:137,sortKey:756},{id:"react-757",number:138,title:"How do you handle errors in event handlers or async code?",question:"How do you handle errors in event handlers or async code?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Use plain `try/catch` for synchronous/async logic and local error state to show a message. For async, catch promise rejections and set state (or dispatch an error action). If you want to "rethrow" into a boundary from async, you can store the error and re-throw during render so the nearest boundary catches it. This bridges async failures into the boundary system when desired.\n\n\n```jsx\nconst [err, setErr] = useState(null);\nuseEffect(() => {\n  load().catch(setErr);\n}, []);\nif (err) throw err; // optionally surface to error boundary\n```\n```\n',tech:"react",displayNumber:138,sortKey:757},{id:"react-758",number:139,title:"What is `React.lazy` and how do you use it?",question:"What is `React.lazy` and how do you use it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`React.lazy(() => import('./Heavy'))` lets you code-split a component: it's loaded only when first rendered, returning a Promise that resolves to a module with a default export component. Wrap it in `<Suspense fallback={...}>` to show a placeholder while loading. This reduces initial bundle size. The dynamic `import()` must resolve to a module whose default export is the component.\n\n\n```jsx\nconst Heavy = React.lazy(() => import('./Heavy'));\n\n<Suspense fallback={<Spinner />}>\n  <Heavy />\n</Suspense>;\n```\n```\n",tech:"react",displayNumber:139,sortKey:758},{id:"react-759",number:140,title:"What is `Suspense` and what does `fallback` do?",question:"What is `Suspense` and what does `fallback` do?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'`<Suspense fallback={...}>` shows the `fallback` UI while any component inside it is "suspended" — currently triggered by `React.lazy` code-splitting or data-fetching libraries that integrate with Suspense. When the suspended resource resolves, React swaps in the real content. You can nest Suspense boundaries for granular loading states. Suspense for data (not just lazy) is an evolving feature; the stable use today is with `React.lazy`.\n\n\n```jsx\n<Suspense fallback={<Loading />}>\n  <Profile />\n  <Posts />\n</Suspense>;\n```\n```\n',tech:"react",displayNumber:140,sortKey:759},{id:"react-760",number:141,title:"Can you use `Suspense` for data fetching?",question:"Can you use `Suspense` for data fetching?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes, but it depends on the data library. Suspense for data is supported by libraries like Relay, React Query (with `useSuspenseQuery`), and `Suspense` integrations — your resource throws a promise that Suspense catches, then renders the fallback. The built-in `fetch` + Suspense isn't stable without a framework (Next.js App Router uses it). For plain `useEffect` fetching, Suspense does NOT apply; you'd use `React.lazy` or a suspense-enabled client.\n\n\n```jsx\n// With a suspense-enabled client:\nconst data = useSuspenseQuery(key); // throws promise while loading\n// Wrapped by <Suspense fallback={<Skeleton />}>\n```\n```\n",tech:"react",displayNumber:141,sortKey:760},{id:"react-761",number:142,title:"How do error boundaries and Suspense work together?",question:"How do error boundaries and Suspense work together?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:'Place an error boundary OUTSIDE (above) the Suspense boundary so that both lazy-load failures and render errors are caught. A boundary catches errors thrown by a failed lazy import or a component that throws during render; Suspense handles the "still loading" state. Order matters: `<ErrorBoundary><Suspense><Lazy/></Suspense></ErrorBoundary>`. If the lazy import rejects (network error), the boundary shows the fallback, not Suspense.\n\n\n```jsx\n<ErrorBoundary fallback={<LoadFailed />}>\n  <Suspense fallback={<Spinner />}>\n    <LazyComponent />\n  </Suspense>\n</ErrorBoundary>\n```\n```\n',tech:"react",displayNumber:142,sortKey:761},{id:"react-762",number:143,title:"What is Strict Mode and what does it do?",question:"What is Strict Mode and what does it do?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`<React.StrictMode>` is a development-only wrapper that activates extra checks and warnings to surface unsafe patterns. It intentionally double-invokes certain functions (render, constructors, some effects' setup+cleanup+setup) to help you find impure code and missing cleanups. It renders components twice, logs deprecated APIs, and detects unsafe lifecycle usage. It has no effect in production — the double-invoke disappears.\n\n\n```jsx\nimport { StrictMode } from 'react';\n<StrictMode><App /></StrictMode>; // dev-only extra checks\n```\n```\n",tech:"react",displayNumber:143,sortKey:762},{id:"react-763",number:144,title:"Why does Strict Mode double-invoke effects?",question:"Why does Strict Mode double-invoke effects?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`In development, Strict Mode mounts, unmounts, then remounts each component once to verify that effects clean up properly and can re-create state correctly. It runs setup → cleanup → setup for effects (and runs reducers/state initializers twice). This surfaces missing cleanup (leaks) and impure render logic. If your effect leaks without cleanup, Strict Mode will expose it. Production runs effects once.


\`\`\`jsx
useEffect(() => {
  const id = setInterval(tick, 1000);
  return () => clearInterval(id); // must clean up or Strict Mode leaks
}, []);
// dev: setup, cleanup, setup again
\`\`\`
\`\`\`
`,tech:"react",displayNumber:144,sortKey:763},{id:"react-764",number:145,title:"How do you handle the double-render in Strict Mode?",question:"How do you handle the double-render in Strict Mode?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`You generally don't "handle" it — you make your code idempotent and side-effect-free in render. For effects, ensure cleanups fully undo setup so the doubled mount/unmount is harmless. Avoid module-level singletons mutated in effects without cleanup. If you must run something exactly once (e.g., analytics), guard with a ref, but first verify it's truly needed — most double-invoke issues reveal a real missing cleanup.


\`\`\`jsx
const done = useRef(false);
useEffect(() => {
  if (done.current) return; // guard one-time side effect
  done.current = true;
  initAnalytics();
}, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:145,sortKey:764},{id:"react-765",number:146,title:"What is the difference between development and production builds?",question:"What is the difference between development and production builds?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Development builds include prop-type checks, extra warnings, Strict Mode double-invocation, and detailed error messages — slower and larger. Production builds are minified, strip these dev-only checks, and run effects once. That's why behavior (especially Strict Mode effects) differs between `npm start` and the production bundle. Always test production-like behavior before shipping.\n\n\n```jsx\n// Production: NODE_ENV=production -> no dev warnings, effects run once\n// Dev: NODE_ENV=development -> Strict Mode, warnings, double effects\n```\n```\n",tech:"react",displayNumber:146,sortKey:765},{id:"react-766",number:147,title:"How do you detect Strict Mode issues early?",question:"How do you detect Strict Mode issues early?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Keep Strict Mode ON in development (it's on by default in create-react-app / Vite templates). Watch the console for "double-invoke" leaking timers, duplicate subscriptions, or "state updates on unmounted component" warnings — these point to missing cleanups. Write effects that are reversible. Treat any leak exposed in dev as a real bug even if production hides the symptom.


\`\`\`jsx
// Symptom in Strict Mode: two console logs on mount -> effect not cleaned
useEffect(() => { console.log('sub'); return () => console.log('unsub'); }, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:147,sortKey:766},{id:"react-767",number:148,title:"Does Strict Mode affect state or context values?",question:"Does Strict Mode affect state or context values?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Strict Mode double-invokes render functions and state initializers to check purity, but it does NOT change the actual committed state or context values the user sees — the second render result is what's used. It may call your `useState` initializer or `useReducer` reducer twice in dev to detect impurity, so those must be pure. The user-visible UI is still correct and single.\n\n\n```jsx\n// Reducer/init must be pure: double-invoked in dev\nconst [s] = useReducer(reducer, 0, init); // init may run twice (dev)\n```\n```\n",tech:"react",displayNumber:148,sortKey:767},{id:"react-768",number:149,title:"How do you opt out of Strict Mode for a subtree?",question:"How do you opt out of Strict Mode for a subtree?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Strict Mode is opt-in per tree. Simply don't wrap a subtree in `<React.StrictMode>`. If the whole app is wrapped, remove the wrapper or wrap only the parts you want checked. There's no per-component \"disable\" flag — it's all-or-nothing at the wrapper boundary. Most teams keep it on; you'd only remove it when a third-party lib misbehaves under double-invoke.\n\n\n```jsx\n// Remove outer StrictMode to opt a subtree out\n<App /> // no StrictMode wrapper = no double-invoke/dev checks\n```\n```\n",tech:"react",displayNumber:149,sortKey:768},{id:"react-769",number:150,title:"Why might production behave differently than dev with effects?",question:"Why might production behave differently than dev with effects?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`Two main reasons: (1) Strict Mode's double mount/cleanup only happens in dev, so a missing cleanup (leak, duplicate listener, double-subscribe) is hidden in production but exposed in dev. (2) Dev builds run extra validation. If production "works" but dev shows warnings, the dev warning usually indicates a latent bug that production merely tolerates. Always fix the root cause (proper cleanup, purity) rather than disabling Strict Mode.


\`\`\`jsx
// Bug only visible in dev Strict Mode:
useEffect(() => { window.addEventListener('resize', onResize); }, []);
// no cleanup -> duplicate listeners under Strict Mode; fix with cleanup
useEffect(() => {
  window.addEventListener('resize', onResize);
  return () => window.removeEventListener('resize', onResize);
}, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:150,sortKey:769},{id:"react-770",number:151,title:"Why must Hooks be called at the top level of a React component?",question:"Why must Hooks be called at the top level of a React component?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Hooks must always be called at the top level of a React function component, never inside loops, conditions, or nested functions. React relies on a linked list internally to track each Hook's state between renders. The order of Hook calls determines which state belongs to which Hook. If a Hook is called conditionally, the order changes between renders, and React assigns state to the wrong Hook, causing unpredictable bugs.


\`\`\`jsx
\`\`\`jsx
// VIOLATION: Hook called conditionally
function BadComponent({ isLoggedIn }) {
  if (isLoggedIn) {
    const [user, setUser] = useState(null); // WRONG: conditional hook call
  }
}

// CORRECT: Always call at top level
function GoodComponent({ isLoggedIn }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    if (isLoggedIn) {
      fetchUser().then(setUser);
    }
  }, [isLoggedIn]); // conditional logic INSIDE the hook, not around it
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:151,sortKey:770},{id:"react-771",number:152,title:"Explain the internal mechanism React uses to track Hook calls.",question:"Explain the internal mechanism React uses to track Hook calls.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"React maintains a linked list of Hook state slots for each component fiber. When a component first renders, React allocates a slot for each Hook call in order. On subsequent renders, it walks the linked list sequentially, matching each `useState`, `useEffect`, etc. to its corresponding slot. This is why the order and count of Hook calls must remain stable. If a Hook is conditionally skipped, every Hook after it shifts up one slot, and React reads the wrong state values.\n\n\n```jsx\n```jsx\n// Pseudocode of React's internal Hook tracking\n// Render 1: useA -> useB -> useC  (slots: [A, B, C])\n// Render 2: useA ->      -> useC  (slots: [A, B, C])\n//           useA reads slot A (correct)\n//           useC reads slot B (WRONG - gets B's old value!)\n```\n```\n",tech:"react",displayNumber:152,sortKey:771},{id:"react-772",number:153,title:"What is the relationship between custom Hooks and the Rules of Hooks?",question:"What is the relationship between custom Hooks and the Rules of Hooks?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Custom Hooks must follow the same Rules of Hooks as built-in Hooks. They must start with the word \`use\` (a naming convention so React's linter can enforce rules), and they can only call other Hooks at the top level of the custom Hook function. Custom Hooks are not special at runtime; they are plain JavaScript functions that call Hooks. The \`use\` prefix signals to developers and the linter that this function follows Hook rules.


\`\`\`jsx
\`\`\`jsx
function useWindowSize() {
  // This is fine because we're at the top level of the custom Hook
  const [size, setSize] = useState({ width: 0, height: 0 });

  useEffect(() => {
    const handleResize = () => setSize({
      width: window.innerWidth,
      height: window.innerHeight,
    });
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return size;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:153,sortKey:772},{id:"react-773",number:154,title:"What happens if you call Hooks conditionally and how does React ESLint plugin catch this?",question:"What happens if you call Hooks conditionally and how does React ESLint plugin catch this?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:"Calling Hooks conditionally causes the \"Rules of Hooks\" violation. The `eslint-plugin-react-hooks` provides the `exhaustive-deps` rule and the `rules-of-hooks` rule. The `rules-of-hooks` rule statically analyzes your code to detect Hook calls inside conditionals, loops, or nested functions. It reports errors at lint time before the bug manifests at runtime. This is one of the most valuable linting rules in the React ecosystem.\n\n\n```jsx\n```jsx\n// ESLint will flag this with \"React Hook 'useState' is called conditionally\"\nfunction Component({ showExtra }) {\n  const [name, setName] = useState('');\n  if (showExtra) {\n    const [extra, setExtra] = useState(''); // Lint error!\n  }\n  const [age, setAge] = useState(0);\n}\n```\n```\n",tech:"react",displayNumber:154,sortKey:773},{id:"react-774",number:155,title:"Compare useState and useReducer: when would you choose one over the other?",question:"Compare useState and useReducer: when would you choose one over the other?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useState\` is ideal for simple, independent state values (strings, booleans, numbers). \`useReducer\` is better when you have complex state logic, multiple related state values, or when the next state depends on the previous one. \`useReducer\` centralizes state transitions in a reducer function, making them predictable and testable. It also avoids the problem of stale closures in setState callbacks since the reducer receives the current state as an argument.


\`\`\`jsx
\`\`\`jsx
// useState for simple state
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}

// useReducer for complex state with multiple sub-values
function todoReducer(state, action) {
  switch (action.type) {
    case 'add':
      return [...state, action.payload];
    case 'toggle':
      return state.map(todo =>
        todo.id === action.id ? { ...todo, done: !todo.done } : todo
      );
    case 'remove':
      return state.filter(todo => todo.id !== action.id);
    default:
      return state;
  }
}

function TodoList() {
  const [todos, dispatch] = useReducer(todoReducer, []);
  return (
    <>
      <button onClick={() => dispatch({ type: 'add', payload: { id: Date.now(), text: 'New', done: false } })}>
        Add Todo
      </button>
      {todos.map(todo => (
        <div key={todo.id} onClick={() => dispatch({ type: 'toggle', id: todo.id })}>
          {todo.text} - {todo.done ? 'Done' : 'Pending'}
        </div>
      ))}
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:155,sortKey:774},{id:"react-775",number:156,title:"Can useState receive a function as its initial value? When is this useful?",question:"Can useState receive a function as its initial value? When is this useful?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Yes. Passing a function to \`useState\` (called a "lazy initializer") causes React to call that function only on the initial render. This is useful when computing the initial state is expensive. Without the lazy initializer, the computation runs on every render even though the result is only used once. The function must be pure and take no arguments.


\`\`\`jsx
\`\`\`jsx
// EXPENSIVE: Computes on every render
function Component1() {
  const [state, setState] = useState(computeExpensiveValue());
}

// OPTIMAL: Computes only on first render
function Component2() {
  const [state, setState] = useState(() => computeExpensiveValue());
}

function computeExpensiveValue() {
  console.log('Computing...');
  return Array.from({ length: 1000000 }, (_, i) => i * 2);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:156,sortKey:775},{id:"react-776",number:157,title:"What is the difference between setState with a value vs. a function updater?",question:"What is the difference between setState with a value vs. a function updater?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When you call \`setState(newValue)\`, React schedules that exact value for the next state. When you call \`setState(prev => newValue)\`, React passes the most recent state to the updater function. The function form is essential when the new state depends on the previous state, especially inside closures like event handlers or \`useEffect\` callbacks that capture stale values. React batches multiple \`setState\` calls, so the function form ensures each update sees the latest state.


\`\`\`jsx
\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // WRONG: Stale closure - all three see count=0
  function handleClickThree() {
    setCount(count + 1);
    setCount(count + 1);
    setCount(count + 1);
    // Result: count = 1 (not 3!)
  }

  // CORRECT: Each updater sees the latest state
  function handleClickCorrect() {
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    // Result: count = 3
  }
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:157,sortKey:776},{id:"react-777",number:158,title:"How does batching work with useState and what changed in React 18?",question:"How does batching work with useState and what changed in React 18?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`React batches state updates to avoid unnecessary re-renders. Before React 18, batching only occurred within React event handlers. Inside \`setTimeout\`, \`Promise.then\`, or native event handlers, each \`setState\` triggered a separate re-render. React 18 introduced "automatic batching" which batches all state updates regardless of where they originate (event handlers, timeouts, promises, native events). This is enabled by default with the new root API (\`createRoot\`).


\`\`\`jsx
\`\`\`jsx
// React 18: This causes only ONE re-render
function handleClick() {
  setTimeout(() => {
    setCount(c => c + 1);  // Batched
    setName('Alice');       // Batched
    setFlag(f => !f);       // Batched
    // Only 1 re-render in React 18 (was 3 in React 17)
  }, 1000);
}

// If you need a synchronous re-render in React 18:
import { flushSync } from 'react-dom';
function handleClick() {
  flushSync(() => {
    setCount(c => c + 1); // Re-render here
  });
  flushSync(() => {
    setName('Alice');      // Another re-render here
  });
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:158,sortKey:777},{id:"react-778",number:159,title:"Explain useEffect dependency array behavior in detail.",question:"Explain useEffect dependency array behavior in detail.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The dependency array controls when \`useEffect\` runs. With no array, the effect runs after every render. With an empty array \`[]\`, it runs only once after the initial render. With specific dependencies \`[a, b]\`, it runs after any render where \`a\` or \`b\` has changed. React uses \`Object.is\` comparison to check if dependencies changed. If you omit a dependency, the effect may use stale values, leading to bugs. The \`exhaustive-deps\` ESLint rule helps catch missing dependencies.


\`\`\`jsx
\`\`\`jsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  // Runs only when userId changes
  useEffect(() => {
    let cancelled = false;
    fetchUser(userId).then(data => {
      if (!cancelled) setUser(data);
    });
    return () => { cancelled = true; };
  }, [userId]); // <-- dependency array

  // WARNING: Every render (no dependency array)
  useEffect(() => {
    document.title = \`User: \${user?.name}\`;
  });

  // Runs once on mount (empty dependency array)
  useEffect(() => {
    analytics.track('page_view');
  }, []);

  return <div>{user?.name}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:159,sortKey:778},{id:"react-779",number:160,title:"What is the purpose of cleanup functions in useEffect?",question:"What is the purpose of cleanup functions in useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Cleanup functions prevent memory leaks, remove event listeners, cancel subscriptions, and abort requests. React runs the cleanup function before the effect re-runs (on dependency change) and when the component unmounts. This lifecycle pattern ensures resources are properly released. Notable use cases include clearing timers, unsubscribing from WebSocket connections, removing DOM event listeners, and aborting fetch requests.


\`\`\`jsx
\`\`\`jsx
function Timer({ interval }) {
  useEffect(() => {
    const id = setInterval(() => {
      console.log('tick');
    }, interval);

    // Cleanup: removes the old interval before creating a new one
    return () => clearInterval(id);
  }, [interval]);

  useEffect(() => {
    const controller = new AbortController();

    fetch('/api/data', { signal: controller.signal })
      .then(res => res.json())
      .then(data => console.log(data));

    // Cleanup: abort in-flight request
    return () => controller.abort();
  }, []);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:160,sortKey:779},{id:"react-780",number:161,title:"What is the execution order of useEffect cleanup and setup?",question:"What is the execution order of useEffect cleanup and setup?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When dependencies change, React follows this order: (1) cleanup of the previous render's effect runs, then (2) the new effect runs. On unmount, only cleanup runs. On mount, only setup runs. Multiple effects in the same component run their cleanups first (in declaration order), then their setups (in declaration order). This means cleanup for all effects happens before any new effect setup.


\`\`\`jsx
\`\`\`jsx
useEffect(() => {
  console.log('Effect 1 setup');
  return () => console.log('Effect 1 cleanup');
}, [dep]);

useEffect(() => {
  console.log('Effect 2 setup');
  return () => console.log('Effect 2 cleanup');
}, [dep]);

// On dep change, output order:
// "Effect 1 cleanup" -> "Effect 2 cleanup" -> "Effect 1 setup" -> "Effect 2 setup"

// On unmount:
// "Effect 1 cleanup" -> "Effect 2 cleanup"
\`\`\`
\`\`\`
`,tech:"react",displayNumber:161,sortKey:780},{id:"react-781",number:162,title:"Compare useEffect, useLayoutEffect, and useInsertionEffect.",question:"Compare useEffect, useLayoutEffect, and useInsertionEffect.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`- **useEffect**: Runs asynchronously after the browser paints. Use for most side effects (data fetching, subscriptions, logging). Does not block the visual update.
- **useLayoutEffect**: Runs synchronously after DOM mutations but before the browser paints. Use when you need to read or modify layout (measure DOM elements, position tooltips). Blocks painting, so keep it fast.
- **useInsertionEffect** (React 18): Runs synchronously after DOM mutations but before layout effects. Designed specifically for CSS-in-JS libraries to inject \`<style>\` tags without causing layout recalculations. Rarely used in application code.


\`\`\`jsx
\`\`\`jsx
// useInsertionEffect - for CSS-in-JS libraries
import { useInsertionEffect } from 'react';
function useStyledComponent(styles) {
  useInsertionEffect(() => {
    const style = document.createElement('style');
    style.textContent = styles;
    document.head.appendChild(style);
    return () => style.remove();
  }, [styles]);
}

// useLayoutEffect - for measuring DOM
function Tooltip({ text, targetRef }) {
  const tooltipRef = useRef(null);
  useLayoutEffect(() => {
    const tooltip = tooltipRef.current;
    const target = targetRef.current;
    if (tooltip && target) {
      const rect = target.getBoundingClientRect();
      tooltip.style.top = \`\${rect.bottom + 8}px\`;
      tooltip.style.left = \`\${rect.left}px\`;
    }
  }, [text, targetRef]);

  return <div ref={tooltipRef} className="tooltip">{text}</div>;
}

// useEffect - standard side effects
function ChatRoom({ roomId }) {
  useEffect(() => {
    const connection = createConnection(roomId);
    connection.connect();
    return () => connection.disconnect();
  }, [roomId]);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:162,sortKey:781},{id:"react-782",number:163,title:"When would you actually need useLayoutEffect over useEffect?",question:"When would you actually need useLayoutEffect over useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`You need \`useLayoutEffect\` when you must measure or mutate the DOM and need the result before the browser paints. Common scenarios: measuring element dimensions for tooltip positioning, preventing visual flicker when changing styles based on measurements, and synchronizing animations. If you use \`useEffect\` for these, users may see a brief flash of incorrect layout (FOUC) because the browser paints before your measurement-based style is applied.


\`\`\`jsx
\`\`\`jsx
function Popover({ targetRect, children }) {
  const popoverRef = useRef(null);
  const [position, setPosition] = useState({ top: 0, left: 0 });

  // Must use useLayoutEffect to avoid visual flicker
  useLayoutEffect(() => {
    const popover = popoverRef.current;
    if (!popover) return;

    const { height, width } = popover.getBoundingClientRect();
    setPosition({
      top: targetRect.bottom + 8,
      left: targetRect.left + (targetRect.width - width) / 2,
    });
  }, [targetRect]);

  return (
    <div
      ref={popoverRef}
      style={{ position: 'absolute', top: position.top, left: position.left }}
    >
      {children}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:163,sortKey:782},{id:"react-783",number:164,title:"What problem does useInsertionEffect solve and who should use it?",question:"What problem does useInsertionEffect solve and who should use it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useInsertionEffect\` solves a specific problem for CSS-in-JS libraries. When a library injects \`<style>\` tags at render time, it can cause layout thrashing because the browser must recalculate styles after insertion but before paint. \`useInsertionEffect\` runs after DOM mutations but before \`useLayoutEffect\`, letting libraries inject styles before layout measurements happen. Application developers rarely need this directly; it is primarily for library authors.


\`\`\`jsx
\`\`\`jsx
// Typical CSS-in-JS library usage (simplified)
import { useInsertionEffect, useRef } from 'react';

let counter = 0;
function useCSS(styleRules) {
  const styleRef = useRef(null);

  useInsertionEffect(() => {
    if (!styleRef.current) {
      const style = document.createElement('style');
      style.dataset.css = \`css-\${counter++}\`;
      style.textContent = styleRules;
      document.head.appendChild(style);
      styleRef.current = style;
    }
  }, [styleRules]);

  useInsertionEffect(() => {
    return () => {
      if (styleRef.current) {
        styleRef.current.remove();
        styleRef.current = null;
      }
    };
  }, []);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:164,sortKey:783},{id:"react-784",number:165,title:"Explain useMemo in depth and common misuse patterns.",question:"Explain useMemo in depth and common misuse patterns.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useMemo\` caches a computed value between renders. It only recalculates when its dependencies change, using \`Object.is\` comparison. Common misuse: memoizing simple calculations that are cheap (adds unnecessary overhead), memoizing primitives that React already handles efficiently, and over-memoizing everything "just in case." \`useMemo\` is valuable for expensive computations, creating stable references for child components, and preventing unnecessary re-renders of memoized children.


\`\`\`jsx
\`\`\`jsx
function DataGrid({ rows, filter }) {
  // GOOD: Expensive computation worth memoizing
  const filteredRows = useMemo(() => {
    return rows.filter(row =>
      row.name.toLowerCase().includes(filter.toLowerCase())
    ).sort((a, b) => a.name.localeCompare(b.name));
  }, [rows, filter]);

  // BAD: Unnecessary memoization (cheap operation)
  const greeting = useMemo(() => \`Hello, \${name}!\`, [name]); // Overkill

  // GOOD: Stable reference for child component
  const columnConfig = useMemo(() => ({
    sortable: true,
    resizable: true,
    columns: ['id', 'name', 'email'],
  }), []); // Stable across renders, prevents child re-renders

  return <Grid rows={filteredRows} config={columnConfig} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:165,sortKey:784},{id:"react-785",number:166,title:"Explain useCallback in depth and when it matters.",question:"Explain useCallback in depth and when it matters.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useCallback\` returns a memoized version of a callback function. It only creates a new function when its dependencies change. This is useful when passing callbacks to memoized child components (\`React.memo\`) to prevent unnecessary re-renders. However, \`useCallback\` adds overhead; if the child is not memoized or the callback is not passed as a prop, \`useCallback\` provides no benefit. Modern React patterns increasingly favor \`useReducer\` or state machines over \`useCallback\`.


\`\`\`jsx
\`\`\`jsx
// Without useCallback - child re-renders every time
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = () => console.log('clicked'); // New reference each render
  return <MemoizedChild onClick={handleClick} />; // Re-renders every time
}

// With useCallback - child skips re-renders
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []); // Stable reference
  return <MemoizedChild onClick={handleClick} />; // Only re-renders when deps change
}

const MemoizedChild = React.memo(({ onClick }) => {
  console.log('Child rendered');
  return <button onClick={onClick}>Click me</button>;
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:166,sortKey:785},{id:"react-786",number:167,title:"When is useCallback NOT worth using?",question:"When is useCallback NOT worth using?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Skip \`useCallback\` when: (1) the child component is not wrapped in \`React.memo\` and the callback is not used as a dependency elsewhere, (2) the component re-renders rarely, (3) the component tree is shallow so re-render cost is negligible, or (4) you're memoizing a function that is recreated on every render due to unstable dependencies. Premature optimization with \`useCallback\` adds cognitive overhead and can make code harder to read without measurable performance benefit.


\`\`\`jsx
\`\`\`jsx
// NO NEED for useCallback here - SimpleComponent is not memoized
function Parent() {
  const [count, setCount] = useState(0);

  // This useCallback provides zero benefit
  const onClick = useCallback(() => {
    setCount(c => c + 1);
  }, []);

  return <SimpleComponent onClick={onClick} />;
}

function SimpleComponent({ onClick }) {
  return <button onClick={onClick}>Click</button>;
  // Re-renders every time Parent re-renders regardless of callback reference
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:167,sortKey:786},{id:"react-787",number:168,title:"Compare useMemo and useCallback. How are they related internally?",question:"Compare useMemo and useCallback. How are they related internally?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"`useCallback` is essentially `useMemo` for functions. Internally, `useCallback(fn, deps)` is equivalent to `useMemo(() => fn, deps)`. Both use the same dependency comparison mechanism (`Object.is`). The key difference: `useMemo` returns the result of calling a function, while `useCallback` returns the function itself. Use `useMemo` for computed values and `useCallback` for stable function references.\n\n\n```jsx\n```jsx\n// These two are equivalent:\nconst memoizedFn = useCallback(() => doSomething(a, b), [a, b]);\nconst memoizedFn = useMemo(() => () => doSomething(a, b), [a, b]);\n\n// Practical difference:\n// useMemo - caches the RESULT of a computation\nconst expensiveResult = useMemo(() => computeExpensive(a, b), [a, b]);\n\n// useCallback - caches the FUNCTION itself\nconst handleSubmit = useCallback((data) => {\n  submitForm(data, a, b);\n}, [a, b]);\n```\n```\n",tech:"react",displayNumber:168,sortKey:787},{id:"react-788",number:169,title:"How does React.memo interact with useMemo and useCallback?",question:"How does React.memo interact with useMemo and useCallback?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`React.memo\` performs a shallow comparison of a component's props to determine if it needs to re-render. Without \`useMemo\`/\`useCallback\`, new object references and function references created on each render cause \`React.memo\` to see "changed" props, defeating the purpose. \`useCallback\` provides stable function references, and \`useMemo\` provides stable object references, ensuring \`React.memo\` correctly skips re-renders when the actual values haven't changed.


\`\`\`jsx
\`\`\`jsx
function Parent() {
  const [count, setCount] = useState(0);
  const [text, setText] = useState('');

  // Without useMemo/useCallback, these create new references each render
  // defeating React.memo on ChildComponent
  const config = useMemo(() => ({ theme: 'dark' }), []);
  const handleClick = useCallback(() => {}, []);

  return (
    <>
      <ChildComponent config={config} onClick={handleClick} />
      <input value={text} onChange={e => setText(e.target.value)} />
    </>
  );
}

const ChildComponent = React.memo(({ config, onClick }) => {
  console.log('Child rendered');
  return <div>{config.theme}</div>;
});
// Without useMemo/useCallback on config/onClick, child re-renders on every Parent render
\`\`\`
\`\`\`
`,tech:"react",displayNumber:169,sortKey:788},{id:"react-789",number:170,title:"What is useRef and how does it differ from a regular variable?",question:"What is useRef and how does it differ from a regular variable?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`\`useRef\` returns a mutable \`.current\` property that persists across renders without triggering re-renders when changed. Unlike a regular variable inside a component (which is re-created on each render), the \`.current\` value survives. Unlike \`useState\`, changing \`.current\` does not cause a re-render. This makes \`useRef\` ideal for storing DOM references, timers, previous values, and any mutable value that should persist but not drive rendering.


\`\`\`jsx
\`\`\`jsx
function Stopwatch() {
  const intervalRef = useRef(null);
  const [elapsed, setElapsed] = useState(0);

  const start = () => {
    intervalRef.current = setInterval(() => {
      setElapsed(prev => prev + 1);
    }, 1000);
  };

  const stop = () => {
    clearInterval(intervalRef.current); // Access persisted value
    intervalRef.current = null;
  };

  return (
    <div>
      <p>Elapsed: {elapsed}s</p>
      <button onClick={start}>Start</button>
      <button onClick={stop}>Stop</button>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:170,sortKey:789},{id:"react-790",number:171,title:"How do you access a DOM element using useRef?",question:"How do you access a DOM element using useRef?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Pass a \`useRef\` return value to a component's \`ref\` prop. After the component mounts, \`ref.current\` contains the underlying DOM node. This is the primary way to interact with DOM elements directly in React: measuring dimensions, focusing inputs, managing scroll positions, or integrating with non-React libraries. The ref is set after the component renders and DOM is committed.


\`\`\`jsx
\`\`\`jsx
function SearchBox() {
  const inputRef = useRef(null);

  useEffect(() => {
    // Auto-focus the input on mount
    inputRef.current.focus();
  }, []);

  const handleSubmit = (e) => {
    e.preventDefault();
    const value = inputRef.current.value;
    console.log('Searching for:', value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input ref={inputRef} type="search" placeholder="Search..." />
      <button type="submit">Search</button>
    </form>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:171,sortKey:790},{id:"react-791",number:172,title:"How can useRef be used to store previous values?",question:"How can useRef be used to store previous values?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`A common pattern uses \`useRef\` to store the previous render's value by comparing it in a \`useLayoutEffect\`. Since ref updates don't trigger re-renders, it's the right tool for tracking "what was the value last time." This is useful for animations that transition between states, logging changes, or conditional logic based on previous vs. current values.


\`\`\`jsx
\`\`\`jsx
function usePrevious(value) {
  const ref = useRef();

  useLayoutEffect(() => {
    ref.current = value;
  }, [value]);

  return ref.current; // Returns the value from the PREVIOUS render
}

function Counter() {
  const [count, setCount] = useState(0);
  const prevCount = usePrevious(count);

  return (
    <div>
      <p>Now: {count}, Before: {prevCount}</p>
      <button onClick={() => setCount(c => c + 1)}>Increment</button>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:172,sortKey:791},{id:"react-792",number:173,title:"What is the difference between useRef(null) and useRef(undefined)?",question:"What is the difference between useRef(null) and useRef(undefined)?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:'Both create a ref with an initial value. The key difference is semantic: `useRef(null)` explicitly signals "no value yet" and is the conventional choice for DOM refs. `useRef(undefined)` works but is less idiomatic. Neither affects rendering or causes bugs. The `.current` property is mutable regardless of initial value. Use `null` for DOM refs and an appropriate initial value (or `undefined`) for general mutable variables.\n\n\n```jsx\n```jsx\n// DOM ref - always use null\nconst divRef = useRef(null);\n\n// Mutable variable with meaningful initial value\nconst timerId = useRef(0);\n\n// Mutable variable without initial value\nconst previousValue = useRef(); // undefined by default\n\n// Both work identically at runtime\nuseEffect(() => {\n  if (divRef.current) { /* DOM node available */ }\n  if (previousValue.current !== undefined) { /* has been set */ }\n}, []);\n```\n```\n',tech:"react",displayNumber:173,sortKey:792},{id:"react-793",number:174,title:"Explain useReducer's dispatch mechanism and how it differs from useState.",question:"Explain useReducer's dispatch mechanism and how it differs from useState.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useReducer\` dispatches action objects to a reducer function that returns the next state. Unlike \`useState\` where you call \`setState\` directly, \`useReducer\` separates "what happened" (action) from "how state changes" (reducer). This makes state transitions explicit, testable, and predictable. The reducer is a pure function receiving \`(state, action)\` and returning new state. \`dispatch\` is stable across renders (unlike setState, which is also stable, but the reducer pattern adds structure).


\`\`\`jsx
\`\`\`jsx
function formReducer(state, action) {
  switch (action.type) {
    case 'SET_FIELD':
      return { ...state, [action.field]: action.value };
    case 'RESET':
      return action.initialState;
    case 'SUBMIT_START':
      return { ...state, isSubmitting: true, error: null };
    case 'SUBMIT_SUCCESS':
      return { ...state, isSubmitting: false, isSubmitted: true };
    case 'SUBMIT_ERROR':
      return { ...state, isSubmitting: false, error: action.error };
    default:
      throw new Error(\`Unknown action: \${action.type}\`);
  }
}

function ContactForm() {
  const [state, dispatch] = useReducer(formReducer, {
    name: '', email: '', isSubmitting: false, isSubmitted: false, error: null,
  });

  return (
    <form onSubmit={async (e) => {
      e.preventDefault();
      dispatch({ type: 'SUBMIT_START' });
      try {
        await submitForm(state);
        dispatch({ type: 'SUBMIT_SUCCESS' });
      } catch (err) {
        dispatch({ type: 'SUBMIT_ERROR', error: err.message });
      }
    }}>
      <input value={state.name} onChange={e => dispatch({ type: 'SET_FIELD', field: 'name', value: e.target.value })} />
    </form>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:174,sortKey:793},{id:"react-794",number:175,title:"When should you extract logic into a custom Hook?",question:"When should you extract logic into a custom Hook?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Extract a custom Hook when you have: (1) component logic that is duplicated across multiple components, (2) complex stateful logic you want to test or reason about independently, (3) side-effect logic that obscures the component's render output, or (4) when you want to follow the "single responsibility" principle at the component level. A custom Hook is just a function that uses other Hooks; the naming convention (\`use*\`) signals to React's tooling that it follows Hook rules.


\`\`\`jsx
\`\`\`jsx
// Custom Hook: encapsulates data fetching logic
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error(\`HTTP \${res.status}\`);
        return res.json();
      })
      .then(data => { if (!cancelled) { setData(data); setLoading(false); } })
      .catch(err => { if (!cancelled) { setError(err.message); setLoading(false); } });
    return () => { cancelled = true; };
  }, [url]);

  return { data, loading, error };
}

// Usage: Component is clean and focused on rendering
function UserList() {
  const { data, loading, error } = useFetch('/api/users');

  if (loading) return <Spinner />;
  if (error) return <ErrorMessage message={error} />;
  return <ul>{data.map(u => <li key={u.id}>{u.name}</li>)}</ul>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:175,sortKey:794},{id:"react-795",number:176,title:"What are the best practices for naming custom Hooks?",question:"What are the best practices for naming custom Hooks?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:"Always prefix custom Hooks with `use`. This is not just convention; it is required for React's linter to enforce the Rules of Hooks. Use descriptive, verb-based names that indicate what the Hook does: `useFetch`, `useLocalStorage`, `useDebounce`. Avoid generic names like `useHelper` or `useUtils`. A good custom Hook name describes the stateful behavior it encapsulates, not the implementation details.\n\n\n```jsx\n```jsx\n// GOOD: Clear purpose in the name\nfunction useDebounce(value, delay) { /* ... */ }\nfunction useMediaQuery(query) { /* ... */ }\nfunction useIntersectionObserver(options) { /* ... */ }\nfunction useLocalStorage(key, initialValue) { /* ... */ }\n\n// BAD: Vague names\nfunction useThing() { /* ... */ }\nfunction useHelper() { /* ... */ }\nfunction useData() { /* ... */ }\n```\n```\n",tech:"react",displayNumber:176,sortKey:795},{id:"react-796",number:177,title:"Can custom Hooks return anything? What are the conventions?",question:"Can custom Hooks return anything? What are the conventions?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Custom Hooks can return any value: primitives, objects, arrays, functions, or combinations. Common conventions: return an array for simple state + setter pairs (mimicking \`useState\`), return an object when exposing multiple named values, and return a function when the Hook encapsulates an action. The caller decides the naming via destructuring. The Hook's return shape should be documented through consistent, predictable patterns.


\`\`\`jsx
\`\`\`jsx
// Array return - allows custom naming via destructuring (like useState)
function useToggle(initial = false) {
  const [value, setValue] = useState(initial);
  const toggle = useCallback(() => setValue(v => !v), []);
  return [value, toggle];
}
const [isOpen, toggleOpen] = useToggle(false);

// Object return - named properties
function useMediaQuery(query) {
  const [matches, setMatches] = useState(() => window.matchMedia(query).matches);
  useEffect(() => {
    const mql = window.matchMedia(query);
    const handler = (e) => setMatches(e.matches);
    mql.addEventListener('change', handler);
    return () => mql.removeEventListener('change', handler);
  }, [query]);
  return { matches, matchesExplicitly: matches };
}
const { matches: isMobile } = useMediaQuery('(max-width: 768px)');
\`\`\`
\`\`\`
`,tech:"react",displayNumber:177,sortKey:796},{id:"react-797",number:178,title:"How do you handle side effects with cleanup in a custom Hook?",question:"How do you handle side effects with cleanup in a custom Hook?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks follow the same cleanup pattern as built-in Hooks. Return a cleanup function from \`useEffect\` inside the custom Hook. The cleanup runs when dependencies change or the component unmounts. Always handle cancellation, cleanup, and resource release in custom Hooks that manage subscriptions, timers, event listeners, or async operations to prevent memory leaks in consuming components.


\`\`\`jsx
\`\`\`jsx
function useEventListener(eventName, handler, element = window) {
  const savedHandler = useRef(handler);
  savedHandler.current = handler;

  useEffect(() => {
    const listener = (event) => savedHandler.current(event);
    element.addEventListener(eventName, listener);
    return () => element.removeEventListener(eventName, listener);
  }, [eventName, element]);
}

// Usage
function Component() {
  useEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeModal();
  });
  return <div>Press Escape to close</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:178,sortKey:797},{id:"react-798",number:179,title:"What are common pitfalls when creating custom Hooks?",question:"What are common pitfalls when creating custom Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Common pitfalls include: (1) calling Hooks conditionally inside the custom Hook, (2) not handling cleanup (memory leaks), (3) creating stable identity issues by returning new object/array references on every render, (4) tightly coupling the Hook to specific props rather than accepting flexible parameters, (5) forgetting that the Hook's state is per-component-instance (not shared), and (6) over-abstracting simple logic into a Hook when a utility function suffices.


\`\`\`jsx
\`\`\`jsx
// PITFALL: Returns new reference every render
function useUserData(userId) {
  const [user, setUser] = useState(null);
  // BAD: Creates new object every render
  return { user, setUser };
  // GOOD: Memoize the return value if consumers depend on reference equality
}

// PITFALL: Not handling cleanup
function useInterval(callback, delay) {
  useEffect(() => {
    // BAD: No cleanup - interval leaks
    setInterval(callback, delay);
  }, [callback, delay]);
}

// CORRECT: Cleanup handled
function useInterval(callback, delay) {
  const savedCallback = useRef(callback);
  savedCallback.current = callback;

  useEffect(() => {
    const id = setInterval(() => savedCallback.current(), delay);
    return () => clearInterval(id);
  }, [delay]);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:179,sortKey:798},{id:"react-799",number:180,title:"Explain useContext and its performance implications.",question:"Explain useContext and its performance implications.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useContext\` reads the nearest context value from the component tree. The problem: when a context value changes, every component that calls \`useContext\` for that context re-renders, even if the component only uses part of the value. This is the "context tearing" or "context consumer re-render" problem. Solutions include: splitting contexts by update frequency, memoizing context values in the provider, and using composition to reduce context scope.


\`\`\`jsx
\`\`\`jsx
// PROBLEM: All consumers re-render when ANY value changes
const AppContext = React.createContext();

function AppProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const [user, setUser] = useState(null);
  const [locale, setLocale] = useState('en');

  // Every context consumer re-renders when any value changes
  return (
    <AppContext.Provider value={{ theme, setTheme, user, setUser, locale, setLocale }}>
      {children}
    </AppContext.Provider>
  );
}

// BETTER: Split into separate contexts
const ThemeContext = React.createContext();
const UserContext = React.createContext();
const LocaleContext = React.createContext();

function AppProvider({ children }) {
  return (
    <ThemeContext.Provider value={useMemo(() => ({ theme, setTheme }), [theme])}>
      <UserContext.Provider value={useMemo(() => ({ user, setUser }), [user])}>
        <LocaleContext.Provider value={useMemo(() => ({ locale, setLocale }), [locale])}>
          {children}
        </LocaleContext.Provider>
      </UserContext.Provider>
    </ThemeContext.Provider>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:180,sortKey:799},{id:"react-800",number:181,title:"How can you optimize context performance in React?",question:"How can you optimize context performance in React?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Several strategies: (1) Split contexts by update frequency so theme changes don't re-render user-dependent components. (2) Memoize the context value in the provider using \`useMemo\` to prevent unnecessary re-renders. (3) Move state down and use composition to limit how many components need context. (4) Use state management libraries (Zustand, Jotai) that provide fine-grained subscriptions. (5) Use the "selector" pattern with \`useSyncExternalStore\` or libraries like \`use-context-selector\`.


\`\`\`jsx
\`\`\`jsx
// Strategy 1: Memoized context value
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const value = useMemo(() => ({ theme, setTheme }), [theme]);
  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

// Strategy 2: Split by update frequency
// ThemeContext (rarely changes) vs RealTimeContext (changes frequently)

// Strategy 3: Move state down
function App() {
  return (
    <Header /> {/* Does not need theme context */}
    <main>
      <ThemeProvider>
        <Dashboard /> {/* Needs theme context - scoped here */}
      </ThemeProvider>
    </main>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:181,sortKey:800},{id:"react-801",number:182,title:"What is the Context Provider pattern and how does it relate to useContext?",question:"What is the Context Provider pattern and how does it relate to useContext?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The Provider pattern wraps a subtree of components with a context provider that supplies a value. Any descendant component can access that value via \`useContext\`. The Provider's value prop is what consumers receive. Providers can be nested, and inner providers override outer ones for the same context. This creates a natural dependency injection system in React without prop drilling.


\`\`\`jsx
\`\`\`jsx
const AuthContext = React.createContext(null);

function AuthProvider({ children }) {
  const [user, setUser] = useState(null);

  const login = useCallback(async (credentials) => {
    const userData = await api.login(credentials);
    setUser(userData);
  }, []);

  const logout = useCallback(() => setUser(null), []);

  const value = useMemo(() => ({ user, login, logout }), [user, login, logout]);

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
}

// Nested override example
function AdminSection() {
  return (
    <AuthProvider> {/* Overrides parent AuthContext */}
      <AdminDashboard />
    </AuthProvider>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:182,sortKey:801},{id:"react-802",number:183,title:"Explain useImperativeHandle and its use cases.",question:"Explain useImperativeHandle and its use cases.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useImperativeHandle\` customizes the instance value exposed when a parent component uses \`ref\` to access a child component. Instead of exposing the entire DOM node, you expose only specific methods and properties. This follows the principle of minimal exposure and is useful for components that need to provide a programmatic API: form validation, focus management, animation controls. Always pair with \`forwardRef\`.


\`\`\`jsx
\`\`\`jsx
const FancyInput = forwardRef(function FancyInput({ placeholder }, ref) {
  const inputRef = useRef(null);
  const [value, setValue] = useState('');

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus(),
    clear: () => { setValue(''); inputRef.current.value = ''; },
    getValue: () => value,
  }), [value]); // Recreate when value changes

  return (
    <input
      ref={inputRef}
      value={value}
      onChange={e => setValue(e.target.value)}
      placeholder={placeholder}
    />
  );
});

// Parent component
function Parent() {
  const inputRef = useRef();

  return (
    <>
      <FancyInput ref={inputRef} placeholder="Type here" />
      <button onClick={() => inputRef.current.focus()}>Focus</button>
      <button onClick={() => inputRef.current.clear()}>Clear</button>
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:183,sortKey:802},{id:"react-803",number:184,title:"How does forwardRef work and why is it needed with useImperativeHandle?",question:"How does forwardRef work and why is it needed with useImperativeHandle?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"By default, function components don't receive `ref` as a prop. `forwardRef` creates a component that accepts a `ref` parameter and forwards it to a child element. `useImperativeHandle` needs access to the ref to customize what `.current` resolves to. Without `forwardRef`, you can't pass the ref into the component for `useImperativeHandle` to intercept. Note: React 19 eliminates the need for `forwardRef` as `ref` becomes a regular prop.\n\n\n```jsx\n```jsx\n// React 18 pattern: forwardRef required\nconst TextInput = forwardRef(function TextInput(props, ref) {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n  }));\n\n  return <input ref={inputRef} {...props} />;\n});\n\n// React 19 pattern: ref is a regular prop\nfunction TextInput({ ref, ...props }) {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n  }));\n\n  return <input ref={inputRef} {...props} />;\n}\n```\n```\n",tech:"react",displayNumber:184,sortKey:803},{id:"react-804",number:185,title:"What is useId and when should you use it?",question:"What is useId and when should you use it?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:'`useId` generates a unique, stable ID that works consistently between server and client rendering (hydration-safe). It solves the problem of generating unique IDs for accessibility attributes (`htmlFor`, `aria-labelledby`, `aria-describedby`) and form elements. Before `useId`, developers used random IDs or libraries, which caused hydration mismatches in SSR. `useId` returns the same ID on both server and client.\n\n\n```jsx\n```jsx\nfunction FormField({ label }) {\n  const id = useId(); // Unique per component instance, consistent SSR/CSR\n\n  return (\n    <div>\n      <label htmlFor={id}>{label}</label>\n      <input id={id} type="text" aria-describedby={`${id}-help`} />\n      <span id={`${id}-help`}>Enter your {label.toLowerCase()}</span>\n    </div>\n  );\n}\n\n// Generates IDs like ":r1:", ":r2:", etc.\n// Same ID on server and client for hydration safety\n```\n```\n',tech:"react",displayNumber:185,sortKey:804},{id:"react-805",number:186,title:"What is the difference between useId and a random ID generator?",question:"What is the difference between useId and a random ID generator?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useId\` produces the same ID on server and client, which is essential for hydration. Random generators (\`Math.random()\`, \`uuid\`) produce different values on server vs. client, causing hydration mismatches that React warns about and can lead to subtle bugs. \`useId\` is also React-aware: it generates IDs that don't collide with other \`useId\` calls in the same render, even across different components.


\`\`\`jsx
\`\`\`jsx
// PROBLEM: Random IDs cause hydration mismatch
function BadField() {
  const id = Math.random().toString(36).substr(2, 9); // Different on server vs client
  return <input id={id} />;
}

// CORRECT: useId is hydration-safe
function GoodField() {
  const id = useId(); // Same value on server and client
  return <input id={id} />;
}

// Can generate multiple related IDs
function GoodField() {
  const id = useId();
  return (
    <>
      <input id={id} aria-describedby={\`\${id}-error\`} />
      <span id={\`\${id}-error\`}>Error message</span>
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:186,sortKey:805},{id:"react-806",number:187,title:"Can useId be used for list keys? Why or why not?",question:"Can useId be used for list keys? Why or why not?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`No. \`useId\` should not be used for list keys. Keys should identify list items based on their data (like a database ID), not their position or a generated ID. \`useId\` generates the same ID for the same component across renders, but it doesn't represent item identity. Using it as a key would cause React to reuse component instances incorrectly when list items reorder, are added, or removed.


\`\`\`jsx
\`\`\`jsx
// WRONG: useId for keys
function BadList({ items }) {
  return items.map(item => {
    const id = useId(); // VIOLATION: Hook called in a loop!
    return <li key={id}>{item.name}</li>;
  });
}

// CORRECT: Use data-based keys
function GoodList({ items }) {
  return items.map(item => (
    <li key={item.id}>{item.name}</li> // Use the item's own ID
  ));
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:187,sortKey:806},{id:"react-807",number:188,title:"Explain useTransition and its benefits for UX.",question:"Explain useTransition and its benefits for UX.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useTransition\` lets you mark state updates as non-urgent transitions, keeping the UI responsive during expensive re-renders. It returns a \`startTransition\` function and an \`isPending\` boolean. Inside \`startTransition\`, React can interrupt the update to handle urgent interactions (typing, clicking). This prevents janky UI when switching between expensive views or filtering large lists.


\`\`\`jsx
\`\`\`jsx
function SearchResults() {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);
  const [isPending, startTransition] = useTransition();

  const handleSearch = (e) => {
    const value = e.target.value;
    setQuery(value); // Urgent: update input immediately

    startTransition(() => {
      setResults(filterLargeList(value)); // Non-urgent: can be interrupted
    });
  };

  return (
    <div>
      <input value={query} onChange={handleSearch} />
      {isPending && <Spinner />}
      <ResultList results={results} />
    </div>
  );
}

function filterLargeList(query) {
  // Expensive computation - won't block input
  return hugeList.filter(item => item.name.includes(query));
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:188,sortKey:807},{id:"react-808",number:189,title:"Explain useDeferredValue and how it differs from useTransition.",question:"Explain useDeferredValue and how it differs from useTransition.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useDeferredValue\` defers updating a derived value, showing a stale version while the new value is being computed. Unlike \`useTransition\` (which wraps a state update), \`useDeferredValue\` takes a value and returns a deferred version of it. It is useful when you can't control the state update (e.g., a prop from a parent). \`useTransition\` requires a synchronous callback; \`useDeferredValue\` works with any value source.


\`\`\`jsx
\`\`\`jsx
// useDeferredValue: defers a derived value
function SearchPage({ query }) {
  const deferredQuery = useDeferredValue(query);
  const isStale = query !== deferredQuery;

  return (
    <div style={{ opacity: isStale ? 0.5 : 1 }}>
      <ResultList query={deferredQuery} />
    </div>
  );
}

// useTransition: wraps a state update
function TabContainer() {
  const [tab, setTab] = useState('home');
  const [isPending, startTransition] = useTransition();

  const selectTab = (nextTab) => {
    startTransition(() => {
      setTab(nextTab);
    });
  };

  return (
    <div>
      <TabBar onSelect={selectTab} />
      <div style={{ opacity: isPending ? 0.7 : 1 }}>
        <TabContent tab={tab} />
      </div>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:189,sortKey:808},{id:"react-809",number:190,title:"What are the prerequisites for using useTransition and useDeferredValue?",question:"What are the prerequisites for using useTransition and useDeferredValue?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Both features require React 18+ and the concurrent rendering mode, which is enabled by default with \`createRoot\` (the new client root API). If you're using the legacy \`ReactDOM.render()\`, concurrent features are not available. Both work best with components that have expensive rendering paths. They are not magic performance boosters; they only help when React can interrupt and prioritize rendering work.


\`\`\`jsx
\`\`\`jsx
// Old way: No concurrent features
import ReactDOM from 'react-dom';
ReactDOM.render(<App />, document.getElementById('root'));

// New way: Concurrent features enabled
import { createRoot } from 'react-dom/client';
const root = createRoot(document.getElementById('root'));
root.render(<App />);

// Now useTransition and useDeferredValue work
function App() {
  const [isPending, startTransition] = useTransition();
  const deferredValue = useDeferredValue(someValue);
  return <div>{/* ... */}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:190,sortKey:809},{id:"react-810",number:191,title:"How does batching work differently inside startTransition?",question:"How does batching work differently inside startTransition?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Inside \`startTransition\`, all state updates are batched into a single render, and the transition can be interrupted by urgent updates. This is different from React 18's automatic batching, which batches updates but doesn't provide interruption semantics. Transitions are deprioritized, so typing or clicking handlers can update the UI immediately while the transition processes in the background. This is what makes the UI feel responsive during expensive updates.


\`\`\`jsx
\`\`\`jsx
function Tabs() {
  const [tab, setTab] = useState('home');
  const [isPending, startTransition] = useTransition();

  const onTabSelect = (nextTab) => {
    startTransition(() => {
      // These updates are batched and deprioritized
      setTab(nextTab);
      setFilter(null);
      setSortBy('date');
    });
    // Urgent updates (like hover effects) still process immediately
  };

  return (
    <div>
      <TabBar onSelect={onTabSelect} />
      {isPending ? <LoadingSkeleton /> : <TabContent tab={tab} />}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:191,sortKey:810},{id:"react-811",number:192,title:"Can you nest multiple useTransition calls?",question:"Can you nest multiple useTransition calls?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Yes, you can call \`useTransition\` in multiple components, and each creates its own independent transition. Each component's \`isPending\` tracks only its own transition's state. However, nested transitions interact: an outer transition can be interrupted by an inner urgent update. This is fine and is by design; transitions form a priority hierarchy.


\`\`\`jsx
\`\`\`jsx
function Parent() {
  const [isPending: parentPending, startTransition: startParent] = useTransition();

  return (
    <div>
      {parentPending && <GlobalSpinner />}
      <ChildA />
      <ChildB />
    </div>
  );
}

function ChildA() {
  const [isPending: childPending, startTransition: startChild] = useTransition();

  return (
    <section style={{ opacity: childPending ? 0.5 : 1 }}>
      <button onClick={() => startChild(() => updateDataA())}>
        Update A
      </button>
    </section>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:192,sortKey:811},{id:"react-812",number:193,title:"Explain the concept of Suspense-compatible Hooks.",question:"Explain the concept of Suspense-compatible Hooks.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's \`use()\` hook (React 19) reads a resource (Promise or Context) and can suspend rendering while waiting. This integrates with Suspense boundaries, enabling declarative loading states. Unlike \`useEffect\` + \`useState\` for data fetching, \`use()\` suspends the component tree at the Suspense boundary level, enabling streaming and selective hydration. This is the future of data fetching in React.


\`\`\`jsx
\`\`\`jsx
// React 19: use() for data fetching with Suspense
function UserProfile({ userId }) {
  const user = use(fetchUser(userId)); // Suspends until resolved
  return <div>{user.name}</div>;
}

function App() {
  return (
    <Suspense fallback={<Spinner />}>
      <UserProfile userId={1} />
    </Suspense>
  );
}

// The entire UserProfile tree is replaced with Spinner
// until fetchUser resolves - no loading states inside component
\`\`\`
\`\`\`
`,tech:"react",displayNumber:193,sortKey:812},{id:"react-813",number:194,title:"How does useSyncExternalStore solve the external store problem?",question:"How does useSyncExternalStore solve the external store problem?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useSyncExternalStore\` subscribes to an external store and ensures the UI stays consistent with the store's state. It solves tearing issues that occurred in concurrent mode when different components read different versions of an external store during a concurrent render. The three arguments are: \`subscribe\` (function to listen for changes), \`getSnapshot\` (function to read current state), and \`getServerSnapshot\` (for SSR).


\`\`\`jsx
\`\`\`jsx
function useWindowSize() {
  return useSyncExternalStore(
    // Subscribe
    (callback) => {
      window.addEventListener('resize', callback);
      return () => window.removeEventListener('resize', callback);
    },
    // Get current snapshot
    () => ({ width: window.innerWidth, height: window.innerHeight }),
    // Server snapshot
    () => ({ width: 0, height: 0 }),
  );
}

// Using a store like Zustand/Redux
function useStore(selector) {
  return useSyncExternalStore(
    store.subscribe,
    () => selector(store.getState()),
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:194,sortKey:813},{id:"react-814",number:195,title:"What is the difference between useEffect and useLayoutEffect in terms of timing?",question:"What is the difference between useEffect and useLayoutEffect in terms of timing?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The timing difference is critical. \`useLayoutEffect\` fires synchronously after all DOM mutations but before the browser paints. \`useEffect\` fires asynchronously after the browser paints. This means \`useLayoutEffect\` can read/modify DOM without causing a visible flash, while \`useEffect\` might show a brief visual artifact if you're measuring or styling based on DOM state. Use \`useLayoutEffect\` sparingly since it blocks painting.


\`\`\`jsx
\`\`\`jsx
// useLayoutEffect: Blocks paint
function Measure() {
  const [height, setHeight] = useState(0);
  const ref = useRef(null);

  useLayoutEffect(() => {
    // This runs BEFORE the browser paints
    // User never sees height=0
    setHeight(ref.current.getBoundingClientRect().height);
  }, []);

  return <div ref={ref}>Measured content: {height}px</div>;
}

// useEffect: After paint
function MeasureAfterPaint() {
  const [height, setHeight] = useState(0);
  const ref = useRef(null);

  useEffect(() => {
    // User may briefly see height=0 before this runs
    setHeight(ref.current.getBoundingClientRect().height);
  }, []);

  return <div ref={ref}>Measured content: {height}px</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:195,sortKey:814},{id:"react-815",number:196,title:'What is the "stale closure" problem and how do Hooks relate to it?',question:'What is the "stale closure" problem and how do Hooks relate to it?',difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A stale closure occurs when a function captures outdated variable values from a previous render. In React, event handlers and effects capture the state and props from the render when they were created. If the component re-renders with new state but the handler still references old state, you get a stale closure bug. This is common with \`setTimeout\`, \`setInterval\`, and async callbacks that reference state.


\`\`\`jsx
\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // STALE CLOSURE: count is always 0
  useEffect(() => {
    const id = setInterval(() => {
      console.log(count); // Always logs 0!
      setCount(count + 1); // Always sets to 1!
    }, 1000);
    return () => clearInterval(id);
  }, []); // Empty deps: effect never re-creates

  // FIXED: Use functional update
  useEffect(() => {
    const id = setInterval(() => {
      setCount(prev => prev + 1); // Always uses latest state
    }, 1000);
    return () => clearInterval(id);
  }, []);

  return <p>{count}</p>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:196,sortKey:815},{id:"react-816",number:197,title:"How do you debug stale closure issues?",question:"How do you debug stale closure issues?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Debugging stale closures: (1) Add the dependency to the dependency array so the effect re-creates with fresh values, (2) Use functional state updates (\`setState(prev => ...)\`), (3) Use a ref to store the latest value (\`useRef\`), (4) Use the React DevTools Profiler to see how often effects re-run, (5) The ESLint \`exhaustive-deps\` rule catches many stale closure issues at lint time.


\`\`\`jsx
\`\`\`jsx
function SearchBox({ onSearch }) {
  const [query, setQuery] = useState('');

  // Solution 1: Add dependency
  useEffect(() => {
    const timer = setTimeout(() => {
      onSearch(query); // Fresh query
    }, 300);
    return () => clearTimeout(timer);
  }, [query, onSearch]); // Re-create when query or onSearch changes

  // Solution 2: Use ref for latest value
  const latestQuery = useRef(query);
  latestQuery.current = query;

  useEffect(() => {
    const timer = setTimeout(() => {
      onSearch(latestQuery.current); // Always current
    }, 300);
    return () => clearTimeout(timer);
  }, [onSearch]); // Only re-create when onSearch changes

  return <input value={query} onChange={e => setQuery(e.target.value)} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:197,sortKey:816},{id:"react-817",number:198,title:"Explain the dependency array comparison mechanism (Object.is).",question:"Explain the dependency array comparison mechanism (Object.is).",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"React uses `Object.is` to compare each dependency value with its previous value. `Object.is` behaves like `===` except for `NaN` (they are equal) and `+0 !== -0`. This means: primitives are compared by value, but objects and arrays are compared by reference. A new `{}` or `[]` created in the parent will always be \"different,\" causing the effect to re-run. This is why you should not create objects or arrays in the dependency array or as inline values passed to `useMemo`/`useCallback`.\n\n\n```jsx\n```jsx\nfunction Component({ config }) {\n  // Object.is({ theme: 'dark' }, { theme: 'dark' }) === false\n  // New object created every render = effect always re-runs\n\n  // BAD: Inline object in deps\n  useEffect(() => {\n    applyTheme(config);\n  }, [{ theme: config.theme }]); // New array every render!\n\n  // GOOD: Use primitive values\n  useEffect(() => {\n    applyTheme(config);\n  }, [config.theme]); // String comparison, stable\n\n  // GOOD: Memoize objects in parent\n  // Parent:\n  const config = useMemo(() => ({ theme: theme }), [theme]);\n  useEffect(() => {\n    applyTheme(config);\n  }, [config]); // Reference comparison, stable when theme unchanged\n}\n```\n```\n",tech:"react",displayNumber:198,sortKey:817},{id:"react-818",number:199,title:"What are the best practices for managing dependency arrays?",question:"What are the best practices for managing dependency arrays?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Best practices: (1) Always include the \`exhaustive-deps\` ESLint rule, (2) Use primitive values as dependencies when possible, (3) Memoize objects and functions that appear in dependency arrays, (4) Don't suppress lint warnings with \`// eslint-disable\` unless absolutely necessary, (5) If an effect needs to run only once, use empty \`[]\` but ensure the effect doesn't depend on mutable values, (6) Split complex effects so each has minimal, accurate dependencies.


\`\`\`jsx
\`\`\`jsx
// GOOD: Minimal, correct dependencies
function ChatRoom({ roomId, theme }) {
  useEffect(() => {
    const conn = createConnection(roomId);
    conn.join();
    return () => conn.leave();
  }, [roomId]); // Only roomId matters for connection

  useEffect(() => {
    document.body.className = theme;
  }, [theme]); // Only theme matters for styling
}

// BAD: Over-broad dependencies
useEffect(() => {
  doSomething(roomId, theme);
}, [roomId, theme]); // Effect re-runs on theme change even though roomId logic is unaffected
// Better: Split into two effects
\`\`\`
\`\`\`
`,tech:"react",displayNumber:199,sortKey:818},{id:"react-819",number:200,title:"What is the useRef pattern for avoiding stale closures?",question:"What is the useRef pattern for avoiding stale closures?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`When you need the latest value of a prop or state inside an effect or callback without re-creating the effect, store it in a ref. The ref's \`.current\` always reflects the latest value because refs are mutable objects, not captured in closures. This breaks the stale closure pattern while keeping the effect's identity stable.


\`\`\`jsx
\`\`\`jsx
function useInterval(callback, delay) {
  const savedCallback = useRef(callback);

  // Always keep the ref up to date
  useEffect(() => {
    savedCallback.current = callback;
  });

  // Stable effect: only re-creates when delay changes
  useEffect(() => {
    const tick = () => savedCallback.current();
    const id = setInterval(tick, delay);
    return () => clearInterval(id);
  }, [delay]);
}

// Usage
function Counter() {
  const [count, setCount] = useState(0);
  useInterval(() => setCount(c => c + 1), 1000);
  return <h1>{count}</h1>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:200,sortKey:819},{id:"react-820",number:201,title:"How do you create a custom Hook that works with both SSR and client rendering?",question:"How do you create a custom Hook that works with both SSR and client rendering?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`For SSR compatibility, your custom Hook must handle the case where browser APIs are unavailable. Check \`typeof window !== 'undefined'\` before accessing browser APIs, provide server-safe defaults, and use \`useSyncExternalStore\`'s \`getServerSnapshot\` parameter for stores. Hooks that depend on \`window\`, \`document\`, or \`localStorage\` need SSR guards.


\`\`\`jsx
\`\`\`jsx
function useMediaQuery(query) {
  const getSnapshot = () => window.matchMedia(query).matches;

  const getServerSnapshot = () => false; // Default for SSR

  const subscribe = (callback) => {
    const mql = window.matchMedia(query);
    mql.addEventListener('change', callback);
    return () => mql.removeEventListener('change', callback);
  };

  return useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
}

function useLocalStorage(key, initialValue) {
  const [storedValue, setStoredValue] = useState(() => {
    if (typeof window === 'undefined') return initialValue; // SSR guard
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch {
      return initialValue;
    }
  });

  const setValue = (value) => {
    const valueToStore = value instanceof Function ? value(storedValue) : value;
    setStoredValue(valueToStore);
    if (typeof window !== 'undefined') {
      window.localStorage.setItem(key, JSON.stringify(valueToStore));
    }
  };

  return [storedValue, setValue];
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:201,sortKey:820},{id:"react-821",number:202,title:"Explain how React.memo, useMemo, and useCallback work together for performance optimization.",question:"Explain how React.memo, useMemo, and useCallback work together for performance optimization.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`These three tools form the "memoization trinity" in React: \`React.memo\` prevents component re-renders when props haven't changed (shallow comparison), \`useMemo\` prevents expensive computations from re-running, and \`useCallback\` prevents function references from changing. They work together: \`useCallback\` and \`useMemo\` stabilize references that \`React.memo\` then correctly identifies as unchanged, skipping the re-render. Without the memoized references, \`React.memo\` sees new objects/functions and re-renders anyway.


\`\`\`jsx
\`\`\`jsx
// Complete optimization pattern
function ProductList({ products, onSort }) {
  const [sortBy, setSortBy] = useState('name');

  // Memoize expensive computation
  const sortedProducts = useMemo(
    () => [...products].sort((a, b) => a[sortBy].localeCompare(b[sortBy])),
    [products, sortBy]
  );

  // Memoize callback for child
  const handleProductClick = useCallback((productId) => {
    console.log('Clicked:', productId);
  }, []);

  return (
    <div>
      {sortedProducts.map(product => (
        // ProductCard only re-renders when its specific props change
        <ProductCard
          key={product.id}
          product={product}
          onClick={handleProductClick}
        />
      ))}
    </div>
  );
}

const ProductCard = React.memo(({ product, onClick }) => (
  <div onClick={() => onClick(product.id)}>{product.name}</div>
));
\`\`\`
\`\`\`
`,tech:"react",displayNumber:202,sortKey:821},{id:"react-822",number:203,title:"What is the useEffect cleanup race condition and how do you prevent it?",question:"What is the useEffect cleanup race condition and how do you prevent it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A race condition occurs in \`useEffect\` when an async operation completes after a newer request has started. For example, if you fetch data for userId=1, then quickly change to userId=2, the first request might complete after the second, overwriting the correct data. Prevent it using an AbortController to cancel the previous request, or a boolean flag to ignore stale responses.


\`\`\`jsx
\`\`\`jsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    let cancelled = false;
    const controller = new AbortController();

    fetch(\`/api/users/\${userId}\`, { signal: controller.signal })
      .then(res => res.json())
      .then(data => {
        if (!cancelled) setUser(data); // Ignore if stale
      })
      .catch(err => {
        if (err.name !== 'AbortError' && !cancelled) {
          console.error(err);
        }
      });

    return () => {
      cancelled = true;     // Flag to ignore stale response
      controller.abort();   // Cancel in-flight request
    };
  }, [userId]);

  return user ? <div>{user.name}</div> : <Spinner />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:203,sortKey:822},{id:"react-823",number:204,title:"How does the React Hook rules linter enforce correctness?",question:"How does the React Hook rules linter enforce correctness?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The \`eslint-plugin-react-hooks\` enforces two rules: \`rules-of-hooks\` (ensures Hooks are only called at the top level of components/custom Hooks, never inside conditions, loops, or nested functions) and \`exhaustive-deps\` (ensures dependency arrays are complete and correct). The plugin performs static analysis of your code at lint time, catching Hook violations before they cause runtime bugs. Both rules are considered essential for any React project.


\`\`\`jsx
\`\`\`jsx
// rules-of-hooks violation (lint error)
function Component({ flag }) {
  if (flag) {
    useEffect(() => {}, []); // ESLint: "Hook called conditionally"
  }
}

// exhaustive-deps violation (lint warning)
function Component({ userId }) {
  const [user, setUser] = useState(null);
  useEffect(() => {
    fetchUser(userId).then(setUser);
  }, []); // ESLint warning: "userId missing from dependency array"
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:204,sortKey:823},{id:"react-824",number:205,title:"Explain the useState functional update pattern and when to use it.",question:"Explain the useState functional update pattern and when to use it.",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The functional update pattern \`setState(prev => newValue)\` passes the most recent state to a function that computes the next state. Use it when: (1) new state depends on previous state, (2) you're updating state inside a \`setTimeout\`, \`setInterval\`, or async callback, (3) multiple \`setState\` calls need to see the latest value, or (4) you want to avoid including state in dependency arrays of callbacks.


\`\`\`jsx
\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // Functional update: always uses latest state
  const increment = () => setCount(prev => prev + 1);

  // Multiple updates: each sees latest
  const triple = () => {
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    // Final count: prevCount + 3
  };

  // Inside async callback
  const incrementAsync = () => {
    setTimeout(() => {
      setCount(prev => prev + 1); // Correct: uses latest state
    }, 1000);
  };

  return <button onClick={triple}>{count}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:205,sortKey:824},{id:"react-825",number:206,title:"What is the recommended pattern for complex state with useReducer?",question:"What is the recommended pattern for complex state with useReducer?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`For complex state, use the "state machine" pattern with useReducer: define all possible states and transitions explicitly, use an enum-like object for action types, keep the reducer pure (no side effects), and separate side effects from state transitions using middleware patterns (like thunks). This makes state transitions predictable, testable, and debuggable.


\`\`\`jsx
\`\`\`jsx
const ACTIONS = {
  FETCH_START: 'FETCH_START',
  FETCH_SUCCESS: 'FETCH_SUCCESS',
  FETCH_ERROR: 'FETCH_ERROR',
  RETRY: 'RETRY',
};

function asyncReducer(state, action) {
  switch (action.type) {
    case ACTIONS.FETCH_START:
      return { ...state, status: 'loading', error: null };
    case ACTIONS.FETCH_SUCCESS:
      return { ...state, status: 'success', data: action.payload, error: null };
    case ACTIONS.FETCH_ERROR:
      return { ...state, status: 'error', error: action.payload };
    case ACTIONS.RETRY:
      return { ...state, status: 'idle' };
    default:
      throw new Error(\`Unhandled action: \${action.type}\`);
  }
}

function useAsync(asyncFn, deps) {
  const [state, dispatch] = useReducer(asyncReducer, {
    status: 'idle', data: null, error: null,
  });

  useEffect(() => {
    let cancelled = false;
    dispatch({ type: ACTIONS.FETCH_START });

    asyncFn()
      .then(data => { if (!cancelled) dispatch({ type: ACTIONS.FETCH_SUCCESS, payload: data }); })
      .catch(err => { if (!cancelled) dispatch({ type: ACTIONS.FETCH_ERROR, payload: err }); });

    return () => { cancelled = true; };
  }, deps);

  const retry = () => dispatch({ type: ACTIONS.RETRY });

  return { ...state, retry };
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:206,sortKey:825},{id:"react-826",number:207,title:"How do you share state between sibling components using custom Hooks?",question:"How do you share state between sibling components using custom Hooks?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks can share state between siblings by having both components call the same custom Hook with the same arguments, but since each call creates independent state, true sharing requires lifting state up or using context. A pattern for shared state is to create a Hook that reads from a shared store (like Zustand) or use context with a custom Hook as the consumer interface.


\`\`\`jsx
\`\`\`jsx
// Pattern 1: Lift state to common ancestor, pass via custom Hook
function useSharedCounter() {
  // This only works if called within the same component
  const [count, setCount] = useState(0);
  return { count, increment: () => setCount(c => c + 1) };
}

// Pattern 2: Share via context + custom Hook
const CounterContext = createContext();

function CounterProvider({ children }) {
  const [count, setCount] = useState(0);
  const value = useMemo(() => ({
    count,
    increment: () => setCount(c => c + 1),
  }), [count]);
  return <CounterContext.Provider value={value}>{children}</CounterContext.Provider>;
}

function useCounter() {
  const context = useContext(CounterContext);
  if (!context) throw new Error('useCounter must be used within CounterProvider');
  return context;
}

// Both components share the same state
function Display() {
  const { count } = useCounter();
  return <span>{count}</span>;
}

function Button() {
  const { increment } = useCounter();
  return <button onClick={increment}>+1</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:207,sortKey:826},{id:"react-827",number:208,title:"What is the useLayoutEffect measurement pattern and why does it matter?",question:"What is the useLayoutEffect measurement pattern and why does it matter?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The measurement pattern uses \`useLayoutEffect\` to read DOM measurements (height, width, position) and immediately apply corrective styles before the browser paints. This prevents layout shifts and visual flicker. It is essential for features like tooltips, popovers, dropdown menus, and responsive layouts where you need to know an element's rendered dimensions before positioning another element.


\`\`\`jsx
\`\`\`jsx
function AutoResizeTextarea({ value, onChange }) {
  const textareaRef = useRef(null);
  const [height, setHeight] = useState('auto');

  useLayoutEffect(() => {
    const textarea = textareaRef.current;
    // Reset to measure natural height
    textarea.style.height = 'auto';
    const newHeight = textarea.scrollHeight;
    textarea.style.height = \`\${newHeight}px\`;
    setHeight(newHeight);
  }, [value]);

  return (
    <textarea
      ref={textareaRef}
      value={value}
      onChange={onChange}
      style={{ height, overflow: 'hidden' }}
    />
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:208,sortKey:827},{id:"react-828",number:209,title:"How do you test custom Hooks?",question:"How do you test custom Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Use \`@testing-library/react-hooks\` (or \`renderHook\` from \`@testing-library/react\` v13+). \`renderHook\` wraps a Hook call in a test component, providing \`result.current\` for assertions and \`rerender\` to simulate re-renders with new props. For Hooks with side effects, use \`act()\` to flush pending state updates. Mock external dependencies (APIs, browser APIs) using \`jest.mock\` or \`msw\`.


\`\`\`jsx
\`\`\`jsx
import { renderHook, act } from '@testing-library/react';
import { useCounter } from './useCounter';

describe('useCounter', () => {
  it('increments count', () => {
    const { result } = renderHook(() => useCounter(0));

    act(() => result.current.increment());
    expect(result.current.count).toBe(1);

    act(() => result.current.increment());
    expect(result.current.count).toBe(2);
  });

  it('resets to initial value', () => {
    const { result } = renderHook(() => useCounter(10));

    act(() => result.current.increment());
    act(() => result.current.reset());
    expect(result.current.count).toBe(10);
  });

  it('responds to prop changes', () => {
    const { result, rerender } = renderHook(
      ({ initialValue }) => useCounter(initialValue),
      { initialProps: { initialValue: 0 } }
    );

    rerender({ initialValue: 5 });
    expect(result.current.count).toBe(5);
  });
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:209,sortKey:828},{id:"react-829",number:210,title:"What is the useSyncExternalStore anti-pattern and how to avoid it?",question:"What is the useSyncExternalStore anti-pattern and how to avoid it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Anti-patterns with \`useSyncExternalStore\`: (1) Creating \`subscribe\` and \`getSnapshot\` inline on every render (causes re-subscription), (2) Not handling the case where the store is not yet available (SSR), (3) Using it for local component state (just use \`useState\`), (4) Not returning a stable function reference from \`subscribe\`. Always memoize or define these functions outside the component to avoid unnecessary re-subscriptions.


\`\`\`jsx
\`\`\`jsx
// BAD: Creates new functions every render
function useStore(store) {
  return useSyncExternalStore(
    (callback) => store.subscribe(callback), // New function each render!
    () => store.getState(),                  // New function each render!
  );
}

// CORRECT: Stable function references
function createSubscription(store) {
  return (callback) => {
    store.subscribe(callback);
    return () => store.unsubscribe(callback);
  };
}

const subscribe = createSubscription(store);
const getSnapshot = () => store.getState();

function MyComponent() {
  const state = useSyncExternalStore(subscribe, getSnapshot);
  return <div>{state.value}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:210,sortKey:829},{id:"react-830",number:211,title:"How does useInsertionEffect prevent style recalculation thrashing?",question:"How does useInsertionEffect prevent style recalculation thrashing?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:"Without `useInsertionEffect`, CSS-in-JS libraries inject `<style>` tags during render (before commit) or in `useLayoutEffect` (after layout). Injecting before commit causes React to see stale styles. Injecting in `useLayoutEffect` means styles are inserted after layout measurement, causing a layout recalculation. `useInsertionEffect` runs after DOM mutation but before layout effects, allowing styles to be injected before layout is measured, eliminating the extra recalculation pass.\n\n\n```jsx\n```jsx\n// Timeline of a render with style injection:\n// 1. Render phase (virtual DOM diff)\n// 2. Commit phase (DOM mutations)\n// 3. useInsertionEffect runs <-- Style injection here\n// 4. useLayoutEffect runs <-- Layout measurement here (sees correct styles)\n// 5. Browser paints\n\n// Without useInsertionEffect (in useLayoutEffect):\n// 1-2. Same\n// 3. useLayoutEffect: measures layout (STALE styles)\n// 4. Injects styles\n// 5. Layout recalculates (WASTED WORK)\n// 6. Browser paints\n```\n```\n",tech:"react",displayNumber:211,sortKey:830},{id:"react-831",number:212,title:'Explain the concept of "effect as synchronization" in React.',question:'Explain the concept of "effect as synchronization" in React.',difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`React's \`useEffect\` documentation describes effects as synchronizing a component with an external system. Rather than thinking of effects as lifecycle methods (componentDidMount, componentDidUpdate, componentWillUnmount), think of them as keeping the component in sync with external state (APIs, subscriptions, DOM). Each effect synchronizes with a specific external system and cleans up when that synchronization is no longer needed.


\`\`\`jsx
\`\`\`jsx
// NOT this (lifecycle thinking):
useEffect(() => {
  // componentDidMount
  const conn = connect(roomId);
  // componentDidUpdate (need to check if roomId changed)
  // componentWillUnmount
  return () => conn.disconnect();
}, [roomId]);

// THIS (synchronization thinking):
useEffect(() => {
  // Synchronize with the chat server for this roomId
  const conn = createConnection(roomId);
  conn.connect();
  // Stop synchronizing (cleanup)
  return () => {
    conn.disconnect();
  };
}, [roomId]); // Re-synchronize when roomId changes
\`\`\`
\`\`\`
`,tech:"react",displayNumber:212,sortKey:831},{id:"react-832",number:213,title:"What are the common performance anti-patterns with Hooks?",question:"What are the common performance anti-patterns with Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Common anti-patterns: (1) Memoizing everything without profiling first, (2) Creating objects/arrays inline as \`useMemo\` dependencies, (3) Using \`useCallback\` when children aren't memoized, (4) Calling expensive computations inside the render body instead of \`useMemo\`, (5) Using \`useEffect\` for derived state (should compute during render), (6) Not splitting context by update frequency, (7) Using \`useState\` for values that could be derived from other state.


\`\`\`jsx
\`\`\`jsx
// ANTI-PATTERN: useEffect for derived state
function FilteredList({ items, filter }) {
  const [filtered, setFiltered] = useState([]);
  useEffect(() => {
    setFiltered(items.filter(i => i.name.includes(filter))); // Unnecessary
  }, [items, filter]);
  return <List items={filtered} />;
}

// CORRECT: Compute during render
function FilteredList({ items, filter }) {
  const filtered = useMemo(
    () => items.filter(i => i.name.includes(filter)),
    [items, filter]
  );
  return <List items={filtered} />;
}

// ANTI-PATTERN: Inline object as dependency
useEffect(() => {}, [{ a, b }]); // New array every render!

// CORRECT: Use primitives
useEffect(() => {}, [a, b]); // Value comparison, stable
\`\`\`
\`\`\`
`,tech:"react",displayNumber:213,sortKey:832},{id:"react-833",number:214,title:"How do you handle form state with useReducer?",question:"How do you handle form state with useReducer?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useReducer\` excels at managing form state because forms have multiple interdependent fields, validation states, and submission states. The reducer centralizes all form logic: field changes, validation, submission, and reset. Each action type represents a specific form event, making the flow easy to follow and test. This pattern scales better than multiple \`useState\` calls for complex forms.


\`\`\`jsx
\`\`\`jsx
function useForm(initialValues, validate, onSubmit) {
  const [state, dispatch] = useReducer(formReducer, {
    values: initialValues,
    errors: {},
    touched: {},
    isSubmitting: false,
    isSubmitted: false,
  });

  function handleChange(field, value) {
    dispatch({ type: 'SET_FIELD', field, value });
  }

  function handleBlur(field) {
    dispatch({ type: 'SET_TOUCHED', field });
    const errors = validate(state.values);
    dispatch({ type: 'SET_ERRORS', errors });
  }

  async function handleSubmit(e) {
    e.preventDefault();
    const errors = validate(state.values);
    if (Object.keys(errors).length > 0) {
      dispatch({ type: 'SET_ERRORS', errors });
      return;
    }
    dispatch({ type: 'SUBMIT_START' });
    try {
      await onSubmit(state.values);
      dispatch({ type: 'SUBMIT_SUCCESS' });
    } catch (err) {
      dispatch({ type: 'SUBMIT_ERROR', error: err.message });
    }
  }

  function reset() {
    dispatch({ type: 'RESET', initialValues });
  }

  return {
    values: state.values,
    errors: state.errors,
    touched: state.touched,
    isSubmitting: state.isSubmitting,
    handleChange,
    handleBlur,
    handleSubmit,
    reset,
  };
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:214,sortKey:833},{id:"react-834",number:215,title:"Explain the relationship between useEffect and component lifecycle.",question:"Explain the relationship between useEffect and component lifecycle.",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`While \`useEffect\` maps conceptually to lifecycle methods, it's more accurately described as synchronization. \`useEffect\` with \`[]\` is similar to \`componentDidMount\`, \`useEffect\` with dependencies is similar to \`componentDidUpdate\`, and the cleanup function is similar to \`componentWillUnmount\`. However, thinking in terms of synchronization is more accurate: effects synchronize with external systems and re-synchronize when dependencies change.


\`\`\`jsx
\`\`\`jsx
// Lifecycle mapping (for reference, not recommended mental model)
function Component({ userId }) {
  // componentDidMount equivalent
  useEffect(() => {
    logMount();
  }, []);

  // componentDidUpdate equivalent (for userId)
  useEffect(() => {
    analytics.track('user_changed', userId);
  }, [userId]);

  // componentWillUnmount equivalent
  useEffect(() => {
    const subscription = subscribe(userId);
    return () => subscription.unsubscribe(); // Cleanup
  }, [userId]);

  // All three in one: mount + update + unmount
  useEffect(() => {
    analytics.track('view', userId);
    return () => analytics.track('unview', userId);
  }, [userId]);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:215,sortKey:834},{id:"react-835",number:216,title:"What happens if you have multiple useEffect calls in one component?",question:"What happens if you have multiple useEffect calls in one component?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Multiple \`useEffect\` calls are independent and run in declaration order. Each effect handles its own synchronization. This is actually a feature: it allows you to separate concerns, with each effect managing a single external system. Cleanup functions from all effects run in reverse declaration order before the component unmounts or before effects re-run.


\`\`\`jsx
\`\`\`jsx
function ChatRoom({ roomId, theme }) {
  // Effect 1: Connection management
  useEffect(() => {
    const conn = createConnection(roomId);
    conn.connect();
    return () => conn.disconnect();
  }, [roomId]);

  // Effect 2: Document title
  useEffect(() => {
    document.title = \`Chat - Room \${roomId}\`;
  }, [roomId]);

  // Effect 3: Theme application
  useEffect(() => {
    document.body.className = theme;
    return () => { document.body.className = ''; };
  }, [theme]);

  // All three are independent, run in order, clean up independently
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:216,sortKey:835},{id:"react-836",number:217,title:"How do you handle errors inside useEffect?",question:"How do you handle errors inside useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useEffect\` callbacks can't use try/catch with async/await directly because the cleanup function can't be an async function. Solutions: (1) Use \`.catch()\` on promises, (2) Wrap the async logic in an async function called from the effect, (3) Use error boundaries (React 18+) with the \`onError\` callback, (4) For fetch errors, check \`response.ok\` and throw. Always handle both network errors and application-level errors.


\`\`\`jsx
\`\`\`jsx
function DataFetcher({ url }) {
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let cancelled = false;

    async function fetchData() {
      try {
        setLoading(true);
        const response = await fetch(url);
        if (!response.ok) throw new Error(\`HTTP \${response.status}\`);
        const json = await response.json();
        if (!cancelled) setData(json);
      } catch (err) {
        if (!cancelled) setError(err.message);
      } finally {
        if (!cancelled) setLoading(false);
      }
    }

    fetchData();
    return () => { cancelled = true; };
  }, [url]);

  if (loading) return <Spinner />;
  if (error) return <ErrorMessage message={error} />;
  return <DataView data={data} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:217,sortKey:836},{id:"react-837",number:218,title:"What is the useEffect event pattern (React canary/experimental)?",question:"What is the useEffect event pattern (React canary/experimental)?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's experimental \`useEffectEvent\` hook (not yet stable) allows you to extract "event-like" logic from effects. It creates a function that always sees the latest values of reactive dependencies without being listed as a dependency. This solves the tension between needing the latest values and not wanting the effect to re-run. It is similar to \`useRef\` for latest values but is designed specifically for effects.


\`\`\`jsx
\`\`\`jsx
// Experimental pattern (not yet stable in React)
import { useEffectEvent } from 'react'; // Experimental

function ChatRoom({ roomId, theme }) {
  const onVisit = useEffectEvent((visitCount) => {
    analytics.track('visit', { roomId, theme, visitCount });
  });

  useEffect(() => {
    let visits = 0;
    const conn = createConnection(roomId);
    conn.on('visit', () => {
      visits++;
      onVisit(visits); // Always sees latest roomId and theme
    });
    conn.connect();
    return () => conn.disconnect();
  }, [roomId]); // onVisit reference is stable, doesn't need to be in deps
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:218,sortKey:837},{id:"react-838",number:219,title:"How do you optimize re-renders caused by context updates?",question:"How do you optimize re-renders caused by context updates?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Strategies: (1) Split context by update frequency. (2) Memoize the context value with \`useMemo\`. (3) Move stateful logic into a custom Hook that uses \`useSyncExternalStore\` for fine-grained subscriptions. (4) Use the "compound component" pattern to reduce context scope. (5) Use state management libraries (Zustand, Jotai) with selector-based subscriptions that only trigger re-renders for specific slices of state.


\`\`\`jsx
\`\`\`jsx
// Strategy: Split context + memoize values
const AuthStateContext = createContext();   // Rarely changes
const AuthActionsContext = createContext(); // Never changes (stable refs)

function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const login = useCallback(async (creds) => {
    const u = await api.login(creds);
    setUser(u);
  }, []);
  const logout = useCallback(() => setUser(null), []);

  const stateValue = useMemo(() => ({ user }), [user]);
  const actionsValue = useMemo(() => ({ login, logout }), [login, logout]);

  return (
    <AuthStateContext.Provider value={stateValue}>
      <AuthActionsContext.Provider value={actionsValue}>
        {children}
      </AuthActionsContext.Provider>
    </AuthStateContext.Provider>
  );
}

// Components that only need actions won't re-render on user changes
function LogoutButton() {
  const { logout } = useContext(AuthActionsContext); // Stable
  return <button onClick={logout}>Logout</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:219,sortKey:838},{id:"react-839",number:220,title:"Explain the useEffect vs useLayoutEffect timing diagram.",question:"Explain the useEffect vs useLayoutEffect timing diagram.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The rendering timeline: (1) React renders (virtual DOM diff), (2) React commits DOM changes, (3) \`useInsertionEffect\` runs (synchronous, for CSS-in-JS), (4) \`useLayoutEffect\` runs (synchronous, for DOM measurements), (5) Browser paints pixels on screen, (6) \`useEffect\` runs (asynchronous, for non-blocking side effects). The key insight: \`useLayoutEffect\` blocks the paint, \`useEffect\` does not.


\`\`\`jsx
\`\`\`
Render Phase (virtual DOM) 
    |
    v
Commit Phase (DOM mutations)
    |
    v
useInsertionEffect (style injection)
    |
    v
useLayoutEffect (DOM measurement/modification)
    |
    v
Browser Paint  <-- visible to user
    |
    v
useEffect (data fetching, subscriptions, etc.)
\`\`\`
\`\`\`
`,tech:"react",displayNumber:220,sortKey:839},{id:"react-840",number:221,title:"How do you handle multiple async operations in useEffect?",question:"How do you handle multiple async operations in useEffect?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`For multiple independent async operations, use separate \`useEffect\` calls (each handles its own cleanup). For dependent async operations (second depends on first's result), chain them within a single effect using async/await. Always handle cleanup with \`cancelled\` flags or AbortControllers, and manage loading/error states appropriately for each operation.


\`\`\`jsx
\`\`\`jsx
function Dashboard({ userId }) {
  const [user, setUser] = useState(null);
  const [posts, setPosts] = useState(null);

  // Independent: parallel fetching
  useEffect(() => {
    let cancelled = false;
    fetchUser(userId).then(data => { if (!cancelled) setUser(data); });
    return () => { cancelled = true; };
  }, [userId]);

  useEffect(() => {
    let cancelled = false;
    fetchPosts(userId).then(data => { if (!cancelled) setPosts(data); });
    return () => { cancelled = true; };
  }, [userId]);

  // Dependent: sequential fetching
  useEffect(() => {
    let cancelled = false;
    async function load() {
      const userData = await fetchUser(userId);
      if (cancelled) return;
      setUser(userData);
      const postData = await fetchPosts(userData.postsUrl);
      if (cancelled) return;
      setPosts(postData);
    }
    load();
    return () => { cancelled = true; };
  }, [userId]);

  if (!user || !posts) return <Spinner />;
  return <div>{user.name} - {posts.length} posts</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:221,sortKey:840},{id:"react-841",number:222,title:"What is the difference between refs created by useRef and callback refs?",question:"What is the difference between refs created by useRef and callback refs?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useRef\` returns a stable object with a \`.current\` property. A callback ref is a function that React calls with the DOM node on mount and \`null\` on unmount. Callback refs are useful when you need to perform cleanup or when the ref callback depends on dynamic values. \`useRef\` is simpler and more common; callback refs are needed for dynamic ref assignments and cleanup logic tied to the DOM element's lifecycle.


\`\`\`jsx
\`\`\`jsx
// useRef: Simple, stable reference
function Input() {
  const inputRef = useRef(null);
  useEffect(() => { inputRef.current.focus(); }, []);
  return <input ref={inputRef} />;
}

// Callback ref: Dynamic, with cleanup
function MeasuredDiv() {
  const [height, setHeight] = useState(0);

  const refCallback = useCallback((node) => {
    if (node !== null) {
      const observer = new ResizeObserver(([entry]) => {
        setHeight(entry.contentRect.height);
      });
      observer.observe(node);
      return () => observer.disconnect(); // Cleanup on unmount
    }
  }, []);

  return <div ref={refCallback}>Height: {height}px</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:222,sortKey:841},{id:"react-842",number:223,title:'Explain the "use the latest ref" pattern for avoiding stale closures.',question:'Explain the "use the latest ref" pattern for avoiding stale closures.',difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`The "latest ref" pattern stores the most current value of a prop or state in a ref. Since refs are mutable and not captured by closures, the ref always provides the latest value. This is essential for callbacks stored in refs, interval functions, and event handlers that need to reference values that change between renders but shouldn't cause the effect to re-run.


\`\`\`jsx
\`\`\`jsx
function useLatestRef(value) {
  const ref = useRef(value);
  ref.current = value; // Update on every render
  return ref;
}

function useInterval(callback, delay) {
  const latestCallback = useLatestRef(callback);

  useEffect(() => {
    const id = setInterval(() => {
      latestCallback.current(); // Always calls the latest version
    }, delay);
    return () => clearInterval(id);
  }, [delay]); // Only re-runs when delay changes
}

// Usage: callback can change without re-creating the interval
function Timer() {
  const [count, setCount] = useState(0);
  useInterval(() => setCount(c => c + 1), 1000);
  return <p>{count}</p>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:223,sortKey:842},{id:"react-843",number:224,title:"How does React ensure Hook stability across renders?",question:"How does React ensure Hook stability across renders?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React ensures Hook stability through its fiber architecture. Each component fiber has a \`memoizedState\` property pointing to the first Hook's state node. Each Hook state node has a \`next\` pointer forming a linked list. On re-renders, React walks this list in order, matching each Hook call to its state node. The \`dispatch\` function from \`useState\`/\`useReducer\` is stable across renders because it's bound to the fiber's state node, not captured in closures.


\`\`\`jsx
\`\`\`jsx
// dispatch is stable - safe to omit from dependencies
function Component() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    // setCount reference never changes between renders
    const id = setInterval(() => setCount(c => c + 1), 1000);
    return () => clearInterval(id);
  }, []); // No dependency needed for setCount
}

// useMemo and useCallback return stable references when deps don't change
function Component({ theme }) {
  const stableFn = useCallback(() => {}, []); // Always same reference
  const stableObj = useMemo(() => ({ a: 1 }), []); // Always same reference
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:224,sortKey:843},{id:"react-844",number:225,title:"What are the key differences between React 17 and React 18 Hooks behavior?",question:"What are the key differences between React 17 and React 18 Hooks behavior?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Key differences: (1) React 18 adds automatic batching for all state updates (not just event handlers), (2) React 18 introduces \`useTransition\`, \`useDeferredValue\`, \`useId\`, and \`useSyncExternalStore\`, (3) React 18 adds \`useInsertionEffect\`, (4) React 18's \`useId\` generates unique IDs for SSR hydration safety, (5) React 18 concurrent features allow transitions to be interrupted, (6) React 18 removes the need for \`act()\` in most cases for testing.


\`\`\`jsx
\`\`\`jsx
// React 17: Batching only in event handlers
function handleClick() {
  setTimeout(() => {
    setA(1); // Re-render 1
    setB(2); // Re-render 2
  }, 0);
}

// React 18: Automatic batching everywhere
function handleClick() {
  setTimeout(() => {
    setA(1); // Batched
    setB(2); // Batched - only 1 re-render
  }, 0);
}

// New Hooks in React 18
function Component() {
  const [isPending, startTransition] = useTransition();
  const deferredValue = useDeferredValue(value);
  const id = useId();
  const externalState = useSyncExternalStore(subscribe, getSnapshot);
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:225,sortKey:844},{id:"react-845",number:226,title:"Explain the useDeferredValue pattern for search filtering.",question:"Explain the useDeferredValue pattern for search filtering.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useDeferredValue\` is ideal for search/filter UIs where typing should feel instant but the filtered results are expensive to compute. The input updates immediately (high priority), while the filtered list updates with a deferred value (low priority). React can interrupt the expensive filter computation if the user types again, keeping the UI responsive. The stale version is shown while the new computation runs.


\`\`\`jsx
\`\`\`jsx
function SearchableList({ items }) {
  const [query, setQuery] = useState('');
  const deferredQuery = useDeferredValue(query);
  const isStale = query !== deferredQuery;

  const filteredItems = useMemo(
    () => items.filter(item => item.name.toLowerCase().includes(deferredQuery.toLowerCase())),
    [items, deferredQuery]
  );

  return (
    <div>
      <input
        value={query}
        onChange={e => setQuery(e.target.value)}
        placeholder="Search..."
      />
      <div style={{ opacity: isStale ? 0.6 : 1, transition: 'opacity 200ms' }}>
        <ItemList items={filteredItems} />
      </div>
      {isStale && <span>Updating...</span>}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:226,sortKey:845},{id:"react-846",number:227,title:"How do you create a useFetch Hook with caching?",question:"How do you create a useFetch Hook with caching?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A cached \`useFetch\` Hook stores responses in a cache (using a Map or similar structure) keyed by URL. On subsequent requests for the same URL, it returns cached data immediately while optionally revalidating in the background. This pattern (stale-while-revalidate) improves perceived performance. Use \`useRef\` for the cache to avoid re-creating it on renders.


\`\`\`jsx
\`\`\`jsx
const cache = new Map(); // Module-level cache persists across components

function useFetch(url) {
  const [data, setData] = useState(() => cache.get(url) || null);
  const [loading, setLoading] = useState(!cache.has(url));
  const [error, setError] = useState(null);

  useEffect(() => {
    if (cache.has(url)) {
      setData(cache.get(url));
      setLoading(false);
      return;
    }

    let cancelled = false;
    setLoading(true);

    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error(\`HTTP \${res.status}\`);
        return res.json();
      })
      .then(json => {
        cache.set(url, json);
        if (!cancelled) { setData(json); setLoading(false); }
      })
      .catch(err => {
        if (!cancelled) { setError(err.message); setLoading(false); }
      });

    return () => { cancelled = true; };
  }, [url]);

  return { data, loading, error };
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:227,sortKey:846},{id:"react-847",number:228,title:"What is the useState 18 cheat pattern for computing initial state?",question:"What is the useState 18 cheat pattern for computing initial state?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When using \`useState\` with a lazy initializer (function form), you can compute expensive initial state only once. The lazy initializer function runs only during the first render. This is particularly useful for expensive operations like parsing large JSON, computing complex default values, or reading from browser APIs (localStorage). Without the lazy form, the computation runs on every render even though the result is discarded.


\`\`\`jsx
\`\`\`jsx
// EXPENSIVE: Runs on every render
function Component1() {
  const [settings, setSettings] = useState(JSON.parse(localStorage.getItem('settings')));
}

// OPTIMAL: Runs only on first render
function Component2() {
  const [settings, setSettings] = useState(() => {
    try {
      return JSON.parse(localStorage.getItem('settings'));
    } catch {
      return defaultSettings;
    }
  });
}

// Also useful for expensive computations
function Component3() {
  const [matrix, setMatrix] = useState(() => generateLargeMatrix(1000, 1000));
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:228,sortKey:847},{id:"react-848",number:229,title:"Explain how custom Hooks enable code reuse across your application.",question:"Explain how custom Hooks enable code reuse across your application.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks are React's primary mechanism for logic reuse. They solve the problems that mixins, higher-order components, and render props tried to address, but without the wrapper hell or component explosion. A custom Hook encapsulates stateful logic that can be shared across components without changing the component hierarchy. Each call to the Hook gets its own independent state, so multiple components using the same Hook don't share data unless explicitly designed to.


\`\`\`jsx
\`\`\`jsx
// Shared logic across many components
function useClickOutside(ref, handler) {
  useEffect(() => {
    function listener(event) {
      if (!ref.current || ref.current.contains(event.target)) return;
      handler(event);
    }
    document.addEventListener('mousedown', listener);
    document.addEventListener('touchstart', listener);
    return () => {
      document.removeEventListener('mousedown', listener);
      document.removeEventListener('touchstart', listener);
    };
  }, [ref, handler]);
}

// Used in a dropdown
function Dropdown({ children }) {
  const ref = useRef(null);
  const [open, setOpen] = useState(false);
  useClickOutside(ref, () => setOpen(false));
  return <div ref={ref}>{open && children}</div>;
}

// Used in a modal
function Modal({ children, onClose }) {
  const ref = useRef(null);
  useClickOutside(ref, onClose);
  return <div ref={ref} className="modal">{children}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:229,sortKey:848},{id:"react-849",number:230,title:"What are the future directions for React Hooks and data fetching?",question:"What are the future directions for React Hooks and data fetching?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's future direction includes: (1) The \`use()\` hook (React 19) for reading resources including promises directly in render with Suspense, (2) Server Components reducing the need for client-side data fetching hooks, (3) React Compiler (formerly React Forget) automatically memoizing components and eliminating the need for manual \`useMemo\`/\`useCallback\`, (4) Transition-based data fetching as the default pattern, (5) Potential deprecation of \`useEffect\` for data fetching in favor of \`use()\` + Suspense, (6) \`useFormStatus\` and \`useFormState\` for form actions integration.


\`\`\`jsx
\`\`\`jsx
// Future: use() with Suspense replaces useEffect for data fetching
function UserProfile({ userId }) {
  const user = use(fetchUser(userId)); // Suspends until resolved
  return <h1>{user.name}</h1>;
}

// Future: Server Components reduce client-side hooks
// app/users/[id]/page.tsx (Server Component)
async function UserPage({ params }) {
  const user = await db.user.findUnique({ where: { id: params.id } });
  return <UserProfile user={user} />; // No useEffect needed
}

// Future: React Compiler eliminates manual memoization
// No need for useMemo/useCallback - compiler handles it automatically
function Component({ data, onClick }) {
  // Compiler auto-memoizes expensive computations
  const processed = expensiveProcess(data);
  // Compiler auto-memoizes callbacks passed to memoized children
  return <MemoizedChild data={processed} onClick={onClick} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:230,sortKey:849},{id:"react-850",number:231,title:"What are the main reasons for unnecessary re-renders in React?",question:"What are the main reasons for unnecessary re-renders in React?",difficulty:"Basic",category:"Performance & Optimization",answer:`Unnecessary re-renders occur when a component renders even though its output wouldn't change. The main causes are:

1. **Parent re-renders** - By default, all children re-render when parent does
2. **Prop changes** - New object/array references created on every render
3. **Context changes** - All consumers re-render when context value changes
4. **State changes** - Any state update triggers re-render, even if value is the same
5. **Anonymous functions** - New function references on every render
6. **Inline objects** - New object references in JSX props

React's default behavior is to re-render children when parent re-renders because it's safer and often cheap. Performance issues arise when expensive computations or large component trees re-render unnecessarily.


\`\`\`jsx
// Problem: New reference on every render
function Parent() {
  const [count, setCount] = useState(0);

  return (
    <Child
      onClick={() => console.log('click')}  // New function each render
      style={{ color: 'red' }}              // New object each render
    />
  );
}

// Solution: Stable references
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = useCallback(() => console.log('click'), []);
  const style = useMemo(() => ({ color: 'red' }), []);

  return <Child onClick={handleClick} style={style} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:231,sortKey:850},{id:"react-851",number:232,title:"How does React.memo work and when should you use it?",question:"How does React.memo work and when should you use it?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`React.memo\` is a higher-order component that memoizes the rendered output. It performs a shallow comparison of props and skips re-rendering if props haven't changed. It's useful for:

- Components that render often with the same props
- Expensive-to-render components
- Components deep in the tree that don't need to re-render when parent changes

It should NOT be used for:
- Components that always receive different props
- Small, cheap components (memoization overhead may outweigh benefits)
- Components where props change frequently anyway


\`\`\`jsx
import React, { memo } from 'react';

// Memoized component
const UserCard = memo(function UserCard({ user, onSelect }) {
  return (
    <div onClick={() => onSelect(user.id)}>
      {user.name}
    </div>
  );
});

// Alternative syntax
const UserCard = memo(function UserCard({ user }) {
  return <div>{user.name}</div>;
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:232,sortKey:851},{id:"react-852",number:233,title:"What is the difference between shallow comparison and custom comparator in React.memo?",question:"What is the difference between shallow comparison and custom comparator in React.memo?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`React.memo\` accepts an optional second argument: a custom comparison function \`(prevProps, nextProps) => boolean\`.

- **Shallow comparison** (default): Compares each prop with \`Object.is\`. Objects/arrays are compared by reference, not content.
- **Custom comparator**: Returns \`true\` if props are "equal" (skip render), \`false\` if different (re-render). Use it when you need deep comparison or want to ignore certain props.

**Best practice**: Avoid custom comparators unless necessary. They run on every render and can hurt performance if expensive. Prefer stable references with \`useMemo\`/\`useCallback\` instead.


\`\`\`jsx
import React, { memo } from 'react';

const ExpensiveList = memo(
  function ExpensiveList({ items }) {
    return items.map(item => <li key={item.id}>{item.text}</li>);
  },
  // Custom comparator: only re-render if length or last item changes
  (prevProps, nextProps) => {
    return (
      prevProps.items.length === nextProps.items.length &&
      prevProps.items[prevProps.items.length - 1]?.id ===
      nextProps.items[nextProps.items.length - 1]?.id
    );
  }
);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:233,sortKey:852},{id:"react-853",number:234,title:"What is code splitting and how does it improve performance?",question:"What is code splitting and how does it improve performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`Code splitting is the practice of breaking your bundle into smaller chunks that are loaded on demand rather than all at once. Benefits:

- **Faster initial load**: Users download only what they need for the current view
- **Smaller initial bundle**: Reduced Time to Interactive (TTI)
- **Better caching**: Unchanged chunks stay cached

With React, you use \`React.lazy\` for component-level splitting and dynamic \`import()\` for route/feature-based splitting. Bundle analyzers (webpack-bundle-analyzer) help identify splitting opportunities.


\`\`\`jsx
// Without splitting - everything in one bundle
import AdminPanel from './AdminPanel';

// With splitting - loaded on demand
const AdminPanel = React.lazy(() => import('./AdminPanel'));

// Route-based splitting
const routes = [
  { path: '/', component: React.lazy(() => import('./Home')) },
  { path: '/admin', component: React.lazy(() => import('./Admin')) },
];
\`\`\`
\`\`\`
`,tech:"react",displayNumber:234,sortKey:853},{id:"react-854",number:235,title:"How do you use React.lazy and Suspense for code splitting?",question:"How do you use React.lazy and Suspense for code splitting?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`React.lazy\` lets you render a dynamic import as a regular component. \`<Suspense>\` shows a fallback while the lazy component loads.

Key points:
- \`React.lazy\` must return a Promise resolving to a module with a default export
- \`<Suspense>\` must wrap lazy components
- You can nest multiple Suspense boundaries for granular loading states
- Works in both Client and Server Components (with caveats in Next.js)

In React 18+, \`Suspense\` also handles data fetching with libraries like Relay or \`use()\` hook.


\`\`\`jsx
import React, { Suspense, lazy } from 'react';

const Chart = lazy(() => import('./Chart'));
const Modal = lazy(() => import('./Modal'));

function Dashboard() {
  const [showModal, setShowModal] = useState(false);

  return (
    <div>
      <Suspense fallback={<Spinner />}>
        <Chart />
      </Suspense>

      {showModal && (
        <Suspense fallback={<Spinner />}>
          <Modal onClose={() => setShowModal(false)} />
        </Suspense>
      )}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:235,sortKey:854},{id:"react-855",number:236,title:"What is virtualization (windowing) and when should you use it?",question:"What is virtualization (windowing) and when should you use it?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Virtualization (windowing) renders only the visible portion of a large list, plus a small buffer. The DOM contains only ~20-50 items regardless of list size (10,000+ items).

Use it when:
- Lists have 100+ items
- Each row is moderately expensive to render
- You need smooth scrolling performance

Libraries: \`react-window\` (lightweight, ~5kb), \`react-virtualized\` (feature-rich), \`@tanstack/react-virtual\` (modern, headless).


\`\`\`jsx
import { FixedSizeList } from 'react-window';

function VirtualList({ items }) {
  return (
    <FixedSizeList
      height={500}
      itemCount={items.length}
      itemSize={50}
      width="100%"
    >
      {({ index, style }) => (
        <div style={style}>
          {items[index].name}
        </div>
      )}
    </FixedSizeList>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:236,sortKey:855},{id:"react-856",number:237,title:"Compare react-window and react-virtualized",question:"Compare react-window and react-virtualized",difficulty:"Advanced",category:"Performance & Optimization",answer:`Both are virtualization libraries by Brian Vaughn, but differ significantly:

| Feature | react-window | react-virtualized |
|---------|-------------|-------------------|
| Size | ~5kb | ~30kb+ |
| API | Simple (List, Grid) | Complex (many components) |
| Features | Core virtualization | AutoSizer, CellMeasurer, MultiGrid, SortableList |
| Maintenance | Actively maintained | Legacy (use react-window) |

**Recommendation**: Use \`react-window\` for standard use cases. Use \`react-virtualized\` only if you need its advanced features (variable heights, masonry, etc.). For modern apps, \`@tanstack/react-virtual\` is increasingly popular.


\`\`\`jsx
// react-window - simple and small
import { FixedSizeGrid } from 'react-window';

// react-virtualized - more features
import { Grid, AutoSizer, CellMeasurer } from 'react-virtualized';

// Modern alternative
import { useVirtualizer } from '@tanstack/react-virtual';
\`\`\`
\`\`\`
`,tech:"react",displayNumber:237,sortKey:856},{id:"react-857",number:238,title:"How does the React DevTools Profiler work?",question:"How does the React DevTools Profiler work?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`The Profiler records component render times and reasons. Features:

1. **Record button**: Start/stop a profiling session
2. **Flamegraph**: Visualize render duration hierarchically
3. **Ranked chart**: Sort components by render time
4. **Commit timeline**: See each commit and what changed
5. **Highlight updates**: Visualize re-renders in real-time
6. **Why did this render?**: Shows changed props/state/hooks/context

To enable, ensure you're using the development build. Production builds disable profiling unless you use the special profiling build.


\`\`\`jsx
// Profiler can also be used programmatically
import { Profiler } from 'react';

function onRenderCallback(
  id,              // Component identifier
  phase,           // "mount" or "update"
  actualDuration,  // Time spent rendering
  baseDuration,    // Estimated time without memoization
  startTime,
  commitTime
) {
  console.log(\`[\${id}] \${phase}: \${actualDuration}ms\`);
}

function App() {
  return (
    <Profiler id="App" onRender={onRenderCallback}>
      <ExpensiveTree />
    </Profiler>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:238,sortKey:857},{id:"react-858",number:239,title:"How do you identify and fix re-render performance issues?",question:"How do you identify and fix re-render performance issues?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Step-by-step approach:

1. **Profile** with React DevTools to find slow components
2. **Enable "Highlight updates"** to see what re-renders unnecessarily
3. **Check props**: Are new object/function references created each render?
4. **Wrap with \`React.memo\`** where props are stable
5. **Use \`useCallback\`/\`useMemo\`** for stable references
6. **Split context** so consumers only re-render when relevant data changes
7. **Use \`useTransition\`** for non-urgent updates

Avoid premature optimization - measure first, then fix the actual bottleneck.


\`\`\`jsx
// BEFORE: Re-renders on every parent state change
function Parent() {
  const [count, setCount] = useState(0);
  const [user] = useState({ name: 'John' });

  return (
    <>
      <button onClick={() => setCount(c => c + 1)}>{count}</button>
      <UserProfile user={user} />
    </>
  );
}

// AFTER: Memoize to prevent unnecessary re-renders
const UserProfile = memo(function UserProfile({ user }) {
  return <div>{user.name}</div>;
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:239,sortKey:858},{id:"react-859",number:240,title:"Why are anonymous functions and inline objects problematic in JSX?",question:"Why are anonymous functions and inline objects problematic in JSX?",difficulty:"Basic",category:"Performance & Optimization",answer:`Anonymous functions and inline objects create new references on every render:

\`\`\`jsx
<Button onClick={() => doSomething()} />  // New function each render
<Button style={{ color: 'red' }} />        // New object each render
\`\`\`

When passed to memoized children, this defeats \`React.memo\` (shallow comparison sees different references) and triggers unnecessary re-renders.

Solutions:
- Extract functions with \`useCallback\`
- Extract objects with \`useMemo\`
- Define static objects/arrays outside the component

This matters most when the JSX is inside a frequently-rendering parent or passed to memoized children.


\`\`\`jsx
// BAD: New references every render
function Toolbar({ onSave }) {
  return (
    <div>
      <button onClick={() => onSave()}>Save</button>
      <button style={{ marginLeft: 8 }}>Cancel</button>
    </div>
  );
}

// GOOD: Stable references
const BUTTON_STYLE = { marginLeft: 8 };

function Toolbar({ onSave }) {
  const handleSave = useCallback(() => onSave(), [onSave]);
  return (
    <div>
      <button onClick={handleSave}>Save</button>
      <button style={BUTTON_STYLE}>Cancel</button>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:240,sortKey:859},{id:"react-860",number:241,title:"How do you detect and fix memory leaks in React components?",question:"How do you detect and fix memory leaks in React components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Common memory leak sources:
1. **Uncleared timers/intervals** - \`setInterval\` never cleared
2. **Uncleaned event listeners** - \`window.addEventListener\` without removal
3. **Pending async requests** - State updates after unmount
4. **Subscriptions** - WebSocket, observables not unsubscribed
5. **Global references** - Storing component instances in globals

Fix: Always clean up in \`useEffect\` return function. Use \`AbortController\` for fetch. Check \`isMounted\` ref pattern (or \`useRef\` guard).


\`\`\`jsx
useEffect(() => {
  let isMounted = true;
  const controller = new AbortController();

  fetch('/api/data', { signal: controller.signal })
    .then(res => res.json())
    .then(data => {
      if (isMounted) setData(data);  // Guard against unmount
    });

  const timer = setInterval(() => console.log('tick'), 1000);
  window.addEventListener('resize', handleResize);

  return () => {
    isMounted = false;
    controller.abort();         // Cancel fetch
    clearInterval(timer);       // Clear timer
    window.removeEventListener('resize', handleResize);
  };
}, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:241,sortKey:860},{id:"react-861",number:242,title:"What is the relationship between React state and memory leaks?",question:"What is the relationship between React state and memory leaks?",difficulty:"Advanced",category:"Performance & Optimization",answer:`The "Can't perform a React state update on an unmounted component" warning indicates a leak: async work completes after the component unmounts, attempting to set state on a destroyed component.

React 18 removed this warning (it's now harmless but wasteful), but the underlying issue remains - you're doing work for a component that no longer exists.

Best practice: Cancel async operations on unmount. For fetch, use \`AbortController\`. For timeouts, track and clear them. Modern approach: \`useSyncExternalStore\` handles subscription lifecycle automatically.


\`\`\`jsx
// React 18+ pattern with AbortController
function useUserData(userId) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    const controller = new AbortController();
    fetch(\`/api/users/\${userId}\`, { signal: controller.signal })
      .then(res => res.json())
      .then(setUser)
      .catch(err => {
        if (err.name !== 'AbortError') throw err;
      });

    return () => controller.abort();  // Critical cleanup
  }, [userId]);

  return user;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:242,sortKey:861},{id:"react-862",number:243,title:"How do you optimize Context re-renders?",question:"How do you optimize Context re-renders?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context re-renders ALL consumers when the value changes. Optimizations:

1. **Split context**: Separate frequently-changing from stable values
2. **Selectors**: Use a library like \`use-context-selector\` to subscribe to slices
3. **Memoize context value**: \`useMemo\` the value object
4. **Move state down**: Keep context state minimal
5. **Combine with reducer**: Centralize but isolate updates

The core issue: every consumer re-renders when ANY part of context changes.


\`\`\`jsx
// BAD: Single context, all consumers re-render on count change
const AppContext = createContext();
function App() {
  const [user, setUser] = useState();
  const [count, setCount] = useState(0);
  return (
    <AppContext.Provider value={{ user, count, setUser, setCount }}>
      <Children />
    </AppContext.Provider>
  );
}

// GOOD: Split contexts
const UserContext = createContext();
const CountContext = createContext();

function App() {
  const [user] = useState();
  const [count, setCount] = useState(0);

  return (
    <UserContext.Provider value={user}>
      <CountContext.Provider value={{ count, setCount }}>
        <Children />
      </CountContext.Provider>
    </UserContext.Provider>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:243,sortKey:862},{id:"react-863",number:244,title:"What are context selectors and how do they help performance?",question:"What are context selectors and how do they help performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context selectors let consumers subscribe to only the slice of context they need, preventing re-renders when unrelated parts change.

The native Context API doesn't support selectors - any context value change re-renders all consumers. Libraries like \`use-context-selector\` solve this by using a subscription model under the hood.

In React 18+, \`useSyncExternalStore\` enables selector-based subscriptions for external stores. For context specifically, \`use-context-selector\` is the popular solution.


\`\`\`jsx
import { createContext, useContextSelector } from 'use-context-selector';

const CounterContext = createContext();

function Display() {
  // Only re-renders when 'count' changes, not 'theme'
  const count = useContextSelector(CounterContext, v => v.count);
  return <div>{count}</div>;
}

function ThemeButton() {
  // Only re-renders when 'theme' changes
  const theme = useContextSelector(CounterContext, v => v.theme);
  return <button className={theme}>Themed</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:244,sortKey:863},{id:"react-864",number:245,title:"How do you optimize images in React web applications?",question:"How do you optimize images in React web applications?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Image optimization strategies:

1. **Use modern formats**: WebP, AVIF instead of PNG/JPG (30-50% smaller)
2. **Lazy loading**: \`loading="lazy"\` attribute
3. **Responsive images**: \`srcset\` and \`sizes\` attributes
4. **Next.js Image component**: Automatic optimization, lazy loading, resizing
5. **Placeholder/blur-up**: Show low-res while loading
6. **CDN**: Serve images from optimized CDN with caching
7. **Avoid layout shift**: Always set \`width\` and \`height\`


\`\`\`jsx
// Native HTML
<img
  src="image.webp"
  loading="lazy"
  width={800}
  height={600}
  alt="Description"
  srcSet="image-400.webp 400w, image-800.webp 800w"
  sizes="(max-width: 600px) 400px, 800px"
/>

// Next.js
import Image from 'next/image';
<Image
  src="/photo.jpg"
  alt="Description"
  width={800}
  height={600}
  placeholder="blur"
  blurDataURL="data:image/..."
  priority={false}  // true for LCP images
/>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:245,sortKey:864},{id:"react-865",number:246,title:"What are Core Web Vitals and why do they matter?",question:"What are Core Web Vitals and why do they matter?",difficulty:"Basic",category:"Performance & Optimization",answer:`Core Web Vitals are Google's metrics for user experience:

1. **LCP (Largest Contentful Paint)**: Loading performance. Should be < 2.5s
2. **INP (Interaction to Next Paint)** (replaced FID in 2024): Responsiveness. Should be < 200ms
3. **CLS (Cumulative Layout Shift)**: Visual stability. Should be < 0.1

They affect SEO rankings and user experience. React apps can struggle with these due to client-side rendering delays and hydration.

Measure with Lighthouse, Chrome DevTools, or \`web-vitals\` library.


\`\`\`jsx
import { onCLS, onINP, onLCP } from 'web-vitals';

function sendToAnalytics(metric) {
  console.log(metric.name, metric.value);
}

onLCP(sendToAnalytics);  // Largest Contentful Paint
onINP(sendToAnalytics);  // Interaction to Next Paint
onCLS(sendToAnalytics);  // Cumulative Layout Shift
\`\`\`
\`\`\`
`,tech:"react",displayNumber:246,sortKey:865},{id:"react-866",number:247,title:"How do you optimize LCP (Largest Contentful Paint) in React?",question:"How do you optimize LCP (Largest Contentful Paint) in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`LCP measures when the largest content element (image, text block) renders. Optimize:

1. **Server-side rendering (SSR)**: Render initial HTML on server
2. **Preload critical resources**: \`<link rel="preload">\` for hero images/fonts
3. **Optimize hero images**: Use WebP/AVIF, proper sizing, CDN
4. **Code splitting**: Reduce JS blocking the main thread
5. **Remove render-blocking CSS/JS**
6. **Prioritize LCP element**: Set \`priority\` prop on Next.js Image
7. **Fast fonts**: \`font-display: swap\`, preload font files


\`\`\`jsx
// Next.js: prioritize LCP image
import Image from 'next/image';

function Hero() {
  return (
    <Image
      src="/hero.jpg"
      alt="Hero"
      priority          // Preloads and prioritizes
      width={1200}
      height={600}
    />
  );
}

// Manual preload in head
<head>
  <link rel="preload" as="image" href="/hero.webp" />
  <link rel="preload" as="font" href="/font.woff2" type="font/woff2" crossOrigin="" />
</head>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:247,sortKey:866},{id:"react-867",number:248,title:"What is INP and how does it differ from FID?",question:"What is INP and how does it differ from FID?",difficulty:"Advanced",category:"Performance & Optimization",answer:`- **FID (First Input Delay)**: Measures delay for first user interaction only. Deprecated in 2024.
- **INP (Interaction to Next Paint)**: Measures responsiveness across ALL interactions throughout page lifecycle. It's the 98th percentile of interaction latency.

INP captures the full interaction lifecycle: input delay + processing time + render time.

Optimize INP:
1. **Break up long tasks** with \`useDeferredValue\`/\`useTransition\`
2. **Move work off main thread** (Web Workers)
3. **Avoid expensive re-renders** with memoization
4. **Debounce/throttle** non-urgent updates


\`\`\`jsx
import { useState, useTransition } from 'react';

function SearchResults({ query }) {
  const [isPending, startTransition] = useTransition();
  const [results, setResults] = useState([]);

  function handleChange(e) {
    const value = e.target.value;
    // Mark expensive update as non-urgent
    startTransition(() => {
      setResults(heavyFilter(value));  // Doesn't block input
    });
  }

  return (
    <div className={isPending ? 'opacity-50' : ''}>
      {/* Results */}
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:248,sortKey:867},{id:"react-868",number:249,title:"How do you minimize CLS (Cumulative Layout Shift) in React?",question:"How do you minimize CLS (Cumulative Layout Shift) in React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`CLS measures unexpected layout shifts. Common React causes:

1. **Images without dimensions**: Browser doesn't reserve space
2. **Async content loading**: Ads, embeds, fonts popping in
3. **Late-rendering components**: Conditional rendering without placeholders
4. **Web fonts**: FOIT/FOUT causing text reflow

Fixes:
- Always set \`width\`/\`height\` on images
- Reserve space with \`min-height\` or skeleton loaders
- Use \`font-display: swap\` with \`size-adjust\`
- Avoid inserting content above existing content


\`\`\`jsx
// BAD: No dimensions, causes shift
<img src="/banner.jpg" alt="Banner" />

// GOOD: Reserve space
<img src="/banner.jpg" alt="Banner" width={800} height={200} />

// Skeleton prevents shift for async content
function ProfileCard() {
  const [user, setUser] = useState(null);
  return user ? (
    <div>{user.name}</div>
  ) : (
    <div style={{ height: 100 }} className="skeleton" />
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:249,sortKey:868},{id:"react-869",number:250,title:"What is useMemo and when should you use it?",question:"What is useMemo and when should you use it?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`useMemo\` memoizes a computed value, recomputing only when dependencies change. It prevents expensive calculations on every render.

Use when:
- Expensive calculations (sorting, filtering large arrays)
- Referential equality needed (passing to memoized children)
- Creating objects that must be stable

Don't use for:
- Cheap calculations (overhead isn't worth it)
- As a substitute for state
- Every value "just in case" (premature optimization)


\`\`\`jsx
import { useMemo } from 'react';

function ProductList({ products, searchTerm }) {
  // Only recomputes when products or searchTerm change
  const filtered = useMemo(() => {
    return products.filter(p =>
      p.name.toLowerCase().includes(searchTerm.toLowerCase())
    );
  }, [products, searchTerm]);

  return <List items={filtered} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:250,sortKey:869},{id:"react-870",number:251,title:"What is useCallback and how does it relate to useMemo?",question:"What is useCallback and how does it relate to useMemo?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`useCallback\` memoizes a function reference. It's equivalent to \`useMemo(() => fn, deps)\`. Returns the same function instance across renders when deps are unchanged.

Primary use: Pass stable function references to memoized children (so they don't re-render) and to effects that depend on stable callbacks.

\`\`\`jsx
// These are equivalent
const fn1 = useCallback(() => doSomething(a), [a]);
const fn2 = useMemo(() => () => doSomething(a), [a]);
\`\`\`


\`\`\`jsx
import { useCallback, memo } from 'react';

const Button = memo(function Button({ onClick, children }) {
  return <button onClick={onClick}>{children}</button>;
});

function Toolbar() {
  const [count, setCount] = useState(0);

  // Without useCallback, Button re-renders every time count changes
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []);

  return (
    <>
      <Button onClick={handleClick}>Click</Button>
      <span>{count}</span>
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:251,sortKey:870},{id:"react-871",number:252,title:"What is the difference between useMemo and useCallback?",question:"What is the difference between useMemo and useCallback?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both are memoization hooks, but differ in what they return:

- **useMemo**: Returns a **memoized value** (result of computation)
- **useCallback**: Returns a **memoized function** (the function itself)

Implementation: \`useCallback(fn, deps)\` is literally \`useMemo(() => fn, deps)\`.

Use \`useMemo\` for expensive calculations and stable object/array references. Use \`useCallback\` for stable function references passed to children or effects.


\`\`\`jsx
// useMemo - memoizes the VALUE
const sortedList = useMemo(() => list.sort(), [list]);

// useCallback - memoizes the FUNCTION
const handleSort = useCallback(() => setList(list.sort()), [list]);

// Equivalent to:
const handleSort = useMemo(() => () => setList(list.sort()), [list]);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:252,sortKey:871},{id:"react-872",number:253,title:"What are the pitfalls of over-using useMemo and useCallback?",question:"What are the pitfalls of over-using useMemo and useCallback?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Common pitfalls:

1. **Premature optimization**: Memoization has overhead; for cheap operations it's slower
2. **Incorrect dependencies**: Stale closures if deps are wrong
3. **False sense of security**: Doesn't prevent re-renders from other causes
4. **Memory usage**: Cached values persist in memory
5. **Breaks when deps change**: Spurious re-renders if deps are unstable

Rule: Profile first. Only memoize when you've measured a problem. The React docs say: "You can wrap [a calculation] in useMemo... But you should only do this if the calculation is slow."


\`\`\`jsx
// BAD: Memoizing trivial work
const doubled = useMemo(() => count * 2, [count]);  // Overkill

// BAD: Wrong deps cause bugs
const handler = useCallback(() => {
  console.log(count);  // Stale! count not in deps
}, []);

// GOOD: Only memoize expensive work
const sortedUsers = useMemo(() => {
  return users.sort(complexSortFn);  // 10k+ items
}, [users]);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:253,sortKey:872},{id:"react-873",number:254,title:"How does React's concurrent rendering improve performance?",question:"How does React's concurrent rendering improve performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Concurrent rendering (React 18+) allows React to:

1. **Interrupt rendering**: Pause/resume render work based on priority
2. **Prevent blocking**: User input stays responsive during large renders
3. **Suspense integration**: Render parts of UI while waiting for data

Key APIs:
- \`useTransition\`: Mark non-urgent updates (stay responsive)
- \`useDeferredValue\`: Defer expensive value updates
- \`<Suspense>\`: Show fallbacks for async work

This means React can work on multiple states simultaneously without blocking the main thread.


\`\`\`jsx
import { useState, useDeferredValue, useTransition } from 'react';

function SearchPage() {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();

  // Defer expensive list update
  const deferredQuery = useDeferredValue(query);

  function onChange(e) {
    const value = e.target.value;
    setQuery(value);  // Urgent - updates input immediately
    startTransition(() => {
      // Non-urgent - can be interrupted
      performSearch(value);
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      <Results query={deferredQuery} pending={isPending} />
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:254,sortKey:873},{id:"react-874",number:255,title:"What is useTransition and how does it help with performance?",question:"What is useTransition and how does it help with performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useTransition\` lets you mark state updates as non-urgent ("transitions"), keeping the UI responsive during expensive re-renders.

Returns: \`[isPending, startTransition]\`.
- \`isPending\`: True while transition is in progress
- \`startTransition\`: Wrap state updates that can be deferred

Urgent updates (typing, clicking) render immediately. Non-urgent updates (filtering large lists, tab switches) can be interrupted by more urgent work.


\`\`\`jsx
import { useState, useTransition } from 'react';

function Tabs() {
  const [tab, setTab] = useState('overview');
  const [isPending, startTransition] = useTransition();

  function selectTab(nextTab) {
    startTransition(() => {
      setTab(nextTab);  // Non-urgent update
    });
  }

  return (
    <>
      <TabBar onSelect={selectTab} />
      {isPending ? <Spinner /> : <TabContent tab={tab} />}
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:255,sortKey:874},{id:"react-875",number:256,title:"What is useDeferredValue and when should you use it?",question:"What is useDeferredValue and when should you use it?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useDeferredValue\` returns a deferred copy of a value that lags behind the latest value during expensive renders. Unlike \`useTransition\` which wraps updates, \`useDeferredValue\` wraps the value itself.

Use when:
- You can't wrap the state update in \`startTransition\` (e.g., third-party state)
- You want to keep showing stale data while new data renders
- The value comes from props or external sources

It's essentially "useTransition for values" - React keeps old value visible while computing new one in background.


\`\`\`jsx
import { useState, useDeferredValue } from 'react';

function SearchResults({ query }) {
  const deferredQuery = useDeferredValue(query);
  const results = useMemo(() => {
    return search(deferredQuery);  // Uses stale value during render
  }, [deferredQuery]);

  // Input stays responsive because query updates immediately
  // Results update after a delay
  return (
    <ul>
      {results.map(r => <li key={r.id}>{r.name}</li>)}
    </ul>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:256,sortKey:875},{id:"react-876",number:257,title:"How do you optimize a large form with many inputs?",question:"How do you optimize a large form with many inputs?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Large forms re-render the entire tree on each keystroke. Optimizations:

1. **Controlled vs uncontrolled**: Use uncontrolled inputs (\`defaultValue\` + ref) for simple cases
2. **Field-level state**: Each input manages its own state
3. **Split components**: Isolate state to sub-components
4. **memo each field**: Prevent re-render when other fields change
5. **Debounce validation**: Don't validate on every keystroke
6. **React Hook Form**: Uses uncontrolled inputs + refs (minimal re-renders)
7. **useDeferredValue**: Defer expensive validation


\`\`\`jsx
import { memo, useState } from 'react';
import { useForm } from 'react-hook-form';

// With React Hook Form (uncontrolled, minimal re-renders)
function Form() {
  const { register, handleSubmit } = useForm();
  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input {...register('name')} />
      <input {...register('email')} />
    </form>
  );
}

// Manual: memoize each field
const Field = memo(function Field({ label, value, onChange }) {
  return (
    <label>
      {label}
      <input value={value} onChange={onChange} />
    </label>
  );
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:257,sortKey:876},{id:"react-877",number:258,title:"How does React's reconciliation algorithm affect performance?",question:"How does React's reconciliation algorithm affect performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`React's reconciliation (the "diffing" algorithm) determines what changed between renders. Key performance aspects:

1. **Element type check**: Different types → destroy and rebuild (expensive)
2. **Same type**: Update props in place (cheap)
3. **Keys**: Help identify list items across renders
4. **Children diffing**: O(n) with heuristics, not optimal O(n^3)

Performance implications:
- Avoid changing element types at same position (destroys subtree)
- Use stable, unique \`key\` props in lists
- Keep component identity stable (don't create components inside render)


\`\`\`jsx
// BAD: Changing element type destroys entire subtree
function Widget({ isButton }) {
  return isButton ? <button>Click</button> : <a href="#">Link</a>;
}

// GOOD: Keep element type stable
function Widget({ isButton }) {
  return isButton
    ? <button className="widget">Click</button>
    : <button className="widget link-style">Link</button>;
}

// Keys must be stable and unique
items.map(item => <li key={item.id}>{item.text}</li>);  // Good
items.map((item, i) => <li key={i}>{item.text}</li>);   // Bad - unstable
\`\`\`
\`\`\`
`,tech:"react",displayNumber:258,sortKey:877},{id:"react-878",number:259,title:"Why is it bad to define components inside other components?",question:"Why is it bad to define components inside other components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Defining a component inside another component creates a NEW component type on every render. This causes:

1. **Full remount**: React sees a different component type, destroys and recreates it (loses state)
2. **Performance hit**: Re-mounting is expensive (DOM recreation, effect re-runs)
3. **State loss**: Internal state resets on every parent render
4. **Effect re-runs**: All effects re-execute

Fix: Define components at module level or use composition/children prop.


\`\`\`jsx
// BAD: New component type every render
function Parent() {
  function Child({ name }) {  // Re-created each render
    return <div>{name}</div>;
  }
  return <Child name="John" />;  // Fully remounts on parent re-render
}

// GOOD: Module-level component
function Child({ name }) {
  return <div>{name}</div>;
}

function Parent() {
  return <Child name="John" />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:259,sortKey:878},{id:"react-879",number:260,title:"How do you handle expensive calculations in render?",question:"How do you handle expensive calculations in render?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`For expensive calculations during render:

1. **useMemo**: Cache result, recompute only when deps change
2. **useDeferredValue**: Show stale result while computing new one
3. **Web Workers**: Move heavy computation off main thread
4. **Memoize intermediate steps**: Break computation into memoized chunks
5. **Virtualize**: Only compute visible items
6. **Debounce**: Delay calculation until input settles

Measure first with Profiler to confirm the calculation is actually the bottleneck.


\`\`\`jsx
import { useMemo } from 'react';

function Analytics({ data }) {
  // Expensive: runs only when data changes
  const stats = useMemo(() => {
    return data.reduce((acc, item) => {
      // Heavy computation here
      return computeDetailedStats(acc, item);
    }, initialStats);
  }, [data]);

  return <StatsDisplay stats={stats} />;
}

// Alternative: Web Worker for very heavy work
const result = useWorker(heavyComputation, inputData);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:260,sortKey:879},{id:"react-880",number:261,title:"What is the purpose of the key prop and how does it affect performance?",question:"What is the purpose of the key prop and how does it affect performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`The \`key\` prop helps React identify which items in a list changed, were added, or removed. Without keys, React uses index-based diffing which is error-prone.

Performance impact:
- **Stable unique keys** (e.g., database IDs): Optimal reconciliation, minimal DOM operations
- **Index keys**: Causes unnecessary re-renders and bugs when list order changes
- **Missing keys**: Forces full re-render of list

Keys must be stable across renders (not random, not array index when items reorder).


\`\`\`jsx
// GOOD: Stable unique identifier
{todos.map(todo => (
  <TodoItem key={todo.id} todo={todo} />
))}

// BAD: Array index as key (breaks on reorder/insert)
{todos.map((todo, index) => (
  <TodoItem key={index} todo={todo} />
))}

// BAD: Random key (always remounts)
{todos.map(todo => (
  <TodoItem key={Math.random()} todo={todo} />
))}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:261,sortKey:880},{id:"react-881",number:262,title:"How do you optimize bundle size in a React app?",question:"How do you optimize bundle size in a React app?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Bundle size optimization techniques:

1. **Code splitting**: \`React.lazy\` + \`Suspense\` for routes/features
2. **Tree shaking**: Use ES modules, avoid side-effectful imports
3. **Bundle analyzer**: Identify large dependencies
4. **Replace heavy libs**: \`date-fns\` instead of \`moment\`, \`lodash-es\` with specific imports
5. **Compression**: Gzip/Brotli on server
6. **Modern build target**: Target modern browsers, use differential loading
7. **Remove unused code**: Dead code elimination


\`\`\`jsx
// BAD: Imports entire library
import _ from 'lodash';

// GOOD: Import only what you need
import debounce from 'lodash/debounce';

// Or use tree-shakeable lodash-es
import { debounce } from 'lodash-es';

// Analyze bundle
// npx webpack-bundle-analyzer stats.json
// or: npm install --save-dev webpack-bundle-analyzer
\`\`\`
\`\`\`
`,tech:"react",displayNumber:262,sortKey:881},{id:"react-882",number:263,title:"What is tree shaking and how does it work with React?",question:"What is tree shaking and how does it work with React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Tree shaking is dead-code elimination that removes unused exports from the final bundle. It works with ES modules (\`import\`/\`export\`) because they're statically analyzable, but NOT with CommonJS (\`require\`).

Requirements:
- Use ES module syntax (\`import\`/\`export\`)
- Build tool must support it (webpack, Rollup, Vite all do)
- Avoid side effects in modules (\`"sideEffects": false\` in package.json)
- Don't use \`import * as\` when you need specific parts

React itself is tree-shakeable in modern versions.


\`\`\`jsx
// Tree-shakeable: only DatePicker imported
import { DatePicker } from 'antd';

// NOT tree-shakeable (default import, pulls everything)
import antd from 'antd';

// package.json
{
  "sideEffects": false  // Enables aggressive tree shaking
}

// Build config (webpack)
optimization: {
  usedExports: true,
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:263,sortKey:882},{id:"react-883",number:264,title:"How do you measure React app performance?",question:"How do you measure React app performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`Measurement tools and techniques:

1. **React DevTools Profiler**: Component render times, re-render reasons
2. **Chrome DevTools Performance tab**: Main thread activity, long tasks
3. **Lighthouse**: Core Web Vitals, best practices
4. **web-vitals library**: Programmatic metric collection
5. **why-did-you-render**: Detect unnecessary re-renders
6. **Bundle analyzer**: Visualize bundle composition
7. **Performance API**: \`performance.now()\` for custom timing

Always measure in production build (dev has overhead) and on real devices.


\`\`\`jsx
// Install why-did-you-render for dev
// npm i -D @welldone-software/why-did-you-render

import whyDidYouRender from '@welldone-software/why-did-you-render';
whyDidYouRender(React, { trackAllPureComponents: true });

// Programmatic timing
const start = performance.now();
heavyOperation();
const duration = performance.now() - start;
console.log(\`Operation took \${duration}ms\`);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:264,sortKey:883},{id:"react-884",number:265,title:"What is the performance impact of CSS-in-JS libraries?",question:"What is the performance impact of CSS-in-JS libraries?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Runtime CSS-in-JS (styled-components, emotion) has performance costs:

1. **Runtime serialization**: Styles computed during render
2. **Re-renders**: Style recalculation on every render
3. **Bundle size**: Library code adds weight

Mitigations:
- Use \`babel-plugin-styled-components\` for static extraction
- Use compiled CSS-in-JS (vanilla-extract, linaria) - zero runtime
- Use CSS modules for static styles
- Avoid dynamic styles based on props when possible

CSS-in-JS tradeoff: developer experience vs runtime performance. For high-performance needs, prefer static CSS or build-time solutions.


\`\`\`jsx
// Runtime CSS-in-JS - recalculated each render
const Button = styled.button\`
  color: \${props => props.color};  // Dynamic, runtime cost
\`;

// Build-time (vanilla-extract) - zero runtime
import { style } from '@vanilla-extract/css';

const button = style({
  color: 'red',  // Static, extracted at build
});

// CSS Modules - also zero runtime
import styles from './Button.module.css';
<button className={styles.button} />;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:265,sortKey:884},{id:"react-885",number:266,title:"How do you optimize React apps for low-end devices?",question:"How do you optimize React apps for low-end devices?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Low-end device optimizations:

1. **Reduce JS execution**: Smaller bundles, code splitting
2. **Avoid long tasks**: Break work with \`useTransition\`/\`scheduler\`
3. **Virtualize lists**: Limit DOM nodes
4. **Debounce/throttle**: Reduce event handler frequency
5. **Lazy load heavy features**: Only load when needed
6. **Minimize re-renders**: Aggressive memoization
7. **Use passive event listeners**: \`{ passive: true }\`
8. **Optimize images**: Smaller formats, responsive sizes
9. **Avoid layout thrashing**: Batch DOM reads/writes


\`\`\`jsx
// Passive listeners for scroll
useEffect(() => {
  const handler = () => console.log('scroll');
  window.addEventListener('scroll', handler, { passive: true });
  return () => window.removeEventListener('scroll', handler);
}, []);

// Reduce work with transitions
const [isPending, startTransition] = useTransition();
startTransition(() => setHeavyState(data));

// Virtualize for low memory
<VirtualizedList items={items} />  // Renders only visible
\`\`\`
\`\`\`
`,tech:"react",displayNumber:266,sortKey:885},{id:"react-886",number:267,title:"What is the difference between useMemo for values vs React.memo for components?",question:"What is the difference between useMemo for values vs React.memo for components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both prevent unnecessary work, but at different levels:

- **React.memo**: Component-level. Skips re-rendering the entire component when props are shallow-equal. Changes the render decision for the component.
- **useMemo**: Calculation-level. Caches a computed value within a component across renders. The component still renders, but skips the expensive calculation.

They're complementary: \`React.memo\` prevents the render, \`useMemo\` makes the render cheaper.


\`\`\`jsx
// React.memo: prevents Child from re-rendering
const Child = memo(function Child({ data }) {
  return <ExpensiveView data={data} />;
});

// useMemo: makes calculation cheaper when Child DOES render
function ExpensiveView({ data }) {
  const processed = useMemo(() => {
    return data.map(transform).filter(predicate);
  }, [data]);

  return <List items={processed} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:267,sortKey:886},{id:"react-887",number:268,title:"How do you debounce and throttle in React for performance?",question:"How do you debounce and throttle in React for performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`**Debounce**: Delays execution until after a pause (good for search input).
**Throttle**: Limits execution to once per interval (good for scroll/resize).

In React, create stable debounced/throttled functions with \`useRef\` or \`useMemo\` to avoid recreating them each render.

Use cases:
- Search-as-you-type (debounce 300ms)
- Window resize handlers (throttle)
- Scroll position tracking (throttle)
- Auto-save (debounce)


\`\`\`jsx
import { useMemo } from 'react';
import { debounce, throttle } from 'lodash-es';

function SearchBox() {
  const [results, setResults] = useState([]);

  // Stable debounced function
  const debouncedSearch = useMemo(
    () => debounce(query => fetchResults(query), 300),
    []
  );

  function onChange(e) {
    debouncedSearch(e.target.value);
  }

  // Throttled scroll handler
  const handleScroll = useMemo(
    () => throttle(() => console.log('scroll'), 100),
    []
  );

  useEffect(() => {
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, [handleScroll]);

  return <input onChange={onChange} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:268,sortKey:887},{id:"react-888",number:269,title:"How do you optimize list rendering in React?",question:"How do you optimize list rendering in React?",difficulty:"Basic",category:"Performance & Optimization",answer:`List rendering optimization techniques:

1. **Virtualization**: Render only visible items (react-window)
2. **memo list items**: Prevent re-render when other items change
3. **Stable keys**: Use unique IDs, not array indices
4. **windowing for large data**: Process in chunks
5. **Avoid inline functions in map**: Extract callbacks
6. **Pagination**: Limit items rendered at once
7. **useMemo for derived data**: Filter/sort once


\`\`\`jsx
import { memo, useMemo } from 'react';

const ListItem = memo(function ListItem({ item, onSelect }) {
  return <li onClick={() => onSelect(item.id)}>{item.name}</li>;
});

function OptimizedList({ items, onSelect }) {
  const handleSelect = useCallback((id) => onSelect(id), [onSelect]);

  const sortedItems = useMemo(
    () => [...items].sort((a, b) => a.name.localeCompare(b.name)),
    [items]
  );

  return (
    <ul>
      {sortedItems.map(item => (
        <ListItem key={item.id} item={item} onSelect={handleSelect} />
      ))}
    </ul>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:269,sortKey:888},{id:"react-889",number:270,title:"What is the role of React.StrictMode in performance?",question:"What is the role of React.StrictMode in performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`React.StrictMode\` is a development-only tool that intentionally double-invokes certain functions to surface bugs:
- Function component bodies (render)
- useState/useMemo/useReducer initializers
- Effect setup/cleanup (runs twice)

It does NOT affect production. The double-invocation helps detect:
- Impure renders (side effects in render)
- Missing effect cleanup (memory leaks)
- Stale closures

Performance note: Your code should run correctly under double-invocation. If it breaks, you have a side-effect bug. Don't disable StrictMode to "fix" performance - fix the underlying issue.


\`\`\`jsx
import { StrictMode } from 'react';

// Wrap app in StrictMode for dev checks
<StrictMode>
  <App />
</StrictMode>

// Effects run twice in dev - must be idempotent
useEffect(() => {
  const subscription = subscribe();
  return () => subscription.unsubscribe();  // Cleanup prevents double-subscribe
}, []);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:270,sortKey:889},{id:"react-890",number:271,title:"How does server-side rendering (SSR) improve performance?",question:"How does server-side rendering (SSR) improve performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`SSR renders React components to HTML on the server, sending complete HTML to the client. Benefits:

1. **Faster FCP/LCP**: Content visible before JS loads
2. **Better SEO**: Crawlers see rendered content
3. **No blank screen**: Users see content immediately
4. **Lower TTI on slow devices**: Less client computation

Tradeoffs:
- Higher server load
- Slower TTFB (server computation)
- Complexity (hydration)

Modern alternatives: SSG (static generation), ISR (incremental static regeneration), Streaming SSR.


\`\`\`jsx
// Express + React SSR
import { renderToString } from 'react-dom/server';
import App from './App';

app.get('/', (req, res) => {
  const html = renderToString(<App />);
  res.send(\`<!DOCTYPE html><html><body>\${html}</body></html>\`);
});

// Next.js handles SSR automatically
export async function getServerSideProps() {
  const data = await fetchData();
  return { props: { data } };
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:271,sortKey:890},{id:"react-891",number:272,title:"What is hydration and how can it be optimized?",question:"What is hydration and how can it be optimized?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Hydration is React "attaching" event listeners to server-rendered HTML, making it interactive. The problem: hydration is synchronous and blocks the main thread, delaying interactivity (hurts TTI/INP).

Optimizations:
1. **Streaming SSR**: Send HTML in chunks with \`<Suspense>\`
2. **Selective hydration** (React 18): Hydrate parts as they arrive
3. **Progressive hydration**: Hydrate on idle/visible
4. **Defer non-critical JS**: Don't load admin components initially
5. **\`hydrationMismatch\` handling**: Avoid SSR/client mismatches


\`\`\`jsx
// Next.js App Router - streaming with Suspense
import { Suspense } from 'react';

export default function Page() {
  return (
    <div>
      <h1>Static Header</h1>
      <Suspense fallback={<Skeleton />}>
        <SlowComponent />  {/* Streams in separately */}
      </Suspense>
    </div>
  );
}

// Selective hydration happens automatically in React 18
// for Suspense boundaries
\`\`\`
\`\`\`
`,tech:"react",displayNumber:272,sortKey:891},{id:"react-892",number:273,title:"How do you use the useId hook for performance and accessibility?",question:"How do you use the useId hook for performance and accessibility?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useId\` generates stable, unique IDs for accessibility attributes (aria-labelledby, htmlFor/id pairs) without causing hydration mismatches.

Before \`useId\`, developers used \`Math.random()\` or \`Date.now()\` which caused SSR/client mismatches (different IDs each render → hydration error).

It's performant because React generates IDs deterministically during render - no Math.random, no flaky hydration.


\`\`\`jsx
import { useId } from 'react';

function FormField({ label, type }) {
  const id = useId();  // Stable across SSR + hydration

  return (
    <div>
      <label htmlFor={id}>{label}</label>
      <input id={id} type={type} />
    </div>
  );
}

// Avoid this (hydration mismatch):
const id = \`field-\${Math.random()}\`;  // BAD - different on server/client
\`\`\`
\`\`\`
`,tech:"react",displayNumber:273,sortKey:892},{id:"react-893",number:274,title:"What is the performance cost of Context vs Redux/Zustand?",question:"What is the performance cost of Context vs Redux/Zustand?",difficulty:"Advanced",category:"Performance & Optimization",answer:`**Context**: Every consumer re-renders on ANY value change. No selector support natively. Fine for low-frequency updates (theme, auth) but problematic for high-frequency state (forms, real-time data).

**Redux**: Uses reference equality + selectors. Components only re-render when their selected slice changes. More boilerplate but better performance for complex state.

**Zustand**: Like Redux but minimal. Uses selector subscriptions - components subscribe to specific state slices, re-render only on those changes. No provider needed.

Verdict: Context for static/low-frequency; Zustand/Redux for dynamic/high-frequency state.


\`\`\`jsx
// Zustand - selector-based, only re-renders on slice change
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  theme: 'dark',
  increment: () => set(s => ({ count: s.count + 1 })),
}));

// Only re-renders when count changes
function Counter() {
  const count = useStore(s => s.count);
  return <div>{count}</div>;
}

// Only re-renders when theme changes
function ThemeButton() {
  const theme = useStore(s => s.theme);
  return <button className={theme}>Theme</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:274,sortKey:893},{id:"react-894",number:275,title:"How do you optimize React apps for SEO and performance simultaneously?",question:"How do you optimize React apps for SEO and performance simultaneously?",difficulty:"Advanced",category:"Performance & Optimization",answer:`SEO and performance align through fast-loading, crawlable content:

1. **SSR/SSG**: Render content server-side for crawlers + fast FCP
2. **Core Web Vitals**: Good LCP/INP/CLS = better rankings
3. **Semantic HTML**: Proper headings, meta tags, structured data
4. **Preload critical resources**: Faster LCP helps SEO
5. **Sitemap + robots.txt**: Help crawlers
6. **Meta tags**: Title, description, Open Graph
7. **Avoid client-only rendering** for important content

Next.js solves both with SSR, metadata API, and image optimization.


\`\`\`jsx
// Next.js Metadata API (App Router)
export const metadata = {
  title: 'Product Page',
  description: 'Best product ever',
  openGraph: {
    title: 'Product Page',
    images: ['/og-image.jpg'],
  },
};

// SSG for static content (fast + SEO-friendly)
export async function generateStaticParams() {
  const products = await getProducts();
  return products.map(p => ({ id: p.id }));
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:275,sortKey:894},{id:"react-895",number:276,title:"What are web workers and when should you use them with React?",question:"What are web workers and when should you use them with React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Web Workers run JavaScript in a separate thread, keeping the main thread (UI) responsive. Use them for:

- Heavy computations (sorting, parsing, encryption)
- Data processing (CSV/JSON transformation)
- Image/video processing
- Real-time data transforms

React integration: Use \`useWorker\` patterns or libraries like \`comlink\`. Pass serializable data only (no functions/DOM).

The main thread stays free for rendering and user input while workers crunch data.


\`\`\`jsx
// worker.js
self.onmessage = (e) => {
  const result = heavyCompute(e.data);
  self.postMessage(result);
};

// React component
import { useEffect, useState, useRef } from 'react';

function HeavyComponent() {
  const [result, setResult] = useState(null);
  const workerRef = useRef(null);

  useEffect(() => {
    workerRef.current = new Worker('/worker.js');
    workerRef.current.onmessage = (e) => setResult(e.data);
    return () => workerRef.current.terminate();
  }, []);

  function process(data) {
    workerRef.current.postMessage(data);
  }

  return <div>{result}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:276,sortKey:895},{id:"react-896",number:277,title:"How do you profile and reduce long tasks?",question:"How do you profile and reduce long tasks?",difficulty:"Advanced",category:"Performance & Optimization",answer:`**Long tasks** (>50ms) block the main thread, hurting INP. To identify:

1. Chrome DevTools Performance tab - look for red "long task" markers
2. \`PerformanceObserver\` API for \`longtask\` entries
3. React Profiler - find slow components

Reduction strategies:
- **Break up work**: Process in chunks with \`requestIdleCallback\`
- **useTransition**: Mark non-urgent updates interruptible
- **Web Workers**: Move computation off main thread
- **Code splitting**: Reduce initial JS parse time
- **Yield to browser**: Split loops with \`setTimeout(0)\` or scheduler


\`\`\`jsx
// Detect long tasks
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    console.log(\`Long task: \${entry.duration}ms\`);
  }
});
observer.observe({ entryTypes: ['longtask'] });

// Break up loop with yielding
async function processLargeArray(items) {
  const results = [];
  for (let i = 0; i < items.length; i++) {
    results.push(process(items[i]));
    if (i % 100 === 0) {
      await new Promise(resolve => setTimeout(resolve));  // Yield
    }
  }
  return results;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:277,sortKey:896},{id:"react-897",number:278,title:"How do you handle expensive third-party library integrations?",question:"How do you handle expensive third-party library integrations?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Third-party libs (charts, editors, maps) can be heavy and slow. Optimizations:

1. **Lazy load**: \`React.lazy\` so they load on demand
2. **Dynamic import**: Only load when feature is used
3. **Isolate in iframe/web worker**: Prevent blocking main thread
4. **Memoize instances**: Create once, reuse
5. **Virtualize**: For data-heavy widgets (charts with many points)
6. **Debounce updates**: Avoid re-initializing on every change
7. **Web Worker**: For data processing before sending to lib


\`\`\`jsx
import { lazy, Suspense, useMemo } from 'react';
import { useRef, useEffect } from 'react';

// Lazy load heavy chart library
const Chart = lazy(() => import('recharts').then(m => ({ default: m.LineChart })));

function Dashboard() {
  const chartRef = useRef(null);

  // Initialize once, update data without re-init
  useEffect(() => {
    const chart = initChart(chartRef.current);
    return () => chart.destroy();
  }, []);

  return (
    <Suspense fallback={<Spinner />}>
      <Chart ref={chartRef} data={data} />
    </Suspense>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:278,sortKey:897},{id:"react-898",number:279,title:"How do you optimize React Native performance? (Bonus cross-platform)",question:"How do you optimize React Native performance? (Bonus cross-platform)",difficulty:"Intermediate",category:"Performance & Optimization",answer:`React Native shares many React optimization principles but with native specifics:

1. **Use \`React.memo\`/\`useCallback\`**: Same as web
2. **Virtualized lists**: \`FlatList\` instead of \`ScrollView\` for long lists
3. **Avoid inline functions/styles**: Re-created each render
4. **Use \`useNativeDriver\`**: Animate on native thread, not JS
5. **Hermes engine**: Faster startup, lower memory
6. **Avoid bridge overload**: Batch native calls
7. **New Architecture (Fabric/TurboModules)**: Direct native communication


\`\`\`jsx
import { FlatList, Animated, useNativeDriver } from 'react-native';

// Virtualized list - renders only visible items
<FlatList
  data={items}
  keyExtractor={item => item.id}
  renderItem={({ item }) => <Item item={item} />}
  initialNumToRender={10}
/>

// Animate on native thread
Animated.timing(opacity, {
  toValue: 1,
  duration: 300,
  useNativeDriver: true,  // Critical for performance
}).start();
\`\`\`
\`\`\`
`,tech:"react",displayNumber:279,sortKey:898},{id:"react-899",number:280,title:"What is the difference between shouldComponentUpdate and React.memo?",question:"What is the difference between shouldComponentUpdate and React.memo?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both control re-rendering but for different paradigms:

**shouldComponentUpdate (Class)**:
- Lifecycle method returning boolean
- \`this.props\`/\`this.state\` comparison
- Class components only
- Manual deep/shallow comparison logic

**React.memo (Function)**:
- Higher-order component wrapper
- Automatic shallow prop comparison
- Function components only
- Optional custom comparator

\`PureComponent\` is the class equivalent of \`React.memo\` (shallow comparison built-in).


\`\`\`jsx
// Class: shouldComponentUpdate
class UserCard extends React.Component {
  shouldComponentUpdate(nextProps) {
    return nextProps.user.id !== this.props.user.id ||
           nextProps.user.name !== this.props.user.name;
  }
  render() { return <div>{this.props.user.name}</div>; }
}

// Function: React.memo
const UserCard = memo(
  function UserCard({ user }) {
    return <div>{user.name}</div>;
  },
  (prev, next) =>
    prev.user.id === next.user.id &&
    prev.user.name === next.user.name
);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:280,sortKey:899},{id:"react-900",number:281,title:"How do you prevent unnecessary re-renders in a context-heavy app?",question:"How do you prevent unnecessary re-renders in a context-heavy app?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context-heavy apps suffer from mass re-renders. Solutions:

1. **Split contexts** by update frequency (static vs dynamic)
2. **Use selectors** (\`use-context-selector\`, Zustand)
3. **Memoize context value** with \`useMemo\`
4. **Separate state from dispatch**: Context for state, separate for actions
5. **Move state to leaf components**: Only components that need it
6. **Use external store** (\`useSyncExternalStore\`) for fine-grained updates

The key insight: context value identity changes → all consumers re-render. Keep value stable.


\`\`\`jsx
// Pattern: Separate state and actions contexts
const StateContext = createContext();
const DispatchContext = createContext();

function Provider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  const stableDispatch = useMemo(() => dispatch, []);  // Stable ref

  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={stableDispatch}>
        {children}
      </DispatchContext.Provider>
    </StateContext.Provider>
  );
}

// Components using only dispatch never re-render on state change
function ActionButton() {
  const dispatch = useContext(DispatchContext);
  return <button onClick={() => dispatch({ type: 'INC' })}>+</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:281,sortKey:900},{id:"react-901",number:282,title:"What is the impact of prop drilling on performance and how do you avoid it?",question:"What is the impact of prop drilling on performance and how do you avoid it?",difficulty:"Basic",category:"Performance & Optimization",answer:`Prop drilling (passing props through many intermediate components) doesn't directly hurt performance, but:

1. **Re-renders cascade**: Changing a drilled prop re-renders all intermediate components
2. **Tight coupling**: Hard to optimize isolated subtrees
3. **Maintenance cost**: Changes ripple through many files

Avoid with:
- **Context** (for low-frequency state)
- **Composition** (children/ render props)
- **State management** (Zustand/Redux for high-frequency)
- **Component extraction**: Pass JSX as children instead of props


\`\`\`jsx
// BAD: Drilling through intermediate components
<Layout user={user}>
  <Page user={user}>
    <Header user={user}>
      <Profile user={user} />  // Only this needs user
    </Header>
  </Page>
</Layout>

// GOOD: Composition - pass as children
<Layout>
  <Page>
    <Header>
      <Profile user={user} />  // Direct
    </Header>
  </Page>
</Layout>

// GOOD: Context for widely-needed state
<UserContext.Provider value={user}>
  <DeepTree />  {/* Profile consumes directly */}
</UserContext.Provider>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:282,sortKey:901},{id:"react-902",number:283,title:"How do you optimize images with the Next.js Image component?",question:"How do you optimize images with the Next.js Image component?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Next.js \`<Image>\` automatically optimizes:

1. **Lazy loading** by default (\`loading="lazy"\`)
2. **Responsive sizes** via \`srcset\` (serves correct size)
3. **Modern formats** (WebP/AVIF) automatically
4. **Prevents CLS** with reserved space (requires width/height)
5. **Priority prop** for LCP images (preloads)
6. **Placeholder** for blur-up loading
7. **CDN optimization** via Next.js image optimizer


\`\`\`jsx
import Image from 'next/image';

// LCP image - prioritize
<Image
  src="/hero.jpg"
  alt="Hero"
  width={1200}
  height={600}
  priority          // Preloads for LCP
/>

// Below-the-fold - lazy by default
<Image
  src="/gallery.jpg"
  alt="Gallery"
  width={400}
  height={300}
  placeholder="blur"
  blurDataURL="data:image/jpeg;base64,..."
/>

// Responsive
<Image
  src="/product.jpg"
  alt="Product"
  sizes="(max-width: 768px) 100vw, 33vw"
  fill              // Fill parent container
/>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:283,sortKey:902},{id:"react-903",number:284,title:"What is the performance difference between inline styles and CSS classes?",question:"What is the performance difference between inline styles and CSS classes?",difficulty:"Basic",category:"Performance & Optimization",answer:`**Inline styles** (\`style={{}}\`):
- Recalculated every render (object creation)
- No CSS cascade, specificity issues
- No pseudo-classes (\`:hover\`), media queries, keyframes
- Slower for static styles

**CSS classes**:
- Browser-optimized, cached stylesheets
- Support full CSS features
- No per-render object creation
- Better for performance and maintainability

Use inline styles only for dynamic values that can't be pre-computed. Prefer CSS modules/classes for everything else.


\`\`\`jsx
// BAD: New object every render, no CSS features
function Bad({ active }) {
  return (
    <div style={{
      color: active ? 'red' : 'blue',
      padding: '10px',
    }}>
      Content
    </div>
  );
}

// GOOD: CSS class, computed once, supports :hover
import styles from './Component.module.css';
function Good({ active }) {
  return (
    <div className={\`\${styles.box} \${active ? styles.active : ''}\`}>
      Content
    </div>
  );
}

// OK: Inline only for truly dynamic values
<div style={{ width: \`\${progress}%\` }} />
\`\`\`
\`\`\`
`,tech:"react",displayNumber:284,sortKey:903},{id:"react-904",number:285,title:"How do you optimize a React app's initial load time?",question:"How do you optimize a React app's initial load time?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Initial load optimization:

1. **Code splitting**: Lazy load routes/features
2. **SSR/SSG**: Send rendered HTML
3. **Bundle size**: Tree shaking, remove unused deps
4. **Preload critical resources**: Fonts, hero images
5. **Compression**: Brotli/Gzip
6. **CDN**: Serve assets close to users
7. **Font optimization**: \`font-display: swap\`, preload
8. **Eliminate render-blocking JS/CSS**
9. **Service worker / caching**: Cache app shell

Measure with Lighthouse and target < 2.5s LCP.


\`\`\`jsx
// Preload in index.html
<link rel="preload" href="/critical.css" as="style" />
<link rel="preload" href="/hero.avif" as="image" />
<link rel="preconnect" href="https://api.example.com" />

// Code split routes
const Home = lazy(() => import('./Home'));
const About = lazy(() => import('./About'));

// Manifest for caching
// service-worker.js caches app shell for instant loads
\`\`\`
\`\`\`
`,tech:"react",displayNumber:285,sortKey:904},{id:"react-905",number:286,title:"What is the useSyncExternalStore hook and how does it optimize performance?",question:"What is the useSyncExternalStore hook and how does it optimize performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`\`useSyncExternalStore\` subscribes to external stores (Redux, Zustand, browser APIs) with safe concurrent rendering. It replaces \`useSubscription\` and prevents tearing.

Benefits:
1. **Fine-grained subscriptions**: Component re-renders only when its slice changes
2. **Concurrent-safe**: No tearing during interrupts
3. **SSR support**: \`getServerSnapshot\` for server rendering
4. **No extra re-renders**: Unlike Context, only subscribers update

This is how modern state libs (Zustand, Redux Toolkit) achieve selector-based performance.


\`\`\`jsx
import { useSyncExternalStore } from 'react';

// Subscribe to window width with fine-grained updates
function useWindowWidth() {
  const subscribe = (callback) => {
    window.addEventListener('resize', callback);
    return () => window.removeEventListener('resize', callback);
  };

  const getSnapshot = () => window.innerWidth;

  return useSyncExternalStore(subscribe, getSnapshot, () => 0);
}

function ResponsiveComponent() {
  const width = useWindowWidth();  // Only re-renders on width change
  return <div>Width: {width}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:286,sortKey:905},{id:"react-906",number:287,title:"How do you detect and prevent layout thrashing in React?",question:"How do you detect and prevent layout thrashing in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Layout thrashing = interleaving DOM reads (forces reflow) and writes (invalidates layout) in a loop. Each read after a write forces synchronous reflow - very expensive.

Prevention:
1. **Batch reads then writes**: Read all first, then write all
2. **Use \`useLayoutEffect\` carefully**: Runs synchronously, can cause thrashing
3. **requestAnimationFrame**: Batch DOM operations per frame
4. **Avoid measuring in loops**: Cache measurements
5. **CSS transforms**: Use \`transform\` (GPU) instead of \`top/left\`


\`\`\`jsx
// BAD: Read/write interleaved = forced reflow each iteration
items.forEach(item => {
  const height = item.offsetHeight;  // READ
  item.style.height = height * 2 + 'px';  // WRITE
});

// GOOD: Batch all reads, then all writes
const heights = items.map(item => item.offsetHeight);  // All READS
items.forEach((item, i) => {
  item.style.height = heights[i] * 2 + 'px';  // All WRITES
});

// Better: useLayoutEffect for measurements
useLayoutEffect(() => {
  const heights = items.map(ref => ref.offsetHeight);
  // Batch writes in same frame
}, [items]);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:287,sortKey:906},{id:"react-907",number:288,title:"How do you optimize font loading to prevent CLS and improve LCP?",question:"How do you optimize font loading to prevent CLS and improve LCP?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Font loading affects both CLS (FOUT/FOIT reflow) and LCP (text is often LCP element).

Optimizations:
1. **\`font-display: swap\`**: Show fallback immediately, swap when loaded (prevents invisible text)
2. **Preload critical fonts**: \`<link rel="preload" as="font">\`
3. **\`size-adjust\`**: Match fallback font metrics to reduce shift
4. **Self-host fonts**: Avoid third-party requests
5. **Subset fonts**: Only include needed characters (especially for CJK)
6. **Use \`next/font\`**: Automatic optimization in Next.js


\`\`\`jsx
/* font-face with swap + size-adjust */
@font-face {
  font-family: 'Custom';
  src: url('/font.woff2') format('woff2');
  font-display: swap;
  size-adjust: 105%;
  ascent-override: 90%;
}

/* Preload in HTML */
<link
  rel="preload"
  href="/font.woff2"
  as="font"
  type="font/woff2"
  crossorigin
/>

/* Next.js automatic font optimization */
import { Inter } from 'next/font/google';
const inter = Inter({ subsets: ['latin'], display: 'swap' });
\`\`\`
\`\`\`
`,tech:"react",displayNumber:288,sortKey:907},{id:"react-908",number:289,title:"What is the difference between passive and active event listeners?",question:"What is the difference between passive and active event listeners?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Event listeners affect scroll/input performance:

**Passive listeners** (\`{ passive: true }\`):
- Browser knows listener won't \`preventDefault()\`
- Allows scrolling without waiting for JS
- Required for \`touchstart\`/\`wheel\` in modern browsers
- Improves scroll performance (no blocking)

**Active listeners** (default):
- Browser waits for listener to finish (in case it calls \`preventDefault\`)
- Can block scrolling if listener is slow

Use passive for scroll/touch/wheel handlers. Next.js sets this automatically for touch events.


\`\`\`jsx
useEffect(() => {
  const handler = (e) => {
    // Reading scroll position - don't preventDefault
    console.log(window.scrollY);
  };

  // Passive: browser can scroll without waiting
  window.addEventListener('scroll', handler, { passive: true });

  return () => window.removeEventListener('scroll', handler);
}, []);

// To prevent default, must be active (not passive)
const blockScroll = (e) => e.preventDefault();
element.addEventListener('touchmove', blockScroll);  // Active
\`\`\`
\`\`\`
`,tech:"react",displayNumber:289,sortKey:908},{id:"react-909",number:290,title:"How do you implement infinite scrolling efficiently?",question:"How do you implement infinite scrolling efficiently?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Efficient infinite scroll combines several techniques:

1. **IntersectionObserver**: Detect when sentinel enters viewport (no scroll listeners)
2. **Virtualization**: Only render loaded items (for millions of rows)
3. **Pagination**: Fetch in chunks, not all at once
4. **Debounce fetch**: Avoid duplicate requests
5. **Prefetch next page**: Load before user reaches bottom
6. **Maintain scroll position**: Use keys, avoid layout shift

Avoid scroll event listeners (use IntersectionObserver instead).


\`\`\`jsx
import { useEffect, useRef, useState } from 'react';

function InfiniteList({ fetchItems }) {
  const [items, setItems] = useState([]);
  const [page, setPage] = useState(0);
  const sentinelRef = useRef(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        if (entries[0].isIntersecting) {
          setPage(p => p + 1);  // Trigger next page load
        }
      },
      { rootMargin: '100px' }  // Prefetch before visible
    );
    observer.observe(sentinelRef.current);
    return () => observer.disconnect();
  }, []);

  useEffect(() => {
    fetchItems(page).then(newItems => setItems(prev => [...prev, ...newItems]));
  }, [page]);

  return (
    <div>
      {items.map(item => <Item key={item.id} {...item} />)}
      <div ref={sentinelRef} />
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:290,sortKey:909},{id:"react-910",number:291,title:"How do you optimize React app memory usage?",question:"How do you optimize React app memory usage?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Memory optimization:

1. **Clean up effects**: Clear timers, unsubscribe, abort fetches
2. **Avoid storing large data in state**: Use refs or external stores
3. **Remove event listeners**: In effect cleanup
4. **Virtualize lists**: Limit DOM nodes (10k items = 10k nodes otherwise)
5. **Debounce expensive computations**: Avoid retaining intermediate arrays
6. **Dispose Web Workers**: \`terminate()\` when done
7. **Avoid closures capturing large objects**: They prevent GC
8. **Profile with Chrome DevTools Memory tab**: Take heap snapshots


\`\`\`jsx
// BAD: Closure captures huge array, prevents GC
function ProcessButton({ hugeData }) {
  const handleClick = () => {
    const result = hugeData.filter(/* ... */);  // Captured forever
  };
  return <button onClick={handleClick}>Process</button>;
}

// GOOD: Use ref, process on demand
function ProcessButton({ hugeDataRef }) {
  const handleClick = useCallback(() => {
    const result = hugeDataRef.current.filter(/* ... */);
    // Process and discard immediately
  }, [hugeDataRef]);

  return <button onClick={handleClick}>Process</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:291,sortKey:910},{id:"react-911",number:292,title:"What are React Server Components and how do they improve performance?",question:"What are React Server Components and how do they improve performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`React Server Components (RSC) render on the server, sending minimal JS to the client. Benefits:

1. **Zero client JS**: Component code never downloaded to browser
2. **Direct backend access**: Query databases without API layer
3. **Automatic code splitting**: Server components excluded from bundle
4. **Smaller bundles**: Only Client Components ship JS
5. **Streaming**: Progressive rendering with Suspense

Tradeoffs: Can't use hooks/state/events (those require Client Components).


\`\`\`jsx
// Server Component (no 'use client')
// Runs on server, zero JS shipped to client
async function ProductList() {
  const products = await db.products.findMany();  // Direct DB access
  return (
    <ul>
      {products.map(p => (
        <li key={p.id}>{p.name} - \${p.price}</li>
      ))}
    </ul>
  );
}

// Client Component - needs 'use client'
'use client';
function AddToCart({ productId }) {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>Add</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:292,sortKey:911},{id:"react-912",number:293,title:"How do you optimize a dashboard with many real-time widgets?",question:"How do you optimize a dashboard with many real-time widgets?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Real-time dashboards with many widgets face re-render storms. Solutions:

1. **Isolate subscriptions**: Each widget subscribes only to its data slice
2. **Virtualize widget grid**: Render only visible widgets
3. **Throttle updates**: Batch WebSocket messages (e.g., 10/sec → 1/sec)
4. **Use external store**: Zustand with selectors (no context re-renders)
5. **memo each widget**: Prevent cross-widget re-renders
6. **Web Worker**: Parse incoming messages off main thread
7. **Skeleton placeholders**: Avoid layout shift during updates


\`\`\`jsx
import { create } from 'zustand';

const useDashboard = create((set) => ({
  cpu: 0,
  memory: 0,
  network: 0,
  update: (metric, value) => set(s => ({ ...s, [metric]: value })),
}));

// Each widget only re-renders when its metric changes
const CpuWidget = memo(function CpuWidget() {
  const cpu = useDashboard(s => s.cpu);
  return <Gauge value={cpu} label="CPU" />;
});

const MemoryWidget = memo(function MemoryWidget() {
  const memory = useDashboard(s => s.memory);
  return <Gauge value={memory} label="Memory" />;
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:293,sortKey:912},{id:"react-913",number:294,title:"How do you handle large form state without performance issues?",question:"How do you handle large form state without performance issues?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Large forms (50+ fields) re-render entire tree on each keystroke. Solutions:

1. **React Hook Form**: Uncontrolled, uses refs, minimal re-renders
2. **Field-level state**: Each field manages own state
3. **Controlled at section level**: Not entire form
4. **Debounce validation**: Validate on blur, not each keystroke
5. **useDeferredValue**: Defer expensive derived calculations
6. **Split into sub-forms**: Isolate state per section


\`\`\`jsx
import { useForm, Controller } from 'react-hook-form';

// React Hook Form: uncontrolled, only re-renders on validation/submit
function LargeForm() {
  const { register, control, handleSubmit } = useForm({
    mode: 'onBlur',  // Validate on blur, not each keystroke
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      {Array.from({ length: 100 }).map((_, i) => (
        <input
          key={i}
          {...register(\`field\${i}\`, { required: true })}
        />
      ))}
      <button type="submit">Submit</button>
    </form>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:294,sortKey:913},{id:"react-914",number:295,title:"What is the performance impact of importing barrel files?",question:"What is the performance impact of importing barrel files?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Barrel files (\`index.js\` that re-exports everything) cause performance issues:

1. **Slower dev startup**: Bundlers must process entire barrel even for one import
2. **Larger initial parse**: All re-exported modules loaded
3. **Circular dependency risk**: Barrels easily create cycles
4. **Tree-shaking complexity**: Harder for bundlers to eliminate unused exports

Mitigations:
- Import directly from specific files (\`import X from './X'\`)
- Use deep imports (\`import { X } from 'lib/X'\`)
- Configure bundler for better barrel handling
- Avoid barrels in hot paths


\`\`\`jsx
// BAD: Imports entire barrel (loads everything)
import { Button, Modal, Table, Chart } from '@company/ui';

// GOOD: Direct import (only loads Button)
import Button from '@company/ui/Button';

// Or named + deep path (better tree-shaking)
import { Button } from '@company/ui/Button';

// Barrel file problem
// index.ts:
export * from './Button';
export * from './Modal';  // All loaded even if unused
export * from './Table';  // All loaded even if unused
\`\`\`
\`\`\`
`,tech:"react",displayNumber:295,sortKey:914},{id:"react-915",number:296,title:"How do you use React.Profiler API programmatically?",question:"How do you use React.Profiler API programmatically?",difficulty:"Advanced",category:"Performance & Optimization",answer:`\`<Profiler>\` wraps components and calls a callback on every commit. Useful for:

1. **Production profiling**: Collect metrics in production
2. **Automated performance tests**: Assert render times in CI
3. **Conditional profiling**: Only profile specific subtrees

Callback receives: \`id\`, \`phase\` (mount/update), \`actualDuration\`, \`baseDuration\`, \`startTime\`, \`commitTime\`.

\`actualDuration\`: time spent rendering this Profiler's subtree
\`baseDuration\`: estimated time without memoization


\`\`\`jsx
import { Profiler } from 'react';

function logProfile(
  id,
  phase,
  actualDuration,
  baseDuration,
  startTime,
  commitTime
) {
  // Send to analytics
  analytics.track('render', {
    component: id,
    phase,
    duration: actualDuration,
    baseline: baseDuration,
  });

  // Alert if slow
  if (actualDuration > 16) {
    console.warn(\`[\${id}] Slow render: \${actualDuration}ms\`);
  }
}

function App() {
  return (
    <Profiler id="Checkout" onRender={logProfile}>
      <CheckoutFlow />
    </Profiler>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:296,sortKey:915},{id:"react-916",number:297,title:"How do you optimize animations in React?",question:"How do you optimize animations in React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Animation performance principles:

1. **Animate transform/opacity only**: GPU-accelerated, no layout/paint
2. **Avoid animating layout properties**: \`width\`, \`height\`, \`top\`, \`left\` trigger reflow
3. **Use \`will-change\`**: Hint browser about upcoming animations
4. **Libraries**: Framer Motion, React Spring (optimized)
5. **CSS animations**: Prefer over JS when possible
6. **useTransition for state-driven anims**: Keep UI responsive
7. **Avoid re-renders during animation**: Animate via refs/CSS


\`\`\`jsx
import { motion } from 'framer-motion';

// GOOD: Transform/opacity (GPU)
<motion.div
  animate={{ x: 100, opacity: 0.5 }}
  transition={{ duration: 0.3 }}
/>

// BAD: Layout properties (reflow every frame)
<motion.div animate={{ width: 200, marginTop: 50 }} />

// CSS-only animation (best performance)
const spinner = styled.div\`
  animation: spin 1s linear infinite;
  will-change: transform;
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
\`;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:297,sortKey:916},{id:"react-917",number:298,title:"What is the difference between useMemo and computed state?",question:"What is the difference between useMemo and computed state?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`**useMemo**: Memoizes a derived value, recomputes on dep change. Still runs during render.

**Computed state**: Derived via \`useState\` + \`useEffect\` or external store. Persists independently.

Key difference: \`useMemo\` recomputes synchronously during render; computed state updates asynchronously via effects.

Use \`useMemo\` for pure derivations. Use state/effects when derivation has side effects or needs to persist across unrelated re-renders.


\`\`\`jsx
// useMemo: Pure derivation during render
function FilteredList({ items, query }) {
  const filtered = useMemo(
    () => items.filter(i => i.name.includes(query)),
    [items, query]
  );
  return <List items={filtered} />;
}

// Computed state: When derivation has side effects
function SearchResults({ query }) {
  const [results, setResults] = useState([]);

  useEffect(() => {
    // Side effect: fetch from API
    fetchResults(query).then(setResults);
  }, [query]);

  return <List items={results} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:298,sortKey:917},{id:"react-918",number:299,title:"How do you implement a performant search-as-you-type feature?",question:"How do you implement a performant search-as-you-type feature?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Performant search requires multiple optimizations:

1. **Debounce input** (200-300ms): Avoid filtering on every keystroke
2. **useDeferredValue**: Keep input responsive while filtering
3. **useMemo for filter**: Cache filtered results
4. **Virtualize results**: For large result sets
5. **Web Worker**: Move filtering off main thread for huge datasets
6. **Abort previous requests**: Cancel stale API searches
7. **Highlight matches via memo**


\`\`\`jsx
import { useState, useDeferredValue, useMemo, useTransition } from 'react';

function SearchPage({ dataset }) {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();
  const deferredQuery = useDeferredValue(query);

  // Filter only when deferredQuery changes (not on every keystroke)
  const results = useMemo(() => {
    if (!deferredQuery) return [];
    return dataset.filter(item =>
      item.name.toLowerCase().includes(deferredQuery.toLowerCase())
    );
  }, [deferredQuery, dataset]);

  function onChange(e) {
    const value = e.target.value;
    setQuery(value);  // Immediate (input responsive)
    startTransition(() => {
      // Mark filtering as non-urgent
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      {isPending ? <Spinner /> : <ResultsList results={results} />}
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:299,sortKey:918},{id:"react-919",number:300,title:"How do you create a performance budget and monitor it in React?",question:"How do you create a performance budget and monitor it in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`A performance budget sets limits for metrics. Monitor continuously:

1. **Define budgets**: Bundle size (e.g., < 200kb JS), LCP < 2.5s, INP < 200ms, CLS < 0.1
2. **CI checks**: Fail build if bundle exceeds budget (size-limit, bundlesize)
3. **Lighthouse CI**: Automated Core Web Vitals checks on every PR
4. **RUM (Real User Monitoring)**: Collect metrics from real users (web-vitals)
5. **Bundle analyzer in CI**: Catch large dependency additions
6. **Alerting**: Notify when metrics regress


\`\`\`jsx
// package.json - size-limit config
{
  "size-limit": [
    {
      "path": "dist/main.js",
      "limit": "200 KB"
    },
    {
      "path": "dist/vendor.js",
      "limit": "150 KB"
    }
  ]
}
\`\`\`

\`\`\`yaml
\`\`\`
`,tech:"react",displayNumber:300,sortKey:919},{id:"react-920",number:301,title:"What is the difference between local state and global state in React?",question:"What is the difference between local state and global state in React?",difficulty:"Basic",category:"State Management & Ecosystem",answer:`Local state is managed within a single component using \`useState\` or \`useReducer\` and is only accessible to that component and its children via props. Global state is shared across multiple components in the application, typically managed through Context API or external state management libraries. Local state is ideal for UI-specific concerns like form inputs or toggle states, while global state suits user authentication, themes, or data shared across distant components.


\`\`\`jsx
// Local state - component-specific
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}

// Global state via Context
const UserContext = createContext(null);

function App() {
  const [user, setUser] = useState({ name: 'John' });
  return (
    <UserContext.Provider value={{ user, setUser }}>
      <Dashboard />
    </UserContext.Provider>
  );
}

function Profile() {
  const { user } = useContext(UserContext);
  return <h1>{user.name}</h1>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:301,sortKey:920},{id:"react-921",number:302,title:"When should you use Context API versus a dedicated state management library?",question:"When should you use Context API versus a dedicated state management library?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Use Context API for low-frequency updates (theme, authentication, locale) where many components need access to rarely-changing data. Avoid Context for high-frequency updates because every consumer re-renders when the context value changes, and you cannot selectively subscribe to parts of the value. Dedicated libraries (Redux, Zustand, Jotai) are better for complex state logic, frequent updates, middleware needs, derived/atomic state, and debugging tools. Reach for Context first for simplicity, then escalate to a library when you hit re-render or scaling pain.


\`\`\`jsx
// Context: good for static-ish, global config
const ThemeContext = createContext('light');
function Toolbar() {
  const theme = useContext(ThemeContext); // re-renders on any provider value change
  return <div className={theme}>...</div>;
}

// Zustand: selective subscription avoids unnecessary re-renders
const useBearStore = create((set) => ({
  bears: 0,
  fish: 10,
  addBear: () => set((s) => ({ bears: s.bears + 1 })),
}));
function BearCount() {
  const bears = useBearStore((s) => s.bears); // only re-renders when bears changes
  return <span>{bears}</span>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:302,sortKey:921},{id:"react-922",number:303,title:"What are the main limitations of Context API for state management?",question:"What are the main limitations of Context API for state management?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Context has three key limitations: (1) Re-render scope — any change to the provider's value re-renders all consuming components, with no built-in selector to subscribe to a slice. (2) No middleware or side-effect handling — you cannot intercept updates for logging, async, or devtools. (3) Performance and splitting complexity — to mitigate re-renders you must split contexts or memoize, which adds boilerplate. Context is not a replacement for a full store; it's a dependency-injection mechanism.


\`\`\`jsx
// Problem: changing count re-renders ThemeConsumer too
const Ctx = createContext();
function Provider({ children }) {
  const [count, setCount] = useState(0);
  const [theme, setTheme] = useState('light');
  return (
    <Ctx.Provider value={{ count, setCount, theme, setTheme }}>
      {children}
    </Ctx.Provider>
  );
}
// Fix: split into separate providers so consumers subscribe narrowly
\`\`\`
\`\`\`
`,tech:"react",displayNumber:303,sortKey:922},{id:"react-923",number:304,title:"What is Redux and what problem does it solve?",question:"What is Redux and what problem does it solve?",difficulty:"Basic",category:"State Management & Ecosystem",answer:`Redux is a predictable state container for JavaScript apps based on three principles: a single source of truth (one store), state is read-only (changes only via dispatched actions), and changes are made with pure reducer functions. It solves managing complex shared state across large apps by centralizing it, making state changes traceable and debuggable (time-travel, action logging). It enforces a unidirectional data flow and is framework-agnostic but widely used with React.


\`\`\`jsx
// Three principles in action
const initialState = { count: 0 };
function counter(state = initialState, action) {
  switch (action.type) {
    case 'INCREMENT':
      return { count: state.count + 1 }; // pure, no mutation
    default:
      return state;
  }
}
// Single store = single source of truth; dispatch actions to mutate
\`\`\`
\`\`\`
`,tech:"react",displayNumber:304,sortKey:923},{id:"react-924",number:305,title:"Explain the core Redux data flow.",question:"Explain the core Redux data flow.",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"The unidirectional flow is: View dispatches an action -> Store passes the action and current state to the root reducer -> Reducer returns a new state -> Store updates and notifies subscribers -> View re-renders with new state. Actions are plain objects describing \"what happened\" with a `type`; reducers compute the next state. This makes every change explicit and replayable.\n\n\n```jsx\n// 1. Action -> 2. Dispatch -> 3. Reducer -> 4. New State -> 5. Subscribers\nstore.dispatch({ type: 'ADD_TODO', payload: 'Learn Redux' });\n// -> reducer(state, action) -> newState -> components subscribed re-render\n```\n```\n",tech:"react",displayNumber:305,sortKey:924},{id:"react-925",number:306,title:"What is the difference between Redux Core and Redux Toolkit (RTK)?",question:"What is the difference between Redux Core and Redux Toolkit (RTK)?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Redux Core requires manual boilerplate: hand-written action types, action creators, switch statements in reducers, `combineReducers`, `applyMiddleware`, and configuring the store yourself. Redux Toolkit (RTK) is the official, opinionated wrapper that reduces boilerplate via `createSlice` (generates actions + reducer), `configureStore` (bundles thunk + devtools + good defaults), `createAsyncThunk`, and `createEntityAdapter`. RTK is the recommended approach for all new Redux code; Redux Core is essentially legacy.\n\n\n```jsx\n// Redux Core (verbose)\nconst INCREMENT = 'INCREMENT';\nconst increment = () => ({ type: INCREMENT });\nfunction reducer(s = { n: 0 }, a) {\n  if (a.type === INCREMENT) return { n: s.n + 1 };\n  return s;\n}\n\n// RTK (concise, same result)\nconst slice = createSlice({\n  name: 'counter',\n  initialState: { n: 0 },\n  reducers: { increment: (s) => { s.n++; } },\n});\n```\n```\n",tech:"react",displayNumber:306,sortKey:925},{id:"react-926",number:307,title:"What is `createSlice` in Redux Toolkit and how does it work?",question:"What is `createSlice` in Redux Toolkit and how does it work?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`\`createSlice\` takes a slice name, initial state, and an object of reducer functions, and automatically generates action creators and action types matching the reducer names. Internally it uses Immer, so you can "mutate" the draft state and it produces an immutable update. It returns \`{ name, reducer, actions, caseReducers }\`. This eliminates the manual action-type/creator boilerplate and keeps related logic together.


\`\`\`jsx
import { createSlice } from '@reduxjs/toolkit';

const todosSlice = createSlice({
  name: 'todos',
  initialState: [],
  reducers: {
    addTodo: (state, action) => {
      state.push(action.payload); // Immer allows "mutation"
    },
    toggle: (state, action) => {
      const todo = state.find(t => t.id === action.payload);
      if (todo) todo.done = !todo.done;
    },
  },
});

export const { addTodo, toggle } = todosSlice.actions;
export default todosSlice.reducer;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:307,sortKey:926},{id:"react-927",number:308,title:"What is Immer and how does Redux Toolkit use it?",question:"What is Immer and how does Redux Toolkit use it?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Immer is a library that lets you write "mutating" code which it transparently converts into immutable updates using structural sharing. RTK uses Immer inside \`createSlice\` reducers, so you can write \`state.push(...)\` or \`state.value++\` instead of spread operations, and Immer returns a new immutable state. This makes reducers far more concise and less error-prone while preserving immutability guarantees.


\`\`\`jsx
// Without Immer (manual immutable update)
return { ...state, user: { ...state.user, name: 'A' } };

// With Immer (inside RTK reducer)
reducers: {
  setName: (state, action) => { state.user.name = action.payload; }
}
// Immer produces a new immutable object automatically
\`\`\`
\`\`\`
`,tech:"react",displayNumber:308,sortKey:927},{id:"react-928",number:309,title:"How do you configure the Redux store with Redux Toolkit's `configureStore`?",question:"How do you configure the Redux store with Redux Toolkit's `configureStore`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`configureStore` accepts an object with `reducer` (a single reducer or an object map that it auto-combines), optional `middleware` (defaults include thunk + serializable-check + immutable-check + devtools), `preloadedState`, and `devTools`. Unlike the classic `createStore`, it bundles sensible defaults and warns about non-serializable state, making setup safer and shorter.\n\n\n```jsx\nimport { configureStore } from '@reduxjs/toolkit';\nimport todos from './todosSlice';\nimport user from './userSlice';\n\nexport const store = configureStore({\n  reducer: { todos, user },\n  devTools: process.env.NODE_ENV !== 'production',\n});\n// auto-combines reducers, adds thunk + devtools\n```\n```\n",tech:"react",displayNumber:309,sortKey:928},{id:"react-929",number:310,title:"What is `combineReducers` and when is it needed?",question:"What is `combineReducers` and when is it needed?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`combineReducers` merges multiple slice reducers into one root reducer, each managing its own slice of state keyed by name. With RTK's `configureStore` this is automatic when you pass an object map, so you rarely call `combineReducers` directly. It is needed when you want to split state management across feature modules and have each operate independently on its own sub-tree.\n\n\n```jsx\nimport { combineReducers } from '@reduxjs/toolkit';\nconst rootReducer = combineReducers({ todos, user, cart });\n// state = { todos: [...], user: {...}, cart: {...} }\n```\n```\n",tech:"react",displayNumber:310,sortKey:929},{id:"react-930",number:311,title:"What is Redux middleware and what is it used for?",question:"What is Redux middleware and what is it used for?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Middleware is a layer between dispatching an action and the moment it reaches the reducer. It can intercept, modify, delay, or short-circuit actions. Common uses: handling async logic (Thunk, Saga), logging, crash reporting, analytics, and enforcing side-effect rules. Middleware is composed into a pipeline; each gets \`(store) => (next) => (action) => {...}\`.


\`\`\`jsx
// Custom logging middleware
const logger = (store) => (next) => (action) => {
  console.log('dispatching', action);
  const result = next(action);
  console.log('next state', store.getState());
  return result;
};
\`\`\`
\`\`\`
`,tech:"react",displayNumber:311,sortKey:930},{id:"react-931",number:312,title:"Explain Redux Thunk and how it handles async logic.",question:"Explain Redux Thunk and how it handles async logic.",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Redux Thunk is middleware that lets action creators return a function (a thunk) instead of a plain object. The function receives \`dispatch\` and \`getState\`, enabling async flows: perform a request, then dispatch real actions on success/failure. Thunk is included by default in RTK. It's the simplest async pattern but can become hard to test/track for complex flows (where Saga shines).


\`\`\`jsx
// Plain thunk (RTK includes thunk by default)
export const fetchUser = (id) => async (dispatch) => {
  dispatch(fetchStart());
  try {
    const res = await api.getUser(id);
    dispatch(fetchSuccess(res.data));
  } catch (e) {
    dispatch(fetchError(e.message));
  }
};
\`\`\`
\`\`\`
`,tech:"react",displayNumber:312,sortKey:931},{id:"react-932",number:313,title:"What is `createAsyncThunk` in Redux Toolkit?",question:"What is `createAsyncThunk` in Redux Toolkit?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`\`createAsyncThunk\` creates a thunk that dispatches \`pending\`, \`fulfilled\`, and \`rejected\` lifecycle actions automatically based on a payload creator's promise. You then handle these in \`extraReducers\` of a slice via \`builder.addCase\`. It standardizes async state (\`status\`, \`data\`, \`error\`) and removes the need to write manual loading/error actions.


\`\`\`jsx
export const fetchPosts = createAsyncThunk(
  'posts/fetch',
  async (userId, { rejectWithValue }) => {
    try {
      const res = await api.getPosts(userId);
      return res.data;
    } catch (e) {
      return rejectWithValue(e.message);
    }
  }
);

const slice = createSlice({
  name: 'posts',
  initialState: { items: [], status: 'idle' },
  extraReducers: (builder) => {
    builder
      .addCase(fetchPosts.pending, (s) => { s.status = 'loading'; })
      .addCase(fetchPosts.fulfilled, (s, a) => { s.status = 'idle'; s.items = a.payload; })
      .addCase(fetchPosts.rejected, (s, a) => { s.status = 'error'; s.error = a.payload; });
  },
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:313,sortKey:932},{id:"react-933",number:314,title:"How do you handle loading and error states with `createAsyncThunk`?",question:"How do you handle loading and error states with `createAsyncThunk`?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Store a `status` (idle/loading/succeeded/failed) and `error` field in the slice. In `extraReducers`, set `status='loading'` on `pending`, set data and `status='succeeded'` on `fulfilled`, and set `error` and `status='failed'` on `rejected`. Components read these to render spinners and error messages. Optionally track `isLoading` per-entity, but a single status works for simple cases.\n\n\n```jsx\nconst { status, error, items } = useSelector(s => s.posts);\nif (status === 'loading') return <Spinner />;\nif (status === 'failed') return <Error msg={error} />;\nreturn <List items={items} />;\n```\n```\n",tech:"react",displayNumber:314,sortKey:933},{id:"react-934",number:315,title:"What is Redux Saga and how does it differ from Thunk?",question:"What is Redux Saga and how does it differ from Thunk?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Redux Saga uses generator functions to handle complex async side effects, listening for dispatched actions and reacting with `takeLatest`, `takeEvery`, `put`, `call`, `fork`. Unlike Thunk, which co-locates async logic inside action creators as plain functions, Saga decouples side effects into a separate, testable, declarative layer. Saga excels at complex sequences, cancellation, race conditions, and retries, but adds significant boilerplate. Use Thunk/RTK Query for simple async, Saga for orchestration-heavy flows.\n\n\n```jsx\nimport { takeLatest, call, put } from 'redux-saga/effects';\n\nfunction* loadUser(action) {\n  try {\n    const user = yield call(api.getUser, action.payload);\n    yield put(userLoaded(user));\n  } catch (e) {\n    yield put(userError(e.message));\n  }\n}\nexport function* userSaga() {\n  yield takeLatest('user/fetch', loadUser); // decoupled from action creators\n}\n```\n```\n",tech:"react",displayNumber:315,sortKey:934},{id:"react-935",number:316,title:"What are the most common Redux Saga effect creators?",question:"What are the most common Redux Saga effect creators?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`take`/`takeEvery`/`takeLatest` (wait for / listen to actions), `put` (dispatch action), `call` (call function, blocking), `fork`/`spawn` (non-blocking task), `select` (read state), `race` (run effects, take first to finish), `all` (run in parallel), `delay`/`cancel`. `takeLatest` cancels in-flight tasks for the same action, perfect for search-as-you-type; `all` parallelizes independent fetches.\n\n\n```jsx\nfunction* root() {\n  yield all([\n    takeLatest('user/fetch', loadUser),\n    takeEvery('log/event', logEvent),\n  ]);\n}\nfunction* parallel() {\n  const [a, b] = yield all([call(fetchA), call(fetchB)]); // runs concurrently\n}\n```\n```\n",tech:"react",displayNumber:316,sortKey:935},{id:"react-936",number:317,title:"When would you choose Redux Saga over RTK Query or Thunk?",question:"When would you choose Redux Saga over RTK Query or Thunk?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Choose Saga when you need complex orchestration: long-running background tasks, cancellation (e.g., debounced search), race conditions, retries with backoff, websocket/event-stream handling, or coordinating multiple dependent side effects. For plain server-state fetching and caching, RTK Query is simpler and handles caching/invalidation for you. For straightforward request-then-dispatch, Thunk suffices. Saga's cost is boilerplate and a steeper learning curve.


\`\`\`jsx
// Debounced search with cancellation - Saga's strength
function* searchSaga() {
  yield debounce(300, 'search/run', function* (action) {
    const results = yield call(api.search, action.payload);
    yield put(searchDone(results));
  });
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:317,sortKey:936},{id:"react-937",number:318,title:"What is RTK Query and what problem does it solve?",question:"What is RTK Query and what problem does it solve?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`RTK Query (RTKQ) is RTK's data-fetching and caching layer. It eliminates hand-written data-fetching reducers, thunks, loading/error state, and caching logic by auto-generating hooks (\`useGetPostsQuery\`). It manages caching, deduplication, background refetch, invalidation, and request lifecycle for you. It's designed for server state (data that lives on the server), not client UI state.


\`\`\`jsx
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';

export const api = createApi({
  reducerPath: 'api',
  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),
  endpoints: (build) => ({
    getPosts: build.query({ query: () => 'posts' }),
  }),
});
// Generates: useGetPostsQuery()
\`\`\`
\`\`\`
`,tech:"react",displayNumber:318,sortKey:937},{id:"react-938",number:319,title:"How do you create an API slice with `createApi` in RTK Query?",question:"How do you create an API slice with `createApi` in RTK Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`createApi` requires `reducerPath`, a `baseQuery` (e.g., `fetchBaseQuery`), and `endpoints` where you define `query` (GET) and `mutation` (POST/PUT/DELETE) endpoints returning hooks. You must add the generated reducer to the store under `reducerPath` and include `api.middleware` in the middleware chain. Each endpoint auto-generates a hook like `useGetXQuery`.\n\n\n```jsx\nexport const api = createApi({\n  reducerPath: 'api',\n  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),\n  tagTypes: ['Post'],\n  endpoints: (build) => ({\n    getPosts: build.query({ query: () => '/posts', providesTags: ['Post'] }),\n    addPost: build.mutation({\n      query: (body) => ({ url: '/posts', method: 'POST', body }),\n      invalidatesTags: ['Post'],\n    }),\n  }),\n});\nexport const { useGetPostsQuery, useAddPostMutation } = api;\n```\n```\n",tech:"react",displayNumber:319,sortKey:938},{id:"react-939",number:320,title:"How do RTK Query auto-generated hooks work?",question:"How do RTK Query auto-generated hooks work?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"For each endpoint, RTKQ generates a React hook (e.g., `useGetPostsQuery()` or `useAddPostMutation()`). The query hook returns `{ data, isLoading, isFetching, isError, error, refetch }`. The mutation hook returns `[trigger, { data, isLoading, isSuccess }]`. Under the hood they subscribe to the cache via `useSelector` and dispatch fetch lifecycle actions. Calling a query hook triggers a fetch if data isn't cached.\n\n\n```jsx\nfunction Posts() {\n  const { data, isLoading, isError } = useGetPostsQuery();\n  const [addPost, { isLoading: saving }] = useAddPostMutation();\n  if (isLoading) return <Spinner />;\n  return <button onClick={() => addPost({ title: 'Hi' })}>Add</button>;\n}\n```\n```\n",tech:"react",displayNumber:320,sortKey:939},{id:"react-940",number:321,title:"Explain caching in RTK Query.",question:"Explain caching in RTK Query.",difficulty:"Advanced",category:"State Management & Ecosystem",answer:'RTKQ caches responses in the store keyed by endpoint + serialized arguments (the "query key"). A query result is served from cache without a new request until it becomes unused (no active subscribers) for `keepUnusedDataFor` (default 60s) and until `staleTime` elapses. While mounted, the same query across components shares one cache entry and dedupes concurrent requests. Re-fetches happen on mount, arg change, `refetch`, or invalidation.\n\n\n```jsx\n// Two components using same args share one network request + one cache entry\nconst { data: a } = useGetPostQuery(1);\nconst { data: b } = useGetPostQuery(1); // no duplicate fetch\n```\n```\n',tech:"react",displayNumber:321,sortKey:940},{id:"react-941",number:322,title:"What are invalidation tags in RTK Query and how do they work?",question:"What are invalidation tags in RTK Query and how do they work?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Tags (`providesTags` on queries, `invalidatesTags` on mutations) link cache entries to logical entities. When a mutation invalidates a tag, RTKQ automatically refetches all active queries that `provide` that tag — keeping server state consistent without manual refetch calls. Tags can be granular (e.g., `{ type: 'Post', id }`) so updating one post only refetches that post's query.\n\n\n```jsx\ngetPost: build.query({\n  query: (id) => `/posts/${id}`,\n  providesTags: (result, err, id) => [{ type: 'Post', id }],\n}),\nupdatePost: build.mutation({\n  query: ({ id, ...patch }) => ({ url: `/posts/${id}`, method: 'PATCH', body: patch }),\n  invalidatesTags: (result, err, { id }) => [{ type: 'Post', id }],\n}),\n```\n```\n",tech:"react",displayNumber:322,sortKey:941},{id:"react-942",number:323,title:"How do you perform mutations in RTK Query?",question:"How do you perform mutations in RTK Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Define a `build.mutation` endpoint and destructure its hook (e.g., `useAddPostMutation`). Calling the trigger function returns a promise you can await for the result or error. Mutations do not cache a value but run `invalidatesTags` to refetch dependent queries. You can also use `onQueryStarted` with `updateQueryData` for optimistic updates.\n\n\n```jsx\nconst [addPost] = useAddPostMutation();\nconst handleClick = async () => {\n  try {\n    await addPost({ title: 'New' }).unwrap(); // unwrap throws on error\n  } catch (e) {\n    console.error(e);\n  }\n};\n```\n```\n",tech:"react",displayNumber:323,sortKey:942},{id:"react-943",number:324,title:"How does RTK Query handle dependent queries?",question:"How does RTK Query handle dependent queries?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"A dependent (chained) query only runs when a prior query's data is available. Pass `skip` when the argument isn't ready: `useGetUserQuery(userId, { skip: !userId })`. For deeper chaining, skip the second query until the first resolves, then use its data as the argument. RTKQ also supports `selectFromResult` for derived data and combining queries.\n\n\n```jsx\nconst { data: user } = useGetUserQuery(id);\nconst { data: posts } = useGetPostsQuery(user?.id, {\n  skip: !user?.id, // dependent query waits for user\n});\n```\n```\n",tech:"react",displayNumber:324,sortKey:943},{id:"react-944",number:325,title:"What is `selectFromResult` in RTK Query?",question:"What is `selectFromResult` in RTK Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`selectFromResult` lets a query hook return a derived/transformed slice of the cache instead of the whole response, and enables fine-grained re-renders. It receives the full `result` (with `data`, `isLoading`, etc.) and returns a customized object. Useful to avoid re-rendering when unrelated parts of the cache change or to precompute derived values.\n\n\n```jsx\nconst { activeOnly } = useGetTodosQuery(undefined, {\n  selectFromResult: ({ data }) => ({\n    activeOnly: (data ?? []).filter((t) => !t.done),\n  }),\n});\n```\n```\n",tech:"react",displayNumber:325,sortKey:944},{id:"react-945",number:326,title:"How do you add RTK Query to an existing Redux store?",question:"How do you add RTK Query to an existing Redux store?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Add the API's reducer under its `reducerPath` and insert `api.middleware` into the store's middleware (using `getDefaultMiddleware` concatenation so RTKQ's cache lifecycle actions are handled). Then wrap the app in `<ApiProvider>` or, with React-Redux, rely on the normal `<Provider>`.\n\n\n```jsx\nexport const store = configureStore({\n  reducer: {\n    [api.reducerPath]: api.reducer,\n    todos,\n  },\n  middleware: (getDefault) => getDefault().concat(api.middleware),\n});\n```\n```\n",tech:"react",displayNumber:326,sortKey:945},{id:"react-946",number:327,title:"What is the difference between server state and client state?",question:"What is the difference between server state and client state?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Server state is data owned by the backend (cached on the client): it's asynchronous, shared across users, potentially stale, and requires fetching/sync. Client state is UI state owned by the frontend: toggles, form inputs, theme, modals. Libraries like RTK Query / React Query manage server state (caching, invalidation, refetch), while Redux/Zustand/Context manage client state. Mixing them causes bugs — keep them separate.


\`\`\`jsx
// Server state -> TanStack Query / RTK Query
const { data } = useQuery(['user', id], () => fetchUser(id));
// Client state -> useState / Zustand
const [isModalOpen, setOpen] = useState(false);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:327,sortKey:946},{id:"react-947",number:328,title:"What is Zustand and why is it popular?",question:"What is Zustand and why is it popular?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Zustand is a minimal, hook-based state management library. You create a store with \`create()\` and read state via selectors inside components. It's popular because of tiny boilerplate, no providers, built-in selective subscriptions (preventing unnecessary re-renders), middleware support (persist, devtools, immer), and an approachable mental model compared to Redux. It suits both local and global state.


\`\`\`jsx
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  inc: () => set((s) => ({ count: s.count + 1 })),
  reset: () => set({ count: 0 }),
}));
// No Provider needed; use anywhere
function Counter() {
  const count = useStore((s) => s.count);
  return <button onClick={() => useStore.getState().inc()}>{count}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:328,sortKey:947},{id:"react-948",number:329,title:"How do you create a store in Zustand?",question:"How do you create a store in Zustand?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"Call `create((set, get) => ({ ...state, ...actions }))`. `set` updates state (partial or function form), `get` reads current state. The returned hook is used to select slices. You can colocate state and actions in one object. No context provider is required — the store is a module-level singleton.\n\n\n```jsx\nconst useBearStore = create((set) => ({\n  bears: 0,\n  increase: () => set((s) => ({ bears: s.bears + 1 })),\n  removeAll: () => set({ bears: 0 }),\n}));\n```\n```\n",tech:"react",displayNumber:329,sortKey:948},{id:"react-949",number:330,title:"How do selectors work in Zustand and why do they matter for performance?",question:"How do selectors work in Zustand and why do they matter for performance?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Selectors are functions passed to the store hook: `useStore((s) => s.x)`. The component re-renders only when the selected value changes (by `Object.is` comparison). This avoids the Context \"re-render everything\" problem. For derived/object selections, use `useShallow` or return primitives to prevent infinite re-renders from new object references each render.\n\n\n```jsx\nimport { useShallow } from 'zustand/react/shallow';\n\n// Safe object selection\nconst { bears, fish } = useBearStore(useShallow((s) => ({ bears: s.bears, fish: s.fish })));\n// Primitive selection re-renders only when value changes\nconst bears = useBearStore((s) => s.bears);\n```\n```\n",tech:"react",displayNumber:330,sortKey:949},{id:"react-950",number:331,title:"How do you add middleware to Zustand?",question:"How do you add middleware to Zustand?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Pass a middleware-wrapped initializer to `create`: `create(persist((set) => ({...}), {name}))`. Zustand ships `persist`, `devtools`, `subscribeWithSelector`, and `immer` middlewares that can be composed with the `compose` utility. Custom middleware follows `(store) => (next) => (partial) => next(partial)`.\n\n\n```jsx\nimport { persist, devtools, immer } from 'zustand/middleware';\n\nconst useStore = create(\n  devtools(\n    persist(\n      immer((set) => ({ count: 0, inc: () => set((s) => { s.count++; }) })),\n      { name: 'counter' }\n    )\n  )\n);\n```\n```\n",tech:"react",displayNumber:331,sortKey:950},{id:"react-951",number:332,title:"What does the `persist` middleware do in Zustand?",question:"What does the `persist` middleware do in Zustand?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`persist` automatically saves selected store state to `localStorage` (or a custom storage) and rehydrates on load, keeping client state across page refreshes. You configure `name` and optionally `partialize` (which keys to persist), `version`, and `migrate`. Great for themes, auth tokens, or cart contents without manual sync code.\n\n\n```jsx\nconst useStore = create(persist(\n  (set) => ({ token: null, setToken: (t) => set({ token: t }) }),\n  { name: 'auth', partialize: (s) => ({ token: s.token }) }\n));\n```\n```\n",tech:"react",displayNumber:332,sortKey:951},{id:"react-952",number:333,title:"How does Zustand compare to Redux?",question:"How does Zustand compare to Redux?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Zustand is simpler and less boilerplate: no actions/types/reducers, no provider, built-in selectors and middleware. Redux (RTK) offers stricter structure, a richer ecosystem (DevTools time-travel, Saga, RTK Query), and is battle-tested for very large teams. Zustand scales well but is less opinionated. Choose Redux for complex, heavily-tooled apps; Zustand for speed and minimalism.\n\n\n```jsx\n// Redux: action + reducer boilerplate (even with RTK slice)\n// Zustand: one function, actions inline, no provider\n```\n```\n",tech:"react",displayNumber:333,sortKey:952},{id:"react-953",number:334,title:"How do you avoid unnecessary re-renders in Zustand?",question:"How do you avoid unnecessary re-renders in Zustand?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Select narrowly with primitive selectors, use `useShallow` for object/array selections, avoid returning new references inline, and consider `subscribeWithSelector` for non-React subscriptions. Also avoid putting rapidly-changing large objects in one store slice; split stores if needed. The key is that each component subscribes only to the data it uses.\n\n\n```jsx\n// Bad: new object each render -> infinite loop risk\nconst data = useStore((s) => ({ a: s.a, b: s.b }));\n// Good\nconst a = useStore((s) => s.a);\nconst b = useStore((s) => s.b);\n// or\nconst { a, b } = useStore(useShallow((s) => ({ a: s.a, b: s.b })));\n```\n```\n",tech:"react",displayNumber:334,sortKey:953},{id:"react-954",number:335,title:"What is Jotai and what is atomic state management?",question:"What is Jotai and what is atomic state management?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Jotai is a primitive, bottom-up state library built on atoms — the smallest unit of state. Instead of one store, you create independent atoms; components subscribe to only the atoms they read. Derived atoms compute from others. This "atomic" model avoids over-rendering, removes boilerplate, and scales naturally. It's a spiritual successor to Recoil with a simpler API and better maintenance status.


\`\`\`jsx
import { atom, useAtom } from 'jotai';

const countAtom = atom(0);
const doubledAtom = atom((get) => get(countAtom) * 2); // derived

function Counter() {
  const [count, setCount] = useAtom(countAtom);
  return <button onClick={() => setCount((c) => c + 1)}>{count}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:335,sortKey:954},{id:"react-955",number:336,title:"How do derived atoms work in Jotai?",question:"How do derived atoms work in Jotai?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"A derived (read-only) atom takes a getter: `atom((get) => get(otherAtom) + 1)`. `get` reads other atoms' values and establishes a dependency; when a dependency changes, the derived atom recomputes and its subscribers re-render. You can also create writeable derived atoms with a `set` function for computed mutations. This replaces Redux selectors/reducers with composable units.\n\n\n```jsx\nconst priceAtom = atom(10);\nconst qtyAtom = atom(2);\nconst totalAtom = atom((get) => get(priceAtom) * get(qtyAtom)); // recomputes on change\n```\n```\n",tech:"react",displayNumber:336,sortKey:955},{id:"react-956",number:337,title:"What is Recoil and how does it differ from Jotai?",question:"What is Recoil and how does it differ from Jotai?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Recoil (by Meta) also used atoms and selectors with a `<RecoilRoot>` provider, and supported atom families and async selectors. It is now in maintenance/limited development. Jotai is lighter, has no provider requirement, a richer middleware/ecosystem, and active maintenance. Both are atomic; Recoil's API (atom, selector, useRecoilState) is similar but Jotai is generally recommended for new projects today.\n\n\n```jsx\n// Recoil (legacy)\nconst textState = atom({ key: 'text', default: '' });\nconst charCount = selector({ key: 'count', get: ({ get }) => get(textState).length });\n// Jotai equivalent is simpler and actively maintained\n```\n```\n",tech:"react",displayNumber:337,sortKey:956},{id:"react-957",number:338,title:"What are atom families and when are they useful?",question:"What are atom families and when are they useful?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Atom families generate a parametrized atom per key/id (e.g., one atom per todo item) so each entity has isolated, independently-subscribable state. This prevents re-rendering all items when one changes and avoids storing large arrays of objects in a single atom. Supported in Jotai via `atomFamily` (or a map of atoms) and in Recoil as `atomFamily`.\n\n\n```jsx\nimport { atomFamily } from 'jotai/utils';\nconst todoAtomFamily = atomFamily((id) => atom({ id, text: '', done: false }));\n\nfunction Todo({ id }) {\n  const [todo, setTodo] = useAtom(todoAtomFamily(id)); // only this item re-renders\n  return <input value={todo.text} onChange={(e) => setTodo({ ...todo, text: e.target.value })} />;\n}\n```\n```\n",tech:"react",displayNumber:338,sortKey:957},{id:"react-958",number:339,title:"How do you handle async with Jotai atoms?",question:"How do you handle async with Jotai atoms?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Jotai supports async atoms: a `read` function can return a promise, and `useAtom` suspends (with Suspense) or resolves it. Alternatively, use `atomWithQuery` (TanStack Query integration) or `loadable` / `useAtomValue` with Suspense. Async atoms let you colocate data-fetching with state while keeping client/server concerns clear.\n\n\n```jsx\nconst userAtom = atom(async (get) => {\n  const res = await fetch('/api/user');\n  return res.json();\n});\n\nfunction Profile() {\n  const user = useAtomValue(userAtom); // suspends until resolved\n  return <h1>{user.name}</h1>;\n}\n```\n```\n",tech:"react",displayNumber:339,sortKey:958},{id:"react-959",number:340,title:"What is TanStack Query (React Query) and what is it used for?",question:"What is TanStack Query (React Query) and what is it used for?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"TanStack Query (formerly React Query) is a server-state management library for fetching, caching, synchronizing, and updating async data. It is not for client UI state. It provides `useQuery` (read), `useMutation` (write), automatic caching, background refetching, retries, pagination, and devtools. It dramatically reduces boilerplate for data fetching and keeps server data in sync.\n\n\n```jsx\nimport { useQuery } from '@tanstack/react-query';\n\nfunction User({ id }) {\n  const { data, isLoading, error } = useQuery({\n    queryKey: ['user', id],\n    queryFn: () => fetch(`/api/users/${id}`).then((r) => r.json()),\n  });\n  if (isLoading) return <Spinner />;\n  return <div>{data.name}</div>;\n}\n```\n```\n",tech:"react",displayNumber:340,sortKey:959},{id:"react-960",number:341,title:"How do you use `useQuery`?",question:"How do you use `useQuery`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`useQuery({ queryKey, queryFn, options })` takes a unique `queryKey` (array used for caching/id) and a `queryFn` returning a promise. It returns `{ data, isLoading, isError, error, isFetching, refetch, status }`. On mount it fetches; subsequent identical keys read from cache. Options control stale time, retry, refetch intervals, and enabled flags.\n\n\n```jsx\nconst { data, isLoading, isError, refetch } = useQuery({\n  queryKey: ['todos'],\n  queryFn: fetchTodos,\n  staleTime: 60_000,\n});\n```\n```\n",tech:"react",displayNumber:341,sortKey:960},{id:"react-961",number:342,title:"What are query keys and why are they important?",question:"What are query keys and why are they important?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Query keys are serializable arrays that uniquely identify a query's cache entry. They must be structured and consistent: `['todos', { status: 'done' }]`. Nested keys let you invalidate by prefix (e.g., `['todos']` invalidates all todo queries). Query keys deterministically serialize, so argument order and shape matter — keep them stable and sort object keys for consistency.\n\n\n```jsx\n// Good: hierarchical, deterministic\n['todos', 'list', { page: 1 }]\n['todos', 'detail', id]\n\n// Invalidating by prefix refetches all todo queries\nqueryClient.invalidateQueries({ queryKey: ['todos'] });\n```\n```\n",tech:"react",displayNumber:342,sortKey:961},{id:"react-962",number:343,title:"How do you use `useMutation`?",question:"How do you use `useMutation`?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`useMutation({ mutationFn, onSuccess, onError, onSettled })` returns `[mutate, { data, isPending, isError, error }]`. Call `mutate(variables)` to trigger. Use it for POST/PUT/DELETE. In `onSuccess` you typically invalidate queries to refetch server state, or `setQueryData` to update the cache manually (e.g., for optimistic updates).\n\n\n```jsx\nconst mutation = useMutation({\n  mutationFn: (newTodo) => api.createTodo(newTodo),\n  onSuccess: () => {\n    queryClient.invalidateQueries({ queryKey: ['todos'] });\n  },\n});\n<button onClick={() => mutation.mutate({ title: 'Hi' })}>Add</button>;\n```\n```\n",tech:"react",displayNumber:343,sortKey:962},{id:"react-963",number:344,title:"Explain cache time vs stale time in React Query.",question:"Explain cache time vs stale time in React Query.",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`staleTime` (default 0) is how long fetched data is considered fresh; while fresh, React Query will not refetch on window focus or remount. `gcTime` (garbage collection time, formerly `cacheTime`, default 5 min) is how long inactive (no subscribers) cache entries are kept before being removed. Data can be stale but still in cache; `staleTime` controls refetch behavior, `gcTime` controls memory retention. Set `staleTime` higher to reduce refetches, `gcTime` higher to keep data available offline-ish.\n\n\n```jsx\nuseQuery({\n  queryKey: ['user', id],\n  queryFn: fn,\n  staleTime: 5 * 60_000,   // 5 min fresh -> no auto refetch\n  gcTime: 30 * 60_000,     // 30 min retained after unmount\n});\n```\n```\n",tech:"react",displayNumber:344,sortKey:963},{id:"react-964",number:345,title:"What are optimistic updates and how do you implement them in React Query?",question:"What are optimistic updates and how do you implement them in React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Optimistic updates reflect the expected server result in the UI immediately, then roll back if the request fails. In React Query: `onMutate` cancels outgoing queries, snapshots previous data via `queryClient.getQueryData`, optimistically `setQueryData`, then in `onError` restores the snapshot, and `onSettled` invalidates to reconcile with server truth. This makes apps feel instant.\n\n\n```jsx\nuseMutation({\n  mutationFn: updateTodo,\n  onMutate: async (newTodo) => {\n    await queryClient.cancelQueries({ queryKey: ['todos'] });\n    const prev = queryClient.getQueryData(['todos']);\n    queryClient.setQueryData(['todos'], (old) =>\n      old.map((t) => (t.id === newTodo.id ? { ...t, ...newTodo } : t))\n    );\n    return { prev };\n  },\n  onError: (err, newTodo, ctx) => queryClient.setQueryData(['todos'], ctx.prev),\n  onSettled: () => queryClient.invalidateQueries({ queryKey: ['todos'] }),\n});\n```\n```\n",tech:"react",displayNumber:345,sortKey:964},{id:"react-965",number:346,title:"How does React Query handle background refetching and stale-while-revalidate?",question:"How does React Query handle background refetching and stale-while-revalidate?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"By default, React Query refetches stale data in the background on window focus, network reconnect, and remount, while showing cached data immediately (stale-while-revalidate). It only forces a loading spinner on the first fetch; subsequent background refetches set `isFetching` while `data` remains available. This keeps UIs responsive with always-fairly-fresh data. Configure via `refetchOnWindowFocus`, `refetchOnReconnect`, `refetchInterval`.\n\n\n```jsx\nuseQuery({\n  queryKey: ['news'],\n  queryFn: fetchNews,\n  refetchOnWindowFocus: true,   // background refresh on tab focus\n  refetchInterval: 60_000,      // poll every minute\n});\n```\n```\n",tech:"react",displayNumber:346,sortKey:965},{id:"react-966",number:347,title:"How do you invalidate queries in React Query?",question:"How do you invalidate queries in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Use `queryClient.invalidateQueries({ queryKey })`. It marks matching queries as stale and triggers a refetch for those currently mounted/observed. You can target a specific key (`['todos', id]`) or a prefix (`['todos']`) to refetch many. After mutations, invalidation is the standard way to reconcile server state without manually updating the cache.\n\n\n```jsx\nconst queryClient = useQueryClient();\nqueryClient.invalidateQueries({ queryKey: ['todos'] });       // refetch all todos\nqueryClient.invalidateQueries({ queryKey: ['todos', 1] });    // only todo #1\n```\n```\n",tech:"react",displayNumber:347,sortKey:966},{id:"react-967",number:348,title:"What is the `QueryClient` and `QueryClientProvider`?",question:"What is the `QueryClient` and `QueryClientProvider`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`QueryClient` is the central cache/manager for all queries and mutations. `QueryClientProvider` wires it into the React tree (wrap your app once). You can configure defaults (`defaultOptions`) like `staleTime`/`retry` on the client. It also exposes imperative methods: `invalidateQueries`, `setQueryData`, `getQueryData`, `prefetchQuery`.\n\n\n```jsx\nconst queryClient = new QueryClient({\n  defaultOptions: { queries: { staleTime: 30_000, retry: 1 } },\n});\n\n<QueryClientProvider client={queryClient}>\n  <App />\n</QueryClientProvider>;\n```\n```\n",tech:"react",displayNumber:348,sortKey:967},{id:"react-968",number:349,title:"How do you prefetch data with React Query?",question:"How do you prefetch data with React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `queryClient.prefetchQuery({ queryKey, queryFn })` to populate the cache before a component mounts (e.g., on hover or route transition), so the data is instantly available. With SSR you can prefetch on the server and hydrate via `HydrationBoundary` / `dehydrate`. Prefetching improves perceived performance for likely-next views.\n\n\n```jsx\nconst queryClient = useQueryClient();\n<Link\n  onMouseEnter={() => queryClient.prefetchQuery({\n    queryKey: ['user', id],\n    queryFn: () => fetchUser(id),\n  })}\n/>;\n```\n```\n",tech:"react",displayNumber:349,sortKey:968},{id:"react-969",number:350,title:"How do you share query results across components without duplicate fetches?",question:"How do you share query results across components without duplicate fetches?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"React Query caches by `queryKey` at the `QueryClient` level, so any component using the same key shares one cache entry and dedupes concurrent requests. Just reuse the same `queryKey` + `queryFn` shape. No context or prop drilling needed — multiple components mounting the same query only trigger a single network call.\n\n\n```jsx\n// Both components mount -> only ONE network request\nfunction Header() { useQuery({ queryKey: ['user'], queryFn }); }\nfunction Sidebar() { useQuery({ queryKey: ['user'], queryFn }); }\n```\n```\n",tech:"react",displayNumber:350,sortKey:969},{id:"react-970",number:351,title:"What is `keepPreviousData` / placeholder data in React Query?",question:"What is `keepPreviousData` / placeholder data in React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`placeholderData: keepPreviousData` keeps the previous query's `data` visible while a new query (e.g., new page/filter) loads, instead of showing a loading state — enabling smooth pagination/infinite scroll. `placeholderData` can also be a function for initial/fallback data. With it, `isPlaceholderData` is true so you can dim stale content.\n\n\n```jsx\nconst { data, isPlaceholderData } = useQuery({\n  queryKey: ['projects', page],\n  queryFn: () => fetchProjects(page),\n  placeholderData: keepPreviousData, // no flash of loading on page change\n});\n```\n```\n",tech:"react",displayNumber:351,sortKey:970},{id:"react-971",number:352,title:"How do you paginate data with React Query?",question:"How do you paginate data with React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Two common approaches: (1) Manual page keys `['items', page]` with `keepPreviousData` for traditional pagination. (2) `useInfiniteQuery` with `getNextPageParam` for infinite scroll, exposing `fetchNextPage` and a flattened `data.pages`. Choose infinite for feeds, manual for paged tables.\n\n\n```jsx\nconst { data, fetchNextPage, hasNextPage } = useInfiniteQuery({\n  queryKey: ['feed'],\n  queryFn: ({ pageParam = 0 }) => fetchFeed(pageParam),\n  initialPageParam: 0,\n  getNextPageParam: (last) => last.nextCursor,\n});\n```\n```\n",tech:"react",displayNumber:352,sortKey:971},{id:"react-972",number:353,title:"How does React Query integrate with Redux or Zustand?",question:"How does React Query integrate with Redux or Zustand?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Keep server state in React Query and client/UI state in Redux/Zustand — don't duplicate fetched data into Redux. If needed, write mutations/invalidation in RTK Query or React Query, and keep UI flags (modal open, selected tab) in Zustand/Redux. You can call `queryClient` from anywhere, and read React Query cache via selectors if bridging is required, but separation is the recommended architecture.\n\n\n```jsx\n// Client UI state in Zustand, server data in React Query\nconst isModalOpen = useStore((s) => s.isModalOpen);\nconst { data } = useQuery({ queryKey: ['todos'], queryFn: fetchTodos });\n```\n```\n",tech:"react",displayNumber:353,sortKey:972},{id:"react-973",number:354,title:"What are React Query devtools and how do you use them?",question:"What are React Query devtools and how do you use them?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`@tanstack/react-query-devtools` renders a panel showing every query's state (fresh/stale/fetching), cache data, observers, and timing. It helps debug caching, see why a refetch happened, and inspect query keys. Install the package, add `<ReactQueryDevtools />` inside the provider. Essential for understanding cache behavior during development.\n\n\n```jsx\nimport { ReactQueryDevtools } from '@tanstack/react-query-devtools';\n\n<QueryClientProvider client={queryClient}>\n  <App />\n  <ReactQueryDevtools initialIsOpen={false} />\n</QueryClientProvider>;\n```\n```\n",tech:"react",displayNumber:354,sortKey:973},{id:"react-974",number:355,title:"How do you handle errors and retries in React Query?",question:"How do you handle errors and retries in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"By default React Query retries failed queries 3 times with exponential backoff. Configure via `retry` (false/number/function), `retryDelay`, and `staleTime`. Surface errors with `isError`/`error` from the hook or via `onError` callbacks. For mutations, handle `onError` to show toasts. Throwing in `queryFn` triggers error state; non-2xx fetch needs manual throw.\n\n\n```jsx\nuseQuery({\n  queryKey: ['user'],\n  queryFn: fetchUser,\n  retry: 2,\n  retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 8000),\n  onError: (err) => toast.error(err.message),\n});\n```\n```\n",tech:"react",displayNumber:355,sortKey:974},{id:"react-975",number:356,title:"What is the difference between Redux Toolkit Query and React Query?",question:"What is the difference between Redux Toolkit Query and React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Both solve server-state caching. RTK Query is tightly coupled to Redux (uses the store, reducers, middleware, devtools) and is great if you already use Redux and want one ecosystem. React Query (TanStack) is standalone, framework-agnostic-ish, with richer features (infinite queries, prefetch, devtools, suspense) and no Redux dependency. Choose based on whether you're already in the Redux ecosystem. They're largely interchangeable for fetching/caching.


\`\`\`jsx
// RTK Query: lives in Redux store, needs reducerPath + middleware
useGetPostsQuery();
// React Query: standalone QueryClientProvider, no store needed
useQuery({ queryKey: ['posts'], queryFn });
\`\`\`
\`\`\`
`,tech:"react",displayNumber:356,sortKey:975},{id:"react-976",number:357,title:"How do you use React Query with Suspense?",question:"How do you use React Query with Suspense?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Set `useQuery({ queryKey, queryFn, suspense: true })` (or global default). With Suspense, the hook throws a promise while loading, so wrap the component in `<Suspense fallback={...}>`. Errors propagate to an error boundary. This lets you use declarative loading boundaries instead of `isLoading` flags. SSR hydration must also support suspense.\n\n\n```jsx\nfunction User() {\n  const { data } = useQuery({ queryKey: ['user'], queryFn, suspense: true });\n  return <h1>{data.name}</h1>; // throws promise until resolved\n}\n<Suspense fallback={<Spinner />}><User /></Suspense>;\n```\n```\n",tech:"react",displayNumber:357,sortKey:976},{id:"react-977",number:358,title:"What is the `select` option in React Query?",question:"What is the `select` option in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`select` transforms the query data before returning it: `useQuery({ queryKey, queryFn, select: (data) => data.filter(d => !d.done) })`. It memoizes the transform and prevents re-renders when the derived result is unchanged (by reference). Useful for deriving/formatting without extra state or effects. Note: `select` runs on every render unless the input data changed.\n\n\n```jsx\nconst { data: active } = useQuery({\n  queryKey: ['todos'],\n  queryFn: fetchTodos,\n  select: (todos) => todos.filter((t) => !t.done),\n});\n```\n```\n",tech:"react",displayNumber:358,sortKey:977},{id:"react-978",number:359,title:"How do you set and get query cache data imperatively?",question:"How do you set and get query cache data imperatively?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `queryClient.setQueryData(queryKey, updater)` to update a cache entry directly (e.g., append a newly created item without refetch) and `queryClient.getQueryData(queryKey)` to read it. This is common in optimistic updates and after mutations to keep the cache in sync. Keys must match exactly (same serialization) as the original query.\n\n\n```jsx\nqueryClient.setQueryData(['todos'], (old = []) => [...old, newTodo]);\nconst current = queryClient.getQueryData(['todos']);\n```\n```\n",tech:"react",displayNumber:359,sortKey:978},{id:"react-979",number:360,title:"How do you structure a large Redux application with slices?",question:"How do you structure a large Redux application with slices?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Organize by feature (ducks pattern): each feature folder has its slice (`createSlice`), selectors, thunks/RTK Query APIs, and types. Combine via `configureStore`'s reducer map keyed by feature. Use `createEntityAdapter` for normalized collections, `createSelector` (reselect) for memoized derived data, and colocate async via `createAsyncThunk`. Keep cross-feature dependencies minimal and shared data in dedicated slices.\n\n\n```jsx\n// features/todos/todosSlice.js\nconst todosSlice = createSlice({ name: 'todos', initialState, reducers: {...} });\n// store: { reducer: { todos: todosSlice.reducer, user: userSlice.reducer } }\n```\n```\n",tech:"react",displayNumber:360,sortKey:979},{id:"react-980",number:361,title:"What is `createEntityAdapter` and when should you use it?",question:"What is `createEntityAdapter` and when should you use it?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`createEntityAdapter` normalizes a collection into `{ ids: [], entities: {} }` for efficient lookups and updates by id, with prebuilt reducers (`setAll`, `addOne`, `upsertMany`, `removeOne`) and selectors (`selectAll`, `selectById`, `selectIds`). Use it whenever you manage a list of entities fetched from a server — it avoids manual normalization and gives O(1) access.\n\n\n```jsx\nconst adapter = createEntityAdapter({ selectId: (u) => u.id });\nconst slice = createSlice({\n  name: 'users',\n  initialState: adapter.getInitialState(),\n  reducers: {\n    usersReceived: adapter.setAll,\n    userAdded: adapter.addOne,\n  },\n});\nconst selectors = adapter.getSelectors((s) => s.users);\n// selectors.selectAll(state) -> array; selectors.selectById(state, id)\n```\n```\n",tech:"react",displayNumber:361,sortKey:980},{id:"react-981",number:362,title:"What is `reselect` / `createSelector` and why is it used?",question:"What is `reselect` / `createSelector` and why is it used?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`createSelector` builds memoized selectors: they only recompute when input selectors change (by reference), preventing expensive derivations on every render and avoiding new object references that cause re-renders. RTK re-exports `createSelector`. Use it for filtered/sorted lists or combined slices derived from state. Input selectors read raw state; the output is memoized.\n\n\n```jsx\nimport { createSelector } from '@reduxjs/toolkit';\nconst selectTodos = (s) => s.todos.items;\nconst selectActive = createSelector(selectTodos, (items) =>\n  items.filter((t) => !t.done)\n); // recomputes only when items reference changes\n```\n```\n",tech:"react",displayNumber:362,sortKey:981},{id:"react-982",number:363,title:"How do you connect Redux to React components (`useSelector`, `useDispatch`)?",question:"How do you connect Redux to React components (`useSelector`, `useDispatch`)?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"With React-Redux, `useSelector((state) => state.x)` reads a slice (re-renders when it changes by `Object.is`), and `useDispatch()` returns the dispatch function to send actions. Always select narrowly and return primitives or memoized data to avoid re-render issues. For actions, import them from slices and dispatch directly.\n\n\n```jsx\nimport { useSelector, useDispatch } from 'react-redux';\nimport { increment } from './counterSlice';\n\nfunction Counter() {\n  const count = useSelector((s) => s.counter.n);\n  const dispatch = useDispatch();\n  return <button onClick={() => dispatch(increment())}>{count}</button>;\n}\n```\n```\n",tech:"react",displayNumber:363,sortKey:982},{id:"react-983",number:364,title:"What are common performance pitfalls with Redux and Context?",question:"What are common performance pitfalls with Redux and Context?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"(1) Returning new object/array references from `useSelector`/Context value causes infinite re-renders — use memoized selectors or `useShallow`. (2) Context value object changes re-render all consumers — split contexts or memoize. (3) Dispatching too frequently or storing rapidly-changing data in global state. (4) Connecting too many components broadly. Mitigate with selectors, memoization, and splitting state.\n\n\n```jsx\n// Pitfall: new array each render -> re-render loop\nconst bad = useSelector((s) => [...s.items]);\n// Fix\nconst items = useSelector((s) => s.items);\n```\n```\n",tech:"react",displayNumber:364,sortKey:983},{id:"react-984",number:365,title:"How do you test Redux slices and async thunks?",question:"How do you test Redux slices and async thunks?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"For slices, call the reducer with initial state and an action, asserting the returned state (pure, easy). For `createAsyncThunk`, test the lifecycle reducers via `extraReducers` by dispatching `pending/fulfilled/rejected` action objects (use `thunk.fulfilled.match`). Integration tests use a real store with `configureStore` and dispatch thunks, possibly mocking the API. RTK's purity makes reducer tests trivial.\n\n\n```jsx\nit('adds todo', () => {\n  const state = todosReducer(undefined, addTodo('x'));\n  expect(state.items).toHaveLength(1);\n});\n// async: dispatch real thunk with mocked api, await, assert store state\n```\n```\n",tech:"react",displayNumber:365,sortKey:984},{id:"react-985",number:366,title:"How do you test React Query hooks?",question:"How do you test React Query hooks?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `@testing-library/react`'s `renderHook` wrapped in `QueryClientProvider` (with `retry: false` and a `Wrapper`). Provide a `queryFn` or mock `fetch`. Assert on `result.current.data` after `waitFor`/`findBy`. For mutations, call `result.current.mutate` and await. Isolate cache per test by creating a fresh `QueryClient`. You can also use `queryClient.setQueryData` to seed state.\n\n\n```jsx\nconst wrapper = ({ children }) => (\n  <QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}>\n    {children}\n  </QueryClientProvider>\n);\nconst { result } = renderHook(() => useQuery({ queryKey: ['x'], queryFn: async () => 1 }), { wrapper });\nawait waitFor(() => expect(result.current.data).toBe(1));\n```\n```\n",tech:"react",displayNumber:366,sortKey:985},{id:"react-986",number:367,title:"What is the difference between a selector returning a new object and a primitive, and why does it matter?",question:"What is the difference between a selector returning a new object and a primitive, and why does it matter?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`useSelector((s) => ({ a: s.a, b: s.b }))` returns a new object reference every call, so Redux's default `Object.is` equality always fails → infinite re-render. Returning a primitive (`s.a`) or using `useShallow`/`createSelector` (memoized) prevents this. The rule: selectors should return stable references or be memoized. This applies equally to Zustand and Context-derived values.\n\n\n```jsx\n// Risky\nconst x = useSelector((s) => ({ a: s.a, b: s.b }));\n// Safe\nimport { useShallow } from 'react-redux';\nconst x = useSelector(useShallow((s) => ({ a: s.a, b: s.b })));\n```\n```\n",tech:"react",displayNumber:367,sortKey:986},{id:"react-987",number:368,title:"How do you migrate from Redux Core to Redux Toolkit?",question:"How do you migrate from Redux Core to Redux Toolkit?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Incrementally: introduce RTK's `configureStore` and keep old reducers (they're still valid reducers). Convert one slice at a time to `createSlice`, replacing hand-written action types/creators with generated ones (keep action type strings stable to avoid breaking existing dispatches). Replace `redux-thunk` manual setup with RTK's built-in thunk. Finally adopt `createAsyncThunk` and `createEntityAdapter` for data slices. RTK is backward compatible with plain reducers, so migration can be gradual.\n\n\n```jsx\n// Old action type 'todos/add' can be reused:\ncreateSlice({ name: 'todos', initialState, reducers: { add: {...} } });\n// generates 'todos/add' automatically -> existing dispatches still work\n```\n```\n",tech:"react",displayNumber:368,sortKey:987},{id:"react-988",number:369,title:"When would you choose Zustand over Redux Toolkit for a new project?",question:"When would you choose Zustand over Redux Toolkit for a new project?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Choose Zustand when you want minimal boilerplate, no provider, fast setup, built-in persist/devtools, and a small-to-medium app where the strict structure of Redux isn't needed. Choose Redux Toolkit when you need a battle-tested ecosystem, RTK Query, time-travel devtools, heavy team conventions, or complex middleware/Saga orchestration. Both handle client state well; Zustand wins on simplicity, Redux on tooling/scale.\n\n\n```jsx\n// Zustand: ~5 lines for a global store\nconst useStore = create((set) => ({ n: 0, inc: () => set((s) => ({ n: s.n + 1 })) }));\n// Redux: slice + store + provider + hooks\n```\n```\n",tech:"react",displayNumber:369,sortKey:988},{id:"react-989",number:370,title:"How do you decide between Context, Redux, Zustand, Jotai, and React Query for a given problem?",question:"How do you decide between Context, Redux, Zustand, Jotai, and React Query for a given problem?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Decision heuristic: (1) Server data (fetching/caching/sync) -> React Query or RTK Query. (2) Rarely-changing global config (theme, auth, locale) -> Context. (3) Simple global client state, low boilerplate -> Zustand. (4) Atomic/fine-grained derived state or component-level isolation -> Jotai/Recoil. (5) Large, complex, team-convention-heavy apps needing devtools/middleware/RTK Query -> Redux Toolkit. Keep server state and client state in separate tools; never put fetched data in Context/Redux unnecessarily.


\`\`\`jsx
// Server state
useQuery({ queryKey: ['user'], queryFn });
// Client UI state
const theme = useContext(ThemeContext);           // static config
const [open, setOpen] = useStore((s) => s.modal); // Zustand
// Atomic
const [n, setN] = useAtom(countAtom);             // Jotai
// Complex global logic
useSelector((s) => s.counter.n);                  // Redux Toolkit
\`\`\`
\`\`\`
`,tech:"react",displayNumber:370,sortKey:989},{id:"react-990",number:371,title:"What is React Router and why is it necessary for SPAs?",question:"What is React Router and why is it necessary for SPAs?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`React Router is the standard routing library for React applications. It enables client-side routing in Single Page Applications (SPAs) by synchronizing the UI with the URL without making full page requests to the server. Without a router, an SPA would show the same UI regardless of the URL, making bookmarking, sharing links, and browser navigation impossible. React Router provides declarative routing through components, allowing you to map URL paths to specific React components.


\`\`\`jsx
// Without React Router - URL never changes UI
function App() {
  return <HomePage />; // Always shows HomePage regardless of URL
}

// With React Router - URL drives UI
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path='/' element={<HomePage />} />
        <Route path='/about' element={<AboutPage />} />
        <Route path='/users/:id' element={<UserProfile />} />
      </Routes>
    </BrowserRouter>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:371,sortKey:990},{id:"react-991",number:372,title:"What is the difference between BrowserRouter, HashRouter, and MemoryRouter?",question:"What is the difference between BrowserRouter, HashRouter, and MemoryRouter?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`- BrowserRouter uses the HTML5 History API to keep UI in sync with the URL, producing clean URLs like /users/123. It requires server-side configuration so that deep links resolve to index.html.
- HashRouter stores the location in the URL hash (#/users/123). It works on any static host without server configuration and is useful for legacy browsers, but creates uglier URLs and the hash is never sent to the server.
- MemoryRouter keeps history in memory (no URL change). It is ideal for tests, React Native, and server-side rendering where there is no browser history.


\`\`\`jsx
import { BrowserRouter, HashRouter, MemoryRouter } from 'react-router-dom';

// Production web app
<BrowserRouter><App /></BrowserRouter>;

// Static host with no server rewrites
<HashRouter><App /></HashRouter>;

// Tests / non-browser environments
<MemoryRouter initialEntries={['/users/1']}><App /></MemoryRouter>;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:372,sortKey:991},{id:"react-992",number:373,title:"How do you define routes in React Router v6 with Routes and Route?",question:"How do you define routes in React Router v6 with Routes and Route?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"In v6, routes are defined using the `<Routes>` component which replaces the older `<Switch>`. You nest `<Route>` elements with a `path` and an `element`. The `Routes` component renders the first matching route. Paths are relative and you can nest routes to build layouts. There is no longer an `exact` prop, because v6 does ranking/score-based matching automatically and uses the most specific match.\n\n\n```jsx\nimport { Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    <Routes>\n      <Route path='/' element={<Layout />}>\n        <Route index element={<Home />} />\n        <Route path='about' element={<About />} />\n        <Route path='users/:id' element={<UserProfile />} />\n        <Route path='*' element={<NotFound />} />\n      </Route>\n    </Routes>\n  );\n}\n```\n```\n",tech:"react",displayNumber:373,sortKey:992},{id:"react-993",number:374,title:"What is the difference between Link, NavLink, and useNavigate?",question:"What is the difference between Link, NavLink, and useNavigate?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`- \`<Link>\` renders an accessible anchor that performs client-side navigation via the History API without a full reload.
- \`<NavLink>\` is a special Link that adds styling hooks (className or style functions) when the link is active, perfect for navigation menus.
- \`useNavigate()\` is a hook returning a function used for imperative navigation inside event handlers or effects (e.g., redirect after a form submit).


\`\`\`jsx
import { Link, NavLink, useNavigate } from 'react-router-dom';

function Nav() {
  const navigate = useNavigate();
  return (
    <nav>
      <NavLink to='/' style={({ isActive }) => ({ color: isActive ? 'red' : 'black' })}>Home</NavLink>
      <Link to='/about'>About</Link>
      <button onClick={() => navigate('/login')}>Go login</button>
    </nav>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:374,sortKey:993},{id:"react-994",number:375,title:"How do you read route parameters with useParams?",question:"How do you read route parameters with useParams?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`\`useParams()\` is a hook that returns an object of key/value pairs of the dynamic segments defined in the route path. For a route defined as /users/:id, the component rendered at that route can read the id via useParams(). Values are always strings and must be type-coerced when needed.


\`\`\`jsx
import { useParams } from 'react-router-dom';

function UserProfile() {
  const { id, tab } = useParams(); // for path /users/:id/:tab
  return <h1>User {id}, tab {tab}</h1>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:375,sortKey:994},{id:"react-995",number:376,title:"How do you read and update query strings with useSearchParams?",question:"How do you read and update query strings with useSearchParams?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`\`useSearchParams()\` is a hook that returns a tuple: the current URLSearchParams and a setter function. It behaves like useState but is backed by the URL query string. You read values with \`.get('key')\`, and update them via the setter which accepts a URLSearchParams instance or a function. This makes shareable, bookmarkable filters and pagination state a natural fit for the URL.


\`\`\`jsx
import { useSearchParams } from 'react-router-dom';

function Products() {
  const [params, setParams] = useSearchParams();
  const page = params.get('page') ?? '1';
  const q = params.get('q') ?? '';

  const onSearch = (text) => {
    setParams(prev => {
      const next = new URLSearchParams(prev);
      next.set('q', text);
      next.set('page', '1');
      return next;
    });
  };

  return <input defaultValue={q} onChange={e => onSearch(e.target.value)} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:376,sortKey:995},{id:"react-996",number:377,title:"What changed from React Router v5 to v6?",question:"What changed from React Router v5 to v6?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Key v5 to v6 changes: `Switch` was replaced by `Routes`; routes are now defined with `element` prop instead of `component`/`render`; the `exact` prop was removed because v6 uses ranked matching; nested routes use relative paths and a parent layout with `<Outlet/>`; `useHistory` became `useNavigate`; redirects use `<Navigate>` or the `navigate` function; and `useRoutes` allows defining routes as JavaScript objects for better composition.\n\n\n```jsx\n// v5\n<Switch>\n  <Route exact path='/' component={Home} />\n  <Redirect to='/login' />\n</Switch>\n\n// v6\n<Routes>\n  <Route path='/' element={<Home />} />\n  <Route path='/login' element={<Navigate to='/dashboard' replace />} />\n</Routes>\n```\n```\n",tech:"react",displayNumber:377,sortKey:996},{id:"react-997",number:378,title:"How do you create nested routes and use Outlet?",question:"How do you create nested routes and use Outlet?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Nested routes let a parent route render a layout while child routes render inside it via \`<Outlet />\`. You define children as nested \`<Route>\` elements. The parent component places \`<Outlet />\` where the child should appear. This avoids repeating shared UI like navbars and sidebars across pages.


\`\`\`jsx
function Dashboard() {
  return (
    <div>
      <Sidebar />
      <Outlet /> {/* child route renders here */}
    </div>
  );
}

<Routes>
  <Route path='dashboard' element={<Dashboard />}>
    <Route index element={<Overview />} />
    <Route path='stats' element={<Stats />} />
  </Route>
</Routes>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:378,sortKey:997},{id:"react-998",number:379,title:"How do you implement protected (authenticated) routes in React Router v6?",question:"How do you implement protected (authenticated) routes in React Router v6?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`A common pattern is a wrapper component, often called \`<RequireAuth>\`, that checks an auth state and either renders \`<Outlet/>\` (allowing children to render) or redirects to a login page using \`<Navigate replace />\`. The protected route is then placed in the tree with the wrapper as its element and the real pages as children. Using \`replace\` prevents the protected URL from staying in history after redirect.


\`\`\`jsx
function RequireAuth() {
  const auth = useAuth();
  const location = useLocation();
  if (!auth.user) {
    return <Navigate to='/login' replace state={{ from: location }} />;
  }
  return <Outlet />;
}

<Route element={<RequireAuth />}>
  <Route path='profile' element={<Profile />} />
  <Route path='settings' element={<Settings />} />
</Route>
\`\`\`
\`\`\`
`,tech:"react",displayNumber:379,sortKey:998},{id:"react-999",number:380,title:"What are loaders and actions in React Router (data routers)?",question:"What are loaders and actions in React Router (data routers)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Data routers (created via `createBrowserRouter` / `RouterProvider`) let you define `loader` and `action` functions on routes. A `loader` runs before rendering to fetch data, returning a promise that the component reads with `useLoaderData()`. An `action` handles mutations (often from forms) and runs on submission. This co-locates data fetching and mutations with routes and supports pending/error states via `useNavigation()` and `errorElement`.\n\n\n```jsx\nconst router = createBrowserRouter([\n  {\n    path: '/users/:id',\n    element: <User />,\n    loader: async ({ params }) => fetchUser(params.id),\n    action: async ({ request, params }) => updateUser(params.id, await request.formData()),\n    errorElement: <ErrorPage />,\n  },\n]);\n```\n```\n",tech:"react",displayNumber:380,sortKey:999},{id:"react-1000",number:381,title:"How do you access loader data with useLoaderData and redirect with loaders?",question:"How do you access loader data with useLoaderData and redirect with loaders?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Inside a component rendered by a route with a loader, call \`useLoaderData()\` to get the value the loader returned. Loaders can also call the \`redirect()\` helper to send the user elsewhere (e.g., to login when unauthenticated) instead of returning data. The redirected response is handled by the router automatically.


\`\`\`jsx
import { useLoaderData, redirect } from 'react-router-dom';

export async function loader({ request }) {
  const user = await getSession(request);
  if (!user) return redirect('/login');
  return user;
}

function Account() {
  const user = useLoaderData();
  return <h1>{user.name}</h1>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:381,sortKey:1e3},{id:"react-1001",number:382,title:"What is the purpose of the Form component in React Router and how does it relate to actions?",question:"What is the purpose of the Form component in React Router and how does it relate to actions?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"React Router's `<Form>` component is a drop-in replacement for the HTML `<form>` that intercepts submission and sends the data to the route's `action` instead of causing a navigation/document reload. It uses the method (GET/POST) and action (defaults to the current route). Combined with `action` functions and `useNavigation`, you get progressive-enhancement-friendly forms with built-in pending states.\n\n\n```jsx\nimport { Form, useNavigation } from 'react-router-dom';\n\nfunction NewPost() {\n  const nav = useNavigation();\n  const submitting = nav.state === 'submitting';\n  return (\n    <Form method='post'>\n      <input name='title' />\n      <button disabled={submitting}>{submitting ? 'Saving…' : 'Save'}</button>\n    </Form>\n  );\n}\n```\n```\n",tech:"react",displayNumber:382,sortKey:1001},{id:"react-1002",number:383,title:"How do you handle errors in routes using errorElement and useRouteError?",question:"How do you handle errors in routes using errorElement and useRouteError?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Each route can declare an `errorElement` which renders when its loader, action, or component throws. Inside that element, `useRouteError()` returns the thrown value so you can show a meaningful message. A root `errorElement` catches errors for the whole tree. This replaces try/catch sprawl with a declarative, per-route error boundary.\n\n\n```jsx\nimport { useRouteError, isRouteErrorResponse } from 'react-router-dom';\n\nfunction ErrorPage() {\n  const error = useRouteError();\n  if (isRouteErrorResponse(error)) {\n    return <h1>{error.status} {error.statusText}</h1>;\n  }\n  return <h1>Oops: {error.message}</h1>;\n}\n```\n```\n",tech:"react",displayNumber:383,sortKey:1002},{id:"react-1003",number:384,title:"How does useNavigation help with loading and submitting states?",question:"How does useNavigation help with loading and submitting states?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`useNavigation()` returns the current navigation state of the router: `idle`, `loading`, or `submitting`. You use it to show spinners or disable buttons while loaders are running or forms are being submitted. `navigation.location` tells you the destination during a transition, which is handy for optimistic highlighting of the next route.\n\n\n```jsx\nfunction Layout() {\n  const nav = useNavigation();\n  return (\n    <div>\n      {nav.state === 'loading' && <GlobalSpinner />}\n      <Outlet />\n    </div>\n  );\n}\n```\n```\n",tech:"react",displayNumber:384,sortKey:1003},{id:"react-1004",number:385,title:"What is the difference between useLocation, useNavigate, and useMatch?",question:"What is the difference between useLocation, useNavigate, and useMatch?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"- `useLocation()` returns the current location object ({ pathname, search, hash, state }) and is useful for reading the full URL or reacting to changes.\n- `useNavigate()` returns a function to programmatically change the route.\n- `useMatch(pattern)` returns match data if the current location matches a given path pattern, useful for conditional logic without rendering a Route.\n\n\n```jsx\nconst location = useLocation();\nconst navigate = useNavigate();\nconst match = useMatch('/users/:id');\nconsole.log(location.search, match?.params.id);\n```\n```\n",tech:"react",displayNumber:385,sortKey:1004},{id:"react-1005",number:386,title:"How do you pass state through navigation and read it at the destination?",question:"How do you pass state through navigation and read it at the destination?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"You can attach a `state` object when navigating via `<Link to='..' state={{ from: 'home' }}>` or `navigate('/x', { state })`. At the destination, `useLocation().state` exposes it. This is ideal for transient data like \"redirect back to\" URLs or modal context without putting it in the query string.\n\n\n```jsx\n<Link to='/checkout' state={{ productId: 42 }}>Buy</Link>;\n\nfunction Checkout() {\n  const { state } = useLocation();\n  return <p>Product: {state?.productId}</p>;\n}\n```\n```\n",tech:"react",displayNumber:386,sortKey:1005},{id:"react-1006",number:387,title:"How do you handle redirects with the Navigate component and replace?",question:"How do you handle redirects with the Navigate component and replace?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`<Navigate to='/path' />` renders and immediately navigates to the target. The `replace` prop replaces the current entry in history instead of pushing a new one, which is what you want after a login redirect so the user does not bounce back to the protected page on \"back\". You can also drive it conditionally.\n\n\n```jsx\nfunction OldRoute() {\n  return <Navigate to='/new-route' replace />;\n}\n```\n```\n",tech:"react",displayNumber:387,sortKey:1006},{id:"react-1007",number:388,title:"What is a controlled component in React forms?",question:"What is a controlled component in React forms?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"A controlled component is a form input whose value is driven by React state. You set `value={state}` and update state in an `onChange` handler. React becomes the single source of truth, which makes validation, conditional UI, and formatting straightforward. The trade-off is more re-renders, though for typical forms this is negligible.\n\n\n```jsx\nfunction Controlled() {\n  const [name, setName] = useState('');\n  return <input value={name} onChange={e => setName(e.target.value)} />;\n}\n```\n```\n",tech:"react",displayNumber:388,sortKey:1007},{id:"react-1008",number:389,title:"What is an uncontrolled component and when would you use one?",question:"What is an uncontrolled component and when would you use one?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"An uncontrolled component stores its own state in the DOM. You read values via a ref (`inputRef.current.value`) rather than React state. It is useful for simple forms, integrating with non-React libraries, large forms where you want to avoid many re-renders, or using native features like file inputs. The default value is set with `defaultValue`/`defaultChecked`.\n\n\n```jsx\nfunction Uncontrolled() {\n  const ref = useRef(null);\n  const submit = () => alert(ref.current.value);\n  return <input ref={ref} defaultValue='initial' />;\n}\n```\n```\n",tech:"react",displayNumber:389,sortKey:1008},{id:"react-1009",number:390,title:"Compare controlled vs uncontrolled components with pros and cons.",question:"Compare controlled vs uncontrolled components with pros and cons.",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Controlled components keep form state in React, enabling instant validation, conditional fields, and dynamic formatting, but cause a re-render per keystroke. Uncontrolled components keep state in the DOM, reducing re-renders and easing integration with third-party widgets, but you must imperatively query values and get validation only on submit. Rule of thumb: use controlled for interactive forms, uncontrolled for performance-sensitive or simple/embedded cases.


\`\`\`jsx
// Controlled: validation as you type
const [email, setEmail] = useState('');
const invalid = !email.includes('@');

// Uncontrolled: read once at submit
const ref = useRef();
const onSubmit = () => console.log(ref.current.value);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:390,sortKey:1009},{id:"react-1010",number:391,title:"What is React Hook Form and why is it performant?",question:"What is React Hook Form and why is it performant?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`React Hook Form (RHF) is a library for building forms with minimal re-renders. Unlike controlled inputs that re-render on every keystroke, RHF uses uncontrolled inputs registered via \`ref\` and reads values on demand, subscribing components selectively to only the fields they care about. It uses native validation and isolates re-renders to individual inputs through its subscription model, which makes it fast even with large forms.


\`\`\`jsx
// RHF avoids re-rendering the whole form on each keystroke
const { register, handleSubmit } = useForm();
const onSubmit = (data) => console.log(data);
<form onSubmit={handleSubmit(onSubmit)}>
  <input {...register('email')} />
</form>;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:391,sortKey:1010},{id:"react-1011",number:392,title:"How do you build a form with useForm, register, and handleSubmit?",question:"How do you build a form with useForm, register, and handleSubmit?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Call `useForm()` to get `register`, `handleSubmit`, `formState`, etc. Spread `register('name')` onto each input to wire it up. `handleSubmit(onValid, onInvalid)` wraps your submit handler and runs validation before calling onValid with the form values. RHF collects all registered values for you, no manual state needed.\n\n\n```jsx\nimport { useForm } from 'react-hook-form';\n\nfunction Signup() {\n  const { register, handleSubmit, formState: { errors } } = useForm();\n  const submit = (data) => console.log(data);\n  return (\n    <form onSubmit={handleSubmit(submit)}>\n      <input {...register('email', { required: 'Email required' })} />\n      {errors.email && <span>{errors.email.message}</span>}\n      <button type='submit'>Go</button>\n    </form>\n  );\n}\n```\n```\n",tech:"react",displayNumber:392,sortKey:1011},{id:"react-1012",number:393,title:"Explain formState in React Hook Form (errors, isSubmitting, isValid, dirtyFields).",question:"Explain formState in React Hook Form (errors, isSubmitting, isValid, dirtyFields).",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`formState` is a proxy object exposing form status: `errors` holds validation messages, `isSubmitting` is true during async submission, `isValid` reflects whether the form passes validation, `dirtyFields` marks which fields changed, and `isDirty`/`touchedFields` track interaction. Because formState is a proxy, you must destructure the fields you use so RHF subscribes to them and triggers re-renders correctly.\n\n\n```jsx\nconst { formState: { errors, isSubmitting, isValid } } = useForm({ mode: 'onChange' });\n// Destructure to subscribe; isSubmitting drives the button spinner\n```\n```\n",tech:"react",displayNumber:393,sortKey:1012},{id:"react-1013",number:394,title:"How do you validate with React Hook Form rules and with a schema (zodResolver)?",question:"How do you validate with React Hook Form rules and with a schema (zodResolver)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"RHF supports inline rules via `register('field', { required, minLength, pattern })`. For complex validation, you use a resolver (e.g., `@hookform/resolvers/zod` with `zodResolver(schema)`). A Zod schema centralizes types and validation, giving you end-to-end type safety and reusable, composable rules that also validate server payloads.\n\n\n```jsx\nimport { z } from 'zod';\nimport { zodResolver } from '@hookform/resolvers/zod';\n\nconst schema = z.object({ email: z.string().email(), age: z.number().min(18) });\nconst { register, handleSubmit } = useForm({ resolver: zodResolver(schema) });\n```\n```\n",tech:"react",displayNumber:394,sortKey:1013},{id:"react-1014",number:395,title:"What are the performance benefits of React Hook Form compared to controlled forms?",question:"What are the performance benefits of React Hook Form compared to controlled forms?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`RHF minimizes re-renders by using uncontrolled inputs and a ref-based registry, so typing in one field does not re-render the whole component. It isolates subscriptions so only components reading a given field re-render. Controlled forms re-render the parent on every keystroke. For large forms, RHF's approach yields significantly better input latency and lower memory churn.


\`\`\`jsx
// Controlled: every keystroke re-renders App
const [v, setV] = useState('');
<input value={v} onChange={e => setV(e.target.value)} />;

// RHF: input value lives in DOM ref; no re-render per keystroke
<input {...register('name')} />;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:395,sortKey:1014},{id:"react-1015",number:396,title:"How do you use Formik for form state and validation?",question:"How do you use Formik for form state and validation?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Formik manages form state, validation, and submission. You use `useFormik()` or the `<Formik>` component with `initialValues`, `onSubmit`, and `validate` (or a validation schema). `values`, `errors`, `touched`, and `handleChange`/`handleSubmit` come from Formik. It is a controlled approach, so it re-renders on each change, which is simpler to reason about but heavier for large forms than RHF.\n\n\n```jsx\nimport { useFormik } from 'formik';\n\nfunction Form() {\n  const f = useFormik({\n    initialValues: { email: '' },\n    validate: (v) => (!v.email ? { email: 'Required' } : {}),\n    onSubmit: (v) => console.log(v),\n  });\n  return (\n    <form onSubmit={f.handleSubmit}>\n      <input name='email' value={f.values.email} onChange={f.handleChange} />\n      {f.errors.email && f.touched.email && <span>{f.errors.email}</span>}\n    </form>\n  );\n}\n```\n```\n",tech:"react",displayNumber:396,sortKey:1015},{id:"react-1016",number:397,title:"How do you validate with Yup and Formik?",question:"How do you validate with Yup and Formik?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Yup is a schema builder that pairs naturally with Formik via the \`validationSchema\` prop. Formik runs the schema on change/blur and populates \`errors\`/\`touched\`. This keeps validation declarative and reusable. The same schema can validate on the server, preventing drift between client and API.


\`\`\`jsx
import * as Yup from 'yup';

const schema = Yup.object({
  email: Yup.string().email('Invalid').required('Required'),
  password: Yup.string().min(8, 'Too short'),
});

<Formik validationSchema={schema} initialValues={{ email: '', password: '' }} onSubmit={...}>
  {/* render props give values, errors, touched */}
</Formik>;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:397,sortKey:1016},{id:"react-1017",number:398,title:"Zod vs Yup: when would you choose one for form validation?",question:"Zod vs Yup: when would you choose one for form validation?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Zod is TypeScript-first: its schemas infer static types automatically (`z.infer`), making it ideal in TS projects and with RHF's resolver. Yup is older, mature, and has a fluent API widely used with Formik. Choose Zod when you want type inference and composable schemas in a TS codebase; choose Yup for existing Formik setups or when you prefer its API ergonomics. Both validate at runtime.\n\n\n```jsx\n// Zod infers the type\nconst schema = z.object({ email: z.string().email() });\ntype Form = z.infer<typeof schema>; // { email: string }\n```\n```\n",tech:"react",displayNumber:398,sortKey:1017},{id:"react-1018",number:399,title:"How do you build dynamic / field-array forms with React Hook Form's useFieldArray?",question:"How do you build dynamic / field-array forms with React Hook Form's useFieldArray?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`useFieldArray({ control, name })` manages arrays of fields with `fields`, `append`, `remove`, and `insert`. It returns stable `id`s for keys. This is how you build repeatable rows (e.g., todo lists, invoice line items) without manually managing array state, and RHF tracks each sub-field's registration automatically.\n\n\n```jsx\nconst { control, register } = useForm({ defaultValues: { items: [{ name: '' }] } });\nconst { fields, append, remove } = useFieldArray({ control, name: 'items' });\n\nfields.map((field, i) => (\n  <div key={field.id}>\n    <input {...register(`items.${i}.name`)} />\n    <button type='button' onClick={() => remove(i)}>x</button>\n  </div>\n));\n<button type='button' onClick={() => append({ name: '' })}>Add</button>;\n```\n```\n",tech:"react",displayNumber:399,sortKey:1018},{id:"react-1019",number:400,title:"How do you handle file uploads in React forms?",question:"How do you handle file uploads in React forms?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"File inputs are inherently uncontrolled because `value` cannot be set for security reasons. Read the selected file from `event.target.files[0]`. With RHF, register the input and read `data.file[0]`. Upload via FormData: append the file and POST with `multipart/form-data`. Always validate type and size before uploading, and show upload progress with XHR or a fetch wrapper.\n\n\n```jsx\nconst onSubmit = async (data) => {\n  const fd = new FormData();\n  fd.append('avatar', data.avatar[0]);\n  await fetch('/upload', { method: 'POST', body: fd });\n};\n<input type='file' {...register('avatar', { validate: f => f[0]?.size < 1e6 || 'Too big' })} />;\n```\n```\n",tech:"react",displayNumber:400,sortKey:1019},{id:"react-1020",number:401,title:"How do you reset a form and set default values in React Hook Form?",question:"How do you reset a form and set default values in React Hook Form?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"Pass `defaultValues` to `useForm()` for initial values. Use `reset(values)` to programmatically clear or repopulate the form, e.g., after a successful submit or when loading data to edit. `reset()` with no args restores defaults. Note that RHF uses uncontrolled inputs, so defaultValues only apply on mount unless you call reset.\n\n\n```jsx\nconst { reset, handleSubmit } = useForm({ defaultValues: { name: '' } });\nconst submit = (data) => { console.log(data); reset(); };\n// To load existing record:\nuseEffect(() => { if (user) reset(user); }, [user, reset]);\n```\n```\n",tech:"react",displayNumber:401,sortKey:1020},{id:"react-1021",number:402,title:"How do you watch form values with the watch method?",question:"How do you watch form values with the watch method?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`\`watch(name?)\` returns the current value(s) of fields and subscribes the component to changes, causing re-renders when watched fields change (a trade-off versus register, which does not re-render). Use it when UI depends on a value (e.g., showing a password strength meter or conditional field). For non-reactive reads, use \`getValues()\`.


\`\`\`jsx
const { register, watch } = useForm();
const pw = watch('password');
return (
  <>
    <input {...register('password')} />
    <Meter strength={pw?.length ?? 0} />
  </>
);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:402,sortKey:1021},{id:"react-1022",number:403,title:"How do you do conditional / dependent fields in a form?",question:"How do you do conditional / dependent fields in a form?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Use `watch()` (RHF) or the controlled value (Formik) to read one field and conditionally render another. For validation dependencies, reference other fields inside resolver rules or the validate function. Avoid mounting/unmounting inputs you want to preserve; instead use conditional rendering with keys or `shouldUnregister` carefully so values do not leak.\n\n\n```jsx\nconst country = watch('country');\nreturn (\n  <> \n    <select {...register('country')}>...</select>\n    {country === 'US' && <input {...register('state')} placeholder='State' />}\n  </>\n);\n```\n```\n",tech:"react",displayNumber:403,sortKey:1022},{id:"react-1023",number:404,title:"What are the best practices for accessible forms (labels, aria, errors)?",question:"What are the best practices for accessible forms (labels, aria, errors)?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Always associate labels with inputs via `htmlFor`/`id` or wrap them. Use `aria-invalid` and `aria-describedby` to connect error messages to inputs for screen readers. Announce errors with `role='alert'` or `aria-live`. Group related controls with `fieldset`/`legend`. Provide clear, specific error text and visible focus styles. Accessible forms improve usability for everyone and are legally required in many contexts.\n\n\n```jsx\n<label htmlFor='email'>Email</label>\n<input id='email' aria-invalid={!!errors.email} aria-describedby='email-err' {...register('email')} />\n{errors.email && <span id='email-err' role='alert'>{errors.email.message}</span>}\n```\n```\n",tech:"react",displayNumber:404,sortKey:1023},{id:"react-1024",number:405,title:"How do you test a React component with React Testing Library and Jest?",question:"How do you test a React component with React Testing Library and Jest?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"RTL promotes testing from the user's perspective. Render the component with `render(<Comp />)`, query DOM nodes the way a user finds them (`screen.getByRole`, `getByLabelText`, `getByText`), interact with `userEvent`, and assert on the resulting DOM. Avoid testing implementation details (internal state, method calls). Jest provides the runner, matchers, and mocking utilities.\n\n\n```jsx\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\n\ntest('shows greeting', async () => {\n  render(<Greeting name='Ada' />);\n  expect(screen.getByText('Hello Ada')).toBeInTheDocument();\n});\n```\n```\n",tech:"react",displayNumber:405,sortKey:1024},{id:"react-1025",number:406,title:"Explain the difference between getBy*, queryBy*, and findBy* queries.",question:"Explain the difference between getBy*, queryBy*, and findBy* queries.",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"- `getBy*` returns the matching node or throws immediately if not found (or if multiple match). Use for elements that should be present synchronously.\n- `queryBy*` returns the node or null, never throws. Use for asserting absence (e.g., element removed).\n- `findBy*` returns a promise that resolves when the element appears; use for async content (after fetch/timeout). There are also `getAllBy`/`queryAllBy`/`findAllBy` variants.\n\n\n```jsx\nscreen.getByText('Saved');          // must exist now\nexpect(screen.queryByText('Error')).toBeNull(); // must be absent\nawait screen.findByText('Loaded');  // appears async\n```\n```\n",tech:"react",displayNumber:406,sortKey:1025},{id:"react-1026",number:407,title:"How do you simulate user interactions with userEvent?",question:"How do you simulate user interactions with userEvent?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`@testing-library/user-event` provides realistic interaction simulation: `userEvent.click(el)`, `userEvent.type(input, 'text')`, `userEvent.selectOptions`. Prefer it over `fireEvent` because it dispatches the full sequence of events (focus, keydown, input, change) like a real browser. In v14, create an instance `const user = userEvent.setup()` (often in `beforeEach`) and await its methods.\n\n\n```jsx\nconst user = userEvent.setup();\nawait user.type(screen.getByLabelText('Email'), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: 'Submit' }));\n```\n```\n",tech:"react",displayNumber:407,sortKey:1026},{id:"react-1027",number:408,title:"How do you test a controlled form with React Testing Library?",question:"How do you test a controlled form with React Testing Library?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Render the form, query the input by role/label, type into it with `userEvent.type`, click submit, then assert the expected outcome (e.g., success message or that the onSubmit handler was called with the typed value). If onSubmit is a mock prop, assert it was called with the correct data. Keep assertions about what the user sees, not internal state.\n\n\n```jsx\nconst onSubmit = jest.fn();\nrender(<Login onSubmit={onSubmit} />);\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: /sign in/i }));\nexpect(onSubmit).toHaveBeenCalledWith(expect.objectContaining({ email: 'a@b.com' }));\n```\n```\n",tech:"react",displayNumber:408,sortKey:1027},{id:"react-1028",number:409,title:"How do you test a React Hook Form component?",question:"How do you test a React Hook Form component?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Treat it like any form: register inputs with proper labels/names so they are queryable. Type into fields and submit, then assert the resulting DOM or the mocked submit handler. To test validation, submit empty required fields and assert the error message appears (`getByText`/`findByText`). Access `handleSubmit`'s resolved data via a mock passed to the component or by asserting side effects.\n\n\n```jsx\nrender(<Signup />);\nawait user.click(screen.getByRole('button', { name: /submit/i }));\nexpect(await screen.findByText(/email required/i)).toBeInTheDocument();\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\n// ... fill rest, submit, assert success\n```\n```\n",tech:"react",displayNumber:409,sortKey:1028},{id:"react-1029",number:410,title:"How do you mock API calls in tests using MSW (Mock Service Worker)?",question:"How do you mock API calls in tests using MSW (Mock Service Worker)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"MSW intercepts network requests at the service worker (or node) level, so your component code uses real fetch/XHR unmodified. You define `handlers` with `http.get/post` returning mocked responses, set up a `server` in `beforeAll`/`afterEach`/`afterAll`, and optionally override per test with `server.use()`. This avoids mocking fetch internals and tests real request/response flows.\n\n\n```jsx\nimport { setupServer } from 'msw/node';\nimport { http, HttpResponse } from 'msw';\n\nconst server = setupServer(\n  http.get('/api/user', () => HttpResponse.json({ id: 1, name: 'Ada' }))\n);\nbeforeAll(() => server.listen());\nafterEach(() => server.resetHandlers());\nafterAll(() => server.close());\n```\n```\n",tech:"react",displayNumber:410,sortKey:1029},{id:"react-1030",number:411,title:"How do you test async behavior (loading / data fetching) in components?",question:"How do you test async behavior (loading / data fetching) in components?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Render the component, assert the loading state is shown (e.g., a spinner via `getByText('Loading')`), then await the async update with `findBy*` which retries until the resolved content appears. Use `waitFor` for states not tied to a specific element. Provide mocked data (MSW or a mocked module) so the test is deterministic and fast.\n\n\n```jsx\nrender(<UserList />);\nexpect(screen.getByText(/loading/i)).toBeInTheDocument();\nexpect(await screen.findByText('Ada')).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:411,sortKey:1030},{id:"react-1031",number:412,title:'Why should you avoid testing implementation details, and what is the "user event" testing philosophy?',question:'Why should you avoid testing implementation details, and what is the "user event" testing philosophy?',difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Testing implementation details (internal state, private methods, props of children) makes tests brittle: refactors that preserve behavior break them. RTL's philosophy is to test as a user would, querying by accessible role/text and asserting visible outcomes. This gives confidence that the feature works for real users and keeps tests resilient to internal rewrites.


\`\`\`jsx
// Avoid: testing internal state
// expect(wrapper.state('open')).toBe(true);
// Prefer: testing what the user sees
expect(screen.getByRole('dialog')).toBeInTheDocument();
\`\`\`
\`\`\`
`,tech:"react",displayNumber:412,sortKey:1031},{id:"react-1032",number:413,title:"How do you test routing and navigation with React Router in components?",question:"How do you test routing and navigation with React Router in components?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Wrap the component in a `<MemoryRouter>` with `initialEntries` to control the starting URL. Provide route definitions so links/navigation render the right destinations, then assert navigation happened (e.g., the new page's text appears). Use `initialEntries={['/users/1']}` to test a dynamic param route. For hooks like `useNavigate`, render within the router and assert location changes via a test component that reads `useLocation`.\n\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/']}>\n    <Routes>\n      <Route path='/' element={<Home />} />\n      <Route path='/about' element={<About />} />\n    </Routes>\n  </MemoryRouter>\n);\nawait user.click(screen.getByRole('link', { name: /about/i }));\nexpect(screen.getByText(/about page/i)).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:413,sortKey:1032},{id:"react-1033",number:414,title:"How do you render a component that uses useParams / useNavigate in tests?",question:"How do you render a component that uses useParams / useNavigate in tests?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Route hooks require a Router context. Render the component inside `<MemoryRouter initialEntries={['/users/42']}>` with a `<Routes><Route path='/users/:id' element={<YourComp/>} /></Routes>`. Then `useParams()` returns `{ id: '42' }`. For `useNavigate`, click a button that navigates and assert the destination renders, or use a spy on navigate via a custom wrapper.\n\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/users/42']}>\n    <Routes><Route path='/users/:id' element={<UserProfile />} /></Routes>\n  </MemoryRouter>\n);\nexpect(screen.getByText('User 42')).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:414,sortKey:1033},{id:"react-1034",number:415,title:"How do you mock modules with Jest (jest.mock) for testing?",question:"How do you mock modules with Jest (jest.mock) for testing?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`jest.mock('module')` replaces a module with a mock for the whole test file; `jest.mock('module', () => ({ ... }), { virtual: true })` lets you supply a factory. Use `jest.fn()` for functions and control return values per test. Mock components you don't want to render (e.g., heavy charts) to keep tests focused. Remember hoisting: `jest.mock` calls are hoisted to the top regardless of placement.\n\n\n```jsx\njest.mock('../api', () => ({ fetchUser: jest.fn() }));\nimport { fetchUser } from '../api';\nfetchUser.mockResolvedValue({ name: 'Ada' });\n```\n```\n",tech:"react",displayNumber:415,sortKey:1034},{id:"react-1035",number:416,title:"What is the role of cleanup() and why does RTL auto-cleanup?",question:"What is the role of cleanup() and why does RTL auto-cleanup?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`cleanup()` unmounts React trees rendered by RTL and removes them from the DOM. Without it, multiple renders in different tests would accumulate nodes and leak state/listeners. RTL auto-configures `afterEach(cleanup)` via its Jest setup, so you usually don't call it manually. It also resets `jest` mocks if configured. Disabling it is rarely needed.\n\n\n```jsx\n// Usually automatic via jest.setup with @testing-library/react\nimport '@testing-library/react'; // exports cleanup, auto-wired by jest config\n```\n```\n",tech:"react",displayNumber:416,sortKey:1035},{id:"react-1036",number:417,title:"How do you use jest.fn() spies and assertions like toHaveBeenCalledWith?",question:"How do you use jest.fn() spies and assertions like toHaveBeenCalledWith?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`jest.fn()` creates a mock function you can pass as a callback/prop. Assertions verify behavior: `toHaveBeenCalled()`, `toHaveBeenCalledWith(args)`, `toHaveBeenCalledTimes(n)`, and `expect.any(Function)`. Inspect calls with `mock.calls`. This is how you test that a child invoked a parent handler with the right payload without rendering the parent's logic.\n\n\n```jsx\nconst onSave = jest.fn();\nrender(<Editor onSave={onSave} />);\nawait user.click(screen.getByRole('button', { name: /save/i }));\nexpect(onSave).toHaveBeenCalledTimes(1);\nexpect(onSave).toHaveBeenCalledWith(expect.objectContaining({ id: 1 }));\n```\n```\n",tech:"react",displayNumber:417,sortKey:1036},{id:"react-1037",number:418,title:"How do you test custom hooks with renderHook and act?",question:"How do you test custom hooks with renderHook and act?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`@testing-library/react`'s `renderHook` renders a hook in a test harness and returns `{ result, rerender, unmount }`. Read the latest value via `result.current`. Wrap state-changing interactions in `act()` (userEvent and fireEvent already wrap in act) so React flushes updates before you assert. For hooks needing context (router, query client), pass a `wrapper` option.\n\n\n```jsx\nconst { result } = renderHook(() => useCounter());\nact(() => { result.current.increment(); });\nexpect(result.current.count).toBe(1);\n```\n```\n",tech:"react",displayNumber:418,sortKey:1037},{id:"react-1038",number:419,title:"How do you test error states and error boundaries?",question:"How do you test error states and error boundaries?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"For a thrown render error, wrap the component in an Error Boundary and assert the fallback UI renders. For data-fetch errors, mock the API to reject and assert the error message appears (using `findBy*` to await it). React 18 `render` does not catch errors during render; use `react-error-boundary`'s `ErrorBoundary` or a custom class boundary as the wrapper in tests.\n\n\n```jsx\nserver.use(http.get('/api', () => HttpResponse.error()));\nrender(<User />);\nexpect(await screen.findByText(/failed to load/i)).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:419,sortKey:1038},{id:"react-1039",number:420,title:"What is the difference between fireEvent and userEvent?",question:"What is the difference between fireEvent and userEvent?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`fireEvent` dispatches a single synthetic event immediately (e.g., `fireEvent.change(input, { target: { value: 'x' } })`). `userEvent` simulates the full, realistic sequence of browser events (pointer down/up, focus, key events, input, change) and is async. Prefer `userEvent` for behavior fidelity; use `fireEvent` when you need a quick, synchronous, single-event dispatch or to avoid async overhead.\n\n\n```jsx\n// fireEvent: fast, single event\nfireEvent.change(input, { target: { value: 'a' } });\n\n// userEvent: realistic, async\nawait user.type(input, 'a');\n```\n```\n",tech:"react",displayNumber:420,sortKey:1039},{id:"react-1040",number:421,title:"How do you use waitFor to assert on eventually-updated DOM?",question:"How do you use waitFor to assert on eventually-updated DOM?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`waitFor(callback, options)` retries the callback until it stops throwing or times out (default 1000ms). Use it when an assertion depends on async side effects not tied to a single query, such as a value updated after a promise resolves. Prefer `findBy*` when waiting for an element to appear because it's more concise; reach for `waitFor` when the condition is custom (e.g., attribute toggles).\n\n\n```jsx\nawait waitFor(() => expect(screen.getByRole('status')).toHaveClass('done'));\n```\n```\n",tech:"react",displayNumber:421,sortKey:1040},{id:"react-1041",number:422,title:"How do you query elements by role, label, text, and test id?",question:"How do you query elements by role, label, text, and test id?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"RTL queries in order of preference: `getByRole` (most accessible, matches ARIA role), `getByLabelText` (form inputs), `getByPlaceholderText`, `getByText` (non-interactive text), `getByDisplayValue` (input value), and lastly `getByTestId` (only when no accessible alternative exists). Prioritize role/label queries because they reflect what users and assistive tech actually encounter.\n\n\n```jsx\nscreen.getByRole('button', { name: /submit/i });\nscreen.getByLabelText(/email/i);\nscreen.getByText('Welcome');\nscreen.getByTestId('custom-widget');\n```\n```\n",tech:"react",displayNumber:422,sortKey:1041},{id:"react-1042",number:423,title:"How do you test conditional rendering with queryBy?",question:"How do you test conditional rendering with queryBy?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"Use `queryBy*` to assert an element is absent (returns null instead of throwing). A common pattern: render, trigger an action that should remove a node, then `expect(screen.queryByText('Old')).not.toBeInTheDocument()`. Pair with `getBy*` to assert presence before removal. This avoids false positives from `getBy` throwing on the wrong assertion.\n\n\n```jsx\nexpect(screen.getByText('Banner')).toBeInTheDocument();\nawait user.click(screen.getByRole('button', { name: /dismiss/i }));\nexpect(screen.queryByText('Banner')).not.toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:423,sortKey:1042},{id:"react-1043",number:424,title:"How do you test a component that uses React Router's Loader/Action?",question:"How do you test a component that uses React Router's Loader/Action?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Use `createMemoryRouter(routes, { initialEntries })` and render with `<RouterProvider router={router} />`. Mock the loader/action dependencies (API module) so they resolve deterministically. Await `findBy*` to assert loaded data or submitted results. Because the data router handles promises, wrap assertions in async and use `findBy`/`waitFor` for the resolved state.\n\n\n```jsx\nconst router = createMemoryRouter(routes, { initialEntries: ['/users/1'] });\nrender(<RouterProvider router={router} />);\nexpect(await screen.findByText('Ada')).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:424,sortKey:1043},{id:"react-1044",number:425,title:"How do you set up a test environment for React (jsdom, jest config, setup file)?",question:"How do you set up a test environment for React (jsdom, jest config, setup file)?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Configure Jest with `testEnvironment: 'jsdom'` so DOM APIs exist, add `setupFilesAfterEach` pointing to a setup file that imports `@testing-library/jest-dom` (for matchers like `toBeInTheDocument`) and configures automatic cleanup. Use `ts-jest` or Babel for TS/JSX. MSW and matchMedia/localStorage polyfills are added in the setup as needed.\n\n\n```jsx\n// jest.config.js\nmodule.exports = {\n  testEnvironment: 'jsdom',\n  setupFilesAfterEach: ['<rootDir>/jest.setup.js'],\n};\n// jest.setup.js\nimport '@testing-library/jest-dom';\n```\n```\n",tech:"react",displayNumber:425,sortKey:1044},{id:"react-1045",number:426,title:"How do you handle timer-based code (setTimeout) in tests with fake timers?",question:"How do you handle timer-based code (setTimeout) in tests with fake timers?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Use Jest fake timers: `jest.useFakeTimers()` in setup, then `jest.advanceTimersByTime(ms)` or `jest.runOnlyPendingTimers()` inside `act()` to move time forward deterministically. This avoids real waits. Be careful combining fake timers with `userEvent` (use `jest.useFakeTimers({ advanceTimers: true })` and the modern async userEvent with `advanceTimers`).\n\n\n```jsx\njest.useFakeTimers();\nact(() => { render(<Toast />); });\nact(() => { jest.advanceTimersByTime(3000); });\nexpect(screen.queryByText('Saved')).not.toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:426,sortKey:1045},{id:"react-1046",number:427,title:"How do you test form validation error messages?",question:"How do you test form validation error messages?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Submit the form (or blur a field) to trigger validation, then assert the error text appears. Use `findByText` because validation may be async (especially with schema resolvers). Ensure error elements have stable, queryable text and ideally `role='alert'`. For RHF/Formik, the error only shows after submit (or touched) unless you set mode to onChange.\n\n\n```jsx\nawait user.click(screen.getByRole('button', { name: /submit/i }));\nexpect(await screen.findByText(/email is required/i)).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:427,sortKey:1046},{id:"react-1047",number:428,title:"How do you test that a form submits the correct data?",question:"How do you test that a form submits the correct data?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Pass a `jest.fn()` as the submit handler (or mock the API), fill fields realistically with `userEvent.type`, click submit, and assert the handler received the expected payload via `toHaveBeenCalledWith`. For real network submission, mock the endpoint with MSW and assert the request body or the resulting UI state (success message).\n\n\n```jsx\nconst onSubmit = jest.fn();\nrender(<Form onSubmit={onSubmit} />);\nawait user.type(screen.getByLabelText(/name/i), 'Ada');\nawait user.click(screen.getByRole('button', { name: /save/i }));\nexpect(onSubmit).toHaveBeenCalledWith({ name: 'Ada' });\n```\n```\n",tech:"react",displayNumber:428,sortKey:1047},{id:"react-1048",number:429,title:"How do you test React Router navigation side effects (redirect after login)?",question:"How do you test React Router navigation side effects (redirect after login)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Render the login flow inside a `MemoryRouter` with route definitions for both `/login` and the post-login destination (e.g., `/dashboard`). After submitting valid credentials (mock the auth API), assert the dashboard content is now rendered, proving navigation occurred. Alternatively, spy on `useNavigate` via a wrapper component to assert it was called with the expected path.\n\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/login']}>\n    <Routes>\n      <Route path='/login' element={<Login />} />\n      <Route path='/dashboard' element={<h1>Dashboard</h1>} />\n    </Routes>\n  </MemoryRouter>\n);\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: /login/i }));\nexpect(await screen.findByText('Dashboard')).toBeInTheDocument();\n```\n```\n",tech:"react",displayNumber:429,sortKey:1048},{id:"react-1049",number:430,title:"How do you mock fetch or axios in a test?",question:"How do you mock fetch or axios in a test?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Prefer MSW for intercepting at the network layer so your code uses real fetch/axios unchanged. Alternatively, `jest.mock('axios')` and provide resolved/rejected values per test. For global fetch, you can spy: `jest.spyOn(global, 'fetch').mockResolvedValue({ json: async () => data })`. MSW is more faithful; module mocks are simpler for unit isolation.\n\n\n```jsx\njest.mock('axios');\nimport axios from 'axios';\naxios.get.mockResolvedValue({ data: { name: 'Ada' } });\n```\n```\n",tech:"react",displayNumber:430,sortKey:1049},{id:"react-1050",number:431,title:"What is the purpose of screen and why prefer it over container queries?",question:"What is the purpose of screen and why prefer it over container queries?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`screen` is an object containing all the queries bound to `document.body`, so you don't need the `container` or `baseElement` returned by `render`. Using `screen.getBy*` keeps queries in the global document, matches how users see the page, and makes refactoring easier (you don't pass containers around). It's the recommended default in modern RTL.\n\n\n```jsx\nconst { container } = render(<App />); // older style\n// Prefer:\nrender(<App />);\nscreen.getByText('Hello');\n```\n```\n",tech:"react",displayNumber:431,sortKey:1050},{id:"react-1051",number:432,title:"How do you test input change and prevent default behavior?",question:"How do you test input change and prevent default behavior?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"With `userEvent.type` into a labelled input, the component's `onChange`/`register` handles updates and React's synthetic event system calls `preventDefault` internally for controlled flows. In tests you don't call preventDefault manually; you just simulate realistic typing and submission. If testing a raw handler, `fireEvent.submit(form)` triggers the submit and React Router's Form prevents the native reload automatically.\n\n\n```jsx\nawait user.type(screen.getByLabelText(/search/i), 'react');\nawait user.click(screen.getByRole('button', { name: /go/i }));\n// assert search results; no manual preventDefault needed\n```\n```\n",tech:"react",displayNumber:432,sortKey:1051},{id:"react-1052",number:433,title:"How do you test React Hook Form's useFieldArray (dynamic rows)?",question:"How do you test React Hook Form's useFieldArray (dynamic rows)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Render the form, query the initial row inputs, type into them, click "Add" to append a row, assert a new row appears (e.g., one more inputs or a count), fill it, submit, and assert the handler receives an array with both rows. Use \`findAllByLabelText\` to count repeated fields. Because RHF uses uncontrolled inputs, query by name/label, not by controlled value.


\`\`\`jsx
const rows = await screen.findAllByLabelText(/item name/i);
expect(rows).toHaveLength(1);
await user.click(screen.getByRole('button', { name: /add/i }));
expect(await screen.findAllByLabelText(/item name/i)).toHaveLength(2);
\`\`\`
\`\`\`
`,tech:"react",displayNumber:433,sortKey:1052},{id:"react-1053",number:434,title:"How do you write a test for a protected route redirect?",question:"How do you write a test for a protected route redirect?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Render the protected route tree inside a \`MemoryRouter\` where the auth context reports an unauthenticated user. Include both the protected page route and the \`/login\` route. After render (and any effect), assert the login page content is shown rather than the protected content, proving the \`<RequireAuth>\` redirect fired. Spy on \`useNavigate\` if you want to assert the call instead.


\`\`\`jsx
render(
  <AuthContext.Provider value={{ user: null }}>
    <MemoryRouter initialEntries={['/profile']}>
      <Routes>
        <Route element={<RequireAuth />}>
          <Route path='/profile' element={<Profile />} />
        </Route>
        <Route path='/login' element={<h1>Login</h1>} />
      </Routes>
    </MemoryRouter>
  </AuthContext.Provider>
);
expect(screen.getByText('Login')).toBeInTheDocument();
expect(screen.queryByText('Profile')).not.toBeInTheDocument();
\`\`\`
\`\`\`
`,tech:"react",displayNumber:434,sortKey:1053},{id:"react-1054",number:435,title:"What are best practices for organizing and writing maintainable tests?",question:"What are best practices for organizing and writing maintainable tests?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Follow the "Arrange, Act, Assert" structure per test. Query by accessibility (role/label) over test ids. Test behavior, not implementation. Keep tests isolated with cleanup and per-test mocks (reset handlers). Co-locate tests with components. Use shared custom render wrappers (with providers/router) to reduce boilerplate. Mock at the boundary (network, modules), not internal logic. Aim for fast, deterministic, readable tests over 100% coverage of trivial code.


\`\`\`jsx
// custom render with providers reused across tests
const customRender = (ui, { route = '/' } = {}) =>
  render(<Providers><MemoryRouter initialEntries={[route]}>{ui}</MemoryRouter></Providers>);
export * from '@testing-library/react';
export { customRender as render };
\`\`\`
\`\`\`
`,tech:"react",displayNumber:435,sortKey:1054},{id:"react-1055",number:436,title:"What is a Higher-Order Component (HOC) and when would you use one?",question:"What is a Higher-Order Component (HOC) and when would you use one?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`A Higher-Order Component is a function that takes a component and returns a new, enhanced component. HOCs are a pattern for reusing component logic without duplication. They follow composition over inheritance. Typical uses: authentication guards, data fetching, logging, theming, and error/loading wrappers. The HOC does not mutate the wrapped component; it composes a new one.


\`\`\`jsx
// A simple HOC that adds a loading indicator
function withLoading(WrappedComponent) {
  return function WithLoading({ isLoading, ...props }) {
    if (isLoading) return <div>Loading...</div>;
    return <WrappedComponent {...props} />;
  };
}

const UserListWithLoading = withLoading(UserList);
<UserListWithLoading isLoading={loading} users={users} />;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:436,sortKey:1055},{id:"react-1056",number:437,title:"What are the caveats and best practices when using HOCs?",question:"What are the caveats and best practices when using HOCs?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"HOCs wrap, never mutate, the input component. They must forward unknown props (`...props`) and `ref`s to the wrapped component. They can break if composed deeply (wrapper chains) and can cause prop name collisions. Always set a meaningful `displayName` for debugging. Avoid using HOCs inside the render method (creates a new component each render, remounting the tree). Prefer composing with `compose()` from a utility library.\n\n\n```jsx\n// Anti-pattern: creating the HOC inside render remounts on every render\nrender() {\n  const Enhanced = withLogging(MyComponent); // new type each render!\n  return <Enhanced />;\n}\n\n// Best practice: define HOCs outside the component\nconst Enhanced = compose(withLoading, withAuth)(MyComponent);\nEnhanced.displayName = 'WithAuth(WithLoading(MyComponent))';\n```\n```\n",tech:"react",displayNumber:437,sortKey:1056},{id:"react-1057",number:438,title:"How do you preserve displayName and forward refs through an HOC?",question:"How do you preserve displayName and forward refs through an HOC?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Wrapped components lose their name in DevTools unless you set `displayName`. Refs passed to the enhanced component won't reach the inner component unless you use `React.forwardRef`. Combine both so the HOC is transparent and supports imperative handles.\n\n\n```jsx\nfunction withMouse(WrappedComponent) {\n  const DisplayName = WrappedComponent.displayName || WrappedComponent.name || 'Component';\n  const Component = React.forwardRef((props, ref) => {\n    const [pos, setPos] = React.useState({ x: 0, y: 0 });\n    return <WrappedComponent ref={ref} mouse={pos} {...props} />;\n  });\n  Component.displayName = `withMouse(${DisplayName})`;\n  return Component;\n}\n```\n```\n",tech:"react",displayNumber:438,sortKey:1057},{id:"react-1058",number:439,title:"What is the Render Props pattern?",question:"What is the Render Props pattern?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`A render prop is a prop whose value is a function that returns a React element. It lets a component share its internal state or logic with its parent, which decides what to render. This is a flexible alternative to HOCs when you need fine-grained control over the rendered output and want to avoid wrapper nesting.


\`\`\`jsx
class MouseTracker extends React.Component {
  state = { x: 0, y: 0 };
  handleMove = (e) => this.setState({ x: e.clientX, y: e.clientY });
  render() {
    return (
      <div onMouseMove={this.handleMove}>
        {this.props.render(this.state)}
      </div>
    );
  }
}

<MouseTracker render={({ x, y }) => <h1>Cursor at {x},{y}</h1>} />;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:439,sortKey:1058},{id:"react-1059",number:440,title:"Show a practical Render Props example for data fetching.",question:"Show a practical Render Props example for data fetching.",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Render props shine for cross-cutting state like data fetching or hover. The container manages loading/error/data and calls the render function with the result. The consumer renders exactly what it wants, including custom loading and error UI.


\`\`\`jsx
function Fetch({ url, children }) {
  const [state, setState] = React.useState({ data: null, loading: true, error: null });
  React.useEffect(() => {
    fetch(url)
      .then((r) => r.json())
      .then((data) => setState({ data, loading: false, error: null }))
      .catch((error) => setState({ error, loading: false, data: null }));
  }, [url]);
  return children(state);
}

<Fetch url='/api/user'>
  {({ data, loading, error }) =>
    loading ? <Spinner /> : error ? <Error /> : <Profile data={data} />
  }
</Fetch>;
\`\`\`
\`\`\`
`,tech:"react",displayNumber:440,sortKey:1059},{id:"react-1060",number:441,title:"Render Props vs the `children`-as-function pattern — what is the difference?",question:"Render Props vs the `children`-as-function pattern — what is the difference?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"They are the same idea; the only difference is which prop holds the function. Using `children` as a function reads naturally (`<Comp>{(api)=>...}</Comp>`) and is the most common form. A named prop like `render` makes the intent explicit. Both avoid HOC nesting and make the data flow visible in JSX.\n\n\n```jsx\n// children as function\n<Toggle>{({ on, toggle }) => <button onClick={toggle}>{on ? 'On' : 'Off'}</button>}</Toggle>\n\n// named render prop\n<Toggle render={({ on, toggle }) => <Switch on={on} onClick={toggle} />} />\n```\n```\n",tech:"react",displayNumber:441,sortKey:1060},{id:"react-1061",number:442,title:"What is the Compound Components pattern?",question:"What is the Compound Components pattern?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Compound components are several components that work together and share implicit state, while letting the parent control the structure/composition. Instead of one big configurable component, you expose \`Menu\`, \`Menu.Item\`, \`Menu.Button\`. This gives a declarative, flexible API and hides coordination logic via context.


\`\`\`jsx
function Select({ children }) {
  const [open, setOpen] = React.useState(false);
  return <Ctx.Provider value={{ open, setOpen }}>{children}</Ctx.Provider>;
}
Select.Trigger = function Trigger({ children }) {
  const { open, setOpen } = React.useContext(Ctx);
  return <button onClick={() => setOpen(!open)}>{children}</button>;
};
Select.List = function List({ children }) {
  const { open } = React.useContext(Ctx);
  return open ? <ul>{children}</ul> : null;
};
\`\`\`
\`\`\`
`,tech:"react",displayNumber:442,sortKey:1061},{id:"react-1062",number:443,title:"Build a working Compound Components example with context.",question:"Build a working Compound Components example with context.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Use a context to share state among sibling subcomponents so consumers can reorder or omit parts freely. The parent sets up the provider; subcomponents read/write via \`useContext\`. This keeps the public API expressive (\`<Tabs><Tabs.List/>...</Tabs>\`).


\`\`\`jsx
const TabsCtx = React.createContext(null);
function Tabs({ children }) {
  const [active, setActive] = React.useState(0);
  return <TabsCtx.Provider value={{ active, setActive }}>{children}</TabsCtx.Provider>;
}
Tabs.List = ({ children }) => <div role='tablist'>{children}</div>;
Tabs.Tab = ({ index, children }) => {
  const { active, setActive } = React.useContext(TabsCtx);
  return <button aria-selected={active === index} onClick={() => setActive(index)}>{children}</button>;
};
Tabs.Panel = ({ index, children }) => {
  const { active } = React.useContext(TabsCtx);
  return active === index ? <div role='tabpanel'>{children}</div> : null;
};
\`\`\`
\`\`\`
`,tech:"react",displayNumber:443,sortKey:1062},{id:"react-1063",number:444,title:"How do you make Compound Components resilient to missing subcomponents?",question:"How do you make Compound Components resilient to missing subcomponents?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Guard against misuse: throw helpful errors when a subcomponent is rendered outside its parent, and document required context. You can also expose a hook (\`useTabs\`) for advanced consumers. Clear runtime errors beat silent failures when the pattern is misused.


\`\`\`jsx
function useTabsCtx() {
  const ctx = React.useContext(TabsCtx);
  if (!ctx) throw new Error('Tabs.Tab must be used within <Tabs>');
  return ctx;
}
// Then each subcomponent calls useTabsCtx() instead of useContext directly.
\`\`\`
\`\`\`
`,tech:"react",displayNumber:444,sortKey:1063},{id:"react-1064",number:445,title:"What is the Control Props pattern?",question:"What is the Control Props pattern?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Control Props let the parent fully control a component's state from outside while still supporting an uncontrolled (internal-state) mode. The component is "controlled" if the relevant prop is provided, otherwise it manages its own state. This mirrors React's own controlled inputs and offers maximum flexibility.


\`\`\`jsx
function Toggle({ on, defaultOn = false, onChange }) {
  const [internal, setInternal] = React.useState(defaultOn);
  const isControlled = on !== undefined;
  const current = isControlled ? on : internal;
  const toggle = () => {
    const next = !current;
    if (!isControlled) setInternal(next);
    onChange?.(next);
  };
  return <button onClick={toggle}>{current ? 'On' : 'Off'}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:445,sortKey:1064},{id:"react-1065",number:446,title:"Show the Control Props pattern handling both controlled and uncontrolled usage.",question:"Show the Control Props pattern handling both controlled and uncontrolled usage.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Detect control by checking whether the prop is \`undefined\`. When controlled, ignore internal state and render the prop; when uncontrolled, keep internal state and call the optional callback. This single component supports \`<Toggle on={x}/>\` and \`<Toggle defaultOn onChange={...}/>\`.


\`\`\`jsx
function useControllableState({ value, defaultValue, onChange }) {
  const [uncontrolled, setUncontrolled] = React.useState(defaultValue);
  const isControlled = value !== undefined;
  const current = isControlled ? value : uncontrolled;
  const set = (next) => {
    if (!isControlled) setUncontrolled(next);
    onChange?.(next);
  };
  return [current, set];
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:446,sortKey:1065},{id:"react-1066",number:447,title:"HOC vs Render Props vs Custom Hooks — when to use which?",question:"HOC vs Render Props vs Custom Hooks — when to use which?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Custom hooks are now the default for sharing stateful logic because they avoid extra component tree layers and are easiest to compose. Use Render Props when the consumer needs to control rendering output. Use HOCs for cross-cutting concerns that wrap a component's contract (auth, error boundary adapters). Prefer hooks; reach for render props/HOCs only when hooks can't express the need.


\`\`\`jsx
// Hook: best default
function useMouse() {
  const [pos, setPos] = React.useState({ x: 0, y: 0 });
  React.useEffect(() => {
    const h = (e) => setPos({ x: e.clientX, y: e.clientY });
    window.addEventListener('mousemove', h);
    return () => window.removeEventListener('mousemove', h);
  }, []);
  return pos;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:447,sortKey:1066},{id:"react-1067",number:448,title:"What are React Server Components (RSC)?",question:"What are React Server Components (RSC)?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`RSC are components that render on the server (or at build time) and send a lightweight serialized description to the client instead of JavaScript. They can access server-only resources (databases, filesystem, secrets) directly and ship zero client JS. They enable a smaller bundle and better initial load. They cannot use state, effects, or browser-only APIs.


\`\`\`jsx
// app/page.js  (Server Component by default)
import { db } from '@/lib/db';
export default async function Page() {
  const posts = await db.post.findMany();
  return <ul>{posts.map((p) => <li key={p.id}>{p.title}</li>)}</ul>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:448,sortKey:1067},{id:"react-1068",number:449,title:"What does the `'use client'` directive do?",question:"What does the `'use client'` directive do?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`\`'use client'\` marks a module boundary: everything below it (and imported into it) becomes a Client Component, rendered on the server for the initial HTML but hydrated and interactive in the browser. Use it only where you need state, effects, event handlers, or browser APIs. Server Components can import Client Components, but not the reverse across the boundary (props passed must be serializable).


\`\`\`jsx
'use client';
import { useState } from 'react';
export default function Counter() {
  const [n, setN] = useState(0);
  return <button onClick={() => setN(n + 1)}>{n}</button>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:449,sortKey:1068},{id:"react-1069",number:450,title:"What are the rules and limitations of Server vs Client Components?",question:"What are the rules and limitations of Server vs Client Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Components: no hooks/state/effects, no event handlers, can be async, may use server-only code. Client Components: support interactivity, run in the browser, but increase JS shipped. You cannot import a Server Component into a Client Component directly; instead pass it as \`children\`/props. Props crossing the boundary must be serializable (no functions, classes, or non-JSON values).


\`\`\`jsx
// app/layout.js (Server)
import ClientNav from './ClientNav'; // ok: server imports client
export default function Layout({ children }) {
  return <html><body><ClientNav />{children}</body></html>;
}
// Passing a server component into a client one via children:
<ClientShell><ServerContent /></ClientShell> // ServerContent rendered on server
\`\`\`
\`\`\`
`,tech:"react",displayNumber:450,sortKey:1069},{id:"react-1070",number:451,title:"How do you pass a Server Component into a Client Component?",question:"How do you pass a Server Component into a Client Component?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`You can't import a Server Component into a Client Component, but you can pass it as a \`children\` or prop from a Server Component that renders both. The Server Component creates the element and hands it down; the Client Component receives it as a serialized React node and renders it. This is the canonical "escape hatch" for mixing the two.


\`\`\`jsx
// ServerComponent.js (Server)
import ClientCard from './ClientCard';
import ServerChart from './ServerChart'; // heavy server work
export default function Page() {
  return <ClientCard title='Report'><ServerChart /></ClientCard>;
}
// ClientCard.js ('use client')
export default function ClientCard({ title, children }) {
  const [open, setOpen] = useState(true);
  return <div><h2>{title}</h2>{open && children}</div>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:451,sortKey:1070},{id:"react-1071",number:452,title:"Can Client Components be rendered on the server? Explain the two-pass rendering.",question:"Can Client Components be rendered on the server? Explain the two-pass rendering.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Yes. Client Components still render to HTML on the server for the initial response (so content is visible without JS), then hydrate in the browser to become interactive. The server pass produces static markup; the client pass attaches listeners. This is why \`'use client'\` ≠ "server-side rendering is skipped" — it means "also runs on the client."


\`\`\`jsx
// Both passes happen: server renders markup, client hydrates.
'use client';
export default function Badge({ label }) {
  // Renders to HTML on server, then hydrates on client
  return <span className='badge'>{label}</span>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:452,sortKey:1071},{id:"react-1072",number:453,title:"What is the Next.js App Router and how does it differ from the Pages Router?",question:"What is the Next.js App Router and how does it differ from the Pages Router?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"The App Router (Next 13+) uses a `app/` directory with file-system routing, React Server Components by default, nested layouts, and colocated `loading`/`error`/`not-found` files. The older Pages Router (`pages/`) renders everything as Client Components by default with `getStaticProps`/`getServerSideProps`. App Router enables more granular server/client splits and streaming.\n\n\n```jsx\napp/\n  layout.js          // root layout (required)\n  page.js            // route /\n  dashboard/\n    layout.js        // nested layout\n    page.js          // route /dashboard\n    loading.js       // streaming fallback\n    error.js         // error boundary\n```\n```\n",tech:"react",displayNumber:453,sortKey:1072},{id:"react-1073",number:454,title:"Explain Layouts in the App Router.",question:"Explain Layouts in the App Router.",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"A `layout.js` wraps its segment and all nested segments, persisting state and avoiding remounts on navigation between sibling routes. The root layout must define `<html>` and `<body>`. Layouts are Server Components by default and receive `children`. Nested layouts compose, so shared UI (nav, sidebar) stays mounted.\n\n\n```jsx\n// app/dashboard/layout.js\nexport default function DashboardLayout({ children }) {\n  return (\n    <section>\n      <Sidebar />\n      <main>{children}</main>\n    </section>\n  );\n}\n```\n```\n",tech:"react",displayNumber:454,sortKey:1073},{id:"react-1074",number:455,title:"How do nested layouts preserve state across navigations?",question:"How do nested layouts preserve state across navigations?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"The App Router keeps parent layouts mounted while only the changed segment re-renders. Because the layout component instance is not discarded on navigation, any state held in a layout (or a client island within it) persists, and client-side navigation does not refetch the layout. This gives SPA-like feel with server rendering.\n\n\n```jsx\n// app/layout.js keeps nav mounted; navigating /a -> /b\n// only the page segment changes, layout state (e.g. open menu) survives.\n```\n```\n",tech:"react",displayNumber:455,sortKey:1074},{id:"react-1075",number:456,title:"What are Pages in the App Router and how are they defined?",question:"What are Pages in the App Router and how are they defined?",difficulty:"Basic",category:"Advanced Patterns & Next.js",answer:"A `page.js` file defines the UI for a route segment and is always the leaf of a route. By default it's a Server Component and can be async. There is one `page.js` per folder; the folder structure maps to the URL. Pages receive `params` and `searchParams` (in async form in recent Next versions).\n\n\n```jsx\n// app/products/[id]/page.js\nexport default async function ProductPage({ params }) {\n  const { id } = await params;\n  const product = await getProduct(id);\n  return <h1>{product.name}</h1>;\n}\n```\n```\n",tech:"react",displayNumber:456,sortKey:1075},{id:"react-1076",number:457,title:"How does Loading UI (`loading.js`) work with Suspense?",question:"How does Loading UI (`loading.js`) work with Suspense?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`loading.js` creates a Suspense boundary around the page. While the page's async work (data, streaming) is pending, Next shows the loading UI instantly, then swaps in the resolved content. This enables streaming SSR without manually wrapping each component in `<Suspense>`. It applies at the segment level.\n\n\n```jsx\n// app/dashboard/loading.js\nexport default function Loading() {\n  return <Skeleton />;\n}\n// Next wraps the page in <Suspense fallback={<Loading/>}>\n```\n```\n",tech:"react",displayNumber:457,sortKey:1076},{id:"react-1077",number:458,title:"How do Error Boundaries work in the App Router (`error.js`)?",question:"How do Error Boundaries work in the App Router (`error.js`)?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`error.js` is a Client Component that catches errors thrown in its segment (and below) and renders a fallback. It must accept `error` and `reset` props. It does not catch errors in the root layout or in `error.js` itself. You can nest error boundaries for granular recovery; `reset` re-renders the segment.\n\n\n```jsx\n'use client';\nexport default function Error({ error, reset }) {\n  return (\n    <div>\n      <p>Something went wrong: {error.message}</p>\n      <button onClick={reset}>Try again</button>\n    </div>\n  );\n}\n```\n```\n",tech:"react",displayNumber:458,sortKey:1077},{id:"react-1078",number:459,title:"What is `global-error.js` and when is it used?",question:"What is `global-error.js` and when is it used?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`global-error.js` catches errors in the root layout (which `error.js` cannot). It must render its own `<html>` and `<body>` because it replaces the whole document on failure. It's the last-resort boundary for catastrophic errors outside normal segments.\n\n\n```jsx\n'use client';\nexport default function GlobalError({ error, reset }) {\n  return (\n    <html><body>\n      <h1>Fatal error</h1>\n      <button onClick={reset}>Reload</button>\n    </body></html>\n  );\n}\n```\n```\n",tech:"react",displayNumber:459,sortKey:1078},{id:"react-1079",number:460,title:"What is `not-found.js` and how do you trigger a 404?",question:"What is `not-found.js` and how do you trigger a 404?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`not-found.js` renders a custom 404 UI for a segment. Call the `notFound()` helper from `next/navigation` inside a Server or Client Component to trigger it and return a 404 status. Without a segment-level file, the root `not-found.js` (or Next's default) is used.\n\n\n```jsx\nimport { notFound } from 'next/navigation';\nexport default async function Page({ params }) {\n  const { id } = await params;\n  const user = await getUser(id);\n  if (!user) notFound();\n  return <Profile user={user} />;\n}\n```\n```\n",tech:"react",displayNumber:460,sortKey:1079},{id:"react-1080",number:461,title:"How do Route Handlers work in the App Router?",question:"How do Route Handlers work in the App Router?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"Route Handlers live in `app/api/.../route.js` and let you build backend endpoints using Web `Request`/`Response`. Export named functions per HTTP method (`GET`, `POST`, `PUT`, `DELETE`, etc.). They run on the server and support caching, dynamic behavior, and streaming. They replace API routes from the Pages Router.\n\n\n```jsx\n// app/api/posts/route.js\nimport { NextResponse } from 'next/server';\nexport async function GET() {\n  const posts = await getPosts();\n  return NextResponse.json(posts);\n}\nexport async function POST(req) {\n  const body = await req.json();\n  const created = await createPost(body);\n  return NextResponse.json(created, { status: 201 });\n}\n```\n```\n",tech:"react",displayNumber:461,sortKey:1080},{id:"react-1081",number:462,title:"How do you handle dynamic route parameters in Route Handlers?",question:"How do you handle dynamic route parameters in Route Handlers?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"Use folder segments with brackets, e.g. `app/api/posts/[id]/route.js`, and read `params` (now async in recent versions) from the second argument of the handler. Validate and parse the id before use, and return 404s for missing records.\n\n\n```jsx\n// app/api/posts/[id]/route.js\nexport async function GET(req, { params }) {\n  const { id } = await params;\n  const post = await getPost(id);\n  if (!post) return new Response('Not found', { status: 404 });\n  return Response.json(post);\n}\n```\n```\n",tech:"react",displayNumber:462,sortKey:1081},{id:"react-1082",number:463,title:"What are Server Actions in Next.js?",question:"What are Server Actions in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Actions are async functions that run on the server and can be invoked directly from Client Components (via form actions or event handlers) without writing a manual API route. They are defined with \`'use server'\` and can mutate data, call databases, and revalidate caches. They use the same RPC mechanism as form submissions, making mutations simple and secure by default.


\`\`\`jsx
// app/actions.js
'use server';
export async function createTodo(formData) {
  const title = formData.get('title');
  await db.todo.create({ data: { title } });
  revalidatePath('/todos');
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:463,sortKey:1082},{id:"react-1083",number:464,title:"How do you call a Server Action from a form?",question:"How do you call a Server Action from a form?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Pass the action to a form's `action` prop. The framework serializes the FormData and calls the server function, then can revalidate and redirect. For progressive enhancement, this works even without JavaScript. Use `useFormState`/`useActionState` to capture return values and pending state.\n\n\n```jsx\n'use client';\nimport { useFormStatus } from 'react-dom';\nfunction Submit() {\n  const { pending } = useFormStatus();\n  return <button disabled={pending}>{pending ? 'Saving...' : 'Save'}</button>;\n}\n// <form action={createTodo}><input name='title'/><Submit/></form>\n```\n```\n",tech:"react",displayNumber:464,sortKey:1083},{id:"react-1084",number:465,title:"What is `'use server'` vs `'use client'`?",question:"What is `'use server'` vs `'use client'`?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`'use client'` marks a module as a Client Component boundary (runs/hydrates in browser). `'use server'` marks a function (or module of functions) as a Server Action callable from the client; these functions must be async, serializable in/out, and cannot return non-serializable values. Both are directives that define execution environment, but they serve opposite directions.",tech:"react",displayNumber:465,sortKey:1084},{id:"react-1085",number:466,title:"How do you revalidate cached data after a Server Action?",question:"How do you revalidate cached data after a Server Action?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use `revalidatePath(path)` to purge the cache for a specific route, or `revalidateTag(tag)` to invalidate data fetched with that tag (via `fetch(url, { next: { tags } })`). After mutation, revalidating ensures the next render reflects fresh data without a full deploy. You can also `redirect()` after success.\n\n\n```jsx\n'use server';\nimport { revalidateTag, revalidatePath } from 'next/cache';\nexport async function likePost(id) {\n  await db.like.create({ data: { postId: id } });\n  revalidateTag(`post-${id}`);\n  revalidatePath('/feed');\n}\n```\n```\n",tech:"react",displayNumber:466,sortKey:1085},{id:"react-1086",number:467,title:"Can Server Actions accept non-form data arguments?",question:"Can Server Actions accept non-form data arguments?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Yes. When invoked programmatically (e.g., `onClick={() => action(id)}`), Server Actions can take any serializable arguments (strings, numbers, plain objects, arrays). When used as a form `action`, the argument is `FormData`. Avoid passing functions, class instances, or unsanitized client input.\n\n\n```jsx\n'use server';\nexport async function deleteItem(id: string) {\n  await db.item.delete({ where: { id } });\n  revalidatePath('/items');\n}\n// Client: <button onClick={() => deleteItem(item.id)}>x</button>\n```\n```\n",tech:"react",displayNumber:467,sortKey:1086},{id:"react-1087",number:468,title:"Explain the difference between Server Actions and Route Handlers.",question:"Explain the difference between Server Actions and Route Handlers.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Route Handlers are explicit HTTP endpoints (useful for external clients, webhooks, non-Next consumers). Server Actions are RPC-style functions tightly coupled to the React tree, enabling progressive-enhancement forms and automatic serialization. Use Route Handlers when you need a public API; use Server Actions for app-internal mutations from forms/UI.\n\n\n```jsx\nNeed a public REST/JSON endpoint?  -> Route Handler (app/api/...)\nNeed to mutate from a form in the tree? -> Server Action ('use server')\n```\n```\n",tech:"react",displayNumber:468,sortKey:1087},{id:"react-1088",number:469,title:"What is Client-Side Rendering (CSR)?",question:"What is Client-Side Rendering (CSR)?",difficulty:"Basic",category:"Advanced Patterns & Next.js",answer:"CSR renders the app in the browser: the server sends a minimal HTML shell plus a JS bundle, and React builds the DOM on the client. It's simple and great for highly interactive dashboards behind auth, but has slower first paint and weaker SEO since content isn't in the initial HTML.\n\n\n```jsx\n// A pure client app (e.g. CRA): browser downloads JS, then renders\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n```\n```\n",tech:"react",displayNumber:469,sortKey:1088},{id:"react-1089",number:470,title:"What is Server-Side Rendering (SSR) and how is it done in Next.js?",question:"What is Server-Side Rendering (SSR) and how is it done in Next.js?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`SSR renders each request on the server into full HTML, sent to the client, then hydrated. In the App Router, any dynamic Server Component is effectively SSR'd per request. In the Pages Router you use \`getServerSideProps\`. Benefits: fast first paint and good SEO. Cost: higher server CPU per request.


\`\`\`jsx
// App Router: dynamic, runs per request
export const dynamic = 'force-dynamic';
export default async function Page() {
  const data = await fetchData(); // executed on each request
  return <List data={data} />;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:470,sortKey:1089},{id:"react-1090",number:471,title:"What is Static Site Generation (SSG)?",question:"What is Static Site Generation (SSG)?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"SSG pre-renders pages at build time into static HTML, served from CDN. It's the fastest and most scalable option, ideal for content that doesn't change per user/request (docs, blogs, marketing). In the App Router, components without dynamic data are static by default; in Pages Router use `getStaticProps`.\n\n\n```jsx\n// App Router: static by default (no dynamic functions used)\nexport default async function Page() {\n  const posts = await getPosts(); // fetched once at build\n  return <Feed posts={posts} />;\n}\n```\n```\n",tech:"react",displayNumber:471,sortKey:1090},{id:"react-1091",number:472,title:"What is Incremental Static Regeneration (ISR)?",question:"What is Incremental Static Regeneration (ISR)?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"ISR lets you keep SSG's speed while updating static content after deploy, without rebuilding the whole site. You set a `revalidate` interval; after it elapses, the next request regenerates the page in the background. In App Router, use `export const revalidate = 60` or `fetch(..., { next: { revalidate: 60 } })`.\n\n\n```jsx\n// App Router ISR\nexport const revalidate = 60; // seconds\nexport default async function Page() {\n  const data = await fetch('https://api.example.com/posts', {\n    next: { revalidate: 60 },\n  }).then((r) => r.json());\n  return <Feed data={data} />;\n}\n```\n```\n",tech:"react",displayNumber:472,sortKey:1091},{id:"react-1092",number:473,title:"What is Streaming SSR and how does Suspense enable it?",question:"What is Streaming SSR and how does Suspense enable it?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Streaming SSR sends HTML in chunks as data becomes ready, instead of blocking the whole page. Wrap slow parts in \`<Suspense>\` (or \`loading.js\`); the shell streams first, then the suspended content fills in. This improves Time to First Byte and perceived performance. It's a core feature of RSC and the App Router.


\`\`\`jsx
import { Suspense } from 'react';
export default function Page() {
  return (
    <div>
      <Header />
      <Suspense fallback={<CommentsSkeleton />}>
        <Comments /> {/* async, streams in later */}
      </Suspense>
    </div>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:473,sortKey:1092},{id:"react-1093",number:474,title:"How do you choose between CSR, SSR, SSG, and ISR?",question:"How do you choose between CSR, SSR, SSG, and ISR?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Pick by data freshness and audience. SSG for static, public, SEO-critical content at scale. ISR for content that changes occasionally. SSR for per-request, personalized, or frequently changing data. CSR for private, highly interactive, behind-login apps where SEO is irrelevant. Often a single app mixes them per route.


\`\`\`jsx
Docs/marketing  -> SSG (or ISR)
User dashboard  -> CSR or SSR (per-user)
News feed       -> SSR or ISR
Admin tool      -> CSR
\`\`\`
\`\`\`
`,tech:"react",displayNumber:474,sortKey:1093},{id:"react-1094",number:475,title:"What is Partial Prerendering (PPR) in Next.js?",question:"What is Partial Prerendering (PPR) in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`PPR (experimental) combines static and dynamic rendering in one request: the static shell is prerendered at build time while dynamic holes are streamed in at request time, using Suspense boundaries to mark what's dynamic. It gives SSG speed with SSR flexibility. Enable via \`experimental.ppr = true\` (canary/experimental).


\`\`\`jsx
// app/page.js with Suspense marking dynamic content
export default function Page() {
  return (
    <>
      <StaticHeader />
      <Suspense fallback={<CartSkeleton />}>
        <DynamicCart /> {/* dynamic, streamed at request time */}
      </Suspense>
    </>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:475,sortKey:1094},{id:"react-1095",number:476,title:"What is the difference between `<Suspense>` and `loading.js`?",question:"What is the difference between `<Suspense>` and `loading.js`?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`<Suspense>` is a React primitive you place manually around any async subtree for streaming. `loading.js` is a Next.js convenience that automatically wraps a route segment's page in a Suspense boundary with its own fallback. Use `<Suspense>` for fine-grained control within a page; use `loading.js` for segment-level loading states.\n\n\n```jsx\n// loading.js => automatic <Suspense fallback={<Loading/>}> around page\n// Manual control inside a page:\n<Suspense fallback={<ChartSkeleton/>}><ExpensiveChart/></Suspense>\n```\n```\n",tech:"react",displayNumber:476,sortKey:1095},{id:"react-1096",number:477,title:"What are Micro-frontends and why use them?",question:"What are Micro-frontends and why use them?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Micro-frontends split a frontend into independently developed, deployed, and owned pieces (by team/domain), composed into one app at runtime or build time. Benefits: team autonomy, independent deploys, tech heterogeneity. Costs: complexity, duplicated dependencies, runtime coordination, and consistency challenges. Use only at organizational scale where the benefits outweigh overhead.\n\n\n```jsx\nTeam A -> Cart app (React 18)\nTeam B -> Search app (React 19)\nShell  -> composes both via Module Federation\n```\n```\n",tech:"react",displayNumber:477,sortKey:1096},{id:"react-1097",number:478,title:"What is Module Federation and how does it enable Micro-frontends?",question:"What is Module Federation and how does it enable Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Module Federation (Webpack 5 / Rspack / Turbopack support evolving) lets multiple builds expose and consume modules at runtime. A "host" app loads "remote" components over the network without a build-time dependency. This is the most common Micro-frontend mechanism, enabling separate deploy cycles and shared, lazily-loaded code.


\`\`\`jsx
// webpack.config.js (host)
new ModuleFederationPlugin({
  name: 'host',
  remotes: { cart: 'cart@https://cart.app/mf-manifest.json' },
});
// Usage in React: const Cart = React.lazy(() => import('cart/Cart'));
\`\`\`
\`\`\`
`,tech:"react",displayNumber:478,sortKey:1097},{id:"react-1098",number:479,title:"How do you load a remote Micro-frontend component in React?",question:"How do you load a remote Micro-frontend component in React?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Lazily import the remote module and render it inside \`<Suspense>\`. The remote must be reachable and expose the component via its Module Federation config. Wrap with error handling so a remote failure degrades gracefully rather than crashing the whole shell.


\`\`\`jsx
const RemoteCart = React.lazy(() => import('cart/Cart'));
function Shell() {
  return (
    <ErrorBoundary fallback={<p>Cart unavailable</p>}>
      <Suspense fallback={<Spinner />}>
        <RemoteCart />
      </Suspense>
    </ErrorBoundary>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:479,sortKey:1098},{id:"react-1099",number:480,title:"What are the challenges of sharing state and dependencies in Micro-frontends?",question:"What are the challenges of sharing state and dependencies in Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Key challenges: duplicated libraries (multiple React copies → broken context/hooks), version skew, shared auth/session, global CSS conflicts, and consistent design systems. Mitigate by singletons (`shared` config), runtime shared libraries, event-bus or URL-based cross-app communication, and a federated design system. Avoid sharing mutable global state directly.\n\n\n```jsx\n// Prevent duplicate React with singletons\nshared: { react: { singleton: true, requiredVersion: '^18.0.0' } }\n```\n```\n",tech:"react",displayNumber:480,sortKey:1099},{id:"react-1100",number:481,title:"How do you implement a shared design system across Micro-frontends?",question:"How do you implement a shared design system across Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Publish the design system as a standalone package (npm, internal registry) and mark it as a \`shared\` singleton in Module Federation so all remotes use the exact same instance. Alternatively, serve it from a CDN with import maps. This guarantees a single theme, components, and tokens across all apps without bundling duplicates.


\`\`\`jsx
// webpack.config.js (each app)
new ModuleFederationPlugin({
  shared: {
    '@company/design-system': { singleton: true, eager: true },
    react: { singleton: true },
    'react-dom': { singleton: true },
  },
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:481,sortKey:1100},{id:"react-1101",number:482,title:"What are the tradeoffs of Runtime vs Build-time Micro-frontend integration?",question:"What are the tradeoffs of Runtime vs Build-time Micro-frontend integration?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Runtime (Module Federation): independent deploys, hot swaps, but runtime complexity, network latency, and duplicate risk. Build-time (monorepo, Nx, Turborepo): single build, zero duplication, atomic deploys, but all teams blocked by one pipeline and must coordinate versions. Most orgs start with build-time and adopt runtime only when deploy independence becomes a real bottleneck.\n\n\n```jsx\nBuild-time: Nx, Turborepo, Yarn workspaces - simpler, fewer footguns\nRuntime: Module Federation - true team autonomy, more complex ops\n```\n```\n",tech:"react",displayNumber:482,sortKey:1101},{id:"react-1102",number:483,title:"How do you handle authentication and session sharing in Micro-frontends?",question:"How do you handle authentication and session sharing in Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Keep auth state in a shared, trusted layer (HTTP-only cookies, a tiny auth MFE, or a JWT in localStorage). Don't share the whole auth object across remotes; instead, each remote validates the token or calls a `/me` endpoint. The shell app holds the session cookie and passes auth headers to backend calls. Avoid syncing auth state via events between remotes.\n\n\n```jsx\n// Shell sets auth cookie on login; each remote reads it or calls auth-service\n// Don't: RemoteA.onLogin(() => RemoteB.updateUser(...))\n// Do:    Cookie set by shell -> all requests include it automatically\n```\n```\n",tech:"react",displayNumber:483,sortKey:1102},{id:"react-1103",number:484,title:"What is the `next/dynamic` import and when to use it?",question:"What is the `next/dynamic` import and when to use it?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/dynamic` is Next's wrapper around `React.lazy` + `Suspense` with SSR control. Use it to code-split heavy components (charts, editors) so their JS loads only when needed. The `ssr: false` option forces client-only rendering, useful for components that use `window` or are incompatible with server rendering.\n\n\n```jsx\nimport dynamic from 'next/dynamic';\nconst HeavyChart = dynamic(() => import('@/components/HeavyChart'), {\n  loading: () => <Skeleton />,\n  ssr: false, // skip SSR, client-only\n});\n```\n```\n",tech:"react",displayNumber:484,sortKey:1103},{id:"react-1104",number:485,title:"How do you use `React.Suspense` with `next/dynamic` for code-splitting?",question:"How do you use `React.Suspense` with `next/dynamic` for code-splitting?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Wrap the dynamic import in \`<Suspense>\` to show a fallback while the chunk loads. The \`loading\` option in \`next/dynamic\` is a shorthand for this. The component only mounts after the chunk is downloaded and parsed, keeping initial bundle small.


\`\`\`jsx
import dynamic from 'next/dynamic';
import { Suspense } from 'react';
const Editor = dynamic(() => import('./Editor'));
export default function Page() {
  return (
    <Suspense fallback={<p>Loading editor...</p>}>
      <Editor />
    </Suspense>
  );
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:485,sortKey:1104},{id:"react-1105",number:486,title:"What is the `use` hook (React 19) and how does it relate to Suspense?",question:"What is the `use` hook (React 19) and how does it relate to Suspense?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`\`use(promise)\` reads the resolved value of a promise, suspending if it's not ready. It works in Server Components (replaces \`await\` with readable composition) and Client Components (lets you kick off fetch early and read later). It integrates with Suspense boundaries and enables streaming with less boilerplate than \`await\` in async components.


\`\`\`jsx
import { use } from 'react';
// Server Component - use reads a promise, suspends if pending
function Post({ promise }) {
  const post = use(promise);
  return <article>{post.title}</article>;
}
// Client Component - fetch starts early, read suspends
const promise = fetch('/api/post/1').then((r) => r.json());
function ClientPost() {
  const post = use(promise);
  return <h1>{post.title}</h1>;
}
\`\`\`
\`\`\`
`,tech:"react",displayNumber:486,sortKey:1105},{id:"react-1106",number:487,title:"How do you optimize bundle size in a Next.js App Router application?",question:"How do you optimize bundle size in a Next.js App Router application?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use RSC by default (zero client JS), split heavy UI with `dynamic` + `ssr: false`, import only needed lodash/date-fns parts, enable `optimizePackageImports` in `next.config.js`, audit with `@next/bundle-analyzer`, prefer native browser APIs, and keep Client Components small and leaf-only. Tree-shaking and granular imports are your biggest levers.\n\n\n```jsx\n// next.config.js\nmodule.exports = {\n  experimental: {\n    optimizePackageImports: ['lodash', 'date-fns', 'react-icons'],\n  },\n};\n```\n```\n",tech:"react",displayNumber:487,sortKey:1106},{id:"react-1107",number:488,title:"What is `optimizePackageImports` and how does it work?",question:"What is `optimizePackageImports` and how does it work?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`optimizePackageImports` in `next.config.js` tells Next's compiler (Turbopack/SWC) to transform barrel-file imports like `import { A, B } from 'lib'` into direct imports (`import A from 'lib/A'`) for specified packages. This improves tree-shaking and reduces bundle size without changing source code. It works for popular icon/utility libraries out of the box.\n\n\n```jsx\n// next.config.js\nmodule.exports = {\n  experimental: {\n    optimizePackageImports: ['@mui/icons-material', 'lucide-react', 'date-fns'],\n  },\n};\n// import { Add, Delete } from '@mui/icons/material'\n// -> auto-rewritten to direct imports\n```\n```\n",tech:"react",displayNumber:488,sortKey:1107},{id:"react-1108",number:489,title:"How do you measure and improve Core Web Vitals in Next.js?",question:"How do you measure and improve Core Web Vitals in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use Vercel Speed Insights, web-vitals library, or Chrome DevTools. Key levers: RSC/SSR for LCP (fast HTML), `next/image` for CLS/LCP, font optimization (`next/font`), code-splitting for INP, streaming with Suspense, ISR for cache hits, and reducing third-party scripts. Set budgets in CI to catch regressions.\n\n\n```jsx\nimport Image from 'next/image';\nimport { Inter } from 'next/font/google';\nconst font = Inter({ subsets: ['latin'], display: 'swap' });\n// Use <Image priority /> for above-fold LCP\n```\n```\n",tech:"react",displayNumber:489,sortKey:1108},{id:"react-1109",number:490,title:"What is the `next/font` optimization and why use it?",question:"What is the `next/font` optimization and why use it?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/font` self-hosts Google/Adobe fonts at build time, strips unused glyphs, inlines CSS, and prevents layout shift (`display: swap`). No external font requests = faster LCP, zero CLS from fonts, and privacy compliance (no Google Fonts requests). Works in RSC and Client Components.\n\n\n```jsx\nimport { Roboto } from 'next/font/google';\nconst roboto = Roboto({ subsets: ['latin'], display: 'swap', variable: '--font-roboto' });\nexport default function Layout({ children }) {\n  return <html className={roboto.variable}>{children}</html>;\n}\n```\n```\n",tech:"react",displayNumber:490,sortKey:1109},{id:"react-1110",number:491,title:"How do you use `next/image` for optimal image loading?",question:"How do you use `next/image` for optimal image loading?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/image` automatically optimizes, resizes, and serves modern formats (AVIF/WebP) on demand. It prevents CLS with explicit dimensions, lazy-loads below-fold, and supports `priority` for above-fold images. Configure `remotePatterns` for external sources. It replaces manual `img` tags entirely.\n\n\n```jsx\nimport Image from 'next/image';\n<Image src='/hero.jpg' alt='Hero' width={1200} height={600} priority />\n// For remote: next.config.js -> images: { remotePatterns: [{ hostname: 'cdn.example.com' }] }\n```\n```\n",tech:"react",displayNumber:491,sortKey:1110},{id:"react-1111",number:492,title:"What is the `Middleware` in Next.js and how does it differ from Server Components?",question:"What is the `Middleware` in Next.js and how does it differ from Server Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Middleware runs before the request reaches the route, at the Edge. It can rewrite, redirect, modify headers, or respond directly. It's not a React component; it has no access to React context or Server Component data. Use it for auth checks, i18n routing, bot protection, A/B testing, or geolocation — anything that must run before rendering.


\`\`\`jsx
// middleware.ts
import { NextResponse } from 'next/server';
export function middleware(req) {
  const { pathname } = req.nextUrl;
  if (pathname.startsWith('/admin') && !req.cookies.get('admin')) {
    return NextResponse.redirect(new URL('/login', req.url));
  }
  return NextResponse.next();
}
export const config = { matcher: ['/admin/:path*'] };
\`\`\`
\`\`\`
`,tech:"react",displayNumber:492,sortKey:1111},{id:"react-1112",number:493,title:"How do you implement internationalization (i18n) with the App Router?",question:"How do you implement internationalization (i18n) with the App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use a `[locale]` segment (e.g., `app/[locale]/page.js`) to capture the locale in `params`. Generate static params for known locales. Use a dictionary per locale (JSON/TS modules) or a library like `next-intl`. Middleware can redirect `/` to the default locale. Avoid the old `next.config.js` i18n; the App Router handles it via file structure.\n\n\n```jsx\n// app/[locale]/page.js\nimport { getDict } from '@/lib/dict';\nexport async function generateStaticParams() {\n  return [{ locale: 'en' }, { locale: 'es' }];\n}\nexport default async function Page({ params }) {\n  const dict = await getDict((await params).locale);\n  return <h1>{dict.welcome}</h1>;\n}\n```\n```\n",tech:"react",displayNumber:493,sortKey:1112},{id:"react-1113",number:494,title:"What are Parallel Routes in the App Router?",question:"What are Parallel Routes in the App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Parallel routes (slots) let you render multiple pages simultaneously in the same layout using named folders \`@folder\`. Each slot has its own loading/error state and can be navigated independently (e.g., modal, sidebar, tabs). Define \`default.js\` for unmatched slots. They enable complex UIs without a single page owning all content.


\`\`\`jsx
app/
  layout.js
  @modal/
    default.js    // shown when no modal active
    settings/
      page.js
  @sidebar/
    default.js
    page.js
// layout.js receives { children, modal, sidebar }
\`\`\`
\`\`\`
`,tech:"react",displayNumber:494,sortKey:1113},{id:"react-1114",number:495,title:"What are Intercepting Routes and when would you use them?",question:"What are Intercepting Routes and when would you use them?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Intercepting routes ( `(..)` and `(...)` folders) let you show a different UI for a route while keeping the same URL — e.g., open a photo in a modal from the feed, but `/photo/1` still works as a standalone page on direct load/refresh. Use for modals, drawers, or preview overlays without breaking deep links.\n\n\n```jsx\napp/\n  feed/\n    page.js           // feed\n    (..)photo/[id]/   // intercepts /photo/:id, shows modal\n      page.js\n  photo/\n    [id]/\n      page.js         // standalone photo page\n// Click feed link -> modal; refresh -> full page\n```\n```\n",tech:"react",displayNumber:495,sortKey:1114},{id:"react-1115",number:496,title:"How do you secure Server Actions against CSRF and malicious input?",question:"How do you secure Server Actions against CSRF and malicious input?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Next.js Server Actions include built-in CSRF protection (origin/header checks) and only accept same-origin requests by default. Validate all inputs with Zod/Valibot inside the action. Use `await` on `headers()`/`cookies()` to read auth. Never trust client data; treat actions like public API endpoints. Rate-limit via middleware if needed.\n\n\n```jsx\n'use server';\nimport { z } from 'zod';\nconst schema = z.object({ title: z.string().min(3).max(100) });\nexport async function createPost(formData) {\n  const data = schema.parse(Object.fromEntries(formData));\n  const session = await getSession();\n  if (!session) throw new Error('Unauthorized');\n  return db.post.create({ data: { ...data, authorId: session.userId } });\n}\n```\n```\n",tech:"react",displayNumber:496,sortKey:1115},{id:"react-1116",number:497,title:"What are the performance implications of using many Client Components?",question:"What are the performance implications of using many Client Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Each `'use client'` boundary adds its component subtree to the client bundle. Deeply nested client trees ship more JS, increase hydration cost, and reduce streaming benefits. Keep Client Components as leaf nodes (buttons, forms, small widgets) and push data fetching up to Server Components. Audit with bundle analyzer; consolidate and lift client boundaries where possible.\n\n\n```jsx\nBad: <ClientLayout><ClientSidebar><ClientFeed><ClientCard/></ClientFeed></ClientSidebar></ClientLayout>\n// All client, huge bundle\nGood: <ServerLayout><ServerSidebar/><ServerFeed><ClientCard/></ServerFeed></ServerLayout>\n// Only Card is client\n```\n```\n",tech:"react",displayNumber:497,sortKey:1116},{id:"react-1117",number:498,title:"How do you test Server Components and Server Actions?",question:"How do you test Server Components and Server Actions?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Components: test via Next.js testing utilities (render to HTML string, assert output) or integration tests with Playwright/Cypress against a running dev server. Server Actions: test the action logic directly (it's just an async function) by calling it with mock data and asserting DB calls, revalidations, and return values. Mock \`next/cache\` and DB.


\`\`\`jsx
// Test Server Action directly
import { createPost } from '@/actions';
jest.mock('@/lib/db', () => ({ post: { create: jest.fn() } }));
test('creates post', async () => {
  const fd = new FormData(); fd.set('title', 'Hello');
  await createPost(fd);
  expect(db.post.create).toHaveBeenCalledWith(expect.objectContaining({ title: 'Hello' }));
});
\`\`\`
\`\`\`
`,tech:"react",displayNumber:498,sortKey:1117},{id:"react-1118",number:499,title:"What is the difference between `revalidatePath` and `revalidateTag`?",question:"What is the difference between `revalidatePath` and `revalidateTag`?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`revalidatePath(path)` invalidates cached output for a specific route (page/layout). `revalidateTag(tag)` invalidates all `fetch` calls that were tagged with that tag, regardless of which route uses them. Tags enable granular, cross-route invalidation (e.g., invalidate all \"product-123\" data across the app). Paths are simpler for page-level refresh.\n\n\n```jsx\n// Tag-based (fine-grained, cross-route)\nfetch(url, { next: { tags: ['post-1'] } });\nrevalidateTag('post-1'); // invalidates everywhere that tag is used\n// Path-based (page-level)\nrevalidatePath('/posts/1'); // invalidates only that route's cache\n```\n```\n",tech:"react",displayNumber:499,sortKey:1118},{id:"react-1119",number:500,title:"What are the key considerations when migrating from Pages Router to App Router?",question:"What are the key considerations when migrating from Pages Router to App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Migrate incrementally: both routers can coexist. Convert leaf pages first. Replace `getStaticProps`/`getServerSideProps` with async RSC or `fetch` with cache options. Move data fetching into components. Replace `_app.js`/_`document.js` with `app/layout.js`. Convert API routes to Route Handlers. Audit `use client` boundaries; many Pages Router components can become Server Components. Test each route for hydration errors, dynamic usage, and streaming behavior.\n\n\n```jsx\nPages -> App migration checklist:\n1. Add app/ dir alongside pages/\n2. Migrate leaf pages to async Server Components\n3. Replace getStaticProps with fetch({ next: { revalidate } })\n4. Replace getServerSideProps with dynamic = 'force-dynamic'\n5. Convert _app/_document to root layout.js\n6. Move API routes to app/api/.../route.js\n7. Audit and minimize 'use client' usage\n8. Enable static optimization where possible\n```\n```\n",tech:"react",displayNumber:500,sortKey:1119},{id:"node-1120",number:1,title:"What is Node.js?",question:"What is Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Node.js is a JavaScript runtime built on Chrome's V8 engine that runs JS outside the browser. It uses a single-threaded, event-driven, non-blocking I/O model, making it efficient for I/O-heavy and real-time applications.\n\n\n```js\nconsole.log('Hello from Node ' + process.version)\n```\n```\n",tech:"node",displayNumber:1,sortKey:1120},{id:"node-1121",number:2,title:"Is Node.js single-threaded or multi-threaded?",question:"Is Node.js single-threaded or multi-threaded?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Your JavaScript runs on a single main thread (the event loop), but Node offloads blocking work (file I/O, DNS, crypto) to a `libuv` thread pool (default 4 threads). So it is single-threaded for JS execution, multi-threaded under the hood.\n\n\n```js\n// UV_THREADPOOL_SIZE controls the pool size\nprocess.env.UV_THREADPOOL_SIZE = 8\n```\n```\n",tech:"node",displayNumber:2,sortKey:1121},{id:"node-1122",number:3,title:"What is V8?",question:"What is V8?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'V8 is Google\'s open-source JavaScript engine written in C++. It compiles JS directly to machine code (JIT), and Node embeds it to execute JavaScript. V8 also manages memory and garbage collection.\n\n\n```js\nconsole.log(process.versions.v8) // e.g. "12.4.254.21-node.20"\n```\n```\n',tech:"node",displayNumber:3,sortKey:1122},{id:"node-1123",number:4,title:"What is libuv?",question:"What is libuv?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`libuv` is a C library that gives Node its asynchronous, event-driven capabilities. It provides the event loop, the thread pool, and cross-platform async I/O (file system, TCP/UDP, timers, child processes).\n\n\n```js\n// crypto.pbkdf2 runs on the libuv thread pool, not the main thread\nrequire('crypto').pbkdf2('pw', 'salt', 100000, 64, 'sha512', () => {})\n```\n```\n",tech:"node",displayNumber:4,sortKey:1123},{id:"node-1124",number:5,title:"What is npm?",question:"What is npm?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'npm (Node Package Manager) is the default package manager for Node. It installs dependencies from the npm registry, manages versions via `package.json`, and runs scripts.\n\n\n```js\n// package.json\n{ "scripts": { "start": "node index.js" }, "dependencies": { "express": "^4.19.0" } }\n```\n```\n',tech:"node",displayNumber:5,sortKey:1124},{id:"node-1125",number:6,title:"What is the difference between `node index.js` and running in the REPL?",question:"What is the difference between `node index.js` and running in the REPL?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`node index.js` executes a file. Running `node` with no argument starts the REPL (Read-Eval-Print-Loop), an interactive shell for evaluating expressions line by line — useful for quick experiments.\n\n\n```js\n$ node\n> 2 + 2\n4\n> .exit\n```\n```\n",tech:"node",displayNumber:6,sortKey:1125},{id:"node-1126",number:7,title:"What are globals in Node.js?",question:"What are globals in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Globals are objects available everywhere without `require`: `global`, `process`, `console`, `Buffer`, `setTimeout`, `__dirname`, `__filename` (in CommonJS). Unlike the browser, there is no `window`; the global object is `global` (or `globalThis`).\n\n\n```js\nconsole.log(__dirname)  // directory of current module\nconsole.log(globalThis === global) // true\n```\n```\n",tech:"node",displayNumber:7,sortKey:1126},{id:"node-1127",number:8,title:"What is `process` in Node.js?",question:"What is `process` in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process` is a global providing information and control over the current Node process: `process.argv`, `process.env`, `process.exit()`, `process.cwd()`, and events like `exit` and `uncaughtException`.\n\n\n```js\nconsole.log(process.argv)   // [node, script, ...args]\nconsole.log(process.env.NODE_ENV)\n```\n```\n",tech:"node",displayNumber:8,sortKey:1127},{id:"node-1128",number:9,title:"What is `process.argv`?",question:"What is `process.argv`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process.argv` is an array of command-line arguments. Index 0 is the Node executable, index 1 is the script path, and the rest are user arguments.\n\n\n```js\n// node app.js --name Alice\nconst args = process.argv.slice(2) // ['--name', 'Alice']\n```\n```\n",tech:"node",displayNumber:9,sortKey:1128},{id:"node-1129",number:10,title:"What is `process.env`?",question:"What is `process.env`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process.env` holds environment variables as key-value strings. It is used for configuration like ports, API keys, and `NODE_ENV`, keeping secrets out of source code.\n\n\n```js\nconst port = process.env.PORT || 3000\n```\n```\n",tech:"node",displayNumber:10,sortKey:1129},{id:"node-1130",number:11,title:"What is the difference between `__dirname` and `process.cwd()`?",question:"What is the difference between `__dirname` and `process.cwd()`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`__dirname` is the directory of the currently executing file. `process.cwd()` is the directory from which the Node process was launched. They differ when you run a script from another folder.\n\n\n```js\n// running: cd /home && node /app/index.js\nconsole.log(__dirname)      // /app\nconsole.log(process.cwd())  // /home\n```\n```\n",tech:"node",displayNumber:11,sortKey:1130},{id:"node-1131",number:12,title:"What is `NODE_ENV` used for?",question:"What is `NODE_ENV` used for?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`NODE_ENV` is a convention (not built into Node) for signaling the environment: `development`, `production`, or `test`. Frameworks like Express enable optimizations and disable verbose errors when it is `production`.\n\n\n```js\nif (process.env.NODE_ENV === 'production') app.use(compression())\n```\n```\n",tech:"node",displayNumber:12,sortKey:1131},{id:"node-1132",number:13,title:"How do you exit a Node process?",question:"How do you exit a Node process?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Call `process.exit(code)` — `0` means success, non-zero means failure. Avoid it in servers because it terminates immediately without flushing async work; prefer letting the event loop drain or setting `process.exitCode`.\n\n\n```js\nprocess.exitCode = 1 // graceful: exits when loop empties\n```\n```\n",tech:"node",displayNumber:13,sortKey:1132},{id:"node-1133",number:14,title:"What is the difference between Node.js and the browser JavaScript environment?",question:"What is the difference between Node.js and the browser JavaScript environment?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Browsers provide `window`, `document`, and DOM APIs; Node provides `global`, `process`, file system, and network modules. Node has no DOM; browsers cannot access the file system directly. Both use V8 (in Chrome/Node).\n\n\n```js\n// Node: fs available, document undefined\nconst fs = require('fs')\n```\n```\n",tech:"node",displayNumber:14,sortKey:1133},{id:"node-1134",number:15,title:"What are the different types of Node.js applications?",question:"What are the different types of Node.js applications?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Common types: REST/GraphQL APIs, real-time apps (chat, WebSockets), CLI tools, microservices, streaming servers, and build tooling. Node excels at I/O-bound and real-time workloads, less so at CPU-bound tasks.\n\n\n```js\n#!/usr/bin/env node\nconsole.log('A CLI tool')\n```\n```\n",tech:"node",displayNumber:15,sortKey:1134},{id:"node-1135",number:16,title:"What is a callback in Node.js?",question:"What is a callback in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:`A callback is a function passed as an argument to be invoked later, typically when an async operation completes. Node uses the "error-first" callback convention: \`(err, data) => {}\`.


\`\`\`js
require('fs').readFile('a.txt', (err, data) => {
  if (err) return console.error(err)
  console.log(data.toString())
})
\`\`\`
\`\`\`
`,tech:"node",displayNumber:16,sortKey:1135},{id:"node-1136",number:17,title:"What is the error-first callback pattern?",question:"What is the error-first callback pattern?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"By convention, the first argument to a Node callback is an error (or `null` if none), and subsequent arguments carry results. This makes error handling consistent across the ecosystem.\n\n\n```js\nfunction done(err, result) {\n  if (err) throw err\n  console.log(result)\n}\n```\n```\n",tech:"node",displayNumber:17,sortKey:1136},{id:"node-1137",number:18,title:"What is callback hell and how do you avoid it?",question:"What is callback hell and how do you avoid it?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Callback hell is deeply nested callbacks that become hard to read and maintain ("pyramid of doom"). Avoid it with named functions, Promises, or `async/await`.\n\n\n```js\n// Instead of nesting, chain promises\nreadFile(a).then(process).then(save).catch(handle)\n```\n```\n',tech:"node",displayNumber:18,sortKey:1137},{id:"node-1138",number:19,title:"What is the difference between synchronous and asynchronous functions in Node?",question:"What is the difference between synchronous and asynchronous functions in Node?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Synchronous functions block the event loop until they finish (e.g., `fs.readFileSync`). Asynchronous functions return immediately and deliver results via callback/Promise, keeping the loop free. Prefer async in servers.\n\n\n```js\nconst data = fs.readFileSync('a.txt')      // blocks\nfs.readFile('a.txt', (e, d) => {})          // non-blocking\n```\n```\n",tech:"node",displayNumber:19,sortKey:1138},{id:"node-1139",number:20,title:"What is `Buffer` in Node.js?",question:"What is `Buffer` in Node.js?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"A `Buffer` is a fixed-length chunk of raw binary data outside the V8 heap. It is used to handle bytes from files, network streams, and encoding conversions before JS strings existed with binary support.\n\n\n```js\nconst buf = Buffer.from('hi', 'utf8')\nconsole.log(buf)             // <Buffer 68 69>\nconsole.log(buf.toString())  // 'hi'\n```\n```\n",tech:"node",displayNumber:20,sortKey:1139},{id:"node-1140",number:21,title:"What is the global object in Node vs the browser?",question:"What is the global object in Node vs the browser?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"In the browser it is `window`; in Node it is `global`. `globalThis` is the standard cross-environment reference to whichever applies. Variables declared with `var` at module scope do NOT attach to `global` in Node (modules are wrapped).\n\n\n```js\nglobal.myConfig = { debug: true }\nconsole.log(globalThis.myConfig.debug) // true\n```\n```\n",tech:"node",displayNumber:21,sortKey:1140},{id:"node-1141",number:22,title:"Why is Node.js good for I/O-bound tasks but not CPU-bound tasks?",question:"Why is Node.js good for I/O-bound tasks but not CPU-bound tasks?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"I/O is offloaded to libuv/OS, so the single JS thread stays free to handle many concurrent requests. CPU-bound work (heavy computation) blocks that one thread, stalling all requests. Offload CPU work to worker threads or a separate service.\n\n\n```js\n// bad: blocks everyone\nfor (let i = 0; i < 1e10; i++) {}\n```\n```\n",tech:"node",displayNumber:22,sortKey:1141},{id:"node-1142",number:23,title:"What is the `console` module?",question:"What is the `console` module?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`console` provides logging: `log`, `error`, `warn`, `info`, `table`, `time`/`timeEnd`, and `dir`. `console.log` writes to stdout, `console.error` to stderr, which lets you separate logs from errors when piping.\n\n\n```js\nconsole.time('t'); doWork(); console.timeEnd('t') // t: 12ms\n```\n```\n",tech:"node",displayNumber:23,sortKey:1142},{id:"node-1143",number:24,title:"What is the difference between `stdout` and `stderr`?",question:"What is the difference between `stdout` and `stderr`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`process.stdout` is the standard output stream (normal program output); `process.stderr` is for errors and diagnostics. Separating them lets you redirect logs and errors independently (`node app 1>out.log 2>err.log`).\n\n\n```js\nprocess.stdout.write('result\\n')\nprocess.stderr.write('warning\\n')\n```\n```\n",tech:"node",displayNumber:24,sortKey:1143},{id:"node-1144",number:25,title:"How do you read user input from the command line?",question:"How do you read user input from the command line?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Use the built-in `readline` module (or `process.stdin`) to read lines interactively from stdin.\n\n\n```js\nconst rl = require('readline').createInterface({ input: process.stdin, output: process.stdout })\nrl.question('Name? ', name => { console.log('Hi ' + name); rl.close() })\n```\n```\n",tech:"node",displayNumber:25,sortKey:1144},{id:"node-1145",number:26,title:"What is the module wrapper function in Node?",question:"What is the module wrapper function in Node?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Before execution, Node wraps every CommonJS module in a function `(exports, require, module, __filename, __dirname) => {}`. This gives each module its own scope and injects those five variables, which is why they are available without importing.\n\n\n```js\n(function (exports, require, module, __filename, __dirname) {\n  // your module code runs here\n})\n```\n```\n",tech:"node",displayNumber:26,sortKey:1145},{id:"node-1146",number:27,title:"What is `globalThis`?",question:"What is `globalThis`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`globalThis` is a standardized way (ES2020) to access the global object regardless of environment (browser `window`, Node `global`, workers `self`). It improves portability of shared code.\n\n\n```js\nglobalThis.crypto ??= require('crypto').webcrypto\n```\n```\n",tech:"node",displayNumber:27,sortKey:1146},{id:"node-1147",number:28,title:"How does Node.js handle concurrency without multiple threads for JS?",question:"How does Node.js handle concurrency without multiple threads for JS?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Node uses an event loop plus non-blocking I/O. Instead of one thread per request, a single thread registers callbacks and processes completed I/O events as they arrive, achieving high concurrency with low overhead.\n\n\n```js\n// thousands of concurrent connections, one JS thread\nrequire('http').createServer((req, res) => res.end('ok')).listen(3000)\n```\n```\n",tech:"node",displayNumber:28,sortKey:1147},{id:"node-1148",number:29,title:"What is the difference between `setTimeout(fn, 0)` and `setImmediate(fn)`?",question:"What is the difference between `setTimeout(fn, 0)` and `setImmediate(fn)`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`setImmediate` runs after the current poll phase (check phase); `setTimeout(fn, 0)` runs in the timers phase after at least 1ms. Inside an I/O callback, `setImmediate` always fires first; at the top level the order is nondeterministic.\n\n\n```js\nfs.readFile('a', () => {\n  setTimeout(() => console.log('timeout'), 0)\n  setImmediate(() => console.log('immediate')) // logs first\n})\n```\n```\n",tech:"node",displayNumber:29,sortKey:1148},{id:"node-1149",number:30,title:"What is `process.nextTick()`?",question:"What is `process.nextTick()`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`process.nextTick(fn)` queues `fn` to run immediately after the current operation completes, before the event loop continues and before any timers/immediates. Overusing it can starve the loop (I/O never runs).\n\n\n```js\nconsole.log('start')\nprocess.nextTick(() => console.log('tick'))\nconsole.log('end') // start, end, tick\n```\n```\n",tech:"node",displayNumber:30,sortKey:1149},{id:"node-1150",number:31,title:"What are the phases of the Node.js event loop?",question:"What are the phases of the Node.js event loop?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Phases in order: timers → pending callbacks → idle/prepare → poll → check → close callbacks. Between each phase (and each callback) Node drains the `nextTick` and microtask (Promise) queues.\n\n\n```js\n// poll = I/O callbacks; check = setImmediate; timers = setTimeout/Interval\n```\n```\n",tech:"node",displayNumber:31,sortKey:1150},{id:"node-1151",number:32,title:"What is the difference between CommonJS and ES Modules?",question:"What is the difference between CommonJS and ES Modules?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"CommonJS uses `require`/`module.exports`, loads synchronously, and is the historical Node format. ES Modules use `import`/`export`, load asynchronously, support top-level `await`, and are the standard. Node picks the format via `.mjs`/`.cjs` or `\"type\"` in package.json.\n\n\n```js\n// CJS\nconst x = require('./x')\n// ESM\nimport x from './x.js'\n```\n```\n",tech:"node",displayNumber:32,sortKey:1151},{id:"node-1152",number:33,title:"How do you enable ES Modules in Node?",question:"How do you enable ES Modules in Node?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Either use the `.mjs` extension, or set `"type": "module"` in `package.json`. Then use `import`/`export` and include file extensions in relative imports.\n\n\n```js\n// package.json\n{ "type": "module" }\n// index.js\nimport { readFile } from \'node:fs/promises\'\n```\n```\n',tech:"node",displayNumber:33,sortKey:1152},{id:"node-1153",number:34,title:"What is the `node:` protocol in imports?",question:"What is the `node:` protocol in imports?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Prefixing a built-in module with `node:` (e.g. `node:fs`) makes it explicit that it is a core module, avoiding ambiguity with a same-named npm package and slightly speeding resolution.\n\n\n```js\nimport { readFile } from 'node:fs/promises'\nconst os = require('node:os')\n```\n```\n",tech:"node",displayNumber:34,sortKey:1153},{id:"node-1154",number:35,title:"What is the difference between `module.exports` and `exports`?",question:"What is the difference between `module.exports` and `exports`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`exports` is a reference to `module.exports`. Adding properties to `exports` works, but reassigning `exports = ...` breaks the link — only `module.exports` is actually returned by `require`.\n\n\n```js\nexports.a = 1            // works\nmodule.exports = { a: 1 } // works\nexports = { a: 1 }        // does NOT export\n```\n```\n",tech:"node",displayNumber:35,sortKey:1154},{id:"node-1155",number:36,title:"How does `require` resolve a module?",question:"How does `require` resolve a module?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`require` checks: core modules, then relative/absolute paths (with `.js`, `.json`, `.node` extensions and `index` files), then walks up `node_modules` folders. Resolved modules are cached so subsequent calls return the same instance.\n\n\n```js\nrequire('fs')        // core\nrequire('./util')    // relative -> ./util.js or ./util/index.js\nrequire('express')   // node_modules\n```\n```\n",tech:"node",displayNumber:36,sortKey:1155},{id:"node-1156",number:37,title:"What is the module cache?",question:"What is the module cache?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Node caches modules after first load in `require.cache`, keyed by resolved path. Repeated `require` returns the same exported object (a singleton). Deleting the cache entry forces a reload.\n\n\n```js\ndelete require.cache[require.resolve('./config')]\nconst fresh = require('./config')\n```\n```\n",tech:"node",displayNumber:37,sortKey:1156},{id:"node-1157",number:38,title:"What is a circular dependency and how does Node handle it?",question:"What is a circular dependency and how does Node handle it?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"A circular dependency is when module A requires B and B requires A. Node returns a partially-completed `exports` object to break the cycle, so one side may see an incomplete export. Refactor shared code into a third module to avoid subtle bugs.\n\n\n```js\n// a.js\nexports.done = false\nconst b = require('./b') // b sees a.done === false here\nexports.done = true\n```\n```\n",tech:"node",displayNumber:38,sortKey:1157},{id:"node-1158",number:39,title:"What is the difference between `require` and `import`?",question:"What is the difference between `require` and `import`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`require` (CJS) is synchronous, dynamic, and can be called anywhere. `import` (ESM) is static, hoisted, asynchronously resolved, and normally must be top-level (dynamic `import()` returns a Promise). ESM enables tree-shaking.\n\n\n```js\nconst mod = await import('./mod.js') // dynamic ESM\n```\n```\n",tech:"node",displayNumber:39,sortKey:1158},{id:"node-1159",number:40,title:"What are Worker Threads?",question:"What are Worker Threads?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"The `worker_threads` module runs JavaScript in parallel OS threads with their own event loop and V8 instance. They are ideal for CPU-bound work, communicating via message passing or `SharedArrayBuffer`.\n\n\n```js\nconst { Worker } = require('node:worker_threads')\nnew Worker('./heavy.js').on('message', console.log)\n```\n```\n",tech:"node",displayNumber:40,sortKey:1159},{id:"node-1160",number:41,title:"When would you use Worker Threads vs Child Processes vs Cluster?",question:"When would you use Worker Threads vs Child Processes vs Cluster?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Worker Threads: CPU-bound JS with shared memory, low overhead. Child Processes: run separate programs/scripts with isolated memory. Cluster: fork multiple Node processes sharing a server port to use all CPU cores for I/O-bound servers.\n\n\n```js\n// Cluster for scaling an HTTP server across cores\nconst cluster = require('node:cluster')\nif (cluster.isPrimary) cluster.fork()\n```\n```\n",tech:"node",displayNumber:41,sortKey:1160},{id:"node-1161",number:42,title:"What is the difference between blocking and non-blocking code in practice?",question:"What is the difference between blocking and non-blocking code in practice?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Blocking calls halt the thread until done, serializing all work; non-blocking calls return control immediately. In a server, one blocking call (e.g., `JSON.parse` of a huge string or `*Sync` I/O) delays every pending request.\n\n\n```js\n// non-blocking preferred in request handlers\nawait fs.promises.readFile('big.json')\n```\n```\n",tech:"node",displayNumber:42,sortKey:1161},{id:"node-1162",number:43,title:"What is `process.hrtime()` / `performance.now()` used for?",question:"What is `process.hrtime()` / `performance.now()` used for?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Both give high-resolution timing for benchmarking. `process.hrtime.bigint()` returns nanoseconds as a BigInt; `perf_hooks.performance.now()` returns fractional milliseconds. Use them instead of `Date.now()` for precise measurements.\n\n\n```js\nconst s = process.hrtime.bigint()\nwork()\nconsole.log(Number(process.hrtime.bigint() - s) / 1e6, 'ms')\n```\n```\n",tech:"node",displayNumber:43,sortKey:1162},{id:"node-1163",number:44,title:"How do you handle uncaught exceptions?",question:"How do you handle uncaught exceptions?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Listen to `process.on('uncaughtException')` and `process.on('unhandledRejection')` to log and then gracefully shut down. These are last-resort handlers — the process is in an undefined state, so restart rather than continue.\n\n\n```js\nprocess.on('uncaughtException', err => { console.error(err); process.exit(1) })\n```\n```\n",tech:"node",displayNumber:44,sortKey:1163},{id:"node-1164",number:45,title:"What is the difference between `process.on('exit')` and `process.on('beforeExit')`?",question:"What is the difference between `process.on('exit')` and `process.on('beforeExit')`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`beforeExit` fires when the event loop empties and can schedule more async work (won't fire on explicit `process.exit()`). `exit` fires just before the process terminates and only allows synchronous work.\n\n\n```js\nprocess.on('exit', code => console.log('exiting', code)) // sync only\n```\n```\n",tech:"node",displayNumber:45,sortKey:1164},{id:"node-1165",number:46,title:"What are signals like SIGINT and SIGTERM?",question:"What are signals like SIGINT and SIGTERM?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"OS signals notify a process of events: `SIGINT` (Ctrl+C), `SIGTERM` (graceful stop, e.g. from orchestrators). Handling them enables graceful shutdown — closing servers and DB connections before exiting.\n\n\n```js\nprocess.on('SIGTERM', async () => { await server.close(); process.exit(0) })\n```\n```\n",tech:"node",displayNumber:46,sortKey:1165},{id:"node-1166",number:47,title:"What is graceful shutdown and why does it matter?",question:"What is graceful shutdown and why does it matter?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Graceful shutdown stops accepting new requests, finishes in-flight requests, closes DB/socket connections, then exits. It prevents dropped requests and data corruption during deploys or autoscaling.\n\n\n```js\nprocess.on('SIGTERM', () => {\n  server.close(() => { db.end(); process.exit(0) })\n})\n```\n```\n",tech:"node",displayNumber:47,sortKey:1166},{id:"node-1167",number:48,title:"How does garbage collection work in Node/V8?",question:"How does garbage collection work in Node/V8?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:'V8 uses a generational, mark-and-sweep collector. New objects live in the "young generation" (scavenged frequently); survivors are promoted to the "old generation" (collected less often). GC pauses the JS thread briefly ("stop-the-world").\n\n\n```js\n// run node --expose-gc to call global.gc() manually in tests\nif (global.gc) global.gc()\n```\n```\n',tech:"node",displayNumber:48,sortKey:1167},{id:"node-1168",number:49,title:"What is a memory leak in Node and how do you detect it?",question:"What is a memory leak in Node and how do you detect it?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"A leak is memory that is retained but never released — common causes are unbounded caches, forgotten timers, and lingering event listeners. Detect via heap snapshots (`--inspect` + Chrome DevTools), `process.memoryUsage()`, or tools like clinic.js.\n\n\n```js\nconsole.log(process.memoryUsage().heapUsed / 1e6, 'MB')\n```\n```\n",tech:"node",displayNumber:49,sortKey:1168},{id:"node-1169",number:50,title:"What is the maximum heap size in Node and how do you change it?",question:"What is the maximum heap size in Node and how do you change it?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"V8 caps the old-space heap (historically ~1.5–2GB on 64-bit). Raise it with `--max-old-space-size=<MB>`. If you routinely need more, reconsider the design (streaming, pagination) rather than just enlarging the heap.\n\n\n```js\n$ node --max-old-space-size=4096 app.js\n```\n```\n",tech:"node",displayNumber:50,sortKey:1169},{id:"node-1170",number:51,title:"What is the difference between `Buffer.alloc` and `Buffer.allocUnsafe`?",question:"What is the difference between `Buffer.alloc` and `Buffer.allocUnsafe`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`Buffer.alloc(n)` returns a zero-filled buffer (safe). `Buffer.allocUnsafe(n)` is faster but may contain old memory contents, which can leak sensitive data if not fully overwritten. Prefer `alloc` unless you immediately fill the buffer.\n\n\n```js\nconst safe = Buffer.alloc(8)        // 00 00 00 00 00 00 00 00\nconst fast = Buffer.allocUnsafe(8)  // uninitialized bytes\n```\n```\n",tech:"node",displayNumber:51,sortKey:1170},{id:"node-1171",number:52,title:"What is the difference between `path.join` and `path.resolve`?",question:"What is the difference between `path.join` and `path.resolve`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`path.join` concatenates segments and normalizes separators. `path.resolve` builds an absolute path by processing segments right-to-left until an absolute path is formed, defaulting to `cwd`. Use them instead of manual string concat for cross-platform safety.\n\n\n```js\npath.join('a', 'b', '..', 'c')     // 'a/c'\npath.resolve('a', '/b', 'c')        // '/b/c'\n```\n```\n",tech:"node",displayNumber:52,sortKey:1171},{id:"node-1172",number:53,title:"What is the `util` module used for?",question:"What is the `util` module used for?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`util` provides helpers: `util.promisify` (callback→Promise), `util.inspect` (deep object formatting), `util.types` (type checks), and `util.format`. It bridges legacy callback APIs to Promises.\n\n\n```js\nconst readFile = require('util').promisify(require('fs').readFile)\nawait readFile('a.txt')\n```\n```\n",tech:"node",displayNumber:53,sortKey:1172},{id:"node-1173",number:54,title:"What does `util.promisify` do?",question:"What does `util.promisify` do?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"It converts an error-first callback function into one that returns a Promise, letting you use `async/await` with legacy APIs.\n\n\n```js\nconst { promisify } = require('util')\nconst sleep = promisify(setTimeout)\nawait sleep(1000)\n```\n```\n",tech:"node",displayNumber:54,sortKey:1173},{id:"node-1174",number:55,title:"What is the difference between `dependencies` and `devDependencies`?",question:"What is the difference between `dependencies` and `devDependencies`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`dependencies` are needed at runtime (e.g. express). `devDependencies` are only needed during development/build (e.g. jest, eslint, webpack). `npm install --production` skips devDependencies.\n\n\n```js\nnpm install express            // dependencies\nnpm install --save-dev jest    // devDependencies\n```\n```\n",tech:"node",displayNumber:55,sortKey:1174},{id:"node-1175",number:56,title:"What is semantic versioning (semver)?",question:"What is semantic versioning (semver)?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Semver is `MAJOR.MINOR.PATCH`: MAJOR for breaking changes, MINOR for backward-compatible features, PATCH for backward-compatible fixes. Ranges: `^1.2.3` allows minor/patch, `~1.2.3` allows patch only.\n\n\n```js\n"express": "^4.19.0"  // >=4.19.0 <5.0.0\n```\n```\n',tech:"node",displayNumber:56,sortKey:1175},{id:"node-1176",number:57,title:"What is `package-lock.json`?",question:"What is `package-lock.json`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`package-lock.json` records the exact resolved version and integrity hash of every installed package (including transitive deps), ensuring reproducible installs across machines and CI. Commit it to source control.\n\n\n```js\nnpm ci  // installs strictly from the lock file\n```\n```\n",tech:"node",displayNumber:57,sortKey:1176},{id:"node-1177",number:58,title:"What is the difference between `npm install` and `npm ci`?",question:"What is the difference between `npm install` and `npm ci`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`npm install` resolves versions and may update the lock file. `npm ci` does a clean, exact install from `package-lock.json` (deletes `node_modules` first), is faster, and is meant for CI/production reproducibility.\n\n\n```js\nnpm ci --omit=dev\n```\n```\n",tech:"node",displayNumber:58,sortKey:1177},{id:"node-1178",number:59,title:"What are npm scripts?",question:"What are npm scripts?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'Scripts in `package.json` under `"scripts"` are command shortcuts run with `npm run <name>`. `start`, `test`, `stop`, and `restart` can be run without `run`. They can chain via `pre`/`post` hooks.\n\n\n```js\n"scripts": { "build": "tsc", "prebuild": "rimraf dist" }\n```\n```\n',tech:"node",displayNumber:59,sortKey:1178},{id:"node-1179",number:60,title:"What is `npx`?",question:"What is `npx`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'`npx` runs a package binary without globally installing it, downloading it temporarily if needed. It is handy for one-off tools like scaffolders (`npx create-react-app`).\n\n\n```js\nnpx cowsay "Hello"\n```\n```\n',tech:"node",displayNumber:60,sortKey:1179},{id:"node-1180",number:61,title:"What is the difference between local and global npm installs?",question:"What is the difference between local and global npm installs?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Local installs (`npm install pkg`) go into the project's `node_modules` and are used by that project. Global installs (`-g`) place binaries on the system PATH for CLI use across projects but should be avoided for project dependencies.\n\n\n```js\nnpm install -g nodemon   // CLI available everywhere\n```\n```\n",tech:"node",displayNumber:61,sortKey:1180},{id:"node-1181",number:62,title:"What is `engines` in package.json?",question:"What is `engines` in package.json?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'The `engines` field declares which Node/npm versions your package supports. With `engine-strict`, npm errors on incompatible versions, preventing subtle runtime bugs.\n\n\n```js\n"engines": { "node": ">=18.0.0" }\n```\n```\n',tech:"node",displayNumber:62,sortKey:1181},{id:"node-1182",number:63,title:"What are peerDependencies?",question:"What are peerDependencies?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:'`peerDependencies` declare a package that the host project must provide (e.g. a React plugin needing React). It avoids duplicate/incompatible copies of a shared library. npm 7+ auto-installs them by default.\n\n\n```js\n"peerDependencies": { "react": ">=17" }\n```\n```\n',tech:"node",displayNumber:63,sortKey:1182},{id:"node-1183",number:64,title:"How do you create a custom global module/binary?",question:"How do you create a custom global module/binary?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:'Add a `bin` field pointing to an executable script with a shebang, then install globally or link. This is how CLI tools expose commands.\n\n\n```js\n// package.json\n"bin": { "mytool": "./cli.js" }\n// cli.js first line\n#!/usr/bin/env node\n```\n```\n',tech:"node",displayNumber:64,sortKey:1183},{id:"node-1184",number:65,title:"How does Node handle unhandled promise rejections?",question:"How does Node handle unhandled promise rejections?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Since Node 15, an unhandled rejection terminates the process by default (previously a warning). Always attach `.catch` or wrap `await` in try/catch, and add a global `unhandledRejection` handler as a safety net.\n\n\n```js\nprocess.on('unhandledRejection', reason => console.error(reason))\n```\n```\n",tech:"node",displayNumber:65,sortKey:1184},{id:"node-1185",number:66,title:"What is the `os` module?",question:"What is the `os` module?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`os` exposes operating-system info: CPU count, memory, platform, hostname, network interfaces, and `os.tmpdir()`. It is often used to size worker/cluster pools.\n\n\n```js\nconst os = require('node:os')\nconsole.log(os.cpus().length, 'cores')\n```\n```\n",tech:"node",displayNumber:66,sortKey:1185},{id:"node-1186",number:67,title:"What is the difference between `fs.readFile` and `fs.createReadStream`?",question:"What is the difference between `fs.readFile` and `fs.createReadStream`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`readFile` loads the entire file into memory before the callback fires — simple but memory-heavy for large files. `createReadStream` reads in chunks, letting you process data incrementally with low, constant memory.\n\n\n```js\nfs.createReadStream('big.log').pipe(res) // stream to client\n```\n```\n",tech:"node",displayNumber:67,sortKey:1186},{id:"node-1187",number:68,title:"What is the purpose of `Buffer.concat`?",question:"What is the purpose of `Buffer.concat`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`Buffer.concat(list)` joins an array of buffers into one, commonly used to assemble chunks collected from a stream into a complete payload.\n\n\n```js\nconst chunks = []\nreq.on('data', c => chunks.push(c))\nreq.on('end', () => console.log(Buffer.concat(chunks).toString()))\n```\n```\n",tech:"node",displayNumber:68,sortKey:1187},{id:"node-1188",number:69,title:"What is the difference between `null` and `undefined` handling in Node config?",question:"What is the difference between `null` and `undefined` handling in Node config?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`process.env` values are always strings or `undefined` (never `null`). Comparisons and defaults should account for `undefined`; use `??` for nullish fallback and explicit parsing for numbers/booleans.\n\n\n```js\nconst port = Number(process.env.PORT ?? 3000)\nconst debug = process.env.DEBUG === 'true'\n```\n```\n",tech:"node",displayNumber:69,sortKey:1188},{id:"node-1189",number:70,title:"How do you profile CPU usage of a Node application?",question:"How do you profile CPU usage of a Node application?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Use `node --prof` to generate a V8 log then `node --prof-process`, or `--inspect` with Chrome DevTools CPU profiler, or tools like `clinic flame` / `0x` for flame graphs. Profiling reveals hot functions and blocking code.\n\n\n```js\n$ node --prof app.js\n$ node --prof-process isolate-*.log > profile.txt\n```\n```\n",tech:"node",displayNumber:70,sortKey:1189},{id:"node-1190",number:1,title:"What is a module in Node.js?",question:"What is a module in Node.js?",difficulty:"Basic",category:"Modules & npm",answer:"A module is a self-contained unit of code (usually one file) with its own scope. It exposes functionality via exports and consumes others via `require`/`import`, enabling reuse and separation of concerns.\n\n\n```js\n// math.js\nmodule.exports.add = (a, b) => a + b\n// app.js\nconst { add } = require('./math')\n```\n```\n",tech:"node",displayNumber:1,sortKey:1190},{id:"node-1191",number:2,title:"What are the three types of modules in Node?",question:"What are the three types of modules in Node?",difficulty:"Basic",category:"Modules & npm",answer:"Core modules (built into Node, e.g. `fs`, `http`), local modules (your own files), and third-party modules (installed from npm into `node_modules`).\n\n\n```js\nconst fs = require('fs')          // core\nconst util = require('./util')    // local\nconst express = require('express')// third-party\n```\n```\n",tech:"node",displayNumber:2,sortKey:1191},{id:"node-1192",number:3,title:"How do you export multiple values from a module?",question:"How do you export multiple values from a module?",difficulty:"Basic",category:"Modules & npm",answer:"Attach properties to `module.exports` (CJS) or use named `export` statements (ESM). A default export represents the single primary value.\n\n\n```js\n// CJS\nmodule.exports = { add, subtract }\n// ESM\nexport const add = ...; export default calculator\n```\n```\n",tech:"node",displayNumber:3,sortKey:1192},{id:"node-1193",number:4,title:"What happens when you `require` a JSON file?",question:"What happens when you `require` a JSON file?",difficulty:"Basic",category:"Modules & npm",answer:"Node parses the JSON and returns it as a JavaScript object. The result is cached like any module, so edits require clearing the cache to reload.\n\n\n```js\nconst config = require('./config.json')\nconsole.log(config.port)\n```\n```\n",tech:"node",displayNumber:4,sortKey:1193},{id:"node-1194",number:5,title:"How do you import JSON in ES Modules?",question:"How do you import JSON in ES Modules?",difficulty:"Intermediate",category:"Modules & npm",answer:"Use an import attribute (`with { type: 'json' }`) in modern Node, or read/parse it manually. Older syntax used `assert { type: 'json' }`.\n\n\n```js\nimport config from './config.json' with { type: 'json' }\n```\n```\n",tech:"node",displayNumber:5,sortKey:1194},{id:"node-1195",number:6,title:"What is the difference between named and default exports?",question:"What is the difference between named and default exports?",difficulty:"Intermediate",category:"Modules & npm",answer:"Named exports export multiple bindings imported by exact name (with `{}`); a default export is a single value imported under any name. A module can mix both.\n\n\n```js\nexport default App\nexport function helper() {}\nimport App, { helper } from './App.js'\n```\n```\n",tech:"node",displayNumber:6,sortKey:1195},{id:"node-1196",number:7,title:"How do you re-export from another module?",question:"How do you re-export from another module?",difficulty:"Intermediate",category:"Modules & npm",answer:"Use `export ... from` (ESM) to create a barrel file that aggregates and re-exposes exports, simplifying imports for consumers.\n\n\n```js\n// index.js barrel\nexport { add } from './math.js'\nexport { default as User } from './User.js'\n```\n```\n",tech:"node",displayNumber:7,sortKey:1196},{id:"node-1197",number:8,title:"What is a barrel file and what is its downside?",question:"What is a barrel file and what is its downside?",difficulty:"Advanced",category:"Modules & npm",answer:"A barrel file re-exports many modules from one index for cleaner imports. Downside: it can hurt tree-shaking and increase load time because importing one symbol may pull in the whole barrel.\n\n\n```js\n// consumers do: import { add } from './utils'\n// but bundlers may load all of ./utils\n```\n```\n",tech:"node",displayNumber:8,sortKey:1197},{id:"node-1198",number:9,title:"How does Node find `node_modules`?",question:"How does Node find `node_modules`?",difficulty:"Intermediate",category:"Modules & npm",answer:"Node searches the current directory's `node_modules`, then walks up parent directories until it reaches the filesystem root. This lets nested packages share or override dependencies.\n\n\n```js\n// require('lodash') from /app/src looks in\n// /app/src/node_modules, /app/node_modules, /node_modules\n```\n```\n",tech:"node",displayNumber:9,sortKey:1198},{id:"node-1199",number:10,title:"What is the difference between `require('./x')` and `require('x')`?",question:"What is the difference between `require('./x')` and `require('x')`?",difficulty:"Basic",category:"Modules & npm",answer:"`./x` (or `../`, `/`) is a path-based local module. A bare specifier `x` is resolved as a core module or from `node_modules`. Missing the `./` is a common cause of \"cannot find module\" errors.\n\n\n```js\nrequire('./config')  // local file\nrequire('config')     // npm package\n```\n```\n",tech:"node",displayNumber:10,sortKey:1199},{id:"node-1200",number:11,title:"What are the fields in package.json for entry points?",question:"What are the fields in package.json for entry points?",difficulty:"Advanced",category:"Modules & npm",answer:'`main` is the CJS entry, `module` hints an ESM entry for bundlers, `exports` (modern) defines conditional/subpath entry points and can restrict access, and `types` points to TypeScript declarations.\n\n\n```js\n"main": "./dist/index.cjs",\n"exports": { ".": { "import": "./dist/index.mjs", "require": "./dist/index.cjs" } }\n```\n```\n',tech:"node",displayNumber:11,sortKey:1200},{id:"node-1201",number:12,title:"What is the `exports` map (conditional exports)?",question:"What is the `exports` map (conditional exports)?",difficulty:"Experienced",category:"Modules & npm",answer:'The `exports` field defines which files consumers may import and provides condition-based resolution (`import`, `require`, `node`, `browser`, `default`). It encapsulates internals and enables dual CJS/ESM packages.\n\n\n```js\n"exports": {\n  "./feature": { "import": "./feature.mjs", "require": "./feature.cjs" }\n}\n```\n```\n',tech:"node",displayNumber:12,sortKey:1201},{id:"node-1202",number:13,title:"What is a dual package (CJS + ESM)?",question:"What is a dual package (CJS + ESM)?",difficulty:"Experienced",category:"Modules & npm",answer:'A dual package ships both CommonJS and ES Module builds so it works everywhere. Risk: the "dual package hazard" — the same module loaded in both formats yields two separate instances with duplicated state.\n\n\n```js\n"exports": { "import": "./esm/index.js", "require": "./cjs/index.js" }\n```\n```\n',tech:"node",displayNumber:13,sortKey:1202},{id:"node-1203",number:14,title:"How do you conditionally load a module?",question:"How do you conditionally load a module?",difficulty:"Intermediate",category:"Modules & npm",answer:"In CJS, call `require` inside a branch (it is dynamic). In ESM, use dynamic `import()` which returns a Promise, enabling lazy or conditional loading.\n\n\n```js\nif (useRedis) var store = require('./redisStore')\nconst mod = await import(`./drivers/${name}.js`)\n```\n```\n",tech:"node",displayNumber:14,sortKey:1203},{id:"node-1204",number:15,title:"What is lazy loading of modules and why use it?",question:"What is lazy loading of modules and why use it?",difficulty:"Advanced",category:"Modules & npm",answer:"Lazy loading defers `require`/`import` until a module is actually needed, reducing startup time and memory when a feature may never run. Common for optional/heavy dependencies.\n\n\n```js\nfunction generatePdf() {\n  const PDFKit = require('pdfkit') // loaded only on first use\n}\n```\n```\n",tech:"node",displayNumber:15,sortKey:1204},{id:"node-1205",number:16,title:"What is the difference between `npm update` and `npm install`?",question:"What is the difference between `npm update` and `npm install`?",difficulty:"Intermediate",category:"Modules & npm",answer:"`npm install` installs versions satisfying `package.json`, adding missing ones. `npm update` bumps installed packages to the latest allowed by their semver ranges and updates the lock file.\n\n\n```js\nnpm update lodash\n```\n```\n",tech:"node",displayNumber:16,sortKey:1205},{id:"node-1206",number:17,title:"How do you check for outdated packages?",question:"How do you check for outdated packages?",difficulty:"Basic",category:"Modules & npm",answer:"`npm outdated` lists installed packages with their current, wanted (max satisfying range), and latest versions, helping you plan upgrades.\n\n\n```js\nnpm outdated\n```\n```\n",tech:"node",displayNumber:17,sortKey:1206},{id:"node-1207",number:18,title:"What is `npm audit`?",question:"What is `npm audit`?",difficulty:"Intermediate",category:"Modules & npm",answer:"`npm audit` scans your dependency tree against a vulnerability database and reports issues with severity. `npm audit fix` applies compatible fixes automatically.\n\n\n```js\nnpm audit --audit-level=high\nnpm audit fix\n```\n```\n",tech:"node",displayNumber:18,sortKey:1207},{id:"node-1208",number:19,title:"What is the difference between `~` and `^` in version ranges?",question:"What is the difference between `~` and `^` in version ranges?",difficulty:"Intermediate",category:"Modules & npm",answer:'`~1.2.3` allows patch updates (`>=1.2.3 <1.3.0`). `^1.2.3` allows minor + patch (`>=1.2.3 <2.0.0`). For `0.x`, `^` is stricter and behaves closer to `~`.\n\n\n```js\n"~1.2.3" // 1.2.x\n"^1.2.3" // 1.x.x\n```\n```\n',tech:"node",displayNumber:19,sortKey:1208},{id:"node-1209",number:20,title:"How do you pin an exact dependency version?",question:"How do you pin an exact dependency version?",difficulty:"Basic",category:"Modules & npm",answer:'Specify the version without a range prefix, or install with `--save-exact`. The lock file already pins exact resolved versions for reproducibility.\n\n\n```js\nnpm install left-pad@1.3.0 --save-exact\n// "left-pad": "1.3.0"\n```\n```\n',tech:"node",displayNumber:20,sortKey:1209},{id:"node-1210",number:21,title:"What are optionalDependencies?",question:"What are optionalDependencies?",difficulty:"Advanced",category:"Modules & npm",answer:"`optionalDependencies` may fail to install without failing the whole install. Your code must handle their absence at runtime (often platform-specific native modules).\n\n\n```js\nlet fsevents\ntry { fsevents = require('fsevents') } catch { /* not on this OS */ }\n```\n```\n",tech:"node",displayNumber:21,sortKey:1210},{id:"node-1211",number:22,title:"What is `bundledDependencies`?",question:"What is `bundledDependencies`?",difficulty:"Advanced",category:"Modules & npm",answer:'`bundledDependencies` (or `bundleDependencies`) lists packages included inside your published tarball, so they ship with your package rather than being fetched from the registry. Rarely needed today.\n\n\n```js\n"bundledDependencies": ["my-private-fork"]\n```\n```\n',tech:"node",displayNumber:22,sortKey:1211},{id:"node-1212",number:23,title:"What is an npm workspace / monorepo?",question:"What is an npm workspace / monorepo?",difficulty:"Experienced",category:"Modules & npm",answer:'npm workspaces let one repo host multiple packages sharing a single `node_modules` and lock file. Dependencies between local packages are symlinked, simplifying multi-package development.\n\n\n```js\n// root package.json\n"workspaces": ["packages/*"]\n```\n```\n',tech:"node",displayNumber:23,sortKey:1212},{id:"node-1213",number:24,title:"How do you run a script across all workspaces?",question:"How do you run a script across all workspaces?",difficulty:"Advanced",category:"Modules & npm",answer:"Use `npm run <script> --workspaces` (or `-w <name>` for one). This executes the script in each package that defines it.\n\n\n```js\nnpm run build --workspaces --if-present\n```\n```\n",tech:"node",displayNumber:24,sortKey:1213},{id:"node-1214",number:25,title:"What is `npm link`?",question:"What is `npm link`?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm link` symlinks a local package into the global folder, then into a consuming project, so you can develop and test a package locally before publishing. Beware duplicate peer deps.\n\n\n```js\ncd my-lib && npm link\ncd ../app && npm link my-lib\n```\n```\n",tech:"node",displayNumber:25,sortKey:1214},{id:"node-1215",number:26,title:"How do you publish a package to npm?",question:"How do you publish a package to npm?",difficulty:"Intermediate",category:"Modules & npm",answer:"Log in (`npm login`), set `name`/`version`/`files` in package.json, then `npm publish`. Scoped packages need `--access public` to be public.\n\n\n```js\nnpm version patch\nnpm publish --access public\n```\n```\n",tech:"node",displayNumber:26,sortKey:1215},{id:"node-1216",number:27,title:"What does the `files` field in package.json do?",question:"What does the `files` field in package.json do?",difficulty:"Intermediate",category:"Modules & npm",answer:'`files` whitelists which files are included in the published tarball. Without it, npm uses `.npmignore` (or `.gitignore`) to exclude files. Whitelisting avoids accidentally shipping source/tests.\n\n\n```js\n"files": ["dist", "README.md"]\n```\n```\n',tech:"node",displayNumber:27,sortKey:1216},{id:"node-1217",number:28,title:"What is `.npmignore` and how does it interact with `.gitignore`?",question:"What is `.npmignore` and how does it interact with `.gitignore`?",difficulty:"Advanced",category:"Modules & npm",answer:"`.npmignore` excludes files from the published package. If it is absent, npm falls back to `.gitignore`. If both exist, `.npmignore` wins for publishing. The `files` whitelist takes precedence over both.\n\n\n```js\n// .npmignore\ntest/\n*.log\n```\n```\n",tech:"node",displayNumber:28,sortKey:1217},{id:"node-1218",number:29,title:"What is a scoped package?",question:"What is a scoped package?",difficulty:"Intermediate",category:"Modules & npm",answer:"A scoped package is namespaced under a user/org with `@scope/name`, avoiding name collisions and enabling private packages. Scopes map to registries/organizations.\n\n\n```js\nnpm install @myorg/logger\n```\n```\n",tech:"node",displayNumber:29,sortKey:1218},{id:"node-1219",number:30,title:"How do you use a private registry?",question:"How do you use a private registry?",difficulty:"Advanced",category:"Modules & npm",answer:"Configure the registry globally or per-scope in `.npmrc`, with an auth token. This routes installs/publishes for a scope to your private registry (e.g. GitHub Packages, Verdaccio).\n\n\n```js\n// .npmrc\n@myorg:registry=https://npm.pkg.github.com\n//npm.pkg.github.com/:_authToken=${TOKEN}\n```\n```\n",tech:"node",displayNumber:30,sortKey:1219},{id:"node-1220",number:31,title:"What is `.npmrc` used for?",question:"What is `.npmrc` used for?",difficulty:"Intermediate",category:"Modules & npm",answer:"`.npmrc` stores npm configuration (registry, auth tokens, `save-exact`, `engine-strict`, proxy). It can be global, per-user, or per-project, with project-level overriding.\n\n\n```js\n// .npmrc\nsave-exact=true\nengine-strict=true\n```\n```\n",tech:"node",displayNumber:31,sortKey:1220},{id:"node-1221",number:32,title:"How do you override a transitive dependency version?",question:"How do you override a transitive dependency version?",difficulty:"Experienced",category:"Modules & npm",answer:'Use the `overrides` field (npm 8+) to force a nested dependency to a specific version, useful for patching a vulnerable transitive package.\n\n\n```js\n"overrides": { "minimist": "1.2.8" }\n```\n```\n',tech:"node",displayNumber:32,sortKey:1221},{id:"node-1222",number:33,title:"What is the difference between npm, yarn, and pnpm?",question:"What is the difference between npm, yarn, and pnpm?",difficulty:"Advanced",category:"Modules & npm",answer:"All manage packages. Yarn added lockfiles/workspaces early and offers Plug'n'Play. pnpm uses a content-addressed global store with symlinks, saving disk space and enforcing stricter dependency isolation. npm is the default and now supports most features.\n\n\n```js\npnpm install   // hard-links from a global store\n```\n```\n",tech:"node",displayNumber:33,sortKey:1222},{id:"node-1223",number:34,title:"Why does pnpm save disk space?",question:"Why does pnpm save disk space?",difficulty:"Advanced",category:"Modules & npm",answer:"pnpm stores each package version once in a global content-addressable store and hard-links it into each project's `node_modules`, so many projects share the same physical files instead of duplicating them.\n\n\n```js\n// ~/.pnpm-store holds a single copy per version\n```\n```\n",tech:"node",displayNumber:34,sortKey:1223},{id:"node-1224",number:35,title:"What is dependency hoisting?",question:"What is dependency hoisting?",difficulty:"Advanced",category:"Modules & npm",answer:"Hoisting moves shared transitive dependencies up to a top-level `node_modules` to deduplicate. It can cause \"phantom dependencies\" — code accidentally using a package it never declared. pnpm avoids this with a strict layout.\n\n\n```js\n// You import 'ms' without declaring it because express hoisted it — fragile\n```\n```\n",tech:"node",displayNumber:35,sortKey:1224},{id:"node-1225",number:36,title:"What is a phantom dependency?",question:"What is a phantom dependency?",difficulty:"Experienced",category:"Modules & npm",answer:"A phantom dependency is a package your code imports but never declared in `package.json`; it works only because hoisting exposed it. It breaks when the transitive dep changes. Always declare what you import.\n\n\n```js\nrequire('lodash') // works today via hoisting, may vanish tomorrow\n```\n```\n",tech:"node",displayNumber:36,sortKey:1225},{id:"node-1226",number:37,title:"How do you create and use a local file dependency?",question:"How do you create and use a local file dependency?",difficulty:"Intermediate",category:"Modules & npm",answer:'Reference a local path or tarball in `package.json` using `file:`. Useful for monorepos or testing before publishing.\n\n\n```js\n"dependencies": { "my-lib": "file:../my-lib" }\n```\n```\n',tech:"node",displayNumber:37,sortKey:1226},{id:"node-1227",number:38,title:"How do you install directly from a Git repository?",question:"How do you install directly from a Git repository?",difficulty:"Intermediate",category:"Modules & npm",answer:"Provide a git URL (optionally with a commit/branch/tag) as the version. npm clones and builds it. Useful for forks or unpublished packages.\n\n\n```js\nnpm install github:user/repo#v2.0.0\n```\n```\n",tech:"node",displayNumber:38,sortKey:1227},{id:"node-1228",number:39,title:"What are npm lifecycle hooks?",question:"What are npm lifecycle hooks?",difficulty:"Advanced",category:"Modules & npm",answer:'npm runs scripts around events: `preinstall`, `install`, `postinstall`, `prepublishOnly`, `prepare`. `pre`/`post` prefixes wrap any custom script too. Beware running untrusted postinstall scripts (supply-chain risk).\n\n\n```js\n"scripts": { "prepare": "husky install" }\n```\n```\n',tech:"node",displayNumber:39,sortKey:1228},{id:"node-1229",number:40,title:"What security risk do postinstall scripts pose?",question:"What security risk do postinstall scripts pose?",difficulty:"Experienced",category:"Modules & npm",answer:"A malicious dependency's `postinstall` runs arbitrary code on your machine/CI, a common supply-chain attack. Mitigate with `--ignore-scripts`, lockfile review, and pinning.\n\n\n```js\nnpm ci --ignore-scripts\n```\n```\n",tech:"node",displayNumber:40,sortKey:1229},{id:"node-1230",number:41,title:"How do you clear the npm cache?",question:"How do you clear the npm cache?",difficulty:"Basic",category:"Modules & npm",answer:"Use `npm cache clean --force`. Modern npm self-heals the cache, so this is rarely necessary; `npm cache verify` checks integrity instead.\n\n\n```js\nnpm cache verify\n```\n```\n",tech:"node",displayNumber:41,sortKey:1230},{id:"node-1231",number:42,title:"What is `npm dedupe`?",question:"What is `npm dedupe`?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm dedupe` rearranges the dependency tree to move compatible duplicate packages to a shared higher level, reducing installed copies and bundle size.\n\n\n```js\nnpm dedupe\n```\n```\n",tech:"node",displayNumber:42,sortKey:1231},{id:"node-1232",number:43,title:"How do you list installed dependencies as a tree?",question:"How do you list installed dependencies as a tree?",difficulty:"Basic",category:"Modules & npm",answer:"`npm ls` prints the dependency tree; add a package name to find why it is installed, or `--all` for the full depth.\n\n\n```js\nnpm ls express\n```\n```\n",tech:"node",displayNumber:43,sortKey:1232},{id:"node-1233",number:44,title:"How do you find why a package is installed?",question:"How do you find why a package is installed?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm explain <pkg>` (or `npm ls <pkg>`) shows the dependency chains that pull the package in, helping remove or override unwanted transitive deps.\n\n\n```js\nnpm explain minimist\n```\n```\n",tech:"node",displayNumber:44,sortKey:1233},{id:"node-1234",number:45,title:"What is `npm-shrinkwrap.json`?",question:"What is `npm-shrinkwrap.json`?",difficulty:"Experienced",category:"Modules & npm",answer:"It is a publishable version of `package-lock.json`. Unlike the lock file, it IS included when you publish, letting library authors lock the entire dependency tree for consumers (rarely recommended for libraries).\n\n\n```js\nnpm shrinkwrap\n```\n```\n",tech:"node",displayNumber:45,sortKey:1234},{id:"node-1235",number:46,title:"What is tree-shaking and how do modules affect it?",question:"What is tree-shaking and how do modules affect it?",difficulty:"Experienced",category:"Modules & npm",answer:'Tree-shaking eliminates unused exports at bundle time. It requires static ES Modules (`import`/`export`) and `"sideEffects": false` hints; dynamic CJS `require` defeats it because usage can\'t be analyzed statically.\n\n\n```js\n"sideEffects": false // safe to drop unused exports\n```\n```\n',tech:"node",displayNumber:46,sortKey:1235},{id:"node-1236",number:47,title:'What does `"sideEffects": false` mean?',question:'What does `"sideEffects": false` mean?',difficulty:"Experienced",category:"Modules & npm",answer:'It tells bundlers that importing a module has no side effects (like polyfills or CSS registration), so unused imports can be safely removed. Mark specific files if some do have side effects.\n\n\n```js\n"sideEffects": ["*.css", "./src/polyfill.js"]\n```\n```\n',tech:"node",displayNumber:47,sortKey:1236},{id:"node-1237",number:48,title:"How do you create a monorepo with shared code?",question:"How do you create a monorepo with shared code?",difficulty:"Experienced",category:"Modules & npm",answer:'Use workspaces (npm/pnpm/yarn) or tools like Turborepo/Nx. Shared packages live under `packages/*`, are symlinked, and imported by name; a build orchestrator caches and parallelizes tasks.\n\n\n```js\n"workspaces": ["apps/*", "packages/*"]\n```\n```\n',tech:"node",displayNumber:48,sortKey:1237},{id:"node-1238",number:49,title:"What is the difference between `main`, `module`, and `browser` fields?",question:"What is the difference between `main`, `module`, and `browser` fields?",difficulty:"Experienced",category:"Modules & npm",answer:'`main` is the default CJS entry, `module` points bundlers to an ESM build for tree-shaking, and `browser` provides browser-specific replacements (e.g. swapping `fs` for a shim). The `exports` map now supersedes these.\n\n\n```js\n"main": "index.cjs", "module": "index.mjs", "browser": "index.browser.js"\n```\n```\n',tech:"node",displayNumber:49,sortKey:1238},{id:"node-1239",number:50,title:"How do you make a package work in both Node and the browser?",question:"How do you make a package work in both Node and the browser?",difficulty:"Experienced",category:"Modules & npm",answer:'Avoid Node-only APIs or provide conditional entries via the `exports`/`browser` fields, and use isomorphic libraries. Bundlers pick the right build per target.\n\n\n```js\n"exports": { "node": "./node.js", "browser": "./browser.js", "default": "./index.js" }\n```\n```\n',tech:"node",displayNumber:50,sortKey:1239},{id:"node-1240",number:51,title:"What is `import.meta.url` and why is it needed in ESM?",question:"What is `import.meta.url` and why is it needed in ESM?",difficulty:"Advanced",category:"Modules & npm",answer:"ESM has no `__dirname`/`__filename`. `import.meta.url` gives the current module's URL, from which you derive paths using `fileURLToPath`.\n\n\n```js\nimport { fileURLToPath } from 'node:url'\nconst __dirname = path.dirname(fileURLToPath(import.meta.url))\n```\n```\n",tech:"node",displayNumber:51,sortKey:1240},{id:"node-1241",number:52,title:"How do you use `__dirname` equivalent in ESM?",question:"How do you use `__dirname` equivalent in ESM?",difficulty:"Intermediate",category:"Modules & npm",answer:"Convert `import.meta.url` with `fileURLToPath`, then take `path.dirname`. Some Node versions expose `import.meta.dirname` directly.\n\n\n```js\nconst dir = import.meta.dirname ?? path.dirname(fileURLToPath(import.meta.url))\n```\n```\n",tech:"node",displayNumber:52,sortKey:1241},{id:"node-1242",number:53,title:"Can you use `require` inside an ES Module?",question:"Can you use `require` inside an ES Module?",difficulty:"Advanced",category:"Modules & npm",answer:"Not directly, but you can create one via `createRequire(import.meta.url)`. This helps load CJS-only packages or JSON from ESM code.\n\n\n```js\nimport { createRequire } from 'node:module'\nconst require = createRequire(import.meta.url)\nconst cjs = require('some-cjs-pkg')\n```\n```\n",tech:"node",displayNumber:53,sortKey:1242},{id:"node-1243",number:54,title:"Can you import an ESM package from CommonJS?",question:"Can you import an ESM package from CommonJS?",difficulty:"Advanced",category:"Modules & npm",answer:"Not with synchronous `require` (ESM is async). Use dynamic `import()`, which returns a Promise, inside your CJS code.\n\n\n```js\n;(async () => { const mod = await import('esm-only-pkg') })()\n```\n```\n",tech:"node",displayNumber:54,sortKey:1243},{id:"node-1244",number:55,title:"What is top-level await and where can you use it?",question:"What is top-level await and where can you use it?",difficulty:"Advanced",category:"Modules & npm",answer:"Top-level `await` lets you `await` at module scope without an async function — only in ES Modules. It is useful for async initialization but delays the module's readiness for importers.\n\n\n```js\n// config.mjs\nexport const config = await loadConfig()\n```\n```\n",tech:"node",displayNumber:55,sortKey:1244},{id:"node-1245",number:56,title:"What is a module's `exports` caching behavior with mutation?",question:"What is a module's `exports` caching behavior with mutation?",difficulty:"Experienced",category:"Modules & npm",answer:"Because modules are cached singletons, mutating an exported object is visible to all importers. This can be used for shared state but risks hidden coupling and hard-to-trace bugs.\n\n\n```js\n// counter.js\nmodule.exports = { n: 0 }\n// any importer that does counter.n++ affects all\n```\n```\n",tech:"node",displayNumber:56,sortKey:1245},{id:"node-1246",number:57,title:"What is the difference between deep and shallow module imports?",question:"What is the difference between deep and shallow module imports?",difficulty:"Advanced",category:"Modules & npm",answer:"A shallow import pulls the package entry; a deep import targets an internal file path. The `exports` map can forbid deep imports to keep internals private and refactorable.\n\n\n```js\nimport { debounce } from 'lodash-es'          // shallow\nimport debounce from 'lodash-es/debounce.js'  // deep (may be blocked)\n```\n```\n",tech:"node",displayNumber:57,sortKey:1246},{id:"node-1247",number:58,title:"How do you handle native (C++) addons?",question:"How do you handle native (C++) addons?",difficulty:"Experienced",category:"Modules & npm",answer:"Native addons are compiled binaries (`.node`) built with node-gyp/N-API. They install via prebuilt binaries or compile on `install`, requiring build tools. They provide performance or OS-level features JS can't.\n\n\n```js\nconst addon = require('bcrypt') // uses a native binding under the hood\n```\n```\n",tech:"node",displayNumber:58,sortKey:1247},{id:"node-1248",number:59,title:"What is N-API / Node-API?",question:"What is N-API / Node-API?",difficulty:"Experienced",category:"Modules & npm",answer:"Node-API is a stable, ABI-versioned C interface for building native addons that remain binary-compatible across Node versions, so addons don't need recompiling for each release.\n\n\n```js\n// addons built with Node-API load without rebuild across Node majors\n```\n```\n",tech:"node",displayNumber:59,sortKey:1248},{id:"node-1249",number:60,title:"How do you mock modules in tests?",question:"How do you mock modules in tests?",difficulty:"Advanced",category:"Modules & npm",answer:"Use a test runner's mock API (`jest.mock`) or Node's built-in `mock` from `node:test`, or dependency injection. Mocks replace a module's exports so you can isolate the unit under test.\n\n\n```js\njest.mock('./db', () => ({ query: jest.fn() }))\n```\n```\n",tech:"node",displayNumber:60,sortKey:1249},{id:"node-1250",number:61,title:"What is dependency injection and why prefer it over hard requires?",question:"What is dependency injection and why prefer it over hard requires?",difficulty:"Experienced",category:"Modules & npm",answer:"Dependency injection passes collaborators in (via constructor/args) rather than importing them internally. It decouples modules, makes testing easy (inject mocks), and avoids tangled module graphs.\n\n\n```js\nfunction createService(db) { return { get: id => db.find(id) } }\n```\n```\n",tech:"node",displayNumber:61,sortKey:1250},{id:"node-1251",number:62,title:"How do you version and publish a breaking change responsibly?",question:"How do you version and publish a breaking change responsibly?",difficulty:"Experienced",category:"Modules & npm",answer:"Bump the MAJOR version, document the migration in a changelog, optionally publish under a `next` dist-tag first, and deprecate the old major with `npm deprecate` to warn users.\n\n\n```js\nnpm version major\nnpm publish --tag next\n```\n```\n",tech:"node",displayNumber:62,sortKey:1251},{id:"node-1252",number:63,title:"What are npm dist-tags?",question:"What are npm dist-tags?",difficulty:"Advanced",category:"Modules & npm",answer:"Dist-tags are named pointers to versions (`latest`, `next`, `beta`). `npm install pkg` uses `latest`; users opt into others explicitly. They enable staged releases.\n\n\n```js\nnpm dist-tag add pkg@2.0.0-rc.1 next\nnpm install pkg@next\n```\n```\n",tech:"node",displayNumber:63,sortKey:1252},{id:"node-1253",number:64,title:"How do you deprecate a package or version?",question:"How do you deprecate a package or version?",difficulty:"Advanced",category:"Modules & npm",answer:'`npm deprecate <pkg>[@range] "<message>"` prints a warning on install, guiding users to alternatives without unpublishing.\n\n\n```js\nnpm deprecate my-lib@"<2.0.0" "Upgrade to v2, v1 is unmaintained"\n```\n```\n',tech:"node",displayNumber:64,sortKey:1253},{id:"node-1254",number:65,title:"Why is unpublishing packages restricted?",question:"Why is unpublishing packages restricted?",difficulty:"Experienced",category:"Modules & npm",answer:'After the left-pad incident, npm restricted unpublish (only within 72 hours and if nothing depends on it) to prevent breaking the ecosystem. Prefer deprecation over unpublishing.\n\n\n```js\nnpm deprecate my-lib "Do not use" // instead of unpublish\n```\n```\n',tech:"node",displayNumber:65,sortKey:1254},{id:"node-1255",number:66,title:"How do you audit and reduce your dependency footprint?",question:"How do you audit and reduce your dependency footprint?",difficulty:"Experienced",category:"Modules & npm",answer:"Review with `npm ls`, remove unused deps (depcheck), prefer small focused libraries or the standard library, and watch install size (`npm install --dry-run`, bundlephobia). Fewer deps mean less risk and faster installs.\n\n\n```js\nnpx depcheck   // finds unused/missing dependencies\n```\n```\n",tech:"node",displayNumber:66,sortKey:1255},{id:"node-1256",number:67,title:"What is the difference between `import x from 'y'` and `import * as x from 'y'`?",question:"What is the difference between `import x from 'y'` and `import * as x from 'y'`?",difficulty:"Intermediate",category:"Modules & npm",answer:"The first imports the default export as `x`. The second imports the entire module namespace object (all named exports) as `x`. Namespace objects are read-only.\n\n\n```js\nimport express from 'express'      // default\nimport * as fs from 'node:fs'      // namespace\n```\n```\n",tech:"node",displayNumber:67,sortKey:1256},{id:"node-1257",number:68,title:"How do you dynamically build an import path safely?",question:"How do you dynamically build an import path safely?",difficulty:"Advanced",category:"Modules & npm",answer:"Validate/whitelist the segment before interpolating to prevent loading arbitrary files, then use dynamic `import()`. Never feed unsanitized user input into a module path.\n\n\n```js\nconst allowed = { pg: './pg.js', mysql: './mysql.js' }\nconst driver = await import(allowed[name])\n```\n```\n",tech:"node",displayNumber:68,sortKey:1257},{id:"node-1258",number:69,title:"What is a polyfill vs a shim?",question:"What is a polyfill vs a shim?",difficulty:"Advanced",category:"Modules & npm",answer:"A polyfill implements a missing standard API using existing features (e.g. `fetch` in old Node). A shim intercepts/normalizes an existing API's behavior. Both smooth over environment differences.\n\n\n```js\nglobalThis.fetch ??= require('node-fetch') // polyfill for old Node\n```\n```\n",tech:"node",displayNumber:69,sortKey:1258},{id:"node-1259",number:70,title:"How do you structure a large Node project's modules?",question:"How do you structure a large Node project's modules?",difficulty:"Experienced",category:"Modules & npm",answer:"Group by feature/domain (not by technical type), keep modules small and single-purpose, expose clear public interfaces via index files, avoid circular deps, and separate pure logic from I/O for testability.\n\n\n```js\n// src/users/{controller,service,repository,index}.js\nexport { userRoutes } from './controller.js'\n```\n```\n",tech:"node",displayNumber:70,sortKey:1259},{id:"node-1260",number:1,title:"What is the event loop in Node.js?",question:"What is the event loop in Node.js?",difficulty:"Basic",category:"Event Loop & Async",answer:"The event loop is the mechanism that lets Node perform non-blocking I/O on a single thread. It continuously checks queues for completed operations and runs their callbacks, phase by phase, until there is no more work.\n\n\n```js\nsetTimeout(() => console.log('later'), 0)\nconsole.log('now') // 'now' then 'later'\n```\n```\n",tech:"node",displayNumber:1,sortKey:1260},{id:"node-1261",number:2,title:"What are the main phases of the event loop?",question:"What are the main phases of the event loop?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Timers (setTimeout/setInterval), pending callbacks, idle/prepare (internal), poll (I/O callbacks), check (setImmediate), and close callbacks. The loop cycles through these in order.\n\n\n```js\n// poll -> check -> close, then back to timers on the next tick\n```\n```\n",tech:"node",displayNumber:2,sortKey:1261},{id:"node-1262",number:3,title:"What is the poll phase?",question:"What is the poll phase?",difficulty:"Advanced",category:"Event Loop & Async",answer:"The poll phase retrieves new I/O events and executes their callbacks. If no timers are pending, it may block here waiting for I/O; if `setImmediate` callbacks are queued, it ends and moves to the check phase.\n\n\n```js\nfs.readFile('a', () => console.log('poll phase callback'))\n```\n```\n",tech:"node",displayNumber:3,sortKey:1262},{id:"node-1263",number:4,title:"What is the check phase?",question:"What is the check phase?",difficulty:"Advanced",category:"Event Loop & Async",answer:"The check phase runs `setImmediate` callbacks immediately after the poll phase completes. It is designed to run code right after I/O callbacks in the same loop iteration.\n\n\n```js\nsetImmediate(() => console.log('check phase'))\n```\n```\n",tech:"node",displayNumber:4,sortKey:1263},{id:"node-1264",number:5,title:"What is the difference between microtasks and macrotasks?",question:"What is the difference between microtasks and macrotasks?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Macrotasks are event-loop phase callbacks (timers, I/O, setImmediate). Microtasks are `process.nextTick` and resolved Promise callbacks. Microtasks run after each macrotask/operation and are fully drained before the loop proceeds — so they can starve I/O.\n\n\n```js\nsetTimeout(() => console.log('macro'))\nPromise.resolve().then(() => console.log('micro')) // micro first\n```\n```\n",tech:"node",displayNumber:5,sortKey:1264},{id:"node-1265",number:6,title:"What is the order between `process.nextTick` and Promises?",question:"What is the order between `process.nextTick` and Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Both are microtasks, but the `nextTick` queue is drained before the Promise microtask queue after each operation. So `nextTick` callbacks run before `.then` callbacks.\n\n\n```js\nPromise.resolve().then(() => console.log('promise'))\nprocess.nextTick(() => console.log('nextTick')) // nextTick prints first\n```\n```\n",tech:"node",displayNumber:6,sortKey:1265},{id:"node-1266",number:7,title:"Why can `process.nextTick` starve the event loop?",question:"Why can `process.nextTick` starve the event loop?",difficulty:"Experienced",category:"Event Loop & Async",answer:"The entire nextTick queue is drained before the loop advances to the next phase. If a nextTick callback keeps scheduling more nextTicks, I/O and timers never run — the loop is starved.\n\n\n```js\nfunction loop() { process.nextTick(loop) } // blocks all I/O forever\n```\n```\n",tech:"node",displayNumber:7,sortKey:1266},{id:"node-1267",number:8,title:"What is a Promise?",question:"What is a Promise?",difficulty:"Basic",category:"Event Loop & Async",answer:"A Promise represents the eventual result of an async operation, with states pending, fulfilled, or rejected. You consume it with `.then`/`.catch`/`.finally` or `await`.\n\n\n```js\nconst p = new Promise((resolve, reject) => resolve(42))\np.then(v => console.log(v)) // 42\n```\n```\n",tech:"node",displayNumber:8,sortKey:1267},{id:"node-1268",number:9,title:"What are the three states of a Promise?",question:"What are the three states of a Promise?",difficulty:"Basic",category:"Event Loop & Async",answer:"Pending (initial), fulfilled (resolved with a value), and rejected (failed with a reason). Once settled (fulfilled/rejected), a Promise is immutable.\n\n\n```js\nPromise.reject(new Error('x')).catch(e => console.log(e.message))\n```\n```\n",tech:"node",displayNumber:9,sortKey:1268},{id:"node-1269",number:10,title:"What is `async/await`?",question:"What is `async/await`?",difficulty:"Basic",category:"Event Loop & Async",answer:"`async` functions return Promises; `await` pauses execution until a Promise settles, letting you write asynchronous code that reads synchronously. Errors are caught with try/catch.\n\n\n```js\nasync function main() {\n  const data = await fetch('/api').then(r => r.json())\n  return data\n}\n```\n```\n",tech:"node",displayNumber:10,sortKey:1269},{id:"node-1270",number:11,title:"Does `await` block the event loop?",question:"Does `await` block the event loop?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"No. `await` suspends only the current async function and returns control to the event loop, which continues handling other work. It is cooperative, not blocking.\n\n\n```js\nasync function h() { await db.query() /* loop free meanwhile */ }\n```\n```\n",tech:"node",displayNumber:11,sortKey:1270},{id:"node-1271",number:12,title:"How do you run async operations in parallel?",question:"How do you run async operations in parallel?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Start the operations first (don't await sequentially), then await them together with `Promise.all`. Awaiting one at a time serializes them unnecessarily.\n\n\n```js\nconst [a, b] = await Promise.all([fetchA(), fetchB()])\n```\n```\n",tech:"node",displayNumber:12,sortKey:1271},{id:"node-1272",number:13,title:"What is the difference between `Promise.all` and `Promise.allSettled`?",question:"What is the difference between `Promise.all` and `Promise.allSettled`?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`Promise.all` rejects as soon as any promise rejects (fail-fast). `Promise.allSettled` waits for all and returns each result's status/value/reason, never rejecting — ideal when partial failures are acceptable.\n\n\n```js\nconst results = await Promise.allSettled([a(), b()])\n// [{status:'fulfilled',value}, {status:'rejected',reason}]\n```\n```\n",tech:"node",displayNumber:13,sortKey:1272},{id:"node-1273",number:14,title:"What does `Promise.race` do?",question:"What does `Promise.race` do?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`Promise.race` settles as soon as the first input promise settles (fulfilled or rejected), adopting its outcome. Common for timeouts.\n\n\n```js\nawait Promise.race([fetchData(), timeout(5000)])\n```\n```\n",tech:"node",displayNumber:14,sortKey:1273},{id:"node-1274",number:15,title:"What does `Promise.any` do?",question:"What does `Promise.any` do?",difficulty:"Advanced",category:"Event Loop & Async",answer:'`Promise.any` fulfills with the first fulfilled promise, ignoring rejections. It rejects only if all reject, throwing an `AggregateError`. Useful for "first success wins".\n\n\n```js\nconst fastest = await Promise.any([mirror1(), mirror2()])\n```\n```\n',tech:"node",displayNumber:15,sortKey:1274},{id:"node-1275",number:16,title:"How do you implement a timeout for a Promise?",question:"How do you implement a timeout for a Promise?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Race the operation against a promise that rejects after a delay, or use `AbortController` with `AbortSignal.timeout`. The latter can actually cancel the underlying operation.\n\n\n```js\nconst res = await fetch(url, { signal: AbortSignal.timeout(5000) })\n```\n```\n",tech:"node",displayNumber:16,sortKey:1275},{id:"node-1276",number:17,title:"What is an AbortController?",question:"What is an AbortController?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`AbortController` provides an `AbortSignal` to cancel async operations (fetch, streams, timers). Calling `abort()` triggers the signal, and supporting APIs reject with an `AbortError`.\n\n\n```js\nconst ac = new AbortController()\nsetTimeout(() => ac.abort(), 100)\nawait fetch(url, { signal: ac.signal })\n```\n```\n",tech:"node",displayNumber:17,sortKey:1276},{id:"node-1277",number:18,title:"What is the difference between `setTimeout` and `setInterval`?",question:"What is the difference between `setTimeout` and `setInterval`?",difficulty:"Basic",category:"Event Loop & Async",answer:"`setTimeout` runs a callback once after a delay. `setInterval` runs it repeatedly at a fixed interval until cleared. Long-running work in an interval can overlap; a self-rescheduling timeout avoids that.\n\n\n```js\nconst id = setInterval(tick, 1000)\nclearInterval(id)\n```\n```\n",tech:"node",displayNumber:18,sortKey:1277},{id:"node-1278",number:19,title:"Why might `setTimeout(fn, 1000)` fire later than 1000ms?",question:"Why might `setTimeout(fn, 1000)` fire later than 1000ms?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Timers specify a minimum delay, not exact timing. If the event loop is busy (blocking code, backed-up phases), the callback runs only when the loop reaches the timers phase after the delay elapses.\n\n\n```js\nsetTimeout(() => console.log('maybe >1000ms'), 1000)\nblockCpuFor2Seconds()\n```\n```\n",tech:"node",displayNumber:19,sortKey:1278},{id:"node-1279",number:20,title:"What is the difference between `setImmediate` and `setTimeout(fn, 0)` again inside I/O?",question:"What is the difference between `setImmediate` and `setTimeout(fn, 0)` again inside I/O?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Within an I/O callback (poll phase), `setImmediate` always runs before `setTimeout(fn,0)` because the check phase follows poll in the same iteration, whereas the timer must wait for the next timers phase.\n\n\n```js\nfs.readFile('f', () => {\n  setImmediate(() => console.log('1'))\n  setTimeout(() => console.log('2'), 0)\n})\n```\n```\n",tech:"node",displayNumber:20,sortKey:1279},{id:"node-1280",number:21,title:"What happens if you don't await a Promise?",question:"What happens if you don't await a Promise?",difficulty:"Intermediate",category:"Event Loop & Async",answer:'The code continues without waiting; the Promise runs in the background ("fire and forget"). If it rejects and nothing handles it, you get an unhandled rejection (which can crash the process in modern Node).\n\n\n```js\nsaveLog() // not awaited: errors become unhandled rejections\n```\n```\n',tech:"node",displayNumber:21,sortKey:1280},{id:"node-1281",number:22,title:"How do you convert a callback-based function to a Promise?",question:"How do you convert a callback-based function to a Promise?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Wrap it in `new Promise`, resolving/rejecting in the callback, or use `util.promisify` for error-first callbacks.\n\n\n```js\nconst wait = ms => new Promise(res => setTimeout(res, ms))\nawait wait(500)\n```\n```\n",tech:"node",displayNumber:22,sortKey:1281},{id:"node-1282",number:23,title:"What is the difference between concurrency and parallelism in Node?",question:"What is the difference between concurrency and parallelism in Node?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Concurrency is managing many tasks by interleaving them on one thread (Node's default for I/O). Parallelism is executing tasks simultaneously on multiple cores (worker threads/cluster). Node gives concurrency for free, parallelism only with extra threads/processes.\n\n\n```js\n// concurrency: many awaits interleaved on one thread\nawait Promise.all(urls.map(fetch))\n```\n```\n",tech:"node",displayNumber:23,sortKey:1282},{id:"node-1283",number:24,title:"How do you limit concurrency of async tasks?",question:"How do you limit concurrency of async tasks?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Process items in batches or use a concurrency-limited pool (e.g. `p-limit`) so you don't launch thousands of simultaneous operations that exhaust memory, sockets, or DB connections.\n\n\n```js\nconst limit = pLimit(5)\nawait Promise.all(items.map(i => limit(() => process(i))))\n```\n```\n",tech:"node",displayNumber:24,sortKey:1283},{id:"node-1284",number:25,title:"What is a race condition in async code and how do you prevent it?",question:"What is a race condition in async code and how do you prevent it?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A race condition occurs when overlapping async operations interleave in an unexpected order, corrupting shared state. Prevent it with locks/mutexes, atomic DB operations, queues, or by serializing critical sections.\n\n\n```js\n// atomic increment avoids read-modify-write race\nawait db.collection('c').updateOne({_id}, { $inc: { count: 1 } })\n```\n```\n",tech:"node",displayNumber:25,sortKey:1284},{id:"node-1285",number:26,title:"What does `Promise.resolve()` do?",question:"What does `Promise.resolve()` do?",difficulty:"Basic",category:"Event Loop & Async",answer:"It returns a Promise already fulfilled with the given value (or the same promise if you pass one). Useful for normalizing sync/async values and starting a microtask chain.\n\n\n```js\nPromise.resolve(5).then(console.log) // 5\n```\n```\n",tech:"node",displayNumber:26,sortKey:1285},{id:"node-1286",number:27,title:"What is promise chaining?",question:"What is promise chaining?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Each `.then` returns a new Promise, so you can chain sequential steps. Returning a value passes it on; returning a Promise waits for it; throwing routes to the next `.catch`.\n\n\n```js\nfetch(url).then(r => r.json()).then(d => d.id).catch(console.error)\n```\n```\n",tech:"node",displayNumber:27,sortKey:1286},{id:"node-1287",number:28,title:"What is the difference between returning and awaiting inside a promise chain?",question:"What is the difference between returning and awaiting inside a promise chain?",difficulty:"Advanced",category:"Event Loop & Async",answer:'In `.then`, returning a Promise makes the chain wait for it; not returning it "floats" the inner promise and its errors escape the chain. Always return inner promises.\n\n\n```js\n.then(() => save())   // waits & propagates errors\n.then(() => { save() }) // floats: errors lost\n```\n```\n',tech:"node",displayNumber:28,sortKey:1287},{id:"node-1288",number:29,title:"How does error handling work with async/await?",question:"How does error handling work with async/await?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Wrap awaited calls in try/catch; a rejected awaited Promise throws. You can also `.catch` on the async function's returned Promise. Uncaught rejections propagate up.\n\n\n```js\ntry { await risky() } catch (e) { handle(e) }\n```\n```\n",tech:"node",displayNumber:29,sortKey:1288},{id:"node-1289",number:30,title:"What is `finally` used for in Promises?",question:"What is `finally` used for in Promises?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`.finally(fn)` (or try/finally with await) runs cleanup regardless of success/failure, without altering the result — good for releasing resources like connections or spinners.\n\n\n```js\ntry { await work() } finally { spinner.stop() }\n```\n```\n",tech:"node",displayNumber:30,sortKey:1289},{id:"node-1290",number:31,title:"What is the difference between sequential and concurrent awaits?",question:"What is the difference between sequential and concurrent awaits?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Sequential awaits run one after another (total = sum of times). Concurrent starts them together and awaits with `Promise.all` (total ≈ max time). Use concurrency when tasks are independent.\n\n\n```js\n// slow: a then b\nconst a = await getA(); const b = await getB()\n// fast: both at once\nconst [x, y] = await Promise.all([getA(), getB()])\n```\n```\n",tech:"node",displayNumber:31,sortKey:1290},{id:"node-1291",number:32,title:"How do you handle errors in `Promise.all`?",question:"How do you handle errors in `Promise.all`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Promise.all` rejects on the first failure, discarding other results. Wrap each promise to catch individually, or use `Promise.allSettled` to inspect all outcomes.\n\n\n```js\nconst safe = ps.map(p => p.catch(e => ({ error: e })))\nconst results = await Promise.all(safe)\n```\n```\n",tech:"node",displayNumber:32,sortKey:1291},{id:"node-1292",number:33,title:"What is an async iterator?",question:"What is an async iterator?",difficulty:"Advanced",category:"Event Loop & Async",answer:"An async iterator produces values over time via `Symbol.asyncIterator`, consumed with `for await...of`. It underpins async generators and streams, letting you loop over asynchronously arriving data.\n\n\n```js\nfor await (const chunk of readableStream) process(chunk)\n```\n```\n",tech:"node",displayNumber:33,sortKey:1292},{id:"node-1293",number:34,title:"What is an async generator?",question:"What is an async generator?",difficulty:"Advanced",category:"Event Loop & Async",answer:"An `async function*` yields values asynchronously, awaiting between yields. It is ideal for streaming/paginated data with backpressure-friendly consumption via `for await`.\n\n\n```js\nasync function* pages(url) {\n  let next = url\n  while (next) { const p = await fetchPage(next); yield p.items; next = p.next }\n}\n```\n```\n",tech:"node",displayNumber:34,sortKey:1293},{id:"node-1294",number:35,title:"How does `for await...of` work?",question:"How does `for await...of` work?",difficulty:"Advanced",category:"Event Loop & Async",answer:"It iterates an async iterable, awaiting each `next()` result before the loop body runs. It reads streams/async generators sequentially with automatic backpressure.\n\n\n```js\nfor await (const line of readLines(file)) console.log(line)\n```\n```\n",tech:"node",displayNumber:35,sortKey:1294},{id:"node-1295",number:36,title:"What is the difference between `map` with async and `for await`?",question:"What is the difference between `map` with async and `for await`?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`array.map(async ...)` starts all operations concurrently and returns Promises (await with `Promise.all`). `for await` processes items sequentially. Choose based on whether you need concurrency or ordering/backpressure.\n\n\n```js\nawait Promise.all(items.map(async i => process(i))) // concurrent\nfor (const i of items) await process(i)              // sequential\n```\n```\n",tech:"node",displayNumber:36,sortKey:1295},{id:"node-1296",number:37,title:"Why should you avoid `await` inside a `forEach`?",question:"Why should you avoid `await` inside a `forEach`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Array.forEach` ignores the returned Promise, so it does not wait — the loop finishes before async work completes, and errors are lost. Use `for...of` with await or `Promise.all` with `map`.\n\n\n```js\nitems.forEach(async i => await save(i)) // does NOT wait\nfor (const i of items) await save(i)     // correct\n```\n```\n",tech:"node",displayNumber:37,sortKey:1296},{id:"node-1297",number:38,title:"What is a deferred/lazy Promise pattern?",question:"What is a deferred/lazy Promise pattern?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A deferred exposes `resolve`/`reject` outside the executor so external code can settle the Promise later — useful for bridging event-based APIs. `Promise.withResolvers()` standardizes this.\n\n\n```js\nconst { promise, resolve } = Promise.withResolvers()\nemitter.once('ready', resolve)\nawait promise\n```\n```\n",tech:"node",displayNumber:38,sortKey:1297},{id:"node-1298",number:39,title:"What happens to a thrown error in an async function?",question:"What happens to a thrown error in an async function?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"It becomes a rejected Promise returned by that async function. Callers must catch it via try/catch on await or `.catch`; otherwise it surfaces as an unhandled rejection.\n\n\n```js\nasync function f() { throw new Error('x') }\nf().catch(e => console.log(e.message)) // 'x'\n```\n```\n",tech:"node",displayNumber:39,sortKey:1298},{id:"node-1299",number:40,title:"What is the microtask queue drain guarantee?",question:"What is the microtask queue drain guarantee?",difficulty:"Experienced",category:"Event Loop & Async",answer:"After each macrotask (and after synchronous script completes), the engine drains the entire microtask queue before rendering/next macrotask. New microtasks scheduled during draining are also run in the same drain.\n\n\n```js\nPromise.resolve().then(() => Promise.resolve().then(() => console.log('still same drain')))\n```\n```\n",tech:"node",displayNumber:40,sortKey:1299},{id:"node-1300",number:41,title:"How do you make a synchronous-looking retry with backoff?",question:"How do you make a synchronous-looking retry with backoff?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Loop with await, catching failures, and sleep with exponential backoff (plus jitter) between attempts up to a max. This smooths transient failures without hammering a service.\n\n\n```js\nfor (let i = 0; i < 5; i++) {\n  try { return await call() } catch { await sleep(2 ** i * 100) }\n}\n```\n```\n",tech:"node",displayNumber:41,sortKey:1300},{id:"node-1301",number:42,title:"What is the thundering herd problem and how does jitter help?",question:"What is the thundering herd problem and how does jitter help?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Thundering herd is many clients retrying at the same instant after a failure, overwhelming the recovering service. Adding random jitter to backoff spreads retries over time, smoothing the load.\n\n\n```js\nconst delay = base * 2 ** attempt + Math.random() * 100 // jitter\n```\n```\n",tech:"node",displayNumber:42,sortKey:1301},{id:"node-1302",number:43,title:"How do timers behave with `unref()`?",question:"How do timers behave with `unref()`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Calling `.unref()` on a timer lets the process exit even if the timer is still pending — it no longer keeps the event loop alive. `ref()` restores the keep-alive behavior.\n\n\n```js\nconst t = setInterval(poll, 1000)\nt.unref() // won't prevent process exit\n```\n```\n",tech:"node",displayNumber:43,sortKey:1302},{id:"node-1303",number:44,title:"What keeps the Node process alive?",question:"What keeps the Node process alive?",difficulty:"Advanced",category:"Event Loop & Async",answer:'Pending "refs": active timers, open sockets/servers, pending I/O, and open handles keep the event loop running. When none remain, the loop empties and the process exits.\n\n\n```js\nsetTimeout(() => {}, 100000) // keeps process alive for 100s\n```\n```\n',tech:"node",displayNumber:44,sortKey:1303},{id:"node-1304",number:45,title:"What is `queueMicrotask`?",question:"What is `queueMicrotask`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`queueMicrotask(fn)` schedules a microtask directly, running after the current task before the next macrotask — like `Promise.resolve().then` but clearer and without creating a Promise.\n\n\n```js\nqueueMicrotask(() => console.log('runs before timers'))\n```\n```\n",tech:"node",displayNumber:45,sortKey:1304},{id:"node-1305",number:46,title:"How do you debounce an async function?",question:"How do you debounce an async function?",difficulty:"Advanced",category:"Event Loop & Async",answer:`Delay execution until calls stop for a quiet period by clearing and resetting a timer each call. For async, also cancel or ignore stale in-flight results.


\`\`\`js
function debounce(fn, ms) {
  let t
  return (...a) => { clearTimeout(t); t = setTimeout(() => fn(...a), ms) }
}
\`\`\`
\`\`\`
`,tech:"node",displayNumber:46,sortKey:1305},{id:"node-1306",number:47,title:"How do you throttle async calls?",question:"How do you throttle async calls?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Throttling limits execution to at most once per interval regardless of call frequency, using a timestamp or a leading/trailing timer. Good for rate-limiting expensive operations.\n\n\n```js\nlet last = 0\nconst throttled = fn => (...a) => { const n = Date.now(); if (n - last > 1000) { last = n; fn(...a) } }\n```\n```\n",tech:"node",displayNumber:47,sortKey:1306},{id:"node-1307",number:48,title:"What is memoization of async functions?",question:"What is memoization of async functions?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Cache the returned Promise (not just the value) keyed by args, so concurrent identical calls share one in-flight request (request coalescing). Evict on error or TTL.\n\n\n```js\nconst cache = new Map()\nconst get = k => cache.get(k) ?? cache.set(k, fetchData(k)).get(k)\n```\n```\n",tech:"node",displayNumber:48,sortKey:1307},{id:"node-1308",number:49,title:"What is the difference between eager and lazy evaluation of Promises?",question:"What is the difference between eager and lazy evaluation of Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Promises are eager: the executor runs immediately when created, even before `await`. To defer work, wrap it in a function (thunk) and call it when needed.\n\n\n```js\nconst eager = fetch(url)        // request already sent\nconst lazy = () => fetch(url)   // sent only when called\n```\n```\n",tech:"node",displayNumber:49,sortKey:1308},{id:"node-1309",number:50,title:"How do you detect and fix event loop lag?",question:"How do you detect and fix event loop lag?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Measure lag with `perf_hooks.monitorEventLoopDelay` or a timer-drift check. High lag means blocking (CPU-bound sync code); fix by offloading to worker threads, chunking work, or streaming.\n\n\n```js\nconst h = require('perf_hooks').monitorEventLoopDelay(); h.enable()\nsetInterval(() => console.log(h.mean / 1e6, 'ms lag'), 1000)\n```\n```\n",tech:"node",displayNumber:50,sortKey:1309},{id:"node-1310",number:51,title:"What is backpressure in async data flow?",question:"What is backpressure in async data flow?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Backpressure is signaling a fast producer to slow down when a slow consumer can't keep up, preventing unbounded memory growth. Streams and async iterators implement it; `for await` respects it naturally.\n\n\n```js\nfor await (const chunk of source) await slowConsumer(chunk) // paced\n```\n```\n",tech:"node",displayNumber:51,sortKey:1310},{id:"node-1311",number:52,title:"Why is mixing callbacks and Promises for the same operation dangerous?",question:"Why is mixing callbacks and Promises for the same operation dangerous?",difficulty:"Advanced",category:"Event Loop & Async",answer:"A function should not both call a callback and return a Promise, or it may resolve twice / double-run logic. Choose one contract to avoid subtle double-execution bugs.\n\n\n```js\n// bad: resolves via callback AND promise\nfunction f(cb) { return new Promise(res => { cb(); res() }) }\n```\n```\n",tech:"node",displayNumber:52,sortKey:1311},{id:"node-1312",number:53,title:"What is a semaphore and when do you use it in Node?",question:"What is a semaphore and when do you use it in Node?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A semaphore limits how many async operations run concurrently by handing out a fixed number of permits. Use it to cap DB connections, file handles, or outbound requests.\n\n\n```js\nconst sem = new Semaphore(10)\nawait sem.acquire(); try { await doWork() } finally { sem.release() }\n```\n```\n",tech:"node",displayNumber:53,sortKey:1312},{id:"node-1313",number:54,title:"How do you cancel an in-flight async operation?",question:"How do you cancel an in-flight async operation?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Pass an `AbortSignal` to APIs that support it and call `controller.abort()`. For custom code, check `signal.aborted` at checkpoints and reject with an `AbortError`.\n\n\n```js\nif (signal.aborted) throw new DOMException('Aborted', 'AbortError')\n```\n```\n",tech:"node",displayNumber:54,sortKey:1313},{id:"node-1314",number:55,title:"What is the difference between `process.nextTick` recursion and `setImmediate` recursion?",question:"What is the difference between `process.nextTick` recursion and `setImmediate` recursion?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Recursive `nextTick` blocks the loop (microtasks never yield to I/O). Recursive `setImmediate` yields between iterations, letting I/O and timers run — safer for chunking long work.\n\n\n```js\nfunction chunk() { doPart(); if (more) setImmediate(chunk) } // non-blocking\n```\n```\n",tech:"node",displayNumber:55,sortKey:1314},{id:"node-1315",number:56,title:"How do you break a long CPU task without blocking the loop?",question:"How do you break a long CPU task without blocking the loop?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Split the work into slices and yield to the loop between slices via `setImmediate`/`await new Promise(setImmediate)`, or move it entirely to a worker thread for true parallelism.\n\n\n```js\nfor (let i = 0; i < n; i += 1000) { processSlice(i); await new Promise(setImmediate) }\n```\n```\n",tech:"node",displayNumber:56,sortKey:1315},{id:"node-1316",number:57,title:`What is a Promise's "resolution" vs "fulfillment"?`,question:`What is a Promise's "resolution" vs "fulfillment"?`,difficulty:"Experienced",category:"Event Loop & Async",answer:"Resolving a Promise locks in its fate — which may be fulfillment with a value or, if resolved with another thenable, following that thenable (possibly ending in rejection). Fulfillment specifically means settled with a value.\n\n\n```js\nnew Promise(res => res(Promise.reject('x'))).catch(console.log) // 'x'\n```\n```\n",tech:"node",displayNumber:57,sortKey:1316},{id:"node-1317",number:58,title:"What happens if you `await` a non-Promise value?",question:"What happens if you `await` a non-Promise value?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`await` wraps non-thenables in `Promise.resolve`, so it returns the value on the next microtask. It still yields to the microtask queue even for plain values.\n\n\n```js\nconst x = await 5 // x === 5, but resolves asynchronously\n```\n```\n",tech:"node",displayNumber:58,sortKey:1317},{id:"node-1318",number:59,title:"How do you implement a simple async queue?",question:"How do you implement a simple async queue?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Keep a promise chain; each enqueued task appends `.then(task)` so tasks run strictly one after another, serializing access to a shared resource.\n\n\n```js\nlet tail = Promise.resolve()\nconst enqueue = task => (tail = tail.then(task))\n```\n```\n",tech:"node",displayNumber:59,sortKey:1318},{id:"node-1319",number:60,title:"What is the difference between `Promise.all` fail-fast and gathering all errors?",question:"What is the difference between `Promise.all` fail-fast and gathering all errors?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Fail-fast (`Promise.all`) stops at the first rejection — good when any failure invalidates the batch. Gathering (`allSettled`) collects every outcome — good for reporting which items succeeded/failed.\n\n\n```js\nconst outcomes = await Promise.allSettled(jobs)\nconst failed = outcomes.filter(o => o.status === 'rejected')\n```\n```\n",tech:"node",displayNumber:60,sortKey:1319},{id:"node-1320",number:61,title:"How does async context propagate across awaits?",question:"How does async context propagate across awaits?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Local variables persist across `await` because the function's state is preserved. For cross-call context (like request IDs), use `AsyncLocalStorage`, which maintains context through async chains.\n\n\n```js\nals.run({ reqId }, async () => { await handler() /* als.getStore().reqId available */ })\n```\n```\n",tech:"node",displayNumber:61,sortKey:1320},{id:"node-1321",number:62,title:"What is `AsyncLocalStorage`?",question:"What is `AsyncLocalStorage`?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`AsyncLocalStorage` (from `async_hooks`) stores data that stays associated with an async execution chain, like thread-local storage. It is commonly used for per-request logging context without passing it everywhere.\n\n\n```js\nconst als = new AsyncLocalStorage()\nals.run(store, () => { /* store available in all awaited callees */ })\n```\n```\n",tech:"node",displayNumber:62,sortKey:1321},{id:"node-1322",number:63,title:"What are async hooks?",question:"What are async hooks?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`async_hooks` lets you track the lifecycle of async resources (init, before, after, destroy). It powers context propagation and diagnostics but has performance overhead, so use sparingly in production.\n\n\n```js\nconst hook = require('async_hooks').createHook({ init() {} })\nhook.enable()\n```\n```\n",tech:"node",displayNumber:63,sortKey:1322},{id:"node-1323",number:64,title:"Why is `Promise.all` order preserved even if tasks finish out of order?",question:"Why is `Promise.all` order preserved even if tasks finish out of order?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Promise.all` returns results in input order regardless of completion order, because it stores each result at its index. Only timing differs, not the result array's ordering.\n\n\n```js\nawait Promise.all([slow(), fast()]) // [slowResult, fastResult]\n```\n```\n",tech:"node",displayNumber:64,sortKey:1323},{id:"node-1324",number:65,title:"What is a leaked Promise and how does it happen?",question:"What is a leaked Promise and how does it happen?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A leaked Promise is one that never settles (e.g. awaiting an event that never fires), leaving the awaiting code stuck and holding resources. Add timeouts/AbortSignals to guarantee settlement.\n\n\n```js\nawait new Promise(() => {}) // never resolves -> leak\n```\n```\n",tech:"node",displayNumber:65,sortKey:1324},{id:"node-1325",number:66,title:"How do you test asynchronous code?",question:"How do you test asynchronous code?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Return or await the Promise in the test so the runner waits for completion, use fake timers to control delays, and assert on resolved values or rejections.\n\n\n```js\ntest('resolves', async () => { await expect(fetchUser(1)).resolves.toHaveProperty('id') })\n```\n```\n",tech:"node",displayNumber:66,sortKey:1325},{id:"node-1326",number:67,title:"What is the difference between `setTimeout` in Node and the browser?",question:"What is the difference between `setTimeout` in Node and the browser?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Node's `setTimeout` returns a `Timeout` object (with `ref`/`unref`), not a number, and integrates with libuv's timers phase. The browser returns a numeric id and clamps nested timeouts to 4ms.\n\n\n```js\nconst t = setTimeout(fn, 100) // Timeout object in Node\nt.unref()\n```\n```\n",tech:"node",displayNumber:67,sortKey:1326},{id:"node-1327",number:68,title:"How do you flatten nested async results cleanly?",question:"How do you flatten nested async results cleanly?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Use await to avoid nested `.then`, and `Promise.all` with `flat`/`flatMap` to combine arrays of async results into one list.\n\n\n```js\nconst nested = await Promise.all(groups.map(g => fetchItems(g)))\nconst all = nested.flat()\n```\n```\n",tech:"node",displayNumber:68,sortKey:1327},{id:"node-1328",number:69,title:"What is the cost of creating many Promises?",question:"What is the cost of creating many Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Each Promise allocates memory and schedules microtasks; creating millions at once causes GC pressure and memory spikes. Batch/stream work and limit concurrency instead of materializing everything up front.\n\n\n```js\n// avoid: urls.map(fetch) for 1M urls; use a bounded pool\n```\n```\n",tech:"node",displayNumber:69,sortKey:1328},{id:"node-1329",number:70,title:"How do you ensure ordered processing of a stream of async events?",question:"How do you ensure ordered processing of a stream of async events?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Serialize with an async queue or `for await`, or tag events with sequence numbers and reorder. Concurrency without ordering guarantees can interleave results unpredictably.\n\n\n```js\nfor await (const event of eventStream) await handleInOrder(event)\n```\n```\n",tech:"node",displayNumber:70,sortKey:1329},{id:"node-1330",number:1,title:"What is a stream in Node.js?",question:"What is a stream in Node.js?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"A stream is an abstraction for reading or writing data piece by piece (chunks) instead of all at once. It enables processing large data with constant, low memory and supports composition via piping.\n\n\n```js\nfs.createReadStream('big.txt').pipe(process.stdout)\n```\n```\n",tech:"node",displayNumber:1,sortKey:1330},{id:"node-1331",number:2,title:"What are the four types of streams?",question:"What are the four types of streams?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"Readable (source, e.g. file read), Writable (sink, e.g. file write), Duplex (both, e.g. TCP socket), and Transform (duplex that modifies data, e.g. gzip).\n\n\n```js\nconst gzip = require('zlib').createGzip() // Transform stream\n```\n```\n",tech:"node",displayNumber:2,sortKey:1331},{id:"node-1332",number:3,title:"What is the advantage of streams over reading a whole file?",question:"What is the advantage of streams over reading a whole file?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"Streams use constant memory regardless of file size and start producing output immediately (lower latency), whereas buffering the whole file consumes memory proportional to its size and delays processing until fully loaded.\n\n\n```js\nfs.createReadStream('10gb.log').pipe(res) // won't blow up memory\n```\n```\n",tech:"node",displayNumber:3,sortKey:1332},{id:"node-1333",number:4,title:"What is `pipe()`?",question:"What is `pipe()`?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`pipe()` connects a readable stream to a writable one, automatically forwarding data and handling backpressure. It returns the destination so pipes can be chained.\n\n\n```js\nreadable.pipe(transform).pipe(writable)\n```\n```\n",tech:"node",displayNumber:4,sortKey:1333},{id:"node-1334",number:5,title:"What is backpressure in streams?",question:"What is backpressure in streams?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Backpressure occurs when a writable can't consume data as fast as a readable produces it. `write()` returns `false` when its buffer is full; the readable should pause until the `drain` event. `pipe` handles this automatically.\n\n\n```js\nif (!dest.write(chunk)) src.pause()\ndest.on('drain', () => src.resume())\n```\n```\n",tech:"node",displayNumber:5,sortKey:1334},{id:"node-1335",number:6,title:"What are flowing and paused modes of readable streams?",question:"What are flowing and paused modes of readable streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"In flowing mode data is pushed via `data` events as fast as possible; in paused mode you pull via `read()`. Adding a `data` listener or calling `pipe`/`resume` switches to flowing; `pause()` switches back.\n\n\n```js\nstream.on('data', chunk => {}) // flowing mode\n```\n```\n",tech:"node",displayNumber:6,sortKey:1335},{id:"node-1336",number:7,title:"What is `highWaterMark`?",question:"What is `highWaterMark`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`highWaterMark` is the internal buffer size threshold (bytes for binary, count for object mode). It controls when a stream signals backpressure and how much it buffers before pausing the source.\n\n\n```js\nfs.createReadStream('f', { highWaterMark: 64 * 1024 }) // 64KB chunks\n```\n```\n",tech:"node",displayNumber:7,sortKey:1336},{id:"node-1337",number:8,title:"What is object mode in streams?",question:"What is object mode in streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"By default streams handle strings/Buffers. In object mode (`objectMode: true`) they can push/consume arbitrary JS objects, useful for record pipelines (e.g. DB rows, parsed CSV lines).\n\n\n```js\nnew Readable({ objectMode: true, read() { this.push({ id: 1 }) } })\n```\n```\n",tech:"node",displayNumber:8,sortKey:1337},{id:"node-1338",number:9,title:"How do you create a custom Readable stream?",question:"How do you create a custom Readable stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Extend `Readable` (or pass options to its constructor) and implement `_read()`, calling `this.push(chunk)` to emit data and `this.push(null)` to signal EOF.\n\n\n```js\nconst { Readable } = require('stream')\nconst r = new Readable({ read() { this.push('hi'); this.push(null) } })\n```\n```\n",tech:"node",displayNumber:9,sortKey:1338},{id:"node-1339",number:10,title:"How do you create a custom Writable stream?",question:"How do you create a custom Writable stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Implement `_write(chunk, encoding, callback)`, doing the work and calling `callback()` when ready for more (or with an error). The callback drives backpressure.\n\n\n```js\nconst { Writable } = require('stream')\nnew Writable({ write(chunk, enc, cb) { console.log(chunk.toString()); cb() } })\n```\n```\n",tech:"node",displayNumber:10,sortKey:1339},{id:"node-1340",number:11,title:"How do you create a Transform stream?",question:"How do you create a Transform stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Implement `_transform(chunk, enc, callback)` to process input and `push` output, calling `callback` when done. Optionally `_flush` for trailing data.\n\n\n```js\nconst { Transform } = require('stream')\nnew Transform({ transform(c, e, cb) { cb(null, c.toString().toUpperCase()) } })\n```\n```\n",tech:"node",displayNumber:11,sortKey:1340},{id:"node-1341",number:12,title:"What is `stream.pipeline()` and why prefer it over `pipe`?",question:"What is `stream.pipeline()` and why prefer it over `pipe`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`pipeline` chains streams and propagates errors and cleanup automatically; plain `pipe` does not forward errors or destroy streams on failure, risking leaks. Always use `pipeline` for multi-stage pipes.\n\n\n```js\nconst { pipeline } = require('stream/promises')\nawait pipeline(fs.createReadStream('a'), gzip, fs.createWriteStream('a.gz'))\n```\n```\n",tech:"node",displayNumber:12,sortKey:1341},{id:"node-1342",number:13,title:"Why is error handling important with `pipe`?",question:"Why is error handling important with `pipe`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`pipe` doesn't destroy the source when the destination errors (or vice versa), leaking file descriptors/memory. You must attach `error` handlers on every stream, or use `pipeline` which handles it.\n\n\n```js\nsrc.on('error', onErr); dest.on('error', onErr) // or use pipeline\n```\n```\n",tech:"node",displayNumber:13,sortKey:1342},{id:"node-1343",number:14,title:"How do you convert a stream to a string/buffer?",question:"How do you convert a stream to a string/buffer?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Collect chunks and concatenate, or use the helper `stream/consumers` (`text`, `buffer`, `json`) in modern Node.\n\n\n```js\nconst { text } = require('stream/consumers')\nconst body = await text(req)\n```\n```\n",tech:"node",displayNumber:14,sortKey:1343},{id:"node-1344",number:15,title:"What is the difference between `stream.Readable.from()` and manual streams?",question:"What is the difference between `stream.Readable.from()` and manual streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`Readable.from(iterable)` builds a readable stream from any (async) iterable/generator without implementing `_read`, greatly simplifying custom sources.\n\n\n```js\nconst r = Readable.from(async function* () { yield 'a'; yield 'b' }())\n```\n```\n",tech:"node",displayNumber:15,sortKey:1344},{id:"node-1345",number:16,title:"What is a Buffer and how is it different from an array?",question:"What is a Buffer and how is it different from an array?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"A Buffer is a fixed-size raw memory region for bytes, stored outside the V8 heap. Unlike arrays it can't grow, holds only 0–255 byte values, and is optimized for binary/encoding operations.\n\n\n```js\nconst b = Buffer.from([104, 105])\nconsole.log(b.toString()) // 'hi'\n```\n```\n",tech:"node",displayNumber:16,sortKey:1345},{id:"node-1346",number:17,title:"How do you convert between Buffers and strings with encodings?",question:"How do you convert between Buffers and strings with encodings?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`Buffer.from(str, encoding)` encodes; `buf.toString(encoding)` decodes. Supported encodings include `utf8`, `hex`, `base64`, `ascii`, `latin1`.\n\n\n```js\nBuffer.from('hi').toString('base64') // 'aGk='\nBuffer.from('aGk=', 'base64').toString() // 'hi'\n```\n```\n",tech:"node",displayNumber:17,sortKey:1346},{id:"node-1347",number:18,title:"What is the risk of `Buffer.allocUnsafe`?",question:"What is the risk of `Buffer.allocUnsafe`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"It returns uninitialized memory that may contain old data (including secrets from prior allocations). It's faster but must be fully overwritten before use or exposure. Prefer `Buffer.alloc` when in doubt.\n\n\n```js\nconst b = Buffer.allocUnsafe(16); b.fill(0) // sanitize before use\n```\n```\n",tech:"node",displayNumber:18,sortKey:1347},{id:"node-1348",number:19,title:"How do you handle a chunk split across multi-byte characters?",question:"How do you handle a chunk split across multi-byte characters?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"A UTF-8 character may straddle two chunks; decoding each chunk independently corrupts it. Use `string_decoder.StringDecoder`, which buffers incomplete multi-byte sequences until complete.\n\n\n```js\nconst dec = new (require('string_decoder').StringDecoder)('utf8')\nstream.on('data', c => output += dec.write(c))\n```\n```\n",tech:"node",displayNumber:19,sortKey:1348},{id:"node-1349",number:20,title:"What is `Buffer.byteLength` vs `String.length`?",question:"What is `Buffer.byteLength` vs `String.length`?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`String.length` counts UTF-16 code units; `Buffer.byteLength(str)` counts actual bytes in a given encoding. They differ for non-ASCII text — important for Content-Length headers.\n\n\n```js\n'é'.length              // 1\nBuffer.byteLength('é')  // 2 (UTF-8)\n```\n```\n",tech:"node",displayNumber:20,sortKey:1349},{id:"node-1350",number:21,title:"What is the EventEmitter?",question:"What is the EventEmitter?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`EventEmitter` is the core class for the observer pattern in Node. Objects emit named events and listeners subscribe via `on`. Streams, servers, and many core APIs are EventEmitters.\n\n\n```js\nconst e = new (require('events'))()\ne.on('greet', name => console.log('Hi ' + name))\ne.emit('greet', 'Ada')\n```\n```\n",tech:"node",displayNumber:21,sortKey:1350},{id:"node-1351",number:22,title:"What is the difference between `on` and `once`?",question:"What is the difference between `on` and `once`?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`on` registers a listener that fires every time the event is emitted; `once` fires only the first time then auto-removes itself.\n\n\n```js\nserver.once('listening', () => console.log('ready')) // one-time\n```\n```\n",tech:"node",displayNumber:22,sortKey:1351},{id:"node-1352",number:23,title:"How do you remove event listeners?",question:"How do you remove event listeners?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Use `removeListener(event, fn)`/`off` with the same function reference, or `removeAllListeners(event)`. You must keep a reference to the original function to remove it.\n\n\n```js\nconst fn = () => {}\ne.on('x', fn); e.off('x', fn)\n```\n```\n",tech:"node",displayNumber:23,sortKey:1352},{id:"node-1353",number:24,title:"What is the max listeners warning?",question:"What is the max listeners warning?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"By default an emitter warns after 10 listeners for one event (a leak heuristic). Genuine cases can raise it with `setMaxListeners(n)`; unexpected growth usually signals a listener leak.\n\n\n```js\nemitter.setMaxListeners(20)\n```\n```\n",tech:"node",displayNumber:24,sortKey:1353},{id:"node-1354",number:25,title:"What is an EventEmitter memory leak?",question:"What is an EventEmitter memory leak?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Repeatedly adding listeners without removing them (e.g. per request) accumulates them, retaining closures/memory and eventually degrading performance. Fix by using `once`, removing listeners, or `AbortSignal`.\n\n\n```js\nemitter.on('data', handler, { signal: controller.signal }) // auto-cleanup\n```\n```\n",tech:"node",displayNumber:25,sortKey:1354},{id:"node-1355",number:26,title:"How do you emit an error event correctly?",question:"How do you emit an error event correctly?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Emit `'error'` with an `Error` object. If no listener is registered for `'error'`, Node throws and crashes the process — so always attach an error listener on emitters/streams.\n\n\n```js\nemitter.on('error', e => log(e))\nemitter.emit('error', new Error('boom'))\n```\n```\n",tech:"node",displayNumber:26,sortKey:1355},{id:"node-1356",number:27,title:"What is the order of listener invocation?",question:"What is the order of listener invocation?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Listeners for an event run synchronously in registration order. `prependListener` adds to the front. Emitting is synchronous — all listeners run before `emit` returns.\n\n\n```js\ne.on('x', () => console.log(1)); e.prependListener('x', () => console.log(0))\ne.emit('x') // 0 then 1\n```\n```\n",tech:"node",displayNumber:27,sortKey:1356},{id:"node-1357",number:28,title:"Is `emit` synchronous or asynchronous?",question:"Is `emit` synchronous or asynchronous?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`emit` is synchronous: it calls listeners immediately and in order. To defer, wrap listener bodies in `setImmediate`/`queueMicrotask`. This sync nature can surprise developers expecting async behavior.\n\n\n```js\ne.emit('x'); console.log('after') // all 'x' listeners ran before 'after'\n```\n```\n",tech:"node",displayNumber:28,sortKey:1357},{id:"node-1358",number:29,title:"How do you wait for an event as a Promise?",question:"How do you wait for an event as a Promise?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Use `events.once(emitter, name)` which returns a Promise resolving with the event args, or `events.on` for an async iterator of events.\n\n\n```js\nconst { once } = require('events')\nconst [data] = await once(stream, 'data')\n```\n```\n",tech:"node",displayNumber:29,sortKey:1358},{id:"node-1359",number:30,title:"How do you iterate events with `for await`?",question:"How do you iterate events with `for await`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`events.on(emitter, name)` returns an async iterator yielding event arguments, letting you consume a stream of events sequentially with backpressure.\n\n\n```js\nfor await (const [msg] of events.on(socket, 'message')) handle(msg)\n```\n```\n",tech:"node",displayNumber:30,sortKey:1359},{id:"node-1360",number:31,title:"What is the `close` event on a stream?",question:"What is the `close` event on a stream?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`close` fires when a stream and its underlying resource (file descriptor) are fully closed. `end` (readable) or `finish` (writable) fire earlier when data is done but before resources are released.\n\n\n```js\nws.on('finish', () => console.log('data flushed'))\nws.on('close', () => console.log('fd closed'))\n```\n```\n",tech:"node",displayNumber:31,sortKey:1360},{id:"node-1361",number:32,title:"What is the difference between `end` and `finish` events?",question:"What is the difference between `end` and `finish` events?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`end` fires on a readable when there is no more data to consume. `finish` fires on a writable after `end()` is called and all data is flushed to the underlying system.\n\n\n```js\nreadable.on('end', () => {})\nwritable.on('finish', () => {})\n```\n```\n",tech:"node",displayNumber:32,sortKey:1361},{id:"node-1362",number:33,title:"How do you handle large file uploads with streams?",question:"How do you handle large file uploads with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pipe the incoming request stream directly to disk or storage instead of buffering it in memory, using `pipeline` for error handling and enforcing size limits to avoid abuse.\n\n\n```js\nawait pipeline(req, fs.createWriteStream('/uploads/file'))\n```\n```\n",tech:"node",displayNumber:33,sortKey:1362},{id:"node-1363",number:34,title:"How do you compress data on the fly with streams?",question:"How do you compress data on the fly with streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Insert a `zlib` transform (gzip/brotli) between source and destination in a pipeline, compressing chunk by chunk without loading the whole payload.\n\n\n```js\nawait pipeline(fs.createReadStream('a'), zlib.createBrotliCompress(), fs.createWriteStream('a.br'))\n```\n```\n",tech:"node",displayNumber:34,sortKey:1363},{id:"node-1364",number:35,title:"What is `stream.finished()`?",question:"What is `stream.finished()`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`stream.finished(stream, cb)` invokes the callback when a stream is no longer readable/writable or errors, giving a reliable single completion signal across stream types.\n\n\n```js\nconst { finished } = require('stream/promises')\nawait finished(readable)\n```\n```\n",tech:"node",displayNumber:35,sortKey:1364},{id:"node-1365",number:36,title:"How do you tee/duplicate a stream to two destinations?",question:"How do you tee/duplicate a stream to two destinations?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use `stream.PassThrough` or pipe the source to multiple writables (via `pipe` twice or a fan-out transform). Note both destinations share backpressure — the slowest paces the source.\n\n\n```js\nconst pass = new PassThrough()\nsrc.pipe(pass); pass.pipe(fileA); pass.pipe(fileB)\n```\n```\n",tech:"node",displayNumber:36,sortKey:1365},{id:"node-1366",number:37,title:"What is a PassThrough stream?",question:"What is a PassThrough stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`PassThrough` is a trivial Transform that forwards input unchanged. It is handy as a placeholder, for measuring throughput, or for connecting/adapting pipelines.\n\n\n```js\nconst pt = new (require('stream').PassThrough)()\n```\n```\n",tech:"node",displayNumber:37,sortKey:1366},{id:"node-1367",number:38,title:"How do you measure throughput of a stream?",question:"How do you measure throughput of a stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Insert a Transform (or PassThrough with a `data` listener) that counts bytes over time to compute bytes/sec, then forwards chunks unchanged.\n\n\n```js\nlet bytes = 0\nstream.on('data', c => bytes += c.length)\n```\n```\n",tech:"node",displayNumber:38,sortKey:1367},{id:"node-1368",number:39,title:"What is the difference between `write()` returning true/false?",question:"What is the difference between `write()` returning true/false?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`write()` returns `true` if the chunk fit in the buffer (keep writing) and `false` if the buffer exceeded `highWaterMark` (stop and wait for `drain`). Ignoring `false` causes unbounded memory growth.\n\n\n```js\nif (!ws.write(chunk)) await once(ws, 'drain')\n```\n```\n",tech:"node",displayNumber:39,sortKey:1368},{id:"node-1369",number:40,title:"How do you convert an async generator into a readable stream?",question:"How do you convert an async generator into a readable stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pass the async generator to `Readable.from`, which pulls values on demand and applies backpressure automatically.\n\n\n```js\nconst stream = Readable.from(rowsGenerator())\nstream.pipe(res)\n```\n```\n",tech:"node",displayNumber:40,sortKey:1369},{id:"node-1370",number:41,title:"What is the `data`, `end`, `error` lifecycle of a readable?",question:"What is the `data`, `end`, `error` lifecycle of a readable?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`data` fires for each chunk (flowing mode), `end` when all data is consumed, and `error` on failure. Always handle `error`; missing it can crash the process.\n\n\n```js\nr.on('data', d => {}).on('end', () => {}).on('error', e => {})\n```\n```\n",tech:"node",displayNumber:41,sortKey:1370},{id:"node-1371",number:42,title:"How do you enforce a max size when buffering a stream?",question:"How do you enforce a max size when buffering a stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Track accumulated bytes as chunks arrive and destroy the stream with an error once a limit is exceeded, preventing memory-exhaustion attacks (e.g. huge request bodies).\n\n\n```js\nlet size = 0\nreq.on('data', c => { size += c.length; if (size > MAX) req.destroy(new Error('too large')) })\n```\n```\n",tech:"node",displayNumber:42,sortKey:1371},{id:"node-1372",number:43,title:"What is the difference between Duplex and Transform streams?",question:"What is the difference between Duplex and Transform streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"A Duplex has independent readable and writable sides (e.g. a socket: what you read isn't what you write). A Transform's output is a function of its input (e.g. compression) — the two sides are linked.\n\n\n```js\n// TCP socket = Duplex; zlib.createGzip() = Transform\n```\n```\n",tech:"node",displayNumber:43,sortKey:1372},{id:"node-1373",number:44,title:"How do you gracefully destroy a stream?",question:"How do you gracefully destroy a stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Call `stream.destroy(err?)` to close it and release resources, optionally emitting an error. It stops further reads/writes and triggers cleanup. Prefer this over leaving streams open.\n\n\n```js\nreadable.destroy() // release fd, stop emitting\n```\n```\n",tech:"node",displayNumber:44,sortKey:1373},{id:"node-1374",number:45,title:"What is the `readableEnded` / `writableFinished` property?",question:"What is the `readableEnded` / `writableFinished` property?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"These booleans report whether the stream has finished reading/writing, useful for state checks without relying solely on events.\n\n\n```js\nif (ws.writableFinished) console.log('all written')\n```\n```\n",tech:"node",displayNumber:45,sortKey:1374},{id:"node-1375",number:46,title:"How do you pipe with transformation and error handling in one call?",question:"How do you pipe with transformation and error handling in one call?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use the promise-based `pipeline` with all stages; it awaits completion and rejects on any stage error, cleaning up every stream.\n\n\n```js\nawait pipeline(source, decompress, parse, writeToDb)\n```\n```\n",tech:"node",displayNumber:46,sortKey:1375},{id:"node-1376",number:47,title:"What is chunked transfer and how do streams relate?",question:"What is chunked transfer and how do streams relate?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"HTTP chunked transfer sends a response in pieces without a known total length. Node streams map naturally onto this: piping a readable to the response emits chunks with `Transfer-Encoding: chunked`.\n\n\n```js\nfs.createReadStream('video.mp4').pipe(res) // chunked automatically\n```\n```\n",tech:"node",displayNumber:47,sortKey:1376},{id:"node-1377",number:48,title:"How do you implement range requests (partial content) with streams?",question:"How do you implement range requests (partial content) with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Parse the `Range` header, respond with 206 and `Content-Range`, and stream only the requested byte range via `createReadStream(path, { start, end })`.\n\n\n```js\nfs.createReadStream(file, { start, end }).pipe(res.writeHead(206, headers))\n```\n```\n",tech:"node",displayNumber:48,sortKey:1377},{id:"node-1378",number:49,title:"What is the `cork`/`uncork` mechanism?",question:"What is the `cork`/`uncork` mechanism?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`cork()` buffers multiple `write()` calls and flushes them together on `uncork()`, reducing syscalls when writing many small chunks — a batching optimization.\n\n\n```js\nsocket.cork(); socket.write('a'); socket.write('b'); socket.uncork()\n```\n```\n",tech:"node",displayNumber:49,sortKey:1378},{id:"node-1379",number:50,title:"How do you compose reusable stream transforms?",question:"How do you compose reusable stream transforms?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Build small Transform factories and combine them in a `pipeline`, or use `stream.compose()` (modern Node) to merge several into one reusable stream.\n\n\n```js\nconst combined = compose(parseJson, validate, enrich)\nawait pipeline(source, combined, sink)\n```\n```\n",tech:"node",displayNumber:50,sortKey:1379},{id:"node-1380",number:51,title:"What is the difference between `Buffer.copy` and `Buffer.slice`?",question:"What is the difference between `Buffer.copy` and `Buffer.slice`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`slice`/`subarray` returns a view sharing the same memory (mutations affect the original). `copy` writes bytes into a separate target buffer (independent memory). Choose based on whether you need isolation.\n\n\n```js\nconst view = buf.subarray(0, 4) // shares memory\nbuf.copy(target)                 // independent copy\n```\n```\n",tech:"node",displayNumber:51,sortKey:1380},{id:"node-1381",number:52,title:"How do you read a binary file structure with Buffers?",question:"How do you read a binary file structure with Buffers?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use typed read methods (`readUInt32BE`, `readInt16LE`, etc.) at byte offsets to parse headers/fields, respecting endianness.\n\n\n```js\nconst magic = buf.readUInt32BE(0)\nconst width = buf.readUInt16LE(4)\n```\n```\n",tech:"node",displayNumber:52,sortKey:1381},{id:"node-1382",number:53,title:"What is endianness and why does it matter for Buffers?",question:"What is endianness and why does it matter for Buffers?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Endianness is byte order for multi-byte numbers: big-endian (most significant first) vs little-endian. Reading with the wrong order yields garbage, so binary protocols specify it and you use the matching `BE`/`LE` methods.\n\n\n```js\nbuf.readUInt16BE(0) // big-endian\nbuf.readUInt16LE(0) // little-endian\n```\n```\n",tech:"node",displayNumber:53,sortKey:1382},{id:"node-1383",number:54,title:"How do you handle newline-delimited data from a stream?",question:"How do you handle newline-delimited data from a stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Use `readline.createInterface` over the stream, or a Transform that buffers until it sees `\\n`, emitting complete lines. Naive chunk splitting breaks lines that span chunks.\n\n\n```js\nconst rl = readline.createInterface({ input: fs.createReadStream('log') })\nfor await (const line of rl) process(line)\n```\n```\n",tech:"node",displayNumber:54,sortKey:1383},{id:"node-1384",number:55,title:"What is `readline` used for besides input?",question:"What is `readline` used for besides input?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`readline` reads any readable line by line (files, sockets), supports async iteration, and offers interactive prompts/history for CLIs.\n\n\n```js\nfor await (const line of readline.createInterface({ input: stream })) {}\n```\n```\n",tech:"node",displayNumber:55,sortKey:1384},{id:"node-1385",number:56,title:"How do you signal end-of-stream from a custom readable?",question:"How do you signal end-of-stream from a custom readable?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Call `this.push(null)` to indicate no more data; consumers then receive the `end` event. Failing to push null leaves consumers hanging.\n\n\n```js\nread() { if (done) this.push(null); else this.push(nextChunk()) }\n```\n```\n",tech:"node",displayNumber:56,sortKey:1385},{id:"node-1386",number:57,title:'What causes "write after end" errors?',question:'What causes "write after end" errors?',difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Calling `write()` on a writable after `end()` has been called throws/emits an error. It usually indicates a logic bug where writes race with stream termination.\n\n\n```js\nws.end('last'); ws.write('oops') // Error: write after end\n```\n```\n",tech:"node",displayNumber:57,sortKey:1386},{id:"node-1387",number:58,title:"How do you throttle a stream's rate?",question:"How do you throttle a stream's rate?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Insert a Transform that delays chunk passthrough (using timers) to cap bytes/sec, applying backpressure to the source. Useful for bandwidth limiting.\n\n\n```js\nnew Transform({ transform(c, e, cb) { setTimeout(() => cb(null, c), delay) } })\n```\n```\n",tech:"node",displayNumber:58,sortKey:1387},{id:"node-1388",number:59,title:"What is the relationship between HTTP req/res and streams?",question:"What is the relationship between HTTP req/res and streams?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`req` (IncomingMessage) is a Readable stream of the request body; `res` (ServerResponse) is a Writable stream for the response. This is why you can pipe files directly to `res`.\n\n\n```js\nreq.pipe(fs.createWriteStream('body.txt'))\n```\n```\n",tech:"node",displayNumber:59,sortKey:1388},{id:"node-1389",number:60,title:"How do you handle stream errors in an Express route?",question:"How do you handle stream errors in an Express route?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Attach an `error` handler (or use `pipeline`) and forward to Express's error middleware via `next(err)`; also ensure the response is ended/destroyed to avoid hanging connections.\n\n\n```js\npipeline(readStream, res, err => { if (err) next(err) })\n```\n```\n",tech:"node",displayNumber:60,sortKey:1389},{id:"node-1390",number:61,title:"What is `Symbol.asyncIterator` on streams?",question:"What is `Symbol.asyncIterator` on streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Readable streams implement `Symbol.asyncIterator`, so `for await (const chunk of stream)` works out of the box, consuming chunks with backpressure and stopping on `end`/`error`.\n\n\n```js\nfor await (const chunk of fs.createReadStream('f')) total += chunk.length\n```\n```\n",tech:"node",displayNumber:61,sortKey:1390},{id:"node-1391",number:62,title:"How do Web Streams differ from Node streams?",question:"How do Web Streams differ from Node streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Web Streams (`ReadableStream`/`WritableStream`) are the WHATWG standard also available in Node for cross-platform code (e.g. `fetch` bodies). Node provides adapters (`Readable.fromWeb`/`toWeb`) to interoperate.\n\n\n```js\nconst nodeStream = require('stream').Readable.fromWeb(webReadable)\n```\n```\n",tech:"node",displayNumber:62,sortKey:1391},{id:"node-1392",number:63,title:"How do you avoid loading a whole JSON array into memory?",question:"How do you avoid loading a whole JSON array into memory?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use a streaming JSON parser (e.g. a Transform that emits each array element) so you process records incrementally instead of `JSON.parse` on the entire file.\n\n\n```js\npipeline(fs.createReadStream('big.json'), streamArrayParser(), handleEach)\n```\n```\n",tech:"node",displayNumber:63,sortKey:1392},{id:"node-1393",number:64,title:"What is the difference between `pause()`/`resume()` and `cork()`/`uncork()`?",question:"What is the difference between `pause()`/`resume()` and `cork()`/`uncork()`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`pause`/`resume` control a readable's flow (whether it emits data). `cork`/`uncork` batch writes on a writable to reduce syscalls. Different sides, different purposes.\n\n\n```js\nreadable.pause() // stop reading\nwritable.cork()  // batch writes\n```\n```\n",tech:"node",displayNumber:64,sortKey:1393},{id:"node-1394",number:65,title:"How do you propagate an abort into a stream pipeline?",question:"How do you propagate an abort into a stream pipeline?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pass an `AbortSignal` to `pipeline`'s options; aborting destroys all streams and rejects the pipeline, enabling cancellable transfers.\n\n\n```js\nawait pipeline(src, dst, { signal: controller.signal })\n```\n```\n",tech:"node",displayNumber:65,sortKey:1394},{id:"node-1395",number:66,title:"What are common causes of stream memory leaks?",question:"What are common causes of stream memory leaks?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Ignoring backpressure, not destroying streams on error, unbounded internal buffering (object mode without highWaterMark), and accumulating chunks in arrays. Use `pipeline` and honor `write()`'s return value.\n\n\n```js\n// leak: chunks.push(c) forever without consuming\n```\n```\n",tech:"node",displayNumber:66,sortKey:1395},{id:"node-1396",number:67,title:"How do you implement a rate-limited log writer with streams?",question:"How do you implement a rate-limited log writer with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Write to a writable file stream and rely on backpressure; batch with `cork`/`uncork` and rotate files by size/time to bound disk usage.\n\n\n```js\nlogStream.cork(); for (const l of batch) logStream.write(l + '\\n'); logStream.uncork()\n```\n```\n",tech:"node",displayNumber:67,sortKey:1396},{id:"node-1397",number:68,title:"What is the difference between `Buffer.from(string)` and `Buffer.from(array)`?",question:"What is the difference between `Buffer.from(string)` and `Buffer.from(array)`?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`Buffer.from(string, enc)` encodes text into bytes. `Buffer.from(array)` treats each element as a byte value (0–255). They produce different bytes for the same-looking input.\n\n\n```js\nBuffer.from('123')          // <Buffer 31 32 33>\nBuffer.from([1, 2, 3])      // <Buffer 01 02 03>\n```\n```\n",tech:"node",displayNumber:68,sortKey:1397},{id:"node-1398",number:69,title:"How do you detect the end of an EventEmitter-based protocol?",question:"How do you detect the end of an EventEmitter-based protocol?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Listen for the protocol's terminal event (`end`, `close`, or a custom `done`), and guard against emitting after termination. Combine with timeouts to detect stalled peers.\n\n\n```js\nsocket.on('end', cleanup); socket.on('close', cleanup)\n```\n```\n",tech:"node",displayNumber:69,sortKey:1398},{id:"node-1399",number:70,title:"When should you NOT use streams?",question:"When should you NOT use streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"For small payloads that fit comfortably in memory, streams add complexity and overhead for little benefit — just buffer and process directly. Use streams when data is large, unbounded, or benefits from incremental/low-latency processing.\n\n\n```js\nconst cfg = JSON.parse(await fs.promises.readFile('config.json')) // fine, small\n```\n```\n",tech:"node",displayNumber:70,sortKey:1399},{id:"node-1400",number:1,title:"What is the `fs` module?",question:"What is the `fs` module?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs` provides file system operations: reading, writing, deleting, watching files and directories. It offers callback, synchronous (`*Sync`), and Promise (`fs/promises`) variants.\n\n\n```js\nconst fs = require('node:fs/promises')\nconst data = await fs.readFile('a.txt', 'utf8')\n```\n```\n",tech:"node",displayNumber:1,sortKey:1400},{id:"node-1401",number:2,title:"What are the three API styles of `fs`?",question:"What are the three API styles of `fs`?",difficulty:"Basic",category:"File System, Process & OS",answer:"Callback-based (`fs.readFile(path, cb)`), synchronous (`fs.readFileSync`), and Promise-based (`require('fs/promises')`). Prefer Promises in modern async code; avoid Sync in servers.\n\n\n```js\nimport { readFile } from 'node:fs/promises'\n```\n```\n",tech:"node",displayNumber:2,sortKey:1401},{id:"node-1402",number:3,title:"Why avoid `fs.readFileSync` in a web server?",question:"Why avoid `fs.readFileSync` in a web server?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Sync methods block the single event-loop thread until I/O completes, freezing all concurrent requests. Sync I/O is acceptable only at startup (loading config) or in CLI scripts.\n\n\n```js\n// startup only\nconst config = JSON.parse(fs.readFileSync('config.json', 'utf8'))\n```\n```\n",tech:"node",displayNumber:3,sortKey:1402},{id:"node-1403",number:4,title:"How do you read a file's contents?",question:"How do you read a file's contents?",difficulty:"Basic",category:"File System, Process & OS",answer:"Use `fs.readFile(path, encoding)`. Without an encoding you get a Buffer; with `'utf8'` you get a string. For large files, prefer streaming.\n\n\n```js\nconst text = await fs.readFile('notes.md', 'utf8')\n```\n```\n",tech:"node",displayNumber:4,sortKey:1403},{id:"node-1404",number:5,title:"How do you write and append to a file?",question:"How do you write and append to a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.writeFile` overwrites (or creates) a file; `fs.appendFile` adds to the end. Both accept strings or Buffers.\n\n\n```js\nawait fs.writeFile('log.txt', 'start\\n')\nawait fs.appendFile('log.txt', 'more\\n')\n```\n```\n",tech:"node",displayNumber:5,sortKey:1404},{id:"node-1405",number:6,title:"How do you check if a file exists?",question:"How do you check if a file exists?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Prefer trying the operation and handling `ENOENT`, or use `fs.access`. Avoid the old `fs.exists` (deprecated) and avoid check-then-act races (TOCTOU).\n\n\n```js\ntry { await fs.access('f.txt') } catch { /* missing */ }\n```\n```\n",tech:"node",displayNumber:6,sortKey:1405},{id:"node-1406",number:7,title:"What is a TOCTOU race in file operations?",question:"What is a TOCTOU race in file operations?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Time-Of-Check to Time-Of-Use: a file's state can change between checking (exists/permissions) and using it, enabling bugs or attacks. Prefer atomic operations that check-and-act in one call, and handle errors instead of pre-checking.\n\n\n```js\n// atomic: fails if it already exists, no separate check\nawait fs.writeFile('f', data, { flag: 'wx' })\n```\n```\n",tech:"node",displayNumber:7,sortKey:1406},{id:"node-1407",number:8,title:"How do you create a directory (including parents)?",question:"How do you create a directory (including parents)?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.mkdir(path, { recursive: true })` creates the directory and any missing parents, and won't error if it already exists.\n\n\n```js\nawait fs.mkdir('a/b/c', { recursive: true })\n```\n```\n",tech:"node",displayNumber:8,sortKey:1407},{id:"node-1408",number:9,title:"How do you read the contents of a directory?",question:"How do you read the contents of a directory?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.readdir(path)` returns file/dir names. With `{ withFileTypes: true }` it returns `Dirent` objects so you can distinguish files from directories without extra `stat` calls.\n\n\n```js\nfor (const d of await fs.readdir('.', { withFileTypes: true }))\n  if (d.isDirectory()) console.log(d.name)\n```\n```\n",tech:"node",displayNumber:9,sortKey:1408},{id:"node-1409",number:10,title:"How do you delete a file or directory?",question:"How do you delete a file or directory?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.unlink` removes a file; `fs.rm(path, { recursive: true, force: true })` removes files or directory trees, replacing the deprecated `fs.rmdir` recursive option.\n\n\n```js\nawait fs.rm('temp', { recursive: true, force: true })\n```\n```\n",tech:"node",displayNumber:10,sortKey:1409},{id:"node-1410",number:11,title:"What does `fs.stat` return?",question:"What does `fs.stat` return?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"A `Stats` object with metadata: size, timestamps (`mtime`, `ctime`, `atime`), and type predicates (`isFile()`, `isDirectory()`, `isSymbolicLink()`).\n\n\n```js\nconst s = await fs.stat('a.txt')\nconsole.log(s.size, s.isFile())\n```\n```\n",tech:"node",displayNumber:11,sortKey:1410},{id:"node-1411",number:12,title:"What is the difference between `fs.stat` and `fs.lstat`?",question:"What is the difference between `fs.stat` and `fs.lstat`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`stat` follows symbolic links and reports the target's info; `lstat` reports the link itself. Use `lstat` when you need to detect symlinks.\n\n\n```js\nconst s = await fs.lstat('link'); s.isSymbolicLink() // true for the link\n```\n```\n",tech:"node",displayNumber:12,sortKey:1411},{id:"node-1412",number:13,title:"How do you rename or move a file?",question:"How do you rename or move a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.rename(old, new)` renames or moves within the same filesystem atomically. Across filesystems it may fail with `EXDEV`, requiring a copy-then-delete.\n\n\n```js\nawait fs.rename('a.txt', 'archive/a.txt')\n```\n```\n",tech:"node",displayNumber:13,sortKey:1412},{id:"node-1413",number:14,title:"How do you copy a file?",question:"How do you copy a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.copyFile(src, dest)` copies a single file; `fs.cp(src, dest, { recursive: true })` copies directory trees.\n\n\n```js\nawait fs.cp('src', 'backup', { recursive: true })\n```\n```\n",tech:"node",displayNumber:14,sortKey:1413},{id:"node-1414",number:15,title:"How do you watch a file or directory for changes?",question:"How do you watch a file or directory for changes?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`fs.watch` emits events on changes (efficient, OS-backed, but can miss/duplicate events and vary by platform). `fs.watchFile` polls stats (reliable but heavier). Libraries like chokidar smooth over platform quirks.\n\n\n```js\nfs.watch('config.json', (event, filename) => reload())\n```\n```\n",tech:"node",displayNumber:15,sortKey:1414},{id:"node-1415",number:16,title:"What is a file descriptor?",question:"What is a file descriptor?",difficulty:"Advanced",category:"File System, Process & OS",answer:"A file descriptor (fd) is an integer handle to an open file/socket from the OS. `fs.open` returns one; you must `close` it to avoid leaking descriptors (there's an OS limit).\n\n\n```js\nconst fh = await fs.open('a.txt', 'r')\ntry { /* read */ } finally { await fh.close() }\n```\n```\n",tech:"node",displayNumber:16,sortKey:1415},{id:"node-1416",number:17,title:"What are file open flags like `r`, `w`, `a`, `wx`?",question:"What are file open flags like `r`, `w`, `a`, `wx`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`r` read, `w` write/truncate, `a` append, `x` fail if exists. Combinations like `wx` create exclusively (atomic \"create if not exists\"), and `r+`/`w+` allow read+write.\n\n\n```js\nawait fs.writeFile('lock', '', { flag: 'wx' }) // errors if lock exists\n```\n```\n",tech:"node",displayNumber:17,sortKey:1416},{id:"node-1417",number:18,title:"How do you read a large file efficiently?",question:"How do you read a large file efficiently?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.createReadStream` to process in chunks with constant memory, optionally through `readline` for line-based data, rather than loading it all with `readFile`.\n\n\n```js\nfor await (const line of readline.createInterface({ input: fs.createReadStream('big.log') })) {}\n```\n```\n",tech:"node",displayNumber:18,sortKey:1417},{id:"node-1418",number:19,title:"How do you handle file paths cross-platform?",question:"How do you handle file paths cross-platform?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use the `path` module (`join`, `resolve`, `sep`, `basename`, `extname`) instead of hardcoding `/` or `\\`, so code works on Windows and POSIX.\n\n\n```js\nconst p = path.join(dir, 'sub', 'file.txt')\n```\n```\n",tech:"node",displayNumber:19,sortKey:1418},{id:"node-1419",number:20,title:"What is `path.normalize`?",question:"What is `path.normalize`?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`path.normalize` resolves `.` and `..` segments and collapses duplicate separators into a canonical path, useful before comparing or using user-supplied paths.\n\n\n```js\npath.normalize('a//b/../c') // 'a/c'\n```\n```\n",tech:"node",displayNumber:20,sortKey:1419},{id:"node-1420",number:21,title:"How do you prevent path traversal attacks?",question:"How do you prevent path traversal attacks?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Resolve the requested path against a base directory and verify the result still starts with that base, rejecting `../` escapes. Never concatenate user input directly into file paths.\n\n\n```js\nconst full = path.resolve(base, userPath)\nif (!full.startsWith(base + path.sep)) throw new Error('traversal')\n```\n```\n",tech:"node",displayNumber:21,sortKey:1420},{id:"node-1421",number:22,title:"What is the difference between `path.extname` and manual splitting?",question:"What is the difference between `path.extname` and manual splitting?",difficulty:"Basic",category:"File System, Process & OS",answer:"`path.extname` reliably returns the extension including the dot (handling edge cases like dotfiles and multiple dots), avoiding brittle manual `split('.')` logic.\n\n\n```js\npath.extname('archive.tar.gz') // '.gz'\npath.extname('.env')            // ''\n```\n```\n",tech:"node",displayNumber:22,sortKey:1421},{id:"node-1422",number:23,title:"How do you create a temporary file/directory?",question:"How do you create a temporary file/directory?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.mkdtemp(prefix)` for a unique temp directory (typically under `os.tmpdir()`), then write files inside it. This avoids name collisions.\n\n\n```js\nconst dir = await fs.mkdtemp(path.join(os.tmpdir(), 'app-'))\n```\n```\n",tech:"node",displayNumber:23,sortKey:1422},{id:"node-1423",number:24,title:"What is `os.tmpdir()`?",question:"What is `os.tmpdir()`?",difficulty:"Basic",category:"File System, Process & OS",answer:"It returns the operating system's default directory for temporary files (e.g. `/tmp` on Linux), which the OS may clean periodically.\n\n\n```js\nconsole.log(os.tmpdir()) // '/tmp'\n```\n```\n",tech:"node",displayNumber:24,sortKey:1423},{id:"node-1424",number:25,title:"How do you set file permissions?",question:"How do you set file permissions?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.chmod(path, mode)` with an octal mode (e.g. `0o600`) on POSIX systems. On Windows, permission bits are largely ignored.\n\n\n```js\nawait fs.chmod('secret.key', 0o600) // owner read/write only\n```\n```\n",tech:"node",displayNumber:25,sortKey:1424},{id:"node-1425",number:26,title:"What does `process.cwd()` return and can it change?",question:"What does `process.cwd()` return and can it change?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"It returns the process's current working directory. `process.chdir(dir)` changes it, affecting relative path resolution. Prefer absolute paths (via `__dirname`) to avoid surprises.\n\n\n```js\nprocess.chdir('/app'); console.log(process.cwd()) // '/app'\n```\n```\n",tech:"node",displayNumber:26,sortKey:1425},{id:"node-1426",number:27,title:"How do you spawn a child process?",question:"How do you spawn a child process?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`child_process.spawn(cmd, args)` launches a process and streams its stdio (good for large output). `exec` buffers output, `execFile` runs a binary directly, `fork` spawns a Node child with an IPC channel.\n\n\n```js\nconst { spawn } = require('node:child_process')\nspawn('ls', ['-la']).stdout.pipe(process.stdout)\n```\n```\n",tech:"node",displayNumber:27,sortKey:1426},{id:"node-1427",number:28,title:"What is the difference between `spawn` and `exec`?",question:"What is the difference between `spawn` and `exec`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`spawn` streams stdout/stderr (constant memory, good for long/large output). `exec` runs a shell command and buffers all output in memory (convenient but risky for large output and shell injection).\n\n\n```js\nexec('git log', (err, stdout) => {}) // buffered\nspawn('git', ['log'])                 // streamed\n```\n```\n",tech:"node",displayNumber:28,sortKey:1427},{id:"node-1428",number:29,title:"Why prefer `execFile`/`spawn` with an args array over `exec` with a string?",question:"Why prefer `execFile`/`spawn` with an args array over `exec` with a string?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`exec` runs through a shell, so interpolated user input enables command injection. `execFile`/`spawn` pass args directly to the binary without shell parsing, preventing injection.\n\n\n```js\nexecFile('convert', [userFile, 'out.png']) // safe, no shell\n```\n```\n",tech:"node",displayNumber:29,sortKey:1428},{id:"node-1429",number:30,title:"What is `child_process.fork`?",question:"What is `child_process.fork`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`fork` spawns a new Node process running a module, with a built-in IPC channel for `send`/`message`. It is used to run separate Node scripts and communicate via message passing.\n\n\n```js\nconst child = fork('worker.js')\nchild.send({ task: 1 }); child.on('message', console.log)\n```\n```\n",tech:"node",displayNumber:30,sortKey:1429},{id:"node-1430",number:31,title:"How do child processes communicate (IPC)?",question:"How do child processes communicate (IPC)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Forked children use `process.send`/`on('message')` over an IPC channel with structured cloning of messages. Spawned processes communicate via stdio streams or sockets.\n\n\n```js\n// in child\nprocess.on('message', m => process.send({ echo: m }))\n```\n```\n",tech:"node",displayNumber:31,sortKey:1430},{id:"node-1431",number:32,title:"What is the `cluster` module?",question:"What is the `cluster` module?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`cluster` forks multiple worker processes that share a server port, letting a Node app use all CPU cores. The primary process distributes incoming connections among workers.\n\n\n```js\nif (cluster.isPrimary) for (let i = 0; i < os.cpus().length; i++) cluster.fork()\nelse http.createServer(handler).listen(3000)\n```\n```\n",tech:"node",displayNumber:32,sortKey:1431},{id:"node-1432",number:33,title:"How does the cluster module share a port across workers?",question:"How does the cluster module share a port across workers?",difficulty:"Experienced",category:"File System, Process & OS",answer:"The primary creates the listening socket and hands connections to workers (round-robin by default on non-Windows), or workers share the descriptor. The OS/primary balances the load.\n\n\n```js\ncluster.schedulingPolicy = cluster.SCHED_RR\n```\n```\n",tech:"node",displayNumber:33,sortKey:1432},{id:"node-1433",number:34,title:"What is the difference between cluster and worker threads?",question:"What is the difference between cluster and worker threads?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Cluster forks full processes (isolated memory, good for scaling I/O-bound servers across cores). Worker threads share memory within one process (lower overhead, good for CPU-bound tasks). Choose by workload and isolation needs.\n\n\n```js\n// I/O scaling -> cluster; CPU crunching -> worker_threads\n```\n```\n",tech:"node",displayNumber:34,sortKey:1433},{id:"node-1434",number:35,title:"How do you pass data between worker threads?",question:"How do you pass data between worker threads?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Via `postMessage`/`on('message')` (structured clone), `workerData` at creation, or shared memory with `SharedArrayBuffer` + `Atomics` for zero-copy communication.\n\n\n```js\nnew Worker('./w.js', { workerData: { rows } })\n```\n```\n",tech:"node",displayNumber:35,sortKey:1434},{id:"node-1435",number:36,title:"What is `SharedArrayBuffer` and `Atomics`?",question:"What is `SharedArrayBuffer` and `Atomics`?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`SharedArrayBuffer` is memory shared between threads without copying; `Atomics` provides atomic read/write/wait operations to coordinate access safely and avoid data races.\n\n\n```js\nconst shared = new Int32Array(new SharedArrayBuffer(4))\nAtomics.add(shared, 0, 1)\n```\n```\n",tech:"node",displayNumber:36,sortKey:1435},{id:"node-1436",number:37,title:"How do you get system information?",question:"How do you get system information?",difficulty:"Basic",category:"File System, Process & OS",answer:"The `os` module exposes `platform()`, `arch()`, `cpus()`, `totalmem()`, `freemem()`, `hostname()`, `uptime()`, and `networkInterfaces()`.\n\n\n```js\nconsole.log(os.platform(), os.cpus().length, os.freemem())\n```\n```\n",tech:"node",displayNumber:37,sortKey:1436},{id:"node-1437",number:38,title:"How do you read memory usage of the Node process?",question:"How do you read memory usage of the Node process?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`process.memoryUsage()` returns `rss`, `heapTotal`, `heapUsed`, `external`, and `arrayBuffers` in bytes — useful for monitoring and leak detection.\n\n\n```js\nconsole.log(process.memoryUsage().rss / 1e6, 'MB RSS')\n```\n```\n",tech:"node",displayNumber:38,sortKey:1437},{id:"node-1438",number:39,title:"What is RSS vs heap in memory usage?",question:"What is RSS vs heap in memory usage?",difficulty:"Experienced",category:"File System, Process & OS",answer:"RSS (Resident Set Size) is total physical memory the process holds (code, stack, heap, buffers). Heap is the V8-managed JS object memory. External/arrayBuffers cover off-heap allocations like Buffers.\n\n\n```js\nconst { rss, heapUsed, external } = process.memoryUsage()\n```\n```\n",tech:"node",displayNumber:39,sortKey:1438},{id:"node-1439",number:40,title:"How do you handle process signals for graceful shutdown?",question:"How do you handle process signals for graceful shutdown?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Listen for `SIGINT`/`SIGTERM`, stop accepting connections, drain in-flight work, close resources, then exit. Add a timeout to force-exit if cleanup hangs.\n\n\n```js\nprocess.on('SIGTERM', async () => { await server.close(); process.exit(0) })\n```\n```\n",tech:"node",displayNumber:40,sortKey:1439},{id:"node-1440",number:41,title:"What is `process.pid` and `process.ppid`?",question:"What is `process.pid` and `process.ppid`?",difficulty:"Basic",category:"File System, Process & OS",answer:"`process.pid` is the current process ID; `process.ppid` is the parent's. Useful for logging, process management, and IPC coordination.\n\n\n```js\nconsole.log(`pid ${process.pid}, parent ${process.ppid}`)\n```\n```\n",tech:"node",displayNumber:41,sortKey:1440},{id:"node-1441",number:42,title:"How do you measure CPU usage of the current process?",question:"How do you measure CPU usage of the current process?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`process.cpuUsage()` returns user/system microseconds; call it before and after work and diff to get consumption. `process.resourceUsage()` gives more detail.\n\n\n```js\nconst start = process.cpuUsage(); work(); console.log(process.cpuUsage(start))\n```\n```\n",tech:"node",displayNumber:42,sortKey:1441},{id:"node-1442",number:43,title:"How do you set and read environment variables at runtime?",question:"How do you set and read environment variables at runtime?",difficulty:"Basic",category:"File System, Process & OS",answer:"Read/write `process.env`. Values are strings; changes affect the current process (and children spawned afterward) but not the parent shell.\n\n\n```js\nprocess.env.FEATURE = 'on'\n```\n```\n",tech:"node",displayNumber:43,sortKey:1442},{id:"node-1443",number:44,title:"How do you load a `.env` file?",question:"How do you load a `.env` file?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use a loader like `dotenv`, or Node's built-in `--env-file` flag (recent versions), which parses key=value pairs into `process.env` at startup. Never commit secret `.env` files.\n\n\n```js\n$ node --env-file=.env app.js\n```\n```\n",tech:"node",displayNumber:44,sortKey:1443},{id:"node-1444",number:45,title:"What is the standard way to structure config for multiple environments?",question:"What is the standard way to structure config for multiple environments?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Read all config from environment variables (12-factor), with sensible defaults and validation at startup. Avoid committing environment-specific secrets; use per-environment env files or a secrets manager.\n\n\n```js\nconst config = { port: Number(process.env.PORT ?? 3000), db: process.env.DB_URL }\n```\n```\n",tech:"node",displayNumber:45,sortKey:1444},{id:"node-1445",number:46,title:"How do you run a shell command and capture its output as a Promise?",question:"How do you run a shell command and capture its output as a Promise?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Promisify `exec` (or use `execFile`), awaiting `{ stdout, stderr }`. Prefer `execFile` with args to avoid shell injection.\n\n\n```js\nconst { stdout } = await promisify(execFile)('node', ['-v'])\n```\n```\n",tech:"node",displayNumber:46,sortKey:1445},{id:"node-1446",number:47,title:"How do you handle a child process that hangs?",question:"How do you handle a child process that hangs?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Set a `timeout` (spawn/exec option) or an external timer that calls `child.kill('SIGKILL')`. Also handle `error`, `exit`, and non-zero exit codes.\n\n\n```js\nconst child = spawn('slow', [], { timeout: 5000, killSignal: 'SIGKILL' })\n```\n```\n",tech:"node",displayNumber:47,sortKey:1446},{id:"node-1447",number:48,title:"What are exit codes and how do you set them?",question:"What are exit codes and how do you set them?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Exit codes signal success (0) or failure (non-zero) to the OS/parent. Set via `process.exit(code)` or `process.exitCode = code` (graceful). CLIs use them so scripts can detect failure.\n\n\n```js\nif (invalid) process.exitCode = 2\n```\n```\n",tech:"node",displayNumber:48,sortKey:1447},{id:"node-1448",number:49,title:"How do you detect the platform and adjust behavior?",question:"How do you detect the platform and adjust behavior?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Check `process.platform` (`win32`, `linux`, `darwin`) or `os.platform()`/`os.type()` to branch on OS-specific paths, commands, or line endings.\n\n\n```js\nconst npmCmd = process.platform === 'win32' ? 'npm.cmd' : 'npm'\n```\n```\n",tech:"node",displayNumber:49,sortKey:1448},{id:"node-1449",number:50,title:"What is `os.EOL`?",question:"What is `os.EOL`?",difficulty:"Basic",category:"File System, Process & OS",answer:"`os.EOL` is the platform's line terminator (`\\n` on POSIX, `\\r\\n` on Windows). Use it when generating text files meant to match native conventions.\n\n\n```js\nfs.writeFile('out.txt', lines.join(os.EOL))\n```\n```\n",tech:"node",displayNumber:50,sortKey:1449},{id:"node-1450",number:51,title:"How do you get the number of CPU cores for scaling?",question:"How do you get the number of CPU cores for scaling?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`os.cpus().length` (or `os.availableParallelism()` in newer Node) gives core count, used to size cluster workers or thread pools.\n\n\n```js\nconst workers = os.availableParallelism()\n```\n```\n",tech:"node",displayNumber:51,sortKey:1450},{id:"node-1451",number:52,title:"What is `process.stdin` used for?",question:"What is `process.stdin` used for?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`process.stdin` is a readable stream of input piped/typed into the process. Read it for CLI input or piped data; combine with `readline` for line-based reading.\n\n\n```js\nprocess.stdin.on('data', d => console.log('got', d.toString()))\n```\n```\n",tech:"node",displayNumber:52,sortKey:1451},{id:"node-1452",number:53,title:"How do you read piped input vs interactive input?",question:"How do you read piped input vs interactive input?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Check `process.stdin.isTTY`: true means an interactive terminal (prompt the user); false means piped/redirected data (read the stream to end).\n\n\n```js\nif (process.stdin.isTTY) prompt(); else readPipedData()\n```\n```\n",tech:"node",displayNumber:53,sortKey:1452},{id:"node-1453",number:54,title:"How do you handle uncaught exceptions in a worker/cluster setup?",question:"How do you handle uncaught exceptions in a worker/cluster setup?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Log the error and let the worker exit; the primary listens for `exit` and forks a replacement. This keeps the service available while replacing crashed workers.\n\n\n```js\ncluster.on('exit', () => cluster.fork()) // respawn\n```\n```\n",tech:"node",displayNumber:54,sortKey:1453},{id:"node-1454",number:55,title:"What is a zombie/orphan process and how do you avoid it?",question:"What is a zombie/orphan process and how do you avoid it?",difficulty:"Experienced",category:"File System, Process & OS",answer:"A zombie is a finished child not yet reaped by its parent; an orphan is a child whose parent died. Avoid by handling `exit` events, killing children on shutdown, and using process managers/`detached` correctly.\n\n\n```js\nprocess.on('exit', () => child.kill())\n```\n```\n",tech:"node",displayNumber:55,sortKey:1454},{id:"node-1455",number:56,title:"How do you make a long-running background process survive terminal close?",question:"How do you make a long-running background process survive terminal close?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Spawn with `{ detached: true, stdio: 'ignore' }` and call `child.unref()`, or use a process manager (pm2, systemd). This decouples the child from the parent's lifecycle.\n\n\n```js\nconst c = spawn('node', ['job.js'], { detached: true, stdio: 'ignore' }); c.unref()\n```\n```\n",tech:"node",displayNumber:56,sortKey:1455},{id:"node-1456",number:57,title:"What is `process.umask()`?",question:"What is `process.umask()`?",difficulty:"Experienced",category:"File System, Process & OS",answer:"The umask controls default permission bits stripped from newly created files/dirs. `process.umask()` reads it; setting it affects security of created files (e.g. preventing world-readable secrets).\n\n\n```js\nconsole.log(process.umask().toString(8)) // e.g. '22'\n```\n```\n",tech:"node",displayNumber:57,sortKey:1456},{id:"node-1457",number:58,title:"How do you atomically write a file to avoid corruption?",question:"How do you atomically write a file to avoid corruption?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Write to a temp file in the same directory, then `fs.rename` it over the target (rename is atomic on the same filesystem). A crash mid-write leaves the original intact.\n\n\n```js\nawait fs.writeFile(tmp, data); await fs.rename(tmp, 'config.json')\n```\n```\n",tech:"node",displayNumber:58,sortKey:1457},{id:"node-1458",number:59,title:"How do you tail a growing log file?",question:"How do you tail a growing log file?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Track the last read position, watch the file for changes, and read only the new bytes with a read stream starting at that offset, handling truncation/rotation.\n\n\n```js\nfs.watch('app.log', () => readFrom(lastPos))\n```\n```\n",tech:"node",displayNumber:59,sortKey:1458},{id:"node-1459",number:60,title:"How do you handle file locking in Node?",question:"How do you handle file locking in Node?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Node has no built-in advisory locks; use exclusive create (`flag: 'wx'`) as a lock file, an OS lock via a native module (`proper-lockfile`), or coordinate through a database.\n\n\n```js\nawait fs.writeFile('app.lock', String(process.pid), { flag: 'wx' })\n```\n```\n",tech:"node",displayNumber:60,sortKey:1459},{id:"node-1460",number:61,title:"What is the `glob` pattern and how do you match files?",question:"What is the `glob` pattern and how do you match files?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Globs (`**/*.js`) match file paths by pattern. Node's `fs` now offers `fs.glob`, or use libraries like `fast-glob` for recursive matching with ignore rules.\n\n\n```js\nfor await (const f of fs.glob('src/**/*.test.js')) console.log(f)\n```\n```\n",tech:"node",displayNumber:61,sortKey:1460},{id:"node-1461",number:62,title:"How do you recursively walk a directory tree?",question:"How do you recursively walk a directory tree?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.readdir(dir, { withFileTypes: true, recursive: true })` (modern) or write a recursive function that descends into subdirectories, guarding against symlink loops.\n\n\n```js\nconst files = await fs.readdir('src', { recursive: true })\n```\n```\n",tech:"node",displayNumber:62,sortKey:1461},{id:"node-1462",number:63,title:"How do you handle EMFILE (too many open files)?",question:"How do you handle EMFILE (too many open files)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Limit concurrent file operations (a queue/pool), always close descriptors, and optionally raise the OS `ulimit`. EMFILE means you exhausted the descriptor limit by opening files faster than closing.\n\n\n```js\nconst limit = pLimit(50)\nawait Promise.all(files.map(f => limit(() => fs.readFile(f))))\n```\n```\n",tech:"node",displayNumber:63,sortKey:1462},{id:"node-1463",number:64,title:"What is the difference between hard links and symbolic links?",question:"What is the difference between hard links and symbolic links?",difficulty:"Advanced",category:"File System, Process & OS",answer:"A hard link is another name for the same inode/data (both must be deleted to free it). A symlink is a pointer to a path that can dangle if the target is removed. `fs.link` vs `fs.symlink` create them.\n\n\n```js\nawait fs.symlink('target.txt', 'alias.txt')\n```\n```\n",tech:"node",displayNumber:64,sortKey:1463},{id:"node-1464",number:65,title:"How do you stream-copy with progress reporting?",question:"How do you stream-copy with progress reporting?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Pipe a read stream through a counting Transform to a write stream, emitting progress from bytes transferred vs total file size.\n\n\n```js\nread.on('data', c => { copied += c.length; report(copied / total) })\n```\n```\n",tech:"node",displayNumber:65,sortKey:1464},{id:"node-1465",number:66,title:"What are `process.stdout.columns` / `rows` used for?",question:"What are `process.stdout.columns` / `rows` used for?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"They report terminal dimensions (when stdout is a TTY), letting CLIs wrap text, draw progress bars, or lay out tables. They're undefined when output is piped.\n\n\n```js\nconst width = process.stdout.columns ?? 80\n```\n```\n",tech:"node",displayNumber:66,sortKey:1465},{id:"node-1466",number:67,title:"How do you gracefully restart workers on deploy (zero-downtime)?",question:"How do you gracefully restart workers on deploy (zero-downtime)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Fork new workers, wait until they're listening, then signal old workers to stop accepting new connections and exit after draining. Process managers (pm2 reload) automate this rolling restart.\n\n\n```js\npm2 reload app // zero-downtime rolling restart\n```\n```\n",tech:"node",displayNumber:67,sortKey:1466},{id:"node-1467",number:68,title:"How do you handle `SIGKILL` vs `SIGTERM`?",question:"How do you handle `SIGKILL` vs `SIGTERM`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`SIGTERM` is catchable and requests graceful shutdown; `SIGKILL` (and `SIGSTOP`) cannot be caught or handled — the OS terminates immediately. Design cleanup around SIGTERM; treat SIGKILL as a hard stop.\n\n\n```js\nprocess.on('SIGTERM', shutdown) // SIGKILL can't be intercepted\n```\n```\n",tech:"node",displayNumber:68,sortKey:1467},{id:"node-1468",number:69,title:"How do you detect if code runs in the primary or a worker?",question:"How do you detect if code runs in the primary or a worker?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use `cluster.isPrimary` (formerly `isMaster`) / `cluster.isWorker`, or in worker threads check `worker_threads.isMainThread`.\n\n\n```js\nif (cluster.isPrimary) setupWorkers(); else runServer()\n```\n```\n",tech:"node",displayNumber:69,sortKey:1468},{id:"node-1469",number:70,title:"What is `process.report` used for?",question:"What is `process.report` used for?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`process.report` generates a diagnostic report (JSON) of the process state — stack traces, heap stats, resource usage, OS info — on demand or on crashes/signals, aiding post-mortem debugging in production.\n\n\n```js\nprocess.report.writeReport('./diagnostic.json')\n```\n```\n",tech:"node",displayNumber:70,sortKey:1469},{id:"node-1470",number:1,title:"How do you create an HTTP server in Node?",question:"How do you create an HTTP server in Node?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use the `http` module's `createServer`, passing a handler `(req, res)`, then call `listen(port)`. `req` is a readable stream, `res` is a writable stream.\n\n\n```js\nrequire('http').createServer((req, res) => res.end('Hello')).listen(3000)\n```\n```\n",tech:"node",displayNumber:1,sortKey:1470},{id:"node-1471",number:2,title:"What are `req` and `res` in an HTTP handler?",question:"What are `req` and `res` in an HTTP handler?",difficulty:"Basic",category:"HTTP & Networking",answer:"`req` is an `IncomingMessage` (readable stream) with method, url, and headers. `res` is a `ServerResponse` (writable stream) used to set status/headers and send the body.\n\n\n```js\n(req, res) => { console.log(req.method, req.url); res.end() }\n```\n```\n",tech:"node",displayNumber:2,sortKey:1471},{id:"node-1472",number:3,title:"How do you read the request body?",question:"How do you read the request body?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The body arrives as stream chunks. Collect `data` events into buffers and concatenate on `end`, then parse. Frameworks/`body-parser` do this for you with size limits.\n\n\n```js\nconst chunks = []\nreq.on('data', c => chunks.push(c)).on('end', () => {\n  const body = Buffer.concat(chunks).toString()\n})\n```\n```\n",tech:"node",displayNumber:3,sortKey:1472},{id:"node-1473",number:4,title:"How do you send a JSON response?",question:"How do you send a JSON response?",difficulty:"Basic",category:"HTTP & Networking",answer:"Set `Content-Type: application/json`, then write `JSON.stringify(data)`. Set an appropriate status code first.\n\n\n```js\nres.writeHead(200, { 'Content-Type': 'application/json' })\nres.end(JSON.stringify({ ok: true }))\n```\n```\n",tech:"node",displayNumber:4,sortKey:1473},{id:"node-1474",number:5,title:"What are HTTP status codes and their categories?",question:"What are HTTP status codes and their categories?",difficulty:"Basic",category:"HTTP & Networking",answer:"1xx informational, 2xx success (200 OK, 201 Created), 3xx redirect (301, 304), 4xx client error (400, 401, 403, 404), 5xx server error (500, 503). Use them accurately so clients/caches behave correctly.\n\n\n```js\nres.statusCode = 404; res.end('Not Found')\n```\n```\n",tech:"node",displayNumber:5,sortKey:1474},{id:"node-1475",number:6,title:"What is the difference between 401 and 403?",question:"What is the difference between 401 and 403?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"401 Unauthorized means authentication is missing/invalid (who are you?). 403 Forbidden means you're authenticated but lack permission (you can't do this).\n\n\n```js\nif (!user) return res.status(401).end()\nif (!user.isAdmin) return res.status(403).end()\n```\n```\n",tech:"node",displayNumber:6,sortKey:1475},{id:"node-1476",number:7,title:"How do you set response headers?",question:"How do you set response headers?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use `res.setHeader(name, value)` before sending the body, or pass a headers object to `res.writeHead`. Headers can't change after the first byte of the body is written.\n\n\n```js\nres.setHeader('Cache-Control', 'no-store')\n```\n```\n",tech:"node",displayNumber:7,sortKey:1476},{id:"node-1477",number:8,title:"How do you make an outbound HTTP request in Node?",question:"How do you make an outbound HTTP request in Node?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use the global `fetch` (built into modern Node), or the lower-level `http`/`https` modules, or libraries like `axios`/`undici`.\n\n\n```js\nconst data = await fetch('https://api.example.com').then(r => r.json())\n```\n```\n",tech:"node",displayNumber:8,sortKey:1477},{id:"node-1478",number:9,title:"What is `undici`?",question:"What is `undici`?",difficulty:"Advanced",category:"HTTP & Networking",answer:"`undici` is Node's modern, high-performance HTTP/1.1 client (the engine behind global `fetch`). It offers connection pooling, pipelining, and low overhead compared to the legacy `http` client.\n\n\n```js\nimport { request } from 'undici'\nconst { body } = await request('https://api.example.com')\n```\n```\n",tech:"node",displayNumber:9,sortKey:1478},{id:"node-1479",number:10,title:"How do you handle query parameters?",question:"How do you handle query parameters?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Parse the URL with the `URL`/`URLSearchParams` API. `req.url` is just a path+query string; construct a full URL to read `searchParams`.\n\n\n```js\nconst url = new URL(req.url, `http://${req.headers.host}`)\nconst page = url.searchParams.get('page')\n```\n```\n",tech:"node",displayNumber:10,sortKey:1479},{id:"node-1480",number:11,title:"What is the `URL` and `URLSearchParams` API?",question:"What is the `URL` and `URLSearchParams` API?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The WHATWG `URL` class parses and builds URLs (protocol, host, pathname, search). `URLSearchParams` parses/serializes query strings and handles encoding, replacing the legacy `url.parse`/`querystring`.\n\n\n```js\nconst u = new URL('https://x.com/a?b=1&b=2')\nu.searchParams.getAll('b') // ['1','2']\n```\n```\n",tech:"node",displayNumber:11,sortKey:1480},{id:"node-1481",number:12,title:"How do you handle different HTTP methods without a framework?",question:"How do you handle different HTTP methods without a framework?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Branch on `req.method` and `req.url` in the handler. This is verbose, which is why routers/frameworks (Express) exist.\n\n\n```js\nif (req.method === 'POST' && req.url === '/users') createUser(req, res)\n```\n```\n",tech:"node",displayNumber:12,sortKey:1481},{id:"node-1482",number:13,title:"What is the difference between HTTP/1.1 and HTTP/2?",question:"What is the difference between HTTP/1.1 and HTTP/2?",difficulty:"Advanced",category:"HTTP & Networking",answer:"HTTP/2 multiplexes many requests over one connection (no head-of-line blocking at HTTP level), compresses headers (HPACK), and supports server push. HTTP/1.1 uses one request per connection (or limited pipelining). Node's `http2` module supports it.\n\n\n```js\nrequire('http2').createSecureServer(opts, handler).listen(443)\n```\n```\n",tech:"node",displayNumber:13,sortKey:1482},{id:"node-1483",number:14,title:"What is keep-alive and why does it matter?",question:"What is keep-alive and why does it matter?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Keep-alive reuses a TCP connection for multiple requests, avoiding repeated handshake overhead and improving throughput/latency. Node clients should enable an agent with keep-alive for outbound calls.\n\n\n```js\nconst agent = new http.Agent({ keepAlive: true })\n```\n```\n",tech:"node",displayNumber:14,sortKey:1483},{id:"node-1484",number:15,title:"What is an HTTP Agent and connection pooling?",question:"What is an HTTP Agent and connection pooling?",difficulty:"Experienced",category:"HTTP & Networking",answer:"An Agent manages a pool of sockets for outbound requests, reusing connections (keep-alive) and limiting concurrency (`maxSockets`). Proper pooling prevents socket exhaustion under load.\n\n\n```js\nnew http.Agent({ keepAlive: true, maxSockets: 100 })\n```\n```\n",tech:"node",displayNumber:15,sortKey:1484},{id:"node-1485",number:16,title:"How do you handle timeouts on HTTP requests?",question:"How do you handle timeouts on HTTP requests?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Set a request/socket timeout so slow or dead peers don't hang forever. With `fetch`, use `AbortSignal.timeout`; with `http`, set `req.setTimeout` and handle the `timeout` event.\n\n\n```js\nawait fetch(url, { signal: AbortSignal.timeout(5000) })\n```\n```\n",tech:"node",displayNumber:16,sortKey:1485},{id:"node-1486",number:17,title:"How do you set server-side timeouts to protect against slow clients?",question:"How do you set server-side timeouts to protect against slow clients?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Configure `server.requestTimeout`, `server.headersTimeout`, and `server.keepAliveTimeout` to bound how long clients can hold connections, mitigating slowloris-style attacks.\n\n\n```js\nserver.requestTimeout = 30000\nserver.headersTimeout = 10000\n```\n```\n",tech:"node",displayNumber:17,sortKey:1486},{id:"node-1487",number:18,title:"What is a slowloris attack?",question:"What is a slowloris attack?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Slowloris opens many connections and sends headers/body extremely slowly, tying up server resources until it can't serve legitimate clients. Mitigate with header/request timeouts, connection limits, and a reverse proxy.\n\n\n```js\nserver.headersTimeout = 5000 // drop slow header senders\n```\n```\n",tech:"node",displayNumber:18,sortKey:1487},{id:"node-1488",number:19,title:"How do you implement graceful shutdown of an HTTP server?",question:"How do you implement graceful shutdown of an HTTP server?",difficulty:"Experienced",category:"HTTP & Networking",answer:"On SIGTERM, call `server.close()` to stop accepting new connections and wait for in-flight requests to finish, then close DB connections and exit. Add a timeout to force-close lingering keep-alive sockets.\n\n\n```js\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```\n```\n",tech:"node",displayNumber:19,sortKey:1488},{id:"node-1489",number:20,title:"What is CORS and how do you enable it?",question:"What is CORS and how do you enable it?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"CORS (Cross-Origin Resource Sharing) lets a browser call an API on a different origin, controlled by `Access-Control-*` response headers. The server whitelists origins/methods/headers; preflight `OPTIONS` requests check permission first.\n\n\n```js\nres.setHeader('Access-Control-Allow-Origin', 'https://app.com')\n```\n```\n",tech:"node",displayNumber:20,sortKey:1489},{id:"node-1490",number:21,title:"What is a CORS preflight request?",question:"What is a CORS preflight request?",difficulty:"Advanced",category:"HTTP & Networking",answer:"For \"non-simple\" requests (custom headers, methods like PUT/DELETE), the browser first sends an `OPTIONS` preflight asking whether the actual request is allowed. The server must respond with the appropriate `Access-Control-Allow-*` headers.\n\n\n```js\nif (req.method === 'OPTIONS') { res.writeHead(204, corsHeaders).end() }\n```\n```\n",tech:"node",displayNumber:21,sortKey:1490},{id:"node-1491",number:22,title:"How do you serve static files?",question:"How do you serve static files?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Map the URL to a safe file path (guard against traversal), stream the file to `res` with the right `Content-Type`, and set caching headers. Express uses `express.static`.\n\n\n```js\napp.use(express.static('public', { maxAge: '1d' }))\n```\n```\n",tech:"node",displayNumber:22,sortKey:1491},{id:"node-1492",number:23,title:"How do you handle file downloads with correct headers?",question:"How do you handle file downloads with correct headers?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Set `Content-Disposition: attachment; filename=...`, the correct `Content-Type`, and stream the file. `Content-Length` (if known) enables progress bars.\n\n\n```js\nres.setHeader('Content-Disposition', 'attachment; filename=\"report.pdf\"')\nfs.createReadStream(path).pipe(res)\n```\n```\n",tech:"node",displayNumber:23,sortKey:1492},{id:"node-1493",number:24,title:"What is chunked transfer encoding?",question:"What is chunked transfer encoding?",difficulty:"Advanced",category:"HTTP & Networking",answer:"When the total body size is unknown, the server sends it in chunks with `Transfer-Encoding: chunked` and no `Content-Length`. Piping a stream to `res` uses this automatically.\n\n\n```js\nstream.pipe(res) // Transfer-Encoding: chunked\n```\n```\n",tech:"node",displayNumber:24,sortKey:1493},{id:"node-1494",number:25,title:"How do you implement Server-Sent Events (SSE)?",question:"How do you implement Server-Sent Events (SSE)?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Respond with `Content-Type: text/event-stream`, keep the connection open, and write `data: ...\\n\\n` messages. SSE is a simple one-way server→client push over HTTP.\n\n\n```js\nres.writeHead(200, { 'Content-Type': 'text/event-stream', Connection: 'keep-alive' })\nsetInterval(() => res.write(`data: ${Date.now()}\\n\\n`), 1000)\n```\n```\n",tech:"node",displayNumber:25,sortKey:1494},{id:"node-1495",number:26,title:"What is the difference between SSE and WebSockets?",question:"What is the difference between SSE and WebSockets?",difficulty:"Advanced",category:"HTTP & Networking",answer:"SSE is one-way (server→client), text-only, over plain HTTP with auto-reconnect. WebSockets are full-duplex, binary-capable, over a dedicated upgraded connection. Use SSE for simple feeds, WebSockets for interactive/bidirectional apps.\n\n\n```js\n// SSE: EventSource; WebSocket: bidirectional ws://\n```\n```\n",tech:"node",displayNumber:26,sortKey:1495},{id:"node-1496",number:27,title:"How do WebSockets work in Node?",question:"How do WebSockets work in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"A WebSocket starts as an HTTP `Upgrade` request, then switches to a persistent full-duplex TCP channel. Libraries like `ws` or `socket.io` handle the handshake and framing.\n\n\n```js\nconst wss = new (require('ws').Server)({ port: 8080 })\nwss.on('connection', ws => ws.on('message', m => ws.send('echo ' + m)))\n```\n```\n",tech:"node",displayNumber:27,sortKey:1496},{id:"node-1497",number:28,title:"What is the WebSocket handshake?",question:"What is the WebSocket handshake?",difficulty:"Experienced",category:"HTTP & Networking",answer:"The client sends an HTTP request with `Upgrade: websocket` and a `Sec-WebSocket-Key`; the server responds 101 Switching Protocols with a derived `Sec-WebSocket-Accept`. After that, both sides exchange WebSocket frames.\n\n\n```js\nserver.on('upgrade', (req, socket, head) => wss.handleUpgrade(req, socket, head, cb))\n```\n```\n",tech:"node",displayNumber:28,sortKey:1497},{id:"node-1498",number:29,title:"How do you create a raw TCP server?",question:"How do you create a raw TCP server?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use the `net` module's `createServer`, which gives a Duplex socket per connection for reading/writing raw bytes — the foundation beneath HTTP.\n\n\n```js\nrequire('net').createServer(sock => sock.pipe(sock)).listen(9000) // echo\n```\n```\n",tech:"node",displayNumber:29,sortKey:1498},{id:"node-1499",number:30,title:"What is the difference between TCP and UDP in Node?",question:"What is the difference between TCP and UDP in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"`net` provides reliable, ordered TCP streams. `dgram` provides UDP: connectionless, unordered, no delivery guarantee, but low latency — good for telemetry, gaming, DNS.\n\n\n```js\nconst udp = require('dgram').createSocket('udp4')\nudp.send('ping', 41234, 'host')\n```\n```\n",tech:"node",displayNumber:30,sortKey:1499},{id:"node-1500",number:31,title:"How do you handle DNS lookups?",question:"How do you handle DNS lookups?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The `dns` module resolves hostnames. `dns.lookup` uses the OS resolver (thread pool), while `dns.resolve*` query DNS servers directly. Caching lookups reduces latency.\n\n\n```js\nconst { address } = await require('dns').promises.lookup('example.com')\n```\n```\n",tech:"node",displayNumber:31,sortKey:1500},{id:"node-1501",number:32,title:"Why can `dns.lookup` block the thread pool?",question:"Why can `dns.lookup` block the thread pool?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`dns.lookup` calls the synchronous OS resolver (`getaddrinfo`) on a libuv thread-pool thread. Many concurrent lookups can saturate the default 4-thread pool, delaying other pool work (fs, crypto). Increase `UV_THREADPOOL_SIZE` or cache DNS.\n\n\n```js\nprocess.env.UV_THREADPOOL_SIZE = 16\n```\n```\n",tech:"node",displayNumber:32,sortKey:1501},{id:"node-1502",number:33,title:"How do you implement HTTPS in Node?",question:"How do you implement HTTPS in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use `https.createServer` with a TLS key and certificate. In production, TLS is often terminated at a reverse proxy/load balancer instead of in Node.\n\n\n```js\nhttps.createServer({ key, cert }, handler).listen(443)\n```\n```\n",tech:"node",displayNumber:33,sortKey:1502},{id:"node-1503",number:34,title:"What is TLS termination and where should it happen?",question:"What is TLS termination and where should it happen?",difficulty:"Experienced",category:"HTTP & Networking",answer:"TLS termination is decrypting HTTPS traffic. It commonly happens at a load balancer/reverse proxy (nginx, ALB) which forwards plain HTTP to Node, simplifying cert management and offloading crypto from the app.\n\n\n```js\n// nginx terminates TLS -> proxies to http://localhost:3000\n```\n```\n",tech:"node",displayNumber:34,sortKey:1503},{id:"node-1504",number:35,title:"How do you trust the `X-Forwarded-For` header safely?",question:"How do you trust the `X-Forwarded-For` header safely?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Behind a proxy, the client IP is in `X-Forwarded-For`, but it's spoofable if you trust it blindly. Configure trusted proxies (Express `trust proxy`) so only your infra's forwarded values are honored.\n\n\n```js\napp.set('trust proxy', 'loopback, 10.0.0.0/8')\n```\n```\n",tech:"node",displayNumber:35,sortKey:1504},{id:"node-1505",number:36,title:"How do you handle large request bodies safely?",question:"How do you handle large request bodies safely?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Enforce a max body size and reject/destroy oversized requests to prevent memory exhaustion. Body parsers accept a `limit` option; for streaming uploads, count bytes and abort past the limit.\n\n\n```js\napp.use(express.json({ limit: '1mb' }))\n```\n```\n",tech:"node",displayNumber:36,sortKey:1505},{id:"node-1506",number:37,title:"What is HTTP pipelining?",question:"What is HTTP pipelining?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Pipelining sends multiple HTTP/1.1 requests without waiting for responses, which must return in order (causing head-of-line blocking). Rarely used; HTTP/2 multiplexing solved the underlying need.\n\n\n```js\n// HTTP/2 multiplexing supersedes HTTP/1.1 pipelining\n```\n```\n",tech:"node",displayNumber:37,sortKey:1506},{id:"node-1507",number:38,title:"How do you proxy a request through Node?",question:"How do you proxy a request through Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Forward the incoming request to an upstream and pipe the upstream response back, copying relevant headers. Libraries like `http-proxy` handle edge cases (websockets, headers, errors).\n\n\n```js\nconst upstream = http.request(target, up => up.pipe(res))\nreq.pipe(upstream)\n```\n```\n",tech:"node",displayNumber:38,sortKey:1507},{id:"node-1508",number:39,title:"What are hop-by-hop vs end-to-end headers?",question:"What are hop-by-hop vs end-to-end headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Hop-by-hop headers (`Connection`, `Keep-Alive`, `Transfer-Encoding`) apply to a single connection and must not be forwarded by proxies. End-to-end headers are passed through unchanged. Proxies must strip hop-by-hop headers.\n\n\n```js\ndelete headers['connection']; delete headers['transfer-encoding']\n```\n```\n",tech:"node",displayNumber:39,sortKey:1508},{id:"node-1509",number:40,title:"How do you implement rate limiting?",question:"How do you implement rate limiting?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Track request counts per client (IP/key) in a window using a counter store (in-memory for one instance, Redis for many), and return 429 when the limit is exceeded. Algorithms: fixed window, sliding window, token bucket.\n\n\n```js\napp.use(rateLimit({ windowMs: 60000, max: 100 }))\n```\n```\n",tech:"node",displayNumber:40,sortKey:1509},{id:"node-1510",number:41,title:"What is the token bucket algorithm?",question:"What is the token bucket algorithm?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Tokens refill at a fixed rate up to a capacity; each request consumes one. Requests proceed if a token is available, else are throttled. It permits bursts up to the bucket size while enforcing an average rate.\n\n\n```js\ntokens = Math.min(cap, tokens + elapsed * rate); if (tokens >= 1) { tokens--; allow() }\n```\n```\n",tech:"node",displayNumber:41,sortKey:1510},{id:"node-1511",number:42,title:"How do you implement retries with idempotency?",question:"How do you implement retries with idempotency?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Retry only idempotent operations (GET/PUT/DELETE) or use an idempotency key so the server dedupes repeated POSTs. Retrying non-idempotent calls blindly can duplicate side effects.\n\n\n```js\nfetch(url, { method: 'POST', headers: { 'Idempotency-Key': uuid } })\n```\n```\n",tech:"node",displayNumber:42,sortKey:1511},{id:"node-1512",number:43,title:"What is a circuit breaker?",question:"What is a circuit breaker?",difficulty:"Experienced",category:"HTTP & Networking",answer:"A circuit breaker stops calling a failing dependency after a threshold of errors (open state), failing fast to protect resources, then periodically probes (half-open) before resuming (closed). It prevents cascading failures.\n\n\n```js\nif (breaker.isOpen()) throw new Error('circuit open')\n```\n```\n",tech:"node",displayNumber:43,sortKey:1512},{id:"node-1513",number:44,title:"How do you handle streaming responses from an upstream API?",question:"How do you handle streaming responses from an upstream API?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Pipe the upstream response stream directly to your client response so you don't buffer the whole payload, preserving low memory and latency.\n\n\n```js\nconst upstream = await fetch(url)\nReadable.fromWeb(upstream.body).pipe(res)\n```\n```\n",tech:"node",displayNumber:44,sortKey:1513},{id:"node-1514",number:45,title:"What is `Content-Type` negotiation?",question:"What is `Content-Type` negotiation?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Content negotiation picks a response format based on the client's `Accept` header (JSON, XML, HTML). The server inspects `Accept` and responds with the best supported representation and matching `Content-Type`.\n\n\n```js\nres.format({ json: () => res.json(data), html: () => res.render('view', data) })\n```\n```\n",tech:"node",displayNumber:45,sortKey:1514},{id:"node-1515",number:46,title:"How do you set cookies from a Node server?",question:"How do you set cookies from a Node server?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Send a `Set-Cookie` header with attributes like `HttpOnly`, `Secure`, `SameSite`, `Max-Age`, and `Path`. `HttpOnly`+`Secure`+`SameSite` protect against XSS/CSRF theft.\n\n\n```js\nres.setHeader('Set-Cookie', 'sid=abc; HttpOnly; Secure; SameSite=Strict')\n```\n```\n",tech:"node",displayNumber:46,sortKey:1515},{id:"node-1516",number:47,title:"What does the `SameSite` cookie attribute do?",question:"What does the `SameSite` cookie attribute do?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`SameSite` controls whether cookies are sent on cross-site requests: `Strict` (never), `Lax` (top-level navigations only), `None` (always, requires `Secure`). It mitigates CSRF.\n\n\n```js\n'session=x; SameSite=Lax; Secure'\n```\n```\n",tech:"node",displayNumber:47,sortKey:1516},{id:"node-1517",number:48,title:"How do you compress HTTP responses?",question:"How do you compress HTTP responses?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Check the client's `Accept-Encoding`, pipe the body through gzip/brotli, and set `Content-Encoding`. Middleware (`compression`) automates this; often done at the proxy instead.\n\n\n```js\napp.use(require('compression')())\n```\n```\n",tech:"node",displayNumber:48,sortKey:1517},{id:"node-1518",number:49,title:"What is ETag and conditional requests?",question:"What is ETag and conditional requests?",difficulty:"Experienced",category:"HTTP & Networking",answer:"An `ETag` is a content fingerprint. Clients send `If-None-Match`; if unchanged, the server returns `304 Not Modified` with no body, saving bandwidth. `Last-Modified`/`If-Modified-Since` work similarly by date.\n\n\n```js\nif (req.headers['if-none-match'] === etag) return res.writeHead(304).end()\n```\n```\n",tech:"node",displayNumber:49,sortKey:1518},{id:"node-1519",number:50,title:"How do you implement HTTP caching headers?",question:"How do you implement HTTP caching headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Use `Cache-Control` (`max-age`, `no-store`, `private`, `immutable`) to instruct browsers/CDNs, plus validators (`ETag`, `Last-Modified`) for revalidation. Correct caching cuts load and latency.\n\n\n```js\nres.setHeader('Cache-Control', 'public, max-age=31536000, immutable')\n```\n```\n",tech:"node",displayNumber:50,sortKey:1519},{id:"node-1520",number:51,title:"What is a reverse proxy and why use one in front of Node?",question:"What is a reverse proxy and why use one in front of Node?",difficulty:"Experienced",category:"HTTP & Networking",answer:"A reverse proxy (nginx, HAProxy) sits in front of Node to terminate TLS, serve static files, load-balance across instances, buffer slow clients, and add caching/compression — offloading concerns from the app.\n\n\n```js\n// nginx: proxy_pass http://localhost:3000\n```\n```\n",tech:"node",displayNumber:51,sortKey:1520},{id:"node-1521",number:52,title:"How do you handle sticky sessions behind a load balancer?",question:"How do you handle sticky sessions behind a load balancer?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Sticky sessions pin a client to one instance (via cookie/IP hash) so in-memory session state works. Better: make servers stateless with a shared session store (Redis) so any instance can serve any request.\n\n\n```js\napp.use(session({ store: new RedisStore({ client }) })) // no stickiness needed\n```\n```\n",tech:"node",displayNumber:52,sortKey:1521},{id:"node-1522",number:53,title:"What is HTTP long polling?",question:"What is HTTP long polling?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Long polling holds a request open until the server has data (or a timeout), then the client immediately re-requests. It simulates push over plain HTTP but is less efficient than SSE/WebSockets.\n\n\n```js\n// server holds res until an event, then res.json(update)\n```\n```\n",tech:"node",displayNumber:53,sortKey:1522},{id:"node-1523",number:54,title:"How do you validate and sanitize incoming request data?",question:"How do you validate and sanitize incoming request data?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Validate structure/types against a schema (zod, Joi) at the boundary, reject invalid input with 400, and sanitize to prevent injection. Never trust client data.\n\n\n```js\nconst body = schema.parse(req.body) // throws on invalid input\n```\n```\n",tech:"node",displayNumber:54,sortKey:1523},{id:"node-1524",number:55,title:"What is the difference between `res.end` and `res.write`?",question:"What is the difference between `res.end` and `res.write`?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"`res.write` sends a body chunk and keeps the response open; `res.end` optionally sends a final chunk and closes the response. You can call `write` many times then `end` once.\n\n\n```js\nres.write('part1'); res.write('part2'); res.end()\n```\n```\n",tech:"node",displayNumber:55,sortKey:1524},{id:"node-1525",number:56,title:'How do you avoid "headers already sent" errors?',question:'How do you avoid "headers already sent" errors?',difficulty:"Advanced",category:"HTTP & Networking",answer:"Once the body/headers are sent, don't set headers or send another response. Guard with `res.headersSent`, and always `return` after sending to avoid falling through to more response code.\n\n\n```js\nif (err) return res.status(500).end() // return prevents double-send\n```\n```\n",tech:"node",displayNumber:56,sortKey:1525},{id:"node-1526",number:57,title:"How do you stream a video with range support?",question:"How do you stream a video with range support?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Parse the `Range` header, respond with 206 Partial Content, `Content-Range`, and `Accept-Ranges: bytes`, then stream the requested byte slice. This enables seeking in media players.\n\n\n```js\nres.writeHead(206, { 'Content-Range': `bytes ${start}-${end}/${size}` })\nfs.createReadStream(file, { start, end }).pipe(res)\n```\n```\n",tech:"node",displayNumber:57,sortKey:1526},{id:"node-1527",number:58,title:"What is HSTS?",question:"What is HSTS?",difficulty:"Experienced",category:"HTTP & Networking",answer:"HTTP Strict Transport Security (`Strict-Transport-Security` header) tells browsers to only use HTTPS for your domain for a duration, preventing protocol-downgrade and SSL-strip attacks.\n\n\n```js\nres.setHeader('Strict-Transport-Security', 'max-age=31536000; includeSubDomains')\n```\n```\n",tech:"node",displayNumber:58,sortKey:1527},{id:"node-1528",number:59,title:"How do you handle multipart/form-data uploads?",question:"How do you handle multipart/form-data uploads?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Parse the multipart body with a streaming parser (`busboy`, `multer`) that separates fields and files, streaming files to storage rather than buffering. Enforce size/type limits.\n\n\n```js\napp.post('/upload', multer({ dest: 'uploads/' }).single('file'), handler)\n```\n```\n",tech:"node",displayNumber:59,sortKey:1528},{id:"node-1529",number:60,title:"What is `keepAliveTimeout` and why tune it behind a load balancer?",question:"What is `keepAliveTimeout` and why tune it behind a load balancer?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`keepAliveTimeout` is how long Node keeps an idle connection open. It should be slightly longer than the upstream load balancer's idle timeout to avoid races where the LB reuses a socket Node just closed (causing 502s).\n\n\n```js\nserver.keepAliveTimeout = 65000 // > ALB's 60s\n```\n```\n",tech:"node",displayNumber:60,sortKey:1529},{id:"node-1530",number:61,title:"How do you detect and handle a dropped client connection?",question:"How do you detect and handle a dropped client connection?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Listen for `req.on('close')` / `res.on('close')` (or an AbortSignal) to know the client disconnected, then stop expensive work and release resources for that request.\n\n\n```js\nreq.on('close', () => controller.abort()) // cancel work if client left\n```\n```\n",tech:"node",displayNumber:61,sortKey:1530},{id:"node-1531",number:62,title:"What is HTTP/3 and QUIC?",question:"What is HTTP/3 and QUIC?",difficulty:"Experienced",category:"HTTP & Networking",answer:"HTTP/3 runs over QUIC (a UDP-based transport) instead of TCP, eliminating TCP head-of-line blocking, enabling faster connection setup (0-RTT), and better mobility. Node support is experimental/via proxies.\n\n\n```js\n// typically terminated at a CDN/edge; Node speaks HTTP/1.1 or /2 upstream\n```\n```\n",tech:"node",displayNumber:62,sortKey:1531},{id:"node-1532",number:63,title:"How do you implement request tracing/correlation IDs?",question:"How do you implement request tracing/correlation IDs?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Generate or propagate a correlation ID (from `X-Request-Id`/`traceparent`) per request, store it in `AsyncLocalStorage`, and include it in all logs and downstream calls to trace a request across services.\n\n\n```js\nconst id = req.headers['x-request-id'] ?? crypto.randomUUID()\nals.run({ id }, () => next())\n```\n```\n",tech:"node",displayNumber:63,sortKey:1532},{id:"node-1533",number:64,title:"How do you secure HTTP headers?",question:"How do you secure HTTP headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Set defensive headers: `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options`, `Referrer-Policy`, HSTS. Middleware like `helmet` applies sensible defaults.\n\n\n```js\napp.use(require('helmet')())\n```\n```\n",tech:"node",displayNumber:64,sortKey:1533},{id:"node-1534",number:65,title:"What is `Content-Security-Policy`?",question:"What is `Content-Security-Policy`?",difficulty:"Experienced",category:"HTTP & Networking",answer:"CSP restricts which sources scripts/styles/images can load from, mitigating XSS and data injection. It's declared via a header listing allowed origins per resource type.\n\n\n```js\nres.setHeader('Content-Security-Policy', \"default-src 'self'\")\n```\n```\n",tech:"node",displayNumber:65,sortKey:1534},{id:"node-1535",number:66,title:"How do you handle concurrent connections limit?",question:"How do you handle concurrent connections limit?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Set `server.maxConnections`, use a reverse proxy for buffering, scale horizontally with cluster/instances, and cap outbound `maxSockets`. Monitor `server.getConnections()` to observe load.\n\n\n```js\nserver.maxConnections = 10000\n```\n```\n",tech:"node",displayNumber:66,sortKey:1535},{id:"node-1536",number:67,title:"What is the difference between `http` and `https` Agent?",question:"What is the difference between `http` and `https` Agent?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Both pool sockets, but the `https.Agent` also manages TLS session parameters and can reuse TLS sessions. Use the matching agent for the protocol; mixing them breaks connection reuse.\n\n\n```js\nconst agent = new https.Agent({ keepAlive: true })\n```\n```\n",tech:"node",displayNumber:67,sortKey:1536},{id:"node-1537",number:68,title:"How do you make outbound requests resilient?",question:"How do you make outbound requests resilient?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Combine timeouts, retries with backoff+jitter (idempotent only), circuit breakers, connection pooling, and bulkheads (concurrency limits). This prevents a slow dependency from taking down your service.\n\n\n```js\nawait retry(() => fetch(url, { signal: AbortSignal.timeout(3000) }), { retries: 3 })\n```\n```\n",tech:"node",displayNumber:68,sortKey:1537},{id:"node-1538",number:69,title:"How do you test HTTP endpoints?",question:"How do you test HTTP endpoints?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use `supertest` (or Node's `fetch` against an ephemeral port) to send requests to the app instance and assert on status, headers, and body — without a real network or fixed port.\n\n\n```js\nawait request(app).get('/health').expect(200)\n```\n```\n",tech:"node",displayNumber:69,sortKey:1538},{id:"node-1539",number:70,title:"How do you detect and prevent SSRF?",question:"How do you detect and prevent SSRF?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Server-Side Request Forgery tricks your server into fetching internal/unintended URLs. Prevent by validating/whitelisting outbound hosts, blocking private IP ranges and redirects to them, and never fetching raw user-supplied URLs.\n\n\n```js\nif (isPrivateIp(new URL(userUrl).hostname)) throw new Error('blocked')\n```\n```\n",tech:"node",displayNumber:70,sortKey:1539},{id:"node-1540",number:1,title:"What is Express.js?",question:"What is Express.js?",difficulty:"Basic",category:"Express & REST APIs",answer:"Express is a minimal, unopinionated web framework for Node that provides routing, middleware, and helpers over the raw `http` module, simplifying building APIs and web apps.\n\n\n```js\nconst app = require('express')()\napp.get('/', (req, res) => res.send('Hello'))\napp.listen(3000)\n```\n```\n",tech:"node",displayNumber:1,sortKey:1540},{id:"node-1541",number:2,title:"What is middleware in Express?",question:"What is middleware in Express?",difficulty:"Basic",category:"Express & REST APIs",answer:"Middleware are functions `(req, res, next)` that run in order on the request pipeline. They can modify req/res, end the response, or call `next()` to pass control. Used for logging, auth, parsing, etc.\n\n\n```js\napp.use((req, res, next) => { req.time = Date.now(); next() })\n```\n```\n",tech:"node",displayNumber:2,sortKey:1541},{id:"node-1542",number:3,title:"What is the role of `next()`?",question:"What is the role of `next()`?",difficulty:"Basic",category:"Express & REST APIs",answer:"`next()` passes control to the next middleware/route handler. `next(err)` skips to error-handling middleware. Forgetting to call `next()` or send a response leaves the request hanging.\n\n\n```js\napp.use((req, res, next) => auth(req) ? next() : res.status(401).end())\n```\n```\n",tech:"node",displayNumber:3,sortKey:1542},{id:"node-1543",number:4,title:"What are the types of middleware?",question:"What are the types of middleware?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Application-level (`app.use`), router-level (`router.use`), built-in (`express.json`, `express.static`), third-party (`cors`, `helmet`), and error-handling (four-arg `(err, req, res, next)`).\n\n\n```js\napp.use(express.json())            // built-in\napp.use('/api', apiRouter)         // router-level\n```\n```\n",tech:"node",displayNumber:4,sortKey:1543},{id:"node-1544",number:5,title:"How does Express match routes?",question:"How does Express match routes?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Express matches by HTTP method and path pattern in the order routes/middleware are registered. The first matching handler that responds (without `next`) wins. Order matters.\n\n\n```js\napp.get('/users/:id', handler) // matches GET /users/42\n```\n```\n",tech:"node",displayNumber:5,sortKey:1544},{id:"node-1545",number:6,title:"What are route parameters?",question:"What are route parameters?",difficulty:"Basic",category:"Express & REST APIs",answer:"Route params are named URL segments (`:id`) captured into `req.params`. They identify resources in RESTful paths.\n\n\n```js\napp.get('/users/:id', (req, res) => res.send(req.params.id))\n```\n```\n",tech:"node",displayNumber:6,sortKey:1545},{id:"node-1546",number:7,title:"What is the difference between `req.params`, `req.query`, and `req.body`?",question:"What is the difference between `req.params`, `req.query`, and `req.body`?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`req.params` holds URL path params (`/users/:id`), `req.query` holds the query string (`?page=2`), and `req.body` holds the parsed request body (needs a body-parsing middleware).\n\n\n```js\n// GET /users/5?full=true  body {name}\nreq.params.id // '5'\nreq.query.full // 'true'\n```\n```\n",tech:"node",displayNumber:7,sortKey:1546},{id:"node-1547",number:8,title:"How do you parse JSON request bodies?",question:"How do you parse JSON request bodies?",difficulty:"Basic",category:"Express & REST APIs",answer:"Add `express.json()` middleware, which reads and parses `application/json` bodies into `req.body`. Set a `limit` to bound size.\n\n\n```js\napp.use(express.json({ limit: '1mb' }))\n```\n```\n",tech:"node",displayNumber:8,sortKey:1547},{id:"node-1548",number:9,title:"What is a Router in Express?",question:"What is a Router in Express?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`express.Router()` is a mini-app for grouping related routes and middleware into a modular, mountable unit, keeping large apps organized.\n\n\n```js\nconst r = express.Router()\nr.get('/', list); app.use('/products', r)\n```\n```\n",tech:"node",displayNumber:9,sortKey:1548},{id:"node-1549",number:10,title:"How do you handle errors in Express?",question:"How do you handle errors in Express?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Define error-handling middleware with four arguments `(err, req, res, next)` after all routes. Pass errors via `next(err)`; Express routes them to this handler.\n\n\n```js\napp.use((err, req, res, next) => res.status(err.status || 500).json({ error: err.message }))\n```\n```\n",tech:"node",displayNumber:10,sortKey:1549},{id:"node-1550",number:11,title:"How do you handle errors in async route handlers?",question:"How do you handle errors in async route handlers?",difficulty:"Advanced",category:"Express & REST APIs",answer:"In Express 4, thrown errors in async handlers aren't caught automatically — wrap handlers to forward rejections to `next`, or use a wrapper/`express-async-errors`. Express 5 handles rejected promises natively.\n\n\n```js\nconst wrap = fn => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next)\napp.get('/x', wrap(async (req, res) => { await work(); res.end() }))\n```\n```\n",tech:"node",displayNumber:11,sortKey:1550},{id:"node-1551",number:12,title:"What are the principles of REST?",question:"What are the principles of REST?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"REST is stateless, resource-oriented (nouns as URIs), uses standard HTTP methods/status codes, supports a uniform interface, and is cacheable. Each request carries all needed context; the server holds no client session state.\n\n\n```js\n// GET /orders/1  (resource), not GET /getOrder?id=1 (RPC)\n```\n```\n",tech:"node",displayNumber:12,sortKey:1551},{id:"node-1552",number:13,title:"How do HTTP methods map to CRUD?",question:"How do HTTP methods map to CRUD?",difficulty:"Basic",category:"Express & REST APIs",answer:"GET=read, POST=create, PUT=replace/update, PATCH=partial update, DELETE=remove. Using them semantically enables caching, idempotency, and predictable APIs.\n\n\n```js\napp.post('/users', create)\napp.patch('/users/:id', update)\n```\n```\n",tech:"node",displayNumber:13,sortKey:1552},{id:"node-1553",number:14,title:"What is idempotency and which methods are idempotent?",question:"What is idempotency and which methods are idempotent?",difficulty:"Advanced",category:"Express & REST APIs",answer:"An idempotent method produces the same result whether called once or many times. GET, PUT, DELETE, HEAD are idempotent; POST is not (each call creates a new resource). This matters for safe retries.\n\n\n```js\n// PUT /users/5 with same body -> same final state every time\n```\n```\n",tech:"node",displayNumber:14,sortKey:1553},{id:"node-1554",number:15,title:"What is the difference between PUT and PATCH?",question:"What is the difference between PUT and PATCH?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"PUT replaces the entire resource (idempotent full update); PATCH applies a partial modification. Sending only changed fields to PUT can wipe omitted fields.\n\n\n```js\napp.put('/users/:id', replaceUser)   // full\napp.patch('/users/:id', patchUser)    // partial\n```\n```\n",tech:"node",displayNumber:15,sortKey:1554},{id:"node-1555",number:16,title:"How do you version a REST API?",question:"How do you version a REST API?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Common approaches: URL prefix (`/v1/...`), a custom header, or media-type versioning (`Accept: application/vnd.api.v2+json`). Versioning lets you evolve without breaking existing clients.\n\n\n```js\napp.use('/v1', v1Router); app.use('/v2', v2Router)\n```\n```\n",tech:"node",displayNumber:16,sortKey:1555},{id:"node-1556",number:17,title:"What status code should a successful POST return?",question:"What status code should a successful POST return?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"201 Created with a `Location` header pointing to the new resource (and often the created body). 200 is acceptable if no resource is created; 204 for success with no content.\n\n\n```js\nres.status(201).location(`/users/${id}`).json(user)\n```\n```\n",tech:"node",displayNumber:17,sortKey:1556},{id:"node-1557",number:18,title:"How do you implement pagination?",question:"How do you implement pagination?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Offset-based (`?page&limit`) is simple but slow/inconsistent on large/changing data. Cursor-based (`?after=<id>`) is stable and efficient for large sets. Return metadata (next cursor/total).\n\n\n```js\nconst rows = await db.find({ _id: { $gt: cursor } }).limit(limit)\n```\n```\n",tech:"node",displayNumber:18,sortKey:1557},{id:"node-1558",number:19,title:"Why is cursor pagination better than offset for large datasets?",question:"Why is cursor pagination better than offset for large datasets?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Offset requires the DB to scan and skip N rows (slow as N grows) and can skip/duplicate items when data changes between pages. Cursors seek directly via an indexed key, giving stable, O(log n) navigation.\n\n\n```js\n// WHERE id > :cursor ORDER BY id LIMIT :n  (uses index, no skip)\n```\n```\n",tech:"node",displayNumber:19,sortKey:1558},{id:"node-1559",number:20,title:"How do you validate request input in Express?",question:"How do you validate request input in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Validate against a schema (zod/Joi/express-validator) in middleware, rejecting invalid requests with 400 and clear error details before they reach business logic.\n\n\n```js\napp.post('/u', (req, res, next) => { req.body = schema.parse(req.body); next() })\n```\n```\n",tech:"node",displayNumber:20,sortKey:1559},{id:"node-1560",number:21,title:"How do you structure a scalable Express app?",question:"How do you structure a scalable Express app?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Layer by responsibility: routes/controllers (HTTP), services (business logic), repositories (data access), and organize by feature. Keep handlers thin, put logic in services, and centralize error/validation handling.\n\n\n```js\n// routes -> controller -> service -> repository -> db\n```\n```\n",tech:"node",displayNumber:21,sortKey:1560},{id:"node-1561",number:22,title:"What is the difference between `app.use` and `app.get`?",question:"What is the difference between `app.use` and `app.get`?",difficulty:"Basic",category:"Express & REST APIs",answer:"`app.use` mounts middleware for all methods (and matches path prefixes). `app.get` (and other verbs) registers a handler for a specific method and exact-ish path.\n\n\n```js\napp.use('/api', logger)     // all methods under /api\napp.get('/api/users', list) // only GET\n```\n```\n",tech:"node",displayNumber:22,sortKey:1561},{id:"node-1562",number:23,title:"How do you serve a REST API and a SPA together?",question:"How do you serve a REST API and a SPA together?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Mount API routes first, serve static assets, then add a catch-all that returns `index.html` for client-side routing (excluding API paths).\n\n\n```js\napp.use('/api', api)\napp.use(express.static('dist'))\napp.get('*', (req, res) => res.sendFile('index.html'))\n```\n```\n",tech:"node",displayNumber:23,sortKey:1562},{id:"node-1563",number:24,title:"What is HATEOAS?",question:"What is HATEOAS?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Hypermedia As The Engine Of Application State: responses include links to related actions/resources, so clients navigate the API dynamically rather than hardcoding URLs. It is the highest REST maturity level, rarely fully adopted.\n\n\n```js\nres.json({ id, _links: { self: `/orders/${id}`, cancel: `/orders/${id}/cancel` } })\n```\n```\n",tech:"node",displayNumber:24,sortKey:1563},{id:"node-1564",number:25,title:"How do you handle 404 for unknown routes?",question:"How do you handle 404 for unknown routes?",difficulty:"Basic",category:"Express & REST APIs",answer:"Add a final catch-all middleware after all routes that responds with 404, since a request reaching it matched nothing earlier.\n\n\n```js\napp.use((req, res) => res.status(404).json({ error: 'Not found' }))\n```\n```\n",tech:"node",displayNumber:25,sortKey:1564},{id:"node-1565",number:26,title:"What is CORS middleware and how do you configure it?",question:"What is CORS middleware and how do you configure it?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"The `cors` middleware sets `Access-Control-*` headers and handles preflight. Configure allowed origins, methods, and credentials explicitly rather than using a wildcard with credentials.\n\n\n```js\napp.use(cors({ origin: 'https://app.com', credentials: true }))\n```\n```\n",tech:"node",displayNumber:26,sortKey:1565},{id:"node-1566",number:27,title:"How do you implement authentication middleware?",question:"How do you implement authentication middleware?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Write middleware that reads a token/cookie, verifies it, attaches the user to `req`, and calls `next()` or returns 401. Apply it to protected routers.\n\n\n```js\nfunction auth(req, res, next) {\n  try { req.user = verify(req.headers.authorization); next() }\n  catch { res.status(401).end() }\n}\n```\n```\n",tech:"node",displayNumber:27,sortKey:1566},{id:"node-1567",number:28,title:"How do you implement role-based authorization?",question:"How do you implement role-based authorization?",difficulty:"Advanced",category:"Express & REST APIs",answer:"After authentication, add middleware that checks the user's role/permissions against the route's requirement, returning 403 if insufficient.\n\n\n```js\nconst requireRole = role => (req, res, next) =>\n  req.user.role === role ? next() : res.status(403).end()\napp.delete('/users/:id', auth, requireRole('admin'), remove)\n```\n```\n",tech:"node",displayNumber:28,sortKey:1567},{id:"node-1568",number:29,title:"What is the difference between authentication and authorization?",question:"What is the difference between authentication and authorization?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Authentication verifies identity (who you are); authorization determines access (what you may do). You authenticate first, then authorize per resource/action.\n\n\n```js\napp.use(authenticate) // who\napp.use('/admin', authorizeAdmin) // what\n```\n```\n",tech:"node",displayNumber:29,sortKey:1568},{id:"node-1569",number:30,title:"How do you handle request logging?",question:"How do you handle request logging?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Use `morgan` for HTTP access logs or a structured logger (`pino`) in middleware capturing method, path, status, and latency. Add a correlation ID for tracing.\n\n\n```js\napp.use(require('morgan')('combined'))\n```\n```\n",tech:"node",displayNumber:30,sortKey:1569},{id:"node-1570",number:31,title:"How do you set response compression?",question:"How do you set response compression?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Add the `compression` middleware to gzip/deflate responses based on `Accept-Encoding`. Often skipped in favor of proxy-level compression.\n\n\n```js\napp.use(require('compression')())\n```\n```\n",tech:"node",displayNumber:31,sortKey:1570},{id:"node-1571",number:32,title:"What is the order of middleware execution and why does it matter?",question:"What is the order of middleware execution and why does it matter?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Middleware runs top-to-bottom as registered. Placement matters: body parsers before handlers that need `req.body`, auth before protected routes, error handler last. Wrong order causes bugs (e.g. missing body).\n\n\n```js\napp.use(express.json()) // must come before routes using req.body\n```\n```\n",tech:"node",displayNumber:32,sortKey:1571},{id:"node-1572",number:33,title:"How do you mount sub-applications?",question:"How do you mount sub-applications?",difficulty:"Advanced",category:"Express & REST APIs",answer:"An Express app or Router can be mounted at a path with `app.use('/path', subApp)`, giving modular composition and independent middleware per mount.\n\n\n```js\napp.use('/admin', adminApp)\n```\n```\n",tech:"node",displayNumber:33,sortKey:1572},{id:"node-1573",number:34,title:"How do you implement API rate limiting in Express?",question:"How do you implement API rate limiting in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use `express-rate-limit` with a shared store (Redis) for multi-instance correctness, returning 429 with `Retry-After` when limits are hit.\n\n\n```js\napp.use(rateLimit({ windowMs: 60000, max: 100, store: redisStore }))\n```\n```\n",tech:"node",displayNumber:34,sortKey:1573},{id:"node-1574",number:35,title:"What is the `trust proxy` setting?",question:"What is the `trust proxy` setting?",difficulty:"Experienced",category:"Express & REST APIs",answer:"`app.set('trust proxy', ...)` tells Express to trust `X-Forwarded-*` headers from your proxy so `req.ip`, protocol, and secure-cookie logic reflect the real client. Misconfiguring it lets clients spoof their IP.\n\n\n```js\napp.set('trust proxy', 1) // trust first proxy hop\n```\n```\n",tech:"node",displayNumber:35,sortKey:1574},{id:"node-1575",number:36,title:"How do you handle file uploads in Express?",question:"How do you handle file uploads in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Use `multer` (multipart parser) to stream files to disk/memory/cloud with size and type limits, exposing files on `req.file`/`req.files`.\n\n\n```js\napp.post('/avatar', multer({ limits: { fileSize: 2e6 } }).single('img'), save)\n```\n```\n",tech:"node",displayNumber:36,sortKey:1575},{id:"node-1576",number:37,title:"How do you send different content types?",question:"How do you send different content types?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`res.json()` sends JSON, `res.send()` infers type, `res.sendFile()` streams files, `res.render()` renders templates, and `res.type()` sets the `Content-Type` explicitly.\n\n\n```js\nres.type('text/csv').send(csvString)\n```\n```\n",tech:"node",displayNumber:37,sortKey:1576},{id:"node-1577",number:38,title:"What is content negotiation in Express?",question:"What is content negotiation in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"`res.format()` picks a handler based on the client's `Accept` header, serving JSON, HTML, or other representations of the same resource.\n\n\n```js\nres.format({ 'application/json': () => res.json(x), 'text/html': () => res.render('x', x) })\n```\n```\n",tech:"node",displayNumber:38,sortKey:1577},{id:"node-1578",number:39,title:"How do you implement idempotency keys for POST requests?",question:"How do you implement idempotency keys for POST requests?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Store the result keyed by a client-supplied `Idempotency-Key`; on repeat, return the cached response instead of reprocessing. Prevents duplicate charges/orders on retries.\n\n\n```js\nconst cached = await store.get(req.headers['idempotency-key'])\nif (cached) return res.json(cached)\n```\n```\n",tech:"node",displayNumber:39,sortKey:1578},{id:"node-1579",number:40,title:"How do you structure error responses consistently?",question:"How do you structure error responses consistently?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use a standard error shape (code, message, details) and centralize it in error middleware. Consider RFC 7807 \"problem+json\". Avoid leaking stack traces in production.\n\n\n```js\nres.status(422).json({ type: 'validation', errors: [{ field: 'email', msg: 'invalid' }] })\n```\n```\n",tech:"node",displayNumber:40,sortKey:1579},{id:"node-1580",number:41,title:"What is the difference between operational and programmer errors?",question:"What is the difference between operational and programmer errors?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Operational errors are expected runtime conditions (invalid input, 404, timeouts) you handle and recover from. Programmer errors are bugs (undefined access) that should crash and be fixed, not caught-and-ignored.\n\n\n```js\nif (!user) throw new NotFoundError() // operational -> 404\n```\n```\n",tech:"node",displayNumber:41,sortKey:1580},{id:"node-1581",number:42,title:"How do you handle graceful shutdown in Express?",question:"How do you handle graceful shutdown in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Capture the `http.Server` from `app.listen`, and on SIGTERM call `server.close()` to drain connections, close DB pools, then exit. Use a timeout to force-close if needed.\n\n\n```js\nconst server = app.listen(3000)\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```\n```\n",tech:"node",displayNumber:42,sortKey:1581},{id:"node-1582",number:43,title:"What is `express.static` and how do you cache it?",question:"What is `express.static` and how do you cache it?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`express.static` serves files from a directory with proper `Content-Type` and caching. Set `maxAge`/`immutable` for hashed assets, and `etag` for revalidation.\n\n\n```js\napp.use(express.static('public', { maxAge: '1y', immutable: true }))\n```\n```\n",tech:"node",displayNumber:43,sortKey:1582},{id:"node-1583",number:44,title:"How do you protect against common web vulnerabilities in Express?",question:"How do you protect against common web vulnerabilities in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use `helmet` for secure headers, validate/sanitize input, parameterized DB queries, `cors` with a strict origin list, rate limiting, CSRF protection for cookie auth, and never trust client data.\n\n\n```js\napp.use(helmet()); app.use(express.json({ limit: '100kb' }))\n```\n```\n",tech:"node",displayNumber:44,sortKey:1583},{id:"node-1584",number:45,title:"What is CSRF and how do you prevent it?",question:"What is CSRF and how do you prevent it?",difficulty:"Experienced",category:"Express & REST APIs",answer:"CSRF tricks an authenticated browser into making unwanted requests using its cookies. Prevent with `SameSite` cookies, anti-CSRF tokens (double-submit/synchronizer), and requiring a custom header for state-changing requests. Token/header auth (not cookies) is inherently less exposed.\n\n\n```js\napp.use(csrf()); // and send token to client for form submissions\n```\n```\n",tech:"node",displayNumber:45,sortKey:1584},{id:"node-1585",number:46,title:"How do you handle long-running requests without blocking?",question:"How do you handle long-running requests without blocking?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Return 202 Accepted immediately, enqueue the job to a background worker/queue, and let clients poll a status endpoint or receive a webhook. Don't hold the HTTP connection for minutes.\n\n\n```js\napp.post('/reports', (req, res) => { queue.add(req.body); res.status(202).json({ jobId }) })\n```\n```\n",tech:"node",displayNumber:46,sortKey:1585},{id:"node-1586",number:47,title:"What is the difference between `res.send`, `res.json`, and `res.end`?",question:"What is the difference between `res.send`, `res.json`, and `res.end`?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`res.json` serializes to JSON and sets the header. `res.send` handles strings/buffers/objects, inferring type. `res.end` (from core) sends raw data without Express conveniences. Prefer `json` for APIs.\n\n\n```js\nres.json({ ok: true }) // sets application/json + stringifies\n```\n```\n",tech:"node",displayNumber:47,sortKey:1586},{id:"node-1587",number:48,title:"How do you implement API documentation?",question:"How do you implement API documentation?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Describe endpoints with an OpenAPI/Swagger spec and serve interactive docs (`swagger-ui-express`). Generating the spec from code/annotations keeps docs in sync.\n\n\n```js\napp.use('/docs', swaggerUi.serve, swaggerUi.setup(openapiSpec))\n```\n```\n",tech:"node",displayNumber:48,sortKey:1587},{id:"node-1588",number:49,title:"What is the difference between REST and GraphQL?",question:"What is the difference between REST and GraphQL?",difficulty:"Advanced",category:"Express & REST APIs",answer:"REST exposes fixed resource endpoints; clients may over/under-fetch. GraphQL exposes one endpoint where clients request exactly the fields they need, avoiding round-trips but adding query-complexity and caching challenges.\n\n\n```js\n// GraphQL: query { user(id:1){ name orders{ total } } }\n```\n```\n",tech:"node",displayNumber:49,sortKey:1588},{id:"node-1589",number:50,title:"How do you handle nested/related resources in REST?",question:"How do you handle nested/related resources in REST?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Model relationships in the URL hierarchy (`/users/:id/orders`), keep nesting shallow (usually one level), and offer filtering/embedding via query params to avoid many round-trips.\n\n\n```js\napp.get('/users/:id/orders', listUserOrders)\n```\n```\n",tech:"node",displayNumber:50,sortKey:1589},{id:"node-1590",number:51,title:"What is the N+1 problem in APIs and how do you fix it?",question:"What is the N+1 problem in APIs and how do you fix it?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Fetching a list then querying related data per item causes N+1 queries. Fix by batching (one `IN` query), joins, or a DataLoader that coalesces per-request lookups.\n\n\n```js\nconst orders = await db.orders.find({ userId: { $in: ids } }) // one query\n```\n```\n",tech:"node",displayNumber:51,sortKey:1590},{id:"node-1591",number:52,title:"How do you implement filtering, sorting, and field selection?",question:"How do you implement filtering, sorting, and field selection?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Parse query params (`?status=open&sort=-createdAt&fields=id,name`), whitelist allowed fields to prevent injection, and translate them to safe DB queries.\n\n\n```js\nconst sort = allowedSort[req.query.sort] ?? { createdAt: -1 }\n```\n```\n",tech:"node",displayNumber:52,sortKey:1591},{id:"node-1592",number:53,title:"What are webhooks and how do you handle them securely?",question:"What are webhooks and how do you handle them securely?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Webhooks are server-to-server HTTP callbacks on events. Secure them by verifying a signature (HMAC of the raw body with a shared secret), checking timestamps to prevent replay, and responding fast (queue processing).\n\n\n```js\nconst sig = hmac(secret, req.rawBody)\nif (sig !== req.headers['x-signature']) return res.status(400).end()\n```\n```\n",tech:"node",displayNumber:53,sortKey:1592},{id:"node-1593",number:54,title:"Why do you need the raw body for webhook signature verification?",question:"Why do you need the raw body for webhook signature verification?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Signatures are computed over exact raw bytes; JSON parsing/reserialization changes whitespace/key order and breaks verification. Capture `req.rawBody` (via a `verify` callback) before parsing.\n\n\n```js\nexpress.json({ verify: (req, res, buf) => { req.rawBody = buf } })\n```\n```\n",tech:"node",displayNumber:54,sortKey:1593},{id:"node-1594",number:55,title:"How do you handle concurrent updates (optimistic locking)?",question:"How do you handle concurrent updates (optimistic locking)?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Include a version/`ETag`; on update, require it via `If-Match` and reject with 409/412 if it no longer matches, preventing lost updates from concurrent writers.\n\n\n```js\nconst r = await db.update({ id, version }, { $set: data, $inc: { version: 1 } })\nif (!r.matchedCount) return res.status(409).end()\n```\n```\n",tech:"node",displayNumber:55,sortKey:1594},{id:"node-1595",number:56,title:"What is the difference between synchronous and event-driven API design?",question:"What is the difference between synchronous and event-driven API design?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Synchronous request/response returns results immediately, coupling caller and service. Event-driven (queues/pub-sub) decouples them: producers emit events, consumers process asynchronously, improving resilience and scalability at the cost of eventual consistency.\n\n\n```js\nawait queue.publish('order.created', order) // async, decoupled\n```\n```\n",tech:"node",displayNumber:56,sortKey:1595},{id:"node-1596",number:57,title:"How do you implement health check endpoints?",question:"How do you implement health check endpoints?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Expose `/health` (liveness: process up) and `/ready` (readiness: dependencies like DB reachable). Orchestrators use these to route traffic and restart unhealthy instances.\n\n\n```js\napp.get('/health', (req, res) => res.json({ status: 'ok' }))\n```\n```\n",tech:"node",displayNumber:57,sortKey:1596},{id:"node-1597",number:58,title:"What is the difference between liveness and readiness probes?",question:"What is the difference between liveness and readiness probes?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Liveness checks if the process is alive (restart if not). Readiness checks if it can serve traffic now (e.g. DB connected, warmed up). Failing readiness removes the pod from load balancing without restarting it.\n\n\n```js\napp.get('/ready', async (req, res) => (await db.ping()) ? res.end() : res.status(503).end())\n```\n```\n",tech:"node",displayNumber:58,sortKey:1597},{id:"node-1598",number:59,title:"How do you handle timeouts on Express routes?",question:"How do you handle timeouts on Express routes?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Set server-level timeouts and per-request guards (e.g. `connect-timeout` or an AbortSignal), responding 503/504 and aborting downstream work when exceeded so slow handlers don't pile up.\n\n\n```js\nreq.setTimeout(10000, () => res.status(503).end())\n```\n```\n",tech:"node",displayNumber:59,sortKey:1598},{id:"node-1599",number:60,title:"How do you test Express routes?",question:"How do you test Express routes?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Export the `app` (without calling `listen`) and drive it with `supertest`, asserting status/body. Mock external services and use a test database.\n\n\n```js\nawait request(app).post('/users').send({ name: 'A' }).expect(201)\n```\n```\n",tech:"node",displayNumber:60,sortKey:1599},{id:"node-1600",number:61,title:"What is dependency injection in Express services?",question:"What is dependency injection in Express services?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Pass dependencies (DB, config, clients) into services/route factories instead of importing them internally. This decouples layers, eases testing with mocks, and avoids hidden global state.\n\n\n```js\nconst makeUserRoutes = ({ db }) => { const r = Router(); r.get('/', () => db.users()); return r }\n```\n```\n",tech:"node",displayNumber:61,sortKey:1600},{id:"node-1601",number:62,title:"How do you handle multi-tenancy in an API?",question:"How do you handle multi-tenancy in an API?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Identify the tenant (subdomain/header/token claim) in middleware, then scope every query/resource to that tenant. Enforce isolation at the data layer so tenants can never read each other's data.\n\n\n```js\napp.use((req, res, next) => { req.tenantId = req.user.tenantId; next() })\n```\n```\n",tech:"node",displayNumber:62,sortKey:1601},{id:"node-1602",number:63,title:"What is API gateway pattern?",question:"What is API gateway pattern?",difficulty:"Experienced",category:"Express & REST APIs",answer:"An API gateway is a single entry point that routes to backend services and handles cross-cutting concerns (auth, rate limiting, aggregation, TLS). It simplifies clients and centralizes policy in a microservices system.\n\n\n```js\n// gateway -> /users -> user-service, /orders -> order-service\n```\n```\n",tech:"node",displayNumber:63,sortKey:1602},{id:"node-1603",number:64,title:"How do you implement request validation errors with clear messages?",question:"How do you implement request validation errors with clear messages?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Validate with a schema, catch validation errors in middleware, and map them to a 400/422 response listing each failing field and reason, aiding client debugging.\n\n\n```js\ncatch (e) { if (e instanceof ZodError) res.status(422).json({ errors: e.issues }) }\n```\n```\n",tech:"node",displayNumber:64,sortKey:1603},{id:"node-1604",number:65,title:"What is the difference between `app.listen` and using an http server directly?",question:"What is the difference between `app.listen` and using an http server directly?",difficulty:"Advanced",category:"Express & REST APIs",answer:"`app.listen` is a shortcut that creates an `http.Server` internally. Creating the server yourself (`http.createServer(app)`) is needed when you also attach WebSockets or HTTP/2, or want the server reference for graceful shutdown.\n\n\n```js\nconst server = http.createServer(app); wss.attach(server); server.listen(3000)\n```\n```\n",tech:"node",displayNumber:65,sortKey:1604},{id:"node-1605",number:66,title:"How do you handle streaming responses in Express?",question:"How do you handle streaming responses in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Pipe a readable stream to `res` (which is writable), setting appropriate headers. Use `pipeline` for error handling so failures clean up and reach the error handler.\n\n\n```js\npipeline(fs.createReadStream('big.csv'), res, err => err && next(err))\n```\n```\n",tech:"node",displayNumber:66,sortKey:1605},{id:"node-1606",number:67,title:"What are the security implications of `express.json()` limits?",question:"What are the security implications of `express.json()` limits?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Without a `limit`, a huge JSON body can exhaust memory (DoS). Setting a conservative `limit` rejects oversized payloads early with 413. Also beware deeply nested JSON causing CPU spikes on parse.\n\n\n```js\napp.use(express.json({ limit: '100kb' }))\n```\n```\n",tech:"node",displayNumber:67,sortKey:1606},{id:"node-1607",number:68,title:"How do you migrate from Express 4 to 5?",question:"How do you migrate from Express 4 to 5?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Express 5 auto-handles rejected promises in handlers, changes some path-matching (`*` syntax), and removes deprecated APIs. Review async error handling, update wildcard routes, and test middleware order.\n\n\n```js\n// Express 5: async errors propagate to error middleware automatically\napp.get('/x', async (req, res) => { await work(); res.end() })\n```\n```\n",tech:"node",displayNumber:68,sortKey:1607},{id:"node-1608",number:69,title:"What are alternatives to Express and why choose them?",question:"What are alternatives to Express and why choose them?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Fastify (faster, schema-based validation/serialization), Koa (async middleware, minimal), NestJS (opinionated, DI, TypeScript), Hapi (config-centric). Choose based on performance, structure, and TypeScript needs.\n\n\n```js\n// Fastify: built-in JSON schema validation & fast serialization\nfastify.post('/u', { schema }, handler)\n```\n```\n",tech:"node",displayNumber:69,sortKey:1608},{id:"node-1609",number:70,title:"How do you achieve zero-downtime deploys for an Express API?",question:"How do you achieve zero-downtime deploys for an Express API?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Run multiple instances behind a load balancer, roll out new instances while draining old ones (readiness probes + graceful shutdown), and keep DB migrations backward-compatible so old and new code coexist during rollout.\n\n\n```js\n// LB removes instance on /ready 503, then SIGTERM -> server.close() drains\n```\n```\n",tech:"node",displayNumber:70,sortKey:1609},{id:"node-1610",number:1,title:"How do you connect to a database in Node?",question:"How do you connect to a database in Node?",difficulty:"Basic",category:"Databases & ORMs",answer:"Use a driver (`pg`, `mysql2`, `mongodb`) or an ORM (Prisma, Sequelize, TypeORM). Create a connection or pool once at startup and reuse it across requests.\n\n\n```js\nconst { Pool } = require('pg')\nconst pool = new Pool({ connectionString: process.env.DB_URL })\n```\n```\n",tech:"node",displayNumber:1,sortKey:1610},{id:"node-1611",number:2,title:"What is a connection pool and why use one?",question:"What is a connection pool and why use one?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A pool maintains a set of reusable open connections, avoiding the cost of opening/closing per query and capping total concurrent connections to protect the database. Requests borrow and return connections.\n\n\n```js\nconst pool = new Pool({ max: 20 })\nconst { rows } = await pool.query('SELECT 1')\n```\n```\n",tech:"node",displayNumber:2,sortKey:1611},{id:"node-1612",number:3,title:"Why not open a new DB connection per request?",question:"Why not open a new DB connection per request?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Connecting is expensive (handshake/auth) and databases limit total connections. Per-request connections cause latency and can exhaust the DB's connection limit under load. Pools solve both.\n\n\n```js\n// reuse pool.query, never new Client() per request\n```\n```\n",tech:"node",displayNumber:3,sortKey:1612},{id:"node-1613",number:4,title:"What is SQL injection and how do you prevent it?",question:"What is SQL injection and how do you prevent it?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"SQL injection inserts malicious SQL via unsanitized input. Prevent it with parameterized queries/prepared statements, which send data separately from the query so it's never executed as SQL.\n\n\n```js\npool.query('SELECT * FROM users WHERE id = $1', [id]) // safe\n```\n```\n",tech:"node",displayNumber:4,sortKey:1613},{id:"node-1614",number:5,title:"What is the difference between parameterized queries and string concatenation?",question:"What is the difference between parameterized queries and string concatenation?",difficulty:"Advanced",category:"Databases & ORMs",answer:"String concatenation mixes untrusted data into the SQL text (injectable). Parameterized queries send the query structure and values separately; the driver/DB binds values as data, immune to injection and often cached/prepared.\n\n\n```js\n// NEVER: `WHERE name = '${name}'`\ndb.query('WHERE name = $1', [name])\n```\n```\n",tech:"node",displayNumber:5,sortKey:1614},{id:"node-1615",number:6,title:"What is an ORM and what are its trade-offs?",question:"What is an ORM and what are its trade-offs?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An ORM maps DB tables to objects, generating SQL and handling relations/migrations. Pros: productivity, safety, portability. Cons: abstraction leaks, N+1 pitfalls, less control over complex/optimized queries.\n\n\n```js\nconst user = await prisma.user.findUnique({ where: { id } })\n```\n```\n",tech:"node",displayNumber:6,sortKey:1615},{id:"node-1616",number:7,title:"What is the difference between SQL and NoSQL databases?",question:"What is the difference between SQL and NoSQL databases?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"SQL (relational) uses fixed schemas, joins, and strong consistency/transactions. NoSQL (document/key-value/graph) offers flexible schemas and horizontal scaling, often with eventual consistency. Choose by data shape and consistency needs.\n\n\n```js\n// SQL: JOIN across tables; MongoDB: embedded documents\n```\n```\n",tech:"node",displayNumber:7,sortKey:1616},{id:"node-1617",number:8,title:"What is the N+1 query problem?",question:"What is the N+1 query problem?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Loading a list then querying related rows one-by-one issues 1 + N queries. Fix by eager loading (join/include), batching with `IN`, or a DataLoader that coalesces lookups.\n\n\n```js\nconst users = await prisma.user.findMany({ include: { orders: true } }) // one round trip\n```\n```\n",tech:"node",displayNumber:8,sortKey:1617},{id:"node-1618",number:9,title:"What is a database transaction?",question:"What is a database transaction?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A transaction groups operations so they all succeed (commit) or all fail (rollback), preserving consistency. Essential for multi-step changes like transferring funds.\n\n\n```js\nawait prisma.$transaction([debit, credit]) // both or neither\n```\n```\n",tech:"node",displayNumber:9,sortKey:1618},{id:"node-1619",number:10,title:"What does ACID stand for?",question:"What does ACID stand for?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Atomicity (all-or-nothing), Consistency (valid state transitions), Isolation (concurrent transactions don't interfere), Durability (committed data survives crashes). Relational DBs provide ACID guarantees.\n\n\n```js\nBEGIN; UPDATE ...; UPDATE ...; COMMIT; -- atomic + durable\n```\n```\n",tech:"node",displayNumber:10,sortKey:1619},{id:"node-1620",number:11,title:"What are transaction isolation levels?",question:"What are transaction isolation levels?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Read Uncommitted, Read Committed, Repeatable Read, Serializable — increasing isolation and cost. They control anomalies (dirty/non-repeatable reads, phantoms). Higher isolation reduces concurrency; pick per use case.\n\n\n```js\nawait client.query('SET TRANSACTION ISOLATION LEVEL SERIALIZABLE')\n```\n```\n",tech:"node",displayNumber:11,sortKey:1620},{id:"node-1621",number:12,title:"What is a dirty read?",question:"What is a dirty read?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A dirty read reads data written by another transaction that hasn't committed yet and might roll back, so you may act on data that never truly existed. Read Committed and above prevent it.\n\n\n```js\n// avoided at READ COMMITTED and higher\n```\n```\n",tech:"node",displayNumber:12,sortKey:1621},{id:"node-1622",number:13,title:"How do you run a transaction with a driver like `pg`?",question:"How do you run a transaction with a driver like `pg`?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Check out a single client from the pool, issue `BEGIN`, run queries, then `COMMIT` (or `ROLLBACK` on error), and always release the client.\n\n\n```js\nconst c = await pool.connect()\ntry { await c.query('BEGIN'); await c.query(sql); await c.query('COMMIT') }\ncatch (e) { await c.query('ROLLBACK'); throw e } finally { c.release() }\n```\n```\n",tech:"node",displayNumber:13,sortKey:1622},{id:"node-1623",number:14,title:"What is optimistic vs pessimistic locking?",question:"What is optimistic vs pessimistic locking?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Optimistic locking assumes low contention: it checks a version column at update time and retries on conflict. Pessimistic locking locks rows (`SELECT ... FOR UPDATE`) upfront, blocking others. Optimistic scales better; pessimistic suits hot contention.\n\n\n```js\nUPDATE t SET x=$1, version=version+1 WHERE id=$2 AND version=$3\n```\n```\n",tech:"node",displayNumber:14,sortKey:1623},{id:"node-1624",number:15,title:"What is `SELECT ... FOR UPDATE`?",question:"What is `SELECT ... FOR UPDATE`?",difficulty:"Experienced",category:"Databases & ORMs",answer:"It locks the selected rows within a transaction so other transactions can't modify them until commit, implementing pessimistic locking to prevent concurrent update conflicts.\n\n\n```js\nawait c.query('SELECT balance FROM acct WHERE id=$1 FOR UPDATE', [id])\n```\n```\n",tech:"node",displayNumber:15,sortKey:1624},{id:"node-1625",number:16,title:"What is database indexing?",question:"What is database indexing?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An index is a data structure (usually B-tree) that speeds up lookups/sorts on indexed columns at the cost of extra storage and slower writes. Index columns used in `WHERE`, `JOIN`, and `ORDER BY`.\n\n\n```js\nCREATE INDEX idx_users_email ON users(email);\n```\n```\n",tech:"node",displayNumber:16,sortKey:1625},{id:"node-1626",number:17,title:"When can an index hurt performance?",question:"When can an index hurt performance?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Indexes slow down INSERT/UPDATE/DELETE (must be maintained) and consume storage. Too many or unused indexes waste resources; low-cardinality columns benefit little. Index selectively based on query patterns.\n\n\n```js\n// avoid indexing a boolean column with 50/50 distribution\n```\n```\n",tech:"node",displayNumber:17,sortKey:1626},{id:"node-1627",number:18,title:"What is a composite index and column order?",question:"What is a composite index and column order?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A composite index covers multiple columns; order matters. It serves queries filtering on a left-prefix of its columns. `(a,b)` helps `WHERE a` and `WHERE a AND b`, but not `WHERE b` alone.\n\n\n```js\nCREATE INDEX idx ON t(status, created_at); -- helps WHERE status ORDER BY created_at\n```\n```\n",tech:"node",displayNumber:18,sortKey:1627},{id:"node-1628",number:19,title:"How do you analyze a slow query?",question:"How do you analyze a slow query?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Use `EXPLAIN ANALYZE` to see the query plan — whether it uses indexes or does sequential scans, join methods, and row estimates vs actuals. Then add indexes or rewrite the query.\n\n\n```js\nEXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 5;\n```\n```\n",tech:"node",displayNumber:19,sortKey:1628},{id:"node-1629",number:20,title:"What is a database migration?",question:"What is a database migration?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A migration is a versioned, incremental schema change (create table, add column) tracked in code so environments stay in sync and changes are reproducible and reversible.\n\n\n```js\nnpx prisma migrate dev --name add_orders\n```\n```\n",tech:"node",displayNumber:20,sortKey:1629},{id:"node-1630",number:21,title:"How do you perform zero-downtime schema migrations?",question:"How do you perform zero-downtime schema migrations?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Make changes backward-compatible and multi-step: add nullable column → backfill → deploy code using it → enforce constraint → remove old column later. Never rename/drop in a single deploy while old code runs.\n\n\n```js\n// step1: ADD COLUMN email_new; step2: backfill; step3: switch reads/writes\n```\n```\n",tech:"node",displayNumber:21,sortKey:1630},{id:"node-1631",number:22,title:"What is the difference between `findOne` and `findMany`?",question:"What is the difference between `findOne` and `findMany`?",difficulty:"Basic",category:"Databases & ORMs",answer:"`findOne`/`findUnique` returns a single record (or null); `findMany` returns an array. Use unique lookups for single entities and filtered/paged queries for lists.\n\n\n```js\nconst u = await prisma.user.findUnique({ where: { email } })\nconst many = await prisma.user.findMany({ where: { active: true } })\n```\n```\n",tech:"node",displayNumber:22,sortKey:1631},{id:"node-1632",number:23,title:"What is eager vs lazy loading of relations?",question:"What is eager vs lazy loading of relations?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Eager loading fetches related data with the main query (`include`/`join`), avoiding extra round trips. Lazy loading fetches relations on access, which is convenient but can trigger N+1 queries.\n\n\n```js\nprisma.post.findMany({ include: { author: true } }) // eager\n```\n```\n",tech:"node",displayNumber:23,sortKey:1632},{id:"node-1633",number:24,title:"What is a DataLoader and how does it help?",question:"What is a DataLoader and how does it help?",difficulty:"Experienced",category:"Databases & ORMs",answer:"DataLoader batches individual key lookups made within a tick into a single query and caches per request, eliminating N+1 in GraphQL/REST resolvers.\n\n\n```js\nconst loader = new DataLoader(ids => db.users.findByIds(ids))\nawait loader.load(1) // batched with other loads\n```\n```\n",tech:"node",displayNumber:24,sortKey:1633},{id:"node-1634",number:25,title:"How do you model relationships in MongoDB?",question:"How do you model relationships in MongoDB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Embed related data for one-to-few, tightly-coupled data read together; reference (store IDs) for one-to-many/many-to-many or independently-updated data. Balance read efficiency against duplication and document size limits.\n\n\n```js\n// embed comments in a post, or reference authorId\n{ title, comments: [{ text }], authorId: ObjectId('...') }\n```\n```\n",tech:"node",displayNumber:25,sortKey:1634},{id:"node-1635",number:26,title:"What is the aggregation pipeline in MongoDB?",question:"What is the aggregation pipeline in MongoDB?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A sequence of stages (`$match`, `$group`, `$sort`, `$lookup`, `$project`) that transform documents, enabling complex analytics/joins server-side without pulling data into the app.\n\n\n```js\ndb.orders.aggregate([{ $match: { status: 'paid' } }, { $group: { _id: '$userId', total: { $sum: '$amount' } } }])\n```\n```\n",tech:"node",displayNumber:26,sortKey:1635},{id:"node-1636",number:27,title:"What is `$lookup` in MongoDB?",question:"What is `$lookup` in MongoDB?",difficulty:"Experienced",category:"Databases & ORMs",answer:"`$lookup` performs a left outer join between collections in an aggregation, joining documents by matching fields — MongoDB's answer to SQL joins.\n\n\n```js\n{ $lookup: { from: 'users', localField: 'userId', foreignField: '_id', as: 'user' } }\n```\n```\n",tech:"node",displayNumber:27,sortKey:1636},{id:"node-1637",number:28,title:"How do you handle schema validation in MongoDB?",question:"How do you handle schema validation in MongoDB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Use an ODM like Mongoose with schemas, or MongoDB's built-in JSON Schema validators on collections. Validation enforces structure despite MongoDB's flexible documents.\n\n\n```js\nconst User = mongoose.model('User', new Schema({ email: { type: String, required: true } }))\n```\n```\n",tech:"node",displayNumber:28,sortKey:1637},{id:"node-1638",number:29,title:"What is Mongoose?",question:"What is Mongoose?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Mongoose is an ODM for MongoDB providing schemas, validation, middleware (hooks), population (references), and typed models over the raw driver.\n\n\n```js\nconst u = await User.findById(id).populate('orders')\n```\n```\n",tech:"node",displayNumber:29,sortKey:1638},{id:"node-1639",number:30,title:"What is the difference between Prisma, Sequelize, and TypeORM?",question:"What is the difference between Prisma, Sequelize, and TypeORM?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Prisma uses a schema file + generated type-safe client (great DX/TS). Sequelize is a mature ORM with a JS API. TypeORM uses decorators/entities (Active Record or Data Mapper). Choose by type-safety, patterns, and ecosystem fit.\n\n\n```js\nconst users = await prisma.user.findMany() // fully typed\n```\n```\n",tech:"node",displayNumber:30,sortKey:1639},{id:"node-1640",number:31,title:"What is a prepared statement?",question:"What is a prepared statement?",difficulty:"Advanced",category:"Databases & ORMs",answer:"A prepared statement is a parameterized query the DB parses/plans once and reuses with different values, improving performance and preventing injection. Drivers often prepare parameterized queries automatically.\n\n\n```js\nconst stmt = db.prepare('INSERT INTO t(x) VALUES(?)'); stmt.run(1); stmt.run(2)\n```\n```\n",tech:"node",displayNumber:31,sortKey:1640},{id:"node-1641",number:32,title:"How do you paginate database results efficiently?",question:"How do you paginate database results efficiently?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Prefer keyset/cursor pagination (`WHERE id > :last ORDER BY id LIMIT n`) using an indexed column over `OFFSET`, which scans and skips rows and degrades on large offsets.\n\n\n```js\nSELECT * FROM t WHERE id > $1 ORDER BY id LIMIT 20\n```\n```\n",tech:"node",displayNumber:32,sortKey:1641},{id:"node-1642",number:33,title:"What is connection pool exhaustion and how do you avoid it?",question:"What is connection pool exhaustion and how do you avoid it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"It happens when all pooled connections are busy (leaked, held by long transactions, or too-small pool), causing requests to queue/timeout. Avoid by releasing clients promptly, sizing the pool, and setting query timeouts.\n\n\n```js\ntry { await c.query(sql) } finally { c.release() } // always release\n```\n```\n",tech:"node",displayNumber:33,sortKey:1642},{id:"node-1643",number:34,title:"How do you size a connection pool?",question:"How do you size a connection pool?",difficulty:"Experienced",category:"Databases & ORMs",answer:'Base it on the DB\'s max connections divided across app instances, workload concurrency, and query latency — not "bigger is better". Too large overwhelms the DB; too small starves the app. Measure and tune.\n\n\n```js\nnew Pool({ max: 20 }) // per instance; sum across instances < DB limit\n```\n```\n',tech:"node",displayNumber:34,sortKey:1643},{id:"node-1644",number:35,title:"What is a read replica and when do you use it?",question:"What is a read replica and when do you use it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A read replica is a copy that serves read queries, offloading the primary. Use it to scale reads, but account for replication lag (eventual consistency) — route critical read-after-write to the primary.\n\n\n```js\nconst rows = await replicaPool.query('SELECT ...') // reads off replica\n```\n```\n",tech:"node",displayNumber:35,sortKey:1644},{id:"node-1645",number:36,title:"What is replication lag and how do you handle it?",question:"What is replication lag and how do you handle it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Replication lag is the delay before a write appears on replicas, so a read right after a write may return stale data. Handle by reading from the primary for read-after-write, or waiting for the replica to catch up.\n\n\n```js\n// after write, read from primary to avoid stale replica data\n```\n```\n",tech:"node",displayNumber:36,sortKey:1645},{id:"node-1646",number:37,title:"What is sharding?",question:"What is sharding?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Sharding partitions data horizontally across multiple servers by a shard key, so each holds a subset. It scales writes/storage beyond one machine but complicates cross-shard queries and transactions.\n\n\n```js\n// shard by user_id % N -> route query to the right shard\n```\n```\n",tech:"node",displayNumber:37,sortKey:1646},{id:"node-1647",number:38,title:"What is a caching layer and cache invalidation?",question:"What is a caching layer and cache invalidation?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A cache (Redis) stores frequently-read data to reduce DB load/latency. Invalidation — expiring or updating stale entries on writes — is the hard part; strategies include TTLs, write-through, and cache-aside.\n\n\n```js\nlet u = await redis.get(k) ?? await db.user(id).then(v => (redis.set(k, v, 'EX', 60), v))\n```\n```\n",tech:"node",displayNumber:38,sortKey:1647},{id:"node-1648",number:39,title:"What is the cache-aside pattern?",question:"What is the cache-aside pattern?",difficulty:"Advanced",category:"Databases & ORMs",answer:"The app checks the cache first; on a miss it loads from the DB and populates the cache. Writes update the DB and invalidate the cached entry. It's simple and common but risks brief staleness.\n\n\n```js\nconst hit = await cache.get(k); if (hit) return hit\nconst v = await db.get(k); await cache.set(k, v); return v\n```\n```\n",tech:"node",displayNumber:39,sortKey:1648},{id:"node-1649",number:40,title:"What is Redis and what is it used for in Node apps?",question:"What is Redis and what is it used for in Node apps?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Redis is an in-memory data store used for caching, session storage, rate limiting, pub/sub, queues, and leaderboards. Its speed and data structures make it a versatile companion to a primary DB.\n\n\n```js\nawait redis.set('session:abc', JSON.stringify(user), 'EX', 3600)\n```\n```\n",tech:"node",displayNumber:40,sortKey:1649},{id:"node-1650",number:41,title:"How do you use Redis for sessions across multiple instances?",question:"How do you use Redis for sessions across multiple instances?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Store session data in Redis (shared) instead of process memory, so any instance can serve any request without sticky sessions. Set TTLs matching session expiry.\n\n\n```js\napp.use(session({ store: new RedisStore({ client: redis }), secret }))\n```\n```\n",tech:"node",displayNumber:41,sortKey:1650},{id:"node-1651",number:42,title:"What is a database deadlock?",question:"What is a database deadlock?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A deadlock occurs when two transactions each hold a lock the other needs, waiting forever. The DB detects it and aborts one (a deadlock error). Avoid by acquiring locks in a consistent order and keeping transactions short.\n\n\n```js\n// always update tables/rows in the same order to avoid cycles\n```\n```\n",tech:"node",displayNumber:42,sortKey:1651},{id:"node-1652",number:43,title:"How do you handle transient DB errors and retries?",question:"How do you handle transient DB errors and retries?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Retry only retryable errors (deadlock, serialization failure, connection reset) with backoff, and make the operation idempotent or wrap it in a transaction. Don't blindly retry non-idempotent writes.\n\n\n```js\nfor (let i = 0; i < 3; i++) { try { return await tx() } catch (e) { if (!isRetryable(e)) throw e } }\n```\n```\n",tech:"node",displayNumber:43,sortKey:1652},{id:"node-1653",number:44,title:"What is the difference between `LEFT JOIN` and `INNER JOIN`?",question:"What is the difference between `LEFT JOIN` and `INNER JOIN`?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"INNER JOIN returns only matching rows in both tables. LEFT JOIN returns all rows from the left table plus matches (NULLs where none). Choose based on whether unmatched left rows should appear.\n\n\n```js\nSELECT u.*, o.id FROM users u LEFT JOIN orders o ON o.user_id = u.id;\n```\n```\n",tech:"node",displayNumber:44,sortKey:1653},{id:"node-1654",number:45,title:"How do you prevent over-fetching data from the DB?",question:"How do you prevent over-fetching data from the DB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Select only needed columns (not `SELECT *`), paginate, filter at the DB, and avoid loading unused relations. Less data transferred means lower latency and memory.\n\n\n```js\nprisma.user.findMany({ select: { id: true, name: true } })\n```\n```\n",tech:"node",displayNumber:45,sortKey:1654},{id:"node-1655",number:46,title:"What are database constraints and why enforce them at the DB level?",question:"What are database constraints and why enforce them at the DB level?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Constraints (NOT NULL, UNIQUE, FOREIGN KEY, CHECK) enforce data integrity regardless of app bugs or multiple writers. The DB is the last line of defense; app-only validation can be bypassed.\n\n\n```js\nCREATE TABLE users (email TEXT UNIQUE NOT NULL);\n```\n```\n",tech:"node",displayNumber:46,sortKey:1655},{id:"node-1656",number:47,title:"How do you handle unique constraint violations gracefully?",question:"How do you handle unique constraint violations gracefully?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Attempt the insert and catch the unique-violation error code, returning a 409 Conflict, or use an upsert (`ON CONFLICT`). Checking-then-inserting has a race window.\n\n\n```js\nINSERT INTO users(email) VALUES($1) ON CONFLICT (email) DO NOTHING;\n```\n```\n",tech:"node",displayNumber:47,sortKey:1656},{id:"node-1657",number:48,title:"What is an upsert?",question:"What is an upsert?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An upsert inserts a row or updates it if it already exists, atomically. It avoids race conditions from separate check/insert/update logic.\n\n\n```js\nprisma.user.upsert({ where: { email }, create: { email }, update: { seen: new Date() } })\n```\n```\n",tech:"node",displayNumber:48,sortKey:1657},{id:"node-1658",number:49,title:"What is the difference between soft delete and hard delete?",question:"What is the difference between soft delete and hard delete?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Hard delete removes the row permanently. Soft delete sets a `deleted_at`/`is_deleted` flag, preserving data for audit/recovery while filtering it from normal queries. Soft delete needs consistent query filters and indexes.\n\n\n```js\nUPDATE users SET deleted_at = now() WHERE id = $1;\n```\n```\n",tech:"node",displayNumber:49,sortKey:1658},{id:"node-1659",number:50,title:"How do you store timestamps and time zones correctly?",question:"How do you store timestamps and time zones correctly?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Store timestamps in UTC (`timestamptz`), convert to local time only for display. Storing local times without offsets causes bugs across zones and DST changes.\n\n\n```js\nINSERT INTO events(at) VALUES (now() AT TIME ZONE 'UTC');\n```\n```\n",tech:"node",displayNumber:50,sortKey:1659},{id:"node-1660",number:51,title:"What is database normalization?",question:"What is database normalization?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Normalization organizes data to reduce redundancy via related tables (1NF–3NF), improving integrity and update efficiency. It can require more joins for reads, which denormalization selectively trades away.\n\n\n```js\n// separate users and addresses tables instead of repeating address columns\n```\n```\n",tech:"node",displayNumber:51,sortKey:1660},{id:"node-1661",number:52,title:"When would you denormalize?",question:"When would you denormalize?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Denormalize (duplicate data) to speed up read-heavy paths by avoiding expensive joins/aggregations, accepting extra write complexity and update anomalies. Common in analytics, caches, and NoSQL.\n\n\n```js\n// store order_count on user to avoid COUNT(*) per request\n```\n```\n",tech:"node",displayNumber:52,sortKey:1661},{id:"node-1662",number:53,title:"How do you seed and manage test data?",question:"How do you seed and manage test data?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Use seed scripts/migrations to create deterministic data, wrap tests in transactions that roll back, or reset a test database between runs to keep tests isolated and repeatable.\n\n\n```js\nbeforeEach(async () => { await db.query('BEGIN') })\nafterEach(async () => { await db.query('ROLLBACK') })\n```\n```\n",tech:"node",displayNumber:53,sortKey:1662},{id:"node-1663",number:54,title:"What is an ORM query builder vs raw SQL?",question:"What is an ORM query builder vs raw SQL?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Query builders (Knex) compose SQL programmatically with safety and portability; raw SQL gives full control for complex/optimized queries. Most apps mix both — builder/ORM for CRUD, raw for hotspots.\n\n\n```js\nknex('users').where('active', true).select('id', 'name')\n```\n```\n",tech:"node",displayNumber:54,sortKey:1663},{id:"node-1664",number:55,title:"How do you handle large bulk inserts efficiently?",question:"How do you handle large bulk inserts efficiently?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Batch rows into multi-row inserts or use `COPY`/bulk APIs, wrap in a transaction, and disable per-row overhead. Inserting one-by-one is slow due to round trips.\n\n\n```js\nINSERT INTO t(a,b) VALUES ($1,$2),($3,$4),($5,$6); -- batched\n```\n```\n",tech:"node",displayNumber:55,sortKey:1664},{id:"node-1665",number:56,title:"What is a stored procedure and when use one?",question:"What is a stored procedure and when use one?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A stored procedure is logic stored/executed in the DB. Use it for data-intensive operations to reduce round trips and enforce logic centrally, at the cost of portability and harder version control.\n\n\n```js\nawait db.query('CALL transfer_funds($1, $2, $3)', [from, to, amt])\n```\n```\n",tech:"node",displayNumber:56,sortKey:1665},{id:"node-1666",number:57,title:"How do you monitor database performance from Node?",question:"How do you monitor database performance from Node?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Log slow queries with durations, track pool metrics (active/idle/waiting), expose DB latency in APM/metrics, and use the DB's own slow-query log and `pg_stat_statements` to find hotspots.\n\n\n```js\nconst t = Date.now(); await pool.query(sql); metrics.timing('db', Date.now() - t)\n```\n```\n",tech:"node",displayNumber:57,sortKey:1666},{id:"node-1667",number:58,title:"What is the difference between `count(*)` performance in SQL and estimates?",question:"What is the difference between `count(*)` performance in SQL and estimates?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Exact `COUNT(*)` on large tables scans many rows and can be slow. For approximate totals (e.g. pagination UI), use planner estimates (`pg_class.reltuples`) or maintained counters instead.\n\n\n```js\nSELECT reltuples::bigint FROM pg_class WHERE relname = 'orders'; -- estimate\n```\n```\n",tech:"node",displayNumber:58,sortKey:1667},{id:"node-1668",number:59,title:"How do you implement full-text search?",question:"How do you implement full-text search?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Use the DB's full-text features (Postgres `tsvector`/GIN index, Mongo text index) for basic search, or a dedicated engine (Elasticsearch/OpenSearch) for advanced relevance, facets, and scale.\n\n\n```js\nSELECT * FROM docs WHERE to_tsvector(body) @@ to_tsquery('node & stream');\n```\n```\n",tech:"node",displayNumber:59,sortKey:1668},{id:"node-1669",number:60,title:"What is eventual consistency?",question:"What is eventual consistency?",difficulty:"Experienced",category:"Databases & ORMs",answer:"In eventually-consistent systems, replicas converge to the same state over time but may return stale reads briefly. It trades immediate consistency for availability/scale (CAP), acceptable for many read-heavy features.\n\n\n```js\n// a just-written value may not appear on a replica read immediately\n```\n```\n",tech:"node",displayNumber:60,sortKey:1669},{id:"node-1670",number:61,title:"What is the CAP theorem?",question:"What is the CAP theorem?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Under a network partition, a distributed system can guarantee at most two of Consistency, Availability, Partition tolerance. Since partitions happen, you effectively choose CP (reject to stay consistent) or AP (serve possibly-stale data).\n\n\n```js\n// CP: refuse writes during partition; AP: accept and reconcile later\n```\n```\n",tech:"node",displayNumber:61,sortKey:1670},{id:"node-1671",number:62,title:"How do you avoid leaking DB credentials?",question:"How do you avoid leaking DB credentials?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Load credentials from environment/secret managers, never hardcode or commit them, use least-privilege DB users, rotate secrets, and require TLS to the database.\n\n\n```js\nnew Pool({ connectionString: process.env.DB_URL, ssl: { rejectUnauthorized: true } })\n```\n```\n",tech:"node",displayNumber:62,sortKey:1671},{id:"node-1672",number:63,title:"How do you implement audit logging of data changes?",question:"How do you implement audit logging of data changes?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Record who/what/when for changes via triggers, an audit table, or app-level change events (outbox). Keep audit records immutable and separate from operational data.\n\n\n```js\nINSERT INTO audit(table, row_id, action, actor, at) VALUES(...);\n```\n```\n",tech:"node",displayNumber:63,sortKey:1672},{id:"node-1673",number:64,title:"What is the transactional outbox pattern?",question:"What is the transactional outbox pattern?",difficulty:"Experienced",category:"Databases & ORMs",answer:'Write domain changes and an event row in the same DB transaction (the "outbox"); a separate process publishes those events to a message broker. It guarantees the event is sent iff the data change committed, avoiding dual-write inconsistency.\n\n\n```js\nawait tx([saveOrder, insertOutboxEvent]) // atomic; publisher reads outbox later\n```\n```\n',tech:"node",displayNumber:64,sortKey:1673},{id:"node-1674",number:65,title:"How do you handle migrations in a team/CI setting?",question:"How do you handle migrations in a team/CI setting?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Keep migrations in version control, apply them automatically in CI/CD before deploy, make them forward-only and idempotent where possible, and coordinate to avoid conflicting concurrent migrations.\n\n\n```js\nnpx prisma migrate deploy // applies pending migrations in CI/prod\n```\n```\n",tech:"node",displayNumber:65,sortKey:1674},{id:"node-1675",number:66,title:"What is connection string SSL and why require it?",question:"What is connection string SSL and why require it?",difficulty:"Advanced",category:"Databases & ORMs",answer:"SSL/TLS encrypts traffic between app and DB, protecting credentials/data in transit from eavesdropping, especially across networks/cloud. Verify the server certificate to prevent MITM.\n\n\n```js\nssl: { rejectUnauthorized: true, ca: fs.readFileSync('rds-ca.pem') }\n```\n```\n",tech:"node",displayNumber:66,sortKey:1675},{id:"node-1676",number:67,title:"How do you handle schema drift between environments?",question:"How do you handle schema drift between environments?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Make migrations the single source of truth, run the same migrations everywhere, and detect drift with tools that diff the live schema against expected. Avoid manual production changes.\n\n\n```js\nnpx prisma migrate status // reports drift/pending migrations\n```\n```\n",tech:"node",displayNumber:67,sortKey:1676},{id:"node-1677",number:68,title:"What is the difference between `TRUNCATE` and `DELETE`?",question:"What is the difference between `TRUNCATE` and `DELETE`?",difficulty:"Advanced",category:"Databases & ORMs",answer:"`DELETE` removes rows one by one (logged, respects triggers, can be filtered/rolled back). `TRUNCATE` quickly empties a whole table by deallocating pages (faster, resets sequences, fewer logs) but can't be filtered.\n\n\n```js\nTRUNCATE TABLE temp_import; -- fast full clear\n```\n```\n",tech:"node",displayNumber:68,sortKey:1677},{id:"node-1678",number:69,title:"How do you avoid loading huge result sets into memory?",question:"How do you avoid loading huge result sets into memory?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Stream/cursor through results in chunks instead of buffering all rows. Drivers offer server-side cursors or query streams that emit rows incrementally.\n\n\n```js\nconst stream = pool.query(new QueryStream('SELECT * FROM big'))\nfor await (const row of stream) process(row)\n```\n```\n",tech:"node",displayNumber:69,sortKey:1678},{id:"node-1679",number:70,title:"How do you choose between SQL and NoSQL for a project?",question:"How do you choose between SQL and NoSQL for a project?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Choose SQL for relational data, strong consistency, complex queries, and transactions. Choose NoSQL for flexible/evolving schemas, massive scale, or specific access patterns (document/key-value/graph). Many systems use both (polyglot persistence).\n\n\n```js\n// orders/payments -> Postgres; session/cache -> Redis; catalog -> Mongo\n```\n```\n",tech:"node",displayNumber:70,sortKey:1679},{id:"node-1680",number:1,title:"How do you securely store passwords?",question:"How do you securely store passwords?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Never store plaintext. Hash with a slow, salted algorithm designed for passwords: bcrypt, scrypt, or Argon2. Salts prevent rainbow-table attacks; slowness resists brute force.\n\n\n```js\nconst hash = await bcrypt.hash(password, 12)\nconst ok = await bcrypt.compare(password, hash)\n```\n```\n",tech:"node",displayNumber:1,sortKey:1680},{id:"node-1681",number:2,title:"Why not use SHA-256 for passwords?",question:"Why not use SHA-256 for passwords?",difficulty:"Advanced",category:"Security & Authentication",answer:"SHA-256 is fast, so attackers can compute billions of guesses per second. Password hashing needs deliberate slowness and memory-hardness (bcrypt/Argon2) to make brute forcing impractical.\n\n\n```js\nawait argon2.hash(password) // memory-hard, slow by design\n```\n```\n",tech:"node",displayNumber:2,sortKey:1681},{id:"node-1682",number:3,title:"What is a salt and why is it needed?",question:"What is a salt and why is it needed?",difficulty:"Intermediate",category:"Security & Authentication",answer:"A salt is unique random data added per password before hashing, so identical passwords produce different hashes and precomputed rainbow tables are useless. bcrypt/Argon2 embed the salt in the output.\n\n\n```js\n// bcrypt output contains cost + salt + hash: $2b$12$salt...hash\n```\n```\n",tech:"node",displayNumber:3,sortKey:1682},{id:"node-1683",number:4,title:"What is a pepper?",question:"What is a pepper?",difficulty:"Experienced",category:"Security & Authentication",answer:"A pepper is a secret added to passwords before hashing, stored separately from the DB (e.g. in an HSM/env), so a database leak alone doesn't enable offline cracking. It complements per-user salts.\n\n\n```js\nconst hash = await bcrypt.hash(password + process.env.PEPPER, 12)\n```\n```\n",tech:"node",displayNumber:4,sortKey:1683},{id:"node-1684",number:5,title:"What is JWT?",question:"What is JWT?",difficulty:"Intermediate",category:"Security & Authentication",answer:"A JSON Web Token is a signed, base64url token with header.payload.signature. The signature lets the server verify claims without server-side session storage, enabling stateless auth.\n\n\n```js\nconst token = jwt.sign({ sub: user.id }, secret, { expiresIn: '15m' })\njwt.verify(token, secret)\n```\n```\n",tech:"node",displayNumber:5,sortKey:1684},{id:"node-1685",number:6,title:"What are the three parts of a JWT?",question:"What are the three parts of a JWT?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Header (algorithm/type), Payload (claims like `sub`, `exp`), Signature (HMAC or RSA over header+payload). The payload is only base64-encoded, not encrypted — never put secrets in it.\n\n\n```js\n// eyJhbGci...  .  eyJzdWIi...  .  signature\n```\n```\n",tech:"node",displayNumber:6,sortKey:1685},{id:"node-1686",number:7,title:"Is JWT payload encrypted?",question:"Is JWT payload encrypted?",difficulty:"Advanced",category:"Security & Authentication",answer:"No. A standard (signed) JWT is only base64url-encoded and fully readable. Anyone can decode the payload; the signature only guarantees integrity/authenticity. For confidentiality use JWE (encrypted) or don't store sensitive data in the token.\n\n\n```js\nJSON.parse(Buffer.from(token.split('.')[1], 'base64')) // readable claims\n```\n```\n",tech:"node",displayNumber:7,sortKey:1686},{id:"node-1687",number:8,title:"What is the difference between JWT and session-based auth?",question:"What is the difference between JWT and session-based auth?",difficulty:"Advanced",category:"Security & Authentication",answer:"Sessions store state server-side (a session ID cookie references it) and are easy to revoke. JWTs are stateless (self-contained, scale across servers) but hard to revoke before expiry. Choose based on revocation and scaling needs.\n\n\n```js\n// session: cookie 'sid' -> store lookup; JWT: verify signature, no lookup\n```\n```\n",tech:"node",displayNumber:8,sortKey:1687},{id:"node-1688",number:9,title:"How do you revoke a JWT?",question:"How do you revoke a JWT?",difficulty:"Experienced",category:"Security & Authentication",answer:"Since JWTs are stateless, use short expiry + refresh tokens, and maintain a denylist/allowlist (by jti/user) in a fast store to invalidate tokens early. True revocation reintroduces some server state.\n\n\n```js\nif (await redis.sismember('revoked', payload.jti)) throw new Error('revoked')\n```\n```\n",tech:"node",displayNumber:9,sortKey:1688},{id:"node-1689",number:10,title:"What is the access token / refresh token pattern?",question:"What is the access token / refresh token pattern?",difficulty:"Experienced",category:"Security & Authentication",answer:"A short-lived access token authorizes API calls; a long-lived refresh token (stored securely, e.g. HttpOnly cookie) obtains new access tokens. This limits exposure if an access token leaks.\n\n\n```js\nconst access = jwt.sign(claims, s, { expiresIn: '15m' })\nconst refresh = jwt.sign({ sub }, r, { expiresIn: '7d' })\n```\n```\n",tech:"node",displayNumber:10,sortKey:1689},{id:"node-1690",number:11,title:"Where should you store tokens on the client?",question:"Where should you store tokens on the client?",difficulty:"Experienced",category:"Security & Authentication",answer:"Prefer HttpOnly, Secure, SameSite cookies (inaccessible to JS, mitigating XSS theft). `localStorage` is vulnerable to XSS. Cookies need CSRF protection; tokens in headers avoid CSRF but are exposed to XSS.\n\n\n```js\nres.cookie('token', jwt, { httpOnly: true, secure: true, sameSite: 'strict' })\n```\n```\n",tech:"node",displayNumber:11,sortKey:1690},{id:"node-1691",number:12,title:"What is the `alg: none` JWT vulnerability?",question:"What is the `alg: none` JWT vulnerability?",difficulty:"Experienced",category:"Security & Authentication",answer:"Some libraries accepted tokens with `alg: none` (no signature), letting attackers forge tokens. Always pin the expected algorithm on verify and reject `none`.\n\n\n```js\njwt.verify(token, key, { algorithms: ['HS256'] }) // pin algorithm\n```\n```\n",tech:"node",displayNumber:12,sortKey:1691},{id:"node-1692",number:13,title:"What is the algorithm confusion attack (RS256 vs HS256)?",question:"What is the algorithm confusion attack (RS256 vs HS256)?",difficulty:"Experienced",category:"Security & Authentication",answer:"If a server accepts either RS256 or HS256, an attacker can sign a token with HS256 using the public RSA key as the HMAC secret. Prevent it by pinning the exact algorithm and separating key usage.\n\n\n```js\njwt.verify(token, publicKey, { algorithms: ['RS256'] }) // no HS fallback\n```\n```\n",tech:"node",displayNumber:13,sortKey:1692},{id:"node-1693",number:14,title:"What is OAuth 2.0?",question:"What is OAuth 2.0?",difficulty:"Advanced",category:"Security & Authentication",answer:"OAuth 2.0 is a delegated authorization framework: a user grants a third-party app limited access to their resources without sharing credentials, via tokens issued by an authorization server.\n\n\n```js\n// redirect to provider -> user consents -> callback with code -> exchange for token\n```\n```\n",tech:"node",displayNumber:14,sortKey:1693},{id:"node-1694",number:15,title:"What is the OAuth authorization code flow with PKCE?",question:"What is the OAuth authorization code flow with PKCE?",difficulty:"Experienced",category:"Security & Authentication",answer:"The app redirects to the provider with a code challenge, receives an auth code on callback, then exchanges it (plus the code verifier) for tokens. PKCE prevents code interception attacks, essential for public/mobile clients.\n\n\n```js\n// challenge = SHA256(verifier); exchange code + verifier for tokens\n```\n```\n",tech:"node",displayNumber:15,sortKey:1694},{id:"node-1695",number:16,title:"What is the difference between OAuth and OpenID Connect?",question:"What is the difference between OAuth and OpenID Connect?",difficulty:"Advanced",category:"Security & Authentication",answer:"OAuth 2.0 is for authorization (access to resources). OpenID Connect (OIDC) is an identity layer on top that adds an ID token (JWT) proving who the user is — i.e. authentication.\n\n\n```js\n// OIDC adds id_token with user identity claims\n```\n```\n",tech:"node",displayNumber:16,sortKey:1695},{id:"node-1696",number:17,title:"What is XSS and how do you prevent it?",question:"What is XSS and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Cross-Site Scripting injects malicious scripts into pages viewed by others. Prevent by escaping/encoding output, using frameworks that auto-escape, a strict Content-Security-Policy, and never inserting untrusted HTML.\n\n\n```js\nres.setHeader('Content-Security-Policy', \"default-src 'self'\")\n```\n```\n",tech:"node",displayNumber:17,sortKey:1696},{id:"node-1697",number:18,title:"What is CSRF and how do you prevent it?",question:"What is CSRF and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Cross-Site Request Forgery makes a logged-in user's browser send unwanted requests using their cookies. Prevent with `SameSite` cookies, anti-CSRF tokens, and requiring a non-simple header. Header-based tokens (not cookies) sidestep it.\n\n\n```js\nres.cookie('sid', v, { sameSite: 'strict' }) // + CSRF token for forms\n```\n```\n",tech:"node",displayNumber:18,sortKey:1697},{id:"node-1698",number:19,title:"What is SQL injection prevention in one line?",question:"What is SQL injection prevention in one line?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Always use parameterized queries/prepared statements; never build SQL by concatenating untrusted input.\n\n\n```js\ndb.query('SELECT * FROM u WHERE email = $1', [email])\n```\n```\n",tech:"node",displayNumber:19,sortKey:1698},{id:"node-1699",number:20,title:"What is command injection and how do you avoid it?",question:"What is command injection and how do you avoid it?",difficulty:"Experienced",category:"Security & Authentication",answer:"Command injection runs attacker-controlled shell commands via `exec` with interpolated input. Avoid it by using `execFile`/`spawn` with argument arrays (no shell) and validating/whitelisting inputs.\n\n\n```js\nexecFile('convert', [userPath, out]) // no shell, no injection\n```\n```\n",tech:"node",displayNumber:20,sortKey:1699},{id:"node-1700",number:21,title:"What is the principle of least privilege?",question:"What is the principle of least privilege?",difficulty:"Advanced",category:"Security & Authentication",answer:"Grant each component/user the minimum permissions needed. Limit DB roles, API scopes, file permissions, and cloud IAM so a compromise has minimal blast radius.\n\n\n```js\n// app DB user: SELECT/INSERT/UPDATE only, no DROP/GRANT\n```\n```\n",tech:"node",displayNumber:21,sortKey:1700},{id:"node-1701",number:22,title:"How do you generate cryptographically secure random values?",question:"How do you generate cryptographically secure random values?",difficulty:"Advanced",category:"Security & Authentication",answer:"Use `crypto.randomBytes`/`crypto.randomUUID`/`crypto.webcrypto`, never `Math.random()` (predictable). Use them for tokens, salts, session IDs, and password-reset codes.\n\n\n```js\nconst token = crypto.randomBytes(32).toString('hex')\n```\n```\n",tech:"node",displayNumber:22,sortKey:1701},{id:"node-1702",number:23,title:"Why is `Math.random()` insecure for tokens?",question:"Why is `Math.random()` insecure for tokens?",difficulty:"Advanced",category:"Security & Authentication",answer:"`Math.random()` is a non-cryptographic PRNG whose output can be predicted from observed values, letting attackers guess tokens/IDs. Security-sensitive randomness must come from a CSPRNG.\n\n\n```js\n// insecure: Math.random().toString(36)\ncrypto.randomUUID() // secure\n```\n```\n",tech:"node",displayNumber:23,sortKey:1702},{id:"node-1703",number:24,title:"What is a timing attack and how do you prevent it?",question:"What is a timing attack and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"A timing attack infers secrets from how long comparisons take (e.g. early-exit string compare). Use constant-time comparison (`crypto.timingSafeEqual`) for tokens/HMACs.\n\n\n```js\ncrypto.timingSafeEqual(Buffer.from(a), Buffer.from(b))\n```\n```\n",tech:"node",displayNumber:24,sortKey:1703},{id:"node-1704",number:25,title:"How do you sign and verify data with HMAC?",question:"How do you sign and verify data with HMAC?",difficulty:"Advanced",category:"Security & Authentication",answer:"HMAC combines a secret key with a hash to produce a tag that proves integrity/authenticity. Recompute and compare in constant time to verify (e.g. webhook signatures).\n\n\n```js\nconst mac = crypto.createHmac('sha256', secret).update(body).digest('hex')\n```\n```\n",tech:"node",displayNumber:25,sortKey:1704},{id:"node-1705",number:26,title:"What is the difference between hashing, encryption, and encoding?",question:"What is the difference between hashing, encryption, and encoding?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Hashing is one-way (integrity, passwords). Encryption is two-way with a key (confidentiality). Encoding (base64/hex) is reversible with no key and provides no security — just representation.\n\n\n```js\nBuffer.from('hi').toString('base64') // encoding, NOT security\n```\n```\n",tech:"node",displayNumber:26,sortKey:1705},{id:"node-1706",number:27,title:"How do you encrypt sensitive data at rest?",question:"How do you encrypt sensitive data at rest?",difficulty:"Experienced",category:"Security & Authentication",answer:"Use authenticated symmetric encryption (AES-256-GCM) with a securely managed key (KMS), storing the IV and auth tag. GCM provides confidentiality and integrity.\n\n\n```js\nconst c = crypto.createCipheriv('aes-256-gcm', key, iv)\nconst enc = Buffer.concat([c.update(data), c.final()]); const tag = c.getAuthTag()\n```\n```\n",tech:"node",displayNumber:27,sortKey:1706},{id:"node-1707",number:28,title:"Why use AES-GCM over AES-CBC?",question:"Why use AES-GCM over AES-CBC?",difficulty:"Experienced",category:"Security & Authentication",answer:"GCM is authenticated encryption (AEAD): it detects tampering via an auth tag. CBC provides only confidentiality and needs a separate MAC; misuse leads to padding-oracle attacks. Prefer GCM.\n\n\n```js\ncrypto.createCipheriv('aes-256-gcm', key, iv) // authenticated\n```\n```\n",tech:"node",displayNumber:28,sortKey:1707},{id:"node-1708",number:29,title:"What is key rotation and why does it matter?",question:"What is key rotation and why does it matter?",difficulty:"Experienced",category:"Security & Authentication",answer:"Key rotation periodically replaces cryptographic keys to limit exposure if a key leaks. Support multiple active keys (by key id) so old data can still be decrypted/verified during transition.\n\n\n```js\nconst key = keyring[token.kid] // pick key by id, enabling rotation\n```\n```\n",tech:"node",displayNumber:29,sortKey:1708},{id:"node-1709",number:30,title:"How do you securely handle environment secrets?",question:"How do you securely handle environment secrets?",difficulty:"Advanced",category:"Security & Authentication",answer:"Load secrets from a secret manager (Vault, AWS Secrets Manager) or injected env vars, never commit them, restrict access, rotate regularly, and avoid logging them.\n\n\n```js\nconst dbPass = await secrets.get('db/password') // not in code/repo\n```\n```\n",tech:"node",displayNumber:30,sortKey:1709},{id:"node-1710",number:31,title:"What is rate limiting's role in security?",question:"What is rate limiting's role in security?",difficulty:"Advanced",category:"Security & Authentication",answer:"Rate limiting throttles requests per client to slow brute-force credential attacks, prevent abuse/DoS, and protect expensive endpoints. Combine per-IP and per-account limits.\n\n\n```js\nloginLimiter({ windowMs: 15 * 60000, max: 5 }) // 5 login tries / 15 min\n```\n```\n",tech:"node",displayNumber:31,sortKey:1710},{id:"node-1711",number:32,title:"How do you protect against brute-force login attacks?",question:"How do you protect against brute-force login attacks?",difficulty:"Experienced",category:"Security & Authentication",answer:"Rate-limit login attempts, add exponential backoff/lockouts per account, use CAPTCHA after failures, require strong passwords/MFA, and use slow password hashing so each guess is costly.\n\n\n```js\nif (attempts > 5) await sleep(2 ** attempts * 100) // backoff\n```\n```\n",tech:"node",displayNumber:32,sortKey:1711},{id:"node-1712",number:33,title:"What is multi-factor authentication (MFA)?",question:"What is multi-factor authentication (MFA)?",difficulty:"Advanced",category:"Security & Authentication",answer:"MFA requires two+ factors: something you know (password), have (TOTP app/hardware key), or are (biometric). It protects accounts even if the password is compromised.\n\n\n```js\nconst valid = speakeasy.totp.verify({ secret, token: userCode })\n```\n```\n",tech:"node",displayNumber:33,sortKey:1712},{id:"node-1713",number:34,title:"How does TOTP work?",question:"How does TOTP work?",difficulty:"Experienced",category:"Security & Authentication",answer:"Time-based One-Time Password derives a short code from a shared secret and the current time window (usually 30s) via HMAC. Both server and authenticator app compute the same code independently.\n\n\n```js\nspeakeasy.totp({ secret, encoding: 'base32' }) // 6-digit code\n```\n```\n",tech:"node",displayNumber:34,sortKey:1713},{id:"node-1714",number:35,title:"What are security headers and which are essential?",question:"What are security headers and which are essential?",difficulty:"Experienced",category:"Security & Authentication",answer:"Essential headers: `Content-Security-Policy`, `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `X-Frame-Options` (clickjacking), and `Referrer-Policy`. `helmet` sets sane defaults.\n\n\n```js\napp.use(helmet())\n```\n```\n",tech:"node",displayNumber:35,sortKey:1714},{id:"node-1715",number:36,title:"What is clickjacking and how do you prevent it?",question:"What is clickjacking and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Clickjacking tricks users into clicking hidden framed content. Prevent by disallowing framing with `X-Frame-Options: DENY` or CSP `frame-ancestors 'none'`.\n\n\n```js\nres.setHeader('Content-Security-Policy', \"frame-ancestors 'none'\")\n```\n```\n",tech:"node",displayNumber:36,sortKey:1715},{id:"node-1716",number:37,title:"How do you prevent NoSQL injection?",question:"How do you prevent NoSQL injection?",difficulty:"Experienced",category:"Security & Authentication",answer:"Attackers can inject query operators (e.g. `{ $ne: null }`) via JSON. Validate/cast types, reject object values where scalars are expected, and use schema validation to strip operators.\n\n\n```js\nif (typeof req.body.email !== 'string') return res.status(400).end()\n```\n```\n",tech:"node",displayNumber:37,sortKey:1716},{id:"node-1717",number:38,title:"What is the danger of `eval` and dynamic code?",question:"What is the danger of `eval` and dynamic code?",difficulty:"Advanced",category:"Security & Authentication",answer:"`eval`/`new Function` executes arbitrary strings as code; with untrusted input this is remote code execution. Avoid them; parse data with `JSON.parse` and use safe alternatives.\n\n\n```js\nJSON.parse(input) // not eval('(' + input + ')')\n```\n```\n",tech:"node",displayNumber:38,sortKey:1717},{id:"node-1718",number:39,title:"What is prototype pollution?",question:"What is prototype pollution?",difficulty:"Experienced",category:"Security & Authentication",answer:"Prototype pollution injects `__proto__`/`constructor.prototype` keys via merged untrusted objects, altering all objects' behavior and enabling DoS/RCE. Guard by rejecting those keys, using `Object.create(null)`, or `Map`.\n\n\n```js\nif (key === '__proto__' || key === 'constructor') continue\n```\n```\n",tech:"node",displayNumber:39,sortKey:1718},{id:"node-1719",number:40,title:"How do you validate and sanitize user input?",question:"How do you validate and sanitize user input?",difficulty:"Advanced",category:"Security & Authentication",answer:"Validate structure/type/range against a schema at the boundary, reject invalid input, and sanitize/escape when outputting (HTML, SQL, shell). Treat all external input as hostile.\n\n\n```js\nconst data = schema.parse(req.body) // whitelist-validate\n```\n```\n",tech:"node",displayNumber:40,sortKey:1719},{id:"node-1720",number:41,title:"What is the OWASP Top 10?",question:"What is the OWASP Top 10?",difficulty:"Advanced",category:"Security & Authentication",answer:"A widely-used list of the most critical web app security risks (e.g. injection, broken access control, cryptographic failures, SSRF). It guides prioritizing defenses.\n\n\n```js\n// checklist: authz, injection, crypto, SSRF, misconfig, ...\n```\n```\n",tech:"node",displayNumber:41,sortKey:1720},{id:"node-1721",number:42,title:"What is broken access control and how do you prevent it?",question:"What is broken access control and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"It's failing to enforce what users may do — e.g. IDOR where changing an ID accesses others' data. Prevent by checking authorization on every request against the resource owner, never trusting client-side checks.\n\n\n```js\nif (order.userId !== req.user.id) return res.status(403).end()\n```\n```\n",tech:"node",displayNumber:42,sortKey:1721},{id:"node-1722",number:43,title:"What is IDOR (Insecure Direct Object Reference)?",question:"What is IDOR (Insecure Direct Object Reference)?",difficulty:"Experienced",category:"Security & Authentication",answer:"IDOR occurs when an object reference (id) in a request isn't checked against the requester's permissions, letting them access others' resources by guessing/changing ids. Always verify ownership/scope server-side.\n\n\n```js\nconst doc = await db.docs.findOne({ _id: id, ownerId: req.user.id })\n```\n```\n",tech:"node",displayNumber:43,sortKey:1722},{id:"node-1723",number:44,title:"How do you securely reset passwords?",question:"How do you securely reset passwords?",difficulty:"Experienced",category:"Security & Authentication",answer:"Email a single-use, expiring, cryptographically-random token (store only its hash), verify it, then update the password and invalidate existing sessions. Don't reveal whether an email exists.\n\n\n```js\nconst token = crypto.randomBytes(32).toString('hex')\nawait db.saveReset(userId, sha256(token), Date.now() + 3600e3)\n```\n```\n",tech:"node",displayNumber:44,sortKey:1723},{id:"node-1724",number:45,title:"Why avoid leaking whether a username/email exists?",question:"Why avoid leaking whether a username/email exists?",difficulty:"Advanced",category:"Security & Authentication",answer:"Different responses for existing vs non-existing accounts enable user enumeration, aiding targeted attacks. Return generic messages and consistent timing for login/reset flows.\n\n\n```js\nreturn res.json({ message: 'If the account exists, an email was sent' })\n```\n```\n",tech:"node",displayNumber:45,sortKey:1724},{id:"node-1725",number:46,title:"What is HTTPS and why is it mandatory?",question:"What is HTTPS and why is it mandatory?",difficulty:"Intermediate",category:"Security & Authentication",answer:"HTTPS encrypts traffic with TLS, protecting credentials/data from eavesdropping and tampering and authenticating the server. Without it, cookies/tokens can be stolen on the network.\n\n\n```js\nres.setHeader('Strict-Transport-Security', 'max-age=31536000')\n```\n```\n",tech:"node",displayNumber:46,sortKey:1725},{id:"node-1726",number:47,title:"What is certificate pinning?",question:"What is certificate pinning?",difficulty:"Experienced",category:"Security & Authentication",answer:"Pinning restricts accepted TLS certificates to a known set (or CA), so a mis-issued/rogue certificate is rejected, defeating some MITM attacks. It adds operational risk when certs rotate.\n\n\n```js\nconst agent = new https.Agent({ ca: fs.readFileSync('pinned-ca.pem') })\n```\n```\n",tech:"node",displayNumber:47,sortKey:1726},{id:"node-1727",number:48,title:"How do you handle dependency vulnerabilities?",question:"How do you handle dependency vulnerabilities?",difficulty:"Advanced",category:"Security & Authentication",answer:"Scan with `npm audit`/Snyk/Dependabot, keep dependencies updated, pin via lockfiles, minimize the dependency surface, and review install scripts to reduce supply-chain risk.\n\n\n```js\nnpm audit --audit-level=high\n```\n```\n",tech:"node",displayNumber:48,sortKey:1727},{id:"node-1728",number:49,title:"What is a supply-chain attack?",question:"What is a supply-chain attack?",difficulty:"Experienced",category:"Security & Authentication",answer:"Attackers compromise a dependency (typosquatting, hijacked maintainer, malicious update) so your build/runtime executes their code. Mitigate with lockfiles, `--ignore-scripts`, provenance checks, and minimal trusted deps.\n\n\n```js\nnpm ci --ignore-scripts\n```\n```\n",tech:"node",displayNumber:49,sortKey:1728},{id:"node-1729",number:50,title:"How do you prevent sensitive data exposure in logs?",question:"How do you prevent sensitive data exposure in logs?",difficulty:"Advanced",category:"Security & Authentication",answer:"Redact passwords, tokens, PII, and secrets before logging; use structured loggers with redaction rules, and never log full request bodies/headers containing credentials.\n\n\n```js\nlogger.info({ user: id }, 'login') // no password/token fields\n```\n```\n",tech:"node",displayNumber:50,sortKey:1729},{id:"node-1730",number:51,title:"What is the secure cookie flag set and its purpose?",question:"What is the secure cookie flag set and its purpose?",difficulty:"Advanced",category:"Security & Authentication",answer:"`HttpOnly` blocks JS access (XSS theft), `Secure` sends only over HTTPS, `SameSite` limits cross-site sending (CSRF), and `__Host-` prefix enforces path/host binding. Combine them for session cookies.\n\n\n```js\nres.cookie('sid', v, { httpOnly: true, secure: true, sameSite: 'strict' })\n```\n```\n",tech:"node",displayNumber:51,sortKey:1730},{id:"node-1731",number:52,title:"What is session fixation and how do you prevent it?",question:"What is session fixation and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"An attacker sets a known session id before login, then hijacks the authenticated session. Prevent by regenerating the session id on privilege change (login) so the pre-set id becomes useless.\n\n\n```js\nreq.session.regenerate(() => { req.session.userId = user.id })\n```\n```\n",tech:"node",displayNumber:52,sortKey:1731},{id:"node-1732",number:53,title:"How do you protect an API with API keys?",question:"How do you protect an API with API keys?",difficulty:"Advanced",category:"Security & Authentication",answer:"Issue random keys, store only their hashes, transmit over HTTPS in a header, scope/rate-limit per key, and support rotation/revocation. Keys identify apps, not end users.\n\n\n```js\nconst hash = sha256(req.headers['x-api-key'])\nconst key = await db.keys.findOne({ hash, active: true })\n```\n```\n",tech:"node",displayNumber:53,sortKey:1732},{id:"node-1733",number:54,title:"What is the difference between authentication tokens in header vs cookie?",question:"What is the difference between authentication tokens in header vs cookie?",difficulty:"Experienced",category:"Security & Authentication",answer:"Header (Bearer) tokens aren't sent automatically, so they resist CSRF but are exposed to XSS if stored in JS-accessible storage. HttpOnly cookies resist XSS theft but need CSRF defenses. Pick based on your threat model.\n\n\n```js\nAuthorization: Bearer <token> // not auto-sent -> CSRF-resistant\n```\n```\n",tech:"node",displayNumber:54,sortKey:1733},{id:"node-1734",number:55,title:"How do you implement logout with JWTs?",question:"How do you implement logout with JWTs?",difficulty:"Experienced",category:"Security & Authentication",answer:"Delete the client token/cookie and, for real invalidation, add the token's `jti` (or user token version) to a denylist until it expires, since JWTs can't be un-signed.\n\n\n```js\nawait redis.set(`revoked:${jti}`, '1', 'EX', ttl); res.clearCookie('token')\n```\n```\n",tech:"node",displayNumber:55,sortKey:1734},{id:"node-1735",number:56,title:"What is CORS misconfiguration risk?",question:"What is CORS misconfiguration risk?",difficulty:"Experienced",category:"Security & Authentication",answer:"Reflecting the request `Origin` with `Access-Control-Allow-Credentials: true` lets any site make authenticated cross-origin requests. Whitelist specific trusted origins; never combine wildcard origin with credentials.\n\n\n```js\nif (allowlist.has(origin)) res.setHeader('Access-Control-Allow-Origin', origin)\n```\n```\n",tech:"node",displayNumber:56,sortKey:1735},{id:"node-1736",number:57,title:"How do you securely handle file uploads?",question:"How do you securely handle file uploads?",difficulty:"Experienced",category:"Security & Authentication",answer:"Validate type/size, store outside the web root with random names, scan for malware, never trust the client filename/MIME, and serve with correct `Content-Type` and `Content-Disposition` to prevent execution.\n\n\n```js\nconst name = crypto.randomUUID() + path.extname(original)\n```\n```\n",tech:"node",displayNumber:57,sortKey:1736},{id:"node-1737",number:58,title:"What is SSRF and how do you defend against it?",question:"What is SSRF and how do you defend against it?",difficulty:"Experienced",category:"Security & Authentication",answer:"Server-Side Request Forgery abuses server-side fetching to reach internal services/metadata endpoints. Defend by whitelisting destinations, blocking private/link-local IPs, disabling redirects to them, and validating URLs.\n\n\n```js\nif (isPrivate(dns.lookup(host))) throw new Error('blocked host')\n```\n```\n",tech:"node",displayNumber:58,sortKey:1737},{id:"node-1738",number:59,title:"How do you prevent open redirects?",question:"How do you prevent open redirects?",difficulty:"Advanced",category:"Security & Authentication",answer:"Don't redirect to arbitrary user-supplied URLs (used in phishing). Whitelist allowed paths/hosts or use mapping keys instead of raw URLs.\n\n\n```js\nconst dest = allowed[req.query.next] ?? '/'\nres.redirect(dest)\n```\n```\n",tech:"node",displayNumber:59,sortKey:1738},{id:"node-1739",number:60,title:"What is content-type sniffing and `nosniff`?",question:"What is content-type sniffing and `nosniff`?",difficulty:"Advanced",category:"Security & Authentication",answer:"Browsers may guess a response's type, potentially executing uploaded files as scripts. `X-Content-Type-Options: nosniff` forces the declared `Content-Type`, preventing MIME-sniffing attacks.\n\n\n```js\nres.setHeader('X-Content-Type-Options', 'nosniff')\n```\n```\n",tech:"node",displayNumber:60,sortKey:1739},{id:"node-1740",number:61,title:"How do you secure inter-service communication?",question:"How do you secure inter-service communication?",difficulty:"Experienced",category:"Security & Authentication",answer:"Use mutual TLS (mTLS) or signed service tokens (JWT with short expiry), authorize by service identity, encrypt in transit, and apply least-privilege network policies between services.\n\n\n```js\nhttps.request({ cert: clientCert, key: clientKey, ... }) // mTLS\n```\n```\n",tech:"node",displayNumber:61,sortKey:1740},{id:"node-1741",number:62,title:"What is the difference between symmetric and asymmetric encryption?",question:"What is the difference between symmetric and asymmetric encryption?",difficulty:"Advanced",category:"Security & Authentication",answer:"Symmetric uses one shared secret key (fast, for bulk data, e.g. AES). Asymmetric uses a public/private key pair (slower, for key exchange and signatures, e.g. RSA/ECDSA). TLS combines both.\n\n\n```js\n// AES for data, RSA/ECDH to exchange the AES key\n```\n```\n",tech:"node",displayNumber:62,sortKey:1741},{id:"node-1742",number:63,title:"How do you sign JWTs asymmetrically (RS256) and why?",question:"How do you sign JWTs asymmetrically (RS256) and why?",difficulty:"Experienced",category:"Security & Authentication",answer:"Sign with a private key; anyone can verify with the public key. This lets multiple services verify tokens without sharing a signing secret, ideal for distributed systems and third-party verification.\n\n\n```js\njwt.sign(claims, privateKey, { algorithm: 'RS256' })\njwt.verify(token, publicKey, { algorithms: ['RS256'] })\n```\n```\n",tech:"node",displayNumber:63,sortKey:1742},{id:"node-1743",number:64,title:"What is a nonce and where is it used?",question:"What is a nonce and where is it used?",difficulty:"Advanced",category:"Security & Authentication",answer:"A nonce is a number used once to prevent replay and ensure uniqueness — in CSP script hashes, OAuth/OIDC requests, and encryption IVs. Reusing a nonce (especially in GCM) breaks security.\n\n\n```js\nconst nonce = crypto.randomBytes(16).toString('base64')\nres.setHeader('Content-Security-Policy', `script-src 'nonce-${nonce}'`)\n```\n```\n",tech:"node",displayNumber:64,sortKey:1743},{id:"node-1744",number:65,title:"How do you protect against replay attacks?",question:"How do you protect against replay attacks?",difficulty:"Experienced",category:"Security & Authentication",answer:"Include a timestamp and nonce in signed requests, reject old/duplicate ones (track seen nonces within a window), and use short token lifetimes. Common for webhooks and API signing.\n\n\n```js\nif (Date.now() - ts > 300000 || seen.has(nonce)) reject()\n```\n```\n",tech:"node",displayNumber:65,sortKey:1744},{id:"node-1745",number:66,title:"What is defense in depth?",question:"What is defense in depth?",difficulty:"Advanced",category:"Security & Authentication",answer:"Layering multiple independent controls (validation, authz, WAF, least privilege, encryption, monitoring) so if one fails, others still protect the system. No single control is trusted alone.\n\n\n```js\n// validate + parameterize + authorize + rate-limit + log\n```\n```\n",tech:"node",displayNumber:66,sortKey:1745},{id:"node-1746",number:67,title:"How do you handle secrets in Docker/CI?",question:"How do you handle secrets in Docker/CI?",difficulty:"Experienced",category:"Security & Authentication",answer:"Inject secrets at runtime via environment/secret stores, not baked into images or committed to CI configs. Use masked CI secrets, avoid `ARG`/`ENV` for secrets in Dockerfiles, and scan images.\n\n\n```js\ndocker run --env-file secrets.env app # not COPY secrets into image\n```\n```\n",tech:"node",displayNumber:67,sortKey:1746},{id:"node-1747",number:68,title:"What is subresource integrity (SRI)?",question:"What is subresource integrity (SRI)?",difficulty:"Experienced",category:"Security & Authentication",answer:'SRI adds an `integrity` hash to script/link tags so the browser verifies a CDN-loaded file wasn\'t tampered with, failing if the hash doesn\'t match.\n\n\n```js\n// <script src="cdn/lib.js" integrity="sha384-..." crossorigin="anonymous">\n```\n```\n',tech:"node",displayNumber:68,sortKey:1747},{id:"node-1748",number:69,title:"How do you audit and monitor security events?",question:"How do you audit and monitor security events?",difficulty:"Experienced",category:"Security & Authentication",answer:"Log authentication attempts, authorization failures, and sensitive actions with context; centralize logs, alert on anomalies (spikes in 401/403, new geos), and retain audit trails immutably for investigation.\n\n\n```js\nlogger.warn({ userId, ip }, 'authz_denied') // feed to SIEM/alerts\n```\n```\n",tech:"node",displayNumber:69,sortKey:1748},{id:"node-1749",number:70,title:"What is a security misconfiguration and common examples?",question:"What is a security misconfiguration and common examples?",difficulty:"Advanced",category:"Security & Authentication",answer:"Insecure defaults or incomplete setup: verbose error stacks in prod, default credentials, open cloud buckets, disabled TLS, permissive CORS, exposed admin endpoints. Harden defaults, minimize surface, and review configs.\n\n\n```js\nif (process.env.NODE_ENV === 'production') app.set('json spaces', 0) // no stack leaks\n```\n```\n",tech:"node",displayNumber:70,sortKey:1749},{id:"node-1750",number:1,title:"How do you profile a Node application?",question:"How do you profile a Node application?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use `node --prof` + `--prof-process`, the `--inspect` DevTools CPU profiler, or flame-graph tools (`clinic flame`, `0x`) to find hot functions and blocking code.\n\n\n```js\n$ node --prof app.js && node --prof-process isolate-*.log\n```\n```\n",tech:"node",displayNumber:1,sortKey:1750},{id:"node-1751",number:2,title:"What is a flame graph?",question:"What is a flame graph?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"A flame graph visualizes stack samples: width shows time spent, stacking shows call hierarchy. Wide bars at the top are hotspots to optimize.\n\n\n```js\n$ npx 0x app.js\n```\n```\n",tech:"node",displayNumber:2,sortKey:1751},{id:"node-1752",number:3,title:"How do you detect event loop blocking?",question:"How do you detect event loop blocking?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Measure event loop delay with `perf_hooks.monitorEventLoopDelay` or a drift timer. High delay means synchronous CPU work is blocking; offload it to worker threads or chunk it.\n\n\n```js\nconst h = require('perf_hooks').monitorEventLoopDelay(); h.enable()\n```\n```\n",tech:"node",displayNumber:3,sortKey:1752},{id:"node-1753",number:4,title:"What are common causes of high memory usage?",question:"What are common causes of high memory usage?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Unbounded caches, buffering large payloads instead of streaming, retained closures/listeners, global accumulation, and large in-memory data structures. Profile heap snapshots to locate retainers.\n\n\n```js\nconsole.log(process.memoryUsage().heapUsed)\n```\n```\n",tech:"node",displayNumber:4,sortKey:1753},{id:"node-1754",number:5,title:"How do you take and analyze a heap snapshot?",question:"How do you take and analyze a heap snapshot?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Start with `--inspect`, capture a heap snapshot in Chrome DevTools (or `v8.writeHeapSnapshot()`), and compare snapshots over time to find objects that grow — the leak's retainers.\n\n\n```js\nrequire('v8').writeHeapSnapshot('./heap.heapsnapshot')\n```\n```\n",tech:"node",displayNumber:5,sortKey:1754},{id:"node-1755",number:6,title:"How do you scale a Node app across CPU cores?",question:"How do you scale a Node app across CPU cores?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Run one process per core with the `cluster` module or a process manager (pm2), or run multiple container replicas behind a load balancer. Node's single thread can't use multiple cores alone.\n\n\n```js\npm2 start app.js -i max # one instance per core\n```\n```\n",tech:"node",displayNumber:6,sortKey:1755},{id:"node-1756",number:7,title:"What is horizontal vs vertical scaling?",question:"What is horizontal vs vertical scaling?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Vertical scaling adds resources (CPU/RAM) to one machine (simple but capped). Horizontal scaling adds more machines/instances behind a balancer (elastic, requires statelessness). Node apps typically scale horizontally.\n\n\n```js\n// stateless app + LB + N replicas -> horizontal scale\n```\n```\n",tech:"node",displayNumber:7,sortKey:1756},{id:"node-1757",number:8,title:"How do you offload CPU-bound work?",question:"How do you offload CPU-bound work?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Move heavy computation to worker threads, a separate service, or a job queue, keeping the main event loop responsive. Never run long synchronous loops in request handlers.\n\n\n```js\nnew Worker('./cpu-task.js', { workerData }).on('message', done)\n```\n```\n",tech:"node",displayNumber:8,sortKey:1757},{id:"node-1758",number:9,title:"What is a job queue and when do you use one?",question:"What is a job queue and when do you use one?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A job queue (BullMQ/Redis, SQS) defers work to background workers, decoupling request handling from slow tasks (emails, image processing, reports) and enabling retries, rate control, and scaling workers independently.\n\n\n```js\nawait queue.add('email', { to, body })\n```\n```\n",tech:"node",displayNumber:9,sortKey:1758},{id:"node-1759",number:10,title:"How do you implement caching to improve performance?",question:"How do you implement caching to improve performance?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Cache expensive results in memory (`lru-cache`) or Redis with TTLs, use HTTP caching headers/CDN for responses, and memoize pure computations. The hard part is invalidation on data change.\n\n\n```js\nconst cache = new LRUCache({ max: 1000, ttl: 60000 })\n```\n```\n",tech:"node",displayNumber:10,sortKey:1759},{id:"node-1760",number:11,title:"What is the difference between in-memory and distributed cache?",question:"What is the difference between in-memory and distributed cache?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"In-memory cache is per-process (fast, but not shared and lost on restart). Distributed cache (Redis) is shared across instances (consistent, survives restarts) at network-call cost. Use in-memory for hot local data, Redis for shared state.\n\n\n```js\n// L1: process LRU (fast) -> L2: Redis (shared)\n```\n```\n",tech:"node",displayNumber:11,sortKey:1760},{id:"node-1761",number:12,title:"How do you reduce startup time / cold starts?",question:"How do you reduce startup time / cold starts?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Lazy-load heavy modules, minimize top-level work, reduce dependencies, use snapshots/bundling, and keep instances warm (provisioned concurrency in serverless). Defer non-critical initialization.\n\n\n```js\nlet pdf; function makePdf() { pdf ??= require('pdfkit') } // lazy\n```\n```\n",tech:"node",displayNumber:12,sortKey:1761},{id:"node-1762",number:13,title:"What is `--max-old-space-size`?",question:"What is `--max-old-space-size`?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"It sets V8's old-generation heap limit in MB. Raise it for memory-heavy apps, but prefer streaming/pagination over just enlarging the heap, and match container memory limits.\n\n\n```js\nnode --max-old-space-size=2048 app.js\n```\n```\n",tech:"node",displayNumber:13,sortKey:1762},{id:"node-1763",number:14,title:"How do you benchmark code performance?",question:"How do you benchmark code performance?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use high-resolution timers (`perf_hooks.performance.now()`), run many iterations, warm up the JIT, and use tools like `benchmark.js` or `autocannon` for HTTP load. Beware micro-benchmark pitfalls.\n\n\n```js\n$ npx autocannon -c 100 -d 10 http://localhost:3000\n```\n```\n",tech:"node",displayNumber:14,sortKey:1763},{id:"node-1764",number:15,title:"What is `autocannon` / load testing?",question:"What is `autocannon` / load testing?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Load testing sends concurrent traffic to measure throughput, latency percentiles, and error rates under stress. It reveals bottlenecks and capacity limits before production traffic does.\n\n\n```js\nautocannon -c 200 -d 30 http://localhost:3000/api\n```\n```\n",tech:"node",displayNumber:15,sortKey:1764},{id:"node-1765",number:16,title:"Why look at latency percentiles (p95/p99) not averages?",question:"Why look at latency percentiles (p95/p99) not averages?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Averages hide tail latency; a few slow requests hurt user experience and cascade. p95/p99 show what the worst-served users experience, guiding SLOs and revealing GC pauses or contention.\n\n\n```js\n// optimize the p99, not just the mean\n```\n```\n",tech:"node",displayNumber:16,sortKey:1765},{id:"node-1766",number:17,title:"What is the difference between throughput and latency?",question:"What is the difference between throughput and latency?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Latency is the time for one request; throughput is requests handled per unit time. They interact — high concurrency can raise throughput while worsening tail latency. Optimize the one your SLO targets.\n\n\n```js\n// 1000 req/s throughput, p99 latency 200ms\n```\n```\n",tech:"node",displayNumber:17,sortKey:1766},{id:"node-1767",number:18,title:"How do you reduce JSON serialization overhead?",question:"How do you reduce JSON serialization overhead?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Serialize only needed fields, use schema-based fast serializers (`fast-json-stringify`), stream large responses, and avoid re-stringifying. `JSON.stringify` on huge objects blocks the loop.\n\n\n```js\nconst stringify = fastJson(schema) // precompiled, ~2x faster\n```\n```\n",tech:"node",displayNumber:18,sortKey:1767},{id:"node-1768",number:19,title:"What is the unit testing pyramid?",question:"What is the unit testing pyramid?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Many fast unit tests at the base, fewer integration tests in the middle, and few slow end-to-end tests at the top. It balances coverage, speed, and confidence.\n\n\n```js\n// lots of unit, some integration, few e2e\n```\n```\n",tech:"node",displayNumber:19,sortKey:1768},{id:"node-1769",number:20,title:"What testing frameworks are used in Node?",question:"What testing frameworks are used in Node?",difficulty:"Basic",category:"Performance, Testing & Deployment",answer:"Jest (all-in-one), Vitest (fast, ESM/TS), Mocha+Chai, and the built-in `node:test` runner. They provide assertions, mocking, and coverage.\n\n\n```js\nimport { test } from 'node:test'\nimport assert from 'node:assert'\ntest('adds', () => assert.equal(1 + 1, 2))\n```\n```\n",tech:"node",displayNumber:20,sortKey:1769},{id:"node-1770",number:21,title:"What is the difference between unit, integration, and e2e tests?",question:"What is the difference between unit, integration, and e2e tests?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Unit tests isolate one function (mock deps). Integration tests verify components together (e.g. route + DB). E2E tests exercise the whole system as a user would. Each catches different bug classes.\n\n\n```js\n// unit: service.calc(); integration: POST /orders + db; e2e: browser flow\n```\n```\n",tech:"node",displayNumber:21,sortKey:1770},{id:"node-1771",number:22,title:"What is mocking and when do you use it?",question:"What is mocking and when do you use it?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Mocking replaces real dependencies (DB, HTTP, time) with controllable fakes to isolate the unit, make tests deterministic/fast, and simulate errors. Over-mocking, however, can make tests brittle and unrealistic.\n\n\n```js\njest.spyOn(mailer, 'send').mockResolvedValue(true)\n```\n```\n",tech:"node",displayNumber:22,sortKey:1771},{id:"node-1772",number:23,title:"What is the difference between a mock, stub, and spy?",question:"What is the difference between a mock, stub, and spy?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A stub returns canned values; a spy records how a real/replaced function was called; a mock is a preprogrammed fake with expectations you assert against. Terms overlap across libraries.\n\n\n```js\nconst spy = jest.fn(); handler(spy); expect(spy).toHaveBeenCalledWith(1)\n```\n```\n",tech:"node",displayNumber:23,sortKey:1772},{id:"node-1773",number:24,title:"How do you test async code?",question:"How do you test async code?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Return/await the promise so the runner waits, assert on resolved values or rejections, and use fake timers to control delays without real waiting.\n\n\n```js\nawait expect(fetchUser(1)).resolves.toMatchObject({ id: 1 })\n```\n```\n",tech:"node",displayNumber:24,sortKey:1773},{id:"node-1774",number:25,title:"What are fake timers?",question:"What are fake timers?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Fake timers replace `setTimeout`/`Date` so tests can advance time instantly, testing debounce/retry/scheduling logic without real delays.\n\n\n```js\njest.useFakeTimers(); run(); jest.advanceTimersByTime(1000)\n```\n```\n",tech:"node",displayNumber:25,sortKey:1774},{id:"node-1775",number:26,title:"What is code coverage and its limits?",question:"What is code coverage and its limits?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Coverage measures which lines/branches tests execute. High coverage doesn't guarantee correctness (you can execute code without asserting behavior). Use it to find untested areas, not as the sole quality metric.\n\n\n```js\n$ node --test --experimental-test-coverage\n```\n```\n",tech:"node",displayNumber:26,sortKey:1775},{id:"node-1776",number:27,title:"How do you test HTTP endpoints without a real server port?",question:"How do you test HTTP endpoints without a real server port?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Export the app and use `supertest`, which spins up the server on an ephemeral port per test, sends requests, and asserts responses — isolated and parallel-safe.\n\n\n```js\nawait request(app).get('/health').expect(200)\n```\n```\n",tech:"node",displayNumber:27,sortKey:1776},{id:"node-1777",number:28,title:"How do you isolate database tests?",question:"How do you isolate database tests?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use a dedicated test DB (containerized), wrap each test in a transaction that rolls back, or truncate between tests. Avoid shared state so tests are deterministic and parallelizable.\n\n\n```js\nafterEach(() => db.query('ROLLBACK'))\n```\n```\n",tech:"node",displayNumber:28,sortKey:1777},{id:"node-1778",number:29,title:"What is test-driven development (TDD)?",question:"What is test-driven development (TDD)?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"TDD writes a failing test first, then minimal code to pass, then refactors (red-green-refactor). It drives design, ensures coverage, and provides a safety net for change.\n\n\n```js\n// 1. write failing test 2. make it pass 3. refactor\n```\n```\n",tech:"node",displayNumber:29,sortKey:1778},{id:"node-1779",number:30,title:"What is a flaky test and how do you fix it?",question:"What is a flaky test and how do you fix it?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A flaky test passes/fails nondeterministically, usually from timing, shared state, ordering, or real network. Fix by removing nondeterminism: fake timers, isolate state, mock external calls, and await properly.\n\n\n```js\n// replace sleep-based waits with deterministic awaits/fake timers\n```\n```\n",tech:"node",displayNumber:30,sortKey:1779},{id:"node-1780",number:31,title:"How do you structure a CI pipeline for Node?",question:"How do you structure a CI pipeline for Node?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"On each push: install with `npm ci`, lint, run tests with coverage, build, and (on main) deploy. Cache dependencies, run stages in parallel, and fail fast on errors.\n\n\n```js\n- run: npm ci\n- run: npm test\n- run: npm run build\n```\n```\n",tech:"node",displayNumber:31,sortKey:1780},{id:"node-1781",number:32,title:"Why use `npm ci` in CI instead of `npm install`?",question:"Why use `npm ci` in CI instead of `npm install`?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"`npm ci` installs exactly from the lockfile (deleting `node_modules` first), giving reproducible, faster installs and failing if `package.json` and the lockfile disagree.\n\n\n```js\nnpm ci --omit=dev\n```\n```\n",tech:"node",displayNumber:32,sortKey:1781},{id:"node-1782",number:33,title:"How do you containerize a Node app with Docker?",question:"How do you containerize a Node app with Docker?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:'Use an official Node base image, copy `package*.json` and install first (layer caching), copy source, set `NODE_ENV=production`, run as a non-root user, and use a multi-stage build to shrink the image.\n\n\n```js\nFROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./ \nRUN npm ci --omit=dev\nCOPY . .\nUSER node\nCMD ["node", "index.js"]\n```\n```\n',tech:"node",displayNumber:33,sortKey:1782},{id:"node-1783",number:34,title:"What is a multi-stage Docker build?",question:"What is a multi-stage Docker build?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:`It uses one stage to build/compile (with dev deps and toolchains) and a slim final stage that copies only the artifacts, producing a smaller, more secure runtime image.


\`\`\`js
FROM node:20 AS build
RUN npm ci && npm run build
FROM node:20-alpine
COPY --from=build /app/dist ./dist
\`\`\`
\`\`\`
`,tech:"node",displayNumber:34,sortKey:1783},{id:"node-1784",number:35,title:"Why run the container as a non-root user?",question:"Why run the container as a non-root user?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Running as root increases blast radius if the app is compromised (container escape, file tampering). Use the built-in `node` user or create one, following least privilege.\n\n\n```js\nUSER node\n```\n```\n",tech:"node",displayNumber:35,sortKey:1784},{id:"node-1785",number:36,title:"How do you handle graceful shutdown in Kubernetes?",question:"How do you handle graceful shutdown in Kubernetes?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"K8s sends SIGTERM before killing a pod; handle it to stop accepting traffic (fail readiness), drain in-flight requests, close connections, and exit within `terminationGracePeriodSeconds`.\n\n\n```js\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```\n```\n",tech:"node",displayNumber:36,sortKey:1785},{id:"node-1786",number:37,title:"What are liveness and readiness probes?",question:"What are liveness and readiness probes?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Liveness checks the process is healthy (restart if not); readiness checks it can serve traffic now (remove from load balancing if not, without restarting). Implement lightweight HTTP endpoints for each.\n\n\n```js\napp.get('/live', (_, res) => res.end())\napp.get('/ready', async (_, res) => (await db.ping()) ? res.end() : res.status(503).end())\n```\n```\n",tech:"node",displayNumber:37,sortKey:1786},{id:"node-1787",number:38,title:"How do you achieve zero-downtime deployment?",question:"How do you achieve zero-downtime deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use rolling/blue-green/canary deploys behind a load balancer with readiness gating and graceful shutdown, plus backward-compatible DB migrations so old and new versions coexist during rollout.\n\n\n```js\n// rolling update: new pods pass readiness before old pods drain\n```\n```\n",tech:"node",displayNumber:38,sortKey:1787},{id:"node-1788",number:39,title:"What is blue-green deployment?",question:"What is blue-green deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Two identical environments (blue live, green idle). Deploy to green, test it, then switch traffic instantly. Rollback is switching back. It reduces risk but doubles infrastructure briefly.\n\n\n```js\n// switch LB target from blue -> green after health checks\n```\n```\n",tech:"node",displayNumber:39,sortKey:1788},{id:"node-1789",number:40,title:"What is canary deployment?",question:"What is canary deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Route a small percentage of traffic to the new version, monitor errors/latency, then gradually increase if healthy or roll back if not. It limits the blast radius of bad releases.\n\n\n```js\n// 5% traffic -> new version, watch metrics, then ramp to 100%\n```\n```\n",tech:"node",displayNumber:40,sortKey:1789},{id:"node-1790",number:41,title:"How do you manage configuration across environments?",question:"How do you manage configuration across environments?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Follow 12-factor: read all config from environment variables, validate at startup, keep secrets in a secret manager, and never bake env-specific values into images.\n\n\n```js\nconst cfg = envSchema.parse(process.env) // validated config\n```\n```\n",tech:"node",displayNumber:41,sortKey:1790},{id:"node-1791",number:42,title:"What is a process manager like pm2?",question:"What is a process manager like pm2?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"pm2 keeps Node processes alive (auto-restart on crash), clusters across cores, does zero-downtime reloads, and centralizes logs/metrics — useful on VMs (containers often use the orchestrator instead).\n\n\n```js\npm2 start app.js -i max && pm2 save\n```\n```\n",tech:"node",displayNumber:42,sortKey:1791},{id:"node-1792",number:43,title:"How do you handle logging in production?",question:"How do you handle logging in production?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use a structured JSON logger (`pino`) writing to stdout, include correlation IDs and levels, avoid logging secrets, and ship logs to a centralized system (ELK/Loki/Datadog) for search and alerting.\n\n\n```js\nlogger.info({ reqId, userId, ms }, 'request completed')\n```\n```\n",tech:"node",displayNumber:43,sortKey:1792},{id:"node-1793",number:44,title:"Why log to stdout instead of files in containers?",question:"Why log to stdout instead of files in containers?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Containers are ephemeral; files vanish on restart. Writing structured logs to stdout lets the platform collect, aggregate, and route them (12-factor), decoupling apps from log storage.\n\n\n```js\nlogger.info(obj) // -> stdout -> platform log pipeline\n```\n```\n",tech:"node",displayNumber:44,sortKey:1793},{id:"node-1794",number:45,title:"What is observability (logs, metrics, traces)?",question:"What is observability (logs, metrics, traces)?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"The three pillars: logs (discrete events), metrics (aggregated numbers like latency/throughput), and traces (request paths across services). Together they let you understand and debug production behavior.\n\n\n```js\nmetrics.increment('http.requests'); span.end() // metrics + trace\n```\n```\n",tech:"node",displayNumber:45,sortKey:1794},{id:"node-1795",number:46,title:"What is distributed tracing?",question:"What is distributed tracing?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Tracing propagates a trace/span context (e.g. `traceparent`) across services so you can see a single request's full path and where latency accrues. OpenTelemetry is the standard instrumentation.\n\n\n```js\nconst span = tracer.startSpan('db.query'); await q(); span.end()\n```\n```\n",tech:"node",displayNumber:46,sortKey:1795},{id:"node-1796",number:47,title:"How do you collect application metrics?",question:"How do you collect application metrics?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Expose counters/gauges/histograms (via `prom-client`) at a `/metrics` endpoint scraped by Prometheus, tracking request rate, error rate, latency, and resource usage for dashboards/alerts.\n\n\n```js\napp.get('/metrics', async (_, res) => res.end(await register.metrics()))\n```\n```\n",tech:"node",displayNumber:47,sortKey:1796},{id:"node-1797",number:48,title:"What are the RED and USE metrics methods?",question:"What are the RED and USE metrics methods?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"RED (Rate, Errors, Duration) monitors request-driven services. USE (Utilization, Saturation, Errors) monitors resources (CPU, memory, disk). Together they cover service health and capacity.\n\n\n```js\n// RED: req/s, error%, p99 latency\n```\n```\n",tech:"node",displayNumber:48,sortKey:1797},{id:"node-1798",number:49,title:"How do you set up health-based auto-restart?",question:"How do you set up health-based auto-restart?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use orchestrator liveness probes or a process manager to restart on crash/hang, plus alerting. Ensure the app crashes on programmer errors (don't swallow) so it can be restarted clean.\n\n\n```js\nprocess.on('uncaughtException', e => { log(e); process.exit(1) })\n```\n```\n",tech:"node",displayNumber:49,sortKey:1798},{id:"node-1799",number:50,title:"What is a memory leak's symptom in production?",question:"What is a memory leak's symptom in production?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Steadily climbing RSS/heap that never returns to baseline, rising GC frequency/pauses, growing latency, and eventual OOM restarts. Confirm with heap snapshots and monitor `memoryUsage` trends.\n\n\n```js\nsetInterval(() => metric('heap', process.memoryUsage().heapUsed), 10000)\n```\n```\n",tech:"node",displayNumber:50,sortKey:1799},{id:"node-1800",number:51,title:"How do you handle secrets in deployment?",question:"How do you handle secrets in deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Inject secrets at runtime from a secret manager or orchestrator secrets, mount as env/files, restrict access, rotate, and keep them out of images, repos, and logs.\n\n\n```js\n// K8s: mount Secret as env; app reads process.env at boot\n```\n```\n",tech:"node",displayNumber:51,sortKey:1800},{id:"node-1801",number:52,title:"What is the difference between `NODE_ENV=production` effects?",question:"What is the difference between `NODE_ENV=production` effects?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"`production` makes frameworks (Express, React) enable optimizations, disable verbose errors/debug, and cache views. Some libraries change behavior. Always set it in production for performance and safety.\n\n\n```js\n// Express caches templates; hides stack traces when production\n```\n```\n",tech:"node",displayNumber:52,sortKey:1801},{id:"node-1802",number:53,title:"How do you minimize Docker image size?",question:"How do you minimize Docker image size?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use slim/alpine bases, multi-stage builds, `npm ci --omit=dev`, `.dockerignore` to exclude junk, and combine/clean layers. Smaller images deploy faster and reduce attack surface.\n\n\n```js\n// .dockerignore: node_modules, .git, tests\n```\n```\n",tech:"node",displayNumber:53,sortKey:1802},{id:"node-1803",number:54,title:"How do you handle database migrations during deploy?",question:"How do you handle database migrations during deploy?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Run migrations as a separate, idempotent step before/at rollout, keep them backward-compatible for rolling deploys, and avoid destructive changes until old code is fully retired.\n\n\n```js\nnpx prisma migrate deploy # in CI/CD before app rollout\n```\n```\n",tech:"node",displayNumber:54,sortKey:1803},{id:"node-1804",number:55,title:"What is feature flagging?",question:"What is feature flagging?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Feature flags gate functionality at runtime so you can deploy code dark, roll out gradually, A/B test, and kill features instantly without redeploying. Decouples deploy from release.\n\n\n```js\nif (flags.isEnabled('new-checkout', user)) return newCheckout()\n```\n```\n",tech:"node",displayNumber:55,sortKey:1804},{id:"node-1805",number:56,title:"How do you implement graceful degradation?",question:"How do you implement graceful degradation?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"When a dependency fails, serve reduced functionality (cached/stale data, defaults, skipped non-critical features) instead of a full outage, using timeouts, circuit breakers, and fallbacks.\n\n\n```js\nconst recs = await getRecs().catch(() => []) // degrade, don't fail page\n```\n```\n",tech:"node",displayNumber:56,sortKey:1805},{id:"node-1806",number:57,title:"What is a circuit breaker in deployment resilience?",question:"What is a circuit breaker in deployment resilience?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"It trips open after repeated failures to a dependency, failing fast and giving it time to recover, then half-opens to test before closing. It prevents cascading failures and resource exhaustion.\n\n\n```js\nbreaker.fire(request).catch(() => fallback())\n```\n```\n",tech:"node",displayNumber:57,sortKey:1806},{id:"node-1807",number:58,title:"How do you handle configuration reload without restart?",question:"How do you handle configuration reload without restart?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Watch a config source (file/consul) or receive a signal (`SIGHUP`) to reload settings into memory, applying changes atomically. Keep it optional; some changes still need a restart.\n\n\n```js\nprocess.on('SIGHUP', () => reloadConfig())\n```\n```\n",tech:"node",displayNumber:58,sortKey:1807},{id:"node-1808",number:59,title:"What are the benefits of serverless for Node?",question:"What are the benefits of serverless for Node?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Serverless (Lambda/Cloud Functions) auto-scales, bills per invocation, and removes server management — great for spiky/event-driven workloads. Trade-offs: cold starts, execution limits, and statelessness.\n\n\n```js\nexport const handler = async (event) => ({ statusCode: 200, body: 'ok' })\n```\n```\n",tech:"node",displayNumber:59,sortKey:1808},{id:"node-1809",number:60,title:"What causes cold starts and how do you reduce them?",question:"What causes cold starts and how do you reduce them?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Cold starts are the latency of initializing a new function instance (runtime + deps + init code). Reduce by shrinking bundle/deps, lazy-loading, provisioned concurrency, and keeping functions warm.\n\n\n```js\n// move heavy require() inside handler only if rarely used\n```\n```\n",tech:"node",displayNumber:60,sortKey:1809},{id:"node-1810",number:61,title:"How do you handle stateful connections in serverless?",question:"How do you handle stateful connections in serverless?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Reuse connections across invocations by declaring them outside the handler, use connection proxies (RDS Proxy) to avoid exhausting DB connections from many concurrent instances, and prefer HTTP/serverless-friendly data stores.\n\n\n```js\nconst db = connect() // module scope: reused on warm invocations\nexport const handler = async () => db.query(...)\n```\n```\n",tech:"node",displayNumber:61,sortKey:1810},{id:"node-1811",number:62,title:"What is the twelve-factor app methodology?",question:"What is the twelve-factor app methodology?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"A set of practices for scalable, portable services: config in env, stateless processes, backing services as attached resources, logs to stdout, dev/prod parity, explicit dependencies, and disposability.\n\n\n```js\n// config via env, stateless, logs to stdout -> 12-factor\n```\n```\n",tech:"node",displayNumber:62,sortKey:1811},{id:"node-1812",number:63,title:"How do you keep processes stateless for scaling?",question:"How do you keep processes stateless for scaling?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Store session/state in external stores (Redis/DB), not process memory, so any instance can serve any request and instances can be added/removed freely without sticky sessions.\n\n\n```js\napp.use(session({ store: new RedisStore({ client }) }))\n```\n```\n",tech:"node",displayNumber:63,sortKey:1812},{id:"node-1813",number:64,title:"How do you run smoke tests after deployment?",question:"How do you run smoke tests after deployment?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"After deploy, hit critical endpoints (health, key user flows) against the live environment to confirm it works before shifting full traffic; roll back automatically if smoke tests fail.\n\n\n```js\nawait request('https://staging').get('/health').expect(200)\n```\n```\n",tech:"node",displayNumber:64,sortKey:1813},{id:"node-1814",number:65,title:"How do you profile production safely?",question:"How do you profile production safely?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use low-overhead continuous profilers (Pyroscope, cloud profilers) or sampled `--inspect` on one instance, capture on-demand heap/CPU snapshots, and prefer metrics/traces to avoid impacting all traffic.\n\n\n```js\nprocess.on('SIGUSR2', () => require('v8').writeHeapSnapshot())\n```\n```\n",tech:"node",displayNumber:65,sortKey:1814},{id:"node-1815",number:66,title:"What is the difference between `dependencies` size and runtime performance?",question:"What is the difference between `dependencies` size and runtime performance?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"More/larger dependencies slow installs, cold starts, and increase attack surface, but don't necessarily slow steady-state runtime. Trim unused deps for faster deploys and security, independent of hot-path speed.\n\n\n```js\nnpx depcheck # remove unused deps\n```\n```\n",tech:"node",displayNumber:66,sortKey:1815},{id:"node-1816",number:67,title:"How do you set up alerting?",question:"How do you set up alerting?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Alert on symptoms users feel (error rate, latency SLO breaches, saturation) rather than every metric, with thresholds and routing to on-call. Avoid noisy alerts that cause fatigue.\n\n\n```js\n// alert if p99 latency > 500ms for 5m OR error rate > 1%\n```\n```\n",tech:"node",displayNumber:67,sortKey:1816},{id:"node-1817",number:68,title:"What is an SLO/SLA/SLI?",question:"What is an SLO/SLA/SLI?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"An SLI is a measured indicator (e.g. success rate). An SLO is the internal target (e.g. 99.9%). An SLA is the external contractual promise with penalties. Error budgets derive from SLOs to balance reliability vs velocity.\n\n\n```js\n// SLI: successful requests / total; SLO: 99.9% monthly\n```\n```\n",tech:"node",displayNumber:68,sortKey:1817},{id:"node-1818",number:69,title:"How do you roll back a bad deployment?",question:"How do you roll back a bad deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Redeploy the previous known-good image/version (kept immutable and tagged), verify with smoke tests, and ensure DB migrations were backward-compatible so rollback doesn't corrupt data.\n\n\n```js\nkubectl rollout undo deployment/app\n```\n```\n",tech:"node",displayNumber:69,sortKey:1818},{id:"node-1819",number:70,title:"How do you ensure database changes are rollback-safe?",question:"How do you ensure database changes are rollback-safe?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use expand-contract: add new columns/tables (expand), deploy code using both old and new, backfill, then remove old (contract) only after the new version is stable. Never drop/rename in the same release as the code change.\n\n\n```js\n// release1: add column; release2: use it; release3: drop old column\n```\n```\n",tech:"node",displayNumber:70,sortKey:1819},{id:"sql-1820",number:1,title:"Select all columns from a table",question:"Select all columns from a table",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`SELECT *` returns every column. In production prefer listing columns explicitly to avoid fetching unneeeded data and to stay stable if the schema changes.\n\n\n```sql\nSELECT * FROM employees;\n```\n```\n",tech:"sql",displayNumber:1,sortKey:1820},{id:"sql-1821",number:2,title:"Select specific columns",question:"Select specific columns",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"List the column names after `SELECT`. This reduces I/O and makes intent clear.\n\n\n```sql\nSELECT id, name, salary FROM employees;\n```\n```\n",tech:"sql",displayNumber:2,sortKey:1821},{id:"sql-1822",number:3,title:"Filter rows with WHERE",question:"Filter rows with WHERE",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`WHERE` restricts rows to those matching a condition, evaluated before grouping and selection.\n\n\n```sql\nSELECT name, salary FROM employees WHERE salary > 50000;\n```\n```\n",tech:"sql",displayNumber:3,sortKey:1822},{id:"sql-1823",number:4,title:"Sort results with ORDER BY",question:"Sort results with ORDER BY",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`ORDER BY column [ASC|DESC]` sorts the result. Default is ascending; use `DESC` for descending and list multiple columns for tie-breaking.\n\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC, name ASC;\n```\n```\n",tech:"sql",displayNumber:4,sortKey:1823},{id:"sql-1824",number:5,title:"Return only unique values with DISTINCT",question:"Return only unique values with DISTINCT",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`DISTINCT` removes duplicate rows from the result set based on the selected columns.\n\n\n```sql\nSELECT DISTINCT department FROM employees;\n```\n```\n",tech:"sql",displayNumber:5,sortKey:1824},{id:"sql-1825",number:6,title:"Limit the number of rows returned",question:"Limit the number of rows returned",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Use `LIMIT n` (MySQL/Postgres) or `FETCH FIRST n ROWS ONLY` / `TOP n` (SQL Server) to cap rows, typically after `ORDER BY`.\n\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC LIMIT 5;\n```\n```\n",tech:"sql",displayNumber:6,sortKey:1825},{id:"sql-1826",number:7,title:"Skip rows with OFFSET (pagination)",question:"Skip rows with OFFSET (pagination)",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`OFFSET n` skips the first n rows; combined with `LIMIT` it implements page-based pagination. Large offsets are slow because the DB still scans skipped rows.\n\n\n```sql\nSELECT name FROM employees ORDER BY id LIMIT 10 OFFSET 20; -- page 3, size 10\n```\n```\n",tech:"sql",displayNumber:7,sortKey:1826},{id:"sql-1827",number:8,title:"Filter with multiple conditions (AND / OR)",question:"Filter with multiple conditions (AND / OR)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Combine conditions with `AND`/`OR`; use parentheses to control precedence since `AND` binds tighter than `OR`.\n\n\n```sql\nSELECT * FROM employees\nWHERE department = 'Sales' AND (salary > 60000 OR manager_id IS NULL);\n```\n```\n",tech:"sql",displayNumber:8,sortKey:1827},{id:"sql-1828",number:9,title:"Filter a range with BETWEEN",question:"Filter a range with BETWEEN",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`BETWEEN a AND b` is inclusive of both bounds. It's shorthand for `col >= a AND col <= b`.\n\n\n```sql\nSELECT name, salary FROM employees WHERE salary BETWEEN 40000 AND 60000;\n```\n```\n",tech:"sql",displayNumber:9,sortKey:1828},{id:"sql-1829",number:10,title:"Filter a list with IN",question:"Filter a list with IN",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`IN (...)` matches any value in the list, a cleaner alternative to multiple `OR`s.\n\n\n```sql\nSELECT * FROM employees WHERE department IN ('Sales', 'HR', 'Finance');\n```\n```\n",tech:"sql",displayNumber:10,sortKey:1829},{id:"sql-1830",number:11,title:"Pattern matching with LIKE",question:"Pattern matching with LIKE",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`LIKE` matches patterns: `%` = any sequence of characters, `_` = a single character. Use `ILIKE` (Postgres) for case-insensitive matching.\n\n\n```sql\nSELECT name FROM employees WHERE name LIKE 'A%';   -- starts with A\nSELECT name FROM employees WHERE name LIKE '_a%';  -- 'a' as 2nd char\n```\n```\n",tech:"sql",displayNumber:11,sortKey:1830},{id:"sql-1831",number:12,title:"Handle NULL values (IS NULL / IS NOT NULL)",question:"Handle NULL values (IS NULL / IS NOT NULL)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:'NULL means "unknown"; comparisons with `=` don\'t work. Use `IS NULL` / `IS NOT NULL`. `NULL = NULL` is not true.\n\n\n```sql\nSELECT name FROM employees WHERE manager_id IS NULL;\n```\n```\n',tech:"sql",displayNumber:12,sortKey:1831},{id:"sql-1832",number:13,title:"Replace NULLs with a default (COALESCE)",question:"Replace NULLs with a default (COALESCE)",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`COALESCE(a, b, ...)` returns the first non-NULL argument, useful for substituting defaults.\n\n\n```sql\nSELECT name, COALESCE(commission, 0) AS commission FROM employees;\n```\n```\n",tech:"sql",displayNumber:13,sortKey:1832},{id:"sql-1833",number:14,title:"Alias columns and tables (AS)",question:"Alias columns and tables (AS)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`AS` renames columns/tables in the output or for shorter references. The keyword is optional for table aliases.\n\n\n```sql\nSELECT e.name AS employee_name, e.salary * 12 AS annual_salary\nFROM employees e;\n```\n```\n",tech:"sql",displayNumber:14,sortKey:1833},{id:"sql-1834",number:15,title:"Concatenate strings",question:"Concatenate strings",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Use `||` (standard/Postgres/Oracle) or `CONCAT(...)` (MySQL/SQL Server) to join strings.\n\n\n```sql\nSELECT CONCAT(first_name, ' ', last_name) AS full_name FROM employees;\n```\n```\n",tech:"sql",displayNumber:15,sortKey:1834},{id:"sql-1835",number:16,title:"Compute values in SELECT (arithmetic)",question:"Compute values in SELECT (arithmetic)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"You can include expressions in `SELECT`; they're evaluated per row.\n\n\n```sql\nSELECT name, salary, salary * 0.10 AS bonus FROM employees;\n```\n```\n",tech:"sql",displayNumber:16,sortKey:1835},{id:"sql-1836",number:17,title:"Conditional logic with CASE",question:"Conditional logic with CASE",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`CASE WHEN ... THEN ... ELSE ... END` returns different values per row based on conditions — SQL's if/else.\n\n\n```sql\nSELECT name,\n  CASE WHEN salary >= 80000 THEN 'High'\n       WHEN salary >= 50000 THEN 'Medium'\n       ELSE 'Low' END AS band\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:17,sortKey:1836},{id:"sql-1837",number:18,title:"Convert case and trim strings",question:"Convert case and trim strings",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`UPPER`/`LOWER` change case; `TRIM`/`LTRIM`/`RTRIM` remove whitespace. Handy for normalizing text before comparison.\n\n\n```sql\nSELECT UPPER(name) AS upper_name, TRIM(department) AS dept FROM employees;\n```\n```\n",tech:"sql",displayNumber:18,sortKey:1837},{id:"sql-1838",number:19,title:"Extract a substring",question:"Extract a substring",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`SUBSTRING(str FROM start FOR length)` (or `SUBSTR`) extracts part of a string; positions are usually 1-based.\n\n\n```sql\nSELECT SUBSTRING(name FROM 1 FOR 3) AS prefix FROM employees;\n```\n```\n",tech:"sql",displayNumber:19,sortKey:1838},{id:"sql-1839",number:20,title:"Get the length of a string",question:"Get the length of a string",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`LENGTH(str)` (or `LEN` in SQL Server) returns the number of characters.\n\n\n```sql\nSELECT name, LENGTH(name) AS name_len FROM employees;\n```\n```\n",tech:"sql",displayNumber:20,sortKey:1839},{id:"sql-1840",number:21,title:"Round and format numbers",question:"Round and format numbers",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`ROUND(value, decimals)` rounds; `CEIL`/`FLOOR` go up/down. Use for currency and display formatting.\n\n\n```sql\nSELECT name, ROUND(salary / 12.0, 2) AS monthly FROM employees;\n```\n```\n",tech:"sql",displayNumber:21,sortKey:1840},{id:"sql-1841",number:22,title:"Get the current date/time",question:"Get the current date/time",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`CURRENT_DATE`, `CURRENT_TIMESTAMP` (or `NOW()`) return the current date/time from the database server.\n\n\n```sql\nSELECT name, hire_date, CURRENT_DATE - hire_date AS days_employed FROM employees;\n```\n```\n",tech:"sql",displayNumber:22,sortKey:1841},{id:"sql-1842",number:23,title:"Extract parts of a date",question:"Extract parts of a date",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`EXTRACT(YEAR FROM date)` (or `YEAR()`/`MONTH()`) pulls date components, useful for grouping by year/month.\n\n\n```sql\nSELECT name, EXTRACT(YEAR FROM hire_date) AS hire_year FROM employees;\n```\n```\n",tech:"sql",displayNumber:23,sortKey:1842},{id:"sql-1843",number:24,title:"Filter by date range",question:"Filter by date range",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Compare date columns with literals or use `BETWEEN`. Prefer half-open ranges (`>= start AND < next_day`) to correctly include timestamps.\n\n\n```sql\nSELECT * FROM orders WHERE order_date >= '2024-01-01' AND order_date < '2024-02-01';\n```\n```\n",tech:"sql",displayNumber:24,sortKey:1843},{id:"sql-1844",number:25,title:"Count rows",question:"Count rows",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`COUNT(*)` counts all rows; `COUNT(col)` counts non-NULL values of a column; `COUNT(DISTINCT col)` counts unique non-NULL values.\n\n\n```sql\nSELECT COUNT(*) AS total, COUNT(DISTINCT department) AS depts FROM employees;\n```\n```\n",tech:"sql",displayNumber:25,sortKey:1844},{id:"sql-1845",number:26,title:"Order NULLs first or last",question:"Order NULLs first or last",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`ORDER BY col NULLS LAST` (Postgres/Oracle) controls NULL placement. In MySQL, sort on `col IS NULL` first to emulate it.\n\n\n```sql\nSELECT name, commission FROM employees ORDER BY commission DESC NULLS LAST;\n```\n```\n",tech:"sql",displayNumber:26,sortKey:1845},{id:"sql-1846",number:27,title:"Filter with NOT",question:"Filter with NOT",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`NOT` negates a condition; combine with `IN`, `LIKE`, `BETWEEN`. Beware `NOT IN` with NULLs, which can return no rows unexpectedly.\n\n\n```sql\nSELECT * FROM employees WHERE department NOT IN ('HR', 'Legal');\n```\n```\n",tech:"sql",displayNumber:27,sortKey:1846},{id:"sql-1847",number:28,title:"Use column aliases in ORDER BY",question:"Use column aliases in ORDER BY",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`ORDER BY` can reference a `SELECT` alias (it's evaluated after projection), unlike `WHERE`, which cannot use aliases.\n\n\n```sql\nSELECT name, salary * 12 AS annual FROM employees ORDER BY annual DESC;\n```\n```\n",tech:"sql",displayNumber:28,sortKey:1847},{id:"sql-1848",number:29,title:"Return a constant/computed column",question:"Return a constant/computed column",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"You can select literals or expressions without a table reference for testing, or add a constant tag column to results.\n\n\n```sql\nSELECT name, 'active' AS status FROM employees;\n```\n```\n",tech:"sql",displayNumber:29,sortKey:1848},{id:"sql-1849",number:30,title:"Filter with comparison operators",question:"Filter with comparison operators",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Standard operators: `=`, `<>`/`!=`, `<`, `>`, `<=`, `>=`. Use them in `WHERE`/`HAVING`.\n\n\n```sql\nSELECT name FROM employees WHERE salary <> 50000;\n```\n```\n",tech:"sql",displayNumber:30,sortKey:1849},{id:"sql-1850",number:31,title:"Combine result sets with UNION",question:"Combine result sets with UNION",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`UNION` stacks two result sets (same columns/types) and removes duplicates; `UNION ALL` keeps duplicates and is faster.\n\n\n```sql\nSELECT name FROM employees\nUNION\nSELECT name FROM contractors;\n```\n```\n",tech:"sql",displayNumber:31,sortKey:1850},{id:"sql-1851",number:32,title:"Difference between UNION and UNION ALL",question:"Difference between UNION and UNION ALL",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`UNION` deduplicates (extra sort/hash cost); `UNION ALL` returns everything including duplicates and is more performant when you know rows are distinct or duplicates are acceptable.\n\n\n```sql\nSELECT city FROM customers\nUNION ALL\nSELECT city FROM suppliers; -- keeps duplicates\n```\n```\n",tech:"sql",displayNumber:32,sortKey:1851},{id:"sql-1852",number:33,title:"INTERSECT and EXCEPT",question:"INTERSECT and EXCEPT",difficulty:"Advanced",category:"SQL Basics & SELECT",answer:"`INTERSECT` returns rows common to both queries; `EXCEPT` (`MINUS` in Oracle) returns rows in the first not in the second. Both deduplicate.\n\n\n```sql\nSELECT id FROM active_users\nEXCEPT\nSELECT id FROM banned_users;\n```\n```\n",tech:"sql",displayNumber:33,sortKey:1852},{id:"sql-1853",number:34,title:"Cast/convert data types",question:"Cast/convert data types",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`CAST(expr AS type)` (or `::type` in Postgres) converts between types, e.g. text to number or date.\n\n\n```sql\nSELECT CAST(salary AS VARCHAR) AS salary_text, order_date::DATE FROM orders;\n```\n```\n",tech:"sql",displayNumber:34,sortKey:1853},{id:"sql-1854",number:35,title:"Filter using a computed condition",question:"Filter using a computed condition",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"You can put expressions in `WHERE`, but applying functions to a column can prevent index use. Prefer sargable predicates (compare the raw column to a computed constant).\n\n\n```sql\n-- non-sargable: WHERE YEAR(order_date) = 2024\nSELECT * FROM orders WHERE order_date >= '2024-01-01' AND order_date < '2025-01-01';\n```\n```\n",tech:"sql",displayNumber:35,sortKey:1854},{id:"sql-1855",number:36,title:"Return top N per simple sort",question:"Return top N per simple sort",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"Combine `ORDER BY` with `LIMIT`/`FETCH FIRST`/`TOP`. Ensure a deterministic order (add a tie-breaker) for stable results.\n\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC, id LIMIT 3;\n```\n```\n",tech:"sql",displayNumber:36,sortKey:1855},{id:"sql-1856",number:37,title:"Use aliases with expressions and functions",question:"Use aliases with expressions and functions",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Give computed columns clear aliases so the result set has meaningful names for downstream code.\n\n\n```sql\nSELECT name, ROUND(salary * 1.1, 2) AS raised_salary FROM employees;\n```\n```\n",tech:"sql",displayNumber:37,sortKey:1856},{id:"sql-1857",number:38,title:"Filter rows by string length",question:"Filter rows by string length",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"Use `LENGTH()` in the `WHERE` clause. Note applying a function to the column may bypass indexes.\n\n\n```sql\nSELECT name FROM employees WHERE LENGTH(name) > 10;\n```\n```\n",tech:"sql",displayNumber:38,sortKey:1857},{id:"sql-1858",number:39,title:"Replace substring values",question:"Replace substring values",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`REPLACE(str, from, to)` substitutes all occurrences of a substring — useful for cleaning/formatting text.\n\n\n```sql\nSELECT REPLACE(phone, '-', '') AS digits FROM customers;\n```\n```\n",tech:"sql",displayNumber:39,sortKey:1858},{id:"sql-1859",number:40,title:"Order of SQL clause evaluation",question:"Order of SQL clause evaluation",difficulty:"Advanced",category:"SQL Basics & SELECT",answer:"Logical order: `FROM` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `DISTINCT` → `ORDER BY` → `LIMIT`. This is why `WHERE` can't use `SELECT` aliases but `ORDER BY` can.\n\n\n```sql\n-- WHERE runs before SELECT, so it can't reference the alias 'annual'\nSELECT salary * 12 AS annual FROM employees WHERE salary > 40000 ORDER BY annual;\n```\n```\n",tech:"sql",displayNumber:40,sortKey:1859},{id:"sql-1860",number:1,title:"What is a JOIN?",question:"What is a JOIN?",difficulty:"Basic",category:"Joins",answer:"A JOIN combines rows from two or more tables based on a related column, letting you query normalized data as a single result set.\n\n\n```sql\nSELECT e.name, d.name AS dept\nFROM employees e\nJOIN departments d ON e.department_id = d.id;\n```\n```\n",tech:"sql",displayNumber:1,sortKey:1860},{id:"sql-1861",number:2,title:"What is an INNER JOIN?",question:"What is an INNER JOIN?",difficulty:"Basic",category:"Joins",answer:"An INNER JOIN returns only rows that have matching values in both tables. Rows without a match on either side are excluded.\n\n\n```sql\nSELECT o.id, c.name\nFROM orders o\nINNER JOIN customers c ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:2,sortKey:1861},{id:"sql-1862",number:3,title:"What is a LEFT JOIN?",question:"What is a LEFT JOIN?",difficulty:"Basic",category:"Joins",answer:"A LEFT (OUTER) JOIN returns all rows from the left table plus matching rows from the right; unmatched right columns are NULL.\n\n\n```sql\nSELECT c.name, o.id\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:3,sortKey:1862},{id:"sql-1863",number:4,title:"What is a RIGHT JOIN?",question:"What is a RIGHT JOIN?",difficulty:"Basic",category:"Joins",answer:"A RIGHT JOIN returns all rows from the right table plus matches from the left; unmatched left columns are NULL. It's a mirror of LEFT JOIN.\n\n\n```sql\nSELECT e.name, d.name AS dept\nFROM employees e\nRIGHT JOIN departments d ON e.department_id = d.id;\n```\n```\n",tech:"sql",displayNumber:4,sortKey:1863},{id:"sql-1864",number:5,title:"What is a FULL OUTER JOIN?",question:"What is a FULL OUTER JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A FULL OUTER JOIN returns all rows from both tables, matching where possible and filling NULLs where there's no match on either side.\n\n\n```sql\nSELECT c.name, o.id\nFROM customers c\nFULL OUTER JOIN orders o ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:5,sortKey:1864},{id:"sql-1865",number:6,title:"What is a CROSS JOIN?",question:"What is a CROSS JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A CROSS JOIN produces the Cartesian product — every row of the first table paired with every row of the second. Useful for generating combinations; dangerous if unintentional.\n\n\n```sql\nSELECT s.size, c.color FROM sizes s CROSS JOIN colors c;\n```\n```\n",tech:"sql",displayNumber:6,sortKey:1865},{id:"sql-1866",number:7,title:"What is a SELF JOIN?",question:"What is a SELF JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A self join joins a table to itself using aliases, useful for hierarchical/related-row queries like employees and their managers.\n\n\n```sql\nSELECT e.name AS employee, m.name AS manager\nFROM employees e\nLEFT JOIN employees m ON e.manager_id = m.id;\n```\n```\n",tech:"sql",displayNumber:7,sortKey:1866},{id:"sql-1867",number:8,title:"Difference between INNER JOIN and LEFT JOIN",question:"Difference between INNER JOIN and LEFT JOIN",difficulty:"Basic",category:"Joins",answer:"INNER JOIN returns only matched rows; LEFT JOIN returns all left rows including unmatched ones (with NULLs). Use LEFT JOIN when you must keep rows that may lack a related record.\n\n\n```sql\n-- customers with no orders appear only with LEFT JOIN\nSELECT c.name, o.id FROM customers c LEFT JOIN orders o ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:8,sortKey:1867},{id:"sql-1868",number:9,title:"Find rows in one table with no match in another",question:"Find rows in one table with no match in another",difficulty:"Intermediate",category:"Joins",answer:"Use a LEFT JOIN and filter where the right key `IS NULL` (anti-join), returning only unmatched left rows.\n\n\n```sql\nSELECT c.name\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nWHERE o.id IS NULL; -- customers who never ordered\n```\n```\n",tech:"sql",displayNumber:9,sortKey:1868},{id:"sql-1869",number:10,title:"Join three or more tables",question:"Join three or more tables",difficulty:"Intermediate",category:"Joins",answer:`Chain JOIN clauses; each adds a table with its own ON condition. Order doesn't change results for inner joins but can affect readability and the optimizer's plan.


\`\`\`sql
SELECT o.id, c.name, p.title
FROM orders o
JOIN customers c ON o.customer_id = c.id
JOIN products p ON o.product_id = p.id;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:10,sortKey:1869},{id:"sql-1870",number:11,title:"Join on multiple columns",question:"Join on multiple columns",difficulty:"Intermediate",category:"Joins",answer:"Combine conditions in `ON` with `AND` when the relationship spans multiple columns (composite keys).\n\n\n```sql\nSELECT *\nFROM sales s\nJOIN targets t ON s.region = t.region AND s.year = t.year;\n```\n```\n",tech:"sql",displayNumber:11,sortKey:1870},{id:"sql-1871",number:12,title:"Difference between ON and WHERE in a join",question:"Difference between ON and WHERE in a join",difficulty:"Advanced",category:"Joins",answer:"For INNER JOIN they're equivalent. For OUTER JOINs, a condition in `ON` filters the match (preserving unmatched rows), while the same condition in `WHERE` runs after the join and can turn a LEFT JOIN into an effective INNER JOIN by removing NULL rows.\n\n\n```sql\n-- keeps customers with no 2024 orders:\nSELECT c.name, o.id FROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id AND o.order_date >= '2024-01-01';\n```\n```\n",tech:"sql",displayNumber:12,sortKey:1871},{id:"sql-1872",number:13,title:"Join a table to itself for hierarchy",question:"Join a table to itself for hierarchy",difficulty:"Intermediate",category:"Joins",answer:"Self-join on the parent-key column to relate a row to its parent, e.g. employee → manager or category → parent category.\n\n\n```sql\nSELECT child.name AS category, parent.name AS parent_category\nFROM categories child\nLEFT JOIN categories parent ON child.parent_id = parent.id;\n```\n```\n",tech:"sql",displayNumber:13,sortKey:1872},{id:"sql-1873",number:14,title:"Count related rows per parent",question:"Count related rows per parent",difficulty:"Intermediate",category:"Joins",answer:"LEFT JOIN then `GROUP BY` the parent and `COUNT` the child key (not `*`) so parents with zero children count as 0.\n\n\n```sql\nSELECT c.name, COUNT(o.id) AS order_count\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```\n```\n",tech:"sql",displayNumber:14,sortKey:1873},{id:"sql-1874",number:15,title:"Why COUNT(child.id) instead of COUNT(*) in a LEFT JOIN?",question:"Why COUNT(child.id) instead of COUNT(*) in a LEFT JOIN?",difficulty:"Advanced",category:"Joins",answer:"`COUNT(*)` counts the joined rows, so an unmatched parent (one row with NULLs) counts as 1. `COUNT(child.id)` counts non-NULL child keys, correctly yielding 0 for parents with no children.\n\n\n```sql\nSELECT c.name, COUNT(o.id) AS orders -- 0 for customers with none\nFROM customers c LEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```\n```\n",tech:"sql",displayNumber:15,sortKey:1874},{id:"sql-1875",number:16,title:"Join and aggregate (sum per group)",question:"Join and aggregate (sum per group)",difficulty:"Intermediate",category:"Joins",answer:"Join related tables, then `GROUP BY` a dimension and aggregate a measure, e.g. total order amount per customer.\n\n\n```sql\nSELECT c.name, SUM(o.amount) AS total_spent\nFROM customers c\nJOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```\n```\n",tech:"sql",displayNumber:16,sortKey:1875},{id:"sql-1876",number:17,title:"Avoid duplicate rows from one-to-many joins",question:"Avoid duplicate rows from one-to-many joins",difficulty:"Advanced",category:"Joins",answer:"Joining a one-to-many relationship multiplies parent rows. Deduplicate with aggregation (`GROUP BY`), `DISTINCT`, or by pre-aggregating the child table in a subquery before joining.\n\n\n```sql\nSELECT c.name, sub.total\nFROM customers c\nJOIN (SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id) sub\n  ON sub.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:17,sortKey:1876},{id:"sql-1877",number:18,title:"Join with a filtered (derived) table",question:"Join with a filtered (derived) table",difficulty:"Advanced",category:"Joins",answer:`Join to a subquery (derived table) that pre-filters/aggregates data, reducing rows and clarifying intent.


\`\`\`sql
SELECT e.name, big.total
FROM employees e
JOIN (SELECT employee_id, SUM(amount) total FROM sales GROUP BY employee_id HAVING SUM(amount) > 100000) big
  ON big.employee_id = e.id;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:18,sortKey:1877},{id:"sql-1878",number:19,title:"Semi-join with EXISTS vs JOIN",question:"Semi-join with EXISTS vs JOIN",difficulty:"Advanced",category:"Joins",answer:"To check existence of related rows without duplicating parents, use `EXISTS` (a semi-join) instead of a JOIN. It stops at the first match and doesn't multiply rows.\n\n\n```sql\nSELECT c.name FROM customers c\nWHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```\n```\n",tech:"sql",displayNumber:19,sortKey:1878},{id:"sql-1879",number:20,title:"Anti-join with NOT EXISTS",question:"Anti-join with NOT EXISTS",difficulty:"Advanced",category:"Joins",answer:"`NOT EXISTS` returns parents with no matching child rows and, unlike `NOT IN`, behaves correctly when the subquery can produce NULLs.\n\n\n```sql\nSELECT c.name FROM customers c\nWHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```\n```\n",tech:"sql",displayNumber:20,sortKey:1879},{id:"sql-1880",number:21,title:"Difference between NOT IN and NOT EXISTS with NULLs",question:"Difference between NOT IN and NOT EXISTS with NULLs",difficulty:"Experienced",category:"Joins",answer:"If the `NOT IN` subquery returns any NULL, the whole predicate becomes UNKNOWN and no rows are returned. `NOT EXISTS` is NULL-safe and generally preferred for anti-joins.\n\n\n```sql\n-- risky if orders.customer_id has NULLs:\nSELECT * FROM customers WHERE id NOT IN (SELECT customer_id FROM orders);\n```\n```\n",tech:"sql",displayNumber:21,sortKey:1880},{id:"sql-1881",number:22,title:"Join using USING clause",question:"Join using USING clause",difficulty:"Intermediate",category:"Joins",answer:"`USING(col)` is shorthand for `ON a.col = b.col` when the join columns share a name, and it collapses them into a single output column.\n\n\n```sql\nSELECT * FROM orders JOIN customers USING (customer_id);\n```\n```\n",tech:"sql",displayNumber:22,sortKey:1881},{id:"sql-1882",number:23,title:"Natural join and why to avoid it",question:"Natural join and why to avoid it",difficulty:"Advanced",category:"Joins",answer:"`NATURAL JOIN` auto-joins on all same-named columns. It's risky because schema changes can silently alter the join condition; prefer explicit `ON`/`USING`.\n\n\n```sql\nSELECT * FROM orders NATURAL JOIN customers; -- fragile, avoid in production\n```\n```\n",tech:"sql",displayNumber:23,sortKey:1882},{id:"sql-1883",number:24,title:"Join to find matching pairs (e.g. same city)",question:"Join to find matching pairs (e.g. same city)",difficulty:"Advanced",category:"Joins",answer:"Self-join on the shared attribute with an inequality on ids to avoid pairing a row with itself and to avoid mirror duplicates.\n\n\n```sql\nSELECT a.name, b.name, a.city\nFROM customers a\nJOIN customers b ON a.city = b.city AND a.id < b.id;\n```\n```\n",tech:"sql",displayNumber:24,sortKey:1883},{id:"sql-1884",number:25,title:"Left join with default values",question:"Left join with default values",difficulty:"Intermediate",category:"Joins",answer:"Wrap nullable right-side columns in `COALESCE` to supply defaults for unmatched rows.\n\n\n```sql\nSELECT c.name, COALESCE(SUM(o.amount), 0) AS total\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```\n```\n",tech:"sql",displayNumber:25,sortKey:1884},{id:"sql-1885",number:26,title:"Join and filter on the right table correctly",question:"Join and filter on the right table correctly",difficulty:"Advanced",category:"Joins",answer:"To keep left rows while filtering the right table, put the right-table condition in `ON`, not `WHERE`; a `WHERE` filter on the right side removes unmatched (NULL) rows.\n\n\n```sql\nSELECT c.name, o.id FROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id AND o.status = 'paid';\n```\n```\n",tech:"sql",displayNumber:26,sortKey:1885},{id:"sql-1886",number:27,title:"Join two tables and pick the latest related row",question:"Join two tables and pick the latest related row",difficulty:"Experienced",category:"Joins",answer:`Use a lateral/correlated subquery or window function to fetch only the most recent child per parent, avoiding fan-out from all children.


\`\`\`sql
SELECT c.name, last_o.amount
FROM customers c
JOIN LATERAL (
  SELECT amount FROM orders o WHERE o.customer_id = c.id
  ORDER BY o.order_date DESC LIMIT 1
) last_o ON true;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:27,sortKey:1886},{id:"sql-1887",number:28,title:"Join with aggregation and HAVING",question:"Join with aggregation and HAVING",difficulty:"Advanced",category:"Joins",answer:"Filter aggregated join results with `HAVING`, e.g. customers whose total spend exceeds a threshold.\n\n\n```sql\nSELECT c.name, SUM(o.amount) AS total\nFROM customers c JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name\nHAVING SUM(o.amount) > 10000;\n```\n```\n",tech:"sql",displayNumber:28,sortKey:1887},{id:"sql-1888",number:29,title:"Multiple LEFT JOINs",question:"Multiple LEFT JOINs",difficulty:"Advanced",category:"Joins",answer:`Chaining multiple LEFT JOINs keeps all base rows while attaching optional related data from several tables. Watch for row multiplication when several joined tables are one-to-many.


\`\`\`sql
SELECT e.name, d.name AS dept, p.name AS project
FROM employees e
LEFT JOIN departments d ON e.department_id = d.id
LEFT JOIN projects p ON p.lead_id = e.id;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:29,sortKey:1888},{id:"sql-1889",number:30,title:"Emulate FULL OUTER JOIN in MySQL",question:"Emulate FULL OUTER JOIN in MySQL",difficulty:"Experienced",category:"Joins",answer:"MySQL lacks FULL OUTER JOIN; emulate it by `UNION`-ing a LEFT JOIN with a RIGHT JOIN (or a LEFT JOIN of the swapped tables with an anti-join filter).\n\n\n```sql\nSELECT c.name, o.id FROM customers c LEFT JOIN orders o ON o.customer_id = c.id\nUNION\nSELECT c.name, o.id FROM customers c RIGHT JOIN orders o ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:30,sortKey:1889},{id:"sql-1890",number:31,title:"Join performance: indexing join keys",question:"Join performance: indexing join keys",difficulty:"Experienced",category:"Joins",answer:"Index the columns used in `ON` (usually the foreign key on the child side; primary keys are already indexed). Without indexes the optimizer may resort to slow nested-loop or full scans.\n\n\n```sql\nCREATE INDEX idx_orders_customer ON orders(customer_id);\n```\n```\n",tech:"sql",displayNumber:31,sortKey:1890},{id:"sql-1891",number:32,title:"Nested loop vs hash vs merge join",question:"Nested loop vs hash vs merge join",difficulty:"Experienced",category:"Joins",answer:"Nested loop is best for small/indexed inputs; hash join suits large unsorted sets joined on equality; merge join is efficient for large pre-sorted/indexed inputs. The optimizer chooses based on statistics; `EXPLAIN` reveals which it picks.\n\n\n```sql\nEXPLAIN SELECT * FROM orders o JOIN customers c ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:32,sortKey:1891},{id:"sql-1892",number:33,title:"Join with a range condition",question:"Join with a range condition",difficulty:"Advanced",category:"Joins",answer:"Joins can use inequalities (non-equi joins), e.g. matching a value to a band/tier table where it falls between bounds.\n\n\n```sql\nSELECT e.name, t.grade\nFROM employees e\nJOIN salary_tiers t ON e.salary BETWEEN t.min_salary AND t.max_salary;\n```\n```\n",tech:"sql",displayNumber:33,sortKey:1892},{id:"sql-1893",number:34,title:"Deduplicate a join result",question:"Deduplicate a join result",difficulty:"Intermediate",category:"Joins",answer:"Use `DISTINCT` or aggregate to collapse duplicate rows produced by one-to-many joins when you only need unique parent rows.\n\n\n```sql\nSELECT DISTINCT c.id, c.name\nFROM customers c JOIN orders o ON o.customer_id = c.id;\n```\n```\n",tech:"sql",displayNumber:34,sortKey:1893},{id:"sql-1894",number:35,title:"Join to a numbers/calendar table",question:"Join to a numbers/calendar table",difficulty:"Experienced",category:"Joins",answer:`Join to a generated calendar/numbers table to include periods (e.g. days/months) that have no data, producing gap-free reports with zeros.


\`\`\`sql
SELECT d.day, COALESCE(COUNT(o.id), 0) AS orders
FROM calendar d
LEFT JOIN orders o ON o.order_date = d.day
GROUP BY d.day ORDER BY d.day;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:35,sortKey:1894},{id:"sql-1895",number:36,title:"Combine data with a UNION vs JOIN — which to use?",question:"Combine data with a UNION vs JOIN — which to use?",difficulty:"Intermediate",category:"Joins",answer:"JOIN combines columns from related tables (widening rows); UNION stacks rows from similar-shaped queries (lengthening the result). Use JOIN for relationships, UNION for concatenating sets.\n\n\n```sql\n-- JOIN adds columns; UNION adds rows\nSELECT name FROM employees UNION SELECT name FROM contractors;\n```\n```\n",tech:"sql",displayNumber:36,sortKey:1895},{id:"sql-1896",number:37,title:"Find common rows between two tables",question:"Find common rows between two tables",difficulty:"Intermediate",category:"Joins",answer:"An INNER JOIN on the key (or `INTERSECT`) returns rows present in both tables.\n\n\n```sql\nSELECT a.id FROM table_a a JOIN table_b b ON a.id = b.id;\n```\n```\n",tech:"sql",displayNumber:37,sortKey:1896},{id:"sql-1897",number:38,title:"Join with conditional aggregation",question:"Join with conditional aggregation",difficulty:"Experienced",category:"Joins",answer:"Combine a join with `SUM(CASE WHEN ...)` to compute multiple conditional measures in one pass (pivot-like).\n\n\n```sql\nSELECT c.name,\n  SUM(CASE WHEN o.status='paid' THEN o.amount ELSE 0 END) AS paid,\n  SUM(CASE WHEN o.status='refunded' THEN o.amount ELSE 0 END) AS refunded\nFROM customers c JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```\n```\n",tech:"sql",displayNumber:38,sortKey:1897},{id:"sql-1898",number:39,title:"Join order and the optimizer",question:"Join order and the optimizer",difficulty:"Experienced",category:"Joins",answer:"For inner joins, the written order doesn't dictate execution — the cost-based optimizer reorders based on statistics and indexes. Keeping statistics fresh and indexing keys matters more than manual ordering.\n\n\n```sql\nANALYZE orders; -- refresh statistics so the optimizer picks a good join order\n```\n```\n",tech:"sql",displayNumber:39,sortKey:1898},{id:"sql-1899",number:40,title:"Join to explode an array/JSON (lateral)",question:"Join to explode an array/JSON (lateral)",difficulty:"Experienced",category:"Joins",answer:"Use a lateral join (`CROSS JOIN LATERAL` / `unnest` / `json_array_elements`) to expand array or JSON values into rows joined back to the parent.\n\n\n```sql\nSELECT p.id, tag\nFROM posts p\nCROSS JOIN LATERAL unnest(p.tags) AS tag;\n```\n```\n",tech:"sql",displayNumber:40,sortKey:1899},{id:"sql-1900",number:1,title:"What are aggregate functions?",question:"What are aggregate functions?",difficulty:"Basic",category:"Aggregation & Grouping",answer:"Aggregate functions compute a single value over a set of rows: `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`. They're used with or without `GROUP BY`.\n\n\n```sql\nSELECT COUNT(*), AVG(salary), MAX(salary) FROM employees;\n```\n```\n",tech:"sql",displayNumber:1,sortKey:1900},{id:"sql-1901",number:2,title:"What is GROUP BY?",question:"What is GROUP BY?",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`GROUP BY` partitions rows into groups by one or more columns, and aggregates are computed per group.\n\n\n```sql\nSELECT department, COUNT(*) FROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:2,sortKey:1901},{id:"sql-1902",number:3,title:"Difference between WHERE and HAVING",question:"Difference between WHERE and HAVING",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`WHERE` filters rows before grouping; `HAVING` filters groups after aggregation. Aggregate conditions must go in `HAVING`.\n\n\n```sql\nSELECT department, AVG(salary) avg_sal\nFROM employees\nWHERE status = 'active'          -- row filter\nGROUP BY department\nHAVING AVG(salary) > 60000;      -- group filter\n```\n```\n",tech:"sql",displayNumber:3,sortKey:1902},{id:"sql-1903",number:4,title:"Sum a column per group",question:"Sum a column per group",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`SUM(col)` totals values within each group defined by `GROUP BY`.\n\n\n```sql\nSELECT department, SUM(salary) AS total_payroll\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:4,sortKey:1903},{id:"sql-1904",number:5,title:"Average with rounding",question:"Average with rounding",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`AVG` ignores NULLs. Wrap in `ROUND` for clean output.\n\n\n```sql\nSELECT department, ROUND(AVG(salary), 2) AS avg_salary\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:5,sortKey:1904},{id:"sql-1905",number:6,title:"Count distinct values per group",question:"Count distinct values per group",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`COUNT(DISTINCT col)` counts unique non-NULL values within each group.\n\n\n```sql\nSELECT department, COUNT(DISTINCT job_title) AS distinct_roles\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:6,sortKey:1905},{id:"sql-1906",number:7,title:"How do aggregates handle NULLs?",question:"How do aggregates handle NULLs?",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Aggregates (except `COUNT(*)`) ignore NULLs. `COUNT(col)` skips NULLs; `AVG`/`SUM` compute over non-NULLs only; `COUNT(*)` counts every row.\n\n\n```sql\n-- AVG divides by count of non-NULL commissions, not total rows\nSELECT AVG(commission) FROM employees;\n```\n```\n",tech:"sql",displayNumber:7,sortKey:1906},{id:"sql-1907",number:8,title:"Group by multiple columns",question:"Group by multiple columns",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"List several columns in `GROUP BY` to form groups per unique combination.\n\n\n```sql\nSELECT department, gender, COUNT(*) \nFROM employees GROUP BY department, gender;\n```\n```\n",tech:"sql",displayNumber:8,sortKey:1907},{id:"sql-1908",number:9,title:"Filter groups with HAVING and COUNT",question:"Filter groups with HAVING and COUNT",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Use `HAVING COUNT(*) > n` to keep only groups meeting a size threshold, e.g. departments with more than 5 employees.\n\n\n```sql\nSELECT department, COUNT(*) AS headcount\nFROM employees GROUP BY department HAVING COUNT(*) > 5;\n```\n```\n",tech:"sql",displayNumber:9,sortKey:1908},{id:"sql-1909",number:10,title:"Find duplicate values",question:"Find duplicate values",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Group by the column(s) and keep groups with `COUNT(*) > 1` to identify duplicates.\n\n\n```sql\nSELECT email, COUNT(*) AS cnt\nFROM users GROUP BY email HAVING COUNT(*) > 1;\n```\n```\n",tech:"sql",displayNumber:10,sortKey:1909},{id:"sql-1910",number:11,title:"Delete duplicate rows keeping one",question:"Delete duplicate rows keeping one",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Use a window function (`ROW_NUMBER` per duplicate group) and delete rows where the row number > 1, or delete rows whose id isn't the min per group.\n\n\n```sql\nDELETE FROM users\nWHERE id NOT IN (SELECT MIN(id) FROM users GROUP BY email);\n```\n```\n",tech:"sql",displayNumber:11,sortKey:1910},{id:"sql-1911",number:12,title:"Can you use aggregate functions in WHERE?",question:"Can you use aggregate functions in WHERE?",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"No. `WHERE` runs before aggregation, so aggregates belong in `HAVING` (or a subquery). Putting `SUM()`/`COUNT()` in `WHERE` is a syntax error.\n\n\n```sql\n-- wrong: WHERE COUNT(*) > 5\nSELECT department FROM employees GROUP BY department HAVING COUNT(*) > 5;\n```\n```\n",tech:"sql",displayNumber:12,sortKey:1911},{id:"sql-1912",number:13,title:"Conditional aggregation (pivot with CASE)",question:"Conditional aggregation (pivot with CASE)",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Use `SUM(CASE WHEN ... THEN 1 ELSE 0 END)` or `COUNT(CASE ...)` to compute multiple conditional counts/sums in one query — a manual pivot.\n\n\n```sql\nSELECT department,\n  SUM(CASE WHEN gender='Male' THEN 1 ELSE 0 END) AS males,\n  SUM(CASE WHEN gender='Female' THEN 1 ELSE 0 END) AS females\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:13,sortKey:1912},{id:"sql-1913",number:14,title:"Group and order by an aggregate",question:"Group and order by an aggregate",difficulty:"Basic",category:"Aggregation & Grouping",answer:"You can `ORDER BY` an aggregate or its alias to rank groups, e.g. departments by total salary descending.\n\n\n```sql\nSELECT department, SUM(salary) AS total\nFROM employees GROUP BY department ORDER BY total DESC;\n```\n```\n",tech:"sql",displayNumber:14,sortKey:1913},{id:"sql-1914",number:15,title:"Every non-aggregated column must be in GROUP BY",question:"Every non-aggregated column must be in GROUP BY",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"In standard SQL, columns in `SELECT` that aren't aggregated must appear in `GROUP BY`. MySQL historically relaxed this (ONLY_FULL_GROUP_BY off), which can return arbitrary values — avoid relying on it.\n\n\n```sql\n-- name isn't functionally dependent on department -> must aggregate or group it\nSELECT department, MAX(name) FROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:15,sortKey:1914},{id:"sql-1915",number:16,title:"Compute min and max per group",question:"Compute min and max per group",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`MIN`/`MAX` return the smallest/largest value per group.\n\n\n```sql\nSELECT department, MIN(salary) AS lowest, MAX(salary) AS highest\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:16,sortKey:1915},{id:"sql-1916",number:17,title:"Percentage of total per group",question:"Percentage of total per group",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Divide a group's aggregate by the grand total using a window function (`SUM(...) OVER ()`) or a subquery for the total.\n\n\n```sql\nSELECT department, SUM(salary) AS total,\n  ROUND(100.0 * SUM(salary) / SUM(SUM(salary)) OVER (), 2) AS pct\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:17,sortKey:1916},{id:"sql-1917",number:18,title:"GROUP BY with ROLLUP (subtotals)",question:"GROUP BY with ROLLUP (subtotals)",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUP BY ROLLUP(a, b)` adds subtotal and grand-total rows (with NULL group keys) to the result, useful for hierarchical reporting.\n\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY ROLLUP(department, gender);\n```\n```\n",tech:"sql",displayNumber:18,sortKey:1917},{id:"sql-1918",number:19,title:"GROUP BY CUBE",question:"GROUP BY CUBE",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`CUBE(a, b)` produces aggregates for all combinations of the grouping columns (all subtotals across every dimension), unlike ROLLUP's hierarchical subtotals.\n\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY CUBE(department, gender);\n```\n```\n",tech:"sql",displayNumber:19,sortKey:1918},{id:"sql-1919",number:20,title:"GROUPING SETS",question:"GROUPING SETS",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUPING SETS` lets you specify exactly which groupings to compute in one query, combining multiple `GROUP BY` results without multiple passes.\n\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY GROUPING SETS ((department), (gender), ());\n```\n```\n",tech:"sql",displayNumber:20,sortKey:1919},{id:"sql-1920",number:21,title:"Count rows matching a condition without WHERE",question:"Count rows matching a condition without WHERE",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Use `COUNT(CASE WHEN cond THEN 1 END)` or `SUM(cond::int)` to count matches while still returning other aggregates over all rows.\n\n\n```sql\nSELECT COUNT(*) AS total,\n       COUNT(CASE WHEN salary > 80000 THEN 1 END) AS high_earners\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:21,sortKey:1920},{id:"sql-1921",number:22,title:"Average excluding outliers",question:"Average excluding outliers",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Filter outliers in `WHERE` before aggregating, or use percentiles to trim. `AVG` is sensitive to extremes, so consider `MEDIAN`/`PERCENTILE_CONT`.\n\n\n```sql\nSELECT AVG(salary) FROM employees\nWHERE salary BETWEEN 20000 AND 200000;\n```\n```\n",tech:"sql",displayNumber:22,sortKey:1921},{id:"sql-1922",number:23,title:"Median with PERCENTILE_CONT",question:"Median with PERCENTILE_CONT",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY col)` computes the median (an ordered-set aggregate) since SQL has no built-in MEDIAN.\n\n\n```sql\nSELECT PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median_salary\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:23,sortKey:1922},{id:"sql-1923",number:24,title:"String aggregation (concatenate group values)",question:"String aggregation (concatenate group values)",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`STRING_AGG(col, ', ')` (Postgres/SQL Server) or `GROUP_CONCAT` (MySQL) concatenates group values into one string, optionally ordered.\n\n\n```sql\nSELECT department, STRING_AGG(name, ', ' ORDER BY name) AS members\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:24,sortKey:1923},{id:"sql-1924",number:25,title:"Array aggregation",question:"Array aggregation",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`ARRAY_AGG(col)` (Postgres) collects group values into an array, useful for nesting related values without a separate query.\n\n\n```sql\nSELECT department, ARRAY_AGG(name) AS employee_names\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:25,sortKey:1924},{id:"sql-1925",number:26,title:"Filter aggregates with FILTER clause",question:"Filter aggregates with FILTER clause",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"The SQL-standard `FILTER (WHERE ...)` clause restricts which rows an aggregate sees, a cleaner alternative to `CASE` inside the aggregate (Postgres).\n\n\n```sql\nSELECT department,\n  COUNT(*) FILTER (WHERE salary > 80000) AS high_earners\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:26,sortKey:1925},{id:"sql-1926",number:27,title:"Count groups (number of distinct groups)",question:"Count groups (number of distinct groups)",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Wrap the grouped query in a subquery and `COUNT(*)`, or use `COUNT(DISTINCT col)` for a single grouping column.\n\n\n```sql\nSELECT COUNT(DISTINCT department) AS num_departments FROM employees;\n```\n```\n",tech:"sql",displayNumber:27,sortKey:1926},{id:"sql-1927",number:28,title:"Find groups where all rows meet a condition",question:"Find groups where all rows meet a condition",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Compare total count to conditional count: a group where every row matches has `COUNT(*) = COUNT(CASE WHEN cond THEN 1 END)`.\n\n\n```sql\nSELECT department FROM employees\nGROUP BY department\nHAVING COUNT(*) = COUNT(CASE WHEN salary > 40000 THEN 1 END);\n```\n```\n",tech:"sql",displayNumber:28,sortKey:1927},{id:"sql-1928",number:29,title:"Find groups where no row meets a condition",question:"Find groups where no row meets a condition",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Use `HAVING COUNT(CASE WHEN cond THEN 1 END) = 0` to keep groups with zero matching rows.\n\n\n```sql\nSELECT department FROM employees\nGROUP BY department\nHAVING COUNT(CASE WHEN salary > 150000 THEN 1 END) = 0;\n```\n```\n",tech:"sql",displayNumber:29,sortKey:1928},{id:"sql-1929",number:30,title:"Aggregate over a date/time bucket",question:"Aggregate over a date/time bucket",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Truncate the timestamp to the desired granularity (`DATE_TRUNC('month', ts)`) and group by it for time-series aggregation.\n\n\n```sql\nSELECT DATE_TRUNC('month', order_date) AS month, SUM(amount) AS revenue\nFROM orders GROUP BY 1 ORDER BY 1;\n```\n```\n",tech:"sql",displayNumber:30,sortKey:1929},{id:"sql-1930",number:31,title:"Group by an expression",question:"Group by an expression",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`GROUP BY` can use expressions (or ordinal positions / aliases in some DBs), e.g. grouping by year extracted from a date.\n\n\n```sql\nSELECT EXTRACT(YEAR FROM hire_date) AS yr, COUNT(*) \nFROM employees GROUP BY EXTRACT(YEAR FROM hire_date);\n```\n```\n",tech:"sql",displayNumber:31,sortKey:1930},{id:"sql-1931",number:32,title:"HAVING without GROUP BY",question:"HAVING without GROUP BY",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`HAVING` can be used without `GROUP BY` when the whole table is one implicit group, filtering based on a table-wide aggregate.\n\n\n```sql\nSELECT COUNT(*) FROM orders HAVING COUNT(*) > 1000;\n```\n```\n",tech:"sql",displayNumber:32,sortKey:1931},{id:"sql-1932",number:33,title:"Running total with SUM OVER",question:"Running total with SUM OVER",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"A windowed `SUM(...) OVER (ORDER BY ...)` computes a cumulative running total without collapsing rows — an aggregate used as a window function.\n\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (ORDER BY order_date) AS running_total\nFROM orders;\n```\n```\n",tech:"sql",displayNumber:33,sortKey:1932},{id:"sql-1933",number:34,title:"Average per group alongside detail rows",question:"Average per group alongside detail rows",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Use `AVG(...) OVER (PARTITION BY group)` to attach a group average to every detail row without a self-join or `GROUP BY`.\n\n\n```sql\nSELECT name, department, salary,\n  AVG(salary) OVER (PARTITION BY department) AS dept_avg\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:34,sortKey:1933},{id:"sql-1934",number:35,title:"Count NULLs in a column",question:"Count NULLs in a column",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`COUNT(*) - COUNT(col)` gives the NULL count, since `COUNT(col)` skips NULLs. Or `SUM(CASE WHEN col IS NULL THEN 1 ELSE 0 END)`.\n\n\n```sql\nSELECT COUNT(*) - COUNT(commission) AS null_commissions FROM employees;\n```\n```\n",tech:"sql",displayNumber:35,sortKey:1934},{id:"sql-1935",number:36,title:"Group data into ranges/buckets",question:"Group data into ranges/buckets",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Derive a bucket with `CASE` or `WIDTH_BUCKET`/`FLOOR`, then group by it to build histograms.\n\n\n```sql\nSELECT FLOOR(salary / 20000) * 20000 AS band, COUNT(*)\nFROM employees GROUP BY FLOOR(salary / 20000) GROUP BY 1;\n```\n```\n",tech:"sql",displayNumber:36,sortKey:1935},{id:"sql-1936",number:37,title:"Total with grand total row using UNION",question:"Total with grand total row using UNION",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`UNION ALL` a grand-total query onto the grouped result (or use `ROLLUP`) to append an overall total.\n\n\n```sql\nSELECT department, SUM(salary) FROM employees GROUP BY department\nUNION ALL\nSELECT 'ALL', SUM(salary) FROM employees;\n```\n```\n",tech:"sql",displayNumber:37,sortKey:1936},{id:"sql-1937",number:38,title:"Weighted average",question:"Weighted average",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Compute `SUM(value * weight) / SUM(weight)` rather than a plain `AVG`, when rows contribute unequally.\n\n\n```sql\nSELECT product_id, SUM(price * quantity) / SUM(quantity) AS avg_price\nFROM order_items GROUP BY product_id;\n```\n```\n",tech:"sql",displayNumber:38,sortKey:1937},{id:"sql-1938",number:39,title:"Aggregate then join back to detail",question:"Aggregate then join back to detail",difficulty:"Experienced",category:"Aggregation & Grouping",answer:`Compute group aggregates in a subquery/CTE, then join back to detail rows to compare each row against its group stat (e.g. above-average earners).


\`\`\`sql
WITH dept_avg AS (
  SELECT department, AVG(salary) avg_sal FROM employees GROUP BY department)
SELECT e.name, e.salary
FROM employees e JOIN dept_avg d ON e.department = d.department
WHERE e.salary > d.avg_sal;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:39,sortKey:1938},{id:"sql-1939",number:40,title:"Why is GROUP BY slow and how to speed it up?",question:"Why is GROUP BY slow and how to speed it up?",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUP BY` may sort/hash large inputs. Speed it up with indexes on grouping columns (enabling index-ordered grouping), filtering rows early in `WHERE`, pre-aggregating, or materialized views for repeated heavy aggregations.\n\n\n```sql\nCREATE INDEX idx_emp_dept ON employees(department);\nEXPLAIN SELECT department, COUNT(*) FROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:40,sortKey:1939},{id:"sql-1940",number:1,title:"What is a subquery?",question:"What is a subquery?",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A subquery is a query nested inside another query (in `SELECT`, `FROM`, `WHERE`, or `HAVING`). It can return a scalar, a row, or a set used by the outer query.\n\n\n```sql\nSELECT name FROM employees\nWHERE salary > (SELECT AVG(salary) FROM employees);\n```\n```\n",tech:"sql",displayNumber:1,sortKey:1940},{id:"sql-1941",number:2,title:"Scalar subquery",question:"Scalar subquery",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A scalar subquery returns exactly one value and can be used anywhere a single value is expected.\n\n\n```sql\nSELECT name, salary, (SELECT MAX(salary) FROM employees) AS top_salary\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:2,sortKey:1941},{id:"sql-1942",number:3,title:"Subquery in WHERE with IN",question:"Subquery in WHERE with IN",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A subquery returning a column of values can feed `IN` to filter the outer query.\n\n\n```sql\nSELECT name FROM employees\nWHERE department_id IN (SELECT id FROM departments WHERE location = 'NY');\n```\n```\n",tech:"sql",displayNumber:3,sortKey:1942},{id:"sql-1943",number:4,title:"Correlated subquery",question:"Correlated subquery",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A correlated subquery references columns from the outer query, so it's re-evaluated per outer row. It's powerful but can be slow if not indexed.\n\n\n```sql\nSELECT e.name FROM employees e\nWHERE e.salary > (SELECT AVG(salary) FROM employees WHERE department = e.department);\n```\n```\n",tech:"sql",displayNumber:4,sortKey:1943},{id:"sql-1944",number:5,title:"Difference between correlated and non-correlated subquery",question:"Difference between correlated and non-correlated subquery",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A non-correlated subquery is independent and runs once. A correlated subquery depends on the outer row and conceptually runs per row. Optimizers often rewrite correlated subqueries as joins.\n\n\n```sql\n-- non-correlated: runs once\nSELECT * FROM orders WHERE amount > (SELECT AVG(amount) FROM orders);\n```\n```\n",tech:"sql",displayNumber:5,sortKey:1944},{id:"sql-1945",number:6,title:"EXISTS vs IN",question:"EXISTS vs IN",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`EXISTS` checks for existence and short-circuits at the first match (good for correlated checks and large subsets). `IN` compares against a materialized list. `EXISTS` is NULL-safe; `NOT IN` is not.\n\n\n```sql\nSELECT name FROM customers c\nWHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```\n```\n",tech:"sql",displayNumber:6,sortKey:1945},{id:"sql-1946",number:7,title:"Subquery in FROM (derived table)",question:"Subquery in FROM (derived table)",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"A subquery in `FROM` acts as an inline (derived) table that the outer query selects from. It must be aliased.\n\n\n```sql\nSELECT dept, avg_sal FROM\n  (SELECT department AS dept, AVG(salary) AS avg_sal FROM employees GROUP BY department) t\nWHERE avg_sal > 50000;\n```\n```\n",tech:"sql",displayNumber:7,sortKey:1946},{id:"sql-1947",number:8,title:"What is a CTE (Common Table Expression)?",question:"What is a CTE (Common Table Expression)?",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"A CTE is a named temporary result defined with `WITH`, usable later in the same query. It improves readability and lets you reference the same subquery multiple times.\n\n\n```sql\nWITH high_earners AS (\n  SELECT * FROM employees WHERE salary > 80000)\nSELECT department, COUNT(*) FROM high_earners GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:8,sortKey:1947},{id:"sql-1948",number:9,title:"CTE vs subquery vs temp table",question:"CTE vs subquery vs temp table",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A subquery is inline and single-use. A CTE names a subquery for readability/reuse within one statement (usually not materialized). A temp table persists across statements and can be indexed but adds overhead.\n\n\n```sql\nWITH t AS (SELECT ...) SELECT * FROM t JOIN t t2 ON ...; -- reuse within one query\n```\n```\n",tech:"sql",displayNumber:9,sortKey:1948},{id:"sql-1949",number:10,title:"Recursive CTE",question:"Recursive CTE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:`A recursive CTE references itself to traverse hierarchies/graphs (org charts, category trees). It has an anchor member and a recursive member joined via \`UNION ALL\`.


\`\`\`sql
WITH RECURSIVE org AS (
  SELECT id, name, manager_id, 1 AS lvl FROM employees WHERE manager_id IS NULL
  UNION ALL
  SELECT e.id, e.name, e.manager_id, o.lvl + 1
  FROM employees e JOIN org o ON e.manager_id = o.id)
SELECT * FROM org ORDER BY lvl;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:10,sortKey:1949},{id:"sql-1950",number:11,title:"What are window functions?",question:"What are window functions?",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:'Window functions compute values across a set of rows (a "window") related to the current row, without collapsing them like `GROUP BY`. They use `OVER (PARTITION BY ... ORDER BY ...)`.\n\n\n```sql\nSELECT name, department, salary,\n  RANK() OVER (PARTITION BY department ORDER BY salary DESC) AS dept_rank\nFROM employees;\n```\n```\n',tech:"sql",displayNumber:11,sortKey:1950},{id:"sql-1951",number:12,title:"ROW_NUMBER vs RANK vs DENSE_RANK",question:"ROW_NUMBER vs RANK vs DENSE_RANK",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROW_NUMBER` gives unique sequential numbers. `RANK` gives ties the same rank but skips subsequent numbers (1,1,3). `DENSE_RANK` gives ties the same rank without gaps (1,1,2).\n\n\n```sql\nSELECT name, salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) rn,\n  RANK()       OVER (ORDER BY salary DESC) rnk,\n  DENSE_RANK() OVER (ORDER BY salary DESC) drnk\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:12,sortKey:1951},{id:"sql-1952",number:13,title:"Find the Nth highest salary with window functions",question:"Find the Nth highest salary with window functions",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Rank rows with `DENSE_RANK` (to handle ties) and filter for the desired rank in an outer query, since you can't filter window results in `WHERE`.\n\n\n```sql\nSELECT name, salary FROM (\n  SELECT name, salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS rnk\n  FROM employees) t\nWHERE rnk = 3; -- 3rd highest\n```\n```\n",tech:"sql",displayNumber:13,sortKey:1952},{id:"sql-1953",number:14,title:"Top N per group",question:"Top N per group",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:'Partition by the group, order within it, and filter by `ROW_NUMBER`/`RANK` ≤ N — a classic "top-N-per-category" pattern.\n\n\n```sql\nSELECT * FROM (\n  SELECT name, department, salary,\n    ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) rn\n  FROM employees) t\nWHERE rn <= 3;\n```\n```\n',tech:"sql",displayNumber:14,sortKey:1953},{id:"sql-1954",number:15,title:"PARTITION BY explained",question:"PARTITION BY explained",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`PARTITION BY` splits rows into groups for the window function, resetting the calculation per partition — like `GROUP BY` but without collapsing rows.\n\n\n```sql\nSELECT name, department,\n  COUNT(*) OVER (PARTITION BY department) AS dept_size\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:15,sortKey:1954},{id:"sql-1955",number:16,title:"LAG and LEAD",question:"LAG and LEAD",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`LAG(col, n)` accesses a previous row's value; `LEAD(col, n)` a following row's — great for period-over-period comparisons.\n\n\n```sql\nSELECT order_date, amount,\n  amount - LAG(amount) OVER (ORDER BY order_date) AS change\nFROM daily_sales;\n```\n```\n",tech:"sql",displayNumber:16,sortKey:1955},{id:"sql-1956",number:17,title:"Running total (cumulative sum)",question:"Running total (cumulative sum)",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`SUM(col) OVER (ORDER BY ...)` accumulates values row by row. Add `PARTITION BY` to reset the running total per group.\n\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (ORDER BY order_date\n       ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS running_total\nFROM orders;\n```\n```\n",tech:"sql",displayNumber:17,sortKey:1956},{id:"sql-1957",number:18,title:"Moving average",question:"Moving average",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Use a windowed `AVG` with a frame (`ROWS BETWEEN n PRECEDING AND CURRENT ROW`) to smooth a series over a sliding window.\n\n\n```sql\nSELECT day, value,\n  AVG(value) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS ma7\nFROM metrics;\n```\n```\n",tech:"sql",displayNumber:18,sortKey:1957},{id:"sql-1958",number:19,title:"FIRST_VALUE and LAST_VALUE",question:"FIRST_VALUE and LAST_VALUE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`FIRST_VALUE`/`LAST_VALUE` return the first/last value in the window frame. For a correct `LAST_VALUE`, set the frame to `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`.\n\n\n```sql\nSELECT name, department, salary,\n  FIRST_VALUE(name) OVER (PARTITION BY department ORDER BY salary DESC) AS top_earner\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:19,sortKey:1958},{id:"sql-1959",number:20,title:"NTILE for bucketing",question:"NTILE for bucketing",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`NTILE(n)` distributes ordered rows into n roughly equal buckets — useful for quartiles/percentile bands.\n\n\n```sql\nSELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:20,sortKey:1959},{id:"sql-1960",number:21,title:"Why can't you use window functions in WHERE?",question:"Why can't you use window functions in WHERE?",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Window functions are computed after `WHERE`/`GROUP BY`/`HAVING` (during `SELECT`), so you must wrap them in a subquery or CTE to filter on their results.\n\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (ORDER BY salary DESC) rn FROM employees) t\nWHERE rn = 1;\n```\n```\n",tech:"sql",displayNumber:21,sortKey:1960},{id:"sql-1961",number:22,title:"Difference between GROUP BY and window functions",question:"Difference between GROUP BY and window functions",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`GROUP BY` collapses rows into one per group. Window functions add aggregate/analytic columns while preserving all detail rows, so you keep row-level data plus group context.\n\n\n```sql\nSELECT name, salary, AVG(salary) OVER (PARTITION BY department) AS dept_avg\nFROM employees; -- every employee row kept\n```\n```\n",tech:"sql",displayNumber:22,sortKey:1961},{id:"sql-1962",number:23,title:"Second highest salary without window functions",question:"Second highest salary without window functions",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Use a subquery: the max salary less than the overall max, which correctly skips ties.\n\n\n```sql\nSELECT MAX(salary) AS second_highest\nFROM employees\nWHERE salary < (SELECT MAX(salary) FROM employees);\n```\n```\n",tech:"sql",displayNumber:23,sortKey:1962},{id:"sql-1963",number:24,title:"Nth highest salary with LIMIT/OFFSET",question:"Nth highest salary with LIMIT/OFFSET",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Order distinct salaries descending and use `OFFSET n-1 LIMIT 1`. Use `DISTINCT` so ties don't shift the position.\n\n\n```sql\nSELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 2; -- 3rd\n```\n```\n",tech:"sql",displayNumber:24,sortKey:1963},{id:"sql-1964",number:25,title:"Correlated subquery to rank rows",question:"Correlated subquery to rank rows",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Before window functions, ranks were computed by counting how many rows have a greater value in a correlated subquery.\n\n\n```sql\nSELECT e.name, e.salary,\n  (SELECT COUNT(DISTINCT salary) FROM employees WHERE salary >= e.salary) AS rank\nFROM employees e;\n```\n```\n",tech:"sql",displayNumber:25,sortKey:1964},{id:"sql-1965",number:26,title:"Using a CTE for readability",question:"Using a CTE for readability",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Break a complex query into named CTE steps, each transforming the previous, making logic easier to follow and test.\n\n\n```sql\nWITH paid AS (SELECT * FROM orders WHERE status='paid'),\n     per_cust AS (SELECT customer_id, SUM(amount) t FROM paid GROUP BY customer_id)\nSELECT * FROM per_cust WHERE t > 1000;\n```\n```\n",tech:"sql",displayNumber:26,sortKey:1965},{id:"sql-1966",number:27,title:"Multiple CTEs in one query",question:"Multiple CTEs in one query",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Define several CTEs separated by commas after a single `WITH`; later CTEs can reference earlier ones.\n\n\n```sql\nWITH a AS (SELECT ...),\n     b AS (SELECT ... FROM a ...)\nSELECT * FROM b;\n```\n```\n",tech:"sql",displayNumber:27,sortKey:1966},{id:"sql-1967",number:28,title:"Recursive CTE to generate a number/date series",question:"Recursive CTE to generate a number/date series",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"A recursive CTE can generate sequences (numbers, dates) when the DB lacks `generate_series`, useful for gap-filling reports.\n\n\n```sql\nWITH RECURSIVE seq(n) AS (\n  SELECT 1 UNION ALL SELECT n + 1 FROM seq WHERE n < 10)\nSELECT n FROM seq;\n```\n```\n",tech:"sql",displayNumber:28,sortKey:1967},{id:"sql-1968",number:29,title:"Detect gaps in a sequence",question:"Detect gaps in a sequence",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Compare each value to the next via `LEAD`; a gap exists where `next - current > 1`.\n\n\n```sql\nSELECT id AS gap_start, next_id AS gap_end FROM (\n  SELECT id, LEAD(id) OVER (ORDER BY id) AS next_id FROM tickets) t\nWHERE next_id - id > 1;\n```\n```\n",tech:"sql",displayNumber:29,sortKey:1968},{id:"sql-1969",number:30,title:"Islands and gaps problem",question:"Islands and gaps problem",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:`Group consecutive rows ("islands") by subtracting \`ROW_NUMBER()\` from the sequential value; equal differences belong to the same run.


\`\`\`sql
SELECT MIN(day) AS start, MAX(day) AS end
FROM (SELECT day, day - ROW_NUMBER() OVER (ORDER BY day) * INTERVAL '1 day' AS grp
      FROM active_days) t
GROUP BY grp;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:30,sortKey:1969},{id:"sql-1970",number:31,title:"Deduplicate keeping the latest row",question:"Deduplicate keeping the latest row",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROW_NUMBER()` partitioned by the dedup key, ordered by recency, then keep `rn = 1`.\n\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY email ORDER BY updated_at DESC) rn\n  FROM users) t\nWHERE rn = 1;\n```\n```\n",tech:"sql",displayNumber:31,sortKey:1970},{id:"sql-1971",number:32,title:"Compare each row to the group max",question:"Compare each row to the group max",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Use `MAX(col) OVER (PARTITION BY group)` to attach the group max to each row, then compute a difference or flag.\n\n\n```sql\nSELECT name, department, salary,\n  MAX(salary) OVER (PARTITION BY department) - salary AS below_top\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:32,sortKey:1971},{id:"sql-1972",number:33,title:"Percent of total using window SUM",question:"Percent of total using window SUM",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Divide each value by `SUM(col) OVER ()` (whole set) or `OVER (PARTITION BY group)` for share within group.\n\n\n```sql\nSELECT department, salary,\n  ROUND(100.0 * salary / SUM(salary) OVER (PARTITION BY department), 2) AS pct_of_dept\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:33,sortKey:1972},{id:"sql-1973",number:34,title:"Cumulative distribution and PERCENT_RANK",question:"Cumulative distribution and PERCENT_RANK",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`CUME_DIST` gives the fraction of rows ≤ current; `PERCENT_RANK` gives relative rank (0..1). Useful for percentiles/scoring.\n\n\n```sql\nSELECT name, salary,\n  ROUND(PERCENT_RANK() OVER (ORDER BY salary)::numeric, 2) AS pct_rank\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:34,sortKey:1973},{id:"sql-1974",number:35,title:"Window frame: ROWS vs RANGE",question:"Window frame: ROWS vs RANGE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROWS` counts physical rows in the frame; `RANGE` groups rows with the same `ORDER BY` value (peers) together. They differ when ordering values tie.\n\n\n```sql\nSUM(x) OVER (ORDER BY d ROWS BETWEEN 1 PRECEDING AND CURRENT ROW)   -- physical\nSUM(x) OVER (ORDER BY d RANGE BETWEEN 1 PRECEDING AND CURRENT ROW)  -- value-based\n```\n```\n",tech:"sql",displayNumber:35,sortKey:1974},{id:"sql-1975",number:36,title:"Difference between two consecutive rows",question:"Difference between two consecutive rows",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Subtract `LAG(col)` from the current value to get the row-to-row delta (e.g. daily growth).\n\n\n```sql\nSELECT day, revenue,\n  revenue - LAG(revenue) OVER (ORDER BY day) AS daily_change\nFROM sales;\n```\n```\n",tech:"sql",displayNumber:36,sortKey:1975},{id:"sql-1976",number:37,title:"Rank with ties broken by a second column",question:"Rank with ties broken by a second column",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Add tie-breaker columns to the window `ORDER BY` so `ROW_NUMBER`/`RANK` produce a deterministic order.\n\n\n```sql\nSELECT name,\n  ROW_NUMBER() OVER (ORDER BY salary DESC, hire_date ASC) AS rn\nFROM employees;\n```\n```\n",tech:"sql",displayNumber:37,sortKey:1976},{id:"sql-1977",number:38,title:"Reuse a window with WINDOW clause",question:"Reuse a window with WINDOW clause",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Define a named window once in a `WINDOW` clause and reference it from multiple functions to avoid repetition.\n\n\n```sql\nSELECT name, salary,\n  RANK() OVER w, DENSE_RANK() OVER w\nFROM employees\nWINDOW w AS (PARTITION BY department ORDER BY salary DESC);\n```\n```\n",tech:"sql",displayNumber:38,sortKey:1977},{id:"sql-1978",number:39,title:"Correlated subquery vs window function performance",question:"Correlated subquery vs window function performance",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"A per-row correlated subquery can be O(n²); a window function computes over sorted partitions in roughly one pass, so window functions are usually faster and clearer for ranking/running totals.\n\n\n```sql\n-- prefer this over a COUNT(*) correlated subquery\nSELECT name, RANK() OVER (ORDER BY salary DESC) FROM employees;\n```\n```\n",tech:"sql",displayNumber:39,sortKey:1978},{id:"sql-1979",number:40,title:"Pivot rows to columns",question:"Pivot rows to columns",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Pivot with conditional aggregation (`SUM(CASE WHEN ...)`) or a dedicated `PIVOT` operator (SQL Server/Oracle), turning distinct row values into columns.\n\n\n```sql\nSELECT department,\n  SUM(CASE WHEN gender='Male' THEN 1 ELSE 0 END) AS male,\n  SUM(CASE WHEN gender='Female' THEN 1 ELSE 0 END) AS female\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:40,sortKey:1979},{id:"sql-1980",number:1,title:"Find the second highest salary",question:"Find the second highest salary",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Take the max salary below the overall max (handles ties), or use `DENSE_RANK`. This is one of the most-asked SQL interview questions.\n\n\n```sql\nSELECT MAX(salary) AS second_highest\nFROM employees WHERE salary < (SELECT MAX(salary) FROM employees);\n```\n```\n",tech:"sql",displayNumber:1,sortKey:1980},{id:"sql-1981",number:2,title:"Find employees earning more than their department average",question:"Find employees earning more than their department average",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:`Compare each employee's salary to their department average via a correlated subquery or a window function.


\`\`\`sql
SELECT name, department, salary FROM (
  SELECT name, department, salary,
    AVG(salary) OVER (PARTITION BY department) AS avg_sal
  FROM employees) t
WHERE salary > avg_sal;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:2,sortKey:1981},{id:"sql-1982",number:3,title:"Find the highest-paid employee in each department",question:"Find the highest-paid employee in each department",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Rank within each department and keep rank 1. Use `RANK` if you want to include ties, `ROW_NUMBER` for exactly one.\n\n\n```sql\nSELECT name, department, salary FROM (\n  SELECT *, RANK() OVER (PARTITION BY department ORDER BY salary DESC) rk\n  FROM employees) t\nWHERE rk = 1;\n```\n```\n",tech:"sql",displayNumber:3,sortKey:1982},{id:"sql-1983",number:4,title:"Find duplicate records",question:"Find duplicate records",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Group by the columns that define a duplicate and keep groups with `COUNT(*) > 1`.\n\n\n```sql\nSELECT email, COUNT(*) FROM users GROUP BY email HAVING COUNT(*) > 1;\n```\n```\n",tech:"sql",displayNumber:4,sortKey:1983},{id:"sql-1984",number:5,title:"Delete duplicate rows, keep the earliest",question:"Delete duplicate rows, keep the earliest",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Rank duplicates by a tiebreaker and delete all but the first, or delete rows whose id isn't the minimum per duplicate group.\n\n\n```sql\nDELETE FROM users a\nUSING users b\nWHERE a.email = b.email AND a.id > b.id;\n```\n```\n",tech:"sql",displayNumber:5,sortKey:1984},{id:"sql-1985",number:6,title:"Find customers who never placed an order",question:"Find customers who never placed an order",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Anti-join with LEFT JOIN + `IS NULL`, or `NOT EXISTS` (NULL-safe).\n\n\n```sql\nSELECT c.name FROM customers c\nWHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```\n```\n",tech:"sql",displayNumber:6,sortKey:1985},{id:"sql-1986",number:7,title:"Find the most recent order per customer",question:"Find the most recent order per customer",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Partition by customer, order by date descending, keep `ROW_NUMBER = 1`.\n\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date DESC) rn\n  FROM orders) t\nWHERE rn = 1;\n```\n```\n",tech:"sql",displayNumber:7,sortKey:1986},{id:"sql-1987",number:8,title:"Running total of daily sales",question:"Running total of daily sales",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Windowed cumulative `SUM` ordered by date.\n\n\n```sql\nSELECT day, amount,\n  SUM(amount) OVER (ORDER BY day) AS cumulative\nFROM daily_sales;\n```\n```\n",tech:"sql",displayNumber:8,sortKey:1987},{id:"sql-1988",number:9,title:"Month-over-month growth",question:"Month-over-month growth",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Aggregate by month, then use \`LAG\` to compare to the previous month and compute percentage change.


\`\`\`sql
WITH m AS (
  SELECT DATE_TRUNC('month', order_date) mo, SUM(amount) rev
  FROM orders GROUP BY 1)
SELECT mo, rev,
  ROUND(100.0 * (rev - LAG(rev) OVER (ORDER BY mo)) / LAG(rev) OVER (ORDER BY mo), 2) AS growth_pct
FROM m;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:9,sortKey:1988},{id:"sql-1989",number:10,title:"Find the top 3 products by revenue",question:"Find the top 3 products by revenue",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Aggregate revenue per product, order descending, and limit to 3 (or rank and filter for ties).\n\n\n```sql\nSELECT product_id, SUM(amount) AS revenue\nFROM order_items GROUP BY product_id\nORDER BY revenue DESC LIMIT 3;\n```\n```\n",tech:"sql",displayNumber:10,sortKey:1989},{id:"sql-1990",number:11,title:"Find gaps between consecutive dates per user",question:"Find gaps between consecutive dates per user",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `LAG` over the date ordered per user and compute the interval difference.\n\n\n```sql\nSELECT user_id, login_date,\n  login_date - LAG(login_date) OVER (PARTITION BY user_id ORDER BY login_date) AS gap\nFROM logins;\n```\n```\n",tech:"sql",displayNumber:11,sortKey:1990},{id:"sql-1991",number:12,title:"Find consecutive login streaks",question:"Find consecutive login streaks",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Subtract `ROW_NUMBER()` from the date to form a constant group per consecutive run, then aggregate min/max and count per group.\n\n\n```sql\nSELECT user_id, MIN(d) AS streak_start, COUNT(*) AS length\nFROM (SELECT user_id, d,\n        d - (ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY d)) * INTERVAL '1 day' AS grp\n      FROM login_days) t\nGROUP BY user_id, grp;\n```\n```\n",tech:"sql",displayNumber:12,sortKey:1991},{id:"sql-1992",number:13,title:"Pivot monthly sales into columns",question:"Pivot monthly sales into columns",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Conditional aggregation with one `SUM(CASE ...)` per target column.\n\n\n```sql\nSELECT product_id,\n  SUM(CASE WHEN EXTRACT(MONTH FROM d)=1 THEN amt END) AS jan,\n  SUM(CASE WHEN EXTRACT(MONTH FROM d)=2 THEN amt END) AS feb\nFROM sales GROUP BY product_id;\n```\n```\n",tech:"sql",displayNumber:13,sortKey:1992},{id:"sql-1993",number:14,title:"Unpivot columns into rows",question:"Unpivot columns into rows",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`UNION ALL` one query per column, or use `UNPIVOT`/`LATERAL` to turn wide columns into (key, value) rows.\n\n\n```sql\nSELECT id, 'q1' AS quarter, q1 AS amt FROM sales\nUNION ALL SELECT id, 'q2', q2 FROM sales;\n```\n```\n",tech:"sql",displayNumber:14,sortKey:1993},{id:"sql-1994",number:15,title:"Find median salary",question:"Find median salary",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary)`, or emulate with row numbering picking the middle row(s).\n\n\n```sql\nSELECT PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median FROM employees;\n```\n```\n",tech:"sql",displayNumber:15,sortKey:1994},{id:"sql-1995",number:16,title:"Find employees with the same salary",question:"Find employees with the same salary",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Group by salary and keep groups with more than one employee, or self-join on equal salary and different id.\n\n\n```sql\nSELECT salary, STRING_AGG(name, ', ') AS people\nFROM employees GROUP BY salary HAVING COUNT(*) > 1;\n```\n```\n",tech:"sql",displayNumber:16,sortKey:1995},{id:"sql-1996",number:17,title:"Find the manager with the most reports",question:"Find the manager with the most reports",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Self-join or group by `manager_id`, count reports, order descending.\n\n\n```sql\nSELECT m.name, COUNT(*) AS reports\nFROM employees e JOIN employees m ON e.manager_id = m.id\nGROUP BY m.name ORDER BY reports DESC LIMIT 1;\n```\n```\n",tech:"sql",displayNumber:17,sortKey:1996},{id:"sql-1997",number:18,title:"Traverse an org hierarchy",question:"Traverse an org hierarchy",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`A recursive CTE walks from a root down the reporting chain, tracking depth/path.


\`\`\`sql
WITH RECURSIVE tree AS (
  SELECT id, name, manager_id, 1 lvl FROM employees WHERE id = 1
  UNION ALL
  SELECT e.id, e.name, e.manager_id, t.lvl+1
  FROM employees e JOIN tree t ON e.manager_id = t.id)
SELECT * FROM tree;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:18,sortKey:1997},{id:"sql-1998",number:19,title:"Calculate retention (users active in consecutive periods)",question:"Calculate retention (users active in consecutive periods)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Self-join or use `EXISTS` to check activity in period N and N+1 per user, then aggregate.\n\n\n```sql\nSELECT COUNT(DISTINCT a.user_id) AS retained\nFROM monthly_active a\nJOIN monthly_active b ON a.user_id = b.user_id AND b.month = a.month + 1;\n```\n```\n",tech:"sql",displayNumber:19,sortKey:1998},{id:"sql-1999",number:20,title:"Find the first purchase date per customer",question:"Find the first purchase date per customer",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`MIN(order_date)` grouped by customer, or `FIRST_VALUE` window if you need it alongside other order fields.\n\n\n```sql\nSELECT customer_id, MIN(order_date) AS first_purchase\nFROM orders GROUP BY customer_id;\n```\n```\n",tech:"sql",displayNumber:20,sortKey:1999},{id:"sql-2000",number:21,title:"Cohort analysis by signup month",question:"Cohort analysis by signup month",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Assign each user a cohort (signup month), then join activity and count actives per cohort per subsequent month.


\`\`\`sql
WITH cohort AS (
  SELECT id, DATE_TRUNC('month', signup_date) AS cohort_month FROM users)
SELECT c.cohort_month, DATE_TRUNC('month', a.event_date) AS active_month, COUNT(DISTINCT a.user_id)
FROM cohort c JOIN events a ON a.user_id = c.id
GROUP BY 1, 2;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:21,sortKey:2e3},{id:"sql-2001",number:22,title:"Find rows where a value changed",question:"Find rows where a value changed",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Compare each row to the previous with `LAG` and keep rows where the value differs — change-detection over an ordered series.\n\n\n```sql\nSELECT * FROM (\n  SELECT *, LAG(status) OVER (PARTITION BY order_id ORDER BY ts) AS prev\n  FROM order_events) t\nWHERE status IS DISTINCT FROM prev;\n```\n```\n",tech:"sql",displayNumber:22,sortKey:2001},{id:"sql-2002",number:23,title:"Deduplicate keeping the row with max value",question:"Deduplicate keeping the row with max value",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`ROW_NUMBER()` partitioned by the key ordered by the value descending, keep `rn = 1`.\n\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY price DESC) rn\n  FROM price_history) t\nWHERE rn = 1;\n```\n```\n",tech:"sql",displayNumber:23,sortKey:2002},{id:"sql-2003",number:24,title:"Find products never sold",question:"Find products never sold",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Anti-join products against order items with `NOT EXISTS`.\n\n\n```sql\nSELECT p.name FROM products p\nWHERE NOT EXISTS (SELECT 1 FROM order_items oi WHERE oi.product_id = p.id);\n```\n```\n",tech:"sql",displayNumber:24,sortKey:2003},{id:"sql-2004",number:25,title:"Compute year-to-date totals",question:"Compute year-to-date totals",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Windowed running sum partitioned by year, ordered by date.\n\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (PARTITION BY EXTRACT(YEAR FROM order_date) ORDER BY order_date) AS ytd\nFROM orders;\n```\n```\n",tech:"sql",displayNumber:25,sortKey:2004},{id:"sql-2005",number:26,title:"Rank customers into quartiles by spend",question:"Rank customers into quartiles by spend",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Aggregate spend per customer, then `NTILE(4)` over spend to segment them.\n\n\n```sql\nSELECT customer_id, total,\n  NTILE(4) OVER (ORDER BY total DESC) AS quartile\nFROM (SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id) s;\n```\n```\n",tech:"sql",displayNumber:26,sortKey:2005},{id:"sql-2006",number:27,title:"Find overlapping date ranges",question:"Find overlapping date ranges",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Self-join and check `a.start <= b.end AND b.start <= a.end` (with an id inequality to avoid self/duplicate pairs).\n\n\n```sql\nSELECT a.id, b.id FROM bookings a\nJOIN bookings b ON a.room_id = b.room_id AND a.id < b.id\nWHERE a.start_time < b.end_time AND b.start_time < a.end_time;\n```\n```\n",tech:"sql",displayNumber:27,sortKey:2006},{id:"sql-2007",number:28,title:"Fill missing dates with zero (gap filling)",question:"Fill missing dates with zero (gap filling)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Generate a full date series (`generate_series`/calendar table) and LEFT JOIN the data, coalescing missing values to 0.\n\n\n```sql\nSELECT d::date, COALESCE(SUM(amount), 0) AS revenue\nFROM generate_series('2024-01-01','2024-01-31',INTERVAL '1 day') d\nLEFT JOIN orders o ON o.order_date = d::date\nGROUP BY d ORDER BY d;\n```\n```\n",tech:"sql",displayNumber:28,sortKey:2007},{id:"sql-2008",number:29,title:"Get the difference between max and min per group",question:"Get the difference between max and min per group",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"`MAX(col) - MIN(col)` per group gives the range/spread.\n\n\n```sql\nSELECT department, MAX(salary) - MIN(salary) AS salary_range\nFROM employees GROUP BY department;\n```\n```\n",tech:"sql",displayNumber:29,sortKey:2008},{id:"sql-2009",number:30,title:"Find top spender per city",question:"Find top spender per city",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Aggregate spend by city+customer, rank within city, keep rank 1.


\`\`\`sql
SELECT * FROM (
  SELECT city, customer_id, SUM(amount) total,
    RANK() OVER (PARTITION BY city ORDER BY SUM(amount) DESC) rk
  FROM orders GROUP BY city, customer_id) t
WHERE rk = 1;
\`\`\`
\`\`\`
`,tech:"sql",displayNumber:30,sortKey:2009},{id:"sql-2010",number:31,title:"Calculate conversion rate",question:"Calculate conversion rate",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Divide converted counts by total counts using conditional aggregation, casting to a decimal to avoid integer division.\n\n\n```sql\nSELECT ROUND(100.0 * COUNT(*) FILTER (WHERE converted) / COUNT(*), 2) AS conversion_pct\nFROM sessions;\n```\n```\n",tech:"sql",displayNumber:31,sortKey:2010},{id:"sql-2011",number:32,title:"Avoid integer division pitfalls",question:"Avoid integer division pitfalls",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Integer / integer truncates. Multiply by `1.0` or cast one operand to numeric/float to get a decimal result.\n\n\n```sql\nSELECT 3 / 2 AS wrong, 3 * 1.0 / 2 AS right; -- 1 vs 1.5\n```\n```\n",tech:"sql",displayNumber:32,sortKey:2011},{id:"sql-2012",number:33,title:"Update rows based on another table (correlated update)",question:"Update rows based on another table (correlated update)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `UPDATE ... FROM`/subquery to set values from a related table, matching on the key.\n\n\n```sql\nUPDATE employees e\nSET department_name = d.name\nFROM departments d\nWHERE e.department_id = d.id;\n```\n```\n",tech:"sql",displayNumber:33,sortKey:2012},{id:"sql-2013",number:34,title:"Upsert (insert or update on conflict)",question:"Upsert (insert or update on conflict)",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`INSERT ... ON CONFLICT DO UPDATE` (Postgres) or `INSERT ... ON DUPLICATE KEY UPDATE` (MySQL) / `MERGE` inserts new rows and updates existing ones atomically.\n\n\n```sql\nINSERT INTO inventory(sku, qty) VALUES ('A1', 5)\nON CONFLICT (sku) DO UPDATE SET qty = inventory.qty + EXCLUDED.qty;\n```\n```\n",tech:"sql",displayNumber:34,sortKey:2013},{id:"sql-2014",number:35,title:"Bulk delete with a join condition",question:"Bulk delete with a join condition",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`DELETE ... USING`/subquery removes rows matched against another table, e.g. orders of inactive customers.\n\n\n```sql\nDELETE FROM orders o\nUSING customers c\nWHERE o.customer_id = c.id AND c.status = 'inactive';\n```\n```\n",tech:"sql",displayNumber:35,sortKey:2014},{id:"sql-2015",number:36,title:"Find the mode (most frequent value)",question:"Find the mode (most frequent value)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Group by the value, order by count descending, limit 1 — or `MODE() WITHIN GROUP (ORDER BY col)` in Postgres.\n\n\n```sql\nSELECT MODE() WITHIN GROUP (ORDER BY department) AS most_common_dept FROM employees;\n```\n```\n",tech:"sql",displayNumber:36,sortKey:2015},{id:"sql-2016",number:37,title:"Compare current vs previous period side by side",question:"Compare current vs previous period side by side",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Aggregate per period, then self-join (or `LAG`) to align this period with the prior one for comparison.\n\n\n```sql\nWITH m AS (SELECT DATE_TRUNC('month',d) mo, SUM(amt) rev FROM sales GROUP BY 1)\nSELECT mo, rev, LAG(rev) OVER (ORDER BY mo) AS prev_rev FROM m;\n```\n```\n",tech:"sql",displayNumber:37,sortKey:2016},{id:"sql-2017",number:38,title:"Explain query performance",question:"Explain query performance",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`EXPLAIN ANALYZE` shows the execution plan with actual timings and row counts, revealing sequential scans, join methods, and where indexes are (or aren't) used.\n\n\n```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE customer_id = 42;\n```\n```\n",tech:"sql",displayNumber:38,sortKey:2017},{id:"sql-2018",number:39,title:"When does an index NOT get used?",question:"When does an index NOT get used?",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"When you apply a function to the indexed column, use leading wildcards (`LIKE '%x'`), have low selectivity, mismatched types, or stale statistics. Keep predicates sargable and statistics fresh, or add a functional/partial index.\n\n\n```sql\n-- non-sargable: index on created_at unused\nWHERE DATE(created_at) = '2024-01-01'\n-- sargable rewrite:\nWHERE created_at >= '2024-01-01' AND created_at < '2024-01-02'\n```\n```\n",tech:"sql",displayNumber:39,sortKey:2018},{id:"sql-2019",number:40,title:"Optimize a slow aggregation query",question:"Optimize a slow aggregation query",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Index filter/group/join columns, filter early, avoid `SELECT *`, pre-aggregate in a CTE, consider a covering index or materialized view, and check the plan with `EXPLAIN ANALYZE` to confirm the fix.\n\n\n```sql\nCREATE INDEX idx_orders_cust_date ON orders(customer_id, order_date);\nCREATE MATERIALIZED VIEW cust_totals AS\n  SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id;\n```\n```\n",tech:"sql",displayNumber:40,sortKey:2019}];function Wm({text:e}){const t=String(e||"").split(/```(\w+)?\n([\s\S]*?)```/g);return p.jsx("div",{className:"markdown prose prose-slate dark:prose-invert max-w-none",children:t.map((n,a)=>{if(a%3===2)return p.jsx("pre",{className:"bg-slate-900 rounded-lg p-4 overflow-x-auto border border-slate-700/50",children:p.jsx("code",{className:"text-slate-100 text-sm leading-relaxed font-mono",children:Km(n.trim())})},a);if(a%3===1)return null;const r=n.split(`
`),s=[];for(let i=0;i<r.length;i++){const o=r[i],l=`${a}-${i}`,u=o.match(/!\[([^\]]*)\]\(([^)"\s]+)(?:\s+"([^"]*)")?\)/);if(u){const[,h,v,w]=u;s.push(p.jsxs("div",{className:"my-4",children:[p.jsx("img",{src:v,alt:h,title:w,className:"max-w-full h-auto rounded-lg border border-slate-200 dark:border-slate-700",loading:"lazy"}),h&&p.jsx("p",{className:"text-xs text-slate-500 dark:text-slate-400 text-center mt-1",children:h})]},l));continue}const f=h=>h&&h.includes("|")&&h.split("|").map(v=>v.trim()).filter(Boolean).length>1,y=h=>h&&/^\s*\|?[\s:|-]*-[\s:|-]*\|?\s*$/.test(h)&&h.includes("-");if(f(o)){const h=[];let v=i;for(;v<r.length&&(f(r[v])||y(r[v]));)y(r[v])||h.push(r[v].split("|").map(T=>T.trim()).filter((T,d,c)=>!(T===""&&(d===0||d===c.length-1)))),v++;const[w,...k]=h;s.push(p.jsx("div",{className:"overflow-x-auto my-4 rounded-lg border border-slate-200 dark:border-slate-700",children:p.jsxs("table",{className:"min-w-full text-sm border-collapse",children:[p.jsx("thead",{children:p.jsx("tr",{className:"bg-slate-100 dark:bg-slate-800",children:w.map((T,d)=>p.jsx("th",{className:"px-4 py-2.5 text-left font-semibold text-slate-900 dark:text-slate-100 border-b border-slate-200 dark:border-slate-700",children:vn(T)},d))})}),p.jsx("tbody",{children:k.map((T,d)=>p.jsx("tr",{className:"even:bg-slate-50 dark:even:bg-slate-800/40",children:T.map((c,m)=>p.jsx("td",{className:"px-4 py-2.5 align-top text-slate-700 dark:text-slate-300 border-b border-slate-100 dark:border-slate-800",children:vn(c)},m))},d))})]})},l)),i=v-1;continue}if(o.trim()!=="```"){if(!o.trim()){s.push(p.jsx("br",{},l));continue}if(o.startsWith("### ")){s.push(p.jsx("h3",{className:"text-lg font-semibold text-slate-900 dark:text-slate-100 mt-4 mb-2",children:o.slice(4)},l));continue}if(o.startsWith("## ")){s.push(p.jsx("h2",{className:"text-xl font-bold text-slate-900 dark:text-slate-100 mt-6 mb-3",children:o.slice(3)},l));continue}if(o.startsWith("# ")){s.push(p.jsx("h1",{className:"text-2xl font-bold text-slate-900 dark:text-slate-100 mt-6 mb-4",children:o.slice(2)},l));continue}if(o.startsWith("> ")){s.push(p.jsx("blockquote",{className:"border-l-4 border-blue-500 pl-4 italic text-slate-700 dark:text-slate-300 my-3",children:o.slice(2)},l));continue}if(o.startsWith("- ")||o.startsWith("* ")){s.push(p.jsx("li",{className:"ml-6 list-disc text-slate-700 dark:text-slate-300 leading-relaxed",children:vn(o.slice(2))},l));continue}if(o.match(/^\d+\.\s/)){s.push(p.jsx("li",{className:"ml-6 list-decimal text-slate-700 dark:text-slate-300 leading-relaxed",children:vn(o.replace(/^\d+\.\s/,""))},l));continue}s.push(p.jsx("p",{className:"text-slate-700 dark:text-slate-300 leading-relaxed my-2",children:vn(o)},l))}}return s})})}const Hm=new Set("abstract assert boolean break byte case catch char class const continue default do double else enum extends final finally float for goto if implements import instanceof int interface long native new package private protected public return short static strictfp super switch synchronized this throw throws transient try void volatile while var let function async await yield typeof instanceof of in delete null true false undefined NaN".split(" "));function Km(e){const t=/(\/\/[^\n]*|\/\*[\s\S]*?\*\/)|("(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|`(?:\\.|[^`\\])*`)|(@\w+)|(\b\d[\d.xXa-fA-F_]*\b)|(\b[A-Za-z_$][\w$]*\b)/g,n=[];let a=0,r,s=0;for(;r=t.exec(e);){r.index>a&&n.push(e.slice(a,r.index));const i=s++;if(r[1])n.push(p.jsx("span",{className:"text-slate-500 italic",children:r[1]},i));else if(r[2])n.push(p.jsx("span",{className:"text-emerald-400",children:r[2]},i));else if(r[3])n.push(p.jsx("span",{className:"text-amber-400",children:r[3]},i));else if(r[4])n.push(p.jsx("span",{className:"text-orange-400",children:r[4]},i));else if(r[5]){const o=r[5];Hm.has(o)?n.push(p.jsx("span",{className:"text-purple-400 font-medium",children:o},i)):/^[A-Z]/.test(o)?n.push(p.jsx("span",{className:"text-cyan-300",children:o},i)):e[t.lastIndex]==="("?n.push(p.jsx("span",{className:"text-blue-400",children:o},i)):n.push(o)}a=t.lastIndex}return a<e.length&&n.push(e.slice(a)),n}function vn(e){return e.split(/(`[^`]+`|\*\*[^*]+\*\*|\*[^*]+\*|~~[^~]+~~)/g).map((n,a)=>n.startsWith("`")&&n.endsWith("`")?p.jsx("code",{className:"bg-slate-100 dark:bg-slate-800 px-1.5 py-0.5 rounded text-sm font-mono text-slate-900 dark:text-slate-100",children:n.slice(1,-1)},a):n.startsWith("**")&&n.endsWith("**")?p.jsx("strong",{className:"font-semibold text-slate-900 dark:text-slate-100",children:n.slice(2,-2)},a):n.startsWith("*")&&n.endsWith("*")?p.jsx("em",{className:"italic text-slate-700 dark:text-slate-300",children:n.slice(1,-1)},a):n.startsWith("~~")&&n.endsWith("~~")?p.jsx("del",{className:"line-through text-slate-500 dark:text-slate-400",children:n.slice(2,-2)},a):p.jsx("span",{children:n},a))}function Bm({question:e,isActive:t,onClick:n}){return p.jsxs("button",{className:`question-link w-full text-left px-3 py-2.5 rounded-lg transition-all duration-150 ${t?"bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 border-l-4 border-blue-500":"text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800/50"}`,onClick:n,children:[p.jsxs("div",{className:"flex items-start gap-2",children:[p.jsxs("span",{className:"qnum flex-shrink-0 text-xs font-mono font-medium text-slate-500 dark:text-slate-400 mt-0.5",children:["Q",e.displayNumber]}),p.jsx("span",{className:"text-sm leading-relaxed truncate",children:e.question})]}),p.jsxs("div",{className:"flex items-center gap-1.5 mt-1.5",children:[p.jsx("span",{className:`badge px-2 py-0.5 text-xs rounded-full ${e.tech==="java"?"bg-orange-100 text-orange-700 dark:bg-orange-900/30 dark:text-orange-300":e.tech==="node"?"bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300":e.tech==="sql"?"bg-purple-100 text-purple-700 dark:bg-purple-900/30 dark:text-purple-300":"bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30 dark:text-cyan-300"}`,children:e.tech.toUpperCase()}),p.jsx("span",{className:"badge px-2 py-0.5 text-xs rounded-full bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-400",children:e.difficulty})]})]})}function Dm({value:e,onChange:t,placeholder:n}){return p.jsxs("div",{className:"relative",children:[p.jsx("svg",{className:"absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"})}),p.jsx("input",{type:"text",className:"search w-full pl-10 pr-10 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 placeholder-slate-500 dark:placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all",placeholder:n,value:e,onChange:a=>t(a.target.value),"aria-label":"Search questions"}),e&&p.jsx("button",{onClick:()=>t(""),className:"absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-700 dark:hover:text-slate-300 transition-colors","aria-label":"Clear search",children:p.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M6 18L18 6M6 6l12 12"})})})]})}function Jo({value:e,onChange:t}){const n=[{value:"all",label:"All",active:"bg-blue-500 text-white shadow-sm shadow-blue-500/30",icon:p.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"})})},{value:"java",label:"Java",active:"bg-orange-500 text-white shadow-sm shadow-orange-500/30",icon:p.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{d:"M18 8h1a3 3 0 010 6h-1.17A5 5 0 0113 18H8a5 5 0 01-5-5V7a1 1 0 011-1h13a1 1 0 011 1v1zm0 2v2h1a1 1 0 000-2h-1zM6 3a1 1 0 012 0v1a1 1 0 01-2 0V3zm4 0a1 1 0 012 0v1a1 1 0 01-2 0V3zM4 20h14v2H4v-2z"})})},{value:"react",label:"React",active:"bg-cyan-500 text-white shadow-sm shadow-cyan-500/30",icon:p.jsxs("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:[p.jsx("circle",{cx:"12",cy:"12",r:"1.6",fill:"currentColor",stroke:"none"}),p.jsx("ellipse",{cx:"12",cy:"12",rx:"10",ry:"4.2",strokeWidth:1.5}),p.jsx("ellipse",{cx:"12",cy:"12",rx:"10",ry:"4.2",strokeWidth:1.5,transform:"rotate(60 12 12)"}),p.jsx("ellipse",{cx:"12",cy:"12",rx:"10",ry:"4.2",strokeWidth:1.5,transform:"rotate(120 12 12)"})]})},{value:"node",label:"Node",active:"bg-green-500 text-white shadow-sm shadow-green-500/30",icon:p.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{d:"M12 2l8.66 5v10L12 22l-8.66-5V7L12 2zm0 4.2L7.5 8.8v6.4L12 17.8l4.5-2.6V8.8L12 6.2z"})})},{value:"sql",label:"SQL",active:"bg-purple-500 text-white shadow-sm shadow-purple-500/30",icon:p.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{d:"M12 2c-4.42 0-8 1.34-8 3v14c0 1.66 3.58 3 8 3s8-1.34 8-3V5c0-1.66-3.58-3-8-3zm6 17c0 .3-2.13 1.5-6 1.5S6 19.3 6 19v-2.23c1.5.77 3.72 1.23 6 1.23s4.5-.46 6-1.23V19zm0-4.5c0 .3-2.13 1.5-6 1.5s-6-1.2-6-1.5v-2.23c1.5.77 3.72 1.23 6 1.23s4.5-.46 6-1.23v2.23zM12 8.5C8.13 8.5 6 7.3 6 7s2.13-1.5 6-1.5S18 6.7 18 7s-2.13 1.5-6 1.5z"})})}];return p.jsx("div",{className:"segmented flex flex-wrap gap-1 bg-slate-100 dark:bg-slate-800/50 p-1 rounded-lg",role:"group","aria-label":"Filter by technology",children:n.map(a=>p.jsxs("button",{className:`flex flex-1 min-w-[68px] items-center justify-center gap-1.5 px-2.5 py-1.5 rounded-md text-sm font-medium transition-all duration-150 ${e===a.value?a.active:"text-slate-700 dark:text-slate-300 hover:text-slate-900 dark:hover:text-slate-100"}`,onClick:()=>t(a.value),"aria-pressed":e===a.value,children:[p.jsx("span",{className:"flex-shrink-0","aria-hidden":"true",children:a.icon}),p.jsx("span",{children:a.label})]},a.value))})}function Go({value:e,onChange:t,options:n,disabled:a}){return a||n.length===0?null:p.jsxs("select",{className:"select w-full px-3 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all cursor-pointer",value:e,onChange:r=>t(r.target.value),"aria-label":"Filter by category",children:[p.jsx("option",{value:"all",children:"All categories"}),n.map(r=>p.jsx("option",{value:r,children:r},r))]})}function _o({value:e,onChange:t}){const n=["Basic","Intermediate","Advanced","Experienced"];return p.jsxs("select",{className:"select w-full px-3 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all cursor-pointer",value:e,onChange:a=>t(a.target.value),"aria-label":"Filter by difficulty",children:[p.jsx("option",{value:"all",children:"All difficulties"}),n.map(a=>p.jsx("option",{value:a,children:a},a))]})}function Vo({count:e,total:t}){return p.jsx("div",{className:"count text-xs text-slate-500 dark:text-slate-400 flex items-center justify-between",children:p.jsxs("span",{children:[e," of ",t," questions"]})})}function od({questions:e,filtered:t,selectedId:n,query:a,setQuery:r,tech:s,setTech:i,category:o,setCategory:l,difficulty:u,setDifficulty:f,onSelect:y,onToggleDark:h,isDark:v,className:w="",isMobile:k=!1}){const T=$.useMemo(()=>[...new Set(e.filter(m=>s==="all"||m.tech===s).map(m=>m.category))].sort(),[s,e]),[d,c]=$.useState(!k);return p.jsxs("aside",{className:`sidebar sidebar-responsive h-full flex flex-col bg-white dark:bg-slate-950 border-r border-slate-200 dark:border-slate-800 ${k?"":"hidden lg:flex"} ${w}`,children:[p.jsx("div",{className:"brand p-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:p.jsxs("div",{className:"flex items-center justify-between gap-3",children:[p.jsxs("div",{className:"flex items-center gap-3 flex-1 min-w-0",children:[p.jsx("div",{className:"logo w-10 h-10 rounded-xl bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white font-bold text-lg shadow-lg flex-shrink-0",children:"JR"}),p.jsxs("div",{className:"min-w-0",children:[p.jsx("h1",{className:"text-xl font-bold text-slate-900 dark:text-slate-100 truncate",children:"Interview Reader"}),p.jsxs("p",{className:"text-xs text-slate-500 dark:text-slate-400 truncate",children:[e.length," Java, React, Node & SQL questions"]})]})]}),p.jsx("button",{onClick:h,className:"flex-shrink-0 p-2 rounded-lg bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors","aria-label":"Toggle dark mode","aria-pressed":v,children:v?p.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"})}):p.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"})})})]})}),p.jsx("div",{className:"p-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:p.jsx(Dm,{value:a,onChange:r,placeholder:"Search questions..."})}),k?p.jsxs("div",{className:"border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:[p.jsxs("button",{onClick:()=>c(!d),className:"w-full px-4 py-3 flex items-center justify-between text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors","aria-expanded":d,"aria-controls":"mobile-filters",children:[p.jsxs("span",{className:"flex items-center gap-2",children:[p.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"})}),"Filters"]}),p.jsx("svg",{className:`w-4 h-4 text-slate-500 transition-transform ${d?"rotate-180":""}`,fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M19 9l-7 7-7-7"})})]}),p.jsx("div",{id:"mobile-filters",className:`${d?"block":"hidden"}`,children:p.jsxs("div",{className:"p-4 space-y-4",children:[p.jsx(Jo,{value:s,onChange:i}),p.jsx(Go,{value:o,onChange:l,options:T,disabled:s==="all"&&T.length>50}),p.jsx(_o,{value:u,onChange:f}),p.jsx(Vo,{count:t.length,total:e.length})]})})]}):p.jsxs("div",{className:"p-4 space-y-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:[p.jsx(Jo,{value:s,onChange:i}),p.jsx(Go,{value:o,onChange:l,options:T,disabled:s==="all"&&T.length>50}),p.jsx(_o,{value:u,onChange:f}),p.jsx(Vo,{count:t.length,total:e.length})]}),p.jsx("nav",{className:"question-list flex-1 overflow-y-auto p-3 space-y-1","aria-label":"Question list",children:t.length===0?p.jsxs("div",{className:"text-center py-8 text-slate-500 dark:text-slate-400",children:[p.jsx("svg",{className:"w-12 h-12 mx-auto mb-3 opacity-50",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"})}),p.jsx("p",{children:"No questions match your filters"})]}):t.map(m=>p.jsx(Bm,{question:m,isActive:n===m.id,onClick:()=>y(m.id)},m.id))})]})}function Fm({question:e,questions:t,onNavigate:n}){if(!e)return p.jsx("main",{className:"reader flex-1 flex items-center justify-center bg-slate-50 dark:bg-slate-950 min-w-0",children:p.jsxs("div",{className:"text-center p-4 sm:p-8 w-full max-w-3xl mx-auto",children:[p.jsx("svg",{className:"w-16 h-16 mx-auto mb-4 text-slate-300 dark:text-slate-700",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"})}),p.jsx("h2",{className:"text-xl font-semibold text-slate-900 dark:text-slate-100 mb-2",children:"Select a question"}),p.jsx("p",{className:"text-slate-500 dark:text-slate-400",children:"Choose from the sidebar to start reading"})]})});const a=t.findIndex(i=>i.id===e.id),r=t[a-1],s=t[a+1];return p.jsxs("main",{className:"reader flex-1 h-screen overflow-hidden bg-slate-50 dark:bg-slate-950 flex flex-col min-w-0",children:[p.jsxs("article",{className:"paper flex-1 overflow-y-auto p-4 sm:p-6 lg:p-8 max-w-3xl mx-auto w-full min-w-0",children:[p.jsxs("div",{className:"lg:hidden mb-4 flex items-center justify-between px-2",children:[p.jsxs("button",{disabled:!r,onClick:()=>n(r.id),className:"px-3 py-1.5 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-1.5","aria-label":"Previous question",children:[p.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M15 19l-7-7 7-7"})}),p.jsx("span",{className:"hidden sm:inline",children:"Previous"})]}),p.jsxs("div",{className:"text-xs text-slate-500 dark:text-slate-400 flex-1 text-center px-2",children:["Q",e.displayNumber," / ",t.length]}),p.jsxs("button",{disabled:!s,onClick:()=>n(s.id),className:"px-3 py-1.5 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-1.5","aria-label":"Next question",children:[p.jsx("span",{className:"hidden sm:inline",children:"Next"}),p.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M9 5l7 7-7 7"})})]})]}),p.jsxs("header",{className:"mb-6 pb-4 border-b border-slate-200 dark:border-slate-800",children:[p.jsxs("div",{className:"badges flex flex-wrap gap-2 mb-4",children:[p.jsx("span",{className:`badge px-3 py-1 text-xs font-medium rounded-full ${e.tech==="java"?"bg-orange-100 text-orange-700 dark:bg-orange-900/30 dark:text-orange-300":e.tech==="node"?"bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300":e.tech==="sql"?"bg-purple-100 text-purple-700 dark:bg-purple-900/30 dark:text-purple-300":"bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30 dark:text-cyan-300"}`,children:e.tech.toUpperCase()}),p.jsx("span",{className:"badge px-3 py-1 text-xs font-medium rounded-full bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-300",children:e.category}),p.jsx("span",{className:`badge px-3 py-1 text-xs font-medium rounded-full ${e.difficulty==="Basic"?"bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300":e.difficulty==="Intermediate"?"bg-yellow-100 text-yellow-700 dark:bg-yellow-900/30 dark:text-yellow-300":"bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-300"}`,children:e.difficulty})]}),p.jsxs("h2",{className:"question-title text-xl sm:text-2xl lg:text-3xl font-bold text-slate-900 dark:text-slate-100 leading-tight",children:["Q",e.displayNumber,". ",e.question]})]}),p.jsx("div",{className:"answer-content",children:p.jsx(Wm,{text:e.answer})})]}),p.jsx("footer",{className:"hidden lg:block pager flex-shrink-0 border-t border-slate-200 dark:border-slate-800 bg-slate-50 dark:bg-slate-950",children:p.jsxs("div",{className:"max-w-3xl mx-auto w-full px-6 lg:px-8 py-4 flex items-center justify-between",children:[p.jsxs("button",{disabled:!r,onClick:()=>n(r.id),className:"px-4 py-2 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-2",children:[p.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M15 19l-7-7 7-7"})}),"Previous"]}),p.jsxs("div",{className:"text-sm text-slate-500 dark:text-slate-400",children:["Question ",e.displayNumber," of ",t.length]}),p.jsxs("button",{disabled:!s,onClick:()=>n(s.id),className:"px-4 py-2 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-2",children:["Next",p.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M9 5l7 7-7 7"})})]})]})})]})}function Um({isOpen:e,onToggle:t}){return p.jsx("button",{className:"lg:hidden fixed bottom-4 right-4 z-50 w-12 h-12 rounded-full bg-blue-600 text-white shadow-lg flex items-center justify-center hover:bg-blue-700 transition-colors touch-target",onClick:t,"aria-label":e?"Close menu":"Open menu","aria-expanded":e,children:e?p.jsx("svg",{className:"w-6 h-6",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M6 18L18 6M6 6l12 12"})}):p.jsx("svg",{className:"w-6 h-6",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:p.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M4 6h16M4 12h16M4 18h16"})})})}function zm({isOpen:e,onClose:t,...n}){return e?p.jsxs(p.Fragment,{children:[p.jsx("div",{className:"fixed inset-0 bg-black/50 z-40 lg:hidden",onClick:t,"aria-hidden":"true"}),p.jsx("aside",{className:"fixed inset-y-0 left-0 z-50 w-80 max-w-[85vw] bg-white dark:bg-slate-950 shadow-xl lg:hidden transform transition-transform duration-300 ease-out translate-x-0",children:p.jsx(od,{...n,isMobile:!0})})]}):null}function Jm(){var g;const[e,t]=$.useState((g=Pt[0])==null?void 0:g.id),[n,a]=$.useState(""),[r,s]=$.useState("all"),[i,o]=$.useState("all"),[l,u]=$.useState("all"),[f,y]=$.useState(!1),[h,v]=$.useState(()=>{if(typeof window<"u"){const S=localStorage.getItem("theme");return S?S==="dark":window.matchMedia("(prefers-color-scheme: dark)").matches}return!1}),w=$.useMemo(()=>{const S=n.trim().toLowerCase();return Pt.filter(x=>{const E=r==="all"||x.tech===r,R=i==="all"||x.category===i,K=l==="all"||x.difficulty===l,A=`q${x.displayNumber} ${x.question} ${x.answer} ${x.category}`.toLowerCase();return E&&R&&K&&(!S||A.includes(S))})},[n,r,i,l]),k=w.find(S=>S.id===e)||w[0]||Pt[0],T=$.useCallback(()=>{v(S=>{const x=!S;return localStorage.setItem("theme",x?"dark":"light"),document.documentElement.classList.toggle("dark",x),x})},[]),d=$.useCallback(S=>{t(S),y(!1),window.innerWidth<1024&&window.scrollTo({top:0,behavior:"smooth"})},[]),c=$.useCallback(S=>{t(S),y(!1),window.innerWidth<1024&&window.scrollTo({top:0,behavior:"smooth"})},[]),m=$.useCallback(S=>{s(S),o("all")},[]);return p.jsxs("div",{className:"app-shell h-screen overflow-hidden bg-slate-50 dark:bg-slate-950 flex",children:[p.jsx(od,{questions:Pt,filtered:w,selectedId:e,query:n,setQuery:a,tech:r,setTech:m,category:i,setCategory:o,difficulty:l,setDifficulty:u,onSelect:d,onToggleDark:T,isDark:h}),f&&p.jsx("div",{className:"lg:hidden w-80 max-w-[85vw]","aria-hidden":"true"}),p.jsx(zm,{isOpen:f,onClose:()=>y(!1),questions:Pt,filtered:w,selectedId:e,query:n,setQuery:a,tech:r,setTech:m,category:i,setCategory:o,difficulty:l,setDifficulty:u,onSelect:d,onToggleDark:T,isDark:h}),p.jsx(Um,{isOpen:f,onToggle:()=>y(!f)}),p.jsx(Fm,{question:k,questions:Pt,onNavigate:c})]})}_r.createRoot(document.getElementById("root")).render(p.jsx(jd.StrictMode,{children:p.jsx(Jm,{})}));
