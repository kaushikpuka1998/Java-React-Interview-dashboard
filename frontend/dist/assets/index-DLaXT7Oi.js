(function(){const t=document.createElement("link").relList;if(t&&t.supports&&t.supports("modulepreload"))return;for(const r of document.querySelectorAll('link[rel="modulepreload"]'))a(r);new MutationObserver(r=>{for(const s of r)if(s.type==="childList")for(const i of s.addedNodes)i.tagName==="LINK"&&i.rel==="modulepreload"&&a(i)}).observe(document,{childList:!0,subtree:!0});function n(r){const s={};return r.integrity&&(s.integrity=r.integrity),r.referrerPolicy&&(s.referrerPolicy=r.referrerPolicy),r.crossOrigin==="use-credentials"?s.credentials="include":r.crossOrigin==="anonymous"?s.credentials="omit":s.credentials="same-origin",s}function a(r){if(r.ep)return;r.ep=!0;const s=n(r);fetch(r.href,s)}})();function ld(e){return e&&e.__esModule&&Object.prototype.hasOwnProperty.call(e,"default")?e.default:e}var Xo={exports:{}},ar={},Yo={exports:{}},P={};/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Xn=Symbol.for("react.element"),dd=Symbol.for("react.portal"),ud=Symbol.for("react.fragment"),pd=Symbol.for("react.strict_mode"),md=Symbol.for("react.profiler"),hd=Symbol.for("react.provider"),yd=Symbol.for("react.context"),fd=Symbol.for("react.forward_ref"),gd=Symbol.for("react.suspense"),bd=Symbol.for("react.memo"),vd=Symbol.for("react.lazy"),Li=Symbol.iterator;function wd(e){return e===null||typeof e!="object"?null:(e=Li&&e[Li]||e["@@iterator"],typeof e=="function"?e:null)}var $o={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},Zo=Object.assign,ec={};function on(e,t,n){this.props=e,this.context=t,this.refs=ec,this.updater=n||$o}on.prototype.isReactComponent={};on.prototype.setState=function(e,t){if(typeof e!="object"&&typeof e!="function"&&e!=null)throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");this.updater.enqueueSetState(this,e,t,"setState")};on.prototype.forceUpdate=function(e){this.updater.enqueueForceUpdate(this,e,"forceUpdate")};function tc(){}tc.prototype=on.prototype;function Fs(e,t,n){this.props=e,this.context=t,this.refs=ec,this.updater=n||$o}var Us=Fs.prototype=new tc;Us.constructor=Fs;Zo(Us,on.prototype);Us.isPureReactComponent=!0;var Ki=Array.isArray,nc=Object.prototype.hasOwnProperty,zs={current:null},ac={key:!0,ref:!0,__self:!0,__source:!0};function rc(e,t,n){var a,r={},s=null,i=null;if(t!=null)for(a in t.ref!==void 0&&(i=t.ref),t.key!==void 0&&(s=""+t.key),t)nc.call(t,a)&&!ac.hasOwnProperty(a)&&(r[a]=t[a]);var o=arguments.length-2;if(o===1)r.children=n;else if(1<o){for(var c=Array(o),p=0;p<o;p++)c[p]=arguments[p+2];r.children=c}if(e&&e.defaultProps)for(a in o=e.defaultProps,o)r[a]===void 0&&(r[a]=o[a]);return{$$typeof:Xn,type:e,key:s,ref:i,props:r,_owner:zs.current}}function kd(e,t){return{$$typeof:Xn,type:e.type,key:t,ref:e.ref,props:e.props,_owner:e._owner}}function Js(e){return typeof e=="object"&&e!==null&&e.$$typeof===Xn}function Sd(e){var t={"=":"=0",":":"=2"};return"$"+e.replace(/[=:]/g,function(n){return t[n]})}var Mi=/\/+/g;function Cr(e,t){return typeof e=="object"&&e!==null&&e.key!=null?Sd(""+e.key):t.toString(36)}function ka(e,t,n,a,r){var s=typeof e;(s==="undefined"||s==="boolean")&&(e=null);var i=!1;if(e===null)i=!0;else switch(s){case"string":case"number":i=!0;break;case"object":switch(e.$$typeof){case Xn:case dd:i=!0}}if(i)return i=e,r=r(i),e=a===""?"."+Cr(i,0):a,Ki(r)?(n="",e!=null&&(n=e.replace(Mi,"$&/")+"/"),ka(r,t,n,"",function(p){return p})):r!=null&&(Js(r)&&(r=kd(r,n+(!r.key||i&&i.key===r.key?"":(""+r.key).replace(Mi,"$&/")+"/")+e)),t.push(r)),1;if(i=0,a=a===""?".":a+":",Ki(e))for(var o=0;o<e.length;o++){s=e[o];var c=a+Cr(s,o);i+=ka(s,t,n,c,r)}else if(c=wd(e),typeof c=="function")for(e=c.call(e),o=0;!(s=e.next()).done;)s=s.value,c=a+Cr(s,o++),i+=ka(s,t,n,c,r);else if(s==="object")throw t=String(e),Error("Objects are not valid as a React child (found: "+(t==="[object Object]"?"object with keys {"+Object.keys(e).join(", ")+"}":t)+"). If you meant to render a collection of children, use an array instead.");return i}function ra(e,t,n){if(e==null)return e;var a=[],r=0;return ka(e,a,"","",function(s){return t.call(n,s,r++)}),a}function Cd(e){if(e._status===-1){var t=e._result;t=t(),t.then(function(n){(e._status===0||e._status===-1)&&(e._status=1,e._result=n)},function(n){(e._status===0||e._status===-1)&&(e._status=2,e._result=n)}),e._status===-1&&(e._status=0,e._result=t)}if(e._status===1)return e._result.default;throw e._result}var le={current:null},Sa={transition:null},xd={ReactCurrentDispatcher:le,ReactCurrentBatchConfig:Sa,ReactCurrentOwner:zs};function sc(){throw Error("act(...) is not supported in production builds of React.")}P.Children={map:ra,forEach:function(e,t,n){ra(e,function(){t.apply(this,arguments)},n)},count:function(e){var t=0;return ra(e,function(){t++}),t},toArray:function(e){return ra(e,function(t){return t})||[]},only:function(e){if(!Js(e))throw Error("React.Children.only expected to receive a single React element child.");return e}};P.Component=on;P.Fragment=ud;P.Profiler=md;P.PureComponent=Fs;P.StrictMode=pd;P.Suspense=gd;P.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=xd;P.act=sc;P.cloneElement=function(e,t,n){if(e==null)throw Error("React.cloneElement(...): The argument must be a React element, but you passed "+e+".");var a=Zo({},e.props),r=e.key,s=e.ref,i=e._owner;if(t!=null){if(t.ref!==void 0&&(s=t.ref,i=zs.current),t.key!==void 0&&(r=""+t.key),e.type&&e.type.defaultProps)var o=e.type.defaultProps;for(c in t)nc.call(t,c)&&!ac.hasOwnProperty(c)&&(a[c]=t[c]===void 0&&o!==void 0?o[c]:t[c])}var c=arguments.length-2;if(c===1)a.children=n;else if(1<c){o=Array(c);for(var p=0;p<c;p++)o[p]=arguments[p+2];a.children=o}return{$$typeof:Xn,type:e.type,key:r,ref:s,props:a,_owner:i}};P.createContext=function(e){return e={$$typeof:yd,_currentValue:e,_currentValue2:e,_threadCount:0,Provider:null,Consumer:null,_defaultValue:null,_globalName:null},e.Provider={$$typeof:hd,_context:e},e.Consumer=e};P.createElement=rc;P.createFactory=function(e){var t=rc.bind(null,e);return t.type=e,t};P.createRef=function(){return{current:null}};P.forwardRef=function(e){return{$$typeof:fd,render:e}};P.isValidElement=Js;P.lazy=function(e){return{$$typeof:vd,_payload:{_status:-1,_result:e},_init:Cd}};P.memo=function(e,t){return{$$typeof:bd,type:e,compare:t===void 0?null:t}};P.startTransition=function(e){var t=Sa.transition;Sa.transition={};try{e()}finally{Sa.transition=t}};P.unstable_act=sc;P.useCallback=function(e,t){return le.current.useCallback(e,t)};P.useContext=function(e){return le.current.useContext(e)};P.useDebugValue=function(){};P.useDeferredValue=function(e){return le.current.useDeferredValue(e)};P.useEffect=function(e,t){return le.current.useEffect(e,t)};P.useId=function(){return le.current.useId()};P.useImperativeHandle=function(e,t,n){return le.current.useImperativeHandle(e,t,n)};P.useInsertionEffect=function(e,t){return le.current.useInsertionEffect(e,t)};P.useLayoutEffect=function(e,t){return le.current.useLayoutEffect(e,t)};P.useMemo=function(e,t){return le.current.useMemo(e,t)};P.useReducer=function(e,t,n){return le.current.useReducer(e,t,n)};P.useRef=function(e){return le.current.useRef(e)};P.useState=function(e){return le.current.useState(e)};P.useSyncExternalStore=function(e,t,n){return le.current.useSyncExternalStore(e,t,n)};P.useTransition=function(){return le.current.useTransition()};P.version="18.3.1";Yo.exports=P;var G=Yo.exports;const Ed=ld(G);/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Rd=G,Td=Symbol.for("react.element"),Nd=Symbol.for("react.fragment"),Ad=Object.prototype.hasOwnProperty,qd=Rd.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner,jd={key:!0,ref:!0,__self:!0,__source:!0};function ic(e,t,n){var a,r={},s=null,i=null;n!==void 0&&(s=""+n),t.key!==void 0&&(s=""+t.key),t.ref!==void 0&&(i=t.ref);for(a in t)Ad.call(t,a)&&!jd.hasOwnProperty(a)&&(r[a]=t[a]);if(e&&e.defaultProps)for(a in t=e.defaultProps,t)r[a]===void 0&&(r[a]=t[a]);return{$$typeof:Td,type:e,key:s,ref:i,props:r,_owner:qd.current}}ar.Fragment=Nd;ar.jsx=ic;ar.jsxs=ic;Xo.exports=ar;var d=Xo.exports,Qr={},oc={exports:{}},we={},cc={exports:{}},lc={};/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */(function(e){function t(E,q){var I=E.length;E.push(q);e:for(;0<I;){var z=I-1>>>1,X=E[z];if(0<r(X,q))E[z]=q,E[I]=X,I=z;else break e}}function n(E){return E.length===0?null:E[0]}function a(E){if(E.length===0)return null;var q=E[0],I=E.pop();if(I!==q){E[0]=I;e:for(var z=0,X=E.length,na=X>>>1;z<na;){var bt=2*(z+1)-1,Sr=E[bt],vt=bt+1,aa=E[vt];if(0>r(Sr,I))vt<X&&0>r(aa,Sr)?(E[z]=aa,E[vt]=I,z=vt):(E[z]=Sr,E[bt]=I,z=bt);else if(vt<X&&0>r(aa,I))E[z]=aa,E[vt]=I,z=vt;else break e}}return q}function r(E,q){var I=E.sortIndex-q.sortIndex;return I!==0?I:E.id-q.id}if(typeof performance=="object"&&typeof performance.now=="function"){var s=performance;e.unstable_now=function(){return s.now()}}else{var i=Date,o=i.now();e.unstable_now=function(){return i.now()-o}}var c=[],p=[],f=1,y=null,h=3,v=!1,w=!1,k=!1,j=typeof setTimeout=="function"?setTimeout:null,u=typeof clearTimeout=="function"?clearTimeout:null,l=typeof setImmediate<"u"?setImmediate:null;typeof navigator<"u"&&navigator.scheduling!==void 0&&navigator.scheduling.isInputPending!==void 0&&navigator.scheduling.isInputPending.bind(navigator.scheduling);function m(E){for(var q=n(p);q!==null;){if(q.callback===null)a(p);else if(q.startTime<=E)a(p),q.sortIndex=q.expirationTime,t(c,q);else break;q=n(p)}}function g(E){if(k=!1,m(E),!w)if(n(c)!==null)w=!0,wr(C);else{var q=n(p);q!==null&&kr(g,q.startTime-E)}}function C(E,q){w=!1,k&&(k=!1,u(S),S=-1),v=!0;var I=h;try{for(m(q),y=n(c);y!==null&&(!(y.expirationTime>q)||E&&!ne());){var z=y.callback;if(typeof z=="function"){y.callback=null,h=y.priorityLevel;var X=z(y.expirationTime<=q);q=e.unstable_now(),typeof X=="function"?y.callback=X:y===n(c)&&a(c),m(q)}else a(c);y=n(c)}if(y!==null)var na=!0;else{var bt=n(p);bt!==null&&kr(g,bt.startTime-q),na=!1}return na}finally{y=null,h=I,v=!1}}var T=!1,R=null,S=-1,N=5,A=-1;function ne(){return!(e.unstable_now()-A<N)}function De(){if(R!==null){var E=e.unstable_now();A=E;var q=!0;try{q=R(!0,E)}finally{q?Be():(T=!1,R=null)}}else T=!1}var Be;if(typeof l=="function")Be=function(){l(De)};else if(typeof MessageChannel<"u"){var ta=new MessageChannel,vr=ta.port2;ta.port1.onmessage=De,Be=function(){vr.postMessage(null)}}else Be=function(){j(De,0)};function wr(E){R=E,T||(T=!0,Be())}function kr(E,q){S=j(function(){E(e.unstable_now())},q)}e.unstable_IdlePriority=5,e.unstable_ImmediatePriority=1,e.unstable_LowPriority=4,e.unstable_NormalPriority=3,e.unstable_Profiling=null,e.unstable_UserBlockingPriority=2,e.unstable_cancelCallback=function(E){E.callback=null},e.unstable_continueExecution=function(){w||v||(w=!0,wr(C))},e.unstable_forceFrameRate=function(E){0>E||125<E?console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"):N=0<E?Math.floor(1e3/E):5},e.unstable_getCurrentPriorityLevel=function(){return h},e.unstable_getFirstCallbackNode=function(){return n(c)},e.unstable_next=function(E){switch(h){case 1:case 2:case 3:var q=3;break;default:q=h}var I=h;h=q;try{return E()}finally{h=I}},e.unstable_pauseExecution=function(){},e.unstable_requestPaint=function(){},e.unstable_runWithPriority=function(E,q){switch(E){case 1:case 2:case 3:case 4:case 5:break;default:E=3}var I=h;h=E;try{return q()}finally{h=I}},e.unstable_scheduleCallback=function(E,q,I){var z=e.unstable_now();switch(typeof I=="object"&&I!==null?(I=I.delay,I=typeof I=="number"&&0<I?z+I:z):I=z,E){case 1:var X=-1;break;case 2:X=250;break;case 5:X=1073741823;break;case 4:X=1e4;break;default:X=5e3}return X=I+X,E={id:f++,callback:q,priorityLevel:E,startTime:I,expirationTime:X,sortIndex:-1},I>z?(E.sortIndex=I,t(p,E),n(c)===null&&E===n(p)&&(k?(u(S),S=-1):k=!0,kr(g,I-z))):(E.sortIndex=X,t(c,E),w||v||(w=!0,wr(C))),E},e.unstable_shouldYield=ne,e.unstable_wrapCallback=function(E){var q=h;return function(){var I=h;h=q;try{return E.apply(this,arguments)}finally{h=I}}}})(lc);cc.exports=lc;var Id=cc.exports;/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */var Pd=G,ve=Id;function b(e){for(var t="https://reactjs.org/docs/error-decoder.html?invariant="+e,n=1;n<arguments.length;n++)t+="&args[]="+encodeURIComponent(arguments[n]);return"Minified React error #"+e+"; visit "+t+" for the full message or use the non-minified dev environment for full errors and additional helpful warnings."}var dc=new Set,Pn={};function It(e,t){Zt(e,t),Zt(e+"Capture",t)}function Zt(e,t){for(Pn[e]=t,e=0;e<t.length;e++)dc.add(t[e])}var Ve=!(typeof window>"u"||typeof window.document>"u"||typeof window.document.createElement>"u"),Xr=Object.prototype.hasOwnProperty,Od=/^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/,Wi={},Hi={};function Dd(e){return Xr.call(Hi,e)?!0:Xr.call(Wi,e)?!1:Od.test(e)?Hi[e]=!0:(Wi[e]=!0,!1)}function Ld(e,t,n,a){if(n!==null&&n.type===0)return!1;switch(typeof t){case"function":case"symbol":return!0;case"boolean":return a?!1:n!==null?!n.acceptsBooleans:(e=e.toLowerCase().slice(0,5),e!=="data-"&&e!=="aria-");default:return!1}}function Kd(e,t,n,a){if(t===null||typeof t>"u"||Ld(e,t,n,a))return!0;if(a)return!1;if(n!==null)switch(n.type){case 3:return!t;case 4:return t===!1;case 5:return isNaN(t);case 6:return isNaN(t)||1>t}return!1}function de(e,t,n,a,r,s,i){this.acceptsBooleans=t===2||t===3||t===4,this.attributeName=a,this.attributeNamespace=r,this.mustUseProperty=n,this.propertyName=e,this.type=t,this.sanitizeURL=s,this.removeEmptyString=i}var te={};"children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(e){te[e]=new de(e,0,!1,e,null,!1,!1)});[["acceptCharset","accept-charset"],["className","class"],["htmlFor","for"],["httpEquiv","http-equiv"]].forEach(function(e){var t=e[0];te[t]=new de(t,1,!1,e[1],null,!1,!1)});["contentEditable","draggable","spellCheck","value"].forEach(function(e){te[e]=new de(e,2,!1,e.toLowerCase(),null,!1,!1)});["autoReverse","externalResourcesRequired","focusable","preserveAlpha"].forEach(function(e){te[e]=new de(e,2,!1,e,null,!1,!1)});"allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(e){te[e]=new de(e,3,!1,e.toLowerCase(),null,!1,!1)});["checked","multiple","muted","selected"].forEach(function(e){te[e]=new de(e,3,!0,e,null,!1,!1)});["capture","download"].forEach(function(e){te[e]=new de(e,4,!1,e,null,!1,!1)});["cols","rows","size","span"].forEach(function(e){te[e]=new de(e,6,!1,e,null,!1,!1)});["rowSpan","start"].forEach(function(e){te[e]=new de(e,5,!1,e.toLowerCase(),null,!1,!1)});var Gs=/[\-:]([a-z])/g;function Vs(e){return e[1].toUpperCase()}"accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(e){var t=e.replace(Gs,Vs);te[t]=new de(t,1,!1,e,null,!1,!1)});"xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(e){var t=e.replace(Gs,Vs);te[t]=new de(t,1,!1,e,"http://www.w3.org/1999/xlink",!1,!1)});["xml:base","xml:lang","xml:space"].forEach(function(e){var t=e.replace(Gs,Vs);te[t]=new de(t,1,!1,e,"http://www.w3.org/XML/1998/namespace",!1,!1)});["tabIndex","crossOrigin"].forEach(function(e){te[e]=new de(e,1,!1,e.toLowerCase(),null,!1,!1)});te.xlinkHref=new de("xlinkHref",1,!1,"xlink:href","http://www.w3.org/1999/xlink",!0,!1);["src","href","action","formAction"].forEach(function(e){te[e]=new de(e,1,!1,e.toLowerCase(),null,!0,!0)});function _s(e,t,n,a){var r=te.hasOwnProperty(t)?te[t]:null;(r!==null?r.type!==0:a||!(2<t.length)||t[0]!=="o"&&t[0]!=="O"||t[1]!=="n"&&t[1]!=="N")&&(Kd(t,n,r,a)&&(n=null),a||r===null?Dd(t)&&(n===null?e.removeAttribute(t):e.setAttribute(t,""+n)):r.mustUseProperty?e[r.propertyName]=n===null?r.type===3?!1:"":n:(t=r.attributeName,a=r.attributeNamespace,n===null?e.removeAttribute(t):(r=r.type,n=r===3||r===4&&n===!0?"":""+n,a?e.setAttributeNS(a,t,n):e.setAttribute(t,n))))}var Ye=Pd.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED,sa=Symbol.for("react.element"),Dt=Symbol.for("react.portal"),Lt=Symbol.for("react.fragment"),Qs=Symbol.for("react.strict_mode"),Yr=Symbol.for("react.profiler"),uc=Symbol.for("react.provider"),pc=Symbol.for("react.context"),Xs=Symbol.for("react.forward_ref"),$r=Symbol.for("react.suspense"),Zr=Symbol.for("react.suspense_list"),Ys=Symbol.for("react.memo"),Ze=Symbol.for("react.lazy"),mc=Symbol.for("react.offscreen"),Bi=Symbol.iterator;function dn(e){return e===null||typeof e!="object"?null:(e=Bi&&e[Bi]||e["@@iterator"],typeof e=="function"?e:null)}var F=Object.assign,xr;function wn(e){if(xr===void 0)try{throw Error()}catch(n){var t=n.stack.trim().match(/\n( *(at )?)/);xr=t&&t[1]||""}return`
`+xr+e}var Er=!1;function Rr(e,t){if(!e||Er)return"";Er=!0;var n=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{if(t)if(t=function(){throw Error()},Object.defineProperty(t.prototype,"props",{set:function(){throw Error()}}),typeof Reflect=="object"&&Reflect.construct){try{Reflect.construct(t,[])}catch(p){var a=p}Reflect.construct(e,[],t)}else{try{t.call()}catch(p){a=p}e.call(t.prototype)}else{try{throw Error()}catch(p){a=p}e()}}catch(p){if(p&&a&&typeof p.stack=="string"){for(var r=p.stack.split(`
`),s=a.stack.split(`
`),i=r.length-1,o=s.length-1;1<=i&&0<=o&&r[i]!==s[o];)o--;for(;1<=i&&0<=o;i--,o--)if(r[i]!==s[o]){if(i!==1||o!==1)do if(i--,o--,0>o||r[i]!==s[o]){var c=`
`+r[i].replace(" at new "," at ");return e.displayName&&c.includes("<anonymous>")&&(c=c.replace("<anonymous>",e.displayName)),c}while(1<=i&&0<=o);break}}}finally{Er=!1,Error.prepareStackTrace=n}return(e=e?e.displayName||e.name:"")?wn(e):""}function Md(e){switch(e.tag){case 5:return wn(e.type);case 16:return wn("Lazy");case 13:return wn("Suspense");case 19:return wn("SuspenseList");case 0:case 2:case 15:return e=Rr(e.type,!1),e;case 11:return e=Rr(e.type.render,!1),e;case 1:return e=Rr(e.type,!0),e;default:return""}}function es(e){if(e==null)return null;if(typeof e=="function")return e.displayName||e.name||null;if(typeof e=="string")return e;switch(e){case Lt:return"Fragment";case Dt:return"Portal";case Yr:return"Profiler";case Qs:return"StrictMode";case $r:return"Suspense";case Zr:return"SuspenseList"}if(typeof e=="object")switch(e.$$typeof){case pc:return(e.displayName||"Context")+".Consumer";case uc:return(e._context.displayName||"Context")+".Provider";case Xs:var t=e.render;return e=e.displayName,e||(e=t.displayName||t.name||"",e=e!==""?"ForwardRef("+e+")":"ForwardRef"),e;case Ys:return t=e.displayName||null,t!==null?t:es(e.type)||"Memo";case Ze:t=e._payload,e=e._init;try{return es(e(t))}catch{}}return null}function Wd(e){var t=e.type;switch(e.tag){case 24:return"Cache";case 9:return(t.displayName||"Context")+".Consumer";case 10:return(t._context.displayName||"Context")+".Provider";case 18:return"DehydratedFragment";case 11:return e=t.render,e=e.displayName||e.name||"",t.displayName||(e!==""?"ForwardRef("+e+")":"ForwardRef");case 7:return"Fragment";case 5:return t;case 4:return"Portal";case 3:return"Root";case 6:return"Text";case 16:return es(t);case 8:return t===Qs?"StrictMode":"Mode";case 22:return"Offscreen";case 12:return"Profiler";case 21:return"Scope";case 13:return"Suspense";case 19:return"SuspenseList";case 25:return"TracingMarker";case 1:case 0:case 17:case 2:case 14:case 15:if(typeof t=="function")return t.displayName||t.name||null;if(typeof t=="string")return t}return null}function mt(e){switch(typeof e){case"boolean":case"number":case"string":case"undefined":return e;case"object":return e;default:return""}}function hc(e){var t=e.type;return(e=e.nodeName)&&e.toLowerCase()==="input"&&(t==="checkbox"||t==="radio")}function Hd(e){var t=hc(e)?"checked":"value",n=Object.getOwnPropertyDescriptor(e.constructor.prototype,t),a=""+e[t];if(!e.hasOwnProperty(t)&&typeof n<"u"&&typeof n.get=="function"&&typeof n.set=="function"){var r=n.get,s=n.set;return Object.defineProperty(e,t,{configurable:!0,get:function(){return r.call(this)},set:function(i){a=""+i,s.call(this,i)}}),Object.defineProperty(e,t,{enumerable:n.enumerable}),{getValue:function(){return a},setValue:function(i){a=""+i},stopTracking:function(){e._valueTracker=null,delete e[t]}}}}function ia(e){e._valueTracker||(e._valueTracker=Hd(e))}function yc(e){if(!e)return!1;var t=e._valueTracker;if(!t)return!0;var n=t.getValue(),a="";return e&&(a=hc(e)?e.checked?"true":"false":e.value),e=a,e!==n?(t.setValue(e),!0):!1}function Pa(e){if(e=e||(typeof document<"u"?document:void 0),typeof e>"u")return null;try{return e.activeElement||e.body}catch{return e.body}}function ts(e,t){var n=t.checked;return F({},t,{defaultChecked:void 0,defaultValue:void 0,value:void 0,checked:n??e._wrapperState.initialChecked})}function Fi(e,t){var n=t.defaultValue==null?"":t.defaultValue,a=t.checked!=null?t.checked:t.defaultChecked;n=mt(t.value!=null?t.value:n),e._wrapperState={initialChecked:a,initialValue:n,controlled:t.type==="checkbox"||t.type==="radio"?t.checked!=null:t.value!=null}}function fc(e,t){t=t.checked,t!=null&&_s(e,"checked",t,!1)}function ns(e,t){fc(e,t);var n=mt(t.value),a=t.type;if(n!=null)a==="number"?(n===0&&e.value===""||e.value!=n)&&(e.value=""+n):e.value!==""+n&&(e.value=""+n);else if(a==="submit"||a==="reset"){e.removeAttribute("value");return}t.hasOwnProperty("value")?as(e,t.type,n):t.hasOwnProperty("defaultValue")&&as(e,t.type,mt(t.defaultValue)),t.checked==null&&t.defaultChecked!=null&&(e.defaultChecked=!!t.defaultChecked)}function Ui(e,t,n){if(t.hasOwnProperty("value")||t.hasOwnProperty("defaultValue")){var a=t.type;if(!(a!=="submit"&&a!=="reset"||t.value!==void 0&&t.value!==null))return;t=""+e._wrapperState.initialValue,n||t===e.value||(e.value=t),e.defaultValue=t}n=e.name,n!==""&&(e.name=""),e.defaultChecked=!!e._wrapperState.initialChecked,n!==""&&(e.name=n)}function as(e,t,n){(t!=="number"||Pa(e.ownerDocument)!==e)&&(n==null?e.defaultValue=""+e._wrapperState.initialValue:e.defaultValue!==""+n&&(e.defaultValue=""+n))}var kn=Array.isArray;function Vt(e,t,n,a){if(e=e.options,t){t={};for(var r=0;r<n.length;r++)t["$"+n[r]]=!0;for(n=0;n<e.length;n++)r=t.hasOwnProperty("$"+e[n].value),e[n].selected!==r&&(e[n].selected=r),r&&a&&(e[n].defaultSelected=!0)}else{for(n=""+mt(n),t=null,r=0;r<e.length;r++){if(e[r].value===n){e[r].selected=!0,a&&(e[r].defaultSelected=!0);return}t!==null||e[r].disabled||(t=e[r])}t!==null&&(t.selected=!0)}}function rs(e,t){if(t.dangerouslySetInnerHTML!=null)throw Error(b(91));return F({},t,{value:void 0,defaultValue:void 0,children:""+e._wrapperState.initialValue})}function zi(e,t){var n=t.value;if(n==null){if(n=t.children,t=t.defaultValue,n!=null){if(t!=null)throw Error(b(92));if(kn(n)){if(1<n.length)throw Error(b(93));n=n[0]}t=n}t==null&&(t=""),n=t}e._wrapperState={initialValue:mt(n)}}function gc(e,t){var n=mt(t.value),a=mt(t.defaultValue);n!=null&&(n=""+n,n!==e.value&&(e.value=n),t.defaultValue==null&&e.defaultValue!==n&&(e.defaultValue=n)),a!=null&&(e.defaultValue=""+a)}function Ji(e){var t=e.textContent;t===e._wrapperState.initialValue&&t!==""&&t!==null&&(e.value=t)}function bc(e){switch(e){case"svg":return"http://www.w3.org/2000/svg";case"math":return"http://www.w3.org/1998/Math/MathML";default:return"http://www.w3.org/1999/xhtml"}}function ss(e,t){return e==null||e==="http://www.w3.org/1999/xhtml"?bc(t):e==="http://www.w3.org/2000/svg"&&t==="foreignObject"?"http://www.w3.org/1999/xhtml":e}var oa,vc=function(e){return typeof MSApp<"u"&&MSApp.execUnsafeLocalFunction?function(t,n,a,r){MSApp.execUnsafeLocalFunction(function(){return e(t,n,a,r)})}:e}(function(e,t){if(e.namespaceURI!=="http://www.w3.org/2000/svg"||"innerHTML"in e)e.innerHTML=t;else{for(oa=oa||document.createElement("div"),oa.innerHTML="<svg>"+t.valueOf().toString()+"</svg>",t=oa.firstChild;e.firstChild;)e.removeChild(e.firstChild);for(;t.firstChild;)e.appendChild(t.firstChild)}});function On(e,t){if(t){var n=e.firstChild;if(n&&n===e.lastChild&&n.nodeType===3){n.nodeValue=t;return}}e.textContent=t}var xn={animationIterationCount:!0,aspectRatio:!0,borderImageOutset:!0,borderImageSlice:!0,borderImageWidth:!0,boxFlex:!0,boxFlexGroup:!0,boxOrdinalGroup:!0,columnCount:!0,columns:!0,flex:!0,flexGrow:!0,flexPositive:!0,flexShrink:!0,flexNegative:!0,flexOrder:!0,gridArea:!0,gridRow:!0,gridRowEnd:!0,gridRowSpan:!0,gridRowStart:!0,gridColumn:!0,gridColumnEnd:!0,gridColumnSpan:!0,gridColumnStart:!0,fontWeight:!0,lineClamp:!0,lineHeight:!0,opacity:!0,order:!0,orphans:!0,tabSize:!0,widows:!0,zIndex:!0,zoom:!0,fillOpacity:!0,floodOpacity:!0,stopOpacity:!0,strokeDasharray:!0,strokeDashoffset:!0,strokeMiterlimit:!0,strokeOpacity:!0,strokeWidth:!0},Bd=["Webkit","ms","Moz","O"];Object.keys(xn).forEach(function(e){Bd.forEach(function(t){t=t+e.charAt(0).toUpperCase()+e.substring(1),xn[t]=xn[e]})});function wc(e,t,n){return t==null||typeof t=="boolean"||t===""?"":n||typeof t!="number"||t===0||xn.hasOwnProperty(e)&&xn[e]?(""+t).trim():t+"px"}function kc(e,t){e=e.style;for(var n in t)if(t.hasOwnProperty(n)){var a=n.indexOf("--")===0,r=wc(n,t[n],a);n==="float"&&(n="cssFloat"),a?e.setProperty(n,r):e[n]=r}}var Fd=F({menuitem:!0},{area:!0,base:!0,br:!0,col:!0,embed:!0,hr:!0,img:!0,input:!0,keygen:!0,link:!0,meta:!0,param:!0,source:!0,track:!0,wbr:!0});function is(e,t){if(t){if(Fd[e]&&(t.children!=null||t.dangerouslySetInnerHTML!=null))throw Error(b(137,e));if(t.dangerouslySetInnerHTML!=null){if(t.children!=null)throw Error(b(60));if(typeof t.dangerouslySetInnerHTML!="object"||!("__html"in t.dangerouslySetInnerHTML))throw Error(b(61))}if(t.style!=null&&typeof t.style!="object")throw Error(b(62))}}function os(e,t){if(e.indexOf("-")===-1)return typeof t.is=="string";switch(e){case"annotation-xml":case"color-profile":case"font-face":case"font-face-src":case"font-face-uri":case"font-face-format":case"font-face-name":case"missing-glyph":return!1;default:return!0}}var cs=null;function $s(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var ls=null,_t=null,Qt=null;function Gi(e){if(e=Zn(e)){if(typeof ls!="function")throw Error(b(280));var t=e.stateNode;t&&(t=cr(t),ls(e.stateNode,e.type,t))}}function Sc(e){_t?Qt?Qt.push(e):Qt=[e]:_t=e}function Cc(){if(_t){var e=_t,t=Qt;if(Qt=_t=null,Gi(e),t)for(e=0;e<t.length;e++)Gi(t[e])}}function xc(e,t){return e(t)}function Ec(){}var Tr=!1;function Rc(e,t,n){if(Tr)return e(t,n);Tr=!0;try{return xc(e,t,n)}finally{Tr=!1,(_t!==null||Qt!==null)&&(Ec(),Cc())}}function Dn(e,t){var n=e.stateNode;if(n===null)return null;var a=cr(n);if(a===null)return null;n=a[t];e:switch(t){case"onClick":case"onClickCapture":case"onDoubleClick":case"onDoubleClickCapture":case"onMouseDown":case"onMouseDownCapture":case"onMouseMove":case"onMouseMoveCapture":case"onMouseUp":case"onMouseUpCapture":case"onMouseEnter":(a=!a.disabled)||(e=e.type,a=!(e==="button"||e==="input"||e==="select"||e==="textarea")),e=!a;break e;default:e=!1}if(e)return null;if(n&&typeof n!="function")throw Error(b(231,t,typeof n));return n}var ds=!1;if(Ve)try{var un={};Object.defineProperty(un,"passive",{get:function(){ds=!0}}),window.addEventListener("test",un,un),window.removeEventListener("test",un,un)}catch{ds=!1}function Ud(e,t,n,a,r,s,i,o,c){var p=Array.prototype.slice.call(arguments,3);try{t.apply(n,p)}catch(f){this.onError(f)}}var En=!1,Oa=null,Da=!1,us=null,zd={onError:function(e){En=!0,Oa=e}};function Jd(e,t,n,a,r,s,i,o,c){En=!1,Oa=null,Ud.apply(zd,arguments)}function Gd(e,t,n,a,r,s,i,o,c){if(Jd.apply(this,arguments),En){if(En){var p=Oa;En=!1,Oa=null}else throw Error(b(198));Da||(Da=!0,us=p)}}function Pt(e){var t=e,n=e;if(e.alternate)for(;t.return;)t=t.return;else{e=t;do t=e,t.flags&4098&&(n=t.return),e=t.return;while(e)}return t.tag===3?n:null}function Tc(e){if(e.tag===13){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function Vi(e){if(Pt(e)!==e)throw Error(b(188))}function Vd(e){var t=e.alternate;if(!t){if(t=Pt(e),t===null)throw Error(b(188));return t!==e?null:e}for(var n=e,a=t;;){var r=n.return;if(r===null)break;var s=r.alternate;if(s===null){if(a=r.return,a!==null){n=a;continue}break}if(r.child===s.child){for(s=r.child;s;){if(s===n)return Vi(r),e;if(s===a)return Vi(r),t;s=s.sibling}throw Error(b(188))}if(n.return!==a.return)n=r,a=s;else{for(var i=!1,o=r.child;o;){if(o===n){i=!0,n=r,a=s;break}if(o===a){i=!0,a=r,n=s;break}o=o.sibling}if(!i){for(o=s.child;o;){if(o===n){i=!0,n=s,a=r;break}if(o===a){i=!0,a=s,n=r;break}o=o.sibling}if(!i)throw Error(b(189))}}if(n.alternate!==a)throw Error(b(190))}if(n.tag!==3)throw Error(b(188));return n.stateNode.current===n?e:t}function Nc(e){return e=Vd(e),e!==null?Ac(e):null}function Ac(e){if(e.tag===5||e.tag===6)return e;for(e=e.child;e!==null;){var t=Ac(e);if(t!==null)return t;e=e.sibling}return null}var qc=ve.unstable_scheduleCallback,_i=ve.unstable_cancelCallback,_d=ve.unstable_shouldYield,Qd=ve.unstable_requestPaint,J=ve.unstable_now,Xd=ve.unstable_getCurrentPriorityLevel,Zs=ve.unstable_ImmediatePriority,jc=ve.unstable_UserBlockingPriority,La=ve.unstable_NormalPriority,Yd=ve.unstable_LowPriority,Ic=ve.unstable_IdlePriority,rr=null,We=null;function $d(e){if(We&&typeof We.onCommitFiberRoot=="function")try{We.onCommitFiberRoot(rr,e,void 0,(e.current.flags&128)===128)}catch{}}var Ie=Math.clz32?Math.clz32:tu,Zd=Math.log,eu=Math.LN2;function tu(e){return e>>>=0,e===0?32:31-(Zd(e)/eu|0)|0}var ca=64,la=4194304;function Sn(e){switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return e&4194240;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return e&130023424;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 1073741824;default:return e}}function Ka(e,t){var n=e.pendingLanes;if(n===0)return 0;var a=0,r=e.suspendedLanes,s=e.pingedLanes,i=n&268435455;if(i!==0){var o=i&~r;o!==0?a=Sn(o):(s&=i,s!==0&&(a=Sn(s)))}else i=n&~r,i!==0?a=Sn(i):s!==0&&(a=Sn(s));if(a===0)return 0;if(t!==0&&t!==a&&!(t&r)&&(r=a&-a,s=t&-t,r>=s||r===16&&(s&4194240)!==0))return t;if(a&4&&(a|=n&16),t=e.entangledLanes,t!==0)for(e=e.entanglements,t&=a;0<t;)n=31-Ie(t),r=1<<n,a|=e[n],t&=~r;return a}function nu(e,t){switch(e){case 1:case 2:case 4:return t+250;case 8:case 16:case 32:case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return t+5e3;case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:return-1;case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function au(e,t){for(var n=e.suspendedLanes,a=e.pingedLanes,r=e.expirationTimes,s=e.pendingLanes;0<s;){var i=31-Ie(s),o=1<<i,c=r[i];c===-1?(!(o&n)||o&a)&&(r[i]=nu(o,t)):c<=t&&(e.expiredLanes|=o),s&=~o}}function ps(e){return e=e.pendingLanes&-1073741825,e!==0?e:e&1073741824?1073741824:0}function Pc(){var e=ca;return ca<<=1,!(ca&4194240)&&(ca=64),e}function Nr(e){for(var t=[],n=0;31>n;n++)t.push(e);return t}function Yn(e,t,n){e.pendingLanes|=t,t!==536870912&&(e.suspendedLanes=0,e.pingedLanes=0),e=e.eventTimes,t=31-Ie(t),e[t]=n}function ru(e,t){var n=e.pendingLanes&~t;e.pendingLanes=t,e.suspendedLanes=0,e.pingedLanes=0,e.expiredLanes&=t,e.mutableReadLanes&=t,e.entangledLanes&=t,t=e.entanglements;var a=e.eventTimes;for(e=e.expirationTimes;0<n;){var r=31-Ie(n),s=1<<r;t[r]=0,a[r]=-1,e[r]=-1,n&=~s}}function ei(e,t){var n=e.entangledLanes|=t;for(e=e.entanglements;n;){var a=31-Ie(n),r=1<<a;r&t|e[a]&t&&(e[a]|=t),n&=~r}}var D=0;function Oc(e){return e&=-e,1<e?4<e?e&268435455?16:536870912:4:1}var Dc,ti,Lc,Kc,Mc,ms=!1,da=[],st=null,it=null,ot=null,Ln=new Map,Kn=new Map,tt=[],su="mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");function Qi(e,t){switch(e){case"focusin":case"focusout":st=null;break;case"dragenter":case"dragleave":it=null;break;case"mouseover":case"mouseout":ot=null;break;case"pointerover":case"pointerout":Ln.delete(t.pointerId);break;case"gotpointercapture":case"lostpointercapture":Kn.delete(t.pointerId)}}function pn(e,t,n,a,r,s){return e===null||e.nativeEvent!==s?(e={blockedOn:t,domEventName:n,eventSystemFlags:a,nativeEvent:s,targetContainers:[r]},t!==null&&(t=Zn(t),t!==null&&ti(t)),e):(e.eventSystemFlags|=a,t=e.targetContainers,r!==null&&t.indexOf(r)===-1&&t.push(r),e)}function iu(e,t,n,a,r){switch(t){case"focusin":return st=pn(st,e,t,n,a,r),!0;case"dragenter":return it=pn(it,e,t,n,a,r),!0;case"mouseover":return ot=pn(ot,e,t,n,a,r),!0;case"pointerover":var s=r.pointerId;return Ln.set(s,pn(Ln.get(s)||null,e,t,n,a,r)),!0;case"gotpointercapture":return s=r.pointerId,Kn.set(s,pn(Kn.get(s)||null,e,t,n,a,r)),!0}return!1}function Wc(e){var t=St(e.target);if(t!==null){var n=Pt(t);if(n!==null){if(t=n.tag,t===13){if(t=Tc(n),t!==null){e.blockedOn=t,Mc(e.priority,function(){Lc(n)});return}}else if(t===3&&n.stateNode.current.memoizedState.isDehydrated){e.blockedOn=n.tag===3?n.stateNode.containerInfo:null;return}}}e.blockedOn=null}function Ca(e){if(e.blockedOn!==null)return!1;for(var t=e.targetContainers;0<t.length;){var n=hs(e.domEventName,e.eventSystemFlags,t[0],e.nativeEvent);if(n===null){n=e.nativeEvent;var a=new n.constructor(n.type,n);cs=a,n.target.dispatchEvent(a),cs=null}else return t=Zn(n),t!==null&&ti(t),e.blockedOn=n,!1;t.shift()}return!0}function Xi(e,t,n){Ca(e)&&n.delete(t)}function ou(){ms=!1,st!==null&&Ca(st)&&(st=null),it!==null&&Ca(it)&&(it=null),ot!==null&&Ca(ot)&&(ot=null),Ln.forEach(Xi),Kn.forEach(Xi)}function mn(e,t){e.blockedOn===t&&(e.blockedOn=null,ms||(ms=!0,ve.unstable_scheduleCallback(ve.unstable_NormalPriority,ou)))}function Mn(e){function t(r){return mn(r,e)}if(0<da.length){mn(da[0],e);for(var n=1;n<da.length;n++){var a=da[n];a.blockedOn===e&&(a.blockedOn=null)}}for(st!==null&&mn(st,e),it!==null&&mn(it,e),ot!==null&&mn(ot,e),Ln.forEach(t),Kn.forEach(t),n=0;n<tt.length;n++)a=tt[n],a.blockedOn===e&&(a.blockedOn=null);for(;0<tt.length&&(n=tt[0],n.blockedOn===null);)Wc(n),n.blockedOn===null&&tt.shift()}var Xt=Ye.ReactCurrentBatchConfig,Ma=!0;function cu(e,t,n,a){var r=D,s=Xt.transition;Xt.transition=null;try{D=1,ni(e,t,n,a)}finally{D=r,Xt.transition=s}}function lu(e,t,n,a){var r=D,s=Xt.transition;Xt.transition=null;try{D=4,ni(e,t,n,a)}finally{D=r,Xt.transition=s}}function ni(e,t,n,a){if(Ma){var r=hs(e,t,n,a);if(r===null)Mr(e,t,a,Wa,n),Qi(e,a);else if(iu(r,e,t,n,a))a.stopPropagation();else if(Qi(e,a),t&4&&-1<su.indexOf(e)){for(;r!==null;){var s=Zn(r);if(s!==null&&Dc(s),s=hs(e,t,n,a),s===null&&Mr(e,t,a,Wa,n),s===r)break;r=s}r!==null&&a.stopPropagation()}else Mr(e,t,a,null,n)}}var Wa=null;function hs(e,t,n,a){if(Wa=null,e=$s(a),e=St(e),e!==null)if(t=Pt(e),t===null)e=null;else if(n=t.tag,n===13){if(e=Tc(t),e!==null)return e;e=null}else if(n===3){if(t.stateNode.current.memoizedState.isDehydrated)return t.tag===3?t.stateNode.containerInfo:null;e=null}else t!==e&&(e=null);return Wa=e,null}function Hc(e){switch(e){case"cancel":case"click":case"close":case"contextmenu":case"copy":case"cut":case"auxclick":case"dblclick":case"dragend":case"dragstart":case"drop":case"focusin":case"focusout":case"input":case"invalid":case"keydown":case"keypress":case"keyup":case"mousedown":case"mouseup":case"paste":case"pause":case"play":case"pointercancel":case"pointerdown":case"pointerup":case"ratechange":case"reset":case"resize":case"seeked":case"submit":case"touchcancel":case"touchend":case"touchstart":case"volumechange":case"change":case"selectionchange":case"textInput":case"compositionstart":case"compositionend":case"compositionupdate":case"beforeblur":case"afterblur":case"beforeinput":case"blur":case"fullscreenchange":case"focus":case"hashchange":case"popstate":case"select":case"selectstart":return 1;case"drag":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"mousemove":case"mouseout":case"mouseover":case"pointermove":case"pointerout":case"pointerover":case"scroll":case"toggle":case"touchmove":case"wheel":case"mouseenter":case"mouseleave":case"pointerenter":case"pointerleave":return 4;case"message":switch(Xd()){case Zs:return 1;case jc:return 4;case La:case Yd:return 16;case Ic:return 536870912;default:return 16}default:return 16}}var at=null,ai=null,xa=null;function Bc(){if(xa)return xa;var e,t=ai,n=t.length,a,r="value"in at?at.value:at.textContent,s=r.length;for(e=0;e<n&&t[e]===r[e];e++);var i=n-e;for(a=1;a<=i&&t[n-a]===r[s-a];a++);return xa=r.slice(e,1<a?1-a:void 0)}function Ea(e){var t=e.keyCode;return"charCode"in e?(e=e.charCode,e===0&&t===13&&(e=13)):e=t,e===10&&(e=13),32<=e||e===13?e:0}function ua(){return!0}function Yi(){return!1}function ke(e){function t(n,a,r,s,i){this._reactName=n,this._targetInst=r,this.type=a,this.nativeEvent=s,this.target=i,this.currentTarget=null;for(var o in e)e.hasOwnProperty(o)&&(n=e[o],this[o]=n?n(s):s[o]);return this.isDefaultPrevented=(s.defaultPrevented!=null?s.defaultPrevented:s.returnValue===!1)?ua:Yi,this.isPropagationStopped=Yi,this}return F(t.prototype,{preventDefault:function(){this.defaultPrevented=!0;var n=this.nativeEvent;n&&(n.preventDefault?n.preventDefault():typeof n.returnValue!="unknown"&&(n.returnValue=!1),this.isDefaultPrevented=ua)},stopPropagation:function(){var n=this.nativeEvent;n&&(n.stopPropagation?n.stopPropagation():typeof n.cancelBubble!="unknown"&&(n.cancelBubble=!0),this.isPropagationStopped=ua)},persist:function(){},isPersistent:ua}),t}var cn={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},ri=ke(cn),$n=F({},cn,{view:0,detail:0}),du=ke($n),Ar,qr,hn,sr=F({},$n,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:si,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return"movementX"in e?e.movementX:(e!==hn&&(hn&&e.type==="mousemove"?(Ar=e.screenX-hn.screenX,qr=e.screenY-hn.screenY):qr=Ar=0,hn=e),Ar)},movementY:function(e){return"movementY"in e?e.movementY:qr}}),$i=ke(sr),uu=F({},sr,{dataTransfer:0}),pu=ke(uu),mu=F({},$n,{relatedTarget:0}),jr=ke(mu),hu=F({},cn,{animationName:0,elapsedTime:0,pseudoElement:0}),yu=ke(hu),fu=F({},cn,{clipboardData:function(e){return"clipboardData"in e?e.clipboardData:window.clipboardData}}),gu=ke(fu),bu=F({},cn,{data:0}),Zi=ke(bu),vu={Esc:"Escape",Spacebar:" ",Left:"ArrowLeft",Up:"ArrowUp",Right:"ArrowRight",Down:"ArrowDown",Del:"Delete",Win:"OS",Menu:"ContextMenu",Apps:"ContextMenu",Scroll:"ScrollLock",MozPrintableKey:"Unidentified"},wu={8:"Backspace",9:"Tab",12:"Clear",13:"Enter",16:"Shift",17:"Control",18:"Alt",19:"Pause",20:"CapsLock",27:"Escape",32:" ",33:"PageUp",34:"PageDown",35:"End",36:"Home",37:"ArrowLeft",38:"ArrowUp",39:"ArrowRight",40:"ArrowDown",45:"Insert",46:"Delete",112:"F1",113:"F2",114:"F3",115:"F4",116:"F5",117:"F6",118:"F7",119:"F8",120:"F9",121:"F10",122:"F11",123:"F12",144:"NumLock",145:"ScrollLock",224:"Meta"},ku={Alt:"altKey",Control:"ctrlKey",Meta:"metaKey",Shift:"shiftKey"};function Su(e){var t=this.nativeEvent;return t.getModifierState?t.getModifierState(e):(e=ku[e])?!!t[e]:!1}function si(){return Su}var Cu=F({},$n,{key:function(e){if(e.key){var t=vu[e.key]||e.key;if(t!=="Unidentified")return t}return e.type==="keypress"?(e=Ea(e),e===13?"Enter":String.fromCharCode(e)):e.type==="keydown"||e.type==="keyup"?wu[e.keyCode]||"Unidentified":""},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:si,charCode:function(e){return e.type==="keypress"?Ea(e):0},keyCode:function(e){return e.type==="keydown"||e.type==="keyup"?e.keyCode:0},which:function(e){return e.type==="keypress"?Ea(e):e.type==="keydown"||e.type==="keyup"?e.keyCode:0}}),xu=ke(Cu),Eu=F({},sr,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0}),eo=ke(Eu),Ru=F({},$n,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:si}),Tu=ke(Ru),Nu=F({},cn,{propertyName:0,elapsedTime:0,pseudoElement:0}),Au=ke(Nu),qu=F({},sr,{deltaX:function(e){return"deltaX"in e?e.deltaX:"wheelDeltaX"in e?-e.wheelDeltaX:0},deltaY:function(e){return"deltaY"in e?e.deltaY:"wheelDeltaY"in e?-e.wheelDeltaY:"wheelDelta"in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0}),ju=ke(qu),Iu=[9,13,27,32],ii=Ve&&"CompositionEvent"in window,Rn=null;Ve&&"documentMode"in document&&(Rn=document.documentMode);var Pu=Ve&&"TextEvent"in window&&!Rn,Fc=Ve&&(!ii||Rn&&8<Rn&&11>=Rn),to=" ",no=!1;function Uc(e,t){switch(e){case"keyup":return Iu.indexOf(t.keyCode)!==-1;case"keydown":return t.keyCode!==229;case"keypress":case"mousedown":case"focusout":return!0;default:return!1}}function zc(e){return e=e.detail,typeof e=="object"&&"data"in e?e.data:null}var Kt=!1;function Ou(e,t){switch(e){case"compositionend":return zc(t);case"keypress":return t.which!==32?null:(no=!0,to);case"textInput":return e=t.data,e===to&&no?null:e;default:return null}}function Du(e,t){if(Kt)return e==="compositionend"||!ii&&Uc(e,t)?(e=Bc(),xa=ai=at=null,Kt=!1,e):null;switch(e){case"paste":return null;case"keypress":if(!(t.ctrlKey||t.altKey||t.metaKey)||t.ctrlKey&&t.altKey){if(t.char&&1<t.char.length)return t.char;if(t.which)return String.fromCharCode(t.which)}return null;case"compositionend":return Fc&&t.locale!=="ko"?null:t.data;default:return null}}var Lu={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function ao(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t==="input"?!!Lu[e.type]:t==="textarea"}function Jc(e,t,n,a){Sc(a),t=Ha(t,"onChange"),0<t.length&&(n=new ri("onChange","change",null,n,a),e.push({event:n,listeners:t}))}var Tn=null,Wn=null;function Ku(e){nl(e,0)}function ir(e){var t=Ht(e);if(yc(t))return e}function Mu(e,t){if(e==="change")return t}var Gc=!1;if(Ve){var Ir;if(Ve){var Pr="oninput"in document;if(!Pr){var ro=document.createElement("div");ro.setAttribute("oninput","return;"),Pr=typeof ro.oninput=="function"}Ir=Pr}else Ir=!1;Gc=Ir&&(!document.documentMode||9<document.documentMode)}function so(){Tn&&(Tn.detachEvent("onpropertychange",Vc),Wn=Tn=null)}function Vc(e){if(e.propertyName==="value"&&ir(Wn)){var t=[];Jc(t,Wn,e,$s(e)),Rc(Ku,t)}}function Wu(e,t,n){e==="focusin"?(so(),Tn=t,Wn=n,Tn.attachEvent("onpropertychange",Vc)):e==="focusout"&&so()}function Hu(e){if(e==="selectionchange"||e==="keyup"||e==="keydown")return ir(Wn)}function Bu(e,t){if(e==="click")return ir(t)}function Fu(e,t){if(e==="input"||e==="change")return ir(t)}function Uu(e,t){return e===t&&(e!==0||1/e===1/t)||e!==e&&t!==t}var Oe=typeof Object.is=="function"?Object.is:Uu;function Hn(e,t){if(Oe(e,t))return!0;if(typeof e!="object"||e===null||typeof t!="object"||t===null)return!1;var n=Object.keys(e),a=Object.keys(t);if(n.length!==a.length)return!1;for(a=0;a<n.length;a++){var r=n[a];if(!Xr.call(t,r)||!Oe(e[r],t[r]))return!1}return!0}function io(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function oo(e,t){var n=io(e);e=0;for(var a;n;){if(n.nodeType===3){if(a=e+n.textContent.length,e<=t&&a>=t)return{node:n,offset:t-e};e=a}e:{for(;n;){if(n.nextSibling){n=n.nextSibling;break e}n=n.parentNode}n=void 0}n=io(n)}}function _c(e,t){return e&&t?e===t?!0:e&&e.nodeType===3?!1:t&&t.nodeType===3?_c(e,t.parentNode):"contains"in e?e.contains(t):e.compareDocumentPosition?!!(e.compareDocumentPosition(t)&16):!1:!1}function Qc(){for(var e=window,t=Pa();t instanceof e.HTMLIFrameElement;){try{var n=typeof t.contentWindow.location.href=="string"}catch{n=!1}if(n)e=t.contentWindow;else break;t=Pa(e.document)}return t}function oi(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t&&(t==="input"&&(e.type==="text"||e.type==="search"||e.type==="tel"||e.type==="url"||e.type==="password")||t==="textarea"||e.contentEditable==="true")}function zu(e){var t=Qc(),n=e.focusedElem,a=e.selectionRange;if(t!==n&&n&&n.ownerDocument&&_c(n.ownerDocument.documentElement,n)){if(a!==null&&oi(n)){if(t=a.start,e=a.end,e===void 0&&(e=t),"selectionStart"in n)n.selectionStart=t,n.selectionEnd=Math.min(e,n.value.length);else if(e=(t=n.ownerDocument||document)&&t.defaultView||window,e.getSelection){e=e.getSelection();var r=n.textContent.length,s=Math.min(a.start,r);a=a.end===void 0?s:Math.min(a.end,r),!e.extend&&s>a&&(r=a,a=s,s=r),r=oo(n,s);var i=oo(n,a);r&&i&&(e.rangeCount!==1||e.anchorNode!==r.node||e.anchorOffset!==r.offset||e.focusNode!==i.node||e.focusOffset!==i.offset)&&(t=t.createRange(),t.setStart(r.node,r.offset),e.removeAllRanges(),s>a?(e.addRange(t),e.extend(i.node,i.offset)):(t.setEnd(i.node,i.offset),e.addRange(t)))}}for(t=[],e=n;e=e.parentNode;)e.nodeType===1&&t.push({element:e,left:e.scrollLeft,top:e.scrollTop});for(typeof n.focus=="function"&&n.focus(),n=0;n<t.length;n++)e=t[n],e.element.scrollLeft=e.left,e.element.scrollTop=e.top}}var Ju=Ve&&"documentMode"in document&&11>=document.documentMode,Mt=null,ys=null,Nn=null,fs=!1;function co(e,t,n){var a=n.window===n?n.document:n.nodeType===9?n:n.ownerDocument;fs||Mt==null||Mt!==Pa(a)||(a=Mt,"selectionStart"in a&&oi(a)?a={start:a.selectionStart,end:a.selectionEnd}:(a=(a.ownerDocument&&a.ownerDocument.defaultView||window).getSelection(),a={anchorNode:a.anchorNode,anchorOffset:a.anchorOffset,focusNode:a.focusNode,focusOffset:a.focusOffset}),Nn&&Hn(Nn,a)||(Nn=a,a=Ha(ys,"onSelect"),0<a.length&&(t=new ri("onSelect","select",null,t,n),e.push({event:t,listeners:a}),t.target=Mt)))}function pa(e,t){var n={};return n[e.toLowerCase()]=t.toLowerCase(),n["Webkit"+e]="webkit"+t,n["Moz"+e]="moz"+t,n}var Wt={animationend:pa("Animation","AnimationEnd"),animationiteration:pa("Animation","AnimationIteration"),animationstart:pa("Animation","AnimationStart"),transitionend:pa("Transition","TransitionEnd")},Or={},Xc={};Ve&&(Xc=document.createElement("div").style,"AnimationEvent"in window||(delete Wt.animationend.animation,delete Wt.animationiteration.animation,delete Wt.animationstart.animation),"TransitionEvent"in window||delete Wt.transitionend.transition);function or(e){if(Or[e])return Or[e];if(!Wt[e])return e;var t=Wt[e],n;for(n in t)if(t.hasOwnProperty(n)&&n in Xc)return Or[e]=t[n];return e}var Yc=or("animationend"),$c=or("animationiteration"),Zc=or("animationstart"),el=or("transitionend"),tl=new Map,lo="abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");function yt(e,t){tl.set(e,t),It(t,[e])}for(var Dr=0;Dr<lo.length;Dr++){var Lr=lo[Dr],Gu=Lr.toLowerCase(),Vu=Lr[0].toUpperCase()+Lr.slice(1);yt(Gu,"on"+Vu)}yt(Yc,"onAnimationEnd");yt($c,"onAnimationIteration");yt(Zc,"onAnimationStart");yt("dblclick","onDoubleClick");yt("focusin","onFocus");yt("focusout","onBlur");yt(el,"onTransitionEnd");Zt("onMouseEnter",["mouseout","mouseover"]);Zt("onMouseLeave",["mouseout","mouseover"]);Zt("onPointerEnter",["pointerout","pointerover"]);Zt("onPointerLeave",["pointerout","pointerover"]);It("onChange","change click focusin focusout input keydown keyup selectionchange".split(" "));It("onSelect","focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));It("onBeforeInput",["compositionend","keypress","textInput","paste"]);It("onCompositionEnd","compositionend focusout keydown keypress keyup mousedown".split(" "));It("onCompositionStart","compositionstart focusout keydown keypress keyup mousedown".split(" "));It("onCompositionUpdate","compositionupdate focusout keydown keypress keyup mousedown".split(" "));var Cn="abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "),_u=new Set("cancel close invalid load scroll toggle".split(" ").concat(Cn));function uo(e,t,n){var a=e.type||"unknown-event";e.currentTarget=n,Gd(a,t,void 0,e),e.currentTarget=null}function nl(e,t){t=(t&4)!==0;for(var n=0;n<e.length;n++){var a=e[n],r=a.event;a=a.listeners;e:{var s=void 0;if(t)for(var i=a.length-1;0<=i;i--){var o=a[i],c=o.instance,p=o.currentTarget;if(o=o.listener,c!==s&&r.isPropagationStopped())break e;uo(r,o,p),s=c}else for(i=0;i<a.length;i++){if(o=a[i],c=o.instance,p=o.currentTarget,o=o.listener,c!==s&&r.isPropagationStopped())break e;uo(r,o,p),s=c}}}if(Da)throw e=us,Da=!1,us=null,e}function K(e,t){var n=t[ks];n===void 0&&(n=t[ks]=new Set);var a=e+"__bubble";n.has(a)||(al(t,e,2,!1),n.add(a))}function Kr(e,t,n){var a=0;t&&(a|=4),al(n,e,a,t)}var ma="_reactListening"+Math.random().toString(36).slice(2);function Bn(e){if(!e[ma]){e[ma]=!0,dc.forEach(function(n){n!=="selectionchange"&&(_u.has(n)||Kr(n,!1,e),Kr(n,!0,e))});var t=e.nodeType===9?e:e.ownerDocument;t===null||t[ma]||(t[ma]=!0,Kr("selectionchange",!1,t))}}function al(e,t,n,a){switch(Hc(t)){case 1:var r=cu;break;case 4:r=lu;break;default:r=ni}n=r.bind(null,t,n,e),r=void 0,!ds||t!=="touchstart"&&t!=="touchmove"&&t!=="wheel"||(r=!0),a?r!==void 0?e.addEventListener(t,n,{capture:!0,passive:r}):e.addEventListener(t,n,!0):r!==void 0?e.addEventListener(t,n,{passive:r}):e.addEventListener(t,n,!1)}function Mr(e,t,n,a,r){var s=a;if(!(t&1)&&!(t&2)&&a!==null)e:for(;;){if(a===null)return;var i=a.tag;if(i===3||i===4){var o=a.stateNode.containerInfo;if(o===r||o.nodeType===8&&o.parentNode===r)break;if(i===4)for(i=a.return;i!==null;){var c=i.tag;if((c===3||c===4)&&(c=i.stateNode.containerInfo,c===r||c.nodeType===8&&c.parentNode===r))return;i=i.return}for(;o!==null;){if(i=St(o),i===null)return;if(c=i.tag,c===5||c===6){a=s=i;continue e}o=o.parentNode}}a=a.return}Rc(function(){var p=s,f=$s(n),y=[];e:{var h=tl.get(e);if(h!==void 0){var v=ri,w=e;switch(e){case"keypress":if(Ea(n)===0)break e;case"keydown":case"keyup":v=xu;break;case"focusin":w="focus",v=jr;break;case"focusout":w="blur",v=jr;break;case"beforeblur":case"afterblur":v=jr;break;case"click":if(n.button===2)break e;case"auxclick":case"dblclick":case"mousedown":case"mousemove":case"mouseup":case"mouseout":case"mouseover":case"contextmenu":v=$i;break;case"drag":case"dragend":case"dragenter":case"dragexit":case"dragleave":case"dragover":case"dragstart":case"drop":v=pu;break;case"touchcancel":case"touchend":case"touchmove":case"touchstart":v=Tu;break;case Yc:case $c:case Zc:v=yu;break;case el:v=Au;break;case"scroll":v=du;break;case"wheel":v=ju;break;case"copy":case"cut":case"paste":v=gu;break;case"gotpointercapture":case"lostpointercapture":case"pointercancel":case"pointerdown":case"pointermove":case"pointerout":case"pointerover":case"pointerup":v=eo}var k=(t&4)!==0,j=!k&&e==="scroll",u=k?h!==null?h+"Capture":null:h;k=[];for(var l=p,m;l!==null;){m=l;var g=m.stateNode;if(m.tag===5&&g!==null&&(m=g,u!==null&&(g=Dn(l,u),g!=null&&k.push(Fn(l,g,m)))),j)break;l=l.return}0<k.length&&(h=new v(h,w,null,n,f),y.push({event:h,listeners:k}))}}if(!(t&7)){e:{if(h=e==="mouseover"||e==="pointerover",v=e==="mouseout"||e==="pointerout",h&&n!==cs&&(w=n.relatedTarget||n.fromElement)&&(St(w)||w[_e]))break e;if((v||h)&&(h=f.window===f?f:(h=f.ownerDocument)?h.defaultView||h.parentWindow:window,v?(w=n.relatedTarget||n.toElement,v=p,w=w?St(w):null,w!==null&&(j=Pt(w),w!==j||w.tag!==5&&w.tag!==6)&&(w=null)):(v=null,w=p),v!==w)){if(k=$i,g="onMouseLeave",u="onMouseEnter",l="mouse",(e==="pointerout"||e==="pointerover")&&(k=eo,g="onPointerLeave",u="onPointerEnter",l="pointer"),j=v==null?h:Ht(v),m=w==null?h:Ht(w),h=new k(g,l+"leave",v,n,f),h.target=j,h.relatedTarget=m,g=null,St(f)===p&&(k=new k(u,l+"enter",w,n,f),k.target=m,k.relatedTarget=j,g=k),j=g,v&&w)t:{for(k=v,u=w,l=0,m=k;m;m=Ot(m))l++;for(m=0,g=u;g;g=Ot(g))m++;for(;0<l-m;)k=Ot(k),l--;for(;0<m-l;)u=Ot(u),m--;for(;l--;){if(k===u||u!==null&&k===u.alternate)break t;k=Ot(k),u=Ot(u)}k=null}else k=null;v!==null&&po(y,h,v,k,!1),w!==null&&j!==null&&po(y,j,w,k,!0)}}e:{if(h=p?Ht(p):window,v=h.nodeName&&h.nodeName.toLowerCase(),v==="select"||v==="input"&&h.type==="file")var C=Mu;else if(ao(h))if(Gc)C=Fu;else{C=Hu;var T=Wu}else(v=h.nodeName)&&v.toLowerCase()==="input"&&(h.type==="checkbox"||h.type==="radio")&&(C=Bu);if(C&&(C=C(e,p))){Jc(y,C,n,f);break e}T&&T(e,h,p),e==="focusout"&&(T=h._wrapperState)&&T.controlled&&h.type==="number"&&as(h,"number",h.value)}switch(T=p?Ht(p):window,e){case"focusin":(ao(T)||T.contentEditable==="true")&&(Mt=T,ys=p,Nn=null);break;case"focusout":Nn=ys=Mt=null;break;case"mousedown":fs=!0;break;case"contextmenu":case"mouseup":case"dragend":fs=!1,co(y,n,f);break;case"selectionchange":if(Ju)break;case"keydown":case"keyup":co(y,n,f)}var R;if(ii)e:{switch(e){case"compositionstart":var S="onCompositionStart";break e;case"compositionend":S="onCompositionEnd";break e;case"compositionupdate":S="onCompositionUpdate";break e}S=void 0}else Kt?Uc(e,n)&&(S="onCompositionEnd"):e==="keydown"&&n.keyCode===229&&(S="onCompositionStart");S&&(Fc&&n.locale!=="ko"&&(Kt||S!=="onCompositionStart"?S==="onCompositionEnd"&&Kt&&(R=Bc()):(at=f,ai="value"in at?at.value:at.textContent,Kt=!0)),T=Ha(p,S),0<T.length&&(S=new Zi(S,e,null,n,f),y.push({event:S,listeners:T}),R?S.data=R:(R=zc(n),R!==null&&(S.data=R)))),(R=Pu?Ou(e,n):Du(e,n))&&(p=Ha(p,"onBeforeInput"),0<p.length&&(f=new Zi("onBeforeInput","beforeinput",null,n,f),y.push({event:f,listeners:p}),f.data=R))}nl(y,t)})}function Fn(e,t,n){return{instance:e,listener:t,currentTarget:n}}function Ha(e,t){for(var n=t+"Capture",a=[];e!==null;){var r=e,s=r.stateNode;r.tag===5&&s!==null&&(r=s,s=Dn(e,n),s!=null&&a.unshift(Fn(e,s,r)),s=Dn(e,t),s!=null&&a.push(Fn(e,s,r))),e=e.return}return a}function Ot(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5);return e||null}function po(e,t,n,a,r){for(var s=t._reactName,i=[];n!==null&&n!==a;){var o=n,c=o.alternate,p=o.stateNode;if(c!==null&&c===a)break;o.tag===5&&p!==null&&(o=p,r?(c=Dn(n,s),c!=null&&i.unshift(Fn(n,c,o))):r||(c=Dn(n,s),c!=null&&i.push(Fn(n,c,o)))),n=n.return}i.length!==0&&e.push({event:t,listeners:i})}var Qu=/\r\n?/g,Xu=/\u0000|\uFFFD/g;function mo(e){return(typeof e=="string"?e:""+e).replace(Qu,`
`).replace(Xu,"")}function ha(e,t,n){if(t=mo(t),mo(e)!==t&&n)throw Error(b(425))}function Ba(){}var gs=null,bs=null;function vs(e,t){return e==="textarea"||e==="noscript"||typeof t.children=="string"||typeof t.children=="number"||typeof t.dangerouslySetInnerHTML=="object"&&t.dangerouslySetInnerHTML!==null&&t.dangerouslySetInnerHTML.__html!=null}var ws=typeof setTimeout=="function"?setTimeout:void 0,Yu=typeof clearTimeout=="function"?clearTimeout:void 0,ho=typeof Promise=="function"?Promise:void 0,$u=typeof queueMicrotask=="function"?queueMicrotask:typeof ho<"u"?function(e){return ho.resolve(null).then(e).catch(Zu)}:ws;function Zu(e){setTimeout(function(){throw e})}function Wr(e,t){var n=t,a=0;do{var r=n.nextSibling;if(e.removeChild(n),r&&r.nodeType===8)if(n=r.data,n==="/$"){if(a===0){e.removeChild(r),Mn(t);return}a--}else n!=="$"&&n!=="$?"&&n!=="$!"||a++;n=r}while(n);Mn(t)}function ct(e){for(;e!=null;e=e.nextSibling){var t=e.nodeType;if(t===1||t===3)break;if(t===8){if(t=e.data,t==="$"||t==="$!"||t==="$?")break;if(t==="/$")return null}}return e}function yo(e){e=e.previousSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n==="$"||n==="$!"||n==="$?"){if(t===0)return e;t--}else n==="/$"&&t++}e=e.previousSibling}return null}var ln=Math.random().toString(36).slice(2),Me="__reactFiber$"+ln,Un="__reactProps$"+ln,_e="__reactContainer$"+ln,ks="__reactEvents$"+ln,ep="__reactListeners$"+ln,tp="__reactHandles$"+ln;function St(e){var t=e[Me];if(t)return t;for(var n=e.parentNode;n;){if(t=n[_e]||n[Me]){if(n=t.alternate,t.child!==null||n!==null&&n.child!==null)for(e=yo(e);e!==null;){if(n=e[Me])return n;e=yo(e)}return t}e=n,n=e.parentNode}return null}function Zn(e){return e=e[Me]||e[_e],!e||e.tag!==5&&e.tag!==6&&e.tag!==13&&e.tag!==3?null:e}function Ht(e){if(e.tag===5||e.tag===6)return e.stateNode;throw Error(b(33))}function cr(e){return e[Un]||null}var Ss=[],Bt=-1;function ft(e){return{current:e}}function M(e){0>Bt||(e.current=Ss[Bt],Ss[Bt]=null,Bt--)}function L(e,t){Bt++,Ss[Bt]=e.current,e.current=t}var ht={},ie=ft(ht),me=ft(!1),Tt=ht;function en(e,t){var n=e.type.contextTypes;if(!n)return ht;var a=e.stateNode;if(a&&a.__reactInternalMemoizedUnmaskedChildContext===t)return a.__reactInternalMemoizedMaskedChildContext;var r={},s;for(s in n)r[s]=t[s];return a&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=t,e.__reactInternalMemoizedMaskedChildContext=r),r}function he(e){return e=e.childContextTypes,e!=null}function Fa(){M(me),M(ie)}function fo(e,t,n){if(ie.current!==ht)throw Error(b(168));L(ie,t),L(me,n)}function rl(e,t,n){var a=e.stateNode;if(t=t.childContextTypes,typeof a.getChildContext!="function")return n;a=a.getChildContext();for(var r in a)if(!(r in t))throw Error(b(108,Wd(e)||"Unknown",r));return F({},n,a)}function Ua(e){return e=(e=e.stateNode)&&e.__reactInternalMemoizedMergedChildContext||ht,Tt=ie.current,L(ie,e),L(me,me.current),!0}function go(e,t,n){var a=e.stateNode;if(!a)throw Error(b(169));n?(e=rl(e,t,Tt),a.__reactInternalMemoizedMergedChildContext=e,M(me),M(ie),L(ie,e)):M(me),L(me,n)}var Ue=null,lr=!1,Hr=!1;function sl(e){Ue===null?Ue=[e]:Ue.push(e)}function np(e){lr=!0,sl(e)}function gt(){if(!Hr&&Ue!==null){Hr=!0;var e=0,t=D;try{var n=Ue;for(D=1;e<n.length;e++){var a=n[e];do a=a(!0);while(a!==null)}Ue=null,lr=!1}catch(r){throw Ue!==null&&(Ue=Ue.slice(e+1)),qc(Zs,gt),r}finally{D=t,Hr=!1}}return null}var Ft=[],Ut=0,za=null,Ja=0,Se=[],Ce=0,Nt=null,ze=1,Je="";function wt(e,t){Ft[Ut++]=Ja,Ft[Ut++]=za,za=e,Ja=t}function il(e,t,n){Se[Ce++]=ze,Se[Ce++]=Je,Se[Ce++]=Nt,Nt=e;var a=ze;e=Je;var r=32-Ie(a)-1;a&=~(1<<r),n+=1;var s=32-Ie(t)+r;if(30<s){var i=r-r%5;s=(a&(1<<i)-1).toString(32),a>>=i,r-=i,ze=1<<32-Ie(t)+r|n<<r|a,Je=s+e}else ze=1<<s|n<<r|a,Je=e}function ci(e){e.return!==null&&(wt(e,1),il(e,1,0))}function li(e){for(;e===za;)za=Ft[--Ut],Ft[Ut]=null,Ja=Ft[--Ut],Ft[Ut]=null;for(;e===Nt;)Nt=Se[--Ce],Se[Ce]=null,Je=Se[--Ce],Se[Ce]=null,ze=Se[--Ce],Se[Ce]=null}var be=null,ge=null,W=!1,je=null;function ol(e,t){var n=xe(5,null,null,0);n.elementType="DELETED",n.stateNode=t,n.return=e,t=e.deletions,t===null?(e.deletions=[n],e.flags|=16):t.push(n)}function bo(e,t){switch(e.tag){case 5:var n=e.type;return t=t.nodeType!==1||n.toLowerCase()!==t.nodeName.toLowerCase()?null:t,t!==null?(e.stateNode=t,be=e,ge=ct(t.firstChild),!0):!1;case 6:return t=e.pendingProps===""||t.nodeType!==3?null:t,t!==null?(e.stateNode=t,be=e,ge=null,!0):!1;case 13:return t=t.nodeType!==8?null:t,t!==null?(n=Nt!==null?{id:ze,overflow:Je}:null,e.memoizedState={dehydrated:t,treeContext:n,retryLane:1073741824},n=xe(18,null,null,0),n.stateNode=t,n.return=e,e.child=n,be=e,ge=null,!0):!1;default:return!1}}function Cs(e){return(e.mode&1)!==0&&(e.flags&128)===0}function xs(e){if(W){var t=ge;if(t){var n=t;if(!bo(e,t)){if(Cs(e))throw Error(b(418));t=ct(n.nextSibling);var a=be;t&&bo(e,t)?ol(a,n):(e.flags=e.flags&-4097|2,W=!1,be=e)}}else{if(Cs(e))throw Error(b(418));e.flags=e.flags&-4097|2,W=!1,be=e}}}function vo(e){for(e=e.return;e!==null&&e.tag!==5&&e.tag!==3&&e.tag!==13;)e=e.return;be=e}function ya(e){if(e!==be)return!1;if(!W)return vo(e),W=!0,!1;var t;if((t=e.tag!==3)&&!(t=e.tag!==5)&&(t=e.type,t=t!=="head"&&t!=="body"&&!vs(e.type,e.memoizedProps)),t&&(t=ge)){if(Cs(e))throw cl(),Error(b(418));for(;t;)ol(e,t),t=ct(t.nextSibling)}if(vo(e),e.tag===13){if(e=e.memoizedState,e=e!==null?e.dehydrated:null,!e)throw Error(b(317));e:{for(e=e.nextSibling,t=0;e;){if(e.nodeType===8){var n=e.data;if(n==="/$"){if(t===0){ge=ct(e.nextSibling);break e}t--}else n!=="$"&&n!=="$!"&&n!=="$?"||t++}e=e.nextSibling}ge=null}}else ge=be?ct(e.stateNode.nextSibling):null;return!0}function cl(){for(var e=ge;e;)e=ct(e.nextSibling)}function tn(){ge=be=null,W=!1}function di(e){je===null?je=[e]:je.push(e)}var ap=Ye.ReactCurrentBatchConfig;function yn(e,t,n){if(e=n.ref,e!==null&&typeof e!="function"&&typeof e!="object"){if(n._owner){if(n=n._owner,n){if(n.tag!==1)throw Error(b(309));var a=n.stateNode}if(!a)throw Error(b(147,e));var r=a,s=""+e;return t!==null&&t.ref!==null&&typeof t.ref=="function"&&t.ref._stringRef===s?t.ref:(t=function(i){var o=r.refs;i===null?delete o[s]:o[s]=i},t._stringRef=s,t)}if(typeof e!="string")throw Error(b(284));if(!n._owner)throw Error(b(290,e))}return e}function fa(e,t){throw e=Object.prototype.toString.call(t),Error(b(31,e==="[object Object]"?"object with keys {"+Object.keys(t).join(", ")+"}":e))}function wo(e){var t=e._init;return t(e._payload)}function ll(e){function t(u,l){if(e){var m=u.deletions;m===null?(u.deletions=[l],u.flags|=16):m.push(l)}}function n(u,l){if(!e)return null;for(;l!==null;)t(u,l),l=l.sibling;return null}function a(u,l){for(u=new Map;l!==null;)l.key!==null?u.set(l.key,l):u.set(l.index,l),l=l.sibling;return u}function r(u,l){return u=pt(u,l),u.index=0,u.sibling=null,u}function s(u,l,m){return u.index=m,e?(m=u.alternate,m!==null?(m=m.index,m<l?(u.flags|=2,l):m):(u.flags|=2,l)):(u.flags|=1048576,l)}function i(u){return e&&u.alternate===null&&(u.flags|=2),u}function o(u,l,m,g){return l===null||l.tag!==6?(l=Vr(m,u.mode,g),l.return=u,l):(l=r(l,m),l.return=u,l)}function c(u,l,m,g){var C=m.type;return C===Lt?f(u,l,m.props.children,g,m.key):l!==null&&(l.elementType===C||typeof C=="object"&&C!==null&&C.$$typeof===Ze&&wo(C)===l.type)?(g=r(l,m.props),g.ref=yn(u,l,m),g.return=u,g):(g=Ia(m.type,m.key,m.props,null,u.mode,g),g.ref=yn(u,l,m),g.return=u,g)}function p(u,l,m,g){return l===null||l.tag!==4||l.stateNode.containerInfo!==m.containerInfo||l.stateNode.implementation!==m.implementation?(l=_r(m,u.mode,g),l.return=u,l):(l=r(l,m.children||[]),l.return=u,l)}function f(u,l,m,g,C){return l===null||l.tag!==7?(l=Rt(m,u.mode,g,C),l.return=u,l):(l=r(l,m),l.return=u,l)}function y(u,l,m){if(typeof l=="string"&&l!==""||typeof l=="number")return l=Vr(""+l,u.mode,m),l.return=u,l;if(typeof l=="object"&&l!==null){switch(l.$$typeof){case sa:return m=Ia(l.type,l.key,l.props,null,u.mode,m),m.ref=yn(u,null,l),m.return=u,m;case Dt:return l=_r(l,u.mode,m),l.return=u,l;case Ze:var g=l._init;return y(u,g(l._payload),m)}if(kn(l)||dn(l))return l=Rt(l,u.mode,m,null),l.return=u,l;fa(u,l)}return null}function h(u,l,m,g){var C=l!==null?l.key:null;if(typeof m=="string"&&m!==""||typeof m=="number")return C!==null?null:o(u,l,""+m,g);if(typeof m=="object"&&m!==null){switch(m.$$typeof){case sa:return m.key===C?c(u,l,m,g):null;case Dt:return m.key===C?p(u,l,m,g):null;case Ze:return C=m._init,h(u,l,C(m._payload),g)}if(kn(m)||dn(m))return C!==null?null:f(u,l,m,g,null);fa(u,m)}return null}function v(u,l,m,g,C){if(typeof g=="string"&&g!==""||typeof g=="number")return u=u.get(m)||null,o(l,u,""+g,C);if(typeof g=="object"&&g!==null){switch(g.$$typeof){case sa:return u=u.get(g.key===null?m:g.key)||null,c(l,u,g,C);case Dt:return u=u.get(g.key===null?m:g.key)||null,p(l,u,g,C);case Ze:var T=g._init;return v(u,l,m,T(g._payload),C)}if(kn(g)||dn(g))return u=u.get(m)||null,f(l,u,g,C,null);fa(l,g)}return null}function w(u,l,m,g){for(var C=null,T=null,R=l,S=l=0,N=null;R!==null&&S<m.length;S++){R.index>S?(N=R,R=null):N=R.sibling;var A=h(u,R,m[S],g);if(A===null){R===null&&(R=N);break}e&&R&&A.alternate===null&&t(u,R),l=s(A,l,S),T===null?C=A:T.sibling=A,T=A,R=N}if(S===m.length)return n(u,R),W&&wt(u,S),C;if(R===null){for(;S<m.length;S++)R=y(u,m[S],g),R!==null&&(l=s(R,l,S),T===null?C=R:T.sibling=R,T=R);return W&&wt(u,S),C}for(R=a(u,R);S<m.length;S++)N=v(R,u,S,m[S],g),N!==null&&(e&&N.alternate!==null&&R.delete(N.key===null?S:N.key),l=s(N,l,S),T===null?C=N:T.sibling=N,T=N);return e&&R.forEach(function(ne){return t(u,ne)}),W&&wt(u,S),C}function k(u,l,m,g){var C=dn(m);if(typeof C!="function")throw Error(b(150));if(m=C.call(m),m==null)throw Error(b(151));for(var T=C=null,R=l,S=l=0,N=null,A=m.next();R!==null&&!A.done;S++,A=m.next()){R.index>S?(N=R,R=null):N=R.sibling;var ne=h(u,R,A.value,g);if(ne===null){R===null&&(R=N);break}e&&R&&ne.alternate===null&&t(u,R),l=s(ne,l,S),T===null?C=ne:T.sibling=ne,T=ne,R=N}if(A.done)return n(u,R),W&&wt(u,S),C;if(R===null){for(;!A.done;S++,A=m.next())A=y(u,A.value,g),A!==null&&(l=s(A,l,S),T===null?C=A:T.sibling=A,T=A);return W&&wt(u,S),C}for(R=a(u,R);!A.done;S++,A=m.next())A=v(R,u,S,A.value,g),A!==null&&(e&&A.alternate!==null&&R.delete(A.key===null?S:A.key),l=s(A,l,S),T===null?C=A:T.sibling=A,T=A);return e&&R.forEach(function(De){return t(u,De)}),W&&wt(u,S),C}function j(u,l,m,g){if(typeof m=="object"&&m!==null&&m.type===Lt&&m.key===null&&(m=m.props.children),typeof m=="object"&&m!==null){switch(m.$$typeof){case sa:e:{for(var C=m.key,T=l;T!==null;){if(T.key===C){if(C=m.type,C===Lt){if(T.tag===7){n(u,T.sibling),l=r(T,m.props.children),l.return=u,u=l;break e}}else if(T.elementType===C||typeof C=="object"&&C!==null&&C.$$typeof===Ze&&wo(C)===T.type){n(u,T.sibling),l=r(T,m.props),l.ref=yn(u,T,m),l.return=u,u=l;break e}n(u,T);break}else t(u,T);T=T.sibling}m.type===Lt?(l=Rt(m.props.children,u.mode,g,m.key),l.return=u,u=l):(g=Ia(m.type,m.key,m.props,null,u.mode,g),g.ref=yn(u,l,m),g.return=u,u=g)}return i(u);case Dt:e:{for(T=m.key;l!==null;){if(l.key===T)if(l.tag===4&&l.stateNode.containerInfo===m.containerInfo&&l.stateNode.implementation===m.implementation){n(u,l.sibling),l=r(l,m.children||[]),l.return=u,u=l;break e}else{n(u,l);break}else t(u,l);l=l.sibling}l=_r(m,u.mode,g),l.return=u,u=l}return i(u);case Ze:return T=m._init,j(u,l,T(m._payload),g)}if(kn(m))return w(u,l,m,g);if(dn(m))return k(u,l,m,g);fa(u,m)}return typeof m=="string"&&m!==""||typeof m=="number"?(m=""+m,l!==null&&l.tag===6?(n(u,l.sibling),l=r(l,m),l.return=u,u=l):(n(u,l),l=Vr(m,u.mode,g),l.return=u,u=l),i(u)):n(u,l)}return j}var nn=ll(!0),dl=ll(!1),Ga=ft(null),Va=null,zt=null,ui=null;function pi(){ui=zt=Va=null}function mi(e){var t=Ga.current;M(Ga),e._currentValue=t}function Es(e,t,n){for(;e!==null;){var a=e.alternate;if((e.childLanes&t)!==t?(e.childLanes|=t,a!==null&&(a.childLanes|=t)):a!==null&&(a.childLanes&t)!==t&&(a.childLanes|=t),e===n)break;e=e.return}}function Yt(e,t){Va=e,ui=zt=null,e=e.dependencies,e!==null&&e.firstContext!==null&&(e.lanes&t&&(pe=!0),e.firstContext=null)}function Re(e){var t=e._currentValue;if(ui!==e)if(e={context:e,memoizedValue:t,next:null},zt===null){if(Va===null)throw Error(b(308));zt=e,Va.dependencies={lanes:0,firstContext:e}}else zt=zt.next=e;return t}var Ct=null;function hi(e){Ct===null?Ct=[e]:Ct.push(e)}function ul(e,t,n,a){var r=t.interleaved;return r===null?(n.next=n,hi(t)):(n.next=r.next,r.next=n),t.interleaved=n,Qe(e,a)}function Qe(e,t){e.lanes|=t;var n=e.alternate;for(n!==null&&(n.lanes|=t),n=e,e=e.return;e!==null;)e.childLanes|=t,n=e.alternate,n!==null&&(n.childLanes|=t),n=e,e=e.return;return n.tag===3?n.stateNode:null}var et=!1;function yi(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,interleaved:null,lanes:0},effects:null}}function pl(e,t){e=e.updateQueue,t.updateQueue===e&&(t.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,effects:e.effects})}function Ge(e,t){return{eventTime:e,lane:t,tag:0,payload:null,callback:null,next:null}}function lt(e,t,n){var a=e.updateQueue;if(a===null)return null;if(a=a.shared,O&2){var r=a.pending;return r===null?t.next=t:(t.next=r.next,r.next=t),a.pending=t,Qe(e,n)}return r=a.interleaved,r===null?(t.next=t,hi(a)):(t.next=r.next,r.next=t),a.interleaved=t,Qe(e,n)}function Ra(e,t,n){if(t=t.updateQueue,t!==null&&(t=t.shared,(n&4194240)!==0)){var a=t.lanes;a&=e.pendingLanes,n|=a,t.lanes=n,ei(e,n)}}function ko(e,t){var n=e.updateQueue,a=e.alternate;if(a!==null&&(a=a.updateQueue,n===a)){var r=null,s=null;if(n=n.firstBaseUpdate,n!==null){do{var i={eventTime:n.eventTime,lane:n.lane,tag:n.tag,payload:n.payload,callback:n.callback,next:null};s===null?r=s=i:s=s.next=i,n=n.next}while(n!==null);s===null?r=s=t:s=s.next=t}else r=s=t;n={baseState:a.baseState,firstBaseUpdate:r,lastBaseUpdate:s,shared:a.shared,effects:a.effects},e.updateQueue=n;return}e=n.lastBaseUpdate,e===null?n.firstBaseUpdate=t:e.next=t,n.lastBaseUpdate=t}function _a(e,t,n,a){var r=e.updateQueue;et=!1;var s=r.firstBaseUpdate,i=r.lastBaseUpdate,o=r.shared.pending;if(o!==null){r.shared.pending=null;var c=o,p=c.next;c.next=null,i===null?s=p:i.next=p,i=c;var f=e.alternate;f!==null&&(f=f.updateQueue,o=f.lastBaseUpdate,o!==i&&(o===null?f.firstBaseUpdate=p:o.next=p,f.lastBaseUpdate=c))}if(s!==null){var y=r.baseState;i=0,f=p=c=null,o=s;do{var h=o.lane,v=o.eventTime;if((a&h)===h){f!==null&&(f=f.next={eventTime:v,lane:0,tag:o.tag,payload:o.payload,callback:o.callback,next:null});e:{var w=e,k=o;switch(h=t,v=n,k.tag){case 1:if(w=k.payload,typeof w=="function"){y=w.call(v,y,h);break e}y=w;break e;case 3:w.flags=w.flags&-65537|128;case 0:if(w=k.payload,h=typeof w=="function"?w.call(v,y,h):w,h==null)break e;y=F({},y,h);break e;case 2:et=!0}}o.callback!==null&&o.lane!==0&&(e.flags|=64,h=r.effects,h===null?r.effects=[o]:h.push(o))}else v={eventTime:v,lane:h,tag:o.tag,payload:o.payload,callback:o.callback,next:null},f===null?(p=f=v,c=y):f=f.next=v,i|=h;if(o=o.next,o===null){if(o=r.shared.pending,o===null)break;h=o,o=h.next,h.next=null,r.lastBaseUpdate=h,r.shared.pending=null}}while(!0);if(f===null&&(c=y),r.baseState=c,r.firstBaseUpdate=p,r.lastBaseUpdate=f,t=r.shared.interleaved,t!==null){r=t;do i|=r.lane,r=r.next;while(r!==t)}else s===null&&(r.shared.lanes=0);qt|=i,e.lanes=i,e.memoizedState=y}}function So(e,t,n){if(e=t.effects,t.effects=null,e!==null)for(t=0;t<e.length;t++){var a=e[t],r=a.callback;if(r!==null){if(a.callback=null,a=n,typeof r!="function")throw Error(b(191,r));r.call(a)}}}var ea={},He=ft(ea),zn=ft(ea),Jn=ft(ea);function xt(e){if(e===ea)throw Error(b(174));return e}function fi(e,t){switch(L(Jn,t),L(zn,e),L(He,ea),e=t.nodeType,e){case 9:case 11:t=(t=t.documentElement)?t.namespaceURI:ss(null,"");break;default:e=e===8?t.parentNode:t,t=e.namespaceURI||null,e=e.tagName,t=ss(t,e)}M(He),L(He,t)}function an(){M(He),M(zn),M(Jn)}function ml(e){xt(Jn.current);var t=xt(He.current),n=ss(t,e.type);t!==n&&(L(zn,e),L(He,n))}function gi(e){zn.current===e&&(M(He),M(zn))}var H=ft(0);function Qa(e){for(var t=e;t!==null;){if(t.tag===13){var n=t.memoizedState;if(n!==null&&(n=n.dehydrated,n===null||n.data==="$?"||n.data==="$!"))return t}else if(t.tag===19&&t.memoizedProps.revealOrder!==void 0){if(t.flags&128)return t}else if(t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return null;t=t.return}t.sibling.return=t.return,t=t.sibling}return null}var Br=[];function bi(){for(var e=0;e<Br.length;e++)Br[e]._workInProgressVersionPrimary=null;Br.length=0}var Ta=Ye.ReactCurrentDispatcher,Fr=Ye.ReactCurrentBatchConfig,At=0,B=null,_=null,Y=null,Xa=!1,An=!1,Gn=0,rp=0;function ae(){throw Error(b(321))}function vi(e,t){if(t===null)return!1;for(var n=0;n<t.length&&n<e.length;n++)if(!Oe(e[n],t[n]))return!1;return!0}function wi(e,t,n,a,r,s){if(At=s,B=t,t.memoizedState=null,t.updateQueue=null,t.lanes=0,Ta.current=e===null||e.memoizedState===null?cp:lp,e=n(a,r),An){s=0;do{if(An=!1,Gn=0,25<=s)throw Error(b(301));s+=1,Y=_=null,t.updateQueue=null,Ta.current=dp,e=n(a,r)}while(An)}if(Ta.current=Ya,t=_!==null&&_.next!==null,At=0,Y=_=B=null,Xa=!1,t)throw Error(b(300));return e}function ki(){var e=Gn!==0;return Gn=0,e}function Ke(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return Y===null?B.memoizedState=Y=e:Y=Y.next=e,Y}function Te(){if(_===null){var e=B.alternate;e=e!==null?e.memoizedState:null}else e=_.next;var t=Y===null?B.memoizedState:Y.next;if(t!==null)Y=t,_=e;else{if(e===null)throw Error(b(310));_=e,e={memoizedState:_.memoizedState,baseState:_.baseState,baseQueue:_.baseQueue,queue:_.queue,next:null},Y===null?B.memoizedState=Y=e:Y=Y.next=e}return Y}function Vn(e,t){return typeof t=="function"?t(e):t}function Ur(e){var t=Te(),n=t.queue;if(n===null)throw Error(b(311));n.lastRenderedReducer=e;var a=_,r=a.baseQueue,s=n.pending;if(s!==null){if(r!==null){var i=r.next;r.next=s.next,s.next=i}a.baseQueue=r=s,n.pending=null}if(r!==null){s=r.next,a=a.baseState;var o=i=null,c=null,p=s;do{var f=p.lane;if((At&f)===f)c!==null&&(c=c.next={lane:0,action:p.action,hasEagerState:p.hasEagerState,eagerState:p.eagerState,next:null}),a=p.hasEagerState?p.eagerState:e(a,p.action);else{var y={lane:f,action:p.action,hasEagerState:p.hasEagerState,eagerState:p.eagerState,next:null};c===null?(o=c=y,i=a):c=c.next=y,B.lanes|=f,qt|=f}p=p.next}while(p!==null&&p!==s);c===null?i=a:c.next=o,Oe(a,t.memoizedState)||(pe=!0),t.memoizedState=a,t.baseState=i,t.baseQueue=c,n.lastRenderedState=a}if(e=n.interleaved,e!==null){r=e;do s=r.lane,B.lanes|=s,qt|=s,r=r.next;while(r!==e)}else r===null&&(n.lanes=0);return[t.memoizedState,n.dispatch]}function zr(e){var t=Te(),n=t.queue;if(n===null)throw Error(b(311));n.lastRenderedReducer=e;var a=n.dispatch,r=n.pending,s=t.memoizedState;if(r!==null){n.pending=null;var i=r=r.next;do s=e(s,i.action),i=i.next;while(i!==r);Oe(s,t.memoizedState)||(pe=!0),t.memoizedState=s,t.baseQueue===null&&(t.baseState=s),n.lastRenderedState=s}return[s,a]}function hl(){}function yl(e,t){var n=B,a=Te(),r=t(),s=!Oe(a.memoizedState,r);if(s&&(a.memoizedState=r,pe=!0),a=a.queue,Si(bl.bind(null,n,a,e),[e]),a.getSnapshot!==t||s||Y!==null&&Y.memoizedState.tag&1){if(n.flags|=2048,_n(9,gl.bind(null,n,a,r,t),void 0,null),$===null)throw Error(b(349));At&30||fl(n,t,r)}return r}function fl(e,t,n){e.flags|=16384,e={getSnapshot:t,value:n},t=B.updateQueue,t===null?(t={lastEffect:null,stores:null},B.updateQueue=t,t.stores=[e]):(n=t.stores,n===null?t.stores=[e]:n.push(e))}function gl(e,t,n,a){t.value=n,t.getSnapshot=a,vl(t)&&wl(e)}function bl(e,t,n){return n(function(){vl(t)&&wl(e)})}function vl(e){var t=e.getSnapshot;e=e.value;try{var n=t();return!Oe(e,n)}catch{return!0}}function wl(e){var t=Qe(e,1);t!==null&&Pe(t,e,1,-1)}function Co(e){var t=Ke();return typeof e=="function"&&(e=e()),t.memoizedState=t.baseState=e,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:Vn,lastRenderedState:e},t.queue=e,e=e.dispatch=op.bind(null,B,e),[t.memoizedState,e]}function _n(e,t,n,a){return e={tag:e,create:t,destroy:n,deps:a,next:null},t=B.updateQueue,t===null?(t={lastEffect:null,stores:null},B.updateQueue=t,t.lastEffect=e.next=e):(n=t.lastEffect,n===null?t.lastEffect=e.next=e:(a=n.next,n.next=e,e.next=a,t.lastEffect=e)),e}function kl(){return Te().memoizedState}function Na(e,t,n,a){var r=Ke();B.flags|=e,r.memoizedState=_n(1|t,n,void 0,a===void 0?null:a)}function dr(e,t,n,a){var r=Te();a=a===void 0?null:a;var s=void 0;if(_!==null){var i=_.memoizedState;if(s=i.destroy,a!==null&&vi(a,i.deps)){r.memoizedState=_n(t,n,s,a);return}}B.flags|=e,r.memoizedState=_n(1|t,n,s,a)}function xo(e,t){return Na(8390656,8,e,t)}function Si(e,t){return dr(2048,8,e,t)}function Sl(e,t){return dr(4,2,e,t)}function Cl(e,t){return dr(4,4,e,t)}function xl(e,t){if(typeof t=="function")return e=e(),t(e),function(){t(null)};if(t!=null)return e=e(),t.current=e,function(){t.current=null}}function El(e,t,n){return n=n!=null?n.concat([e]):null,dr(4,4,xl.bind(null,t,e),n)}function Ci(){}function Rl(e,t){var n=Te();t=t===void 0?null:t;var a=n.memoizedState;return a!==null&&t!==null&&vi(t,a[1])?a[0]:(n.memoizedState=[e,t],e)}function Tl(e,t){var n=Te();t=t===void 0?null:t;var a=n.memoizedState;return a!==null&&t!==null&&vi(t,a[1])?a[0]:(e=e(),n.memoizedState=[e,t],e)}function Nl(e,t,n){return At&21?(Oe(n,t)||(n=Pc(),B.lanes|=n,qt|=n,e.baseState=!0),t):(e.baseState&&(e.baseState=!1,pe=!0),e.memoizedState=n)}function sp(e,t){var n=D;D=n!==0&&4>n?n:4,e(!0);var a=Fr.transition;Fr.transition={};try{e(!1),t()}finally{D=n,Fr.transition=a}}function Al(){return Te().memoizedState}function ip(e,t,n){var a=ut(e);if(n={lane:a,action:n,hasEagerState:!1,eagerState:null,next:null},ql(e))jl(t,n);else if(n=ul(e,t,n,a),n!==null){var r=ce();Pe(n,e,a,r),Il(n,t,a)}}function op(e,t,n){var a=ut(e),r={lane:a,action:n,hasEagerState:!1,eagerState:null,next:null};if(ql(e))jl(t,r);else{var s=e.alternate;if(e.lanes===0&&(s===null||s.lanes===0)&&(s=t.lastRenderedReducer,s!==null))try{var i=t.lastRenderedState,o=s(i,n);if(r.hasEagerState=!0,r.eagerState=o,Oe(o,i)){var c=t.interleaved;c===null?(r.next=r,hi(t)):(r.next=c.next,c.next=r),t.interleaved=r;return}}catch{}finally{}n=ul(e,t,r,a),n!==null&&(r=ce(),Pe(n,e,a,r),Il(n,t,a))}}function ql(e){var t=e.alternate;return e===B||t!==null&&t===B}function jl(e,t){An=Xa=!0;var n=e.pending;n===null?t.next=t:(t.next=n.next,n.next=t),e.pending=t}function Il(e,t,n){if(n&4194240){var a=t.lanes;a&=e.pendingLanes,n|=a,t.lanes=n,ei(e,n)}}var Ya={readContext:Re,useCallback:ae,useContext:ae,useEffect:ae,useImperativeHandle:ae,useInsertionEffect:ae,useLayoutEffect:ae,useMemo:ae,useReducer:ae,useRef:ae,useState:ae,useDebugValue:ae,useDeferredValue:ae,useTransition:ae,useMutableSource:ae,useSyncExternalStore:ae,useId:ae,unstable_isNewReconciler:!1},cp={readContext:Re,useCallback:function(e,t){return Ke().memoizedState=[e,t===void 0?null:t],e},useContext:Re,useEffect:xo,useImperativeHandle:function(e,t,n){return n=n!=null?n.concat([e]):null,Na(4194308,4,xl.bind(null,t,e),n)},useLayoutEffect:function(e,t){return Na(4194308,4,e,t)},useInsertionEffect:function(e,t){return Na(4,2,e,t)},useMemo:function(e,t){var n=Ke();return t=t===void 0?null:t,e=e(),n.memoizedState=[e,t],e},useReducer:function(e,t,n){var a=Ke();return t=n!==void 0?n(t):t,a.memoizedState=a.baseState=t,e={pending:null,interleaved:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:t},a.queue=e,e=e.dispatch=ip.bind(null,B,e),[a.memoizedState,e]},useRef:function(e){var t=Ke();return e={current:e},t.memoizedState=e},useState:Co,useDebugValue:Ci,useDeferredValue:function(e){return Ke().memoizedState=e},useTransition:function(){var e=Co(!1),t=e[0];return e=sp.bind(null,e[1]),Ke().memoizedState=e,[t,e]},useMutableSource:function(){},useSyncExternalStore:function(e,t,n){var a=B,r=Ke();if(W){if(n===void 0)throw Error(b(407));n=n()}else{if(n=t(),$===null)throw Error(b(349));At&30||fl(a,t,n)}r.memoizedState=n;var s={value:n,getSnapshot:t};return r.queue=s,xo(bl.bind(null,a,s,e),[e]),a.flags|=2048,_n(9,gl.bind(null,a,s,n,t),void 0,null),n},useId:function(){var e=Ke(),t=$.identifierPrefix;if(W){var n=Je,a=ze;n=(a&~(1<<32-Ie(a)-1)).toString(32)+n,t=":"+t+"R"+n,n=Gn++,0<n&&(t+="H"+n.toString(32)),t+=":"}else n=rp++,t=":"+t+"r"+n.toString(32)+":";return e.memoizedState=t},unstable_isNewReconciler:!1},lp={readContext:Re,useCallback:Rl,useContext:Re,useEffect:Si,useImperativeHandle:El,useInsertionEffect:Sl,useLayoutEffect:Cl,useMemo:Tl,useReducer:Ur,useRef:kl,useState:function(){return Ur(Vn)},useDebugValue:Ci,useDeferredValue:function(e){var t=Te();return Nl(t,_.memoizedState,e)},useTransition:function(){var e=Ur(Vn)[0],t=Te().memoizedState;return[e,t]},useMutableSource:hl,useSyncExternalStore:yl,useId:Al,unstable_isNewReconciler:!1},dp={readContext:Re,useCallback:Rl,useContext:Re,useEffect:Si,useImperativeHandle:El,useInsertionEffect:Sl,useLayoutEffect:Cl,useMemo:Tl,useReducer:zr,useRef:kl,useState:function(){return zr(Vn)},useDebugValue:Ci,useDeferredValue:function(e){var t=Te();return _===null?t.memoizedState=e:Nl(t,_.memoizedState,e)},useTransition:function(){var e=zr(Vn)[0],t=Te().memoizedState;return[e,t]},useMutableSource:hl,useSyncExternalStore:yl,useId:Al,unstable_isNewReconciler:!1};function Ae(e,t){if(e&&e.defaultProps){t=F({},t),e=e.defaultProps;for(var n in e)t[n]===void 0&&(t[n]=e[n]);return t}return t}function Rs(e,t,n,a){t=e.memoizedState,n=n(a,t),n=n==null?t:F({},t,n),e.memoizedState=n,e.lanes===0&&(e.updateQueue.baseState=n)}var ur={isMounted:function(e){return(e=e._reactInternals)?Pt(e)===e:!1},enqueueSetState:function(e,t,n){e=e._reactInternals;var a=ce(),r=ut(e),s=Ge(a,r);s.payload=t,n!=null&&(s.callback=n),t=lt(e,s,r),t!==null&&(Pe(t,e,r,a),Ra(t,e,r))},enqueueReplaceState:function(e,t,n){e=e._reactInternals;var a=ce(),r=ut(e),s=Ge(a,r);s.tag=1,s.payload=t,n!=null&&(s.callback=n),t=lt(e,s,r),t!==null&&(Pe(t,e,r,a),Ra(t,e,r))},enqueueForceUpdate:function(e,t){e=e._reactInternals;var n=ce(),a=ut(e),r=Ge(n,a);r.tag=2,t!=null&&(r.callback=t),t=lt(e,r,a),t!==null&&(Pe(t,e,a,n),Ra(t,e,a))}};function Eo(e,t,n,a,r,s,i){return e=e.stateNode,typeof e.shouldComponentUpdate=="function"?e.shouldComponentUpdate(a,s,i):t.prototype&&t.prototype.isPureReactComponent?!Hn(n,a)||!Hn(r,s):!0}function Pl(e,t,n){var a=!1,r=ht,s=t.contextType;return typeof s=="object"&&s!==null?s=Re(s):(r=he(t)?Tt:ie.current,a=t.contextTypes,s=(a=a!=null)?en(e,r):ht),t=new t(n,s),e.memoizedState=t.state!==null&&t.state!==void 0?t.state:null,t.updater=ur,e.stateNode=t,t._reactInternals=e,a&&(e=e.stateNode,e.__reactInternalMemoizedUnmaskedChildContext=r,e.__reactInternalMemoizedMaskedChildContext=s),t}function Ro(e,t,n,a){e=t.state,typeof t.componentWillReceiveProps=="function"&&t.componentWillReceiveProps(n,a),typeof t.UNSAFE_componentWillReceiveProps=="function"&&t.UNSAFE_componentWillReceiveProps(n,a),t.state!==e&&ur.enqueueReplaceState(t,t.state,null)}function Ts(e,t,n,a){var r=e.stateNode;r.props=n,r.state=e.memoizedState,r.refs={},yi(e);var s=t.contextType;typeof s=="object"&&s!==null?r.context=Re(s):(s=he(t)?Tt:ie.current,r.context=en(e,s)),r.state=e.memoizedState,s=t.getDerivedStateFromProps,typeof s=="function"&&(Rs(e,t,s,n),r.state=e.memoizedState),typeof t.getDerivedStateFromProps=="function"||typeof r.getSnapshotBeforeUpdate=="function"||typeof r.UNSAFE_componentWillMount!="function"&&typeof r.componentWillMount!="function"||(t=r.state,typeof r.componentWillMount=="function"&&r.componentWillMount(),typeof r.UNSAFE_componentWillMount=="function"&&r.UNSAFE_componentWillMount(),t!==r.state&&ur.enqueueReplaceState(r,r.state,null),_a(e,n,r,a),r.state=e.memoizedState),typeof r.componentDidMount=="function"&&(e.flags|=4194308)}function rn(e,t){try{var n="",a=t;do n+=Md(a),a=a.return;while(a);var r=n}catch(s){r=`
Error generating stack: `+s.message+`
`+s.stack}return{value:e,source:t,stack:r,digest:null}}function Jr(e,t,n){return{value:e,source:null,stack:n??null,digest:t??null}}function Ns(e,t){try{console.error(t.value)}catch(n){setTimeout(function(){throw n})}}var up=typeof WeakMap=="function"?WeakMap:Map;function Ol(e,t,n){n=Ge(-1,n),n.tag=3,n.payload={element:null};var a=t.value;return n.callback=function(){Za||(Za=!0,Ms=a),Ns(e,t)},n}function Dl(e,t,n){n=Ge(-1,n),n.tag=3;var a=e.type.getDerivedStateFromError;if(typeof a=="function"){var r=t.value;n.payload=function(){return a(r)},n.callback=function(){Ns(e,t)}}var s=e.stateNode;return s!==null&&typeof s.componentDidCatch=="function"&&(n.callback=function(){Ns(e,t),typeof a!="function"&&(dt===null?dt=new Set([this]):dt.add(this));var i=t.stack;this.componentDidCatch(t.value,{componentStack:i!==null?i:""})}),n}function To(e,t,n){var a=e.pingCache;if(a===null){a=e.pingCache=new up;var r=new Set;a.set(t,r)}else r=a.get(t),r===void 0&&(r=new Set,a.set(t,r));r.has(n)||(r.add(n),e=Ep.bind(null,e,t,n),t.then(e,e))}function No(e){do{var t;if((t=e.tag===13)&&(t=e.memoizedState,t=t!==null?t.dehydrated!==null:!0),t)return e;e=e.return}while(e!==null);return null}function Ao(e,t,n,a,r){return e.mode&1?(e.flags|=65536,e.lanes=r,e):(e===t?e.flags|=65536:(e.flags|=128,n.flags|=131072,n.flags&=-52805,n.tag===1&&(n.alternate===null?n.tag=17:(t=Ge(-1,1),t.tag=2,lt(n,t,1))),n.lanes|=1),e)}var pp=Ye.ReactCurrentOwner,pe=!1;function oe(e,t,n,a){t.child=e===null?dl(t,null,n,a):nn(t,e.child,n,a)}function qo(e,t,n,a,r){n=n.render;var s=t.ref;return Yt(t,r),a=wi(e,t,n,a,s,r),n=ki(),e!==null&&!pe?(t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~r,Xe(e,t,r)):(W&&n&&ci(t),t.flags|=1,oe(e,t,a,r),t.child)}function jo(e,t,n,a,r){if(e===null){var s=n.type;return typeof s=="function"&&!ji(s)&&s.defaultProps===void 0&&n.compare===null&&n.defaultProps===void 0?(t.tag=15,t.type=s,Ll(e,t,s,a,r)):(e=Ia(n.type,null,a,t,t.mode,r),e.ref=t.ref,e.return=t,t.child=e)}if(s=e.child,!(e.lanes&r)){var i=s.memoizedProps;if(n=n.compare,n=n!==null?n:Hn,n(i,a)&&e.ref===t.ref)return Xe(e,t,r)}return t.flags|=1,e=pt(s,a),e.ref=t.ref,e.return=t,t.child=e}function Ll(e,t,n,a,r){if(e!==null){var s=e.memoizedProps;if(Hn(s,a)&&e.ref===t.ref)if(pe=!1,t.pendingProps=a=s,(e.lanes&r)!==0)e.flags&131072&&(pe=!0);else return t.lanes=e.lanes,Xe(e,t,r)}return As(e,t,n,a,r)}function Kl(e,t,n){var a=t.pendingProps,r=a.children,s=e!==null?e.memoizedState:null;if(a.mode==="hidden")if(!(t.mode&1))t.memoizedState={baseLanes:0,cachePool:null,transitions:null},L(Gt,fe),fe|=n;else{if(!(n&1073741824))return e=s!==null?s.baseLanes|n:n,t.lanes=t.childLanes=1073741824,t.memoizedState={baseLanes:e,cachePool:null,transitions:null},t.updateQueue=null,L(Gt,fe),fe|=e,null;t.memoizedState={baseLanes:0,cachePool:null,transitions:null},a=s!==null?s.baseLanes:n,L(Gt,fe),fe|=a}else s!==null?(a=s.baseLanes|n,t.memoizedState=null):a=n,L(Gt,fe),fe|=a;return oe(e,t,r,n),t.child}function Ml(e,t){var n=t.ref;(e===null&&n!==null||e!==null&&e.ref!==n)&&(t.flags|=512,t.flags|=2097152)}function As(e,t,n,a,r){var s=he(n)?Tt:ie.current;return s=en(t,s),Yt(t,r),n=wi(e,t,n,a,s,r),a=ki(),e!==null&&!pe?(t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~r,Xe(e,t,r)):(W&&a&&ci(t),t.flags|=1,oe(e,t,n,r),t.child)}function Io(e,t,n,a,r){if(he(n)){var s=!0;Ua(t)}else s=!1;if(Yt(t,r),t.stateNode===null)Aa(e,t),Pl(t,n,a),Ts(t,n,a,r),a=!0;else if(e===null){var i=t.stateNode,o=t.memoizedProps;i.props=o;var c=i.context,p=n.contextType;typeof p=="object"&&p!==null?p=Re(p):(p=he(n)?Tt:ie.current,p=en(t,p));var f=n.getDerivedStateFromProps,y=typeof f=="function"||typeof i.getSnapshotBeforeUpdate=="function";y||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==a||c!==p)&&Ro(t,i,a,p),et=!1;var h=t.memoizedState;i.state=h,_a(t,a,i,r),c=t.memoizedState,o!==a||h!==c||me.current||et?(typeof f=="function"&&(Rs(t,n,f,a),c=t.memoizedState),(o=et||Eo(t,n,o,a,h,c,p))?(y||typeof i.UNSAFE_componentWillMount!="function"&&typeof i.componentWillMount!="function"||(typeof i.componentWillMount=="function"&&i.componentWillMount(),typeof i.UNSAFE_componentWillMount=="function"&&i.UNSAFE_componentWillMount()),typeof i.componentDidMount=="function"&&(t.flags|=4194308)):(typeof i.componentDidMount=="function"&&(t.flags|=4194308),t.memoizedProps=a,t.memoizedState=c),i.props=a,i.state=c,i.context=p,a=o):(typeof i.componentDidMount=="function"&&(t.flags|=4194308),a=!1)}else{i=t.stateNode,pl(e,t),o=t.memoizedProps,p=t.type===t.elementType?o:Ae(t.type,o),i.props=p,y=t.pendingProps,h=i.context,c=n.contextType,typeof c=="object"&&c!==null?c=Re(c):(c=he(n)?Tt:ie.current,c=en(t,c));var v=n.getDerivedStateFromProps;(f=typeof v=="function"||typeof i.getSnapshotBeforeUpdate=="function")||typeof i.UNSAFE_componentWillReceiveProps!="function"&&typeof i.componentWillReceiveProps!="function"||(o!==y||h!==c)&&Ro(t,i,a,c),et=!1,h=t.memoizedState,i.state=h,_a(t,a,i,r);var w=t.memoizedState;o!==y||h!==w||me.current||et?(typeof v=="function"&&(Rs(t,n,v,a),w=t.memoizedState),(p=et||Eo(t,n,p,a,h,w,c)||!1)?(f||typeof i.UNSAFE_componentWillUpdate!="function"&&typeof i.componentWillUpdate!="function"||(typeof i.componentWillUpdate=="function"&&i.componentWillUpdate(a,w,c),typeof i.UNSAFE_componentWillUpdate=="function"&&i.UNSAFE_componentWillUpdate(a,w,c)),typeof i.componentDidUpdate=="function"&&(t.flags|=4),typeof i.getSnapshotBeforeUpdate=="function"&&(t.flags|=1024)):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=1024),t.memoizedProps=a,t.memoizedState=w),i.props=a,i.state=w,i.context=c,a=p):(typeof i.componentDidUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=4),typeof i.getSnapshotBeforeUpdate!="function"||o===e.memoizedProps&&h===e.memoizedState||(t.flags|=1024),a=!1)}return qs(e,t,n,a,s,r)}function qs(e,t,n,a,r,s){Ml(e,t);var i=(t.flags&128)!==0;if(!a&&!i)return r&&go(t,n,!1),Xe(e,t,s);a=t.stateNode,pp.current=t;var o=i&&typeof n.getDerivedStateFromError!="function"?null:a.render();return t.flags|=1,e!==null&&i?(t.child=nn(t,e.child,null,s),t.child=nn(t,null,o,s)):oe(e,t,o,s),t.memoizedState=a.state,r&&go(t,n,!0),t.child}function Wl(e){var t=e.stateNode;t.pendingContext?fo(e,t.pendingContext,t.pendingContext!==t.context):t.context&&fo(e,t.context,!1),fi(e,t.containerInfo)}function Po(e,t,n,a,r){return tn(),di(r),t.flags|=256,oe(e,t,n,a),t.child}var js={dehydrated:null,treeContext:null,retryLane:0};function Is(e){return{baseLanes:e,cachePool:null,transitions:null}}function Hl(e,t,n){var a=t.pendingProps,r=H.current,s=!1,i=(t.flags&128)!==0,o;if((o=i)||(o=e!==null&&e.memoizedState===null?!1:(r&2)!==0),o?(s=!0,t.flags&=-129):(e===null||e.memoizedState!==null)&&(r|=1),L(H,r&1),e===null)return xs(t),e=t.memoizedState,e!==null&&(e=e.dehydrated,e!==null)?(t.mode&1?e.data==="$!"?t.lanes=8:t.lanes=1073741824:t.lanes=1,null):(i=a.children,e=a.fallback,s?(a=t.mode,s=t.child,i={mode:"hidden",children:i},!(a&1)&&s!==null?(s.childLanes=0,s.pendingProps=i):s=hr(i,a,0,null),e=Rt(e,a,n,null),s.return=t,e.return=t,s.sibling=e,t.child=s,t.child.memoizedState=Is(n),t.memoizedState=js,e):xi(t,i));if(r=e.memoizedState,r!==null&&(o=r.dehydrated,o!==null))return mp(e,t,i,a,o,r,n);if(s){s=a.fallback,i=t.mode,r=e.child,o=r.sibling;var c={mode:"hidden",children:a.children};return!(i&1)&&t.child!==r?(a=t.child,a.childLanes=0,a.pendingProps=c,t.deletions=null):(a=pt(r,c),a.subtreeFlags=r.subtreeFlags&14680064),o!==null?s=pt(o,s):(s=Rt(s,i,n,null),s.flags|=2),s.return=t,a.return=t,a.sibling=s,t.child=a,a=s,s=t.child,i=e.child.memoizedState,i=i===null?Is(n):{baseLanes:i.baseLanes|n,cachePool:null,transitions:i.transitions},s.memoizedState=i,s.childLanes=e.childLanes&~n,t.memoizedState=js,a}return s=e.child,e=s.sibling,a=pt(s,{mode:"visible",children:a.children}),!(t.mode&1)&&(a.lanes=n),a.return=t,a.sibling=null,e!==null&&(n=t.deletions,n===null?(t.deletions=[e],t.flags|=16):n.push(e)),t.child=a,t.memoizedState=null,a}function xi(e,t){return t=hr({mode:"visible",children:t},e.mode,0,null),t.return=e,e.child=t}function ga(e,t,n,a){return a!==null&&di(a),nn(t,e.child,null,n),e=xi(t,t.pendingProps.children),e.flags|=2,t.memoizedState=null,e}function mp(e,t,n,a,r,s,i){if(n)return t.flags&256?(t.flags&=-257,a=Jr(Error(b(422))),ga(e,t,i,a)):t.memoizedState!==null?(t.child=e.child,t.flags|=128,null):(s=a.fallback,r=t.mode,a=hr({mode:"visible",children:a.children},r,0,null),s=Rt(s,r,i,null),s.flags|=2,a.return=t,s.return=t,a.sibling=s,t.child=a,t.mode&1&&nn(t,e.child,null,i),t.child.memoizedState=Is(i),t.memoizedState=js,s);if(!(t.mode&1))return ga(e,t,i,null);if(r.data==="$!"){if(a=r.nextSibling&&r.nextSibling.dataset,a)var o=a.dgst;return a=o,s=Error(b(419)),a=Jr(s,a,void 0),ga(e,t,i,a)}if(o=(i&e.childLanes)!==0,pe||o){if(a=$,a!==null){switch(i&-i){case 4:r=2;break;case 16:r=8;break;case 64:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:case 67108864:r=32;break;case 536870912:r=268435456;break;default:r=0}r=r&(a.suspendedLanes|i)?0:r,r!==0&&r!==s.retryLane&&(s.retryLane=r,Qe(e,r),Pe(a,e,r,-1))}return qi(),a=Jr(Error(b(421))),ga(e,t,i,a)}return r.data==="$?"?(t.flags|=128,t.child=e.child,t=Rp.bind(null,e),r._reactRetry=t,null):(e=s.treeContext,ge=ct(r.nextSibling),be=t,W=!0,je=null,e!==null&&(Se[Ce++]=ze,Se[Ce++]=Je,Se[Ce++]=Nt,ze=e.id,Je=e.overflow,Nt=t),t=xi(t,a.children),t.flags|=4096,t)}function Oo(e,t,n){e.lanes|=t;var a=e.alternate;a!==null&&(a.lanes|=t),Es(e.return,t,n)}function Gr(e,t,n,a,r){var s=e.memoizedState;s===null?e.memoizedState={isBackwards:t,rendering:null,renderingStartTime:0,last:a,tail:n,tailMode:r}:(s.isBackwards=t,s.rendering=null,s.renderingStartTime=0,s.last=a,s.tail=n,s.tailMode=r)}function Bl(e,t,n){var a=t.pendingProps,r=a.revealOrder,s=a.tail;if(oe(e,t,a.children,n),a=H.current,a&2)a=a&1|2,t.flags|=128;else{if(e!==null&&e.flags&128)e:for(e=t.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&Oo(e,n,t);else if(e.tag===19)Oo(e,n,t);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===t)break e;for(;e.sibling===null;){if(e.return===null||e.return===t)break e;e=e.return}e.sibling.return=e.return,e=e.sibling}a&=1}if(L(H,a),!(t.mode&1))t.memoizedState=null;else switch(r){case"forwards":for(n=t.child,r=null;n!==null;)e=n.alternate,e!==null&&Qa(e)===null&&(r=n),n=n.sibling;n=r,n===null?(r=t.child,t.child=null):(r=n.sibling,n.sibling=null),Gr(t,!1,r,n,s);break;case"backwards":for(n=null,r=t.child,t.child=null;r!==null;){if(e=r.alternate,e!==null&&Qa(e)===null){t.child=r;break}e=r.sibling,r.sibling=n,n=r,r=e}Gr(t,!0,n,null,s);break;case"together":Gr(t,!1,null,null,void 0);break;default:t.memoizedState=null}return t.child}function Aa(e,t){!(t.mode&1)&&e!==null&&(e.alternate=null,t.alternate=null,t.flags|=2)}function Xe(e,t,n){if(e!==null&&(t.dependencies=e.dependencies),qt|=t.lanes,!(n&t.childLanes))return null;if(e!==null&&t.child!==e.child)throw Error(b(153));if(t.child!==null){for(e=t.child,n=pt(e,e.pendingProps),t.child=n,n.return=t;e.sibling!==null;)e=e.sibling,n=n.sibling=pt(e,e.pendingProps),n.return=t;n.sibling=null}return t.child}function hp(e,t,n){switch(t.tag){case 3:Wl(t),tn();break;case 5:ml(t);break;case 1:he(t.type)&&Ua(t);break;case 4:fi(t,t.stateNode.containerInfo);break;case 10:var a=t.type._context,r=t.memoizedProps.value;L(Ga,a._currentValue),a._currentValue=r;break;case 13:if(a=t.memoizedState,a!==null)return a.dehydrated!==null?(L(H,H.current&1),t.flags|=128,null):n&t.child.childLanes?Hl(e,t,n):(L(H,H.current&1),e=Xe(e,t,n),e!==null?e.sibling:null);L(H,H.current&1);break;case 19:if(a=(n&t.childLanes)!==0,e.flags&128){if(a)return Bl(e,t,n);t.flags|=128}if(r=t.memoizedState,r!==null&&(r.rendering=null,r.tail=null,r.lastEffect=null),L(H,H.current),a)break;return null;case 22:case 23:return t.lanes=0,Kl(e,t,n)}return Xe(e,t,n)}var Fl,Ps,Ul,zl;Fl=function(e,t){for(var n=t.child;n!==null;){if(n.tag===5||n.tag===6)e.appendChild(n.stateNode);else if(n.tag!==4&&n.child!==null){n.child.return=n,n=n.child;continue}if(n===t)break;for(;n.sibling===null;){if(n.return===null||n.return===t)return;n=n.return}n.sibling.return=n.return,n=n.sibling}};Ps=function(){};Ul=function(e,t,n,a){var r=e.memoizedProps;if(r!==a){e=t.stateNode,xt(He.current);var s=null;switch(n){case"input":r=ts(e,r),a=ts(e,a),s=[];break;case"select":r=F({},r,{value:void 0}),a=F({},a,{value:void 0}),s=[];break;case"textarea":r=rs(e,r),a=rs(e,a),s=[];break;default:typeof r.onClick!="function"&&typeof a.onClick=="function"&&(e.onclick=Ba)}is(n,a);var i;n=null;for(p in r)if(!a.hasOwnProperty(p)&&r.hasOwnProperty(p)&&r[p]!=null)if(p==="style"){var o=r[p];for(i in o)o.hasOwnProperty(i)&&(n||(n={}),n[i]="")}else p!=="dangerouslySetInnerHTML"&&p!=="children"&&p!=="suppressContentEditableWarning"&&p!=="suppressHydrationWarning"&&p!=="autoFocus"&&(Pn.hasOwnProperty(p)?s||(s=[]):(s=s||[]).push(p,null));for(p in a){var c=a[p];if(o=r!=null?r[p]:void 0,a.hasOwnProperty(p)&&c!==o&&(c!=null||o!=null))if(p==="style")if(o){for(i in o)!o.hasOwnProperty(i)||c&&c.hasOwnProperty(i)||(n||(n={}),n[i]="");for(i in c)c.hasOwnProperty(i)&&o[i]!==c[i]&&(n||(n={}),n[i]=c[i])}else n||(s||(s=[]),s.push(p,n)),n=c;else p==="dangerouslySetInnerHTML"?(c=c?c.__html:void 0,o=o?o.__html:void 0,c!=null&&o!==c&&(s=s||[]).push(p,c)):p==="children"?typeof c!="string"&&typeof c!="number"||(s=s||[]).push(p,""+c):p!=="suppressContentEditableWarning"&&p!=="suppressHydrationWarning"&&(Pn.hasOwnProperty(p)?(c!=null&&p==="onScroll"&&K("scroll",e),s||o===c||(s=[])):(s=s||[]).push(p,c))}n&&(s=s||[]).push("style",n);var p=s;(t.updateQueue=p)&&(t.flags|=4)}};zl=function(e,t,n,a){n!==a&&(t.flags|=4)};function fn(e,t){if(!W)switch(e.tailMode){case"hidden":t=e.tail;for(var n=null;t!==null;)t.alternate!==null&&(n=t),t=t.sibling;n===null?e.tail=null:n.sibling=null;break;case"collapsed":n=e.tail;for(var a=null;n!==null;)n.alternate!==null&&(a=n),n=n.sibling;a===null?t||e.tail===null?e.tail=null:e.tail.sibling=null:a.sibling=null}}function re(e){var t=e.alternate!==null&&e.alternate.child===e.child,n=0,a=0;if(t)for(var r=e.child;r!==null;)n|=r.lanes|r.childLanes,a|=r.subtreeFlags&14680064,a|=r.flags&14680064,r.return=e,r=r.sibling;else for(r=e.child;r!==null;)n|=r.lanes|r.childLanes,a|=r.subtreeFlags,a|=r.flags,r.return=e,r=r.sibling;return e.subtreeFlags|=a,e.childLanes=n,t}function yp(e,t,n){var a=t.pendingProps;switch(li(t),t.tag){case 2:case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return re(t),null;case 1:return he(t.type)&&Fa(),re(t),null;case 3:return a=t.stateNode,an(),M(me),M(ie),bi(),a.pendingContext&&(a.context=a.pendingContext,a.pendingContext=null),(e===null||e.child===null)&&(ya(t)?t.flags|=4:e===null||e.memoizedState.isDehydrated&&!(t.flags&256)||(t.flags|=1024,je!==null&&(Bs(je),je=null))),Ps(e,t),re(t),null;case 5:gi(t);var r=xt(Jn.current);if(n=t.type,e!==null&&t.stateNode!=null)Ul(e,t,n,a,r),e.ref!==t.ref&&(t.flags|=512,t.flags|=2097152);else{if(!a){if(t.stateNode===null)throw Error(b(166));return re(t),null}if(e=xt(He.current),ya(t)){a=t.stateNode,n=t.type;var s=t.memoizedProps;switch(a[Me]=t,a[Un]=s,e=(t.mode&1)!==0,n){case"dialog":K("cancel",a),K("close",a);break;case"iframe":case"object":case"embed":K("load",a);break;case"video":case"audio":for(r=0;r<Cn.length;r++)K(Cn[r],a);break;case"source":K("error",a);break;case"img":case"image":case"link":K("error",a),K("load",a);break;case"details":K("toggle",a);break;case"input":Fi(a,s),K("invalid",a);break;case"select":a._wrapperState={wasMultiple:!!s.multiple},K("invalid",a);break;case"textarea":zi(a,s),K("invalid",a)}is(n,s),r=null;for(var i in s)if(s.hasOwnProperty(i)){var o=s[i];i==="children"?typeof o=="string"?a.textContent!==o&&(s.suppressHydrationWarning!==!0&&ha(a.textContent,o,e),r=["children",o]):typeof o=="number"&&a.textContent!==""+o&&(s.suppressHydrationWarning!==!0&&ha(a.textContent,o,e),r=["children",""+o]):Pn.hasOwnProperty(i)&&o!=null&&i==="onScroll"&&K("scroll",a)}switch(n){case"input":ia(a),Ui(a,s,!0);break;case"textarea":ia(a),Ji(a);break;case"select":case"option":break;default:typeof s.onClick=="function"&&(a.onclick=Ba)}a=r,t.updateQueue=a,a!==null&&(t.flags|=4)}else{i=r.nodeType===9?r:r.ownerDocument,e==="http://www.w3.org/1999/xhtml"&&(e=bc(n)),e==="http://www.w3.org/1999/xhtml"?n==="script"?(e=i.createElement("div"),e.innerHTML="<script><\/script>",e=e.removeChild(e.firstChild)):typeof a.is=="string"?e=i.createElement(n,{is:a.is}):(e=i.createElement(n),n==="select"&&(i=e,a.multiple?i.multiple=!0:a.size&&(i.size=a.size))):e=i.createElementNS(e,n),e[Me]=t,e[Un]=a,Fl(e,t,!1,!1),t.stateNode=e;e:{switch(i=os(n,a),n){case"dialog":K("cancel",e),K("close",e),r=a;break;case"iframe":case"object":case"embed":K("load",e),r=a;break;case"video":case"audio":for(r=0;r<Cn.length;r++)K(Cn[r],e);r=a;break;case"source":K("error",e),r=a;break;case"img":case"image":case"link":K("error",e),K("load",e),r=a;break;case"details":K("toggle",e),r=a;break;case"input":Fi(e,a),r=ts(e,a),K("invalid",e);break;case"option":r=a;break;case"select":e._wrapperState={wasMultiple:!!a.multiple},r=F({},a,{value:void 0}),K("invalid",e);break;case"textarea":zi(e,a),r=rs(e,a),K("invalid",e);break;default:r=a}is(n,r),o=r;for(s in o)if(o.hasOwnProperty(s)){var c=o[s];s==="style"?kc(e,c):s==="dangerouslySetInnerHTML"?(c=c?c.__html:void 0,c!=null&&vc(e,c)):s==="children"?typeof c=="string"?(n!=="textarea"||c!=="")&&On(e,c):typeof c=="number"&&On(e,""+c):s!=="suppressContentEditableWarning"&&s!=="suppressHydrationWarning"&&s!=="autoFocus"&&(Pn.hasOwnProperty(s)?c!=null&&s==="onScroll"&&K("scroll",e):c!=null&&_s(e,s,c,i))}switch(n){case"input":ia(e),Ui(e,a,!1);break;case"textarea":ia(e),Ji(e);break;case"option":a.value!=null&&e.setAttribute("value",""+mt(a.value));break;case"select":e.multiple=!!a.multiple,s=a.value,s!=null?Vt(e,!!a.multiple,s,!1):a.defaultValue!=null&&Vt(e,!!a.multiple,a.defaultValue,!0);break;default:typeof r.onClick=="function"&&(e.onclick=Ba)}switch(n){case"button":case"input":case"select":case"textarea":a=!!a.autoFocus;break e;case"img":a=!0;break e;default:a=!1}}a&&(t.flags|=4)}t.ref!==null&&(t.flags|=512,t.flags|=2097152)}return re(t),null;case 6:if(e&&t.stateNode!=null)zl(e,t,e.memoizedProps,a);else{if(typeof a!="string"&&t.stateNode===null)throw Error(b(166));if(n=xt(Jn.current),xt(He.current),ya(t)){if(a=t.stateNode,n=t.memoizedProps,a[Me]=t,(s=a.nodeValue!==n)&&(e=be,e!==null))switch(e.tag){case 3:ha(a.nodeValue,n,(e.mode&1)!==0);break;case 5:e.memoizedProps.suppressHydrationWarning!==!0&&ha(a.nodeValue,n,(e.mode&1)!==0)}s&&(t.flags|=4)}else a=(n.nodeType===9?n:n.ownerDocument).createTextNode(a),a[Me]=t,t.stateNode=a}return re(t),null;case 13:if(M(H),a=t.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(W&&ge!==null&&t.mode&1&&!(t.flags&128))cl(),tn(),t.flags|=98560,s=!1;else if(s=ya(t),a!==null&&a.dehydrated!==null){if(e===null){if(!s)throw Error(b(318));if(s=t.memoizedState,s=s!==null?s.dehydrated:null,!s)throw Error(b(317));s[Me]=t}else tn(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;re(t),s=!1}else je!==null&&(Bs(je),je=null),s=!0;if(!s)return t.flags&65536?t:null}return t.flags&128?(t.lanes=n,t):(a=a!==null,a!==(e!==null&&e.memoizedState!==null)&&a&&(t.child.flags|=8192,t.mode&1&&(e===null||H.current&1?Q===0&&(Q=3):qi())),t.updateQueue!==null&&(t.flags|=4),re(t),null);case 4:return an(),Ps(e,t),e===null&&Bn(t.stateNode.containerInfo),re(t),null;case 10:return mi(t.type._context),re(t),null;case 17:return he(t.type)&&Fa(),re(t),null;case 19:if(M(H),s=t.memoizedState,s===null)return re(t),null;if(a=(t.flags&128)!==0,i=s.rendering,i===null)if(a)fn(s,!1);else{if(Q!==0||e!==null&&e.flags&128)for(e=t.child;e!==null;){if(i=Qa(e),i!==null){for(t.flags|=128,fn(s,!1),a=i.updateQueue,a!==null&&(t.updateQueue=a,t.flags|=4),t.subtreeFlags=0,a=n,n=t.child;n!==null;)s=n,e=a,s.flags&=14680066,i=s.alternate,i===null?(s.childLanes=0,s.lanes=e,s.child=null,s.subtreeFlags=0,s.memoizedProps=null,s.memoizedState=null,s.updateQueue=null,s.dependencies=null,s.stateNode=null):(s.childLanes=i.childLanes,s.lanes=i.lanes,s.child=i.child,s.subtreeFlags=0,s.deletions=null,s.memoizedProps=i.memoizedProps,s.memoizedState=i.memoizedState,s.updateQueue=i.updateQueue,s.type=i.type,e=i.dependencies,s.dependencies=e===null?null:{lanes:e.lanes,firstContext:e.firstContext}),n=n.sibling;return L(H,H.current&1|2),t.child}e=e.sibling}s.tail!==null&&J()>sn&&(t.flags|=128,a=!0,fn(s,!1),t.lanes=4194304)}else{if(!a)if(e=Qa(i),e!==null){if(t.flags|=128,a=!0,n=e.updateQueue,n!==null&&(t.updateQueue=n,t.flags|=4),fn(s,!0),s.tail===null&&s.tailMode==="hidden"&&!i.alternate&&!W)return re(t),null}else 2*J()-s.renderingStartTime>sn&&n!==1073741824&&(t.flags|=128,a=!0,fn(s,!1),t.lanes=4194304);s.isBackwards?(i.sibling=t.child,t.child=i):(n=s.last,n!==null?n.sibling=i:t.child=i,s.last=i)}return s.tail!==null?(t=s.tail,s.rendering=t,s.tail=t.sibling,s.renderingStartTime=J(),t.sibling=null,n=H.current,L(H,a?n&1|2:n&1),t):(re(t),null);case 22:case 23:return Ai(),a=t.memoizedState!==null,e!==null&&e.memoizedState!==null!==a&&(t.flags|=8192),a&&t.mode&1?fe&1073741824&&(re(t),t.subtreeFlags&6&&(t.flags|=8192)):re(t),null;case 24:return null;case 25:return null}throw Error(b(156,t.tag))}function fp(e,t){switch(li(t),t.tag){case 1:return he(t.type)&&Fa(),e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 3:return an(),M(me),M(ie),bi(),e=t.flags,e&65536&&!(e&128)?(t.flags=e&-65537|128,t):null;case 5:return gi(t),null;case 13:if(M(H),e=t.memoizedState,e!==null&&e.dehydrated!==null){if(t.alternate===null)throw Error(b(340));tn()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 19:return M(H),null;case 4:return an(),null;case 10:return mi(t.type._context),null;case 22:case 23:return Ai(),null;case 24:return null;default:return null}}var ba=!1,se=!1,gp=typeof WeakSet=="function"?WeakSet:Set,x=null;function Jt(e,t){var n=e.ref;if(n!==null)if(typeof n=="function")try{n(null)}catch(a){U(e,t,a)}else n.current=null}function Os(e,t,n){try{n()}catch(a){U(e,t,a)}}var Do=!1;function bp(e,t){if(gs=Ma,e=Qc(),oi(e)){if("selectionStart"in e)var n={start:e.selectionStart,end:e.selectionEnd};else e:{n=(n=e.ownerDocument)&&n.defaultView||window;var a=n.getSelection&&n.getSelection();if(a&&a.rangeCount!==0){n=a.anchorNode;var r=a.anchorOffset,s=a.focusNode;a=a.focusOffset;try{n.nodeType,s.nodeType}catch{n=null;break e}var i=0,o=-1,c=-1,p=0,f=0,y=e,h=null;t:for(;;){for(var v;y!==n||r!==0&&y.nodeType!==3||(o=i+r),y!==s||a!==0&&y.nodeType!==3||(c=i+a),y.nodeType===3&&(i+=y.nodeValue.length),(v=y.firstChild)!==null;)h=y,y=v;for(;;){if(y===e)break t;if(h===n&&++p===r&&(o=i),h===s&&++f===a&&(c=i),(v=y.nextSibling)!==null)break;y=h,h=y.parentNode}y=v}n=o===-1||c===-1?null:{start:o,end:c}}else n=null}n=n||{start:0,end:0}}else n=null;for(bs={focusedElem:e,selectionRange:n},Ma=!1,x=t;x!==null;)if(t=x,e=t.child,(t.subtreeFlags&1028)!==0&&e!==null)e.return=t,x=e;else for(;x!==null;){t=x;try{var w=t.alternate;if(t.flags&1024)switch(t.tag){case 0:case 11:case 15:break;case 1:if(w!==null){var k=w.memoizedProps,j=w.memoizedState,u=t.stateNode,l=u.getSnapshotBeforeUpdate(t.elementType===t.type?k:Ae(t.type,k),j);u.__reactInternalSnapshotBeforeUpdate=l}break;case 3:var m=t.stateNode.containerInfo;m.nodeType===1?m.textContent="":m.nodeType===9&&m.documentElement&&m.removeChild(m.documentElement);break;case 5:case 6:case 4:case 17:break;default:throw Error(b(163))}}catch(g){U(t,t.return,g)}if(e=t.sibling,e!==null){e.return=t.return,x=e;break}x=t.return}return w=Do,Do=!1,w}function qn(e,t,n){var a=t.updateQueue;if(a=a!==null?a.lastEffect:null,a!==null){var r=a=a.next;do{if((r.tag&e)===e){var s=r.destroy;r.destroy=void 0,s!==void 0&&Os(t,n,s)}r=r.next}while(r!==a)}}function pr(e,t){if(t=t.updateQueue,t=t!==null?t.lastEffect:null,t!==null){var n=t=t.next;do{if((n.tag&e)===e){var a=n.create;n.destroy=a()}n=n.next}while(n!==t)}}function Ds(e){var t=e.ref;if(t!==null){var n=e.stateNode;switch(e.tag){case 5:e=n;break;default:e=n}typeof t=="function"?t(e):t.current=e}}function Jl(e){var t=e.alternate;t!==null&&(e.alternate=null,Jl(t)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(t=e.stateNode,t!==null&&(delete t[Me],delete t[Un],delete t[ks],delete t[ep],delete t[tp])),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}function Gl(e){return e.tag===5||e.tag===3||e.tag===4}function Lo(e){e:for(;;){for(;e.sibling===null;){if(e.return===null||Gl(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.flags&2||e.child===null||e.tag===4)continue e;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Ls(e,t,n){var a=e.tag;if(a===5||a===6)e=e.stateNode,t?n.nodeType===8?n.parentNode.insertBefore(e,t):n.insertBefore(e,t):(n.nodeType===8?(t=n.parentNode,t.insertBefore(e,n)):(t=n,t.appendChild(e)),n=n._reactRootContainer,n!=null||t.onclick!==null||(t.onclick=Ba));else if(a!==4&&(e=e.child,e!==null))for(Ls(e,t,n),e=e.sibling;e!==null;)Ls(e,t,n),e=e.sibling}function Ks(e,t,n){var a=e.tag;if(a===5||a===6)e=e.stateNode,t?n.insertBefore(e,t):n.appendChild(e);else if(a!==4&&(e=e.child,e!==null))for(Ks(e,t,n),e=e.sibling;e!==null;)Ks(e,t,n),e=e.sibling}var Z=null,qe=!1;function $e(e,t,n){for(n=n.child;n!==null;)Vl(e,t,n),n=n.sibling}function Vl(e,t,n){if(We&&typeof We.onCommitFiberUnmount=="function")try{We.onCommitFiberUnmount(rr,n)}catch{}switch(n.tag){case 5:se||Jt(n,t);case 6:var a=Z,r=qe;Z=null,$e(e,t,n),Z=a,qe=r,Z!==null&&(qe?(e=Z,n=n.stateNode,e.nodeType===8?e.parentNode.removeChild(n):e.removeChild(n)):Z.removeChild(n.stateNode));break;case 18:Z!==null&&(qe?(e=Z,n=n.stateNode,e.nodeType===8?Wr(e.parentNode,n):e.nodeType===1&&Wr(e,n),Mn(e)):Wr(Z,n.stateNode));break;case 4:a=Z,r=qe,Z=n.stateNode.containerInfo,qe=!0,$e(e,t,n),Z=a,qe=r;break;case 0:case 11:case 14:case 15:if(!se&&(a=n.updateQueue,a!==null&&(a=a.lastEffect,a!==null))){r=a=a.next;do{var s=r,i=s.destroy;s=s.tag,i!==void 0&&(s&2||s&4)&&Os(n,t,i),r=r.next}while(r!==a)}$e(e,t,n);break;case 1:if(!se&&(Jt(n,t),a=n.stateNode,typeof a.componentWillUnmount=="function"))try{a.props=n.memoizedProps,a.state=n.memoizedState,a.componentWillUnmount()}catch(o){U(n,t,o)}$e(e,t,n);break;case 21:$e(e,t,n);break;case 22:n.mode&1?(se=(a=se)||n.memoizedState!==null,$e(e,t,n),se=a):$e(e,t,n);break;default:$e(e,t,n)}}function Ko(e){var t=e.updateQueue;if(t!==null){e.updateQueue=null;var n=e.stateNode;n===null&&(n=e.stateNode=new gp),t.forEach(function(a){var r=Tp.bind(null,e,a);n.has(a)||(n.add(a),a.then(r,r))})}}function Ne(e,t){var n=t.deletions;if(n!==null)for(var a=0;a<n.length;a++){var r=n[a];try{var s=e,i=t,o=i;e:for(;o!==null;){switch(o.tag){case 5:Z=o.stateNode,qe=!1;break e;case 3:Z=o.stateNode.containerInfo,qe=!0;break e;case 4:Z=o.stateNode.containerInfo,qe=!0;break e}o=o.return}if(Z===null)throw Error(b(160));Vl(s,i,r),Z=null,qe=!1;var c=r.alternate;c!==null&&(c.return=null),r.return=null}catch(p){U(r,t,p)}}if(t.subtreeFlags&12854)for(t=t.child;t!==null;)_l(t,e),t=t.sibling}function _l(e,t){var n=e.alternate,a=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:if(Ne(t,e),Le(e),a&4){try{qn(3,e,e.return),pr(3,e)}catch(k){U(e,e.return,k)}try{qn(5,e,e.return)}catch(k){U(e,e.return,k)}}break;case 1:Ne(t,e),Le(e),a&512&&n!==null&&Jt(n,n.return);break;case 5:if(Ne(t,e),Le(e),a&512&&n!==null&&Jt(n,n.return),e.flags&32){var r=e.stateNode;try{On(r,"")}catch(k){U(e,e.return,k)}}if(a&4&&(r=e.stateNode,r!=null)){var s=e.memoizedProps,i=n!==null?n.memoizedProps:s,o=e.type,c=e.updateQueue;if(e.updateQueue=null,c!==null)try{o==="input"&&s.type==="radio"&&s.name!=null&&fc(r,s),os(o,i);var p=os(o,s);for(i=0;i<c.length;i+=2){var f=c[i],y=c[i+1];f==="style"?kc(r,y):f==="dangerouslySetInnerHTML"?vc(r,y):f==="children"?On(r,y):_s(r,f,y,p)}switch(o){case"input":ns(r,s);break;case"textarea":gc(r,s);break;case"select":var h=r._wrapperState.wasMultiple;r._wrapperState.wasMultiple=!!s.multiple;var v=s.value;v!=null?Vt(r,!!s.multiple,v,!1):h!==!!s.multiple&&(s.defaultValue!=null?Vt(r,!!s.multiple,s.defaultValue,!0):Vt(r,!!s.multiple,s.multiple?[]:"",!1))}r[Un]=s}catch(k){U(e,e.return,k)}}break;case 6:if(Ne(t,e),Le(e),a&4){if(e.stateNode===null)throw Error(b(162));r=e.stateNode,s=e.memoizedProps;try{r.nodeValue=s}catch(k){U(e,e.return,k)}}break;case 3:if(Ne(t,e),Le(e),a&4&&n!==null&&n.memoizedState.isDehydrated)try{Mn(t.containerInfo)}catch(k){U(e,e.return,k)}break;case 4:Ne(t,e),Le(e);break;case 13:Ne(t,e),Le(e),r=e.child,r.flags&8192&&(s=r.memoizedState!==null,r.stateNode.isHidden=s,!s||r.alternate!==null&&r.alternate.memoizedState!==null||(Ti=J())),a&4&&Ko(e);break;case 22:if(f=n!==null&&n.memoizedState!==null,e.mode&1?(se=(p=se)||f,Ne(t,e),se=p):Ne(t,e),Le(e),a&8192){if(p=e.memoizedState!==null,(e.stateNode.isHidden=p)&&!f&&e.mode&1)for(x=e,f=e.child;f!==null;){for(y=x=f;x!==null;){switch(h=x,v=h.child,h.tag){case 0:case 11:case 14:case 15:qn(4,h,h.return);break;case 1:Jt(h,h.return);var w=h.stateNode;if(typeof w.componentWillUnmount=="function"){a=h,n=h.return;try{t=a,w.props=t.memoizedProps,w.state=t.memoizedState,w.componentWillUnmount()}catch(k){U(a,n,k)}}break;case 5:Jt(h,h.return);break;case 22:if(h.memoizedState!==null){Wo(y);continue}}v!==null?(v.return=h,x=v):Wo(y)}f=f.sibling}e:for(f=null,y=e;;){if(y.tag===5){if(f===null){f=y;try{r=y.stateNode,p?(s=r.style,typeof s.setProperty=="function"?s.setProperty("display","none","important"):s.display="none"):(o=y.stateNode,c=y.memoizedProps.style,i=c!=null&&c.hasOwnProperty("display")?c.display:null,o.style.display=wc("display",i))}catch(k){U(e,e.return,k)}}}else if(y.tag===6){if(f===null)try{y.stateNode.nodeValue=p?"":y.memoizedProps}catch(k){U(e,e.return,k)}}else if((y.tag!==22&&y.tag!==23||y.memoizedState===null||y===e)&&y.child!==null){y.child.return=y,y=y.child;continue}if(y===e)break e;for(;y.sibling===null;){if(y.return===null||y.return===e)break e;f===y&&(f=null),y=y.return}f===y&&(f=null),y.sibling.return=y.return,y=y.sibling}}break;case 19:Ne(t,e),Le(e),a&4&&Ko(e);break;case 21:break;default:Ne(t,e),Le(e)}}function Le(e){var t=e.flags;if(t&2){try{e:{for(var n=e.return;n!==null;){if(Gl(n)){var a=n;break e}n=n.return}throw Error(b(160))}switch(a.tag){case 5:var r=a.stateNode;a.flags&32&&(On(r,""),a.flags&=-33);var s=Lo(e);Ks(e,s,r);break;case 3:case 4:var i=a.stateNode.containerInfo,o=Lo(e);Ls(e,o,i);break;default:throw Error(b(161))}}catch(c){U(e,e.return,c)}e.flags&=-3}t&4096&&(e.flags&=-4097)}function vp(e,t,n){x=e,Ql(e)}function Ql(e,t,n){for(var a=(e.mode&1)!==0;x!==null;){var r=x,s=r.child;if(r.tag===22&&a){var i=r.memoizedState!==null||ba;if(!i){var o=r.alternate,c=o!==null&&o.memoizedState!==null||se;o=ba;var p=se;if(ba=i,(se=c)&&!p)for(x=r;x!==null;)i=x,c=i.child,i.tag===22&&i.memoizedState!==null?Ho(r):c!==null?(c.return=i,x=c):Ho(r);for(;s!==null;)x=s,Ql(s),s=s.sibling;x=r,ba=o,se=p}Mo(e)}else r.subtreeFlags&8772&&s!==null?(s.return=r,x=s):Mo(e)}}function Mo(e){for(;x!==null;){var t=x;if(t.flags&8772){var n=t.alternate;try{if(t.flags&8772)switch(t.tag){case 0:case 11:case 15:se||pr(5,t);break;case 1:var a=t.stateNode;if(t.flags&4&&!se)if(n===null)a.componentDidMount();else{var r=t.elementType===t.type?n.memoizedProps:Ae(t.type,n.memoizedProps);a.componentDidUpdate(r,n.memoizedState,a.__reactInternalSnapshotBeforeUpdate)}var s=t.updateQueue;s!==null&&So(t,s,a);break;case 3:var i=t.updateQueue;if(i!==null){if(n=null,t.child!==null)switch(t.child.tag){case 5:n=t.child.stateNode;break;case 1:n=t.child.stateNode}So(t,i,n)}break;case 5:var o=t.stateNode;if(n===null&&t.flags&4){n=o;var c=t.memoizedProps;switch(t.type){case"button":case"input":case"select":case"textarea":c.autoFocus&&n.focus();break;case"img":c.src&&(n.src=c.src)}}break;case 6:break;case 4:break;case 12:break;case 13:if(t.memoizedState===null){var p=t.alternate;if(p!==null){var f=p.memoizedState;if(f!==null){var y=f.dehydrated;y!==null&&Mn(y)}}}break;case 19:case 17:case 21:case 22:case 23:case 25:break;default:throw Error(b(163))}se||t.flags&512&&Ds(t)}catch(h){U(t,t.return,h)}}if(t===e){x=null;break}if(n=t.sibling,n!==null){n.return=t.return,x=n;break}x=t.return}}function Wo(e){for(;x!==null;){var t=x;if(t===e){x=null;break}var n=t.sibling;if(n!==null){n.return=t.return,x=n;break}x=t.return}}function Ho(e){for(;x!==null;){var t=x;try{switch(t.tag){case 0:case 11:case 15:var n=t.return;try{pr(4,t)}catch(c){U(t,n,c)}break;case 1:var a=t.stateNode;if(typeof a.componentDidMount=="function"){var r=t.return;try{a.componentDidMount()}catch(c){U(t,r,c)}}var s=t.return;try{Ds(t)}catch(c){U(t,s,c)}break;case 5:var i=t.return;try{Ds(t)}catch(c){U(t,i,c)}}}catch(c){U(t,t.return,c)}if(t===e){x=null;break}var o=t.sibling;if(o!==null){o.return=t.return,x=o;break}x=t.return}}var wp=Math.ceil,$a=Ye.ReactCurrentDispatcher,Ei=Ye.ReactCurrentOwner,Ee=Ye.ReactCurrentBatchConfig,O=0,$=null,V=null,ee=0,fe=0,Gt=ft(0),Q=0,Qn=null,qt=0,mr=0,Ri=0,jn=null,ue=null,Ti=0,sn=1/0,Fe=null,Za=!1,Ms=null,dt=null,va=!1,rt=null,er=0,In=0,Ws=null,qa=-1,ja=0;function ce(){return O&6?J():qa!==-1?qa:qa=J()}function ut(e){return e.mode&1?O&2&&ee!==0?ee&-ee:ap.transition!==null?(ja===0&&(ja=Pc()),ja):(e=D,e!==0||(e=window.event,e=e===void 0?16:Hc(e.type)),e):1}function Pe(e,t,n,a){if(50<In)throw In=0,Ws=null,Error(b(185));Yn(e,n,a),(!(O&2)||e!==$)&&(e===$&&(!(O&2)&&(mr|=n),Q===4&&nt(e,ee)),ye(e,a),n===1&&O===0&&!(t.mode&1)&&(sn=J()+500,lr&&gt()))}function ye(e,t){var n=e.callbackNode;au(e,t);var a=Ka(e,e===$?ee:0);if(a===0)n!==null&&_i(n),e.callbackNode=null,e.callbackPriority=0;else if(t=a&-a,e.callbackPriority!==t){if(n!=null&&_i(n),t===1)e.tag===0?np(Bo.bind(null,e)):sl(Bo.bind(null,e)),$u(function(){!(O&6)&&gt()}),n=null;else{switch(Oc(a)){case 1:n=Zs;break;case 4:n=jc;break;case 16:n=La;break;case 536870912:n=Ic;break;default:n=La}n=ad(n,Xl.bind(null,e))}e.callbackPriority=t,e.callbackNode=n}}function Xl(e,t){if(qa=-1,ja=0,O&6)throw Error(b(327));var n=e.callbackNode;if($t()&&e.callbackNode!==n)return null;var a=Ka(e,e===$?ee:0);if(a===0)return null;if(a&30||a&e.expiredLanes||t)t=tr(e,a);else{t=a;var r=O;O|=2;var s=$l();($!==e||ee!==t)&&(Fe=null,sn=J()+500,Et(e,t));do try{Cp();break}catch(o){Yl(e,o)}while(!0);pi(),$a.current=s,O=r,V!==null?t=0:($=null,ee=0,t=Q)}if(t!==0){if(t===2&&(r=ps(e),r!==0&&(a=r,t=Hs(e,r))),t===1)throw n=Qn,Et(e,0),nt(e,a),ye(e,J()),n;if(t===6)nt(e,a);else{if(r=e.current.alternate,!(a&30)&&!kp(r)&&(t=tr(e,a),t===2&&(s=ps(e),s!==0&&(a=s,t=Hs(e,s))),t===1))throw n=Qn,Et(e,0),nt(e,a),ye(e,J()),n;switch(e.finishedWork=r,e.finishedLanes=a,t){case 0:case 1:throw Error(b(345));case 2:kt(e,ue,Fe);break;case 3:if(nt(e,a),(a&130023424)===a&&(t=Ti+500-J(),10<t)){if(Ka(e,0)!==0)break;if(r=e.suspendedLanes,(r&a)!==a){ce(),e.pingedLanes|=e.suspendedLanes&r;break}e.timeoutHandle=ws(kt.bind(null,e,ue,Fe),t);break}kt(e,ue,Fe);break;case 4:if(nt(e,a),(a&4194240)===a)break;for(t=e.eventTimes,r=-1;0<a;){var i=31-Ie(a);s=1<<i,i=t[i],i>r&&(r=i),a&=~s}if(a=r,a=J()-a,a=(120>a?120:480>a?480:1080>a?1080:1920>a?1920:3e3>a?3e3:4320>a?4320:1960*wp(a/1960))-a,10<a){e.timeoutHandle=ws(kt.bind(null,e,ue,Fe),a);break}kt(e,ue,Fe);break;case 5:kt(e,ue,Fe);break;default:throw Error(b(329))}}}return ye(e,J()),e.callbackNode===n?Xl.bind(null,e):null}function Hs(e,t){var n=jn;return e.current.memoizedState.isDehydrated&&(Et(e,t).flags|=256),e=tr(e,t),e!==2&&(t=ue,ue=n,t!==null&&Bs(t)),e}function Bs(e){ue===null?ue=e:ue.push.apply(ue,e)}function kp(e){for(var t=e;;){if(t.flags&16384){var n=t.updateQueue;if(n!==null&&(n=n.stores,n!==null))for(var a=0;a<n.length;a++){var r=n[a],s=r.getSnapshot;r=r.value;try{if(!Oe(s(),r))return!1}catch{return!1}}}if(n=t.child,t.subtreeFlags&16384&&n!==null)n.return=t,t=n;else{if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return!0;t=t.return}t.sibling.return=t.return,t=t.sibling}}return!0}function nt(e,t){for(t&=~Ri,t&=~mr,e.suspendedLanes|=t,e.pingedLanes&=~t,e=e.expirationTimes;0<t;){var n=31-Ie(t),a=1<<n;e[n]=-1,t&=~a}}function Bo(e){if(O&6)throw Error(b(327));$t();var t=Ka(e,0);if(!(t&1))return ye(e,J()),null;var n=tr(e,t);if(e.tag!==0&&n===2){var a=ps(e);a!==0&&(t=a,n=Hs(e,a))}if(n===1)throw n=Qn,Et(e,0),nt(e,t),ye(e,J()),n;if(n===6)throw Error(b(345));return e.finishedWork=e.current.alternate,e.finishedLanes=t,kt(e,ue,Fe),ye(e,J()),null}function Ni(e,t){var n=O;O|=1;try{return e(t)}finally{O=n,O===0&&(sn=J()+500,lr&&gt())}}function jt(e){rt!==null&&rt.tag===0&&!(O&6)&&$t();var t=O;O|=1;var n=Ee.transition,a=D;try{if(Ee.transition=null,D=1,e)return e()}finally{D=a,Ee.transition=n,O=t,!(O&6)&&gt()}}function Ai(){fe=Gt.current,M(Gt)}function Et(e,t){e.finishedWork=null,e.finishedLanes=0;var n=e.timeoutHandle;if(n!==-1&&(e.timeoutHandle=-1,Yu(n)),V!==null)for(n=V.return;n!==null;){var a=n;switch(li(a),a.tag){case 1:a=a.type.childContextTypes,a!=null&&Fa();break;case 3:an(),M(me),M(ie),bi();break;case 5:gi(a);break;case 4:an();break;case 13:M(H);break;case 19:M(H);break;case 10:mi(a.type._context);break;case 22:case 23:Ai()}n=n.return}if($=e,V=e=pt(e.current,null),ee=fe=t,Q=0,Qn=null,Ri=mr=qt=0,ue=jn=null,Ct!==null){for(t=0;t<Ct.length;t++)if(n=Ct[t],a=n.interleaved,a!==null){n.interleaved=null;var r=a.next,s=n.pending;if(s!==null){var i=s.next;s.next=r,a.next=i}n.pending=a}Ct=null}return e}function Yl(e,t){do{var n=V;try{if(pi(),Ta.current=Ya,Xa){for(var a=B.memoizedState;a!==null;){var r=a.queue;r!==null&&(r.pending=null),a=a.next}Xa=!1}if(At=0,Y=_=B=null,An=!1,Gn=0,Ei.current=null,n===null||n.return===null){Q=1,Qn=t,V=null;break}e:{var s=e,i=n.return,o=n,c=t;if(t=ee,o.flags|=32768,c!==null&&typeof c=="object"&&typeof c.then=="function"){var p=c,f=o,y=f.tag;if(!(f.mode&1)&&(y===0||y===11||y===15)){var h=f.alternate;h?(f.updateQueue=h.updateQueue,f.memoizedState=h.memoizedState,f.lanes=h.lanes):(f.updateQueue=null,f.memoizedState=null)}var v=No(i);if(v!==null){v.flags&=-257,Ao(v,i,o,s,t),v.mode&1&&To(s,p,t),t=v,c=p;var w=t.updateQueue;if(w===null){var k=new Set;k.add(c),t.updateQueue=k}else w.add(c);break e}else{if(!(t&1)){To(s,p,t),qi();break e}c=Error(b(426))}}else if(W&&o.mode&1){var j=No(i);if(j!==null){!(j.flags&65536)&&(j.flags|=256),Ao(j,i,o,s,t),di(rn(c,o));break e}}s=c=rn(c,o),Q!==4&&(Q=2),jn===null?jn=[s]:jn.push(s),s=i;do{switch(s.tag){case 3:s.flags|=65536,t&=-t,s.lanes|=t;var u=Ol(s,c,t);ko(s,u);break e;case 1:o=c;var l=s.type,m=s.stateNode;if(!(s.flags&128)&&(typeof l.getDerivedStateFromError=="function"||m!==null&&typeof m.componentDidCatch=="function"&&(dt===null||!dt.has(m)))){s.flags|=65536,t&=-t,s.lanes|=t;var g=Dl(s,o,t);ko(s,g);break e}}s=s.return}while(s!==null)}ed(n)}catch(C){t=C,V===n&&n!==null&&(V=n=n.return);continue}break}while(!0)}function $l(){var e=$a.current;return $a.current=Ya,e===null?Ya:e}function qi(){(Q===0||Q===3||Q===2)&&(Q=4),$===null||!(qt&268435455)&&!(mr&268435455)||nt($,ee)}function tr(e,t){var n=O;O|=2;var a=$l();($!==e||ee!==t)&&(Fe=null,Et(e,t));do try{Sp();break}catch(r){Yl(e,r)}while(!0);if(pi(),O=n,$a.current=a,V!==null)throw Error(b(261));return $=null,ee=0,Q}function Sp(){for(;V!==null;)Zl(V)}function Cp(){for(;V!==null&&!_d();)Zl(V)}function Zl(e){var t=nd(e.alternate,e,fe);e.memoizedProps=e.pendingProps,t===null?ed(e):V=t,Ei.current=null}function ed(e){var t=e;do{var n=t.alternate;if(e=t.return,t.flags&32768){if(n=fp(n,t),n!==null){n.flags&=32767,V=n;return}if(e!==null)e.flags|=32768,e.subtreeFlags=0,e.deletions=null;else{Q=6,V=null;return}}else if(n=yp(n,t,fe),n!==null){V=n;return}if(t=t.sibling,t!==null){V=t;return}V=t=e}while(t!==null);Q===0&&(Q=5)}function kt(e,t,n){var a=D,r=Ee.transition;try{Ee.transition=null,D=1,xp(e,t,n,a)}finally{Ee.transition=r,D=a}return null}function xp(e,t,n,a){do $t();while(rt!==null);if(O&6)throw Error(b(327));n=e.finishedWork;var r=e.finishedLanes;if(n===null)return null;if(e.finishedWork=null,e.finishedLanes=0,n===e.current)throw Error(b(177));e.callbackNode=null,e.callbackPriority=0;var s=n.lanes|n.childLanes;if(ru(e,s),e===$&&(V=$=null,ee=0),!(n.subtreeFlags&2064)&&!(n.flags&2064)||va||(va=!0,ad(La,function(){return $t(),null})),s=(n.flags&15990)!==0,n.subtreeFlags&15990||s){s=Ee.transition,Ee.transition=null;var i=D;D=1;var o=O;O|=4,Ei.current=null,bp(e,n),_l(n,e),zu(bs),Ma=!!gs,bs=gs=null,e.current=n,vp(n),Qd(),O=o,D=i,Ee.transition=s}else e.current=n;if(va&&(va=!1,rt=e,er=r),s=e.pendingLanes,s===0&&(dt=null),$d(n.stateNode),ye(e,J()),t!==null)for(a=e.onRecoverableError,n=0;n<t.length;n++)r=t[n],a(r.value,{componentStack:r.stack,digest:r.digest});if(Za)throw Za=!1,e=Ms,Ms=null,e;return er&1&&e.tag!==0&&$t(),s=e.pendingLanes,s&1?e===Ws?In++:(In=0,Ws=e):In=0,gt(),null}function $t(){if(rt!==null){var e=Oc(er),t=Ee.transition,n=D;try{if(Ee.transition=null,D=16>e?16:e,rt===null)var a=!1;else{if(e=rt,rt=null,er=0,O&6)throw Error(b(331));var r=O;for(O|=4,x=e.current;x!==null;){var s=x,i=s.child;if(x.flags&16){var o=s.deletions;if(o!==null){for(var c=0;c<o.length;c++){var p=o[c];for(x=p;x!==null;){var f=x;switch(f.tag){case 0:case 11:case 15:qn(8,f,s)}var y=f.child;if(y!==null)y.return=f,x=y;else for(;x!==null;){f=x;var h=f.sibling,v=f.return;if(Jl(f),f===p){x=null;break}if(h!==null){h.return=v,x=h;break}x=v}}}var w=s.alternate;if(w!==null){var k=w.child;if(k!==null){w.child=null;do{var j=k.sibling;k.sibling=null,k=j}while(k!==null)}}x=s}}if(s.subtreeFlags&2064&&i!==null)i.return=s,x=i;else e:for(;x!==null;){if(s=x,s.flags&2048)switch(s.tag){case 0:case 11:case 15:qn(9,s,s.return)}var u=s.sibling;if(u!==null){u.return=s.return,x=u;break e}x=s.return}}var l=e.current;for(x=l;x!==null;){i=x;var m=i.child;if(i.subtreeFlags&2064&&m!==null)m.return=i,x=m;else e:for(i=l;x!==null;){if(o=x,o.flags&2048)try{switch(o.tag){case 0:case 11:case 15:pr(9,o)}}catch(C){U(o,o.return,C)}if(o===i){x=null;break e}var g=o.sibling;if(g!==null){g.return=o.return,x=g;break e}x=o.return}}if(O=r,gt(),We&&typeof We.onPostCommitFiberRoot=="function")try{We.onPostCommitFiberRoot(rr,e)}catch{}a=!0}return a}finally{D=n,Ee.transition=t}}return!1}function Fo(e,t,n){t=rn(n,t),t=Ol(e,t,1),e=lt(e,t,1),t=ce(),e!==null&&(Yn(e,1,t),ye(e,t))}function U(e,t,n){if(e.tag===3)Fo(e,e,n);else for(;t!==null;){if(t.tag===3){Fo(t,e,n);break}else if(t.tag===1){var a=t.stateNode;if(typeof t.type.getDerivedStateFromError=="function"||typeof a.componentDidCatch=="function"&&(dt===null||!dt.has(a))){e=rn(n,e),e=Dl(t,e,1),t=lt(t,e,1),e=ce(),t!==null&&(Yn(t,1,e),ye(t,e));break}}t=t.return}}function Ep(e,t,n){var a=e.pingCache;a!==null&&a.delete(t),t=ce(),e.pingedLanes|=e.suspendedLanes&n,$===e&&(ee&n)===n&&(Q===4||Q===3&&(ee&130023424)===ee&&500>J()-Ti?Et(e,0):Ri|=n),ye(e,t)}function td(e,t){t===0&&(e.mode&1?(t=la,la<<=1,!(la&130023424)&&(la=4194304)):t=1);var n=ce();e=Qe(e,t),e!==null&&(Yn(e,t,n),ye(e,n))}function Rp(e){var t=e.memoizedState,n=0;t!==null&&(n=t.retryLane),td(e,n)}function Tp(e,t){var n=0;switch(e.tag){case 13:var a=e.stateNode,r=e.memoizedState;r!==null&&(n=r.retryLane);break;case 19:a=e.stateNode;break;default:throw Error(b(314))}a!==null&&a.delete(t),td(e,n)}var nd;nd=function(e,t,n){if(e!==null)if(e.memoizedProps!==t.pendingProps||me.current)pe=!0;else{if(!(e.lanes&n)&&!(t.flags&128))return pe=!1,hp(e,t,n);pe=!!(e.flags&131072)}else pe=!1,W&&t.flags&1048576&&il(t,Ja,t.index);switch(t.lanes=0,t.tag){case 2:var a=t.type;Aa(e,t),e=t.pendingProps;var r=en(t,ie.current);Yt(t,n),r=wi(null,t,a,e,r,n);var s=ki();return t.flags|=1,typeof r=="object"&&r!==null&&typeof r.render=="function"&&r.$$typeof===void 0?(t.tag=1,t.memoizedState=null,t.updateQueue=null,he(a)?(s=!0,Ua(t)):s=!1,t.memoizedState=r.state!==null&&r.state!==void 0?r.state:null,yi(t),r.updater=ur,t.stateNode=r,r._reactInternals=t,Ts(t,a,e,n),t=qs(null,t,a,!0,s,n)):(t.tag=0,W&&s&&ci(t),oe(null,t,r,n),t=t.child),t;case 16:a=t.elementType;e:{switch(Aa(e,t),e=t.pendingProps,r=a._init,a=r(a._payload),t.type=a,r=t.tag=Ap(a),e=Ae(a,e),r){case 0:t=As(null,t,a,e,n);break e;case 1:t=Io(null,t,a,e,n);break e;case 11:t=qo(null,t,a,e,n);break e;case 14:t=jo(null,t,a,Ae(a.type,e),n);break e}throw Error(b(306,a,""))}return t;case 0:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Ae(a,r),As(e,t,a,r,n);case 1:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Ae(a,r),Io(e,t,a,r,n);case 3:e:{if(Wl(t),e===null)throw Error(b(387));a=t.pendingProps,s=t.memoizedState,r=s.element,pl(e,t),_a(t,a,null,n);var i=t.memoizedState;if(a=i.element,s.isDehydrated)if(s={element:a,isDehydrated:!1,cache:i.cache,pendingSuspenseBoundaries:i.pendingSuspenseBoundaries,transitions:i.transitions},t.updateQueue.baseState=s,t.memoizedState=s,t.flags&256){r=rn(Error(b(423)),t),t=Po(e,t,a,n,r);break e}else if(a!==r){r=rn(Error(b(424)),t),t=Po(e,t,a,n,r);break e}else for(ge=ct(t.stateNode.containerInfo.firstChild),be=t,W=!0,je=null,n=dl(t,null,a,n),t.child=n;n;)n.flags=n.flags&-3|4096,n=n.sibling;else{if(tn(),a===r){t=Xe(e,t,n);break e}oe(e,t,a,n)}t=t.child}return t;case 5:return ml(t),e===null&&xs(t),a=t.type,r=t.pendingProps,s=e!==null?e.memoizedProps:null,i=r.children,vs(a,r)?i=null:s!==null&&vs(a,s)&&(t.flags|=32),Ml(e,t),oe(e,t,i,n),t.child;case 6:return e===null&&xs(t),null;case 13:return Hl(e,t,n);case 4:return fi(t,t.stateNode.containerInfo),a=t.pendingProps,e===null?t.child=nn(t,null,a,n):oe(e,t,a,n),t.child;case 11:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Ae(a,r),qo(e,t,a,r,n);case 7:return oe(e,t,t.pendingProps,n),t.child;case 8:return oe(e,t,t.pendingProps.children,n),t.child;case 12:return oe(e,t,t.pendingProps.children,n),t.child;case 10:e:{if(a=t.type._context,r=t.pendingProps,s=t.memoizedProps,i=r.value,L(Ga,a._currentValue),a._currentValue=i,s!==null)if(Oe(s.value,i)){if(s.children===r.children&&!me.current){t=Xe(e,t,n);break e}}else for(s=t.child,s!==null&&(s.return=t);s!==null;){var o=s.dependencies;if(o!==null){i=s.child;for(var c=o.firstContext;c!==null;){if(c.context===a){if(s.tag===1){c=Ge(-1,n&-n),c.tag=2;var p=s.updateQueue;if(p!==null){p=p.shared;var f=p.pending;f===null?c.next=c:(c.next=f.next,f.next=c),p.pending=c}}s.lanes|=n,c=s.alternate,c!==null&&(c.lanes|=n),Es(s.return,n,t),o.lanes|=n;break}c=c.next}}else if(s.tag===10)i=s.type===t.type?null:s.child;else if(s.tag===18){if(i=s.return,i===null)throw Error(b(341));i.lanes|=n,o=i.alternate,o!==null&&(o.lanes|=n),Es(i,n,t),i=s.sibling}else i=s.child;if(i!==null)i.return=s;else for(i=s;i!==null;){if(i===t){i=null;break}if(s=i.sibling,s!==null){s.return=i.return,i=s;break}i=i.return}s=i}oe(e,t,r.children,n),t=t.child}return t;case 9:return r=t.type,a=t.pendingProps.children,Yt(t,n),r=Re(r),a=a(r),t.flags|=1,oe(e,t,a,n),t.child;case 14:return a=t.type,r=Ae(a,t.pendingProps),r=Ae(a.type,r),jo(e,t,a,r,n);case 15:return Ll(e,t,t.type,t.pendingProps,n);case 17:return a=t.type,r=t.pendingProps,r=t.elementType===a?r:Ae(a,r),Aa(e,t),t.tag=1,he(a)?(e=!0,Ua(t)):e=!1,Yt(t,n),Pl(t,a,r),Ts(t,a,r,n),qs(null,t,a,!0,e,n);case 19:return Bl(e,t,n);case 22:return Kl(e,t,n)}throw Error(b(156,t.tag))};function ad(e,t){return qc(e,t)}function Np(e,t,n,a){this.tag=e,this.key=n,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.ref=null,this.pendingProps=t,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=a,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function xe(e,t,n,a){return new Np(e,t,n,a)}function ji(e){return e=e.prototype,!(!e||!e.isReactComponent)}function Ap(e){if(typeof e=="function")return ji(e)?1:0;if(e!=null){if(e=e.$$typeof,e===Xs)return 11;if(e===Ys)return 14}return 2}function pt(e,t){var n=e.alternate;return n===null?(n=xe(e.tag,t,e.key,e.mode),n.elementType=e.elementType,n.type=e.type,n.stateNode=e.stateNode,n.alternate=e,e.alternate=n):(n.pendingProps=t,n.type=e.type,n.flags=0,n.subtreeFlags=0,n.deletions=null),n.flags=e.flags&14680064,n.childLanes=e.childLanes,n.lanes=e.lanes,n.child=e.child,n.memoizedProps=e.memoizedProps,n.memoizedState=e.memoizedState,n.updateQueue=e.updateQueue,t=e.dependencies,n.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext},n.sibling=e.sibling,n.index=e.index,n.ref=e.ref,n}function Ia(e,t,n,a,r,s){var i=2;if(a=e,typeof e=="function")ji(e)&&(i=1);else if(typeof e=="string")i=5;else e:switch(e){case Lt:return Rt(n.children,r,s,t);case Qs:i=8,r|=8;break;case Yr:return e=xe(12,n,t,r|2),e.elementType=Yr,e.lanes=s,e;case $r:return e=xe(13,n,t,r),e.elementType=$r,e.lanes=s,e;case Zr:return e=xe(19,n,t,r),e.elementType=Zr,e.lanes=s,e;case mc:return hr(n,r,s,t);default:if(typeof e=="object"&&e!==null)switch(e.$$typeof){case uc:i=10;break e;case pc:i=9;break e;case Xs:i=11;break e;case Ys:i=14;break e;case Ze:i=16,a=null;break e}throw Error(b(130,e==null?e:typeof e,""))}return t=xe(i,n,t,r),t.elementType=e,t.type=a,t.lanes=s,t}function Rt(e,t,n,a){return e=xe(7,e,a,t),e.lanes=n,e}function hr(e,t,n,a){return e=xe(22,e,a,t),e.elementType=mc,e.lanes=n,e.stateNode={isHidden:!1},e}function Vr(e,t,n){return e=xe(6,e,null,t),e.lanes=n,e}function _r(e,t,n){return t=xe(4,e.children!==null?e.children:[],e.key,t),t.lanes=n,t.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},t}function qp(e,t,n,a,r){this.tag=t,this.containerInfo=e,this.finishedWork=this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.pendingContext=this.context=null,this.callbackPriority=0,this.eventTimes=Nr(0),this.expirationTimes=Nr(-1),this.entangledLanes=this.finishedLanes=this.mutableReadLanes=this.expiredLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Nr(0),this.identifierPrefix=a,this.onRecoverableError=r,this.mutableSourceEagerHydrationData=null}function Ii(e,t,n,a,r,s,i,o,c){return e=new qp(e,t,n,o,c),t===1?(t=1,s===!0&&(t|=8)):t=0,s=xe(3,null,null,t),e.current=s,s.stateNode=e,s.memoizedState={element:a,isDehydrated:n,cache:null,transitions:null,pendingSuspenseBoundaries:null},yi(s),e}function jp(e,t,n){var a=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:Dt,key:a==null?null:""+a,children:e,containerInfo:t,implementation:n}}function rd(e){if(!e)return ht;e=e._reactInternals;e:{if(Pt(e)!==e||e.tag!==1)throw Error(b(170));var t=e;do{switch(t.tag){case 3:t=t.stateNode.context;break e;case 1:if(he(t.type)){t=t.stateNode.__reactInternalMemoizedMergedChildContext;break e}}t=t.return}while(t!==null);throw Error(b(171))}if(e.tag===1){var n=e.type;if(he(n))return rl(e,n,t)}return t}function sd(e,t,n,a,r,s,i,o,c){return e=Ii(n,a,!0,e,r,s,i,o,c),e.context=rd(null),n=e.current,a=ce(),r=ut(n),s=Ge(a,r),s.callback=t??null,lt(n,s,r),e.current.lanes=r,Yn(e,r,a),ye(e,a),e}function yr(e,t,n,a){var r=t.current,s=ce(),i=ut(r);return n=rd(n),t.context===null?t.context=n:t.pendingContext=n,t=Ge(s,i),t.payload={element:e},a=a===void 0?null:a,a!==null&&(t.callback=a),e=lt(r,t,i),e!==null&&(Pe(e,r,i,s),Ra(e,r,i)),i}function nr(e){if(e=e.current,!e.child)return null;switch(e.child.tag){case 5:return e.child.stateNode;default:return e.child.stateNode}}function Uo(e,t){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var n=e.retryLane;e.retryLane=n!==0&&n<t?n:t}}function Pi(e,t){Uo(e,t),(e=e.alternate)&&Uo(e,t)}function Ip(){return null}var id=typeof reportError=="function"?reportError:function(e){console.error(e)};function Oi(e){this._internalRoot=e}fr.prototype.render=Oi.prototype.render=function(e){var t=this._internalRoot;if(t===null)throw Error(b(409));yr(e,t,null,null)};fr.prototype.unmount=Oi.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var t=e.containerInfo;jt(function(){yr(null,e,null,null)}),t[_e]=null}};function fr(e){this._internalRoot=e}fr.prototype.unstable_scheduleHydration=function(e){if(e){var t=Kc();e={blockedOn:null,target:e,priority:t};for(var n=0;n<tt.length&&t!==0&&t<tt[n].priority;n++);tt.splice(n,0,e),n===0&&Wc(e)}};function Di(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function gr(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11&&(e.nodeType!==8||e.nodeValue!==" react-mount-point-unstable "))}function zo(){}function Pp(e,t,n,a,r){if(r){if(typeof a=="function"){var s=a;a=function(){var p=nr(i);s.call(p)}}var i=sd(t,a,e,0,null,!1,!1,"",zo);return e._reactRootContainer=i,e[_e]=i.current,Bn(e.nodeType===8?e.parentNode:e),jt(),i}for(;r=e.lastChild;)e.removeChild(r);if(typeof a=="function"){var o=a;a=function(){var p=nr(c);o.call(p)}}var c=Ii(e,0,!1,null,null,!1,!1,"",zo);return e._reactRootContainer=c,e[_e]=c.current,Bn(e.nodeType===8?e.parentNode:e),jt(function(){yr(t,c,n,a)}),c}function br(e,t,n,a,r){var s=n._reactRootContainer;if(s){var i=s;if(typeof r=="function"){var o=r;r=function(){var c=nr(i);o.call(c)}}yr(t,i,e,r)}else i=Pp(n,t,e,r,a);return nr(i)}Dc=function(e){switch(e.tag){case 3:var t=e.stateNode;if(t.current.memoizedState.isDehydrated){var n=Sn(t.pendingLanes);n!==0&&(ei(t,n|1),ye(t,J()),!(O&6)&&(sn=J()+500,gt()))}break;case 13:jt(function(){var a=Qe(e,1);if(a!==null){var r=ce();Pe(a,e,1,r)}}),Pi(e,1)}};ti=function(e){if(e.tag===13){var t=Qe(e,134217728);if(t!==null){var n=ce();Pe(t,e,134217728,n)}Pi(e,134217728)}};Lc=function(e){if(e.tag===13){var t=ut(e),n=Qe(e,t);if(n!==null){var a=ce();Pe(n,e,t,a)}Pi(e,t)}};Kc=function(){return D};Mc=function(e,t){var n=D;try{return D=e,t()}finally{D=n}};ls=function(e,t,n){switch(t){case"input":if(ns(e,n),t=n.name,n.type==="radio"&&t!=null){for(n=e;n.parentNode;)n=n.parentNode;for(n=n.querySelectorAll("input[name="+JSON.stringify(""+t)+'][type="radio"]'),t=0;t<n.length;t++){var a=n[t];if(a!==e&&a.form===e.form){var r=cr(a);if(!r)throw Error(b(90));yc(a),ns(a,r)}}}break;case"textarea":gc(e,n);break;case"select":t=n.value,t!=null&&Vt(e,!!n.multiple,t,!1)}};xc=Ni;Ec=jt;var Op={usingClientEntryPoint:!1,Events:[Zn,Ht,cr,Sc,Cc,Ni]},gn={findFiberByHostInstance:St,bundleType:0,version:"18.3.1",rendererPackageName:"react-dom"},Dp={bundleType:gn.bundleType,version:gn.version,rendererPackageName:gn.rendererPackageName,rendererConfig:gn.rendererConfig,overrideHookState:null,overrideHookStateDeletePath:null,overrideHookStateRenamePath:null,overrideProps:null,overridePropsDeletePath:null,overridePropsRenamePath:null,setErrorHandler:null,setSuspenseHandler:null,scheduleUpdate:null,currentDispatcherRef:Ye.ReactCurrentDispatcher,findHostInstanceByFiber:function(e){return e=Nc(e),e===null?null:e.stateNode},findFiberByHostInstance:gn.findFiberByHostInstance||Ip,findHostInstancesForRefresh:null,scheduleRefresh:null,scheduleRoot:null,setRefreshHandler:null,getCurrentFiber:null,reconcilerVersion:"18.3.1-next-f1338f8080-20240426"};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<"u"){var wa=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!wa.isDisabled&&wa.supportsFiber)try{rr=wa.inject(Dp),We=wa}catch{}}we.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED=Op;we.createPortal=function(e,t){var n=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!Di(t))throw Error(b(200));return jp(e,t,null,n)};we.createRoot=function(e,t){if(!Di(e))throw Error(b(299));var n=!1,a="",r=id;return t!=null&&(t.unstable_strictMode===!0&&(n=!0),t.identifierPrefix!==void 0&&(a=t.identifierPrefix),t.onRecoverableError!==void 0&&(r=t.onRecoverableError)),t=Ii(e,1,!1,null,null,n,!1,a,r),e[_e]=t.current,Bn(e.nodeType===8?e.parentNode:e),new Oi(t)};we.findDOMNode=function(e){if(e==null)return null;if(e.nodeType===1)return e;var t=e._reactInternals;if(t===void 0)throw typeof e.render=="function"?Error(b(188)):(e=Object.keys(e).join(","),Error(b(268,e)));return e=Nc(t),e=e===null?null:e.stateNode,e};we.flushSync=function(e){return jt(e)};we.hydrate=function(e,t,n){if(!gr(t))throw Error(b(200));return br(null,e,t,!0,n)};we.hydrateRoot=function(e,t,n){if(!Di(e))throw Error(b(405));var a=n!=null&&n.hydratedSources||null,r=!1,s="",i=id;if(n!=null&&(n.unstable_strictMode===!0&&(r=!0),n.identifierPrefix!==void 0&&(s=n.identifierPrefix),n.onRecoverableError!==void 0&&(i=n.onRecoverableError)),t=sd(t,null,e,1,n??null,r,!1,s,i),e[_e]=t.current,Bn(e),a)for(e=0;e<a.length;e++)n=a[e],r=n._getVersion,r=r(n._source),t.mutableSourceEagerHydrationData==null?t.mutableSourceEagerHydrationData=[n,r]:t.mutableSourceEagerHydrationData.push(n,r);return new fr(t)};we.render=function(e,t,n){if(!gr(t))throw Error(b(200));return br(null,e,t,!1,n)};we.unmountComponentAtNode=function(e){if(!gr(e))throw Error(b(40));return e._reactRootContainer?(jt(function(){br(null,null,e,!1,function(){e._reactRootContainer=null,e[_e]=null})}),!0):!1};we.unstable_batchedUpdates=Ni;we.unstable_renderSubtreeIntoContainer=function(e,t,n,a){if(!gr(n))throw Error(b(200));if(e==null||e._reactInternals===void 0)throw Error(b(38));return br(e,t,n,!1,a)};we.version="18.3.1-next-f1338f8080-20240426";function od(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>"u"||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!="function"))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(od)}catch(e){console.error(e)}}od(),oc.exports=we;var Lp=oc.exports,Jo=Lp;Qr.createRoot=Jo.createRoot,Qr.hydrateRoot=Jo.hydrateRoot;const bn=[{id:"hld-1",number:1,title:"What is High-Level Design (HLD)?",question:"What is High-Level Design (HLD)?",difficulty:"Basic",category:"HLD Fundamentals",answer:`HLD is the architecture-level view of a system: it defines the major components (services, databases, caches, queues, load balancers), how they interact, the data flow, and the technology choices — without diving into class/method details. It answers "what are the building blocks and how do they fit together to meet scale, availability, and latency goals?" HLD is driven by non-functional requirements (throughput, availability, consistency) and is what you draw on the whiteboard in a system-design interview.

\`\`\`text
HLD deliverables:
  - component diagram (services, DB, cache, queue, LB, CDN)
  - data flow + API contracts between components
  - data model & storage choices (SQL/NoSQL/blob)
  - scaling, availability, consistency strategy
Focus: WHAT the blocks are and HOW they interact (not code internals).
\`\`\``,tech:"hld",displayNumber:1,sortKey:1},{id:"hld-2",number:2,title:"What is the difference between HLD and LLD?",question:"What is the difference between HLD and LLD?",difficulty:"Basic",category:"HLD Fundamentals",answer:`- **HLD (High-Level Design)**: system architecture — services, databases, caches, queues, communication, scaling, and trade-offs. Audience: architects/teams. Altitude: the whole system.
- **LLD (Low-Level Design)**: the internals of a component — classes, interfaces, methods, data structures, design patterns, and schemas. Audience: developers. Altitude: within a module.

HLD decides *what* components exist; LLD decides *how* each is built.

\`\`\`text
HLD: services, DB, cache, queue, LB, scaling, consistency  (system altitude)
LLD: classes, interfaces, methods, patterns, schemas       (module altitude)
Interview: HLD = boxes & arrows + trade-offs; LLD = class diagram + API design.
\`\`\``,tech:"hld",displayNumber:2,sortKey:2},{id:"hld-3",number:3,title:"What is scalability?",question:"What is scalability?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Scalability is a system's ability to handle increased load (users, requests, data) by adding resources, ideally with near-linear cost. It has two forms: **vertical** (bigger machines) and **horizontal** (more machines). A scalable design avoids single bottlenecks (shared DB, single node), uses stateless services behind load balancers, partitions data, and caches hot reads. Good scalability means you can grow capacity without redesigning the system.

\`\`\`text
Scalable design levers:
  - stateless services + load balancer (scale out easily)
  - partition/shard data (no single DB bottleneck)
  - cache hot reads, async heavy work via queues
  - avoid single points of failure
Goal: add resources -> handle more load, near-linear cost.
\`\`\``,tech:"hld",displayNumber:3,sortKey:3},{id:"hld-4",number:4,title:"What is the difference between vertical and horizontal scaling?",question:"What is the difference between vertical and horizontal scaling?",difficulty:"Basic",category:"HLD Fundamentals",answer:`- **Vertical (scale up)**: add more power (CPU/RAM/disk) to one machine. Simple, no app changes, but has a hard ceiling, is costly at the high end, and the machine is a single point of failure.
- **Horizontal (scale out)**: add more machines and distribute load. Effectively unlimited, fault-tolerant, but needs stateless services, load balancing, and data partitioning — more architectural complexity.

Most large systems scale horizontally; verticals help for stateful stores up to a point.

\`\`\`text
Vertical:   1 big box (CPU/RAM++)  -> simple, ceiling, SPOF, costly
Horizontal: many boxes + LB        -> near-limitless, fault-tolerant, complex
Prefer horizontal for stateless tiers; vertical + replication for stateful DBs.
\`\`\``,tech:"hld",displayNumber:4,sortKey:4},{id:"hld-5",number:5,title:"What is availability?",question:"What is availability?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Availability is the percentage of time a system is operational and serving requests, usually expressed in "nines" (99.9% = ~8.7h downtime/year; 99.99% = ~52min; 99.999% = ~5min). It's achieved through redundancy (no single point of failure), replication, failover, health checks, and graceful degradation. Availability often trades against consistency (CAP) — highly available systems may serve slightly stale data during partitions.

\`\`\`text
Availability "nines":
  99%     -> ~3.65 days/year downtime
  99.9%   -> ~8.76 hours/year
  99.99%  -> ~52.6 min/year
  99.999% -> ~5.26 min/year
Achieve via: redundancy, replication, failover, health checks, no SPOF.
\`\`\``,tech:"hld",displayNumber:5,sortKey:5},{id:"hld-6",number:6,title:"What is reliability?",question:"What is reliability?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Reliability is the probability that a system performs correctly (produces correct results without failing or losing data) over a period of time. It's broader than availability: a system can be available (responding) but unreliable (returning wrong data or losing writes). Reliability comes from durable storage, replication, idempotency, data validation, thorough testing, and handling partial failures gracefully. Metrics: MTBF (mean time between failures), error rate.

\`\`\`text
Availability = "is it up?"   Reliability = "does it work correctly, without loss?"
Reliability via: durable/replicated storage, idempotency, validation,
                 graceful partial-failure handling, testing.
Metrics: MTBF, error rate, data-loss incidents.
\`\`\``,tech:"hld",displayNumber:6,sortKey:6},{id:"hld-7",number:7,title:"What is fault tolerance?",question:"What is fault tolerance?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Fault tolerance is a system's ability to keep operating (possibly degraded) when components fail — the defining trait of distributed systems, where partial failure is normal. Techniques: redundancy/replication (survive node loss), failover (promote a standby), retries with backoff, circuit breakers and bulkheads (contain failures), and graceful degradation (drop non-critical features). The goal is that any single failure has a contained, recoverable impact — no total outage.

\`\`\`text
Fault tolerance techniques:
  redundancy/replication -> survive node loss
  failover -> promote standby
  retry+backoff, circuit breaker, bulkhead -> contain failures
  graceful degradation -> drop non-critical features
Assume any component can fail; keep blast radius small.
\`\`\``,tech:"hld",displayNumber:7,sortKey:7},{id:"hld-8",number:8,title:"What is elasticity?",question:"What is elasticity?",difficulty:"Basic",category:"HLD Fundamentals",answer:"Elasticity is the ability to **automatically** add or remove resources in response to real-time demand — scaling out during spikes and back in when idle to save cost. It builds on horizontal scalability plus autoscaling (metrics-driven: CPU, queue depth, RPS) and stateless services. Elasticity differs from scalability: scalability is *can* it grow; elasticity is *automatically* matching capacity to load, both up and down.\n\n```text\nScalability = can grow.   Elasticity = auto-matches capacity to demand (up AND down).\nRequires: stateless services + autoscaler (CPU/RPS/queue-depth triggers).\nBenefit: handle spikes without over-provisioning; cut cost when idle.\n```",tech:"hld",displayNumber:8,sortKey:8},{id:"hld-9",number:9,title:"What is a stateless service?",question:"What is a stateless service?",difficulty:"Basic",category:"HLD Fundamentals",answer:"A stateless service keeps no client/session state between requests — each request carries everything needed (or fetches it from a shared store like Redis/DB). Any instance can handle any request, so you can freely add/remove instances behind a load balancer, restart them without data loss, and scale horizontally. Session data is externalized. Statelessness is the foundation of horizontal scalability and elasticity.\n\n```text\nStateless: request has all it needs (or reads shared store) -> any instance serves it\n  -> free horizontal scaling, easy restarts, simple load balancing.\nExternalize state: session/cache in Redis, data in DB (not in-process memory).\n```",tech:"hld",displayNumber:9,sortKey:9},{id:"hld-10",number:10,title:"What is a stateful service?",question:"What is a stateful service?",difficulty:"Basic",category:"HLD Fundamentals",answer:`A stateful service maintains state across requests locally (in-memory sessions, connections, or local data) — e.g. databases, caches, WebSocket servers, or Kafka brokers. It's harder to scale: requests may need to reach the specific instance holding their state (sticky sessions), scaling/restarting risks state loss (needs replication), and rebalancing is complex. Use stateful services where state locality is essential (datastores, real-time connections), and manage them with replication and careful partitioning.

\`\`\`text
Stateful: instance holds state locally (sessions, connections, data).
Challenges: sticky routing, replication for durability, complex scaling/rebalancing.
Examples: databases, caches, WebSocket servers, Kafka brokers.
Keep the stateless tier large; contain state in well-managed stateful stores.
\`\`\``,tech:"hld",displayNumber:10,sortKey:10},{id:"hld-11",number:11,title:"What is a load balancer?",question:"What is a load balancer?",difficulty:"Basic",category:"HLD Fundamentals",answer:"A load balancer distributes incoming traffic across multiple backend instances to improve throughput, availability, and to avoid overloading any one node. It does health checks (routing only to healthy instances), supports strategies (round robin, least connections, weighted, hash), and can terminate TLS. It's essential to horizontal scaling and enables zero-downtime deploys (drain/rotate instances). Can operate at L4 (TCP) or L7 (HTTP-aware).\n\n```text\nClients -> LB -> [instance A, B, C]  (health-checked, load-distributed)\nStrategies: round robin, least connections, weighted, IP/consistent hash.\nEnables: horizontal scale, HA, zero-downtime deploys, TLS termination.\n```",tech:"hld",displayNumber:11,sortKey:11},{id:"hld-12",number:12,title:"What is the difference between Layer 4 and Layer 7 load balancing?",question:"What is the difference between Layer 4 and Layer 7 load balancing?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **Layer 4 (transport)**: routes by IP/port/TCP without inspecting content. Very fast, protocol-agnostic, but can't make content-based decisions.
- **Layer 7 (application)**: understands HTTP — can route by URL path, headers, cookies, method; do TLS termination, caching, compression, and rewrites. More features and smarter routing, at slightly higher cost.

Use L4 for raw throughput/non-HTTP; L7 for HTTP-aware routing (path-based microservice routing, sticky sessions, A/B).

\`\`\`text
L4: routes by IP:port/TCP (fast, content-blind)      -> raw throughput, any protocol
L7: routes by HTTP path/header/cookie (smart)         -> path routing, TLS, caching, A/B
Example L7: /api/orders -> order-svc ; /api/users -> user-svc
\`\`\``,tech:"hld",displayNumber:12,sortKey:12},{id:"hld-13",number:13,title:"What is DNS?",question:"What is DNS?",difficulty:"Basic",category:"HLD Fundamentals",answer:"DNS (Domain Name System) is the internet's distributed directory that translates human-readable domain names (`example.com`) into IP addresses. It's hierarchical (root → TLD → authoritative servers) and heavily cached (TTLs). In system design it also enables load distribution (round-robin DNS, GeoDNS routing users to the nearest region) and is the first hop in service discovery for many architectures.\n\n```text\nexample.com -> DNS -> 93.184.216.34\nHierarchy: root -> TLD (.com) -> authoritative NS -> record (A/AAAA/CNAME)\nUses in HLD: GeoDNS (nearest region), round-robin DNS (crude LB), TTL caching.\n```",tech:"hld",displayNumber:13,sortKey:13},{id:"hld-14",number:14,title:"How does DNS resolution work?",question:"How does DNS resolution work?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:'When you request `example.com`: the resolver checks caches (browser → OS → recursive resolver). On a miss, the recursive resolver queries the **root** server (returns the `.com` TLD server), then the **TLD** server (returns the domain\'s authoritative nameserver), then the **authoritative** server (returns the A/AAAA record). The IP is cached per TTL at each level, so subsequent lookups are fast. The client then connects to the returned IP.\n\n```text\nbrowser cache -> OS cache -> recursive resolver\n  miss -> ROOT server -> ".com" TLD server -> AUTHORITATIVE NS -> A record (IP)\nResult cached at each hop per TTL. Then client connects to the IP.\n```',tech:"hld",displayNumber:14,sortKey:14},{id:"hld-15",number:15,title:"What is a CDN?",question:"What is a CDN?",difficulty:"Basic",category:"HLD Fundamentals",answer:`A CDN (Content Delivery Network) is a globally distributed network of edge servers that cache content close to users, reducing latency and offloading origin servers. It's used for static assets (images, JS/CSS, video) and increasingly dynamic content via edge compute. Users are routed (GeoDNS/anycast) to the nearest edge; on a cache miss the edge fetches from origin and caches it per TTL. Benefits: lower latency, less origin load, DDoS absorption, and bandwidth savings.

\`\`\`text
User -> nearest CDN edge (cached asset) -> served fast
  cache miss -> edge fetches from ORIGIN -> caches per TTL -> serves
Benefits: low latency (geo-close), origin offload, DDoS absorption, cheaper bandwidth.
Best for: static assets, video; edge compute for some dynamic content.
\`\`\``,tech:"hld",displayNumber:15,sortKey:15},{id:"hld-16",number:16,title:"What is a reverse proxy?",question:"What is a reverse proxy?",difficulty:"Basic",category:"HLD Fundamentals",answer:"A reverse proxy sits in front of backend servers and handles client requests on their behalf — doing load balancing, TLS termination, caching, compression, routing, and security filtering. Clients think they talk to one server; the proxy hides the backend topology. Contrast with a forward proxy (fronts clients for outbound traffic). Examples: Nginx, Envoy, HAProxy, API gateways. It's a common single entry point that improves security, performance, and flexibility.\n\n```text\nClients -> Reverse Proxy (Nginx/Envoy) -> [backend servers]\nDoes: load balance, TLS terminate, cache, compress, route, filter, hide topology.\nReverse proxy = protects/serves the SERVERS (vs forward proxy = fronts CLIENTS).\n```",tech:"hld",displayNumber:16,sortKey:16},{id:"hld-17",number:17,title:"What is an API Gateway?",question:"What is an API Gateway?",difficulty:"Basic",category:"HLD Fundamentals",answer:`An API Gateway is the single entry point for clients into a microservices backend. It handles cross-cutting concerns so services don't each reimplement them: routing/composition, authentication/authorization, rate limiting, TLS, request/response transformation, caching, and observability. It decouples clients from internal topology. Risk: it can become a bottleneck or "god" component — keep business logic out of it. Examples: Kong, AWS API Gateway, Spring Cloud Gateway.

\`\`\`text
Clients -> API Gateway -> [order-svc, user-svc, payment-svc]
Handles: routing, authN/authZ, rate limiting, TLS, transformation, caching, metrics.
Keep it thin (no business logic). Beware it becoming a bottleneck/SPOF (scale it).
\`\`\``,tech:"hld",displayNumber:17,sortKey:17},{id:"hld-18",number:18,title:"What is service discovery?",question:"What is service discovery?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Service discovery lets services find each other by logical name instead of hardcoded IPs, essential in dynamic environments where instances come and go with ephemeral addresses. A **registry** (Eureka, Consul, etcd, or Kubernetes DNS) tracks healthy instances via registration + heartbeats; clients or a router resolve the name to a current healthy instance. It underpins load balancing and elasticity by keeping routing up to date as instances scale.

\`\`\`text
Instance startup -> REGISTER {name, host:port, health} + heartbeat
Caller -> "give me healthy 'orders' instances" -> [ip1, ip2] -> load balance
Registry evicts dead instances. Tools: Eureka, Consul, etcd, k8s DNS.
\`\`\``,tech:"hld",displayNumber:18,sortKey:18},{id:"hld-19",number:19,title:"What is the difference between client-side and server-side service discovery?",question:"What is the difference between client-side and server-side service discovery?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **Client-side**: the client queries the registry for healthy instances and picks one itself (client-side load balancing). Fewer hops, but every client needs discovery logic (e.g. Netflix Eureka + Ribbon).
- **Server-side**: the client calls a stable endpoint (load balancer / k8s Service / DNS) that queries the registry and routes for it. Clients stay simple; the router is a managed component (e.g. Kubernetes Services, AWS ELB).

\`\`\`text
Client-side:  client -> registry -> pick instance -> call it   (client does LB)
Server-side:  client -> stable LB/DNS -> registry -> route     (LB does it)
Client-side = fewer hops, smart clients; Server-side = simple clients, managed router.
\`\`\``,tech:"hld",displayNumber:19,sortKey:19},{id:"hld-20",number:20,title:"What is a microservice?",question:"What is a microservice?",difficulty:"Basic",category:"HLD Fundamentals",answer:`A microservice is a small, independently deployable service that owns one business capability and its data, communicating with others over the network (REST/gRPC/events). Benefits: independent deployment/scaling, team autonomy, technology freedom, and fault isolation. Costs: distributed-system complexity (network calls, eventual consistency, observability, ops overhead). Services are organized around bounded contexts (DDD), and each owns its database (no shared schema).

\`\`\`text
Microservice = one business capability + its own data + independent deploy.
Pros: independent scale/deploy, team autonomy, tech freedom, fault isolation.
Cons: network complexity, eventual consistency, observability & ops overhead.
Organize by bounded context; database-per-service.
\`\`\``,tech:"hld",displayNumber:20,sortKey:20},{id:"hld-21",number:21,title:"What is the difference between a monolith and microservices?",question:"What is the difference between a monolith and microservices?",difficulty:"Basic",category:"HLD Fundamentals",answer:`- **Monolith**: one deployable unit; all modules share a codebase, process, and often one database. Simple to build/deploy/test early, low latency (in-process calls), but scaling is all-or-nothing, deploys are risky, and it can become tangled at scale.
- **Microservices**: many independently deployable services. Independent scaling/deploys and team autonomy, at the cost of network complexity and eventual consistency.

Start monolith-first; extract services when scaling/team-friction pain is real.

\`\`\`text
Monolith:      1 deploy, 1 DB, in-process calls  -> simple early, scales as a whole
Microservices: N deploys, DB-per-service, network -> independent scale, more complexity
Rule: modular monolith first; split along proven seams when pain justifies it.
\`\`\``,tech:"hld",displayNumber:21,sortKey:21},{id:"hld-22",number:22,title:"What is horizontal partitioning?",question:"What is horizontal partitioning?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:"Horizontal partitioning splits a table's **rows** across multiple partitions/databases, each holding a subset (e.g. users A–M in one, N–Z in another), based on a partition key. It spreads data and load so no single store holds everything — enabling scale beyond one machine. This is the basis of **sharding**. Contrast with vertical partitioning (splitting **columns**/features into separate tables/services).\n\n```text\nHorizontal (rows): users 1..1M -> shard0; 1M..2M -> shard1  (same schema, split rows)\nVertical (columns): profile table vs activity table (split by column/feature)\nHorizontal partitioning = foundation of sharding -> scale writes & storage.\n```",tech:"hld",displayNumber:22,sortKey:22},{id:"hld-23",number:23,title:"What is database sharding?",question:"What is database sharding?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Sharding is horizontal partitioning of data across multiple independent databases (shards), each owning a subset determined by a **shard key**. It scales writes and storage beyond a single machine. Strategies: **range** (by value ranges), **hash** (even distribution), **directory** (lookup table), or **consistent hashing** (minimal reshuffling on change). Costs: cross-shard queries/joins/transactions are hard, rebalancing is complex, and a poor shard key causes **hot shards**. Pick a high-cardinality, evenly-accessed key.

\`\`\`text
shard = hash(shard_key) % N  ->  data spread across N databases
Strategies: range | hash | directory | consistent hashing
Costs: no cross-shard JOIN/txn, complex rebalance, hot shards on bad key.
Pick high-cardinality, evenly-accessed shard key.
\`\`\``,tech:"hld",displayNumber:23,sortKey:23},{id:"hld-24",number:24,title:"What is replication?",question:"What is replication?",difficulty:"Basic",category:"HLD Fundamentals",answer:"Replication keeps copies of data on multiple nodes for availability, durability, and read scaling. If one node fails, another serves the data (failover). Models: **synchronous** (write waits for replicas — strong consistency, higher latency) vs **asynchronous** (write returns immediately, replicas catch up — lower latency, possible staleness/loss). Topologies: primary-replica (single writer) and multi-primary (multiple writers, needs conflict resolution). Replication complements sharding (each shard is also replicated).\n\n```text\nPrimary --replicate--> Replica1, Replica2   (copies for HA + read scaling)\nSync:  wait for replicas (strong, slower).  Async: return now (fast, may lag/lose).\nFailover: promote a replica if primary dies. Combine with sharding for scale + HA.\n```",tech:"hld",displayNumber:24,sortKey:24},{id:"hld-25",number:25,title:"What is primary-replica (master-slave) database architecture?",question:"What is primary-replica (master-slave) database architecture?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`One **primary** node handles all writes; one or more **replicas** copy the primary's data and serve reads. Benefits: read scaling (fan reads across replicas), high availability (promote a replica on primary failure), and offloading backups/analytics to replicas. Trade-offs: **replication lag** means replica reads can be slightly stale (eventual consistency for reads), and writes remain single-primary (a write bottleneck). Route writes to primary, reads to replicas, and handle read-your-writes carefully.

\`\`\`text
Writes -> PRIMARY -> replicate -> REPLICAS -> serve reads
Pros: read scaling, HA (promote replica), offload backups/analytics.
Cons: replication lag (stale reads), single-primary write bottleneck.
Read-your-writes: pin the user to primary briefly after a write.
\`\`\``,tech:"hld",displayNumber:25,sortKey:25},{id:"hld-26",number:26,title:"What is database indexing?",question:"What is database indexing?",difficulty:"Basic",category:"HLD Fundamentals",answer:`An index is an auxiliary data structure (usually a B-tree, or hash/inverted index) that lets the database find rows by a column's value without scanning the whole table — turning O(n) lookups into O(log n). It dramatically speeds reads/filters/sorts on indexed columns. Cost: extra storage and slower writes (indexes must be updated on insert/update/delete). Index the columns you filter/join/sort on; avoid over-indexing.

\`\`\`text
No index:  SELECT ... WHERE email=? -> full table scan (O(n))
With index on email: B-tree lookup (O(log n))
Trade-off: faster reads, slower writes + extra storage.
Index columns used in WHERE/JOIN/ORDER BY; don't over-index.
\`\`\``,tech:"hld",displayNumber:26,sortKey:26},{id:"hld-27",number:27,title:"What is the difference between SQL and NoSQL?",question:"What is the difference between SQL and NoSQL?",difficulty:"Basic",category:"HLD Fundamentals",answer:`- **SQL (relational)**: structured schema, ACID transactions, strong consistency, powerful joins/queries. Scales mainly vertically + read replicas; sharding is harder. Best for structured, relational, transactional data (payments, orders).
- **NoSQL**: flexible schema, horizontal scaling, high throughput, often eventual consistency. Types: document (MongoDB), key-value (Redis/DynamoDB), wide-column (Cassandra), graph (Neo4j). Best for large scale, flexible/semi-structured data, high write volume.

Choose by data model, consistency needs, and scale.

\`\`\`text
SQL:   fixed schema, ACID, joins, strong consistency   -> transactional/relational data
NoSQL: flexible schema, horizontal scale, high throughput, often eventual consistency
       (document | key-value | wide-column | graph)
Pick by: data model, consistency requirement, scale/write volume.
\`\`\``,tech:"hld",displayNumber:27,sortKey:27},{id:"hld-28",number:28,title:"When would you choose MongoDB?",question:"When would you choose MongoDB?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Choose MongoDB (document store) when your data is semi-structured/hierarchical and the schema evolves (product catalogs, user profiles, content/CMS), you want to store an aggregate as one document (fewer joins), and you need horizontal scaling via sharding with flexible querying. It offers rich queries, secondary indexes, and tunable consistency. Avoid it when you need complex multi-entity ACID transactions across many relations — a relational DB fits better.

\`\`\`text
Choose MongoDB when:
  - semi-structured / evolving schema (catalogs, profiles, CMS)
  - aggregate stored as one document (fewer joins)
  - horizontal scale via sharding + flexible queries + secondary indexes
Avoid when: heavy multi-entity ACID transactions across relations (use SQL).
\`\`\``,tech:"hld",displayNumber:28,sortKey:28},{id:"hld-29",number:29,title:"When would you choose PostgreSQL?",question:"When would you choose PostgreSQL?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Choose PostgreSQL when you need strong **ACID transactions**, relational integrity (foreign keys, constraints), complex queries/joins, and correctness — e.g. financial/orders/inventory systems. It's also very versatile: JSONB for semi-structured data, full-text search, geospatial (PostGIS), and extensions. It scales via read replicas and (with more effort) partitioning/sharding. Prefer it as a safe default for transactional workloads; reach for NoSQL when scale/flexibility outweighs relational needs.

\`\`\`text
Choose PostgreSQL when:
  - ACID transactions + relational integrity (FKs, constraints)
  - complex joins/queries, correctness-critical (payments, orders, inventory)
  - versatile: JSONB, full-text, PostGIS, extensions
Scale: read replicas, partitioning. Great transactional default.
\`\`\``,tech:"hld",displayNumber:29,sortKey:29},{id:"hld-30",number:30,title:"What is Redis?",question:"What is Redis?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Redis is an in-memory data store (key-value + rich data structures: strings, hashes, lists, sets, sorted sets, streams) used primarily as a **cache**, but also for rate limiting, sessions, leaderboards, pub/sub, distributed locks, and queues. It's extremely fast (sub-millisecond, RAM-based), supports TTLs/eviction, optional persistence (RDB/AOF), and clustering/replication for scale and HA. Its versatility makes it a Swiss-army knife in system design.

\`\`\`text
Redis = in-memory store (sub-ms). Uses:
  cache, session store, rate limiter, leaderboard (sorted sets),
  pub/sub, distributed lock, queue/stream.
TTL + eviction; persistence (RDB/AOF); replication + cluster for scale/HA.
\`\`\``,tech:"hld",displayNumber:30,sortKey:30},{id:"hld-31",number:31,title:"When should Redis be used?",question:"When should Redis be used?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Use Redis when you need very low-latency access to hot, ephemeral, or computed data: caching frequent DB reads (cache-aside), sessions/tokens, rate-limiting counters, leaderboards/rankings (sorted sets), real-time presence, distributed locks, or lightweight pub/sub. It's not your durable source of truth (data lives in RAM; persistence is best-effort). Use it to offload the database and speed reads — with a TTL/invalidation strategy to manage staleness.

\`\`\`text
Use Redis for: cache-aside, sessions, rate-limit counters, leaderboards,
               presence, distributed locks, pub/sub.
Not for: system of record (RAM-based; persistence best-effort).
Always pair with TTL/invalidation to control staleness.
\`\`\``,tech:"hld",displayNumber:31,sortKey:31},{id:"hld-32",number:32,title:"What is the cache-aside pattern?",question:"What is the cache-aside pattern?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`In cache-aside (lazy loading) the application manages the cache: on read, check the cache; on a **miss**, load from the DB, populate the cache (with TTL), and return. On write, update the DB and **invalidate** the cache entry. Only requested data is cached (efficient), and a cache outage just means more DB hits. Concerns: stale data (use TTLs) and thundering-herd on popular misses (use locks/singleflight).

\`\`\`text
READ:  cache hit? return; else DB -> populate cache (TTL) -> return
WRITE: update DB -> invalidate cache key
Pros: cache only what's used; cache outage = more DB load, not failure.
Watch: staleness (TTL), thundering herd on hot miss (lock/singleflight).
\`\`\``,tech:"hld",displayNumber:32,sortKey:32},{id:"hld-33",number:33,title:"What is a write-through cache?",question:"What is a write-through cache?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`In write-through, every write goes to the cache **and** the database **synchronously** (as one operation), so the cache is always consistent with the DB and reads are fast/fresh. Trade-off: writes are slower (two writes) and you may cache data that's never read (wasted memory). Good when reads dominate and you want cache freshness without invalidation logic. Often paired with cache-aside reads.

\`\`\`text
WRITE: app -> write cache AND DB synchronously -> both consistent
Pros: cache always fresh, fast fresh reads, no separate invalidation.
Cons: slower writes (double write); may cache never-read data.
Best when reads >> writes and freshness matters.
\`\`\``,tech:"hld",displayNumber:33,sortKey:33},{id:"hld-34",number:34,title:"What is a write-back (write-behind) cache?",question:"What is a write-back (write-behind) cache?",difficulty:"Hard",category:"HLD Fundamentals",answer:`In write-back, writes go to the cache and are acknowledged immediately; the cache **asynchronously** flushes to the database later (batched). This gives very fast writes and absorbs bursts, but risks **data loss** if the cache fails before flushing, and adds complexity (ordering, consistency). Use it for high-write, loss-tolerant workloads (metrics, counters, view counts) where throughput matters more than durability of each write.

\`\`\`text
WRITE: app -> cache (ack immediately) -> async batch flush -> DB (later)
Pros: very fast writes, absorbs bursts, fewer DB writes (batched).
Cons: DATA LOSS risk if cache dies pre-flush; more complexity.
Use for: high-write, loss-tolerant (counters, metrics, view counts).
\`\`\``,tech:"hld",displayNumber:34,sortKey:34},{id:"hld-35",number:35,title:"What is cache invalidation?",question:"What is cache invalidation?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Cache invalidation is removing or updating stale cache entries when the underlying data changes — famously "one of the two hard things in CS." Strategies: **TTL** (expire after N seconds — simple, bounded staleness), **explicit invalidation** (delete/update the key on write), and **event-based** (invalidate via a change stream/Kafka). Trade-offs: too aggressive → low hit rate; too lax → stale data. Choose per data's tolerance for staleness.

\`\`\`text
Strategies:
  TTL         -> expire after N sec (simple, bounded staleness)
  explicit    -> delete/update key on write (fresh, needs write hooks)
  event-based -> invalidate on CDC/Kafka change event (scales, decoupled)
Balance: aggressive = low hit rate; lax = stale data. Match data's staleness tolerance.
\`\`\``,tech:"hld",displayNumber:35,sortKey:35},{id:"hld-36",number:36,title:"What is a message queue?",question:"What is a message queue?",difficulty:"Basic",category:"HLD Fundamentals",answer:"A message queue is a broker that buffers messages between producers and consumers, decoupling them in time. Producers enqueue and move on; consumers process at their own pace. It enables async processing, load leveling (absorb spikes), reliable delivery (acks, retries, DLQ), and horizontal scaling via competing consumers. Classic queues (RabbitMQ, SQS) deliver each message to one consumer and delete on ack. Improves resilience and responsiveness.\n\n```text\nProducer -> [ Queue ] -> Consumer(s)   (decouple in time)\nBenefits: async processing, load leveling, retries/DLQ, competing-consumer scaling.\nClassic queue: one consumer per message, delete on ack (RabbitMQ, SQS).\n```",tech:"hld",displayNumber:36,sortKey:36},{id:"hld-37",number:37,title:"What is the difference between a queue and a topic?",question:"What is the difference between a queue and a topic?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **Queue (point-to-point)**: each message is delivered to exactly **one** consumer among competing consumers — used for distributing tasks (do this once).
- **Topic (publish-subscribe)**: each message is delivered to **all** subscribers — used for broadcasting events to many independent consumers.

Kafka blends these: a topic with one consumer group behaves queue-like (work split across the group), while multiple groups get pub/sub fan-out.

\`\`\`text
Queue (P2P):    msg -> ONE consumer (competing consumers) -> task distribution
Topic (Pub/Sub): msg -> ALL subscribers -> event broadcast/fan-out
Kafka: one consumer group = queue-like; multiple groups = pub/sub.
\`\`\``,tech:"hld",displayNumber:37,sortKey:37},{id:"hld-38",number:38,title:"What is the difference between Kafka and RabbitMQ?",question:"What is the difference between Kafka and RabbitMQ?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **Kafka**: a distributed, partitioned **log** — high throughput, retained/replayable, per-partition ordering, many consumer groups. Best for event streaming, pipelines, and multiple consumers of the same data.
- **RabbitMQ**: a **message broker** with rich routing (exchanges) — pushes to consumers, deletes on ack, flexible routing, lower-latency task delivery. Best for complex routing, RPC, and traditional task queues.

Kafka = durable replayable streams; RabbitMQ = flexible routing/task queue.

\`\`\`text
Kafka:    log, pull, retained+replayable, per-partition order, high throughput
          -> streams, pipelines, many consumers, event sourcing
RabbitMQ: broker, push, delete-on-ack, rich exchange routing, low-latency tasks
          -> complex routing, RPC, task queues
\`\`\``,tech:"hld",displayNumber:38,sortKey:38},{id:"hld-39",number:39,title:"What is asynchronous processing?",question:"What is asynchronous processing?",difficulty:"Basic",category:"HLD Fundamentals",answer:`Async processing hands off work to be done later (via a queue/log/background worker) instead of doing it inline within the request. The caller returns immediately (e.g. "order accepted, processing"), and the heavy/slow work (emails, image processing, payments settlement) happens in the background. Benefits: fast responses, load leveling, resilience (retries), and decoupling. Cost: eventual consistency and more moving parts (queues, workers, status tracking).

\`\`\`text
Sync inline: request -> do everything -> respond (slow, coupled)
Async: request -> enqueue job -> respond NOW; worker processes later
Benefits: fast responses, load leveling, retries, decoupling.
Cost: eventual consistency, extra infra (queue, workers, status).
\`\`\``,tech:"hld",displayNumber:39,sortKey:39},{id:"hld-40",number:40,title:"What is the difference between synchronous and asynchronous communication?",question:"What is the difference between synchronous and asynchronous communication?",difficulty:"Basic",category:"HLD Fundamentals",answer:`- **Synchronous**: caller waits for a response (REST/gRPC request-response). Simple, immediate, but couples caller availability/latency to the callee; chains multiply latency and cascading-failure risk.
- **Asynchronous**: caller emits a message/event and doesn't wait (queue/Kafka). Decoupled, resilient, absorbs spikes, but eventual consistency and harder debugging.

Use sync for queries needing an immediate answer; async for commands/events processable in the background.

\`\`\`text
Sync:  request -> wait -> response (coupled latency/availability) -> queries
Async: emit event -> continue; consumer processes later -> commands/events
Choose sync when you need the answer now; async to decouple & absorb load.
\`\`\``,tech:"hld",displayNumber:40,sortKey:40},{id:"hld-41",number:41,title:"What is the difference between REST and gRPC?",question:"What is the difference between REST and gRPC?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **REST**: HTTP/JSON, ubiquitous, human-readable, cacheable, language-agnostic, browser-friendly. Verbose, slower serialization, no built-in contract (needs OpenAPI).
- **gRPC**: HTTP/2 + Protobuf (binary), strongly-typed contract via \`.proto\`, fast, compact, supports streaming (client/server/bidi). Best for internal, high-throughput, low-latency service-to-service calls; limited browser support without gRPC-Web.

Use REST at the public edge; gRPC for internal microservice communication.

\`\`\`text
REST:  HTTP/JSON, human-readable, cacheable, browser-friendly, no strict contract
gRPC:  HTTP/2 + Protobuf, typed contract, compact/fast, streaming
Use: REST at public edge; gRPC for internal high-throughput service-to-service.
\`\`\``,tech:"hld",displayNumber:41,sortKey:41},{id:"hld-42",number:42,title:"What is a WebSocket?",question:"What is a WebSocket?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:"WebSocket is a protocol providing a **full-duplex, persistent** TCP connection between client and server. After an HTTP `Upgrade` handshake, either side can push messages any time with low overhead (small frames). It's ideal for real-time bidirectional apps: chat, multiplayer games, collaborative editing, live trading, notifications. Costs: stateful connections complicate load balancing/scaling (need sticky sessions + a pub/sub backplane) and require heartbeats + reconnection logic.\n\n```text\nHTTP Upgrade -> persistent full-duplex TCP -> both sides push anytime (low overhead)\nUse: chat, games, collab editing, live feeds, notifications.\nScale: sticky sessions + pub/sub backplane (Redis/Kafka); heartbeats + reconnect.\n```",tech:"hld",displayNumber:42,sortKey:42},{id:"hld-43",number:43,title:"What is the difference between HTTP polling and long polling?",question:"What is the difference between HTTP polling and long polling?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`- **Short polling**: client requests on a fixed interval (every N sec); the server responds immediately (often empty). Simple, but wasteful and laggy (latency up to the interval).
- **Long polling**: client sends a request and the server **holds it open** until data is available (or timeout), then responds; the client immediately re-requests. Near-real-time over plain HTTP, fewer empty responses — but holds connections and has reconnection overhead.

For true push, prefer SSE/WebSocket.

\`\`\`text
Short polling: request every N sec -> often empty -> wasteful, lag up to N
Long polling:  request held open until data/timeout -> near real-time, connection churn
Better for push: SSE (server->client) or WebSocket (bidirectional).
\`\`\``,tech:"hld",displayNumber:43,sortKey:43},{id:"hld-44",number:44,title:"What is idempotency?",question:"What is idempotency?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`An idempotent operation produces the same result whether applied once or many times — critical when clients/networks retry (at-least-once delivery, timeouts). GET/PUT/DELETE are naturally idempotent; POST usually isn't. Make operations idempotent with an **idempotency key** (unique request id) + a dedup store/unique constraint, so a retried "charge $10" doesn't double-charge. It's foundational for safe retries in distributed and payment systems.

\`\`\`text
Idempotent: apply once or N times -> same result (safe to retry).
Implement: client sends Idempotency-Key -> server stores it (unique constraint)
           -> duplicate key -> return stored result, don't re-execute.
Essential for retries, at-least-once delivery, payments.
\`\`\``,tech:"hld",displayNumber:44,sortKey:44},{id:"hld-45",number:45,title:"What is rate limiting?",question:"What is rate limiting?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:"Rate limiting caps how many requests a client can make in a time window to protect services from overload, abuse, and to enforce fair use/quotas. Algorithms: **token bucket** (refill at a rate, allow bursts), **leaky bucket** (smooth constant rate), **fixed/sliding window** counters. In distributed systems, back the counter with Redis so limits hold across instances. Exceeding returns HTTP 429 with `Retry-After`.\n\n```text\nAlgorithms: token bucket (bursts), leaky bucket (smooth), fixed/sliding window\nDistributed: Redis-backed counter keyed by client (API key/user/IP)\nExceed -> HTTP 429 + Retry-After. Protects from overload/abuse, enforces quotas.\n```",tech:"hld",displayNumber:45,sortKey:45},{id:"hld-46",number:46,title:"What is a circuit breaker?",question:"What is a circuit breaker?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:"A circuit breaker stops calling a failing dependency to let it recover and to fail fast (avoiding wasted work and cascading failures). States: **Closed** (calls flow, failures counted), **Open** (once a threshold is crossed, calls short-circuit to a fallback for a cooldown), **Half-Open** (a few trial calls test recovery; success closes, failure re-opens). It converts a slow/failing dependency into a fast, contained failure.\n\n```text\nClosed (calls flow, count failures) --threshold--> Open (fail fast to fallback)\nOpen --cooldown--> Half-Open (trial calls) --success--> Closed / --fail--> Open\nPrevents cascading failures; gives the downstream time to recover.\n```",tech:"hld",displayNumber:46,sortKey:46},{id:"hld-47",number:47,title:"What is retry with exponential backoff?",question:"What is retry with exponential backoff?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:"Retrying re-attempts a transient failure; **exponential backoff** increases the delay between attempts (1s, 2s, 4s…) so you don't hammer a struggling service, and **jitter** randomizes delays so many clients don't retry in lockstep (thundering herd). Only retry **idempotent** operations, cap total attempts/time, and pair with a circuit breaker so retries don't prolong an outage.\n\n```text\nattempt delays: 1s, 2s, 4s, 8s ... (exponential) + random jitter (avoid herd)\nRules: only idempotent ops, bound attempts/time, combine with circuit breaker.\nPrevents overwhelming a recovering service and synchronized retry storms.\n```",tech:"hld",displayNumber:47,sortKey:47},{id:"hld-48",number:48,title:"What is a timeout?",question:"What is a timeout?",difficulty:"Basic",category:"HLD Fundamentals",answer:`A timeout caps how long you wait for an operation (connect, request, query) before giving up, so a slow dependency fails fast instead of pinning threads/connections indefinitely — the #1 defense against cascading failures. Set connect and request timeouts on every network call, tune them to realistic latencies (not too tight → false failures, not too loose → hung threads), and combine with retries and circuit breakers.

\`\`\`text
No timeout: slow dependency hangs your threads forever -> cascading failure.
Set connect + request timeouts on EVERY network call.
Tune: too tight = false failures; too loose = hung threads.
Combine with retry + circuit breaker.
\`\`\``,tech:"hld",displayNumber:48,sortKey:48},{id:"hld-49",number:49,title:"What is a distributed system?",question:"What is a distributed system?",difficulty:"Basic",category:"HLD Fundamentals",answer:`A distributed system is a collection of independent computers (nodes) that appear to users as one coherent system, coordinating over a network to share work and data. It provides scalability, availability, and fault tolerance beyond a single machine — but introduces hard problems: partial failure, network unreliability/latency, no global clock, consistency vs availability (CAP), and coordination/consensus. Almost all large-scale systems are distributed.

\`\`\`text
Many nodes over a network -> appear as one system (scale, HA, fault tolerance).
Hard problems: partial failure, unreliable network, no global clock,
               CAP (consistency vs availability), consensus/coordination.
Design assuming any node/link can fail at any time.
\`\`\``,tech:"hld",displayNumber:49,sortKey:49},{id:"hld-50",number:50,title:"What are the major components you consider when designing a scalable system?",question:"What are the major components you consider when designing a scalable system?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`A standard toolkit: **DNS/CDN** (route + cache at edge), **load balancers** (distribute traffic), **stateless app services** (scale out), **caching** (Redis for hot reads), **databases** (SQL/NoSQL, replicated + sharded), **message queues/streams** (async, decoupling — Kafka/RabbitMQ), **object/blob storage** (files/media), **search** (Elasticsearch), plus cross-cutting: **API gateway**, **service discovery**, **observability** (logs/metrics/traces), **rate limiting/circuit breakers**, and **multi-region/DR**. Drive choices from requirements (RPS, data size, latency, consistency, availability).

\`\`\`text
Client -> DNS/CDN -> LB -> API Gateway -> stateless services
   services -> Cache (Redis) -> DB (replicated + sharded)
   services -> Queue/Kafka (async) -> workers
   services -> Object storage (files), Search (Elasticsearch)
Cross-cutting: discovery, observability, rate limit, circuit breaker, multi-region/DR.
Drive every choice from requirements (RPS, data, latency, consistency, availability).
\`\`\``,tech:"hld",displayNumber:50,sortKey:50},{id:"hld-51",number:51,title:"How do you do back-of-envelope estimation (capacity planning) in a system design interview?",question:"How do you do back-of-envelope estimation (capacity planning) in a system design interview?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Back-of-envelope estimation is a quick quantitative sanity check to size components **before** you draw boxes. The goal is to convert product requirements into concrete numbers (RPS, storage, bandwidth, memory) so you can pick the right tech and spot bottlenecks early.

**Step-by-step flow:**

1. **Clarify scope & scale** — ask: DAU, peak/avg RPS, read:write ratio, data size per object, retention, geo distribution.
2. **Estimate traffic** — compute peak QPS = DAU × actions/user/day ÷ 86,400 × peak factor (2–10×).
3. **Estimate storage** — raw data = objects × size × replicas; add index overhead (20–50%).
4. **Estimate bandwidth** — in/out = QPS × request/response size.
5. **Estimate memory (cache)** — working set = active users × session size, or hot keys × value size.
6. **Size components** — map numbers to: DB shards, cache nodes, queue partitions, LB capacity, network.
7. **Round & sanity-check** — compare to known limits (e.g., single MySQL ~10k QPS, Redis ~100k ops/s, Kafka partition ~10 MB/s).

**Cheat-sheet constants:**
- 1M DAU, 10 actions/day → ~115 avg QPS; peak ~1k QPS (10×)
- 1 KB/request → 1 MB/s per 1k QPS
- 1 TB SSD ≈ $100; 1 GB RAM ≈ $5–10 (cloud)
- 1 Gbps NIC ≈ 100 MB/s practical

**Example: URL Shortener (100M DAU, 10 shortens/day, 100 redirects/day, 10 yr retention)**
\`\`\`
Writes: 100M × 10 / 86400 ≈ 11.5k QPS (peak ~115k)
Reads:  100M × 100 / 86400 ≈ 115k QPS (peak ~1.1M)
Storage: 100M × 10 × 365 × 10yr × 200B ≈ 73 TB raw × 3 replicas ≈ 220 TB
Cache:  top 1% hot URLs (1M) × 500B ≈ 500 MB → single Redis node
DB:    115k write QPS → need ~12 shards (10k QPS each)
       1.1M read QPS → read replicas + cache absorbs most
\`\`\`

#### Key Takeaways
\`\`\`text
Estimation checklist:
  ☐ DAU & peak factor     ☐ Read:write ratio
  ☐ Object size           ☐ Retention
  ☐ QPS (avg + peak)      ☐ Storage (raw + index + replicas)
  ☐ Bandwidth (in/out)    ☐ Cache working set
  ☐ Shard/partition count ☐ Network / instance limits
Always state assumptions. Round to 1 sig-fig. Show the math.
\`\`\``,tech:"hld",displayNumber:51,sortKey:51},{id:"hld-52",number:52,title:"What is the structured HLD design process (flowchart) for a system design interview?",question:"What is the structured HLD design process (flowchart) for a system design interview?",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`A repeatable, step-by-step flowchart avoids haphazard design. Follow this sequence — each step feeds the next. Time-box each phase in a 45–60 min interview.

\`\`\`mermaid
flowchart TD
    A[1. Requirements Clarification] --> B[2. Back-of-Envelope Estimation]
    B --> C[3. API Contract / Data Model]
    C --> D[4. High-Level Architecture]
    D --> E[5. Component Deep-Dives]
    E --> F[6. Cross-Cutting Concerns]
    F --> G[7. Failure Modes & Trade-offs]
    G --> H[8. Wrap-up & Scaling Discussion]
    
    style A fill:#e3f2fd,stroke:#1976d2
    style B fill:#fff3e0,stroke:#f57c00
    style C fill:#e8f5e9,stroke:#388e3c
    style D fill:#fce4ec,stroke:#c2185b
    style E fill:#f3e5f5,stroke:#7b1fa2
    style F fill:#e0f2f1,stroke:#00796b
    style G fill:#fafafa,stroke:#616161
    style H fill:#e8eaf6,stroke:#3f51b5
\`\`\`

**Phase breakdown:**

| Phase | Time | Key Output |
|-------|------|------------|
| **1. Requirements** | 5–8 min | Functional (use cases), Non-functional (RPS, latency P99, availability, consistency, durability, geo, retention, compliance). **Explicitly list what's OUT of scope.** |
| **2. Estimation** | 5 min | QPS (avg/peak), storage (TB), bandwidth (Gbps), cache working set, shard/partition counts. State assumptions, show math. |
| **3. API & Data Model** | 5–8 min | REST/gRPC endpoints (request/response JSON), core entities & relationships, access patterns. Pick SQL vs NoSQL per entity. |
| **4. High-Level Architecture** | 8–12 min | **Component diagram**: client → DNS/CDN → LB → API GW → services → (cache, DB, queue, blob, search). Label sync vs async paths. |
| **5. Deep-Dives** | 10–15 min | Pick 2–3 critical flows: write path, read path, async pipeline, sharding scheme, cache invalidation, consistency model. Draw sequence diagrams. |
| **6. Cross-Cutting** | 5 min | Observability (logs/metrics/traces), rate limiting, circuit breakers, authZ/authN, deployments (CI/CD), multi-region/DR, cost. |
| **7. Failure Modes** | 5 min | For each component: what fails? Detection? Failover? Data loss? Degraded mode? CAP trade-off explicit. |
| **8. Wrap-up** | 2–3 min | How it scales 10×, 100×. Cost optimizations. Open questions. |

**Golden rules:**
- **Talk before you draw** — clarify, estimate, then diagram.
- **Data drives architecture** — access patterns → storage choice → component topology.
- **Explicit trade-offs** — every arrow has latency, consistency, availability cost.
- **No magic boxes** — if you say "cache", say invalidation strategy; "queue", say ordering/ack/DLQ; "DB", say sharding key.

#### Key Takeaways
\`\`\`text
Interview flowchart (memorize this sequence):
  1️⃣ Requirements → 2️⃣ Estimation → 3️⃣ API/Data → 4️⃣ Architecture
  5️⃣ Deep-Dives → 6️⃣ Cross-Cutting → 7️⃣ Failures → 8️⃣ Scale discussion

Per-phase deliverable:
  1: Scope list (in/out)          2: Numbers with units
  3: Endpoints + ER diagram       4: Box-and-arrow diagram
  5: Sequence diagrams            6: Checklist
  7: Failure table                8: 10x/100x scaling story
\`\`\``,tech:"hld",displayNumber:52,sortKey:52},{id:"hld-53",number:53,title:"Back-of-envelope estimation for common system design problems",question:"Back-of-envelope estimation for common system design problems",difficulty:"Intermediate",category:"HLD Fundamentals",answer:`Below are worked estimations for five classic system-design problems. Each follows the same template: **assumptions → traffic → storage → bandwidth → cache → component sizing**. Numbers are rounded to 1 significant figure.

#### 1. Parking Lot System (e.g., airport/mall garage)

**Assumptions:**
- 500 spots, 20 entry/exit gates
- 10k vehicles/day peak (holiday), 2k avg
- Each ticket/entry event ~200 B; 10-yr retention for analytics
- Read:write ≈ 10:1 (status checks, payments, analytics dashboards)

| Metric | Calculation | Result |
|--------|-------------|--------|
| **Avg write QPS** | 2k vehicles × 2 events (entry+exit) / 86,400 | **~0.05 QPS** |
| **Peak write QPS** | 10k × 2 / 86,400 × 3 (peak factor) | **~0.7 QPS** |
| **Avg read QPS** | 10× writes | **~0.5 QPS** |
| **Peak read QPS** | 10× peak writes | **~7 QPS** |
| **Storage (10 yr)** | 10k/day × 365 × 10 × 200 B × 3 replicas | **~22 GB** |
| **Cache** | Active tickets (500) × 500 B | **~250 KB** (tiny, in-memory) |
| **Bandwidth** | (7 QPS × 1 KB) in + out | **~14 KB/s** |

**Component sizing:**
- **DB**: Single Postgres instance handles this easily (10k QPS capacity). No sharding needed.
- **Cache**: Redis optional; can serve real-time availability from in-memory set in app.
- **Gates**: 20 edge devices → MQTT/HTTP to API GW. Stateless services behind LB.
- **Cost**: ~$50/mo (db.t3.medium + cache.t3.micro).

#### 2. Bitly / URL Shortener (recap + comparison)

**Assumptions:**
- 100M DAU, 10 shortens/user/day, 100 redirects/user/day
- 10-yr retention, URL record ~200 B (short + long + metadata)
- Read:write = 10:1

| Metric | Avg | Peak (10×) |
|--------|-----|------------|
| **Write QPS** | 11.5k | 115k |
| **Read QPS** | 115k | 1.1M |
| **Storage (10 yr)** | 73 TB raw × 3 = **220 TB** |
| **Cache (1% hot)** | 1M × 500 B = **500 MB** |
| **Bandwidth** | 115k × 0.5 KB ≈ **60 MB/s** in, **600 MB/s** out |

**Component sizing:**
- **DB**: ~12 write shards (10k QPS each) + read replicas
- **Cache**: Single Redis node (or small cluster for HA)
- **CDN**: Critical for redirect latency — edge caches absorb 99% of reads
- **Cost**: ~$15k–25k/mo (sharded DB + CDN + cache)

#### 3. Meeting Room Booking System (enterprise, 10k employees)

**Assumptions:**
- 10k employees, 500 rooms, 200 bookings/employee/month
- Peak: 9–11 AM, 2–4 PM → 5× avg
- Booking record ~300 B (room, user, start, end, recurrence ref)
- Read:write = 20:1 (calendar views, availability checks, search)
- 5-yr retention for audit

| Metric | Calculation | Result |
|--------|-------------|--------|
| **Avg write QPS** | 10k × 200 / (30×86,400) | **~0.08 QPS** |
| **Peak write QPS** | × 5 peak factor | **~0.4 QPS** |
| **Avg read QPS** | 20× writes | **~1.6 QPS** |
| **Peak read QPS** | 20× peak writes | **~8 QPS** |
| **Storage (5 yr)** | 10k × 200 × 12 × 5 × 300 B × 3 | **~1.1 GB** |
| **Cache** | Next 7 days bookings (10k × 20 × 300 B) | **~60 MB** |
| **Bandwidth** | 8 QPS × 2 KB | **~16 KB/s** |

**Component sizing:**
- **DB**: Single Postgres (10k QPS headroom). Recurring bookings need careful interval indexing.
- **Cache**: Redis for 7-day rolling window availability (key = room:date → sorted set of intervals)
- **Search**: Elasticsearch for "find room with projector, capacity 10, 2–3 PM"
- **Cost**: ~$200/mo (db.r6g.large + cache.t3.small + ES.t3.small)

#### 4. Calendar Application (Google Calendar style, 1B users)

**Assumptions:**
- 1B registered, 100M DAU
- 5 events/user/day avg (create/update/delete/view)
- Event record ~500 B (title, time, guests, recurrence, notifications)
- Read:write = 50:1 (calendar grid renders, notifications, search)
- Infinite retention (user data forever)

| Metric | Avg | Peak (5×) |
|--------|-----|-----------|
| **Write QPS** | 100M × 5 / 86,400 ≈ **5.8k** | **~29k** |
| **Read QPS** | 50× writes ≈ **290k** | **~1.5M** |
| **Storage (1 yr)** | 100M × 5 × 365 × 500 B × 3 | **~275 TB** |
| **Cache** | Next 30 days active users (30M) × 20 events × 500 B | **~300 GB** |
| **Bandwidth** | 1.5M QPS × 1 KB | **1.5 GB/s** out |

**Component sizing:**
- **DB**: Sharded by \`user_id\` → ~30 shards (1k write QPS each). Recurring events expanded on write or virtualized.
- **Cache**: Redis Cluster (6+ nodes) for 30-day hot window + notification queue
- **Search**: Elasticsearch for cross-user search (shared calendars, meeting insights)
- **Notifications**: Separate pipeline (Kafka → push workers) — ~5M notifications/min peak
- **Cost**: ~$200k–500k/mo (sharded DB fleet + Redis cluster + ES + Kafka + push infra)

#### 5. Feedback / Rating System (e.g., Uber driver ratings, product reviews)

**Assumptions:**
- 50M DAU, 2 ratings/user/day (give + receive)
- Rating record ~300 B (score, text, tags, driver/rider/product ref)
- Read:write = 100:1 (profiles, leaderboards, search, analytics)
- 3-yr retention for ratings, 30-day for raw text (GDPR)

| Metric | Avg | Peak (3×) |
|--------|-----|-----------|
| **Write QPS** | 50M × 2 / 86,400 ≈ **1.2k** | **~3.5k** |
| **Read QPS** | 100× writes ≈ **120k** | **~350k** |
| **Storage (3 yr)** | 50M × 2 × 365 × 3 × 300 B × 3 | **~98 TB** |
| **Cache** | Top 1M entities (drivers/products) × 1 KB | **~1 GB** |
| **Bandwidth** | 350k QPS × 0.5 KB | **~175 MB/s** out |

**Component sizing:**
- **DB**: Sharded by \`entity_id\` (driver/product) → ~4 shards. Time-series compaction for analytics.
- **Cache**: Redis for aggregate scores (avg, count, percentiles) + recent reviews
- **Search/Analytics**: ClickHouse / Druid for aggregations (percentiles, trends, anomaly detection)
- **Moderation queue**: Kafka → human/AI moderation workers
- **Cost**: ~$15k–30k/mo (sharded DB + Redis + OLAP + moderation)

#### Summary Comparison Table

| System | DAU | Peak Write QPS | Peak Read QPS | Storage (yrs) | Cache | DB Strategy |
|--------|-----|----------------|---------------|---------------|-------|-------------|
| **Parking Lot** | 10k | 0.7 | 7 | 22 GB (10) | 250 KB | Single node |
| **Bitly** | 100M | 115k | 1.1M | 220 TB (10) | 500 MB | 12 shards + CDN |
| **Meeting Room** | 10k (emp) | 0.4 | 8 | 1.1 GB (5) | 60 MB | Single + ES |
| **Calendar** | 100M | 29k | 1.5M | 275 TB (1/yr) | 300 GB | 30 shards by user |
| **Feedback** | 50M | 3.5k | 350k | 98 TB (3) | 1 GB | 4 shards by entity + OLAP |

#### Key Patterns to Recognize
\`\`\`text
Low scale (parking, meeting room):
  → Single DB, optional cache, focus on correctness (transactions, overlaps)
  
High write + massive read (Bitly, Calendar):
  → Shard by write key (URL, user_id), CDN for reads, cache hot window
  
High read:write + analytics (Feedback):
  → Shard by entity, Redis for aggregates, OLAP column store for dashboards
  
Recurring/time-range data (Calendar, Meeting Room):
  → Interval trees / B-tree on (start, end), materialize or virtualize recurrences
\`\`\`

#### Estimation Checklist (Reusable)
\`\`\`text
For ANY system:
  ☐ DAU / MAU              ☐ Actions per user per day
  ☐ Read:write ratio       ☐ Payload size (req + resp)
  ☐ Retention (years)      ☐ Peak factor (2–10×)
  ☐ Hot data % for cache   ☐ Consistency needs (strong/eventual)
  ☐ Geo distribution       ☐ Compliance (GDPR, PCI, etc.)
  ☐ 10× / 100× scaling story
\`\`\``,tech:"hld",displayNumber:53,sortKey:53},{id:"hld-54",number:51,title:"Design a URL shortener (like bit.ly).",question:"Design a URL shortener (like bit.ly).",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Functional**: shorten long URL → short code; redirect short → long; optional custom alias, expiry, analytics. **Non-functional**: read-heavy (~100:1), very low redirect latency, high availability. **Core**: generate a unique short code (base62 of an auto-increment id, or hash + collision check), store \`code → longURL\`, and 301/302 redirect on GET. Cache hot codes in Redis, shard the store by code, front with a CDN. **Challenges**: unique id at scale, hot codes, custom aliases, analytics (async).

\`\`\`text
── BACK-OF-ENVELOPE ──
Writes:   100M new URLs/day  = ~1,160 writes/sec
Reads:    100:1 ratio        = ~116,000 redirects/sec (peak ~350k)
Storage:  100M/day × 500 B   = ~50 GB/day → ~90 TB in 5 yrs (×3 replication)
Keys:     base62, 7 chars    = 62^7 ≈ 3.5 trillion codes (plenty)
Cache:    hot 20% of reads → Redis, ~10 GB working set

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               │ GET /abc123
               ▼
        ┌──────────────┐
        │  CDN / Edge  │  (cache popular redirects)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Load Balancer│
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ URL Service  │  (stateless, autoscaled)
        └──────┬───────┘
        ┌──────┴───────┐
        ▼              ▼
 ┌────────────┐  ┌──────────────┐
 │ Redis Cache│  │  KV Store    │  (sharded by code)
 │ code→url   │  │  code→url    │
 └────────────┘  └──────────────┘
               │ (async)
               ▼
        ┌──────────────┐
        │ Kafka → Analytics│ (click events)
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:51,sortKey:54},{id:"hld-55",number:52,title:"Design a file upload service.",question:"Design a file upload service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Functional**: upload files, store durably, download later, large files. **Core**: client requests a **pre-signed URL** and uploads **directly to object storage** (S3), bypassing app servers; metadata (id, name, size, owner, url) in a DB. Download via signed URL/CDN. Multipart for large files. **Challenges**: virus scanning (async), access control, dedup (hash), resumable uploads.

\`\`\`text
── BACK-OF-ENVELOPE ──
Uploads:  10M files/day       = ~116 uploads/sec (peak ~350)
Avg size: 5 MB               → 50 TB/day ingested
Storage:  50 TB/day × 365     = ~18 PB/yr → object storage + tiering
Metadata: 10M/day × 1 KB      = ~10 GB/day (small, in DB)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       (1) ask for pre-signed URL
               ▼
        ┌──────────────┐
        │ Upload API   │──► writes metadata ──►┌──────────┐
        └──────┬───────┘                       │ Metadata │
       (2) pre-signed URL                       │   DB     │
               ▼                                └──────────┘
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       (3) PUT file bytes DIRECTLY
               ▼
        ┌──────────────┐
        │Object Storage│  (S3, multipart)
        └──────┬───────┘
       (4) event on upload
               ▼
        ┌──────────────┐
        │ Async Worker │  (virus scan, dedup)
        └──────────────┘
Download: Client ◄── signed URL / CDN ◄── Object Storage
\`\`\``,tech:"hld",displayNumber:52,sortKey:55},{id:"hld-56",number:53,title:"Design an image upload service.",question:"Design an image upload service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`Like file upload plus **image processing**: on upload (direct-to-S3), emit an event → worker generates **thumbnails/multiple resolutions** + optimizes (WebP), stores variants in S3, served via CDN per device. Metadata in DB. **Challenges**: async processing pipeline, CDN caching/invalidation, EXIF stripping, moderation.

\`\`\`text
── BACK-OF-ENVELOPE ──
Uploads:  20M images/day  = ~230/sec (peak ~700)
Avg size: 2 MB original → 4 variants (thumb/sm/md/lg) ≈ +1.5 MB
Storage:  20M × 3.5 MB/day = 70 TB/day → object storage + CDN
Reads:    50:1 (views)   = ~11,500 image views/sec → CDN offloads ~95%

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       upload (pre-signed URL, direct-to-S3)
               ▼
        ┌──────────────┐
        │Object Storage│──► event ──►┌──────────────┐
        │  (original)  │             │ Image Worker │
        └──────────────┘             │ resize+WebP  │
               ▲                     │ strip EXIF   │
               │  store variants ◄───┴──────┬───────┘
               ▼                            ▼
        ┌──────────────┐             ┌──────────────┐
        │Object Storage│             │ Metadata DB  │
        │  (variants)  │             │ + moderation │
        └──────┬───────┘             └──────────────┘
               ▼
        ┌──────────────┐
        │     CDN      │──► device-appropriate size
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:53,sortKey:56},{id:"hld-57",number:54,title:"Design a basic authentication service.",question:"Design a basic authentication service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: register (store email + **bcrypt/argon2** hashed password + salt), login (verify → issue **JWT** access + refresh token), validate tokens per request (stateless signature, or session store). Refresh tokens in Redis for revocation. **Challenges**: never store plaintext, secure token storage (httpOnly cookies), rate-limit login (brute force), MFA, lockout.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    100M registered, 10M DAU
Logins:   10M/day        = ~116/sec (peak ~350)
Validations: every API call ≈ 50k/sec → JWT stateless (no DB hit)
Storage:  100M users × 1 KB = ~100 GB (small)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Rate Limiter │  (block brute force)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Auth Service │
        └──────┬───────┘
        ┌──────┴──────────────┐
        ▼                     ▼
 ┌────────────┐        ┌────────────┐
 │  User DB   │        │Redis        │
 │ hashed pwd │        │refresh token│ (revocable)
 └────────────┘        └────────────┘
Login OK → issue JWT access (15m) + refresh token
Per request → verify JWT signature (stateless, no DB) → allow/deny
\`\`\``,tech:"hld",displayNumber:54,sortKey:57},{id:"hld-58",number:55,title:"Design a password reset system.",question:"Design a password reset system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Flow**: request reset → generate a **single-use, time-limited token** (random, hashed in DB) → email a reset link → user submits new password → validate token (unexpired, unused) → update hash + invalidate token + all sessions. **Security**: short TTL (~15 min), one-time use, don't reveal if the email exists (no enumeration), rate-limit, HTTPS.

\`\`\`text
── BACK-OF-ENVELOPE ──
Requests: ~1% of DAU/day = 100k/day = ~1.2/sec (tiny)
Token:    32-byte random, hashed, TTL 15 min, one-time
Storage:  ephemeral (Redis w/ TTL) — negligible

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       (1) request reset (email)
               ▼
        ┌──────────────┐
        │ Reset Service│──► store token (hashed, TTL) ─►┌────────┐
        └──────┬───────┘                                │ Redis  │
       (2) send link                                    └────────┘
               ▼
        ┌──────────────┐
        │ Email Service│──► reset link with token
        └──────────────┘
               │
       (3) user clicks + submits new password
               ▼
        ┌──────────────┐
        │ Reset Service│  validate token (unexpired/unused)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  User DB     │  update hash + invalidate token + sessions
        └──────────────┘
Security: one-time, 15-min TTL, no email enumeration, rate-limited, HTTPS.
\`\`\``,tech:"hld",displayNumber:55,sortKey:58},{id:"hld-59",number:56,title:"Design a notification service.",question:"Design a notification service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Functional**: send push/email/SMS/in-app reliably at scale. **Core**: notification API publishes events to **Kafka**; per-channel **workers** consume and call providers (APNs/FCM, SendGrid, Twilio). Store templates, preferences, delivery status. **Challenges**: retries + DLQ, idempotency (no duplicate sends), per-provider rate limits, opt-out, prioritization.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   500M notifications/day = ~5,800/sec (peak ~20k)
Fan-out:  1 event → up to 4 channels
Storage:  status 500M × 200 B/day = ~100 GB/day (retain 30d, then archive)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │  Producers   │  (order svc, etc.)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Notif API    │──► check preferences/templates
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Kafka topic  │  (buffer, absorb spikes)
        └──────┬───────┘
        ┌──────┼───────┬───────────┐
        ▼      ▼       ▼           ▼
   ┌───────┐┌──────┐┌──────┐ ┌─────────┐
   │ Push  ││Email ││ SMS  │ │ In-App  │  (per-channel workers)
   │worker ││worker││worker│ │ worker  │
   └───┬───┘└──┬───┘└──┬───┘ └────┬────┘
       ▼       ▼       ▼          ▼
    APNs/FCM SendGrid Twilio    WebSocket
       │       │       │          │
       └───────┴───► retries+DLQ, dedupe, status DB ◄──┘
\`\`\``,tech:"hld",displayNumber:56,sortKey:59},{id:"hld-60",number:57,title:"Design an email sending service.",question:"Design an email sending service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: API → queue → workers render templates → email provider (SES/SendGrid) or SMTP pool. Track status via **webhooks** (delivered/bounced/opened). **Challenges**: deliverability (SPF/DKIM/DMARC, IP warm-up, reputation), bounce/complaint handling (suppression list), retries + DLQ, idempotency, unsubscribe/compliance.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   200M emails/day = ~2,300/sec (peak ~8k)
Provider: rate-limited → queue smooths bursts
Storage:  status + suppression list: 200M × 300 B = ~60 GB/day (retain 30d)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │  Producers   │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Email API    │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │    Queue     │  (load leveling)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Send Workers │──► render template, check suppression
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Provider/SMTP│  (SES/SendGrid; SPF/DKIM/DMARC)
        └──────┬───────┘
               ▼ webhooks
        ┌──────────────┐
        │ Status DB    │  delivered/bounce/open → suppression list
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:57,sortKey:60},{id:"hld-61",number:58,title:"Design an SMS sending service.",question:"Design an SMS sending service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`Similar to email: API → queue → workers → SMS **gateway** (Twilio/SNS) with **failover/least-cost routing** by country. Delivery receipts via webhooks; OTP use cases (short TTL, rate-limited). **Challenges**: per-country regulations, sender-id rules, cost optimization, retries + DLQ, idempotency (no duplicate OTPs), abuse rate limiting.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   50M SMS/day = ~580/sec (peak ~2k); OTP spikes on login waves
Cost:     $/SMS varies by country → least-cost routing matters
Storage:  status 50M × 200 B = ~10 GB/day

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │  Producers   │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  SMS API     │──► rate limit (abuse/OTP)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │    Queue     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Send Workers │──► least-cost routing by country
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────┐   ┌────────┐
   │Provider│   │Provider│  (failover)
   │   A    │   │   B    │
   └───┬────┘   └───┬────┘
       └────► webhooks → Status DB, retries+DLQ
\`\`\``,tech:"hld",displayNumber:58,sortKey:61},{id:"hld-62",number:59,title:"Design a simple chat application.",question:"Design a simple chat application.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: clients connect via **WebSocket** to chat servers; messages persisted (DB) + routed via a **pub/sub backplane** (Redis/Kafka) so a message from a user on server A reaches a recipient on server B. Store history, unread counts, receipts; offline → push. **Challenges**: connection scale (sticky sessions + backplane), ordering, receipts, presence, offline delivery.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    50M DAU, ~5M concurrent connections
Messages: 20 msgs/user/day × 50M = 1B/day = ~11,600 msgs/sec (peak ~40k)
Conns/node: ~50k → need ~100 gateway nodes for 5M connections
Storage:  1B × 300 B/day = ~300 GB/day (retain 1 yr → shard by conversation)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐        ┌──────────────┐
        │  Client A    │        │  Client B    │
        └──────┬───────┘        └──────┬───────┘
         WebSocket                WebSocket
               ▼                        ▼
        ┌──────────────┐        ┌──────────────┐
        │ Gateway N1   │        │ Gateway N2   │
        └──────┬───────┘        └──────┬───────┘
               ▼   (publish)     (subscribe) ▲
        ┌──────────────────────────────┴─────┐
        │  Pub/Sub Backplane (Redis/Kafka)   │
        └──────┬─────────────────────────────┘
               ▼
        ┌──────────────┐
        │  Message DB  │  (sharded by conversation) + push for offline
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:59,sortKey:62},{id:"hld-63",number:60,title:"Design a simple task management system.",question:"Design a simple task management system.",difficulty:"Basic",category:"Design Problems (Easy)",answer:`**Core**: CRUD tasks (title, status, assignee, due, priority) with users/projects; filtering, sorting, status transitions. Relational DB (tasks, users, projects, FKs). Read replicas + caching for list views. **Challenges**: notifications (due/assignment), activity/audit log, real-time updates, per-project permissions, search.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    1M users, 100k DAU (internal-scale)
Ops:      100k × 50 actions/day = 5M/day = ~60/sec (small)
Storage:  10M tasks × 2 KB = ~20 GB → single DB + replicas suffices
Read-heavy: list/board views → cache + read replicas

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Load Balancer│
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Task Service │  (RBAC per project)
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────┐   ┌──────────────┐
   │ Cache  │   │ Relational DB│  primary + read replicas
   │ (lists)│   │ tasks/users/ │
   └────────┘   │ projects (FK)│
                └──────┬───────┘
                       ▼
                ┌──────────────┐
                │Notif + Audit │  (due/assignment, activity log)
                └──────────────┘
\`\`\``,tech:"hld",displayNumber:60,sortKey:63},{id:"hld-64",number:61,title:"Design a to-do application.",question:"Design a to-do application.",difficulty:"Basic",category:"Design Problems (Easy)",answer:`A minimal task manager: per-user items (text, done, due, order). REST API (or offline-first with sync); table \`todos(user_id, text, done, position, updated_at)\`. Multi-device sync via \`updated_at\` delta + conflict resolution (LWW/CRDT). **Challenges**: offline support, multi-device sync, reminders.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    5M users, 500k DAU
Ops:      500k × 20/day = 10M/day = ~120/sec (tiny)
Storage:  50M todos × 500 B = ~25 GB → single DB + replicas
Sync:     delta by updated_at cursor (only changed rows)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │ Client (multi│  offline-first cache
        │  device)     │
        └──────┬───────┘
       sync (since=updated_at)
               ▼
        ┌──────────────┐
        │ Todo Service │  conflict resolution (LWW/CRDT)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  Todo DB     │  todos(user_id, text, done, position, updated_at)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  Reminders   │  (scheduled jobs)
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:61,sortKey:64},{id:"hld-65",number:62,title:"Design a library management system.",question:"Design a library management system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Entities**: Book, BookCopy (per-copy status), Member, Loan (copy, member, due), Reservation. Relational DB with **transactions** for borrow/return (a copy can't be lent twice → row locking). **Flows**: search, borrow (check availability → loan → mark borrowed), return, reserve, fines. **Challenges**: concurrency on a copy, reservation queue, fine calc, due-date notifications.

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    bounded (one library/chain): 100k books, 500k copies, 200k members
Ops:      ~10k borrow/return per day = <1/sec → trivial load
Storage:  ~1M loan records/yr × 500 B = ~500 MB/yr (tiny)
Key concern: correctness (no double-lending), not throughput

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Library Service│
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Relational DB│  (transactional)
        │  Book        │
        │  BookCopy ◄──┼── lock row on borrow (no double-lend)
        │  Member      │
        │  Loan        │
        │  Reservation │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Notifications│  (due dates, reservation ready, fines)
        └──────────────┘
Borrow (TX): available? → create Loan + set copy=BORROWED
Return: free copy + compute fine if late; Reserve → queue.
\`\`\``,tech:"hld",displayNumber:62,sortKey:65},{id:"hld-66",number:63,title:"Design a parking lot system.",question:"Design a parking lot system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Entities**: ParkingLot → Levels → Spots (compact/large/handicap), Vehicle, Ticket. **Flows**: on entry, **assign an available spot** of the right size (transactional — no double-assign), issue ticket; on exit, compute fee by duration, free the spot. Track availability counts per level. **Challenges**: spot-assignment concurrency, pricing tiers, real-time availability, payment.

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    bounded — e.g. 2,000 spots, ~5k entries/exits per day
Load:     ~0.1 req/sec average (spiky at rush hour, still tiny)
Storage:  ~5k tickets/day × 300 B = ~1.5 MB/day (negligible)
Key concern: concurrency (two cars, one spot), not scale

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │Entry Terminal│
        └──────┬───────┘
       request spot
               ▼
        ┌──────────────┐
        │Parking Service│
        └──────┬───────┘
       assign spot (TRANSACTIONAL, lock row)
               ▼
        ┌──────────────┐
        │      DB      │
        │ Lot→Level→Spot(size, occupied)
        │ Ticket(spot, entry_time)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Availability  │  per-level free counts (cache/display)
        └──────────────┘
Exit: fee = f(duration) → pay → set spot free.
Assign spot uses SELECT ... FOR UPDATE so two cars can't grab one spot.
\`\`\``,tech:"hld",displayNumber:63,sortKey:66},{id:"hld-67",number:64,title:"Design a movie ticket booking system (like BookMyShow).",question:"Design a movie ticket booking system (like BookMyShow).",difficulty:"Hard",category:"Design Problems (Easy)",answer:`**Core**: browse movies/theatres/shows; select seats; **hold seats** for 5–10 min (transactional/locked so two users can't book the same seat), confirm on payment. Entities: Movie, Theatre, Screen, Show, Seat, Booking. Read-heavy browsing (cache+CDN), write-critical seat locking. **Challenges**: seat-locking concurrency (the crux), payment + timeout release, high-demand shows (waiting room), idempotent booking.

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    50M users, browse 100:1 vs book
Browse:   ~20k/sec (cache+CDN)
Bookings: 2M/day = ~25/sec avg, but HOT show = 10k seats sold in seconds
Storage:  seats/shows small; bookings 2M/day × 500 B = ~1 GB/day
Crux:     seat-lock concurrency on popular shows

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ CDN + Cache  │  (browse: movies/shows — read-heavy)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Booking Service│
        └──────┬───────┘
      (1) HOLD seats (lock, TTL 8 min)
               ▼
        ┌──────────────┐
        │ Seat Store   │  Seat(show_id, seat_id, status) — row lock / Redis lock
        └──────┬───────┘
      (2) pay
               ▼
        ┌──────────────┐
        │Payment Service│──► success → CONFIRM ; timeout → RELEASE hold
        └──────────────┘
Hot show: virtual waiting room + rate limiting in front of Booking Service.
\`\`\``,tech:"hld",displayNumber:64,sortKey:67},{id:"hld-68",number:65,title:"Design a restaurant reservation system.",question:"Design a restaurant reservation system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: search by time/party/location; check **table availability** for a slot; reserve (transactional hold — no double-booking), confirm, notify. Entities: Restaurant, Table, TimeSlot, Reservation. Read-heavy search (cache, geo-index), write-critical availability. **Challenges**: availability concurrency (lock table+slot), overbooking policy, no-show/reminders, waitlists, time zones.

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    10M users, 500k restaurants
Search:   ~5k/sec (geo + time filters) → cache + geo-index
Reserves: 1M/day = ~12/sec (write-critical, low volume)
Storage:  reservations 1M/day × 400 B = ~400 MB/day

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Search (geo) │  cache + geo-index (nearby, time, party size)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Reservation Svc│
        └──────┬───────┘
       reserve (TRANSACTIONAL: lock table+slot)
               ▼
        ┌──────────────┐
        │      DB      │
        │ Restaurant   │
        │ Table        │
        │ Reservation(table, slot) ◄── no double-book
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Notify+Reminder│  (confirm, reminder, no-show handling)
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:65,sortKey:68},{id:"hld-69",number:66,title:"Design a meeting-room booking system.",question:"Design a meeting-room booking system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: list rooms; check availability for a time range; book (transactional — reject overlapping bookings via a constraint/lock on room+interval); modify/cancel; calendar view. Entities: Room, Booking(room, start, end, organizer). **Key**: **interval-overlap** detection. **Challenges**: concurrency, recurring bookings, time zones, capacity/equipment filters, calendar integration.

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    corporate — 10k rooms, 100k employees, ~50k bookings/day
Load:     ~0.6 bookings/sec (tiny); reads (calendar) higher, cacheable
Storage:  50k/day × 400 B = ~20 MB/day → single DB
Key concern: no overlapping bookings for a room

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Booking Service│
        └──────┬───────┘
       book (reject if overlapping range exists)
               ▼
        ┌──────────────┐
        │      DB      │
        │ Room         │
        │ Booking(room, start, end, organizer)
        │  ▲ EXCLUDE constraint: (room =, tsrange &&) → no overlap
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Calendar/Notify│  (recurring, reminders, TZ handling)
        └──────────────┘
Postgres: EXCLUDE USING gist (room WITH =, during WITH &&) blocks overlaps atomically.
\`\`\``,tech:"hld",displayNumber:66,sortKey:69},{id:"hld-70",number:67,title:"Design a calendar application (like Google Calendar).",question:"Design a calendar application (like Google Calendar).",difficulty:"Hard",category:"Design Problems (Easy)",answer:`**Core**: events (title, start/end, attendees, recurrence), calendars per user, invitations/RSVP, reminders. Store **recurrence rules (RRULE)** not expanded copies; expand on read for a date range. Read-heavy (cache month views), shard by user. **Challenges**: recurring events + exceptions, **time zones/DST**, cross-user invitations (fan-out), reminders (scheduled jobs), free/busy, delta sync.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    500M users, 100M DAU
Reads:    100M × 20 views/day = 2B/day = ~23k/sec (cache month views)
Writes:   100M × 5 events/day = 500M/day = ~5,800/sec
Storage:  store RRULE not expansions → ~2 KB/event × 5B events = ~10 TB (shard by user)
Reminders: distributed scheduler fires millions of reminders/day

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │  (delta sync via sync token)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Calendar Service│
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────┐   ┌──────────────┐
   │ Cache  │   │  Event DB    │  (sharded by user)
   │(views) │   │ Event(RRULE, │
   └────────┘   │ attendees)   │  ← store rule, expand on read
                └──────┬───────┘
       invite fan-out  │
               ▼       ▼
        ┌──────────────┐   ┌──────────────┐
        │ Attendees'   │   │ Reminder     │
        │ calendars    │   │ Scheduler    │
        └──────────────┘   └──────────────┘
Hard parts: recurrence + exceptions, TZ/DST, free/busy, invite fan-out.
\`\`\``,tech:"hld",displayNumber:67,sortKey:70},{id:"hld-71",number:68,title:"Design a simple polling/voting system.",question:"Design a simple polling/voting system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: create a poll with options; users cast votes; show tallies. Entities: Poll, Option, Vote(user, option). Enforce **one vote per user** (unique constraint). For viral polls (write-heavy), use a **distributed counter** (Redis INCR / Kafka-buffered counts) + cache results. **Challenges**: preventing double/fraud voting, real-time results, high write throughput on popular polls.

\`\`\`text
── BACK-OF-ENVELOPE ──
Normal:   1M votes/day = ~12/sec
Viral:    a hot poll → 100k votes/min = ~1,700/sec on ONE poll (hot key!)
Storage:  votes 1M/day × 100 B = ~100 MB/day
Hot key:  single poll counter → shard the counter or Redis INCR

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Vote Service │  enforce one-vote-per-user (unique(user,poll))
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────────┐  ┌──────────────┐
   │ Redis INCR │  │  Vote DB     │  (raw votes, audit)
   │ (sharded   │  │ Vote(user,   │
   │  counter)  │  │  option)     │
   └─────┬──────┘  └──────────────┘
         ▼ (flush)
   ┌────────────┐
   │ Results    │  cached tallies → real-time display
   └────────────┘
Hot poll: shard counter into N sub-counters, sum on read (avoid single-row contention).
\`\`\``,tech:"hld",displayNumber:68,sortKey:71},{id:"hld-72",number:69,title:"Design a feedback/rating system.",question:"Design a feedback/rating system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: users submit ratings (1–5) + reviews for an entity; compute/display **average + count**. Entities: Rating(entity, user, score, text). Store raw ratings; maintain a **running aggregate** (sum, count → avg) updated on write, or recompute async. Read-heavy (cache aggregates); one rating per user per entity. **Challenges**: efficient averages, spam/fake reviews (moderation), recency/verified weighting, pagination.

\`\`\`text
── BACK-OF-ENVELOPE ──
Reads:    product pages show avg → ~50k/sec → cache the aggregate
Writes:   10M ratings/day = ~116/sec
Storage:  raw ratings 10M/day × 500 B = ~5 GB/day; aggregates tiny
Aggregate: maintain (sum, count) per entity → avg = sum/count (O(1) read)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Rating Service│  unique(user, entity)
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────────┐  ┌──────────────┐
   │ Aggregate  │  │  Rating DB   │  (raw reviews)
   │ Cache      │  │ Rating(entity│
   │ sum/count  │  │  user,score, │
   │ → avg      │  │  text)       │
   └────────────┘  └──────┬───────┘
                          ▼
                   ┌──────────────┐
                   │ Moderation   │  (spam/fake detection, async)
                   └──────────────┘
On write: update (sum += score, count += 1) → avg recomputed cheaply.
\`\`\``,tech:"hld",displayNumber:69,sortKey:72},{id:"hld-73",number:70,title:"Design a basic blogging platform.",question:"Design a basic blogging platform.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: authors write posts (title, body/markdown, tags, draft/published), readers view posts + comments. Entities: User, Post, Comment, Tag. Relational DB; render markdown; SEO slugs. Read-heavy → **cache rendered posts + CDN**; search via Elasticsearch. **Challenges**: comment moderation/spam, drafts, media uploads, feeds/RSS, full-text search.

\`\`\`text
── BACK-OF-ENVELOPE ──
Reads:    1B page views/day = ~11,600/sec → 95% served by CDN/cache
Writes:   100k posts/day = ~1/sec (write-light, read-heavy)
Storage:  10M posts × 10 KB = ~100 GB → single DB + replicas + search index

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Reader     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │     CDN      │  (rendered posts — read-heavy)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Blog Service │
        └──────┬───────┘
        ┌──────┼──────────┐
        ▼      ▼          ▼
   ┌────────┐┌──────┐┌──────────────┐
   │ Cache  ││ Post ││Elasticsearch │
   │(render)││  DB  ││ (full-text)  │
   └────────┘└──┬───┘└──────────────┘
               ▼
        ┌──────────────┐
        │Comment + Media│  moderation; media → object storage + CDN
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:70,sortKey:73},{id:"hld-74",number:71,title:"Design a document storage system.",question:"Design a document storage system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: upload documents to **object storage** (pre-signed URLs), metadata in DB (name, type, size, owner, folder, version), organize in folders, search by metadata/content, share with permissions. Index content in Elasticsearch. **Challenges**: **versioning**, access control/sharing (ACLs), full-text search (extract text async), large/resumable uploads, audit logging.

\`\`\`text
── BACK-OF-ENVELOPE ──
Uploads:  5M docs/day × 1 MB = 5 TB/day → object storage
Reads:    20:1 (views/downloads) = ~1,200/sec
Storage:  5 TB/day × 365 = ~1.8 PB/yr; metadata 5M × 1 KB = ~5 GB/day
Search:   index extracted text in Elasticsearch

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       upload (pre-signed URL, direct)
               ▼
        ┌──────────────┐
        │Object Storage│──► event ──►┌──────────────┐
        │  (docs)      │             │ Text Extract │
        └──────────────┘             │ Worker (async)│
               ▲                     └──────┬───────┘
     store metadata + versions              ▼
               ▼                     ┌──────────────┐
        ┌──────────────┐             │Elasticsearch │
        │ Metadata DB  │             │ (full-text)  │
        │ folders,     │             └──────────────┘
        │ versions,ACL │
        └──────────────┘
Sharing via ACLs; versioning keeps history; audit log on access.
\`\`\``,tech:"hld",displayNumber:71,sortKey:74},{id:"hld-75",number:72,title:"Design a photo storage service (like Google Photos, basic).",question:"Design a photo storage service (like Google Photos, basic).",difficulty:"Hard",category:"Design Problems (Easy)",answer:`**Core**: upload direct-to-S3, generate thumbnails/resolutions (async), store metadata (album, timestamp, EXIF, geo), serve via CDN, albums, search. Huge storage → object storage + tiering; sharded metadata. **Challenges**: async pipeline, dedup (content hash), face/scene tagging (ML), privacy (EXIF/geo), massive read scale.

\`\`\`text
── BACK-OF-ENVELOPE ──
Uploads:  50M photos/day × 3 MB = 150 TB/day ingested
Variants: +4 sizes ≈ +1.5 MB each → ~200 TB/day total
Storage:  ~70 PB/yr → object storage + hot/cold tiering
Reads:    50:1 (views) → CDN offloads ~95%; metadata sharded by user

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │
        └──────┬───────┘
       upload direct-to-S3
               ▼
        ┌──────────────┐
        │Object Storage│──► event ──►┌──────────────┐
        │ (original)   │             │ Photo Worker │
        └──────────────┘             │ resize, dedup│
               ▲                     │ ML tags      │
      variants │                     └──────┬───────┘
               ▼                            ▼
        ┌──────────────┐             ┌──────────────┐
        │Object Storage│             │ Metadata DB  │
        │ (variants)   │             │ album/EXIF/  │
        └──────┬───────┘             │ geo (sharded)│
               ▼                     └──────────────┘
        ┌──────────────┐
        │     CDN      │  (device-appropriate delivery)
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:72,sortKey:75},{id:"hld-76",number:73,title:"Design a contact management system.",question:"Design a contact management system.",difficulty:"Basic",category:"Design Problems (Easy)",answer:`**Core**: CRUD contacts (name, phones, emails, addresses, groups/tags) per user; search; import/export (vCard/CSV); multi-device sync. Entities: Contact, ContactField, Group. **Challenges**: **deduplication/merge** (same person, multiple entries), sync + conflict resolution, search (name/phone/email), import mapping.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    100M users, avg 500 contacts each = 50B contact records
Ops:      mostly reads + occasional edits/sync → ~2k/sec
Storage:  50B × 500 B = ~25 TB → shard by user
Sync:     delta by updated_at

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │Client (multi │
        │ device)      │
        └──────┬───────┘
       sync (since=updated_at)
               ▼
        ┌──────────────┐
        │Contact Service│  dedup/merge, conflict resolution
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  Contact DB  │  (sharded by user)
        │ Contact(name,│
        │ phones[],    │
        │ emails[])    │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │Import/Export │  (vCard/CSV), search index
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:73,sortKey:76},{id:"hld-77",number:74,title:"Design an employee management system.",question:"Design an employee management system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: employees (profile, department, manager, role, salary), org hierarchy, attendance/leave, payroll integration. Entities: Employee (self-referential manager for the **org tree**), Department, Role, LeaveRequest. Relational DB + RBAC. **Challenges**: hierarchical queries (recursive CTE), sensitive-data access control, audit trails, leave-approval workflow, integrations (payroll, SSO).

\`\`\`text
── BACK-OF-ENVELOPE ──
Scale:    enterprise — 500k employees, ~50k DAU
Load:     ~500/sec (HR ops, self-service) — modest
Storage:  500k × 5 KB = ~2.5 GB (tiny) + audit log grows
Key:      org-tree queries + strict RBAC on salary/PII

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │  (SSO)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  HR Service  │  RBAC (HR vs employee views)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │      DB      │
        │ Employee(manager_id self-ref) ◄─ recursive CTE = org chart
        │ Department, Role, LeaveRequest
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Workflow +   │  leave approval, audit trail
        │ Payroll/SSO  │  integrations
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:74,sortKey:77},{id:"hld-78",number:75,title:"Design an API key management service.",question:"Design an API key management service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: generate API keys, store only a **hash** (show once), validate keys on requests (fast + cached), scopes/permissions, rotation, revocation, **usage tracking + rate limits** per key. Entities: ApiKey(hash, owner, scopes, status, expires). Validation is hot → cache in Redis. **Challenges**: secure storage (hash), fast validation, rotation without downtime, per-key quotas, audit.

\`\`\`text
── BACK-OF-ENVELOPE ──
Keys:     1M active keys
Validations: every API request ≈ 100k/sec → MUST be cached (no DB per call)
Storage:  keys 1M × 500 B = ~500 MB; usage metrics grow (aggregate)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Client     │  (sends API key)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ API Gateway  │  hash key → validate
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────────┐  ┌──────────────┐
   │Redis (hot  │  │  Key DB      │
   │ key cache) │  │ ApiKey(hash, │
   │ hash→meta  │  │ scopes,      │
   └────────────┘  │ status)      │
        │          └──────────────┘
        ▼
   ┌────────────┐
   │ Rate Limit │  per-key quota + usage metrics
   └────────────┘
Generate → store HASH only (show once). Rotation: overlap old+new. Revoke → evict cache.
\`\`\``,tech:"hld",displayNumber:75,sortKey:78},{id:"hld-79",number:76,title:"Design a feature flag service.",question:"Design a feature flag service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: define flags with targeting (on/off, % rollout, per-user/segment); SDKs evaluate flags **locally** using rules fetched/streamed from the service; changes apply without redeploy. Components: management API/UI, config store, delivery (poll/stream/CDN) + local caching. **Challenges**: fast/consistent propagation, deterministic % bucketing (hash userId), low-latency evaluation, audit, kill-switches.

\`\`\`text
── BACK-OF-ENVELOPE ──
Flags:    ~10k flags; evaluations happen IN-APP (SDK, local) → billions/day, ~0 latency
Rule fetch: SDKs poll/stream rules every few sec → tiny bandwidth
Storage:  flag config tiny (~MBs); audit log grows

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │ Admin UI/API │  define flags + rules
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Config Store │  (versioned, audit)
        └──────┬───────┘
       push/stream changes
               ▼
        ┌──────────────┐
        │ Delivery/CDN │  (poll/stream rules)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ App + SDK    │  evaluate LOCALLY (cached rules) — no network per check
        └──────────────┘
% rollout: bucket = hash(flag + userId) % 100 < percent  (deterministic).
\`\`\``,tech:"hld",displayNumber:76,sortKey:79},{id:"hld-80",number:77,title:"Design a configuration management service.",question:"Design a configuration management service.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: centrally store config per app/env; services fetch on startup + get **dynamic updates** (poll/watch/stream) without redeploy; versioning + rollback; secrets integration. Components: config store (with history), API, client/agent with local cache. Read-heavy → cache + edge. **Challenges**: consistency/propagation speed, validation before apply, env overrides, secrets (Vault), audit.

\`\`\`text
── BACK-OF-ENVELOPE ──
Configs:  per (app, env) — thousands of keys; small data
Fetch:    every service instance watches → low bandwidth (deltas)
Storage:  config + version history ~ GBs; secrets in Vault (separate)

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │ Admin/API    │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │ Config Store │  versioned (rollback), validated
        └──────┬───────┘
       watch/stream changes
               ▼
        ┌──────────────┐
        │Service + Agent│  local cache (serve last-known if store down)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │    Vault     │  (secrets — separate, encrypted)
        └──────────────┘
Dynamic refresh: @RefreshScope picks up changes without restart.
\`\`\``,tech:"hld",displayNumber:77,sortKey:80},{id:"hld-81",number:78,title:"Design a simple log aggregation system.",question:"Design a simple log aggregation system.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: agents ship structured logs → **Kafka buffer** → processors parse/enrich (add service, trace id) → searchable store (Elasticsearch/Loki) → UI (Kibana/Grafana). Kafka absorbs bursts. Index sharding, hot/warm/cold tiers, sampling. **Challenges**: high write volume, retention cost, searchability (structured + correlation id), PII scrubbing.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   1M log lines/sec (large fleet) × 500 B = ~500 MB/sec = ~43 TB/day
Kafka:    buffers bursts; retention 3–7 days
Index:    hot (7d in ES) + warm/cold (object storage) → control cost
Sampling: drop/sample noisy DEBUG logs

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │ Hosts + Agent│  (Filebeat/Fluentd)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │    Kafka     │  (buffer, absorb bursts)
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │  Processors  │  parse/enrich (+ trace id), scrub PII
        └──────┬───────┘
        ┌──────┴──────┐
        ▼             ▼
   ┌────────────┐  ┌──────────────┐
   │Elasticsearch│  │Object Storage│
   │ (hot, 7d)  │  │ (cold tier)  │
   └─────┬──────┘  └──────────────┘
         ▼
   ┌────────────┐
   │Kibana/Grafana│  query by trace id
   └────────────┘
\`\`\``,tech:"hld",displayNumber:78,sortKey:81},{id:"hld-82",number:79,title:"Design a health-check service.",question:"Design a health-check service.",difficulty:"Basic",category:"Design Problems (Easy)",answer:`**Core**: each service exposes **liveness** (process alive? restart if not) + **readiness** (can serve? pull from LB if not); a central monitor / orchestrator / LB probes periodically and acts (restart, de-route, alert). **Challenges**: liveness vs readiness distinction, dependency checks (shallow for liveness, deeper for readiness), flapping avoidance (thresholds), alerting integration.

\`\`\`text
── BACK-OF-ENVELOPE ──
Probes:   10k services × 1 probe/5s = ~2k probes/sec (cheap, distributed)
Data:     ephemeral status; only alerts/history persisted

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │ Orchestrator │  (k8s) / LB / Monitor
        └──────┬───────┘
       probe every N sec
        ┌──────┴──────┐
        ▼             ▼
 ┌────────────┐ ┌────────────┐
 │ Service    │ │ Service    │
 │ /liveness  │ │ /readiness │
 └─────┬──────┘ └─────┬──────┘
       │              │
   liveness fail   readiness fail
       ▼              ▼
   RESTART pod    REMOVE from LB rotation
       │
       ▼
   ┌────────────┐
   │  Alerting  │  (thresholds → avoid flapping)
   └────────────┘
Liveness = shallow (is process up?). Readiness = deeper (deps OK, can serve?).
\`\`\``,tech:"hld",displayNumber:79,sortKey:82},{id:"hld-83",number:80,title:"Design a simple job scheduler.",question:"Design a simple job scheduler.",difficulty:"Intermediate",category:"Design Problems (Easy)",answer:`**Core**: schedule jobs at a time or cron; scheduler polls due jobs → dispatches to **workers** via a queue; track status + retries. Store jobs with \`next_run_at\` (indexed). **HA**: multiple schedulers need **atomic claim** (\`SELECT ... FOR UPDATE SKIP LOCKED\`) or leader election to avoid double-dispatch. **Challenges**: exactly-once dispatch, retries + backoff, missed-run handling, coordination.

\`\`\`text
── BACK-OF-ENVELOPE ──
Jobs:     10M scheduled jobs, ~100k fire/min = ~1,700/sec dispatch
Storage:  jobs 10M × 500 B = ~5 GB; index on next_run_at
HA:       atomic claim so a job fires exactly once across schedulers

── ARCHITECTURE (top → bottom) ──
        ┌──────────────┐
        │   Job DB     │  Job(schedule, next_run_at, status), index(next_run_at)
        └──────┬───────┘
       poll due jobs (atomic claim)
               ▼
        ┌──────────────┐
        │  Scheduler   │  SELECT ... FOR UPDATE SKIP LOCKED (no double-dispatch)
        │  (N replicas)│
        └──────┬───────┘
       enqueue
               ▼
        ┌──────────────┐
        │    Queue     │
        └──────┬───────┘
               ▼
        ┌──────────────┐
        │   Workers    │  execute → update status; retries + backoff
        └──────────────┘
After run: recompute next_run_at (cron) or mark done. Missed runs → catch-up policy.
\`\`\``,tech:"hld",displayNumber:80,sortKey:83},{id:"hld-84",number:81,title:"Design a distributed cache.",question:"Design a distributed cache.",difficulty:"Hard",category:"Distributed Systems",answer:`Partition the keyspace across cache nodes using **consistent hashing** (so adding/removing a node reshuffles minimal keys), replicate each partition for availability, and route clients via a hash of the key. Support TTL/eviction (LRU/LFU), cache-aside reads, and invalidation. **Scale**: add nodes → ring rebalances; replicas serve reads and enable failover. **Challenges**: hot keys (replicate/local-cache them), consistency vs freshness, node failure handling, and cache stampede (locks/singleflight). Redis Cluster is a real implementation.

\`\`\`text
── BACK-OF-ENVELOPE ──
Reads:    1M ops/sec → sharded across N nodes (~50k/node)
Memory:   1 TB working set → ~10 nodes × 128 GB (+ replicas)
Hit rate: target >90% → sized to hold hot 10-20% of data

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │  hash(key) → route
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Consistent    │  ring: key → node (minimal reshuffle on change)
   │Hash Router   │
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Node A││Node B││Node C│  each + replica (failover)
│(LRU  ││      ││      │
│ +TTL)││      ││      │
└──────┘└──────┘└──────┘
Hot key → replicate / local cache. Stampede → singleflight. (Redis Cluster)
\`\`\``,tech:"hld",displayNumber:81,sortKey:84},{id:"hld-85",number:82,title:"Design a distributed rate limiter.",question:"Design a distributed rate limiter.",difficulty:"Hard",category:"Distributed Systems",answer:`Keep counters in a **shared store (Redis)** keyed by client so the limit holds across all app instances. Use **token bucket** or **sliding-window** logic executed atomically (Redis Lua script / \`INCR\` + \`EXPIRE\`). Return 429 + \`Retry-After\` when exceeded. **Scale**: Redis cluster shards by key; for extreme scale, allow slight approximation (local buckets + async sync). **Challenges**: atomicity, clock skew, hot keys, accuracy-vs-latency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Checks:   every request → 1 Redis op (atomic Lua) → ~500k/sec
State:    per client key ~50 B → millions of clients = few GB (sharded)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Request    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ API Instance │
   └──────┬───────┘
     atomic check (Lua)
          ▼
   ┌──────────────┐
   │ Redis (shared│  token-bucket/sliding-window per client key
   │ , sharded)   │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────┐   ┌──────────┐
 │Allow │   │429 + Retry│
 │      │   │-After    │
 └──────┘   └──────────┘
Extreme scale → local buckets + async sync (approximate).
\`\`\``,tech:"hld",displayNumber:82,sortKey:85},{id:"hld-86",number:83,title:"Design a token bucket rate limiter.",question:"Design a token bucket rate limiter.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Each client has a bucket with \`capacity\` tokens that refills at \`rate\` tokens/sec. A request consumes one token if available; else it's rejected (429). Allows **bursts** up to capacity while enforcing an average rate. Store \`{tokens, lastRefill}\` per client (Redis); on each request compute refill = elapsed × rate, cap at capacity, then consume — atomically. Simple, efficient, burst-friendly (the most common choice).

\`\`\`text
── ALGORITHM + FLOW (top → bottom) ──
State per client: { tokens, lastRefill }
   ┌──────────────┐
   │   Request    │
   └──────┬───────┘
          ▼
   ┌──────────────────────────────┐
   │ refill = (now - lastRefill)×rate │
   │ tokens = min(capacity, tokens+refill) │
   └──────┬───────────────────────┘
          ▼
   ┌──────────────┐
   │ tokens >= 1 ?│
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ yes        ▼ no
 ┌──────────┐ ┌──────┐
 │tokens--; │ │ 429  │
 │ allow    │ │      │
 └──────────┘ └──────┘
Bursts up to capacity; avg = rate. Atomic in Redis (Lua).
\`\`\``,tech:"hld",displayNumber:83,sortKey:86},{id:"hld-87",number:84,title:"Design a sliding-window rate limiter.",question:"Design a sliding-window rate limiter.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Fixed-window counters have a boundary problem (2× burst at the edge). **Sliding-window log** stores request timestamps in a sorted set and counts those within the last window — accurate but memory-heavy. **Sliding-window counter** approximates by weighting previous + current fixed windows by overlap — accurate enough and cheap. Both keyed per client in Redis. Counter for scale, log for strict accuracy.

\`\`\`text
── THREE APPROACHES ──
   ┌──────────────────────────────────────────┐
   │ Fixed window: count/window → 2× burst at edge (cheap, inaccurate)
   ├──────────────────────────────────────────┤
   │ Sliding LOG: sorted set of timestamps      │
   │   count in [now-window, now] → accurate, heavy memory
   ├──────────────────────────────────────────┤
   │ Sliding COUNTER: weight(prev, cur) by overlap
   │   → accurate + cheap (recommended)         │
   └──────────────────────────────────────────┘
Keyed per client in Redis. Pick counter for scale, log for strict accuracy.
\`\`\``,tech:"hld",displayNumber:84,sortKey:87},{id:"hld-88",number:85,title:"Design a distributed lock.",question:"Design a distributed lock.",difficulty:"Hard",category:"Distributed Systems",answer:`Ensures only one process holds a resource across machines. **Redis \`SET key value NX PX ttl\`** (acquire if absent, TTL auto-releases a crashed holder); release only if you own it (compare value via Lua). Stronger: **ZooKeeper/etcd** (ephemeral nodes/leases + Raft). Include a **fencing token** (monotonic id) so a paused-then-resumed holder can't act after losing the lock. **Challenges**: TTL vs long tasks, clock skew, safety under GC pauses.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Process A   │
   └──────┬───────┘
     SET lock:res <A> NX PX 30000  (acquire if absent, TTL)
          ▼
   ┌──────────────┐
   │ Lock Store   │  (Redis / etcd / ZooKeeper)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ acquired   ▼ held by other
 ┌──────────┐ ┌──────────┐
 │do work + │ │ wait /   │
 │renew lease│ │ retry    │
 │+ FENCING │ │          │
 │TOKEN     │ └──────────┘
 └────┬─────┘
      ▼
   release: Lua → delete only if value == A
Fencing token prevents stale holder acting after lease loss. Crash → TTL frees lock.
\`\`\``,tech:"hld",displayNumber:85,sortKey:88},{id:"hld-89",number:86,title:"Design a leader-election system.",question:"Design a leader-election system.",difficulty:"Hard",category:"Distributed Systems",answer:`Elect one coordinator among nodes so work isn't duplicated (single scheduler, write master). Nodes race to create an ephemeral lock/lease in a **coordination service** (ZooKeeper/etcd/Consul); the winner is leader and renews via heartbeat; if it dies, the lease expires and another node wins. Raft/Paxos underpin this. **Fencing tokens** prevent split-brain. **Challenges**: split-brain avoidance, failover speed, lease/heartbeat tuning.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │  Node 1  │ │  Node 2  │ │  Node 3  │  race to acquire lease
   └────┬─────┘ └────┬─────┘ └────┬─────┘
        └───────┬────┴────────────┘
                ▼
        ┌──────────────┐
        │Coordination  │  ephemeral lease (etcd/ZooKeeper, Raft)
        │Service       │
        └──────┬───────┘
          winner
                ▼
        ┌──────────────┐
        │  LEADER      │  heartbeats to renew lease
        └──────┬───────┘
          leader dies → lease expires
                ▼
        ┌──────────────┐
        │ Re-election  │  another node wins (+ fencing token)
        └──────────────┘
\`\`\``,tech:"hld",displayNumber:86,sortKey:89},{id:"hld-90",number:87,title:"Design a distributed ID generator.",question:"Design a distributed ID generator.",difficulty:"Hard",category:"Distributed Systems",answer:`Generate unique, roughly time-ordered IDs across many nodes without per-ID coordination. Options: **UUID** (no coordination, random/large, not sortable), **DB auto-increment** (bottleneck/SPOF), **ticket server ranges** (each node gets a block), or **Snowflake** (64-bit: timestamp + machine id + sequence — sortable, high throughput, decentralized). Snowflake is the standard. **Challenges**: clock skew, coordination-free uniqueness, ordering.

\`\`\`text
── BACK-OF-ENVELOPE ──
Throughput: Snowflake = 4096 ids/ms/node × 1024 nodes = ~4B ids/sec
No coordination per id (local generation) → scales linearly

── OPTIONS (top → bottom) ──
   ┌──────────────────────────────────────┐
   │ UUID         → no coord, NOT sortable, 128-bit
   ├──────────────────────────────────────┤
   │ DB auto-inc  → simple, but SPOF/bottleneck
   ├──────────────────────────────────────┤
   │ Range alloc  → node reserves block [1000-1999], local
   ├──────────────────────────────────────┤
   │ SNOWFLAKE    → [timestamp|machineId|seq] local, sortable ✓
   └──────────────────────────────────────┘
Snowflake = decentralized + time-sortable + high throughput.
\`\`\``,tech:"hld",displayNumber:87,sortKey:90},{id:"hld-91",number:88,title:"Design Twitter Snowflake-like ID generation.",question:"Design Twitter Snowflake-like ID generation.",difficulty:"Hard",category:"Distributed Systems",answer:`A 64-bit ID = 1 sign bit + **41 bits timestamp** (~69 years) + **10 bits machine id** (1024 nodes) + **12 bits sequence** (4096 ids/ms/node). Each node: take current ms; if same ms as last, increment sequence (wait for next ms on overflow), else reset. Unique, time-sortable, ~4M ids/sec/node, no central coordination. **Challenges**: clock going backwards (reject/wait), machine-id assignment, epoch choice.

\`\`\`text
── 64-BIT LAYOUT ──
 ┌─┬───────────────────┬──────────┬────────────┐
 │0│ 41-bit timestamp  │10 machine│ 12 sequence│
 │ │ (ms since epoch)  │ id (1024)│ (4096/ms)  │
 └─┴───────────────────┴──────────┴────────────┘

── GENERATION FLOW (top → bottom) ──
   ┌──────────────┐
   │ now = ms     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ now == last? │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ yes        ▼ no
 ┌──────────┐ ┌──────────┐
 │seq++     │ │seq = 0   │
 │(overflow │ │last = now│
 │→wait ms) │ └──────────┘
 └────┬─────┘
      ▼
   id = (ts<<22) | (machineId<<12) | seq
Clock backwards → reject/wait. ~4M ids/sec/node, sortable.
\`\`\``,tech:"hld",displayNumber:88,sortKey:91},{id:"hld-92",number:89,title:"Design a service discovery system.",question:"Design a service discovery system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`A **registry** (HA, on etcd/Consul/ZooKeeper) stores instances (name → host:port + health). Instances **register** on startup + send **heartbeats**; the registry evicts non-reporting ones. Clients discover via query (client-side) or a router/DNS resolves (server-side). Replicate the registry (consensus); cache lookups (TTL). **Challenges**: registry availability (critical infra), stale entries, health accuracy, fast propagation.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Instance    │  (1) REGISTER {name, host:port, health} + heartbeat
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Registry    │  (HA, consensus-backed) — evicts dead instances
   │ name→[hosts] │
   └──────┬───────┘
          ▲ (2) query "healthy 'orders' instances?"
   ┌──────┴───────┐
   │   Caller     │  → [ip1, ip2] → load balance (cache w/ TTL)
   └──────────────┘
Client-side: caller queries + LBs. Server-side: LB/DNS resolves. (Eureka/Consul)
\`\`\``,tech:"hld",displayNumber:89,sortKey:92},{id:"hld-93",number:90,title:"Design a configuration service.",question:"Design a configuration service.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Centralized store for app/env config with **versioning**, **dynamic updates** (watch/poll), rollback, access control. Built on a consistent store (etcd/Consul/DB); clients cache locally + subscribe to changes so updates apply without redeploy. Read-heavy → cache + edge. **Challenges**: consistency/propagation, validation before apply, env overrides, secrets (Vault), audit.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Admin / API  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Config Store │  versioned (rollback), validated, audited
   └──────┬───────┘
     watch/stream changes
          ▼
   ┌──────────────┐
   │Service + Local│  cache (fast reads; serve last-known if store down)
   │Cache          │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    Vault     │  (secrets — separate, encrypted)
   └──────────────┘
Dynamic refresh without redeploy.
\`\`\``,tech:"hld",displayNumber:90,sortKey:93},{id:"hld-94",number:91,title:"Design a centralized logging system.",question:"Design a centralized logging system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Agents ship structured logs → **Kafka buffer** → processors (parse/enrich, add service + trace id) → searchable store (Elasticsearch/Loki) → UI (Kibana/Grafana). Kafka absorbs bursts. Index sharding, hot/warm/cold tiers, sampling. **Challenges**: huge write volume, retention cost, structured + correlatable logs, PII scrubbing, query performance.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   1M lines/sec × 500 B = ~43 TB/day → Kafka buffers, ES hot 7d, cold tier after

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Hosts + Agent │  (Filebeat/Fluentd)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    Kafka     │  (buffer, absorb bursts)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Processors  │  parse/enrich (+ trace id), scrub PII
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────────┐┌──────────────┐
 │Elasticsearch││Object Storage│
 │(hot, 7d)   ││(cold tier)   │
 └─────┬──────┘└──────────────┘
       ▼
 ┌────────────┐
 │Kibana/Grafana│  query by trace id
 └────────────┘
\`\`\``,tech:"hld",displayNumber:91,sortKey:94},{id:"hld-95",number:92,title:"Design a metrics collection system.",question:"Design a metrics collection system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Services expose metrics; a collector **scrapes** (Prometheus pull) or receives **pushed** metrics → **time-series DB** → dashboard (Grafana) + alerting. Labels/tags for dimensions. TSDB with downsampling/retention tiers, federation/sharding, long-term storage (Thanos/Cortex/M3). **Challenges**: high cardinality (label explosion), storage cost, aggregation, SLO alerting.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Services    │  expose /metrics (counters, gauges, histograms)
   └──────┬───────┘
     scrape (pull) or push
          ▼
   ┌──────────────┐
   │ Prometheus   │  (+ Thanos/Cortex for long-term & scale)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Time-Series DB│  downsampling + retention tiers
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────┐ ┌──────────┐
 │Grafana │ │Alerting  │  (SLO-based)
 │(dash)  │ │          │
 └────────┘ └──────────┘
Watch: cardinality explosion (labels), storage cost.
\`\`\``,tech:"hld",displayNumber:92,sortKey:95},{id:"hld-96",number:93,title:"Design a distributed tracing system.",question:"Design a distributed tracing system.",difficulty:"Hard",category:"Distributed Systems",answer:`Instrument services to generate **spans** linked by a **trace id** propagated across calls (W3C \`traceparent\`). Spans exported (OpenTelemetry) → collector → storage (Jaeger/Tempo) → UI (end-to-end trace + per-hop latency). **Sampling** (head/tail) controls volume; async export. **Challenges**: context propagation (sync/async), sampling strategy, clock skew, storage cost. Correlate with logs/metrics via trace id.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Edge / API  │  generate trace id (traceparent)
   └──────┬───────┘
     propagate id on every call
          ▼
   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
   │ Service A    │─►│ Service B    │─►│ Service C    │
   │ span         │  │ span (child) │  │ span (child) │
   └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
          └──────── export spans ─────────────┘
                ▼
        ┌──────────────┐
        │  Collector   │  (OpenTelemetry) + SAMPLING (head/tail)
        └──────┬───────┘
                ▼
        ┌──────────────┐
        │Jaeger/Tempo  │  → UI (latency waterfall)
        └──────────────┘
Correlate with logs/metrics by trace id.
\`\`\``,tech:"hld",displayNumber:93,sortKey:96},{id:"hld-97",number:94,title:"Design an event-driven architecture.",question:"Design an event-driven architecture.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Components communicate by producing/consuming **events** via a broker (Kafka) rather than direct calls — loose coupling, scalability, real-time reactions. Producers emit domain events (past tense, immutable, versioned); consumers react + may emit their own. Patterns: notification, state transfer, event sourcing, CQRS. **Challenges**: eventual consistency, idempotent/order-tolerant consumers, schema versioning, observability across async hops.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Producer    │  emit domain event (OrderPlaced)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Broker (Kafka)│
   └──────┬───────┘
   ┌──────┼──────────┬───────────┐
   ▼      ▼          ▼           ▼
┌──────┐┌──────┐┌────────┐┌──────────┐
│Payment││Invent││Notify  ││Analytics │  independent consumers
│       ││ory   ││        ││          │  (react + may emit events)
└──────┘└──────┘└────────┘└──────────┘
Add consumers freely (loose coupling). Idempotent + order-tolerant. Trace async hops.
\`\`\``,tech:"hld",displayNumber:94,sortKey:97},{id:"hld-98",number:95,title:"Design a publish-subscribe system.",question:"Design a publish-subscribe system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Publishers send to **topics** without knowing subscribers; the broker delivers each message to **all** subscribers (fan-out). Core: topic registry, subscription management, delivery (push/pull) + acks/retries. Partition topics for parallelism, replicate for HA, buffer for slow subscribers. **Challenges**: delivery guarantees (at-least-once + idempotency), slow/failed subscribers (backpressure, DLQ), ordering, durable vs ephemeral subs.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Publisher   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Broker (topic,│  partitioned + replicated
   │ subscriptions)│
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Sub A ││Sub B ││Sub C     │  ALL receive (fan-out) + ack/retry
└──────┘└──────┘└──────────┘
Slow sub → backpressure/DLQ. (Kafka/Google Pub/Sub)
\`\`\``,tech:"hld",displayNumber:95,sortKey:98},{id:"hld-99",number:96,title:"Design a message broker.",question:"Design a message broker.",difficulty:"Hard",category:"Distributed Systems",answer:`Accepts messages, stores durably, delivers with guarantees. Core: **queues/topics**, durable storage (append log or persistent queue), **partitioning** (scale), **replication** (HA), consumer tracking (offsets/acks), routing (exchanges). Delivery semantics (at-least-once), retries, DLQ, backpressure. **Challenges**: durability vs throughput, ordering, exactly-once, failover. Decide log (streaming) vs queue (routing) model first.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Producers   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │   Broker     │  durable store (log or queue)
   │  ┌────────┐  │  partitioned + replicated (HA)
   │  │Partition│ │  routing, offsets/acks, DLQ, backpressure
   │  └────────┘  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Consumers   │  (offsets/acks, at-least-once)
   └──────────────┘
Decide model: log/streaming (Kafka) vs smart-routing queue (RabbitMQ).
\`\`\``,tech:"hld",displayNumber:96,sortKey:99},{id:"hld-100",number:97,title:"Design a Kafka-like system.",question:"Design a Kafka-like system.",difficulty:"Hard",category:"Distributed Systems",answer:`Topics as **partitioned append-only logs**; producers append to a partition (by key hash) on the **leader broker**; partitions **replicated** to followers (ISR); consumers **pull** by offset within **consumer groups** (one consumer/partition). Retain by time/size (replayable). Metadata/leadership via controller (Raft/KRaft). **Challenges**: ordering (per partition), replication + leader election, offsets, high-throughput sequential I/O + zero-copy, exactly-once.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Producer    │  hash(key) → partition
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Leader Broker │  append to partition log
   │  (partition) │
   └──────┬───────┘
     replicate to ISR
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Follower 1││Follower 2│  (ISR — durability)
└──────────┘└──────────┘
          ▲ pull by offset
   ┌──────┴───────┐
   │Consumer Group│  1 consumer / partition; retain → replay
   └──────────────┘
Controller (KRaft) = metadata/leaders. Ordering per partition.
\`\`\``,tech:"hld",displayNumber:97,sortKey:100},{id:"hld-101",number:98,title:"Design a RabbitMQ-like system.",question:"Design a RabbitMQ-like system.",difficulty:"Hard",category:"Distributed Systems",answer:`A **smart broker** with **exchanges** (direct/topic/fanout/headers) routing messages to bound **queues**; consumers subscribe + ack; unacked/failed → requeue or **DLQ**. Push to consumers (competing consumers per queue), delete on ack. Durability (persistent queues), clustering + mirrored queues (HA), flow control. **Challenges**: routing flexibility vs throughput, ordering, ack/redelivery correctness, HA.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Producer    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  EXCHANGE    │  (direct/topic/fanout/headers)
   └──────┬───────┘
     bindings route to queues
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Queue1││Queue2││ DLQ  │  (unacked → requeue/DLQ)
└──┬───┘└──┬───┘└──────┘
   ▼       ▼
┌──────┐┌──────┐
│Consumer│Consumer  push + delete-on-ack (competing)
└──────┘└──────┘
Rich routing vs Kafka's log/replay. HA: mirrored queues.
\`\`\``,tech:"hld",displayNumber:98,sortKey:101},{id:"hld-102",number:99,title:"Design a dead-letter queue system.",question:"Design a dead-letter queue system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Captures messages that repeatedly fail (after bounded retries) so they don't block the main queue/partition. On failure, increment an attempt counter; once exhausted, route the message + failure context (original topic, error, timestamp) to a **DLQ** for investigation. Provide a **replay** path after fixing the cause. **Challenges**: transient vs permanent failures, no infinite retries, alerting on DLQ growth, safe curated replay.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Consumer    │  process message
   └──────┬───────┘
     fail?
          ▼
   ┌──────────────┐
   │ retries left?│  (bounded, backoff)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ yes        ▼ no (exhausted)
 ┌──────────┐ ┌──────────────┐
 │ retry    │ │   DLQ        │  + error context (orig topic/offset/error)
 │          │ │ (main flow   │
 └──────────┘ │  unblocked)  │
              └──────┬───────┘
                     ▼
              ┌──────────────┐
              │Alert + Replay│  fix cause → dedup + rate-limit replay
              └──────────────┘
\`\`\``,tech:"hld",displayNumber:99,sortKey:102},{id:"hld-103",number:100,title:"Design a retry queue.",question:"Design a retry queue.",difficulty:"Intermediate",category:"Distributed Systems",answer:`For transient failures, republish to a **retry queue/topic** with a delay, then reprocess (non-blocking). **Tiered retry topics** with increasing delays (5s, 1m, 10m) so a failing message doesn't block the main partition; after exhausting tiers → DLQ. Track attempt count. **Challenges**: implementing delay, avoiding retry storms (backoff + jitter), idempotent processing (retries duplicate).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Main Topic   │
   └──────┬───────┘
     transient fail
          ▼
   ┌──────────────┐
   │ retry.5s     │  (delay, reprocess)
   └──────┬───────┘  fail
          ▼
   ┌──────────────┐
   │ retry.1m     │
   └──────┬───────┘  fail
          ▼
   ┌──────────────┐
   │ retry.10m    │
   └──────┬───────┘  exhausted
          ▼
   ┌──────────────┐
   │    DLQ       │
   └──────────────┘
Non-blocking (main keeps flowing). Idempotent processing; backoff + jitter.
\`\`\``,tech:"hld",displayNumber:100,sortKey:103},{id:"hld-104",number:101,title:"Design a delayed message system.",question:"Design a delayed message system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Deliver a message only after a delay / at a future time. Approaches: **delay queue** (SQS delay, RabbitMQ TTL + DLX), **scheduled store** (Redis sorted set keyed by delivery time, poller dispatches due), or a timing-wheel. Kafka (no native delay) → per-delay topics or external scheduler. Shard by time bucket. **Challenges**: timing accuracy, scale of pending, exactly-once dispatch, clock reliability.

\`\`\`text
── FLOW (Redis sorted-set approach, top → bottom) ──
   ┌──────────────┐
   │  Producer    │  ZADD delayed <deliverAt> <msg>
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Redis Sorted  │  score = deliverAt (sharded by time bucket)
   │Set           │
   └──────┬───────┘
     poller (every 1s)
          ▼
   ┌──────────────┐
   │  Poller      │  ZRANGEBYSCORE 0..now → dispatch → ZREM
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Downstream    │
   └──────────────┘
Alt: SQS delay / RabbitMQ TTL+DLX / timing wheel / per-delay Kafka topics.
\`\`\``,tech:"hld",displayNumber:101,sortKey:104},{id:"hld-105",number:102,title:"Design an exactly-once processing system.",question:"Design an exactly-once processing system.",difficulty:"Hard",category:"Distributed Systems",answer:`True end-to-end exactly-once is hard; achieve exactly-once **effects** via at-least-once delivery + **idempotency**. Within Kafka: **transactions** (idempotent producer + transactional offset commits + read_committed). For external side effects: **idempotency keys** + dedup store, or the **outbox pattern**. **Challenges**: no distributed magic — make operations idempotent or use transactions within one system; cross-system → dedup at the sink.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Message     │  (unique event id)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Consumer     │
   └──────┬───────┘
     seen event id?
          ▼
   ┌──────────────┐
   │ Dedup Store  │  (unique constraint)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ new        ▼ duplicate
 ┌──────────┐ ┌──────┐
 │process + │ │ skip │
 │record id │ │      │
 └──────────┘ └──────┘
Kafka-internal: transactions (idempotent producer + tx offsets + read_committed).
External: idempotency key / outbox. Exactly-once EFFECTS, not magic.
\`\`\``,tech:"hld",displayNumber:102,sortKey:105},{id:"hld-106",number:103,title:"Design an idempotent payment API.",question:"Design an idempotent payment API.",difficulty:"Hard",category:"Distributed Systems",answer:`Client sends a unique **Idempotency-Key** per attempt. Server: in a transaction, insert the key (unique constraint); if new → process + store result; if it exists → **return stored result** without charging again. Makes retries (timeouts, network) safe — no double charge. **Challenges**: atomic key-insert + charge, in-progress duplicates (lock/"processing"), key TTL, provider-side idempotency alignment.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Client      │  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment API   │  INSERT key (unique, transactional)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ new        ▼ exists
 ┌──────────┐ ┌──────────────┐
 │charge +  │ │return stored │  (no re-charge)
 │store     │ │result        │
 │result    │ └──────────────┘
 └──────────┘
In-progress dup → lock / "processing". Align with provider idempotency.
\`\`\``,tech:"hld",displayNumber:103,sortKey:106},{id:"hld-107",number:104,title:"Design an idempotent API gateway.",question:"Design an idempotent API gateway.",difficulty:"Hard",category:"Distributed Systems",answer:`The gateway deduplicates at the edge: for requests with an **Idempotency-Key**, check a shared store (Redis) — if seen, return the cached response; else forward to backend, then cache the response (with TTL). Protects non-idempotent backends from client retries centrally. **Challenges**: only cache idempotent-eligible methods, concurrent duplicates (lock), key scoping (per endpoint/user), TTL vs correctness.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Client      │  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ API Gateway  │  check Redis for key
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ cached     ▼ new
 ┌──────────┐ ┌──────────────┐
 │return    │ │forward → back│
 │cached    │ │end → cache   │
 │response  │ │response (TTL)│
 └──────────┘ └──────────────┘
Lock on concurrent dup. Scope key per endpoint/user.
\`\`\``,tech:"hld",displayNumber:104,sortKey:107},{id:"hld-108",number:105,title:"Design a distributed scheduler.",question:"Design a distributed scheduler.",difficulty:"Hard",category:"Distributed Systems",answer:`Schedule and dispatch jobs reliably across nodes at scale. Store jobs with \`next_run_at\` (indexed); scheduler instances **atomically claim** due jobs (\`SELECT ... FOR UPDATE SKIP LOCKED\` or distributed lock/leader) to avoid double-dispatch, then enqueue to workers. Track status + retries; handle missed runs. **Challenges**: exactly-once dispatch, HA (no scheduler SPOF), clock sync, hotspots at popular times.

\`\`\`text
── BACK-OF-ENVELOPE ──
Jobs:   10M scheduled; ~100k fire/min = ~1,700/sec; index on next_run_at

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Job DB     │  Job(next_run_at, status), index(next_run_at)
   └──────┬───────┘
     poll due (atomic claim)
          ▼
   ┌──────────────┐
   │ Schedulers   │  FOR UPDATE SKIP LOCKED (no double-dispatch)
   │ (N replicas) │
   └──────┬───────┘
     enqueue
          ▼
   ┌──────────────┐
   │    Queue     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │   Workers    │  execute → status; retries + missed-run catch-up
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:105,sortKey:108},{id:"hld-109",number:106,title:"Design a cron-job platform.",question:"Design a cron-job platform.",difficulty:"Hard",category:"Distributed Systems",answer:`Multi-tenant cron: users define cron expressions; the platform computes each job's next fire time; a distributed scheduler dispatches due jobs to isolated workers, tracking history + retries. Store \`{cron, next_run_at, owner}\`; recompute after each run. Atomic claim / leader election → no duplicate fires. **Challenges**: exactly-once firing, tenant isolation + quotas, missed-run policy, TZ/DST, observability, scale.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  User (cron  │  define "0 9 * * *"
   │  expression) │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Job Store   │  {cronExpr, next_run_at, owner}
   └──────┬───────┘
     scheduler claims due (atomic → no duplicate fire)
          ▼
   ┌──────────────┐
   │  Scheduler   │  after run → recompute next_run_at + record history
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Isolated      │  (tenant quotas / bulkheads)
   │Workers       │
   └──────────────┘
TZ/DST handling; missed-run catch-up vs skip.
\`\`\``,tech:"hld",displayNumber:106,sortKey:109},{id:"hld-110",number:107,title:"Design a workflow orchestration system.",question:"Design a workflow orchestration system.",difficulty:"Hard",category:"Distributed Systems",answer:`Coordinate multi-step, long-running workflows with dependencies, retries, compensation. A central **orchestrator** runs a workflow definition (DAG/state machine), persisting state after each step (resumable), invoking activities with retries + timeouts; compensation (Saga) on failure. Partition workflows, durable state store, worker pools. **Challenges**: durable state + exactly-once steps, long-running timers, versioning definitions, visibility. (Temporal, Step Functions.)

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Orchestrator │  runs DAG / state machine
   └──────┬───────┘
     persist state after each step (resumable)
          ▼
   ┌──────────────┐
   │  Step 1      │──► activity (retry/timeout)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Step 2      │──► activity
   └──────┬───────┘
     failure?
          ▼
   ┌──────────────┐
   │ Compensate   │  (Saga: undo prior steps)
   └──────────────┘
Durable state store; versioned definitions. (Temporal/Step Functions)
\`\`\``,tech:"hld",displayNumber:107,sortKey:110},{id:"hld-111",number:108,title:"Design a distributed task queue.",question:"Design a distributed task queue.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Producers enqueue tasks to a broker (Redis/RabbitMQ/Kafka/SQS); a pool of **workers** consume + process concurrently, scaling horizontally (competing consumers). Track status, retries + DLQ, priorities, delayed tasks. Partition/shard the queue; add workers up to partition count. **Challenges**: at-least-once + idempotent tasks, priorities, backpressure, visibility timeout (redeliver on crash), monitoring queue depth. (Celery/Sidekiq/BullMQ.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Producers   │  enqueue task
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Queue/Broker  │  (priorities, delayed, DLQ)
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Worker││Worker││Worker│  competing consumers (scale out)
└──────┘└──────┘└──────┘
Visibility timeout → redeliver if worker dies. Idempotent tasks. Monitor depth.
\`\`\``,tech:"hld",displayNumber:108,sortKey:111},{id:"hld-112",number:109,title:"Design a background-job processing system.",question:"Design a background-job processing system.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Offload slow/async work (emails, image processing, reports) from the request path: enqueue → workers process later → update status. Job persistence, retries + backoff, DLQ, scheduling/delays, progress tracking. Horizontal workers, priority queues, resource isolation (bulkheads per job type). **Challenges**: idempotency, long jobs (checkpointing), failure isolation, observability, graceful shutdown.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Request     │  enqueue job → respond NOW
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Job Queue   │  (per job type — bulkheads)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Workers     │  process async → update status
   └──────┬───────┘
     fail → retries/backoff → DLQ
          ▼
   ┌──────────────┐
   │Status + Progress│  checkpoint long jobs; graceful shutdown
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:109,sortKey:112},{id:"hld-113",number:110,title:"Design a webhook delivery system.",question:"Design a webhook delivery system.",difficulty:"Hard",category:"Distributed Systems",answer:`Deliver event notifications to customer HTTP endpoints reliably. On an event, enqueue a delivery job; workers POST the payload (HMAC-signed), expect 2xx, and on failure **retry with exponential backoff** up to N attempts, then DLQ + alert. Store attempts/status; support replay. Queue + worker pool, per-endpoint rate limiting. **Challenges**: unreliable endpoints (retries, circuit-break), ordering, at-least-once + idempotency (send event id), SSRF protection, delivery monitoring.

\`\`\`text
── BACK-OF-ENVELOPE ──
Events:  100M webhooks/day = ~1,160/sec; retries multiply attempts

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Event       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Delivery Queue│
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Workers     │  POST (HMAC-signed) → expect 2xx
   └──────┬───────┘
     fail → retry (exp backoff, N attempts)
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Customer      │        │DLQ + Alert   │  (exhausted)
   │Endpoint      │        └──────────────┘
   └──────────────┘
Send event id (customer dedup); per-endpoint rate limit; SSRF guard; replay.
\`\`\``,tech:"hld",displayNumber:110,sortKey:113},{id:"hld-114",number:111,title:"Design a webhook retry mechanism.",question:"Design a webhook retry mechanism.",difficulty:"Intermediate",category:"Distributed Systems",answer:`On failed delivery (non-2xx/timeout), schedule a retry with **exponential backoff + jitter** (1m, 5m, 30m, 2h, 6h) up to a max, via a delayed/scheduled queue. Track attempt count + next-attempt time. After exhausting retries → failed → DLQ + notify (offer manual replay). **Circuit-break** persistently-down endpoints. **Challenges**: delayed retries, idempotency (event id), retry storms, customer visibility/replay.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Delivery Fail│  (non-2xx / timeout)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Schedule Retry│  exp backoff + jitter: 1m → 5m → 30m → 2h → 6h
   │(delayed queue)│
   └──────┬───────┘
     exhausted?
          ▼
   ┌──────────────┐
   │Failed → DLQ  │  + notify + manual replay
   └──────────────┘
Circuit-break dead endpoints. Send event id → customer dedup.
\`\`\``,tech:"hld",displayNumber:111,sortKey:114},{id:"hld-115",number:112,title:"Design an API gateway for microservices.",question:"Design an API gateway for microservices.",difficulty:"Hard",category:"Distributed Systems",answer:`Single entry point: routing (via service discovery), **authN/authZ** (JWT), **rate limiting**, TLS, request/response transformation, aggregation, caching, observability. Deploy **horizontally scaled + HA** behind an LB (no SPOF). **Challenges**: keep it thin, latency overhead (one hop), bottleneck risk (scale it), consistent auth, versioning/routing, resilience (circuit breakers to backends).

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Load Balancer │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ API Gateway  │  authN/Z, rate limit, TLS, transform, aggregate, cache
   │ (scaled, HA) │  emits logs/traces/metrics
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│order ││user  ││payment   │  (via discovery; circuit-break)
│svc   ││svc   ││svc       │
└──────┘└──────┘└──────────┘
Keep thin (no business logic); version routes.
\`\`\``,tech:"hld",displayNumber:112,sortKey:115},{id:"hld-116",number:113,title:"Design a Backend-for-Frontend (BFF) architecture.",question:"Design a Backend-for-Frontend (BFF) architecture.",difficulty:"Intermediate",category:"Distributed Systems",answer:`A dedicated backend per client type (web, iOS, Android) that **aggregates and shapes** data for that frontend — trimming fields, batching calls, adapting to device — so clients make one tailored call. Each BFF owned by the frontend team. **Benefits**: optimized payloads, fewer round-trips, independent iteration. **Challenges**: duplicated aggregation across BFFs, keep BFFs thin, versioning per client.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────┐  ┌──────────┐
   │Mobile App│  │ Web App  │
   └────┬─────┘  └────┬─────┘
        ▼             ▼
   ┌──────────┐  ┌──────────┐
   │Mobile BFF│  │ Web BFF  │  (aggregate + shape per client)
   └────┬─────┘  └────┬─────┘
        └──────┬──────┘
               ▼
   ┌──────────────────────────┐
   │ user-svc, order-svc,     │
   │ promo-svc                │
   └──────────────────────────┘
One tailored call, trimmed payload. Each BFF owned by its frontend team.
\`\`\``,tech:"hld",displayNumber:113,sortKey:116},{id:"hld-117",number:114,title:"Design a service mesh architecture.",question:"Design a service mesh architecture.",difficulty:"Hard",category:"Distributed Systems",answer:`Handles service-to-service comms via **sidecar proxies** (Envoy) next to each service, moving cross-cutting concerns — mTLS, retries, timeouts, circuit breaking, load balancing, canary, telemetry — out of app code into the platform, configured declaratively. A **control plane** configures the **data plane** (sidecars). **Benefits**: consistent, language-agnostic resilience/security/observability. **Challenges**: complexity, per-hop latency, ops overhead. (Istio/Linkerd.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Control Plane │  (Istio) — configures sidecars declaratively
   └──────┬───────┘
     push config
   ┌──────┴─────────────┐
   ▼                    ▼
┌──────────────┐  ┌──────────────┐
│ Service A    │  │ Service B    │
│ ┌──────────┐ │  │ ┌──────────┐ │
│ │Envoy     │◄┼──┼►│Envoy     │ │  sidecars: mTLS, retries, circuit
│ │sidecar   │ │  │ │sidecar   │ │  breaking, LB, telemetry
│ └──────────┘ │  │ └──────────┘ │
└──────────────┘  └──────────────┘
No app code changes. Cost: complexity + per-hop latency.
\`\`\``,tech:"hld",displayNumber:114,sortKey:117},{id:"hld-118",number:115,title:"Design a circuit-breaker service.",question:"Design a circuit-breaker service.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Wrap calls with a state machine tracking failures: **Closed** (calls pass, count failures/slow calls in a window), **Open** (threshold crossed → short-circuit to fallback for a cooldown), **Half-Open** (a few trial calls; success → Closed, failure → Open). Emit metrics, provide fallbacks. **Challenges**: threshold/window tuning, per-dependency isolation, distributed vs per-instance state, combining with retries/bulkheads. (Resilience4j/Hystrix.)

\`\`\`text
── STATE MACHINE (flow) ──
   ┌──────────────┐
   │   CLOSED     │  calls pass; count failures in window
   └──────┬───────┘
     failure rate > threshold
          ▼
   ┌──────────────┐
   │    OPEN      │  fail fast → fallback (cooldown)
   └──────┬───────┘
     after cooldown
          ▼
   ┌──────────────┐
   │  HALF-OPEN   │  allow trial calls
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ success    ▼ fail
 ┌──────┐    ┌──────┐
 │CLOSED│    │ OPEN │
 └──────┘    └──────┘
Per-dependency breaker + fallback + metrics.
\`\`\``,tech:"hld",displayNumber:115,sortKey:118},{id:"hld-119",number:116,title:"Design a distributed configuration cache.",question:"Design a distributed configuration cache.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Cache config locally in each service instance for **low-latency reads**, synced with a central store via **watch/subscribe** (push) or short-TTL polling. On change, the store notifies subscribers, which update their local cache atomically. **Benefits**: no per-read network call, resilience (serve last-known if store down). **Challenges**: propagation speed/consistency, versioning (atomic apply), validation, store outages (stale-but-available).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Central Config│  (source of truth)
   │Store         │
   └──────┬───────┘
     push / watch changes
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Inst A││Inst B││Inst C│  each: LOCAL cache (atomic swap on change)
│cache ││cache ││cache │
└──────┘└──────┘└──────┘
Reads hit local cache (fast). Store down → serve last-known (resilient).
\`\`\``,tech:"hld",displayNumber:116,sortKey:119},{id:"hld-120",number:117,title:"Design a global traffic routing system.",question:"Design a global traffic routing system.",difficulty:"Hard",category:"Distributed Systems",answer:`Route users to the best region using **GeoDNS** or **anycast** (nearest by topology), with **health-aware** routing (fail away from unhealthy regions) and **weighted** routing (canary, migration). A global LB (Route 53 + accelerators) considers latency, geography, health. **Challenges**: failover speed (DNS TTL vs anycast), latency-routing accuracy, data locality/consistency, sticky routing, DDoS absorption.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │   User       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │GeoDNS/Anycast│  latency + geo + health + weights
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Region││Region││Region│  (unhealthy → traffic fails away)
│ US   ││ EU   ││ APAC │
└──────┘└──────┘└──────┘
Weighted routing for canary/migration. DDoS absorbed at edge.
\`\`\``,tech:"hld",displayNumber:117,sortKey:120},{id:"hld-121",number:118,title:"Design a multi-region application.",question:"Design a multi-region application.",difficulty:"Hard",category:"Distributed Systems",answer:`Deploy the full stack in multiple regions for low latency + resilience; route users to the nearest healthy region. The hard part is **data**: active-passive (one write region + async replication) or active-active (multi-region writes + conflict resolution); per-dataset consistency (strong for money, eventual for feeds). Replicate DBs cross-region; regional caches. **Challenges**: replication lag, cross-region consistency/conflicts, failover (RPO/RTO), data residency, cost.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  → nearest healthy region
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Region US ││Region EU │  full stack each (services + cache + DB)
│ ┌──────┐ ││ ┌──────┐ │
│ │ DB   │◄┼┼►│ DB   │ │  cross-region replication
│ └──────┘ ││ └──────┘ │
└──────────┘└──────────┘
Data: active-passive OR active-active. Per-dataset consistency (money=strong).
\`\`\``,tech:"hld",displayNumber:118,sortKey:121},{id:"hld-122",number:119,title:"Design an active-active architecture.",question:"Design an active-active architecture.",difficulty:"Hard",category:"Distributed Systems",answer:`All regions serve **reads and writes** simultaneously — max availability, low latency (local writes), no idle capacity. Needs **multi-master data**: conflict resolution (LWW, CRDTs, app-merge), per-region write acceptance, async cross-region replication. Best for eventually-consistent data; for money, pin each account to a home region. **Challenges**: write conflicts, replication lag, global invariants, split-brain.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  users → nearest region (both R + W)
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐   ┌──────────┐
│Region A  │◄─►│Region B  │  BOTH accept writes (multi-master)
│(R + W)   │   │(R + W)   │  async replication + conflict resolution
└──────────┘   └──────────┘
Conflicts: LWW / CRDT / app-merge. Money → pin account to home region.
\`\`\``,tech:"hld",displayNumber:119,sortKey:122},{id:"hld-123",number:120,title:"Design an active-passive architecture.",question:"Design an active-passive architecture.",difficulty:"Intermediate",category:"Distributed Systems",answer:`One **active** (primary) region serves all traffic; a **passive** (standby) region replicates + waits to take over. Simpler than active-active (single writer → no conflicts), but idle standby + failover downtime (RPO = replication lag; RTO = failover time). Warm standby for faster failover. **Challenges**: failover automation + testing, replication lag (RPO), cutover speed (RTO), split-brain avoidance.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  → active region
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Active Region │  serves ALL traffic (single writer)
   │ (primary)    │
   └──────┬───────┘
     async replicate
          ▼
   ┌──────────────┐
   │Passive Region│  standby (replicates, waits)
   │ (warm)       │
   └──────────────┘
Failover → promote passive. RPO = lag; RTO = failover time. Test DR!
\`\`\``,tech:"hld",displayNumber:120,sortKey:123},{id:"hld-124",number:121,title:"Design database failover.",question:"Design database failover.",difficulty:"Hard",category:"Distributed Systems",answer:`Detect primary failure (health checks), then **promote a replica** to primary and repoint clients (via proxy/DNS/discovery, not hardcoded IPs). Semi-sync replication to a standby minimizes data loss. Automate with a manager (Patroni, RDS Multi-AZ, Orchestrator) that handles election + prevents **split-brain** (fence the old primary). **Challenges**: split-brain, async data loss, fast repointing, tested automation.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Health Monitor│  (Patroni/RDS) detects primary failure
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │FENCE old     │  (prevent split-brain)
   │primary       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Promote replica│  → new primary
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Repoint clients│  (proxy/DNS/discovery, NOT hardcoded IPs)
   └──────────────┘
Semi-sync replica minimizes data loss.
\`\`\``,tech:"hld",displayNumber:121,sortKey:124},{id:"hld-125",number:122,title:"Design database replication.",question:"Design database replication.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Copy data from a primary to replicas for HA + read scaling. **Synchronous**: wait for replica ack (no loss, higher latency); **asynchronous**: return immediately (fast, may lag/lose); **semi-sync**: wait for ≥1 replica (balance). WAL/binlog shipping. Route writes→primary, reads→replicas. **Challenges**: replication lag (stale reads, read-your-writes), failover, multi-master conflicts, lag monitoring.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Writes      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │   PRIMARY    │  ships WAL/binlog
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Replica 1 ││Replica 2 │  serve READS
└──────────┘└──────────┘
Sync (no loss, slow) | async (fast, lag) | semi-sync (balance). Monitor lag.
\`\`\``,tech:"hld",displayNumber:122,sortKey:125},{id:"hld-126",number:123,title:"Design read replicas.",question:"Design read replicas.",difficulty:"Intermediate",category:"Distributed Systems",answer:`Add replica DBs that asynchronously copy the primary and serve **read-only** traffic, scaling reads + offloading analytics/backups. A routing layer sends writes→primary, reads→replicas (load-balanced). Handle **replication lag**: for read-your-writes, route a user's reads to primary briefly after a write. **Challenges**: staleness, routing logic, lag monitoring, rebalancing read load.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Router/Proxy  │  writes → primary ; reads → replicas
   └──────┬───────┘
   ┌──────┴──────┐
   ▼ writes      ▼ reads (load-balanced)
┌──────────┐┌──────────────┐
│ PRIMARY  ││Replica 1..N  │  (+ analytics/backups)
└──────────┘└──────────────┘
Read-your-writes: pin user to primary briefly post-write. Monitor lag.
\`\`\``,tech:"hld",displayNumber:123,sortKey:126},{id:"hld-127",number:124,title:"Design database sharding.",question:"Design database sharding.",difficulty:"Hard",category:"Distributed Systems",answer:`Partition data across independent DBs by a **shard key** to scale writes/storage. Strategies: **hash** (even spread), **range** (range queries, hotspot risk), **directory** (flexible lookup), **consistent hashing** (minimal reshuffle). A router maps keys→shards; each shard replicated. **Challenges**: high-cardinality/even-access shard key; **hot shards**; cross-shard queries/joins/txns; resharding/rebalancing.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Shard Router  │  shard = f(shard_key)
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Shard1││Shard2││Shard3│  each replicated (HA)
│(A-H) ││(I-P) ││(Q-Z) │
└──────┘└──────┘└──────┘
Strategy: hash | range | directory | consistent hash. Avoid cross-shard JOIN/txn.
\`\`\``,tech:"hld",displayNumber:124,sortKey:127},{id:"hld-128",number:125,title:"Design consistent hashing.",question:"Design consistent hashing.",difficulty:"Hard",category:"Distributed Systems",answer:`Map both nodes and keys onto a hash ring; a key is owned by the first node **clockwise** from its hash. Adding/removing a node only remaps keys between it and its neighbor (≈1/N), vs \`hash % N\` which remaps almost everything. **Virtual nodes** (many ring positions per physical node) give even distribution + smooth rebalancing. Used in caches, sharded DBs, LBs. **Challenges**: even distribution (vnodes), replication (next K nodes), churn.

\`\`\`text
── HASH RING (clockwise ownership) ──
              0/360°
          ┌─────────┐
     NodeC│    •keyX│ → owned by first node clockwise (NodeA)
          │ •NodeA  │
   270° ──┤         ├── 90°
          │  NodeB• │
          │•keyY    │
          └─────────┘
             180°
Add/remove node → only ~1/N keys move (vs hash%N remaps all).
Virtual nodes → even spread. Replicas = next K distinct nodes.
\`\`\``,tech:"hld",displayNumber:125,sortKey:128},{id:"hld-129",number:126,title:"Design a shard rebalancing system.",question:"Design a shard rebalancing system.",difficulty:"Hard",category:"Distributed Systems",answer:`When shards become uneven (hot shards) or capacity changes, redistribute data with **minimal movement + no downtime**. Use consistent hashing (or shard-map) so only affected ranges move; migrate in background (copy → dual-write/verify → cut over routing → delete old), throttled. **Challenges**: online migration, consistency during move (dual-write), avoiding rebalance hotspots, atomic routing update.

\`\`\`text
── MIGRATION FLOW (top → bottom) ──
   ┌──────────────┐
   │Detect hot/   │  imbalance → pick key ranges to move
   │imbalanced    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Copy data     │  (background, throttled)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Dual-write +  │  keep old + new consistent
   │verify        │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Switch routing│  (atomic) → drop old
   └──────────────┘
No downtime; consistency during move.
\`\`\``,tech:"hld",displayNumber:126,sortKey:129},{id:"hld-130",number:127,title:"Design a distributed counter.",question:"Design a distributed counter.",difficulty:"Hard",category:"Distributed Systems",answer:`Counting at scale (likes, views) with a single row is a write hotspot. Solutions: **sharded counters** (N sub-counters, increment a random shard, sum on read), **Redis INCR** (atomic, shard by key), or **approximate** (HyperLogLog for uniques, or Kafka-buffered increments flushed in batches). Trade exactness/latency for throughput. **Challenges**: write contention (sharding), read cost (sum shards / cached total), eventual consistency, hot keys.

\`\`\`text
── BACK-OF-ENVELOPE ──
Hot item:  100k likes/min = ~1,700/sec on ONE counter → shard into N sub-counters

── ARCHITECTURE (sharded counter) ──
   ┌──────────────┐
   │  Increment   │  → random of N shards
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Shard0││Shard1││ShardN│  (Redis INCR — no single-row contention)
└──────┘└──────┘└──────┘
   read = SUM(shards)  (or cached total)
Approximate: HyperLogLog (uniques) / Kafka-buffered batched flush.
\`\`\``,tech:"hld",displayNumber:127,sortKey:130},{id:"hld-131",number:128,title:"Design a distributed sequence generator.",question:"Design a distributed sequence generator.",difficulty:"Hard",category:"Distributed Systems",answer:`Generate monotonic/unique sequences without a single-row bottleneck. Options: **range allocation** (each node reserves a block from a central allocator, serves locally, refills when low), **Snowflake** (time-ordered, decentralized), or **DB sequence** (simple, bottleneck at scale). Strict global monotonicity needs coordination (bottleneck); for scale relax to "unique + roughly ordered." **Challenges**: contention vs strict ordering, gaps (range blocks), coordinator HA.

\`\`\`text
── RANGE ALLOCATOR (flow) ──
   ┌──────────────┐
   │Central       │  hands out blocks
   │Allocator     │
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Node A    ││Node B    │
│block     ││block     │  serve ids locally (refill when low)
│[1000-1999]││[2000-2999]│
└──────────┘└──────────┘
Rare coordination. Strict global monotonic → needs coordination (bottleneck).
Alt: Snowflake (time-ordered, decentralized). Blocks leave gaps.
\`\`\``,tech:"hld",displayNumber:128,sortKey:131},{id:"hld-132",number:129,title:"Design a distributed semaphore.",question:"Design a distributed semaphore.",difficulty:"Hard",category:"Distributed Systems",answer:`Limit concurrent access to **N permits** across a cluster (e.g. max 10 workers hitting an API). Maintain a permit count in a coordination store: acquire = atomically decrement if >0 (with a **TTL/lease** so a crashed holder auto-releases); release = increment. Redis (Lua) or ZooKeeper/etcd. **Challenges**: atomicity, leaked permits (leases + reconciliation), fairness/queuing, thundering herd on release.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Worker      │  acquire permit
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Permit Store  │  atomic: if count>0 → count-- + lease TTL
   │(count = N)   │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ got permit ▼ none
 ┌──────────┐ ┌──────┐
 │do work → │ │ wait │
 │release   │ │/queue│
 │(count++) │ └──────┘
 └──────────┘
Crash → lease expires → permit reclaimed. (Redis Lua / etcd)
\`\`\``,tech:"hld",displayNumber:129,sortKey:132},{id:"hld-133",number:130,title:"Design a distributed lease system.",question:"Design a distributed lease system.",difficulty:"Hard",category:"Distributed Systems",answer:`A lease grants time-bounded ownership of a resource (lock, leadership, partition) that **auto-expires** unless renewed via heartbeat — so a crashed holder's lease lapses and others take over. Store \`{resource, owner, expiry}\` in a consistent store (etcd/ZooKeeper); owner renews before expiry; on expiry, resource is free. Pair with **fencing tokens** so a paused holder can't act after its lease lapsed. **Challenges**: clock skew, renewal timing, split-brain (fencing).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Client A    │  acquire lease {resource, A, expiry}
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Lease Store   │  (etcd/ZooKeeper)
   └──────┬───────┘
     A renews via heartbeat before expiry
          ▼
   ┌──────────────┐
   │ A crashes    │  → no renewal → lease expires
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Client B      │  acquires freed lease (+ fencing token)
   └──────────────┘
Fencing token → stale holder can't act after lease loss.
\`\`\``,tech:"hld",displayNumber:130,sortKey:133},{id:"hld-134",number:131,title:"Design a scalable relational database architecture.",question:"Design a scalable relational database architecture.",difficulty:"Hard",category:"Database & Storage",answer:`Start with a primary + **read replicas** (scale reads, HA), add **caching** (Redis) for hot reads, then **partition/shard** by a good key when writes/storage exceed one node. Use connection pooling, indexing, and a proxy for read/write splitting. For further scale, denormalize hot paths and offload analytics to a warehouse. **Challenges**: write scaling (sharding → lose cross-shard joins/txn), replication lag, hot shards, schema migrations at scale. Keep strong consistency where it matters.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   App        │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Proxy (R/W    │  writes → primary ; reads → replicas
   │ split)       │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────┐
 │Redis ││PRIMARY ││Read      │
 │Cache ││(writes)││Replicas  │
 └──────┘└───┬────┘└──────────┘
     shard when writes/storage exceed one node
             ▼
      ┌──────────────┐
      │Shard1..N     │  (lose cross-shard joins/txn)
      └──────────────┘
\`\`\``,tech:"hld",displayNumber:131,sortKey:134},{id:"hld-135",number:132,title:"Design a NoSQL database architecture.",question:"Design a NoSQL database architecture.",difficulty:"Hard",category:"Database & Storage",answer:`Choose a model by access pattern: **key-value** (fast lookups), **document** (flexible aggregates), **wide-column** (huge write scale, Cassandra), **graph** (relationships). Partition via **consistent hashing**, **replicate** each partition (quorum R/W for tunable consistency), design the schema around queries (denormalize, no joins). Add nodes → ring rebalances. **Challenges**: eventual consistency (tune with quorums), no joins, hot partitions, model/partition-key choice.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │  key → consistent hash
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Node A││Node B││Node C│  each partition replicated (quorum R/W)
│      ││      ││      │  R + W > N → strong; else eventual
└──────┘└──────┘└──────┘
Model: key-value | document | wide-column | graph. Schema = query-driven (denormalize).
Add nodes → ring rebalances. Watch hot partitions.
\`\`\``,tech:"hld",displayNumber:132,sortKey:135},{id:"hld-136",number:133,title:"Design a time-series database.",question:"Design a time-series database.",difficulty:"Hard",category:"Database & Storage",answer:`Optimized for append-heavy, time-ordered data (metrics, IoT). Store points \`(series, timestamp, value)\`; partition by **time windows** (+ series), keep time-sorted for fast range scans, **downsample** old data with tiered retention (hot/cold). Compress heavily (delta/columnar). **Challenges**: extreme write throughput (LSM), high cardinality, efficient time-range + aggregation queries, retention/downsampling. (InfluxDB/TimescaleDB/Prometheus.)

\`\`\`text
── BACK-OF-ENVELOPE ──
Ingest:  1M metrics/sec × 20 B (delta-compressed) → ~1.7 TB/day → downsample + tier

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Metrics/IoT  │  point(series, ts, value)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Ingest (LSM,  │  append-optimized, time-sorted
   │ append)      │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ recent     ▼ old
 ┌──────────┐┌──────────────┐
 │Hot (fine ││Cold (downsam-│
 │ granular)││ pled rollups)│
 └──────────┘└──────────────┘
Partition by time window (+ series). Heavy compression. Watch cardinality.
\`\`\``,tech:"hld",displayNumber:133,sortKey:136},{id:"hld-137",number:134,title:"Design a key-value store.",question:"Design a key-value store.",difficulty:"Hard",category:"Database & Storage",answer:`Map keys to values with O(1) get/put. **Partition** via consistent hashing, **replicate** to N nodes (quorum R/W), store on disk with an **LSM-tree** (write-optimized) or B-tree + memtable + WAL. Gossip for membership. **Challenges**: partitioning + rebalancing, replication + consistency (quorum/vector clocks), durability (WAL), compaction (LSM), hot keys. (DynamoDB/Cassandra.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  key → consistent hash
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Node  ││Node  ││Node  │  N replicas each (quorum R/W)
│      ││      ││      │  conflicts: vector clocks / LWW
└───┬──┘└──────┘└──────┘
    ▼ per node storage
 ┌──────────────┐
 │Memtable → WAL│  → LSM-tree (SSTables) + compaction
 └──────────────┘
Gossip membership. Watch: rebalancing, compaction, hot keys.
\`\`\``,tech:"hld",displayNumber:134,sortKey:137},{id:"hld-138",number:135,title:"Design a document database.",question:"Design a document database.",difficulty:"Intermediate",category:"Database & Storage",answer:`Store schema-flexible **documents** (JSON/BSON) keyed by id, grouped in collections, with **secondary indexes**. **Shard** by a shard key (hash/range) across nodes, **replicate** each shard (replica set) for HA. Documents store aggregates together (fewer joins). **Challenges**: shard-key choice (hot shards), index vs write cost, limited multi-doc transactions, unindexed-field query performance. (MongoDB.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  query by id / indexed field
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Router (mongos)│  shard by key
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Shard1││Shard2││Shard3│  each = replica set (HA)
│(JSON ││      ││      │  + secondary indexes
│docs) ││      ││      │
└──────┘└──────┘└──────┘
Aggregates stored together → fewer joins. Index fields you query.
\`\`\``,tech:"hld",displayNumber:135,sortKey:138},{id:"hld-139",number:136,title:"Design a distributed database.",question:"Design a distributed database.",difficulty:"Hard",category:"Database & Storage",answer:`Combine **partitioning** (shard for scale) + **replication** (copies per shard, HA) + a **consistency protocol** (quorum or consensus like Raft/Paxos) + a **query router** that locates data and runs distributed queries/txns. Decide CAP per workload. **Challenges**: distributed transactions (2PC/Saga), cross-shard queries, consistency vs availability, rebalancing, clock coordination. (Spanner/CockroachDB = distributed ACID via consensus + synced clocks.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Query Router  │  locate data + distributed query/txn
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Shard1││Shard2││Shard3│  each replicated + consensus (Raft)
│ ┌──┐ ││ ┌──┐ ││ ┌──┐ │
│ │R1│ ││ │R1│ ││ │R1│ │  R1/R2/R3 agree via Raft/Paxos
│ │R2│ ││ │R2│ ││ │R2│ │
│ └──┘ ││ └──┘ ││ └──┘ │
└──────┘└──────┘└──────┘
Distributed txn: 2PC/Saga or consensus. CAP choice per workload.
\`\`\``,tech:"hld",displayNumber:136,sortKey:139},{id:"hld-140",number:137,title:"Design a search system using Elasticsearch.",question:"Design a search system using Elasticsearch.",difficulty:"Hard",category:"Database & Storage",answer:`Ingest documents → **inverted index** (term → doc list) sharded + replicated. Queries fan out to shards, each returns top matches (BM25), results merged/ranked. Keep the source of truth in a primary DB and **sync** to the index async (Kafka/CDC). Shard by index; replicas for query throughput. **Challenges**: index/DB sync (eventual consistency), relevance tuning, analyzers, aggregations/facets, reindexing.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Primary DB   │  (source of truth)
   └──────┬───────┘
     async sync (Kafka/CDC)
          ▼
   ┌──────────────┐
   │ Indexer      │  build inverted index (term → docs)
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Shard1││Shard2││Shard3│  each + replicas (query throughput)
└──────┘└──────┘└──────┘
          ▲ query fans out → merge/rank (BM25)
   ┌──────┴───────┐
   │  Search API  │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:137,sortKey:140},{id:"hld-141",number:138,title:"Design an autocomplete system.",question:"Design an autocomplete system.",difficulty:"Hard",category:"Database & Storage",answer:`Return top suggestions for a prefix with very low latency. Build a **trie** where each node stores the top-K completions (precomputed by popularity), or a completion suggester. Serve from memory/cache; update popularity async from query logs. Shard the trie by prefix; edge-cache hot prefixes. **Challenges**: sub-100ms latency, ranking (frequency + recency + personalization), typo tolerance, streaming updates, multi-language.

\`\`\`text
── BACK-OF-ENVELOPE ──
Latency:  <100ms per keystroke → in-memory trie + edge cache
Updates:  popularity from query-log stream (async, not per-request)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  types prefix "lap"
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Edge Cache    │  (hot prefixes)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Trie Service  │  node "lap" → top-K [laptop, lapland...]
   │(in-memory,   │
   │ sharded by   │
   │ prefix)      │
   └──────┬───────┘
          ▲ update popularity
   ┌──────┴───────┐
   │Query-Log     │  (async stream)
   │Stream        │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:138,sortKey:141},{id:"hld-142",number:139,title:"Design a full-text search system.",question:"Design a full-text search system.",difficulty:"Hard",category:"Database & Storage",answer:`Tokenize/normalize text (lowercase, stemming, stop-words) → **inverted index** (term → postings with positions) → query by looking up terms, intersecting/scoring (BM25), supporting phrase/boolean/fuzzy. Sharded + replicated. Index async from a DB. **Challenges**: relevance ranking, analyzers/language, index size, near-real-time indexing, source-DB consistency.

\`\`\`text
── PIPELINE (top → bottom) ──
   ┌──────────────┐
   │  Document    │  "The quick brown fox"
   └──────┬───────┘
     tokenize + normalize (stem, stopwords)
          ▼
   ┌──────────────┐
   │Inverted Index│  quick → [doc1@2, doc7@5]
   │(term→postings│  brown → [doc1@3, doc3@1]
   │ + positions) │
   └──────┬───────┘
     query → lookup terms → intersect/score (BM25)
          ▼
   ┌──────────────┐
   │Ranked Results│  phrase / boolean / fuzzy
   └──────────────┘
Sharded + replicated. Index async from DB. NRT indexing.
\`\`\``,tech:"hld",displayNumber:139,sortKey:142},{id:"hld-143",number:140,title:"Design a product search engine (e-commerce).",question:"Design a product search engine (e-commerce).",difficulty:"Hard",category:"Database & Storage",answer:`Combine full-text search (Elasticsearch) + **faceted filtering** (category/price/brand via aggregations) + **ranking** (relevance + business signals: popularity, margin, availability, personalization) + **autocomplete**. Index products async from the catalog DB (Kafka/CDC). Handle typos (fuzzy), synonyms, out-of-stock demotion. Shard, cache facets/queries, precompute rankings. **Challenges**: relevance + business rules, facet performance, freshness, personalization, query understanding.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Catalog DB   │
   └──────┬───────┘
     async index (Kafka/CDC)
          ▼
   ┌──────────────┐
   │Elasticsearch │  inverted index + facet aggregations
   └──────┬───────┘
          ▲ query
   ┌──────┴───────┐
   │Search Service│  relevance + BUSINESS signals (popularity/
   │              │  margin/availability/personalization)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Results:      │  ranked + facets (category/price/brand)
   │ + autocomplete│  typo/synonym; demote out-of-stock
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:140,sortKey:143},{id:"hld-144",number:141,title:"Design a recommendation storage system.",question:"Design a recommendation storage system.",difficulty:"Hard",category:"Database & Storage",answer:`Separate offline **computation** (batch/streaming ML pipeline → recs per user/item) from online **serving** (fast lookup). Store precomputed recs in a low-latency KV store (\`user_id → [item ids]\`, Redis/DynamoDB) for O(1) reads; features/embeddings in a feature store; optional real-time re-rank. Shard by user; recompute periodically + incremental stream updates. **Challenges**: freshness (batch lag), huge user×item storage, cold start, serving latency.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ML Pipeline   │  (batch/stream) → recs per user
   │(OFFLINE)     │
   └──────┬───────┘
     precompute
          ▼
   ┌──────────────┐
   │KV Store      │  user_id → [item ids]  (O(1) read)
   │(Redis/DynamoDB│  sharded by user
   │, ONLINE)     │
   └──────┬───────┘
          ▲ serve
   ┌──────┴───────┐
   │Serving + real│  optional real-time re-rank (feature store)
   │-time re-rank │
   └──────────────┘
Recompute periodically + incremental stream updates.
\`\`\``,tech:"hld",displayNumber:141,sortKey:144},{id:"hld-145",number:142,title:"Design a database backup system.",question:"Design a database backup system.",difficulty:"Intermediate",category:"Database & Storage",answer:`Combine **full backups** (periodic snapshots) + **incremental/differential** + continuous **WAL/binlog archiving** for point-in-time recovery (PITR). Store in durable, versioned object storage (S3), encrypted, cross-region. Automate + **verify (test restores!)** + retention. **Challenges**: minimal performance impact (snapshot from a replica), transaction-consistent snapshots, verification, retention/cost, RPO via WAL frequency.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Replica     │  (backup from replica → low impact)
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────┐
 │Full  ││Increm- ││WAL/binlog│
 │snap- ││ental   ││archive   │  (continuous → PITR)
 │shot  ││        ││          │
 └───┬──┘└───┬────┘└────┬─────┘
     └───────┼──────────┘
             ▼
      ┌──────────────┐
      │S3 (versioned,│  encrypted, cross-region
      │ tiered)      │
      └──────┬───────┘
             ▼
      ┌──────────────┐
      │VERIFY (test  │  a backup you can't restore is useless!
      │ restores)    │
      └──────────────┘
RPO = WAL archive frequency.
\`\`\``,tech:"hld",displayNumber:142,sortKey:145},{id:"hld-146",number:143,title:"Design a database restore system.",question:"Design a database restore system.",difficulty:"Intermediate",category:"Database & Storage",answer:`Restore = load latest **full backup** → apply **incrementals** → replay **WAL/binlog** to a target time (PITR). Provision a new instance, restore, validate, repoint clients. Support restoring to a timestamp (before an accidental delete). **Challenges**: **RTO** (restore speed — parallelize, snapshots), correctness/validation, PITR precision, restoring at scale, testing restores regularly.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Full Backup   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Apply         │  incrementals
   │Incrementals  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Replay WAL    │  → target timestamp (PITR, "before bad delete")
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Validate →    │  repoint clients
   │Repoint       │
   └──────────────┘
RTO = restore speed (parallelize, snapshots). Test restores regularly!
\`\`\``,tech:"hld",displayNumber:143,sortKey:146},{id:"hld-147",number:144,title:"Design a database migration architecture.",question:"Design a database migration architecture.",difficulty:"Hard",category:"Database & Storage",answer:`Migrate between databases/schemas/systems with minimal risk. Phases: **dual-write** or **CDC replication** source→target, **backfill** history, **verify** (counts/checksums), then **cut over** reads then writes gradually, keeping a **rollback** path. Versioned schema tools (Flyway/Liquibase). **Challenges**: zero-downtime cutover, source/target consistency (CDC lag), validation, rollback safety, schema differences.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Source DB    │
   └──────┬───────┘
     backfill history + CDC/dual-write (keep in sync)
          ▼
   ┌──────────────┐
   │ Target DB    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Verify (counts│  / checksums)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Shift reads → │  then shift writes (gradual) → decommission source
   └──────────────┘
Keep rollback path. Schema: Flyway/Liquibase.
\`\`\``,tech:"hld",displayNumber:144,sortKey:147},{id:"hld-148",number:145,title:"Design a zero-downtime database migration.",question:"Design a zero-downtime database migration.",difficulty:"Hard",category:"Database & Storage",answer:`Keep the app running via **expand-migrate-contract**: (1) **expand** — add new schema/column alongside old (backward compatible); (2) **dual-write** + backfill + verify; (3) **migrate reads** to new; (4) **contract** — remove old once unused. For system moves, CDC replication + gradual cutover. **Challenges**: backward-compatible changes, dual-write consistency, backfill without locking (batched), verification, reversible steps.

\`\`\`text
── EXPAND-MIGRATE-CONTRACT (top → bottom) ──
   ┌──────────────┐
   │1. EXPAND     │  add new col/schema (backward-compatible)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │2. DUAL-WRITE │  write old + new; backfill old data (batched); verify
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │3. MIGRATE    │  switch reads to new
   │   READS      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │4. CONTRACT   │  drop old (once nothing uses it)
   └──────────────┘
App never stops. Every step reversible.
\`\`\``,tech:"hld",displayNumber:145,sortKey:148},{id:"hld-149",number:146,title:"Design an online schema migration.",question:"Design an online schema migration.",difficulty:"Hard",category:"Database & Storage",answer:`Alter large tables without locking/downtime. Naive \`ALTER TABLE\` locks; tools (gh-ost, pt-online-schema-change) create a **shadow copy** with the new schema, **copy rows in batches**, capture live changes (triggers/binlog), then atomically **swap** tables. Or expand-contract with additive changes. **Challenges**: no long locks (batched copy), sync during copy, atomic swap, FK/replication interactions, rollback.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Create Shadow │  table with NEW schema
   │Table         │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Copy rows in  │  + capture live changes (triggers/binlog)
   │batches       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Atomic RENAME │  swap shadow ↔ original
   │swap          │
   └──────────────┘
Tools: gh-ost, pt-online-schema-change. No long locks.
\`\`\``,tech:"hld",displayNumber:146,sortKey:149},{id:"hld-150",number:147,title:"Design data archiving.",question:"Design data archiving.",difficulty:"Intermediate",category:"Database & Storage",answer:`Move old, rarely-accessed data out of the hot operational DB into cheaper **cold storage** (warehouse/object storage) to keep the primary lean. A scheduled job selects data past a retention/age threshold, copies to the archive (time-partitioned), verifies, then deletes from the primary. Keep a query/restore path. **Challenges**: archive criteria, queryability, referential integrity, verify-before-delete, compliance.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Hot Primary DB│
   └──────┬───────┘
     scheduled job: select data older than threshold
          ▼
   ┌──────────────┐
   │Copy to Cold  │  (S3/warehouse, time-partitioned)
   │Store         │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Verify → Delete│  from primary (keeps hot DB lean)
   │from primary  │
   └──────────────┘
Keep query/restore path for archived data.
\`\`\``,tech:"hld",displayNumber:147,sortKey:150},{id:"hld-151",number:148,title:"Design cold storage.",question:"Design cold storage.",difficulty:"Intermediate",category:"Database & Storage",answer:`Store infrequently-accessed data very cheaply, trading retrieval speed/cost for storage cost (S3 Glacier). Write-once, rarely read, with **retrieval latency** (min–hours) + per-retrieval cost. **Lifecycle policies** auto-move data hot→warm→cold by age/access. Uses: backups, archives, compliance, logs. **Challenges**: retrieval latency/cost, lifecycle policy design, restorability, cost vs access.

\`\`\`text
── LIFECYCLE TIERING (top → bottom, by age) ──
   ┌──────────────┐
   │HOT (S3 Std)  │  frequent access, instant, $$$
   └──────┬───────┘  after 30d
          ▼
   ┌──────────────┐
   │WARM (S3 IA)  │  occasional, instant, $$
   └──────┬───────┘  after 90d
          ▼
   ┌──────────────┐
   │COLD (Glacier)│  rare, minutes-hours retrieval, $
   └──────────────┘
Auto lifecycle policies by age/access. Plan retrieval latency/cost.
\`\`\``,tech:"hld",displayNumber:148,sortKey:151},{id:"hld-152",number:149,title:"Design object storage similar to S3.",question:"Design object storage similar to S3.",difficulty:"Hard",category:"Database & Storage",answer:`Store immutable **objects** in **buckets**, keyed, with metadata. Core: a **metadata service** (bucket/key → location, size, checksum, ACL) + a **storage layer** that chunks objects and **replicates/erasure-codes** them across nodes/AZs for 11-nines durability. Versioning, lifecycle tiering, REST API. Partition metadata, distribute chunks. **Challenges**: durability (erasure coding), metadata consistency, huge scale, multipart uploads, availability. Serve via CDN.

\`\`\`text
── BACK-OF-ENVELOPE ──
Durability: 11 nines via erasure coding (e.g. 10 data + 4 parity) across AZs
Scale:    exabytes; partition metadata, distribute chunks

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  PUT/GET bucket/key
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Metadata      │  bucket/key → chunk locations, checksum, ACL, version
   │Service       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Storage Nodes │  object → chunks → erasure-coded across AZs
   │(chunks)      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │     CDN      │  (hot objects)
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:149,sortKey:152},{id:"hld-153",number:150,title:"Design a distributed file system.",question:"Design a distributed file system.",difficulty:"Hard",category:"Database & Storage",answer:`Store large files across many nodes with a POSIX-like/blob interface. Split files into **blocks/chunks**, replicate each across data nodes, and use a **metadata/name node** mapping files → block locations. Clients read/write blocks directly from data nodes (parallel throughput). **Challenges**: metadata scale + HA (single NameNode = classic SPOF), block replication on failure, consistency, sequential throughput. (HDFS/GFS.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  (1) ask NameNode for block locations
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  NameNode    │  file → [block locations] (metadata; HA needed — SPOF)
   └──────┬───────┘
     (2) read/write blocks DIRECTLY (parallel)
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Data  ││Data  ││Data  │  block replicated 3× across nodes
│Node 1││Node 2││Node 3│  re-replicate on failure
└──────┘└──────┘└──────┘
File → 128MB blocks. (HDFS/GFS)
\`\`\``,tech:"hld",displayNumber:150,sortKey:153},{id:"hld-154",number:151,title:"Design a large-file upload system.",question:"Design a large-file upload system.",difficulty:"Hard",category:"Database & Storage",answer:`Use **multipart/chunked upload**: the client splits the file into parts, uploads each in parallel (direct to object storage via pre-signed URLs), and the service assembles on completion. Track session + received parts. Enables parallelism, retrying only failed parts, huge files. **Challenges**: assembling parts, retrying failed chunks, resumability, per-part checksums, cleaning up abandoned uploads.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Client      │  split file into N parts
   └──────┬───────┘
     init multipart → get session + pre-signed URLs
          ▼
   ┌──────────────┐
   │Upload parts  │  IN PARALLEL, direct to S3 (retry failed parts)
   │1..N          │
   └──────┬───────┘
     all parts done
          ▼
   ┌──────────────┐
   │Complete →    │  S3 assembles parts into one object
   │Assemble      │
   └──────────────┘
Per-part checksums. Clean up abandoned uploads.
\`\`\``,tech:"hld",displayNumber:151,sortKey:154},{id:"hld-155",number:152,title:"Design resumable file uploads.",question:"Design resumable file uploads.",difficulty:"Hard",category:"Database & Storage",answer:`Let an interrupted upload continue rather than restart. Client creates an upload **session** (server records id + total size + chunk size); uploads chunks with offset/index; server persists which chunks are received. On resume, client asks which chunks are missing and uploads only those. Complete when all arrive. **Challenges**: tracking received chunks, per-chunk integrity, session expiry, concurrent chunks, idempotent chunk writes. (tus / S3 multipart.)

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Create Session│  {id, totalSize, chunkSize}
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Upload chunks │  by index; server records received set
   └──────┬───────┘
     interrupted!
          ▼
   ┌──────────────┐
   │RESUME: "which│  chunks missing?" → upload only those
   │missing?"     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Complete when │  all chunks present
   │all present   │
   └──────────────┘
Idempotent chunk writes + checksums; session expiry cleanup.
\`\`\``,tech:"hld",displayNumber:152,sortKey:155},{id:"hld-156",number:153,title:"Design file deduplication.",question:"Design file deduplication.",difficulty:"Hard",category:"Database & Storage",answer:`Avoid storing identical data twice. Compute a **content hash** (SHA-256) of a file (or of chunks for partial dedup); before storing, check if the hash exists — if so, add a reference instead of storing again. Store hash→location + reference counts (delete only when refs hit zero). Chunk-level dedup catches partial overlaps (backups). **Challenges**: hash collisions (strong hashes), content-defined chunking, ref counting, hashing cost.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Upload file  │
   └──────┬───────┘
     hash chunks (SHA-256)
          ▼
   ┌──────────────┐
   │Hash exists?  │  (lookup hash → location)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ yes        ▼ no
 ┌──────────┐ ┌──────────────┐
 │add ref   │ │store bytes + │
 │(ref++)   │ │record hash   │
 │skip bytes│ └──────────────┘
 └──────────┘
Delete data only when ref count = 0. Chunk-level → partial overlap dedup.
\`\`\``,tech:"hld",displayNumber:153,sortKey:156},{id:"hld-157",number:154,title:"Design content-addressable storage.",question:"Design content-addressable storage.",difficulty:"Hard",category:"Database & Storage",answer:`Address data by its **content hash** rather than location/name: \`store(data) → hash\`, \`get(hash) → data\`. Identical data → one address (auto dedup); immutable (change → new hash); integrity verifiable (rehash + compare). Used in Git, IPFS, Docker layers. **Challenges**: hash choice (collision-resistant), garbage collection (unreferenced objects), no in-place updates (versioning via new hashes), name→hash mapping (separate layer).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  store(data) │
   └──────┬───────┘
     address = hash(content)  (SHA-256)
          ▼
   ┌──────────────┐
   │Store keyed by│  hash → immutable, auto-dedup, verifiable
   │HASH          │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │get(hash)     │  → data (rehash to verify integrity)
   └──────────────┘
Names map to hashes separately. GC unreferenced objects. (Git/IPFS/Docker)
\`\`\``,tech:"hld",displayNumber:154,sortKey:157},{id:"hld-158",number:155,title:"Design a blob storage service.",question:"Design a blob storage service.",difficulty:"Intermediate",category:"Database & Storage",answer:`Store binary large objects (images, videos, docs) with put/get/delete. Metadata (id, size, content-type, owner, checksum) in a DB; bytes in a distributed storage layer (replicated/erasure-coded). Serve via signed URLs + CDN. Multipart uploads + lifecycle tiering. **Challenges**: durability (replication/erasure coding), large uploads (multipart/resumable), access control (signed URLs), CDN integration, cost/tiering. (A focused object store.)

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  put/get/delete (signed URL)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ metadata   ▼ bytes
 ┌──────────┐┌──────────────┐
 │Metadata  ││Storage Layer │  (replicated / erasure-coded)
 │DB(id,size││              │
 │,type,ACL)││              │
 └──────────┘└──────┬───────┘
                    ▼
             ┌──────────────┐
             │     CDN      │  (serve hot blobs)
             └──────────────┘
Multipart for large blobs; lifecycle tiering.
\`\`\``,tech:"hld",displayNumber:155,sortKey:158},{id:"hld-159",number:156,title:"Design database partitioning.",question:"Design database partitioning.",difficulty:"Intermediate",category:"Database & Storage",answer:`Split a large table for manageability + performance. **Horizontal** (rows): partition by range/hash/list on a key (e.g. date, region) → queries hit one partition (**partition pruning**), old partitions dropped/archived cheaply. **Vertical** (columns): split rarely-used/large columns out. Within one DB (partitions) or across DBs (sharding). **Challenges**: partition-key choice, cross-partition queries, rebalancing, per-partition indexes/constraints.

\`\`\`text
── TWO TYPES ──
   ┌──────────────────────────────────────┐
   │ HORIZONTAL (rows): partition by key   │
   │   orders_2023 | orders_2024 | orders_2025
   │   → partition pruning; drop old cheaply
   ├──────────────────────────────────────┤
   │ VERTICAL (columns): split by feature   │
   │   users_core | users_profile_blob      │
   └──────────────────────────────────────┘
Within one DB (table partitions) or across DBs (sharding).
Challenges: key choice, cross-partition queries, rebalancing.
\`\`\``,tech:"hld",displayNumber:156,sortKey:159},{id:"hld-160",number:157,title:"Design read/write splitting.",question:"Design read/write splitting.",difficulty:"Intermediate",category:"Database & Storage",answer:`Route **writes** to the primary and **reads** to replicas to scale read-heavy workloads. A proxy (ProxySQL) or driver/app logic directs by query type (write/txn → primary; read-only → replica, load-balanced). Handle **replication lag**: read-your-writes → send a user's reads to primary briefly after a write. **Challenges**: staleness, correct routing (avoid stale reads for critical data), lag monitoring, failover.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   App        │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Proxy (ProxySQL│  route by query type
   │)             │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ write/txn  ▼ read-only
 ┌──────────┐┌──────────────┐
 │ PRIMARY  ││Replicas      │  (load-balanced)
 └──────────┘└──────────────┘
Read-your-writes → pin reads to primary briefly post-write. Monitor lag.
\`\`\``,tech:"hld",displayNumber:157,sortKey:160},{id:"hld-161",number:158,title:"Design CQRS architecture.",question:"Design CQRS architecture.",difficulty:"Hard",category:"Database & Storage",answer:`Separate the **write model** (commands, normalized, validated, consistency-focused) from the **read model** (queries, denormalized, per-view), kept in sync via events. Writes emit events; projectors update read models (possibly different DBs). **Benefits**: independent read/write scaling, tailored read models, pairs with event sourcing. **Challenges**: eventual consistency between sides, complexity, idempotent projectors. Use only where read/write needs diverge.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Command     │  (write intent)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Write Model   │  normalized, validated → emit event
   └──────┬───────┘
     event
          ▼
   ┌──────────────┐
   │  Projector   │  update read model (idempotent)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Read Model    │  denormalized (fast queries)
   └──────────────┘
Scale reads/writes independently. Cost: eventual consistency + complexity.
\`\`\``,tech:"hld",displayNumber:158,sortKey:161},{id:"hld-162",number:159,title:"Design event sourcing.",question:"Design event sourcing.",difficulty:"Hard",category:"Database & Storage",answer:`Store state as an **immutable, ordered log of events** rather than current state; rebuild state by replaying events. Gives audit trail, time-travel, retroactive projections. Use **snapshots** to avoid replaying millions of events. Pairs with **CQRS** (events feed read models). **Challenges**: querying current state (fold/snapshots + read models), event schema/versioning (events live forever), eventual consistency, complexity. Append-only (never mutate/delete events).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Command     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Event Store   │  append events: Deposited(+100), Withdrawn(-30)...
   │(immutable log)│  (never mutate/delete)
   └──────┬───────┘
     state = fold(events)  (+ periodic snapshots)
          ▼
   ┌──────────────┐
   │Current State │  = snapshot + replay events after it
   └──────────────┘
Audit trail + time-travel + retroactive projections. Pairs with CQRS.
\`\`\``,tech:"hld",displayNumber:159,sortKey:162},{id:"hld-163",number:160,title:"Design a polyglot persistence architecture.",question:"Design a polyglot persistence architecture.",difficulty:"Intermediate",category:"Database & Storage",answer:`Use different databases per workload's access pattern — a natural consequence of database-per-service. PostgreSQL for orders (ACID), MongoDB for catalog (flexible docs), Redis for sessions/cache, Elasticsearch for search, Cassandra for high-write events, Neo4j for relationships. **Benefit**: fit-for-purpose storage. **Challenges**: ops overhead (backups/expertise/monitoring per engine), cross-store consistency (sync via events/CDC), avoiding sprawl (standardize on a small set).

\`\`\`text
── FIT-FOR-PURPOSE (per service) ──
   ┌──────────────────────────────────────┐
   │ orders    → PostgreSQL  (ACID)         │
   │ catalog   → MongoDB     (flexible docs)│
   │ sessions  → Redis       (cache, TTL)   │
   │ search    → Elasticsearch (full-text)  │
   │ events    → Cassandra   (high write)   │
   │ social    → Neo4j       (relationships)│
   └──────────────────────────────────────┘
Cross-store consistency via events/CDC. Cost: ops overhead per engine.
Standardize on a small approved set (avoid sprawl).
\`\`\``,tech:"hld",displayNumber:160,sortKey:163},{id:"hld-164",number:161,title:"Design an event-driven order system using Kafka.",question:"Design an event-driven order system using Kafka.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`The order service publishes \`OrderPlaced\` to an \`orders\` topic (keyed by orderId for ordering). Downstream services each consume as their own **consumer group**: payment charges, inventory reserves, shipping schedules, notifications email — reacting independently and emitting their own events. Use the **outbox pattern** so the DB write and event publish are atomic. Reliability via idempotent consumers, retry topics + DLT. **Challenges**: eventual consistency, ordering per order (key), idempotency, multi-step flow (Saga).

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Order Service│  write order + outbox (same DB tx)
   └──────┬───────┘
     CDC/Debezium
          ▼
   ┌──────────────┐
   │Kafka "orders"│  key = orderId (per-order ordering)
   └──────┬───────┘
   ┌──────┼──────────┬───────────┐
   ▼      ▼          ▼           ▼
┌──────┐┌──────┐┌────────┐┌──────────┐
│Payment││Invent││Shipping││Notify    │  each = own consumer group
│(charge││ory   ││(sched) ││(email)   │  (independent, replayable)
│)      ││(rsrv)││        ││          │
└──────┘└──────┘└────────┘└──────────┘
Idempotent consumers + retry/DLT. Multi-step consistency via Saga.
\`\`\``,tech:"hld",displayNumber:161,sortKey:164},{id:"hld-165",number:162,title:"Design an order-processing pipeline.",question:"Design an order-processing pipeline.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`A staged pipeline: \`orders\` → validation → payment → fulfillment → shipping, each stage a consumer that processes and emits the next event (or an orchestrator drives it). Persist state per stage; use **Saga** compensations on failure (refund if fulfillment fails after payment). Idempotent stages, retry topics + DLT, monitor lag per stage. **Challenges**: partial failures + compensation, exactly-once effects, ordering per order, backpressure between stages, observability.

\`\`\`text
── PIPELINE (top → bottom) ──
   ┌──────────────┐
   │  orders      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Validate    │──► emit event
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Payment     │──► emit event
   └──────┬───────┘
          ▼ (fulfillment fails?)
   ┌──────────────┐        ┌──────────────┐
   │  Fulfillment │───────►│ COMPENSATE   │  (Saga: refund/release)
   └──────┬───────┘        └──────────────┘
          ▼
   ┌──────────────┐
   │  Shipping    │
   └──────────────┘
Idempotent stages + retry/DLT. Trace order across stages; monitor per-stage lag.
\`\`\``,tech:"hld",displayNumber:162,sortKey:165},{id:"hld-166",number:163,title:"Design a Kafka-based notification system.",question:"Design a Kafka-based notification system.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Producers publish notification events to Kafka; per-channel consumer groups (push, email, SMS, in-app) consume and call providers, honoring preferences + templates. Kafka decouples producers from delivery and absorbs spikes; each channel scales by partition count. Retry topics + DLT for provider failures; idempotency (dedupe by notification id). **Challenges**: per-channel scaling, provider rate limits, idempotency, preferences/opt-out, priority (separate topics), delivery-status tracking.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:  500M notifications/day = ~5,800/sec (peak ~20k); 1 event → up to 4 channels

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Producers   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Kafka         │  (decouple + absorb spikes)
   │"notifications"│
   └──────┬───────┘
   ┌──────┼──────────┬───────────┐
   ▼      ▼          ▼           ▼
┌──────┐┌──────┐┌──────┐┌──────────┐
│Push  ││Email ││ SMS  ││In-App    │  per-channel groups (scale by partitions)
│group ││group ││group ││group     │  → providers (retry/DLT + dedupe)
└──────┘└──────┘└──────┘└──────────┘
Honor preferences/opt-out; priority via separate topics; track status.
\`\`\``,tech:"hld",displayNumber:163,sortKey:166},{id:"hld-167",number:164,title:"Design a Kafka-based logging pipeline.",question:"Design a Kafka-based logging pipeline.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Apps/agents produce structured logs to a Kafka \`logs\` topic (buffer absorbing bursts, decoupling producers from the indexer); stream processors parse/enrich (service, trace id), then sink to Elasticsearch/Loki (+ cold object storage). Kibana/Grafana query. Partition by service/host; indexer scales out. **Challenges**: huge write volume (Kafka buffers), indexer lag (Kafka retains), sampling noisy logs, PII scrubbing, retention/cost tiering.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:  1M lines/sec × 500 B = ~43 TB/day → Kafka buffers, ES hot 7d, cold tier after

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Apps/Agents   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Kafka "logs"  │  (buffer, absorb bursts; indexer can lag)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Stream        │  parse/enrich (+ trace id), scrub PII, sample
   │Processor     │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────────┐┌──────────────┐
 │ES/Loki(hot)││Object Storage│
 └─────┬──────┘│(cold)        │
       ▼       └──────────────┘
 ┌────────────┐
 │Kibana/Grafana│
 └────────────┘
\`\`\``,tech:"hld",displayNumber:164,sortKey:167},{id:"hld-168",number:165,title:"Design a Kafka-based analytics pipeline.",question:"Design a Kafka-based analytics pipeline.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Ingest events (clicks, views, transactions) into Kafka; a **stream processor** (Kafka Streams/Flink) does real-time aggregations (counts, sessions, windows) → serving store (Druid/ClickHouse/Redis) for dashboards; also sink raw events to a **data lake/warehouse** (S3 + Spark/BigQuery) for batch/ML. **Lambda/Kappa** architecture. **Challenges**: real-time vs batch consistency, windowing + late events, EOS aggregation, high cardinality, schema evolution.

\`\`\`text
── ARCHITECTURE (Kappa/Lambda, top → bottom) ──
   ┌──────────────┐
   │ Events       │  (clicks/views/transactions)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │   Kafka      │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ real-time  ▼ raw
 ┌────────────┐┌──────────────┐
 │Stream Proc ││Data Lake/    │
 │(Flink):    ││Warehouse     │
 │windowed agg││(S3/BigQuery) │  batch/ML
 └─────┬──────┘└──────────────┘
       ▼
 ┌────────────┐
 │Serving Store│  (Druid/ClickHouse) → dashboards (sub-sec)
 └────────────┘
Handle late events, EOS aggregation, schema evolution.
\`\`\``,tech:"hld",displayNumber:165,sortKey:168},{id:"hld-169",number:166,title:"Design Kafka consumer scaling.",question:"Design Kafka consumer scaling.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Scale a consumer group by adding consumers, but effective parallelism is **capped at the partition count** (extra consumers idle). Size partitions for peak parallelism up front. At the cap, add partitions (mind key-ordering) or parallelize within a consumer (worker threads, preserving offset correctness). Cooperative rebalancing + static membership for smooth scaling. **Challenges**: partition-count ceiling, rebalance disruption, hot partitions, offset correctness with intra-consumer parallelism.

\`\`\`text
── PARTITION → CONSUMER MAPPING ──
   Topic (6 partitions P0-P5), consumer group:
   ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐
   │ P0   ││ P1   ││ P2   ││ P3   ││ P4   ││ P5   │
   └──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘
      ▼       ▼       ▼       ▼       ▼       ▼
   ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐
   │ C1   ││ C2   ││ C3   ││ C4   ││ C5   ││ C6   │  (max = #partitions)
   └──────┘└──────┘└──────┘└──────┘└──────┘└──────┘
   C7 → IDLE (no partition). Hit cap → add partitions or intra-consumer workers.
Cooperative rebalancing + static membership = smooth scaling.
\`\`\``,tech:"hld",displayNumber:166,sortKey:169},{id:"hld-170",number:167,title:"Design a Kafka partitioning strategy.",question:"Design a Kafka partitioning strategy.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Choose a **key** that (a) preserves ordering where needed (same entity → same partition) and (b) distributes load evenly (high cardinality, no skew). Key by \`orderId\`/\`userId\`. Partition count from throughput + consumer parallelism + headroom. Avoid low-cardinality keys (hot partitions). Salt hot keys (sacrificing strict per-key order). **Challenges**: ordering vs even distribution, hot partitions from skew, partition-count sizing (increasing re-hashes keys), null-key behavior.

\`\`\`text
── KEY → PARTITION ──
   ┌──────────────┐
   │  Producer    │  key = orderId
   └──────┬───────┘
     hash(key) % partitions
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│ P0   ││ P1   ││ P2   │  same key → same partition (ordered)
└──────┘└──────┘└──────┘
Good key: high cardinality + no skew + preserves entity order.
Hot key → salt into sub-keys (loses strict order). Increasing partitions re-hashes.
\`\`\``,tech:"hld",displayNumber:167,sortKey:170},{id:"hld-171",number:168,title:"Design a Kafka consumer group architecture.",question:"Design a Kafka consumer group architecture.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Model each independent app/purpose as its own **consumer group** (each gets all events, own offsets); scale within a group by adding consumers. Use **CooperativeSticky** assignor + **static membership** for stable rebalances, commit offsets after processing (at-least-once) with idempotency, and handle rebalances via listeners (commit on revoke). **Challenges**: partition-count ceiling, rebalance disruption, offset correctness, hot partitions, group isolation (independent offsets).

\`\`\`text
── ONE GROUP PER PURPOSE (top → bottom) ──
   ┌──────────────┐
   │Topic "orders"│  (all events)
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Group ││Group ││Group     │  each gets ALL events, OWN offsets
│billing││audit ││analytics │  (isolated — one slow group ≠ affects others)
└──────┘└──────┘└──────────┘
Scale within group (add consumers). CooperativeSticky + static membership.
Commit after processing + idempotent; commit-on-revoke listener.
\`\`\``,tech:"hld",displayNumber:168,sortKey:171},{id:"hld-172",number:169,title:"Design Kafka retry topics.",question:"Design Kafka retry topics.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`For transient failures, republish to **tiered retry topics** with increasing delays (\`retry-5s\`, \`retry-1m\`, \`retry-10m\`) instead of blocking the main partition; a consumer per tier waits the delay before reprocessing. After exhausting tiers → **DLT**. Track attempts in headers. Spring's \`@RetryableTopic\` automates non-blocking retries. **Challenges**: implementing delay, idempotency, retry storms (backoff + jitter), non-blocking main flow.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ main topic   │
   └──────┬───────┘  transient fail
          ▼
   ┌──────────────┐
   │ retry-5s     │  (wait 5s, reprocess)
   └──────┬───────┘  fail
          ▼
   ┌──────────────┐
   │ retry-1m     │
   └──────┬───────┘  fail
          ▼
   ┌──────────────┐
   │ retry-10m    │
   └──────┬───────┘  exhausted
          ▼
   ┌──────────────┐
   │    DLT       │
   └──────────────┘
Non-blocking (main partition keeps flowing). Spring: @RetryableTopic.
\`\`\``,tech:"hld",displayNumber:169,sortKey:172},{id:"hld-173",number:170,title:"Design Kafka dead-letter topics.",question:"Design Kafka dead-letter topics.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Route records that fail after bounded retries to a **DLT** so they don't block the partition. Include failure context in headers (orig topic/partition/offset, exception, timestamp). Monitor/alert on DLT growth. **Curated replay**: fix root cause, validate, republish to source with dedup + rate limiting. **Challenges**: transient vs permanent classification, no infinite retries (stall partition), safe replay (idempotency).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Consumer     │  retries exhausted
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    DLT       │  + headers (orig topic/offset/error/ts)
   │(main partition│
   │ unblocked)   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Alert + Replay│  fix cause → validate → republish (dedup + rate limit)
   └──────────────┘
Classify transient vs permanent; never infinite-retry inline.
\`\`\``,tech:"hld",displayNumber:170,sortKey:173},{id:"hld-174",number:171,title:"Design Kafka message ordering.",question:"Design Kafka message ordering.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Kafka guarantees order **within a partition** only. Keep related events ordered via the **same key** (entity id) → same partition. Enable the **idempotent producer** so retries don't reorder. Strict **global** ordering → single partition (throughput bottleneck), usually avoided; prefer per-key. One consumer per partition per group processes sequentially. **Challenges**: no cross-partition ordering, retries reordering (idempotence), increasing partitions changes key mapping.

\`\`\`text
── ORDERING GUARANTEE ──
   Partition P2: [ e0 → e1 → e2 → e3 ]   (strict order within partition)
   P0, P1, P2 consumed IN PARALLEL → no order across partitions

   Same key "A1" → always P2 → ordered:
   ┌──────────────┐
   │OrderPlaced(A1)│──┐
   ├──────────────┤  ├─► P2 (sequential)
   │OrderPaid(A1)  │──┘
   └──────────────┘
Idempotent producer preserves order on retry. Global order = single partition.
\`\`\``,tech:"hld",displayNumber:171,sortKey:174},{id:"hld-175",number:172,title:"Design exactly-once Kafka processing.",question:"Design exactly-once Kafka processing.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`For read-process-write within Kafka, use **transactions**: idempotent + transactional producer (\`transactional.id\`), consume with \`isolation.level=read_committed\` + auto-commit off, atomically produce outputs + commit offsets via \`sendOffsetsToTransaction\`. Kafka Streams: \`processing.guarantee=exactly_once_v2\`. External side effects → **idempotency keys** at the sink. **Challenges**: transaction overhead/latency, LSO read delay, Kafka txn can't span a DB (idempotent sink/outbox), producer fencing.

\`\`\`text
── TRANSACTIONAL READ-PROCESS-WRITE (top → bottom) ──
   ┌──────────────┐
   │Consumer      │  read_committed, auto-commit OFF
   └──────┬───────┘
          ▼ beginTransaction()
   ┌──────────────┐
   │Process       │
   └──────┬───────┘
          ▼ atomic
   ┌──────────────┐
   │Produce output│  + sendOffsetsToTransaction(offsets)
   └──────┬───────┘
          ▼ commitTransaction()  (output + input offsets ATOMIC)
   ┌──────────────┐
   │Output Topic  │
   └──────────────┘
Kafka Streams: exactly_once_v2. External side effects → idempotency key/outbox.
\`\`\``,tech:"hld",displayNumber:172,sortKey:175},{id:"hld-176",number:173,title:"Design at-least-once processing.",question:"Design at-least-once processing.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Process the record **first, then commit** the offset — a crash before commit re-reads (never lost, possible duplicates). The common default. Pair with **idempotent processing** (dedupe by event id / unique constraint / upsert). Manual commits (auto-commit off), retry topics + DLT. **Challenges**: duplicates on retry/rebalance (idempotency), commit timing, commit after full batch.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  poll()      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  PROCESS     │  (idempotent: dedupe by event id / upsert)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  COMMIT      │  (after processing → no loss, possible dup)
   └──────────────┘
Crash before commit → re-read (dup, handled by idempotency). Auto-commit OFF.
\`\`\``,tech:"hld",displayNumber:173,sortKey:176},{id:"hld-177",number:174,title:"Design at-most-once processing.",question:"Design at-most-once processing.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Commit the offset **before** processing — a crash mid-processing skips the record: **no duplicates, but possible loss**. Only for loss-tolerant data (metrics, telemetry) where duplicates are worse than gaps. Simplest/fastest but rarely wanted for important data. **Challenges**: accepting data loss; recognizing when acceptable (rarely for business-critical → use at-least-once + idempotency).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  poll()      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  COMMIT      │  (BEFORE processing)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  PROCESS     │  crash here → record SKIPPED (loss, no dup)
   └──────────────┘
Only for loss-tolerant data (metrics/telemetry). Not for business-critical events.
\`\`\``,tech:"hld",displayNumber:174,sortKey:177},{id:"hld-178",number:175,title:"Design duplicate-event handling.",question:"Design duplicate-event handling.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`At-least-once produces duplicates (retries, rebalances); consumers must **dedupe**: attach a unique **event id**, track processed ids (durable dedup store / DB unique constraint), skip seen ids. Or **upserts** (write final state) / version checks (ignore stale). **Challenges**: dedup store size/TTL, ordering vs dedup, exactly-once effects without transactions, durability across restarts.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Event(id)    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ seen id?     │  (durable dedup store / unique constraint)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ yes        ▼ no
 ┌──────┐   ┌──────────────┐
 │ skip │   │process +     │
 │      │   │record id     │
 └──────┘   └──────────────┘
Or upsert (state not delta) / version check. Dedup store must be durable.
\`\`\``,tech:"hld",displayNumber:175,sortKey:178},{id:"hld-179",number:176,title:"Design event replay.",question:"Design event replay.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Kafka **retains** events, so reprocess history: reset a group's offsets (earliest, a specific offset, or a timestamp via \`offsetsForTimes\`) and re-consume. Uses: rebuild a broken read model, backfill a new consumer, bug recovery. Consumers must be **idempotent** (replay re-applies); use a separate group/topic to avoid disrupting live processing. **Challenges**: idempotency, side effects (no re-charge/re-email), replay volume/time, isolation from prod.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Kafka topic   │  (retained events — replayable)
   │[e0...eN]     │
   └──────┬───────┘
     reset group offset (earliest / offset / timestamp)
          ▼
   ┌──────────────┐
   │Replay Consumer│  (SEPARATE group — isolate from live)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Rebuild read  │  model / backfill / recover
   │model         │
   └──────────────┘
MUST be idempotent (replay duplicates). Guard side effects (no re-charge/email).
\`\`\``,tech:"hld",displayNumber:176,sortKey:179},{id:"hld-180",number:177,title:"Design event versioning.",question:"Design event versioning.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Events are immutable and long-lived, so schemas evolve. Version with **schema (Avro/Protobuf) + Schema Registry** enforcing compatibility; make changes **additive** (new optional fields with defaults; avoid removing/renaming). Consumers tolerate unknown fields, upcast old events to the new shape. **Challenges**: backward/forward compatibility (old + new coexist), never break existing consumers, upcasting, deploy-order coordination.

\`\`\`text
── VERSIONED EVENTS (flow) ──
   ┌──────────────┐
   │Schema Registry│  enforce compatibility on register
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Producer      │  v1 → v2 = ADDITIVE (new optional field + default)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Consumer      │  tolerate unknown fields; upcast old → new shape
   └──────────────┘
No rename/remove/retype. Deploy order per compatibility mode.
\`\`\``,tech:"hld",displayNumber:177,sortKey:180},{id:"hld-181",number:178,title:"Design schema evolution.",question:"Design schema evolution.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Evolve contracts safely via a Schema Registry with an enforced **compatibility mode**: **backward** (new schema reads old → upgrade consumers first), **forward** (old reads new → upgrade producers first), or **full**. Additive changes (optional + defaults); avoid renames/removals/type changes. Validate in CI before rollout. **Challenges**: choosing compatibility mode, deploy order, breaking changes (new topic + migration), pre-deploy testing.

\`\`\`text
── COMPATIBILITY MODES ──
   ┌──────────────────────────────────────┐
   │ BACKWARD: new schema reads OLD data    │
   │   → upgrade CONSUMERS first            │
   ├──────────────────────────────────────┤
   │ FORWARD: old schema reads NEW data     │
   │   → upgrade PRODUCERS first            │
   ├──────────────────────────────────────┤
   │ FULL: both directions hold             │
   └──────────────────────────────────────┘
Additive only. Validate in CI. Breaking change → new topic + migrate.
\`\`\``,tech:"hld",displayNumber:178,sortKey:181},{id:"hld-182",number:179,title:"Design a Schema Registry architecture.",question:"Design a Schema Registry architecture.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`A central service storing versioned schemas per subject (topic), enforcing **compatibility** on registration, returning a numeric **schema id**. Producers register/validate + embed only the id (\`[magic][id][payload]\`); consumers fetch (cache) the schema by id. Store durably (compacted Kafka topic), replicate for HA. **Challenges**: availability (on the serialization critical path → cache + HA), compatibility enforcement, subject naming, governance.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Producer      │  register/validate schema → get schema id
   └──────┬───────┘
     message = [magic byte][schema id][payload]
          ▼
   ┌──────────────┐
   │Kafka         │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Consumer      │  read id → fetch+CACHE schema → deserialize
   └──────┬───────┘
          ▲
   ┌──────┴───────┐
   │Schema Registry│  subject → versioned schemas + compatibility (HA, cached)
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:179,sortKey:182},{id:"hld-183",number:180,title:"Design Kafka disaster recovery.",question:"Design Kafka disaster recovery.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Replicate topics (+ consumer offsets) to a **secondary cluster** in another region via **MirrorMaker 2** (active-passive/active-active). Define **RPO** (bounded by replication lag) + **RTO** (failover automation). On disaster, redirect clients to the secondary; offset translation lets consumers resume near their position. Rack/AZ awareness within a cluster + regular DR drills. **Challenges**: replication lag, offset translation, consumer failover, active-active split-brain, testing.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Primary Cluster│  (region A)
   │(topics+offsets)│
   └──────┬───────┘
     MirrorMaker 2 (replicate + offset translation)
          ▼
   ┌──────────────┐
   │Secondary     │  (region B) — standby
   │Cluster       │
   └──────┬───────┘
     disaster → redirect clients
          ▼
   ┌──────────────┐
   │Consumers      │  resume near last position (translated offsets)
   └──────────────┘
RPO = replication lag; RTO = failover automation. Test DR drills.
\`\`\``,tech:"hld",displayNumber:180,sortKey:183},{id:"hld-184",number:181,title:"Design cross-region Kafka replication.",question:"Design cross-region Kafka replication.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Use **MirrorMaker 2** (Connect-based) to replicate topics between regional clusters with topic renaming/prefixing + **offset translation**. Patterns: **aggregation** (many → one), **active-passive** (DR), **active-active** (bidirectional — loop-safe via naming). Async replication → lag, bandwidth cost. **Challenges**: replication lag, avoiding loops (active-active), offset translation, cross-region bandwidth/cost, schema/registry replication.

\`\`\`text
── PATTERNS ──
   Aggregation (N→1):
     RegionA.orders ─┐
     RegionB.orders ─┼─► Central Cluster
     RegionC.orders ─┘
   Active-Passive (DR):
     Primary ──MM2──► Secondary (standby)
   Active-Active (bidirectional, loop-safe naming):
     A.orders ◄──MM2──► B.orders
Async → lag. Watch: loops, offset translation, bandwidth/cost, schema replication.
\`\`\``,tech:"hld",displayNumber:181,sortKey:184},{id:"hld-185",number:182,title:"Design an outbox pattern using Kafka.",question:"Design an outbox pattern using Kafka.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`Solve the **dual-write problem**: write the business row + an \`outbox\` event row in the **same DB transaction**; a relay or **CDC** (Debezium) tails the outbox and publishes committed rows to Kafka. Makes the DB write + event intent atomic (no lost/orphaned events). At-least-once → consumers dedupe. **Challenges**: CDC/relay reliability, at-least-once (idempotent consumers), outbox cleanup, ordering (by id/key), publish-lag monitoring.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Service      │  SAME DB TX:
   │              │    INSERT business row
   │              │    INSERT outbox(event)
   └──────┬───────┘
     commit (atomic — no dual write)
          ▼
   ┌──────────────┐
   │ Outbox Table │
   └──────┬───────┘
     CDC/Debezium tails committed rows
          ▼
   ┌──────────────┐
   │    Kafka     │  → consumers (dedupe, at-least-once)
   └──────────────┘
Cleanup outbox; order by id/key; monitor CDC/publish lag.
\`\`\``,tech:"hld",displayNumber:182,sortKey:185},{id:"hld-186",number:183,title:"Design CDC-based event streaming.",question:"Design CDC-based event streaming.",difficulty:"Hard",category:"Kafka & Event-Driven",answer:`**Change Data Capture** streams a DB's committed changes (inserts/updates/deletes) as events by tailing its **transaction log** (binlog/WAL) via Debezium → Kafka — no app changes, low latency, no dual-write. Downstream: read models, cache/search sync, analytics, outbox. Emit before/after images. **Challenges**: snapshot→stream handoff, DDL changes, deletes (tombstones), ordering per key, downstream idempotency, source log retention.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Database    │  commits (insert/update/delete)
   └──────┬───────┘
     tail transaction log (binlog/WAL)
          ▼
   ┌──────────────┐
   │  Debezium    │  change events (before/after, op)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    Kafka     │
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Read  ││Cache/││Analytics │  no app change, low latency
│Models││Search││/Outbox   │
└──────┘└──────┘└──────────┘
Snapshot→stream handoff; deletes → tombstones; downstream idempotency.
\`\`\``,tech:"hld",displayNumber:183,sortKey:186},{id:"hld-187",number:184,title:"Design Kafka-based cache invalidation.",question:"Design Kafka-based cache invalidation.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Publish data-change events (from CDC or the writing service) to a Kafka topic; each app instance/cache node consumes and **invalidates/updates** the relevant entries — decoupled, scalable, near-real-time across many nodes. Better than TTL-only (fresher) or direct calls (coupled). **Challenges**: eventual consistency (brief staleness), idempotent/order-tolerant invalidation (versions), all nodes consume (own consumer per node), missed events (fallback TTL).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Data Change   │  (writer or CDC)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Kafka         │
   │"invalidations"│
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Cache ││Cache ││Cache │  every node consumes → evict/update key
│Node A││Node B││Node C│  (own consumer per node = broadcast)
└──────┘└──────┘└──────┘
Version-guard invalidations (order-tolerant); fallback TTL for missed events.
\`\`\``,tech:"hld",displayNumber:184,sortKey:187},{id:"hld-188",number:185,title:"Design Kafka-based microservice communication.",question:"Design Kafka-based microservice communication.",difficulty:"Intermediate",category:"Kafka & Event-Driven",answer:`Services communicate **asynchronously via events** on Kafka instead of direct sync calls: a service publishes domain events; interested services subscribe (own groups). Gives loose coupling, resilience (a down consumer lags then catches up), spike absorption, easy new consumers. Sync (REST/gRPC) only where an immediate response is required. **Challenges**: eventual consistency, idempotent + order-tolerant consumers, schema versioning, tracing across async hops, async vs sync choice.

\`\`\`text
── EVENT-DRIVEN COMMUNICATION (top → bottom) ──
   ┌──────────────┐
   │ Service A    │  publish domain event
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    Kafka     │
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Svc B ││Svc C ││Svc D     │  subscribe (own groups)
└──────┘└──────┘└──────────┘
Loose coupling; down consumer catches up; spike absorption; easy new consumers.
Sync (REST/gRPC) only when immediate response needed.
\`\`\``,tech:"hld",displayNumber:185,sortKey:188},{id:"hld-189",number:186,title:"Design WhatsApp.",question:"Design WhatsApp.",difficulty:"Hard",category:"Product Design",answer:`**Functional**: 1:1 & group messaging, delivery/read receipts, presence, media, offline delivery. **Core**: persistent **WebSocket** connections to gateways; messages routed via a **pub/sub backplane** so a sender on gateway A reaches a recipient on gateway B; store until delivered, then delete (privacy). Offline → push. **Challenges**: connection scale, ordering, receipts, presence, group fan-out, E2E encryption.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    2B users, ~500M concurrent connections
Messages: 100B msgs/day = ~1.15M msgs/sec (peak ~3M)
Conns/node: ~100k → ~5,000 gateway nodes
Storage:  transient (delete on delivery) + media in object storage + CDN

── ARCHITECTURE (top → bottom) ──
   ┌──────────┐          ┌──────────┐
   │ Client A │          │ Client B │
   └────┬─────┘          └────┬─────┘
    WebSocket             WebSocket
        ▼                     ▼
   ┌──────────┐          ┌──────────┐
   │Gateway N1│          │Gateway N2│
   └────┬─────┘          └────▲─────┘
        │ publish       subscribe
        ▼                     │
   ┌────────────────────────────────┐
   │  Pub/Sub Backplane (Kafka)     │
   └────┬───────────────────────────┘
        ▼
   ┌──────────────┐   ┌──────────────┐
   │ Message Store│   │ Push Service │ (offline → APNs/FCM)
   │ (until deliv)│   └──────────────┘
   └──────────────┘
Media: object storage + CDN. E2E encryption end-to-end.
\`\`\``,tech:"hld",displayNumber:186,sortKey:189},{id:"hld-190",number:187,title:"Design Telegram.",question:"Design Telegram.",difficulty:"Hard",category:"Product Design",answer:`Like WhatsApp but **cloud-based** (messages stored server-side, multi-device sync) with huge **channels** (broadcast to millions). Persistent connections, sharded message storage per chat, fan-out for mega-channels, per-user sequence for sync. **Challenges**: multi-device sync (ordering/sequence), mega-channel broadcast (fan-out-on-read), media (CDN), secret chats (E2E).

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    800M MAU, ~100M concurrent
Messages: 50B/day = ~580k/sec; mega-channel = 1 post → 10M reads
Storage:  cloud-stored → 50B × 300 B/day = ~15 TB/day (shard by chat)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Client (multi │  sync via per-user seq
   │  device)     │
   └──────┬───────┘
      WebSocket
          ▼
   ┌──────────────┐
   │   Gateway    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Message Svc  │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────┐ ┌──────────────┐
 │Chat DB │ │Channel Fan-out│ (fan-out-on-read for mega-channels)
 │(sharded│ └──────────────┘
 │by chat)│
 └────────┘
Media → CDN. Secret chats → E2E.
\`\`\``,tech:"hld",displayNumber:187,sortKey:190},{id:"hld-191",number:188,title:"Design Slack.",question:"Design Slack.",difficulty:"Hard",category:"Product Design",answer:`**Core**: workspaces → channels → threaded messages, real-time via WebSocket, search, mentions, integrations. Store per channel (sharded by workspace), per-user unread state, fan-out to members via backplane. Full-text search (Elasticsearch). **Challenges**: real-time fan-out, unread counts, search, threading, presence, integrations/webhooks.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    20M DAU, workspaces of 10–100k users
Messages: 5B/day = ~58k/sec; fan-out to channel members
Storage:  5B × 500 B/day = ~2.5 TB/day (shard by workspace) + search index

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
      WebSocket
          ▼
   ┌──────────────┐
   │   Gateway    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Message Service│──► fan-out to channel members (backplane)
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │ Msg  ││Unread  ││Elasticsearch │
 │ DB   ││ state  ││ (search)     │
 │(shard││(per    │└──────────────┘
 │ by ws)││ user) │
 └──────┘└────────┘
Integrations/webhooks (rate-limited).
\`\`\``,tech:"hld",displayNumber:188,sortKey:191},{id:"hld-192",number:189,title:"Design Discord.",question:"Design Discord.",difficulty:"Hard",category:"Product Design",answer:`**Core**: servers (guilds) → text + **voice/video** channels; real-time messaging (WebSocket) + voice (WebRTC/SFU). Fan-out to online members; messages in Cassandra (huge write volume). Voice via SFU. **Challenges**: massive connections, voice (WebRTC+SFU), large-server presence, message history (Cassandra), roles/permissions.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    150M MAU, ~10M concurrent; big guilds = 100k+ members
Messages: 20B/day = ~230k/sec → Cassandra (write-optimized)
Storage:  trillions of messages → wide-column, sharded

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
   WebSocket │ WebRTC
      ┌──────┴──────┐
      ▼             ▼
 ┌──────────┐  ┌──────────┐
 │Text      │  │Voice     │
 │Gateway   │  │SFU (media│
 └────┬─────┘  │ forward) │
      ▼        └──────────┘
 ┌──────────────┐
 │Message Service│──► fan-out to online members
 └──────┬───────┘
        ▼
 ┌──────────────┐
 │  Cassandra   │  (messages, high write, sharded by channel)
 └──────────────┘
Roles/permissions per channel.
\`\`\``,tech:"hld",displayNumber:189,sortKey:192},{id:"hld-193",number:190,title:"Design Zoom.",question:"Design Zoom.",difficulty:"Hard",category:"Product Design",answer:`**Core**: video meetings via **WebRTC**, media through **SFU** (each participant sends 1 upstream, SFU forwards to others — scales better than mesh, cheaper than MCU). WebSocket signaling; regional media servers; simulcast (multi-resolution); recording/screen-share. **Challenges**: low-latency media at scale, SFU/geo-routing, bandwidth adaptation, NAT (STUN/TURN), recording.

\`\`\`text
── BACK-OF-ENVELOPE ──
Meetings: 300M participants/day; avg meeting 10 people
Media:    1 upstream/user, N downstreams via SFU → SFU CPU is the bottleneck
Bandwidth: ~1.5 Mbps/stream → huge egress → regional media servers

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Participant │
   └──────┬───────┘
  (1) WebSocket signaling (SDP/ICE)
          ▼
   ┌──────────────┐
   │Signaling Svc │──► sets up WebRTC
   └──────┬───────┘
  (2) media (WebRTC/UDP)
          ▼
   ┌──────────────┐
   │ Regional SFU │  forward streams (not mesh/mix)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────┐ ┌────────────┐
 │Other   │ │ Recording  │
 │partic. │ │ Service    │
 └────────┘ └────────────┘
Simulcast + STUN/TURN. Geo-route to nearest SFU.
\`\`\``,tech:"hld",displayNumber:190,sortKey:193},{id:"hld-194",number:191,title:"Design Google Meet.",question:"Design Google Meet.",difficulty:"Hard",category:"Product Design",answer:`Architecturally like Zoom: **WebRTC** media via regional **SFUs**, WebSocket signaling, simulcast/SVC adaptive quality, STUN/TURN. Browser-first, Google-account/Calendar integration. Large meetings forward only active speakers. **Challenges**: browser low-latency media, adaptive bitrate, large-meeting scaling, NAT, media-server failover.

\`\`\`text
── BACK-OF-ENVELOPE ──
Similar to Zoom: millions of concurrent participants, ~1.5 Mbps/stream
Large meetings: forward active speakers only (not all N streams)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Browser Client│
   └──────┬───────┘
    WebSocket signaling
          ▼
   ┌──────────────┐
   │Signaling Svc │  (+ Calendar/auth integration)
   └──────┬───────┘
      WebRTC media
          ▼
   ┌──────────────┐
   │ Regional SFU │  SVC/simulcast, active-speaker forwarding
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Other partic. │
   └──────────────┘
STUN/TURN for NAT; media-server failover.
\`\`\``,tech:"hld",displayNumber:191,sortKey:194},{id:"hld-195",number:192,title:"Design a video conferencing system (generic).",question:"Design a video conferencing system (generic).",difficulty:"Hard",category:"Product Design",answer:`**Core**: signaling (WebSocket) exchanges SDP/ICE; **WebRTC** media; topology: mesh (tiny groups), MCU (mix — heavy CPU), or **SFU** (forward — standard). STUN/TURN (NAT), simulcast/SVC, recording, regional media servers. **Challenges**: topology by group size, low latency, bandwidth adaptation, NAT, scaling media servers.

\`\`\`text
── BACK-OF-ENVELOPE ──
Topology by size: mesh (<4, N² connections), SFU (standard), MCU (mix, heavy)
Bandwidth: ~1.5 Mbps/stream; SFU CPU scales with participants

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Participant │
   └──────┬───────┘
     signaling (WebSocket)
          ▼
   ┌──────────────┐
   │Signaling Svc │  exchange SDP + ICE candidates
   └──────┬───────┘
      WebRTC media
          ▼
   ┌──────────────┐
   │ SFU (forward)│  (or MCU=mix / mesh=P2P)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Other partic. │
   └──────────────┘
STUN/TURN, simulcast/SVC, recording, regional servers.
\`\`\``,tech:"hld",displayNumber:192,sortKey:195},{id:"hld-196",number:193,title:"Design YouTube.",question:"Design YouTube.",difficulty:"Hard",category:"Product Design",answer:`**Upload** → object storage → **transcode** (async, multiple resolutions, HLS/DASH segments) → **CDN**. Adaptive bitrate playback; metadata DB (sharded); search (ES); recommendations (ML); view counts (distributed counters). **Challenges**: massive storage/egress (CDN + tiering), transcoding at scale, adaptive streaming, recommendations, view-count accuracy, global delivery.

\`\`\`text
── BACK-OF-ENVELOPE ──
Uploads:  500 hrs/min → huge transcode farm
Views:    5B views/day = ~58k/sec → 99% served by CDN
Storage:  ~1 EB total; each video → 5+ transcoded renditions
Egress:   petabytes/day → CDN offloads origin

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Uploader    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Object Storage│──► event ──►┌──────────────┐
   │  (raw)       │             │Transcode Farm│
   └──────────────┘             │ (renditions, │
          ▲                     │  HLS/DASH)   │
  segments │                    └──────┬───────┘
          ▼                            ▼
   ┌──────────────┐             ┌──────────────┐
   │Object Storage│             │ Metadata DB  │
   │ (segments)   │             │ + Search(ES) │
   └──────┬───────┘             │ + Recs (ML)  │
          ▼                     └──────────────┘
   ┌──────────────┐
   │     CDN      │──► adaptive bitrate playback
   └──────────────┘
View counts → distributed counter.
\`\`\``,tech:"hld",displayNumber:193,sortKey:196},{id:"hld-197",number:194,title:"Design Netflix.",question:"Design Netflix.",difficulty:"Hard",category:"Product Design",answer:`**Content** pre-transcoded into many bitrates, pushed to **CDN edge caches** (Open Connect in ISPs) for low-latency streaming; clients do adaptive bitrate. Control plane: microservices (catalog, playback, recs) on cloud; recs via ML. **Challenges**: global delivery (edge + pre-positioning popular content), adaptive streaming, personalized recs, resilience (chaos), encoding efficiency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    250M subscribers; peak = large % streaming simultaneously
Bandwidth: video is 90%+ of internet egress in some regions → CDN critical
Encoding: each title → dozens of renditions (device/network)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
    (1) control plane
          ▼
   ┌──────────────┐
   │ Microservices│  catalog, playback, recs (ML) — on cloud
   └──────┬───────┘
    (2) get stream URL → nearest edge
          ▼
   ┌──────────────┐
   │ CDN Edge     │  Open Connect appliances (inside ISPs)
   │ (pre-cached  │
   │  popular)    │
   └──────┬───────┘
          ▼ adaptive bitrate
   ┌──────────────┐
   │  Playback    │
   └──────────────┘
Pre-position popular content at edges. Chaos engineering for resilience.
\`\`\``,tech:"hld",displayNumber:194,sortKey:197},{id:"hld-198",number:195,title:"Design Spotify.",question:"Design Spotify.",difficulty:"Hard",category:"Product Design",answer:`**Audio** transcoded to multiple bitrates → object storage → **CDN**; clients stream/prefetch + cache locally. Metadata DB; search (ES); recs (collaborative filtering + audio features, ML). Play events → Kafka (analytics/royalties). **Challenges**: low-latency streaming + offline caching, discovery/recs, playlists, royalty accounting (accurate play counting), search.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    500M MAU; 100M+ tracks × few bitrates
Streams:  billions/day → CDN offloads; each song ~3-5 MB
Play events: → Kafka for analytics + royalty counting

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │  prefetch + local cache (offline)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Music Service │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │ CDN  ││Metadata││ Recs (ML:    │
 │(audio││ + ES   ││ collab filter│
 │)     ││ search ││ + audio feat)│
 └──────┘└────────┘└──────────────┘
Play events → Kafka → analytics + royalties.
\`\`\``,tech:"hld",displayNumber:195,sortKey:198},{id:"hld-199",number:196,title:"Design Instagram.",question:"Design Instagram.",difficulty:"Hard",category:"Product Design",answer:`**Core**: photo/video upload → object storage + async processing → CDN; **feed** (fan-out-on-write for normal users, **fan-out-on-read** for celebrities), likes/comments, follow graph, stories, DMs. Sharded metadata; media via CDN. **Challenges**: feed generation (hybrid fan-out), celebrity problem, media scale, follow-graph queries, engagement counts.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    2B MAU, 500M DAU
Uploads:  100M posts/day; Reads: 50:1 → billions of feed loads/day
Feed:     precompute (fan-out-on-write) for most; on-read for celebs (>1M followers)
Storage:  media → object storage + CDN; metadata sharded

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  API Gateway │
   └──────┬───────┘
    ┌─────┴──────────┐
    ▼                ▼
 ┌──────────┐  ┌──────────────┐
 │Media     │  │ Feed Service │
 │Upload→S3 │  │ fan-out-write│──► followers' feed cache
 │→worker   │  │ (celebs: read)│
 │→CDN      │  └──────┬───────┘
 └──────────┘         ▼
              ┌──────────────┐
              │ Post DB +    │  follow graph (sharded)
              │ Feed Cache   │
              └──────────────┘
\`\`\``,tech:"hld",displayNumber:196,sortKey:199},{id:"hld-200",number:197,title:"Design Facebook.",question:"Design Facebook.",difficulty:"Hard",category:"Product Design",answer:`**Core**: news feed (ranked, hybrid fan-out), **social graph** (TAO-like), posts/comments/likes, media (CDN), messaging, notifications, groups. Feed ranking via ML. Massive read scale → memcached tier + sharded stores. **Challenges**: feed ranking/generation, graph queries (friend-of-friend), cache invalidation, fan-out (celebrities/pages), count consistency, privacy rules, multi-region.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    3B MAU; reads dominate (feeds, profiles) → memcached tier
Feed:     ML-ranked; hybrid fan-out
Storage:  social graph (TAO) + posts + media (CDN), sharded, multi-region

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Web/API tier │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Memcached tier│  (huge read cache)
   └──────┬───────┘
    ┌─────┴──────────┐
    ▼                ▼
 ┌──────────┐  ┌──────────────┐
 │Social    │  │ Feed Ranking │  (ML)
 │Graph (TAO│  │  Service     │
 │sharded)  │  └──────────────┘
 └──────────┘
Media → CDN. Privacy/visibility rules enforced per query.
\`\`\``,tech:"hld",displayNumber:197,sortKey:200},{id:"hld-201",number:198,title:"Design LinkedIn.",question:"Design LinkedIn.",difficulty:"Hard",category:"Product Design",answer:`**Core**: connection graph (degrees), profiles, feed, jobs, messaging, search/recs ("People You May Know" via graph). Graph engine + precompute for degree queries; feed fan-out; search via ES. **Challenges**: graph traversal at scale (2nd/3rd degree, PYMK), job matching, feed relevance, notification fan-out, recommendations (graph + ML).

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    1B members; connection graph = billions of edges
PYMK:     precompute friend-of-friend (expensive graph traversal)
Search:   jobs ↔ candidates matching → ES + ML ranking

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  API Gateway │
   └──────┬───────┘
    ┌─────┼──────────┬───────────┐
    ▼     ▼          ▼           ▼
 ┌──────┐┌────────┐┌──────┐┌──────────┐
 │Profile││Graph   ││Feed  ││Search(ES)│
 │DB    ││Engine  ││Service││+ Jobs    │
 │      ││(degrees││      ││ matching │
 │      ││PYMK)   ││      ││          │
 └──────┘└────────┘└──────┘└──────────┘
PYMK/recs precomputed (graph + ML).
\`\`\``,tech:"hld",displayNumber:198,sortKey:201},{id:"hld-202",number:199,title:"Design Twitter/X.",question:"Design Twitter/X.",difficulty:"Hard",category:"Product Design",answer:`**Core**: post tweets, **home timeline** (followed users' tweets), follow graph, likes/retweets, search/trends. Timeline: **fan-out-on-write** (push into followers' timeline cache) for most, **fan-out-on-read** for celebrities. Tweets sharded; timelines in Redis; trends via streaming. **Challenges**: timeline generation (hybrid fan-out), celebrity problem, real-time trends, search, read/write volume.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    400M MAU; 500M tweets/day = ~5,800/sec
Reads:    timeline loads ~300k/sec → Redis timeline cache
Celebs:   1 tweet → 100M followers → fan-out-on-read (don't push to 100M)
Storage:  tweets 500M/day × 300 B = ~150 GB/day (sharded)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Tweet Service │──► store tweet (sharded)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Fan-out Service│
   └──────┬───────┘
    ┌─────┴────────────┐
    ▼ (normal)         ▼ (celebrity)
 ┌────────────┐  ┌──────────────┐
 │Push to     │  │ Fan-out-on-  │
 │followers'  │  │ read (merge  │
 │timeline    │  │ at read time)│
 │cache(Redis)│  └──────────────┘
 └────────────┘
Trends → stream aggregation. Media → CDN.
\`\`\``,tech:"hld",displayNumber:199,sortKey:202},{id:"hld-203",number:200,title:"Design Reddit.",question:"Design Reddit.",difficulty:"Hard",category:"Product Design",answer:`**Core**: subreddits → posts → **nested comments**, voting, **ranking** (hot/top/new via score + time decay). Comment trees (materialized path), votes (distributed counters), precomputed ranked listings (cached). Feed = subscribed subreddits. **Challenges**: comment-tree storage/rendering, ranking (hot score/decay), vote counting, caching listings, moderation.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    50M DAU; reads >> writes (lurkers)
Reads:    ranked listings ~50k/sec → cache heavily + CDN
Votes:    hot post → thousands/sec → distributed counter
Comments: deep trees → materialized path for efficient fetch

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ CDN + Cache  │  (ranked listings — read-heavy)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Post Service │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │Post/ ││Vote    ││Ranking       │
 │Comment││Counter ││(hot/top/new, │
 │DB(tree││(distrib││ time decay,  │
 │path)  ││uted)   ││ precomputed) │
 └──────┘└────────┘└──────────────┘
Moderation service.
\`\`\``,tech:"hld",displayNumber:200,sortKey:203},{id:"hld-204",number:201,title:"Design TikTok.",question:"Design TikTok.",difficulty:"Hard",category:"Product Design",answer:`**Core**: short-video upload → transcode → CDN; **For You feed** driven by a heavy **recommendation engine** (watch time, likes, shares → ML), not a follow graph. Precompute candidates per user, real-time re-rank. Engagement events → Kafka → recsys. **Challenges**: recommendation quality (the product), real-time signals, video delivery, cold start, fast feedback loops.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    1B MAU; each watches 100s of short videos/day
Views:    billions/day → CDN; videos ~5-10 MB, pre-transcoded
Recsys:   real-time engagement (watch %) → continuous re-ranking

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
    ┌─────┴──────────┐
    ▼ upload         ▼ For You feed
 ┌──────────┐  ┌──────────────┐
 │Transcode │  │Recommendation│
 │→ CDN     │  │Engine (ML)   │
 └──────────┘  └──────┬───────┘
                      ▼
              ┌──────────────┐
              │Candidate Gen │  precomputed per user
              │+ real-time   │
              │re-rank       │
              └──────┬───────┘
   engagement events │
              ▼
         ┌──────────────┐
         │Kafka → recsys│  (watch time/likes/shares → features)
         └──────────────┘
\`\`\``,tech:"hld",displayNumber:201,sortKey:204},{id:"hld-205",number:202,title:"Design Pinterest.",question:"Design Pinterest.",difficulty:"Hard",category:"Product Design",answer:`**Core**: users save **pins** (image + link) to **boards**; discovery feed via interest + **visual similarity** recs; image-heavy. Pins/boards sharded; images → object storage + CDN; related-pins precomputed (embeddings). Visual search. **Challenges**: discovery/recs (interest + visual), image scale, visual search (embeddings), feed generation, pin→board→user graph.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    450M MAU; billions of pins (image + link)
Reads:    discovery feed heavy → CDN + precomputed related pins
Storage:  images → object storage + CDN; embeddings for visual similarity

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Pin Service │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │Images││Pin/    ││Recommendation│
 │→CDN  ││Board DB││(interest +   │
 │      ││(sharded││ visual sim,  │
 │      ││)       ││ embeddings)  │
 └──────┘└────────┘└──────────────┘
Visual search via image embeddings.
\`\`\``,tech:"hld",displayNumber:202,sortKey:205},{id:"hld-206",number:203,title:"Design Snapchat.",question:"Design Snapchat.",difficulty:"Hard",category:"Product Design",answer:`**Core**: **ephemeral** photo/video messages (deleted after view), stories (24h), chat. Media → object storage (short TTL) + CDN, deleted after view/expiry. Track view state; server deletes on view. **Challenges**: ephemerality (reliable deletion), real-time delivery, AR filters (client-side), stories fan-out, privacy (no persistence, screenshot detection).

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    400M DAU; billions of snaps/day (mostly ephemeral)
Media:    stored briefly (short TTL) → deleted after view/expiry
Storage:  low long-term (privacy-first); high throughput short-lived

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │  (AR filters applied client-side)
   └──────┬───────┘
      upload snap
          ▼
   ┌──────────────┐
   │Object Storage│  (short TTL)
   └──────┬───────┘
          ▼ via CDN
   ┌──────────────┐
   │  Recipient   │  view → triggers deletion
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Delete Service│  remove after view/expiry (privacy)
   └──────────────┘
Stories (24h) fan-out; screenshot detection.
\`\`\``,tech:"hld",displayNumber:203,sortKey:206},{id:"hld-207",number:204,title:"Design Google Drive.",question:"Design Google Drive.",difficulty:"Hard",category:"Product Design",answer:`**Core**: store files/folders, sync across devices, share with permissions, versioning. Files chunked → object storage (dedup via content hash); metadata (hierarchy, permissions, versions) in DB; **sync service** pushes chunk-level deltas. **Challenges**: efficient sync (chunk delta + dedup), conflict resolution, sharing/permissions (ACLs), versioning, large/resumable uploads, offline.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    1B+; avg 15 GB/user → exabytes total
Sync:     only changed CHUNKS transferred (dedup by content hash)
Storage:  object storage + block dedup; metadata sharded by user

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Client (multi │
   │  device)     │
   └──────┬───────┘
    hash chunks → sync deltas
          ▼
   ┌──────────────┐
   │ Sync Service │──► push deltas to other devices
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Object    ││ Metadata DB  │
 │Storage   ││ tree, perms, │
 │(chunks,  ││ versions     │
 │ dedup)   ││(sharded)     │
 └──────────┘└──────────────┘
Sharing via ACLs; versioning history. CDN for downloads.
\`\`\``,tech:"hld",displayNumber:204,sortKey:207},{id:"hld-208",number:205,title:"Design Dropbox.",question:"Design Dropbox.",difficulty:"Hard",category:"Product Design",answer:`Like Google Drive, emphasizing **file sync**: client watches the folder, computes chunk hashes, syncs only **changed blocks** (block-level dedup across users). Metadata service tracks file→chunks + versions; notification service tells devices to pull changes. **Challenges**: block-level sync + dedup, conflict resolution (conflicted copies), change detection (watchers + hashing), large files, cross-device consistency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    700M; block-level dedup saves huge storage
Sync:     watch folder → hash 4MB blocks → upload only changed blocks
Storage:  blocks → object storage; metadata sharded

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Client Watcher│  hash blocks, detect changes
   └──────┬───────┘
    upload changed blocks
          ▼
   ┌──────────────┐
   │ Sync/Metadata│  file → blocks + versions
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Block     ││Notification  │──► other devices pull deltas
 │Storage   ││Service       │
 │(dedup)   │└──────────────┘
 └──────────┘
Conflict → conflicted copy. Large files chunked.
\`\`\``,tech:"hld",displayNumber:205,sortKey:208},{id:"hld-209",number:206,title:"Design Google Docs (collaborative editing).",question:"Design Google Docs (collaborative editing).",difficulty:"Hard",category:"Product Design",answer:`**Core**: real-time **collaborative editing** — concurrent edits merged consistently via **Operational Transformation (OT)** or **CRDTs**; clients send operations to a doc server (WebSocket) that orders/transforms and broadcasts. Persist doc + op history (undo/versions). Shard by doc; presence/cursors. **Challenges**: concurrent-edit convergence (OT/CRDT — the crux), low-latency sync, presence, offline edits, versioning.

\`\`\`text
── BACK-OF-ENVELOPE ──
Docs:     billions; typically small concurrent editor count per doc
Ops:      each keystroke = an op → high op rate per active doc
Model:    OT/CRDT to converge concurrent edits; shard by doc (1 authoritative server)

── ARCHITECTURE (top → bottom) ──
   ┌──────────┐   ┌──────────┐
   │ Editor A │   │ Editor B │
   └────┬─────┘   └────┬─────┘
    WebSocket       WebSocket
        ▼               ▼
   ┌────────────────────────┐
   │  Doc Server (per doc)  │  order + transform ops (OT/CRDT)
   └──────────┬─────────────┘
       broadcast merged ops
              ▼
   ┌────────────────────────┐
   │ Doc Store + Op History │  (undo, versions)
   └────────────────────────┘
Presence + cursors. Offline edits reconciled via OT/CRDT.
\`\`\``,tech:"hld",displayNumber:206,sortKey:209},{id:"hld-210",number:207,title:"Design Google Calendar.",question:"Design Google Calendar.",difficulty:"Hard",category:"Product Design",answer:`**Core**: events with recurrence (store **RRULE**, expand on read), calendars per user, invitations/RSVP (fan-out to attendees), reminders (scheduled jobs), free/busy, sync. Shard by user; cache month views. **Challenges**: recurring events + exceptions, **time zones/DST**, cross-user invites, reminders at scale (distributed scheduler), free/busy queries, delta sync.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    500M; reads (views) >> writes → cache month views
Storage:  store RRULE not expansions → compact; shard by user
Reminders: millions fire/day → distributed scheduler

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Client (delta │
   │  sync)       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Calendar Service│
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │Cache ││Event DB││ Reminder     │
 │(views││(RRULE, ││ Scheduler    │
 │)     ││sharded)││(distributed) │
 └──────┘└───┬────┘└──────────────┘
    invite fan-out │
             ▼
      ┌──────────────┐
      │Attendees' cal│
      └──────────────┘
\`\`\``,tech:"hld",displayNumber:207,sortKey:210},{id:"hld-211",number:208,title:"Design Gmail.",question:"Design Gmail.",difficulty:"Hard",category:"Product Design",answer:`**Core**: receive (SMTP) → spam/virus filter → per-user mailbox; send (SMTP + queue/retries); **search** (index every email), labels/threads, attachments (object storage). Push notifications. Sharded per user. **Challenges**: reliable delivery (SMTP retries, deliverability), spam filtering (ML), massive storage + search, threading, attachments, mailbox consistency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    1.5B; avg 15 GB mailbox → exabytes total
Volume:   hundreds of billions of emails/day (with spam)
Search:   index every email → huge search infrastructure (per user)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Inbound SMTP  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Spam/Virus    │  (ML filter)
   │Filter        │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Mailbox Store│  (sharded per user) + search index
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Push Notify   │  + labels/threads
   └──────────────┘
Outbound: SMTP + queue + retries. Attachments → object storage.
\`\`\``,tech:"hld",displayNumber:208,sortKey:211},{id:"hld-212",number:209,title:"Design Google Maps.",question:"Design Google Maps.",difficulty:"Hard",category:"Product Design",answer:`**Core**: map **tiles** (pre-rendered per zoom → CDN), **geocoding**, **routing** (road graph → A*/contraction hierarchies), **real-time traffic** (aggregate user GPS → adjust edge weights). Spatial index (geohash/quadtree). **Challenges**: routing at scale (precompute + live traffic), spatial indexing, tile serving, ETA accuracy, continuous updates.

\`\`\`text
── BACK-OF-ENVELOPE ──
Tiles:    pre-rendered per zoom level → CDN (mostly static)
Routing:  billions of route requests/day → precomputed structures
Traffic:  aggregate live GPS from millions of devices → edge weights

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
    ┌─────┼──────────┬───────────┐
    ▼     ▼          ▼           ▼
 ┌──────┐┌────────┐┌──────┐┌──────────┐
 │Tiles ││Geocode ││Route ││Traffic   │
 │(CDN) ││(addr↔  ││Engine││(GPS aggr │
 │      ││ coord) ││A*/CH ││→ weights)│
 │      ││        ││+graph││          │
 └──────┘└────────┘└──────┘└──────────┘
Spatial index (geohash/quadtree) for nearby queries.
\`\`\``,tech:"hld",displayNumber:209,sortKey:212},{id:"hld-213",number:210,title:"Design Uber.",question:"Design Uber.",difficulty:"Hard",category:"Product Design",answer:`**Core**: riders request rides; **match** to nearby drivers via **geospatial indexing** (drivers publish location → geohash/H3/QuadTree); matching service finds best driver, tracks trip (WebSocket), computes fare, handles payment. Events (location, trip state) → Kafka. **Challenges**: real-time geospatial matching, high-volume location updates, ETA/surge pricing, trip state machine, payments, driver-availability consistency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Drivers:  5M active; each sends location every 4s = ~1.25M updates/sec
Rides:    20M/day = ~230/sec; matching = nearby-driver query
Storage:  latest location (in-memory, geo-indexed) + trip history (Kafka)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐        ┌──────────────┐
   │   Rider      │        │   Driver     │
   └──────┬───────┘        └──────┬───────┘
     request ride          location every 4s
          ▼                       ▼
   ┌──────────────┐        ┌──────────────┐
   │Matching Svc  │◄───────│Location Svc  │
   │              │ query   │(geo index    │
   │              │ nearby  │ geohash/H3)  │
   └──────┬───────┘        └──────────────┘
     matched → trip
          ▼
   ┌──────────────┐
   │Trip Service  │  state machine + fare + surge
   └──────┬───────┘
          ▼ WebSocket tracking + Kafka events
   ┌──────────────┐
   │Payment       │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:210,sortKey:213},{id:"hld-214",number:211,title:"Design Ola.",question:"Design Ola.",difficulty:"Hard",category:"Product Design",answer:`Like Uber: geospatial driver indexing + matching, real-time tracking, dynamic pricing, payments, plus multiple vehicle types (auto/cab/bike), cash payments, local regulations. **Challenges**: nearby matching, location-update volume, ETA/surge, trip lifecycle, payments (cash reconciliation), multi-modal vehicles.

\`\`\`text
── BACK-OF-ENVELOPE ──
Same core as Uber: millions of drivers × frequent location updates
Extra: multiple vehicle types; cash payments need reconciliation

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐        ┌──────────────┐
   │   Rider      │        │   Driver     │
   └──────┬───────┘        └──────┬───────┘
     request              location updates
          ▼                       ▼
   ┌──────────────┐        ┌──────────────┐
   │Matching Svc  │◄───────│Location Svc  │
   │(by vehicle   │        │(geo index)   │
   │ type)        │        └──────────────┘
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Trip + Pricing│  (surge; multi-modal)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment (card │  + cash reconciliation
   │ / UPI / cash)│
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:211,sortKey:214},{id:"hld-215",number:212,title:"Design Airbnb.",question:"Design Airbnb.",difficulty:"Hard",category:"Product Design",answer:`**Core**: hosts list properties (search by location/dates/filters — geo + availability); guests **book** (availability check + reservation, transactional — no double-booking); payments (split host/platform); reviews; messaging. Search: geo-index + availability + ranking (ES). Booking locks the date range. **Challenges**: search (geo + availability + ranking), booking concurrency, pricing/availability calendars, payments (escrow/split), reviews, messaging.

\`\`\`text
── BACK-OF-ENVELOPE ──
Listings: ~7M; searches heavy (geo + dates) → cache + geo-index
Bookings: ~2M/day = ~25/sec (write-critical, no double-book)
Storage:  listings + calendars + bookings → sharded; reviews

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Guest      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Search (geo + │  cache + geo-index + availability + ranking (ES)
   │ dates)       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Booking Service│  reserve (TRANSACTIONAL: lock date range)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Listing/  ││Payment (split│
 │Calendar  ││ host/platform│
 │DB        ││ escrow)      │
 └──────────┘└──────────────┘
Reviews + host/guest messaging.
\`\`\``,tech:"hld",displayNumber:212,sortKey:215},{id:"hld-216",number:213,title:"Design Amazon (e-commerce).",question:"Design Amazon (e-commerce).",difficulty:"Hard",category:"Product Design",answer:`**Core**: catalog + **search**, product pages, cart, **checkout/orders**, **inventory** (reserve on order — no overselling), payments, fulfillment — microservices. Search (ES), catalog (cached + CDN), cart (Redis), orders (transactional), recs (ML). Async via Kafka. **Challenges**: inventory consistency, search/recs, traffic spikes, payment idempotency, order orchestration (Saga), catalog scale.

\`\`\`text
── BACK-OF-ENVELOPE ──
Catalog:  billions of products; reads dominate → cache + CDN
Orders:   millions/day; peak events (Prime Day) 10× → autoscale + queue
Inventory: reserve to avoid oversell (strong consistency)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  API Gateway │
   └──────┬───────┘
    ┌─────┼──────────┬───────────┐
    ▼     ▼          ▼           ▼
 ┌──────┐┌──────┐┌────────┐┌──────────┐
 │Catalog││Cart  ││Order   ││Inventory │
 │+Search││(Redis││Service ││(reserve, │
 │(ES,CDN││)     ││(TX)    ││no oversell)│
 └──────┘└──────┘└───┬────┘└──────────┘
                     ▼ Kafka events
              ┌──────────────┐
              │Payment +     │  Saga orchestration
              │Fulfillment   │
              └──────────────┘
\`\`\``,tech:"hld",displayNumber:213,sortKey:216},{id:"hld-217",number:214,title:"Design Flipkart.",question:"Design Flipkart.",difficulty:"Hard",category:"Product Design",answer:`Like Amazon, emphasizing **high-traffic sale events** (Big Billion Days): aggressive caching, queue-based load leveling, **virtual waiting room** for flash sales. Inventory reservation; payments (UPI/cards) idempotent + retries; recs. **Challenges**: flash-sale spikes (waiting room, rate limiting), inventory consistency, payment reliability, search, fulfillment.

\`\`\`text
── BACK-OF-ENVELOPE ──
Normal:   millions of orders/day; SALE = 10-50× spike in minutes
Flash sale: limited stock + millions of buyers → virtual waiting room
Inventory: strong consistency (no oversell during flash sale)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Virtual Waiting│  (flash sale: admit N at a time)
   │Room + RateLimit│
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  API Gateway │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │Catalog││Order   ││Inventory     │
 │(cache)││(queue- ││(reserve, no  │
 │       ││leveled)││ oversell)    │
 └──────┘└───┬────┘└──────────────┘
             ▼
      ┌──────────────┐
      │Payment (UPI/ │  idempotent + retries
      │ card)        │
      └──────────────┘
\`\`\``,tech:"hld",displayNumber:214,sortKey:217},{id:"hld-218",number:215,title:"Design an e-commerce platform (generic).",question:"Design an e-commerce platform (generic).",difficulty:"Hard",category:"Product Design",answer:`Building blocks: **catalog + search** (ES, cached/CDN), **cart** (Redis), **order** (transactional), **inventory** (reserve — no oversell), **payment** (idempotent + retries), **fulfillment**, notifications, recs. Async via Kafka; Saga for the order flow. **Challenges**: inventory consistency, order orchestration (Saga + compensations), payment idempotency, search/recs, spikes, checkout reliability.

\`\`\`text
── BACK-OF-ENVELOPE ──
Reads (browse) >> writes (order): ~100:1 → cache + CDN
Orders:  size for peak (events 10×) → autoscale + queue leveling
Inventory: reserve → strong consistency (no oversell)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  API Gateway │
   └──────┬───────┘
    ┌─────┼──────────┬───────────┐
    ▼     ▼          ▼           ▼
 ┌──────┐┌──────┐┌────────┐┌──────────┐
 │Catalog││Cart  ││Order   ││Inventory │
 │+Search││(Redis││(TX,Saga││(reserve) │
 │       ││)     ││)       ││          │
 └──────┘└──────┘└───┬────┘└──────────┘
                     ▼ Kafka
              ┌──────────────┐
              │Payment(idempo│
              │)+Fulfillment │
              └──────────────┘
\`\`\``,tech:"hld",displayNumber:215,sortKey:218},{id:"hld-219",number:216,title:"Design an online food-delivery platform.",question:"Design an online food-delivery platform.",difficulty:"Hard",category:"Product Design",answer:`**Core**: browse restaurants (geo + availability), place order, **assign a delivery partner** (geospatial matching), track delivery (WebSocket), payments. Three-sided (customer/restaurant/partner); order state machine; events → Kafka; ETA estimation. **Challenges**: real-time partner matching + tracking, ETA, order state orchestration, surge/peak, payments, restaurant availability.

\`\`\`text
── BACK-OF-ENVELOPE ──
Orders:   10M/day = ~115/sec (peak dinner 3-5×)
Partners: 500k, frequent location updates → geo index
Tracking: live delivery location → WebSocket to customer

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Customer   │
   └──────┬───────┘
     browse + order
          ▼
   ┌──────────────┐
   │Order Service │  (state machine: placed→prep→pickup→delivered)
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Partner Match │◄───────│Partner Location│ (geo index)
   │(geospatial)  │        └──────────────┘
   └──────┬───────┘
          ▼ WebSocket tracking + Kafka events
   ┌──────────────┐
   │Payment + ETA │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:216,sortKey:219},{id:"hld-220",number:217,title:"Design Swiggy/Zomato.",question:"Design Swiggy/Zomato.",difficulty:"Hard",category:"Product Design",answer:`Food-delivery platform tuned for **partner matching + order batching** (assign nearby orders to one rider), restaurant discovery (geo + search + ratings), live tracking, dynamic ETA, payments (incl. COD), ratings. **Challenges**: matching + batching, live tracking, ETA (prep + travel), peak surge, three-sided consistency, payments.

\`\`\`text
── BACK-OF-ENVELOPE ──
Orders:   millions/day; dinner peak = biggest spike
Batching: assign multiple nearby orders to one rider (efficiency)
Discovery: geo + search + ratings → cache + geo-index

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Customer   │
   └──────┬───────┘
     discovery (geo/search/ratings) → order
          ▼
   ┌──────────────┐
   │Order Service │
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Partner Assign│◄───────│Partner Location│
   │+ Batching    │        │(geo index)   │
   └──────┬───────┘        └──────────────┘
          ▼
   ┌──────────────┐
   │Tracking + ETA│  (prep + travel time)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment (card/│
   │ UPI / COD)   │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:217,sortKey:220},{id:"hld-221",number:218,title:"Design an online grocery platform.",question:"Design an online grocery platform.",difficulty:"Hard",category:"Product Design",answer:`Like e-commerce + food delivery but with **perishable inventory**, **real-time per-store stock**, **delivery-slot booking** (limited capacity → concurrency control), and **substitutions**. Browse by store/location, reserve inventory, book a slot, pick/pack, deliver. **Challenges**: real-time inventory (oversell), slot concurrency, substitutions, route optimization, perishability.

\`\`\`text
── BACK-OF-ENVELOPE ──
Stores:   thousands, each with real-time stock (perishable)
Orders:   millions/day; delivery slots = limited capacity resource
Slots:    booking a slot = concurrency-controlled (limited seats)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Customer   │  (browse by store/location)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Order Service │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────────┐┌────────┐┌──────────────┐
 │Per-store ││Slot    ││Substitution  │
 │Inventory ││Booking ││(OOS items)   │
 │(reserve, ││(limited││              │
 │ real-time││ capacity│              │
 │)         ││concur.)│              │
 └──────────┘└────────┘└──────────────┘
Pick/pack → route-optimized delivery.
\`\`\``,tech:"hld",displayNumber:218,sortKey:221},{id:"hld-222",number:219,title:"Design an online travel booking system.",question:"Design an online travel booking system.",difficulty:"Hard",category:"Product Design",answer:`**Core**: search flights/hotels (aggregate from many **third-party suppliers/GDS** — fan-out + cache), show availability + price, **book** (reserve with supplier, transactional, handle timeouts), pay, confirm. Supplier-heavy. **Challenges**: aggregating slow/unreliable suppliers (timeouts, caching), price/availability freshness vs cache, booking consistency (hold → confirm), payment + failure recovery, idempotency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Search:   fan-out to many suppliers per query → cache (short TTL, prices change)
Bookings: lower volume, but write-critical (no double-book, unknown-outcome handling)
Suppliers: slow/unreliable → timeouts + caching essential

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
     search
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Search Aggreg.│──fan-out►│ Suppliers/GDS│
   │(cache short  │◄────────│ (flights,    │
   │ TTL)         │         │  hotels)     │
   └──────┬───────┘        └──────────────┘
     book
          ▼
   ┌──────────────┐
   │Booking (hold │  transactional; handle timeouts (query status)
   │ → confirm)   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment + fail│  recovery + idempotency
   │ handling     │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:219,sortKey:222},{id:"hld-223",number:220,title:"Design an airline reservation system.",question:"Design an airline reservation system.",difficulty:"Hard",category:"Product Design",answer:`**Core**: search flights (routes, schedules, availability), **seat inventory** (per flight/fare class), **booking** (hold seat → transactional confirm — no oversell), pricing (dynamic fare classes), payments, ticketing, check-in. Strong seat consistency. **Challenges**: seat-inventory consistency (the crux — transactional holds), overbooking policy, dynamic pricing, PNR management, cancellations, GDS integration.

\`\`\`text
── BACK-OF-ENVELOPE ──
Search:   read-heavy (schedules) → cache
Bookings: write-critical; seat inventory must be strongly consistent
Seats:    per flight × fare class; hold → confirm to avoid oversell

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │
   └──────┬───────┘
     search flights
          ▼
   ┌──────────────┐
   │Search (cached│
   │ schedules)   │
   └──────┬───────┘
     select seats
          ▼
   ┌──────────────┐
   │Booking Service│  hold seat (lock) → transactional confirm
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Seat      ││Dynamic Pricing│ (fare classes)
 │Inventory ││              │
 │(strong   │└──────────────┘
 │ consist.)│
 └──────────┘
Pay → ticket (PNR) → check-in. GDS integration.
\`\`\``,tech:"hld",displayNumber:220,sortKey:223},{id:"hld-224",number:221,title:"Design a payment gateway.",question:"Design a payment gateway.",difficulty:"Hard",category:"FinTech & Payments",answer:`A gateway sits between merchants and processors/banks. **Flow**: merchant sends a payment request → gateway validates + **tokenizes** the card (PCI) → routes to the acquiring processor → returns auth → captures/settles later. Store transactions (idempotent by payment id), retries, merchant webhooks, reconciliation. **Challenges**: idempotency (no double charge), PCI/tokenization, processor failover/routing, auth/capture/settle consistency, fraud, reliable webhooks.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   100M transactions/day = ~1,160/sec (peak ~5k on sale days)
Latency:  auth must be <2s → low-latency path
Storage:  100M × 1 KB/day = ~100 GB/day immutable txn log (retain years)
Consistency: STRONG (no double charge) + idempotency keys

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Merchant    │
   └──────┬───────┘
     payment req (+ Idempotency-Key)
          ▼
   ┌──────────────┐
   │Payment Gateway│  validate + TOKENIZE card (PCI)
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Processor     │───────►│ Fraud Check  │
   │Router        │        └──────────────┘
   │(failover)    │
   └──────┬───────┘
     auth → capture → settle
          ▼
   ┌──────────────┐
   │Txn Store     │  immutable, idempotent → merchant webhooks
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:221,sortKey:224},{id:"hld-225",number:222,title:"Design a payment processing system.",question:"Design a payment processing system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Orchestrate the lifecycle: **authorization** (hold) → **capture** → **settlement**, with a durable state machine per transaction and a **double-entry ledger**. Idempotency keys for retries; Saga for multi-step; async events (Kafka) for downstream. **Challenges**: exactly-once effects, ledger/balance consistency (transactions), partial failures + compensation, bank reconciliation, fraud, immutable audit.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   millions of payments/day; each = multi-step (auth→capture→settle)
Ledger:   every movement = 2 entries (double-entry); immutable, append-only
Storage:  ledger grows forever → partition by account/time + archive

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Payment Req │  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment Orchestr.│  state machine: authorize → capture → settle
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Double-   ││Kafka events  │──► notifications, reconciliation
 │Entry     ││              │
 │Ledger    │└──────────────┘
 │(immutable│
 │,strong)  │
 └──────────┘
Saga compensations on partial failure.
\`\`\``,tech:"hld",displayNumber:222,sortKey:225},{id:"hld-226",number:223,title:"Design a wallet system.",question:"Design a wallet system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Holds a user's balance; top-up, pay, transfer, withdraw. Model via an **immutable ledger** (balance = sum, or a maintained balance row updated **atomically** with the ledger entry). Ensure **no negative balance** (check + lock the row). Idempotency keys prevent double-processing. Shard by user. **Challenges**: strong consistency (no double-spend/negative), atomic balance+ledger, idempotency, auditability, hot-account concurrency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    100M wallets; millions of transactions/day
Balance:  atomic update WITH ledger entry (same DB tx); lock row (no negative)
Storage:  ledger append-only → shard by user; balance snapshot per wallet

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   Client     │  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Wallet Service│
   └──────┬───────┘
     transaction (lock wallet row)
          ▼
   ┌──────────────┐
   │  Wallet DB   │  (sharded by user)
   │  Balance ◄───┼── lock row: reject if would go negative
   │  Ledger(append,│  balance + ledger updated in SAME tx
   │  immutable)  │
   └──────────────┘
Ops: top-up, pay, transfer, withdraw — all idempotent + atomic.
\`\`\``,tech:"hld",displayNumber:223,sortKey:226},{id:"hld-227",number:224,title:"Design PayPal.",question:"Design PayPal.",difficulty:"Hard",category:"FinTech & Payments",answer:`Digital wallet + payment network: balances (ledger), send/receive, link banks/cards, merchant payments, FX, fraud. Money movement is **transactional + double-entry ledgered**, idempotent, async events. Strong balance consistency; risk/fraud engine screens transactions. Shard by user. **Challenges**: consistency (no double-spend), real-time fraud, KYC/AML compliance, multi-currency, reconciliation, disputes/refunds.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    400M+; billions of transactions/yr
Money:    transfers = transactional double-entry; strong consistency
Fraud:    real-time scoring on every transaction (<100ms)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │   User       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment Service│  (+ Idempotency-Key)
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────────┐
 │Ledger││Fraud   ││Funding       │
 │(double││Engine  ││Sources (bank/│
 │-entry,││(real-  ││ card) + FX   │
 │strong)││ time)  ││              │
 └──────┘└────────┘└──────────────┘
KYC/AML compliance; reconciliation; disputes/refunds.
\`\`\``,tech:"hld",displayNumber:224,sortKey:227},{id:"hld-228",number:225,title:"Design Stripe-like payment infrastructure.",question:"Design Stripe-like payment infrastructure.",difficulty:"Hard",category:"FinTech & Payments",answer:`Developer-first API: create PaymentIntents, tokenize cards (PCI), route to processors, subscriptions/billing, **webhooks** to merchants. Everything **idempotent** (idempotency keys), a **ledger** for money movement, reliable webhook delivery (retries + signing). Multi-region HA. **Challenges**: API idempotency, reliable webhooks (retries/DLQ/signing), PCI/tokenization, processor routing/failover, ledger consistency, subscriptions/proration, API reliability/observability.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   billions of API calls/yr; payments must never drop (HA critical)
Idempotency: EVERY mutating API call carries an idempotency key
Webhooks: reliable delivery (retries + HMAC signing) to millions of merchants

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Merchant App│
   └──────┬───────┘
     API call (+ Idempotency-Key)
          ▼
   ┌──────────────┐
   │Payment API   │  PaymentIntent → tokenize → processor
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Ledger    ││Webhook       │──► merchant (retries + HMAC sign)
 │          ││Delivery      │
 └──────────┘└──────────────┘
Subscriptions/billing; processor failover; multi-region HA.
\`\`\``,tech:"hld",displayNumber:225,sortKey:228},{id:"hld-229",number:226,title:"Design UPI-like payment architecture.",question:"Design UPI-like payment architecture.",difficulty:"Hard",category:"FinTech & Payments",answer:`Real-time bank-to-bank rail: a central **switch (NPCI)** routes payment requests between payer and payee banks (PSPs) using **VPAs** (virtual addresses). Flow: initiate → switch → debit payer + credit payee → confirm — in seconds. Idempotency + reconciliation essential. **Challenges**: real-time cross-bank consistency (switch coordinates), idempotency (no double debit), high throughput + low latency, failure handling (debited-not-credited → reversal), security (2FA/PIN).

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   billions of transactions/month (India-scale) → very high throughput
Latency:  end-to-end in seconds (real-time)
Consistency: cross-bank; failure → auto-reversal via reconciliation

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Payer App(VPA)│
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  PSP         │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Central Switch│  (NPCI) — routes by VPA
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Payer Bank││Payee Bank    │
 │ DEBIT    ││ CREDIT       │
 └──────────┘└──────────────┘
Debited-not-credited → reconciliation → auto-reversal. 2FA/PIN.
\`\`\``,tech:"hld",displayNumber:226,sortKey:229},{id:"hld-230",number:227,title:"Design a banking transaction system.",question:"Design a banking transaction system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Core banking: accounts + balances backed by a **double-entry ledger** (equal debits/credits) with **ACID** guarantees. Strongly consistent, idempotent, fully auditable (immutable ledger). Shard by account; cross-account transfers need transactional consistency. **Challenges**: strong consistency (ACID, no double-spend), atomic cross-account transfers (2PC/Saga if sharded), idempotency, auditability, compliance, reconciliation.

\`\`\`text
── BACK-OF-ENVELOPE ──
Accounts: 100M+; millions of transactions/day
Ledger:   double-entry, ACID, immutable; balances derivable
Consistency: STRONG (correctness > availability) — CP system

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transaction Req│  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Core Banking  │
   └──────┬───────┘
     atomic debit + credit (ACID tx)
          ▼
   ┌──────────────┐
   │Double-Entry  │  (immutable, sharded by account)
   │Ledger        │
   │ debit  = credit (always balances)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Audit + Recon.│  (compliance)
   └──────────────┘
Cross-shard transfer → Saga/2PC. No double-spend (row locks).
\`\`\``,tech:"hld",displayNumber:227,sortKey:230},{id:"hld-231",number:228,title:"Design a money-transfer system.",question:"Design a money-transfer system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Transfer A→B atomically: debit A + credit B as **one logical transaction** with double-entry entries. Same DB → single transaction; **across shards/banks** → **Saga** (debit A → credit B → reverse A on failure) or 2PC, with idempotency + reconciliation. Never leave money in limbo. **Challenges**: atomicity across accounts/shards (Saga + compensation), idempotency, no negative balance (locks), consistency (debited-not-credited), auditability.

\`\`\`text
── BACK-OF-ENVELOPE ──
Transfers: millions/day; each = debit + credit (atomic)
Same shard: single ACID tx. Cross-shard: Saga (debit → credit → reverse on fail)
Consistency: never lose/duplicate money → idempotency + reconciliation

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transfer Req  │  (A → B, + Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Transfer Service│
   └──────┬───────┘
    ┌─────┴─────────────┐
    ▼ same shard        ▼ cross-shard
 ┌────────────┐   ┌──────────────┐
 │Single ACID │   │ Saga:        │
 │tx: debit A │   │ debit A →    │
 │+ credit B  │   │ credit B →   │
 │(double-    │   │ reverse A on │
 │ entry)     │   │ failure      │
 └────────────┘   └──────────────┘
Row locks → no negative. Reconciliation for stragglers.
\`\`\``,tech:"hld",displayNumber:228,sortKey:231},{id:"hld-232",number:229,title:"Design an international remittance system.",question:"Design an international remittance system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Cross-border with **currency conversion**: sender pays in currency A → **FX** (locked rate) → compliance (KYC/AML/sanctions) → payout in currency B via local rails/partners; multi-day settlement in some corridors. Saga across partners + idempotency + reconciliation; ledger tracks in-transit states. **Challenges**: FX (rate lock, spread), per-country compliance, multi-partner integration (async/unreliable), settlement timing, cross-partner consistency, refund on payout failure.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   millions of remittances/day across corridors
Settlement: seconds to days (corridor-dependent) → ledger "in-transit" states
Compliance: KYC/AML/sanctions screening per transaction

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Sender (cur A)│
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │FX Conversion │  (locked rate)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Compliance    │  (KYC/AML/sanctions)
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Payout via    │───────►│Local Partner │ (cur B)
   │Saga          │        └──────────────┘
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Ledger (in-   │  reconciliation; refund on payout failure
   │ transit)     │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:229,sortKey:232},{id:"hld-233",number:230,title:"Design a payment reconciliation system.",question:"Design a payment reconciliation system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Ensure internal records match external ones (bank/processor statements). Ingest both sides (internal ledger + provider files/APIs), **match** by id/amount/date, flag **discrepancies** (missing, duplicate, mismatch) for investigation/auto-correction. Batch or streaming. **Challenges**: matching (fuzzy, timing differences), discrepancy handling, scale (millions of txns), idempotent reprocessing, auditability. Automate matching; human-review exceptions.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   match millions of internal txns vs external statements daily
Timing:   settlement lands next day → time-window matching
Output:   matched (auto) vs discrepancies (missing/dup/mismatch → review)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐        ┌──────────────┐
   │Internal      │        │External      │
   │Ledger        │        │Statements    │
   └──────┬───────┘        └──────┬───────┘
          ▼                       ▼
   ┌────────────────────────────────┐
   │  Reconciliation Engine         │  match by (id, amount, date)
   └──────┬─────────────────────────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Matched   ││Discrepancies │──► investigate / auto-correct / alert
 │(auto)    ││(missing/dup/ │
 │          ││ mismatch)    │
 └──────────┘└──────────────┘
\`\`\``,tech:"hld",displayNumber:230,sortKey:233},{id:"hld-234",number:231,title:"Design payment retries.",question:"Design payment retries.",difficulty:"Intermediate",category:"FinTech & Payments",answer:`Retry **transient** failures but never blindly (double-charge risk). Use **idempotency keys** so retrying is safe, **exponential backoff + jitter**, bounded attempts, and only retry retryable codes (not "declined"). For a timeout with unknown outcome, **query status** before retrying. **Challenges**: idempotency (the crux), classifying retryable vs terminal, unknown outcomes (status check), retry storms (backoff), give-up (mark failed + notify).

\`\`\`text
── BACK-OF-ENVELOPE ──
Retryable: network/timeout/5xx transient errors only
Terminal:  card declined, insufficient funds → DON'T retry
Unknown:   timeout → query payment status BEFORE retrying (no double charge)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Payment Attempt│  (Idempotency-Key)
   └──────┬───────┘
     fail?
          ▼
   ┌──────────────┐
   │Classify Error│
   └──────┬───────┘
    ┌─────┴─────────────┐
    ▼ transient         ▼ terminal / unknown
 ┌────────────┐   ┌──────────────┐
 │Retry (exp  │   │ Query status │ (unknown) / stop (terminal)
 │backoff +   │   │ before retry │
 │jitter,     │   └──────────────┘
 │bounded)    │
 └────────────┘
Exhausted → mark failed + notify.
\`\`\``,tech:"hld",displayNumber:231,sortKey:234},{id:"hld-235",number:232,title:"Design payment idempotency.",question:"Design payment idempotency.",difficulty:"Hard",category:"FinTech & Payments",answer:`Client sends a unique **Idempotency-Key** per attempt. Server: in a transaction, insert the key (unique constraint); if new → process + store result; if exists → **return stored result** without re-charging. Handle in-progress duplicates (lock/"processing"). Key TTL; align with provider idempotency. **Challenges**: atomic key-insert + charge, concurrent duplicates (locking), unknown-outcome timeouts, key scoping/TTL, provider alignment.

\`\`\`text
── BACK-OF-ENVELOPE ──
Key:      client-supplied unique id per payment attempt
Store:    idempotency_key (unique constraint) + result; TTL (e.g. 24h)
Guarantee: retry with same key → NEVER double charge

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  (Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Payment Service│
   └──────┬───────┘
     INSERT key (unique, transactional)
          ▼
   ┌──────────────┐
   │Idempotency   │
   │Store         │
   │ new? → charge + store result
   │ exists? → return stored result (no re-charge)
   │ in-progress? → lock / "processing"
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:232,sortKey:235},{id:"hld-236",number:233,title:"Design transaction history.",question:"Design transaction history.",difficulty:"Intermediate",category:"FinTech & Payments",answer:`Store an **immutable, append-only** record of every transaction (id, amount, type, parties, timestamp, status), queryable by user with pagination + filters. Read-heavy, never mutated → append log / wide-column, indexed by (user, time). Derived from the ledger. **Challenges**: high write volume, efficient user time-range queries (pagination), immutability/audit, retention/archival, consistency with the ledger.

\`\`\`text
── BACK-OF-ENVELOPE ──
Writes:   every transaction appended (immutable)
Reads:    user views history → paginated, filtered by (user, time)
Storage:  grows forever → shard by user + time-partition + archive old

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transaction   │  (from ledger)
   └──────┬───────┘
     append (immutable)
          ▼
   ┌──────────────┐
   │History Store │  Txn(id, amount, type, parties, ts, status)
   │ index(user,ts)│  (sharded by user, time-partitioned)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Query API     │  paginated + filtered; cache recent
   └──────────────┘
Archive old partitions to cold storage.
\`\`\``,tech:"hld",displayNumber:233,sortKey:236},{id:"hld-237",number:234,title:"Design a ledger system.",question:"Design a ledger system.",difficulty:"Hard",category:"FinTech & Payments",answer:`The immutable, append-only record of all money movements — the **source of truth** for balances. Use **double-entry** (matching debits/credits sum to zero → provably consistent). Entries never modified (corrections = new entries). Balance = sum of entries (or a snapshot updated atomically with entries). **Challenges**: strong consistency (atomic multi-account entries), immutability/audit, balance-at-scale (snapshots), double-entry integrity, idempotency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Entries:  2 per transaction (debit + credit), append-only
Balance:  sum of entries, or snapshot updated atomically with entries
Storage:  grows forever → partition by account/time; snapshots bound balance calc

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transaction   │
   └──────┬───────┘
     append balanced entries (debit + credit, sum = 0)
          ▼
   ┌──────────────┐
   │Ledger (append│  IMMUTABLE — corrections = new entries
   │ -only,       │
   │ double-entry)│
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Balance       │  = sum(entries) or snapshot (updated atomically)
   │Snapshots     │
   └──────────────┘
Source of truth. Atomic multi-account entries.
\`\`\``,tech:"hld",displayNumber:234,sortKey:237},{id:"hld-238",number:235,title:"Design double-entry accounting.",question:"Design double-entry accounting.",difficulty:"Hard",category:"FinTech & Payments",answer:`Every transaction affects ≥2 accounts with equal **debits and credits** summing to zero — a built-in integrity check (books always balance). E.g. $10 payment: debit customer −10, credit merchant +10. Record entries **atomically** (one transaction) so partial writes can't unbalance. Balance = sum of an account's entries. **Challenges**: enforce balanced entries (debits==credits validation), cross-account atomicity, immutability, efficient balance (snapshots).

\`\`\`text
── BACK-OF-ENVELOPE ──
Invariant: every transaction's entries sum to ZERO (debits = credits)
Atomicity: write all entries in one tx (no partial → no unbalanced books)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │$10 Payment   │
   └──────┬───────┘
     one atomic transaction
          ▼
   ┌──────────────┐
   │Ledger Entries│
   │ debit  customer −10  ┐
   │ credit merchant +10  ┘ = 0 (balanced)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Balance = sum(│  per account (snapshot for speed)
   │ entries)     │
   └──────────────┘
Validation: reject if debits ≠ credits.
\`\`\``,tech:"hld",displayNumber:235,sortKey:238},{id:"hld-239",number:236,title:"Design a distributed ledger.",question:"Design a distributed ledger.",difficulty:"Hard",category:"FinTech & Payments",answer:`A ledger replicated across nodes/parties with **consensus** on the ordered transaction set, so no single party controls it and all agree. Permissioned → BFT/Raft; blockchains → PoW/PoS. Each node validates + appends; hashing/chaining = tamper-evident immutability. **Challenges**: consensus (agreement despite faults/malice — BFT), throughput vs decentralization, immutability, double-spend prevention (ordering), latency (consensus rounds). Contrast with centralized ledger (simpler).

\`\`\`text
── BACK-OF-ENVELOPE ──
Consensus: majority/BFT agreement per block → limits throughput
Immutability: hash-chained blocks → tamper-evident
Trade-off: decentralization ↓ throughput, ↑ latency

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transaction   │
   └──────┬───────┘
     broadcast to nodes
          ▼
   ┌──────────────┐
   │Consensus     │  (BFT / Raft / PoW-PoS) — agree on order
   └──────┬───────┘
          ▼
   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
   │Node 1 Ledger │   │Node 2 Ledger │   │Node 3 Ledger │
   │(hash-chained)│   │(hash-chained)│   │(hash-chained)│
   └──────────────┘   └──────────────┘   └──────────────┘
No single owner; tamper-evident; double-spend prevented via ordering.
\`\`\``,tech:"hld",displayNumber:236,sortKey:239},{id:"hld-240",number:237,title:"Design fraud detection architecture.",question:"Design fraud detection architecture.",difficulty:"Hard",category:"FinTech & Payments",answer:`Screen transactions in **real time**: a scoring service evaluates each against **rules** (velocity, blacklists, geo/amount anomalies) + **ML models** (features from history/device/behavior), returning approve/deny/review within tight latency. Stream transactions (Kafka) to update features + train models; a **feature store** serves features. High risk → block/step-up auth. **Challenges**: low-latency scoring (<100ms), feature freshness (streaming), model accuracy (false positives hurt UX), evolving fraud (retraining), explainability, security vs friction.

\`\`\`text
── BACK-OF-ENVELOPE ──
Latency:  score every txn <100ms (in the payment path)
Features: real-time (velocity, device) from a feature store (streaming updates)
Output:   approve / deny / step-up (review)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Transaction   │
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Fraud Scoring │◄───────│Feature Store │ (real-time features)
   │(rules + ML)  │        └──────▲───────┘
   └──────┬───────┘               │ stream updates
     <100ms                       │
    ┌─────┴──────┐         ┌──────────────┐
    ▼            ▼         │Kafka (txn    │
 ┌──────┐  ┌──────────┐   │ stream) →    │
 │Approve│  │Deny/Step-│   │ features +   │
 │      │  │up (review)│   │ model train  │
 └──────┘  └──────────┘   └──────────────┘
\`\`\``,tech:"hld",displayNumber:237,sortKey:240},{id:"hld-241",number:238,title:"Design a credit-card transaction system.",question:"Design a credit-card transaction system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Card network flow: **authorization** (merchant → acquirer → network → issuer approves/holds, real-time, low latency), then **clearing & settlement** (batch, funds move). Track auth holds vs captures, fraud at auth, idempotency; immutable txns; reconcile with the network. **Challenges**: real-time low-latency auth (strict SLA), auth/capture state (holds expire), fraud at auth, PCI (tokenization), settlement/reconciliation, chargebacks, HA (auth can't be down).

\`\`\`text
── BACK-OF-ENVELOPE ──
Auth:     real-time, <2s, HA (auth can't be down); millions/day
Settlement: batch (next day) — funds actually move
Fraud:    screened at auth time

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Merchant    │
   └──────┬───────┘
     authorize (real-time)
          ▼
   ┌──────────────┐
   │  Acquirer    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Card Network  │──► fraud check
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Issuer      │  approve + HOLD funds
   └──────┬───────┘
     later: batch
          ▼
   ┌──────────────┐
   │Clearing +    │  funds move → reconcile; chargebacks
   │Settlement    │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:238,sortKey:241},{id:"hld-242",number:239,title:"Design a subscription billing system.",question:"Design a subscription billing system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Manage recurring plans: subscriptions (plan, price, cycle, status); a **scheduler** generates invoices on each billing date, charges the method (retries/**dunning** on failure), handles **proration** (upgrades/downgrades), trials, cancellations, taxes. Events (invoice created, payment failed). Distributed scheduler for millions of subs; idempotent charging. **Challenges**: proration/plan changes, failed-payment dunning + involuntary churn, idempotency, billing-date scheduling at scale, taxes/currencies, invoice correctness.

\`\`\`text
── BACK-OF-ENVELOPE ──
Subs:     10M+ subscriptions; billing dates spread across the month
Charges:  on billing date → charge; fail → dunning retries (day 1,3,7)
Proration: mid-cycle plan change → prorated charge/credit

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Subscription  │  (plan, cycle, next_billing_date, status)
   │DB            │
   └──────┬───────┘
     due (distributed scheduler)
          ▼
   ┌──────────────┐
   │Billing Engine│  generate invoice (proration, tax)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Charge (idempo│  fail → dunning (retries) → involuntary churn
   │tent)         │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Events (invoice│
   │/payment-fail) │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:239,sortKey:242},{id:"hld-243",number:240,title:"Design recurring payments.",question:"Design recurring payments.",difficulty:"Intermediate",category:"FinTech & Payments",answer:`Automatically charge a stored method on a schedule (subscriptions, EMIs). Store a **mandate/token** (authorization), a schedule (\`next_charge_at\`), and a distributed scheduler that picks due charges and processes them idempotently with retries/dunning. Notify on success/failure; handle method expiry. **Challenges**: idempotent charging, failed-payment handling (retries, grace, dunning), scheduling at scale, method expiry (account updater), mandates/compliance, notifications.

\`\`\`text
── BACK-OF-ENVELOPE ──
Mandates: millions of stored tokens + schedules
Charges:  distributed scheduler fires due charges → idempotent
Expiry:   card expiry → account updater refreshes token

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Mandate + Sched│  token + next_charge_at
   │DB            │
   └──────┬───────┘
     due (distributed scheduler)
          ▼
   ┌──────────────┐
   │Charge Service│  idempotent; fail → retries/dunning (grace period)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Notify + Update│  success/failure; account updater on card expiry
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:240,sortKey:243},{id:"hld-244",number:241,title:"Design an invoice generation system.",question:"Design an invoice generation system.",difficulty:"Intermediate",category:"FinTech & Payments",answer:`Generate invoices from billing events: compute line items, taxes, discounts, totals → immutable invoice (unique number) → render (PDF) → deliver (email) → track status (paid/unpaid/overdue). Adjustments via **credit notes** (new docs, not edits). Async generation (queue + workers); PDFs in object storage. **Challenges**: correct calculation (taxes/discounts/proration), immutability + **sequential numbering** (legal), idempotency (no duplicate invoices), rendering at scale, multi-currency/tax jurisdictions, delivery + status.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   millions of invoices/month (async generation)
Numbering: sequential + immutable (legal requirement)
Storage:  PDFs → object storage; invoice records in DB

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Billing Event │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Invoice Engine│  compute line items + tax + discounts (idempotent)
   └──────┬───────┘
     immutable invoice (unique sequential #)
          ▼
   ┌──────────────┐        ┌──────────────┐
   │Render (PDF)  │───────►│Object Storage│
   └──────┬───────┘        └──────────────┘
          ▼
   ┌──────────────┐
   │Email + Status│  paid/unpaid/overdue; adjustments = credit notes
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:241,sortKey:244},{id:"hld-245",number:242,title:"Design a refund system.",question:"Design a refund system.",difficulty:"Hard",category:"FinTech & Payments",answer:`Reverse a payment (full or partial): validate the original (not already fully refunded, within window), create a **refund transaction** in the ledger (credit customer), call the processor to return funds, update order/invoice status. **Idempotent** (no double refund), consistent with the ledger. Async (processor may be slow) with status tracking. **Challenges**: idempotency, partial refunds (track remaining refundable), consistency (ledger + processor), reconciliation, refund window/policy, processor failures (retries + status).

\`\`\`text
── BACK-OF-ENVELOPE ──
Refunds:  subset of payments; must never double-refund
Partial:  track remaining refundable amount per original payment
Async:    processor return may be slow → status tracking

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Refund Request│  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Refund Service│  validate (refundable amount, window)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Ledger    ││Processor     │  return funds (retries + status)
 │(credit   ││              │
 │ customer)│└──────────────┘
 └──────────┘
Update order/invoice; reconcile. Idempotent (no double refund).
\`\`\``,tech:"hld",displayNumber:242,sortKey:245},{id:"hld-246",number:243,title:"Design payment failure recovery.",question:"Design payment failure recovery.",difficulty:"Hard",category:"FinTech & Payments",answer:`Handle failures without losing/duplicating money. For **unknown outcomes** (timeout after sending), **query status** before acting. Durable state machine + **reconciliation** detects and fixes stuck transactions (debited-not-credited → reverse). Idempotency keys make retries safe; Saga compensations undo partial multi-step flows. **Challenges**: unknown-outcome handling (status query), stuck/in-limbo txns (reconciliation + auto-reversal), idempotency, compensation, consistency (money never lost/duplicated), anomaly alerting.

\`\`\`text
── BACK-OF-ENVELOPE ──
Unknown outcome: timeout → query status (never blindly retry)
Stuck txn: reconciliation finds debited-not-credited → auto-reverse
Consistency: money never lost or duplicated

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Payment (fail/│
   │ timeout)     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Recovery Logic│
   └──────┬───────┘
    ┌─────┴─────────────┐
    ▼ unknown outcome   ▼ partial multi-step
 ┌────────────┐   ┌──────────────┐
 │Query status│   │ Saga compensate
 │before retry│   │ (undo prior steps)
 └────────────┘   └──────────────┘
          ▼
   ┌──────────────┐
   │Reconciliation│  detect stuck (debited-not-credited) → reverse + alert
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:243,sortKey:246},{id:"hld-247",number:244,title:"Design a financial notification system.",question:"Design a financial notification system.",difficulty:"Intermediate",category:"FinTech & Payments",answer:`Notify users of financial events (transactions, low balance, payment success/failure, statements) reliably and **exactly the right number of times**. Publish events (Kafka) → per-channel workers → providers, honoring preferences. **Idempotency** critical (never send "charged" twice, never miss it). Prioritize security-critical alerts. **Challenges**: idempotency (no duplicate/missed alerts), reliability (guaranteed critical delivery), prioritization, security (minimal sensitive content), preferences/compliance, provider failover.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:   every transaction → notification → millions/day
Idempotency: never double-send "you were charged"; never miss it
Priority:  security alerts (large txn, login) > marketing

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Financial Event│  (txn, low balance, payment status)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Kafka topic   │  (priority topics for critical alerts)
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌──────┐┌──────┐
 │Push  ││Email ││ SMS  │  (dedupe by event id; minimal content)
 └──────┘└──────┘└──────┘
Honor preferences/compliance; provider failover; guaranteed delivery for critical.
\`\`\``,tech:"hld",displayNumber:244,sortKey:247},{id:"hld-248",number:245,title:"Design a high-consistency financial transaction system.",question:"Design a high-consistency financial transaction system.",difficulty:"Hard",category:"FinTech & Payments",answer:`For money, favor **strong consistency (CP)** over availability: ACID transactions (single DB or distributed ACID DB like Spanner/CockroachDB), a **double-entry ledger**, **idempotency**, and for cross-service/shard flows a **Saga** with compensations (or 2PC where truly needed). Accept lower availability during partitions rather than risk inconsistency. **Challenges**: strong consistency at scale (distributed ACID or careful sharding), atomicity across accounts (Saga/2PC), idempotency, no double-spend/negative (locks), auditability, availability trade-off (CP over AP).

\`\`\`text
── BACK-OF-ENVELOPE ──
Consistency: STRONG (CP) — reject/block during partitions vs risk inconsistency
Storage:  ACID DB (single or Spanner/CockroachDB) + double-entry ledger
Cross-shard: Saga (compensations) or 2PC where strict atomicity needed

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Financial Txn │  (+ Idempotency-Key)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Transaction   │  ACID (strong consistency, CP)
   │Service       │
   └──────┬───────┘
    ┌─────┴─────────────┐
    ▼ single shard      ▼ cross-shard
 ┌────────────┐   ┌──────────────┐
 │ACID tx +   │   │ Saga (compensa│
 │double-entry│   │ tions) / 2PC │
 │ledger(locks│   └──────────────┘
 │→no double- │
 │spend)      │
 └────────────┘
Choose consistency over availability during partitions. Full audit trail.
\`\`\``,tech:"hld",displayNumber:245,sortKey:248},{id:"hld-249",number:246,title:"Explain the CAP theorem through system design.",question:"Explain the CAP theorem through system design.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`CAP states that during a **network partition (P)**, a distributed system can guarantee either **Consistency (C)** (every read sees the latest write) or **Availability (A)** (every request gets a response), not both. Partitions are unavoidable, so the real choice is **CP** (reject/block to stay consistent — banks, config) vs **AP** (serve possibly-stale data to stay available — feeds, carts). Without a partition you can have both; CAP only forces the trade-off during partitions.

\`\`\`text
── DURING A PARTITION (choose one) ──
   ┌──────────────┐
   │Network       │  splits cluster into two sides
   │Partition (P) │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ choose CP  ▼ choose AP
 ┌────────────┐┌──────────────┐
 │Consistency ││Availability  │
 │reject/block││always answer,│
 │(no stale)  ││may be stale  │
 │banks/config││feeds/carts   │
 └────────────┘└──────────────┘
No partition → both C and A achievable. CAP = partition-time trade-off.
\`\`\``,tech:"hld",displayNumber:246,sortKey:249},{id:"hld-250",number:247,title:"Design a CP system.",question:"Design a CP system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`A CP system prioritizes **consistency** over availability during partitions — refuses/blocks operations that can't be made consistent rather than risk stale data. Achieve with **consensus** (Raft/Paxos) + **quorum** writes/reads, so a minority partition can't accept writes (becomes unavailable). Use for money, inventory, locks, config, leader election. **Challenges**: reduced availability during partitions, latency (consensus round-trips), quorum sizing. (ZooKeeper/etcd/Spanner.)

\`\`\`text
── PARTITION BEHAVIOR (top → bottom) ──
   ┌──────────────┐
   │  Partition   │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ majority   ▼ minority
 ┌────────────┐┌──────────────┐
 │has quorum  ││NO quorum     │
 │→ accepts   ││→ REJECTS     │  (unavailable, but consistent)
 │writes      ││writes        │
 └────────────┘└──────────────┘
Consensus (Raft/Paxos) + majority quorum. Use: money, locks, config.
Cost: availability + latency during partitions.
\`\`\``,tech:"hld",displayNumber:247,sortKey:250},{id:"hld-251",number:248,title:"Design an AP system.",question:"Design an AP system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`An AP system prioritizes **availability** — every node answers even during a partition, accepting data may be **stale/divergent**, reconciling later (eventual consistency + conflict resolution: LWW, vector clocks, CRDTs). Leaderless/multi-master replication + low quorums (write any node). Use for feeds, carts, DNS, caches, presence. **Challenges**: conflict resolution, eventual consistency (stale/out-of-order), read-repair/anti-entropy. (DynamoDB/Cassandra.)

\`\`\`text
── PARTITION BEHAVIOR (top → bottom) ──
   ┌──────────────┐
   │  Partition   │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ side A     ▼ side B
 ┌────────────┐┌──────────────┐
 │accepts     ││accepts       │  BOTH stay available
 │writes      ││writes        │  (may diverge)
 └─────┬──────┘└──────┬───────┘
       └──────┬───────┘
     partition heals
              ▼
   ┌──────────────┐
   │Reconcile     │  LWW / vector clocks / CRDTs (converge)
   └──────────────┘
Use: feeds, carts, DNS, caches. Cost: staleness + conflicts.
\`\`\``,tech:"hld",displayNumber:248,sortKey:251},{id:"hld-252",number:249,title:"Design eventual consistency.",question:"Design eventual consistency.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Replicas may diverge briefly but **converge** once writes stop propagating. Achieve via async replication + conflict resolution (LWW/vector clocks/CRDTs) + anti-entropy (read-repair, background sync, Merkle trees). Honest UX ("processing"). Use where brief staleness is acceptable (feeds, counts, caches). **Challenges**: conflict resolution, session guarantees (read-your-writes), anti-entropy, idempotent + order-tolerant consumers. Convergence, not instant agreement.

\`\`\`text
── CONVERGENCE (top → bottom) ──
   ┌──────────────┐
   │  Write to    │  Replica A
   │  Replica A   │
   └──────┬───────┘
     async replicate (B, C lag briefly)
          ▼
   ┌──────────────┐
   │Replicas      │  A, B, C temporarily DIVERGE
   │diverge       │
   └──────┬───────┘
     anti-entropy (read-repair, Merkle sync)
          ▼
   ┌──────────────┐
   │CONVERGE      │  all replicas equal (conflict resolution: LWW/CRDT)
   └──────────────┘
Session guarantees (read-your-writes) as needed.
\`\`\``,tech:"hld",displayNumber:249,sortKey:252},{id:"hld-253",number:250,title:"Design strong consistency.",question:"Design strong consistency.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Every read reflects the latest committed write (linearizability) — as if a single copy. Achieve via single-leader + synchronous replication, or **consensus** (Raft/Paxos) + **majority quorum** (R + W > N). Costs: higher latency (coordination) + lower availability during partitions (CP). Use for money, inventory, locks, uniqueness. **Challenges**: latency, availability trade-off (CAP), leader bottleneck/failover, coordination overhead.

\`\`\`text
── LINEARIZABLE WRITE (top → bottom) ──
   ┌──────────────┐
   │  Write       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Leader        │  append → replicate synchronously
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Replica 1 ││Replica 2 │  wait for MAJORITY ack (R + W > N)
└──────────┘└──────────┘
          ▼
   ┌──────────────┐
   │Commit → every│  read now sees latest write
   │read sees it  │
   └──────────────┘
Cost: latency (coordination) + CP availability trade-off.
\`\`\``,tech:"hld",displayNumber:250,sortKey:253},{id:"hld-254",number:251,title:"Design quorum-based reads/writes.",question:"Design quorum-based reads/writes.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:"With N replicas, a write needs **W** acks and a read queries **R** replicas. If **R + W > N**, quorums overlap → a read always sees the latest write (strong consistency). Tune: `W=N` (durable, slow), `R=1` (fast, weak), or `R=W=majority` (balanced). **Challenges**: choosing R/W (consistency vs latency), stale replicas (read-repair), version conflicts (vector clocks), availability (need W/R nodes up).\n\n```text\n── R + W > N → OVERLAP (N=3, W=2, R=2) ──\n   Write to W=2:          Read from R=2:\n   ┌────┐┌────┐┌────┐     ┌────┐┌────┐┌────┐\n   │ R1 ││ R2 ││ R3 │     │ R1 ││ R2 ││ R3 │\n   │ ✓  ││ ✓  ││    │     │ ✓  ││    ││ ✓  │\n   └────┘└────┘└────┘     └────┘└────┘└────┘\n   W={R1,R2}              R={R1,R3}\n   Overlap = R1 → read sees the latest write ✓\nTune: R=W=majority (balanced) | W=N (durable) | R=1 (fast, weak).\n```",tech:"hld",displayNumber:251,sortKey:254},{id:"hld-255",number:252,title:"Design a consensus system.",question:"Design a consensus system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Consensus makes nodes **agree on a single value/log order** despite failures — the foundation of leader election, replicated state machines, distributed locks. **Paxos** (classic) / **Raft** (understandable: leader + log replication + majority commit). A value commits once a **majority** acknowledges, surviving minority failures. **Challenges**: correctness under failures/partitions (majority quorum, no split-brain), leader election/failover, log replication, latency, majority-up requirement. (etcd/ZooKeeper/Spanner.)

\`\`\`text
── COMMIT ON MAJORITY (top → bottom) ──
   ┌──────────────┐
   │  Client      │  propose value
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Leader      │  append to log → replicate
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Follower 1││Follower 2│  ack
└──────────┘└──────────┘
          ▼
   ┌──────────────┐
   │COMMIT (once  │  majority acked → survives minority failure
   │majority ack) │
   └──────────────┘
No split-brain (majority quorum). Needs majority up.
\`\`\``,tech:"hld",displayNumber:252,sortKey:255},{id:"hld-256",number:253,title:"Explain Raft-based architecture.",question:"Explain Raft-based architecture.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Raft = **leader election** (majority vote per term; heartbeats maintain), **log replication** (clients send commands to leader → appends → replicates; commits on majority), **safety** (terms + log-matching → only up-to-date nodes win elections, committed entries persist). Leader failure → new election picks a follower with the latest log. **Challenges**: failover speed, majority availability, log compaction/snapshots, membership changes. (etcd/Consul/CockroachDB.)

\`\`\`text
── RAFT (three parts) ──
   ┌──────────────┐
   │1. LEADER      │  nodes vote (majority) per term; leader heartbeats
   │   ELECTION   │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │2. LOG         │  client → leader appends → replicate to followers
   │   REPLICATION│  → commit on majority
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │3. SAFETY      │  terms + log-matching; leader dies → re-elect
   │              │  node with latest log
   └──────────────┘
Snapshots for log compaction. Needs majority up.
\`\`\``,tech:"hld",displayNumber:253,sortKey:256},{id:"hld-257",number:254,title:"Design a ZooKeeper-like coordination system.",question:"Design a ZooKeeper-like coordination system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`A coordination service: small, strongly-consistent hierarchical KV store (znodes) with **watches**, **ephemeral nodes** (auto-deleted on session end), **sequential nodes** — primitives for locks, leader election, config, discovery, membership. Backed by consensus (ZAB/Raft) across an odd ensemble (majority quorum). **Challenges**: strong consistency + availability (CP), watch delivery, session/ephemeral management, read scaling (observers), keeping data small.

\`\`\`text
── ZNODE TREE + PRIMITIVES ──
   ┌──────────────┐
   │Ensemble      │  odd # nodes, consensus (ZAB/Raft), majority quorum
   └──────┬───────┘
          ▼
   /  (znode tree, strongly consistent)
   ├── /locks/       → ephemeral + sequential → distributed lock
   ├── /election/    → ephemeral → leader election
   ├── /config/      → watches → config + notification
   └── /services/    → ephemeral → discovery + membership
Ephemeral node auto-deletes when client session ends. Keep data small (CP).
\`\`\``,tech:"hld",displayNumber:254,sortKey:257},{id:"hld-258",number:255,title:"Design a distributed lock service at scale.",question:"Design a distributed lock service at scale.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Mutual exclusion across many clients/resources at scale. Use a coordination service (**etcd/ZooKeeper**) with **leases** (auto-expire on failure) + **fencing tokens** (monotonic ids so a paused-then-resumed holder can't act after losing the lock), or Redis for speed. Shard locks; clients renew via heartbeat. **Challenges**: safety under GC pauses (fencing essential), lease TTL vs long tasks, coordination bottleneck (sharding), split-brain, lock-service failover.

\`\`\`text
── FENCING TOKEN SAFETY (top → bottom) ──
   ┌──────────────┐
   │Client A      │  acquire lock → gets fencing token 33
   └──────┬───────┘
     A pauses (GC) → lease expires
          ▼
   ┌──────────────┐
   │Client B      │  acquires lock → fencing token 34
   └──────┬───────┘
     A resumes, tries to write with token 33
          ▼
   ┌──────────────┐
   │Resource      │  rejects token 33 < 34 (stale holder blocked)
   └──────────────┘
Lease auto-expires on crash; shard locks for scale. (etcd/ZooKeeper)
\`\`\``,tech:"hld",displayNumber:255,sortKey:258},{id:"hld-259",number:256,title:"Design a distributed transaction system.",question:"Design a distributed transaction system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Coordinate an atomic operation across services/DBs. Options: **2PC** (prepare→commit; atomic but blocking, coordinator SPOF), **Saga** (local transactions + compensations; available, eventually consistent, no locks), or a **distributed ACID DB** (Spanner/CockroachDB). Microservices prefer Sagas + idempotency over 2PC. **Challenges**: atomicity (2PC blocking vs Saga eventual), failure handling (compensations), consistency vs availability, coordinator reliability, idempotency.

\`\`\`text
── THREE OPTIONS ──
   ┌──────────────────────────────────────┐
   │ 2PC: prepare → commit (atomic, but     │
   │   BLOCKING, coordinator SPOF, slow)    │
   ├──────────────────────────────────────┤
   │ SAGA: local txns + compensations       │
   │   (available, eventual, no locks) ✓    │
   ├──────────────────────────────────────┤
   │ Distributed ACID DB (Spanner/          │
   │   CockroachDB): consensus + clocks     │
   └──────────────────────────────────────┘
Microservices → Saga + idempotency. 2PC only for strict atomicity.
\`\`\``,tech:"hld",displayNumber:256,sortKey:259},{id:"hld-260",number:257,title:"Design two-phase commit (2PC).",question:"Design two-phase commit (2PC).",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`A coordinator drives atomic commit in two phases: **Phase 1 (prepare)** — ask all participants to prepare + vote (lock resources, log, reply yes/no); **Phase 2 (commit/abort)** — all yes → commit, else abort. Guarantees atomicity. **Weaknesses**: **blocking** (coordinator crash after prepare → participants hold locks indefinitely), latency, reduced availability, coordinator SPOF. 3PC/consensus mitigate. Rarely used at high scale.

\`\`\`text
── TWO PHASES (top → bottom) ──
   ┌──────────────┐
   │Coordinator   │
   └──────┬───────┘
     PHASE 1: "prepare?"
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Participant││Participant│  lock + vote yes/no
│1 (yes)   ││2 (yes)   │
└──────────┘└──────────┘
     all yes?
          ▼
   ┌──────────────┐
   │Coordinator   │  PHASE 2: "commit" (else "abort")
   └──────────────┘
BLOCKING: coordinator crash after prepare → locks held. SPOF. Avoid at scale.
\`\`\``,tech:"hld",displayNumber:257,sortKey:260},{id:"hld-261",number:258,title:"Design Saga orchestration.",question:"Design Saga orchestration.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`A central **orchestrator** drives a distributed transaction as local steps, explicitly calling each service; on failure, invokes **compensating** actions (semantic rollback). Persists Saga state (resumable). E.g. book trip: reserve flight → hotel → charge; charge fails → cancel hotel + flight. **Benefits**: central visibility, easy to change flow. **Challenges**: orchestrator coupling, compensation design, idempotency, durable state, compensation failures.

\`\`\`text
── ORCHESTRATED SAGA (top → bottom) ──
   ┌──────────────┐
   │ Orchestrator │  (persists state, resumable)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │reserve flight│  ✓
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │reserve hotel │  ✓
   └──────┬───────┘
          ▼
   ┌──────────────┐        ┌──────────────┐
   │  charge      │──fail─►│COMPENSATE:   │  cancel hotel + flight
   └──────────────┘        └──────────────┘
Central visibility/monitoring. Idempotent steps.
\`\`\``,tech:"hld",displayNumber:258,sortKey:261},{id:"hld-262",number:259,title:"Design Saga choreography.",question:"Design Saga choreography.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`No central coordinator — each service reacts to events and emits its own, driving the flow implicitly. E.g. OrderPlaced → payment → PaymentCompleted → inventory → Reserved → shipping; on failure, a service emits a failure event triggering **compensating** reactions upstream. **Benefits**: loose coupling, no bottleneck, scalable. **Challenges**: implicit flow (hard to trace), event cycles, harder to change, compensation correctness, idempotency. Simple flows → choreography; complex → orchestration.

\`\`\`text
── CHOREOGRAPHED SAGA (event chain, top → bottom) ──
   ┌──────────────┐
   │OrderPlaced   │
   └──────┬───────┘
          ▼ (event)
   ┌──────────────┐
   │Payment →     │  emits PaymentCompleted
   └──────┬───────┘
          ▼ (event)
   ┌──────────────┐
   │Inventory →   │  emits Reserved
   └──────┬───────┘
          ▼ (event)
   ┌──────────────┐
   │Shipping      │
   └──────────────┘
Failure event → compensations upstream. Loose coupling, no bottleneck,
but implicit flow (hard to trace).
\`\`\``,tech:"hld",displayNumber:259,sortKey:262},{id:"hld-263",number:260,title:"Design a transactional outbox.",question:"Design a transactional outbox.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Solve the **dual-write problem**: write the business row + an \`outbox\` event row in the **same DB transaction**; a relay or **CDC** (Debezium) reads committed outbox rows, publishes to the broker, marks them sent. Atomic write; at-least-once publish → consumers dedupe. **Challenges**: relay/CDC reliability, at-least-once (idempotent consumers), outbox cleanup, ordering (by id/aggregate), publish-lag monitoring.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │ Service      │  SAME DB TX: business row + outbox(event)
   └──────┬───────┘
     commit (atomic — no dual write)
          ▼
   ┌──────────────┐
   │ Outbox Table │
   └──────┬───────┘
     relay/CDC reads committed rows
          ▼
   ┌──────────────┐
   │  Broker      │  → mark sent; consumers dedupe (at-least-once)
   └──────────────┘
Cleanup outbox; order by id/aggregate; monitor publish lag.
\`\`\``,tech:"hld",displayNumber:260,sortKey:263},{id:"hld-264",number:261,title:"Design change-data-capture architecture.",question:"Design change-data-capture architecture.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`CDC streams a DB's committed changes as events by reading its **transaction log** (binlog/WAL) via Debezium → Kafka — no app changes, low latency. Downstream: read models, cache/search sync, analytics, outbox. Initial snapshot then streaming. **Challenges**: snapshot→stream handoff, schema/DDL changes, deletes (tombstones), ordering per key, downstream idempotency, source log retention, lag.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Database    │  commits (insert/update/delete)
   └──────┬───────┘
     tail transaction log (binlog/WAL)
          ▼
   ┌──────────────┐
   │  Debezium    │  → change events (before/after)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │    Kafka     │
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Read  ││Cache/││Analytics │  no app change, low latency
│Models││Search││/Outbox   │
└──────┘└──────┘└──────────┘
\`\`\``,tech:"hld",displayNumber:261,sortKey:264},{id:"hld-265",number:262,title:"Design a globally distributed database.",question:"Design a globally distributed database.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Store/serve data across regions for low latency + resilience. Options: **partition by region** (data near users) + cross-region replication; or a **globally consistent DB** (Spanner: Paxos-replicated shards + TrueTime; CockroachDB). Local reads; writes coordinate per consistency need. **Challenges**: cross-region latency (consensus is expensive globally), consistency vs latency (relax to per-region strong + global eventual), residency, multi-region conflicts, failover.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  → nearest region (local reads)
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Region US ││Region EU │  data partitioned by region
│ ┌──────┐ ││ ┌──────┐ │  OR globally-consistent (Spanner TrueTime)
│ │Shard │◄┼┼►│Shard │ │  cross-region replication (consensus)
│ └──────┘ ││ └──────┘ │
└──────────┘└──────────┘
Strong global consistency = high latency. Relax where possible.
\`\`\``,tech:"hld",displayNumber:262,sortKey:265},{id:"hld-266",number:263,title:"Design multi-region writes.",question:"Design multi-region writes.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Allow writes in multiple regions (active-active). Approaches: **home-region ownership** per key (route writes there → no conflicts), **multi-master + conflict resolution** (LWW/CRDT) for eventual data, or **cross-region consensus** (strong but slow). **Challenges**: write conflicts (resolution), consistency vs latency, global invariants, replication lag, split-brain.

\`\`\`text
── THREE APPROACHES ──
   ┌──────────────────────────────────────┐
   │ HOME-REGION ownership per key:         │
   │   route writes to key's home → NO      │
   │   conflicts (best for strong data) ✓   │
   ├──────────────────────────────────────┤
   │ MULTI-MASTER + conflict resolution     │
   │   (LWW/CRDT) → eventual data           │
   ├──────────────────────────────────────┤
   │ CROSS-REGION consensus → strong but    │
   │   SLOW (global round-trips)            │
   └──────────────────────────────────────┘
Challenges: conflicts, latency, invariants, split-brain.
\`\`\``,tech:"hld",displayNumber:263,sortKey:266},{id:"hld-267",number:264,title:"Design global database replication.",question:"Design global database replication.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Replicate across regions so each serves local reads and survives a region loss. **Async** (fast writes, lag/loss risk — common) vs **sync** (no loss, high latency — sparingly). Topologies: active-passive (primary + async replicas) or active-active (multi-master + conflict resolution). Rack/AZ + region awareness. **Challenges**: lag (stale reads, RPO), cross-region bandwidth/cost, conflicts, per-dataset consistency, failover automation.

\`\`\`text
── TOPOLOGIES (top → bottom) ──
   ┌──────────────────────────────────────┐
   │ ACTIVE-PASSIVE:                        │
   │   Primary ──async──► Replica (standby) │
   │   (fast writes; failover = promote)    │
   ├──────────────────────────────────────┤
   │ ACTIVE-ACTIVE:                         │
   │   Region A ◄──► Region B (multi-master)│
   │   + conflict resolution                │
   └──────────────────────────────────────┘
Async (lag/loss) vs sync (no loss, slow). RPO = replication lag.
\`\`\``,tech:"hld",displayNumber:264,sortKey:267},{id:"hld-268",number:265,title:"Design conflict resolution.",question:"Design conflict resolution.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Resolve concurrent-write divergence deterministically: **LWW** (by timestamp — simple, lossy), **vector clocks** (detect concurrency → surface conflicts), **CRDTs** (mathematically merge — counters/sets/sequences), or **app-level merge** (domain logic). Choose by data semantics. **Challenges**: LWW data loss (clock skew), concurrency detection (vector clocks), CRDT fit (not all data), merge correctness, predictable outcomes.

\`\`\`text
── STRATEGIES (top → bottom) ──
   ┌──────────────────────────────────────┐
   │ LWW: keep write with latest timestamp  │
   │   (simple, but LOSES data; clock skew) │
   ├──────────────────────────────────────┤
   │ VECTOR CLOCKS: detect concurrency →    │
   │   surface conflict to app/user         │
   ├──────────────────────────────────────┤
   │ CRDTs: auto-merge (counters/sets/seq)  │
   │   → no conflicts, no loss ✓            │
   ├──────────────────────────────────────┤
   │ APP-LEVEL MERGE: domain-specific logic │
   └──────────────────────────────────────┘
Choose by data semantics.
\`\`\``,tech:"hld",displayNumber:265,sortKey:268},{id:"hld-269",number:266,title:"Design disaster recovery.",question:"Design disaster recovery.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`DR restores service/data after a region/DC outage per **RPO** (max data loss) + **RTO** (max downtime). Cross-region **replication/backups** + IaC automation + a **failover strategy**: backup-restore (cheap, slow), pilot light, warm standby, or active-active (fast, costly). **Test regularly** — untested DR fails. **Challenges**: meeting RPO/RTO, post-failover consistency, split-brain, cost vs speed, tested failover.

\`\`\`text
── STRATEGIES (cost ↑ / recovery-time ↓) ──
   ┌──────────────────────────────────────┐
   │ backup-restore  → cheapest, slow (hrs) │
   │ pilot light     → core running, scale up│
   │ warm standby    → running smaller copy  │
   │ active-active   → instant, costliest    │
   └──────────────────────────────────────┘
RPO = replication frequency; RTO = failover automation.
TEST with drills (untested DR fails when needed).
\`\`\``,tech:"hld",displayNumber:266,sortKey:269},{id:"hld-270",number:267,title:"Design backup across multiple regions.",question:"Design backup across multiple regions.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Store backups in **multiple regions** so a single region loss doesn't destroy them. Take backups (full + incremental + WAL), replicate to geographically separate object storage, encrypt, version, lifecycle-tier. **Verify** with periodic cross-region test restores. **Challenges**: transfer cost/time, encryption/access, restorability verification, retention/compliance, RPO, isolation (separate accounts/regions).

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Backups       │  full + incremental + WAL
   └──────┬───────┘
     replicate cross-region (encrypted, versioned)
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Region A  ││Region B  │  ISOLATED (separate account) so one
│backup    ││backup    │  failure can't destroy both
└──────────┘└──────────┘
          ▼
   ┌──────────────┐
   │Verify (cross-│  region test restore)
   │region)       │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:267,sortKey:270},{id:"hld-271",number:268,title:"Design an active-active global system.",question:"Design an active-active global system.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`All regions serve reads **and** writes → low latency, full HA (region loss sheds its share), no idle capacity. Requires multi-region data: **home-region ownership** (no conflicts) or **multi-master + conflict resolution** (CRDT/LWW); strong data may pin to a region. GeoDNS routes to nearest healthy region. **Challenges**: write conflicts, replication lag, global invariants (uniqueness), split-brain, residency.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │GeoDNS        │  → nearest HEALTHY region (both R + W)
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐   ┌──────────┐
│Region A  │◄─►│Region B  │  BOTH read + write; region loss
│(R + W)   │   │(R + W)   │  → survivors absorb load (no idle)
└──────────┘   └──────────┘
Data: home-region ownership (no conflicts) or multi-master + CRDT/LWW.
Challenges: conflicts, lag, global invariants, split-brain.
\`\`\``,tech:"hld",displayNumber:268,sortKey:271},{id:"hld-272",number:269,title:"Design a system with five-nines availability.",question:"Design a system with five-nines availability.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`99.999% = ~5 min/yr — **eliminate SPOFs** at every layer: redundant stateless services (multi-AZ + multi-region), replicated DBs + fast auto-failover, health-checked LBs, graceful degradation, circuit breakers, **zero-downtime deploys** (rolling/blue-green/canary). Rigorous monitoring/alerting, auto-recovery, chaos testing, tested DR. **Challenges**: no SPOF anywhere (hardest), fast failover (RTO seconds), rolling deploys, graceful degradation, redundancy cost. Availability = redundancy × automation × testing.

\`\`\`text
── NO SPOF AT ANY LAYER (top → bottom) ──
   ┌──────────────┐
   │Global routing│  multi-region, health-aware
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Health-checked│  LBs (redundant)
   │LBs           │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Stateless svc │  multi-AZ + multi-region + autoscale
   │(redundant)   │  + circuit breakers + graceful degradation
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Replicated DB │  fast auto-failover
   └──────────────┘
+ zero-downtime deploys + monitoring + chaos + tested DR.
Availability = redundancy × automation × testing.
\`\`\``,tech:"hld",displayNumber:269,sortKey:272},{id:"hld-273",number:270,title:"Design a system that survives an entire region failure.",question:"Design a system that survives an entire region failure.",difficulty:"Hard",category:"Advanced Distributed Systems",answer:`Deploy across **multiple regions** + cross-region replication + global routing that **fails traffic away** from a dead region (GeoDNS/health checks). Active-active (instant, costly — survivors absorb load) or active-passive (promote standby — some RTO). Ensure **capacity headroom** (survivors handle 100%). Bounded RPO; GameDay-test failover. **Challenges**: capacity planning, post-failover consistency (RPO), reroute speed (DNS TTL/anycast), split-brain, stateful sessions, cost.

\`\`\`text
── REGION FAILOVER (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  health checks detect dead region
   └──────┬───────┘
     Region A DOWN → fail traffic away
   ┌──────┴──────┐
   ▼ (dead)      ▼
┌──────────┐   ┌──────────┐
│Region A  │   │Region B  │  absorbs 100% load
│  ✗       │   │(headroom)│  (must be pre-sized!)
└──────────┘   └──────────┘
Active-active (instant) or active-passive (promote, some RTO).
Bounded RPO; GameDay-test. Challenges: capacity, reroute speed, split-brain.
\`\`\``,tech:"hld",displayNumber:270,sortKey:273},{id:"hld-274",number:271,title:"Design a real-time chat system.",question:"Design a real-time chat system.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Clients connect via **WebSocket** to gateway servers; messages persist (per-conversation store) and route to recipients through a **pub/sub backplane** (Redis/Kafka) so a sender on gateway A reaches a recipient on gateway B. Track presence, receipts, unread counts; offline → push. Many gateways + sticky sessions + backplane; shard by conversation. **Challenges**: connection scale, cross-server routing, ordering, receipts, presence, offline delivery, group fan-out.

\`\`\`text
── BACK-OF-ENVELOPE ──
Users:    50M DAU, ~5M concurrent; 1B msgs/day = ~11,600/sec (peak ~40k)
Conns/node: ~50k → ~100 gateway nodes; msgs 300 B → shard by conversation

── ARCHITECTURE (top → bottom) ──
   ┌──────────┐          ┌──────────┐
   │ Client A │          │ Client B │
   └────┬─────┘          └────▲─────┘
    WebSocket             WebSocket
        ▼                     │
   ┌──────────┐          ┌──────────┐
   │Gateway N1│          │Gateway N2│
   └────┬─────┘          └────▲─────┘
     publish            subscribe
        ▼                     │
   ┌──────────────────────────┴─────┐
   │ Pub/Sub Backplane (Redis/Kafka)│
   └────┬───────────────────────────┘
        ▼
   ┌──────────────┐
   │Message Store │  (sharded by conversation) + offline → push
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:271,sortKey:274},{id:"hld-275",number:272,title:"Design real-time notifications.",question:"Design real-time notifications.",difficulty:"Intermediate",category:"Real-Time & High Scale",answer:`Deliver to online users **instantly** (WebSocket/SSE push via a connection gateway + backplane) and offline users via **push/email/SMS**. An event → notification service → fan-out to the user's active connections and/or providers. Store notifications for in-app history + unread counts. **Challenges**: online vs offline routing (presence-aware), fan-out to multiple devices, idempotency, ordering, unread-count consistency, provider rate limits/failover.

\`\`\`text
── BACK-OF-ENVELOPE ──
Volume:  500M notifications/day = ~5,800/sec (peak ~20k); user may have N devices

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Event       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Notif Service │  presence-aware routing
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ online     ▼ offline
 ┌────────────┐┌──────────────┐
 │WebSocket/SSE││Push/Email/SMS│
 │Gateway (+   ││(APNs/FCM)    │
 │backplane)   │└──────────────┘
 └─────┬──────┘
       ▼
 ┌────────────┐
 │In-app store │  history + unread counts (dedupe by id)
 └────────────┘
\`\`\``,tech:"hld",displayNumber:272,sortKey:275},{id:"hld-276",number:273,title:"Design live sports score updates.",question:"Design live sports score updates.",difficulty:"Intermediate",category:"Real-Time & High Scale",answer:`A high **fan-out, read-heavy** broadcast: a data feed updates scores → a service publishes to a pub/sub layer → millions of clients receive via **WebSocket/SSE** (or CDN-cached short-poll for extreme scale). One-to-many broadcast of the same data → push through a scalable backplane + edge. **Challenges**: massive fan-out (millions concurrent), low latency, connection scaling (or CDN offload), efficient broadcast, match-time spikes.

\`\`\`text
── BACK-OF-ENVELOPE ──
Viewers:  10M concurrent on a big match; SAME payload to all (broadcast)
Options:  WebSocket/SSE gateways + backplane, OR push to CDN edge (clients poll)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Data Feed   │  (score change)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Score Service │  → publish
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Pub/Sub     │
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────────┐
│Gateway││Gateway││CDN Edge  │  fan-out to millions
│(WS/SSE││(WS/SSE││(cached   │  (same payload)
│)      ││)      ││ poll)    │
└──────┘└──────┘└──────────┘
\`\`\``,tech:"hld",displayNumber:273,sortKey:276},{id:"hld-277",number:274,title:"Design stock price streaming.",question:"Design stock price streaming.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Ingest market data (very high throughput) → normalize → publish per-symbol updates → fan-out to subscribed clients via **WebSocket** (subscribe to specific symbols → filter/route). Low latency critical. Fast in-memory pub/sub, **conflation** (send only latest price if a client is slow), per-symbol topics. Partition by symbol. **Challenges**: ultra-low latency, firehose throughput, subscription filtering, conflation/backpressure, tick ordering, fan-out at scale.

\`\`\`text
── BACK-OF-ENVELOPE ──
Ingest:  millions of ticks/sec (market firehose); latency budget = milliseconds
Conflation: slow client gets LATEST price only (drop intermediate ticks)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Market Data  │  (firehose)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │ Normalize    │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Per-symbol    │  in-memory bus (partitioned by symbol)
   │Pub/Sub       │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │WebSocket      │  clients subscribe to symbols (filter/route)
   │Gateways       │  conflation for slow clients (backpressure)
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:274,sortKey:277},{id:"hld-278",number:275,title:"Design a real-time bidding system.",question:"Design a real-time bidding system.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Ad RTB: on an impression, an exchange sends bid requests to many bidders who must respond within **~100ms**; highest bid wins, ad served. Extreme low latency + high throughput (millions auctions/sec). Bidders use fast in-memory lookups (budgets, targeting, ML value) + strict timeouts. Horizontally scaled, regional. **Challenges**: ~100ms budget, massive QPS, real-time budget pacing, ML scoring in budget, timeout drops, budget consistency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Auctions: millions/sec; HARD ~100ms end-to-end budget (drop slow bidders)
Bidders:  in-memory targeting/budget + ML value prediction, regional

── ARCHITECTURE (top → bottom, ~100ms) ──
   ┌──────────────┐
   │  Impression  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Exchange    │  parallel bid requests (strict timeout)
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Bidder││Bidder││Bidder│  in-memory budget/targeting + ML score
│1     ││2     ││3     │  (respond <100ms or dropped)
└──────┘└──────┘└──────┘
          ▼
   ┌──────────────┐
   │Highest bid   │  → serve ad
   │wins          │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:275,sortKey:278},{id:"hld-279",number:276,title:"Design an online multiplayer game backend.",question:"Design an online multiplayer game backend.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Players connect to **game servers** (authoritative for state) via **UDP**/WebSocket; a **matchmaking** service groups players into sessions; each session runs on a server that simulates the game loop and broadcasts state (client-side prediction + server reconciliation hide latency). Many instances, regional (low ping), session-based sharding. **Challenges**: low latency, authoritative state + cheat prevention, skill-based matchmaking, state sync, session scaling, disconnects/reconnects.

\`\`\`text
── BACK-OF-ENVELOPE ──
Latency:  <50ms ping → regional game servers; tick rate 20-60 Hz
State:    server-authoritative (anti-cheat); client predicts + reconciles

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Players     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Matchmaking   │  (skill-based) → group into session
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Game Server   │  authoritative game loop (regional, low ping)
   │(session,     │  ◄── UDP/WebSocket ──► players
   │ sharded)     │  broadcast state; prediction + reconciliation
   └──────────────┘
Scale: many instances, session sharding.
\`\`\``,tech:"hld",displayNumber:276,sortKey:279},{id:"hld-280",number:277,title:"Design a ride-location tracking system.",question:"Design a ride-location tracking system.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Drivers/riders send frequent **location updates** (every few seconds) → high-volume ingest → **geospatial index** (geohash/H3, sharded) for nearby queries + streamed to the paired rider for **live tracking** (WebSocket). Fast in-memory store for latest location + Kafka stream for history. Geo-sharded. **Challenges**: high-frequency ingestion (millions writes), geospatial queries, real-time delivery, conflation (latest only), battery/bandwidth.

\`\`\`text
── BACK-OF-ENVELOPE ──
Drivers:  5M active × update/4s = ~1.25M writes/sec → store LATEST only (conflate)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Driver/Rider  │  location every 4s
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Ingest (high  │
   │volume)       │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌────────────┐┌──────────────┐
 │Geo Index   ││Kafka Stream  │
 │(geohash/H3,││(history/     │
 │ latest loc)││ analytics)   │
 └─────┬──────┘└──────────────┘
       ▼ WebSocket
 ┌────────────┐
 │Paired Rider │  live tracking
 └────────────┘
\`\`\``,tech:"hld",displayNumber:277,sortKey:280},{id:"hld-281",number:278,title:"Design real-time GPS tracking.",question:"Design real-time GPS tracking.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Devices report GPS continuously → ingestion (high write volume) → latest position (Redis) + full history (time-series/Kafka) → serve live positions to dashboards (WebSocket/SSE) + geo-queries (nearby/within-area). **Geofencing** (alert on enter/exit). Partition by device/region. **Challenges**: massive write throughput, geo + geofence queries, real-time delivery, history storage (downsampling), offline buffering, battery/bandwidth.

\`\`\`text
── BACK-OF-ENVELOPE ──
Devices:  1M × update/5s = ~200k writes/sec; history downsampled + tiered

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Devices     │  GPS coordinates
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Ingest      │
   └──────┬───────┘
    ┌─────┼──────────┐
    ▼     ▼          ▼
 ┌──────┐┌────────┐┌──────────┐
 │Redis ││Time-   ││Geofencing│
 │latest││series/ ││(enter/   │
 │pos   ││Kafka   ││ exit)    │
 └──┬───┘└(history)└──────────┘
    ▼ WebSocket/SSE
 ┌──────────┐
 │Dashboards│  + geo-queries (nearby/area)
 └──────────┘
\`\`\``,tech:"hld",displayNumber:278,sortKey:281},{id:"hld-282",number:279,title:"Design a live-streaming platform.",question:"Design a live-streaming platform.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Broadcaster ingests via **RTMP/WebRTC** → transcode (multiple bitrates) → segment into HLS/DASH → **CDN** to viewers (adaptive bitrate). Low-latency: LL-HLS or WebRTC. Separate WebSocket **chat** + viewer counts. CDN handles viewer fan-out; regional ingest + transcode. **Challenges**: latency (HLS ~10s vs LL-HLS/WebRTC ~2s), transcoding at scale, massive fan-out (CDN), ABR, synchronized chat, viral spikes.

\`\`\`text
── BACK-OF-ENVELOPE ──
Viewers:  1M concurrent per popular stream → CDN fan-out; ~3-6 Mbps/stream
Latency:  HLS ~10s | LL-HLS ~3s | WebRTC ~1s (trade latency vs scale)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Broadcaster  │  RTMP/WebRTC ingest
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Transcode   │  multiple bitrates → HLS/DASH segments
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │     CDN      │  → viewers (adaptive bitrate)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Viewers     │  (+ separate WebSocket chat, viewer counts)
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:279,sortKey:282},{id:"hld-283",number:280,title:"Design Twitch.",question:"Design Twitch.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`A live-streaming platform tuned for **interactive** streaming: low-latency video (ingest → transcode → CDN with LL-HLS), high-volume real-time **chat** (millions of messages, sharded WebSocket + fan-out), subscriptions/bits/emotes, VOD, recommendations. CDN for video fan-out, sharded chat, regional ingest. **Challenges**: low-latency video at scale, extreme chat fan-out (popular streams → millions of chatters), moderation, monetization, VOD, discovery.

\`\`\`text
── BACK-OF-ENVELOPE ──
Big stream: millions of viewers (CDN video) + millions of chatters (sharded WS)
Chat fan-out is the hard part (one message → millions of clients)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │ Broadcaster  │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ video      ▼ chat
 ┌────────────┐┌──────────────┐
 │Transcode → ││Sharded       │
 │CDN (LL-HLS)││WebSocket +   │
 │            ││fan-out       │
 └─────┬──────┘└──────┬───────┘
       ▼              ▼
 ┌──────────┐  ┌──────────────┐
 │Viewers   │  │Chatters      │  subs/bits/emotes, moderation
 └──────────┘  └──────────────┘
VOD recording; recommendations; regional ingest.
\`\`\``,tech:"hld",displayNumber:280,sortKey:283},{id:"hld-284",number:281,title:"Design a real-time analytics platform.",question:"Design a real-time analytics platform.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Ingest event streams (Kafka) → **stream processing** (Flink/Kafka Streams) computes real-time aggregations (counts, windows, sessions) → fast OLAP/serving store (Druid/ClickHouse/Pinot) for sub-second dashboards. Raw events → lake for batch/ML (**Lambda/Kappa**). Partition streams, scale processors, columnar serving. **Challenges**: real-time aggregation (windowing + late events), ingestion throughput, low-latency queries (pre-aggregation), high cardinality, EOS aggregation, real-time vs batch accuracy.

\`\`\`text
── BACK-OF-ENVELOPE ──
Ingest:  millions of events/sec → Kafka partitions; pre-aggregate for sub-sec queries

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Events      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │   Kafka      │
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ real-time  ▼ raw
 ┌────────────┐┌──────────────┐
 │Stream Proc ││Data Lake     │  batch/ML
 │(Flink):    ││(S3)          │
 │windowed agg│└──────────────┘
 └─────┬──────┘
       ▼
 ┌────────────┐
 │OLAP Store   │  (Druid/ClickHouse/Pinot) → dashboards (sub-sec)
 └────────────┘
\`\`\``,tech:"hld",displayNumber:281,sortKey:284},{id:"hld-285",number:282,title:"Design a real-time dashboard.",question:"Design a real-time dashboard.",difficulty:"Intermediate",category:"Real-Time & High Scale",answer:`Show live metrics that update continuously. Backend aggregates data (from a stream processor / OLAP / metrics system) and **pushes updates** to the browser via **WebSocket/SSE** (or polls a fast query API). Pre-aggregate for low-latency reads; push only **deltas**. Connection gateways for many viewers, cached data, backplane for fan-out. **Challenges**: real-time push scaling, low-latency aggregation, delta updates, number consistency, many concurrent dashboards.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Stream Proc/  │  aggregate metrics (pre-aggregated)
   │OLAP          │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Dashboard     │  compute deltas
   │Service       │
   └──────┬───────┘
     push deltas (WebSocket/SSE)
          ▼
   ┌──────────────┐
   │Browser       │  (+ gateways + backplane for many viewers)
   └──────────────┘
Push DELTAS not full state; pre-aggregate for low latency.
\`\`\``,tech:"hld",displayNumber:282,sortKey:285},{id:"hld-286",number:283,title:"Design millions of WebSocket connections.",question:"Design millions of WebSocket connections.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Horizontally scale **connection gateways** (event-driven servers — epoll/Netty/Go/Elixir — for high connections-per-node), front with **L4 load balancers** (sticky), use a **pub/sub backplane** (Redis/Kafka) to route between gateways. Track connection→gateway mapping for targeted delivery. Heartbeats + reconnection. **Challenges**: connections-per-node limits (tune OS: fds, memory), cross-gateway routing, presence at scale, heartbeat/dead-connection cleanup, reconnection storms (backoff + jitter).

\`\`\`text
── BACK-OF-ENVELOPE ──
Conns:   10M connections ÷ 100k/node = ~100 gateway nodes (tune OS fds/memory)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Clients     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │L4 Load Balancer│  sticky by connection
   └──────┬───────┘
   ┌──────┼──────────┐
   ▼      ▼          ▼
┌──────┐┌──────┐┌──────┐
│Gateway││Gateway││Gateway│  event-driven (epoll/Netty/Go/Elixir)
│(50k+  ││conns) ││      │  heartbeats + reconnect (backoff/jitter)
└──┬───┘└──┬───┘└──┬───┘
   └───────┼───────┘
           ▼
   ┌──────────────┐
   │Pub/Sub       │  route cross-gateway + connection→gateway registry
   │Backplane     │
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:283,sortKey:286},{id:"hld-287",number:284,title:"Design presence detection.",question:"Design presence detection.",difficulty:"Hard",category:"Real-Time & High Scale",answer:`Track who's online in real time. On connect, mark online (Redis with **TTL/heartbeat** so a crash auto-expires); on disconnect/heartbeat-miss, mark offline. **Fan-out** presence changes to interested users via pub/sub — but for high-follower users, avoid broadcasting every change (batch, or on-demand query). Redis for state, pub/sub for updates. **Challenges**: accurate online/offline (TTL vs flapping), fan-out (the expensive part — many watchers), state scale, eventual consistency.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Connect     │
   └──────┬───────┘
     SET presence:user TTL (heartbeat refreshes)
          ▼
   ┌──────────────┐
   │Redis         │  key present = online; TTL expiry = offline
   │(presence     │
   │ state)       │
   └──────┬───────┘
     change → publish
          ▼
   ┌──────────────┐
   │Pub/Sub       │  fan-out to watchers (batch / on-demand for high-follower)
   └──────────────┘
Accurate status = TTL tuning (avoid flapping). Fan-out is the expensive part.
\`\`\``,tech:"hld",displayNumber:284,sortKey:287},{id:"hld-288",number:285,title:"Design online/offline user status.",question:"Design online/offline user status.",difficulty:"Intermediate",category:"Real-Time & High Scale",answer:`A focused presence system: each user's status (online/offline/last-seen) driven by heartbeats in Redis with a TTL; update **last-seen** on disconnect. Expose via query (pull) or push to contacts. To limit fan-out cost, prefer **on-demand queries** (when viewing a chat) over broadcasting every change. **Challenges**: heartbeat accuracy (TTL tuning, flapping), last-seen updates, fan-out vs pull trade-off, privacy (last-seen visibility), read scale.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │  Heartbeat   │  (while connected)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Redis         │  key refreshed = ONLINE; expires = OFFLINE
   │status + TTL  │  update last-seen on disconnect
   └──────┬───────┘
     read (prefer query-on-view over broadcast)
          ▼
   ┌──────────────┐
   │  Viewer      │  sees online / last-seen (respect privacy settings)
   └──────────────┘
Query-on-view limits fan-out. TTL tuning avoids flapping.
\`\`\``,tech:"hld",displayNumber:285,sortKey:288},{id:"hld-289",number:286,title:"Design an entire cloud platform.",question:"Design an entire cloud platform.",difficulty:"Hard",category:"Expert Systems",answer:`A cloud platform offers **compute** (VMs/containers/serverless), **storage** (object/block/file), **networking** (VPC, LB, DNS), **databases**, and **control-plane** services (IAM, billing, monitoring, provisioning API). Core: a **control plane** (APIs, orchestration, scheduling, state) + a **data plane** (VMs/storage/network serving workloads), atop physical **regions/AZs** with virtualization, SDN, and distributed storage. **Challenges**: multi-tenancy + isolation, scale, scheduling, metering/billing, IAM/security, HA, unified API over heterogeneous hardware.

\`\`\`text
── LAYERS (top → bottom) ──
   ┌──────────────┐
   │Managed Svcs  │  compute / storage / DB / networking APIs
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Control Plane │  APIs, IAM, scheduler, billing, provisioning, state
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Data Plane    │  actual VMs, storage, network serving workloads
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Virtualization│  hypervisor + SDN + distributed storage
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Physical      │  regions / AZs / hardware
   └──────────────┘
Challenges: multi-tenant isolation, scheduling, metering, IAM, HA.
\`\`\``,tech:"hld",displayNumber:286,sortKey:289},{id:"hld-290",number:287,title:"Design an AWS-like object storage service (S3).",question:"Design an AWS-like object storage service (S3).",difficulty:"Hard",category:"Expert Systems",answer:`Store immutable **objects** in **buckets**, keyed via a REST API. A **metadata service** (bucket/key → chunk locations, size, checksum, ACL, version) + a **storage layer** that chunks objects and **erasure-codes/replicates** across nodes/AZs for 11-nines durability. Versioning, lifecycle tiering, read-after-write consistency. **Challenges**: durability (erasure coding + cross-AZ), metadata scale/consistency, throughput, multipart uploads, tiering. CDN for hot content.

\`\`\`text
── BACK-OF-ENVELOPE ──
Durability: 11 nines via erasure coding (e.g. 10 data + 4 parity) across AZs
Scale:    exabytes, millions of req/sec → partition metadata, distribute chunks

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  PUT/GET bucket/key (REST)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Metadata Svc  │  bucket/key → chunk locations, checksum, ACL, version
   │(partitioned) │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Storage Layer │  object → chunks → erasure-coded across AZs (11 nines)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │     CDN      │  (hot objects); versioning + lifecycle tiering
   └──────────────┘
\`\`\``,tech:"hld",displayNumber:287,sortKey:290},{id:"hld-291",number:288,title:"Design an AWS-like message queue (SQS).",question:"Design an AWS-like message queue (SQS).",difficulty:"Hard",category:"Expert Systems",answer:`A managed, horizontally-scalable queue: producers send (stored **redundantly across AZs**); consumers **poll** with a **visibility timeout** (message hidden while processing; not deleted in time → reappears → at-least-once). Delete on success. Standard (best-effort order) vs FIFO (ordered), DLQs, delays, long polling. **Challenges**: visibility timeout redelivery, scaling (partition queues), durability (multi-AZ), ordering vs throughput, dedup. Idempotent consumers.

\`\`\`text
── VISIBILITY TIMEOUT (top → bottom) ──
   ┌──────────────┐
   │  Producer    │  send → stored redundantly (multi-AZ)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Queue       │  (partitioned, replicated)
   └──────┬───────┘
     receive → message HIDDEN (visibility timeout)
          ▼
   ┌──────────────┐
   │  Consumer    │  process
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ delete     ▼ not deleted in time
 ┌──────┐   ┌──────────────┐
 │ done │   │ REAPPEARS    │  → redelivery (at-least-once)
 └──────┘   └──────────────┘
Standard vs FIFO; DLQ; delays; long poll. Idempotent consumers.
\`\`\``,tech:"hld",displayNumber:288,sortKey:291},{id:"hld-292",number:289,title:"Design a Kubernetes-like orchestration platform.",question:"Design a Kubernetes-like orchestration platform.",difficulty:"Hard",category:"Expert Systems",answer:`A **control plane** (API server, **etcd** for state, **scheduler** placing pods, **controllers** reconciling desired vs actual) + **worker nodes** (kubelet + runtime). Users declare desired state; controllers continuously **reconcile** (self-healing: restart/reschedule failed pods). Add discovery, LB, autoscaling, rolling deploys. **Challenges**: reconciliation loop, scheduling (bin-packing + constraints), etcd consistency/HA (source of truth), self-healing, networking (CNI), scale (thousands of nodes).

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌────────────────────────────────────┐
   │ CONTROL PLANE                       │
   │  ┌────────┐ ┌──────┐ ┌───────────┐  │
   │  │API     │ │etcd  │ │Scheduler  │  │
   │  │Server  │ │(state)│ │(place pod)│  │
   │  └────────┘ └──────┘ └───────────┘  │
   │  ┌──────────────────────────────┐   │
   │  │Controllers (reconcile:        │   │
   │  │ desired vs actual, self-heal) │   │
   │  └──────────────────────────────┘   │
   └──────────────┬─────────────────────┘
          ▼ (schedule pods)
   ┌──────────────────────────────────┐
   │ WORKER NODES (kubelet + runtime)  │
   │  ┌──────┐ ┌──────┐ ┌──────┐       │
   │  │ Pod  │ │ Pod  │ │ Pod  │       │
   │  └──────┘ └──────┘ └──────┘       │
   └──────────────────────────────────┘
Declarative desired state → continuous reconciliation. etcd = source of truth.
\`\`\``,tech:"hld",displayNumber:289,sortKey:292},{id:"hld-293",number:290,title:"Design a container scheduling system.",question:"Design a container scheduling system.",difficulty:"Hard",category:"Expert Systems",answer:`The **scheduler** assigns pending containers to nodes by resource requests (CPU/memory), constraints (affinity/anti-affinity, taints, node selectors), and goals (bin-packing for utilization, spreading for HA). It **filters** feasible nodes then **scores** them, picking the best. Handle priorities/preemption, gang scheduling, reschedule on failure. **Challenges**: placement at scale, constraints, bin-pack vs spread, fairness/preemption, hotspots, decision throughput. An online constrained-optimization problem.

\`\`\`text
── SCHEDULING PIPELINE (top → bottom) ──
   ┌──────────────┐
   │Pending       │  container (needs 2 CPU, 4 GB, affinity=zone-a)
   │Container     │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │1. FILTER     │  feasible nodes (resources, affinity, taints)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │2. SCORE      │  bin-pack (utilization) vs spread (HA)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │3. ASSIGN best│  (+ priorities/preemption, gang scheduling)
   └──────────────┘
Reschedule on node failure. Fast decisions at scale (1000s nodes × pods).
\`\`\``,tech:"hld",displayNumber:290,sortKey:293},{id:"hld-294",number:291,title:"Design a serverless computing platform.",question:"Design a serverless computing platform.",difficulty:"Hard",category:"Expert Systems",answer:`Run functions on demand without managing servers: an **event source** (HTTP/queue/schedule) triggers a function; the platform **provisions an isolated env** (microVM like Firecracker), runs the code, tears it down — scaling to zero and up to thousands, billing per invocation/duration. Keep **warm** envs to reduce **cold starts**. **Challenges**: cold-start latency (pre-warming, lightweight microVMs), secure fast isolation, per-request autoscaling, statelessness, execution limits, multi-tenant security.

\`\`\`text
── FLOW (top → bottom) ──
   ┌──────────────┐
   │Event Source  │  HTTP / queue / cron
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Provision env │  warm pool? reuse : cold start (microVM/Firecracker)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Run function  │  (stateless; externalize state)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Tear down     │  scale to zero; bill per invocation/duration
   └──────────────┘
Warm pools cut cold starts. Scale 0 → thousands. Secure isolation (microVM).
\`\`\``,tech:"hld",displayNumber:291,sortKey:294},{id:"hld-295",number:292,title:"Design a multi-tenant SaaS platform.",question:"Design a multi-tenant SaaS platform.",difficulty:"Hard",category:"Expert Systems",answer:`Serve many tenants from shared infra while keeping them isolated. **Isolation models**: shared DB + \`tenant_id\` (cheapest, must filter everywhere), schema-per-tenant, or DB-per-tenant (strongest, costlier). Per-tenant auth, config, quotas, billing/metering, customization. **Challenges**: tenant isolation (data leakage = catastrophic — enforce \`tenant_id\` every query), noisy-neighbor (quotas/rate limits), per-tenant scaling, customization vs shared code, onboarding, metering/billing.

\`\`\`text
── ISOLATION MODELS (cost ↑ / isolation ↑) ──
   ┌──────────────────────────────────────┐
   │ Shared DB + tenant_id (cheap; MUST     │
   │   filter every query — leakage risk)   │
   ├──────────────────────────────────────┤
   │ Schema-per-tenant (medium)             │
   ├──────────────────────────────────────┤
   │ DB-per-tenant (strongest, costliest) ✓ │
   └──────────────────────────────────────┘
Per-tenant: auth, config, quotas, metering/billing.
Every request scoped to tenant. Noisy-neighbor → quotas/bulkheads.
\`\`\``,tech:"hld",displayNumber:292,sortKey:295},{id:"hld-296",number:293,title:"Design tenant isolation architecture.",question:"Design tenant isolation architecture.",difficulty:"Hard",category:"Expert Systems",answer:`Ensure a tenant can never access another's data or starve resources. **Data isolation**: \`tenant_id\` on every row + row-level security, or separate schemas/DBs. **Compute isolation**: per-tenant quotas, rate limits, dedicated pools/**bulkheads**. **Security**: scope every request to its tenant (from the auth token), audit. **Challenges**: zero cross-tenant access (defense in depth — RLS + app checks), noisy-neighbor, isolation vs cost, per-tenant observability.

\`\`\`text
── DEFENSE IN DEPTH (top → bottom) ──
   ┌──────────────┐
   │Request       │  → extract tenant_id from auth token
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │App Layer     │  scope every query to tenant_id
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Row-Level     │  DB enforces tenant_id filter (RLS)
   │Security      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Compute       │  per-tenant quotas + bulkheads (no noisy neighbor)
   │Isolation     │
   └──────────────┘
Data leakage = catastrophic → RLS + app checks + audit.
\`\`\``,tech:"hld",displayNumber:293,sortKey:296},{id:"hld-297",number:294,title:"Design an enterprise IAM platform.",question:"Design an enterprise IAM platform.",difficulty:"Hard",category:"Expert Systems",answer:`Manage **identities** (users, service accounts), **authentication** (passwords, MFA, SSO via SAML/OIDC, federation), and **authorization** (RBAC/ABAC). Core: identity store, auth service (issues JWT/OAuth2 tokens), policy engine, admin/audit tooling. SSO across apps, SCIM provisioning, session management. **Challenges**: secure credential storage, low-latency authz (cache), SSO/federation complexity, fine-grained perms, auditability, HA (auth is critical-path), token revocation.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │  User / App  │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Auth Service  │  passwords/MFA/SSO (SAML/OIDC) → issue JWT
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │Identity  ││Policy Engine │  RBAC/ABAC (cache decisions)
 │Store     ││(authz)       │
 └──────────┘└──────────────┘
          ▼
   ┌──────────────┐
   │Audit + Session│  SCIM provisioning; token revocation
   │Management     │
   └──────────────┘
HA critical (auth on every request). Low-latency authz via cache.
\`\`\``,tech:"hld",displayNumber:294,sortKey:297},{id:"hld-298",number:295,title:"Design OAuth2/OpenID Connect authentication architecture.",question:"Design OAuth2/OpenID Connect authentication architecture.",difficulty:"Hard",category:"Expert Systems",answer:`**OAuth2** delegates authorization: a client gets an **access token** to call APIs on a user's behalf without seeing credentials (Authorization Code + PKCE for web/mobile; client-credentials for S2S). **OIDC** adds an **ID token** (JWT) for authentication (who the user is). Components: authorization server, resource server, client. Short-lived + refreshable tokens. **Challenges**: secure flows (Auth Code + PKCE, avoid implicit), token validation (signature/issuer/audience/expiry), refresh + revocation, scopes, token leakage/CSRF.

\`\`\`text
── AUTHORIZATION CODE + PKCE (top → bottom) ──
   ┌──────────────┐
   │  Client      │  (1) redirect to auth server (+ PKCE challenge)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Auth Server   │  (2) user logs in → returns auth CODE
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │  Client      │  (3) exchange code + PKCE verifier → tokens
   └──────┬───────┘
     access token (OAuth2) + ID token (OIDC, JWT)
          ▼
   ┌──────────────┐
   │Resource Server│  (4) validate token (sig/issuer/aud/expiry) → serve
   └──────────────┘
Short-lived + refresh + revocation. Avoid implicit flow.
\`\`\``,tech:"hld",displayNumber:295,sortKey:298},{id:"hld-299",number:296,title:"Design RBAC + ABAC authorization architecture.",question:"Design RBAC + ABAC authorization architecture.",difficulty:"Hard",category:"Expert Systems",answer:`**RBAC**: users → **roles** → **permissions** (coarse, scalable — "admin can delete"). **ABAC**: decisions from **attributes** of user/resource/action/context ("edit if user.dept == doc.dept and business hours") — fine-grained, dynamic, complex. Combine: RBAC for broad access + ABAC for fine rules via a central **policy engine** (OPA); cache decisions. **Challenges**: low-latency eval at scale (cache + local), ABAC complexity, auditability, permission sprawl, policy propagation.

\`\`\`text
── DECISION FLOW (top → bottom) ──
   ┌──────────────┐
   │Request       │  user + action + resource + context
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Policy Engine │  (OPA)
   │  RBAC: user→roles→permissions (coarse)
   │  ABAC: attributes (user.dept == doc.dept, time...)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼ allow      ▼ deny
 ┌──────┐   ┌──────┐
 │serve │   │403   │
 └──────┘   └──────┘
Cache decisions for low latency. RBAC (broad) + ABAC (fine-grained).
\`\`\``,tech:"hld",displayNumber:296,sortKey:299},{id:"hld-300",number:297,title:"Design a global API platform handling billions of requests.",question:"Design a global API platform handling billions of requests.",difficulty:"Hard",category:"Expert Systems",answer:`Front with **CDN + edge** (cache, absorb, TLS near users) → **API gateways** (auth, rate limiting, routing) in multiple regions → horizontally-scaled stateless services → sharded/replicated data + caching. GeoDNS/anycast routes to the nearest region. Everything redundant (no SPOF), autoscaled, aggressive caching + rate limiting. **Challenges**: massive throughput (edge cache + horizontal scale), global latency (multi-region + CDN), distributed rate limiting, auth at scale (token validation + cache), sampled tracing, DDoS, multi-region consistency.

\`\`\`text
── BACK-OF-ENVELOPE ──
Requests: billions/day; peak millions/sec → CDN offloads most; multi-region
Auth:     validate token per request → cache; distributed rate limiting (Redis)

── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │CDN + Edge    │  cache, TLS, DDoS/WAF, absorb near users
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Regional API  │  auth, rate limit, route (GeoDNS → nearest region)
   │Gateways      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Stateless Svcs│  autoscaled, redundant (no SPOF)
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │Sharded/Cached│  data (replicated)
   │Data          │
   └──────────────┘
Sampled tracing (billions of traces). Everything redundant + autoscaled.
\`\`\``,tech:"hld",displayNumber:297,sortKey:300},{id:"hld-301",number:298,title:"Design a globally distributed e-commerce platform.",question:"Design a globally distributed e-commerce platform.",difficulty:"Hard",category:"Expert Systems",answer:`Multi-region e-commerce: catalog/search (cached + CDN, per-region replicas), cart (regional Redis), **inventory** (the hard part — global consistency to avoid overselling: per-warehouse/region ownership or a strongly-consistent inventory service), orders (transactional, replicated), payments (idempotent), fulfillment (event-driven). Route to nearest region; replicate catalog globally; inventory authoritative. **Challenges**: global inventory consistency, catalog replication (eventual OK), order/payment consistency (strong), failover, latency, flash-sale spikes.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  → nearest region
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Region US ││Region EU │  catalog/search (CDN, eventual OK), cart (Redis),
│          ││          │  orders (transactional + replicate)
└────┬─────┘└────┬─────┘
     └─────┬──────┘
           ▼
   ┌──────────────┐
   │INVENTORY     │  authoritative + globally consistent (region/warehouse
   │(strong)      │  ownership) → NO overselling
   └──────────────┘
Fulfillment event-driven. Payments idempotent. Flash-sale → waiting room.
\`\`\``,tech:"hld",displayNumber:298,sortKey:301},{id:"hld-302",number:299,title:"Design a globally distributed social-media platform.",question:"Design a globally distributed social-media platform.",difficulty:"Hard",category:"Expert Systems",answer:`Multi-region social: user data + **social graph** (sharded, replicated), **feed** generation (hybrid fan-out: write for normal, read for celebrities), posts/media (object storage + **CDN**), messaging, notifications — served from nearest region with cross-region replication (mostly **eventual consistency**, fine for feeds). Heavy caching (feed/graph caches). **Challenges**: feed generation (hybrid fan-out, celebrity), graph queries, read scale, eventual consistency, global media, real-time features, moderation.

\`\`\`text
── ARCHITECTURE (top → bottom) ──
   ┌──────────────┐
   │Global Routing│  → nearest region (mostly eventual consistency)
   └──────┬───────┘
   ┌──────┴──────┐
   ▼             ▼
┌──────────┐┌──────────┐
│Region A  ││Region B  │  cross-region replication
│ ┌──────┐ ││ ┌──────┐ │
│ │Graph │ ││ │Graph │ │  sharded + replicated + cached
│ │Feed  │ ││ │Feed  │ │  hybrid fan-out (write normal / read celeb)
│ └──────┘ ││ └──────┘ │
└──────────┘└──────────┘
Posts/media → object storage + CDN. Messaging/notifications real-time.
Challenges: feed fan-out (celebrity), graph queries, read scale, moderation.
\`\`\``,tech:"hld",displayNumber:299,sortKey:302},{id:"hld-303",number:300,title:"Design a system for billions of users, millions of RPS, multi-region failures, strong security, observability, DR, and zero-downtime deploys.",question:"Design a system for billions of users, millions of RPS, multi-region failures, strong security, observability, DR, and zero-downtime deploys.",difficulty:"Hard",category:"Expert Systems",answer:`The capstone — combine everything. **Edge**: CDN + GeoDNS + DDoS + WAF. **Compute**: stateless microservices, autoscaled, multi-region active-active behind regional gateways. **Data**: sharded + replicated, per-dataset consistency (strong for money, eventual for feeds), cross-region with defined **RPO/RTO**. **Async**: Kafka. **Resilience**: no SPOF, circuit breakers, bulkheads, graceful degradation, region failover with capacity headroom. **Security**: zero-trust, mTLS, IAM, encryption, secrets. **Observability**: metrics/logs/traces (sampled) + SLOs. **Deploys**: CI/CD rolling/blue-green/canary + feature flags. **Testing**: chaos + DR drills. Drive every decision from requirements + trade-offs.

\`\`\`text
── FULL STACK (top → bottom) ──
   ┌──────────────┐
   │EDGE          │  CDN + GeoDNS + DDoS/WAF
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │REGIONAL      │  auth, rate limit, route (multi-region active-active)
   │GATEWAYS      │
   └──────┬───────┘
          ▼
   ┌──────────────┐
   │STATELESS     │  autoscaled + circuit breakers + bulkheads
   │MICROSERVICES │  + graceful degradation (no SPOF)
   └──────┬───────┘
    ┌─────┴──────┐
    ▼            ▼
 ┌──────────┐┌──────────────┐
 │DATA      ││Kafka (async  │
 │sharded + ││ pipelines)   │
 │replicated││              │
 │(per-data ││              │
 │ consist) │└──────────────┘
 └──────────┘
Cross-cutting: zero-trust/mTLS/IAM/encryption + metrics/logs/traces (sampled)
+ SLOs + CI/CD (rolling/blue-green/canary + feature flags) + chaos + DR drills.
Availability = redundancy × automation × testing.
\`\`\``,tech:"hld",displayNumber:300,sortKey:303},{id:"hld-304",number:301,title:"What are the functional requirements?",question:"What are the functional requirements?",difficulty:"Basic",category:"Follow-Up Questions",answer:`Functional requirements define **what the system does** — the features and behaviors (e.g. for a URL shortener: shorten a URL, redirect, custom aliases, analytics). Clarify them first to scope the design and avoid over/under-building. In an interview, list the core features and explicitly **defer** out-of-scope ones ("I'll focus on shorten + redirect; analytics is out of scope for now"). This drives the API and data model.

\`\`\`text
Functional = WHAT it does (features/behaviors). List core features + defer non-core explicitly.
URL shortener: create short link, redirect, (optional) custom alias, expiry, analytics.
Drives the API surface + data model. Always clarify before designing.
\`\`\``,tech:"hld",displayNumber:301,sortKey:304},{id:"hld-305",number:302,title:"What are the non-functional requirements?",question:"What are the non-functional requirements?",difficulty:"Basic",category:"Follow-Up Questions",answer:`Non-functional requirements define **how well** the system performs: scalability (RPS/users), availability (nines), latency (p99), consistency, durability, security, and cost. These drive the architecture far more than features do — a URL shortener that's read-heavy at 100k RPS with 99.99% availability needs caching, replication, and CDN. Always quantify them (numbers, not "fast"/"scalable").

\`\`\`text
Non-functional = HOW WELL: scalability (RPS/users), availability (nines), latency (p99),
   consistency, durability, security, cost. QUANTIFY (numbers, not adjectives).
These drive architecture more than features. Clarify early.
\`\`\``,tech:"hld",displayNumber:302,sortKey:305},{id:"hld-306",number:303,title:"What assumptions are you making?",question:"What assumptions are you making?",difficulty:"Basic",category:"Follow-Up Questions",answer:'State assumptions explicitly to bound the problem and show reasoning: scale (users, RPS), read/write ratio, data size, growth, and constraints (budget, latency, regions). E.g. "Assume 100M users, 10:1 read:write, ~1KB per record, single-region to start." Assumptions let you do capacity estimation and justify choices; the interviewer will correct you if they differ, which is fine — it shows structured thinking.\n\n```text\nState assumptions to bound the problem: users, RPS, read:write, data size, growth, constraints.\nE.g. "100M users, 10:1 read:write, 1KB/record, single-region first."\nEnables capacity math + justifies choices; interviewer corrects if needed.\n```',tech:"hld",displayNumber:303,sortKey:306},{id:"hld-307",number:304,title:"How many users (scale)?",question:"How many users (scale)?",difficulty:"Basic",category:"Follow-Up Questions",answer:"Establish the user scale to size everything downstream. Distinguish total registered users vs concurrent/active. A system for 1M users differs vastly from 1B (single-region + one DB vs multi-region + sharding + CDN). Ask for the number (or assume one) and derive DAU/MAU, RPS, and storage from it. Scale is the primary driver of architectural complexity.\n\n```text\nTotal users vs concurrent/active. 1M -> simple (single region, one DB); 1B -> sharding + multi-region + CDN.\nDerive DAU/MAU -> RPS -> storage. Scale = primary driver of complexity. Ask or assume a number.\n```",tech:"hld",displayNumber:304,sortKey:307},{id:"hld-308",number:305,title:"What is the DAU/MAU?",question:"What is the DAU/MAU?",difficulty:"Basic",category:"Follow-Up Questions",answer:"DAU (Daily Active Users) / MAU (Monthly Active Users) measure real load, not just registrations. They drive RPS and storage estimates. E.g. 100M DAU, each doing 10 actions/day → 1B requests/day → ~11.5k RPS average (and 2-5× at peak). Use DAU × actions-per-user to estimate request volume, and DAU × data-per-user for storage growth. Active users, not total, determine capacity.\n\n```text\nDAU/MAU = real load. 100M DAU × 10 actions/day = 1B req/day ≈ 11.5k RPS avg (peak 2-5×).\nRequests ≈ DAU × actions/user. Storage growth ≈ DAU × data/user. Size capacity from active users.\n```",tech:"hld",displayNumber:305,sortKey:308},{id:"hld-309",number:306,title:"How many requests per second (RPS)?",question:"How many requests per second (RPS)?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Estimate average RPS = total daily requests ÷ 86,400 seconds. E.g. 1B requests/day ÷ 86,400 ≈ 11.5k RPS average. This sizes your service fleet, database, and caching. Always compute peak too (see next) — average RPS under-provisions. RPS drives horizontal scaling (how many stateless instances), DB throughput (reads/writes per sec), and cache sizing.\n\n```text\nAvg RPS = daily requests / 86,400. 1B/day ≈ 11.5k RPS avg.\nSizes: service fleet, DB throughput, cache. Always also compute PEAK (avg under-provisions).\n```",tech:"hld",displayNumber:306,sortKey:309},{id:"hld-310",number:307,title:"What is the peak RPS?",question:"What is the peak RPS?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Peak RPS is what you must actually provision for — it's typically **2-5× (or more) the average** due to daily cycles, time zones, and events (flash sales, viral moments can spike 10-100×). Size capacity + autoscaling headroom for peak, not average, or you'll fall over exactly when it matters. E.g. 11.5k avg → provision for ~50k peak + burst headroom. Handle extreme spikes with queues, rate limiting, and load shedding.\n\n```text\nPeak ≈ 2-5× avg (events/viral: 10-100×). PROVISION for peak, not average (+ autoscaling headroom).\n11.5k avg -> plan ~50k peak. Extreme spikes: queues, rate limiting, load shedding, waiting rooms.\n```",tech:"hld",displayNumber:307,sortKey:310},{id:"hld-311",number:308,title:"What is the read/write ratio?",question:"What is the read/write ratio?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"The read:write ratio shapes the architecture. **Read-heavy** (e.g. 100:1 — social feeds, URL shorteners) → invest in caching, read replicas, CDN, denormalized read models. **Write-heavy** (e.g. logging, IoT, analytics) → optimize ingestion (LSM stores, Kafka buffering, sharding writes, async processing). Most user-facing systems are read-heavy. Always ask/assume this ratio — it decides where you spend your scaling effort.\n\n```text\nRead-heavy (100:1, feeds/URLs) -> caching + read replicas + CDN + denormalized reads.\nWrite-heavy (logs/IoT/analytics) -> LSM stores, Kafka buffering, shard writes, async processing.\nRatio decides where scaling effort goes. Most user-facing systems are read-heavy.\n```",tech:"hld",displayNumber:308,sortKey:311},{id:"hld-312",number:309,title:"How much data is generated per day?",question:"How much data is generated per day?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Estimate daily data = writes/day × average record size (plus media). E.g. 100M new records/day × 1KB = 100GB/day. This drives storage capacity, database choice (can one node hold it? need sharding?), retention policy, and cost. Include replication factor (×3) and indexes (overhead). Combine with growth to project storage over years (see next). Data volume is a key scaling constraint.\n\n```text\nDaily data = writes/day × avg record size (+ media). 100M × 1KB = 100GB/day.\nMultiply by replication (×3) + index overhead. Drives storage, DB choice (shard?), retention, cost.\n```",tech:"hld",displayNumber:309,sortKey:312},{id:"hld-313",number:310,title:"What is the storage requirement after 5 years?",question:"What is the storage requirement after 5 years?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Project cumulative storage: daily data × 365 × 5 (× replication factor, + indexes/overhead), accounting for growth. E.g. 100GB/day × 365 × 5 ≈ 182TB raw, ~550TB with 3× replication. This determines whether you need sharding (no single node holds it), tiering (hot/cold, archive old data), and the storage budget. Long-term projection justifies partitioning and lifecycle/retention decisions up front.\n\n```text\n5-yr storage ≈ daily data × 365 × 5 × replication (+ index overhead + growth).\n100GB/day -> ~182TB raw, ~550TB at 3× replication -> needs sharding + hot/cold tiering + archival.\nLong-term projection justifies partitioning + lifecycle decisions.\n```",tech:"hld",displayNumber:310,sortKey:313},{id:"hld-314",number:311,title:"What is the latency requirement?",question:"What is the latency requirement?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'Define target latency, usually at **percentiles** (p50/p95/**p99**) not average — tail latency is what users feel. E.g. "p99 < 200ms for reads." Latency targets drive caching (avoid slow DB hits), CDN/edge (reduce network distance), data locality (co-locate/regional), avoiding chatty sync calls (aggregate), and async processing (move slow work off the request path). Tight latency = more caching, fewer hops, and regional deployment.\n\n```text\nTarget by percentile (p50/p95/p99), not average — tail latency is what users feel. e.g. "p99 < 200ms".\nAchieve via: caching, CDN/edge, data locality/regional, fewer sync hops (aggregate), async slow work.\n```',tech:"hld",displayNumber:311,sortKey:314},{id:"hld-315",number:312,title:"What availability do you need?",question:"What availability do you need?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:`Pick a target in "nines" based on business criticality: 99.9% (~8.7h/yr) for internal tools, 99.99% (~52min/yr) for most consumer apps, 99.999% (~5min/yr) for critical infra/payments. Higher availability = eliminating SPOFs, multi-AZ/region redundancy, automated failover, and zero-downtime deploys — each nine costs significantly more. Match the target to the cost of downtime; don't over-engineer availability where it isn't needed.

\`\`\`text
99.9% (~8.7h/yr) internal | 99.99% (~52min/yr) consumer | 99.999% (~5min/yr) critical/payments.
Higher nines = no SPOF + multi-region redundancy + auto-failover + zero-downtime deploys (costs more each nine).
Match target to cost of downtime; don't over-engineer.
\`\`\``,tech:"hld",displayNumber:312,sortKey:315},{id:"hld-316",number:313,title:"What happens if one service goes down?",question:"What happens if one service goes down?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Design so a single service failure is **contained**, not catastrophic: run multiple instances (redundancy → traffic reroutes to healthy ones), use **circuit breakers** (fail fast when a dependency is down), **timeouts** (don't hang), **graceful degradation** (drop the non-critical feature, keep core working), and **retries** for transient issues. Distinguish critical vs optional dependencies — an optional one (recommendations) degrades; a critical one (payment) needs failover. Answer with: redundancy + isolation + degradation.\n\n```text\nOne service down -> contained, not catastrophic: redundant instances (reroute) + circuit breaker + timeout\n   + graceful degradation (optional dep off, core works) + retries.\nCritical dep -> failover; optional dep -> degrade. Isolation limits blast radius.\n```",tech:"hld",displayNumber:313,sortKey:316},{id:"hld-317",number:314,title:"What happens if the database goes down?",question:"What happens if the database goes down?",difficulty:"Hard",category:"Follow-Up Questions",answer:"The DB is often the most critical component, so: run **replicas** with **automated failover** (promote a replica → primary, repoint via proxy/discovery), use **multi-AZ** so an AZ loss doesn't take it out, and serve reads from replicas/cache during a primary failure. A cache can absorb reads temporarily (cache-aside → DB outage = more misses, not total failure). For writes, queue them (if the write path can be async) or degrade gracefully. **Avoid split-brain** on failover (fencing).\n\n```text\nDB down -> replicas + automated failover (promote replica, repoint clients) + multi-AZ.\nReads: serve from replicas/cache. Writes: queue (if async-able) or degrade. Prevent split-brain (fencing).\nCache absorbs reads during primary loss (cache-aside = more misses, not outage).\n```",tech:"hld",displayNumber:314,sortKey:317},{id:"hld-318",number:315,title:"What happens if Redis (cache) goes down?",question:"What happens if Redis (cache) goes down?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"With **cache-aside**, a Redis outage means reads fall through to the database — higher latency and DB load, but not a total failure (design the DB to survive the added load, or the cache miss storm). Mitigate: Redis **replication + cluster** (HA, so it rarely fully fails), a **local/in-process cache** as a second layer, and **request coalescing/singleflight** to prevent a thundering herd hammering the DB on cold cache. Never let the cache be a hard dependency — it should be an optimization.\n\n```text\nCache-aside + Redis down -> reads hit DB (slower, more load) but not fatal. Ensure DB can absorb it.\nMitigate: Redis cluster/replication (HA), local L1 cache, singleflight (avoid herd on cold cache).\nCache = optimization, not a hard dependency.\n```",tech:"hld",displayNumber:315,sortKey:318},{id:"hld-319",number:316,title:"What happens if Kafka goes down?",question:"What happens if Kafka goes down?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Kafka is usually **replicated** (RF=3, multi-broker/AZ), so a broker loss triggers leader election and continues — full-cluster failure is rare. If it does: producers **buffer** locally (bounded by `buffer.memory`, then block/fail — decide to block or drop per use case) and consumers pause (resume from committed offsets, no data loss due to retention). For critical writes, the **outbox pattern** keeps events in the DB until Kafka recovers. Design producers to handle unavailability (retry/buffer/degrade) and consumers to resume cleanly.\n\n```text\nKafka replicated (RF=3, multi-AZ) -> broker loss survives via leader election. Full outage rare.\nProducers: buffer (bounded) then block/drop per use case; outbox keeps events in DB until recovery.\nConsumers: pause, resume from committed offset (retention -> no loss). Design for unavailability.\n```",tech:"hld",displayNumber:316,sortKey:319},{id:"hld-320",number:317,title:"How do you scale horizontally?",question:"How do you scale horizontally?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Add more instances and distribute load — the foundation of scale. Prerequisites: **stateless services** (any instance serves any request; externalize state to Redis/DB), a **load balancer**, and **partitioned data** (shard so the DB isn't a bottleneck). Add autoscaling (scale with load), caching (reduce backend load), and async processing (offload spikes). For stateful components (DBs), scale via sharding + replication. Answer: stateless tier + LB + sharded data + autoscaling.\n\n```text\nHorizontal scale = more instances + LB. Needs: stateless services (externalize state), sharded data, autoscaling.\nReduce backend load with caching + async processing. Stateful (DB) scales via sharding + replication.\n```",tech:"hld",displayNumber:317,sortKey:320},{id:"hld-321",number:318,title:"Where would you use caching?",question:"Where would you use caching?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'Cache at multiple layers to cut latency and backend load: **CDN/edge** (static assets, some dynamic), **client/browser**, **API gateway** (response cache), **application cache** (Redis for hot DB reads, sessions, computed results), and **database** (query/result cache, buffer pool). Cache read-heavy, expensive-to-compute, and slow-to-fetch data. Each layer catches more before hitting the origin. Answer with the layered picture, not just "use Redis."\n\n```text\nLayered caching: CDN/edge -> client -> API gateway -> app cache (Redis) -> DB cache/buffer pool.\nCache: read-heavy, expensive-to-compute, slow-to-fetch data. Each layer offloads the origin.\n```',tech:"hld",displayNumber:318,sortKey:321},{id:"hld-322",number:319,title:"What would you cache?",question:"What would you cache?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Cache data that is **read-frequently, changes rarely, and is expensive to fetch/compute**: hot DB rows, computed aggregates (counts, rankings), session/auth data, rendered pages/fragments, static assets (CDN), and results of slow queries or external API calls. Don't cache rarely-read or highly-volatile data (low hit rate, constant invalidation), or sensitive data without care. Prioritize by (access frequency × fetch cost). Answer: hot, expensive, slowly-changing data.\n\n```text\nCache: hot rows, computed aggregates, sessions/auth, rendered fragments, static assets, slow-query/API results.\nDon't cache: rarely-read or highly-volatile data (low hit rate / constant invalidation).\nPrioritize by access frequency × fetch cost.\n```",tech:"hld",displayNumber:319,sortKey:322},{id:"hld-323",number:320,title:"How do you invalidate the cache?",question:"How do you invalidate the cache?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Choose per data's staleness tolerance: **TTL** (expire after N seconds — simple, bounded staleness, good default), **write-through/explicit invalidation** (delete/update the key on write — fresh, needs write hooks), or **event-based** (invalidate via CDC/Kafka change events — scales across nodes). Combine (TTL as a safety net + event-based for freshness). Watch for the **thundering herd** on invalidation (singleflight) and cross-node invalidation (pub/sub). Answer: TTL + explicit/event-based, matched to freshness needs.\n\n```text\nTTL (bounded staleness, simple default) | explicit invalidation on write (fresh) | event-based (CDC/Kafka, cross-node).\nCombine: event-based for freshness + TTL safety net. Guard thundering herd (singleflight); cross-node via pub/sub.\nMatch strategy to data's staleness tolerance.\n```",tech:"hld",displayNumber:320,sortKey:323},{id:"hld-324",number:321,title:"SQL or NoSQL — and why?",question:"SQL or NoSQL — and why?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'Decide from data + access patterns: **SQL** for structured, relational, transactional data needing ACID/joins/strong consistency (payments, orders, inventory). **NoSQL** for large scale, flexible/semi-structured data, high write throughput, or specific access patterns (document, key-value, wide-column, graph). Many systems use **both** (polyglot persistence): SQL for transactions, NoSQL for scale/flexibility. Justify with the specific need (consistency? scale? flexibility? relationships?), not preference. Often the answer is "SQL for core transactions, NoSQL where scale/pattern demands it."\n\n```text\nSQL: relational, ACID, joins, strong consistency (payments/orders/inventory).\nNoSQL: scale, flexible schema, high write, specific pattern (document/KV/wide-column/graph).\nOften BOTH (polyglot). Justify by need: consistency vs scale vs flexibility vs relationships.\n```',tech:"hld",displayNumber:321,sortKey:324},{id:"hld-325",number:322,title:"How would you partition the database?",question:"How would you partition the database?",difficulty:"Hard",category:"Follow-Up Questions",answer:"**Horizontally shard** by a key so no single node holds everything. Strategies: **hash** (even distribution, no range scans), **range** (range queries, risk hotspots), **directory** (flexible lookup), or **consistent hashing** (minimal reshuffle on scaling). Pick a shard key with **high cardinality + even access** (e.g. user_id). Also consider **vertical** partitioning (split columns/features). Plan for cross-shard queries (avoid or scatter-gather) and resharding. Answer: shard by a well-chosen key, with a strategy fitting the query pattern.\n\n```text\nHorizontal shard by key: hash (even) | range (scans, hotspot risk) | directory | consistent hashing (easy resharding).\nShard key: high cardinality + even access (e.g. user_id). Avoid cross-shard joins; plan resharding.\n```",tech:"hld",displayNumber:322,sortKey:325},{id:"hld-326",number:323,title:"How would you choose the shard key?",question:"How would you choose the shard key?",difficulty:"Hard",category:"Follow-Up Questions",answer:"A good shard key has: **high cardinality** (many distinct values → even spread), **uniform access** (no hotspots), and aligns with the **most common query** (so queries hit one shard, avoiding scatter-gather). E.g. `user_id` for user-centric data. Avoid low-cardinality (few values → imbalance) or monotonic keys (timestamps → all writes hit one shard). If queries span keys, you may need a composite key or secondary index. The key choice determines whether sharding actually helps or creates hot shards.\n\n```text\nGood shard key: high cardinality + uniform access + matches common query (single-shard lookups).\nAvoid: low cardinality (imbalance), monotonic/timestamp (all writes -> one shard).\ne.g. user_id for user-centric data. Bad key -> hot shards + scatter-gather.\n```",tech:"hld",displayNumber:323,sortKey:326},{id:"hld-327",number:324,title:"What happens when a shard becomes hot?",question:"What happens when a shard becomes hot?",difficulty:"Hard",category:"Follow-Up Questions",answer:"A hot shard (disproportionate load — bad key or a viral entity) becomes a bottleneck while others idle. Fixes: **split** the hot shard (rebalance its key range to new shards), **choose a better shard key** (higher cardinality), **salt** hot keys (spread one key across shards, sacrificing single-shard queries), add a **cache** in front of the hot data, or use **read replicas** for the hot shard. Consistent hashing eases rebalancing. Answer: rebalance/split + better key + caching + salting.\n\n```text\nHot shard -> bottleneck. Fixes: split/rebalance the shard (consistent hashing), better shard key,\n   salt hot keys (spread, loses single-shard query), cache the hot data, read replicas for it.\nRoot cause is usually key choice or a viral entity.\n```",tech:"hld",displayNumber:324,sortKey:327},{id:"hld-328",number:325,title:"How do you handle hot keys?",question:"How do you handle hot keys?",difficulty:"Hard",category:"Follow-Up Questions",answer:"A hot key (one item getting massive traffic — a viral post, a celebrity) overloads its shard/cache node. Mitigate: **cache** it aggressively (even replicate the hot key across cache nodes or use a local in-process cache), **salt/shard** the key into sub-keys to spread writes (e.g. distributed counter shards), add **read replicas**, and use **request coalescing** (singleflight) so concurrent requests for it hit the backend once. For counters, use sharded/approximate counting. Answer: cache + replicate + salt + coalesce.\n\n```text\nHot key -> overloads one node. Mitigate: aggressive cache (+ replicate hot key / local L1), salt into sub-keys\n   (spread writes, e.g. sharded counter), read replicas, singleflight (coalesce concurrent requests).\nCounters -> sharded/approximate.\n```",tech:"hld",displayNumber:325,sortKey:328},{id:"hld-329",number:326,title:"How do you handle duplicate requests?",question:"How do you handle duplicate requests?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Duplicates arise from client retries, network timeouts, and at-least-once delivery. Make operations **idempotent**: attach a unique **idempotency key** (request id), track processed keys (dedup store / DB unique constraint), and on a duplicate return the stored result without re-executing. For reads it's harmless; for writes/side-effects it's essential (no double charge/order). Alternatively use **upserts** (write final state) so reprocessing is a no-op. Answer: idempotency keys + dedup store.\n\n```text\nDuplicates (retries/timeouts/at-least-once) -> idempotency key + dedup store (unique constraint):\n   seen? return stored result : process + record. Or upsert (state, not delta) = no-op on replay.\nEssential for writes/side-effects (no double charge).\n```",tech:"hld",displayNumber:326,sortKey:329},{id:"hld-330",number:327,title:"How do you make APIs idempotent?",question:"How do you make APIs idempotent?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'Client sends a unique **Idempotency-Key** header per operation; the server, in a transaction, inserts the key (unique constraint) — if new, process and store the result against the key; if it exists, return the stored result without re-executing. Handle in-progress duplicates with a lock/"processing" state. GET/PUT/DELETE are naturally idempotent; POST needs the key. Set a key TTL. This makes retries safe. Answer: idempotency key + atomic dedup + return cached result.\n\n```text\nIdempotency-Key header. Server (tx): INSERT key (unique) -> new? process + store result : return stored result.\nIn-progress dup -> lock/"processing". GET/PUT/DELETE naturally idempotent; POST needs the key. Key TTL.\n```',tech:"hld",displayNumber:327,sortKey:330},{id:"hld-331",number:328,title:"How do you handle retries?",question:"How do you handle retries?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Retry only **transient** failures (timeouts, 503, network) and only **idempotent** operations (or use idempotency keys). Use **exponential backoff + jitter** (avoid hammering + thundering herd), cap total attempts/time, and don't retry terminal errors (400/validation/declined). Pair with a **circuit breaker** so retries don't prolong an outage. For unknown outcomes (timeout after sending), **query status** before retrying. Answer: bounded, backoff+jitter, idempotent-only, transient-only, + circuit breaker.\n\n```text\nRetry: transient errors only, idempotent ops only, exponential backoff + jitter, bounded attempts.\nDon't retry terminal errors. Add circuit breaker (don't prolong outages). Unknown outcome -> query status first.\n```",tech:"hld",displayNumber:328,sortKey:331},{id:"hld-332",number:329,title:"How do you prevent retry storms?",question:"How do you prevent retry storms?",difficulty:"Hard",category:"Follow-Up Questions",answer:"A retry storm = many clients retrying simultaneously, overwhelming a recovering service. Prevent with: **exponential backoff + jitter** (spread retries out, break synchronization), **circuit breakers** (stop calling a failing service entirely), **retry budgets** (cap the % of traffic that is retries — e.g. max 10%), **load shedding** at the server (reject excess early), and idempotency (so failed retries don't corrupt). Jitter is the key ingredient — without it, backoff still synchronizes. Answer: jitter + circuit breaker + retry budget + load shedding.\n\n```text\nRetry storm = synchronized retries overwhelm recovery. Prevent: backoff + JITTER (de-sync), circuit breakers,\n   retry budgets (cap retry % of traffic), server-side load shedding. Jitter is essential.\n```",tech:"hld",displayNumber:329,sortKey:332},{id:"hld-333",number:330,title:"How do you handle message duplication?",question:"How do you handle message duplication?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"At-least-once delivery (Kafka, queues) produces duplicates on retries/rebalances. Handle with **idempotent consumers**: attach a unique event id, track processed ids (dedup store / DB unique constraint), and skip already-seen ids. Or use **upserts** (write final state) / version checks so reprocessing is harmless. For Kafka-internal exactly-once, use transactions. Ensure the dedup store is **durable** (survives restarts), not in-memory. Answer: unique event id + durable dedup, or idempotent upserts.\n\n```text\nAt-least-once -> duplicates. Idempotent consumer: unique event id + durable dedup store (unique constraint) -> skip seen.\nOr upsert / version check (reprocessing harmless). Kafka-internal EOS via transactions. Dedup store must be durable.\n```",tech:"hld",displayNumber:330,sortKey:333},{id:"hld-334",number:331,title:"How do you guarantee ordering?",question:"How do you guarantee ordering?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Order is guaranteed within a **partition/queue**, not globally. To keep related events ordered, route them to the same partition via a **key** (entity id → same partition → sequential processing by one consumer). Enable **idempotent producers** so retries don't reorder. For strict **global** ordering, use a single partition (throughput bottleneck) or sequence numbers + reordering at the consumer. Usually per-key ordering is sufficient. Answer: key-based partitioning for per-entity order; global ordering is expensive (single partition).\n\n```text\nOrder guaranteed per partition. Key by entity (orderId) -> same partition -> sequential -> per-entity order.\nIdempotent producer avoids reorder on retry. Global order = single partition (bottleneck) or seq# + reorder.\nPrefer per-key ordering (sufficient for most).\n```",tech:"hld",displayNumber:331,sortKey:334},{id:"hld-335",number:332,title:"At-most-once, at-least-once, or exactly-once?",question:"At-most-once, at-least-once, or exactly-once?",difficulty:"Hard",category:"Follow-Up Questions",answer:`- **At-most-once**: no duplicates, possible loss (commit before processing) — only for loss-tolerant data.
- **At-least-once**: no loss, possible duplicates (commit after processing) — the common default; pair with idempotency.
- **Exactly-once**: no loss, no duplicates — via Kafka transactions (read-process-write) or idempotency keys for effects; more overhead.

Most systems choose **at-least-once + idempotent processing** (achieves exactly-once effects simply). Answer: at-least-once + idempotency is the pragmatic default; true exactly-once only where required.

\`\`\`text
at-most-once (no dup, may lose) | at-least-once (no loss, may dup) | exactly-once (neither, costly).
Pragmatic default: at-least-once + idempotency = exactly-once EFFECTS, simply.
True EOS (transactions) only where genuinely required.
\`\`\``,tech:"hld",displayNumber:332,sortKey:335},{id:"hld-336",number:333,title:"How do you handle failures (general)?",question:"How do you handle failures (general)?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Assume any component can fail and design for **contained, recoverable** failure: **redundancy** (no SPOF), **timeouts** (fail fast), **retries with backoff** (transient), **circuit breakers** (stop calling failing deps), **bulkheads** (isolate resources), **graceful degradation** (drop non-critical features), **failover** (promote standbys), and **idempotency** (safe retries). Add monitoring/alerting + automated recovery. Distinguish critical vs optional dependencies. Answer with this layered resilience toolkit, applied per dependency.\n\n```text\nAssume failure. Toolkit: redundancy (no SPOF), timeouts, retry+backoff, circuit breaker, bulkhead,\n   graceful degradation, failover, idempotency + monitoring/auto-recovery.\nCritical dep -> failover; optional dep -> degrade. Keep blast radius small.\n```",tech:"hld",displayNumber:333,sortKey:336},{id:"hld-337",number:334,title:"How do you monitor the system?",question:"How do you monitor the system?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Use the **three pillars**: **metrics** (time-series: RED — Rate/Errors/Duration, and USE — Utilization/Saturation/Errors — via Prometheus/Grafana), **logs** (structured, centralized — ELK/Loki), and **traces** (distributed tracing across services — OpenTelemetry/Jaeger), correlated by trace id. Add **health checks** (liveness/readiness), **SLOs + alerting** on user-facing symptoms (error rate, latency), and dashboards. Alert on symptoms users feel plus leading indicators (saturation, disk). Answer: metrics + logs + traces + health checks + SLO-based alerting.\n\n```text\n3 pillars: metrics (RED/USE, Prometheus) + logs (structured, ELK/Loki) + traces (OpenTelemetry/Jaeger), correlated by trace id.\n+ health checks (liveness/readiness) + SLO-based alerting (on user-facing symptoms + leading indicators) + dashboards.\n```",tech:"hld",displayNumber:334,sortKey:337},{id:"hld-338",number:335,title:"What metrics would you collect?",question:"What metrics would you collect?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:`- **RED** (per service): **R**ate (RPS), **E**rrors (error rate), **D**uration (latency p50/p95/p99).
- **USE** (per resource): **U**tilization, **S**aturation, **E**rrors (CPU, memory, disk, network).
- Plus **business metrics** (orders/sec, signups), **dependency health** (DB connections, queue depth/lag, cache hit rate), and **saturation signals** (thread pools, connection pools).

Focus on user-facing signals (latency, errors) + capacity signals (saturation). Answer: RED + USE + business + dependency metrics.

\`\`\`text
RED (service): Rate, Errors, Duration (p50/p95/p99). USE (resource): Utilization, Saturation, Errors.
+ business (orders/sec), dependency health (queue lag, cache hit rate, DB connections), saturation (pools).
Focus: user-facing (latency/errors) + capacity (saturation).
\`\`\``,tech:"hld",displayNumber:335,sortKey:338},{id:"hld-339",number:336,title:"How would you trace requests?",question:"How would you trace requests?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Use **distributed tracing**: generate a **trace id** at the edge, propagate it on every call (W3C `traceparent` header, across sync and async/Kafka hops), and have each service emit **spans** (with parent-child links) to a collector (OpenTelemetry → Jaeger/Tempo). The UI reconstructs the end-to-end request path with per-hop latency, pinpointing bottlenecks and failures. Correlate traces with logs/metrics via the trace id. Use **sampling** to control volume. Answer: trace id propagation + spans + OpenTelemetry + sampling.\n\n```text\nTrace id at edge -> propagate (traceparent) across all hops (sync + async) -> each service emits spans -> collector (Jaeger).\nEnd-to-end latency waterfall; correlate with logs/metrics by trace id; sample to control volume. (OpenTelemetry)\n```",tech:"hld",displayNumber:336,sortKey:339},{id:"hld-340",number:337,title:"How would you deploy it?",question:"How would you deploy it?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Use **CI/CD**: automated build → test → deploy pipeline, containerized (Docker) and orchestrated (Kubernetes), with **immutable artifacts** promoted across environments (dev→staging→prod). Deploy via **rolling/blue-green/canary** for zero downtime (see next), gated by automated tests + health checks, with automated rollback on failure. Externalize config (12-factor) and use feature flags to decouple deploy from release. Answer: CI/CD + containers/orchestration + progressive rollout + automated rollback.\n\n```text\nCI/CD: build -> test -> deploy (containerized, orchestrated by k8s). Immutable artifacts promoted dev->staging->prod.\nRolling/blue-green/canary (zero downtime) + health-gated + auto-rollback. Externalized config + feature flags.\n```",tech:"hld",displayNumber:337,sortKey:340},{id:"hld-341",number:338,title:"How would you perform zero-downtime deployment?",question:"How would you perform zero-downtime deployment?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Deploy without dropping traffic using: **rolling** (replace instances gradually, health-checked), **blue-green** (run new version alongside old, switch traffic at once, instant rollback), or **canary** (route a small % to the new version, watch metrics, ramp up). Prerequisites: **backward-compatible** API/DB changes (old + new coexist — expand/contract migrations), health/readiness checks (only route to ready instances), and automated rollback. Feature flags decouple deploy from release. Answer: rolling/blue-green/canary + backward compatibility + health checks + rollback.\n\n```text\nRolling (gradual, health-checked) | Blue-green (switch at once, instant rollback) | Canary (small % -> ramp on good metrics).\nRequires: backward-compatible API/DB (expand/contract), readiness checks, auto-rollback, feature flags.\nOld + new must coexist during rollout.\n```",tech:"hld",displayNumber:338,sortKey:341},{id:"hld-342",number:339,title:"How would you handle a traffic spike?",question:"How would you handle a traffic spike?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Absorb and shed: **autoscaling** (add instances on load — but it has lag, so pre-warm for known events), **caching + CDN** (serve without hitting the backend), **queues** (buffer writes, process async — load leveling), **rate limiting + load shedding** (reject/queue excess to protect the core), and a **virtual waiting room** for extreme events (flash sales). Provision headroom for known peaks; degrade gracefully under extreme load. Answer: autoscale + cache/CDN + queue buffering + rate limit/shed + waiting room.\n\n```text\nSpike -> autoscale (pre-warm for known events) + cache/CDN (offload) + queues (buffer writes, level load)\n   + rate limit/load shedding (protect core) + virtual waiting room (extreme events).\nProvision headroom for known peaks; degrade gracefully.\n```",tech:"hld",displayNumber:339,sortKey:342},{id:"hld-343",number:340,title:"How would you design disaster recovery?",question:"How would you design disaster recovery?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Define **RPO** (max data loss) and **RTO** (max downtime), then build to meet them: cross-region **replication/backups**, infrastructure automation (IaC), and a **failover** strategy sized to cost/speed — backup-restore (cheap, slow), pilot light, warm standby, or active-active (fast, costly). **Test regularly** (DR drills/GameDays) — untested DR fails. Ensure surviving capacity, avoid split-brain, and automate promotion + traffic rerouting. Answer: RPO/RTO-driven, cross-region replication + failover strategy + tested drills.\n\n```text\nDefine RPO (data loss) + RTO (downtime). Build: cross-region replication/backups + IaC + failover strategy.\nStrategy by cost/speed: backup-restore < pilot-light < warm-standby < active-active. TEST (DR drills/GameDay).\nAvoid split-brain; ensure surviving capacity; automate promotion + reroute.\n```",tech:"hld",displayNumber:340,sortKey:343},{id:"hld-344",number:341,title:"What is your RPO?",question:"What is your RPO?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'RPO (Recovery Point Objective) = the **maximum acceptable data loss**, measured in time (e.g. "at most 5 minutes"). It\'s driven by **replication/backup frequency**: continuous/synchronous replication → near-zero RPO (no loss, higher cost/latency); periodic backups → RPO = backup interval. Set it by how much data loss the business can tolerate — payments need ~zero RPO; analytics can tolerate more. Answer: define RPO from data-loss tolerance, meet it via replication frequency.\n\n```text\nRPO = max acceptable DATA LOSS (in time). Sync replication -> ~0 RPO (costly); periodic backup -> RPO = interval.\nPayments -> ~0 RPO; analytics -> minutes/hours OK. Set from business data-loss tolerance.\n```',tech:"hld",displayNumber:341,sortKey:344},{id:"hld-345",number:342,title:"What is your RTO?",question:"What is your RTO?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'RTO (Recovery Time Objective) = the **maximum acceptable downtime** to restore service after a failure (e.g. "back within 15 minutes"). It\'s driven by **failover automation** and standby readiness: active-active → near-zero RTO (instant); warm standby → minutes; backup-restore → hours. Lower RTO costs more (warm/hot standby, automation). Set it by how long the business can tolerate being down. Answer: define RTO from downtime tolerance, meet it via failover automation/standby.\n\n```text\nRTO = max acceptable DOWNTIME to recover. Active-active -> ~0 RTO; warm standby -> minutes; backup-restore -> hours.\nLower RTO = more cost (hot standby + automation). Set from business downtime tolerance.\n```',tech:"hld",displayNumber:342,sortKey:345},{id:"hld-346",number:343,title:"How would you make it multi-region?",question:"How would you make it multi-region?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Deploy the full stack in multiple regions and route users to the nearest healthy one (GeoDNS/global LB). The hard part is **data**: pick active-passive (one write region + async replication — simpler) or active-active (multi-region writes + conflict resolution — complex), and set per-dataset consistency (strong for money → pin to a region; eventual for feeds → replicate freely). Regional caches, cross-region replication with defined RPO/RTO, and failover. Answer: full stack per region + global routing + a data-consistency strategy per dataset.\n\n```text\nFull stack per region + global routing to nearest healthy region. Data is the hard part:\n   active-passive (1 writer + async) or active-active (multi-write + conflict resolution);\n   per-dataset consistency (money = strong/pinned; feeds = eventual). Regional caches; cross-region replication (RPO/RTO).\n```",tech:"hld",displayNumber:343,sortKey:346},{id:"hld-347",number:344,title:"Active-active or active-passive?",question:"Active-active or active-passive?",difficulty:"Hard",category:"Follow-Up Questions",answer:`- **Active-passive**: one region serves, standby replicates + waits. Simpler (single writer → no conflicts), but idle capacity and failover downtime (RTO). Good default for most.
- **Active-active**: all regions serve reads+writes. Low latency everywhere, full HA, no idle capacity — but needs multi-master conflict resolution and is complex. Best for global scale / eventually-consistent data.

Choose by RTO needs, write-conflict tolerance, and cost. Answer: active-passive for simplicity, active-active for global low-latency + HA (at the cost of conflict handling).

\`\`\`text
Active-passive: 1 writer + standby -> simple (no conflicts), idle capacity + failover RTO. Good default.
Active-active: all regions read+write -> low latency + full HA, but multi-master conflict resolution + complexity.
Choose by RTO, conflict tolerance, cost.
\`\`\``,tech:"hld",displayNumber:344,sortKey:347},{id:"hld-348",number:345,title:"What consistency model do you choose?",question:"What consistency model do you choose?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Choose **per dataset**, not globally: **strong consistency** where correctness is critical (money, inventory, uniqueness, locks) — accept higher latency/lower availability (CP); **eventual consistency** where staleness is tolerable (feeds, counts, caches, presence) — gain availability/latency (AP). Many systems mix both. Justify with the CAP trade-off and the business need. Answer: strong where correctness matters, eventual where availability/latency matter — decided per data, not one-size-fits-all.\n\n```text\nChoose PER dataset: strong (money/inventory/uniqueness/locks -> CP, higher latency) vs eventual (feeds/counts/caches -> AP, available).\nMix both in one system. Justify via CAP + business need. Not one-size-fits-all.\n```",tech:"hld",displayNumber:345,sortKey:348},{id:"hld-349",number:346,title:"Where can eventual consistency be accepted?",question:"Where can eventual consistency be accepted?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:'Accept eventual consistency where brief staleness is harmless: **social feeds/timelines** (a post appearing a second late is fine), **like/view counts** (approximate is OK), **caches** (bounded staleness via TTL), **search indexes** (slight indexing lag), **recommendations**, **presence/last-seen**, and **analytics dashboards**. These favor availability + low latency over instant correctness. Give honest UX where needed ("processing"). Answer: non-critical, staleness-tolerant, read-heavy data — feeds, counts, caches, search, presence.\n\n```text\nEventual OK: feeds/timelines, like/view counts, caches (TTL), search indexes, recommendations, presence, analytics.\nBrief staleness harmless -> favor availability + latency. Honest UX ("processing") where user-visible.\n```',tech:"hld",displayNumber:346,sortKey:349},{id:"hld-350",number:347,title:"Where is strong consistency mandatory?",question:"Where is strong consistency mandatory?",difficulty:"Intermediate",category:"Follow-Up Questions",answer:"Strong consistency is mandatory where stale/conflicting data causes real harm: **money/payments/balances** (no double-spend), **inventory** (no overselling), **uniqueness constraints** (usernames, seat/booking allocation), **distributed locks/leader election**, and **config/coordination**. These need ACID transactions, quorum/consensus, and accept the CP trade-off (latency/availability). Answer: money, inventory, uniqueness, locks, coordination — anywhere a wrong read causes incorrect actions or lost money.\n\n```text\nStrong required: money/balances (no double-spend), inventory (no oversell), uniqueness (usernames/seats),\n   locks/leader election, config/coordination. Use ACID + quorum/consensus (accept CP trade-off).\nRule: anywhere a stale read causes wrong actions or lost money.\n```",tech:"hld",displayNumber:347,sortKey:350},{id:"hld-351",number:348,title:"What are the bottlenecks?",question:"What are the bottlenecks?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Identify where the system will saturate first: usually the **database** (single writer, hot shard, connection limits), then **network** (bandwidth, chatty calls), **cache** (hot keys), **single-threaded/stateful components**, **external dependencies** (rate limits), and **shared resources** (locks, coordination). Analyze the critical path and the highest-traffic data. Mitigate: caching, sharding, replication, async, and removing coordination. In an interview, proactively call out the likely bottleneck and how you'd address it. Answer: DB first (usually), then network/cache/hot keys/coordination.\n\n```text\nLikely bottlenecks: DB (single writer/hot shard/connections) -> network (chatty/bandwidth) -> cache (hot keys)\n   -> stateful/single-threaded parts -> external deps (rate limits) -> locks/coordination.\nMitigate: cache, shard, replicate, async, remove coordination. Call it out proactively.\n```",tech:"hld",displayNumber:348,sortKey:351},{id:"hld-352",number:349,title:"What is the single point of failure?",question:"What is the single point of failure?",difficulty:"Hard",category:"Follow-Up Questions",answer:'A SPOF is any component whose failure takes down the whole system — a single database primary, one load balancer, one cache node, a single region, or a coordination service. Eliminate each with **redundancy**: replicas + failover (DB), multiple LBs (+ DNS/anycast), cache clustering, multi-AZ/region, and consensus-based coordination (quorum). Audit the architecture component by component asking "what if this dies?" Answer: proactively identify each SPOF and pair it with a redundancy/failover strategy — a good design has none.\n\n```text\nSPOF = component whose failure kills the system (single DB primary, one LB, one cache, one region, coordinator).\nEliminate via redundancy: DB replicas + failover, multiple LBs + DNS, cache cluster, multi-AZ/region, consensus quorum.\nAudit "what if this dies?" per component. Good design = no SPOF.\n```',tech:"hld",displayNumber:349,sortKey:352},{id:"hld-353",number:350,title:"How would you reduce cost?",question:"How would you reduce cost?",difficulty:"Hard",category:"Follow-Up Questions",answer:"Optimize without sacrificing SLAs: **autoscaling + scale-to-zero** (pay for actual load, not peak), **caching/CDN** (fewer expensive backend/DB calls), **storage tiering** (hot→cold/archive, compression, retention/lifecycle policies), **right-sizing** instances (avoid over-provisioning), **reserved/spot** capacity for predictable/interruptible workloads, **efficient data formats** (compression, columnar), and reducing cross-region/egress traffic (a big cloud cost). Measure cost per request/tenant and target the biggest line items. Answer: autoscale + cache + tier storage + right-size + reserved/spot + cut egress.\n\n```text\nCut cost w/o breaking SLAs: autoscale/scale-to-zero (pay for load), cache/CDN (fewer DB/egress calls),\n   storage tiering + compression + retention, right-size instances, reserved/spot capacity, reduce cross-region/egress.\nMeasure cost per request/tenant; target biggest line items.\n```",tech:"hld",displayNumber:350,sortKey:353},{id:"java-354",number:1,title:"What are the four fundamental OOP principles?",question:"What are the four fundamental OOP principles?",difficulty:"Basic",category:"Core Java & OOP",answer:`The four fundamental Object-Oriented Programming principles are:
1. **Encapsulation** - Bundling data and methods within a class and restricting direct access via access modifiers.
2. **Abstraction** - Hiding complex implementation details and showing only essential features.
3. **Inheritance** - A subclass acquires properties/behaviors from a superclass.
4. **Polymorphism** - One interface, many implementations (overloading at compile-time, overriding at runtime).

\`\`\`java
public class BankAccount {
    private double balance; // encapsulated
    public void deposit(double amount) { if (amount > 0) balance += amount; }
    public double getBalance() { return balance; }
}

abstract class Animal { abstract void sound(); }
class Dog extends Animal {
    @Override void sound() { System.out.println("Dog barks"); } // polymorphism
}
\`\`\``,tech:"java",displayNumber:1,sortKey:354},{id:"java-355",number:2,title:"Abstract class vs interface in Java?",question:"Abstract class vs interface in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`| Feature | Abstract Class | Interface |
|---------|---------------|-----------|
| Inheritance | Single class only | Multiple interfaces |
| Constructors | Allowed | Not allowed |
| Fields | Instance variables | \`public static final\` only |
| Methods | Abstract + concrete | Abstract (pre-Java8), default/static (Java8+) |

\`\`\`java
abstract class Vehicle { abstract void drive(); void stop() {} }
interface Flyable { void fly(); default void land() {} }
class Drone extends Vehicle implements Flyable {
    void drive() {}
    public void fly() {}
}
\`\`\``,tech:"java",displayNumber:2,sortKey:355},{id:"java-356",number:3,title:"What is the String pool in Java?",question:"What is the String pool in Java?",difficulty:"Basic",category:"Core Java & OOP",answer:'The String pool is a special heap memory region storing unique string literals. When you create a literal, the JVM reuses an existing identical string if present, saving memory. `new String()` always creates a new object; `intern()` adds it to the pool.\n\n```java\nString a = "hello";\nString b = "hello";\nString c = new String("hello");\nSystem.out.println(a == b); // true (same pool object)\nSystem.out.println(a == c); // false (different objects)\n```',tech:"java",displayNumber:3,sortKey:356},{id:"java-357",number:4,title:"Explain method overloading vs overriding.",question:"Explain method overloading vs overriding.",difficulty:"Basic",category:"Core Java & OOP",answer:`**Overloading** is compile-time polymorphism: multiple methods with same name but different parameter lists within the same class. **Overriding** is runtime polymorphism: a subclass redefines a superclass method with identical signature.

\`\`\`java
class Calc {
    int add(int a, int b) { return a + b; }       // overload
    int add(int a, int b, int c) { return a + b + c; } // overload
}
class Animal { void sound() { System.out.println("..."); } }
class Cat extends Animal { @Override void sound() { System.out.println("Meow"); } } // override
\`\`\``,tech:"java",displayNumber:4,sortKey:357},{id:"java-358",number:5,title:"What is the difference between `==` and `.equals()`?",question:"What is the difference between `==` and `.equals()`?",difficulty:"Basic",category:"Core Java & OOP",answer:'`==` compares object references (memory addresses). `.equals()` compares logical equality (content), which can be overridden. For Strings, always use `.equals()` to compare values.\n\n```java\nString s1 = new String("x");\nString s2 = new String("x");\nSystem.out.println(s1 == s2);      // false\nSystem.out.println(s1.equals(s2)); // true\n```',tech:"java",displayNumber:5,sortKey:358},{id:"java-359",number:6,title:"How does Java achieve platform independence?",question:"How does Java achieve platform independence?",difficulty:"Basic",category:"Core Java & OOP",answer:'Java source code compiles to bytecode (.class), which runs on the JVM. The JVM is platform-specific but interprets the same bytecode everywhere, so "write once, run anywhere."\n\n```\n.java (source) -> javac -> .class (bytecode) -> JVM -> native machine code\n```',tech:"java",displayNumber:6,sortKey:359},{id:"java-360",number:7,title:"What are access modifiers and their scopes?",question:"What are access modifiers and their scopes?",difficulty:"Basic",category:"Core Java & OOP",answer:"- `private`: accessible only within the same class\n- `default` (no modifier): accessible within the same package\n- `protected`: same package + subclasses\n- `public`: accessible everywhere\n\n```java\npublic class A {\n    private int x;\n    protected int y;\n    public int z;\n    int w; // default\n}\n```",tech:"java",displayNumber:7,sortKey:360},{id:"java-361",number:8,title:"Explain constructor chaining.",question:"Explain constructor chaining.",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Constructor chaining is calling one constructor from another using `this()` (same class) or `super()` (parent class). It must be the first statement in the constructor. It promotes code reuse for initialization.\n\n```java\nclass Person {\n    String name;\n    Person() { this("Unknown"); }   // chaining\n    Person(String name) { this.name = name; }\n}\n```',tech:"java",displayNumber:8,sortKey:361},{id:"java-362",number:9,title:"What is the `final` keyword used for?",question:"What is the `final` keyword used for?",difficulty:"Basic",category:"Core Java & OOP",answer:'`final` means "cannot change":\n- `final` variable: constant, must be initialized once\n- `final` method: cannot be overridden\n- `final` class: cannot be subclassed (e.g., `String`)\n\n```java\nfinal double PI = 3.14159;\nfinal class Immutable {}\n// class Sub extends Immutable {} // compile error\n```',tech:"java",displayNumber:9,sortKey:362},{id:"java-363",number:10,title:"What is `this` and `super`?",question:"What is `this` and `super`?",difficulty:"Basic",category:"Core Java & OOP",answer:`\`this\` refers to the current instance; \`super\` refers to the parent class instance. Both can access fields, methods, and constructors.

\`\`\`java
class Parent { int val = 10; }
class Child extends Parent {
    int val = 20;
    void show() {
        System.out.println(val);      // 20
        System.out.println(this.val);  // 20
        System.out.println(super.val); // 10
    }
}
\`\`\``,tech:"java",displayNumber:10,sortKey:363},{id:"java-364",number:11,title:"What is immutability and how to create an immutable class?",question:"What is immutability and how to create an immutable class?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"An immutable object cannot be modified after creation. Steps: declare class `final`, make fields `private final`, no setters, return defensive copies of mutable fields, initialize via constructor.\n\n```java\npublic final class ImmutablePoint {\n    private final int x, y;\n    public ImmutablePoint(int x, int y) { this.x = x; this.y = y; }\n    public int getX() { return x; }\n    public int getY() { return y; }\n}\n```",tech:"java",displayNumber:11,sortKey:364},{id:"java-365",number:12,title:"Difference between checked and unchecked exceptions?",question:"Difference between checked and unchecked exceptions?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'**Checked exceptions** (extend `Exception`) are verified at compile-time and must be handled or declared (`IOException`). **Unchecked exceptions** (extend `RuntimeException`) occur at runtime (`NullPointerException`, `ArrayIndexOutOfBoundsException`) and need not be declared.\n\n```java\nvoid read() throws IOException { // checked - must declare\n    Files.readAllBytes(Path.of("x"));\n}\nvoid div(int a, int b) {\n    int r = a / b; // ArithmeticException - unchecked, no need to declare\n}\n```',tech:"java",displayNumber:12,sortKey:365},{id:"java-366",number:13,title:"What is the `try-with-resources` statement?",question:"What is the `try-with-resources` statement?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Introduced in Java 7, `try-with-resources` automatically closes resources (those implementing `AutoCloseable`) at the end of the block, eliminating boilerplate `finally` blocks and preventing resource leaks.\n\n```java\ntry (BufferedReader br = new BufferedReader(new FileReader("file.txt"))) {\n    System.out.println(br.readLine());\n} catch (IOException e) {\n    e.printStackTrace();\n} // br auto-closed here\n```',tech:"java",displayNumber:13,sortKey:366},{id:"java-367",number:14,title:"Explain the `Exception` hierarchy in Java.",question:"Explain the `Exception` hierarchy in Java.",difficulty:"Basic",category:"Core Java & OOP",answer:"`Throwable` is the root. It has two subclasses: `Error` (serious JVM problems like `OutOfMemoryError`, typically unrecoverable) and `Exception` (recoverable conditions). `Exception` splits into checked and unchecked (`RuntimeException`).\n\n```\nThrowable\n├── Error (OutOfMemoryError)\n└── Exception\n    ├── IOException (checked)\n    └── RuntimeException (unchecked)\n```",tech:"java",displayNumber:14,sortKey:367},{id:"java-368",number:15,title:"What is method hiding vs method overriding?",question:"What is method hiding vs method overriding?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Static methods are not overridden — they are **hidden**. If a subclass defines a static method with the same signature, the parent's is hidden, and resolution is based on the reference type (compile-time), not the object type.

\`\`\`java
class A { static void m() { System.out.println("A"); } }
class B extends A { static void m() { System.out.println("B"); } }
A a = new B();
a.m(); // prints "A" (static binding on reference type A)
\`\`\``,tech:"java",displayNumber:15,sortKey:368},{id:"java-369",number:16,title:"What is a singleton class and how to implement it?",question:"What is a singleton class and how to implement it?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`A singleton ensures only one instance exists. Common approaches: eager initialization, lazy initialization with double-checked locking, or the enum approach (Joshua Bloch's preferred, serialization-safe).

\`\`\`java
public enum Singleton { INSTANCE; }
// or
public class Singleton {
    private static volatile Singleton instance;
    private Singleton() {}
    public static Singleton getInstance() {
        if (instance == null) {
            synchronized (Singleton.class) {
                if (instance == null) instance = new Singleton();
            }
        }
        return instance;
    }
}
\`\`\``,tech:"java",displayNumber:16,sortKey:369},{id:"java-370",number:17,title:"What is the difference between `StringBuilder` and `StringBuffer`?",question:"What is the difference between `StringBuilder` and `StringBuffer`?",difficulty:"Basic",category:"Core Java & OOP",answer:'Both build mutable strings. `StringBuilder` is not thread-safe (faster, Java 5+) and `StringBuffer` is thread-safe (synchronized methods, legacy). For single-threaded use, prefer `StringBuilder`.\n\n```java\nStringBuilder sb = new StringBuilder();\nsb.append("Hello").append(" ").append("World");\nString result = sb.toString();\n```',tech:"java",displayNumber:17,sortKey:370},{id:"java-371",number:18,title:"How does autoboxing and unboxing work?",question:"How does autoboxing and unboxing work?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Autoboxing automatically converts primitives to wrapper objects (e.g., `int` → `Integer`). Unboxing does the reverse. Added in Java 5. Be careful with `null` unboxing (throws `NullPointerException`).\n\n```java\nInteger i = 10;        // autoboxing (int -> Integer)\nint j = i;             // unboxing (Integer -> int)\nList<Integer> list = new ArrayList<>();\nlist.add(5);           // autoboxing\n```",tech:"java",displayNumber:18,sortKey:371},{id:"java-372",number:19,title:"What is the difference between `HashMap` and `Hashtable`?",question:"What is the difference between `HashMap` and `Hashtable`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`HashMap` is non-synchronized, allows one null key and multiple null values, and is faster. `Hashtable` is thread-safe (synchronized), does not allow null keys/values, and is legacy.\n\n```java\nMap<String, Integer> map = new HashMap<>();\nmap.put(null, 1); // OK\n// Hashtable: map.put(null, 1); // throws NullPointerException\n```",tech:"java",displayNumber:19,sortKey:372},{id:"java-373",number:20,title:"What is a marker interface?",question:"What is a marker interface?",difficulty:"Basic",category:"Core Java & OOP",answer:"A marker interface has no methods or fields; it marks a class for special treatment by the JVM or frameworks. Examples: `Serializable`, `Cloneable`, `Remote`.\n\n```java\npublic class Person implements Serializable {\n    private String name;\n} // marks Person as serializable\n```",tech:"java",displayNumber:20,sortKey:373},{id:"java-374",number:21,title:"Explain the `Object` class methods.",question:"Explain the `Object` class methods.",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Every Java class inherits from `Object`. Key methods: `equals()`, `hashCode()`, `toString()`, `clone()`, `finalize()` (deprecated), `getClass()`, `notify()`, `wait()`, `notifyAll()`.\n\n```java\n@Override\npublic boolean equals(Object o) {\n    if (this == o) return true;\n    if (!(o instanceof Person p)) return false;\n    return name.equals(p.name);\n}\n@Override\npublic int hashCode() { return Objects.hash(name); }\n```",tech:"java",displayNumber:21,sortKey:374},{id:"java-375",number:22,title:"What is the difference between `equals()` and `hashCode()` contract?",question:"What is the difference between `equals()` and `hashCode()` contract?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"If two objects are equal via `equals()`, they must have the same `hashCode()`. The reverse is not required. Violating this breaks hash-based collections like `HashMap` and `HashSet`.\n\n```java\n// Always override both together\n@Override public boolean equals(Object o) { /* ... */ }\n@Override public int hashCode() { return Objects.hash(id); }\n```",tech:"java",displayNumber:22,sortKey:375},{id:"java-376",number:23,title:"What is method reference in Java?",question:"What is method reference in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Method references (`::`) are shorthand for lambda expressions that call a single method. Forms: static (`Class::method`), instance (`obj::method`), constructor (`Class::new`), arbitrary instance (`Class::method`).\n\n```java\nList<String> names = Arrays.asList("a", "b");\nnames.forEach(System.out::println); // method reference\n```',tech:"java",displayNumber:23,sortKey:376},{id:"java-377",number:24,title:"What is the `instanceof` operator?",question:"What is the `instanceof` operator?",difficulty:"Basic",category:"Core Java & OOP",answer:'`instanceof` checks whether an object is an instance of a specific class or interface (including subclasses). Returns boolean. Useful before casting to avoid `ClassCastException`.\n\n```java\nObject o = "text";\nif (o instanceof String s) {\n    System.out.println(s.length()); // pattern matching (Java 16+)\n}\n```',tech:"java",displayNumber:24,sortKey:377},{id:"java-378",number:25,title:"Explain the difference between `throw` and `throws`.",question:"Explain the difference between `throw` and `throws`.",difficulty:"Basic",category:"Core Java & OOP",answer:'`throw` is used to explicitly throw an exception instance. `throws` is used in a method signature to declare that the method may throw checked exceptions.\n\n```java\nvoid validate(int age) throws IllegalArgumentException {\n    if (age < 0) throw new IllegalArgumentException("negative age");\n}\n```',tech:"java",displayNumber:25,sortKey:378},{id:"java-379",number:26,title:"What is the difference between `final`, `finally`, and `finalize`?",question:"What is the difference between `final`, `finally`, and `finalize`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'- `final`: restricts modification (variable/method/class)\n- `finally`: block that always executes after try/catch (cleanup)\n- `finalize()`: deprecated Object method called by GC before reclaiming memory\n\n```java\ntry { /* code */ }\nfinally { System.out.println("always runs"); }\n```',tech:"java",displayNumber:26,sortKey:379},{id:"java-380",number:27,title:"What is a static block and when is it used?",question:"What is a static block and when is it used?",difficulty:"Basic",category:"Core Java & OOP",answer:`A static block (\`static {}\`) runs once when the class is loaded by the JVM. Used for static initialization that cannot fit in a single expression.

\`\`\`java
class Config {
    static Properties props;
    static {
        props = new Properties();
        props.setProperty("env", "prod");
    }
}
\`\`\``,tech:"java",displayNumber:27,sortKey:380},{id:"java-381",number:28,title:"Can a constructor be `private`? Why?",question:"Can a constructor be `private`? Why?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Yes. A private constructor prevents external instantiation. Used in singletons, factory methods, and utility classes (where all methods are static).

\`\`\`java
public class Utility {
    private Utility() {} // no instantiation
    public static int add(int a, int b) { return a + b; }
}
\`\`\``,tech:"java",displayNumber:28,sortKey:381},{id:"java-382",number:29,title:"What is the difference between `int` and `Integer`?",question:"What is the difference between `int` and `Integer`?",difficulty:"Basic",category:"Core Java & OOP",answer:'`int` is a primitive (stores value directly, default 0, no methods). `Integer` is a wrapper class (object, default null, has methods like `parseInt`). Autoboxing bridges them.\n\n```java\nint a = 5;\nInteger b = Integer.valueOf(5);\nint c = Integer.parseInt("10");\n```',tech:"java",displayNumber:29,sortKey:382},{id:"java-383",number:30,title:"What is a `record` in Java (Java 16+)?",question:"What is a `record` in Java (Java 16+)?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'A `record` is a concise immutable data carrier. The compiler auto-generates private final fields, a canonical constructor, `equals()`, `hashCode()`, and `toString()`. Great for DTOs.\n\n```java\npublic record User(String name, int age) {}\nUser u = new User("Alice", 30);\nSystem.out.println(u.name()); // accessor, not getter\n```',tech:"java",displayNumber:30,sortKey:383},{id:"java-384",number:31,title:"What is shadowing in Java?",question:"What is shadowing in Java?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Shadowing occurs when a local variable or parameter has the same name as a field, hiding it within that scope. Use `this.` to access the shadowed field.\n\n```java\nclass Example {\n    int x = 10;\n    void setX(int x) { this.x = x; } // parameter shadows field\n}\n```",tech:"java",displayNumber:31,sortKey:384},{id:"java-385",number:32,title:"Explain covariance and contravariance in Java.",question:"Explain covariance and contravariance in Java.",difficulty:"Advanced",category:"Core Java & OOP",answer:'Covariance: a method can return a subtype of the declared return type (e.g., `Object` → `String`). Contravariance: a parameter can accept a supertype. Arrays are covariant; generics are invariant.\n\n```java\nclass A { Object get() { return null; } }\nclass B extends A { @Override String get() { return ""; } } // covariant return\n```',tech:"java",displayNumber:32,sortKey:385},{id:"java-386",number:33,title:"What is the difference between deep copy and shallow copy?",question:"What is the difference between deep copy and shallow copy?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`**Shallow copy** duplicates the object but shared referenced objects (both point to same nested objects). **Deep copy** recursively duplicates all nested objects, creating fully independent copies.

\`\`\`java
// Shallow: clone() default
// Deep: manually copy nested objects or use serialization
class Address implements Cloneable {
    String city;
    protected Object clone() throws CloneNotSupportedException {
        return super.clone();
    }
}
\`\`\``,tech:"java",displayNumber:33,sortKey:386},{id:"java-387",number:34,title:"What is the `transient` keyword?",question:"What is the `transient` keyword?",difficulty:"Basic",category:"Core Java & OOP",answer:"`transient` marks a field to be skipped during serialization. Useful for sensitive data (passwords) or derived/computed fields.\n\n```java\nclass User implements Serializable {\n    String name;\n    transient String password; // not serialized\n}\n```",tech:"java",displayNumber:34,sortKey:387},{id:"java-388",number:35,title:"What is the difference between `Comparable` and `Comparator`?",question:"What is the difference between `Comparable` and `Comparator`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`Comparable` defines natural ordering of a class (one method: `compareTo`). `Comparator` is external sorting logic (separate class, multiple orderings possible).\n\n```java\nclass Person implements Comparable<Person> {\n    int age;\n    public int compareTo(Person o) { return Integer.compare(age, o.age); }\n}\nComparator<Person> byName = (p1, p2) -> p1.name.compareTo(p2.name);\n```",tech:"java",displayNumber:35,sortKey:388},{id:"java-389",number:36,title:"What is the purpose of the `volatile` keyword?",question:"What is the purpose of the `volatile` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`volatile` ensures a variable is read/written directly from main memory (not CPU cache), guaranteeing visibility across threads. It does NOT provide atomicity (use `Atomic` classes for that).\n\n```java\nclass Flag {\n    volatile boolean running = true;\n    void stop() { running = false; }\n}\n```",tech:"java",displayNumber:36,sortKey:389},{id:"java-390",number:37,title:"What is the difference between `Stack` and `Heap` memory?",question:"What is the difference between `Stack` and `Heap` memory?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`**Stack** stores local variables and method call frames; thread-private, fast, automatically cleaned on method exit. **Heap** stores objects; shared across threads, managed by GC.

\`\`\`java
void method() {
    int x = 5;        // stack
    Object o = new Object(); // reference on stack, object on heap
}
\`\`\``,tech:"java",displayNumber:37,sortKey:390},{id:"java-391",number:38,title:"What are enums and when to use them?",question:"What are enums and when to use them?",difficulty:"Basic",category:"Core Java & OOP",answer:"Enums define a fixed set of constants with type safety. They can have fields, methods, and implement interfaces. Prefer enums over `int` constants for readability and safety.\n\n```java\nenum Day { MONDAY, TUESDAY, WEDNESDAY }\nDay today = Day.MONDAY;\nswitch (today) { case MONDAY: break; }\n```",tech:"java",displayNumber:38,sortKey:391},{id:"java-392",number:39,title:"What is reflection in Java?",question:"What is reflection in Java?",difficulty:"Advanced",category:"Core Java & OOP",answer:'Reflection allows inspecting/modifying runtime behavior of classes (fields, methods, constructors) at runtime. Used by frameworks (Spring, JUnit). It has performance overhead and breaks encapsulation.\n\n```java\nClass<?> clazz = Class.forName("java.lang.String");\nMethod[] methods = clazz.getDeclaredMethods();\n```',tech:"java",displayNumber:39,sortKey:392},{id:"java-393",number:40,title:"What is the difference between `public static void main(String[] args)` components?",question:"What is the difference between `public static void main(String[] args)` components?",difficulty:"Basic",category:"Core Java & OOP",answer:"`public`: accessible from JVM. `static`: no instance needed. `void`: no return value. `main`: entry point name. `String[] args`: command-line arguments.\n\n```java\npublic static void main(String[] args) {\n    for (String arg : args) System.out.println(arg);\n}\n```",tech:"java",displayNumber:40,sortKey:393},{id:"java-394",number:41,title:"What is a nested class vs inner class?",question:"What is a nested class vs inner class?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`A **static nested class** doesn't need an outer instance. A **non-static inner class** has an implicit reference to the outer instance, can access its private members.

\`\`\`java
class Outer {
    static class Nested {}    // no outer ref
    class Inner {}            // has outer ref
}
\`\`\``,tech:"java",displayNumber:41,sortKey:394},{id:"java-395",number:42,title:"What is the difference between `null` and empty string?",question:"What is the difference between `null` and empty string?",difficulty:"Basic",category:"Core Java & OOP",answer:'`null` means no reference (points to nothing; calling methods on it throws NPE). Empty string `""` is a valid String object with zero length.\n\n```java\nString a = null;    // no object\nString b = "";      // valid object, length 0\n// a.length(); // NullPointerException\n```',tech:"java",displayNumber:42,sortKey:395},{id:"java-396",number:43,title:"What is the diamond problem and how does Java avoid it?",question:"What is the diamond problem and how does Java avoid it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"The diamond problem occurs with multiple inheritance of classes (ambiguity over which parent method to call). Java avoids it by allowing only single class inheritance but permitting multiple interface implementation. With default methods, conflicts are resolved explicitly via `Interface.super.method()`.\n\n```java\ninterface A { default void foo() {} }\ninterface B { default void foo() {} }\nclass C implements A, B {\n    public void foo() { A.super.foo(); } // must resolve explicitly\n}\n```",tech:"java",displayNumber:43,sortKey:396},{id:"java-397",number:44,title:"What is a lambda expression?",question:"What is a lambda expression?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"A lambda is a concise way to express an anonymous function (a block of code passed as an argument). It implements a functional interface. Syntax: `(params) -> expression`.\n\n```java\nList<Integer> nums = Arrays.asList(1, 2, 3);\nnums.forEach(n -> System.out.println(n * 2));\n```",tech:"java",displayNumber:44,sortKey:397},{id:"java-398",number:45,title:"What is a functional interface?",question:"What is a functional interface?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'An interface with exactly one abstract method (SAM). Can have default/static methods. `@FunctionalInterface` annotation enforces this. Examples: `Runnable`, `Callable`, `Comparator`, `Function`.\n\n```java\n@FunctionalInterface\ninterface Greeting { void say(String name); }\nGreeting g = name -> System.out.println("Hi " + name);\n```',tech:"java",displayNumber:45,sortKey:398},{id:"java-399",number:46,title:"What is the difference between `sleep()` and `wait()`?",question:"What is the difference between `sleep()` and `wait()`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`sleep()` (Thread class) pauses the current thread for a specified time without releasing the lock. `wait()` (Object class) releases the lock and waits for `notify()`/`notifyAll()`.\n\n```java\nThread.sleep(1000); // keeps lock\nsynchronized (obj) { obj.wait(); } // releases lock\n```",tech:"java",displayNumber:46,sortKey:399},{id:"java-400",number:47,title:"What is the difference between `ArrayList` and `LinkedList`?",question:"What is the difference between `ArrayList` and `LinkedList`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`ArrayList` uses a dynamic array (fast random access O(1), slow insertion in middle O(n)). `LinkedList` uses nodes (fast insert/delete O(1), slow access O(n)).\n\n```java\nList<String> arr = new ArrayList<>();  // random access heavy\nList<String> link = new LinkedList<>(); // frequent insert/delete\n```",tech:"java",displayNumber:47,sortKey:400},{id:"java-401",number:48,title:"What is the `super` keyword used for in constructors?",question:"What is the `super` keyword used for in constructors?",difficulty:"Basic",category:"Core Java & OOP",answer:'`super()` calls the parent class constructor. It must be the first statement in a subclass constructor. If omitted, the compiler inserts an implicit `super()` (no-arg) call.\n\n```java\nclass Animal { Animal(String n) {} }\nclass Dog extends Animal { Dog() { super("Dog"); } }\n```',tech:"java",displayNumber:48,sortKey:401},{id:"java-402",number:49,title:"What is the difference between compile-time and runtime polymorphism?",question:"What is the difference between compile-time and runtime polymorphism?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"**Compile-time** (static) polymorphism: method overloading resolved at compile time. **Runtime** (dynamic) polymorphism: method overriding resolved at runtime based on the actual object type via vtable dispatch.\n\n```java\n// Overloading - compile time\n// Overriding - runtime (JVM picks actual object's method)\n```",tech:"java",displayNumber:49,sortKey:402},{id:"java-403",number:50,title:"What is the purpose of `@Override` annotation?",question:"What is the purpose of `@Override` annotation?",difficulty:"Basic",category:"Core Java & OOP",answer:'`@Override` tells the compiler you intend to override a superclass/interface method. If the signature doesn\'t match, the compiler errors — catching typos at compile time.\n\n```java\n@Override\npublic String toString() { return "MyClass"; }\n```',tech:"java",displayNumber:50,sortKey:403},{id:"java-404",number:51,title:"What is a deadlock and how to avoid it?",question:"What is a deadlock and how to avoid it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"A deadlock is when two or more threads block forever, each waiting for a lock held by another. Avoid by: locking in consistent order, using `tryLock` with timeout, minimizing lock scope, and using higher-level concurrency utilities.\n\n```java\n// Deadlock: Thread1 locks A then B, Thread2 locks B then A\n// Solution: always acquire locks in same order (A then B)\n```",tech:"java",displayNumber:51,sortKey:404},{id:"java-405",number:52,title:"What is the difference between `Error` and `Exception`?",question:"What is the difference between `Error` and `Exception`?",difficulty:"Basic",category:"Core Java & OOP",answer:"Both extend `Throwable`. `Error` represents serious JVM failures (`OutOfMemoryError`, `StackOverflowError`) — typically unrecoverable. `Exception` represents conditions a program might handle.\n\n```java\ntry {\n    recursiveCall(); // StackOverflowError - can't really handle\n} catch (Exception e) { }\n```",tech:"java",displayNumber:52,sortKey:405},{id:"java-406",number:53,title:"What is the `assert` keyword?",question:"What is the `assert` keyword?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`assert` validates assumptions during development. `assert condition;` throws `AssertionError` if false. Disabled by default; enable with `-ea` JVM flag. Not for production error handling.\n\n```java\nassert x > 0 : "x must be positive"; // custom message\n```',tech:"java",displayNumber:53,sortKey:406},{id:"java-407",number:54,title:"What is the difference between `var` (Java 10+) and explicit type?",question:"What is the difference between `var` (Java 10+) and explicit type?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`var` uses local type inference — the compiler determines the type from the initializer. Type is still static (not dynamic). Only allowed for local variables with initializers.\n\n```java\nvar list = new ArrayList<String>(); // inferred as ArrayList<String>\nvar name = "Java";                   // inferred as String\n```',tech:"java",displayNumber:54,sortKey:407},{id:"java-408",number:55,title:"What is a strong reference vs weak reference?",question:"What is a strong reference vs weak reference?",difficulty:"Advanced",category:"Core Java & OOP",answer:"**Strong**: normal reference, GC won't collect while reachable. **Weak** (`WeakReference`): collected on next GC even if referenced. Used in caches (`WeakHashMap`). **Soft**: collected only when memory is low. **Phantom**: for pre-cleanup notifications.\n\n```java\nWeakReference<ExpensiveObj> ref = new WeakReference<>(new ExpensiveObj());\n// GC may reclaim the object even though ref exists\n```",tech:"java",displayNumber:55,sortKey:408},{id:"java-409",number:56,title:"What is the `strictfp` keyword?",question:"What is the `strictfp` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`strictfp` ensures floating-point calculations follow IEEE 754 exactly across platforms (no platform-specific extended precision). Rarely needed today since Java 17 made strict FP the default.\n\n```java\npublic strictfp class MathUtils { }\n```",tech:"java",displayNumber:56,sortKey:409},{id:"java-410",number:57,title:"What is the difference between `import` and `static import`?",question:"What is the difference between `import` and `static import`?",difficulty:"Basic",category:"Core Java & OOP",answer:"`import` brings a class into scope. `import static` brings static members (methods/fields) directly, allowing unqualified use. Overuse reduces readability.\n\n```java\nimport static java.lang.Math.PI;\nimport static java.lang.Math.sqrt;\ndouble r = sqrt(PI);\n```",tech:"java",displayNumber:57,sortKey:410},{id:"java-411",number:58,title:"What is a sealed class (Java 17)?",question:"What is a sealed class (Java 17)?",difficulty:"Advanced",category:"Core Java & OOP",answer:"A sealed class restricts which classes can extend it via the `permits` clause. Enables exhaustive pattern matching and controlled hierarchies. Combines open inheritance with safety.\n\n```java\npublic sealed class Shape permits Circle, Square {}\nfinal class Circle extends Shape {}\nfinal class Square extends Shape {}\n```",tech:"java",displayNumber:58,sortKey:411},{id:"java-412",number:59,title:"What is the difference between `ExecutionException` and `InterruptedException`?",question:"What is the difference between `ExecutionException` and `InterruptedException`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`InterruptedException` is thrown when a thread is interrupted while waiting (sleep/wait). `ExecutionException` wraps an exception thrown by a task submitted to an executor.\n\n```java\ntry { future.get(); }\ncatch (InterruptedException e) { Thread.currentThread().interrupt(); }\ncatch (ExecutionException e) { Throwable cause = e.getCause(); }\n```",tech:"java",displayNumber:59,sortKey:412},{id:"java-413",number:60,title:"What is the difference between a mutable and immutable object?",question:"What is the difference between a mutable and immutable object?",difficulty:"Basic",category:"Core Java & OOP",answer:'Mutable objects can change state after creation (`StringBuilder`, arrays). Immutable objects cannot (`String`, `Integer`, `LocalDate`). Immutability aids thread-safety and caching.\n\n```java\nString s = "a";\ns = s + "b"; // new object created, original unchanged\n```',tech:"java",displayNumber:60,sortKey:413},{id:"java-414",number:61,title:"What is the purpose of `Math.random()` vs `Random` class?",question:"What is the purpose of `Math.random()` vs `Random` class?",difficulty:"Basic",category:"Core Java & OOP",answer:"`Math.random()` returns a double in [0,1) using an internal `Random`. The `Random` class offers more control (seeds, `nextInt`, `nextGaussian`, streams). For cryptography, use `SecureRandom`.\n\n```java\ndouble d = Math.random();\nRandom r = new Random(42); // seeded\nint n = r.nextInt(100);\n```",tech:"java",displayNumber:61,sortKey:414},{id:"java-415",number:62,title:"What is a static factory method?",question:"What is a static factory method?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'A static method that returns an instance of the class (e.g., `LocalDate.now()`, `List.of()`). Advantages: meaningful names, can return cached/subtype, not required to create new object each call.\n\n```java\npublic static User createAdmin() { return new User("admin", Role.ADMIN); }\nList<String> list = List.of("a", "b"); // static factory\n```',tech:"java",displayNumber:62,sortKey:415},{id:"java-416",number:63,title:"What is the difference between `String`, `StringBuffer`, and `StringBuilder` thread-safety?",question:"What is the difference between `String`, `StringBuffer`, and `StringBuilder` thread-safety?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'`String` is immutable (thread-safe by nature). `StringBuffer` is synchronized (thread-safe but slower). `StringBuilder` is not synchronized (fastest, use in single-threaded code).\n\n```java\nString s = "immutable";\nStringBuilder sb = new StringBuilder(); // fast\nStringBuffer sbf = new StringBuffer();   // synchronized\n```',tech:"java",displayNumber:63,sortKey:416},{id:"java-417",number:64,title:'What is the concept of "fail-fast" in Java?',question:'What is the concept of "fail-fast" in Java?',difficulty:"Advanced",category:"Core Java & OOP",answer:"Fail-fast iterators (e.g., `ArrayList`'s) throw `ConcurrentModificationException` immediately if the collection is structurally modified during iteration (except via the iterator's own methods).\n\n```java\nfor (String s : list) {\n    list.add(\"x\"); // ConcurrentModificationException\n}\n```",tech:"java",displayNumber:64,sortKey:417},{id:"java-418",number:65,title:"What is the difference between `clone()` and copy constructor?",question:"What is the difference between `clone()` and copy constructor?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`clone()` (from `Cloneable`) is fragile, needs casting, doesn't call constructors. A copy constructor (e.g., `Person(Person other)`) is explicit, type-safe, and easier to maintain.\n\n```java\nclass Person {\n    Person(Person other) { this.name = other.name; } // copy constructor\n}\n```",tech:"java",displayNumber:65,sortKey:418},{id:"java-419",number:66,title:"What is the difference between a class and an object?",question:"What is the difference between a class and an object?",difficulty:"Basic",category:"Core Java & OOP",answer:"A **class** is a blueprint/template defining structure and behavior. An **object** is a concrete instance of a class occupying memory with its own state.\n\n```java\nclass Car {}          // blueprint\nCar myCar = new Car(); // object/instance\n```",tech:"java",displayNumber:66,sortKey:419},{id:"java-420",number:67,title:"What is the `default` method in interfaces?",question:"What is the `default` method in interfaces?",difficulty:"Intermediate",category:"Core Java & OOP",answer:'Introduced in Java 8, `default` methods provide implementation in interfaces without breaking existing implementers. Enables interface evolution and multiple inheritance of behavior.\n\n```java\ninterface Vehicle {\n    default void start() { System.out.println("Starting..."); }\n}\n```',tech:"java",displayNumber:67,sortKey:420},{id:"java-421",number:68,title:"What is the difference between `Class.forName()` and `ClassLoader.loadClass()`?",question:"What is the difference between `Class.forName()` and `ClassLoader.loadClass()`?",difficulty:"Advanced",category:"Core Java & OOP",answer:'`Class.forName()` initializes the class (runs static blocks) by default. `ClassLoader.loadClass()` only loads it without initialization. JDBC uses `forName()` to register drivers.\n\n```java\nClass<?> c1 = Class.forName("com.mysql.Driver"); // initializes\nClass<?> c2 = getClass().getClassLoader().loadClass("com.mysql.Driver");\n```',tech:"java",displayNumber:68,sortKey:421},{id:"java-422",number:69,title:"What is the purpose of the `native` keyword?",question:"What is the purpose of the `native` keyword?",difficulty:"Advanced",category:"Core Java & OOP",answer:"`native` declares a method implemented in platform-specific C/C++ code (JNI). Used for low-level OS interactions (e.g., `Object.hashCode()` in hotspot JVM).\n\n```java\npublic native int hashCode(); // implemented in C\n```",tech:"java",displayNumber:69,sortKey:422},{id:"java-423",number:70,title:"What is the difference between `abstract` method and `default` method?",question:"What is the difference between `abstract` method and `default` method?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"An `abstract` method has no body and must be implemented by subclasses. A `default` method has a body and is optional to override.\n\n```java\ninterface I {\n    void mustImplement();     // abstract\n    default void optional() {} // default\n}\n```",tech:"java",displayNumber:70,sortKey:423},{id:"java-424",number:71,title:'What is the concept of "composition over inheritance"?',question:'What is the concept of "composition over inheritance"?',difficulty:"Intermediate",category:"Core Java & OOP",answer:"Favoring composition (has-a relationship) over inheritance (is-a) promotes flexibility and reduces tight coupling. Inheritance breaks encapsulation; composition allows runtime behavior changes.\n\n```java\nclass Engine {}\nclass Car { private Engine engine; } // composition (has-a)\n```",tech:"java",displayNumber:71,sortKey:424},{id:"java-425",number:72,title:"What is the difference between a pass-by-value and pass-by-reference?",question:"What is the difference between a pass-by-value and pass-by-reference?",difficulty:"Intermediate",category:"Core Java & OOP",answer:`Java is **always pass-by-value**. For objects, the value is the reference (memory address), so changes to the object's fields are visible, but reassigning the parameter doesn't affect the caller's reference.

\`\`\`java
void modify(List<String> l) { l.add("x"); } // visible
void reassign(List<String> l) { l = new ArrayList<>(); } // not visible to caller
\`\`\``,tech:"java",displayNumber:72,sortKey:425},{id:"java-426",number:73,title:"What is the `switch` expression (Java 14+)?",question:"What is the `switch` expression (Java 14+)?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"Switch expressions (vs statements) return a value using `->` syntax and `yield`, are exhaustive with `default` or enum coverage, and can be assigned. Arrow case doesn't fall through.\n\n```java\nint days = switch (month) {\n    case 1, 3, 5, 7, 8, 10, 12 -> 31;\n    case 2 -> 28;\n    default -> 30;\n};\n```",tech:"java",displayNumber:73,sortKey:426},{id:"java-427",number:74,title:"What is the difference between `getClass()` and `instanceof`?",question:"What is the difference between `getClass()` and `instanceof`?",difficulty:"Intermediate",category:"Core Java & OOP",answer:"`getClass()` returns the exact runtime class (no subclass match). `instanceof` returns true for the class and all its subclasses.\n\n```java\nObject o = new ArrayList<>();\nSystem.out.println(o instanceof List);    // true\nSystem.out.println(o.getClass() == List.class); // false (it's ArrayList)\n```",tech:"java",displayNumber:74,sortKey:427},{id:"java-428",number:75,title:"What is a memory leak in Java and how to prevent it?",question:"What is a memory leak in Java and how to prevent it?",difficulty:"Advanced",category:"Core Java & OOP",answer:"Even with GC, leaks occur when objects are unintentionally retained (static collections, unclosed resources, listeners not removed, thread-local not cleaned). Prevent via weak references, try-with-resources, and careful lifecycle management.\n\n```java\n// Leak: static Map holds references forever\nstatic Map<String, Object> cache = new HashMap<>();\n// Fix: use WeakHashMap or bounded cache with eviction\n```",tech:"java",displayNumber:75,sortKey:428},{id:"java-429",number:76,title:"What is the Java Collections Framework?",question:"What is the Java Collections Framework?",difficulty:"Basic",category:"Collections & Generics",answer:'The Java Collections Framework is the standard set of interfaces, implementations, and algorithms for storing and processing groups of objects. Core interfaces include Collection, List, Set, Queue, Deque, and Map. Implementations such as ArrayList, HashSet, HashMap, PriorityQueue, and TreeMap choose different trade-offs for ordering, uniqueness, lookup speed, memory use, and concurrency. Prefer coding to interfaces so the implementation can change without rewriting callers.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(); names.add("Ava"); names.add("Ben"); System.out.println(names); } }\n```',tech:"java",displayNumber:76,sortKey:429},{id:"java-430",number:77,title:"How does ArrayList work internally?",question:"How does ArrayList work internally?",difficulty:"Basic",category:"Collections & Generics",answer:"ArrayList is a resizable array. It stores elements in a contiguous Object[] and grows when capacity is exceeded, usually by allocating a larger array and copying existing elements. Random access by index is O(1), appending is amortized O(1), and insertion or removal in the middle is O(n) because elements must shift. It allows duplicates and null values.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<Integer> list = new ArrayList<>(); list.add(10); list.add(20); list.add(1, 15); System.out.println(list.get(1)); } }\n```",tech:"java",displayNumber:77,sortKey:430},{id:"java-431",number:78,title:"When should you use LinkedList instead of ArrayList?",question:"When should you use LinkedList instead of ArrayList?",difficulty:"Intermediate",category:"Collections & Generics",answer:'LinkedList is a doubly linked list implementing List and Deque. It can add or remove at the ends in O(1), but finding an index is O(n). In practice, ArrayList is usually faster for most list workloads due to cache locality and lower memory overhead. Use LinkedList mainly when you specifically need Deque operations and do frequent additions/removals at both ends, though ArrayDeque is often better for that.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<String> deque = new LinkedList<>(); deque.addFirst("front"); deque.addLast("back"); System.out.println(deque.removeFirst()); } }\n```',tech:"java",displayNumber:78,sortKey:431},{id:"java-432",number:79,title:"Compare ArrayList and LinkedList.",question:"Compare ArrayList and LinkedList.",difficulty:"Basic",category:"Collections & Generics",answer:'ArrayList is backed by an array; LinkedList is backed by nodes. ArrayList gives O(1) random reads and compact memory usage, but middle inserts/removes shift elements. LinkedList gives O(1) insert/remove only when you already have a node position or operate at ends, but random access is O(n) and each element costs extra node references. For interview answers, default to ArrayList unless queue/deque behavior is required.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> fastReads = new ArrayList<>(); Deque<String> ends = new LinkedList<>(); fastReads.add("x"); ends.addFirst("y"); System.out.println(fastReads.get(0) + ends.peekFirst()); } }\n```',tech:"java",displayNumber:79,sortKey:432},{id:"java-433",number:80,title:"What is Vector and why is it legacy?",question:"What is Vector and why is it legacy?",difficulty:"Basic",category:"Collections & Generics",answer:'Vector is a synchronized, resizable array from early Java. Its methods are synchronized, which makes individual operations thread-safe but often slower and not enough for compound actions such as check-then-act. Modern code usually uses ArrayList for non-concurrent lists, Collections.synchronizedList for a synchronized wrapper, or CopyOnWriteArrayList/concurrent queues for specific concurrent patterns.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Vector<String> v = new Vector<>(); v.add("legacy"); System.out.println(v.firstElement()); } }\n```',tech:"java",displayNumber:80,sortKey:433},{id:"java-434",number:81,title:"What is CopyOnWriteArrayList?",question:"What is CopyOnWriteArrayList?",difficulty:"Intermediate",category:"Collections & Generics",answer:'CopyOnWriteArrayList is a thread-safe List optimized for many reads and few writes. Every mutating operation copies the underlying array, so iterators see a stable snapshot and never throw ConcurrentModificationException. Reads are fast and lock-free, but writes are expensive and memory-heavy. It is useful for listener lists, routing tables, and configuration snapshots.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<String> listeners = new CopyOnWriteArrayList<>(); listeners.add("A"); for (String s : listeners) { listeners.add("B"); System.out.println(s); } System.out.println(listeners); } }\n```',tech:"java",displayNumber:81,sortKey:434},{id:"java-435",number:82,title:"Why should ArrayList initial capacity matter?",question:"Why should ArrayList initial capacity matter?",difficulty:"Intermediate",category:"Collections & Generics",answer:"ArrayList grows automatically, but each growth allocates a new array and copies elements. If you know the approximate size, passing an initial capacity reduces reallocations and copies. Capacity is not the same as size: capacity is internal storage; size is the number of elements actually present.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<Integer> ids = new ArrayList<>(1000); for (int i = 0; i < 3; i++) ids.add(i); System.out.println(ids.size()); } }\n```",tech:"java",displayNumber:82,sortKey:435},{id:"java-436",number:83,title:"What is fail-fast iteration in lists?",question:"What is fail-fast iteration in lists?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Most non-concurrent collection iterators are fail-fast. If the collection is structurally modified outside the iterator after iterator creation, the iterator may throw ConcurrentModificationException. This is a bug detector, not a concurrency guarantee. Use Iterator.remove, collect changes separately, or use concurrent/snapshot collections when mutation during iteration is required.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = new ArrayList<>(List.of("a", "b")); Iterator<String> it = xs.iterator(); while (it.hasNext()) if (it.next().equals("a")) it.remove(); System.out.println(xs); } }\n```',tech:"java",displayNumber:83,sortKey:436},{id:"java-437",number:84,title:"How do you create unmodifiable and immutable lists?",question:"How do you create unmodifiable and immutable lists?",difficulty:"Basic",category:"Collections & Generics",answer:'Collections.unmodifiableList returns a read-only view of an existing list; changes to the backing list are still visible. List.of and List.copyOf create unmodifiable lists that reject nulls and are not backed by the original mutable list. For defensive APIs, prefer List.copyOf so callers cannot mutate state through an alias.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> mutable = new ArrayList<>(List.of("a")); List<String> copy = List.copyOf(mutable); mutable.add("b"); System.out.println(copy); } }\n```',tech:"java",displayNumber:84,sortKey:437},{id:"java-438",number:85,title:"How do you safely sort a List?",question:"How do you safely sort a List?",difficulty:"Basic",category:"Collections & Generics",answer:'Use List.sort or Collections.sort with a Comparator. Sorting mutates the list, so copy first if callers expect the original order to remain unchanged. Comparators should be consistent, handle nulls if nulls are possible, and avoid subtraction for numeric comparison because it can overflow.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(List.of("Bo", "Alice")); names.sort(Comparator.comparingInt(String::length)); System.out.println(names); } }\n```',tech:"java",displayNumber:85,sortKey:438},{id:"java-439",number:86,title:"What makes a Set different from a List?",question:"What makes a Set different from a List?",difficulty:"Basic",category:"Collections & Generics",answer:'A Set stores unique elements and does not expose positional access. Uniqueness is determined either by equals/hashCode for hash-based sets or by comparison for sorted sets. Choose a Set when membership and duplicate prevention matter more than index order.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new HashSet<>(); s.add("a"); s.add("a"); System.out.println(s.size()); } }\n```',tech:"java",displayNumber:86,sortKey:439},{id:"java-440",number:87,title:"How does HashSet work internally?",question:"How does HashSet work internally?",difficulty:"Intermediate",category:"Collections & Generics",answer:"HashSet is backed by a HashMap where each set element is stored as a map key. add, contains, and remove are O(1) on average when hashCode distributes values well. It does not preserve insertion order and allows one null element.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<Integer> ids = new HashSet<>(); ids.add(3); ids.add(1); System.out.println(ids.contains(1)); } }\n```",tech:"java",displayNumber:87,sortKey:440},{id:"java-441",number:88,title:"Why must equals and hashCode agree for HashSet?",question:"Why must equals and hashCode agree for HashSet?",difficulty:"Intermediate",category:"Collections & Generics",answer:'HashSet first uses hashCode to find a bucket, then equals to check logical equality inside that bucket. If equal objects have different hash codes, duplicates can appear and lookups can fail. The contract is: if a.equals(b) is true, a.hashCode() must equal b.hashCode().\n\n```java\nimport java.util.*;\nrecord User(int id, String name) {}\nclass Demo { public static void main(String[] args) { Set<User> users = new HashSet<>(); users.add(new User(1,"A")); users.add(new User(1,"A")); System.out.println(users.size()); } }\n```',tech:"java",displayNumber:88,sortKey:441},{id:"java-442",number:89,title:"What is LinkedHashSet used for?",question:"What is LinkedHashSet used for?",difficulty:"Basic",category:"Collections & Generics",answer:'LinkedHashSet combines HashSet uniqueness with predictable iteration order. It keeps a linked list across entries, normally in insertion order. It costs slightly more memory than HashSet but is useful when stable output or predictable tests matter.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new LinkedHashSet<>(); s.add("b"); s.add("a"); System.out.println(s); } }\n```',tech:"java",displayNumber:89,sortKey:442},{id:"java-443",number:90,title:"How does TreeSet order elements?",question:"How does TreeSet order elements?",difficulty:"Intermediate",category:"Collections & Generics",answer:'TreeSet is a NavigableSet backed by a balanced tree. It keeps elements sorted by natural ordering or a Comparator. add, contains, and remove are O(log n). Its equality for uniqueness follows comparison: if compare returns 0, the elements are considered duplicates.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Set<String> s = new TreeSet<>(String.CASE_INSENSITIVE_ORDER); s.add("java"); s.add("JAVA"); System.out.println(s); } }\n```',tech:"java",displayNumber:90,sortKey:443},{id:"java-444",number:91,title:"What is EnumSet?",question:"What is EnumSet?",difficulty:"Intermediate",category:"Collections & Generics",answer:"EnumSet is a high-performance Set specialized for enum values. Internally it uses bit vectors, making it compact and fast. It rejects nulls and can only contain values from a single enum type. Use it instead of HashSet for enum flags.\n\n```java\nimport java.util.*;\nenum Role { USER, ADMIN }\nclass Demo { public static void main(String[] args) { EnumSet<Role> roles = EnumSet.of(Role.USER, Role.ADMIN); System.out.println(roles.contains(Role.ADMIN)); } }\n```",tech:"java",displayNumber:91,sortKey:444},{id:"java-445",number:92,title:"How do HashSet, LinkedHashSet, and TreeSet compare?",question:"How do HashSet, LinkedHashSet, and TreeSet compare?",difficulty:"Basic",category:"Collections & Generics",answer:"HashSet is fastest on average but unordered. LinkedHashSet preserves insertion order with a little extra memory. TreeSet keeps elements sorted and supports range queries, but operations are O(log n). Pick by ordering requirement first, then performance.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { System.out.println(new HashSet<>(List.of(3,1,2))); System.out.println(new LinkedHashSet<>(List.of(3,1,2))); System.out.println(new TreeSet<>(List.of(3,1,2))); } }\n```",tech:"java",displayNumber:92,sortKey:445},{id:"java-446",number:93,title:"What are NavigableSet operations?",question:"What are NavigableSet operations?",difficulty:"Intermediate",category:"Collections & Generics",answer:"NavigableSet adds search methods such as lower, floor, ceiling, higher, pollFirst, pollLast, descendingSet, and range views. TreeSet implements it. These operations are ideal for ordered lookups like nearest value, leaderboard ranges, or time windows.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { NavigableSet<Integer> s = new TreeSet<>(List.of(10,20,30)); System.out.println(s.floor(25)); System.out.println(s.higher(20)); } }\n```",tech:"java",displayNumber:93,sortKey:446},{id:"java-447",number:94,title:"Can Sets contain mutable objects?",question:"Can Sets contain mutable objects?",difficulty:"Advanced",category:"Collections & Generics",answer:"They can, but mutating fields used by equals, hashCode, or compareTo while the object is inside a Set breaks lookup and uniqueness. The object may become stored in the wrong hash bucket or tree position. Prefer immutable keys/elements or remove, mutate, and re-add.\n\n```java\nimport java.util.*;\nclass Demo { static class Box { int id; Box(int id){this.id=id;} public boolean equals(Object o){return o instanceof Box b && b.id==id;} public int hashCode(){return id;} } public static void main(String[] args) { Box b=new Box(1); Set<Box> s=new HashSet<>(); s.add(b); b.id=2; System.out.println(s.contains(b)); } }\n```",tech:"java",displayNumber:94,sortKey:447},{id:"java-448",number:95,title:"How do you remove duplicates while preserving order?",question:"How do you remove duplicates while preserving order?",difficulty:"Basic",category:"Collections & Generics",answer:'Use LinkedHashSet when you need uniqueness and original encounter order. Construct it from the list, then optionally create a new ArrayList. This avoids manual contains checks and is usually clearer and faster.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = List.of("a","b","a"); List<String> unique = new ArrayList<>(new LinkedHashSet<>(xs)); System.out.println(unique); } }\n```',tech:"java",displayNumber:95,sortKey:448},{id:"java-449",number:96,title:"What is a Map?",question:"What is a Map?",difficulty:"Basic",category:"Collections & Generics",answer:'A Map stores key-value pairs and is not a subtype of Collection. Keys are unique; values may be duplicated. Common implementations include HashMap for general lookup, LinkedHashMap for predictable order, TreeMap for sorted keys, ConcurrentHashMap for concurrency, and EnumMap for enum keys.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> ages = new HashMap<>(); ages.put("Ana", 30); System.out.println(ages.get("Ana")); } }\n```',tech:"java",displayNumber:96,sortKey:449},{id:"java-450",number:97,title:"How does HashMap work internally?",question:"How does HashMap work internally?",difficulty:"Intermediate",category:"Collections & Generics",answer:'HashMap stores entries in an array of buckets. It hashes the key, maps the hash to a bucket, and resolves collisions within that bucket. Since Java 8, heavily-collided buckets can become balanced trees when thresholds are met, improving worst-case lookup from O(n) to O(log n). Average get and put are O(1).\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new HashMap<>(); m.put("one", 1); m.put("two", 2); System.out.println(m.get("two")); } }\n```',tech:"java",displayNumber:97,sortKey:450},{id:"java-451",number:98,title:"What are HashMap capacity and load factor?",question:"What are HashMap capacity and load factor?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Capacity is the number of buckets; load factor controls when resizing happens. The default load factor is 0.75, balancing memory and collisions. When size exceeds capacity times load factor, HashMap resizes and rehashes entries. If expected size is known, initialize capacity to reduce resizing.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Integer,String> m = new HashMap<>(128, 0.75f); m.put(1, "a"); System.out.println(m); } }\n```',tech:"java",displayNumber:98,sortKey:451},{id:"java-452",number:99,title:"Can HashMap use null keys and values?",question:"Can HashMap use null keys and values?",difficulty:"Basic",category:"Collections & Generics",answer:'HashMap permits one null key and multiple null values. This differs from Hashtable and ConcurrentHashMap, which reject null. Null can make missing-value logic ambiguous because get returns null both for absent keys and keys mapped to null; use containsKey when needed.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,String> m = new HashMap<>(); m.put(null, "root"); m.put("x", null); System.out.println(m.containsKey("x")); } }\n```',tech:"java",displayNumber:99,sortKey:452},{id:"java-453",number:100,title:"Why are mutable keys dangerous in HashMap?",question:"Why are mutable keys dangerous in HashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'HashMap lookup depends on the key hash and equality at insertion time. If fields used by hashCode or equals change while the key is in the map, the entry may become unreachable even though it still exists. Use immutable key classes such as records, String, Integer, UUID, or remove and reinsert after mutation.\n\n```java\nimport java.util.*;\nclass Demo { record Key(int id) {} public static void main(String[] args) { Map<Key,String> m = new HashMap<>(); m.put(new Key(1), "ok"); System.out.println(m.get(new Key(1))); } }\n```',tech:"java",displayNumber:100,sortKey:453},{id:"java-454",number:101,title:"What is LinkedHashMap?",question:"What is LinkedHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'LinkedHashMap extends hash-table lookup with a doubly linked list over entries. It preserves insertion order by default and can also maintain access order. Access-order LinkedHashMap is commonly used for simple LRU caches by overriding removeEldestEntry.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new LinkedHashMap<>(); m.put("b",2); m.put("a",1); System.out.println(m.keySet()); } }\n```',tech:"java",displayNumber:101,sortKey:454},{id:"java-455",number:102,title:"How do you build a simple LRU cache with LinkedHashMap?",question:"How do you build a simple LRU cache with LinkedHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'Create a LinkedHashMap with accessOrder=true and override removeEldestEntry to evict when size exceeds the limit. This is simple and works for single-threaded or externally synchronized use. For high-concurrency or production caching features, use a real cache library.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Integer,String> cache = new LinkedHashMap<>(16, .75f, true) { protected boolean removeEldestEntry(Map.Entry<Integer,String> e) { return size() > 2; } }; cache.put(1,"a"); cache.put(2,"b"); cache.get(1); cache.put(3,"c"); System.out.println(cache.keySet()); } }\n```',tech:"java",displayNumber:102,sortKey:455},{id:"java-456",number:103,title:"What is TreeMap?",question:"What is TreeMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'TreeMap is a NavigableMap backed by a red-black tree. It stores keys sorted by natural order or a Comparator. get, put, and remove are O(log n). It is useful for sorted iteration and range queries such as subMap, headMap, and tailMap.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { NavigableMap<Integer,String> m = new TreeMap<>(); m.put(20,"b"); m.put(10,"a"); System.out.println(m.firstEntry()); } }\n```',tech:"java",displayNumber:103,sortKey:456},{id:"java-457",number:104,title:"How does ConcurrentHashMap differ from HashMap?",question:"How does ConcurrentHashMap differ from HashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'ConcurrentHashMap supports safe concurrent access without synchronizing the whole map for every operation. It rejects null keys and values to avoid ambiguity in concurrent reads. Retrievals are highly concurrent, and updates lock only affected internal regions/nodes. Iterators are weakly consistent, not fail-fast.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> m = new ConcurrentHashMap<>(); m.put("a", 1); m.compute("a", (k,v) -> v + 1); System.out.println(m); } }\n```',tech:"java",displayNumber:104,sortKey:457},{id:"java-458",number:105,title:"What is EnumMap?",question:"What is EnumMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'EnumMap is a Map optimized for enum keys. It stores values in an array indexed by enum ordinal, so it is fast, compact, and preserves enum declaration order. It rejects null keys but allows null values. Use it instead of HashMap whenever all keys are from one enum type.\n\n```java\nimport java.util.*;\nenum Day { MON, TUE }\nclass Demo { public static void main(String[] args) { EnumMap<Day,String> m = new EnumMap<>(Day.class); m.put(Day.MON, "work"); System.out.println(m); } }\n```',tech:"java",displayNumber:105,sortKey:458},{id:"java-459",number:106,title:"How do getOrDefault, putIfAbsent, and computeIfAbsent differ?",question:"How do getOrDefault, putIfAbsent, and computeIfAbsent differ?",difficulty:"Intermediate",category:"Collections & Generics",answer:'getOrDefault reads a value or returns a fallback without mutating. putIfAbsent writes a value only when no mapping exists or the current value is null for maps that allow null. computeIfAbsent lazily computes and stores a value only when absent, which is ideal for grouping or memoizing.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,List<Integer>> m = new HashMap<>(); m.computeIfAbsent("x", k -> new ArrayList<>()).add(1); System.out.println(m.getOrDefault("y", List.of())); } }\n```',tech:"java",displayNumber:106,sortKey:459},{id:"java-460",number:107,title:"How do you iterate over a Map efficiently?",question:"How do you iterate over a Map efficiently?",difficulty:"Basic",category:"Collections & Generics",answer:'Use entrySet when both key and value are needed; it avoids a second lookup for each key. Use keySet when only keys are needed and values when only values are needed. Do not assume HashMap iteration order.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = Map.of("a",1,"b",2); for (Map.Entry<String,Integer> e : m.entrySet()) System.out.println(e.getKey()+"="+e.getValue()); } }\n```',tech:"java",displayNumber:107,sortKey:460},{id:"java-461",number:108,title:"What are Map views?",question:"What are Map views?",difficulty:"Intermediate",category:"Collections & Generics",answer:'keySet, values, and entrySet are backed views of the map, not independent copies. Removing from a mutable view removes from the map. Adding is generally unsupported through views except by putting into the map itself. Copy the view if you need a snapshot.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new HashMap<>(Map.of("a",1,"b",2)); m.keySet().remove("a"); System.out.println(m); } }\n```',tech:"java",displayNumber:108,sortKey:461},{id:"java-462",number:109,title:"What is hash collision handling in HashMap?",question:"What is hash collision handling in HashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'A collision occurs when different keys map to the same bucket. HashMap stores colliding entries in that bucket and checks equality to find the correct key. With enough collisions and sufficient table size, Java can treeify the bucket to reduce lookup cost. Good hashCode implementations keep collisions rare.\n\n```java\nimport java.util.*;\nclass Demo { static class K { int id; K(int id){this.id=id;} public int hashCode(){return 1;} public boolean equals(Object o){return o instanceof K k && k.id==id;} } public static void main(String[] args) { Map<K,String> m=new HashMap<>(); m.put(new K(1),"a"); System.out.println(m.get(new K(1))); } }\n```',tech:"java",displayNumber:109,sortKey:462},{id:"java-463",number:110,title:"What is IdentityHashMap?",question:"What is IdentityHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'IdentityHashMap compares keys with == instead of equals and uses identity-based hashing. It is not a general-purpose Map. It is useful for object graph traversal, serialization bookkeeping, or cycle detection where distinct object instances must remain distinct even if equals says they are equal.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new IdentityHashMap<>(); m.put(new String("x"), 1); m.put(new String("x"), 2); System.out.println(m.size()); } }\n```',tech:"java",displayNumber:110,sortKey:463},{id:"java-464",number:111,title:"What is WeakHashMap?",question:"What is WeakHashMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'WeakHashMap stores keys as weak references. If a key is no longer strongly reachable elsewhere, the garbage collector may remove its entry. It is useful for metadata associated with objects without preventing their collection. It should not be used when entries must remain reliably present.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<Object,String> m = new WeakHashMap<>(); Object key = new Object(); m.put(key, "metadata"); System.out.println(m.containsKey(key)); } }\n```',tech:"java",displayNumber:111,sortKey:464},{id:"java-465",number:112,title:"How do equals and compareTo affect TreeMap keys?",question:"How do equals and compareTo affect TreeMap keys?",difficulty:"Advanced",category:"Collections & Generics",answer:'TreeMap uses compareTo or Comparator to decide both order and key uniqueness. If compare returns 0, TreeMap treats keys as the same even if equals would return false. A comparator inconsistent with equals can be valid but surprising, so document it and use it deliberately.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = new TreeMap<>(String.CASE_INSENSITIVE_ORDER); m.put("java",1); m.put("JAVA",2); System.out.println(m); } }\n```',tech:"java",displayNumber:112,sortKey:465},{id:"java-466",number:113,title:"How do you merge counts in a Map?",question:"How do you merge counts in a Map?",difficulty:"Basic",category:"Collections & Generics",answer:'Use merge for frequency maps and accumulation. It inserts the initial value when absent and combines old and new values when present. This avoids verbose containsKey checks and is safe for concise counting logic.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> counts = new HashMap<>(); for (String w : List.of("a","b","a")) counts.merge(w, 1, Integer::sum); System.out.println(counts); } }\n```',tech:"java",displayNumber:113,sortKey:466},{id:"java-467",number:114,title:"What makes Hashtable legacy?",question:"What makes Hashtable legacy?",difficulty:"Basic",category:"Collections & Generics",answer:'Hashtable is synchronized and does not allow null keys or values. Like Vector, it is a legacy class whose synchronized methods rarely provide the right concurrency model for compound operations. Prefer HashMap, ConcurrentHashMap, or explicit synchronization depending on the requirement.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Hashtable<String,Integer> h = new Hashtable<>(); h.put("a",1); System.out.println(h.get("a")); } }\n```',tech:"java",displayNumber:114,sortKey:467},{id:"java-468",number:115,title:"How do immutable Map factories work?",question:"How do immutable Map factories work?",difficulty:"Basic",category:"Collections & Generics",answer:'Map.of and Map.ofEntries create unmodifiable maps that reject null keys and values and duplicate keys. They are compact and clear for constants and small maps. Iteration order is unspecified unless documented by a specific implementation, so do not rely on it.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> m = Map.of("a",1,"b",2); System.out.println(m.get("a")); } }\n```',tech:"java",displayNumber:115,sortKey:468},{id:"java-469",number:116,title:"What is the Queue interface?",question:"What is the Queue interface?",difficulty:"Basic",category:"Collections & Generics",answer:'Queue represents elements waiting to be processed, usually FIFO. offer/poll/peek are preferred because they return special values instead of throwing when an operation cannot be completed. add/remove/element throw exceptions on failure.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<String> q = new ArrayDeque<>(); q.offer("job"); System.out.println(q.poll()); } }\n```',tech:"java",displayNumber:116,sortKey:469},{id:"java-470",number:117,title:"How does PriorityQueue work?",question:"How does PriorityQueue work?",difficulty:"Intermediate",category:"Collections & Generics",answer:"PriorityQueue orders elements by natural order or Comparator, not insertion order. The head is the least element according to that ordering by default. It is backed by a binary heap, so offer and poll are O(log n), peek is O(1), and iteration is not sorted.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<Integer> pq = new PriorityQueue<>(); pq.addAll(List.of(5,1,3)); System.out.println(pq.poll()); } }\n```",tech:"java",displayNumber:117,sortKey:470},{id:"java-471",number:118,title:"How do you create a max-heap PriorityQueue?",question:"How do you create a max-heap PriorityQueue?",difficulty:"Basic",category:"Collections & Generics",answer:"PriorityQueue is a min-heap by default. Provide Comparator.reverseOrder for a max-heap. Avoid subtraction comparators like (a,b)->b-a because integer overflow can break ordering.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Queue<Integer> pq = new PriorityQueue<>(Comparator.reverseOrder()); pq.addAll(List.of(5,1,3)); System.out.println(pq.poll()); } }\n```",tech:"java",displayNumber:118,sortKey:471},{id:"java-472",number:119,title:"What is ArrayDeque?",question:"What is ArrayDeque?",difficulty:"Basic",category:"Collections & Generics",answer:'ArrayDeque is a resizable-array implementation of Deque. It is usually faster than Stack and LinkedList for stack or queue behavior. It does not allow null elements because null is used by methods such as poll to mean no element.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<String> stack = new ArrayDeque<>(); stack.push("a"); stack.push("b"); System.out.println(stack.pop()); } }\n```',tech:"java",displayNumber:119,sortKey:472},{id:"java-473",number:120,title:"Why prefer ArrayDeque over Stack?",question:"Why prefer ArrayDeque over Stack?",difficulty:"Basic",category:"Collections & Generics",answer:"Stack is a legacy synchronized Vector subclass. ArrayDeque provides cleaner Deque methods and better performance for single-threaded stack usage. Use push, pop, and peek for stack behavior or offer/poll for queue behavior.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { Deque<Integer> stack = new ArrayDeque<>(); stack.push(1); stack.push(2); System.out.println(stack.pop()); } }\n```",tech:"java",displayNumber:120,sortKey:473},{id:"java-474",number:121,title:"What is BlockingQueue?",question:"What is BlockingQueue?",difficulty:"Intermediate",category:"Collections & Generics",answer:'BlockingQueue is a concurrent queue whose operations can wait for space or elements. put blocks until space is available; take blocks until an element is available. It is commonly used for producer-consumer designs and avoids manual wait/notify code.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { BlockingQueue<String> q = new ArrayBlockingQueue<>(1); q.put("job"); System.out.println(q.take()); } }\n```',tech:"java",displayNumber:121,sortKey:474},{id:"java-475",number:122,title:"Compare ArrayBlockingQueue and LinkedBlockingQueue.",question:"Compare ArrayBlockingQueue and LinkedBlockingQueue.",difficulty:"Intermediate",category:"Collections & Generics",answer:"ArrayBlockingQueue is bounded and backed by an array; its capacity is fixed. LinkedBlockingQueue is node-based and optionally bounded, but the no-arg constructor creates a very large capacity that can hide backpressure. Prefer explicit bounds in server code to avoid memory growth.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { BlockingQueue<Integer> q = new ArrayBlockingQueue<>(2); q.offer(1); q.offer(2); System.out.println(q.offer(3)); } }\n```",tech:"java",displayNumber:122,sortKey:475},{id:"java-476",number:123,title:"What is PriorityBlockingQueue?",question:"What is PriorityBlockingQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:"PriorityBlockingQueue is an unbounded blocking queue that orders elements by priority. take blocks when empty, but put does not block for capacity because it is unbounded. Since equal-priority ordering is not guaranteed, include a sequence number if stable ordering matters.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { PriorityBlockingQueue<Integer> q = new PriorityBlockingQueue<>(); q.put(3); q.put(1); System.out.println(q.take()); } }\n```",tech:"java",displayNumber:123,sortKey:476},{id:"java-477",number:124,title:"What is SynchronousQueue?",question:"What is SynchronousQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:'SynchronousQueue has no internal capacity. Each put must rendezvous with a take, so it directly hands off work between threads. It is useful in executor designs where tasks should not queue internally and producers should block until a consumer is ready.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) throws Exception { SynchronousQueue<String> q = new SynchronousQueue<>(); new Thread(() -> { try { q.put("handoff"); } catch (InterruptedException ignored) {} }).start(); System.out.println(q.take()); } }\n```',tech:"java",displayNumber:124,sortKey:477},{id:"java-478",number:125,title:"What is DelayQueue?",question:"What is DelayQueue?",difficulty:"Advanced",category:"Collections & Generics",answer:"DelayQueue stores Delayed elements and only releases each element after its delay expires. It is unbounded and useful for scheduled retries, timeout handling, and expiring items. Elements must implement getDelay and compareTo consistently.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { static class Task implements Delayed { public long getDelay(TimeUnit u){ return 0; } public int compareTo(Delayed d){ return 0; } } public static void main(String[] args) throws Exception { DelayQueue<Task> q = new DelayQueue<>(); q.put(new Task()); System.out.println(q.take().getClass().getSimpleName()); } }\n```",tech:"java",displayNumber:125,sortKey:478},{id:"java-479",number:126,title:"Why use generics?",question:"Why use generics?",difficulty:"Basic",category:"Collections & Generics",answer:'Generics provide compile-time type safety and remove most casts. A List<String> can only accept strings, so mistakes are caught before runtime. Generics also make APIs self-documenting because type relationships are visible in method signatures.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = new ArrayList<>(); names.add("Ada"); String first = names.get(0); System.out.println(first.toUpperCase()); } }\n```',tech:"java",displayNumber:126,sortKey:479},{id:"java-480",number:127,title:"What is a bounded type parameter?",question:"What is a bounded type parameter?",difficulty:"Intermediate",category:"Collections & Generics",answer:"A bounded type parameter restricts acceptable types. <T extends Number> means T must be Number or a subclass. Bounds let generic code call methods from the bound while preserving specific type information.\n\n```java\nclass Demo { static <T extends Number> double twice(T n) { return n.doubleValue() * 2; } public static void main(String[] args) { System.out.println(twice(21)); } }\n```",tech:"java",displayNumber:127,sortKey:480},{id:"java-481",number:128,title:"What are multiple bounds in generics?",question:"What are multiple bounds in generics?",difficulty:"Advanced",category:"Collections & Generics",answer:"A type parameter can have multiple bounds, with at most one class bound first and then interfaces. For example, <T extends Number & Comparable<T>> requires both numeric behavior and comparability. This lets methods rely on several capabilities without accepting unrelated types.\n\n```java\nclass Demo { static <T extends Number & Comparable<T>> T max(T a, T b) { return a.compareTo(b) >= 0 ? a : b; } public static void main(String[] args) { System.out.println(max(3, 7)); } }\n```",tech:"java",displayNumber:128,sortKey:481},{id:"java-482",number:129,title:"What is an unbounded wildcard?",question:"What is an unbounded wildcard?",difficulty:"Basic",category:"Collections & Generics",answer:"The wildcard ? means an unknown type. List<?> is useful when code only needs to read objects or use size/iteration without adding typed values. You cannot add arbitrary elements except null because the compiler does not know the actual element type.\n\n```java\nimport java.util.*;\nclass Demo { static void printSize(List<?> xs) { System.out.println(xs.size()); } public static void main(String[] args) { printSize(List.of(1,2,3)); } }\n```",tech:"java",displayNumber:129,sortKey:482},{id:"java-483",number:130,title:"What is an upper-bounded wildcard?",question:"What is an upper-bounded wildcard?",difficulty:"Intermediate",category:"Collections & Generics",answer:"? extends T accepts T or any subtype of T. It is best for producers: you can read values as T, but cannot safely add T values because the actual list might be a more specific subtype. This is the extends part of PECS.\n\n```java\nimport java.util.*;\nclass Demo { static double sum(List<? extends Number> nums) { double s=0; for (Number n: nums) s+=n.doubleValue(); return s; } public static void main(String[] args) { System.out.println(sum(List.of(1,2,3))); } }\n```",tech:"java",displayNumber:130,sortKey:483},{id:"java-484",number:131,title:"What is a lower-bounded wildcard?",question:"What is a lower-bounded wildcard?",difficulty:"Intermediate",category:"Collections & Generics",answer:"? super T accepts T or any supertype of T. It is best for consumers: you can add T values safely, but reads come back only as Object. This is the super part of PECS.\n\n```java\nimport java.util.*;\nclass Demo { static void addInts(List<? super Integer> out) { out.add(1); out.add(2); } public static void main(String[] args) { List<Number> nums = new ArrayList<>(); addInts(nums); System.out.println(nums); } }\n```",tech:"java",displayNumber:131,sortKey:484},{id:"java-485",number:132,title:"What does PECS mean?",question:"What does PECS mean?",difficulty:"Intermediate",category:"Collections & Generics",answer:"PECS means Producer Extends, Consumer Super. If a parameter produces values for your method to read, use ? extends T. If it consumes values your method writes, use ? super T. If both reading and writing exact type values are needed, use a concrete generic type such as List<T>.\n\n```java\nimport java.util.*;\nclass Demo { static <T> void copy(List<? extends T> src, List<? super T> dst) { for (T x : src) dst.add(x); } public static void main(String[] args) { List<Number> out = new ArrayList<>(); copy(List.of(1,2), out); System.out.println(out); } }\n```",tech:"java",displayNumber:132,sortKey:485},{id:"java-486",number:133,title:"What is type erasure?",question:"What is type erasure?",difficulty:"Advanced",category:"Collections & Generics",answer:"Java implements generics mostly by type erasure. Generic type information is checked at compile time and erased to raw types or bounds in bytecode. This preserves backward compatibility but means you cannot directly create new T(), use primitive type arguments, or check instanceof List<String>.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> a = new ArrayList<>(); List<Integer> b = new ArrayList<>(); System.out.println(a.getClass() == b.getClass()); } }\n```",tech:"java",displayNumber:133,sortKey:486},{id:"java-487",number:134,title:"What are raw types and why avoid them?",question:"What are raw types and why avoid them?",difficulty:"Intermediate",category:"Collections & Generics",answer:'A raw type is a generic type used without type arguments, such as List instead of List<String>. Raw types disable generic type checking and can cause ClassCastException later. They exist for backward compatibility with pre-generics code; modern code should avoid them except at legacy boundaries.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> safe = new ArrayList<>(); safe.add("x"); System.out.println(safe.get(0)); } }\n```',tech:"java",displayNumber:134,sortKey:487},{id:"java-488",number:135,title:"What is a generic method?",question:"What is a generic method?",difficulty:"Basic",category:"Collections & Generics",answer:'A generic method declares its own type parameter before the return type. It can infer types from arguments and is useful when the type relationship belongs to one method rather than the whole class.\n\n```java\nclass Demo { static <T> T first(T a, T b) { return a; } public static void main(String[] args) { String s = first("a", "b"); System.out.println(s); } }\n```',tech:"java",displayNumber:135,sortKey:488},{id:"java-489",number:136,title:"How are ConcurrentHashMap iterators different?",question:"How are ConcurrentHashMap iterators different?",difficulty:"Advanced",category:"Collections & Generics",answer:'ConcurrentHashMap iterators are weakly consistent. They do not throw ConcurrentModificationException and may reflect some, all, or none of the updates made after iterator creation. This is useful for concurrent monitoring and traversal, but not for exact snapshots.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> m = new ConcurrentHashMap<>(); m.put("a",1); for (String k : m.keySet()) { m.put("b",2); System.out.println(k); } } }\n```',tech:"java",displayNumber:136,sortKey:489},{id:"java-490",number:137,title:"How do atomic updates work in ConcurrentHashMap?",question:"How do atomic updates work in ConcurrentHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Use compute, computeIfAbsent, merge, or replace to perform per-key atomic updates. Avoid get-then-put because another thread can modify the value between calls. The mapping function should be short and should not depend on external locks.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentHashMap<String,Integer> counts = new ConcurrentHashMap<>(); counts.merge("x", 1, Integer::sum); counts.merge("x", 1, Integer::sum); System.out.println(counts); } }\n```',tech:"java",displayNumber:137,sortKey:490},{id:"java-491",number:138,title:"When is CopyOnWriteArrayList a good concurrent choice?",question:"When is CopyOnWriteArrayList a good concurrent choice?",difficulty:"Intermediate",category:"Collections & Generics",answer:'It is good when reads and iteration dominate and writes are rare. Iterators are snapshot-based, so readers avoid locking and are insulated from concurrent modifications. It is bad for high write volume because every mutation copies the array.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<String> xs = new CopyOnWriteArrayList<>(); xs.add("v1"); for (String x : xs) xs.add("v2"); System.out.println(xs); } }\n```',tech:"java",displayNumber:138,sortKey:491},{id:"java-492",number:139,title:"What is ConcurrentLinkedQueue?",question:"What is ConcurrentLinkedQueue?",difficulty:"Intermediate",category:"Collections & Generics",answer:'ConcurrentLinkedQueue is an unbounded, non-blocking FIFO queue based on linked nodes. It is safe for multiple producers and consumers and uses lock-free algorithms. poll returns null when empty; use BlockingQueue if consumers should wait.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentLinkedQueue<String> q = new ConcurrentLinkedQueue<>(); q.offer("a"); System.out.println(q.poll()); } }\n```',tech:"java",displayNumber:139,sortKey:492},{id:"java-493",number:140,title:"What is ConcurrentSkipListMap?",question:"What is ConcurrentSkipListMap?",difficulty:"Advanced",category:"Collections & Generics",answer:'ConcurrentSkipListMap is a concurrent, sorted NavigableMap implemented with a skip-list. It gives expected O(log n) operations and supports range queries under concurrent access. Use it when you need both ordering and concurrency; otherwise ConcurrentHashMap is usually faster.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentSkipListMap<Integer,String> m = new ConcurrentSkipListMap<>(); m.put(2,"b"); m.put(1,"a"); System.out.println(m.firstKey()); } }\n```',tech:"java",displayNumber:140,sortKey:493},{id:"java-494",number:141,title:"What is ConcurrentSkipListSet?",question:"What is ConcurrentSkipListSet?",difficulty:"Advanced",category:"Collections & Generics",answer:"ConcurrentSkipListSet is a concurrent sorted set backed by ConcurrentSkipListMap. It supports NavigableSet operations such as ceiling and subSet while allowing concurrent access. It is useful for sorted registries, time windows, and leaderboards with concurrent updates.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { ConcurrentSkipListSet<Integer> s = new ConcurrentSkipListSet<>(); s.add(10); s.add(5); System.out.println(s.ceiling(6)); } }\n```",tech:"java",displayNumber:141,sortKey:494},{id:"java-495",number:142,title:"When should you choose blocking vs non-blocking concurrent collections?",question:"When should you choose blocking vs non-blocking concurrent collections?",difficulty:"Advanced",category:"Collections & Generics",answer:'Choose BlockingQueue when threads should wait for work or backpressure. Choose non-blocking collections such as ConcurrentLinkedQueue when threads should keep running and handle empty results themselves. Blocking designs are simpler for producer-consumer pipelines; non-blocking designs can fit event loops and low-latency systems.\n\n```java\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { BlockingQueue<String> blocking = new LinkedBlockingQueue<>(10); ConcurrentLinkedQueue<String> nonBlocking = new ConcurrentLinkedQueue<>(); System.out.println(blocking.offer("a") && nonBlocking.offer("b")); } }\n```',tech:"java",displayNumber:142,sortKey:495},{id:"java-496",number:143,title:"Vector vs ArrayList: which should modern code use?",question:"Vector vs ArrayList: which should modern code use?",difficulty:"Basic",category:"Collections & Generics",answer:'Use ArrayList for normal single-threaded lists. Vector synchronizes individual methods, which adds overhead and does not automatically make compound workflows correct. If sharing across threads, choose a collection designed for the access pattern or synchronize externally around the full operation.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> list = new ArrayList<>(); list.add("modern"); System.out.println(list); } }\n```',tech:"java",displayNumber:143,sortKey:496},{id:"java-497",number:144,title:"Hashtable vs HashMap vs ConcurrentHashMap?",question:"Hashtable vs HashMap vs ConcurrentHashMap?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Hashtable is legacy, synchronized, and rejects null. HashMap is unsynchronized and permits null. ConcurrentHashMap is designed for concurrent access, rejects null, and scales better than synchronizing an entire map. Pick HashMap by default; pick ConcurrentHashMap for shared mutable maps.\n\n```java\nimport java.util.*;\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { Map<String,Integer> local = new HashMap<>(); Map<String,Integer> shared = new ConcurrentHashMap<>(); local.put("a",1); shared.put("b",2); System.out.println(local.size()+shared.size()); } }\n```',tech:"java",displayNumber:144,sortKey:497},{id:"java-498",number:145,title:"What do Collections.synchronizedXxx wrappers do?",question:"What do Collections.synchronizedXxx wrappers do?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collections.synchronizedList, synchronizedSet, and synchronizedMap return wrappers that synchronize each method on a common lock. Iteration still requires manually synchronizing on the wrapper during the whole traversal. These wrappers are useful for simple compatibility but concurrent collections are often better.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> list = Collections.synchronizedList(new ArrayList<>()); list.add("x"); synchronized (list) { for (String s : list) System.out.println(s); } } }\n```',tech:"java",displayNumber:145,sortKey:498},{id:"java-499",number:146,title:"What is the difference between fail-fast and fail-safe terminology?",question:"What is the difference between fail-fast and fail-safe terminology?",difficulty:"Intermediate",category:"Collections & Generics",answer:"Java documentation usually says fail-fast for ordinary iterators and weakly consistent or snapshot for concurrent iterators. Fail-safe is an informal term, not a precise guarantee. CopyOnWriteArrayList iterates over a snapshot; ConcurrentHashMap iterators are weakly consistent.\n\n```java\nimport java.util.*;\nimport java.util.concurrent.*;\nclass Demo { public static void main(String[] args) { CopyOnWriteArrayList<Integer> xs = new CopyOnWriteArrayList<>(List.of(1)); for (int x : xs) xs.add(2); System.out.println(xs); } }\n```",tech:"java",displayNumber:146,sortKey:499},{id:"java-500",number:147,title:"How do streams integrate with collections?",question:"How do streams integrate with collections?",difficulty:"Basic",category:"Collections & Generics",answer:'Collections expose stream() and parallelStream() for functional-style processing. Streams do not store data; they process elements from the source through intermediate operations and a terminal operation. Avoid mutating the source while streaming unless the source is concurrent and the operation is designed for it.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> names = List.of("Ana", "Bob"); List<String> upper = names.stream().map(String::toUpperCase).toList(); System.out.println(upper); } }\n```',tech:"java",displayNumber:147,sortKey:500},{id:"java-501",number:148,title:"How do Collectors create collections?",question:"How do Collectors create collections?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collectors turn stream results into lists, sets, maps, groups, or custom collections. toList does not promise a mutable list in modern Java Stream.toList; Collectors.toCollection lets you choose the exact implementation when mutability or ordering matters.\n\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Demo { public static void main(String[] args) { Set<String> set = Stream.of("b","a","b").collect(Collectors.toCollection(LinkedHashSet::new)); System.out.println(set); } }\n```',tech:"java",displayNumber:148,sortKey:501},{id:"java-502",number:149,title:"How do you collect a stream into a Map safely?",question:"How do you collect a stream into a Map safely?",difficulty:"Intermediate",category:"Collections & Generics",answer:'Collectors.toMap needs key mapper, value mapper, and often a merge function for duplicate keys. Without a merge function, duplicate keys throw IllegalStateException. Provide a map supplier when the result order or implementation matters.\n\n```java\nimport java.util.*;\nimport java.util.stream.*;\nclass Demo { public static void main(String[] args) { Map<Character,String> m = Stream.of("ape","ant").collect(Collectors.toMap(s -> s.charAt(0), s -> s, (a,b) -> a + "," + b, LinkedHashMap::new)); System.out.println(m); } }\n```',tech:"java",displayNumber:149,sortKey:502},{id:"java-503",number:150,title:"What immutable collection options exist in modern Java?",question:"What immutable collection options exist in modern Java?",difficulty:"Basic",category:"Collections & Generics",answer:'Modern Java provides List.of, Set.of, Map.of, copyOf methods, and stream terminal toList for unmodifiable results. These collections reject mutation and usually reject nulls. Use them for constants, defensive copies, and API return values that callers should not modify.\n\n```java\nimport java.util.*;\nclass Demo { public static void main(String[] args) { List<String> xs = List.of("a", "b"); Set<String> ys = Set.copyOf(xs); Map<String,Integer> zs = Map.of("size", xs.size()); System.out.println(ys + " " + zs); } }\n```',tech:"java",displayNumber:150,sortKey:503},{id:"java-504",number:151,title:"What is a Thread in Java and how does it differ from a Process?",question:"What is a Thread in Java and how does it differ from a Process?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:`A thread is the smallest unit of execution scheduled by the operating system, living inside a process. A process is an independent program with its own memory space (heap, code, data), whereas threads within the same process share the heap and method area but have private program counters, stacks, and local variables. Threads are lightweight: creating/spawning a thread consumes far fewer resources than a process, and inter-thread communication is cheap (shared memory) but requires synchronization. A JVM runs as a single process with at least one main thread plus background threads (GC, finalizer, etc.).

\`\`\`java
// Threads share the heap but have isolated stacks.
Thread t = Thread.currentThread();
System.out.println("Name: " + t.getName());
System.out.println("Priority: " + t.getPriority());
System.out.println("State: " + t.getState());
System.out.println("Is daemon: " + t.isDaemon());
\`\`\``,tech:"java",displayNumber:151,sortKey:504},{id:"java-505",number:152,title:"What are the different ways to create a Thread in Java?",question:"What are the different ways to create a Thread in Java?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'Two classic approaches: (1) extend `Thread` and override `run()`, then call `start()`; (2) implement `Runnable` and pass it to a `Thread` constructor. The `Runnable` approach is preferred because it decouples the task from the threading mechanism and lets the class extend another class. Java 8+ lets you use lambda expressions for `Runnable`. A third, modern approach is `Callable` (returns a value, can throw checked exceptions) executed via an `ExecutorService`. Always call `start()`, not `run()` — calling `run()` executes synchronously on the current thread.\n\n```java\n// 1. Extending Thread\nclass MyThread extends Thread {\n    public void run() { System.out.println("extends Thread"); }\n}\nnew MyThread().start();\n\n// 2. Implementing Runnable (preferred)\nRunnable r = () -> System.out.println("implements Runnable");\nnew Thread(r).start();\n\n// 3. Callable via ExecutorService (covered later)\n```',tech:"java",displayNumber:152,sortKey:505},{id:"java-506",number:153,title:"What is the difference between calling `start()` and `run()`?",question:"What is the difference between calling `start()` and `run()`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'`start()` registers the thread with the JVM/OS scheduler and causes a new OS thread to be spawned; the scheduler then invokes `run()` asynchronously on that new thread. Calling `run()` directly simply executes the method body on the *current* thread, with no new thread created. So `run()` called directly is plain sequential code, while `start()` is the only way to achieve true concurrency. A thread\'s `start()` can be called only once — a second call throws `IllegalThreadStateException`.\n\n```java\nRunnable r = () -> System.out.println("Running in: " + Thread.currentThread().getName());\nThread t = new Thread(r);\nt.run();   // prints "Running in: main" — no new thread\nt.start(); // prints "Running in: Thread-0" — new thread (throws if called twice)\n```',tech:"java",displayNumber:153,sortKey:506},{id:"java-507",number:154,title:"Explain the life cycle / states of a Thread.",question:"Explain the life cycle / states of a Thread.",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'A Java thread moves through states defined in `Thread.State`: `NEW` (created, not started), `RUNNABLE` (ready to run or running — JVM does not distinguish), `BLOCKED` (waiting for a monitor lock), `WAITING` (waiting indefinitely via `wait()`, `join()`, `park()`), `TIMED_WAITING` (waiting with a timeout via `sleep()`, `wait(timeout)`, `join(timeout)`), and `TERMINATED` (completed). Note that OS "running" vs "ready" is collapsed into `RUNNABLE`; the `BLOCKED` state is specifically for synchronized-lock contention, not for waiting on conditions.\n\n```java\nThread t = new Thread(() -> {});\nSystem.out.println(t.getState()); // NEW\nt.start();\nSystem.out.println(t.getState()); // RUNNABLE (or TERMINATED if already done)\n// BLOCKED: waiting for a synchronized lock\n// WAITING: Object.wait() / Thread.join() / LockSupport.park()\n// TIMED_WAITING: Thread.sleep() / wait(timeout)\n```',tech:"java",displayNumber:154,sortKey:507},{id:"java-508",number:155,title:"What is the difference between `Runnable` and `Callable`?",question:"What is the difference between `Runnable` and `Callable`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:'Both represent tasks, but `Runnable.run()` returns `void` and cannot throw checked exceptions, while `Callable.call()` returns a result of type `V` and may throw checked exceptions. `Callable` is designed for use with `ExecutorService`/`Future`/`CompletableFuture` where you need a result or need to propagate exceptions. `Runnable` is simpler, used when no result is needed (fire-and-forget). To run a `Callable`, you submit it to an `ExecutorService` which wraps it in a `FutureTask`.\n\n```java\nRunnable r = () -> System.out.println("no result");\nCallable<Integer> c = () -> { return 42; };\n\nExecutorService es = Executors.newSingleThreadExecutor();\nFuture<?> f1 = es.submit(r);        // Future<?> — no useful result\nFuture<Integer> f2 = es.submit(c);  // Future<Integer> — f2.get() returns 42\nes.shutdown();\n```',tech:"java",displayNumber:155,sortKey:508},{id:"java-509",number:156,title:"What is the purpose of the `volatile` keyword?",question:"What is the purpose of the `volatile` keyword?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`volatile` guarantees **visibility**: a write to a `volatile` variable happens-before every subsequent read of that variable by any thread, ensuring all threads see the latest value without caching in thread-local registers. It also prevents compiler/JIT reordering of instructions around the volatile access. However, `volatile` does **not** provide atomicity for compound operations (e.g., `volatile int i; i++` is still a race). Use `AtomicInteger` or `synchronized` for atomic compound actions. `volatile` is suitable for simple flags, status indicators, or safely publishing an object reference (if the object is immutable after construction).\n\n```java\nclass Flag { volatile boolean stopped = false; }\nFlag f = new Flag();\nnew Thread(() -> { while (!f.stopped) {} }).start();\nf.stopped = true; // visible to the loop thread immediately\n\n// volatile prevents reordering of this write with the construction of Helper\nclass Helper { int n = 1; }\nclass Publisher { volatile Helper h; }\n```",tech:"java",displayNumber:156,sortKey:509},{id:"java-510",number:157,title:"What is the difference between `synchronized` and `volatile`?",question:"What is the difference between `synchronized` and `volatile`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`synchronized` provides both **mutual exclusion** (only one thread holds the monitor at a time) and **visibility** (a thread exiting a synchronized block flushes writes, and entering one reloads them). `volatile` provides **visibility only**, with no mutual exclusion — multiple threads can read/write concurrently, so compound actions still race. Use `synchronized` when you need to make a *block* of operations atomic; use `volatile` for single-variable status flags where no atomicity is needed. Overuse of `synchronized` hurts throughput; `volatile` is cheaper but limited.\n\n```java\n// volatile: safe for a single read/write flag\nvolatile boolean ready = false;\n\n// synchronized: makes the compound increment atomic + visible\nint count = 0;\nsynchronized void increment() { count++; } // atomic read-modify-write\n```",tech:"java",displayNumber:157,sortKey:510},{id:"java-511",number:158,title:"What is a `synchronized` method and a `synchronized` block?",question:"What is a `synchronized` method and a `synchronized` block?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:`A \`synchronized\` method acquires the monitor (lock) of \`this\` for instance methods, or the Class object for static methods, for the method's duration. A \`synchronized\` block acquires the lock of an explicitly specified object only for the statements inside the block, allowing finer-grained locking and better concurrency. Prefer synchronized blocks with a private, dedicated lock object to avoid exposing the lock (e.g., locking on \`this\` or a public class can be hijacked by external code). The same monitor must be used by all threads that need to be mutually excluded.

\`\`\`java
// synchronized method (locks on 'this')
public synchronized void foo() { /* critical section */ }

// synchronized static method (locks on Class object)
public static synchronized void bar() { /* ... */ }

// synchronized block with dedicated lock (finer-grained, safer)
private final Object lock = new Object();
public void baz() {
    // non-critical work here (concurrent)
    synchronized (lock) {
        // critical section only
    }
}
\`\`\``,tech:"java",displayNumber:158,sortKey:511},{id:"java-512",number:159,title:"What is the difference between a static and an instance `synchronized` method?",question:"What is the difference between a static and an instance `synchronized` method?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"An instance `synchronized` method locks on the specific object instance (`this`), so two threads on *different* instances can run simultaneously. A static `synchronized` method locks on the `Class` object (`MyClass.class`), which is shared across all instances — so it serializes access for all instances of that class. They use different monitors and do **not** block each other: a thread in a static synchronized method and a thread in an instance synchronized method on the same instance can run concurrently. Choose based on whether the shared state being protected is per-instance or class-level.\n\n```java\nclass Counter {\n    private static int global = 0;\n    private int local = 0;\n\n    public static synchronized void incGlobal() { global++; } // locks Counter.class\n    public synchronized void incLocal() { local++; }           // locks 'this'\n}\n// Thread A: Counter.incGlobal()  -> locks Class\n// Thread B: new Counter().incLocal() -> locks instance; runs concurrently with A\n```",tech:"java",displayNumber:159,sortKey:512},{id:"java-513",number:160,title:"What are atomic classes in `java.util.concurrent.atomic`?",question:"What are atomic classes in `java.util.concurrent.atomic`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Atomic classes (`AtomicInteger`, `AtomicLong`, `AtomicReference`, `AtomicBoolean`, and array/field updaters) provide lock-free, thread-safe operations on single variables using Compare-And-Swap (CAS) hardware instructions. They support atomic read-modify-write patterns like `incrementAndGet()`, `compareAndSet(expect, update)`, `getAndUpdate(fn)`, etc. They are faster than `synchronized` under low-to-moderate contention because they avoid kernel context switches, but can suffer under high contention due to CAS retry loops. `LongAdder`/`DoubleAdder` and `LongAccumulator`/`DoubleAccumulator` (Java 8) use striped cells for better high-contention scalability.\n\n```java\nAtomicInteger count = new AtomicInteger(0);\n// thread-safe increment without locks\nint newVal = count.incrementAndGet(); // returns updated value\n// CAS pattern\nboolean updated = count.compareAndSet(5, 10); // true if value was 5, now 10\n\n// For high-contention counters, prefer LongAdder (Java 8)\nimport java.util.concurrent.atomic.LongAdder;\nLongAdder adder = new LongAdder();\nadder.increment(); // no contention on single CAS\nlong sum = adder.sum();\n```",tech:"java",displayNumber:160,sortKey:513},{id:"java-514",number:161,title:"How does `AtomicInteger` work internally?",question:"How does `AtomicInteger` work internally?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`\`AtomicInteger\` wraps a \`volatile int value\` and uses \`Unsafe.compareAndSwapInt\` (CAS) to perform atomic updates. CAS is a hardware instruction: it atomically checks if the current value equals an expected value, and if so, swaps it to the new value; otherwise it fails and the caller retries. The JVM intrinsic compiles this to a single CPU instruction (\`LOCK CMPXCHG\` on x86). Methods like \`incrementAndGet()\` loop: read current, compute next, CAS; on failure, retry. This is lock-free (no thread ever blocks indefinitely waiting for a lock), but not wait-free (a thread may retry many times under contention).

\`\`\`java
// Simplified conceptual implementation
public class AtomicInteger {
    private volatile int value;
    public final int getAndIncrement() {
        for (;;) {
            int current = value;
            int next = current + 1;
            if (compareAndSet(current, next)) return current;
        }
    }
    public final boolean compareAndSet(int expect, int update) {
        return unsafe.compareAndSwapInt(this, valueOffset, expect, update);
    }
}
// Under the hood: JVM intrinsic -> CPU LOCK CMPXCHG
\`\`\``,tech:"java",displayNumber:161,sortKey:514},{id:"java-515",number:162,title:"What is ABA problem and how do you solve it?",question:"What is ABA problem and how do you solve it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"The ABA problem occurs in CAS-based algorithms when a value changes from A to B and back to A; a thread doing CAS(A→C) succeeds because it still sees A, unaware of the intermediate change. This corrupts algorithms that attach meaning to value identity (e.g., lock-free stack pop). Solutions: (1) `AtomicStampedReference` pairs the value with an integer stamp that increments on every update, so A→B→A becomes (A,0)→(B,1)→(A,2) — CAS also checks the stamp. (2) `AtomicMarkableReference` uses a boolean mark instead of a stamp. (3) Avoid mutable references — use immutable value objects so identity change implies value change.\n\n```java\nAtomicStampedReference<Integer> ref = new AtomicStampedReference<>(100, 0);\nint[] stamp = new int[1];\nInteger cur = ref.get(stamp);      // cur=100, stamp[0]=0\n// Another thread: 100 -> 200 -> 100 (stamp becomes 2)\nboolean ok = ref.compareAndSet(cur, 101, stamp[0], stamp[0] + 1);\n// CAS fails because stamp[0]=0 != expected 2, preventing the ABA bug.\n```",tech:"java",displayNumber:162,sortKey:515},{id:"java-516",number:163,title:"What is `ReentrantLock` and how does it differ from `synchronized`?",question:"What is `ReentrantLock` and how does it differ from `synchronized`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ReentrantLock` is an explicit lock from `java.util.concurrent.locks` that can be acquired/released via `lock()`/`unlock()` (always in a `try-finally`). Advantages over `synchronized`: (1) **fairness** option (`new ReentrantLock(true)`) for FIFO ordering; (2) **try-lock with timeout** (`tryLock(timeout)`) to avoid deadlock; (3) **interruptible** locking (`lockInterruptibly()`); (4) `tryLock()` non-blocking attempt; (5) multiple `Condition` objects via `newCondition()` for fine-grained wait/notify. Downsides: you must remember to `unlock()` in `finally` (easy to leak), and it's more verbose. `synchronized` is simpler, auto-released on exceptions/block exit, and JVM-optimized.\n\n```java\nReentrantLock lock = new ReentrantLock(true); // fair\nlock.lock();\ntry {\n    // critical section\n} finally {\n    lock.unlock(); // MUST release, or lock leaks\n}\n\n// Differences from synchronized:\n// - lock.tryLock(1, TimeUnit.SECONDS) // timeout, avoids deadlock\n// - lock.lockInterruptibly()          // can be interrupted\n// - lock.newCondition()               // multiple wait sets\n```",tech:"java",displayNumber:163,sortKey:516},{id:"java-517",number:164,title:'What does "reentrant" mean in `ReentrantLock`?',question:'What does "reentrant" mean in `ReentrantLock`?',difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Reentrant means a thread that already holds the lock can acquire it again without deadlocking on itself; the lock keeps a **hold count** that increments on each re-acquisition and decrements on each `unlock()`. The lock is fully released only when the count returns to zero. This lets synchronized methods call each other and nested `synchronized` blocks on the same monitor work. `synchronized` monitors are also reentrant. If a thread not holding the lock calls `unlock()`, `ReentrantLock` throws `IllegalMonitorStateException`.\n\n```java\nReentrantLock lock = new ReentrantLock();\nlock.lock();\ntry {\n    lock.lock(); // re-acquire: hold count -> 2 (allowed)\n    try {\n        // nested critical section\n    } finally {\n        lock.unlock(); // count -> 1\n    }\n} finally {\n    lock.unlock(); // count -> 0, fully released\n}\n```",tech:"java",displayNumber:164,sortKey:517},{id:"java-518",number:165,title:"What is a `ReadWriteLock`?",question:"What is a `ReadWriteLock`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:`\`ReadWriteLock\` (\`ReentrantReadWriteLock\` is the standard impl) maintains a pair of associated locks: a read lock (shared) and a write lock (exclusive). Multiple threads can hold the read lock simultaneously, but the write lock is exclusive — no reads or other writes occur while a write is held. This boosts concurrency for read-heavy data structures (caches, configuration) where writes are rare. Caveats: by default it is unfair and can starve writers; prefer fairness or \`ReentrantReadWriteLock(true)\` for write-priority if needed. The write lock is reentrant, and it can be downgraded to a read lock but not upgraded.

\`\`\`java
ReadWriteLock rw = new ReentrantReadWriteLock();
Map<String, String> cache = new HashMap<>();

// Read - many threads allowed concurrently
rw.readLock().lock();
try { return cache.get(key); } finally { rw.readLock().unlock(); }

// Write - exclusive
rw.writeLock().lock();
try { cache.put(key, value); } finally { rw.writeLock().unlock(); }
\`\`\``,tech:"java",displayNumber:165,sortKey:518},{id:"java-519",number:166,title:"What is a `Semaphore`?",question:"What is a `Semaphore`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"A `Semaphore` maintains a set of permits. Threads call `acquire()` to take a permit (blocking if none available) and `release()` to return one. It controls access to a resource pool of fixed size, not mutual exclusion per se. A `Semaphore` with 1 permit acts like a mutex (binary semaphore); with N permits it bounds concurrency (e.g., limit to 10 simultaneous connections). Unlike a lock, a semaphore has no notion of ownership — any thread may `release()` a permit even if it didn't `acquire()` it. Constructors support fairness; `tryAcquire()` offers non-blocking/timeout variants.\n\n```java\nSemaphore sem = new Semaphore(3); // max 3 concurrent accesses\nvoid access() throws InterruptedException {\n    sem.acquire();\n    try {\n        // use limited resource\n    } finally {\n        sem.release(); // always release\n    }\n}\n// Binary semaphore (1 permit) ~ mutex\nSemaphore mutex = new Semaphore(1);\n```",tech:"java",displayNumber:166,sortKey:519},{id:"java-520",number:167,title:"What is a `CountDownLatch`?",question:"What is a `CountDownLatch`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`CountDownLatch` is a one-shot synchronizer initialized with a count. Threads call `countDown()` to decrement the counter; other threads call `await()` to block until the count reaches zero (or a timeout elapses). It is **not reusable** — once it hits zero, all subsequent `await()` calls return immediately and it cannot be reset. Typical uses: (1) start gate where N worker threads wait for a signal to begin together; (2) end gate where a master thread waits for N tasks to finish. For a reusable barrier, use `CyclicBarrier`.\n\n```java\nint n = 5;\nCountDownLatch start = new CountDownLatch(1); // start gate\nCountDownLatch done  = new CountDownLatch(n); // end gate\n\nfor (int i = 0; i < n; i++) {\n    new Thread(() -> {\n        try { start.await(); /* wait for go */ runTask(); }\n        finally { done.countDown(); }\n    }).start();\n}\nstart.countDown(); // release all workers\ndone.await();      // wait for all to finish (one-shot, not reusable)\n```",tech:"java",displayNumber:167,sortKey:520},{id:"java-521",number:168,title:"What is a `CyclicBarrier`?",question:"What is a `CyclicBarrier`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CyclicBarrier` is a reusable barrier where a fixed number of threads call `await()` and all block until the last one arrives; then all are released simultaneously, and the barrier resets for the next cycle. It supports an optional barrier action (`Runnable`) executed once per cycle by the last arriving thread — useful for merging partial results. Unlike `CountDownLatch`, it is reusable, but a broken barrier (thread times out or is interrupted) throws `BrokenBarrierException` for all waiting threads, requiring barrier.reset() to reuse. Use for phased computations (map-reduce, game loop steps).\n\n```java\n// 4 threads, barrier action runs after each phase\nCyclicBarrier barrier = new CyclicBarrier(4, () -> System.out.println("All done, next phase"));\nfor (int i = 0; i < 4; i++) {\n    new Thread(() -> {\n        for (int phase = 0; phase < 3; phase++) {\n            doPhaseWork();\n            try { barrier.await(); } // all wait, then proceed\n            catch (InterruptedException | BrokenBarrierException e) { Thread.currentThread().interrupt(); }\n        }\n    }).start();\n}\n```',tech:"java",displayNumber:168,sortKey:521},{id:"java-522",number:169,title:"What is the difference between `CountDownLatch` and `CyclicBarrier`?",question:"What is the difference between `CountDownLatch` and `CyclicBarrier`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Key differences: (1) **Reusability**: `CountDownLatch` is one-shot; `CyclicBarrier` resets automatically after each trip. (2) **Waiting style**: Latch has two roles — threads call `countDown()` (producers) and others call `await()` (consumers). Barrier: all threads call `await()` symmetrically. (3) **Barrier action**: `CyclicBarrier` supports an optional `Runnable` executed per cycle; `CountDownLatch` does not. (4) **Failure modes**: `CyclicBarrier.await()` can throw `BrokenBarrierException` if a thread interrupts or times out, invalidating the barrier for all; `CountDownLatch` never breaks — threads just continue once count hits zero.\n\n```java\n// Use CountDownLatch when: one group signals completion, another waits (fire-once)\n// Use CyclicBarrier when: all threads must meet repeatedly (phased algorithms)\n```",tech:"java",displayNumber:169,sortKey:522},{id:"java-523",number:170,title:"What is an `ExecutorService` and why would you use it instead of raw threads?",question:"What is an `ExecutorService` and why would you use it instead of raw threads?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ExecutorService` is an interface in `java.util.concurrent` that abstracts thread management: you submit `Runnable`/`Callable` tasks and it schedules them on a managed pool of worker threads. Benefits over creating threads manually: (1) thread reuse avoids the high cost of creating/destroying threads per task; (2) bounded pools prevent resource exhaustion; (3) it returns `Future` results for `Callable`; (4) rich lifecycle control (`shutdown()`/`shutdownNow()`); (5) built-in scheduling (`ScheduledExecutorService`). Raw `new Thread()` per task scales poorly and leaks resources under load. Always prefer an executor for production code.\n\n```java\nExecutorService pool = Executors.newFixedThreadPool(10);\nFuture<Integer> f = pool.submit(() -> compute());\nInteger result = f.get(); // blocks for result\npool.shutdown();          // no new tasks, finish submitted ones\npool.awaitTermination(1, TimeUnit.MINUTES);\n```",tech:"java",displayNumber:170,sortKey:523},{id:"java-524",number:171,title:"What are the common factory methods in `Executors`?",question:"What are the common factory methods in `Executors`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`Executors` provides static factory methods for common executor configurations:\n- `newFixedThreadPool(n)` — exactly `n` threads; excess tasks queue unbounded (OOM risk).\n- `newCachedThreadPool()` — creates threads on demand, reuses idle ones (60s keep-alive); unbounded pool size (OOM risk under burst).\n- `newSingleThreadExecutor()` — one thread, FIFO queue; guarantees sequential execution.\n- `newWorkStealingPool()` — `ForkJoinPool` for parallel tasks (uses all processors).\n- `newScheduledThreadPool(n)` — for delayed/periodic tasks.\nPrefer `ThreadPoolExecutor` directly for production to control queue bounds and rejection policies.\n\n```java\nExecutorService fixed = Executors.newFixedThreadPool(4);\nExecutorService cached = Executors.newCachedThreadPool();\nExecutorService single = Executors.newSingleThreadExecutor();\nScheduledExecutorService scheduled = Executors.newScheduledThreadPool(2);\n// WARNING: Fixed & Single use unbounded LinkedBlockingQueue — can OOM.\n// Cached creates unlimited threads — can OOM.\n// In production: use ThreadPoolExecutor with bounded queue.\n```",tech:"java",displayNumber:171,sortKey:524},{id:"java-525",number:172,title:"Explain the `ThreadPoolExecutor` parameters.",question:"Explain the `ThreadPoolExecutor` parameters.",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ThreadPoolExecutor(corePoolSize, maxPoolSize, keepAliveTime, unit, workQueue, threadFactory, handler)`:\n- **corePoolSize**: target number of always-kept-alive threads (even idle).\n- **maxPoolSize**: upper bound on threads when queue is full.\n- **keepAliveTime/unit**: idle timeout for *non-core* threads before termination.\n- **workQueue**: `BlockingQueue` for tasks when all core threads busy (`SynchronousQueue` bypasses queuing; `LinkedBlockingQueue` is unbounded; `ArrayBlockingQueue` is bounded).\n- **threadFactory**: creates threads (naming, daemon, priority).\n- **handler** (RejectedExecutionHandler): what happens when queue is full and pool maxed: `AbortPolicy` (throws, default), `CallerRunsPolicy` (caller runs task), `DiscardPolicy`, `DiscardOldestPolicy`.\nBehavior: tasks use core threads → then queue → then grow to max → then reject.\n\n```java\nThreadPoolExecutor ex = new ThreadPoolExecutor(\n    4,                       // corePoolSize\n    16,                      // maxPoolSize\n    60, TimeUnit.SECONDS,    // keepAlive for non-core\n    new ArrayBlockingQueue<>(100), // bounded queue\n    Executors.defaultThreadFactory(),\n    new ThreadPoolExecutor.CallerRunsPolicy()); // backpressure\n```",tech:"java",displayNumber:172,sortKey:525},{id:"java-526",number:173,title:"What is a `Future`?",question:"What is a `Future`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"A `Future<V>` represents the result of an asynchronous computation. After submitting a `Callable`/`Runnable` to an `ExecutorService`, you get a `Future` to: `get()` (blocks until done, possibly throwing `ExecutionException` wrapping the task's exception), `get(timeout, unit)` (throws `TimeoutException`), `cancel(mayInterruptIfRunning)`, `isDone()`, and `isCancelled()`. The base `Future` is limited: blocking `get()`, no chaining, no combining. `CompletableFuture` (covered later) addresses these gaps.\n\n```java\nExecutorService es = Executors.newSingleThreadExecutor();\nFuture<String> f = es.submit(() -> \"result\");\ntry {\n    String r = f.get(5, TimeUnit.SECONDS); // blocks, with timeout\n} catch (TimeoutException e) {\n    f.cancel(true); // attempt cancellation\n} catch (ExecutionException e) {\n    Throwable cause = e.getCause(); // the task's real exception\n}\n```",tech:"java",displayNumber:173,sortKey:526},{id:"java-527",number:174,title:"What is `CompletableFuture`?",question:"What is `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletableFuture` (Java 8) extends `Future` and implements `CompletionStage`, providing a fluent, non-blocking API for asynchronous programming. You can chain dependent stages (`thenApply`, `thenAccept`, `thenRun`), combine independent futures (`thenCombine`, `thenCompose`, `allOf`, `anyOf`), handle exceptions (`exceptionally`, `handle`), and complete manually (`complete`, `completeExceptionally`). It runs callbacks on the thread that completes the stage (or a supplied executor), and supports both `Runnable` (void) and functional (value) chains. It replaces callback hell and `Future.get()` blocking.\n\n```java\nCompletableFuture<String> f = CompletableFuture.supplyAsync(() -> fetchData())\n    .thenApply(data -> transform(data))\n    .thenAccept(System.out::println)\n    .exceptionally(ex -> { log(ex); return "fallback"; });\n\n// Combine two independent futures\nCompletableFuture<Integer> a = CompletableFuture.supplyAsync(() -> 1);\nCompletableFuture<Integer> b = CompletableFuture.supplyAsync(() -> 2);\nCompletableFuture<Integer> sum = a.thenCombine(b, (x, y) -> x + y);\n// sum.get() -> 3 (non-blocking composition)\n\n// allOf / anyOf for multiple futures\nCompletableFuture.allOf(f1, f2, f3).join();\n```',tech:"java",displayNumber:174,sortKey:527},{id:"java-528",number:175,title:"How does `thenApply`, `thenAccept`, `thenRun` differ?",question:"How does `thenApply`, `thenAccept`, `thenRun` differ?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'All three chain an action after a stage completes, but differ in input/output:\n- `thenApply(Function<T,R>)` — consumes the result, returns a transformed value (maps `T` → `R`).\n- `thenAccept(Consumer<T>)` — consumes the result, returns nothing (`CompletableFuture<Void>`); terminal side-effect.\n- `thenRun(Runnable)` — ignores the result, just runs after completion; for pure side-effects like logging or cleanup.\nThere are also async variants (`thenApplyAsync`, etc.) that run on a supplied or `ForkJoinPool.commonPool()` executor rather than the completing thread.\n\n```java\nCompletableFuture.supplyAsync(() -> "abc")\n    .thenApply(s -> s.toUpperCase())  // Function: "ABC"\n    .thenAccept(s -> System.out.println(s)) // Consumer: prints ABC\n    .thenRun(() -> System.out.println("done")); // Runnable: prints done\n```',tech:"java",displayNumber:175,sortKey:528},{id:"java-529",number:176,title:"What is the difference between `thenCompose` and `thenCombine`?",question:"What is the difference between `thenCompose` and `thenCombine`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`thenCompose(Function<T, CompletionStage<U>>)` flattens a dependent future — the result of the first stage is fed into a function that *returns another future*, yielding a single flat future (`Future<U>`). It's like `flatMap` for futures (sequential dependency: B needs A's result).\n`thenCombine(CompletionStage<U>, BiFunction<T,U,V>)` joins two *independent* futures and combines their results when both complete; the two futures run in parallel and combine (`Future<V>`). It's like `zip`/`map2`.\nUse `thenCompose` for chaining dependent steps; `thenCombine`/`thenAcceptBoth` for merging concurrent results.\n\n```java\n// thenCompose: sequential dependency (flatMap)\nCompletableFuture<User> f = fetchUserId()\n    .thenCompose(id -> fetchUser(id));   // Future<Id> -> Future<User>\n\n// thenCombine: parallel, merge results\nCompletableFuture<Double> price = fetchPrice();\nCompletableFuture<Integer> qty = fetchQty();\nCompletableFuture<Double> total = price.thenCombine(qty, (p, q) -> p * q);\n```",tech:"java",displayNumber:176,sortKey:529},{id:"java-530",number:177,title:"What is deadlock and how do you prevent it?",question:"What is deadlock and how do you prevent it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Deadlock occurs when two or more threads are blocked forever, each waiting for a lock held by another. Four Coffman conditions must hold simultaneously: mutual exclusion, hold-and-wait, no preemption, circular wait. Prevention strategies: (1) **Lock ordering** — always acquire locks in a consistent global order; (2) **Timeout-based locks** — use `tryLock(timeout)` to avoid indefinite wait; (3) **Avoid nested locks** — design to hold at most one lock; (4) **Lock coarsening/granularity** — sometimes fewer, broader locks reduce nesting; (5) **Detect & recover** — JVM has no built-in deadlock detection, but `jstack` / `ThreadMXBean` can diagnose; design for `LockSupport.parkNanos` instead of indefinite blocking.\n\n```java\n// Deadlock example\nObject a = new Object(), b = new Object();\nThread t1 = new Thread(() -> { synchronized(a) { synchronized(b) {} } });\nThread t2 = new Thread(() -> { synchronized(b) { synchronized(a) {} } });\n// t1 holds a, wants b; t2 holds b, wants a -> deadlock\n\n// Prevention: consistent lock ordering\n// Always lock a before b (or compare hash codes for arbitrary objects)\nsynchronized (a) { synchronized (b) { } }\n// Both threads use same order -> no circular wait\n```",tech:"java",displayNumber:177,sortKey:530},{id:"java-531",number:178,title:"What is the difference between deadlock, livelock, and starvation?",question:"What is the difference between deadlock, livelock, and starvation?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:`- **Deadlock**: threads are permanently blocked waiting on each other's locks; none make progress and none are "active" (they're parked/blocked).
- **Livelock**: threads are *active* and keep responding to each other but make no real progress — like two people stepping aside in the same direction repeatedly. Often from over-eager retry/back-off logic where each thread gives up its lock to avoid deadlock and immediately retries.
- **Starvation**: a thread is perpetually denied access to a resource because higher-priority or luckier threads always win (e.g., unfair lock, \`synchronized\` favoring some threads). The thread is runnable but never scheduled. Fair locks and \`ReentrantLock(true)\` mitigate starvation.

\`\`\`java
// Livelock: both yield endlessly
while (conflict) { /* give up resource */ Thread.yield(); } // neither progresses

// Starvation example: many high-priority writers starve a low-priority reader
// under an unfair lock. Fix: ReentrantLock(true) or priority adjustment.
\`\`\``,tech:"java",displayNumber:178,sortKey:531},{id:"java-532",number:179,title:"What is `wait()`, `notify()`, and `notifyAll()` and how are they used?",question:"What is `wait()`, `notify()`, and `notifyAll()` and how are they used?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"These are `Object` methods for inter-thread coordination and **must be called from within a `synchronized` block or method on the same monitor**. `wait()` releases the monitor and blocks the thread until another thread calls `notify()`/`notifyAll()` on the same object. `notify()` wakes one (arbitrary) waiting thread; `notifyAll()` wakes all. Always call `wait()` in a **loop** checking the condition (guards against spurious wakeups and missed signals). Prefer `notifyAll()` unless you have a single specific waiter. Modern code usually prefers `Condition`, `BlockingQueue`, or higher-level concurrent collections instead.\n\n```java\nfinal Object lock = new Object();\nboolean ready = false;\n\n// Consumer\nsynchronized (lock) {\n    while (!ready) lock.wait(); // ALWAYS loop, not if\n    // proceed\n}\n\n// Producer\nsynchronized (lock) {\n    ready = true;\n    lock.notifyAll(); // wake all waiters\n}\n```",tech:"java",displayNumber:179,sortKey:532},{id:"java-533",number:180,title:"What is a `Condition` and how does it differ from `wait/notify`?",question:"What is a `Condition` and how does it differ from `wait/notify`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`Condition` (from `Lock.newCondition()`) is the `Lock` API\'s analog of `Object.wait/notify`. Differences: (1) a `Lock` can have multiple `Condition` objects (separate wait-sets) for different predicates — e.g., `notEmpty` and `notFull` for a bounded buffer — instead of one shared monitor wait-set; (2) `Condition` methods are `await()`, `signal()`, `signalAll()` (clearer names); (3) `await()` throws `InterruptedException` which you must handle; (4) `Condition` requires explicit `Lock` ownership, making locking discipline visible. Use `Condition` when you need multiple wait queues per lock.\n\n```java\nReentrantLock lock = new ReentrantLock();\nCondition notEmpty = lock.newCondition();\nCondition notFull  = lock.newCondition();\n\nvoid put(E e) {\n    lock.lock();\n    try {\n        while (full) notFull.await();\n        add(e);\n        notEmpty.signal();\n    } finally { lock.unlock(); }\n}\n// wait-set per condition avoids "signal to wrong thread" issue with notify()\n```',tech:"java",displayNumber:180,sortKey:533},{id:"java-534",number:181,title:"What is the `ForkJoinPool` and `ForkJoinTask`?",question:"What is the `ForkJoinPool` and `ForkJoinTask`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ForkJoinPool` (Java 7) is an `ExecutorService` specialized for divide-and-conquer tasks that **fork** (split into subtasks) and **join** (wait for results). Its worker threads use **work-stealing**: each worker has its own deque; idle workers steal tasks from busy workers' deques, keeping CPUs saturated and reducing contention. Tasks extend `RecursiveTask<V>` (returns result) or `RecursiveAction` (void) and override `compute()`, recursively splitting when the problem is large (`threshold`) and solving directly when small. `ForkJoinPool.commonPool()` is the shared default used by parallel streams and `CompletableFuture` async without explicit executor.\n\n```java\nclass SumTask extends RecursiveTask<Long> {\n    final long[] a; final int lo, hi;\n    SumTask(long[] a, int lo, int hi) { this.a=a; this.lo=lo; this.hi=hi; }\n    protected Long compute() {\n        if (hi - lo <= 1000) { // threshold\n            long s = 0; for (int i=lo; i<hi; i++) s += a[i]; return s;\n        }\n        int mid = (lo+hi)/2;\n        SumTask left = new SumTask(a, lo, mid);\n        SumTask right = new SumTask(a, mid, hi);\n        left.fork();               // async execute\n        long r = right.compute();  // compute current thread\n        long l = left.join();      // wait for forked\n        return l + r;\n    }\n}\nLong total = new ForkJoinPool().invoke(new SumTask(arr, 0, arr.length));\n```",tech:"java",displayNumber:181,sortKey:534},{id:"java-535",number:182,title:"What is work-stealing in `ForkJoinPool`?",question:"What is work-stealing in `ForkJoinPool`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Work-stealing is the scheduling algorithm of `ForkJoinPool`: each worker thread maintains a double-ended queue (deque) of tasks. A worker pushes new forked subtasks to the *top* of its own deque and pops from the top (LIFO, cache-friendly). When a worker runs out of work, it *steals* a task from the *bottom* of another worker's deque (FIFO). This minimizes contention (owners use one end, thieves the other) and balances load automatically. It's why `ForkJoinPool` scales well for many small tasks, and is the backbone of parallel streams. Caveat: tasks must be independent (no shared mutable state) and ideally of balanced size for good stealing.\n\n```java\n// Owner pushes/pops from TOP (LIFO); thieves steal from BOTTOM (FIFO).\n// Idle worker scans other workers' deques to \"steal\" work.\nForkJoinPool pool = ForkJoinPool.commonPool();\n// parallel stream uses commonPool (work-stealing) under the hood\nList<Integer> result = list.parallelStream().map(x -> x*2).toList();\n```",tech:"java",displayNumber:182,sortKey:535},{id:"java-536",number:183,title:"What is `ThreadLocal` and when would you use it?",question:"What is `ThreadLocal` and when would you use it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`ThreadLocal<T>` provides thread-confined variables: each thread accessing the `ThreadLocal` gets its own independent copy, so there's no sharing and thus no synchronization needed. Typical uses: per-thread state like user session, transaction ID, `SimpleDateFormat` (not thread-safe), database connection/source binding (Spring's `TransactionSynchronizationManager`), and profiling/tracing context. Caution: in thread pools, a `ThreadLocal` set on a worker thread persists across tasks and can leak memory or leak data between unrelated tasks — always `remove()` in a `finally` block. `InheritableThreadLocal` propagates values from parent to child threads (but not across executor thread handoffs).\n\n```java\nprivate static final ThreadLocal<SimpleDateFormat> FMT =\n    ThreadLocal.withInitial(() -> new SimpleDateFormat(\"yyyy-MM-dd\"));\n\nString format(Date d) {\n    return FMT.get().format(d); // each thread has its own formatter\n}\n// ALWAYS clean up in thread pools:\ntry {\n    context.set(value);\n    doWork();\n} finally {\n    context.remove(); // prevent leak / cross-task data bleed\n}\n```",tech:"java",displayNumber:183,sortKey:536},{id:"java-537",number:184,title:"What is `InheritableThreadLocal`?",question:"What is `InheritableThreadLocal`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'`InheritableThreadLocal` extends `ThreadLocal` so that when a new thread is created from a parent thread, the child receives a *copy* of the parent\'s values (via `childValue(parentValue)` override). This works for plain `new Thread()` but **does not propagate across thread pool hand-offs** — if a task is submitted to an `ExecutorService`, the worker thread is not a child of the submitting thread. For thread-pool-safe context propagation, use `TransmittableThreadLocal` (third-party) or Java 21\'s `ScopedValue` (preview).\n\n```java\nInheritableThreadLocal<String> traceId = new InheritableThreadLocal<>() {\n    @Override protected String childValue(String parent) { return parent + "-child"; }\n};\ntraceId.set("req-1");\nnew Thread(() -> System.out.println(traceId.get())).start(); // prints "req-1-child"\n// But: Executors.newSingleThreadExecutor().submit(() -> traceId.get()) -> null!\n```',tech:"java",displayNumber:184,sortKey:537},{id:"java-538",number:185,title:"What are Virtual Threads in Java 21?",question:"What are Virtual Threads in Java 21?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`Virtual threads (Project Loom, JEP 444, Java 21) are lightweight, JVM-managed threads that are not 1:1 mapped to OS threads. A virtual thread is cheap to create (millions can exist) because its stack is stored on the Java heap and is *unmounted* from its carrier (platform) thread when it blocks on I/O, allowing the carrier to run other virtual threads. This makes the "one thread per request" model scale to huge concurrency without the OS-thread bottleneck. Create via \`Thread.ofVirtual().start(runnable)\` or \`Executors.newVirtualThreadPerTaskExecutor()\`. They are not a replacement for CPU-bound work — they shine for I/O-bound, blocking-style code.

\`\`\`java
// Java 21
try (var exec = Executors.newVirtualThreadPerTaskExecutor()) {
    for (int i = 0; i < 1_000_000; i++) {
        exec.submit(() -> {
            Thread.sleep(Duration.ofSeconds(1)); // cheap: unmounts carrier
            return "ok";
        });
    }
}
// Thread.ofVirtual().name("vt-", 0).start(() -> doWork());
\`\`\``,tech:"java",displayNumber:185,sortKey:538},{id:"java-539",number:186,title:"How do virtual threads differ from platform threads?",question:"How do virtual threads differ from platform threads?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'Platform threads (the traditional `Thread`) are 1:1 with OS threads: each has a large native stack (typically 1–2 MB), limited by OS/kernel resources (a few thousand typically). Virtual threads are many-to-one: many virtual threads share a small pool of carrier (platform) threads. Virtual threads have tiny, growable Java-heap stacks and can be created in the millions. When a virtual thread blocks on I/O (socket, file, `LockSupport.park`), it unmounts from its carrier, freeing it to run another virtual thread. CPU-bound work still occupies a carrier, so virtual threads do not magically parallelize CPU-heavy tasks. Pinning (holding a `synchronized` monitor or native frame) prevents unmounting and can stall the carrier.\n\n```java\n// Platform: expensive, limited\nThread.ofPlatform().name("p-", 0).start(() -> {});\n// Virtual: cheap, many\nThread.ofVirtual().name("v-", 0).start(() -> {});\n// Mixed: virtual threads run ON platform carrier threads internally\n```',tech:"java",displayNumber:186,sortKey:539},{id:"java-540",number:187,title:"What is pinning in virtual threads and why does it matter?",question:"What is pinning in virtual threads and why does it matter?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"A virtual thread is *pinned* to its carrier when it cannot unmount — typically because it holds a `synchronized` monitor or is executing a native (JNI) method. While pinned, blocking I/O keeps the carrier occupied, reducing the pool of available carriers and potentially starving other virtual threads. Java 21's `synchronized` pins; Java 24+ (JEP 491) unpins `synchronized`. Workarounds: (1) replace `synchronized` with `ReentrantLock` (does not pin); (2) keep synchronized blocks short and CPU-only; (3) avoid blocking I/O inside `synchronized`. JDK Flight Recorder (`jdk.VirtualThreadPinned` event) can detect pinning.\n\n```java\n// BAD: synchronized + blocking I/O = pinned virtual thread\nsynchronized (this) {\n    socket.read(); // carrier cannot unmount\n}\n\n// GOOD: ReentrantLock does not pin\nlock.lock();\ntry { socket.read(); } finally { lock.unlock(); }\n```",tech:"java",displayNumber:187,sortKey:540},{id:"java-541",number:188,title:"What is `happens-before` in the Java Memory Model?",question:"What is `happens-before` in the Java Memory Model?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"The Java Memory Model (JMM, JLS 17) defines *happens-before* as a partial order guaranteeing visibility: if action A happens-before action B, then B is guaranteed to see the effects of A. Key happens-before rules: (1) program order in a single thread; (2) unlock of a monitor happens-before subsequent lock of the same monitor; (3) write to `volatile` happens-before subsequent read of that `volatile`; (4) `Thread.start()` happens-before any action in the started thread; (5) last action in a thread happens-before `join()` returns in another; (6) transitivity. Without a happens-before edge, the compiler/CPU may reorder, and a thread may see stale values.\n\n```java\nint x = 0; volatile boolean ready = false;\n// Thread A:\nx = 42;\nready = true; // volatile write happens-before subsequent volatile read\n// Thread B:\nif (ready) { // volatile read\n    System.out.println(x); // guaranteed to see 42\n}\n```",tech:"java",displayNumber:188,sortKey:541},{id:"java-542",number:189,title:"What is a race condition vs data race?",question:"What is a race condition vs data race?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"A **data race** is a JMM term: two threads access the same memory location concurrently, at least one is a write, and there is no happens-before relationship. Data races cause stale or inconsistent visibility. A **race condition** is a higher-level logic bug: correctness depends on the relative timing of events, even if individual operations are thread-safe (for example, a check-then-act sequence using separate `containsKey` and `put` calls). You can have a race condition without a data race (TOCTOU). Fix data races with `volatile`/`synchronized`/atomics; fix race conditions with atomic compound operations (`putIfAbsent`, `computeIfAbsent`, `AtomicInteger.incrementAndGet`) or by locking the entire compound sequence.\n\n```java\n// Data race: unsynchronized write + read of shared field\nint x; // Thread A writes, Thread B reads — no happens-before\n\n// Race condition: check-then-act with individually thread-safe operations\nif (!map.containsKey(k)) map.put(k, v); // another thread may insert between calls\n// Fix: map.putIfAbsent(k, v) — atomic compound\n// Or synchronize the whole check-and-put block on one shared lock\n```",tech:"java",displayNumber:189,sortKey:542},{id:"java-543",number:190,title:"What is `Thread.sleep()` vs `Object.wait()` vs `LockSupport.park()`?",question:"What is `Thread.sleep()` vs `Object.wait()` vs `LockSupport.park()`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"`Thread.sleep(ms)` pauses the *current* thread for a duration; it does **not** release any locks, cannot be woken by `notify()`, and is interruptible (`InterruptedException`). `Object.wait()` must be called while holding the object's monitor; it **releases** the monitor, waits until notified or timeout, then re-acquires the monitor. `LockSupport.park()` is a low-level primitive used by `Lock`/`Condition`/`Future`; it parks the thread until `unpark(thread)` or interrupt, does not require a monitor, and is not tied to a timeout unless `parkNanos`. Use `sleep` for delays, `wait`/`notify` for monitor-based coordination, `park` for building synchronizers.\n\n```java\nThread.sleep(1000);          // does not release locks; interruptible\nsynchronized (obj) { obj.wait(); } // releases monitor, waits for notify\nLockSupport.park();          // parks until unpark or interrupt\nLockSupport.unpark(otherThread);\n```",tech:"java",displayNumber:190,sortKey:543},{id:"java-544",number:191,title:"How does interruption work in Java?",question:"How does interruption work in Java?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Interruption is cooperative: `Thread.interrupt()` sets the interrupt status flag. Blocking methods (`sleep`, `wait`, `join`, `BlockingQueue.take`, `Lock.lockInterruptibly`) throw `InterruptedException` and *clear* the flag. Non-blocking code must poll `Thread.interrupted()` (clears flag) or `isInterrupted()` (does not). Best practice: catch `InterruptedException`, restore the interrupt status (`Thread.currentThread().interrupt()`), and exit — never swallow it. Cancellation of `Future` with `cancel(true)` interrupts the worker. Virtual threads honor interruption the same way.\n\n```java\nvoid work() {\n    try {\n        while (!Thread.currentThread().isInterrupted()) {\n            BlockingQueue.take(); // throws InterruptedException if interrupted\n        }\n    } catch (InterruptedException e) {\n        Thread.currentThread().interrupt(); // restore flag\n        return; // exit cooperatively\n    }\n}\n```",tech:"java",displayNumber:191,sortKey:544},{id:"java-545",number:192,title:"What is `join()` and how does it work?",question:"What is `join()` and how does it work?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`t.join()` blocks the current thread until thread `t` terminates (or a timeout elapses). Internally it waits on `t` as a monitor (`wait()`) until `t` finishes and the JVM `notifyAll`s. `join()` is interruptible. Overloads: `join()`, `join(millis)`, `join(millis, nanos)`. Use it to wait for spawned threads to complete before proceeding — but prefer `ExecutorService.invokeAll`/`CountDownLatch`/`CompletableFuture.allOf` for production. Calling `join()` on a thread that hasn't started or has already terminated returns immediately.\n\n```java\nThread t = new Thread(() -> doWork());\nt.start();\nt.join(); // current thread waits until t terminates\nt.join(1000); // wait at most 1 second\n```",tech:"java",displayNumber:192,sortKey:545},{id:"java-546",number:193,title:"What are daemon vs user threads?",question:"What are daemon vs user threads?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"User (non-daemon) threads keep the JVM alive; the JVM exits only after all user threads have terminated. Daemon threads are background workers (GC, finalizer, `Timer` if `setDaemon(true)`) that the JVM will kill when no user threads remain — they do not prevent JVM shutdown. Set daemon status with `t.setDaemon(true)` *before* `start()`; after start it throws `IllegalThreadStateException`. A thread inherits the daemon status of its creator. Executor threads are typically user threads; if you want a pool that doesn't keep the JVM alive, wrap the `ThreadFactory` to set daemon.\n\n```java\nThread t = new Thread(() -> { while (true) { /* background */ } });\nt.setDaemon(true); // MUST before start\nt.start();\n// JVM can exit even if t is still looping\n```",tech:"java",displayNumber:193,sortKey:546},{id:"java-547",number:194,title:"What is `Phaser` and when would you use it?",question:"What is `Phaser` and when would you use it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`Phaser` is a more flexible, reusable barrier that supports a **dynamic number of parties** (register/deregister at runtime) and multiple phases. Threads call `arriveAndAwaitAdvance()` to wait for the current phase to complete, then all advance together. Unlike `CyclicBarrier`, parties can be added (`register()`) or removed (`arriveAndDeregister()`) mid-run, making it suitable for tree-structured or dynamically sized parallel algorithms. `onAdvance(phase, registeredParties)` can terminate the phaser when it returns `true`. Use Phaser when party count is not known up front or changes over time.\n\n```java\nPhaser phaser = new Phaser(1); // register self\nfor (int i = 0; i < n; i++) {\n    phaser.register();\n    new Thread(() -> {\n        doWork();\n        phaser.arriveAndDeregister();\n    }).start();\n}\nphaser.arriveAndAwaitAdvance(); // wait for all workers\n```",tech:"java",displayNumber:194,sortKey:547},{id:"java-548",number:195,title:"What is `Exchanger`?",question:"What is `Exchanger`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`Exchanger<V>` is a two-party synchronizer that lets two threads swap objects at a rendezvous point. Thread A calls `exchange(a)` and blocks until thread B calls `exchange(b)`; then A receives `b` and B receives `a`. Typical use: producer/consumer pipelines with double-buffering — producer fills a buffer, exchanges it with consumer's empty buffer, and they swap roles without extra copying. Timeout and interruptible variants exist. Only two parties — for N-way, use `CyclicBarrier` or a queue.\n\n```java\nExchanger<List<Integer>> ex = new Exchanger<>();\n// Producer\nList<Integer> buf = new ArrayList<>();\nwhile (true) {\n    fill(buf);\n    buf = ex.exchange(buf); // swap full for empty\n}\n// Consumer\nList<Integer> empty = new ArrayList<>();\nwhile (true) {\n    empty = ex.exchange(empty); // swap empty for full\n    drain(empty);\n}\n```",tech:"java",displayNumber:195,sortKey:548},{id:"java-549",number:196,title:"What are concurrent collections (`ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`)?",question:"What are concurrent collections (`ConcurrentHashMap`, `CopyOnWriteArrayList`, `BlockingQueue`)?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`java.util.concurrent` collections are thread-safe without wrapping in `Collections.synchronizedXxx`:\n- **`ConcurrentHashMap`**: lock-striping (Java 7) / CAS + synchronized bins (Java 8+); allows concurrent reads and a bounded number of concurrent writes. Iterators are weakly consistent (no `ConcurrentModificationException`).\n- **`CopyOnWriteArrayList`/`CopyOnWriteArraySet`**: every mutation copies the entire array; reads are lock-free. Great for rare writes, frequent reads (listeners).\n- **`BlockingQueue`** (`ArrayBlockingQueue`, `LinkedBlockingQueue`, `SynchronousQueue`, `PriorityBlockingQueue`, `DelayQueue`): producer-consumer queues with blocking `put`/`take`.\nNever wrap a concurrent collection in extra `synchronized` unless you need a compound atomic operation not provided by the API.\n\n```java\nConcurrentHashMap<String, Integer> map = new ConcurrentHashMap<>();\nmap.putIfAbsent("a", 1);\nmap.compute("a", (k, v) -> v == null ? 1 : v + 1); // atomic compound\nmap.merge("a", 1, Integer::sum);\n\nCopyOnWriteArrayList<Listener> listeners = new CopyOnWriteArrayList<>();\nlisteners.forEach(Listener::onEvent); // snapshot, no CME\n\nBlockingQueue<Task> q = new ArrayBlockingQueue<>(100);\nq.put(task);   // blocks if full\nTask t = q.take(); // blocks if empty\n```',tech:"java",displayNumber:196,sortKey:549},{id:"java-550",number:197,title:"How does `ConcurrentHashMap` work internally (Java 8+)?",question:"How does `ConcurrentHashMap` work internally (Java 8+)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Java 8+ `ConcurrentHashMap` uses a table of bins. Empty bins are filled with CAS. Colliding keys form a linked list; if a bin grows past `TREEIFY_THRESHOLD` (8) and table is large enough, it treeifies into a red-black tree (`TreeBin`) for O(log n) lookup. Writes lock the *bin* (`synchronized` on the first node) rather than the whole map. Size is computed via a `CounterCell` array (similar to `LongAdder`) to avoid a global lock. Iterators are weakly consistent — they may or may not see concurrent updates, but they never throw `ConcurrentModificationException` and never return the same key twice.\n\n```java\n// Java 8+ ConcurrentHashMap\n// - CAS for empty bins\n// - synchronized(bin-head) for colliding bins\n// - treeify at 8, untreeify at 6\n// - size via striped CounterCells (LongAdder-like)\nConcurrentHashMap<K,V> m = new ConcurrentHashMap<>();\nm.computeIfAbsent(k, this::load); // lock-free-ish compound\n```",tech:"java",displayNumber:197,sortKey:550},{id:"java-551",number:198,title:"What is the difference between `HashMap`, `Hashtable`, `Collections.synchronizedMap`, and `ConcurrentHashMap`?",question:"What is the difference between `HashMap`, `Hashtable`, `Collections.synchronizedMap`, and `ConcurrentHashMap`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"- **`HashMap`**: not thread-safe; concurrent mutation can cause infinite loops (pre-Java 8) or lost updates. Fine for single-thread.\n- **`Hashtable`**: fully synchronized (legacy); every method locks the entire table — correct but slow; `null` keys/values forbidden.\n- **`Collections.synchronizedMap(map)`**: wraps any map with a mutex on every method; compound operations (`if (!contains) put`) still race unless you lock the wrapper yourself.\n- **`ConcurrentHashMap`**: designed for concurrency — finer-grained locking, compound atomic methods (`putIfAbsent`, `compute`), no `null`s, weakly consistent iterators. Default choice for concurrent maps.\n\n```java\n// Hashtable / synchronizedMap: whole-map lock, compound ops still racy\nMap<K,V> sync = Collections.synchronizedMap(new HashMap<>());\nsynchronized (sync) { if (!sync.containsKey(k)) sync.put(k, v); } // extra lock needed\n\n// ConcurrentHashMap: built-in atomic compounds\nConcurrentHashMap<K,V> chm = new ConcurrentHashMap<>();\nchm.putIfAbsent(k, v);\n```",tech:"java",displayNumber:198,sortKey:551},{id:"java-552",number:199,title:"What is `StampedLock`?",question:"What is `StampedLock`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`StampedLock` (Java 8) is an optimistic read-write lock that returns a `long` stamp on acquire. Modes: (1) **write lock** (exclusive); (2) **read lock** (pessimistic shared); (3) **optimistic read** (`tryOptimisticRead`) — no lock, just a stamp; after reading, `validate(stamp)` checks if a write occurred in between. If invalid, fall back to a pessimistic read. Optimistic reads are extremely cheap for read-heavy, write-rare data. Caveats: not reentrant, no `Condition`, deadlock-prone if you mix modes incorrectly, and a thread holding a read lock cannot upgrade without deadlock (must convert via `tryConvertToWriteLock`).\n\n```java\nStampedLock sl = new StampedLock();\ndouble x, y;\ndouble distance() {\n    long stamp = sl.tryOptimisticRead();\n    double cx = x, cy = y;\n    if (!sl.validate(stamp)) { // write happened — fall back\n        stamp = sl.readLock();\n        try { cx = x; cy = y; } finally { sl.unlockRead(stamp); }\n    }\n    return Math.hypot(cx, cy);\n}\n```",tech:"java",displayNumber:199,sortKey:552},{id:"java-553",number:200,title:"What is `ThreadFactory` and why customize it?",question:"What is `ThreadFactory` and why customize it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`ThreadFactory` is an interface (`newThread(Runnable)`) used by executors to create worker threads. The default factory produces unnamed, non-daemon, normal-priority threads (`pool-N-thread-M`). Custom factories let you: (1) set meaningful names for debugging (`jstack`/`jconsole`); (2) set daemon status; (3) set priority; (4) set `UncaughtExceptionHandler`; (5) wrap `Runnable` to restore `ThreadLocal`s. Always name threads in production — unnamed threads make dumps unreadable.\n\n```java\nThreadFactory named = r -> {\n    Thread t = new Thread(r);\n    t.setName("worker-" + t.getId());\n    t.setUncaughtExceptionHandler((th, ex) -> log.error(th.getName(), ex));\n    t.setDaemon(false);\n    return t;\n};\nExecutorService es = Executors.newFixedThreadPool(8, named);\n```',tech:"java",displayNumber:200,sortKey:553},{id:"java-554",number:201,title:"What is `RejectedExecutionHandler`?",question:"What is `RejectedExecutionHandler`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"When a `ThreadPoolExecutor` cannot accept a task (queue full and pool at `maxPoolSize`, or after `shutdown()`), it invokes the `RejectedExecutionHandler`. Built-in policies:\n- **`AbortPolicy`** (default): throws `RejectedExecutionException`.\n- **`CallerRunsPolicy`**: the *calling* thread runs the task — natural backpressure.\n- **`DiscardPolicy`**: silently drop the task.\n- **`DiscardOldestPolicy`**: drop the oldest queued task and retry.\nChoose based on whether losing work is acceptable. `CallerRunsPolicy` is a good default for bounded work that must not be lost.\n\n```java\nnew ThreadPoolExecutor(4, 8, 60, TimeUnit.SECONDS,\n    new ArrayBlockingQueue<>(100),\n    new ThreadPoolExecutor.CallerRunsPolicy()); // backpressure: caller runs\n```",tech:"java",displayNumber:201,sortKey:554},{id:"java-555",number:202,title:"How do you shut down an `ExecutorService` correctly?",question:"How do you shut down an `ExecutorService` correctly?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:"Never just abandon a pool — threads will keep the JVM alive. Pattern: (1) `shutdown()` — stop accepting new tasks, finish queued ones; (2) `awaitTermination(timeout)` — wait for completion; (3) if timeout, `shutdownNow()` — interrupt running tasks and return unexecuted ones; (4) `awaitTermination` again. Java 19+ `ExecutorService` is `AutoCloseable` — `try-with-resources` calls `close()` which does this dance. Always restore interrupt status if `awaitTermination` is interrupted.\n\n```java\nExecutorService es = Executors.newFixedThreadPool(4);\ntry {\n    // submit work\n} finally {\n    es.shutdown();\n    try {\n        if (!es.awaitTermination(30, TimeUnit.SECONDS)) {\n            es.shutdownNow();\n            es.awaitTermination(10, TimeUnit.SECONDS);\n        }\n    } catch (InterruptedException e) {\n        es.shutdownNow();\n        Thread.currentThread().interrupt();\n    }\n}\n// Java 19+: try (var es = Executors.newVirtualThreadPerTaskExecutor()) { ... }\n```",tech:"java",displayNumber:202,sortKey:555},{id:"java-556",number:203,title:"What is `ScheduledExecutorService`?",question:"What is `ScheduledExecutorService`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`ScheduledExecutorService` (`Executors.newScheduledThreadPool(n)`) schedules tasks with delays or periodically. Methods: `schedule(task, delay, unit)` (one-shot delayed), `scheduleAtFixedRate` (period measured from *start* of previous run — can overlap if a run exceeds the period), `scheduleWithFixedDelay` (period measured from *end* of previous run — never overlaps). Prefer `ScheduledThreadPoolExecutor` over `java.util.Timer` (Timer is single-threaded and a thrown exception kills it). Catch exceptions inside the task — an uncaught exception silently cancels subsequent periodic runs.\n\n```java\nScheduledExecutorService sch = Executors.newScheduledThreadPool(2);\nsch.schedule(() -> log("once"), 5, TimeUnit.SECONDS);\nsch.scheduleAtFixedRate(this::tick, 0, 1, TimeUnit.SECONDS); // every 1s from start\nsch.scheduleWithFixedDelay(this::tick, 0, 1, TimeUnit.SECONDS); // 1s after previous ends\n```',tech:"java",displayNumber:203,sortKey:556},{id:"java-557",number:204,title:"What is `invokeAll` vs `invokeAny`?",question:"What is `invokeAll` vs `invokeAny`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Both take a collection of `Callable`s. `invokeAll` submits all, waits for *all* to complete (or timeout), and returns a list of `Future`s in the same order — some may have failed (`ExecutionException` on `get()`). `invokeAny` returns the result of the *first successful* task and cancels the rest; if all fail, it throws `ExecutionException`. Use `invokeAll` for fan-out-and-gather; `invokeAny` for racing redundant sources (first successful DNS lookup, first healthy replica).\n\n```java\nList<Callable<String>> tasks = List.of(() -> "a", () -> "b");\nList<Future<String>> all = es.invokeAll(tasks); // wait for both\nString first = es.invokeAny(tasks);             // first success, cancel rest\n```',tech:"java",displayNumber:204,sortKey:557},{id:"java-558",number:205,title:"What is `CompletionService`?",question:"What is `CompletionService`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`ExecutorCompletionService` wraps an `Executor` and a `BlockingQueue` of completed `Future`s. As each submitted task finishes, its `Future` is placed on the completion queue, so you can `take()` results in *completion order* rather than submission order. Useful when you want to process results as they arrive (don't wait for the slowest). Contrast with `invokeAll`, which returns in submission order after all complete.\n\n```java\nCompletionService<String> cs = new ExecutorCompletionService<>(es);\nint n = 10;\nfor (int i = 0; i < n; i++) cs.submit(() -> fetch(i));\nfor (int i = 0; i < n; i++) {\n    String r = cs.take().get(); // next completed, not next submitted\n    process(r);\n}\n```",tech:"java",displayNumber:205,sortKey:558},{id:"java-559",number:206,title:"What is double-checked locking and is it safe in Java?",question:"What is double-checked locking and is it safe in Java?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`Double-checked locking (DCL) is a singleton idiom: check without lock, if null then lock and check again, then initialize. Pre-Java 5 it was broken because the constructor write could be reordered with the reference write, so another thread could see a non-null reference to a partially constructed object. Java 5+ JMM makes it safe **if the instance field is \`volatile\`**, because a volatile write happens-before subsequent volatile reads, publishing the fully constructed object. Prefer enum singleton or holder-class (lazy, thread-safe without explicit locks) unless you have a measured need for DCL.

\`\`\`java
class Dcl {
    private static volatile Dcl instance; // volatile is REQUIRED
    static Dcl get() {
        if (instance == null) {
            synchronized (Dcl.class) {
                if (instance == null) instance = new Dcl();
            }
        }
        return instance;
    }
}
// Safer: holder class
class Holder {
    private static class H { static final Holder I = new Holder(); }
    static Holder get() { return H.I; }
}
\`\`\``,tech:"java",displayNumber:206,sortKey:559},{id:"java-560",number:207,title:"What is `AtomicReference` and when is it useful?",question:"What is `AtomicReference` and when is it useful?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`AtomicReference<V>` holds a single object reference that can be updated atomically via CAS (`compareAndSet`, `getAndSet`, `updateAndGet`). Use it to atomically swap immutable snapshots (config objects, caches) without locks. Combined with immutable data, it implements lock-free publish: writers construct a new object, CAS it in; readers always see a consistent snapshot. For ABA, use `AtomicStampedReference`. `AtomicReferenceArray` is the array analog.\n\n```java\nrecord Config(int timeout, String url) {}\nAtomicReference<Config> cfg = new AtomicReference<>(new Config(30, "http://x"));\n// lock-free update\ncfg.updateAndGet(old -> new Config(old.timeout() + 10, old.url()));\nConfig snap = cfg.get(); // readers never see a torn Config\n```',tech:"java",displayNumber:207,sortKey:560},{id:"java-561",number:208,title:"What is `LongAdder` and when should you prefer it over `AtomicLong`?",question:"What is `LongAdder` and when should you prefer it over `AtomicLong`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`LongAdder` (Java 8) is a scalable counter that internally uses a `base` plus a striped array of `Cell`s. Under contention, threads increment different cells (via probing), avoiding a single hot CAS location. `sum()` aggregates all cells (may miss concurrent updates — weakly consistent). Prefer `LongAdder` for high-contention, write-heavy counters (request counts, metrics) where you mostly increment and occasionally read the total. Prefer `AtomicLong` when you need a precise current value often, or CAS-based algorithms (`compareAndSet`). `DoubleAdder`/`LongAccumulator` follow the same idea.\n\n```java\nLongAdder hits = new LongAdder();\nhits.increment();        // cheap under contention\nlong total = hits.sum(); // approximate under concurrent writes\n// vs AtomicLong: one CAS location, retries under contention\n```",tech:"java",displayNumber:208,sortKey:561},{id:"java-562",number:209,title:"What is `Thread.yield()` and `Thread.priority`?",question:"What is `Thread.yield()` and `Thread.priority`?",difficulty:"Basic",category:"Multithreading & Concurrency",answer:"`Thread.yield()` is a hint to the scheduler that the current thread is willing to yield its remaining time slice. The JVM/OS may ignore it; never use it for correctness (not a lock, not a wait). Thread priority (`MIN_PRIORITY=1`, `NORM=5`, `MAX=10`) is also a hint; many OSes ignore or coarsen it, and relying on it for correctness is a bug. Starvation can occur if you mix high and low priorities under a non-fair scheduler. Prefer proper synchronization and fair locks over priority tricks.\n\n```java\nThread.currentThread().setPriority(Thread.MAX_PRIORITY); // hint only\nThread.yield(); // hint only — never for correctness\n```",tech:"java",displayNumber:209,sortKey:562},{id:"java-563",number:210,title:"What is `LockSupport`?",question:"What is `LockSupport`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`LockSupport` is the lowest-level parking primitive used to implement locks, `Future`, `CompletableFuture`, `ForkJoinPool`, etc. `park()` parks the current thread; `unpark(t)` makes a permit available so the next `park()` of `t` returns immediately (or wakes it if already parked). Unlike `wait/notify`, `unpark` is *not* lost if it happens before `park` — a permit is stored. No monitor is required. Interrupt unparks and sets the interrupt flag without throwing. Building custom synchronizers on `LockSupport` + `AtomicInteger` is how AQS (`AbstractQueuedSynchronizer`) works.\n\n```java\nThread worker = Thread.currentThread();\nLockSupport.park();          // wait for permit\nLockSupport.unpark(worker);  // grant permit (safe if called first)\n// AQS: FIFO queue of waiters + park/unpark\n```",tech:"java",displayNumber:210,sortKey:563},{id:"java-564",number:211,title:"What is `AbstractQueuedSynchronizer` (AQS)?",question:"What is `AbstractQueuedSynchronizer` (AQS)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"AQS is the framework behind `ReentrantLock`, `Semaphore`, `CountDownLatch`, `ReentrantReadWriteLock`, `FutureTask`, etc. It maintains a FIFO wait queue of threads (CLH-style) and an `int state` that subclasses interpret (e.g., hold count, permit count). Subclasses implement `tryAcquire`/`tryRelease` (exclusive) or `tryAcquireShared`/`tryReleaseShared` (shared). AQS handles queuing, parking (`LockSupport`), fairness, and interruption. Understanding AQS explains why `ReentrantLock` can have conditions, why `Semaphore` has no owner, and why these primitives share similar performance characteristics.\n\n```java\n// Conceptual: ReentrantLock is an AQS subclass\n// state = hold count; exclusive owner thread\n// tryAcquire: if state==0 CAS 0->1 and set owner; if owner==current, state++\n// tryRelease: state--; if 0, clear owner, unpark successor\nclass Mutex extends AbstractQueuedSynchronizer {\n    protected boolean tryAcquire(int a) { return compareAndSetState(0, 1); }\n    protected boolean tryRelease(int a) { setState(0); return true; }\n}\n```",tech:"java",displayNumber:211,sortKey:564},{id:"java-565",number:212,title:"What is `CompletionStage` vs `CompletableFuture`?",question:"What is `CompletionStage` vs `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletionStage` is the interface defining the async chaining contract (`thenApply`, `thenCombine`, `exceptionally`, etc.). `CompletableFuture` is the standard concrete implementation that also implements `Future` (blocking `get()`). You can write code that accepts `CompletionStage` for flexibility (other impls like `CompletionStage` from reactive libs). `CompletableFuture` adds manual completion (`complete`, `completeExceptionally`) and `Future` methods. Prefer `CompletionStage` in APIs to decouple from the implementation.\n\n```java\n// API takes CompletionStage — caller can pass CompletableFuture or other impl\nCompletionStage<String> fetch() { return CompletableFuture.supplyAsync(() -> "x"); }\n// CompletableFuture = CompletionStage + Future (blocking get, cancel)\n```',tech:"java",displayNumber:212,sortKey:565},{id:"java-566",number:213,title:"How do you handle exceptions in `CompletableFuture` chains?",question:"How do you handle exceptions in `CompletableFuture` chains?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Exceptions in a stage propagate to the next stage that has an exception handler. Handlers: `exceptionally(Function<Throwable, T>)` returns a recovery value (stage completes normally with it); `handle(BiFunction<T, Throwable, U>)` receives both value and exception (exactly one is null) and returns a new value; `whenComplete(BiConsumer<T, Throwable>)` is a terminal side-effect that propagates the result/exception unchanged. If no handler is present, the final `Future.get()` throws `ExecutionException` wrapping the cause. `get()` on a completed exceptionally future rethrows the cause wrapped in `CompletionException` (subclass of `RuntimeException`).\n\n```java\nCompletableFuture.supplyAsync(() -> { throw new IOException("fail"); })\n    .exceptionally(ex -> "fallback")          // recover value\n    .thenAccept(System.out::println);         // prints "fallback"\n\n// handle: both value and ex available\nCompletableFuture.supplyAsync(() -> 1/0)\n    .handle((v, ex) -> ex == null ? v : -1);\n\n// whenComplete: terminal logging, propagates unchanged\nCompletableFuture.supplyAsync(() -> "ok")\n    .whenComplete((v, ex) -> log.info("done: " + v));\n```',tech:"java",displayNumber:213,sortKey:566},{id:"java-567",number:214,title:"What is `allOf` and `anyOf` in `CompletableFuture`?",question:"What is `allOf` and `anyOf` in `CompletableFuture`?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'`CompletableFuture.allOf(f1, f2, ...)` returns a `CompletableFuture<Void>` that completes when *all* given futures complete (or any fails). Use `allOf(...).join()` to wait for a batch, then inspect individual futures for results. `anyOf(f1, f2, ...)` returns a `CompletableFuture<Object>` that completes with the result of the *first* to complete (success or failure); use for timeout races or redundant calls. Note: `allOf` does not cancel others on failure; `anyOf` does not cancel the rest on completion — you may want to cancel manually.\n\n```java\nCompletableFuture<String> a = CompletableFuture.supplyAsync(() -> "a");\nCompletableFuture<String> b = CompletableFuture.supplyAsync(() -> "b");\nCompletableFuture<Void> both = CompletableFuture.allOf(a, b);\nboth.join(); // wait for both\nList<String> results = List.of(a.join(), b.join());\n\nCompletableFuture<Object> first = CompletableFuture.anyOf(a, b);\nObject fast = first.join(); // first result\n```',tech:"java",displayNumber:214,sortKey:567},{id:"java-568",number:215,title:"What is `StructuredTaskScope` (Java 21/24)?",question:"What is `StructuredTaskScope` (Java 21/24)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`StructuredTaskScope` (JEP 453, preview Java 21, finalized Java 24) implements **structured concurrency**: a scope that owns a set of subtasks and guarantees all complete (or are cancelled) before the scope exits. `try (var scope = new StructuredTaskScope.ShutdownOnFailure()) { Future<A> a = scope.fork(() -> fetchA()); Future<B> b = scope.fork(() -> fetchB()); scope.join(); }` — on any failure, `ShutdownOnFailure` cancels the others; `ShutdownOnSuccess` cancels remaining on first success. This eliminates orphaned threads and resource leaks. Scopes are nestable and enforce that children outlive parents.\n\n```java\n// Java 24+ finalized\ntry (var scope = new StructuredTaskScope.ShutdownOnFailure()) {\n    Future<String> user = scope.fork(() -> fetchUser(id));\n    Future<List<Order>> orders = scope.fork(() -> fetchOrders(id));\n    scope.join();            // wait for all, propagate exceptions\n    scope.throwIfFailed();   // throw first exception if any\n    return new Profile(user.get(), orders.get());\n}\n```",tech:"java",displayNumber:215,sortKey:568},{id:"java-569",number:216,title:"What is `ScopedValue` (Java 21+)?",question:"What is `ScopedValue` (Java 21+)?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'`ScopedValue` (JEP 446, preview Java 21, finalized Java 24) provides **implicit context propagation** across virtual threads without `ThreadLocal`. It is a write-once (per scope) immutable key-value pair that is captured when a `ScopedValue.where(key, value).run(() -> ...)` scope is entered, and automatically visible to all virtual threads created within that scope (including those in `StructuredTaskScope`). It avoids the memory leak and cross-task contamination of `ThreadLocal` in thread pools. Use for request IDs, auth tokens, DB sessions.\n\n```java\n// Java 24+ finalized\nstatic final ScopedValue<String> TRACE_ID = ScopedValue.newInstance();\n\nvoid handle(Request req) {\n    ScopedValue.where(TRACE_ID, req.traceId()).run(() -> {\n        // any virtual thread spawned here sees TRACE_ID\n        StructuredTaskScope.ShutdownOnFailure scope = new StructuredTaskScope.ShutdownOnFailure();\n        scope.fork(() -> log(TRACE_ID.get())); // "abc-123"\n        scope.join();\n    });\n}\n```',tech:"java",displayNumber:216,sortKey:569},{id:"java-570",number:217,title:"What is the Memory Consistency Error and how to avoid it?",question:"What is the Memory Consistency Error and how to avoid it?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'A memory consistency error occurs when different threads have inconsistent views of shared data due to lack of a happens-before relationship (reordering, caching). Avoiding it means establishing happens-before edges: use `synchronized`, `volatile`, atomic classes, or concurrent collections; follow the safe-publication rules (final fields, `volatile`, thread-confined). The key is not just "atomicity" but "visibility" — without a happens-before edge, there is no guarantee a thread sees another\'s writes.\n\n```java\n// Inconsistent view without happens-before\nboolean ready = false; int value = 0;\n// Writer: value = 42; ready = true;\n// Reader: if (ready) System.out.println(value); // may print 0 or 42!\n// Fix: make ready volatile (write happens-before read) -> reader sees 42.\n```',tech:"java",displayNumber:217,sortKey:570},{id:"java-571",number:218,title:"What is safe publication in Java?",question:"What is safe publication in Java?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Safe publication means ensuring that when one thread makes an object visible to another, the other sees the object in a fully constructed, consistent state. Ways to safely publish: (1) store in a `static final` field (JMM guarantees final-field semantics + class-init lock); (2) store in a `volatile` field or `AtomicReference`; (3) store in a properly locked field; (4) put in a thread-safe collection (`ConcurrentHashMap`, `BlockingQueue`); (5) publish through a thread's `start()` or `join()`. Danger: letting `this` escape during construction (e.g., registering `this` in the constructor) exposes a partially constructed object.\n\n```java\n// Safe: static final (class init lock + final semantics)\nstatic final Config CFG = new Config();\n// Safe: volatile publish\nvolatile Resource r;\nr = new Resource(); // any thread reading r sees full object\n// UNSAFE: this escapes constructor -> partial construction visible\nclass Bad { Bad() { registry.register(this); } }\n```",tech:"java",displayNumber:218,sortKey:571},{id:"java-572",number:219,title:"What is `final` field semantics in the JMM?",question:"What is `final` field semantics in the JMM?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:`The JMM guarantees that a \`final\` field initialized in the constructor is visible to other threads *without further synchronization*, provided the object reference does not escape during construction. The write of the final field in the constructor happens-before the first read of that field by another thread, and the constructor's actions happen-before the object becomes visible. This is why immutable objects with all fields \`final\` are inherently thread-safe after safe publication.

\`\`\`java
class Immutable {
    final int x;
    final String s;
    Immutable(int x, String s) {
        this.x = x;
        this.s = s;
    }
}
// Thread A: Immutable i = new Immutable(1, "a");
// Thread B: reads i.x, i.s — guaranteed to see 1 and "a" without volatile/sync
\`\`\``,tech:"java",displayNumber:219,sortKey:572},{id:"java-573",number:220,title:"What is false sharing and how do you avoid it?",question:"What is false sharing and how do you avoid it?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"False sharing occurs when independent variables updated by different threads reside on the same CPU cache line (typically 64 bytes). Even though the variables are logically independent, writes invalidate the shared cache line, causing heavy cache-coherence traffic and poor performance. Java 8 introduced `@Contended` (internal annotation) to pad fields, but it requires `-XX:-RestrictContended`. `LongAdder` avoids this via padded striped cells. Avoid by grouping read-mostly fields, padding hot counters, using `LongAdder`, and measuring with JMH — false sharing is a performance issue, not a correctness bug.\n\n```java\n// False sharing: x and y likely same cache line; two threads update separately\nclass Counters { volatile long x, y; }\n// Avoid: LongAdder has padded cells (better for hot counters)\nLongAdder requests = new LongAdder();\nrequests.increment();\n// Or @jdk.internal.vm.annotation.Contended with JVM flag (not portable API)\n```",tech:"java",displayNumber:220,sortKey:573},{id:"java-574",number:221,title:"How do you detect deadlocks in production?",question:"How do you detect deadlocks in production?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'No automatic runtime deadlock detection in JVM, but tooling exists: (1) `jstack <pid>` — thread dump shows "BLOCKED" on locks; look for cycles. (2) `ThreadMXBean.findDeadlockedThreads()` / `findMonitorDeadlockedThreads()` — programmatic; returns thread IDs in a deadlock cycle. (3) VisualVM / JConsole — "Detect Deadlock" button. (4) JVM flags: `-XX:+UnlockDiagnosticVMOptions -XX:+LogVMOutput` for deadlock logging (experimental). Add a monitoring thread that periodically checks `ThreadMXBean` and alerts/logs.\n\n```java\nThreadMXBean mx = ManagementFactory.getThreadMXBean();\nlong[] deadlocked = mx.findDeadlockedThreads();\nif (deadlocked != null) {\n    ThreadInfo[] infos = mx.getThreadInfo(deadlocked);\n    for (ThreadInfo ti : infos) System.err.println(ti);\n}\n```',tech:"java",displayNumber:221,sortKey:574},{id:"java-575",number:222,title:"What is `CompletableFuture` `thenComposeAsync` vs `thenCompose`?",question:"What is `CompletableFuture` `thenComposeAsync` vs `thenCompose`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"`thenCompose` executes the dependent function on the *same thread* that completes the previous stage (which may be a caller thread, a ForkJoinPool worker, or a custom executor's thread). `thenComposeAsync` submits the dependent function to an `Executor` (default: `ForkJoinPool.commonPool()`) — ensuring it runs asynchronously and avoiding \"blocking\" the completing thread. Use `thenComposeAsync` when the next stage may block or is CPU-intensive, to keep the upstream thread (or common pool) free. Overloads let you supply a custom executor.\n\n```java\n// runs on same thread that completed fetchId\nfetchId().thenCompose(id -> fetchUser(id));\n\n// runs on commonPool (or supplied executor), not on fetchId's thread\nfetchId().thenComposeAsync(id -> fetchUser(id));\nfetchId().thenComposeAsync(id -> fetchUser(id), myExecutor);\n```",tech:"java",displayNumber:222,sortKey:575},{id:"java-576",number:223,title:"What is the difference between `Thread.startVirtualThread` and `Thread.ofVirtual().start`?",question:"What is the difference between `Thread.startVirtualThread` and `Thread.ofVirtual().start`?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:'Java 21 introduced two ways: `Thread.startVirtualThread(Runnable)` (static factory, creates and starts in one call, unnamed) and `Thread.ofVirtual().name("vt-", 0).start(Runnable)` (builder API for naming, inheriting thread-local behavior, `ThreadGroup`, etc.). `startVirtualThread` is a shortcut; `ofVirtual()` returns a `Thread.Builder.OfVirtual` for configuration. Both schedule on the virtual-thread scheduler. The builder also supports `start(() -> ...)` and `unstarted(() -> ...)` if you need a reference before starting.\n\n```java\n// Java 21+\nThread.startVirtualThread(() -> System.out.println("hi")); // quick one-off\nThread vt = Thread.ofVirtual().name("worker-", 0).unstarted(() -> work());\nvt.start(); // later\n```',tech:"java",displayNumber:223,sortKey:576},{id:"java-577",number:224,title:"When should you NOT use virtual threads?",question:"When should you NOT use virtual threads?",difficulty:"Advanced",category:"Multithreading & Concurrency",answer:"Virtual threads are for I/O-bound, blocking-style code with many concurrent tasks. Avoid or use platform threads when: (1) **CPU-bound** workloads — virtual threads don't add parallelism beyond core count; use a bounded `ForkJoinPool`/fixed pool. (2) **Pinning** — code that holds `synchronized` or native frames while doing blocking I/O stalls carriers (Java 21). (3) **Thread-local heavy usage** — millions of virtual threads each with large `ThreadLocal`s can blow heap; clean up with `remove()`. (4) **Tight loops without blocking** — retains carrier, no benefit. (5) **Synchronized thread pools** — `Executors.newVirtualThreadPerTaskExecutor()` is the right fit, not a fixed-size virtual pool.\n\n```java\n// WRONG: CPU-bound, no blocking — virtual threads add nothing\ntry (var e = Executors.newVirtualThreadPerTaskExecutor()) {\n    for (int i = 0; i < 1000; i++) e.submit(() -> heavyCpuLoop()); // use fixed pool\n}\n// RIGHT: I/O-bound blocking\ne.submit(() -> { db.query(); http.get(); }); // virtual threads shine\n```",tech:"java",displayNumber:224,sortKey:577},{id:"java-578",number:225,title:"What are best practices for writing concurrent Java code?",question:"What are best practices for writing concurrent Java code?",difficulty:"Intermediate",category:"Multithreading & Concurrency",answer:'Key principles: (1) **Prefer immutability** — final fields, no setters; safe publication eliminates synchronization. (2) **Use thread-safe libraries** — concurrent collections, `CompletableFuture`, `ExecutorService` — don\'t roll your own locks unless necessary. (3) **Minimize shared mutable state** — thread confinement, `ThreadLocal`, or copy-on-write. (4) **Use the right synchronizer** — `CountDownLatch` for one-shot wait, `CyclicBarrier` for phases, `Semaphore` for resource limits, `Phaser` for dynamic parties. (5) **Avoid premature optimization** — `synchronized` is fast enough for most; profile first. (6) **Always clean up** — `shutdown()`, `ThreadLocal.remove()`, close resources. (7) **Test under concurrency** — JMH for performance, `jcstress` for correctness, ThreadSanitizer for data races.\n\n```java\n// Example: immutable config + thread-safe map + CompletableFuture\nrecord Config(String url, int timeout) {}\nConfig cfg = new Config("http://api", 30);\n\nConcurrentHashMap<String, Object> cache = new ConcurrentHashMap<>();\ncache.computeIfAbsent(key, k -> load(k)); // atomic\n\nCompletableFuture.supplyAsync(() -> fetch(cfg))\n    .thenApply(this::transform)\n    .exceptionally(ex -> fallback)\n    .thenAccept(this::store);\n```',tech:"java",displayNumber:225,sortKey:578},{id:"java-579",number:226,title:"What are Lambda Expressions in Java 8 and how do they work?",question:"What are Lambda Expressions in Java 8 and how do they work?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Lambda expressions introduce functional programming to Java, allowing you to treat functionality as a method argument or code as data. They provide a concise way to represent anonymous functions. A lambda consists of a parameter list, an arrow token (\`->\`), and a body. They reduce boilerplate from anonymous inner classes and are only usable with functional interfaces (interfaces with exactly one abstract method). The compiler infers parameter types, and lambdas can capture variables from the enclosing scope provided those variables are effectively final.

\`\`\`java
// Anonymous inner class vs lambda
Runnable r1 = new Runnable() {
    @Override public void run() { System.out.println("Hello"); }
};
Runnable r2 = () -> System.out.println("Hello");

// With parameters and a block body
BiFunction<Integer, Integer, Integer> add = (a, b) -> {
    int sum = a + b;
    return sum;
};

// Capturing effectively final variable
int factor = 2;
Function<Integer, Integer> times = x -> x * factor; // factor must not be reassigned
\`\`\``,tech:"java",displayNumber:226,sortKey:579},{id:"java-580",number:227,title:"What is a Functional Interface and how does it differ from a regular interface?",question:"What is a Functional Interface and how does it differ from a regular interface?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`A functional interface has exactly one abstract method (a "single abstract method" or SAM type). It is the target type for lambda expressions and method references. While it may declare any number of default, static, or \`java.lang.Object\` methods, only one method may be abstract. The \`@FunctionalInterface\` annotation is optional but recommended: it signals intent and causes a compile error if the interface is later given a second abstract method.

\`\`\`java
@FunctionalInterface
interface StringFormatter {
    String format(String input); // the single abstract method

    default String formatUpper(String s) { return s.toUpperCase(); } // allowed
    static String empty() { return ""; }                            // allowed
    boolean equals(Object obj);                                      // allowed: from Object
}

StringFormatter f = s -> s.trim();
\`\`\``,tech:"java",displayNumber:227,sortKey:580},{id:"java-581",number:228,title:"Describe the built-in `Function<T, R>` interface and its default methods.",question:"Describe the built-in `Function<T, R>` interface and its default methods.",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Function<T, R>` represents a function that accepts one argument of type `T` and produces a result of type `R`. Its single abstract method is `R apply(T t)`. It provides two useful default methods: `andThen(Function)` composes so that the current function runs first then the other, and `compose(Function)` runs the other function first. A static `identity()` returns a function that returns its input unchanged.\n\n```java\nFunction<Integer, Integer> times2 = x -> x * 2;\nFunction<Integer, Integer> plus1  = x -> x + 1;\n\nFunction<Integer, Integer> composed = times2.andThen(plus1); // (x*2)+1\nSystem.out.println(composed.apply(5)); // 11\n\nFunction<String, String> id = Function.identity();\nSystem.out.println(id.apply("hi")); // hi\n```',tech:"java",displayNumber:228,sortKey:581},{id:"java-582",number:229,title:"How does the `Predicate<T>` interface work and what default methods does it provide?",question:"How does the `Predicate<T>` interface work and what default methods does it provide?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Predicate<T>` represents a boolean-valued test on a single argument via `boolean test(T t)`. It offers default/static composition methods: `and(Predicate)` (logical AND), `or(Predicate)` (logical OR), `negate()` (logical NOT), and the static `Predicate.isEqual(Object)`. Predicates are heavily used with `Stream.filter` to select elements.\n\n```java\nPredicate<Integer> positive = n -> n > 0;\nPredicate<Integer> even     = n -> n % 2 == 0;\n\nPredicate<Integer> posEven = positive.and(even);\nPredicate<Integer> notPos  = positive.negate();\n\nList<Integer> nums = List.of(-2, -1, 0, 1, 2, 3, 4);\nnums.stream().filter(posEven).forEach(System.out::println); // 2, 4\n```",tech:"java",displayNumber:229,sortKey:582},{id:"java-583",number:230,title:"What is the `Consumer<T>` interface used for?",question:"What is the `Consumer<T>` interface used for?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Consumer<T>` represents an operation that accepts a single input argument and returns no result. Its abstract method is `void accept(T t)`. It is commonly used for side effects such as printing or collecting. The default method `andThen(Consumer)` returns a composed consumer that performs the current action then the passed one.\n\n```java\nConsumer<String> shout = s -> System.out.println(s.toUpperCase());\nConsumer<String> greet = s -> System.out.println("Hi " + s);\n\nList.of("alice", "bob").forEach(shout);\n\nConsumer<String> both = shout.andThen(greet);\nboth.accept("carol"); // CAROL then Hi carol\n```',tech:"java",displayNumber:230,sortKey:583},{id:"java-584",number:231,title:"When would you use the `Supplier<T>` interface?",question:"When would you use the `Supplier<T>` interface?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Supplier<T>` represents a supplier of results; it takes no arguments and returns a value via `T get()`. It is useful for deferred or lazy computation, generating values on demand, and providing factory methods. Because it accepts no input, it is the natural choice for lazy initialization and for factories passed to constructors or methods.\n\n```java\nSupplier<Double> random = () -> Math.random();\nSystem.out.println(random.get());\n\nSupplier<List<String>> listFactory = () -> new ArrayList<>();\nList<String> a = listFactory.get();\nList<String> b = listFactory.get(); // fresh list each call\n```",tech:"java",displayNumber:231,sortKey:584},{id:"java-585",number:232,title:"What is the difference between `Function`, `BiFunction`, and `UnaryOperator`?",question:"What is the difference between `Function`, `BiFunction`, and `UnaryOperator`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Function<T,R>` takes one argument and returns a possibly different type. `BiFunction<T,U,R>` takes two arguments and returns a result. `UnaryOperator<T>` is a specialization of `Function<T,T>` where the argument and result are the same type, providing `andThen`/`compose`. Similarly `BinaryOperator<T>` extends `BiFunction<T,T,T>` and adds `minBy`/`maxBy` static helpers. Choosing the right type communicates intent and avoids unsafe casting.\n\n```java\nFunction<String, Integer> length = String::length;\nBiFunction<Integer, Integer, Integer> sum = Integer::sum;\nUnaryOperator<Integer> square = x -> x * x;\nBinaryOperator<Integer> max = BinaryOperator.maxBy(Integer::compare);\n```",tech:"java",displayNumber:232,sortKey:585},{id:"java-586",number:233,title:"What are Default Methods in interfaces and why were they introduced?",question:"What are Default Methods in interfaces and why were they introduced?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Default methods, declared with the \`default\` keyword, let interfaces provide a concrete implementation for a method. They were introduced primarily to allow the Collections API to be evolved with lambda-friendly methods (like \`forEach\`, \`stream\`) without breaking every existing implementation. A class implementing the interface can use, override, or ignore the default. If a class implements two interfaces with conflicting default methods, the class must override the method to resolve the ambiguity.

\`\`\`java
interface Greeter {
    default void greet() { System.out.println("Hello"); }
}
class EnglishGreeter implements Greeter { } // inherits greet()

interface A { default void m() { System.out.println("A"); } }
interface B { default void m() { System.out.println("B"); } }
class C implements A, B {
    public void m() { A.super.m(); } // must resolve conflict explicitly
}
\`\`\``,tech:"java",displayNumber:233,sortKey:586},{id:"java-587",number:234,title:"What are Static Methods in interfaces?",question:"What are Static Methods in interfaces?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'Static interface methods are utility methods associated with the interface itself, not with instances. They cannot be overridden by implementing classes and are invoked through the interface name (e.g., `Comparator.comparing(...)`). They were added in Java 8 so interfaces could carry factory and helper logic that previously lived in separate utility classes such as `Collections` or `Paths`.\n\n```java\nComparator<String> byLength = Comparator.comparing(String::length);\nList<String> list = List.of("a", "ccc", "bb");\nlist.stream().sorted(byLength).forEach(System.out::println);\n```',tech:"java",displayNumber:234,sortKey:587},{id:"java-588",number:235,title:"How does Java resolve method invocation when multiple interfaces provide the same default method?",question:"How does Java resolve method invocation when multiple interfaces provide the same default method?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:`Java uses a strict set of rules: (1) Most specific class wins over interfaces, so an overriding class method takes precedence. (2) Among interfaces, a default method in a more specific (sub)interface wins over a less specific (super)interface. (3) If two independent interfaces declare the same default and neither is more specific, the compiler forces the implementing class to override the method; otherwise it is a compile-time error. The \`InterfaceName.super.method()\` syntax explicitly invokes a specific interface's default.

\`\`\`java
interface Animal { default void sound() { System.out.println("..."); } }
interface Pet extends Animal { default void sound() { System.out.println("pet"); } }
interface Robot { default void sound() { System.out.println("beep"); } }

class Cat implements Pet, Robot {
    public void sound() { Pet.super.sound(); } // must override: Pet and Robot conflict
}
\`\`\``,tech:"java",displayNumber:235,sortKey:588},{id:"java-589",number:236,title:"What is the Stream API and what problems does it solve?",question:"What is the Stream API and what problems does it solve?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'The Stream API (in `java.util.stream`) provides a declarative, functional approach to processing sequences of data. A stream is not a data structure but a pipeline of operations over a source (collection, array, generator). It solves the verbosity of iterative loops by expressing what to compute rather than how, and it enables easy parallelization via `parallelStream()`. Streams are lazy (intermediate operations are not executed until a terminal operation is invoked) and consume-once (cannot be reused after a terminal operation).\n\n```java\nList<String> names = List.of("Alice", "Bob", "Charlie");\nlong count = names.stream()\n                  .filter(n -> n.length() > 3)\n                  .count();\nSystem.out.println(count); // 2\n```',tech:"java",displayNumber:236,sortKey:589},{id:"java-590",number:237,title:"Explain the difference between intermediate and terminal stream operations.",question:"Explain the difference between intermediate and terminal stream operations.",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, `distinct`, `peek`, `limit`, `skip`) return a new stream and are always lazy—they do not process data until a terminal operation runs. Terminal operations (`forEach`, `collect`, `reduce`, `count`, `findFirst`, `anyMatch`, `toArray`) trigger the pipeline and produce a non-stream result (or side effect). After a terminal operation, the stream is considered consumed and can no longer be used.\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4);\nnums.stream()\n    .filter(n -> n % 2 == 0)   // intermediate (lazy)\n    .map(n -> n * 10)          // intermediate (lazy)\n    .forEach(System.out::println); // terminal (executes pipeline)\n```",tech:"java",displayNumber:237,sortKey:590},{id:"java-591",number:238,title:"How does the `map` operation work in the Stream API?",question:"How does the `map` operation work in the Stream API?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`map(Function)` transforms each element of a stream into another object by applying a function, producing a new stream of the same size. It performs a one-to-one transformation. It is commonly used to extract fields, convert types, or compute derived values. The element type of the resulting stream may differ from the source.\n\n```java\nList<String> words = List.of("hello", "world");\nList<Integer> lengths = words.stream()\n                             .map(String::length)\n                             .toList();\nSystem.out.println(lengths); // [5, 5]\n```',tech:"java",displayNumber:238,sortKey:591},{id:"java-592",number:239,title:"How does `flatMap` differ from `map`?",question:"How does `flatMap` differ from `map`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:`\`flatMap(Function)\` maps each element to a stream and then flattens all the resulting streams into a single stream. It is used for one-to-many transformations—for example, splitting sentences into words or extracting elements from nested collections. \`map\` would produce a stream of streams, whereas \`flatMap\` produces a single merged stream.

\`\`\`java
List<List<Integer>> matrix = List.of(List.of(1, 2), List.of(3, 4));
List<Integer> flat = matrix.stream()
                           .flatMap(List::stream)
                           .toList();
System.out.println(flat); // [1, 2, 3, 4]

List<String> sentences = List.of("hello world", "foo bar");
List<String> words = sentences.stream()
                              .flatMap(s -> Arrays.stream(s.split(" ")))
                              .toList();
\`\`\``,tech:"java",displayNumber:239,sortKey:592},{id:"java-593",number:240,title:"What is the `filter` operation and how is it used with a Predicate?",question:"What is the `filter` operation and how is it used with a Predicate?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`filter(Predicate)` retains only the elements of a stream for which the predicate evaluates to `true`, producing a new stream of the same or smaller size. It does not modify the source; instead it lazily selects elements that satisfy the condition. Combined with other intermediate operations, `filter` is the primary mechanism for selecting data within a pipeline.\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4, 5, 6);\nList<Integer> evens = nums.stream()\n                          .filter(n -> n % 2 == 0)\n                          .toList();\nSystem.out.println(evens); // [2, 4, 6]\n```",tech:"java",displayNumber:240,sortKey:593},{id:"java-594",number:241,title:"Explain the `reduce` operation with its three overloads.",question:"Explain the `reduce` operation with its three overloads.",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'`reduce` performs a reduction (fold) that combines stream elements into a single result using an associative accumulator. The three overloads are: (1) `reduce(identity, accumulator)` returns the accumulated value with an identity that is both the initial value and the result for an empty stream; (2) `reduce(accumulator)` returns an `Optional<T>` (no identity, useful when the stream may be empty); (3) `reduce(identity, accumulator, combiner)` for parallel streams where the combiner merges partial results from different threads. The accumulator must be associative and the identity must satisfy `accumulator.apply(identity, x) == x`.\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4);\nint sum1 = nums.stream().reduce(0, Integer::sum);            // identity 0\nOptional<Integer> sum2 = nums.stream().reduce(Integer::sum); // Optional[10]\nString joined = nums.stream()\n                    .map(String::valueOf)\n                    .reduce("", (a, b) -> a + b);            // "1234"\nSystem.out.println(sum1 + " " + sum2 + " " + joined);\n```',tech:"java",displayNumber:241,sortKey:594},{id:"java-595",number:242,title:"How does the `collect` operation work, and what are Collectors?",question:"How does the `collect` operation work, and what are Collectors?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`collect` is a terminal operation that mutably accumulates stream elements into a result container (such as a `List`, `Set`, `Map`, or a summary). A `Collector` (from `java.util.stream.Collectors`) describes the reduction: a supplier for the container, an accumulator, a combiner for parallel execution, and a finisher. Common collectors include `toList()`, `toSet()`, `toMap()`, `joining()`, `groupingBy()`, `partitioningBy()`, and `summarizingInt()`.\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann");\nList<String> upper = names.stream().collect(Collectors.toList());\nSet<String> unique = names.stream().collect(Collectors.toSet());\nString csv = names.stream().collect(Collectors.joining(", "));\nSystem.out.println(csv); // Alice, Bob, Ann\n```',tech:"java",displayNumber:242,sortKey:595},{id:"java-596",number:243,title:"What does `Collectors.groupingBy` do and what variations exist?",question:"What does `Collectors.groupingBy` do and what variations exist?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`groupingBy(classifier)` groups elements by a key derived from each element, returning a `Map<K, List<T>>`. Variations include `groupingBy(classifier, downstream)` to perform a downstream reduction on each group (e.g., `counting()`, `mapping()`, `summingInt()`), and `groupingBy(classifier, mapFactory, downstream)` to control the returned map type. `partitioningBy(Predicate)` is a specialized two-bucket grouping (true/false).\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann", "Brian");\nMap<Character, List<String>> byFirst =\n    names.stream().collect(Collectors.groupingBy(s -> s.charAt(0)));\nSystem.out.println(byFirst); // {A=[Alice, Ann], B=[Bob, Brian]}\n\nMap<Boolean, Long> countByLength =\n    names.stream().collect(Collectors.partitioningBy(s -> s.length() > 3, Collectors.counting()));\n```',tech:"java",displayNumber:243,sortKey:596},{id:"java-597",number:244,title:"What is the difference between `collect` and `reduce` for building a Collection?",question:"What is the difference between `collect` and `reduce` for building a Collection?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"`reduce` is meant for immutable reduction producing a single value and is general-purpose but awkward for mutable containers because it typically creates a new accumulator at each step, which is inefficient and not parallel-friendly. `collect` is designed specifically for mutable reduction: it provides a mutable container via a supplier and folds elements into it, making it both efficient and naturally parallel. Generally prefer `collect`/`Collectors.toList()` over `reduce` for gathering into collections.\n\n```java\n// Inefficient: creates a new list at every step\nList<Integer> bad = List.of(1,2,3).stream()\n    .reduce(new ArrayList<Integer>(),\n            (list, e) -> { list.add(e); return list; },\n            (l1, l2) -> { l1.addAll(l2); return l1; });\n\n// Idiomatic and efficient\nList<Integer> good = List.of(1,2,3).stream().collect(Collectors.toList());\n```",tech:"java",displayNumber:244,sortKey:597},{id:"java-598",number:245,title:"What is the `Optional` class and why was it introduced?",question:"What is the `Optional` class and why was it introduced?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Optional<T>` is a container object that may or may not hold a non-null value. It was introduced to represent the absence of a value explicitly, reducing `NullPointerException`s and eliminating ambiguous `null` returns. Instead of returning `null`, APIs can return `Optional.empty()` to signal "no value." It encourages the caller to handle the absent case explicitly. Note that `Optional` is intended for return types, not for fields, method parameters, or collection elements.\n\n```java\nOptional<String> name = Optional.ofNullable(findName(42));\nname.ifPresent(System.out::println);\nString result = name.orElse("default");\n```',tech:"java",displayNumber:245,sortKey:598},{id:"java-599",number:246,title:"What are the common methods of the `Optional` class?",question:"What are the common methods of the `Optional` class?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Key methods include: `of(value)` (throws if null), `ofNullable(value)` (allows null → empty), `empty()`, `isPresent()`, `ifPresent(Consumer)`, `get()` (throws if empty—avoid), `orElse(default)`, `orElseGet(Supplier)` (lazy default), `orElseThrow()` / `orElseThrow(Supplier)`, `map(Function)`, `flatMap(Function)`, `filter(Predicate)`, and `stream()` (since Java 9, turns Optional into a 0/1 element stream). `orElseGet` is preferred over `orElse` when computing the default is expensive.\n\n```java\nOptional<User> user = findUser(1);\nString email = user\n    .map(User::email)\n    .filter(e -> e.contains("@"))\n    .orElseGet(() -> generateTempEmail()); // lazy\n```',tech:"java",displayNumber:246,sortKey:599},{id:"java-600",number:247,title:"Why should you avoid using `Optional.get()` and what are safer alternatives?",question:"Why should you avoid using `Optional.get()` and what are safer alternatives?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.get()` throws `NoSuchElementException` if the Optional is empty, which is little better than a `NullPointerException` and defeats the purpose of `Optional`. Safer alternatives express intent: `orElse(default)` or `orElseGet(Supplier)` for a fallback, `ifPresent(Consumer)` for side effects, `orElseThrow(Supplier)` when absence is genuinely exceptional, and `isPresent()` checks only when necessary. Since Java 10, `orElseThrow()` with no args exists for the exceptional case.\n\n```java\nOptional<String> opt = Optional.empty();\n// String v = opt.get(); // NoSuchElementException - avoid\n\nString v = opt.orElse("fallback");\nopt.ifPresent(System.out::println);\nString must = opt.orElseThrow(() -> new IllegalStateException("missing"));\n```',tech:"java",displayNumber:247,sortKey:600},{id:"java-601",number:248,title:"What are Method References and what are the four kinds?",question:"What are Method References and what are the four kinds?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Method references are a shorthand notation for lambdas that simply call an existing method. They improve readability. The four kinds are: (1) Static method reference: `ClassName::staticMethod`; (2) Instance method of a particular object: `instance::method`; (3) Instance method of an arbitrary object of a given type: `ClassName::instanceMethod` (the object becomes the first argument); (4) Constructor reference: `ClassName::new`. The target type must be a functional interface compatible with the method signature.\n\n```java\nList<String> names = List.of("a", "b");\nnames.forEach(System.out::println);        // instance method of particular object\n\nList<String> upper = names.stream()\n                          .map(String::toUpperCase)   // instance method of arbitrary object\n                          .toList();\nList<Integer> nums = names.stream().map(Integer::parseInt).toList(); // static\nSupplier<List<String>> factory = ArrayList::new;                     // constructor\n```',tech:"java",displayNumber:248,sortKey:601},{id:"java-602",number:249,title:"How does `String::toUpperCase` as a method reference differ from `s -> s.toUpperCase()`?",question:"How does `String::toUpperCase` as a method reference differ from `s -> s.toUpperCase()`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'They are functionally equivalent and the compiler produces essentially the same bytecode. `String::toUpperCase` is a method reference of the "instance method of an arbitrary receiver of type String" kind: the stream element (the `String`) becomes the implicit `this` on which `toUpperCase()` is invoked. The lambda `s -> s.toUpperCase()` makes this explicit. Method references are preferred when they make the code clearer; lambdas are used when more complex logic is needed.\n\n```java\nStream<String> s = Stream.of("a", "b");\ns.map(String::toUpperCase).forEach(System.out::println); // cleaner\n// Equivalent lambda:\nStream.of("a","b").map(s2 -> s2.toUpperCase()).forEach(System.out::println);\n```',tech:"java",displayNumber:249,sortKey:602},{id:"java-603",number:250,title:"What are Records in Java and what problem do they solve?",question:"What are Records in Java and what problem do they solve?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Records (preview in Java 14, final in Java 16) are a concise way to model immutable data carriers. A record automatically derives the canonical constructor, private final fields, accessor methods (named after the component, not \`getX\`), \`equals\`, \`hashCode\`, and \`toString\`. They eliminate the boilerplate of traditional POJOs used purely to hold data. Records are implicitly final and cannot extend other classes (but can implement interfaces). They are ideal for DTOs, value objects, and stream aggregate results.

\`\`\`java
// Before: lots of boilerplate
public class Person {
    private final String name;
    private final int age;
    public Person(String name, int age) { this.name = name; this.age = age; }
    public String getName() { return name; }
    public int getAge() { return age; }
    // equals, hashCode, toString...
}

// After: one line, same behavior
public record Person(String name, int age) { }
\`\`\``,tech:"java",displayNumber:250,sortKey:603},{id:"java-604",number:251,title:"How do you define a Record and what is auto-generated?",question:"How do you define a Record and what is auto-generated?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"A record is declared with the `record` keyword followed by a name and a parenthesized list of components. For `record Point(int x, int y)`, the compiler generates: a private final field for each component, a public accessor `x()` and `y()` (not `getX`), a canonical constructor `Point(int x, int y)`, plus `equals`, `hashCode`, and `toString`. You may add additional constructors, static fields/methods, and instance methods, and you may override the auto-generated members.\n\n```java\npublic record Point(int x, int y) { }\n\nPoint p = new Point(1, 2);\nSystem.out.println(p.x());        // 1\nSystem.out.println(p);            // Point[x=1, y=2]\n```",tech:"java",displayNumber:251,sortKey:604},{id:"java-605",number:252,title:"Can a Record have custom constructors or additional methods?",question:"Can a Record have custom constructors or additional methods?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Yes. A record may declare an additional (non-canonical) constructor, but any additional constructor must eventually delegate to the canonical constructor via `this(...)`. You can use a compact canonical constructor to validate or normalize components without redeclaring parameters. You can also add static methods, static fields, and instance methods, and override `equals`/`hashCode`/`toString` if needed. You cannot add instance fields that are not components.\n\n```java\npublic record Range(int lo, int hi) {\n    public Range {                       // compact canonical constructor\n        if (lo > hi) throw new IllegalArgumentException("lo > hi");\n    }\n    public int size() { return hi - lo; } // additional method\n}\n```',tech:"java",displayNumber:252,sortKey:605},{id:"java-606",number:253,title:"How are Records used effectively with the Stream API?",question:"How are Records used effectively with the Stream API?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Records make excellent intermediate or final results in stream pipelines because they are immutable and come with `equals`/`hashCode`/`toString` for free. They are commonly used as keys in `groupingBy`, as the element type after a `map`, or to bundle multiple computed values for downstream collectors like `summarizing` or `toMap`. Their value-based equality also makes them handy as `Set`/map keys for deduplication.\n\n```java\nrecord Person(String name, int age) { }\nList<Person> people = List.of(new Person("Ann", 30), new Person("Bob", 25));\nMap<String, Integer> nameToAge = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age));\n```',tech:"java",displayNumber:253,sortKey:606},{id:"java-607",number:254,title:"What is Pattern Matching for `instanceof` (Java 16+) and how does it improve code?",question:"What is Pattern Matching for `instanceof` (Java 16+) and how does it improve code?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'Pattern matching for `instanceof` (standard in Java 16, preview earlier) lets you combine a type test with a binding of a typed variable in a single expression: `if (obj instanceof String s) { ... use s ... }`. The bound variable `s` is in scope only when the test succeeds and does not need an explicit cast. This removes the boilerplate `if (obj instanceof String) { String s = (String) obj; }` and reduces casting errors.\n\n```java\nObject obj = "hello";\nif (obj instanceof String s) {\n    System.out.println(s.length()); // no cast needed\n}\n```',tech:"java",displayNumber:254,sortKey:607},{id:"java-608",number:255,title:"How does Pattern Matching for `switch` work in Java 17/21?",question:"How does Pattern Matching for `switch` work in Java 17/21?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Pattern matching for `switch` (preview in Java 17, finalized in Java 21) allows `case` labels to be type patterns and guards. You write `case String s -> ...`, `case Integer i -> ...`, and use `when` guards for refinements like `case Integer i when i > 0 -> ...`. The switch can mix type patterns, constants, and `default`/`case null` handling. It removes the need for chained `if (x instanceof ...)` blocks and provides exhaustiveness checking for sealed type hierarchies.\n\n```java\nstatic String classify(Object o) {\n    return switch (o) {\n        case Integer i when i > 0 -> "positive int";\n        case Integer i            -> "non-positive int";\n        case String s             -> "string: " + s;\n        case null                 -> "null";\n        default                   -> "other";\n    };\n}\n```',tech:"java",displayNumber:255,sortKey:608},{id:"java-609",number:256,title:"What is the `case null` handling in modern switch expressions?",question:"What is the `case null` handling in modern switch expressions?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Traditional `switch` on a reference would throw `NullPointerException` if the selector was null and there was no null case. Modern pattern switch requires you to explicitly handle null: you can add `case null -> ...`, or, if you omit it, the compiler inserts an implicit null check that throws `NullPointerException` (to preserve the legacy behavior) unless a `case null` is present. This makes null handling explicit and avoids accidental NPEs.\n\n```java\nString result = switch (input) {     // input may be null\n    case null  -> "was null";\n    case "x"   -> "X";\n    default    -> "other";\n};\n```',tech:"java",displayNumber:256,sortKey:609},{id:"java-610",number:257,title:"What are Sealed Classes (Java 17) and what problem do they solve?",question:"What are Sealed Classes (Java 17) and what problem do they solve?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Sealed classes (finalized in Java 17) restrict which other classes or interfaces may extend or implement them, using the `sealed` modifier and a `permits` clause. They solve the problem of wanting inheritance for a known, closed set of subtypes (e.g., an algebraic data type) while preventing arbitrary third-party subclasses. This enables exhaustive pattern matching in `switch` because the compiler knows all permitted subtypes. Subtypes must be `final`, `sealed`, or `non-sealed`.\n\n```java\npublic sealed interface Shape permits Circle, Rectangle, Square { }\npublic final class Circle    extends Shape { }\npublic final class Rectangle extends Shape { }\npublic non-sealed class Square extends Shape { }\n```",tech:"java",displayNumber:257,sortKey:610},{id:"java-611",number:258,title:"What are the rules for permitted subclasses of a sealed type?",question:"What are the rules for permitted subclasses of a sealed type?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Each permitted subclass must be accessible to the sealed class and must declare itself as `final`, `sealed`, or `non-sealed`. A `final` subclass cannot be further extended; a `sealed` subclass must itself declare its own `permits`; a `non-sealed` subclass opens the hierarchy to arbitrary extension (breaking closure). All permitted subclasses must reside in the same module (or same package if unnamed module) as the sealed parent, and the `permits` list must name exactly the direct subclasses.\n\n```java\npublic sealed class Expr permits Const, Add, Var { }\npublic final class Const extends Expr { }\npublic sealed class Add extends Expr permits AddInt, AddDbl { }\npublic non-sealed class Var extends Expr { } // open to extension\n```",tech:"java",displayNumber:258,sortKey:611},{id:"java-612",number:259,title:"How do Sealed Classes enable exhaustive `switch` pattern matching?",question:"How do Sealed Classes enable exhaustive `switch` pattern matching?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Because a sealed type's permitted subclasses are known at compile time, a `switch` over that type with a `case` for each permitted subtype (plus optionally `default`) is provably exhaustive. When all subtypes are covered, you can omit `default` and the compiler will error if a new subtype is later added without handling it—turning a runtime bug into a compile-time error. This is the Java equivalent of algebraic data type matching.\n\n```java\ndouble area(Shape s) {\n    return switch (s) {                       // exhaustive over permits\n        case Circle c    -> Math.PI * c.r() * c.r();\n        case Rectangle r -> r.w() * r.h();\n        case Square sq   -> sq.side() * sq.side();\n        // no default needed; compiler knows all cases\n    };\n}\n```",tech:"java",displayNumber:259,sortKey:612},{id:"java-613",number:260,title:"What are Text Blocks (Java 15) and how do they improve multi-line strings?",question:"What are Text Blocks (Java 15) and how do they improve multi-line strings?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:`Text blocks, finalized in Java 15, let you write multi-line string literals using triple double-quotes (\`"""\`). They automatically handle newlines and reduce the need for concatenation and escape sequences. Indentation is managed by incidentally removing common leading whitespace based on the closing delimiter's position. Text blocks also simplify embedding JSON, SQL, HTML, or XML in code.

\`\`\`java
String json = """
    {
      "name": "Alice",
      "age": 30
    }
    """;
System.out.println(json);
\`\`\``,tech:"java",displayNumber:260,sortKey:613},{id:"java-614",number:261,title:"How does Java handle indentation and escaping in Text Blocks?",question:"How does Java handle indentation and escaping in Text Blocks?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'For indentation, Java computes the minimum indentation of all non-blank content lines (relative to the closing `"""`) and strips that amount from every line—a process called incidental whitespace removal. You can shift the block by positioning the closing delimiter. Escape sequences like `\\n` and `\\"` still work but are usually unnecessary. The `\\` at the end of a line suppresses the line terminator (for a single logical line), and `String.translateEscapes()` (Java 15) handles escapes. The result is a normal `String`.\n\n```java\nString sql = """\n    SELECT id, name \\\n    FROM users \\\n    WHERE active = true\n    """;\n// produced String has no newlines between clauses (line continuation via \\)\n```',tech:"java",displayNumber:261,sortKey:614},{id:"java-615",number:262,title:"What is the `var` keyword (Java 10) and what are its limitations?",question:"What is the `var` keyword (Java 10) and what are its limitations?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`var` is local-variable type inference: the compiler infers the type from the initializer, so you write `var list = new ArrayList<String>();` instead of spelling out the type. It is limited to local variables with initializers, indexes in enhanced `for` loops, and locals in `try-with-resources`. You cannot use `var` for fields, method parameters, return types, or without an initializer. The type is still static and strongly typed—`var` is not `dynamic` or a `Object` wildcard.\n\n```java\nvar name = "Alice";            // String\nvar numbers = List.of(1, 2, 3);// List<Integer>\nfor (var n : numbers) { }      // int\n// var x; // error: needs initializer\n```',tech:"java",displayNumber:262,sortKey:615},{id:"java-616",number:263,title:"When should you avoid using `var`?",question:"When should you avoid using `var`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Avoid `var` when the inferred type is not obvious from the initializer, which hurts readability—e.g., `var result = someMethod();` where the method's return type is unclear. Also avoid it when the code relies on a specific type for overloading or when mixing numeric types where the literal type (`var x = 5L` vs `var x = 5`) matters. A good rule: use `var` when it makes code cleaner, keep explicit types when the type carries meaning or aids comprehension.\n\n```java\n// Less clear\nvar data = fetch(); // what type? avoid\n// Clearer\nList<User> users = fetchUsers(); // explicit type helps here\n```",tech:"java",displayNumber:263,sortKey:616},{id:"java-617",number:264,title:"What are Sequenced Collections (Java 21) and which interfaces were added?",question:"What are Sequenced Collections (Java 21) and which interfaces were added?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Sequenced Collections (Java 21) fill a gap where the collections framework lacked a universal type for collections with a defined encounter order. Three interfaces were added: `SequencedCollection` (extends `Collection`, has `reversed()`, `addFirst`, `addLast`, `getFirst`, `getLast`, `removeFirst`, `removeLast`), `SequencedSet` (extends `SequencedCollection` and `Set`), and `SequencedMap` (with `reversed()`, `firstEntry`, `lastEntry`, `putFirst`, `putLast`). `List`, `Deque`, `LinkedHashSet`, and `SortedMap` now implement these, providing uniform first/last and reverse-view access.\n\n```java\n// Uniform first/last access across List and LinkedHashSet\nSequencedCollection<String> list = new ArrayList<>(List.of("a", "b", "c"));\nSequencedSet<String> set = new LinkedHashSet<>(List.of("a", "b", "c"));\n\nlist.getFirst();          // "a"\nlist.getLast();           // "c"\nset.reversed();           // reverse view: [c, b, a]\n```',tech:"java",displayNumber:264,sortKey:617},{id:"java-618",number:265,title:"How does the `reversed()` view work in Sequenced Collections?",question:"How does the `reversed()` view work in Sequenced Collections?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`reversed()` returns a reverse-ordered *view* of the collection, not a copy. Mutations to the original are reflected in the reversed view and vice versa. This is efficient and uniform across `List`, `SequencedSet`, and `SequencedMap`. It replaces ad-hoc reverse iteration and makes encounter-order operations consistent. Because it is a view, calling `reversed().reversed()` yields a view back to the original orientation.\n\n```java\nSequencedCollection<String> seq = new ArrayList<>(List.of("a", "b", "c"));\nSequencedCollection<String> rev = seq.reversed();\nSystem.out.println(rev);      // [c, b, a]\nrev.addFirst("z");            // affects the original list too\nSystem.out.println(seq);      // [z, a, b, c]\n```',tech:"java",displayNumber:265,sortKey:618},{id:"java-619",number:266,title:"What are Virtual Threads (Java 21) and how do they differ from platform threads?",question:"What are Virtual Threads (Java 21) and how do they differ from platform threads?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:`Virtual threads (Project Loom, finalized in Java 21) are lightweight, user-mode threads managed by the JVM rather than the operating system. They are cheap to create (millions can exist) and are scheduled on a small pool of carrier OS threads (platform threads) via the ForkJoinPool. When a virtual thread blocks on I/O, the JVM unmounts it from its carrier, allowing the carrier to run other virtual threads—so blocking is cheap. Platform threads remain for CPU-bound or native-code-heavy tasks. Virtual threads dramatically improve throughput for I/O-bound concurrent code.

\`\`\`java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    for (int i = 0; i < 10_000; i++) {
        executor.submit(() -> {
            Thread.sleep(Duration.ofMillis(100)); // cheap blocking
            return "done";
        });
    }
} // all tasks complete; executor closed
\`\`\``,tech:"java",displayNumber:266,sortKey:619},{id:"java-620",number:267,title:"How do you create and run a Virtual Thread?",question:"How do you create and run a Virtual Thread?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'You can create a virtual thread in three main ways: (1) `Thread.startVirtualThread(Runnable)`, (2) `Thread.ofVirtual().start(Runnable)`, or (3) via `Executors.newVirtualThreadPerTaskExecutor()` which creates a new virtual thread per submitted task. Virtual threads are daemon threads by default and cannot be pooled like platform threads—you create a fresh one per task. `Thread.isVirtual()` distinguishes them at runtime.\n\n```java\nThread vt = Thread.startVirtualThread(() -> System.out.println("running"));\nThread.ofVirtual().name("worker").start(() -> {});\n\ntry (var ex = Executors.newVirtualThreadPerTaskExecutor()) {\n    ex.submit(() -> "task");\n}\n```',tech:"java",displayNumber:267,sortKey:620},{id:"java-621",number:268,title:"What are the best practices and pitfalls when using Virtual Threads?",question:"What are the best practices and pitfalls when using Virtual Threads?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Best practices: use one virtual thread per task (do not pool them), avoid `synchronized` blocks on shared monitors inside hot paths (they pin the carrier thread—use `ReentrantLock` instead if long-held), and be cautious with thread-local usage (millions of virtual threads make thread-locals expensive). Don't use virtual threads for CPU-bound work where they offer no benefit. Avoid `Thread.sleep` in tight loops for signaling; prefer `LockSupport`. Virtual threads are not faster for computation, only for concurrency under blocking.\n\n```java\n// Pinning problem: synchronized pins carrier thread\nsynchronized (lock) { doBlockingIo(); } // avoid in virtual threads\n\n// Prefer ReentrantLock\nlock.lock();\ntry { doBlockingIo(); } finally { lock.unlock(); }\n```",tech:"java",displayNumber:268,sortKey:621},{id:"java-622",number:269,title:"How do Virtual Threads relate to the existing `ExecutorService` API?",question:"How do Virtual Threads relate to the existing `ExecutorService` API?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Virtual threads integrate cleanly with the existing concurrency API: `Executors.newVirtualThreadPerTaskExecutor()` returns an `ExecutorService` so existing code using `submit`/`invokeAll` works unchanged. The executor creates a new virtual thread per task rather than reusing pool threads. You should call `close()` (it is `AutoCloseable`) to wait for all tasks to finish. This backward compatibility lets you adopt virtual threads without rewriting concurrency logic.\n\n```java\ntry (ExecutorService es = Executors.newVirtualThreadPerTaskExecutor()) {\n    List<Future<String>> futures = IntStream.range(0, 100)\n        .mapToObj(i -> es.submit(() -> "r" + i))\n        .toList();\n    for (Future<String> f : futures) System.out.println(f.get());\n}\n```',tech:"java",displayNumber:269,sortKey:622},{id:"java-623",number:270,title:"What is the `Stream.toList()` method (Java 16) and how does it differ from `Collectors.toList()`?",question:"What is the `Stream.toList()` method (Java 16) and how does it differ from `Collectors.toList()`?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Stream.toList()` (Java 16) is a convenience terminal operation that returns an immutable `List`. It is simpler than `Collectors.toList()`, which returns a mutable `List` (mutable per spec). Because `toList()` returns an unmodifiable list, attempting to add/remove throws `UnsupportedOperationException`. Use `toList()` when you want a result you won't mutate; use `Collectors.toList()` or `Collectors.toCollection(...)` when you need a mutable list.\n\n```java\nList<Integer> imm = Stream.of(1, 2, 3).toList();   // unmodifiable\nList<Integer> mut = Stream.of(1,2,3).collect(Collectors.toList()); // mutable\n// imm.add(4); // throws UnsupportedOperationException\n```",tech:"java",displayNumber:270,sortKey:623},{id:"java-624",number:271,title:"What is the `Stream.takeWhile` and `dropWhile` (Java 9) operations?",question:"What is the `Stream.takeWhile` and `dropWhile` (Java 9) operations?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`takeWhile(predicate)` returns the longest prefix of elements that satisfy the predicate, stopping at the first element that fails (best for ordered streams). `dropWhile(predicate)` drops the longest prefix that satisfies the predicate and returns the remainder. They are order-sensitive unlike `filter`, which evaluates every element. They are useful when processing streams where a condition holds for an initial segment.\n\n```java\nList<Integer> nums = List.of(1, 2, 3, 4, 1);\nSystem.out.println(nums.stream().takeWhile(n -> n < 3).toList()); // [1, 2]\nSystem.out.println(nums.stream().dropWhile(n -> n < 3).toList()); // [3, 4, 1]\n```",tech:"java",displayNumber:271,sortKey:624},{id:"java-625",number:272,title:"What are the new `Optional` methods added in Java 9/10/11?",question:"What are the new `Optional` methods added in Java 9/10/11?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Several enhancements: `Optional.stream()` (Java 9) converts empty to an empty stream and present to a single-element stream, making it easy to flatMap optionals. `Optional.ifPresentOrElse(action, emptyAction)` (Java 9) runs one of two actions. `Optional.or(Supplier)` (Java 9) returns this optional or a fallback supplier. `Optional.orElseThrow()` with no args (Java 10) throws `NoSuchElementException` if empty. These reduce branching when working with optional values.\n\n```java\nOptional<String> o = Optional.empty();\no.ifPresentOrElse(System.out::println, () -> System.out.println("none"));\nOptional<String> fallback = o.or(() -> Optional.of("def"));\nStream<String> s = o.stream(); // empty stream\n```',tech:"java",displayNumber:272,sortKey:625},{id:"java-626",number:273,title:"How does `Collectors.toMap` handle duplicate keys and how can you control it?",question:"How does `Collectors.toMap` handle duplicate keys and how can you control it?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"The two-arg `toMap(keyMapper, valueMapper)` throws `IllegalStateException` on duplicate keys. To control this, use the three-arg overload `toMap(keyMapper, valueMapper, mergeFunction)`, where the merge function decides how to combine values for the same key. A four-arg overload also accepts a `mapFactory` to choose the returned map type (e.g., `LinkedHashMap::new` for ordering). The merge function receives the existing and new value and returns the value to keep.\n\n```java\nMap<String, Integer> byName = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age, (a, b) -> a)); // keep first\nMap<String, Integer> lhm = people.stream()\n    .collect(Collectors.toMap(Person::name, Person::age, (a,b)->b, LinkedHashMap::new));\n```",tech:"java",displayNumber:273,sortKey:626},{id:"java-627",number:274,title:"What is the difference between `map` and `peek` in streams?",question:"What is the difference between `map` and `peek` in streams?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`peek(Consumer)` is an intermediate operation intended for observing elements as they flow through the pipeline (e.g., logging or debugging) without modifying them; it returns the same stream of unchanged elements. `map(Function)` is for transforming elements into new values and changing the stream type. Using `peek` to mutate state is discouraged; use `map` when you want to change data and `forEach` (terminal) when you only want a side effect at the end.\n\n```java\nList<Integer> result = Stream.of(1, 2, 3)\n    .peek(n -> System.out.println("before: " + n))\n    .map(n -> n * 2)\n    .peek(n -> System.out.println("after: " + n))\n    .toList();\n```',tech:"java",displayNumber:274,sortKey:627},{id:"java-628",number:275,title:"How do you use `Comparator` with method references for sorting streams?",question:"How do you use `Comparator` with method references for sorting streams?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"`Comparator.comparing(keyExtractor)` builds a comparator from a function that extracts a comparable key, and you can pass a method reference as the extractor. Chain with `thenComparing` for secondary sort. `Comparator.reverseOrder()` and `reversed()` flip ordering. This is far cleaner than writing anonymous comparators and integrates directly with `Stream.sorted`.\n\n```java\nList<Person> sorted = people.stream()\n    .sorted(Comparator.comparing(Person::age).thenComparing(Person::name))\n    .toList();\nList<String> desc = words.stream()\n    .sorted(Comparator.reverseOrder())\n    .toList();\n```",tech:"java",displayNumber:275,sortKey:628},{id:"java-629",number:276,title:"What are some useful `Collectors` for numeric aggregation?",question:"What are some useful `Collectors` for numeric aggregation?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'For numeric aggregation: `summingInt/Long/Double`, `averagingInt/...`, `summarizingInt/...` (returns `IntSummaryStatistics` with count/sum/avg/min/max), `counting()`, and `reducing(...)`. These are useful as downstream collectors in `groupingBy`. For grouping with aggregation, combine them: `groupingBy(Person::city, summingInt(Person::age))`.\n\n```java\nIntSummaryStatistics stats = people.stream()\n    .collect(Collectors.summarizingInt(Person::age));\nSystem.out.println(stats.getAverage() + " " + stats.getMax());\nMap<String, Double> avgByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.averagingInt(Person::age)));\n```',tech:"java",displayNumber:276,sortKey:629},{id:"java-630",number:277,title:"How does `Collectors.partitioningBy` differ from `groupingBy`?",question:"How does `Collectors.partitioningBy` differ from `groupingBy`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`partitioningBy(Predicate)` is a specialized form of grouping that always produces a `Map<Boolean, List<T>>` with exactly two keys: `true` and `false`. It is slightly more efficient and clearer when you simply want to split elements into "matching" vs "non-matching". `groupingBy(classifier)` produces a `Map<K, List<T>>` keyed by arbitrary classifier results (any type), and works for more than two buckets. `partitioningBy` also accepts a downstream collector.\n\n```java\nList<String> names = List.of("Alice", "Bob", "Ann", "Brian");\n\n// partitioningBy: exactly two buckets\nMap<Boolean, List<String>> p = names.stream()\n    .collect(Collectors.partitioningBy(s -> s.startsWith("A")));\n// {false=[Bob, Brian], true=[Alice, Ann]}\n\n// groupingBy: arbitrary keys\nMap<Character, List<String>> g = names.stream()\n    .collect(Collectors.groupingBy(s -> s.charAt(0)));\n// {A=[Alice, Ann], B=[Bob, Brian]}\n```',tech:"java",displayNumber:277,sortKey:630},{id:"java-631",number:278,title:"What is `flatMap` commonly used for with `Optional` and `Stream`?",question:"What is `flatMap` commonly used for with `Optional` and `Stream`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.flatMap(Function)` chains another `Optional` returning function: it returns the inner `Optional` rather than wrapping it, avoiding `Optional<Optional<T>>`. In streams, `flatMap` converts each element to a stream and concatenates them. Together, `Optional::stream` (Java 9) and `flatMap` let you flatten a `Stream<Optional<T>>` into a `Stream<T>`, filtering out empties: `streamOfOptionals.flatMap(Optional::stream)`.\n\n```java\nList<Optional<String>> opts = List.of(Optional.of("a"), Optional.empty(), Optional.of("b"));\nList<String> present = opts.stream()\n    .flatMap(Optional::stream)\n    .toList(); // ["a", "b"]\n```',tech:"java",displayNumber:278,sortKey:631},{id:"java-632",number:279,title:"How do you handle checked exceptions in lambda expressions?",question:"How do you handle checked exceptions in lambda expressions?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"Lambdas can only throw checked exceptions if the target functional interface's method declares them. Since standard interfaces like `Runnable`, `Function`, `Consumer` do not declare checked exceptions, you cannot directly throw them. Workarounds: (1) Wrap the checked exception in an unchecked one (e.g., `throw new RuntimeException(e)`), (2) Use a wrapper method that catches and wraps, (3) Define your own functional interface that declares the checked exception, (4) Use try-catch inside the lambda body. There is no built-in mechanism in the standard functional interfaces to pass checked exceptions through.\n\n```java\nFunction<String, Integer> parse = s -> {\n    try { return Integer.parseInt(s); }\n    catch (NumberFormatException e) { throw new RuntimeException(e); }\n};\n\n// Or define your own\n@FunctionalInterface interface CheckedFunction<T,R,E extends Exception> {\n    R apply(T t) throws E;\n}\n```",tech:"java",displayNumber:279,sortKey:632},{id:"java-633",number:280,title:"What is the difference between `Stream.of` and `Arrays.stream` for creating streams?",question:"What is the difference between `Stream.of` and `Arrays.stream` for creating streams?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Stream.of(T...)` works for object arrays and varargs of objects. `Arrays.stream(array)` has overloads for primitive arrays (`int[]`, `long[]`, `double[]`) producing `IntStream`/`LongStream`/`DoubleStream`, and an `Object[]` overload that returns `Stream<T>`. For primitives, `Arrays.stream` is the direct way to get a primitive stream; `Stream.of(int[])` would treat the array as a single element. For object varargs, `Stream.of` is more convenient.\n\n```java\nint[] arr = {1, 2, 3};\nIntStream is = Arrays.stream(arr);   // correct for primitives\n// Stream.of(arr) would be Stream<int[]>, not IntStream\n\nString[] objs = {"a", "b"};\nStream<String> s1 = Stream.of(objs);\nStream<String> s2 = Arrays.stream(objs);\n```',tech:"java",displayNumber:280,sortKey:633},{id:"java-634",number:281,title:"What are Primitive Streams (`IntStream`, `LongStream`, `DoubleStream`) and why use them?",question:"What are Primitive Streams (`IntStream`, `LongStream`, `DoubleStream`) and why use them?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Primitive streams avoid boxing/unboxing overhead for `int`, `long`, `double` values. They provide specialized methods: `sum()`, `average()`, `min()`, `max()`, `range()`, `rangeClosed()`, and `toArray()`. Converting between object and primitive streams uses `mapToInt`, `boxed()`, etc. Primitive streams are significantly faster and use less memory for numeric-heavy workloads.\n\n```java\nIntStream.rangeClosed(1, 100).sum();          // 5050\nIntStream.of(1, 2, 3).average().orElse(0);    // 2.0\nint[] squares = IntStream.range(0, 5).map(i -> i*i).toArray();\n```",tech:"java",displayNumber:281,sortKey:634},{id:"java-635",number:282,title:"How do you convert between object streams and primitive streams?",question:"How do you convert between object streams and primitive streams?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"To go from object to primitive: `mapToInt(ToIntFunction)`, `mapToLong`, `mapToDouble`. To go from primitive to object: `boxed()` (e.g., `IntStream.boxed()` returns `Stream<Integer>`). The `mapToObj` methods on primitive streams convert to object streams. These conversions are necessary when mixing numeric pipelines with generic collections.\n\n```java\nList<Integer> nums = List.of(1, 2, 3);\nint sum = nums.stream().mapToInt(Integer::intValue).sum(); // object -> primitive\nStream<Integer> back = IntStream.of(1,2,3).boxed();        // primitive -> object\n```",tech:"java",displayNumber:282,sortKey:635},{id:"java-636",number:283,title:"What is the `Collectors.joining` method and how is it used?",question:"What is the `Collectors.joining` method and how is it used?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`Collectors.joining()` concatenates the string representations of stream elements. The no-arg version joins with no delimiter. Overloads accept a delimiter, and a delimiter + prefix + suffix. It is the idiomatic replacement for `StringBuilder` loops and is more readable and parallel-friendly.\n\n```java\nList<String> parts = List.of("a", "b", "c");\nString csv = parts.stream().collect(Collectors.joining(", "));\nString wrapped = parts.stream().collect(Collectors.joining(", ", "[", "]"));\nSystem.out.println(csv);     // a, b, c\nSystem.out.println(wrapped); // [a, b, c]\n```',tech:"java",displayNumber:283,sortKey:636},{id:"java-637",number:284,title:"What are the `minBy` and `maxBy` collectors and how are they used?",question:"What are the `minBy` and `maxBy` collectors and how are they used?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.minBy(Comparator)` and `Collectors.maxBy(Comparator)` are reduction collectors that return an `Optional` containing the minimum or maximum element according to the comparator. They are useful as downstream collectors in `groupingBy` to find the min/max per group. For the whole stream, `Stream.min/max` is simpler; `minBy/maxBy` shine when you need the extremum per group.\n\n```java\nMap<String, Optional<Person>> oldestByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.maxBy(Comparator.comparing(Person::age))));\n```",tech:"java",displayNumber:284,sortKey:637},{id:"java-638",number:285,title:"How does `Stream.distinct()` work and what determines equality?",question:"How does `Stream.distinct()` work and what determines equality?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`distinct()` returns a stream of unique elements by removing duplicates based on `equals()`. It is a stateful intermediate operation (it must remember seen elements). For ordered streams, the first occurrence is kept; for unordered streams (like those from `Set`), the choice is nondeterministic but duplicates are removed. For records, `equals` compares component values, making `distinct` effective for deduplication.\n\n```java\nList<String> unique = Stream.of("a", "b", "a", "c").distinct().toList(); // [a, b, c]\n```',tech:"java",displayNumber:285,sortKey:638},{id:"java-639",number:286,title:"What is the difference between `sorted()` and `sorted(Comparator)`?",question:"What is the difference between `sorted()` and `sorted(Comparator)`?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:'`sorted()` sorts elements using their natural order (requires elements to implement `Comparable`). `sorted(Comparator)` sorts using the provided comparator, which allows custom ordering or sorting types that don\'t implement `Comparable`. Both are stateful intermediate operations; `sorted()` on an unordered source may still produce a deterministic order.\n\n```java\nList.of(3, 1, 2).stream().sorted().toList();                    // [1, 2, 3]\nList.of("b", "a").stream().sorted(Comparator.reverseOrder()).toList(); // [b, a]\n```',tech:"java",displayNumber:286,sortKey:639},{id:"java-640",number:287,title:"What is the purpose of `Stream.iterate` and `Stream.generate`?",question:"What is the purpose of `Stream.iterate` and `Stream.generate`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Stream.iterate(seed, unaryOperator)` produces an infinite sequential stream by repeatedly applying the function to the previous result (e.g., `iterate(0, n -> n + 2)` yields even numbers). `Stream.generate(supplier)` produces an infinite stream by repeatedly invoking a `Supplier` (e.g., `generate(Math::random)`). Both are unordered unless limited. Java 9 added `iterate(seed, predicate, unaryOperator)` which stops when the predicate fails, giving a finite stream.\n\n```java\nStream.iterate(0, n -> n + 2).limit(5).toList();      // [0, 2, 4, 6, 8]\nStream.iterate(0, n -> n < 10, n -> n + 2).toList();  // Java 9: [0, 2, 4, 6, 8]\nStream.generate(() -> (int)(Math.random()*10)).limit(5).toList(); // random\n```",tech:"java",displayNumber:287,sortKey:640},{id:"java-641",number:288,title:"How do you make a Stream parallel and when is it beneficial?",question:"How do you make a Stream parallel and when is it beneficial?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"Call `parallelStream()` on a collection or `.parallel()` on an existing stream. Parallel streams use a common `ForkJoinPool` and split the work into chunks processed by multiple threads. They are beneficial for CPU-intensive operations on large datasets with minimal synchronization (e.g., numeric reductions, complex mappings). They add overhead, so they can be slower for small streams or I/O-bound work where virtual threads (Java 21) are better suited.\n\n```java\nlong sum = LongStream.rangeClosed(1, 10_000_000).parallel().sum(); // CPU-bound, large data\n// For I/O-bound: use virtual threads instead of parallel streams\n```",tech:"java",displayNumber:288,sortKey:641},{id:"java-642",number:289,title:"What is the common `ForkJoinPool` used by parallel streams and how can you isolate workloads?",question:"What is the common `ForkJoinPool` used by parallel streams and how can you isolate workloads?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"All parallel streams share the common `ForkJoinPool.commonPool()` (configured via system properties like `java.util.concurrent.ForkJoinPool.common.parallelism`). This means a long-running parallel stream can starve other parallel work. To isolate, submit the parallel stream task to a custom `ForkJoinPool`: `new ForkJoinPool(n).submit(() -> stream.parallel()...).join()`. This prevents thread starvation in mixed workloads.\n\n```java\nForkJoinPool pool = new ForkJoinPool(4);\npool.submit(() -> bigData.parallelStream().map(heavy).toList()).join();\n```",tech:"java",displayNumber:289,sortKey:642},{id:"java-643",number:290,title:"What is the `Record Patterns` feature in Java 21 and how does it work?",question:"What is the `Record Patterns` feature in Java 21 and how does it work?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'Record patterns (Java 21) allow deconstructing a record directly in a pattern: `case Point(int x, int y) -> ...`. The components are bound to variables matching their types. You can also use nested record patterns: `case Pair(Point(int x, int y), String label) -> ...`. Record patterns compose with type patterns, `switch` expressions, and `instanceof`. They eliminate boilerplate accessor calls for destructuring.\n\n```java\nrecord Point(int x, int y) {}\nstatic String quadrant(Point p) {\n    return switch (p) {\n        case Point(var x, var y) when x > 0 && y > 0 -> "I";\n        case Point(var x, var y) when x < 0 && y > 0 -> "II";\n        // ...\n    };\n}\n```',tech:"java",displayNumber:290,sortKey:643},{id:"java-644",number:291,title:"What are `switch` expressions with `yield` and how do they differ from statements?",question:"What are `switch` expressions with `yield` and how do they differ from statements?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'Switch expressions (Java 12/14) return a value and use `->` for single expressions or `{ ... yield value; }` for blocks. They must be exhaustive (cover all cases) and cannot fall through. A `yield` statement provides the value for a block case. This replaces the old switch statement with `break` and separate assignment. The expression form is preferred for assignments and ternary-like logic.\n\n```java\nString result = switch (day) {\n    case MONDAY, FRIDAY -> "busy";\n    case SATURDAY, SUNDAY -> "weekend";\n    default -> { yield "midweek"; } // block with yield\n};\n```',tech:"java",displayNumber:291,sortKey:644},{id:"java-645",number:292,title:"What is the difference between `yield`, `return`, and `break` in modern switch?",question:"What is the difference between `yield`, `return`, and `break` in modern switch?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'In a `switch` expression: `yield` returns a value from a block case and exits the switch expression (required for block cases). `break` with a value (`break value`) is not allowed in expressions; it was a preview syntax replaced by `yield`. `return` exits the *enclosing method*, not just the switch. In a traditional switch statement, `break` exits the switch; `yield` is not allowed there.\n\n```java\n// Switch expression - must yield\nint x = switch (v) { case 1 -> { yield 10; } default -> 0; };\n\n// Switch statement - uses break\nswitch (v) { case 1: System.out.println("one"); break; }\n```',tech:"java",displayNumber:292,sortKey:645},{id:"java-646",number:293,title:"How do you use `Collectors.mapping` as a downstream collector?",question:"How do you use `Collectors.mapping` as a downstream collector?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.mapping(mapper, downstream)` transforms each element before passing it to the downstream collector. It is commonly used with `groupingBy` when you want to group by one property but collect a different property. It allows decoupling the key from the collected value. For example, `groupingBy(Person::city, mapping(Person::name, toList()))` gives `Map<String, List<String>>`.\n\n```java\nMap<String, List<String>> namesByCity = people.stream()\n    .collect(Collectors.groupingBy(Person::city, Collectors.mapping(Person::name, Collectors.toList())));\n```",tech:"java",displayNumber:293,sortKey:646},{id:"java-647",number:294,title:"What is the `Optional.or` method and how does it differ from `orElse`/`orElseGet`?",question:"What is the `Optional.or` method and how does it differ from `orElse`/`orElseGet`?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`Optional.or(Supplier<Optional<T>>)` returns this Optional if present, otherwise returns the Optional produced by the supplier. Unlike `orElse`/`orElseGet` which unwrap and return the contained value `T`, `or` returns an `Optional<T>`, preserving the Optional container. This enables chaining multiple optional fallbacks: `opt1.or(() -> opt2).or(() -> opt3)`.\n\n```java\nOptional<String> a = Optional.empty();\nOptional<String> b = Optional.of("b");\nOptional<String> c = a.or(() -> b); // Optional["b"]\n```',tech:"java",displayNumber:294,sortKey:647},{id:"java-648",number:295,title:"How do you use `Collectors.collectingAndThen` for post-processing?",question:"How do you use `Collectors.collectingAndThen` for post-processing?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:"`Collectors.collectingAndThen(downstream, finisher)` applies a finisher function to the result of the downstream collector. It is useful for transforming the collector's output, e.g., making an immutable copy: `collectingAndThen(toList(), Collections::unmodifiableList)`. It can also adapt to different return types or enforce invariants after collection. It runs after the downstream reduction completes.\n\n```java\nList<String> unmod = stream.collect(Collectors.collectingAndThen(Collectors.toList(), Collections::unmodifiableList));\n```",tech:"java",displayNumber:295,sortKey:648},{id:"java-649",number:296,title:"What are the rules for `switch` exhaustiveness with sealed types?",question:"What are the rules for `switch` exhaustiveness with sealed types?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:`A switch on a sealed type is exhaustive if every permitted subtype is covered by a case. If the switch is exhaustive, no \`default\` is required. If a new permitted subtype is added to the sealed hierarchy and the switch is not updated, it becomes a compile-time error—ensuring no case is missed. For non-sealed types, \`default\` is required unless all possible values are covered (e.g., \`enum\` switches are exhaustive by default).

\`\`\`java
sealed interface Shape permits Circle, Square {}
record Circle(int r) implements Shape {}
record Square(int s) implements Shape {}

double area(Shape sh) {
    return switch (sh) {
        case Circle c -> Math.PI * c.r() * c.r();
        case Square s -> s.s() * s.s();
        // no default needed; exhaustive
    };
}
\`\`\``,tech:"java",displayNumber:296,sortKey:649},{id:"java-650",number:297,title:"How does `Stream.concat` work and what are its constraints?",question:"How does `Stream.concat` work and what are its constraints?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:"`Stream.concat(a, b)` creates a lazily concatenated stream whose elements are all elements of `a` followed by all elements of `b`. It is an ordered stream if both inputs are ordered. The resulting stream is a sequential stream even if inputs are parallel; you must call `.parallel()` again if needed. The spliterator is composed, so it may have poorer performance characteristics than a single source.\n\n```java\nStream<Integer> combined = Stream.concat(Stream.of(1, 2), Stream.of(3, 4));\ncombined.forEach(System.out::print); // 1 2 3 4\n```",tech:"java",displayNumber:297,sortKey:650},{id:"java-651",number:298,title:"What is the `java.time` API and how does it relate to functional programming?",question:"What is the `java.time` API and how does it relate to functional programming?",difficulty:"Basic",category:"Modern Java Features (Java 8-21)",answer:"The `java.time` API (Java 8) is a modern, immutable, thread-safe date/time API. While not part of the Stream/Functional interfaces, it integrates well: `LocalDate`, `LocalDateTime`, `Instant`, `Duration`, `Period` are all immutable value types. They work naturally with lambdas (e.g., `Stream.iterate(LocalDate.now(), d -> d.plusDays(1))`) and streams. It replaces the flawed `java.util.Date`/`Calendar`.\n\n```java\nLocalDate start = LocalDate.of(2024, 1, 1);\nStream.iterate(start, d -> d.plusWeeks(1))\n      .limit(4)\n      .forEach(System.out::println);\n```",tech:"java",displayNumber:298,sortKey:651},{id:"java-652",number:299,title:"What are some common stream anti-patterns to avoid?",question:"What are some common stream anti-patterns to avoid?",difficulty:"Advanced",category:"Modern Java Features (Java 8-21)",answer:'Common anti-patterns: (1) Using streams for simple loops where `for` is clearer; (2) Modifying external state inside `peek`/`forEach` (side effects break parallelism); (3) Using `parallelStream()` without measuring—overhead can outweigh benefit; (4) Calling `stream()` on a collection while mutating it (concurrent modification); (5) Using `findFirst` on unordered parallel streams (nondeterministic); (6) Collecting to a list then mutating it—use `toList()` or a concurrent collector; (7) Ignoring that streams are consume-once.\n\n```java\n// Bad: side effect in peek\nList<String> bad = new ArrayList<>();\nStream.of("a", "b").peek(bad::add).count();\n\n// Good: collect directly\nList<String> good = Stream.of("a", "b").toList();\n```',tech:"java",displayNumber:299,sortKey:652},{id:"java-653",number:300,title:"What is the `SequencedMap` interface and its new methods in Java 21?",question:"What is the `SequencedMap` interface and its new methods in Java 21?",difficulty:"Intermediate",category:"Modern Java Features (Java 8-21)",answer:'`SequencedMap` (Java 21) extends `Map` and adds encounter-order semantics plus first/last access. New methods: `sequencedKeySet()` (returns `SequencedSet<K>`), `sequencedValues()` (returns `SequencedCollection<V>`), `sequencedEntrySet()` (returns `SequencedSet<Entry<K,V>>`), `firstEntry()`, `lastEntry()`, `pollFirstEntry()`, `pollLastEntry()`, `putFirst(K, V)`, `putLast(K, V)`, and `reversed()` (returns a reversed view). Implementations include `LinkedHashMap`, `TreeMap`, `IdentityHashMap`. It enables uniform first/last access regardless of underlying implementation.\n\n```java\nSequencedMap<String, Integer> map = new LinkedHashMap<>();\nmap.put("a", 1); map.put("b", 2);\nmap.putFirst("z", 0);          // z, a, b\nMap.Entry<String, Integer> first = map.firstEntry(); // z=0\nSequencedMap<String, Integer> rev = map.reversed();  // b, a, z\n```',tech:"java",displayNumber:300,sortKey:653},{id:"java-654",number:301,title:"What are the main memory areas of the JVM?",question:"What are the main memory areas of the JVM?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`The JVM divides memory into several runtime data areas. The most important are:
- **Heap**: shared across all threads; holds all object instances and arrays. This is where GC happens.
- **Stack (Java Virtual Machine Stack)**: per-thread; holds frames with local variables, operand stack, and return info.
- **Metaspace** (since Java 8, replacing PermGen): stores class metadata (methods, bytecode, symbols).
- **Code Cache**: holds native code produced by the JIT compiler.
- **Program Counter (PC) Register**: per-thread; points to the current instruction.
- **Native Method Stack**: for native (JNI) method calls.

Thread-private areas (PC, stack, native stack) are reclaimed when the thread ends. Heap and metaspace are shared and managed by GC.

\`\`\`java
// Inspect memory areas using JVM flags at startup:
// java -Xmx512m -Xms512m -XX:MaxMetaspaceSize=128m -XX:+PrintGCDetails MyApp
//
// Programmatic peek at the runtime memory:
public class MemoryAreas {
    public static void main(String[] args) {
        Runtime rt = Runtime.getRuntime();
        System.out.println("Max heap:  " + rt.maxMemory()  / 1024 / 1024 + " MB");
        System.out.println("Total heap:" + rt.totalMemory()/ 1024 / 1024 + " MB");
        System.out.println("Free heap: " + rt.freeMemory() / 1024 / 1024 + " MB");
        long threadId = Thread.currentThread().getId(); // own PC + stack
        System.out.println("Thread id: " + threadId);
    }
}
\`\`\``,tech:"java",displayNumber:301,sortKey:654},{id:"java-655",number:302,title:"What is the difference between the heap and the stack?",question:"What is the difference between the heap and the stack?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`- **Heap** is shared by all threads and holds objects/arrays. Allocation is dynamic; lifetime is managed by GC. Access is slower and it can throw \`OutOfMemoryError\`.
- **Stack** is per-thread; each method call pushes a frame holding local primitives and object references (not the objects themselves). It is LIFO, fast, and fixed-size per thread (\`-Xss\`). Excessive recursion throws \`StackOverflowError\`.

A common confusion: a \`List<String>\` reference lives on the stack; the \`ArrayList\` object and its elements live on the heap.

\`\`\`java
public class HeapVsStack {
    public static void main(String[] args) {
        int x = 10;                 // primitive 'x' -> stack
        StringBuilder sb = new StringBuilder(); // reference on stack, object on heap
        sb.append("hello");
    }
    // Deep recursion exhausts the STACK, not the heap:
    static void recurse(int n) { recurse(n + 1); } // -> StackOverflowError
}
\`\`\``,tech:"java",displayNumber:302,sortKey:655},{id:"java-656",number:303,title:"What is Metaspace and how does it differ from PermGen?",question:"What is Metaspace and how does it differ from PermGen?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"Before Java 8, class metadata lived in a fixed-size **PermGen** (part of the heap), causing `OutOfMemoryError: PermGen space`. Since Java 8, **Metaspace** uses native memory (outside the heap), auto-growing by default up to the OS limit (or `-XX:MaxMetaspaceSize`). This removed most PermGen OOMs. Metaspace is still collected by GC, but only when classes are unloaded (e.g., when their classloader is collected).\n\nKey flags: `-XX:MetaspaceSize` (initial threshold that triggers GC), `-XX:MaxMetaspaceSize`, `-XX:MinMetaspaceFreeRatio`.\n\n```text\n// Java 7 (PermGen):                      Java 8+ (Metaspace):\n// -XX:PermSize=64m                        -XX:MetaspaceSize=64m\n// -XX:MaxPermSize=256m                    -XX:MaxMetaspaceSize=256m\n// String pool + class meta in heap        Class meta in native memory\n// -> OutOfMemoryError: PermGen space      -> auto-grows, bounded by MaxMetaspaceSize\n```",tech:"java",displayNumber:303,sortKey:656},{id:"java-657",number:304,title:"What is the Code Cache and what fills it?",question:"What is the Code Cache and what fills it?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'The **Code Cache** is a native-memory region holding machine code compiled by the JIT compilers (C1/C2). When a method is "hot" enough, the JIT compiles its bytecode into optimized native code, which is stored here and executed instead of interpreted bytecode. If the code cache fills (`-XX:ReservedCodeCacheSize`, default ~240 MB), the JIT stops compiling and the JVM falls back to the slower interpreter — a subtle performance cliff.\n\nFlags: `-XX:ReservedCodeCacheSize`, `-XX:+UseCodeCacheFlushing`.\n\n```text\n// Tune the code cache when you have MANY hot methods (meta-frameworks, big apps):\n// java -XX:ReservedCodeCacheSize=512m -XX:+UseCodeCacheFlushing MyApp\n//\n// Watch with JIT logging:\n// java -XX:+PrintCompilation -XX:+UnlockDiagnosticVMOptions -XX:+PrintCodeCache MyApp\n```',tech:"java",displayNumber:304,sortKey:657},{id:"java-658",number:305,title:"What are Compressed OOPs and why do they matter?",question:"What are Compressed OOPs and why do they matter?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**Compressed Ordinary Object Pointers (Compressed OOPs)** encode 64-bit heap pointers into 32-bit values by assuming all objects are 8-byte aligned. This shifts the pointer left by 3 bits, so 32 bits can address up to 32 GB (2³² × 8). Benefits: smaller object headers and references → better CPU cache density and throughput. Enabled by default on 64-bit JVMs when heap < ~32 GB. Beyond 32 GB the JVM disables them, making each reference 8 bytes — so a 32 GB heap can actually hold *fewer* objects than a 31 GB one. This is the famous "heap looks bigger but holds less" effect.

Flag: \`-XX:+UseCompressedOops\` (default on).

\`\`\`text
// Sweet spot: keep heap under 32 GB to keep compressed oops.
// java -Xmx31g -XX:+UseCompressedOops MyApp   // oops remain 32-bit
// java -Xmx40g                                // compressed oops auto-disabled
//
// Verify it is on:
// java -Xmx31g -XX:+PrintFlagsFinal -version | grep UseCompressedOops
// -> bool UseCompressedOops = true
\`\`\``,tech:"java",displayNumber:305,sortKey:658},{id:"java-659",number:306,title:"What is the object header and how does it affect memory?",question:"What is the object header and how does it affect memory?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Every Java object has a header: a **mark word** (8 bytes: hashcode, GC age, locking state, biased-lock info) and a **klass pointer** (8 bytes, or 4 bytes with compressed oops). So an empty \`new Object()\` is 16 bytes (8 + 4 + 4 padding) with compressed oops, or 16 bytes (8 + 8) without. Arrays add a 4-byte length field. The header overhead matters a lot for small objects (e.g., an \`Integer\` is 16 bytes overhead for 4 bytes of data). This is why object churn and tiny wrappers are expensive.

\`\`\`java
import java.lang.instrument.Instrumentation;

public class ObjectSize {
    // Add: java -javaagent:sizeof.jar -XX:+UseCompressedOops MyApp
    static Instrumentation inst;
    public static void premain(String a, Instrumentation i) { inst = i; }
    public static void main(String[] args) {
        System.out.println("Object:    " + inst.getObjectSize(new Object()));    // 16
        System.out.println("Integer:   " + inst.getObjectSize(Integer.valueOf(1)));// 16
        System.out.println("int[0]:    " + inst.getObjectSize(new int[0]));        // 16 (header+len+pad)
    }
}
\`\`\``,tech:"java",displayNumber:306,sortKey:659},{id:"java-660",number:307,title:"What is the difference between a Java process's native memory and the Java heap?",question:"What is the difference between a Java process's native memory and the Java heap?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`The **Java heap** is only one consumer of a process's total memory. The OS sees the whole process, which also includes: thread stacks (\`-Xss\` each, ×number of threads), Metaspace, Code Cache, GC control structures, direct ByteBuffers (\`-XX:MaxDirectMemorySize\`), JIT, and the JVM native libraries. A process can OOM on the OS even when the heap is not full — the classic "why is my 4 GB heap using 8 GB RSS?" question. Tools: \`jcmd <pid> VM.native_memory summary\` (Native Memory Tracking).

\`\`\`text
// Enable Native Memory Tracking and inspect:
// java -XX:NativeMemoryTracking=detail -XX:MaxDirectMemorySize=256m MyApp
// jcmd <pid> VM.native_memory summary
//
// Sample output categories:
//   Java Heap    (reserved/committed)
//   Class        (metaspace)
//   Thread       (stacks)
//   Code         (code cache)
//   Internal     (direct buffers, GC)
\`\`\``,tech:"java",displayNumber:307,sortKey:660},{id:"java-661",number:308,title:"What are escape analysis and scalar replacement?",question:"What are escape analysis and scalar replacement?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**Escape analysis** (C2, enabled via \`-XX:+DoEscapeAnalysis\`, default on) determines whether an object's reference escapes the method/thread. If a newly-created object does not escape, the JIT can: (1) **stack-allocate** it instead of heap-allocating, and (2) **scalar-replace** it — break it into its individual fields (scalars) held in registers/locals, eliminating the object entirely. This removes allocation and GC pressure. If the object *does* escape (returned, stored in a field, passed to another thread), it must be heap-allocated.

\`\`\`java
public class EscapeAnalysis {
    // 'p' does not escape -> scalar replacement / stack allocation possible.
    static int sum(int a, int b) {
        Point p = new Point(a, b); // may be eliminated entirely
        return p.x + p.y;
    }
    static Point leak(int a, int b) {
        return new Point(a, b);    // escapes -> heap allocated
    }
    static class Point { int x, y; Point(int x,int y){this.x=x;this.y=y;} }
}
// Demonstrate allocation elimination:
// java -XX:+PrintGCDetails -XX:-DoEscapeAnalysis EscapeAnalysis   // more GC
// java -XX:+PrintGCDetails -XX:+DoEscapeAnalysis EscapeAnalysis   // fewer/faster
\`\`\``,tech:"java",displayNumber:308,sortKey:661},{id:"java-662",number:309,title:"How is memory laid out for a multithreaded program in the JVM?",question:"How is memory laid out for a multithreaded program in the JVM?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Each thread gets its own **PC register** and **JVM stack** (with one frame per nested method call). Threads share the **heap**, **metaspace**, and **code cache**. The stack size is set by \`-Xss\` (default ~1 MB on Linux). Too many threads × stack size → native \`OutOfMemoryError: unable to create new native thread\`. Work-stealing / GC threads also consume native memory. Local variables of primitive type are thread-private; object references on the stack point into the shared heap.

\`\`\`java
public class ThreadMemory {
    static int shared = 0; // in heap (class static field)
    public static void main(String[] args) throws Exception {
        Runnable r = () -> {
            int local = 42;          // on THIS thread's stack
            System.out.println(local);
        };
        Thread t = new Thread(r);
        t.start(); t.join();
        // -Xss controls per-thread stack: java -Xss256k MyApp
    }
}
\`\`\``,tech:"java",displayNumber:309,sortKey:662},{id:"java-663",number:310,title:"What is a memory barrier / happens-before and how does it relate to the JVM memory model?",question:"What is a memory barrier / happens-before and how does it relate to the JVM memory model?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`The **JVM Memory Model (JMM, JSR-133)** defines **happens-before** rules guaranteeing one thread's writes are visible to another. It is implemented with **memory barriers** (CPU/fence instructions) inserted by the JIT around volatile fields, synchronized blocks, and \`final\` fields. Key rules: program order (within a thread), monitor lock (unlock happens-before subsequent lock), volatile write happens-before volatile read, thread start/join, and \`final\` field safe publication. Without a happens-before edge, the JIT/CPU may reorder or cache reads, causing subtle bugs. This is about *visibility and ordering*, not just heap/stack layout.

\`\`\`java
public class HappensBefore {
    private volatile boolean ready = false;
    private int value = 0;

    void writer() {
        value = 42;          // write
        ready = true;        // volatile write -> barrier: flushes 'value'
    }
    void reader() {
        if (ready) {         // volatile read -> sees prior writes
            System.out.println(value); // always 42
        }
    }
}
\`\`\``,tech:"java",displayNumber:310,sortKey:663},{id:"java-664",number:311,title:"What is object reachability and how does GC determine live objects?",question:"What is object reachability and how does GC determine live objects?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`An object is **reachable** if it can be accessed through a chain of references starting from **GC roots**. Unreachable objects are considered garbage and eligible for collection. Reachability is computed by a **mark** phase that traverses the object graph from roots; anything not marked is dead. Note "eligible" ≠ "collected" — collection happens later during a GC cycle. \`System.gc()\` is only a *suggestion*.

\`\`\`java
public class Reachability {
    public static void main(String[] args) {
        Object a = new Object(); // 'a' is a root reference -> reachable
        Object b = a;            // b points to same object
        a = null;
        // object still reachable via 'b'
        b = null;
        // now unreachable -> eligible for GC on next cycle
    }
}
\`\`\``,tech:"java",displayNumber:311,sortKey:664},{id:"java-665",number:312,title:"What are GC roots?",question:"What are GC roots?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`**GC roots** are the starting points of the reachability graph. They include:
- Local variables and parameters on active thread stacks.
- Active Java threads themselves.
- Static fields of loaded classes.
- JNI (native) references / global JNI handles.
- Synchronized monitors (objects held as locks).
- Objects referenced by the JVM itself (e.g., classloaders, some internal structures).

Anything reachable by following references from these roots is live; everything else is garbage.

\`\`\`java
public class GCRoots {
    static Object staticRoot = new Object(); // GC root: static field (class-level)
    public static void main(String[] args) {
        Object localRoot = new Object();     // GC root: local var on stack
        synchronized (localRoot) {           // monitor held -> also a root
            // while here, localRoot is strongly reachable
        }
    }
}
\`\`\``,tech:"java",displayNumber:312,sortKey:665},{id:"java-666",number:313,title:"Describe the Mark-Sweep-Compact algorithm.",question:"Describe the Mark-Sweep-Compact algorithm.",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Three classic phases:
- **Mark**: traverse from GC roots, mark every reachable object.
- **Sweep**: walk the heap, reclaim memory of unmarked (dead) objects, adding them to a free list.
- **Compact**: move live objects together to eliminate fragmentation, updating references.

Mark-Sweep alone leaves fragmentation; Compact fixes it but is expensive (must update all pointers). This trio underlies most collectors' old-generation handling.

\`\`\`text
Mark-Sweep-Compact lifecycle:
  Heap before: [L][D][L][D][D][L]   (L=live, D=dead)
  Mark:        [L][ ][L][ ][ ][L]
  Sweep:       [L][_][L][_][_][L]   (free list grows)
  Compact:     [L][L][L][_][_][_]   (no fragmentation, refs updated)
\`\`\``,tech:"java",displayNumber:313,sortKey:666},{id:"java-667",number:314,title:"What is the difference between strong, soft, weak, and phantom references?",question:"What is the difference between strong, soft, weak, and phantom references?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Java has four reference strengths, giving you varying control over GC eligibility:
- **Strong** (\`new Object()\`): never collected while reachable.
- **Soft** (\`SoftReference\`): collected only when the JVM *needs* memory; great for caches.
- **Weak** (\`WeakReference\`): collected on the next GC regardless; basis of \`WeakHashMap\`.
- **Phantom** (\`PhantomReference\`): enqueued after finalization for post-mortem cleanup; must go through a \`ReferenceQueue\` and doesn't give access to the object.

\`\`\`java
import java.lang.ref.*;
import java.util.WeakHashMap;

public class References {
    public static void main(String[] args) {
        Object strong = new Object();
        SoftReference<Object> soft = new SoftReference<>(new Object());
        WeakReference<Object> weak = new WeakReference<>(new Object());
        PhantomReference<Object> phantom = new PhantomReference<>(
            new Object(), new ReferenceQueue<>());

        System.gc();
        System.out.println(soft.get());  // maybe non-null
        System.out.println(weak.get());  // almost certainly null now
        System.out.println(phantom.get()); // ALWAYS null

        WeakHashMap<Object,String> cache = new WeakHashMap<>();
        cache.put(new Object(), "v"); // entry vanishes when key is GC'd
    }
}
\`\`\``,tech:"java",displayNumber:314,sortKey:667},{id:"java-668",number:315,title:"What is finalization and why is it discouraged?",question:"What is finalization and why is it discouraged?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`\`finalize()\` was a hook called by the GC before reclaiming an object. It is discouraged because: (1) calls are unpredictable and may never run (fast exit, no GC); (2) objects with a non-trivial \`finalize()\` are promoted to a finalization queue, surviving an extra GC cycle (hurts performance); (3) it can resurrect objects; (4) it serializes cleanup on a single thread. Modern Java uses **try-with-resources** / \`AutoCloseable\` and \`java.lang.ref.Cleaner\` / \`PhantomReference\` instead. \`finalize()\` is deprecated since Java 9 and will be removed.

\`\`\`java
import java.lang.ref.Cleaner;

public class NoFinalize {
    private static final Cleaner cleaner = Cleaner.create();
    private final Cleaner.Cleanable cleanable;
    private final Resource res;

    NoFinalize() {
        res = new Resource();
        cleanable = cleaner.register(this, res::close); // runs when GC'd
    }
    // Prefer explicit close(); Cleaner is a fallback only.
    public void close() { cleanable.clean(); }

    static class Resource { void close() { System.out.println("cleaned"); } }

    // DON'T: protected void finalize() { ... } // deprecated, slow, unreliable
}
\`\`\``,tech:"java",displayNumber:315,sortKey:668},{id:"java-669",number:316,title:"What is the generational hypothesis?",question:"What is the generational hypothesis?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"The **weak generational hypothesis** observes that most objects die young (short-lived), and few references point from old objects to young ones. The JVM exploits this by splitting the heap into **young** and **old** generations, collecting the young generation frequently and cheaply (most objects die there), and the old generation rarely. This makes GC far more efficient than a single monolithic heap scan.\n\n```text\nGenerational hypothesis (empirically ~98% of objects die young):\n  Young Gen: collected often, fast, cheap (most garbage here)\n  Old Gen:   collected rarely, slow, expensive (long-lived survivors)\n```",tech:"java",displayNumber:316,sortKey:669},{id:"java-670",number:317,title:"Describe the young generation layout: Eden and Survivor spaces.",question:"Describe the young generation layout: Eden and Survivor spaces.",difficulty:"Basic",category:"JVM Architecture & GC",answer:`The **young generation** is split into:
- **Eden**: where new objects are allocated.
- **Survivor spaces (S0, S1)**: hold objects that survived a young GC. Only one survivor space is active at a time; a young GC copies live Eden + active survivor objects into the inactive survivor space (or promotes them to old gen if too old/full). This is a **copying collector** — Eden is emptied each young GC. Default ratio: young ≈ 1/3 of heap, Eden : survivors ≈ 8 : 1 : 1.

\`\`\`text
Young Gen layout (default):
  +---------+-------+-------+
  |  Eden   |  S0   |  S1   |
  | 80%     | 10%   | 10%   |
  +---------+-------+-------+
After a Minor GC: live objects copied S0->S1 (or promoted to Old), Eden+S0 cleared.
\`\`\``,tech:"java",displayNumber:317,sortKey:670},{id:"java-671",number:318,title:"What is a Minor GC, Major GC, and Full GC?",question:"What is a Minor GC, Major GC, and Full GC?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Minor GC**: collects the **young generation** only. Frequent, fast, stop-the-world.
- **Major GC**: collects the **old generation** only (terminology varies by collector).
- **Full GC**: collects **entire heap + metaspace**. Rare, slow, long pauses.
In HotSpot, "Major GC" and "Full GC" are often conflated; the logs (\`-Xlog:gc*\`) are the source of truth. A Full GC is what you want to avoid in latency-sensitive apps.

\`\`\`text
// Modern GC logging (Java 9+ unified logging):
// java -Xlog:gc*:file=gc.log:time,uptime,level -jar MyApp.jar
//
// Sample:
// [0.3s] GC(0) Pause Young (Allocation Failure) 25M->5M(100M) 12ms   <- Minor
// [1.2s] GC(3) Pause Full (Ergonomics)           90M->40M(512M) 220ms <- Full
\`\`\``,tech:"java",displayNumber:318,sortKey:671},{id:"java-672",number:319,title:"What is object tenuring and the tenuring threshold?",question:"What is object tenuring and the tenuring threshold?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Objects that survive multiple young GCs are eventually **tenured** (promoted) to the old generation. The **tenuring threshold** is the number of young-GC survivals before promotion (tracked as an object's "age" in its header, max 15). \`-XX:MaxTenuringThreshold\` sets the cap. Premature promotion floods the old gen; overly high thresholds keep objects bouncing between survivor spaces. Adaptive sizing (\`-XX:+UseAdaptiveSizePolicy\`, default) tunes this automatically.

\`\`\`text
// java -XX:MaxTenuringThreshold=15 -XX:+PrintTenuringDistribution MyApp
//
// Log shows age histogram each minor GC:
// Desired survivor size 5M, new threshold 15 (max 15)
// - age   1:   2M bytes,   2M total
// - age   2: 500K bytes, 2.5M total
// Objects reaching threshold -> promoted to Old Gen.
\`\`\``,tech:"java",displayNumber:319,sortKey:672},{id:"java-673",number:320,title:"What is a promotion failure and how does it cause a Full GC?",question:"What is a promotion failure and how does it cause a Full GC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`During a Minor GC, surviving young objects must be promoted into the old generation. If the old gen lacks contiguous space (fragmentation) or is nearly full, a **promotion failure** occurs. The JVM then triggers a **Full GC** to compact the old gen and make room — turning a cheap minor pause into a long full pause. This is a classic cause of unpredictable latency spikes. Mitigations: larger old gen, lower promotion rate, or a collector with concurrent old-gen collection (G1/ZGC/Shenandoah).

\`\`\`text
// Promotion failure symptom in logs:
// [2.1s] GC(5) Pause Young (Allocation Failure)
// [2.1s] GC(5) Promotion failed
// [2.1s] GC(6) Pause Full (Allocation Failure) ... long pause
//
// Fix paths:
//  - increase old gen (lower -XX:NewRatio)
//  - reduce allocation rate (object reuse, pooling)
//  - switch to G1/ZGC with concurrent old-gen collection
\`\`\``,tech:"java",displayNumber:320,sortKey:673},{id:"java-674",number:321,title:"Compare Serial, Parallel, CMS, G1, ZGC, and Shenandoah collectors.",question:"Compare Serial, Parallel, CMS, G1, ZGC, and Shenandoah collectors.",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"- **Serial** (`-XX:+UseSerialGC`): single-threaded; young + old. Simplest, lowest overhead for tiny heaps/CLI tools.\n- **Parallel (Throughput)** (`-XX:+UseParallelGC`): multi-threaded young + old, focuses on max throughput, longer STW pauses.\n- **CMS** (`-XX:+UseConcMarkSweepGC`, removed in Java 14): concurrent old-gen marking to reduce pause, but fragments and can fall back to Full GC. Deprecated.\n- **G1** (`-XX:+UseG1GC`, default since Java 9): region-based, incremental, predictable pause goals via `-XX:MaxGCPauseMillis`.\n- **ZGC** (`-XX:+UseZGC`, production since Java 15): concurrent, sub-millisecond pauses regardless of heap size (TB-scale), uses colored pointers + load barriers.\n- **Shenandoah** (OpenJDK, `-XX:+UseShenandoahGC`): concurrent compaction, low pauses, region-based like G1 but with concurrent move.\n\n```text\n// Select a collector:\n// java -XX:+UseSerialGC        MyApp   // tiny apps, single core\n// java -XX:+UseParallelGC      MyApp   // batch/throughput\n// java -XX:+UseG1GC            MyApp   // balanced default (Java 9+)\n// java -XX:+UseZGC             MyApp   // low-latency, large heaps\n// java -XX:+UseShenandoahGC    MyApp   // low-latency (Red Hat/OpenJDK)\n//\n// CMS removed in JDK 14 -> do not use in modern Java.\n```",tech:"java",displayNumber:321,sortKey:674},{id:"java-675",number:322,title:"How does the Serial GC work?",question:"How does the Serial GC work?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"Serial GC uses a **single thread** for both young (copying) and old (mark-sweep-compact) collections, pausing all application threads (STW) during each. It has the smallest footprint and simplest implementation, making it ideal for small heaps (< 100 MB), single-processor machines, or short-lived CLI tools. It is a poor choice for multi-core servers where parallel collectors shine.\n\n```text\n// Enable Serial GC:  java -XX:+UseSerialGC -Xmx64m MyApp\n// Best for: embedded, CLI utilities, constrained containers.\n// Avoid for: web servers, multi-core, latency-sensitive services.\n```",tech:"java",displayNumber:322,sortKey:675},{id:"java-676",number:323,title:"How does the Parallel (Throughput) GC work?",question:"How does the Parallel (Throughput) GC work?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'Parallel GC (a.k.a. "Throughput Collector") uses **multiple threads** for both minor and full GCs, maximizing throughput (work done per unit time) at the cost of longer stop-the-world pauses. It was the default in Java 8. Key tunables: `-XX:ParallelGCThreads`, and adaptive sizing goals `-XX:MaxGCPauseMillis` and `-XX:GCTimeRatio` (e.g., 99 = 1% time in GC).\n\n```text\n// java -XX:+UseParallelGC -XX:ParallelGCThreads=8 -XX:GCTimeRatio=99 MyApp\n//   GCTimeRatio=99 -> GC time <= 1% of total (throughput goal)\n//   MaxGCPauseMillis=N -> hint for pause goal (best-effort)\n// Goal: maximize throughput, accept longer STW pauses.\n```',tech:"java",displayNumber:323,sortKey:676},{id:"java-677",number:324,title:"How does the Concurrent Mark Sweep (CMS) collector work and why was it removed?",question:"How does the Concurrent Mark Sweep (CMS) collector work and why was it removed?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`CMS collected the **old generation concurrently** with the application to minimize pause times. Phases: initial mark (STW, short) → concurrent mark → remark (STW, short) → concurrent sweep. It did **not** compact, so the old gen fragmented over time, eventually forcing a long stop-the-world **Full GC** to defragment. Combined with its complexity and bugginess, it was deprecated in Java 9 and **removed in Java 14**, superseded by G1/ZGC/Shenandoah.

\`\`\`text
// Deprecated/removed:  java -XX:+UseConcMarkSweepGC MyApp
//   error: 'CMS' is deprecated and will likely be removed in a future release
//   removed entirely in JDK 14.
// Problems: old-gen fragmentation -> promotion failures -> Full GC.
// Modern replacement: G1 (default) or ZGC/Shenandoah for low latency.
\`\`\``,tech:"java",displayNumber:324,sortKey:677},{id:"java-678",number:325,title:"How does G1 GC organize the heap and why is it the default?",question:"How does G1 GC organize the heap and why is it the default?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:'**G1 (Garbage-First)** splits the heap into ~2048 equal **regions** (each 1–32 MB), each dynamically tagged as Eden, Survivor, Old, or Humongous (for large objects > half a region). Instead of collecting generations wholesale, G1 tracks region **liveness** and collects the regions with the most garbage first ("garbage-first"). It can meet a **pause-time goal** (`-XX:MaxGCPauseMillis`, default 200 ms) by limiting how many regions it evacuates per cycle, using a **mixed collection** that reclaims both young and some old regions. It compacts concurrently-ish (evacuation), avoiding fragmentation. Default since Java 9.\n\n```text\n// G1 region view:\n// [E][E][S][O][H][E][O][O][E][S]...   each ~1-32 MB\n// Collector picks high-garbage regions first to hit pause goal.\n// java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -XX:G1HeapRegionSize=4m MyApp\n```',tech:"java",displayNumber:325,sortKey:678},{id:"java-679",number:326,title:"What is a humongous object in G1 and how is it handled?",question:"What is a humongous object in G1 and how is it handled?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`In G1, an object larger than **half a region** is a **humongous object**, allocated directly into one or more contiguous **Humongous regions** (outside young/old). Humongous objects are expensive: they are collected only during a Full GC or a **concurrent marking / mixed cycle**, and can fragment the heap quickly. Allocating many short-lived large arrays is a known G1 anti-pattern. Mitigation: increase region size (\`-XX:G1HeapRegionSize\`) or reduce large allocations.

\`\`\`java
public class Humongous {
    public static void main(String[] args) {
        // A 3 MB array in a 4 MB region -> humongous (needs > 2 MB)
        byte[] big = new byte[3 * 1024 * 1024]; // allocated in humongous regions
        // Many of these churn -> G1 pressure; consider pooling or bigger regions.
    }
}
// java -Xmx1g -XX:+UseG1GC -XX:G1HeapRegionSize=8m Humongous
// Logs: "Humongous regions: N" with -Xlog:gc*=info
\`\`\``,tech:"java",displayNumber:326,sortKey:679},{id:"java-680",number:327,title:"How does ZGC achieve sub-millisecond pauses?",question:"How does ZGC achieve sub-millisecond pauses?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`ZGC's hallmark is **concurrent** collection: almost all work (marking, relocation/compaction, reference remapping) happens *while the app runs*, with only tiny (sub-ms) STW pauses for root scanning. Two key mechanisms:
- **Colored pointers**: 64-bit pointers embed metadata (mark, remap, relocated bits) in unused high bits, so the GC can track object state without modifying the object.
- **Load barriers**: every object load is intercepted to fix up references pointing to relocated objects (self-healing pointers).
This keeps pause times independent of heap size — from MBs to multi-TB.

\`\`\`text
// java -XX:+UseZGC -Xmx16g -XX:+UnlockExperimentalVMOptions MyApp  (JDK 11-15 exp)
// java -XX:+UseZGC -Xmx16g MyApp                                (JDK 15+ prod)
//
// ZGC phases (all concurrent except tiny pauses):
//   - Concurrent Mark     (colored pointers + load barriers)
//   - Concurrent Relocate (move objects, leave forwarding info)
//   - Load barrier fixes stale refs on access (self-healing)
// Result: typical pauses < 1-2 ms regardless of heap size.
\`\`\``,tech:"java",displayNumber:327,sortKey:680},{id:"java-681",number:328,title:"How does Shenandoah GC differ from G1 and ZGC?",question:"How does Shenandoah GC differ from G1 and ZGC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Shenandoah is a **low-pause, concurrent compaction** collector (originally Red Hat, now in OpenJDK). Like G1 it uses regions, but its differentiator is **concurrent evacuation** of old regions (G1 only evacuates young + selected old in mixed GCs; Shenandoah moves live objects concurrently with the app). It uses a **Brooks pointer** (a forwarding pointer field in each object) plus read/write barriers to make concurrent moves safe. ZGC achieves similar goals with colored pointers + load barriers instead. Both target < 10 ms pauses; Shenandoah historically ran on a wider range of JDK versions and heaps.

\`\`\`text
// java -XX:+UseShenandoahGC -Xmx8g MyApp
//
// Shenandoah phases (concurrent):
//   Init Mark (STW) -> Concurrent Mark -> Final Mark (STW)
//   Concurrent Compaction (evacuate regions concurrently)
//   Brooks pointer + barriers handle in-flight refs.
// vs ZGC: colored-pointer load barrier vs Brooks-pointer R/W barrier.
\`\`\``,tech:"java",displayNumber:328,sortKey:681},{id:"java-682",number:329,title:"What is the String Deduplication feature in G1?",question:"What is the String Deduplication feature in G1?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"**String deduplication** (`-XX:+UseStringDeduplication`, G1-only) scans the heap for equal `String` contents and makes them share the same underlying `char[]`/`byte[]` (the strings themselves remain distinct objects, only the backing array is merged). This can significantly cut heap usage for apps with many duplicate strings (e.g., XML/JSON parsers). It adds minor overhead during GC and only applies to strings that have survived a young GC (to avoid deduplicating short-lived strings).\n\n```text\n// java -XX:+UseG1GC -XX:+UseStringDeduplication -XX:+PrintStringDeduplicationStatistics MyApp\n//\n// Trade-off: small GC CPU overhead for potentially large memory savings.\n// Only with G1 (and Shenandoah has a similar feature).\n// Not a substitute for interning when you need identity equality.\n```",tech:"java",displayNumber:329,sortKey:682},{id:"java-683",number:330,title:"What are the trade-offs between throughput-oriented and latency-oriented collectors?",question:"What are the trade-offs between throughput-oriented and latency-oriented collectors?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Throughput-oriented** (Parallel): maximize application work per second; accept longer STW pauses. Best for batch jobs, offline processing, where total runtime matters more than individual pause.
- **Latency-oriented** (G1, ZGC, Shenandoah): minimize individual pause times to keep response times predictable; accept some throughput overhead (concurrent work, barriers). Best for interactive services, trading systems, user-facing apps.
Choose based on SLAs: if p99 latency matters → ZGC/G1; if batch throughput matters → Parallel.

\`\`\`text
Throughput vs latency decision:
  Batch / ETL / CI        -> Parallel GC (max work/sec)
  Web/API with SLA p99    -> G1 (default, ~100-200ms) or ZGC (<2ms)
  HFT / real-time service -> ZGC or Shenandoah

Tune intent, not just flags:
  Parallel: -XX:GCTimeRatio=99
  G1/ZGC:   -XX:MaxGCPauseMillis=50
\`\`\``,tech:"java",displayNumber:330,sortKey:683},{id:"java-684",number:331,title:"How do you size the heap with -Xms and -Xmx?",question:"How do you size the heap with -Xms and -Xmx?",difficulty:"Basic",category:"JVM Architecture & GC",answer:"- `-Xms` sets the **initial** heap size; `-Xmx` the **maximum**. The JVM starts at `-Xms` and grows up to `-Xmx` as needed.\n- For server apps, set **`-Xms` = `-Xmx`** to avoid runtime resizing pauses and to commit all memory up front (more predictable). For containers, don't exceed the container memory limit or the OOM killer will terminate the process.\n- Also consider `-XX:MaxRAMPercentage` (Java 10+) to size the heap relative to the container's RAM automatically.\n\n```text\n// Fixed heap (avoids resize pauses):\n// java -Xms4g -Xmx4g MyApp\n//\n// Container-aware (Java 10+): use a % of container RAM\n// java -XX:MaxRAMPercentage=75.0 -XX:InitialRAMPercentage=50.0 MyApp\n//\n// NEVER: -Xmx larger than the container limit -> OOM kill by the OS.\n```",tech:"java",displayNumber:331,sortKey:684},{id:"java-685",number:332,title:"How do you set a GC pause-time goal?",question:"How do you set a GC pause-time goal?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Pause-time goals tell the collector to prefer shorter pauses, often by doing less work per cycle:
- **G1**: \`-XX:MaxGCPauseMillis=200\` (default 200 ms) — a *soft* goal; G1 shrinks young gen to meet it.
- **ZGC**: \`-XX:MaxGCPauseMillis=10\` (default ~none hard, inherently low).
- **Parallel**: also accepts the flag but it's a hint; throughput collector prioritizes overall time.
The goal is best-effort: the JVM will trade throughput/heap headroom to approach it but won't violate correctness. Setting it unrealistically low forces very frequent, tiny collections.

\`\`\`text
// G1 targeting 100 ms pauses:
// java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -Xmx8g MyApp
//
// Watch realized pauses in logs:
// java -Xlog:gc*:MyApp  -> "Pause Young ... 45ms" vs goal 100ms
// If real pauses >> goal, either lower allocation rate or accept the goal.
\`\`\``,tech:"java",displayNumber:332,sortKey:685},{id:"java-686",number:333,title:"What is the NewRatio and how does it affect GC?",question:"What is the NewRatio and how does it affect GC?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"`-XX:NewRatio=N` sets the ratio of **old generation to young generation** (old : young = N : 1). Default is 2 (old is 2× young, so young ≈ 1/3 of heap). A **larger NewRatio** shrinks the young gen → fewer but longer minor GCs, more promotion. A **smaller NewRatio** enlarges young gen → more frequent but cheaper minor GCs, with more room for objects to die young. Tuned for allocation-heavy vs long-lived workloads. (G1 also accepts `-XX:NewSize`/`-XX:MaxNewSize` but manages regions adaptively.)\n\n```text\n// -XX:NewRatio=2  -> old:young = 2:1  (young = 1/3 heap, default)\n// -XX:NewRatio=1  -> old:young = 1:1  (young = 1/2 heap, more room to die young)\n// -XX:NewRatio=3  -> young smaller, more promotion to old\n//\n// java -Xmx6g -XX:NewRatio=2 -XX:+UseParallelGC MyApp\n```",tech:"java",displayNumber:333,sortKey:686},{id:"java-687",number:334,title:"What GC logging options should you enable in production?",question:"What GC logging options should you enable in production?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"Use **unified JVM logging** (`-Xlog`) introduced in Java 9 (retire the old `-XX:+PrintGCDetails`, removed in Java 9+):\n- `-Xlog:gc*:file=gc.log:time,uptime,level:filecount=5,filesize=10M` — full GC detail, rotated logs.\n- For overhead-sensitive prod, log at `info` (not `debug`/`trace`).\n- Add `-Xlog:safepoint*` to see STW pause sources beyond GC.\nAlways capture GC logs in production — they are the primary forensic tool for OOMs and latency spikes, with negligible overhead at `info`.\n\n```text\n// Recommended production GC logging:\n// java -Xlog:gc*,gc+heap=info,safepoint:file=/var/log/myapp/gc-%t.log:time,tid,level:filecount=10,filesize=20M -jar MyApp.jar\n//\n// Parse with: GCViewer, GCEasy (gceasy.io), or Prometheus + jmx_exporter.\n// Key metrics: pause time, frequency, heap before/after, promotion rate.\n```",tech:"java",displayNumber:334,sortKey:687},{id:"java-688",number:335,title:"How do you read a GC log line (G1 example)?",question:"How do you read a GC log line (G1 example)?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`A typical G1 \`info\` line:
\`[2026-08-20T10:00:01.234+0000][gc,start] GC(12) Pause Young (Normal) (G1 Evacuation Pause)\`
\`[gc ] GC(12) Eden: 200.0M(200.0M)->0.0B(220.0M) Survivors: 0.0B->20.0M Heap: 600.0M(1.0G)->420.0M(1.0G)\`
\`[gc ] GC(12) Pause Young (Normal) (G1 Evacuation Pause) 12.345ms\`
Reading it: a **young evacuation pause** collected Eden (200M→0) and reduced total heap (600M→420M) in **12.3 ms**. "Heap: before->after (capacity)" tells you live set and headroom. Mixed collections show Old reclaimed too.

\`\`\`text
Fields to watch:
  Pause Young / Mixed / Full  -> which collection
  Eden: A->B (C)              -> used before->after (capacity)
  Heap: A->B (C)              -> total used before->after (max)
  final "Xms" number          -> STW pause duration (tune against goal)
//
// High 'after' Heap with frequent Full GCs => memory leak / heap too small.
\`\`\``,tech:"java",displayNumber:335,sortKey:688},{id:"java-689",number:336,title:"What is survivor space sizing and the TargetSurvivorRatio?",question:"What is survivor space sizing and the TargetSurvivorRatio?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"The **survivor spaces** hold age-1+ objects between young GCs. `-XX:TargetSurvivorRatio` (default 50) is the desired occupancy of a survivor space; if live objects exceed it after a young GC, the **tenuring threshold is lowered** so more objects promote to old gen early, preventing survivor overflow. This is part of G1/Parallel adaptive sizing. If survivors overflow, objects get promoted prematurely even if young — a promotion-rate problem. Tuning is usually left to the adaptive policy unless you see unwanted early promotions.\n\n```text\n// java -XX:TargetSurvivorRatio=50 -XX:MaxTenuringThreshold=15 MyApp\n//\n// If logs show tenuring threshold dropping to 1 quickly:\n//   -> survivor spaces too small OR objects live too long\n//   -> raise young gen (lower -XX:NewRatio) to give them room to die young.\n```",tech:"java",displayNumber:336,sortKey:689},{id:"java-690",number:337,title:"How does -XX:+UseAdaptiveSizePolicy work?",question:"How does -XX:+UseAdaptiveSizePolicy work?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:"Enabled by default with Parallel and G1, **UseAdaptiveSizePolicy** lets the JVM continuously retune generation sizes, tenuring threshold, and survivor ratios at runtime to meet the pause-time (`MaxGCPauseMillis`) and throughput (`GCTimeRatio`) goals. It reacts to live-set and allocation-rate measurements after each GC. Practical implication: manual `-Xmn`/survivor tuning is often counterproductive because the policy will override it; set high-level goals and let it adapt, only pinning sizes when you have strong evidence.\n\n```text\n// Let the JVM adapt (default):\n// java -XX:+UseParallelGC -XX:+UseAdaptiveSizePolicy -XX:GCTimeRatio=99 MyApp\n//\n// Disable to pin your own sizes (experts only):\n// java -XX:-UseAdaptiveSizePolicy -Xmn2g -XX:SurvivorRatio=8 MyApp\n//   (-Xmn sets fixed young gen = Eden+Survivors)\n```",tech:"java",displayNumber:337,sortKey:690},{id:"java-691",number:338,title:"What is GC throughput vs latency and how do you measure them?",question:"What is GC throughput vs latency and how do you measure them?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **Throughput** = (time doing app work) / (total time). High throughput = GC overhead low (e.g., 99% app time). Measured via \`-XX:GCTimeRatio\` target or from logs (total GC time / uptime).
- **Latency** = length of individual STW pauses (and their frequency). Measured as p50/p95/p99 pause times from GC logs.
A collector optimizing throughput (Parallel) may have long pauses; one optimizing latency (ZGC) sacrifices some throughput to keep pauses tiny. You cannot maximize both — pick per SLA. Measure with GC logs + Prometheus/Grafana or GCEasy.

\`\`\`text
// Throughput target:
//   -XX:GCTimeRatio=99  => GC <= ~1% of time
// Latency target:
//   -XX:MaxGCPauseMillis=50
//
// From logs compute:
//   throughput = 100 * (1 - totalGCTime/uptime)%
//   p99 latency = 99th percentile of individual pause durations
\`\`\``,tech:"java",displayNumber:338,sortKey:691},{id:"java-692",number:339,title:"What is the class loading process (loading, linking, initialization)?",question:"What is the class loading process (loading, linking, initialization)?",difficulty:"Basic",category:"JVM Architecture & GC",answer:`Class loading has three phases:
1. **Loading**: find the \`.class\` bytes (via classloader) and create a \`Class\` object in metaspace.
2. **Linking**: (a) **verification** — bytecode is valid/safe; (b) **preparation** — static fields get default zero values and memory; (c) **resolution** — symbolic references resolved to direct (can be lazy).
3. **Initialization**: run static initializers and static field assignments (\`<clinit>\`), under a lock so only one thread initializes a class.
A class is usable only after initialization completes.

\`\`\`java
public class LoadingPhases {
    static int x = compute();          // runs during INITIALIZATION
    static int compute() { return 42; }
    static { System.out.println("initialized"); } // <clinit>

    public static void main(String[] args) throws Exception {
        // Loading + linking happen lazily on first active use:
        Class<?> c = Class.forName("LoadingPhases"); // triggers init
        System.out.println(c.getSimpleName());
    }
}
\`\`\``,tech:"java",displayNumber:339,sortKey:692},{id:"java-693",number:340,title:"What are the built-in class loaders and their hierarchy?",question:"What are the built-in class loaders and their hierarchy?",difficulty:"Basic",category:"JVM Architecture & GC",answer:'Three built-in loaders, parent-child by delegation:\n- **Bootstrap (Primordial)**: loads core JDK classes from `jmods`/`rt.jar` (C++ implemented, `null` from Java).\n- **Platform/Extension** (`PlatformClassLoader` in Java 9+, formerly `ExtClassLoader`): loads JDK modules/platform classes.\n- **Application/System** (`AppClassLoader`): loads your app classes and classpath entries.\nCustom loaders extend `ClassLoader` and delegate to a parent. The hierarchy is **not** inheritance but a `parent` reference chain.\n\n```java\npublic class Loaders {\n    public static void main(String[] args) {\n        ClassLoader app = Loaders.class.getClassLoader();\n        System.out.println("App:      " + app);                     // AppClassLoader\n        System.out.println("Platform: " + app.getParent());         // PlatformClassLoader\n        System.out.println("Bootstrap:" + app.getParent().getParent()); // null\n    }\n}\n```',tech:"java",displayNumber:340,sortKey:693},{id:"java-694",number:341,title:"Explain the delegation model (parent-first) of class loading.",question:"Explain the delegation model (parent-first) of class loading.",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:'The **parent-delegation model**: when asked to load class `C`, a loader first delegates to its **parent**; only if the parent cannot find `C` does the loader attempt to load it itself. This prevents duplication and protects core classes (you cannot replace `java.lang.String` because Bootstrap loads it first). It enforces namespace isolation and security. Violating it (e.g., loading a core class in a child) throws `SecurityException` / `LinkageError` ("attempted duplicate class definition").\n\n```java\n// Pseudocode of loadClass:\nprotected Class<?> loadClass(String name, boolean resolve) {\n    Class<?> c = findLoadedClass(name);          // 1. already loaded?\n    if (c == null) {\n        try { c = parent.loadClass(name); }      // 2. delegate UP first\n        catch (ClassNotFoundException e) {\n            c = findClass(name);                 // 3. only now, load self\n        }\n    }\n    if (resolve) resolveClass(c);\n    return c;\n}\n```',tech:"java",displayNumber:341,sortKey:694},{id:"java-695",number:342,title:"How do you write a custom ClassLoader?",question:"How do you write a custom ClassLoader?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`Extend \`ClassLoader\` and override \`findClass(String name)\` to locate and define bytes (never break delegation by overriding \`loadClass\` unless you intend to). Read the \`.class\` bytes, call \`defineClass(...)\`, and the JVM handles linking/initialization. Common uses: hot-reload, plugin isolation, loading from non-file sources (network, encrypted jars), and per-tenant isolation. Each classloader defines its own namespace, so the same class loaded by two loaders are distinct types.

\`\`\`java
import java.io.IOException;
import java.nio.file.Files;
import java.nio.file.Path;

public class CustomLoader extends ClassLoader {
    private final Path dir;
    CustomLoader(ClassLoader parent, Path dir) { super(parent); this.dir = dir; }

    @Override
    protected Class<?> findClass(String name) throws ClassNotFoundException {
        try {
            byte[] bytes = Files.readAllBytes(dir.resolve(name.replace('.', '/') + ".class"));
            return defineClass(name, bytes, 0, bytes.length); // links + prepares
        } catch (IOException e) { throw new ClassNotFoundException(name, e); }
    }
}
// Usage: new CustomLoader(getClass().getClassLoader(), Path.of("/plugins")).loadClass("com.x.Plugin");
\`\`\``,tech:"java",displayNumber:342,sortKey:695},{id:"java-696",number:343,title:"What is a ClassNotFoundException vs NoClassDefFoundError?",question:"What is a ClassNotFoundException vs NoClassDefFoundError?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`- **ClassNotFoundException** is a **checked exception** thrown when an *explicit* load is attempted at runtime (e.g., \`Class.forName\`, \`loadClass\`, reflection, deserialization) and the class is not found.
- **NoClassDefFoundError** is an **Error** (unchecked) thrown when the JVM *expected* a class to already be linked/initialized (e.g., it was present at compile time) but cannot find its definition at runtime — often because a static initializer threw, or the class vanished from the classpath after compile.
Roughly: explicit dynamic load fails → exception; implicit runtime reference fails → error.

\`\`\`java
public class CNFvsNCD {
    public static void main(String[] args) throws Exception {
        try {
            Class.forName("DoesNotExist"); // explicit -> ClassNotFoundException
        } catch (ClassNotFoundException e) { System.out.println("checked: " + e); }

        Missing m = new Missing(); // if Missing failed to link/init -> NoClassDefFoundError
    }
}
// NoClassDefFoundError commonly caused by: missing JAR at runtime,
// a thrown exception in a static initializer of the class.
\`\`\``,tech:"java",displayNumber:343,sortKey:696},{id:"java-697",number:344,title:"What is the danger of classloader leaks in long-running apps?",question:"What is the danger of classloader leaks in long-running apps?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`A **classloader leak** happens when a long-lived object (e.g., a static field in a core classloader, a thread, or a JNDI/registry entry) holds a reference to a class or instance loaded by a child (e.g., webapp) classloader. That keeps the entire child classloader — and all its loaded classes and their static state — reachable, so metaspace never gets collected even after the webapp is "undeployed." Repeated redeploys then exhaust Metaspace (\`OutOfMemoryError: Metaspace\`). Common culprits: unclosed threads, \`ThreadLocal\` not removed, JDBC drivers registered in a parent, static caches. Fix: remove refs on shutdown, use \`ThreadLocal.remove()\`, clean up registries.

\`\`\`java
public class Leak {
    // Anti-pattern: static ThreadLocal in a shared (parent) classloader
    // holding a value from a child (webapp) classloader -> leak on redeploy.
    private static final ThreadLocal<byte[]> CACHE = ThreadLocal.withInitial(() -> new byte[1024*1024]);

    void use() {
        CACHE.get();
        // MUST: CACHE.remove() when done, or the webapp classloader is pinned.
    }
}
// Detect via: jmap -clstats <pid>  (classes per loader) or Eclipse MAT on a heap dump.
\`\`\``,tech:"java",displayNumber:344,sortKey:697},{id:"java-698",number:345,title:"What are the C1 and C2 JIT compilers?",question:"What are the C1 and C2 JIT compilers?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`The HotSpot JVM uses two JIT compilers:
- **C1 (Client)**: fast to compile, performs light optimizations. Used for quick startup and less-hot methods.
- **C2 (Server)**: slower to compile but applies aggressive, heavy optimizations (inlining, loop unrolling, escape analysis, vectorization). Used for hot methods where the compile cost pays off.
The JVM starts in **interpreted** mode and compiles methods to native code once they're "hot" (called enough times, tracked by invocation/branch counters). C2 produces much faster code than C1.

\`\`\`text
// Force a single compiler if needed:
// java -Xint            MyApp   // interpreter only (slow, for debugging)
// java -Xcomp           MyApp   // compile everything eagerly (slow startup)
// java -client / -server (legacy selects C1/C2 bias;modern JDKs ignore)
//
// Normal: tiered (C1 then C2) is the default and best for most apps.
\`\`\``,tech:"java",displayNumber:345,sortKey:698},{id:"java-699",number:346,title:"What is tiered compilation?",question:"What is tiered compilation?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:`**Tiered compilation** (\`-XX:+TieredCompilation\`, default on since Java 8) combines C1 and C2: a method is first compiled quickly by **C1** at low optimization levels (so it runs fast without long warmup), then, if it stays hot, recompiled by **C2** at high optimization. This gives fast startup (C1) plus peak performance (C2) without waiting for full C2 compilation. Levels: 0 = interpreter, 1-3 = C1 (increasing opts), 4 = C2. Watch with \`-XX:+PrintCompilation\`.

\`\`\`java
public class Tiered {
    public static void main(String[] args) {
        long sum = 0;
        for (int i = 0; i < 1_000_000; i++) {       // loop gets C1 then C2
            sum += compute(i);
        }
        System.out.println(sum);
    }
    static long compute(long x) { return x * x + 1; } // inlined by C2 when hot
}
// java -XX:+TieredCompilation -XX:+PrintCompilation Tiered
\`\`\``,tech:"java",displayNumber:346,sortKey:699},{id:"java-700",number:347,title:"How does the JVM decide what to compile (hot methods, counters)?",question:"How does the JVM decide what to compile (hot methods, counters)?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Each method/branch has **invocation counters** and **back-edge (loop) counters**. When a counter crosses a threshold (\`-XX:CompileThreshold\`, default 10,000 for C2 under non-tiered; tiered uses different level-trigger points), the method is queued for JIT compilation. Because counters are **decayed** (halved periodically), bursty methods don't stay permanently "hot." Once compiled, future calls use the native code; the interpreter still runs until the on-stack replacement (OSR) or next invocation. This is why there's a **warmup period** before peak performance in benchmarks.

\`\`\`text
// Tune how "hot" a method must be (non-tiered baseline):
// java -XX:CompileThreshold=5000 MyApp   // compile sooner
//
// Watch compilation decisions:
// java -XX:+PrintCompilation -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining MyApp
//   Output: "123  3  %      Tiered.run @ 2 (MyApp)"  (level 3 C1, OSR %)
//
// Key lesson: measure AFTER warmup, or you benchmark the interpreter.
\`\`\``,tech:"java",displayNumber:347,sortKey:700},{id:"java-701",number:348,title:"What is On-Stack Replacement (OSR)?",question:"What is On-Stack Replacement (OSR)?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`**On-Stack Replacement** lets the JVM switch a *long-running method already on the stack* from interpreted to compiled code **in the middle of its execution** — most importantly for hot loops. Without OSR, a method already running a tight loop would keep interpreting until it returned and was called again. OSR compiles the loop body (with a special entry point) and transfers control mid-execution. This is why a single long-running loop still gets optimized without needing to restart.

\`\`\`java
public class OSR {
    public static void main(String[] args) {
        long sum = 0;
        // This loop may be interpreted first, then OSR-swapped to compiled
        // mid-loop once the back-edge counter trips:
        for (long i = 0; i < 2_000_000_000L; i++) {
            sum += i;
        }
        System.out.println(sum);
    }
}
// java -XX:+PrintCompilation OSR   -> look for '%' (OSR) compile entries.
\`\`\``,tech:"java",displayNumber:348,sortKey:701},{id:"java-702",number:349,title:"What are the common types of OutOfMemoryError and their causes?",question:"What are the common types of OutOfMemoryError and their causes?",difficulty:"Intermediate",category:"JVM Architecture & GC",answer:"- `java.lang.OutOfMemoryError: Java heap space` — live objects exceed `-Xmx`; true leak or heap too small.\n- `: GC overhead limit exceeded` — >98% of time in GC but <2% heap reclaimed; thrashing.\n- `: Metaspace` — too many classes / classloader leak; raise `-XX:MaxMetaspaceSize`.\n- `: Unable to create new native thread` — too many threads or `-Xss` too big vs OS limits.\n- `: Direct buffer memory` — off-heap `ByteBuffer.allocateDirect` exceeds `-XX:MaxDirectMemorySize`.\n- `: Requested array size exceeds VM limit` — attempted huge array (`> Integer.MAX_VALUE-headers`).\n- `: Out of swap space` / `: heap dump` — native/OS memory exhaustion.\n\n```text\n// Reproduce a few:\n// java -Xmx32m OOMJavaHeap      -> Java heap space\n// java -XX:MaxMetaspaceSize=8m ManyClasses -> Metaspace\n// java -XX:MaxDirectMemorySize=10m BigDirect -> Direct buffer memory\n//\n// Diagnose: capture heap dump on OOM and analyze:\n// java -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/tmp/dump.hprof MyApp\n```",tech:"java",displayNumber:349,sortKey:702},{id:"java-703",number:350,title:"What profiling and diagnostic tools are available for JVM memory/GC?",question:"What profiling and diagnostic tools are available for JVM memory/GC?",difficulty:"Advanced",category:"JVM Architecture & GC",answer:`Built-in (JDK/bin):
- **jstat** — GC/heap/compilation stats live (\`jstat -gcutil <pid> 1s\`).
- **jmap** — heap histogram, \`jmap -dump:live,format=b,file=d.hprof <pid>\`, \`-clstats\`.
- **jcmd** — Swiss army knife: \`VM.native_memory\`, \`GC.heap_dump\`, \`Thread.print\`, \`VM.flags\`.
- **jstack** — thread dumps / deadlock detection.
- **jconsole / jvisualvm / Java Mission Control (JMC)** — GUI monitoring, flight recordings (JFR).
- **jfr** — low-overhead Java Flight Recorder for production profiling.
Third-party: **Eclipse MAT** (heap dump analysis, leak suspects), **async-profiler** (CPU/alloc/lock), **GCViewer/GCEasy** (GC logs), **Prometheus + jmx_exporter** (metrics).

\`\`\`text
// Live GC view:
// jstat -gcutil <pid> 1000
//   S0  S1  E   O   M    YGC YGCT FGC FGCT GCT
//   0  12  45  60  92    120 3.4  5  2.1 5.5
//
// Heap dump + analyze:
// jmap -dump:live,format=b,file=/tmp/d.hprof <pid>
//   -> open in Eclipse MAT, run "Leak Suspects Report"
//
// Continuous low-overhead profiling:
// java -XX:StartFlightRecording=duration=60s,filename=rec.jfr MyApp
//   -> open in JMC
//
// Allocation profiling (no GC needed):
// asprof -e alloc -d 30 -f alloc.html <pid>
\`\`\``,tech:"java",displayNumber:350,sortKey:703},{id:"java-704",number:351,title:"What is Inversion of Control (IoC) in the Spring Framework?",question:"What is Inversion of Control (IoC) in the Spring Framework?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Inversion of Control (IoC) is a design principle where the responsibility of creating and managing objects is handed over from the application code to the Spring container. Instead of classes instantiating their own dependencies with \`new\`, the container creates them and "wires" them together. This reduces coupling, improves testability, and centralizes object lifecycle management. The Spring IoC container reads configuration metadata (annotations, Java config, or XML) to know which beans to create and how to inject dependencies.

\`\`\`java
// Tight coupling (no IoC)
class OrderService {
    private PaymentService payment = new PaymentService(); // manual creation
}

// Loose coupling with IoC
@Component
class OrderService {
    private final PaymentService payment;
    public OrderService(PaymentService payment) { this.payment = payment; }
}
\`\`\``,tech:"java",displayNumber:351,sortKey:704},{id:"java-705",number:352,title:"What is Dependency Injection (DI) and what are its types?",question:"What is Dependency Injection (DI) and what are its types?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Dependency Injection is the implementation mechanism of IoC: the container supplies an object's dependencies rather than the object constructing them. Spring supports three injection styles:

1. **Constructor Injection** – dependencies passed via the constructor. Best for required, immutable dependencies.
2. **Setter Injection** – dependencies set via setter methods. Best for optional or reconfigurable dependencies.
3. **Field Injection** – dependencies injected directly into fields via \`@Autowired\`. Convenient but discouraged (harder to test, hides dependencies).

Spring recommends constructor injection for mandatory dependencies.

\`\`\`java
@Component
class UserService {
    private final UserRepository repo;          // constructor
    private EmailService email;                 // setter

    public UserService(UserRepository repo) { this.repo = repo; }

    @Autowired
    public void setEmailService(EmailService email) { this.email = email; }

    @Autowired private AuditService audit;      // field (discouraged)
}
\`\`\``,tech:"java",displayNumber:352,sortKey:705},{id:"java-706",number:353,title:"What is the difference between Constructor, Setter and Field injection?",question:"What is the difference between Constructor, Setter and Field injection?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **Constructor**: Dependencies are final/immutable, injected at creation, ideal for required beans, enables easy unit testing without a Spring context, and prevents partially-initialized objects. This is the recommended approach.
- **Setter**: Allows optional dependencies and post-construction reconfiguration; useful when a dependency is optional or circular references exist.
- **Field**: Shortest syntax but cannot be used for \`final\` fields, makes unit testing without reflection difficult, and hides the class's dependencies. Spring itself discourages it.

\`\`\`java
// Preferred: constructor injection
@Service
public class AccountService {
    private final AccountRepository repo;
    public AccountService(AccountRepository repo) { this.repo = repo; }
}
\`\`\``,tech:"java",displayNumber:353,sortKey:706},{id:"java-707",number:354,title:"What is the Spring IoC container and the difference between BeanFactory and ApplicationContext?",question:"What is the Spring IoC container and the difference between BeanFactory and ApplicationContext?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"The **IoC container** is the core of Spring and is responsible for instantiating, configuring, and assembling beans. There are two main interfaces:\n\n- **BeanFactory**: The basic container; lazy-instantiates beans on first request. Lightweight, suitable for resource-constrained environments.\n- **ApplicationContext**: A superset of BeanFactory that adds enterprise features: event propagation, internationalization (MessageSource), AOP integration, and eager singleton instantiation by default. This is what most applications use (e.g., `AnnotationConfigApplicationContext`, `ClassPathXmlApplicationContext`).\n\n```java\nApplicationContext ctx =\n    new AnnotationConfigApplicationContext(AppConfig.class);\nUserService svc = ctx.getBean(UserService.class);\n```",tech:"java",displayNumber:354,sortKey:707},{id:"java-708",number:355,title:"How does Spring resolve which bean to inject when multiple candidates exist?",question:"How does Spring resolve which bean to inject when multiple candidates exist?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`When multiple beans of the same type exist, Spring throws a \`NoUniqueBeanDefinitionException\`. You resolve ambiguity with:
- \`@Primary\` – marks one bean as the default choice.
- \`@Qualifier("name")\` – explicitly names the bean to inject.
- Custom qualifier annotations for type-safe selection.

\`\`\`java
@Primary @Component
class MySQLRepo implements UserRepository {}

@Component @Qualifier("mongo")
class MongoRepo implements UserRepository {}

@Autowired
public UserService(@Qualifier("mongo") UserRepository repo) { ... }
\`\`\``,tech:"java",displayNumber:355,sortKey:708},{id:"java-709",number:356,title:"What is `@Component` and how does component scanning work?",question:"What is `@Component` and how does component scanning work?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Component` is a generic stereotype annotation that marks a class as a Spring-managed bean. Component scanning (enabled by `@ComponentScan` or `@SpringBootApplication`) automatically detects classes annotated with `@Component` (and its specializations) on the classpath and registers them as beans. The default scan starts from the package of the configuration class and recurses downward.\n\n```java\n@Configuration\n@ComponentScan("com.example.service")\npublic class AppConfig {}\n\n@Component\npublic class NotificationService {}\n```',tech:"java",displayNumber:356,sortKey:709},{id:"java-710",number:357,title:"What is the difference between `@Component`, `@Service`, `@Repository` and `@Controller`?",question:"What is the difference between `@Component`, `@Service`, `@Repository` and `@Controller`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"All four are stereotypes that register a class as a bean. The difference is semantic and behavioral:\n- `@Component`: generic stereotype.\n- `@Service`: marks a service-layer/business-logic class.\n- `@Repository`: marks a persistence-layer class; additionally enables automatic translation of persistence exceptions into Spring's `DataAccessException` hierarchy.\n- `@Controller`: marks a web MVC controller that returns views; `@RestController` is its REST variant returning data directly.\n\nUsing the specific annotations conveys intent and unlocks targeted features (e.g., AOP pointcuts by stereotype, exception translation for repositories).\n\n```java\n@Repository public class UserRepository { /* DAO */ }\n@Service public class UserService { /* business logic */ }\n@RestController public class UserController { /* REST endpoints */ }\n```",tech:"java",displayNumber:357,sortKey:710},{id:"java-711",number:358,title:"What is `@Configuration` and `@Bean`?",question:"What is `@Configuration` and `@Bean`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@Configuration` marks a class as a source of bean definitions. Methods annotated with `@Bean` inside it return objects that Spring registers as beans in the container. This is Java-based configuration (an alternative to XML). It is used for third-party classes you cannot annotate, or when you need explicit control over bean construction. `@Configuration` classes use CGLIB proxying so that calling `@Bean` methods returns the same singleton instance.\n\n```java\n@Configuration\npublic class AppConfig {\n    @Bean\n    public DataSource dataSource() {\n        return new HikariDataSource();\n    }\n}\n```",tech:"java",displayNumber:358,sortKey:711},{id:"java-712",number:359,title:"Explain the Spring Bean Lifecycle.",question:"Explain the Spring Bean Lifecycle.",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"The bean lifecycle is:\n1. Container reads configuration and instantiates the bean.\n2. Dependencies are injected (populate properties).\n3. If `BeanNameAware`, `BeanFactoryAware`, etc., callbacks fire.\n4. `BeanPostProcessor.postProcessBeforeInitialization` runs.\n5. `@PostConstruct` / `InitializingBean.afterPropertiesSet` runs.\n6. `BeanPostProcessor.postProcessAfterInitialization` runs (AOP proxies are applied here).\n7. Bean is ready for use.\n8. On container shutdown: `@PreDestroy` / `DisposableBean.destroy` runs.\n\n```java\n@Component\npublic class CacheManager {\n    @PostConstruct public void init() { loadCache(); }\n    @PreDestroy public void cleanup() { flushCache(); }\n}\n```",tech:"java",displayNumber:359,sortKey:712},{id:"java-713",number:360,title:"How do you customize bean initialization and destruction?",question:"How do you customize bean initialization and destruction?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Three ways:\n1. JSR-250 annotations `@PostConstruct` and `@PreDestroy` (recommended, least coupled).\n2. `InitializingBean` / `DisposableBean` interfaces (couples to Spring).\n3. `initMethod` / `destroyMethod` attributes on `@Bean` (useful for third-party classes).\n\nOrder: interface methods run before annotation methods, which run before `initMethod`.\n\n```java\n@Bean(initMethod = "start", destroyMethod = "stop")\npublic Server server() { return new Server(); }\n```',tech:"java",displayNumber:360,sortKey:713},{id:"java-714",number:361,title:"What are the different Spring Bean scopes?",question:"What are the different Spring Bean scopes?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **singleton** (default): one shared instance per Spring container.
- **prototype**: a new instance is created every time it is requested.
- **request**: one instance per HTTP request (web only).
- **session**: one instance per HTTP session (web only).
- **application**: one instance per \`ServletContext\` (web only).
- **websocket**: one instance per WebSocket session.

Use prototype for stateful beans; singleton for stateless services.

\`\`\`java
@Component @Scope("prototype")
public class Command { /* stateful per use */ }

@Component @Scope("singleton") // default
public class ConfigService {}
\`\`\``,tech:"java",displayNumber:361,sortKey:714},{id:"java-715",number:362,title:"What is the difference between singleton and prototype scope?",question:"What is the difference between singleton and prototype scope?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"A **singleton** bean is instantiated once and the same instance is injected everywhere — ideal for stateless services. A **prototype** bean is created fresh on every lookup/injection — ideal for stateful objects. Caveat: when a singleton depends on a prototype, the prototype is injected only once (at the singleton's creation). To get a new prototype per call, use `ObjectFactory`/`Provider` or lookup method injection.\n\n```java\n@Autowired private ObjectFactory<Command> commandFactory;\npublic void run() { Command c = commandFactory.getObject(); } // new each time\n```",tech:"java",displayNumber:362,sortKey:715},{id:"java-716",number:363,title:"What are request and session scopes in Spring?",question:"What are request and session scopes in Spring?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"**request** scope creates a new bean instance for each HTTP request; the bean lives for the duration of that request. **session** scope creates one instance per user HTTP session, shared across requests from that user. Both require a web-aware `ApplicationContext` and are commonly used for storing per-user or per-request state (e.g., a shopping cart as a session bean). You often must use `ObjectFactory`/`Provider` when injecting these into singletons.\n\n```java\n@Component @Scope(value = WebApplicationContext.SCOPE_SESSION, proxyMode = ScopedProxyMode.TARGET_CLASS)\npublic class ShoppingCart {}\n```",tech:"java",displayNumber:363,sortKey:716},{id:"java-717",number:364,title:"What is `@Lazy` and when would you use it?",question:"What is `@Lazy` and when would you use it?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@Lazy` defers bean instantiation until it is first requested rather than at container startup. It is used to:\n- Reduce startup time for large applications.\n- Resolve circular dependency issues in some cases.\n- Avoid initializing expensive resources until needed.\n\nYou can apply it on `@Bean` definitions, `@Component`, or on injection points.\n\n```java\n@Bean @Lazy\npublic HeavyReportService heavyReportService() { return new HeavyReportService(); }\n```",tech:"java",displayNumber:364,sortKey:717},{id:"java-718",number:365,title:"What is `@Primary` and `@Qualifier`?",question:"What is `@Primary` and `@Qualifier`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'Both resolve ambiguity when multiple beans of the same type exist. `@Primary` designates a default bean to use when no specific qualifier is given. `@Qualifier` names the exact bean to inject. `@Qualifier` takes precedence over `@Primary` and is more explicit. You can also create custom qualifier annotations for type-safe selection.\n\n```java\n@Primary @Bean public PaymentGateway paypal() { return new Paypal(); }\n@Bean @Qualifier("stripe") public PaymentGateway stripe() { return new Stripe(); }\n\n@Autowired @Qualifier("stripe") PaymentGateway gateway;\n```',tech:"java",displayNumber:365,sortKey:718},{id:"java-719",number:366,title:"Explain `@Autowired`, `@Resource` and `@Inject`.",question:"Explain `@Autowired`, `@Resource` and `@Inject`.",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'- `@Autowired` (Spring-specific): default by type; falls back to name; supports `required=false`.\n- `@Resource` (JSR-250): default by name, then by type; comes from Java EE.\n- `@Inject` (JSR-330): like `@Autowired` but no `required` attribute; portable across DI frameworks.\n\nFor portability, prefer `@Inject`; for Spring-only apps, `@Autowired` is fine.\n\n```java\n@Autowired private OrderRepository repo;   // by type\n@Resource(name="userRepo") private UserRepository ur; // by name\n@Inject private AuditService audit;        // JSR-330\n```',tech:"java",displayNumber:366,sortKey:719},{id:"java-720",number:367,title:"What is `@Value` and how is it used?",question:"What is `@Value` and how is it used?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Value` injects values into fields, constructor params, or method params from properties files, environment variables, system properties, or SpEL expressions. It supports `${property:default}` placeholder syntax and `#{spEL}` expressions.\n\n```java\n@Value("${app.timeout:30}")\nprivate int timeout;\n\n@Value("#{systemProperties[\'user.home\']}")\nprivate String homeDir;\n\n@Value("${app.name}")\nprivate String appName;\n```',tech:"java",displayNumber:367,sortKey:720},{id:"java-721",number:368,title:"What is Spring Boot and how does it differ from Spring?",question:"What is Spring Boot and how does it differ from Spring?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Spring is the core framework providing IoC, DI, AOP, etc. **Spring Boot** is a layer on top that simplifies setup: it offers opinionated defaults, embedded servers (Tomcat/Netty), starter dependencies, auto-configuration, and production-ready features (metrics, health checks). It reduces boilerplate and lets you create a runnable app with minimal configuration.

\`\`\`java
@SpringBootApplication
public class MyApp {
    public static void main(String[] args) {
        SpringApplication.run(MyApp.class, args);
    }
}
\`\`\``,tech:"java",displayNumber:368,sortKey:721},{id:"java-722",number:369,title:"Explain `@SpringBootApplication`.",question:"Explain `@SpringBootApplication`.",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@SpringBootApplication` is a convenience annotation that combines three annotations:\n- `@Configuration` – marks the class as a bean source.\n- `@EnableAutoConfiguration` – enables Spring Boot's auto-configuration.\n- `@ComponentScan` – scans the package and sub-packages for components.\n\nIt is typically placed on the main application class. Component scanning starts from its package, so the main class should live in a base package above all others.\n\n```java\n@SpringBootApplication // = @Configuration + @EnableAutoConfiguration + @ComponentScan\npublic class Application { public static void main(String[] a){ SpringApplication.run(Application.class,a);} }\n```",tech:"java",displayNumber:369,sortKey:722},{id:"java-723",number:370,title:"How does Spring Boot Auto-Configuration work?",question:"How does Spring Boot Auto-Configuration work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"Auto-configuration uses `@EnableAutoConfiguration`, which imports `AutoConfigurationImportSelector`. This selector reads `META-INF/spring.factories` (or `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` in newer versions) to find candidate auto-configuration classes. Each class is conditionally applied using `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`, etc. So a bean is auto-configured only if its required classes are on the classpath and the user hasn't defined their own.\n\n```java\n@Configuration\n@ConditionalOnClass(DataSource.class)\n@ConditionalOnMissingBean(DataSource.class)\npublic class DataSourceAutoConfiguration { /* configures HikariCP if none provided */ }\n```",tech:"java",displayNumber:370,sortKey:723},{id:"java-724",number:371,title:"What is the role of `spring.factories` / `AutoConfiguration.imports`?",question:"What is the role of `spring.factories` / `AutoConfiguration.imports`?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"These files register auto-configuration classes so Boot can discover them without component scanning. `META-INF/spring.factories` was the legacy mechanism (keyed by `org.springframework.boot.autoconfigure.EnableAutoConfiguration`). Boot 2.7+ deprecates it in favor of `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`, which simply lists fully-qualified class names, one per line. This improves startup performance and clarity.\n\n```text\n# META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports\ncom.example.autoconfig.MyServiceAutoConfiguration\n```",tech:"java",displayNumber:371,sortKey:724},{id:"java-725",number:372,title:"What are Spring Boot Starters?",question:"What are Spring Boot Starters?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Starters are curated dependency descriptors that bundle a group of libraries needed for a particular functionality, with compatible versions managed by the parent `spring-boot-starter-parent`. Examples: `spring-boot-starter-web` (web + Tomcat + Jackson), `spring-boot-starter-data-jpa`, `spring-boot-starter-security`, `spring-boot-starter-test`. They eliminate version conflicts and reduce `pom.xml`/`build.gradle` complexity.\n\n```xml\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n</dependency>\n```",tech:"java",displayNumber:372,sortKey:725},{id:"java-726",number:373,title:"How do you externalize configuration in Spring Boot?",question:"How do you externalize configuration in Spring Boot?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Spring Boot externalizes config via properties/yaml files, environment variables, command-line arguments, and more. The precedence order (low to high) includes: `application.properties`, profile-specific files, environment variables, and command-line arguments. You can use `@Value` or inject an `Environment`/`@ConfigurationProperties` object. Externalized config lets the same artifact run in different environments.\n\n```yaml\n# application.yml\nspring:\n  datasource:\n    url: jdbc:mysql://localhost:3306/db\n    username: ${DB_USER}\n```",tech:"java",displayNumber:373,sortKey:726},{id:"java-727",number:374,title:"What is `@ConfigurationProperties`?",question:"What is `@ConfigurationProperties`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@ConfigurationProperties` binds a hierarchical group of properties to a strongly-typed Java bean, supporting relaxed binding, validation (`@Validated`), and type conversion. It is superior to multiple `@Value` annotations for grouped, structured configuration. Enable it with `@EnableConfigurationProperties` or by annotating the class with `@Component`.\n\n```java\n@ConfigurationProperties(prefix = "app.mail")\n@Validated\npublic class MailProperties {\n    @NotBlank private String host;\n    private int port = 25;\n    // getters/setters\n}\n```',tech:"java",displayNumber:374,sortKey:727},{id:"java-728",number:375,title:"What is the difference between `@Value` and `@ConfigurationProperties`?",question:"What is the difference between `@Value` and `@ConfigurationProperties`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@Value` injects a single scalar value with SpEL support but no validation or structured binding. `@ConfigurationProperties` binds a whole subtree of properties to a POJO with type-safe access, validation, and relaxed binding (`app.mail-host` vs `app.mail.host`). Use `@Value` for a quick single value; use `@ConfigurationProperties` for feature/domain configuration.\n\n```java\n@Value("${app.mail.host}") private String host;        // single value\n@ConfigurationProperties("app.mail") MailProps props;  // structured\n```',tech:"java",displayNumber:375,sortKey:728},{id:"java-729",number:376,title:"What is the `application.properties` vs `application.yml` difference?",question:"What is the `application.properties` vs `application.yml` difference?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Both configure the application. `application.properties` uses `key=value` flat syntax; `application.yml` uses YAML hierarchical indentation, which is more readable for nested structures. YAML requires correct indentation (2 spaces) and does not allow tabs. YAML support is provided by the `snakeyaml` dependency bundled in Boot.\n\n```properties\n# properties\nserver.port=8081\nspring.datasource.url=jdbc:h2:mem:test\n```\n```yaml\n# yml\nserver:\n  port: 8081\nspring:\n  datasource:\n    url: jdbc:h2:mem:test\n```",tech:"java",displayNumber:376,sortKey:729},{id:"java-730",number:377,title:"How does Spring Boot handle profiles?",question:"How does Spring Boot handle profiles?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Profiles isolate environment-specific configuration and beans. You activate a profile via `spring.profiles.active` property, `SPRING_PROFILES_ACTIVE` env var, or `--spring.profiles.active=dev` argument. Configuration can live in `application-{profile}.yml`, and beans can be annotated with `@Profile("dev")` to load conditionally.\n\n```java\n@Bean @Profile("dev") public DataSource devDs() { return new H2(); }\n@Bean @Profile("prod") public DataSource prodDs() { return new Mysql(); }\n```\n```yaml\n# application-dev.yml\nspring:\n  datasource:\n    url: jdbc:h2:mem:devdb\n```',tech:"java",displayNumber:377,sortKey:730},{id:"java-731",number:378,title:"What is `@Profile` and how is it used?",question:"What is `@Profile` and how is it used?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@Profile` designates that a component, `@Bean` method, or `@Configuration` class should only be registered when the specified profile(s) are active. It accepts a single profile or an array, and supports negation (`!prod`). It is the mechanism for environment-specific wiring.\n\n```java\n@Configuration\n@Profile("test")\npublic class TestConfig {\n    @Bean public EmailService email() { return new NoOpEmailService(); }\n}\n```',tech:"java",displayNumber:378,sortKey:731},{id:"java-732",number:379,title:"What are the embedded servers in Spring Boot?",question:"What are the embedded servers in Spring Boot?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot embeds a servlet container so the app runs as a standalone JAR with no external server. For servlet stacks, **Tomcat** is the default (via `spring-boot-starter-web`); **Jetty** and **Undertow** can replace it. For reactive stacks (`spring-boot-starter-webflux`), **Netty** is the default. This enables fat-jar deployment and simpler operations.\n\n```xml\n<!-- swap Tomcat for Jetty -->\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-web</artifactId>\n  <exclusions><exclusion><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-tomcat</artifactId></exclusion></exclusions>\n</dependency>\n<dependency><groupId>org.springframework.boot</groupId><artifactId>spring-boot-starter-jetty</artifactId></dependency>\n```",tech:"java",displayNumber:379,sortKey:732},{id:"java-733",number:380,title:"How do you change the embedded server port?",question:"How do you change the embedded server port?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Set `server.port` in `application.properties`/`yml`. Use `server.port=0` to pick a random available port (useful in integration tests). You can also configure it programmatically via `WebServerFactoryCustomizer` or by setting the `SERVER_PORT` environment variable.\n\n```properties\nserver.port=9090\n# server.port=0 -> random port\n```",tech:"java",displayNumber:380,sortKey:733},{id:"java-734",number:381,title:"What is `@RestController`?",question:"What is `@RestController`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@RestController` is a convenience stereotype combining `@Controller` and `@ResponseBody`. It indicates that every handler method returns the serialized response body (typically JSON via Jackson) directly, rather than a view name. It is the primary annotation for building REST APIs in Spring Boot.\n\n```java\n@RestController\n@RequestMapping("/api/users")\npublic class UserController {\n    @GetMapping("/{id}")\n    public User getUser(@PathVariable Long id) { return service.findById(id); }\n}\n```',tech:"java",displayNumber:381,sortKey:734},{id:"java-735",number:382,title:"Explain `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`.",question:"Explain `@GetMapping`, `@PostMapping`, `@PutMapping`, `@DeleteMapping`.",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'These are composed mapping annotations (shortcuts over `@RequestMapping(method=...)`) that bind handler methods to specific HTTP methods:\n- `@GetMapping` → HTTP GET (read)\n- `@PostMapping` → HTTP POST (create)\n- `@PutMapping` / `@PatchMapping` → HTTP PUT/PATCH (update)\n- `@DeleteMapping` → HTTP DELETE (remove)\n\nThey support path patterns, consumes/produces media types, and headers.\n\n```java\n@PostMapping(consumes = "application/json", produces = "application/json")\npublic ResponseEntity<User> create(@RequestBody User user) { ... }\n```',tech:"java",displayNumber:382,sortKey:735},{id:"java-736",number:383,title:"What is `@PathVariable` and `@RequestParam`?",question:"What is `@PathVariable` and `@RequestParam`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@PathVariable` extracts a value from the URI template (e.g., `/users/{id}`). `@RequestParam` extracts query parameters (e.g., `/users?page=2`). `@PathVariable` is for resource identification in the path; `@RequestParam` is for optional filters, pagination, or sorting. Both support `required`, `defaultValue`, and type conversion.\n\n```java\n@GetMapping("/{id}")\npublic User get(@PathVariable Long id,\n                @RequestParam(defaultValue = "0") int page) { ... }\n```',tech:"java",displayNumber:383,sortKey:736},{id:"java-737",number:384,title:"What is `@RequestBody` and `@ResponseBody`?",question:"What is `@RequestBody` and `@ResponseBody`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@RequestBody` binds the HTTP request body (typically JSON) to a method argument using `HttpMessageConverter`s (Jackson by default). `@ResponseBody` writes the return value directly into the response body instead of a view. With `@RestController`, `@ResponseBody` is implicit on every method. These drive JSON serialization in REST APIs.\n\n```java\n@PostMapping\npublic User create(@RequestBody UserDto dto) {\n    return service.create(dto); // serialized to JSON automatically\n}\n```",tech:"java",displayNumber:384,sortKey:737},{id:"java-738",number:385,title:"What is `ResponseEntity` and when should you use it?",question:"What is `ResponseEntity` and when should you use it?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`\`ResponseEntity<T>\` represents the full HTTP response: status code, headers, and body. Use it when you need fine-grained control over the response — setting custom headers, cookies, status codes (201 Created, 204 No Content, 409 Conflict), or conditional responses. For simple cases, returning the POJO directly suffices.

\`\`\`java
@PostMapping
public ResponseEntity<User> create(@RequestBody User u) {
    User saved = service.save(u);
    URI loc = URI.create("/users/" + saved.getId());
    return ResponseEntity.created(loc).body(saved);
}
\`\`\``,tech:"java",displayNumber:385,sortKey:738},{id:"java-739",number:386,title:"How do you handle exceptions in Spring REST with `@ExceptionHandler`?",question:"How do you handle exceptions in Spring REST with `@ExceptionHandler`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@ExceptionHandler` annotated methods in a controller handle specific exceptions thrown by that controller's methods. For global handling, use `@ControllerAdvice`/`@RestControllerAdvice` with `@ExceptionHandler` to centralize error responses across all controllers. Return `ResponseEntity` with an appropriate status and a structured error body.\n\n```java\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(UserNotFoundException.class)\n    public ResponseEntity<ErrorDto> handle(UserNotFoundException ex) {\n        return ResponseEntity.status(404).body(new ErrorDto(ex.getMessage()));\n    }\n}\n```",tech:"java",displayNumber:386,sortKey:739},{id:"java-740",number:387,title:"What is `@ControllerAdvice` / `@RestControllerAdvice`?",question:"What is `@ControllerAdvice` / `@RestControllerAdvice`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'`@ControllerAdvice` is a specialization of `@Component` that allows you to apply `@ExceptionHandler`, `@InitBinder`, and `@ModelAttribute` methods globally across many controllers. `@RestControllerAdvice` adds `@ResponseBody`, making it ideal for REST APIs (returns JSON error bodies). It centralizes cross-cutting controller concerns.\n\n```java\n@RestControllerAdvice\npublic class ApiAdvice {\n    @ExceptionHandler(IllegalArgumentException.class)\n    public ResponseEntity<Map<String,String>> badReq(IllegalArgumentException ex) {\n        return ResponseEntity.badRequest().body(Map.of("error", ex.getMessage()));\n    }\n}\n```',tech:"java",displayNumber:387,sortKey:740},{id:"java-741",number:388,title:"What is `@ResponseStatus`?",question:"What is `@ResponseStatus`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:'`@ResponseStatus` maps an exception (or handler method) to a specific HTTP status code and optional reason. When placed on a custom exception class, Spring returns the given status whenever that exception is thrown. It is simpler than `@ExceptionHandler` for straightforward status mapping but offers no control over the response body.\n\n```java\n@ResponseStatus(value = HttpStatus.NOT_FOUND, reason = "User not found")\npublic class UserNotFoundException extends RuntimeException {}\n```',tech:"java",displayNumber:388,sortKey:741},{id:"java-742",number:389,title:"How do you validate request bodies in Spring?",question:"How do you validate request bodies in Spring?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Use Bean Validation (Jakarta Validation / Hibernate Validator). Annotate the DTO with constraints (`@NotNull`, `@Size`, `@Email`) and mark the controller parameter with `@Valid` (or `@Validated`). If validation fails, Spring throws `MethodArgumentNotValidException`, which you can catch in an `@ExceptionHandler` to return a 400 with field errors.\n\n```java\npublic record CreateUser(@NotBlank @Email String email, @Size(min=6) String password) {}\n\n@PostMapping\npublic ResponseEntity<Void> create(@Valid @RequestBody CreateUser req) { ... }\n```",tech:"java",displayNumber:389,sortKey:742},{id:"java-743",number:390,title:"What is Spring AOP and what problem does it solve?",question:"What is Spring AOP and what problem does it solve?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`Aspect-Oriented Programming (AOP) modularizes cross-cutting concerns (logging, security, transactions, monitoring) that cut across many classes. Instead of scattering this code, AOP lets you define an "aspect" that runs at defined points. Spring AOP uses dynamic proxies (JDK or CGLIB) and supports method-execution join points only.

\`\`\`java
@Aspect
@Component
public class LoggingAspect {
    @Before("execution(* com.example.service.*.*(..))")
    public void log(JoinPoint jp) { System.out.println("call " + jp.getSignature()); }
}
\`\`\``,tech:"java",displayNumber:390,sortKey:743},{id:"java-744",number:391,title:"What are Aspect, JoinPoint, Advice, and Pointcut?",question:"What are Aspect, JoinPoint, Advice, and Pointcut?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'- **Aspect**: a modularization of a cross-cutting concern (a class annotated `@Aspect`).\n- **Join point**: a point in program execution (in Spring, a method execution) where an aspect can be applied.\n- **Advice**: the action taken at a join point (`@Before`, `@After`, `@AfterReturning`, `@AfterThrowing`, `@Around`).\n- **Pointcut**: an expression that matches a set of join points (where the advice should run).\n\n```java\n@Aspect @Component\npublic class PerfAspect {\n    @Around("execution(* com.example.service.*.*(..))")  // pointcut\n    public Object time(ProceedingJoinPoint pjp) throws Throwable { // advice\n        long t = System.nanoTime();\n        Object r = pjp.proceed();                          // join point\n        System.out.println((System.nanoTime()-t)/1e6 + "ms");\n        return r;\n    }\n}\n```',tech:"java",displayNumber:391,sortKey:744},{id:"java-745",number:392,title:"What are the different types of Advice?",question:"What are the different types of Advice?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`- **@Before** – runs before the join point.
- **@After** – runs after the join point completes (always, like \`finally\`).
- **@AfterReturning** – runs after normal return; can access the return value.
- **@AfterThrowing** – runs if the method throws an exception.
- **@Around** – wraps the join point; can control whether it proceeds, modify arguments/return, and measure time. Most powerful.

\`\`\`java
@AfterReturning(pointcut="...", returning="result")
public void after(Object result) { /* use result */ }

@AfterThrowing(pointcut="...", throwing="ex")
public void onError(Exception ex) { /* handle */ }
\`\`\``,tech:"java",displayNumber:392,sortKey:745},{id:"java-746",number:393,title:"What is a Pointcut expression and what is the `execution` designator?",question:"What is a Pointcut expression and what is the `execution` designator?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A pointcut expression selects join points. The `execution` designator is the most common: `execution(modifiers? return-type declaring-type? method-name(params) throws?)`. Wildcards (`*`, `..`) help. Other designators: `within`, `this`, `target`, `args`, `annotation`, `@annotation`. Example: `execution(* com.example.service..*(..))` matches all methods in `service` and sub-packages.\n\n```java\n@Pointcut("execution(public * com.example..*Service.*(..))")\npublic void serviceMethods() {}\n\n@Before("serviceMethods()")\npublic void before() {}\n```',tech:"java",displayNumber:393,sortKey:746},{id:"java-747",number:394,title:"What is the difference between Spring AOP and AspectJ?",question:"What is the difference between Spring AOP and AspectJ?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`- **Spring AOP**: runtime weaving via proxies; only supports method-execution join points on Spring beans; simpler, no separate compilation step.
- **AspectJ**: full AOP with compile-time or load-time weaving; supports field access, constructor, static init join points; works on non-Spring objects. More powerful but more complex.

Use Spring AOP for typical enterprise cross-cutting concerns; use AspectJ when you need non-method join points or to advise objects not managed by Spring.

\`\`\`java
// Spring AOP: proxy-based, method join points only
@EnableAspectJAutoProxy
@Configuration
public class AopConfig {}
\`\`\``,tech:"java",displayNumber:394,sortKey:747},{id:"java-748",number:395,title:"What is `@EnableAspectJAutoProxy` and proxying (JDK vs CGLIB)?",question:"What is `@EnableAspectJAutoProxy` and proxying (JDK vs CGLIB)?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"`@EnableAspectJAutoProxy` enables Spring AOP. Spring creates proxies: **JDK dynamic proxies** (default when the bean implements an interface) and **CGLIB** (when no interface, or to proxy concrete classes). Since Boot 2.0, proxying defaults to CGLIB (`spring.aop.proxy-target-class=true`). CGLIB subclasses the class, so `final` methods cannot be advised.\n\n```java\n@EnableAspectJAutoProxy(proxyTargetClass = true) // force CGLIB\n@Configuration\npublic class AopConfig {}\n```",tech:"java",displayNumber:395,sortKey:748},{id:"java-749",number:396,title:"What is `@Transactional` and how does Spring manage transactions?",question:"What is `@Transactional` and how does Spring manage transactions?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@Transactional` demarcates a method (or class) as a single transaction. Spring uses AOP to create a proxy that begins a transaction before the method, commits on success, and rolls back on a runtime exception (by default; checked exceptions do not roll back unless configured). It relies on a `PlatformTransactionManager`. Key attributes: `propagation`, `isolation`, `readOnly`, `rollbackFor`, `timeout`.\n\n```java\n@Service\npublic class TransferService {\n    @Transactional(propagation = Propagation.REQUIRED, isolation = Isolation.READ_COMMITTED)\n    public void transfer(Long from, Long to, BigDecimal amt) { ... }\n}\n```",tech:"java",displayNumber:396,sortKey:749},{id:"java-750",number:397,title:"What are transaction propagation levels?",question:"What are transaction propagation levels?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Propagation defines how a transaction relates to an existing one:
- **REQUIRED** (default): join existing or create new.
- **REQUIRES_NEW**: suspend existing, always create new.
- **NESTED**: nested transaction with savepoint.
- **SUPPORTS**: use existing, else non-transactional.
- **NOT_SUPPORTED**: suspend existing, run non-transactional.
- **MANDATORY**: must exist, else throw.
- **NEVER**: must not exist, else throw.

\`\`\`java
@Transactional(propagation = Propagation.REQUIRES_NEW)
public void audit(String msg) { /* independent tx */ }
\`\`\``,tech:"java",displayNumber:397,sortKey:750},{id:"java-751",number:398,title:"What are transaction isolation levels?",question:"What are transaction isolation levels?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Isolation controls visibility of changes between concurrent transactions, mapped to standard SQL levels:
- **DEFAULT**: DB default.
- **READ_UNCOMMITTED**: may read uncommitted (dirty reads possible).
- **READ_COMMITTED**: no dirty reads.
- **REPEATABLE_READ**: no dirty/non-repeatable reads.
- **SERIALIZABLE**: full isolation, slowest.

Higher isolation reduces anomaly risk but lowers concurrency.

\`\`\`java
@Transactional(isolation = Isolation.REPEATABLE_READ)
public Account read(Long id) { ... }
\`\`\``,tech:"java",displayNumber:398,sortKey:751},{id:"java-752",number:399,title:"What is the difference between `@Component` and `@Bean`?",question:"What is the difference between `@Component` and `@Bean`?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"`@Component` is used on a class you own so Spring auto-detects it via component scanning. `@Bean` is used inside a `@Configuration` class to explicitly instantiate and register a bean — typically for third-party classes you cannot annotate (e.g., `DataSource`, `RestTemplate`). `@Bean` gives full control over construction logic.\n\n```java\n@Component              // you own this class\npublic class MyService {}\n\n@Bean                   // explicit, for external class\npublic RestTemplate restTemplate() { return new RestTemplate(); }\n```",tech:"java",displayNumber:399,sortKey:752},{id:"java-753",number:400,title:"How do you create a custom auto-configuration?",question:"How do you create a custom auto-configuration?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"To provide auto-configuration (e.g., for a library):\n1. Create a `@Configuration` class guarded by `@ConditionalOnClass` / `@ConditionalOnMissingBean`.\n2. Register it in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`.\n3. Optionally use `@EnableConfigurationProperties` to bind config.\nThis lets users get beans automatically only when conditions are met, while still allowing override.\n\n```java\n@Configuration\n@ConditionalOnClass(MyClient.class)\n@EnableConfigurationProperties(MyProps.class)\npublic class MyAutoConfig {\n    @Bean @ConditionalOnMissingBean\n    public MyClient client(MyProps p) { return new MyClient(p.getUrl()); }\n}\n```",tech:"java",displayNumber:400,sortKey:753},{id:"java-754",number:401,title:"What is `@Conditional` and conditional annotations?",question:"What is `@Conditional` and conditional annotations?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'`@Conditional` registers a bean only when a specified `Condition` matches. Spring Boot extends this with ready-made annotations: `@ConditionalOnClass`, `@ConditionalOnMissingBean`, `@ConditionalOnProperty`, `@ConditionalOnResource`, `@ConditionalOnWebApplication`, `@ConditionalOnExpression`. These power auto-configuration logic.\n\n```java\n@Bean\n@ConditionalOnProperty(name = "feature.cache.enabled", havingValue = "true")\npublic CacheService cacheService() { return new CacheService(); }\n```',tech:"java",displayNumber:401,sortKey:754},{id:"java-755",number:402,title:"What is a circular dependency and how does Spring handle it?",question:"What is a circular dependency and how does Spring handle it?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A circular dependency occurs when two or more beans depend on each other (A → B → A). Spring resolves singleton circular references via "early references" (exposing a partially-constructed bean through a singleton factory). This works for constructor injection only if you use `@Lazy` on one side, since constructor cycles cannot be resolved normally and will throw `BeanCurrentlyInCreationException`. Setter/field injection can be resolved. The best fix is to redesign to remove the cycle.\n\n```java\n@Component\npublic class A {\n    public A(@Lazy B b) {} // break constructor cycle with @Lazy\n}\n```',tech:"java",displayNumber:402,sortKey:755},{id:"java-756",number:403,title:"How do you test a Spring Boot application?",question:"How do you test a Spring Boot application?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Spring Boot testing uses `@SpringBootTest` to load the full application context (or a sliced context). Slices load only relevant layers: `@WebMvcTest` (controllers), `@DataJpaTest` (JPA), `@JsonTest`, `@RestClientTest`. Use `MockMvc` for HTTP testing, `@MockBean` to mock dependencies, and `@TestPropertySource`/`@DynamicPropertySource` for test config. `spring-boot-starter-test` bundles JUnit 5, Mockito, and AssertJ.\n\n```java\n@WebMvcTest(UserController.class)\nclass UserControllerTest {\n    @Autowired MockMvc mvc;\n    @MockBean UserService service;\n\n    @Test void get_ok() throws Exception {\n        when(service.findById(1L)).thenReturn(new User(1L,"a"));\n        mvc.perform(get("/api/users/1")).andExpect(status().isOk());\n    }\n}\n```',tech:"java",displayNumber:403,sortKey:756},{id:"java-757",number:404,title:"What is `@DataJpaTest` and `@WebMvcTest`?",question:"What is `@DataJpaTest` and `@WebMvcTest`?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"These are test slices that load only a subset of the Spring context for speed:\n- `@DataJpaTest`: configures an in-memory JPA setup (H2), repositories, `TestEntityManager`; good for repository tests.\n- `@WebMvcTest`: loads only web layer (controllers, filters, MVC infrastructure), not services; use `@MockBean` for dependencies.\n\nSlices dramatically reduce startup time versus `@SpringBootTest`.\n\n```java\n@DataJpaTest\nclass UserRepoTest {\n    @Autowired TestEntityManager em;\n    @Autowired UserRepository repo;\n}\n```",tech:"java",displayNumber:404,sortKey:757},{id:"java-758",number:405,title:"What is Actuator and what endpoints does it provide?",question:"What is Actuator and what endpoints does it provide?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Spring Boot Actuator exposes operational endpoints for monitoring and management. Key endpoints: `/health` (app status), `/info` (build info), `/metrics` (counters/gauges), `/env`, `/beans`, `/mappings`, `/loggers`, `/shutdown` (disabled by default). In Boot 2+, most are disabled except `health` and `info` by default; enable with `management.endpoints.web.exposure.include=*`.\n\n```properties\nmanagement.endpoints.web.exposure.include=health,info,metrics\nmanagement.endpoint.health.show-details=always\n```",tech:"java",displayNumber:405,sortKey:758},{id:"java-759",number:406,title:"What is the difference between `@SpringBootTest` and test slices?",question:"What is the difference between `@SpringBootTest` and test slices?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"`@SpringBootTest` starts the full application context (optionally with a real embedded server via `webEnvironment`), making it closest to production but slowest. Test slices (`@WebMvcTest`, `@DataJpaTest`, etc.) start a minimal, targeted context for speed and isolation. Use slices for unit/integration tests of specific layers; use `@SpringBootTest` for end-to-end/context-load tests.\n\n```java\n@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)\nclass AppE2E { @LocalServerPort int port; }\n```",tech:"java",displayNumber:406,sortKey:759},{id:"java-760",number:407,title:"What is Microservices Architecture?",question:"What is Microservices Architecture?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Microservices is an architectural style where an application is composed of small, independently deployable services, each owning its own data and communicating over lightweight protocols (HTTP/REST, messaging). Benefits: independent scaling, technology diversity, fault isolation, and team autonomy. Trade-offs: distributed complexity, network latency, data consistency challenges, and operational overhead.\n\n```text\nMonolith ──> [Auth Svc] [Order Svc] [Payment Svc] [Inventory Svc]\n             each: own DB, own deploy, talk via REST/gRPC\n```",tech:"java",displayNumber:407,sortKey:760},{id:"java-761",number:408,title:"What is Service Discovery and how does Eureka work?",question:"What is Service Discovery and how does Eureka work?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Service Discovery lets services locate each other by logical name instead of hardcoded host/port. **Netflix Eureka** is a registry: services register themselves with a Eureka Server on startup and send heartbeats. Clients query the registry to resolve service instances. Spring Cloud provides `@EnableEurekaServer` (registry) and `@EnableDiscoveryClient` (client) with `spring-cloud-starter-netflix-eureka-*`.\n\n```java\n@EnableEurekaServer\n@SpringBootApplication\npublic class DiscoveryServer { public static void main(String[] a){ SpringApplication.run(DiscoveryServer.class,a);} }\n```\n```yaml\n# client\neureka:\n  client:\n    service-url:\n      defaultZone: http://localhost:8761/eureka\n```",tech:"java",displayNumber:408,sortKey:761},{id:"java-762",number:409,title:"What is an API Gateway and what does it do?",question:"What is an API Gateway and what does it do?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:`An API Gateway is a single entry point that routes requests to downstream microservices, handling cross-cutting concerns: routing, load balancing, authentication/authorization, rate limiting, request/response transformation, caching, and logging. Spring Cloud Gateway (reactive, built on WebFlux/Netty) is the modern choice, replacing the legacy Zuul. It uses Route/Predicate/Filter model.

\`\`\`yaml
spring:
  cloud:
    gateway:
      routes:
        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**
          filters:
            - name: CircuitBreaker
              args:
                name: orderCB
\`\`\``,tech:"java",displayNumber:409,sortKey:762},{id:"java-763",number:410,title:"What is Spring Cloud Config Server?",question:"What is Spring Cloud Config Server?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"Config Server provides externalized, centralized configuration for microservices. It serves config from a Git repository (or Vault/file) to clients over HTTP. Clients (`@EnableConfigServer` on the server; `spring-cloud-config-client` on clients) fetch their config at startup. It supports profiles, encryption, and refresh via Spring Cloud Bus. This keeps configuration out of individual service artifacts.\n\n```java\n@EnableConfigServer\n@SpringBootApplication\npublic class ConfigServer { public static void main(String[] a){ SpringApplication.run(ConfigServer.class,a);} }\n```\n```yaml\n# client bootstrap\nspring:\n  cloud:\n    config:\n      uri: http://localhost:8888\n      name: order-service\n```",tech:"java",displayNumber:410,sortKey:763},{id:"java-764",number:411,title:"What is a Circuit Breaker and how does Resilience4j work?",question:"What is a Circuit Breaker and how does Resilience4j work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'A circuit breaker prevents a service from repeatedly calling a failing dependency, failing fast and allowing recovery. **Resilience4j** provides `CircuitBreaker` with states CLOSED, OPEN, HALF_OPEN, plus `Retry`, `Bulkhead`, `RateLimiter`, and `TimeLimiter`. When failures exceed a threshold, the breaker opens; after a wait, it goes half-open to test recovery. Integrate via `@CircuitBreaker(name="svc")` or the Spring Cloud Circuit Breaker abstraction.\n\n```java\n@CircuitBreaker(name = "paymentService", fallbackMethod = "fallback")\npublic String charge() { return paymentClient.charge(); }\n\npublic String fallback(Throwable t) { return "default"; }\n```',tech:"java",displayNumber:411,sortKey:764},{id:"java-765",number:412,title:"What are the Circuit Breaker states?",question:"What are the Circuit Breaker states?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`- **CLOSED**: normal operation; calls pass through; failures are counted.
- **OPEN**: failure threshold exceeded; calls fail fast without invoking the dependency. After a configured \`waitDuration\`, transitions to HALF_OPEN.
- **HALF_OPEN**: a limited number of test calls are allowed; if they succeed, back to CLOSED; if they fail, back to OPEN.

This protects the system and gives the downstream time to recover.

\`\`\`yaml
resilience4j.circuitbreaker:
  instances:
    paymentService:
      failureRateThreshold: 50
      waitDurationInOpenState: 5s
      slidingWindowType: COUNT_BASED
      slidingWindowSize: 10
\`\`\``,tech:"java",displayNumber:412,sortKey:765},{id:"java-766",number:413,title:"What is Distributed Tracing and how do Sleuth/Zipkin work?",question:"What is Distributed Tracing and how do Sleuth/Zipkin work?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Distributed tracing tracks a request as it flows across multiple microservices, helping diagnose latency and failures. **Spring Cloud Sleuth** (Micrometer Tracing in newer versions) adds trace/span IDs to logs and propagates them via headers across service calls. **Zipkin** is a backend that collects and visualizes these spans. Together, you see the full call tree and per-service timing.

\`\`\`yaml
# application.yml
management:
  tracing:
    sampling:
      probability: 1.0
spring:
  zipkin:
    base-url: http://localhost:9411
\`\`\`
\`\`\`text
TraceId=abc SpanId=123 service=order -> service=payment (parent span)
\`\`\``,tech:"java",displayNumber:413,sortKey:766},{id:"java-767",number:414,title:"What is an API Gateway vs Load Balancer?",question:"What is an API Gateway vs Load Balancer?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:"A **Load Balancer** (e.g., client-side Ribbon/Spring Cloud LoadBalancer, or hardware/cloud LB) distributes traffic across instances of a single service for scalability/availability. An **API Gateway** is an application-layer facade that routes to many different services and applies cross-cutting policies (auth, throttling, transformation). The gateway often uses a load balancer internally (note `lb://service-name` in routes) to pick an instance.\n\n```yaml\n# Gateway route uses client-side load balancing\nuri: lb://order-service   # lb = Spring Cloud LoadBalancer\n```",tech:"java",displayNumber:414,sortKey:767},{id:"java-768",number:415,title:"What is `@LoadBalanced` RestTemplate?",question:"What is `@LoadBalanced` RestTemplate?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Annotating a `RestTemplate` bean with `@LoadBalanced` enables client-side load balancing: instead of a concrete URL, you call by service name (e.g., `http://order-service/api/orders`), and Spring Cloud LoadBalancer resolves it to a registered instance and balances across replicas. It works by intercepting requests via `LoadBalancerInterceptor`.\n\n```java\n@Bean @LoadBalanced\npublic RestTemplate restTemplate() { return new RestTemplate(); }\n\n// usage\nrestTemplate.getForObject("http://order-service/api/orders", List.class);\n```',tech:"java",displayNumber:415,sortKey:768},{id:"java-769",number:416,title:"What is Spring Cloud and how does it relate to microservices?",question:"What is Spring Cloud and how does it relate to microservices?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:`Spring Cloud is a collection of tools that implement common distributed-system patterns on top of Spring Boot: service discovery (Eureka), config (Config Server), gateway (Spring Cloud Gateway), circuit breakers (Resilience4j), load balancing, distributed tracing (Sleuth/Zipkin), and messaging. It standardizes microservices building blocks so teams don't reinvent them.

\`\`\`xml
<dependency>
  <groupId>org.springframework.cloud</groupId>
  <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
</dependency>
\`\`\``,tech:"java",displayNumber:416,sortKey:769},{id:"java-770",number:417,title:"What is the difference between monolith and microservices?",question:"What is the difference between monolith and microservices?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"A **monolith** is a single deployable unit with one codebase and one database — simpler to develop, test, and deploy, but harder to scale parts independently and riskier to change. **Microservices** split functionality into independent services with separate data stores and deployments — better scalability, isolation, and team autonomy, but add distributed-system complexity (networking, data consistency, observability). Choose based on team size, scale, and domain complexity.\n\n```text\nMonolith: 1 app + 1 DB, scale all-or-nothing\nMicroservices: N apps + N DBs, scale per service\n```",tech:"java",displayNumber:417,sortKey:770},{id:"java-771",number:418,title:"How do microservices communicate?",question:"How do microservices communicate?",difficulty:"Intermediate",category:"Spring Framework & Microservices",answer:'Two main styles:\n- **Synchronous (HTTP/REST, gRPC)**: request/response, easy to use, but creates runtime coupling and latency chains.\n- **Asynchronous (messaging: Kafka, RabbitMQ, Spring AMQP)**: event-driven, decoupled, resilient, but adds eventual consistency and broker complexity.\n\nSpring supports both via `RestTemplate`/`WebClient` (sync) and `Spring Kafka`/`Spring AMQP` (async). Choose sync for request/response needs; async for high-throughput, decoupled workflows.\n\n```java\n// Async with Spring Kafka\n@KafkaListener(topics = "orders")\npublic void onOrder(OrderEvent e) { process(e); }\n```',tech:"java",displayNumber:418,sortKey:771},{id:"java-772",number:419,title:"What is eventual consistency in microservices?",question:"What is eventual consistency in microservices?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Since each microservice owns its own database, cross-service transactions cannot use a single ACID transaction. Instead, systems become *eventually consistent*: an update propagates asynchronously (via events/messaging) and all data stores converge to a consistent state after a short delay. Patterns include Saga (choreography or orchestration), outbox, and CDC (Change Data Capture). This trades immediate consistency for availability and resilience.

\`\`\`java
@Transactional
public void placeOrder(Order o) {
    repo.save(o);
    outbox.save(new OutboxEvent("OrderPlaced", o)); // published later
}
\`\`\``,tech:"java",displayNumber:419,sortKey:772},{id:"java-773",number:420,title:"What is the Saga pattern?",question:"What is the Saga pattern?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"The Saga pattern manages a distributed transaction spanning multiple services without a global lock. It sequences local transactions; each step publishes an event that triggers the next. If a step fails, compensating transactions undo prior steps. Two styles: **Choreography** (services react to events, decentralized) and **Orchestration** (a central orchestrator directs steps). Spring integrates via event buses (Spring Cloud Stream/Kafka) or orchestrators.\n\n```text\nChoreography: OrderPlaced -> [Inventory] reserved -> [Payment] charged\nFailure: PaymentFailed -> Inventory.compensate()\n```",tech:"java",displayNumber:420,sortKey:773},{id:"java-774",number:421,title:"How do you secure microservices with Spring Security / OAuth2?",question:"How do you secure microservices with Spring Security / OAuth2?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:'Microservices typically use **OAuth2 / JWT**: an Authorization Server issues tokens; the API Gateway validates them and forwards user context. Each service uses `spring-boot-starter-security` + `spring-boot-starter-oauth2-resource-server` to validate JWTs (`spring.security.oauth2.resourceserver.jwt.issuer-uri`). Centralized auth at the gateway plus per-service method security (`@PreAuthorize`) enforces fine-grained access.\n\n```yaml\nspring:\n  security:\n    oauth2:\n      resourceserver:\n        jwt:\n          issuer-uri: http://auth-server:9000\n```\n```java\n@PreAuthorize("hasRole(\'ADMIN\')")\n@GetMapping("/admin")\npublic List<User> all() { ... }\n```',tech:"java",displayNumber:421,sortKey:774},{id:"java-775",number:422,title:"What is CQRS and Event Sourcing?",question:"What is CQRS and Event Sourcing?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:"**CQRS** (Command Query Responsibility Segregation) separates write (command) and read (query) models, often using different datastores optimized for each, improving performance and scalability. **Event Sourcing** stores state as a sequence of immutable events rather than current state; the current state is replayed from events. They complement each other: commands emit events (sourced), and a read model is built from them. Spring supports via Axon Framework or custom event handlers.\n\n```java\n@EventHandler\npublic void on(OrderPlaced e) { readModel.insert(e.toView()); }\n```",tech:"java",displayNumber:422,sortKey:775},{id:"java-776",number:423,title:"What is Spring Boot DevTools?",question:"What is Spring Boot DevTools?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot DevTools improves developer experience: automatic restart on classpath changes, LiveReload of browser, sensible defaults for development (disabled caching for templates), and a `RemoteDebug` option. It is intended only for development — exclude it from production builds. Add `spring-boot-devtools` as an `optional` dependency.\n\n```xml\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-devtools</artifactId>\n  <optional>true</optional>\n</dependency>\n```",tech:"java",displayNumber:423,sortKey:776},{id:"java-777",number:424,title:"How do you package and run a Spring Boot application?",question:"How do you package and run a Spring Boot application?",difficulty:"Basic",category:"Spring Framework & Microservices",answer:"Spring Boot produces a fat/uber JAR via `spring-boot-maven-plugin` (or Gradle plugin) that includes the embedded server and all dependencies. Run with `java -jar app.jar`. Profiles and config can be overridden at runtime (`--spring.profiles.active=prod`, `--server.port=8080`). Containers (Docker) wrap the JAR for cloud deployment; Buildpacks are supported out of the box with `mvn spring-boot:build-image`.\n\n```bash\n./mvnw package\njava -jar target/app.jar --spring.profiles.active=prod\n```",tech:"java",displayNumber:424,sortKey:777},{id:"java-778",number:425,title:"What are the 12-factor app principles relevant to Spring Boot microservices?",question:"What are the 12-factor app principles relevant to Spring Boot microservices?",difficulty:"Advanced",category:"Spring Framework & Microservices",answer:`Key 12-factor principles Spring Boot naturally supports:
1. **Codebase**: one repo per service.
2. **Dependencies**: declared in Maven/Gradle.
3. **Config**: externalized via properties/env (Spring Boot excels here).
4. **Backing services**: treated as attached resources (datasource beans).
5. **Build/Release/Run**: separated; fat JAR + profile.
6. **Processes**: stateless; use request/session scopes carefully.
7. **Port binding**: embedded server exposes via port.
8. **Concurrency**: scale horizontally.
9. **Disposability**: fast startup/shutdown (important for containers).
10. **Dev/prod parity**: same artifact across environments.

Spring Boot's externalized config, actuators, and embedded servers make most of these trivial.

\`\`\`properties
# Config from environment (factor III)
spring.datasource.url=\${DB_URL}
spring.datasource.username=\${DB_USER}
\`\`\``,tech:"java",displayNumber:425,sortKey:778},{id:"java-779",number:426,title:"What is JPA and how does it relate to Hibernate?",question:"What is JPA and how does it relate to Hibernate?",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`JPA (Java Persistence API) is a specification (JSR 338) that defines a standard for ORM in Java. It provides annotations and interfaces for mapping Java objects to relational databases. JPA is just the API — it doesn't provide implementation.

Hibernate is one of the most popular JPA implementations. It was the inspiration for JPA and provides the actual persistence logic. Other implementations include EclipseLink and OpenJPA.

Think of JPA as the interface and Hibernate as the concrete class that implements it.

\`\`\`java
// JPA is just annotations/interfaces — no implementation
@Entity
public class User {
    @Id @GeneratedValue
    private Long id;
    private String name;
}

// Hibernate provides the implementation
// In persistence.xml or application.yml:
// spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

// Spring Boot auto-configures Hibernate as default JPA provider
\`\`\``,tech:"java",displayNumber:426,sortKey:779},{id:"java-780",number:427,title:"What is ORM and what problem does it solve?",question:"What is ORM and what problem does it solve?",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`ORM (Object-Relational Mapping) is a technique that maps Java objects to database tables, bridging the gap between object-oriented programming and relational databases (the impedance mismatch).

Without ORM, developers write tedious JDBC code for CRUD operations. ORM handles:
- Mapping objects to tables and fields to columns
- Automatic SQL generation
- Transaction management
- Caching for performance
- Lazy loading of relationships

\`\`\`java
// Without ORM - verbose JDBC
String sql = "SELECT id, name FROM users WHERE id = ?";
PreparedStatement ps = conn.prepareStatement(sql);
ps.setLong(1, userId);
ResultSet rs = ps.executeQuery();

// With ORM - clean and type-safe
User user = entityManager.find(User.class, userId);
// or Spring Data JPA
Optional<User> user = userRepository.findById(userId);
\`\`\``,tech:"java",displayNumber:427,sortKey:780},{id:"java-781",number:428,title:"Entity and table mapping",question:"Entity and table mapping",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Entity marks a persistent class, while @Table controls its physical table name and constraints. The class needs a no-argument constructor and an identity field.

\`\`\`java
@Entity
@Table(name = "customers")
class Customer {
    @Id Long id;
    protected Customer() {}
}
\`\`\``,tech:"java",displayNumber:428,sortKey:781},{id:"java-782",number:429,title:"Primary key with @Id",question:"Primary key with @Id",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Id identifies the column that uniquely identifies each entity. It may be placed on a field or getter, but the choice determines access strategy for all mappings.

\`\`\`java
@Entity class Account {
    @Id private String iban;
    protected Account() {}
}
\`\`\``,tech:"java",displayNumber:429,sortKey:782},{id:"java-783",number:430,title:"IDENTITY key generation",question:"IDENTITY key generation",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"IDENTITY delegates key creation to an auto-increment/identity column. Hibernate usually must insert before it knows the identifier, so batching can be less efficient.\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.IDENTITY) Long id;\n```",tech:"java",displayNumber:430,sortKey:783},{id:"java-784",number:431,title:"SEQUENCE key generation",question:"SEQUENCE key generation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'SEQUENCE obtains identifiers from a database sequence and can allocate blocks with allocationSize, improving batching. It is commonly preferred on PostgreSQL and Oracle.\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.SEQUENCE, generator="user_seq")\n@SequenceGenerator(name="user_seq", sequenceName="user_seq", allocationSize=50)\nLong id;\n```',tech:"java",displayNumber:431,sortKey:784},{id:"java-785",number:432,title:"TABLE key generation",question:"TABLE key generation",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'TABLE stores the next identifier in a dedicated table. It is portable but requires row locking and is generally slower than native sequences or identity columns.\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.TABLE, generator="ids")\n@TableGenerator(name="ids", table="id_gen", pkColumnName="name", valueColumnName="value", pkColumnValue="customer")\nLong id;\n```',tech:"java",displayNumber:432,sortKey:785},{id:"java-786",number:433,title:"AUTO key generation",question:"AUTO key generation",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"AUTO lets the provider select a strategy from the dialect. It is convenient for portable examples, but production schemas should choose and migrate an explicit strategy.\n\n```java\n@Id @GeneratedValue(strategy=GenerationType.AUTO) Long id;\n```",tech:"java",displayNumber:433,sortKey:786},{id:"java-787",number:434,title:"@Column options",question:"@Column options",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'@Column customizes name, nullability, uniqueness, length, and precision. These are mapping hints and should be backed by actual database constraints and migrations.\n\n```java\n@Column(name="email_address", nullable=false, unique=true, length=320)\nString email;\n```',tech:"java",displayNumber:434,sortKey:787},{id:"java-788",number:435,title:"@GeneratedValue composite keys",question:"@GeneratedValue composite keys",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Composite keys use an @EmbeddableId class or multiple @Id fields with @IdClass. The key class must implement equals/hashCode and be serializable. @GeneratedValue applies only to simple keys.

\`\`\`java
@Embeddable class OrderItemId implements Serializable {
    Long orderId; Long productId;
    // equals, hashCode
}
@Entity @IdClass(OrderItemId.class) class OrderItem {
    @Id Long orderId; @Id Long productId;
}
\`\`\``,tech:"java",displayNumber:435,sortKey:788},{id:"java-789",number:436,title:"One-to-one relationship",question:"One-to-one relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'One-to-one maps one row to one related row. Put the foreign key on the owning side with @JoinColumn; make the inverse side mappedBy to avoid a second join column.\n\n```java\n@OneToOne @JoinColumn(name="profile_id", nullable=false)\nProfile profile;\n```',tech:"java",displayNumber:436,sortKey:789},{id:"java-790",number:437,title:"One-to-many relationship",question:"One-to-many relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'A one-to-many collection is usually inverse and paired with a many-to-one foreign key on the child. mappedBy identifies that child association as owner.\n\n```java\n@OneToMany(mappedBy="order")\nList<Line> lines = new ArrayList<>();\n```',tech:"java",displayNumber:437,sortKey:790},{id:"java-791",number:438,title:"Many-to-one relationship",question:"Many-to-one relationship",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'Many child entities can reference one parent. It is the owning side and normally uses a foreign-key column; prefer LAZY loading for large associations.\n\n```java\n@ManyToOne(fetch=FetchType.LAZY, optional=false)\n@JoinColumn(name="customer_id")\nCustomer customer;\n```',tech:"java",displayNumber:438,sortKey:791},{id:"java-792",number:439,title:"Many-to-many relationship",question:"Many-to-many relationship",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Many-to-many uses a join table and is convenient for simple links. For attributes on the link, model the join table as an entity instead.

\`\`\`java
@ManyToMany
@JoinTable(name="user_role",
    joinColumns=@JoinColumn(name="user_id"),
    inverseJoinColumns=@JoinColumn(name="role_id"))
Set<Role> roles;
\`\`\``,tech:"java",displayNumber:439,sortKey:792},{id:"java-793",number:440,title:"Cascade operations",question:"Cascade operations",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Cascade propagates persistence operations from parent to child; ALL includes remove, which can delete shared data accidentally. Use only the operations the aggregate owns.\n\n```java\n@OneToMany(mappedBy="invoice", cascade=CascadeType.ALL, orphanRemoval=true)\nList<Item> items;\n```',tech:"java",displayNumber:440,sortKey:793},{id:"java-794",number:441,title:"orphanRemoval",question:"orphanRemoval",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"orphanRemoval deletes a child removed from an owned collection or whose one-to-one reference is cleared. It is not a replacement for cascade remove and should not be used for shared children.\n\n```java\nitems.remove(item); // owned orphan is deleted on flush\n```",tech:"java",displayNumber:441,sortKey:794},{id:"java-795",number:442,title:"LAZY versus EAGER fetch",question:"LAZY versus EAGER fetch",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"LAZY loads an association when accessed, whereas EAGER loads it immediately. LAZY reduces unnecessary work but requires an open persistence context or an explicit fetch query.\n\n```java\n@ManyToOne(fetch=FetchType.LAZY) Customer customer;\n@OneToMany(fetch=FetchType.LAZY) List<Line> lines;\n```",tech:"java",displayNumber:442,sortKey:795},{id:"java-796",number:443,title:"Owning side and mappedBy",question:"Owning side and mappedBy",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"The owning side writes the foreign key or join table. mappedBy points to the Java property on that owner; setting only the inverse side does not update the database.\n\n```java\nparent.children.add(child);\nchild.parent = parent; // keep both sides synchronized\n```",tech:"java",displayNumber:443,sortKey:796},{id:"java-797",number:444,title:"Bidirectional relationship sync",question:"Bidirectional relationship sync",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Always update both sides of a bidirectional association in code; the persistence provider only synchronizes from the owning side. Helper methods on the parent prevent mistakes.

\`\`\`java
@Entity class Order {
    @OneToMany(mappedBy="order") List<Line> lines = new ArrayList<>();
    void addLine(Line l) { lines.add(l); l.setOrder(this); }
}
\`\`\``,tech:"java",displayNumber:444,sortKey:797},{id:"java-798",number:445,title:"Join table customization",question:"Join table customization",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`@JoinTable customizes the join table name, columns, and constraints for many-to-many or unidirectional one-to-many. Prefer explicit names for clarity in migrations.

\`\`\`java
@ManyToMany
@JoinTable(name="order_product",
    joinColumns=@JoinColumn(name="order_id"),
    inverseJoinColumns=@JoinColumn(name="product_id"))
Set<Product> products;
\`\`\``,tech:"java",displayNumber:445,sortKey:798},{id:"java-799",number:446,title:"Embeddable value object",question:"Embeddable value object",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Embeddable maps a value type into the owner table rather than a separate table. It has no independent identity and is persisted with its owner.

\`\`\`java
@Embeddable class Address {
    String city; String country;
}
@Embedded Address address;
\`\`\``,tech:"java",displayNumber:446,sortKey:799},{id:"java-800",number:447,title:"Element collection",question:"Element collection",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@ElementCollection persists basic values or embeddables in a collection table. Elements have no identity and the collection is commonly replaced as a unit.\n\n```java\n@ElementCollection\n@CollectionTable(name="user_phone", joinColumns=@JoinColumn(name="user_id"))\nSet<String> phones;\n```',tech:"java",displayNumber:447,sortKey:800},{id:"java-801",number:448,title:"AttributeOverride",question:"AttributeOverride",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@AttributeOverride renames or changes a field of an embedded value for a particular owner, useful when embedding the same type twice.\n\n```java\n@Embedded\n@AttributeOverrides(@AttributeOverride(name="city", column=@Column(name="billing_city")))\nAddress billing;\n```',tech:"java",displayNumber:448,sortKey:801},{id:"java-802",number:449,title:"Nested embeddables",question:"Nested embeddables",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Embeddables can contain other embeddables, letting domain value objects remain cohesive while columns stay in the owner table. Override nested paths when names collide.

\`\`\`java
@Embeddable class Contact {
    @Embedded Address address;
}
@Embedded Contact contact;
\`\`\``,tech:"java",displayNumber:449,sortKey:802},{id:"java-803",number:450,title:"JPQL basics",question:"JPQL basics",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'JPQL queries entities and their attributes, not table names and columns. Parameters should be named or positional; binding avoids injection and type conversion errors.\n\n```java\nList<Customer> cs = em.createQuery(\n    "select c from Customer c where c.email=:e", Customer.class)\n    .setParameter("e", email).getResultList();\n```',tech:"java",displayNumber:450,sortKey:803},{id:"java-804",number:451,title:"JPQL joins",question:"JPQL joins",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"JPQL joins navigate mapped associations and can filter or select related data. join fetch also initializes the association, but collection fetch joins can duplicate root rows.\n\n```java\nselect o from Order o join o.customer c where c.id = :id\n```",tech:"java",displayNumber:451,sortKey:804},{id:"java-805",number:452,title:"JPQL bulk update",question:"JPQL bulk update",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Bulk UPDATE and DELETE operate directly in the database and bypass entity callbacks, dirty checking, and managed state. Clear or refresh the persistence context afterward.

\`\`\`java
int n = em.createQuery(
    "update Account a set a.active=false where a.lastLogin < :d")
    .setParameter("d", date).executeUpdate();
em.clear();
\`\`\``,tech:"java",displayNumber:452,sortKey:805},{id:"java-806",number:453,title:"HQL extensions",question:"HQL extensions",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Hibernate Query Language is JPQL-compatible but offers provider-specific features. Use JPQL for portability and HQL only when a Hibernate capability is intentional.\n\n```java\nsession.createQuery("from Customer c where c.email like :prefix", Customer.class)\n    .setParameter("prefix", "a%").list();\n```',tech:"java",displayNumber:453,sortKey:806},{id:"java-807",number:454,title:"Criteria API",question:"Criteria API",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Criteria builds type-safe dynamic queries without string concatenation. It is verbose but useful when predicates and joins are assembled conditionally.

\`\`\`java
CriteriaBuilder cb = em.getCriteriaBuilder();
CriteriaQuery<Customer> q = cb.createQuery(Customer.class);
Root<Customer> c = q.from(Customer.class);
q.where(cb.equal(c.get("active"), true));
List<Customer> r = em.createQuery(q).getResultList();
\`\`\``,tech:"java",displayNumber:454,sortKey:807},{id:"java-808",number:455,title:"Static metamodel",question:"Static metamodel",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"The generated JPA metamodel replaces string property names in Criteria queries with compile-time symbols. Configure annotation processing to generate Customer_.\n\n```java\nRoot<Customer> c = q.from(Customer.class);\nq.select(c).where(cb.equal(c.get(Customer_.active), true));\n```",tech:"java",displayNumber:455,sortKey:808},{id:"java-809",number:456,title:"Native SQL query",question:"Native SQL query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Native queries use database SQL directly and are appropriate for vendor features, complex reporting, or tuned statements. Map the result carefully and avoid database-specific lock-in without need.\n\n```java\nList<Customer> r = em.createNativeQuery(\n    "select * from customers where status=?", Customer.class)\n    .setParameter(1, "ACTIVE").getResultList();\n```',tech:"java",displayNumber:456,sortKey:809},{id:"java-810",number:457,title:"Named query",question:"Named query",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'Named queries are defined once and validated or parsed early by the provider. They centralize frequently reused statements and can improve query plan reuse.\n\n```java\n@NamedQuery(name="Customer.active", query="select c from Customer c where c.active=true")\n```',tech:"java",displayNumber:457,sortKey:810},{id:"java-811",number:458,title:"N+1 select problem",question:"N+1 select problem",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"N+1 occurs when one query loads N parents and each lazy association triggers another query. Detect it with SQL logging and fix the access pattern rather than making everything eager.\n\n```java\nList<Order> orders = repo.findAll();\norders.forEach(o -> o.getLines().size()); // potentially N+1\n```",tech:"java",displayNumber:458,sortKey:811},{id:"java-812",number:459,title:"Fetch join solution",question:"Fetch join solution",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'A fetch join loads an association in the original query. Use distinct for collection joins and paginate cautiously because SQL row multiplication breaks many pagination strategies.\n\n```java\n@Query("select distinct o from Order o join fetch o.lines")\nList<Order> withLines();\n```',tech:"java",displayNumber:459,sortKey:812},{id:"java-813",number:460,title:"EntityGraph",question:"EntityGraph",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@EntityGraph declares an attribute loading plan independently of JPQL. It is readable for repository methods and can override default fetch behavior for a use case.\n\n```java\n@EntityGraph(attributePaths="lines")\nOptional<Order> findById(Long id);\n```',tech:"java",displayNumber:460,sortKey:813},{id:"java-814",number:461,title:"Batch fetching",question:"Batch fetching",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Hibernate batch fetching groups lazy association loads into IN queries, reducing round trips without one huge join. Tune batch size and verify SQL for your database.\n\n```java\n@BatchSize(size=25)\n@OneToMany(mappedBy="order")\nList<Line> lines;\n```',tech:"java",displayNumber:461,sortKey:814},{id:"java-815",number:462,title:"DTO projections for reads",question:"DTO projections for reads",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Selecting a DTO avoids managing full entities and avoids accidental lazy loads. Constructor expressions are portable JPQL and ideal for read-only API responses.\n\n```java\nselect new com.acme.OrderView(o.id, c.name)\nfrom Order o join o.customer c\n```",tech:"java",displayNumber:462,sortKey:815},{id:"java-816",number:463,title:"Subselect fetching",question:"Subselect fetching",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'@Fetch(FetchMode.SUBSELECT) loads a collection with a single additional query using a subselect derived from the original query. It works well for larger collections when the parent query has no join fetch.\n\n```java\n@OneToMany(mappedBy="order")\n@Fetch(FetchMode.SUBSELECT)\nList<Line> lines;\n```',tech:"java",displayNumber:463,sortKey:816},{id:"java-817",number:464,title:"Persistence context L1 cache",question:"Persistence context L1 cache",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Each EntityManager has a first-level cache. Repeated find calls for the same identity return the managed instance; it is mandatory, scoped to the persistence context, and not shared between requests.\n\n```java\nCustomer a = em.find(Customer.class, 1L);\nCustomer b = em.find(Customer.class, 1L);\nassert a == b;\n```",tech:"java",displayNumber:464,sortKey:817},{id:"java-818",number:465,title:"Second-level cache",question:"Second-level cache",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"L2 cache is shared across persistence contexts and must be explicitly configured. Cache entities only when their change rate and access pattern justify invalidation complexity.\n\n```java\n@Cacheable\n@org.hibernate.annotations.Cache(usage=CacheConcurrencyStrategy.READ_WRITE)\nclass Country {}\n```",tech:"java",displayNumber:465,sortKey:818},{id:"java-819",number:466,title:"Ehcache L2 provider",question:"Ehcache L2 provider",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Ehcache can provide a durable, configurable Hibernate second-level cache through JCache or a native integration. Configure regions, expiry, and provider properties; it is not a substitute for database correctness.\n\n```java\nspring.jpa.properties.hibernate.cache.use_second_level_cache=true\nspring.jpa.properties.hibernate.javax.cache.provider=org.ehcache.jsr107.EhcacheCachingProvider\n```",tech:"java",displayNumber:466,sortKey:819},{id:"java-820",number:467,title:"Caffeine caching",question:"Caffeine caching",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Caffeine is an in-memory high-performance cache often used through Spring Cache. It is generally application-local; use a distributed cache when multiple nodes must share entries.

\`\`\`java
@Cacheable(cacheNames="countries", key="#id")
Country get(Long id) {
    return repo.findById(id).orElseThrow();
}
\`\`\``,tech:"java",displayNumber:467,sortKey:820},{id:"java-821",number:468,title:"Query cache",question:"Query cache",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'Hibernate query cache stores query result identifiers, not complete rows, and depends on L2 entity cache for objects. Updates invalidate regions, so use it only for stable, repeated queries.\n\n```java\nquery.setHint("org.hibernate.cacheable", true);\n```',tech:"java",displayNumber:468,sortKey:821},{id:"java-822",number:469,title:"Cache concurrency strategies",question:"Cache concurrency strategies",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"READ_ONLY suits immutable data; NONSTRICT_READ_WRITE tolerates brief staleness; READ_WRITE adds coordination; TRANSACTIONAL requires a compatible transactional cache. Choose based on consistency needs.\n\n```java\n@Cache(usage=CacheConcurrencyStrategy.READ_ONLY)\nclass Currency {}\n```",tech:"java",displayNumber:469,sortKey:822},{id:"java-823",number:470,title:"@Transactional boundary",question:"@Transactional boundary",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`@Transactional binds an EntityManager and transaction to a service method. Keep the boundary around a business operation, not a controller, and remember self-invocation bypasses proxies.

\`\`\`java
@Transactional
public void transfer(long from, long to, BigDecimal amount) {
    debit(from, amount);
    credit(to, amount);
}
\`\`\``,tech:"java",displayNumber:470,sortKey:823},{id:"java-824",number:471,title:"Transaction propagation",question:"Transaction propagation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"REQUIRED joins or creates a transaction; REQUIRES_NEW suspends the current one; SUPPORTS is optional; MANDATORY requires one; NEVER rejects one. Pick semantics deliberately for side effects.\n\n```java\n@Transactional(propagation=Propagation.REQUIRES_NEW)\nvoid writeAudit(Audit a) {}\n```",tech:"java",displayNumber:471,sortKey:824},{id:"java-825",number:472,title:"Transaction isolation",question:"Transaction isolation",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Isolation controls visibility of concurrent changes: READ_COMMITTED is a common default, REPEATABLE_READ prevents non-repeatable reads, and SERIALIZABLE is strongest but costly. Database support varies.

\`\`\`java
@Transactional(isolation=Isolation.READ_COMMITTED)
void reserve() {
    // atomic DB update
}
\`\`\``,tech:"java",displayNumber:472,sortKey:825},{id:"java-826",number:473,title:"Rollback rules",question:"Rollback rules",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Spring rolls back unchecked exceptions by default. Configure rollbackFor for checked exceptions, and avoid catching failures unless you rethrow or mark rollback-only.\n\n```java\n@Transactional(rollbackFor=IOException.class)\nvoid importFile() throws IOException {}\n```",tech:"java",displayNumber:473,sortKey:826},{id:"java-827",number:474,title:"Read-only transactions",question:"Read-only transactions",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:`readOnly=true communicates intent and may enable provider optimizations; it does not universally enforce that no writes occur. Do not rely on it as authorization or data protection.

\`\`\`java
@Transactional(readOnly=true)
List<Customer> search(String text) {
    return repo.findByNameContaining(text);
}
\`\`\``,tech:"java",displayNumber:474,sortKey:827},{id:"java-828",number:475,title:"Transaction timeout",question:"Transaction timeout",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"A timeout limits transaction duration and helps release locks under slow work. It is a guardrail, not a performance fix; keep remote calls outside database transactions.\n\n```java\n@Transactional(timeout=5)\nvoid updateInventory() {}\n```",tech:"java",displayNumber:475,sortKey:828},{id:"java-829",number:476,title:"Declarative vs programmatic transactions",question:"Declarative vs programmatic transactions",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:"Declarative @Transactional is the default and sufficient for most cases. Programmatic TransactionTemplate gives fine-grained control when business logic needs multiple commit boundaries in one method.\n\n```java\n@Autowired PlatformTransactionManager tx;\nTransactionTemplate tt = new TransactionTemplate(tx);\ntt.execute(status -> { /* work */ return result; });\n```",tech:"java",displayNumber:476,sortKey:829},{id:"java-830",number:477,title:"Repository abstraction",question:"Repository abstraction",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"JpaRepository supplies CRUD, paging, sorting, and flush operations for an entity and identifier. Spring creates the implementation from the interface.\n\n```java\ninterface CustomerRepository extends JpaRepository<Customer, Long> {}\n```",tech:"java",displayNumber:477,sortKey:830},{id:"java-831",number:478,title:"Derived query methods",question:"Derived query methods",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Spring Data parses method names into predicates. Use clear names for simple queries; switch to @Query when names become unreadable.\n\n```java\nList<Customer> findByActiveTrueAndNameContainingIgnoreCase(String name);\n```",tech:"java",displayNumber:478,sortKey:831},{id:"java-832",number:479,title:"@Query repository query",question:"@Query repository query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@Query defines JPQL on a repository method and supports named parameters. Use a modifying query for updates and manage its persistence context explicitly.\n\n```java\n@Query("select c from Customer c where c.email=:email")\nOptional<Customer> findByEmail(@Param("email") String email);\n```',tech:"java",displayNumber:479,sortKey:832},{id:"java-833",number:480,title:"Modifying repository query",question:"Modifying repository query",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'@Modifying marks UPDATE or DELETE queries so Spring executes them as writes. clearAutomatically prevents stale managed entities after bulk SQL.\n\n```java\n@Modifying(clearAutomatically=true)\n@Query("delete from Customer c where c.active=false")\nint deleteInactive();\n```',tech:"java",displayNumber:480,sortKey:833},{id:"java-834",number:481,title:"Paging and sorting",question:"Paging and sorting",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Pageable applies limit/offset and sorting while Page includes a count query. Slice avoids the count when callers only need to know whether another page exists.\n\n```java\nPage<Customer> findByActiveTrue(Pageable page);\n```",tech:"java",displayNumber:481,sortKey:834},{id:"java-835",number:482,title:"Specifications",question:"Specifications",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:'JpaSpecificationExecutor composes reusable Criteria predicates for optional filters. It keeps dynamic search logic out of string-built JPQL.\n\n```java\nSpecification<Customer> active = (r, q, cb) -> cb.isTrue(r.get("active"));\nrepo.findAll(active);\n```',tech:"java",displayNumber:482,sortKey:835},{id:"java-836",number:483,title:"Interface projections",question:"Interface projections",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"Interface projections select only requested properties and expose a lightweight view. Nested projections can traverse associations but may still trigger joins or loads.\n\n```java\ninterface NameView { String getName(); }\nList<NameView> findByActiveTrue();\n```",tech:"java",displayNumber:483,sortKey:836},{id:"java-837",number:484,title:"Class and record projections",question:"Class and record projections",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Class projections use constructor expressions or repository support to create immutable result objects. They are useful at service boundaries where entities should not escape.\n\n```java\nrecord CustomerView(Long id, String name) {}\n@Query("select new com.acme.CustomerView(c.id, c.name) from Customer c")\nList<CustomerView> views();\n```',tech:"java",displayNumber:484,sortKey:837},{id:"java-838",number:485,title:"Query by example",question:"Query by example",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Query by Example builds a dynamic predicate from a probe instance with matching rules. It is convenient for filter forms but less explicit than specifications for complex queries.\n\n```java\nExample<Customer> ex = Example.of(new Customer(null, "A%", true));\nrepo.findAll(ex);\n```',tech:"java",displayNumber:485,sortKey:838},{id:"java-839",number:486,title:"Auditing timestamps",question:"Auditing timestamps",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Spring Data auditing populates creation and modification timestamps when auditing is enabled. Use an auditable base class or annotations and ensure the entity is managed through Spring Data.\n\n```java\n@CreatedDate Instant createdAt;\n@LastModifiedDate Instant updatedAt;\n```",tech:"java",displayNumber:486,sortKey:839},{id:"java-840",number:487,title:"AuditorAware",question:"AuditorAware",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`AuditorAware supplies the current principal for @CreatedBy and @LastModifiedBy. Return empty for system jobs or unauthenticated operations rather than inventing a user.

\`\`\`java
class CurrentAuditor implements AuditorAware<String> {
    public Optional<String> getCurrentAuditor() {
        return Optional.of("system");
    }
}
\`\`\``,tech:"java",displayNumber:487,sortKey:840},{id:"java-841",number:488,title:"Enable JPA auditing",question:"Enable JPA auditing",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"@EnableJpaAuditing activates entity listeners that fill audit fields. Register AuditorAware when actor identity is required.\n\n```java\n@Configuration @EnableJpaAuditing\nclass AuditConfig {}\n```",tech:"java",displayNumber:488,sortKey:841},{id:"java-842",number:489,title:"Auditing entity listener",question:"Auditing entity listener",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`AuditingEntityListener connects lifecycle callbacks to Spring Data's auditing handler. Add it to entities or a mapped superclass, and do not confuse it with database triggers.

\`\`\`java
@EntityListeners(AuditingEntityListener.class)
@MappedSuperclass class Audited {
    @CreatedDate Instant created;
}
\`\`\``,tech:"java",displayNumber:489,sortKey:842},{id:"java-843",number:490,title:"Authentication versus authorization",question:"Authentication versus authorization",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Authentication establishes who the caller is; authorization decides what that principal may do. Spring Security stores the authenticated principal in SecurityContext for downstream checks.\n\n```java\nSecurityContextHolder.getContext().getAuthentication().getName();\n```",tech:"java",displayNumber:490,sortKey:843},{id:"java-844",number:491,title:"Security filter chain",question:"Security filter chain",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`The filter chain intercepts requests before controllers, extracts credentials, establishes security context, applies CSRF/session rules, and enforces authorization. Order matters.

\`\`\`java
@Bean SecurityFilterChain api(HttpSecurity http) throws Exception {
    return http.authorizeHttpRequests(a -> a
        .requestMatchers("/public/**").permitAll()
        .anyRequest().authenticated()).build();
}
\`\`\``,tech:"java",displayNumber:491,sortKey:844},{id:"java-845",number:492,title:"Password encoding",question:"Password encoding",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:"Passwords must be stored as slow, salted one-way hashes, never plaintext or reversible encryption. DelegatingPasswordEncoder supports an algorithm id and future migration.\n\n```java\nPasswordEncoder encoder = PasswordEncoderFactories.createDelegatingPasswordEncoder();\nString hash = encoder.encode(rawPassword);\n```",tech:"java",displayNumber:492,sortKey:845},{id:"java-846",number:493,title:"Method security",question:"Method security",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Method security enforces authorization at service methods, including calls not originating from HTTP. Enable it and express rules with roles or authorities.

\`\`\`java
@EnableMethodSecurity
@PreAuthorize("hasRole('ADMIN')")
void deleteUser(long id) {}
\`\`\``,tech:"java",displayNumber:493,sortKey:846},{id:"java-847",number:494,title:"CSRF and stateless APIs",question:"CSRF and stateless APIs",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"CSRF protects browser sessions because browsers attach cookies automatically. A stateless bearer-token API commonly disables CSRF only when it does not authenticate through cookies.\n\n```java\nhttp.csrf(csrf -> csrf.disable())\n    .oauth2ResourceServer(o -> o.jwt());\n```",tech:"java",displayNumber:494,sortKey:847},{id:"java-848",number:495,title:"JWT structure",question:"JWT structure",difficulty:"Basic",category:"JPA, Hibernate & Security",answer:'A JWT has base64url header, claims payload, and signature separated by dots. The signature authenticates integrity; claims such as exp and iss still require validation.\n\n```java\nString[] parts = token.split("\\\\.");\nassert parts.length == 3; // header.payload.signature\n```',tech:"java",displayNumber:495,sortKey:848},{id:"java-849",number:496,title:"JWT validation",question:"JWT validation",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:'Validate signature, issuer, audience, expiration, and required scopes using a trusted key source. Never trust decoded payload claims before signature verification.\n\n```java\nJwtDecoder decoder = JwtDecoders.fromIssuerLocation("https://issuer.example");\n```',tech:"java",displayNumber:496,sortKey:849},{id:"java-850",number:497,title:"OAuth2 resource server",question:"OAuth2 resource server",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:"A resource server accepts access tokens issued by an authorization server and maps validated scopes/claims to authorities. It does not issue tokens.\n\n```java\nhttp.oauth2ResourceServer(o -> o\n    .jwt(jwt -> jwt.jwtAuthenticationConverter(converter)));\n```",tech:"java",displayNumber:497,sortKey:850},{id:"java-851",number:498,title:"OAuth2 scopes and authorities",question:"OAuth2 scopes and authorities",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Scopes represent delegated permissions and are commonly mapped to SCOPE_x authorities. Keep endpoint requirements least-privilege and distinguish roles from scopes.

\`\`\`java
@PreAuthorize("hasAuthority('SCOPE_orders.read')")
List<Order> orders() {}
\`\`\``,tech:"java",displayNumber:498,sortKey:851},{id:"java-852",number:499,title:"JWT method security",question:"JWT method security",difficulty:"Advanced",category:"JPA, Hibernate & Security",answer:`Combine request and method checks so defense in depth protects service calls. Use a stable claim-to-authority converter and test missing, expired, and insufficient scopes.

\`\`\`java
@PreAuthorize("#owner == authentication.name or hasAuthority('SCOPE_admin')")
Order read(String owner) {}
\`\`\``,tech:"java",displayNumber:499,sortKey:852},{id:"java-853",number:500,title:"Spring Security test support",question:"Spring Security test support",difficulty:"Intermediate",category:"JPA, Hibernate & Security",answer:`Spring Security test annotations like @WithMockUser and @WithUserDetails simulate authentication in integration tests without a full login flow. Use them to verify authorization logic.

\`\`\`java
@Test @WithMockUser(roles="ADMIN")
void adminCanDelete() { ... }

@Test @WithMockUser(username="alice")
void userSeesOwnData() { ... }
\`\`\``,tech:"java",displayNumber:500,sortKey:853},{id:"java-854",number:1,title:"Find the sum of all numbers greater than 10 using stream",question:"Find the sum of all numbers greater than 10 using stream",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `filter` to keep elements matching the predicate, then collect (or `mapToInt().sum()` for an actual sum). `filter` keeps only the elements where the lambda returns true.\n\n```java\nList<Integer> numbers = List.of(10, 11, 12, 15);\nList<Integer> list = numbers.stream()\n        .filter(i -> i > 10)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 11 12 15\n```",tech:"java",displayNumber:1,sortKey:854},{id:"java-855",number:2,title:"Convert List<Integer> to List<String>",question:"Convert List<Integer> to List<String>",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `map` with `String::valueOf` to transform each element, then collect. `map` applies a function to every element and returns a new stream of results.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nList<String> list = numbers.stream()\n        .map(String::valueOf)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 2 3 4 5\n```",tech:"java",displayNumber:2,sortKey:855},{id:"java-856",number:3,title:"Find all even numbers using Streams",question:"Find all even numbers using Streams",difficulty:"Basic",category:"Java Streams & Lambdas",answer:`Filter elements where \`i % 2 == 0\`. The modulo predicate keeps only even values.

\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);
List<Integer> list = numbers.stream()
        .filter(i -> i % 2 == 0)
        .collect(Collectors.toList());
list.forEach(System.out::println);
// Output: 2 4
\`\`\``,tech:"java",displayNumber:3,sortKey:856},{id:"java-857",number:4,title:"Find all odd numbers using stream",question:"Find all odd numbers using stream",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Filter with `(i & 1) == 1` (bitwise AND) or `i % 2 != 0`. The lowest bit is 1 for odd numbers.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nList<Integer> list = numbers.stream()\n        .filter(i -> (i & 1) == 1)\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 3 5\n```",tech:"java",displayNumber:4,sortKey:857},{id:"java-858",number:5,title:"Find duplicate elements",question:"Find duplicate elements",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Use a `HashSet`'s `add` return value: `add` returns false when the element already exists, so `!seen.add(i)` is true only for duplicates.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nSet<Integer> seen = new HashSet<>();\nSet<Integer> list = numbers.stream()\n        .filter(i -> !seen.add(i))\n        .collect(Collectors.toSet());\nlist.forEach(System.out::println);\n// Output: 3\n```",tech:"java",displayNumber:5,sortKey:858},{id:"java-859",number:6,title:"Find unique elements while preserving order",question:"Find unique elements while preserving order",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"`distinct()` removes duplicates while preserving encounter order. A `HashSet` with `seen.add(i)` also works but a Set doesn't preserve order — prefer `distinct()` into a List.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nList<Integer> list = numbers.stream()\n        .distinct()\n        .collect(Collectors.toList());\nlist.forEach(System.out::println);\n// Output: 1 2 3 5\n```",tech:"java",displayNumber:6,sortKey:859},{id:"java-860",number:7,title:"Find the second-highest number",question:"Find the second-highest number",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Remove duplicates, sort in reverse order, `skip(1)` to drop the highest, then take `findFirst`. Returns an `Optional` in case the stream is too small.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> res = numbers.stream()\n        .distinct()\n        .sorted(Comparator.reverseOrder())\n        .skip(1)\n        .findFirst();\nSystem.out.println(res.get());\n// Output: 3\n```",tech:"java",displayNumber:7,sortKey:860},{id:"java-861",number:8,title:"Find the second lowest number",question:"Find the second lowest number",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`Same idea as second-highest but sort ascending (natural order), skip the lowest, then \`findFirst\`.

\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 3, 5);
Optional<Integer> res = numbers.stream()
        .distinct()
        .sorted()
        .skip(1)
        .findFirst();
System.out.println(res.get());
// Output: 2
\`\`\``,tech:"java",displayNumber:8,sortKey:861},{id:"java-862",number:9,title:"Find maximum and minimum",question:"Find maximum and minimum",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Use `max`/`min` with a comparator (returns `Optional`), or `mapToInt` to an `IntStream` and call `.max().orElseThrow()` for a primitive result.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> maximum = numbers.stream().max(Integer::compareTo);\nOptional<Integer> minimum = numbers.stream().min(Integer::compareTo);\nSystem.out.println(maximum.get()); // 5\nSystem.out.println(minimum.get()); // 1\n\nint max = numbers.stream().mapToInt(i -> i).max().orElseThrow();\nSystem.out.println(max); // 5\n```",tech:"java",displayNumber:9,sortKey:862},{id:"java-863",number:10,title:"Count numbers greater than a value",question:"Count numbers greater than a value",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"Filter by the condition, then `count()` returns the number of matching elements as a `long`.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nlong count = numbers.stream()\n        .filter(i -> i > 2)\n        .count();\nSystem.out.println(count); // 3\n```",tech:"java",displayNumber:10,sortKey:863},{id:"java-864",number:11,title:"Find first element satisfying a condition",question:"Find first element satisfying a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`filter` then `findFirst` returns an `Optional` with the first matching element in encounter order (short-circuits once found).\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nOptional<Integer> res = numbers.stream()\n        .filter(i -> i > 2)\n        .findFirst();\nSystem.out.println(res.get()); // 3\n```",tech:"java",displayNumber:11,sortKey:864},{id:"java-865",number:12,title:"Check whether all elements satisfy a condition",question:"Check whether all elements satisfy a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`allMatch(predicate)` returns true if every element matches (short-circuits on the first mismatch). For an empty stream it returns true.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().allMatch(n -> n > 0);\nSystem.out.println(res); // true\n```",tech:"java",displayNumber:12,sortKey:865},{id:"java-866",number:13,title:"Check whether any element satisfies a condition",question:"Check whether any element satisfies a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`anyMatch(predicate)` returns true if at least one element matches, short-circuiting on the first success.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().anyMatch(n -> n % 2 == 0);\nSystem.out.println(res); // true\n```",tech:"java",displayNumber:13,sortKey:866},{id:"java-867",number:14,title:"Check whether no element satisfies a condition",question:"Check whether no element satisfies a condition",difficulty:"Basic",category:"Java Streams & Lambdas",answer:"`noneMatch(predicate)` returns true if no element matches. Useful for validating that a collection contains no invalid values.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 3, 5);\nboolean res = numbers.stream().noneMatch(n -> n < 0);\nSystem.out.println(res); // true\n```",tech:"java",displayNumber:14,sortKey:867},{id:"java-868",number:15,title:"Convert strings to uppercase",question:"Convert strings to uppercase",difficulty:"Basic",category:"Java Streams & Lambdas",answer:`Map each string with the \`String::toUpperCase\` method reference and collect into a list.

\`\`\`java
List<String> numbers = List.of("a", "b", "c");
List<String> res = numbers.stream()
        .map(String::toUpperCase)
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: A B C
\`\`\``,tech:"java",displayNumber:15,sortKey:868},{id:"java-869",number:16,title:"Sort strings by length",question:"Sort strings by length",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Use `sorted` with `Comparator.comparingInt(String::length)` to order elements by their length ascending.\n\n```java\nList<String> numbers = List.of("aA", "bBBB", "cA", "M");\nList<String> res = numbers.stream()\n        .sorted(Comparator.comparingInt(String::length))\n        .collect(Collectors.toList());\nres.forEach(System.out::println);\n// Output: M aA cA bBBB\n```',tech:"java",displayNumber:16,sortKey:869},{id:"java-870",number:17,title:"Sort strings by length in descending order",question:"Sort strings by length in descending order",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`Chain \`.reversed()\` onto the comparator to flip the ordering to descending.

\`\`\`java
List<String> numbers = List.of("aA", "bBBB", "cA", "m");
List<String> res = numbers.stream()
        .sorted(Comparator.comparingInt(String::length).reversed())
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: bBBB cA aA m
\`\`\``,tech:"java",displayNumber:17,sortKey:870},{id:"java-871",number:18,title:"Flatten List<List<Integer>>",question:"Flatten List<List<Integer>>",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:`\`flatMap(List::stream)\` turns each inner list into a stream and concatenates them into one flat stream of elements.

\`\`\`java
List<List<Integer>> numbers = Arrays.asList(
        Arrays.asList(1, 2),
        Arrays.asList(3, 4),
        Arrays.asList(5, 6));
List<Integer> res = numbers.stream()
        .flatMap(List::stream)
        .collect(Collectors.toList());
res.forEach(System.out::println);
// Output: 1 2 3 4 5 6
\`\`\``,tech:"java",displayNumber:18,sortKey:871},{id:"java-872",number:19,title:"Find unique words from sentences",question:"Find unique words from sentences",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`flatMap` each sentence into a stream of words (split on whitespace), then collect into a `Set` to deduplicate.\n\n```java\nList<String> sentences = Arrays.asList("Java is powerful", "Java is popular");\nSet<String> res = sentences.stream()\n        .flatMap(sentence -> Arrays.stream(sentence.split("\\\\s+")))\n        .collect(Collectors.toSet());\nres.forEach(System.out::println);\n// Output: Java, is, powerful, popular\n```',tech:"java",displayNumber:19,sortKey:872},{id:"java-873",number:20,title:"Character frequency using stream",question:"Character frequency using stream",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`chars()` gives an `IntStream` of code points; box to `Character`, then group by identity counting occurrences with `Collectors.groupingBy` + `Collectors.counting`.\n\n```java\nString word = "kaushik";\nMap<Character, Long> freq = word.chars()\n        .mapToObj(c -> (char) c)\n        .collect(Collectors.groupingBy(\n                Function.identity(), Collectors.counting()));\nfreq.forEach((ch, count) -> System.out.println(ch + " : " + count));\n```',tech:"java",displayNumber:20,sortKey:873},{id:"java-874",number:21,title:"Word frequency across sentences",question:"Word frequency across sentences",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'Split each sentence into words with `flatMap`, then `groupingBy(identity, counting)` builds a word→count map.\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nMap<String, Long> freq = sentences.stream()\n        .flatMap(s -> Arrays.stream(s.split("\\\\s+")))\n        .collect(Collectors.groupingBy(Function.identity(), Collectors.counting()));\nfreq.forEach((word, count) -> System.out.println(word + " : " + count));\n// Output: java:3 is:3 good:1 powerful:1 easy:1\n```',tech:"java",displayNumber:21,sortKey:874},{id:"java-875",number:22,title:"Find the longest string",question:"Find the longest string",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Use `max` with `Comparator.comparingInt(String::length)` to get the element with the greatest length.\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nOptional<String> longest = sentences.stream()\n        .max(Comparator.comparingInt(String::length));\nSystem.out.println(longest.get()); // java is powerful\n```',tech:"java",displayNumber:22,sortKey:875},{id:"java-876",number:23,title:"Find the shortest string",question:"Find the shortest string",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'Same comparator but use `min` to get the element with the smallest length.\n\n```java\nList<String> sentences = Arrays.asList("java is good", "java is powerful", "java is easy");\nOptional<String> shortest = sentences.stream()\n        .min(Comparator.comparingInt(String::length));\nSystem.out.println(shortest.get()); // java is good\n```',tech:"java",displayNumber:23,sortKey:876},{id:"java-877",number:24,title:"Sort employees by salary descending",question:"Sort employees by salary descending",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:"Use `sorted` with `Comparator.comparingDouble(Employee::getSalary).reversed()` to order objects by a numeric field, highest first.\n\n```java\nclass Employee { String name; String department; double salary; }\n\nList<Employee> result = employees.stream()\n        .sorted(Comparator.comparingDouble(Employee::getSalary).reversed())\n        .collect(Collectors.toList());\n```",tech:"java",displayNumber:24,sortKey:877},{id:"java-878",number:25,title:"Group, aggregate and transform employees with collectors",question:"Group, aggregate and transform employees with collectors",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`\`Collectors\` power common aggregations: \`toMap\` builds id→name, \`groupingBy\` buckets by department, and downstream collectors (\`averagingDouble\`, \`summingDouble\`, \`counting\`) compute per-group stats. Combine \`groupingBy\` with a downstream collector for one-pass aggregation.

\`\`\`java
// id -> name
Map<Integer, String> idToName = employees.stream()
        .collect(Collectors.toMap(Employee::getId, Employee::getName));

// department -> employees
Map<String, List<Employee>> byDept = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment));

// department -> average salary
Map<String, Double> avgSalary = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment,
                Collectors.averagingDouble(Employee::getSalary)));

// department -> total salary
Map<String, Double> sumSalary = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment,
                Collectors.summingDouble(Employee::getSalary)));

// department -> headcount
Map<String, Long> deptCount = employees.stream()
        .collect(Collectors.groupingBy(Employee::getDepartment, Collectors.counting()));

// names appearing more than once
Map<String, Long> nameFreq = employees.stream()
        .collect(Collectors.groupingBy(Employee::getName, Collectors.counting()));
nameFreq.entrySet().stream()
        .filter(e -> e.getValue() > 1)
        .forEach(System.out::println);
\`\`\``,tech:"java",displayNumber:25,sortKey:878},{id:"java-879",number:26,title:"Partition numbers into even and odd",question:"Partition numbers into even and odd",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.partitioningBy(predicate)` splits a stream into a `Map<Boolean, List<T>>` with exactly two keys (`true`/`false`). It's more efficient than `groupingBy` when there are only two buckets.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5, 6);\nMap<Boolean, List<Integer>> parts = numbers.stream()\n        .collect(Collectors.partitioningBy(n -> n % 2 == 0));\nSystem.out.println(parts.get(true));  // [2, 4, 6]\nSystem.out.println(parts.get(false)); // [1, 3, 5]\n```",tech:"java",displayNumber:26,sortKey:879},{id:"java-880",number:27,title:"Sum a numeric field with reduce",question:"Sum a numeric field with reduce",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`\`reduce(identity, accumulator)\` folds the stream into a single value. The identity is the start/empty value; the accumulator combines the running result with each element.

\`\`\`java
List<Integer> numbers = List.of(1, 2, 3, 4, 5);
int sum = numbers.stream().reduce(0, Integer::sum);
System.out.println(sum); // 15

// product via reduce
int product = numbers.stream().reduce(1, (a, b) -> a * b);
System.out.println(product); // 120
\`\`\``,tech:"java",displayNumber:27,sortKey:880},{id:"java-881",number:28,title:"Join strings with a delimiter, prefix and suffix",question:"Join strings with a delimiter, prefix and suffix",difficulty:"Intermediate",category:"Java Streams & Lambdas",answer:'`Collectors.joining(delimiter, prefix, suffix)` concatenates the stream of strings into one string with separators and optional wrapping.\n\n```java\nList<String> names = List.of("Rahul", "Amit", "Priya");\nString csv = names.stream()\n        .collect(Collectors.joining(", ", "[", "]"));\nSystem.out.println(csv); // [Rahul, Amit, Priya]\n```',tech:"java",displayNumber:28,sortKey:881},{id:"java-882",number:29,title:"Get statistics (count, sum, min, max, avg) in one pass",question:"Get statistics (count, sum, min, max, avg) in one pass",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.summarizingInt/Double` (or `IntStream.summaryStatistics()`) computes count, sum, min, max, and average in a single traversal, returning an `IntSummaryStatistics`.\n\n```java\nList<Integer> numbers = List.of(1, 2, 3, 4, 5);\nIntSummaryStatistics stats = numbers.stream()\n        .collect(Collectors.summarizingInt(i -> i));\nSystem.out.println(stats.getSum());     // 15\nSystem.out.println(stats.getAverage()); // 3.0\nSystem.out.println(stats.getMax());     // 5\n```",tech:"java",displayNumber:29,sortKey:882},{id:"java-883",number:30,title:"Build a Map handling duplicate keys (merge function)",question:"Build a Map handling duplicate keys (merge function)",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`Collectors.toMap` throws `IllegalStateException` on duplicate keys unless you supply a merge function to resolve collisions (e.g. keep the first, sum values).\n\n```java\nList<String> words = List.of("apple", "banana", "avocado", "cherry");\nMap<Character, String> byFirst = words.stream()\n        .collect(Collectors.toMap(\n                w -> w.charAt(0),\n                w -> w,\n                (existing, replacement) -> existing)); // keep first on clash\nSystem.out.println(byFirst); // {a=apple, b=banana, c=cherry}\n```',tech:"java",displayNumber:30,sortKey:883},{id:"java-884",number:31,title:"Group by a field and map to another (downstream mapping)",question:"Group by a field and map to another (downstream mapping)",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`groupingBy(classifier, Collectors.mapping(mapper, toList()))` groups then transforms each group's elements, e.g. department → list of employee names.\n\n```java\nMap<String, List<String>> deptNames = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.mapping(Employee::getName, Collectors.toList())));\nSystem.out.println(deptNames);\n```",tech:"java",displayNumber:31,sortKey:884},{id:"java-885",number:32,title:"Find the highest-paid employee per department",question:"Find the highest-paid employee per department",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'Use `groupingBy` with the downstream `Collectors.maxBy(comparator)`, which yields an `Optional` per group. Wrap with `collectingAndThen` to unwrap the Optional if desired.\n\n```java\nMap<String, Optional<Employee>> topPaid = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.maxBy(Comparator.comparingDouble(Employee::getSalary))));\ntopPaid.forEach((dept, emp) ->\n        System.out.println(dept + " -> " + emp.map(Employee::getName).orElse("none")));\n```',tech:"java",displayNumber:32,sortKey:885},{id:"java-886",number:33,title:"Two-level (nested) grouping",question:"Two-level (nested) grouping",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:"Nest `groupingBy` inside `groupingBy` to bucket by two keys, producing a `Map<K1, Map<K2, List<T>>>` — e.g. department then gender.\n\n```java\nMap<String, Map<String, List<Employee>>> byDeptThenGender = employees.stream()\n        .collect(Collectors.groupingBy(\n                Employee::getDepartment,\n                Collectors.groupingBy(Employee::getGender)));\nSystem.out.println(byDeptThenGender);\n```",tech:"java",displayNumber:33,sortKey:886},{id:"java-887",number:34,title:"Count occurrences and sort the result by frequency",question:"Count occurrences and sort the result by frequency",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:`Group-count into a map, then stream its entries, sort by value descending, and collect back into an ordered \`LinkedHashMap\` to preserve the sorted order.

\`\`\`java
List<String> items = List.of("a", "b", "a", "c", "b", "a");
Map<String, Long> sorted = items.stream()
        .collect(Collectors.groupingBy(s -> s, Collectors.counting()))
        .entrySet().stream()
        .sorted(Map.Entry.<String, Long>comparingByValue().reversed())
        .collect(Collectors.toMap(
                Map.Entry::getKey, Map.Entry::getValue,
                (a, b) -> a, LinkedHashMap::new));
System.out.println(sorted); // {a=3, b=2, c=1}
\`\`\``,tech:"java",displayNumber:34,sortKey:887},{id:"java-888",number:35,title:"Multi-field sorting with thenComparing",question:"Multi-field sorting with thenComparing",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"Chain `Comparator.comparing(...).thenComparing(...)` to sort by a primary key, breaking ties with a secondary key — e.g. by department, then salary descending.\n\n```java\nList<Employee> sorted = employees.stream()\n        .sorted(Comparator.comparing(Employee::getDepartment)\n                .thenComparing(Comparator.comparingDouble(Employee::getSalary).reversed()))\n        .collect(Collectors.toList());\nsorted.forEach(System.out::println);\n```",tech:"java",displayNumber:35,sortKey:888},{id:"java-889",number:36,title:"flatMap over map entries to invert a multimap",question:"flatMap over map entries to invert a multimap",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'Stream `entrySet()`, `flatMap` each value list into (value, key) pairs, then collect — a common technique to invert a `Map<K, List<V>>` into `Map<V, K>`.\n\n```java\nMap<String, List<Integer>> deptIds = Map.of(\n        "HR", List.of(1, 2), "IT", List.of(3, 4));\nMap<Integer, String> idToDept = deptIds.entrySet().stream()\n        .flatMap(e -> e.getValue().stream()\n                .map(id -> Map.entry(id, e.getKey())))\n        .collect(Collectors.toMap(Map.Entry::getKey, Map.Entry::getValue));\nSystem.out.println(idToDept); // {1=HR, 2=HR, 3=IT, 4=IT}\n```',tech:"java",displayNumber:36,sortKey:889},{id:"java-890",number:37,title:"takeWhile and dropWhile",question:"takeWhile and dropWhile",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`takeWhile` keeps a prefix while the predicate holds (stops at the first failure); `dropWhile` discards that prefix and keeps the rest. Both are for ordered streams (Java 9+).\n\n```java\nList<Integer> numbers = List.of(2, 4, 6, 7, 8, 10);\nSystem.out.println(numbers.stream().takeWhile(n -> n % 2 == 0).toList()); // [2, 4, 6]\nSystem.out.println(numbers.stream().dropWhile(n -> n % 2 == 0).toList()); // [7, 8, 10]\n```",tech:"java",displayNumber:37,sortKey:890},{id:"java-891",number:38,title:"Generate an infinite stream with iterate and limit",question:"Generate an infinite stream with iterate and limit",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Stream.iterate(seed, next)` produces an infinite sequence; bound it with `limit`. The 3-arg `iterate(seed, hasNext, next)` (Java 9+) adds a built-in stop condition.\n\n```java\n// first 5 powers of 2\nList<Integer> powers = Stream.iterate(1, n -> n * 2)\n        .limit(5)\n        .toList();\nSystem.out.println(powers); // [1, 2, 4, 8, 16]\n\n// iterate with predicate (Java 9+)\nStream.iterate(1, n -> n <= 20, n -> n + 5).forEach(System.out::println); // 1 6 11 16\n```",tech:"java",displayNumber:38,sortKey:891},{id:"java-892",number:39,title:"Three-argument reduce for parallel-safe accumulation",question:"Three-argument reduce for parallel-safe accumulation",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'`reduce(identity, accumulator, combiner)` supports a different result type and parallel execution: the accumulator folds elements into partial results, the combiner merges partials. The combiner must be associative and consistent with the accumulator.\n\n```java\nList<String> words = List.of("java", "stream", "api");\nint totalChars = words.stream()\n        .reduce(0, (sum, w) -> sum + w.length(), Integer::sum);\nSystem.out.println(totalChars); // 13\n```',tech:"java",displayNumber:39,sortKey:892},{id:"java-893",number:40,title:"Write a custom Collector",question:"Write a custom Collector",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:`\`Collector.of(supplier, accumulator, combiner, finisher)\` builds a bespoke reduction. The supplier creates the mutable container, accumulator adds elements, combiner merges containers (parallel), finisher produces the final result.

\`\`\`java
Collector<Integer, ?, String> toRange = Collector.of(
        () -> new int[]{Integer.MAX_VALUE, Integer.MIN_VALUE},
        (acc, i) -> { acc[0] = Math.min(acc[0], i); acc[1] = Math.max(acc[1], i); },
        (a, b) -> new int[]{Math.min(a[0], b[0]), Math.max(a[1], b[1])},
        acc -> acc[0] + ".." + acc[1]);
String range = List.of(4, 1, 9, 3).stream().collect(toRange);
System.out.println(range); // 1..9
\`\`\``,tech:"java",displayNumber:40,sortKey:893},{id:"java-894",number:41,title:"collectingAndThen to produce an immutable result",question:"collectingAndThen to produce an immutable result",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:"`Collectors.collectingAndThen(downstream, finisher)` post-processes a collector's result — e.g. wrap the collected list in `Collections.unmodifiableList` for an immutable view.\n\n```java\nList<Integer> immutable = List.of(1, 2, 3, 4).stream()\n        .filter(n -> n % 2 == 0)\n        .collect(Collectors.collectingAndThen(\n                Collectors.toList(), Collections::unmodifiableList));\nSystem.out.println(immutable); // [2, 4]\n```",tech:"java",displayNumber:41,sortKey:894},{id:"java-895",number:42,title:"Parallel stream and when it helps (or hurts)",question:"Parallel stream and when it helps (or hurts)",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:"`parallelStream()` splits work across the common ForkJoinPool for CPU-bound, large, easily-splittable data with stateless, associative operations. For small data, ordered/stateful ops, or I/O it often hurts due to overhead and contention. Never mutate shared state inside it.\n\n```java\nlong count = LongStream.rangeClosed(1, 10_000_000)\n        .parallel()\n        .filter(n -> n % 2 == 0)\n        .count();\nSystem.out.println(count); // 5000000\n```",tech:"java",displayNumber:42,sortKey:895},{id:"java-896",number:43,title:"Why peek should not be used for logic",question:"Why peek should not be used for logic",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:'`peek` is intended for debugging. It may be skipped entirely when the pipeline can compute results without traversing (e.g. `count()` on a sized stream), and its ordering/execution isn\'t guaranteed under parallelism — so never rely on it for side-effecting logic.\n\n```java\nlong c = Stream.of(1, 2, 3)\n        .peek(x -> System.out.println("peeked " + x)) // may NOT print in some JDKs\n        .count();\nSystem.out.println(c); // 3\n```',tech:"java",displayNumber:43,sortKey:896},{id:"java-897",number:44,title:"Collectors.teeing to compute two aggregates at once",question:"Collectors.teeing to compute two aggregates at once",difficulty:"Experienced",category:"Java Streams & Lambdas",answer:`\`Collectors.teeing(c1, c2, merger)\` (Java 12+) runs two collectors over the same stream in one pass and merges their results — e.g. compute sum and count together to derive an average.

\`\`\`java
double avg = Stream.of(2, 4, 6, 8)
        .collect(Collectors.teeing(
                Collectors.summingInt(i -> i),
                Collectors.counting(),
                (sum, cnt) -> (double) sum / cnt));
System.out.println(avg); // 5.0
\`\`\``,tech:"java",displayNumber:44,sortKey:897},{id:"java-898",number:45,title:"Safely chain nullable lookups with Optional",question:"Safely chain nullable lookups with Optional",difficulty:"Advanced",category:"Java Streams & Lambdas",answer:'`Optional.map`/`flatMap`/`orElseGet` chain transformations that short-circuit on absence, avoiding nested null checks. `filter` can also drop values not matching a condition.\n\n```java\nOptional<Employee> emp = employees.stream()\n        .filter(e -> e.getDepartment().equals("Finance"))\n        .max(Comparator.comparingDouble(Employee::getSalary));\n\nString name = emp.map(Employee::getName)\n        .filter(n -> !n.isBlank())\n        .orElse("No employee");\nSystem.out.println(name);\n```',tech:"java",displayNumber:45,sortKey:898},{id:"java-899",number:1,title:"What are the major features introduced in Java 8?",question:"What are the major features introduced in Java 8?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Lambda expressions, functional interfaces, the Stream API, method references, default and static methods in interfaces, the `Optional` class, a new Date/Time API (`java.time`), and `CompletableFuture`. Together they brought functional-style programming to Java.\n\n```java\nList.of(1, 2, 3).stream().map(n -> n * 2).forEach(System.out::println);\n```",tech:"java",displayNumber:1,sortKey:899},{id:"java-900",number:2,title:"Why was Java 8 considered a major Java release?",question:"Why was Java 8 considered a major Java release?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"It introduced a functional programming paradigm alongside OOP, enabling concise, declarative code with lambdas and streams. It also modernized long-standing pain points (dates, nulls, parallelism), making it the most impactful release since generics.",tech:"java",displayNumber:2,sortKey:900},{id:"java-901",number:3,title:"What is a functional interface?",question:"What is a functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"An interface with exactly one abstract method (SAM — Single Abstract Method). It can be the target type of a lambda expression or method reference. It may also have default/static methods.\n\n```java\n@FunctionalInterface\ninterface Calculator { int apply(int a, int b); }\nCalculator add = (a, b) -> a + b;\n```",tech:"java",displayNumber:3,sortKey:901},{id:"java-902",number:4,title:"What is the `@FunctionalInterface` annotation?",question:"What is the `@FunctionalInterface` annotation?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A marker annotation that tells the compiler to enforce the single-abstract-method rule. It's optional but recommended: if someone adds a second abstract method, compilation fails.\n\n```java\n@FunctionalInterface\ninterface Greeting { void say(String name); }\n```",tech:"java",displayNumber:4,sortKey:902},{id:"java-903",number:5,title:"Can a functional interface have multiple default methods?",question:"Can a functional interface have multiple default methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. A functional interface can have any number of default methods; only the count of abstract methods is restricted to one.\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); default int twice(int x){ return apply(apply(x)); } default int id(int x){ return x; } }\n```",tech:"java",displayNumber:5,sortKey:903},{id:"java-904",number:6,title:"Can a functional interface have static methods?",question:"Can a functional interface have static methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. Static methods don't count as abstract methods, so a functional interface can declare any number of them (e.g. factory helpers like `Function.identity()`).\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); static Fn identity(){ return x -> x; } }\n```",tech:"java",displayNumber:6,sortKey:904},{id:"java-905",number:7,title:"Can a functional interface extend another interface?",question:"Can a functional interface extend another interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes, as long as the combined set still has exactly one abstract method. If the parent already declares the single abstract method and the child adds none, it remains functional.\n\n```java\ninterface A { int run(); }\n@FunctionalInterface\ninterface B extends A {} // still one abstract method\n```",tech:"java",displayNumber:7,sortKey:905},{id:"java-906",number:8,title:"What is a lambda expression?",question:"What is a lambda expression?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A lambda is an anonymous function — a concise way to implement a functional interface\'s single method inline, without a named class. It captures behavior as data you can pass around.\n\n```java\nRunnable r = () -> System.out.println("running");\n```',tech:"java",displayNumber:8,sortKey:906},{id:"java-907",number:9,title:"Why were lambda expressions introduced?",question:"Why were lambda expressions introduced?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"To reduce boilerplate of anonymous inner classes, enable passing behavior as arguments (higher-order functions), and support the Stream API's declarative, functional style. They make code shorter and more readable.",tech:"java",displayNumber:9,sortKey:907},{id:"java-908",number:10,title:"What is the syntax of a lambda expression?",question:"What is the syntax of a lambda expression?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'`(parameters) -> expression` or `(parameters) -> { statements; }`. Parameter types are usually inferred; single parameters need no parentheses; a single expression needs no braces or `return`.\n\n```java\nx -> x * x                     // one param, expression\n(a, b) -> a + b                // two params\n() -> System.out.println("hi") // no params\n(int x) -> { return x + 1; }   // explicit type + block\n```',tech:"java",displayNumber:10,sortKey:908},{id:"java-909",number:11,title:"What is a method reference?",question:"What is a method reference?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A shorthand for a lambda that just calls an existing method. `System.out::println` is equivalent to `x -> System.out.println(x)`. It uses the `::` operator.\n\n```java\nList.of("a", "b").forEach(System.out::println);\n```',tech:"java",displayNumber:11,sortKey:909},{id:"java-910",number:12,title:"What are the different types of method references?",question:"What are the different types of method references?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Four kinds: static (`ClassName::staticMethod`), bound instance (`object::method`), unbound instance (`ClassName::instanceMethod`), and constructor (`ClassName::new`).\n\n```java\nInteger::parseInt        // static\nSystem.out::println      // bound instance\nString::toUpperCase      // unbound instance\nArrayList::new           // constructor\n```",tech:"java",displayNumber:12,sortKey:910},{id:"java-911",number:13,title:"Lambda vs anonymous inner class?",question:"Lambda vs anonymous inner class?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A lambda is more concise, has no separate `.class` file, and `this` refers to the enclosing instance. An anonymous class creates its own scope, can implement multiple methods/have state, and its `this` refers to the anonymous instance.\n\n```java\nRunnable a = () -> System.out.println(this);       // enclosing 'this'\nRunnable b = new Runnable(){ public void run(){ System.out.println(this); } }; // its own 'this'\n```",tech:"java",displayNumber:13,sortKey:911},{id:"java-912",number:14,title:"What is a closure in Java?",question:"What is a closure in Java?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:`A closure is a function that captures variables from its enclosing scope. Java lambdas are closures: they can reference effectively-final local variables, "closing over" their values.

\`\`\`java
int factor = 3;
Function<Integer,Integer> multiply = n -> n * factor; // captures 'factor'
\`\`\``,tech:"java",displayNumber:14,sortKey:912},{id:"java-913",number:15,title:"What is effectively final?",question:"What is effectively final?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A local variable that is never reassigned after initialization, even without the `final` keyword. Lambdas and anonymous classes may only capture effectively-final locals.\n\n```java\nint x = 10;            // never reassigned -> effectively final\nRunnable r = () -> System.out.println(x);\n```",tech:"java",displayNumber:15,sortKey:913},{id:"java-914",number:16,title:"Why can a lambda access only final/effectively-final local variables?",question:"Why can a lambda access only final/effectively-final local variables?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:'Captured locals are copied into the lambda; the local variable lives on the stack and may be gone when the lambda runs later. Requiring immutability avoids the ambiguity of which value/copy is "current" and prevents subtle concurrency bugs.',tech:"java",displayNumber:16,sortKey:914},{id:"java-915",number:17,title:"What is a target type?",question:"What is a target type?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'The expected functional-interface type of a lambda in a given context (assignment, method argument, return). The compiler infers the lambda\'s type from this target, since a lambda has no type on its own.\n\n```java\nRunnable r = () -> {};          // target type: Runnable\nCallable<String> c = () -> "x"; // same-shaped lambda, different target\n```',tech:"java",displayNumber:17,sortKey:915},{id:"java-916",number:18,title:"Can a lambda exist without a functional interface?",question:"Can a lambda exist without a functional interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. A lambda has no intrinsic type; it must be assigned to (or inferred as) a functional interface. Without a target functional type, it won't compile.",tech:"java",displayNumber:18,sortKey:916},{id:"java-917",number:19,title:"What is a default method?",question:"What is a default method?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A method in an interface with a body, declared with the `default` keyword. Implementing classes inherit it without overriding, enabling interfaces to gain behavior.\n\n```java\ninterface Vehicle { default void start(){ System.out.println("starting"); } }\n```',tech:"java",displayNumber:19,sortKey:917},{id:"java-918",number:20,title:"Why were default methods introduced?",question:"Why were default methods introduced?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"To allow evolving interfaces without breaking existing implementations. Adding `default` methods (e.g. `List.forEach`, `Collection.stream`) let the JDK enhance interfaces while keeping backward compatibility.",tech:"java",displayNumber:20,sortKey:918},{id:"java-919",number:21,title:"What is a static method inside an interface?",question:"What is a static method inside an interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A method with a body belonging to the interface itself (not instances), called via the interface name. Useful for utility/factory methods related to the interface.\n\n```java\ninterface Util { static int square(int x){ return x * x; } }\nint r = Util.square(5);\n```",tech:"java",displayNumber:21,sortKey:919},{id:"java-920",number:22,title:"Can an interface have both default and static methods?",question:"Can an interface have both default and static methods?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes. An interface can mix abstract, default, and static methods. `Function`, for example, has an abstract `apply`, default `andThen`/`compose`, and static `identity`.",tech:"java",displayNumber:22,sortKey:920},{id:"java-921",number:23,title:"Can a default method be overridden?",question:"Can a default method be overridden?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Yes. An implementing class or sub-interface can override a default method to provide its own behavior; the override wins over the inherited default.\n\n```java\nclass Car implements Vehicle { public void start(){ System.out.println("car starts"); } }\n```',tech:"java",displayNumber:23,sortKey:921},{id:"java-922",number:24,title:"Can a static interface method be overridden?",question:"Can a static interface method be overridden?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. Static methods are not inherited or polymorphic; they belong to the interface. A class can declare its own static method with the same name, but that's hiding, not overriding.",tech:"java",displayNumber:24,sortKey:922},{id:"java-923",number:25,title:"What happens when two interfaces contain the same default method?",question:"What happens when two interfaces contain the same default method?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:'The "diamond problem": the class won\'t compile until it overrides the method. It can delegate to a specific one via `InterfaceName.super.method()`.\n\n```java\nclass C implements A, B {\n    public void hello(){ A.super.hello(); } // resolve ambiguity explicitly\n}\n```',tech:"java",displayNumber:25,sortKey:923},{id:"java-924",number:26,title:"What is `Predicate<T>`?",question:"What is `Predicate<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A functional interface representing a boolean-valued condition: `boolean test(T t)`. Widely used in `filter`.\n\n```java\nPredicate<Integer> isEven = n -> n % 2 == 0;\nSystem.out.println(isEven.test(4)); // true\n```",tech:"java",displayNumber:26,sortKey:924},{id:"java-925",number:27,title:"What is `Function<T,R>`?",question:"What is `Function<T,R>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'Represents a transformation from an input of type T to a result of type R: `R apply(T t)`. Used in `map`.\n\n```java\nFunction<String,Integer> length = String::length;\nSystem.out.println(length.apply("java")); // 4\n```',tech:"java",displayNumber:27,sortKey:925},{id:"java-926",number:28,title:"What is `Consumer<T>`?",question:"What is `Consumer<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'Represents an operation that accepts one input and returns nothing (side effect only): `void accept(T t)`. Used in `forEach`.\n\n```java\nConsumer<String> print = System.out::println;\nprint.accept("hi");\n```',tech:"java",displayNumber:28,sortKey:926},{id:"java-927",number:29,title:"What is `Supplier<T>`?",question:"What is `Supplier<T>`?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Represents a source of values, taking no input and returning a result: `T get()`. Used for lazy generation/defaults.\n\n```java\nSupplier<Double> random = Math::random;\nSystem.out.println(random.get());\n```",tech:"java",displayNumber:29,sortKey:927},{id:"java-928",number:30,title:"Difference between Predicate and Function?",question:"Difference between Predicate and Function?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"`Predicate<T>` always returns a `boolean` (`test`), used for conditions. `Function<T,R>` returns any type R (`apply`), used for transformations. A Predicate is essentially a `Function<T, Boolean>` specialized for filtering.",tech:"java",displayNumber:30,sortKey:928},{id:"java-929",number:31,title:"Difference between Consumer and Supplier?",question:"Difference between Consumer and Supplier?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"`Consumer<T>` takes input and returns nothing (consumes/uses a value). `Supplier<T>` takes nothing and produces a value. They are opposites: one is a sink, the other a source.",tech:"java",displayNumber:31,sortKey:929},{id:"java-930",number:32,title:"What is `UnaryOperator<T>`?",question:"What is `UnaryOperator<T>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'A specialization of `Function<T,T>` where input and output are the same type. Used in operations like `List.replaceAll`.\n\n```java\nUnaryOperator<String> upper = String::toUpperCase;\nSystem.out.println(upper.apply("java")); // JAVA\n```',tech:"java",displayNumber:32,sortKey:930},{id:"java-931",number:33,title:"What is `BinaryOperator<T>`?",question:"What is `BinaryOperator<T>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A specialization of `BiFunction<T,T,T>` where both inputs and the result share one type. Used in `reduce`.\n\n```java\nBinaryOperator<Integer> sum = Integer::sum;\nSystem.out.println(sum.apply(3, 4)); // 7\n```",tech:"java",displayNumber:33,sortKey:931},{id:"java-932",number:34,title:"What is `BiFunction<T,U,R>`?",question:"What is `BiFunction<T,U,R>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Represents a function taking two inputs (types T and U) and producing a result R: `R apply(T t, U u)`.\n\n```java\nBiFunction<Integer,Integer,String> combine = (a, b) -> (a + b) + "";\nSystem.out.println(combine.apply(2, 3)); // "5"\n```',tech:"java",displayNumber:34,sortKey:932},{id:"java-933",number:35,title:"What is `BiPredicate<T,U>`?",question:"What is `BiPredicate<T,U>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'A predicate of two arguments: `boolean test(T t, U u)`. Useful for conditions involving two values.\n\n```java\nBiPredicate<String,Integer> longerThan = (s, n) -> s.length() > n;\nSystem.out.println(longerThan.test("java", 2)); // true\n```',tech:"java",displayNumber:35,sortKey:933},{id:"java-934",number:36,title:"What is `BiConsumer<T,U>`?",question:"What is `BiConsumer<T,U>`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Accepts two inputs and returns nothing: `void accept(T t, U u)`. Used by `Map.forEach` to consume key-value pairs.\n\n```java\nBiConsumer<String,Integer> print = (k, v) -> System.out.println(k + "=" + v);\nMap.of("a", 1).forEach(print);\n```',tech:"java",displayNumber:36,sortKey:934},{id:"java-935",number:37,title:"Difference between `Function` and `UnaryOperator`?",question:"Difference between `Function` and `UnaryOperator`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`Function<T,R>` maps T to a possibly different type R. `UnaryOperator<T>` extends `Function<T,T>`, constraining input and output to the same type — a convenience for same-type transformations.",tech:"java",displayNumber:37,sortKey:935},{id:"java-936",number:38,title:"Difference between `BiFunction` and `BinaryOperator`?",question:"Difference between `BiFunction` and `BinaryOperator`?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`BiFunction<T,U,R>` takes two inputs of any types and returns any type. `BinaryOperator<T>` extends `BiFunction<T,T,T>` — both inputs and the output are the same type, used for combining like values (e.g. reduce).",tech:"java",displayNumber:38,sortKey:936},{id:"java-937",number:39,title:"How does `Predicate.and()` work?",question:"How does `Predicate.and()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`and` returns a composed predicate that is true only if both predicates are true, short-circuiting (the second isn't evaluated if the first is false).\n\n```java\nPredicate<Integer> positive = n -> n > 0;\nPredicate<Integer> even = n -> n % 2 == 0;\nSystem.out.println(positive.and(even).test(4)); // true\n```",tech:"java",displayNumber:39,sortKey:937},{id:"java-938",number:40,title:"How does `Predicate.or()` work?",question:"How does `Predicate.or()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`or` returns a composed predicate that is true if either predicate is true, short-circuiting when the first is true.\n\n```java\nPredicate<Integer> neg = n -> n < 0;\nPredicate<Integer> big = n -> n > 100;\nSystem.out.println(neg.or(big).test(150)); // true\n```",tech:"java",displayNumber:40,sortKey:938},{id:"java-939",number:41,title:"How does `Predicate.negate()` work?",question:"How does `Predicate.negate()` work?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"`negate` returns a predicate representing the logical inverse of the original.\n\n```java\nPredicate<Integer> even = n -> n % 2 == 0;\nSystem.out.println(even.negate().test(3)); // true (3 is odd)\n```",tech:"java",displayNumber:41,sortKey:939},{id:"java-940",number:42,title:"What are primitive functional interfaces?",question:"What are primitive functional interfaces?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Specialized functional interfaces for primitive types (`IntPredicate`, `IntFunction`, `IntUnaryOperator`, `ToIntFunction`, `IntConsumer`, `IntSupplier`, etc.) that avoid autoboxing overhead by working directly with `int`/`long`/`double`.\n\n```java\nIntPredicate isPositive = n -> n > 0; // no Integer boxing\n```",tech:"java",displayNumber:42,sortKey:940},{id:"java-941",number:43,title:"Why do `IntPredicate`, `IntFunction`, `IntConsumer`, etc. exist?",question:"Why do `IntPredicate`, `IntFunction`, `IntConsumer`, etc. exist?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"To eliminate the performance cost of autoboxing/unboxing between primitives and wrapper objects in hot code paths (like large streams), reducing memory churn and improving speed.",tech:"java",displayNumber:43,sortKey:941},{id:"java-942",number:44,title:"How do you create your own functional interface?",question:"How do you create your own functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Declare an interface with a single abstract method and (optionally) annotate it `@FunctionalInterface`. Then it can be targeted by lambdas.\n\n```java\n@FunctionalInterface\ninterface TriFunction<A,B,C,R> { R apply(A a, B b, C c); }\nTriFunction<Integer,Integer,Integer,Integer> sum3 = (a, b, c) -> a + b + c;\n```",tech:"java",displayNumber:44,sortKey:942},{id:"java-943",number:45,title:"Can a functional interface have methods inherited from Object?",question:"Can a functional interface have methods inherited from Object?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Yes. Abstract declarations that override `Object`'s public methods (`equals`, `hashCode`, `toString`) don't count toward the single-abstract-method rule, so the interface stays functional.\n\n```java\n@FunctionalInterface\ninterface Fn { int apply(int x); boolean equals(Object o); } // still functional\n```",tech:"java",displayNumber:45,sortKey:943},{id:"java-944",number:46,title:"Why is `Comparator` considered a functional interface?",question:"Why is `Comparator` considered a functional interface?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"It has a single abstract method `int compare(T a, T b)` (its other methods are default/static), so it can be implemented with a lambda.\n\n```java\nComparator<String> byLen = (a, b) -> a.length() - b.length();\n```",tech:"java",displayNumber:46,sortKey:944},{id:"java-945",number:47,title:"Why is `Runnable` a functional interface?",question:"Why is `Runnable` a functional interface?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'It declares one abstract method `void run()` and no others, making it a valid target for a lambda or method reference.\n\n```java\nRunnable r = () -> System.out.println("task");\nnew Thread(r).start();\n```',tech:"java",displayNumber:47,sortKey:945},{id:"java-946",number:48,title:"Write a lambda to sort employees by salary.",question:"Write a lambda to sort employees by salary.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Pass a `Comparator` lambda (or `Comparator.comparing`) to `sort`. Prefer `comparingDouble` for numeric fields to avoid boxing.\n\n```java\nemployees.sort(Comparator.comparingDouble(Employee::getSalary));\n// or: employees.sort((a, b) -> Double.compare(a.getSalary(), b.getSalary()));\n```",tech:"java",displayNumber:48,sortKey:946},{id:"java-947",number:49,title:"Write a lambda to filter employees by age.",question:"Write a lambda to filter employees by age.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Use a `Predicate` lambda in `stream().filter(...)`.\n\n```java\nList<Employee> adults = employees.stream()\n        .filter(e -> e.getAge() >= 18)\n        .collect(Collectors.toList());\n```",tech:"java",displayNumber:49,sortKey:947},{id:"java-948",number:50,title:"Write a lambda to calculate the square of a number.",question:"Write a lambda to calculate the square of a number.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `Function<Integer,Integer>` (or `IntUnaryOperator`) that returns `n * n`.\n\n```java\nFunction<Integer,Integer> square = n -> n * n;\nSystem.out.println(square.apply(5)); // 25\n```",tech:"java",displayNumber:50,sortKey:948},{id:"java-949",number:51,title:"Write a lambda to check whether a number is even.",question:"Write a lambda to check whether a number is even.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `Predicate<Integer>` returning `n % 2 == 0`.\n\n```java\nPredicate<Integer> isEven = n -> n % 2 == 0;\nSystem.out.println(isEven.test(6)); // true\n```",tech:"java",displayNumber:51,sortKey:949},{id:"java-950",number:52,title:"Write a lambda to find the maximum of two numbers.",question:"Write a lambda to find the maximum of two numbers.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A `BinaryOperator<Integer>` using `Math.max`.\n\n```java\nBinaryOperator<Integer> max = Math::max;\nSystem.out.println(max.apply(3, 9)); // 9\n```",tech:"java",displayNumber:52,sortKey:950},{id:"java-951",number:53,title:"Write a lambda that accepts two Strings and concatenates them.",question:"Write a lambda that accepts two Strings and concatenates them.",difficulty:"Basic",category:"Java 8 Functional Programming",answer:'A `BinaryOperator<String>` or `BiFunction<String,String,String>` that joins the two inputs.\n\n```java\nBinaryOperator<String> concat = (a, b) -> a + b;\nSystem.out.println(concat.apply("Java", "8")); // Java8\n```',tech:"java",displayNumber:53,sortKey:951},{id:"java-952",number:54,title:"Can lambda parameters have explicit types?",question:"Can lambda parameters have explicit types?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. You may declare types explicitly `(int x, int y) -> x + y`, or use `var` (Java 11+). But you must be consistent — either all parameters are typed or all rely on inference.\n\n```java\nBinaryOperator<Integer> add = (Integer a, Integer b) -> a + b;\n```",tech:"java",displayNumber:54,sortKey:952},{id:"java-953",number:55,title:"Can lambda have multiple statements?",question:"Can lambda have multiple statements?",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"Yes, using a block body with braces and an explicit `return` (if it returns a value).\n\n```java\nFunction<Integer,Integer> f = n -> { int r = n * 2; return r + 1; };\n```",tech:"java",displayNumber:55,sortKey:953},{id:"java-954",number:56,title:"Can lambda throw checked exceptions?",question:"Can lambda throw checked exceptions?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"Only if the target functional interface's method declares that checked exception. Standard interfaces like `Function` don't declare any, so lambdas targeting them can't throw checked exceptions directly.\n\n```java\nCallable<String> c = () -> readFile(); // Callable declares 'throws Exception' -> OK\n```",tech:"java",displayNumber:56,sortKey:954},{id:"java-955",number:57,title:"How do you handle checked exceptions inside lambda?",question:"How do you handle checked exceptions inside lambda?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:`Wrap the checked exception in an unchecked one inside a try/catch, or use a custom "throwing" functional interface that declares the exception, or a utility wrapper.

\`\`\`java
Function<String,String> read = path -> {
    try { return Files.readString(Path.of(path)); }
    catch (IOException e) { throw new UncheckedIOException(e); }
};
\`\`\``,tech:"java",displayNumber:57,sortKey:955},{id:"java-956",number:58,title:"What happens when a lambda captures a local variable?",question:"What happens when a lambda captures a local variable?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"The variable's value is copied into the lambda at capture time, and it must be effectively final. The lambda uses that captured snapshot even after the enclosing method returns.\n\n```java\nint base = 10;\nSupplier<Integer> s = () -> base; // captures value 10\n```",tech:"java",displayNumber:58,sortKey:956},{id:"java-957",number:59,title:"Can a lambda modify an instance variable?",question:"Can a lambda modify an instance variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. Instance (and static) fields are not subject to the effectively-final rule; a lambda can read and mutate them through the enclosing object.\n\n```java\nclass Counter { int count; Runnable inc = () -> count++; }\n```",tech:"java",displayNumber:59,sortKey:957},{id:"java-958",number:60,title:"Can a lambda modify a static variable?",question:"Can a lambda modify a static variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes. Static fields belong to the class, not the stack, so lambdas may read and modify them freely (mind thread-safety).\n\n```java\nclass C { static int total; Runnable add = () -> total += 5; }\n```",tech:"java",displayNumber:60,sortKey:958},{id:"java-959",number:61,title:"Can a lambda modify a local variable?",question:"Can a lambda modify a local variable?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"No. Captured local variables must be effectively final, so a lambda can read but not reassign them. A common workaround is a single-element array or an `AtomicInteger`.\n\n```java\nAtomicInteger sum = new AtomicInteger();\nList.of(1, 2, 3).forEach(n -> sum.addAndGet(n)); // mutate via holder\n```",tech:"java",displayNumber:61,sortKey:959},{id:"java-960",number:62,title:"Lambda variable scope vs anonymous class variable scope?",question:"Lambda variable scope vs anonymous class variable scope?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"A lambda shares the enclosing method's scope — its variables live in the same scope, so you can't redeclare an enclosing variable name. An anonymous class introduces a new scope and can shadow enclosing names.\n\n```java\nint x = 1;\nRunnable r = () -> { /* int x = 2; would be a compile error (same scope) */ };\n```",tech:"java",displayNumber:62,sortKey:960},{id:"java-961",number:63,title:"What does `this` refer to inside a lambda?",question:"What does `this` refer to inside a lambda?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"`this` refers to the enclosing instance (the class where the lambda is defined), because a lambda does not create a new scope.\n\n```java\nclass Outer { Runnable r = () -> System.out.println(this.getClass()); } // Outer\n```",tech:"java",displayNumber:63,sortKey:961},{id:"java-962",number:64,title:"What does `this` refer to inside an anonymous inner class?",question:"What does `this` refer to inside an anonymous inner class?",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"`this` refers to the anonymous class instance itself, not the enclosing object. To reach the outer instance you use `Outer.this`.\n\n```java\nRunnable r = new Runnable(){ public void run(){ System.out.println(this.getClass()); } }; // the anonymous class\n```",tech:"java",displayNumber:64,sortKey:962},{id:"java-963",number:65,title:"What is a method reference? (recap)",question:"What is a method reference? (recap)",difficulty:"Basic",category:"Java 8 Functional Programming",answer:"A compact, readable alternative to a lambda that simply invokes an existing method or constructor, written with `::`. The compiler matches the referenced method's signature to the target functional interface.\n\n```java\nFunction<String,Integer> len = String::length; // instead of s -> s.length()\n```",tech:"java",displayNumber:65,sortKey:963},{id:"java-964",number:66,title:"Explain `ClassName::staticMethod`.",question:"Explain `ClassName::staticMethod`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A reference to a static method. The functional interface's arguments become the static method's arguments.\n\n```java\nFunction<String,Integer> parse = Integer::parseInt; // s -> Integer.parseInt(s)\n```",tech:"java",displayNumber:66,sortKey:964},{id:"java-965",number:67,title:"Explain `object::instanceMethod`.",question:"Explain `object::instanceMethod`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A bound instance reference: the method is called on a specific, already-known object. The lambda's arguments are passed to that method.\n\n```java\nConsumer<String> printer = System.out::println; // s -> System.out.println(s)\n```",tech:"java",displayNumber:67,sortKey:965},{id:"java-966",number:68,title:"Explain `ClassName::instanceMethod`.",question:"Explain `ClassName::instanceMethod`.",difficulty:"Advanced",category:"Java 8 Functional Programming",answer:"An unbound instance reference: the first argument becomes the receiver on which the method is invoked, and remaining arguments are passed along.\n\n```java\nFunction<String,String> up = String::toUpperCase; // s -> s.toUpperCase()\nComparator<String> c = String::compareTo;         // (a, b) -> a.compareTo(b)\n```",tech:"java",displayNumber:68,sortKey:966},{id:"java-967",number:69,title:"Explain `ClassName::new`.",question:"Explain `ClassName::new`.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A constructor reference: the target interface's method arguments are forwarded to a matching constructor, producing a new instance.\n\n```java\nSupplier<List<String>> list = ArrayList::new;   // () -> new ArrayList<>()\nFunction<Integer,int[]> arr = int[]::new;         // n -> new int[n]\n```",tech:"java",displayNumber:69,sortKey:967},{id:"java-968",number:70,title:"Convert a lambda into a method reference.",question:"Convert a lambda into a method reference.",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:`When a lambda merely forwards its parameters to a single method call, replace it with a method reference for clarity.

\`\`\`java
// lambda
Function<String,Integer> a = s -> Integer.parseInt(s);
// method reference
Function<String,Integer> b = Integer::parseInt;
\`\`\``,tech:"java",displayNumber:70,sortKey:968},{id:"java-969",number:71,title:"When should you prefer method references?",question:"When should you prefer method references?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:'Prefer them when a lambda does nothing but call an existing method with the same arguments — they\'re shorter and more expressive. Keep a lambda when you transform arguments or add logic.\n\n```java\nnames.forEach(System.out::println);          // prefer reference\nnames.forEach(n -> System.out.println("* " + n)); // lambda needed (extra logic)\n```',tech:"java",displayNumber:71,sortKey:969},{id:"java-970",number:72,title:"Can constructors be referenced?",question:"Can constructors be referenced?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Yes, via `ClassName::new`. The compiler selects the constructor whose signature matches the functional interface's method.\n\n```java\nFunction<String,StringBuilder> sb = StringBuilder::new; // s -> new StringBuilder(s)\n```",tech:"java",displayNumber:72,sortKey:970},{id:"java-971",number:73,title:"What is a constructor reference?",question:"What is a constructor reference?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"A method reference of the form `Type::new` that refers to a constructor, letting a factory-style functional interface (`Supplier`, `Function`) create objects without an explicit lambda.\n\n```java\nSupplier<Employee> factory = Employee::new;\nEmployee e = factory.get();\n```",tech:"java",displayNumber:73,sortKey:971},{id:"java-972",number:74,title:"Difference between a lambda expression and a method reference?",question:"Difference between a lambda expression and a method reference?",difficulty:"Intermediate",category:"Java 8 Functional Programming",answer:"Both implement a functional interface. A lambda defines an inline body (and can transform arguments or add logic); a method reference is a shorthand that delegates directly to an existing method/constructor with matching arguments. Every method reference can be written as a lambda, but not every lambda simplifies to a method reference.\n\n```java\nFunction<String,Integer> lambda = s -> s.length(); // inline body\nFunction<String,Integer> ref    = String::length;   // delegates to existing method\n```",tech:"java",displayNumber:74,sortKey:972},{id:"react-973",number:1,title:"What is React and why was it created?",question:"What is React and why was it created?",difficulty:"Basic",category:"React Basics & JSX",answer:`React is an open-source JavaScript library for building user interfaces, created by Facebook (Meta) and released in 2013. Jordan Walke developed it to address the pain points of building complex, data-driven UIs at scale.

Before React, developers dealt with two major problems: (1) the DOM was slow to manipulate directly, and (2) keeping the UI in sync with application state was error-prone and verbose. React introduced a declarative model where developers describe *what* the UI should look like for any given state, and React handles *how* to update the DOM efficiently.

Key design decisions:
- **Component-based architecture**: UIs built from small, reusable, self-contained pieces.
- **Virtual DOM**: An in-memory representation of the real DOM enabling efficient updates.
- **One-way data flow**: Data flows downward through props, making the data flow predictable.
- **JSX**: A syntax extension letting you write HTML-like code in JavaScript.

React is a view layer library. It does not enforce a full framework structure and can be paired with routing, state management, and other libraries as needed.

\`\`\`jsx
// React is declarative: describe WHAT the UI should look like
function Greeting({ userName }) {
  return <h1>Welcome, {userName}!</h1>;
}

// You never manually call document.getElementById or element.innerHTML
// React figures out the minimal DOM changes needed
function App() {
  const [name, setName] = React.useState('Alice');
  return (
    <div>
      <Greeting userName={name} />
      <button onClick={() => setName('Bob')}>Change Name</button>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:1,sortKey:973},{id:"react-974",number:2,title:"What is the Virtual DOM and how does it work?",question:"What is the Virtual DOM and how does it work?",difficulty:"Basic",category:"React Basics & JSX",answer:`The Virtual DOM (VDOM) is a lightweight JavaScript object representation of the real DOM. When React renders a component, it builds a tree of plain JS objects (React elements) describing the UI. On a state or prop change, React creates a new VDOM tree and compares it to the previous one via **diffing** (reconciliation). It then computes the minimal set of changes and applies only those to the real DOM in a batch.

This matters because direct DOM operations are expensive (they trigger layout, reflow, and repaint). By batching and minimizing real DOM writes, React keeps UIs fast even under frequent updates.

Important nuance: the VDOM is *not* faster than the real DOM per se; the real win is avoiding unnecessary DOM mutations and batching updates intelligently.

\`\`\`jsx
// A "Virtual DOM" node is just a plain JS object
const vdomNode = {
  type: 'div',
  props: { className: 'greeting', children: 'Hello' },
};

// React.createElement produces plain objects (the VDOM)
const element = React.createElement('div', { className: 'greeting' }, 'Hello');
console.log(element);
// { type: 'div', props: { className: 'greeting', children: 'Hello' }, ... }
\`\`\``,tech:"react",displayNumber:2,sortKey:974},{id:"react-975",number:3,title:"What are the advantages of the Virtual DOM?",question:"What are the advantages of the Virtual DOM?",difficulty:"Basic",category:"React Basics & JSX",answer:`The Virtual DOM provides several concrete advantages:
1. **Performance via batching**: React groups multiple state updates and flushes them together, reducing DOM operations.
2. **Minimal updates**: The diffing algorithm only patches changed nodes rather than re-rendering the whole tree.
3. **Declarative programming**: You write what the UI should be; React handles the imperative DOM updates.
4. **Cross-platform rendering**: The same React element tree can render to the DOM (ReactDOM), Native (React Native), or even canvas — the VDOM is platform-agnostic.
5. **Predictability**: Diffing is deterministic, making UI behavior easier to reason about and test.

A common misconception is that VDOM makes every app automatically fast. In reality, unnecessary re-renders, large component trees, and heavy computations still cause slowness; the VDOM only optimizes the DOM-write layer.

\`\`\`jsx
// Multiple state updates are batched in React 18 (automatic batching)
function Counter() {
  const [a, setA] = React.useState(0);
  const [b, setB] = React.useState(0);
  function handleClick() {
    setA(a + 1);
    setB(b + 1); // Only ONE re-render of the DOM, not two
  }
  return <button onClick={handleClick}>{a} {b}</button>;
}
\`\`\``,tech:"react",displayNumber:3,sortKey:975},{id:"react-976",number:4,title:"What is React Fiber and how does it relate to reconciliation?",question:"What is React Fiber and how does it relate to reconciliation?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React Fiber is React's **reconciliation algorithm rewrite** (introduced in React 16). The old "stack reconciler" was synchronous and recursive — once it started, it couldn't be interrupted. This caused dropped frames on complex UIs.

Fiber reimagines reconciliation as **incremental work** that can be paused, resumed, and prioritized. A "fiber" is a JavaScript object representing a unit of work (roughly one component). Each fiber has:
- \`type\`, \`stateNode\`, \`return\`, \`child\`, \`sibling\` (tree links)
- \`alternate\` (the current vs. work-in-progress fiber)
- \`effectTag\` (what DOM mutations to perform)
- \`lanes\` / \`priority\` (when this work should run)

The Fiber architecture enables:
- **Concurrent features** (React 18): \`useTransition\`, \`useDeferredValue\`, Suspense
- **Time-slicing**: Low-priority work yields to high-priority (user input)
- **Error boundaries**: Errors can be caught at any fiber level
- **Return points**: Components can return arrays, strings, or null without wrappers

\`\`\`jsx
// Fiber work loop (simplified conceptual view)
function workLoop(deadline) {
  let shouldYield = false;
  while (nextUnitOfWork && !shouldYield) {
    nextUnitOfWork = performUnitOfWork(nextUnitOfWork);
    shouldYield = deadline.timeRemaining() < 1;
  }
  if (!nextUnitOfWork && finishedWork) {
    commitRoot(); // Apply DOM changes
  }
}

// Fiber priority lanes (conceptual)
const SyncLane = 1;        // High: click, typing
const DefaultLane = 2;     // Normal: data fetching
const IdleLane = 4;        // Low: background analytics
\`\`\``,tech:"react",displayNumber:4,sortKey:976},{id:"react-977",number:5,title:"Explain the reconciliation (diffing) algorithm in React.",question:"Explain the reconciliation (diffing) algorithm in React.",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Reconciliation is the process React uses to update the DOM efficiently. When a component re-renders, React produces a new element tree and reconciles it with the previous one. The diffing algorithm is based on two key assumptions for performance:

1. **Different element types produce different trees**: If the root element type changes (e.g., \`<div>\` to \`<span>\`), React tears down the old subtree entirely and builds a new one. No diffing of children occurs.

2. **Lists use \`key\` props for identity**: Children of the same type are diffed by their \`key\`. React matches elements with the same key across renders to preserve state and DOM nodes; without stable keys it falls back to positional matching, which causes bugs (see Q on keys).

The algorithm is **O(n)** (not O(n³) of a general tree-diff algorithm) because it never compares two trees of different branches — it assumes components at the same level produce similar structure.

Three sub-steps during reconciliation:
- **Element type changed** → unmount old, mount new (state lost).
- **Element type same (DOM)** → keep node, update only changed attributes.
- **Element type same (component)** → update props, re-render children, keep state.

\`\`\`jsx
// Case 1: Different types -> full remount (state of <Counter/> is lost)
function App({ useButton }) {
  return useButton ? <button>Click</button> : <div>Click</div>;
}

// Case 2: Same type -> only attributes update, node reused
function App({ title }) {
  return <h1 className="title">{title}</h1>; // DOM node reused, text updated
}
\`\`\``,tech:"react",displayNumber:5,sortKey:977},{id:"react-978",number:6,title:"What is the difference between the Virtual DOM and the Shadow DOM?",question:"What is the difference between the Virtual DOM and the Shadow DOM?",difficulty:"Basic",category:"React Basics & JSX",answer:`These are completely unrelated concepts that happen to share the word "DOM":

| Aspect | Virtual DOM | Shadow DOM |
|--------|-------------|------------|
| Origin | React library concept | Web standard (W3C) |
| Purpose | Optimize UI re-rendering | Encapsulate styles/markup in components |
| Scope | A JS object tree of the UI | A real DOM subtree with style isolation |
| Who uses it | React, Vue, etc. | Native web components, Lit |

The **Shadow DOM** is a browser feature that lets you attach a hidden, encapsulated DOM tree to an element so that CSS and JS don't leak in or out. The **Virtual DOM** is a purely JS-level abstraction with no browser involvement — it never touches rendering isolation.

\`\`\`jsx
// Virtual DOM: a JS object, not real DOM
const vdom = React.createElement('div', null, 'Hi');

// Shadow DOM: a real browser feature (works in plain JS, no React)
const host = document.createElement('div');
const shadow = host.attachShadow({ mode: 'open' });
shadow.innerHTML = '<style>h1 { color: red; }</style><h1>Scoped</h1>';
// Styles here do NOT affect the rest of the page
\`\`\``,tech:"react",displayNumber:6,sortKey:978},{id:"react-979",number:7,title:"What is JSX and why is it used in React?",question:"What is JSX and why is it used in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`JSX (JavaScript XML) is a syntax extension for JavaScript that lets you write HTML-like markup directly inside JS files. It's not required to use React — you could call \`React.createElement\` directly — but it makes component structure far more readable and visually matches the output.

JSX provides:
- **Familiarity**: UI structure looks like HTML.
- **Composition**: Nesting components reads naturally.
- **Expressiveness**: You can embed any JS expression in \`{ }\`.
- **Compile-time checks**: Static analysis tools can catch typos in element names.

JSX is NOT valid JavaScript — it must be transformed (by Babel, SWC, or the TypeScript compiler) into \`React.createElement()\` calls (or the newer automatic JSX runtime) before the browser can run it.

\`\`\`jsx
// JSX - what you write
const element = <h1 className="greeting">Hello, world!</h1>;

// What it compiles to (classic transform):
const element = React.createElement(
  'h1',
  { className: 'greeting' },
  'Hello, world!'
);
\`\`\``,tech:"react",displayNumber:7,sortKey:979},{id:"react-980",number:8,title:"How does JSX get transformed under the hood? (createElement / JSX transform)",question:"How does JSX get transformed under the hood? (createElement / JSX transform)",difficulty:"Intermediate",category:"React Basics & JSX",answer:`JSX is syntax sugar. A compiler (Babel/SWC/TS) transforms it. There are two transforms:

**Classic Transform** (React 16 and earlier default):
\`\`\`jsx
const el = <div className="a">Hi</div>;
// becomes:
const el = React.createElement('div', { className: 'a' }, 'Hi');
\`\`\`
This requires \`React\` in scope.

**Automatic Runtime** (React 17+, the default now): The compiler imports a function from \`react/jsx-runtime\` automatically, so you no longer need to \`import React\` to use JSX.
\`\`\`jsx
// <div className="a">Hi</div>
import { jsx as _jsx } from "react/jsx-runtime";
const el = _jsx("div", { className: "a", children: "Hi" });
\`\`\`

\`React.createElement(type, props, ...children)\` returns a plain object:
\`\`\`
{ $$typeof: Symbol(react.element), type, props: {children, ...}, key, ref }
\`\`\`
This object is a **React element** — the smallest building block of the VDOM.

\`\`\`jsx
// Children can be passed variadically:
React.createElement('ul', null,
  React.createElement('li', null, 'A'),
  React.createElement('li', null, 'B')
);
// JSX equivalent:
// <ul><li>A</li><li>B</li></ul>

// Elements are just objects describing what to render
const el = <span>x</span>;
console.log(el.$$typeof); // Symbol(react.element)
\`\`\``,tech:"react",displayNumber:8,sortKey:980},{id:"react-981",number:9,title:"What is the difference between React.createElement and JSX?",question:"What is the difference between React.createElement and JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:`There is **no functional difference** at runtime — JSX compiles to \`React.createElement\` (or the automatic runtime). JSX is purely a developer-experience improvement. Both produce identical React element objects.

Trade-offs:
- **JSX**: Readable, visual, matches HTML structure, easier to maintain for complex trees. But requires a build step and can hide what's happening.
- **createElement**: Verbose, nested function calls become unreadable, but explicit and needs no special syntax (useful in environments without JSX support, or for programmatic/dynamic element creation).

Modern best practice is almost always JSX, except for very dynamic trees where you build elements in loops/conditionals in plain JS.

\`\`\`jsx
// Equivalent representations

// 1. JSX
const a = (
  <div className="box" onClick={handleClick}>
    <span>Text</span>
  </div>
);

// 2. createElement
const b = React.createElement(
  'div',
  { className: 'box', onClick: handleClick },
  React.createElement('span', null, 'Text')
);
// a and b describe the exact same UI
\`\`\``,tech:"react",displayNumber:9,sortKey:981},{id:"react-982",number:10,title:"What is a React element vs a React component vs an instance?",question:"What is a React element vs a React component vs an instance?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`These three terms are often confused:

- **React Element**: A plain object describing what to render (\`{ type, props, key, ref }\`). It is immutable and is what \`createElement\`/\`JSX\` return. Elements are not components.
- **React Component**: A function or class that, given props, returns a React element (or tree of them). It's a blueprint, not a rendered thing.
- **Component Instance**: The internal Fiber/state object React creates to track a mounted component. For function components, you don't get a direct handle to the instance; for class components, \`this\` refers to it. The DOM node is also a separate concept.

Think: element = instruction, component = function, instance = the running record React keeps.

\`\`\`jsx
// Component (a function/blueprint)
function Button({ label }) {
  return <button>{label}</button>; // returns an element
}

// Element (an instruction object created when you write JSX)
const el = <Button label="Save" />; // equals createElement(Button, {label:'Save'})

// Instance: React internally creates one when <Button/> mounts (no direct handle)
\`\`\``,tech:"react",displayNumber:10,sortKey:982},{id:"react-983",number:11,title:"What are Functional Components and Class Components in React?",question:"What are Functional Components and Class Components in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`React supports two ways to define components:

**Functional Components** (modern, recommended):
- Plain JavaScript functions that accept \`props\` and return JSX.
- Can use Hooks (\`useState\`, \`useEffect\`, etc.) for state and side effects.
- Simpler, easier to test, no \`this\` binding issues.
- Smaller bundle size after minification (no class boilerplate).

**Class Components** (legacy):
- ES6 classes extending \`React.Component\`.
- Have \`this.props\`, \`this.state\`, lifecycle methods (\`componentDidMount\`, etc.).
- \`this\` binding required in constructors or via class fields.
- Still supported but not recommended for new code.

As of React 18, both are officially supported. Function components + Hooks are the present and future.

\`\`\`jsx
// Functional component (modern)
function Greeting({ name }) {
  const [count, setCount] = React.useState(0);
  return <div>Hello {name}, clicked {count} times</div>;
}

// Class component (legacy)
class Greeting extends React.Component {
  state = { count: 0 };
  render() {
    return <div>Hello {this.props.name}, clicked {this.state.count} times</div>;
  }
}
\`\`\``,tech:"react",displayNumber:11,sortKey:983},{id:"react-984",number:12,title:"What is the difference between Functional and Class Components?",question:"What is the difference between Functional and Class Components?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Beyond syntax, key differences:

| Aspect | Functional | Class |
|--------|-----------|-------|
| State | \`useState\` (multiple) | single \`this.state\` object |
| Side effects | \`useEffect\` | lifecycle (\`componentDidMount\`, etc.) |
| \`this\` | none | required, binding headaches |
| State updates | functional updater \`setX(prev => prev+1)\` | \`this.setState({x: this.state.x+1})\` |
| Performance | No instance allocation overhead | instances created |
| Code reuse | Custom Hooks | HOCs / render props |
| Error handling | Error boundaries still need classes | \`componentDidCatch\` |

Functional components can now do everything class components can (including error boundaries via libraries, though React's built-in error boundary still requires a class). They are the recommended default.

\`\`\`jsx
// Side effect comparison
function Func() {
  React.useEffect(() => {
    const id = setInterval(() => {}, 1000);
    return () => clearInterval(id); // cleanup
  }, []);
  return null;
}

class Cls extends React.Component {
  componentDidMount() { this.id = setInterval(() => {}, 1000); }
  componentWillUnmount() { clearInterval(this.id); }
  render() { return null; }
}
\`\`\``,tech:"react",displayNumber:12,sortKey:984},{id:"react-985",number:13,title:"Can a functional component fully replace a class component? Are there exceptions?",question:"Can a functional component fully replace a class component? Are there exceptions?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"For 99% of cases, yes. Hooks cover state, side effects, context, refs, memoization, and even `getSnapshotBeforeUpdate`/`componentDidUpdate`-style logic. The one notable exception historically is **Error Boundaries**: React's `componentDidCatch` / `getDerivedStateFromError` can ONLY be implemented in a class component. There is no `useErrorBoundary` hook built into React.\n\nThat said, you typically need just one tiny class-based error boundary in an entire app, and community libraries wrap it for you. All other lifecycle behaviors map cleanly to Hooks:\n- `componentDidMount` → `useEffect(fn, [])`\n- `componentDidUpdate` → `useEffect(fn, [deps])`\n- `shouldComponentUpdate` → `React.memo` / `useMemo`\n- `getDerivedStateFromProps` → `useState` + `useEffect` or derived state directly in render\n\n```jsx\n// The one thing still needing a class: Error Boundary\nclass ErrorBoundary extends React.Component {\n  state = { hasError: false };\n  static getDerivedStateFromError() { return { hasError: true }; }\n  componentDidCatch(error, info) { logError(error, info); }\n  render() {\n    return this.state.hasError ? <Fallback /> : this.props.children;\n  }\n}\n// Wrap any tree: <ErrorBoundary><App/></ErrorBoundary>\n```",tech:"react",displayNumber:13,sortKey:985},{id:"react-986",number:14,title:"What are Props in React?",question:"What are Props in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`Props (short for "properties") are read-only inputs passed from a parent component to a child. They flow **downward** (one-way data flow). A child can never modify its own props; it can only use them to render. If a child needs to influence the parent's data, the parent passes down a callback function as a prop.

Props can be of any type: strings, numbers, objects, arrays, functions, even other React elements (children). Because props are immutable from the child's perspective, React can optimize rendering and treat components as pure functions of their props.

\`\`\`jsx
// Parent passes props down
function Parent() {
  const user = { name: 'Ada', age: 36 };
  return <Profile user={user} onEdit={() => save(user)} />;
}

// Child receives and uses props (read-only!)
function Profile({ user, onEdit }) {
  return (
    <div>
      <h2>{user.name}</h2>
      <button onClick={onEdit}>Edit</button>
      {/* user.name = 'Bob' would throw or be ignored; never mutate props */}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:14,sortKey:986},{id:"react-987",number:15,title:"What is State in React?",question:"What is State in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`State is a component's private, mutable data that affects what is rendered. When state changes (via a setter from \`useState\`), React re-renders that component (and its children, unless memoized). Unlike props, state is owned and controlled by the component itself.

Key rules:
- Never mutate state directly (\`state.count++\` is wrong). Always use the setter.
- Treat state as immutable; replace it with a new object/array.
- State updates may be asynchronous/batched, so don't rely on the value right after calling the setter.
- State is local: only the component that owns it (and its descendants via props/callbacks) can read or change it.

\`\`\`jsx
function Counter() {
  const [count, setCount] = React.useState(0); // initial state

  function increment() {
    // CORRECT: use the setter (functional form for latest value)
    setCount(prev => prev + 1);

    // WRONG: mutating state directly does not trigger re-render
    // count++; // never do this
  }
  return <button onClick={increment}>{count}</button>;
}
\`\`\``,tech:"react",displayNumber:15,sortKey:987},{id:"react-988",number:16,title:"What is the difference between Props and State?",question:"What is the difference between Props and State?",difficulty:"Basic",category:"React Basics & JSX",answer:`| Aspect | Props | State |
|--------|-------|-------|
| Ownership | Parent owns it; child receives it | Component owns it itself |
| Mutability | Immutable from child's view | Mutable via setter |
| Purpose | Configure/feed data into a component | Track changing data internal to component |
| Trigger re-render | When parent re-renders | When setter called |
| Source of truth | Parent decides | Component decides |

A helpful mental model: **props are like function arguments; state is like a variable declared inside the function**. Both are inputs to render; re-render happens when either changes.

Another nuance: you can "lift state up" so that what was state in a child becomes a prop passed from the parent — this is how sibling components share data.

\`\`\`jsx
// Props: passed in, read-only
function Child({ value }) { return <p>{value}</p>; }

// State: owned, mutable
function Parent() {
  const [value, setValue] = React.useState(0);
  return <Child value={value} />; // state of Parent becomes prop of Child
}
\`\`\``,tech:"react",displayNumber:16,sortKey:988},{id:"react-989",number:17,title:"Why is immutability important in React?",question:"Why is immutability important in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React relies on reference equality to detect changes. When you call a state setter, React compares the new state to the old. If you mutate an object/array in place, its reference stays the same, so React may **skip the re-render** because \`prevState === nextState\`.

Immutability guarantees:
1. **Correct change detection**: A new reference always signals a change.
2. **Time-travel debugging**: Libraries like Redux DevTools rely on snapshotting previous states; mutation breaks that.
3. **Predictable re-renders**: Memoized components (\`React.memo\`, \`useMemo\`) only re-render when inputs change by reference.
4. **Concurrency safety**: React 18 concurrent features may read the same state at multiple points; mutation risks tearing.

Rule: always create a new object/array when updating state.

\`\`\`jsx
function TodoList() {
  const [todos, setTodos] = React.useState([{ id: 1, text: 'a' }]);

  // WRONG - mutates, reference unchanged, may not re-render
  function badAdd() {
    todos.push({ id: 2, text: 'b' });
    setTodos(todos);
  }

  // CORRECT - new array reference
  function goodAdd() {
    setTodos(prev => [...prev, { id: 2, text: 'b' }]);
  }

  // CORRECT - new object for nested update
  function updateFirst() {
    setTodos(prev => prev.map((t, i) =>
      i === 0 ? { ...t, text: 'updated' } : t
    ));
  }
}
\`\`\``,tech:"react",displayNumber:17,sortKey:989},{id:"react-990",number:18,title:"What are Controlled and Uncontrolled Components?",question:"What are Controlled and Uncontrolled Components?",difficulty:"Basic",category:"React Basics & JSX",answer:`**Controlled Components**: Form inputs whose value is driven by React state. The input's \`value\` (or \`checked\`) prop is set from state, and every change updates that state via \`onChange\`. React is the single source of truth.

**Uncontrolled Components**: Form inputs that manage their own state internally via the DOM. You access their value using a \`ref\` when needed (e.g., on submit). The DOM is the source of truth.

Controlled components are preferred for most cases because they enable validation, formatting, disabling, and conditional behavior in React logic. Uncontrolled are simpler for simple forms, file inputs (\`<input type="file">\`), or integrating non-React libraries.

\`\`\`jsx
// Controlled: React owns the value
function Controlled() {
  const [name, setName] = React.useState('');
  return <input value={name} onChange={e => setName(e.target.value)} />;
}

// Uncontrolled: DOM owns the value, read via ref on submit
function Uncontrolled() {
  const inputRef = React.useRef(null);
  function handleSubmit(e) {
    e.preventDefault();
    console.log(inputRef.current.value);
  }
  return <form onSubmit={handleSubmit}><input ref={inputRef} /></form>;
}
\`\`\``,tech:"react",displayNumber:18,sortKey:990},{id:"react-991",number:19,title:"When should you use Controlled vs Uncontrolled components?",question:"When should you use Controlled vs Uncontrolled components?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Use **Controlled** when:
- You need instant validation/feedback as the user types.
- The input value affects other parts of the UI (e.g., a live preview, disabling a submit button).
- You need to enforce formatting (uppercase, mask digits).
- You want a single predictable source of truth and easier testing.

Use **Uncontrolled** when:
- You have a simple form with a few fields and no live cross-field logic.
- You need a \`<input type="file">\` (always uncontrolled — its value can't be set programmatically for security).
- You're integrating a third-party DOM library (e.g., a datepicker) that manages its own input.
- You want to reduce re-renders for very high-frequency input (though debouncing a controlled input is usually better).

A middle ground: \`defaultValue\` / \`defaultChecked\` give uncontrolled inputs an initial value set by React without taking over updates.

\`\`\`jsx
// Hybrid: uncontrolled with a sensible initial value via defaultValue
function Form() {
  return <input defaultValue="starter" />; // DOM controls updates
}

// Controlled with validation
function EmailField() {
  const [email, setEmail] = React.useState('');
  const valid = /^[^@]+@[^@]+$/.test(email);
  return (
    <>
      <input value={email} onChange={e => setEmail(e.target.value)} />
      {email && !valid && <span>Invalid email</span>}
    </>
  );
}
\`\`\``,tech:"react",displayNumber:19,sortKey:991},{id:"react-992",number:20,title:"Why are keys important in React lists, and why is using the array index dangerous?",question:"Why are keys important in React lists, and why is using the array index dangerous?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Keys help React identify which items in a list have changed, been added, or removed across renders. During reconciliation, React matches children by key rather than by position. With a stable, unique key, React can move/reuse DOM nodes correctly and preserve component state.

Using the **array index as a key is dangerous** when the list is reordered, filtered, or items are inserted/removed. Because the index changes as the list changes, items get mismatched: a component that should keep its state gets a different key, causing:
- Loss of local component state (input values, scroll position).
- Incorrect association between DOM and state.
- Subtle UI bugs (a checkbox checked on item A suddenly appears on item B).

Index keys are acceptable **only** for static lists that never reorder, filter, or add/remove items. Otherwise use a stable unique id.

\`\`\`jsx
const todos = [{ id: 'a', text: 'Buy milk' }, { id: 'b', text: 'Walk dog' }];

// GOOD: stable unique id
todos.map(t => <li key={t.id}>{t.text}</li>);

// BAD: index changes when list mutates
todos.map((t, i) => <li key={i}>{t.text}</li>);
// If you delete index 0, the old index-1 item now has key 0 -> state mismatch
\`\`\``,tech:"react",displayNumber:20,sortKey:992},{id:"react-993",number:21,title:"What happens if you don't provide a key to a list item?",question:"What happens if you don't provide a key to a list item?",difficulty:"Basic",category:"React Basics & JSX",answer:`React will still render the list but will emit a warning: "Each child in a list should have a unique 'key' prop." Without keys, React falls back to positional (index) matching. As explained in Q20, positional matching causes state mismatches when the list changes. The key must be on the **innermost element returned by the map**, not on the \`<div>\` wrapping the loop.

Keys only need to be unique among siblings — they don't need to be globally unique across the whole app. Never use \`Math.random()\` as a key (it changes every render, defeating the purpose).

\`\`\`jsx
// WRONG - warning, and no key on the mapped element
{items.map(item => (
  <div key={item.id}> {/* key here is ignored for the list item itself */}
    {item.name}
  </div>
))}

// CORRECT - key on the element returned by map
{items.map(item => (
  <li key={item.id}>{item.name}</li>
))}
\`\`\``,tech:"react",displayNumber:21,sortKey:993},{id:"react-994",number:22,title:"What is a React Fragment and when should you use it?",question:"What is a React Fragment and when should you use it?",difficulty:"Basic",category:"React Basics & JSX",answer:`A **Fragment** (\`<>...</>\` or \`<React.Fragment>...</React.Fragment>\`) lets a component return multiple sibling elements without wrapping them in an extra DOM node. This keeps the DOM shallow and avoids invalid HTML (e.g., a \`<div>\` inside a \`<table>\` or \`<ul>\`).

Use fragments when:
- You need to return multiple elements from a component.
- A wrapper \`<div>\` would break semantics or CSS.
- You want to avoid unnecessary depth in the DOM tree.

The shorthand \`<>...</>\` is syntactic sugar for \`<React.Fragment>...</React.Fragment>\`. It does not create a DOM node.

\`\`\`jsx
// Without fragment: adds extra <div> to DOM
function Bad() { return <div><td>1</td><td>2</td></div>; }

// With fragment: no extra node
function Good() {
  return (
    <>
      <td>1</td>
      <td>2</td>
    </>
  );
}

// Explicit form needed when you need a key (see Q23)
function GoodWithKey({ items }) {
  return (
    <React.Fragment>
      {items.map(item => <React.Fragment key={item.id}><td>{item.a}</td><td>{item.b}</td></React.Fragment>)}
    </React.Fragment>
  );
}
\`\`\``,tech:"react",displayNumber:22,sortKey:994},{id:"react-995",number:23,title:"How do you use a Fragment with a key?",question:"How do you use a Fragment with a key?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`The shorthand \`<>...</>\` syntax **cannot** accept a \`key\` prop. To use a key on a fragment — which you need when mapping fragments in a list — you must use the **explicit** \`<React.Fragment key={...}>\` form. This is extremely common in tables where each row must render multiple \`<td>\`s without a wrapping \`<div>\` but still needs a key.

Under the hood, \`React.Fragment\` is just another element type; the reconciler treats it as a no-op DOM node, but it still carries a \`key\` used for list diffing.

\`\`\`jsx
// Shorthand cannot take a key -> this would warn/break
// {items.map(item => <>{item.a}{item.b}</>)} // no key allowed

// Explicit form with key:
function TableBody({ rows }) {
  return (
    <tbody>
      {rows.map(row => (
        <React.Fragment key={row.id}>
          <td>{row.name}</td>
          <td>{row.age}</td>
        </React.Fragment>
      ))}
    </tbody>
  );
}
\`\`\``,tech:"react",displayNumber:23,sortKey:995},{id:"react-996",number:24,title:"What are the different patterns for conditional rendering in React?",question:"What are the different patterns for conditional rendering in React?",difficulty:"Basic",category:"React Basics & JSX",answer:'Common patterns, from most to least common:\n\n1. **Inline ternary**: `{condition ? <A /> : <B />}`\n2. **Logical &&**: `{condition && <A />}` (renders nothing if falsy)\n3. **Early return**: `if (!condition) return null; return <A />;` (at top of component)\n4. **Switch/ternary chain**: for multiple mutually exclusive states\n5. **Higher-order / wrapper components**: e.g., `<AuthGate><Dashboard/></AuthGate>`\n6. **Render props / children as function**: `<Wrapper>{isLoading ? <Spinner/> : <Content/>}</Wrapper>`\n\nNotes:\n- `&&` can render `0` as `0` if the left side is `0` (a number). Use `condition > 0 && ...` or `Boolean(condition) && ...`.\n- `null`, `undefined`, `false`, `true` render nothing (useful for `&&`).\n- Prefer early returns for complex conditionals — they avoid nested JSX and improve readability.\n\n```jsx\nfunction Status({ user }) {\n  if (!user) return <Login />;\n  if (user.isAdmin) return <AdminPanel />;\n  return <Dashboard />;\n}\n\n// Gotcha: 0 renders as "0"\n{count && <span>{count}</span>} // if count=0, renders "0"!\n{count > 0 && <span>{count}</span>} // safe\n```',tech:"react",displayNumber:24,sortKey:996},{id:"react-997",number:25,title:"What is the difference between `&&` and ternary conditional rendering?",question:"What is the difference between `&&` and ternary conditional rendering?",difficulty:"Basic",category:"React Basics & JSX",answer:'- **`condition && <X />`**: Renders `<X />` when truthy; when falsy renders the falsy value (`false`, `null`, `undefined` render nothing, but `0` or `\'\'` render literally). Best for "show or hide" with no else branch.\n- **`condition ? <X /> : <Y />`**: Always renders exactly one branch — ideal when you have both an "if" and "else" path (including an explicit `: null`).\n\nChoose `&&` when there is no else. Choose ternary when you need an explicit else (even `: null`). Avoid deeply nested ternaries; extract to a variable or early return for readability.\n\n```jsx\n// Ternary: always one branch\n{isLoggedIn ? <LogoutButton /> : <LoginButton />}\n\n// && : only-if, else renders nothing (unless 0/\'\' )\n{showBanner && <Banner />}\n\n// Bad: nested ternary hard to read\n{isLoading ? <Spinner/> : error ? <Error/> : <Data/>}\n// Better: extract or early-return\n```',tech:"react",displayNumber:25,sortKey:997},{id:"react-998",number:26,title:"How do you handle multiple conditions (more than two branches) in JSX?",question:"How do you handle multiple conditions (more than two branches) in JSX?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`For more than two branches, several clean approaches exist:

1. **Extract to a variable** (most readable): compute \`const content = ...\` before \`return\`, then render \`{content}\`.
2. **Lookup object/map**: \`const views = { loading: <Spinner/>, error: <Error/>, ready: <Data/> }; return views[state];\`
3. **Early returns** in the component body.
4. **Switch statement** in a helper function that returns JSX.

Avoid chaining multiple ternaries inline; it becomes unreadable and error-prone. The object-map approach is especially clean when branches are independent of order.

\`\`\`jsx
function Panel({ status }) {
  const views = {
    loading: <Spinner />,
    error: <ErrorBox />,
    empty: <EmptyState />,
    ready: <Data />,
  };
  return <section>{views[status] ?? <ErrorBox />}</section>;
}

// Or early returns:
function Panel2({ status }) {
  if (status === 'loading') return <Spinner />;
  if (status === 'error') return <ErrorBox />;
  return <Data />;
}
\`\`\``,tech:"react",displayNumber:26,sortKey:998},{id:"react-999",number:27,title:"How are events handled in React compared to plain HTML/JavaScript?",question:"How are events handled in React compared to plain HTML/JavaScript?",difficulty:"Basic",category:"React Basics & JSX",answer:`React implements a **synthetic event system**. Differences from native DOM events:

1. **Naming**: React uses **camelCase** (\`onClick\`, \`onSubmit\`) vs HTML's lowercase (\`onclick\`).
2. **Handler value**: You pass a **function** as the handler, not a string (\`onClick={handleClick}\` vs \`onclick="handleClick()"\`).
3. **SyntheticEvent**: React wraps native events in a cross-browser \`SyntheticEvent\` object that normalizes behavior across browsers. Events are pooled in older versions (React 16 and earlier pooled objects for performance; React 17+ no longer pools).
4. **Delegation**: React 17+ attaches listeners to the root container, not \`document\`. This makes event handling more predictable and allows multiple React versions on one page.
5. **\`this\` binding**: In class components you must bind handlers (or use class fields / arrow functions).

\`\`\`jsx
// HTML
// <button onclick="handleClick()">Click</button>

// React
function Button() {
  function handleClick(e) {
    e.preventDefault(); // SyntheticEvent, cross-browser
    console.log('clicked');
  }
  return <button onClick={handleClick}>Click</button>;
}
\`\`\``,tech:"react",displayNumber:27,sortKey:999},{id:"react-1000",number:28,title:"What is the SyntheticEvent in React?",question:"What is the SyntheticEvent in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`SyntheticEvent` is React's cross-browser wrapper around the native browser event. It provides a consistent API regardless of browser quirks, exposing standard methods like `preventDefault()`, `stopPropagation()`, `target`, `currentTarget`, `nativeEvent`, and `type`.\n\nKey points:\n- It is a **normalized** object — same interface in all browsers.\n- **Event pooling** (React <17): event objects were reused for performance; you had to call `e.persist()` to access them asynchronously. **React 17+ removed pooling**, so async access works normally.\n- `e.nativeEvent` gives the underlying browser event when you need low-level access.\n- React 17+ attaches root listeners at the React root container, not `document`, fixing issues with event ordering across React trees.\n\n```jsx\nfunction Form() {\n  function onSubmit(e) {\n    e.preventDefault();          // works in all browsers\n    const value = e.target.value; // normalized target\n    // e.nativeEvent gives the raw DOM event if needed\n    console.log(e.nativeEvent);\n  }\n  return <form onSubmit={onSubmit}><input /></form>;\n}\n```",tech:"react",displayNumber:28,sortKey:1e3},{id:"react-1001",number:29,title:"How does event delegation work in React?",question:"How does event delegation work in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`React uses **event delegation**: instead of attaching a listener to every element, it attaches a single listener per event type at the root (in React 17+, the root container; previously \`document\`). When an event fires, it bubbles up to the root, React looks up the internal fiber tree to find which component owns the handler, and dispatches the synthetic event.

Benefits:
- **Performance**: Fewer actual DOM listeners (memory savings).
- **New components**: No need to re-attach listeners when components mount — they just register their handlers with React's internal system.
- **Consistent behavior**: Works uniformly across all components.

This is why \`e.stopPropagation()\` in React doesn't stop native handlers on non-React elements outside the React root, and why \`e.nativeEvent.stopImmediatePropagation()\` is needed for that.

\`\`\`jsx
// One listener on root handles ALL onClick in the tree
// React internally maps: fiber -> handlers
function App() {
  return (
    <div> {/* root container */}
      <Button /> {/* no real DOM listener added here */}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:29,sortKey:1001},{id:"react-1002",number:30,title:"What is the difference between passing a function vs a string as an event handler?",question:"What is the difference between passing a function vs a string as an event handler?",difficulty:"Basic",category:"React Basics & JSX",answer:"In HTML you pass a **string** of code: `onclick=\"alert('hi')\"`. In JSX you pass a **function reference**: `onClick={handleClick}`. Passing a string in JSX would just set the prop to a string and never be called. Passing a function means React will invoke it with a `SyntheticEvent` when the event fires.\n\nA common mistake: `onClick={handleClick()}` (with parentheses) calls the function **immediately during render** and passes its *return value* as the handler — usually `undefined` or an unwanted result. Always pass the reference: `onClick={handleClick}`. If you need arguments, use an arrow: `onClick={() => handleClick(id)}`.\n\n```jsx\nconst handleClick = (id) => console.log(id);\n\n// WRONG: invoked during render\n<button onClick={handleClick(id)}>X</button>\n\n// CORRECT: arrow preserves lazy invocation\n<button onClick={() => handleClick(id)}>X</button>\n\n// CORRECT: no-arg reference\n<button onClick={handleClick}>X</button>\n```",tech:"react",displayNumber:30,sortKey:1002},{id:"react-1003",number:31,title:"How do you pass arguments to event handlers in React?",question:"How do you pass arguments to event handlers in React?",difficulty:"Basic",category:"React Basics & JSX",answer:"You wrap the handler in an arrow function so it is called with both the event and your custom argument: `onClick={(e) => handle(id, e)}`. Two patterns:\n\n1. **Inline arrow** (most common): `onClick={(e) => deleteItem(id, e)}`.\n2. **`bind`**: `onClick={deleteItem.bind(this, id)}` (used historically with class components; arrow is cleaner now).\n\nAvoid defining a new function on every render inside a large list if performance is critical — but for typical UIs the cost is negligible. If profiling shows issues, use a data attribute: read `e.target.dataset.id` inside one stable handler.\n\n```jsx\nfunction List({ items, onDelete }) {\n  return items.map(item => (\n    <li key={item.id}>\n      {item.name}\n      <button onClick={(e) => onDelete(item.id, e)}>Delete</button>\n    </li>\n  ));\n}\n```",tech:"react",displayNumber:31,sortKey:1003},{id:"react-1004",number:32,title:"Why does React use camelCase event names like `onClick` instead of `onclick`?",question:"Why does React use camelCase event names like `onClick` instead of `onclick`?",difficulty:"Basic",category:"React Basics & JSX",answer:"React props are JavaScript identifiers, and JavaScript convention (and JSX) uses camelCase. Since JSX is essentially JavaScript, `onClick` must be camelCase to be valid and consistent with the rest of the JS ecosystem (`onMouseEnter`, `onKeyDown`). HTML attributes are case-insensitive and use lowercase (`onclick`), but in JSX you're writing JS expressions, not HTML strings — so the DOM `onclick` lowercase convention doesn't apply. React's event system maps these camelCase props to the correct native event types internally.\n\n```jsx\n// Valid JSX (camelCase)\n<button onClick={fn} onMouseEnter={fn} onKeyDown={fn}>X</button>\n\n// INVALID in JSX (this would be treated as unknown prop / not an event)\n// <button onclick={fn}>X</button>\n```",tech:"react",displayNumber:32,sortKey:1004},{id:"react-1005",number:33,title:"What is `React.createElement` signature and return value?",question:"What is `React.createElement` signature and return value?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"Signature: `React.createElement(type, props, ...children)`.\n\n- `type`: a string (`'div'`) for host elements, or a function/class component, or another React element type (Fragment, etc.).\n- `props`: an object of attributes; `null` or `undefined` is allowed (treated as empty). `key` and `ref` are special — extracted out, not in `props`.\n- `children`: any number of child elements, strings, or arrays, flattened into `props.children`.\n\nReturn value: a **React element** object: `{ $$typeof: Symbol.for('react.element'), type, key, ref, props, _owner }`. It is a plain description of the UI — not a rendered instance. React uses it to build the fiber tree during render.\n\n```jsx\nconst el = React.createElement('button', { className: 'btn' }, 'Click');\n// = JSX: <button className=\"btn\">Click</button>\n// el = {\n//   $$typeof: Symbol(react.element),\n//   type: 'button',\n//   key: null, ref: null,\n//   props: { className: 'btn', children: 'Click' }\n// }\n```",tech:"react",displayNumber:33,sortKey:1005},{id:"react-1006",number:34,title:"What is `$$typeof` on a React element and why does it exist?",question:"What is `$$typeof` on a React element and why does it exist?",difficulty:"Advanced",category:"React Basics & JSX",answer:`\`$$typeof\` is a property on every React element set to \`Symbol.for('react.element')\`. Its purpose is **security/XSS protection**. 

When React renders, it trusts the structure you give it. If an attacker could inject a plain JSON object that *looks* like a React element (e.g., \`{ type: 'div', props: {...} }\`), React might treat it as a valid element and render it — a classic XSS vector via \`JSON.parse\` from an untrusted API.

Because \`Symbol.for('react.element')\` **cannot be recreated via JSON** (Symbols don't survive serialization), a malicious object from \`JSON.parse\` will never have a valid \`$$typeof\`, and React will throw instead of rendering it. This is a deliberate guard against server-injected fake elements.

\`\`\`jsx
const el = <div>hi</div>;
console.log(el.$$typeof); // Symbol(react.element)
console.log(el.$$typeof === Symbol.for('react.element')); // true

// Attacker JSON cannot forge this:
const fake = JSON.parse('{"type":"script","props":{"dangerouslySetInnerHTML":{"__html":"<script>evil<\/script>"}}}');
// fake.$$typeof === undefined -> React rejects it
\`\`\``,tech:"react",displayNumber:34,sortKey:1006},{id:"react-1007",number:35,title:"Explain the difference between the classic and automatic JSX runtime.",question:"Explain the difference between the classic and automatic JSX runtime.",difficulty:"Intermediate",category:"React Basics & JSX",answer:'**Classic runtime** (pre-React 17 default): JSX compiles to `React.createElement(...)`, so `React` must be in scope in every file using JSX. This led to the ubiquitous `import React from \'react\'` even when you never referenced `React` directly.\n\n**Automatic runtime** (React 17+ default in modern toolchains): The compiler auto-imports from `react/jsx-runtime` (functions `jsx`, `jsxs`, `Fragment`). You no longer need to import `React` just to use JSX. This reduces bundle size slightly and removes a common source of "React is not defined" errors.\n\nModern tools (Babel `preset-react` with `runtime: \'automatic\'`, Next.js, Vite, Create React App) use automatic by default. The produced elements are functionally identical.\n\n```jsx\n// Automatic runtime: NO need to import React\nexport function Hi() {\n  return <h1>Hello</h1>;\n}\n// compiles to: import { jsx } from "react/jsx-runtime";\n//   jsx("h1", { children: "Hello" });\n\n// Classic runtime: requires React in scope\nimport React from \'react\';\nexport function Hi() { return <h1>Hello</h1>; }\n```',tech:"react",displayNumber:35,sortKey:1007},{id:"react-1008",number:36,title:"How does React render a component for the first time (mount)?",question:"How does React render a component for the first time (mount)?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`On mount:
1. **Render phase**: React calls the component function (or \`render()\` for class), producing a tree of React elements. It recursively reconciles these against "nothing" (the empty tree) and builds the fiber tree, marking each fiber with an effect (e.g., \`Placement\`).
2. **Commit phase**: React takes the finished work and applies it to the real DOM in one synchronous pass — inserting new nodes, running \`useLayoutEffect\`/\`componentDidMount\`.
3. **Browser paint**: The browser paints the updated DOM.
4. **Passive effects**: \`useEffect\` callbacks run asynchronously after paint.

The render phase is pure and may be interrupted (concurrent mode); the commit phase is synchronous and not interruptible.

\`\`\`jsx
function App() {
  React.useEffect(() => {
    console.log('passive effect (after paint)');
  }, []);
  React.useLayoutEffect(() => {
    console.log('layout effect (before paint)');
  }, []);
  console.log('render');
  return <h1>Hi</h1>;
}
// Order on mount: render -> layout effect -> paint -> passive effect
\`\`\``,tech:"react",displayNumber:36,sortKey:1008},{id:"react-1009",number:37,title:"What is the difference between the render phase and the commit phase?",question:"What is the difference between the render phase and the commit phase?",difficulty:"Advanced",category:"React Basics & JSX",answer:`- **Render phase** (reconciliation): React calls component bodies and \`render()\`, diffs the new element tree against the previous, and decides what changes are needed. It is **pure and can be paused, abandoned, or restarted** (especially in concurrent mode). No DOM mutations happen here.
- **Commit phase**: React applies the computed changes to the real DOM (insertions, updates, deletions), then runs layout effects and, after paint, passive effects. It is **synchronous and not interruptible** — once a commit starts, it must finish to keep the UI consistent.

Implication: never put side effects (network calls, subscriptions) in the render phase (component body). Use \`useEffect\`/\`useLayoutEffect\`. Side effects in render can run multiple times due to interruptions.

\`\`\`jsx
function Bad() {
  // SIDE EFFECT IN RENDER - runs twice in StrictMode, may be interrupted
  fetch('/api').then(r => r.json()); // never do this
  return <div />;
}

function Good() {
  React.useEffect(() => {            // side effect in commit phase
    fetch('/api').then(r => r.json());
  }, []);
  return <div />;
}
\`\`\``,tech:"react",displayNumber:37,sortKey:1009},{id:"react-1010",number:38,title:'What does "React re-renders a component" actually mean?',question:'What does "React re-renders a component" actually mean?',difficulty:"Intermediate",category:"React Basics & JSX",answer:`"Re-render" means React **calls the component function again** (or \`render()\`), producing a new React element tree. It does NOT mean the DOM is updated directly. After the function returns, React reconciles the new tree against the previous one and applies only the *differences* to the real DOM.

Key clarifications:
- Re-render != DOM update. A re-render may produce identical output and result in zero DOM mutations (React bails out where possible, although it still ran the function).
- A component re-renders when its state changes, its parent re-renders (unless memoized), or its context value changes.
- "Render" is cheap; the expensive part is the reconeneration + DOM writes, which React optimizes.

\`\`\`jsx
function Child({ value }) {
  console.log('Child rendered'); // logs on every parent render
  return <p>{value}</p>;
}

function Parent() {
  const [n, setN] = React.useState(0);
  return (
    <div>
      <button onClick={() => setN(n + 1)}>inc</button>
      <Child value="static" /> {/* re-renders too, though value unchanged */}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:38,sortKey:1010},{id:"react-1011",number:39,title:"Why can't you use `if` statements or loops directly inside JSX?",question:"Why can't you use `if` statements or loops directly inside JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"JSX is an expression that compiles to a function call returning an element. Inside JSX, the `{ }` braces accept a **JavaScript expression** (something that evaluates to a value), not a statement. `if`, `for`, `while` are statements and cannot appear directly inside `{ }`. \n\nTo use conditional logic, use expressions: ternaries, `&&`, or compute a variable beforehand. To repeat, use `.map()` (an expression returning an array) rather than `for`. You *can* use `if`/`for` in the **component body** outside JSX — that's the correct place for statements.\n\n```jsx\nfunction List({ items }) {\n  // OK: statements go in the body\n  const rows = [];\n  for (const item of items) {\n    rows.push(<li key={item.id}>{item.text}</li>);\n  }\n\n  // OK inside JSX: .map is an expression\n  return <ul>{items.map(i => <li key={i.id}>{i.text}</li>)}</ul>;\n\n  // WRONG inside JSX: if/for are statements\n  // return <ul>{ for (...) {} }</ul>;\n}\n```",tech:"react",displayNumber:39,sortKey:1011},{id:"react-1012",number:40,title:"What can you put inside `{ }` in JSX?",question:"What can you put inside `{ }` in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"Inside `{ }` you can place **any JavaScript expression** that evaluates to a value:\n- Variables: `{name}`\n- Function calls: `{formatDate(date)}`\n- Ternaries: `{isLoading ? <Spinner /> : <Content />}`\n- `&&` / `||`: `{items.length && <List items={items} />}`\n- Objects/arrays: `{style}` (as a prop), `{children}` (nested)\n- Template literals: `{'Hello ' + name}`\n\nYou CANNOT place statements (`if`, `for`, `const x = 1`), but you can wrap statements in an IIFE if desperate (not recommended). Also `undefined`, `null`, `true`, `false` render nothing — useful for conditional rendering.\n\n```jsx\nconst user = { name: 'Ada', roles: ['admin', 'editor'] };\n\nfunction Card() {\n  return (\n    <div className=\"card\">\n      <h2>{user.name}</h2>                    {/* variable */}\n      <p>{user.name.toUpperCase()}</p>        {/* method call */}\n      <p>{`Hello, ${user.name}`}</p>          {/* template literal */}\n      {user.roles.length > 0 && <Badge />}   {/* logical && */}\n      {user.roles.map(r => <span key={r}>{r}</span>)} {/* array from map */}\n    </div>\n  );\n}\n```",tech:"react",displayNumber:40,sortKey:1012},{id:"react-1013",number:41,title:"What are the rules of JSX?",question:"What are the rules of JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:'1. **Single root element**: A component must return exactly one parent element (or Fragment). JSX cannot return multiple siblings without a wrapper.\n2. **Close all tags**: Self-close (`<img />`) or have a closing tag (`<div></div>`). No unclosed tags.\n3. **camelCase for attributes**: `className`, `htmlFor`, `onClick`, `tabIndex`, not `class`, `for`, `onclick`.\n4. **Expressions in `{ }`**: Only JavaScript expressions, no statements.\n5. **Keys for lists**: Each item in a list needs a unique `key` prop on the top-level element.\n6. **HTML entities**: Use Unicode or numbers (`©` or `&copy;`) not `&copy;` directly in text (JSX is not HTML).\n7. **Comments**: Use `{/* comment */}` inside JSX children, not `<!-- -->`.\n\n```jsx\nfunction App() {\n  return (\n    <>           // Fragment: single root without extra DOM node\n      <input className="input" htmlFor="name" />\n      <button onClick={fn} tabIndex={0}>Go</button>\n      {items.map(i => <li key={i.id}>{i.name}</li>)}\n    </>\n  );\n}\n```',tech:"react",displayNumber:41,sortKey:1013},{id:"react-1014",number:42,title:"What is the difference between `className` and `class` in JSX?",question:"What is the difference between `className` and `class` in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:'`class` is a reserved keyword in JavaScript (ES6 classes). Since JSX compiles to JavaScript, you cannot use `class` as a prop name. React uses `className` instead, which maps directly to the DOM `class` attribute. Similarly, `for` becomes `htmlFor` (because `for` is a loop keyword). This is a JSX-to-JS compilation constraint, not a React runtime constraint.\n\n```jsx\n// WRONG: \'class\' is a reserved word in JS\n// <div class="box">Hi</div>\n\n// CORRECT:\n<div className="box">Hi</div>\n\n// CORRECT for <label>:\n<label htmlFor="input-id">Label</label>\n```',tech:"react",displayNumber:42,sortKey:1014},{id:"react-1015",number:43,title:"What is `children` in React and how does it work?",question:"What is `children` in React and how does it work?",difficulty:"Basic",category:"React Basics & JSX",answer:`\`children\` is a special prop automatically populated with whatever content you place between a component's opening and closing tags. It enables **component composition** — a parent passes arbitrary content to a child component, which decides where to render it.

\`children\` can be a single element, an array of elements, a string, number, or even a function (render props pattern). React provides \`React.Children\` utilities to iterate/manipulate it safely without assuming its type.

\`\`\`jsx
function Card({ children, title }) {
  return (
    <div className="card">
      <h3>{title}</h3>
      <div className="content">{children}</div>
    </div>
  );
}

// Usage: content between tags becomes children
<Card title="Welcome">
  <p>This is the children!</p>
  <button>Click</button>
</Card>

// Inside Card, children = [<p>..., <button>...]
\`\`\``,tech:"react",displayNumber:43,sortKey:1015},{id:"react-1016",number:44,title:"What is prop drilling and why can it be a problem?",question:"What is prop drilling and why can it be a problem?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Prop drilling means passing data through multiple layers of components just so a deeply nested component can use it. The intermediate components don't need the data; they merely forward it.

Problems:
- Components become tightly coupled to data they don't use.
- Refactoring becomes harder because many component signatures change.
- Reusable components become less reusable because they accept irrelevant props.

Solutions depend on scale:
- For 1-2 levels: props are fine — don't over-engineer.
- For many levels or global-ish data: use Context, state management, or component composition (pass the ready-made child instead of raw props).

\`\`\`jsx
// Prop drilling: Layout and Sidebar don't care about user, but must forward it
function App() { return <Layout user={user} />; }
function Layout({ user }) { return <Sidebar user={user} />; }
function Sidebar({ user }) { return <UserAvatar user={user} />; }

// Composition alternative:
function App() {
  return <Layout sidebar={<UserAvatar user={user} />} />;
}
function Layout({ sidebar }) { return <aside>{sidebar}</aside>; }
\`\`\``,tech:"react",displayNumber:44,sortKey:1016},{id:"react-1017",number:45,title:"What is lifting state up in React?",question:"What is lifting state up in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`Lifting state up means moving state from a child component to the closest common parent so multiple sibling components can share it. Since React data flows down through props, siblings cannot directly share state. The parent owns the state and passes values/callbacks down.

Use it when two or more components need to read or update the same data. Don't lift state higher than necessary; putting everything at the top causes unnecessary re-renders and makes data flow harder to follow.

\`\`\`jsx
function Parent() {
  const [text, setText] = React.useState('');
  return (
    <>
      <Input value={text} onChange={setText} />
      <Preview value={text} />
    </>
  );
}
function Input({ value, onChange }) {
  return <input value={value} onChange={e => onChange(e.target.value)} />;
}
function Preview({ value }) { return <p>{value}</p>; }
\`\`\``,tech:"react",displayNumber:45,sortKey:1017},{id:"react-1018",number:46,title:"What is one-way data flow in React?",question:"What is one-way data flow in React?",difficulty:"Basic",category:"React Basics & JSX",answer:`One-way data flow means data moves from parent to child via props, never upward directly. A child can request a change by calling a function prop, but the parent decides whether and how to update state. After the parent state changes, new props flow downward again.

This model makes UIs predictable: for any state, the rendered UI is deterministic. It also makes debugging easier because you can trace data from its source downward through the component tree.

\`\`\`jsx
function Parent() {
  const [count, setCount] = React.useState(0);
  return <Child count={count} onIncrement={() => setCount(c => c + 1)} />;
}
function Child({ count, onIncrement }) {
  return <button onClick={onIncrement}>{count}</button>;
}
// Child does not mutate Parent's state; it asks Parent to update via callback
\`\`\``,tech:"react",displayNumber:46,sortKey:1018},{id:"react-1019",number:47,title:"What does it mean that React components should be pure?",question:"What does it mean that React components should be pure?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`A pure component returns the same JSX for the same props/state and does not cause side effects during render. Rendering should be a calculation, not an action. This matters because React may render components multiple times, skip commits, or restart renders under Strict Mode and concurrent rendering.

Avoid in render:
- Mutating props/state/global variables
- Network requests
- Subscriptions
- Timers
- DOM manipulation
- Random IDs without stable hooks

Side effects belong in event handlers or effects (\`useEffect\`, \`useLayoutEffect\`).

\`\`\`jsx
// Bad: mutation in render
function Bad({ items }) {
  items.push('extra');
  return <ul>{items.map(x => <li key={x}>{x}</li>)}</ul>;
}

// Good: derive new values without mutating input
function Good({ items }) {
  const displayItems = [...items, 'extra'];
  return <ul>{displayItems.map(x => <li key={x}>{x}</li>)}</ul>;
}
\`\`\``,tech:"react",displayNumber:47,sortKey:1019},{id:"react-1020",number:48,title:"What is Strict Mode in React?",question:"What is Strict Mode in React?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`\`React.StrictMode\` is a development-only wrapper that helps detect unsafe patterns. It does not render UI and has no production effect. In React 18, Strict Mode intentionally double-invokes certain functions (component render, state initializer, effect setup/cleanup) in development to expose accidental side effects.

It helps detect:
- Impure rendering
- Missing effect cleanup
- Deprecated APIs
- Unsafe lifecycle methods
- Legacy string refs

If code breaks under Strict Mode, the code likely has side effects in the wrong place or missing cleanup.

\`\`\`jsx
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// In dev, effects may run setup -> cleanup -> setup to verify cleanup logic
\`\`\``,tech:"react",displayNumber:48,sortKey:1020},{id:"react-1021",number:49,title:"What is the difference between `null`, `false`, `undefined`, and an empty string in JSX rendering?",question:"What is the difference between `null`, `false`, `undefined`, and an empty string in JSX rendering?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"React renders `null`, `undefined`, `true`, and `false` as nothing. Numbers and strings render as text nodes. This is why conditional rendering often returns `null` to show nothing.\n\nGotchas:\n- `0` renders visibly as `0`, so `count && <Badge />` can accidentally render `0`.\n- Empty string `''` technically renders an empty text node, usually invisible.\n- Returning `undefined` from a component is allowed in modern React, but returning `null` is clearer and intentional.\n\n```jsx\nfunction Demo({ count }) {\n  return (\n    <div>\n      {false}{null}{undefined}{true} {/* renders nothing */}\n      {0}                            {/* renders 0 */}\n      {count > 0 && <span>{count}</span>} {/* safe */}\n    </div>\n  );\n}\n```",tech:"react",displayNumber:49,sortKey:1021},{id:"react-1022",number:50,title:"How do you render lists in React?",question:"How do you render lists in React?",difficulty:"Basic",category:"React Basics & JSX",answer:"Lists are rendered by transforming an array of data into an array of React elements, usually using `.map()`. Each rendered item must have a stable `key` prop so React can track identity across re-renders.\n\nAvoid mutating the source array in render (`sort`, `reverse`, `splice`) unless you copy it first, because props/state should be immutable. Use `filter`, `map`, and `slice` which return new arrays.\n\n```jsx\nfunction TodoList({ todos }) {\n  return (\n    <ul>\n      {todos.map(todo => (\n        <li key={todo.id}>{todo.text}</li>\n      ))}\n    </ul>\n  );\n}\n\n// Sort safely: copy first\nconst sorted = [...todos].sort((a, b) => a.text.localeCompare(b.text));\n```",tech:"react",displayNumber:50,sortKey:1022},{id:"react-1023",number:51,title:"What is the purpose of `key` beyond removing warnings?",question:"What is the purpose of `key` beyond removing warnings?",difficulty:"Advanced",category:"React Basics & JSX",answer:"A key defines a component's identity among siblings. React uses it to decide whether to preserve an existing fiber and its state or unmount it and mount a new one. Therefore changing a key intentionally resets all state in that subtree; keeping a key preserves it. Keys are reconciliation hints, not props delivered to the component.\n\n```jsx\nfunction Form({ userId }) {\n  return <UserForm key={userId} userId={userId} />; // reset form when user changes\n}\n```",tech:"react",displayNumber:51,sortKey:1023},{id:"react-1024",number:52,title:"Why does React not pass `key` and `ref` through props?",question:"Why does React not pass `key` and `ref` through props?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`key` and `ref` are special fields consumed by React's reconciler and ref system. They are metadata about an element, not ordinary component input, so React removes them before constructing `props`. Reading `props.key` or `props.ref` produces a warning. If a component needs the value, pass a separately named prop.\n\n```jsx\nfunction Row({ id }) { return <div>{id}</div>; }\n<Row key={item.id} id={item.id} />; // key identifies; id is component data\n```",tech:"react",displayNumber:52,sortKey:1024},{id:"react-1025",number:53,title:"How can changing a key reset component state?",question:"How can changing a key reset component state?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"State belongs to a component's position and identity in the rendered tree. A different key gives React a different identity, so it unmounts the old component and mounts a fresh one. This is useful for clearing forms or restarting animations, but accidental key changes lose state and effects.\n\n```jsx\nfunction App({ userId }) {\n  return <ChatInput key={userId} />; // fresh draft for each user\n}\n```",tech:"react",displayNumber:53,sortKey:1025},{id:"react-1026",number:54,title:"What is conditional component mounting versus hiding?",question:"What is conditional component mounting versus hiding?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`{open && <Panel />}` unmounts Panel when false: its state is destroyed and effects clean up. Rendering `<Panel hidden={!open} />` keeps it mounted, preserving state and effects while CSS/HTML hides it. Choose unmounting for expensive or inactive content; choose hiding when preserving user input or subscriptions is required.\n\n```jsx\n{open ? <Editor /> : null}       // unmounts\n<Editor hidden={!open} />        // remains mounted\n```",tech:"react",displayNumber:54,sortKey:1026},{id:"react-1027",number:55,title:"What is the difference between an element type and an element instance?",question:"What is the difference between an element type and an element instance?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"A type is the component function/class or host tag such as `'div'`; an element is the immutable description created by JSX. React creates and tracks runtime fibers internally when elements mount. You normally work with elements and components, not instances.\n\n```jsx\nfunction Button() { return <button>OK</button>; }\nconst element = <Button />; // description\n// Button is the type; React creates its internal mounted fiber\n```",tech:"react",displayNumber:55,sortKey:1027},{id:"react-1028",number:56,title:"Why must component names start with an uppercase letter?",question:"Why must component names start with an uppercase letter?",difficulty:"Basic",category:"React Basics & JSX",answer:"JSX uses capitalization to distinguish user components from intrinsic DOM tags. `<button>` compiles with the string type `'button'`, while `<Button>` compiles with the variable `Button`. Lowercase custom names are treated as unknown HTML tags and won't invoke the component function.\n\n```jsx\nfunction Profile() { return <p>Profile</p>; }\n<Profile />; // component\n<profile />; // treated as an intrinsic tag, not Profile\n```",tech:"react",displayNumber:56,sortKey:1028},{id:"react-1029",number:57,title:"How do you spread props in JSX?",question:"How do you spread props in JSX?",difficulty:"Basic",category:"React Basics & JSX",answer:"`<Component {...obj} />` copies enumerable properties from `obj` into props. Explicit props after the spread override earlier values; therefore spread order matters. Avoid blindly forwarding unknown props to DOM elements because React may warn or create invalid attributes. Destructure and forward only intended values at component boundaries.\n\n```jsx\nconst common = { type: 'button', disabled: false };\n<button {...common} disabled={isBusy}>Save</button> // explicit wins\n```",tech:"react",displayNumber:57,sortKey:1029},{id:"react-1030",number:58,title:"What is the difference between `defaultValue` and `value`?",question:"What is the difference between `defaultValue` and `value`?",difficulty:"Basic",category:"React Basics & JSX",answer:'`value` makes an input controlled: React remains the source of truth and must update it through `onChange`. `defaultValue` sets only the initial DOM value; afterward the browser owns it, making the input uncontrolled. Changing `defaultValue` after mount does not update the current value.\n\n```jsx\n<input value={name} onChange={e => setName(e.target.value)} />\n<input defaultValue="Initial" ref={inputRef} />\n```',tech:"react",displayNumber:58,sortKey:1030},{id:"react-1031",number:59,title:"Why is a file input always uncontrolled?",question:"Why is a file input always uncontrolled?",difficulty:"Intermediate",category:"React Basics & JSX",answer:'Browsers prohibit scripts from setting a file input\'s selected path for security and privacy. React therefore cannot control its `value`; use a ref and read `input.files` after a change or submit event. You can reset it by changing the DOM value to an empty string or remounting with a new key.\n\n```jsx\nfunction Upload() {\n  const ref = React.useRef(null);\n  return <input type="file" ref={ref} onChange={() => console.log(ref.current.files)} />;\n}\n```',tech:"react",displayNumber:59,sortKey:1031},{id:"react-1032",number:60,title:"How does React handle boolean and custom attributes?",question:"How does React handle boolean and custom attributes?",difficulty:"Basic",category:"React Basics & JSX",answer:'Boolean DOM properties use JSX booleans: `<button disabled={true}>`. For a boolean attribute, `disabled` is present when true and omitted when false. Custom data and accessibility attributes retain lowercase hyphenated spelling (`data-id`, `aria-label`). Unknown ordinary attributes are supported in modern React but should represent valid HTML or data.\n\n```jsx\n<button disabled={loading} aria-label="Save" data-testid="save">Save</button>\n```',tech:"react",displayNumber:60,sortKey:1032},{id:"react-1033",number:61,title:"What is the difference between `onChange` in React and HTML?",question:"What is the difference between `onChange` in React and HTML?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"React's `onChange` is normalized to provide immediate updates for text inputs as the user types, unlike traditional HTML's change event which historically fires on blur for text fields. For checkboxes, use `e.target.checked`; for text controls, use `e.target.value`. React still exposes a synthetic event with standard cancellation and propagation methods.\n\n```jsx\n<input value={text} onChange={e => setText(e.target.value)} />\n<input type=\"checkbox\" checked={ok} onChange={e => setOk(e.target.checked)} />\n```",tech:"react",displayNumber:61,sortKey:1033},{id:"react-1034",number:62,title:"How do `preventDefault` and `stopPropagation` differ?",question:"How do `preventDefault` and `stopPropagation` differ?",difficulty:"Basic",category:"React Basics & JSX",answer:"`preventDefault()` cancels the browser's default action, such as following a link or submitting a form. `stopPropagation()` prevents the event from traveling to ancestor handlers during capture/bubble phases. They solve different problems and can be used together. Neither stops other handlers on the same element in every native scenario; `stopImmediatePropagation` is a lower-level native API.\n\n```jsx\n<form onSubmit={e => { e.preventDefault(); save(); }}>\n  <button type=\"submit\">Save</button>\n</form>\n<div onClick={() => console.log('parent')}>\n  <button onClick={e => { e.stopPropagation(); }}>Child</button>\n</div>\n```",tech:"react",displayNumber:62,sortKey:1034},{id:"react-1035",number:63,title:"What are capture and bubble phases in React events?",question:"What are capture and bubble phases in React events?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"An event travels down the tree in the capture phase, reaches its target, then travels upward in the bubble phase. React's normal `onClick` handlers run during bubbling. Appending `Capture` (`onClickCapture`) runs during capture, before the target and bubble handlers. Capture is useful for global logging or policies that must observe events before children stop propagation.\n\n```jsx\n<div onClickCapture={() => console.log('capture')} onClick={() => console.log('bubble')}>\n  <button onClick={() => console.log('target')}>Go</button>\n</div>\n// capture -> target -> bubble\n```",tech:"react",displayNumber:63,sortKey:1035},{id:"react-1036",number:64,title:"How do you handle keyboard events accessibly?",question:"How do you handle keyboard events accessibly?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"Prefer native interactive elements (`button`, `a`, `input`) because browsers provide keyboard and accessibility behavior automatically. Use `onKeyDown`/`onKeyUp` only when needed, inspect `event.key`, and avoid replacing a button with a clickable `div`. If a custom widget is unavoidable, provide focusability, appropriate ARIA roles, and equivalent keyboard behavior.\n\n```jsx\n<button onKeyDown={e => e.key === 'Escape' && close()} onClick={close}>Close</button>\n```",tech:"react",displayNumber:64,sortKey:1036},{id:"react-1037",number:65,title:"What is a ref and how does it differ from state?",question:"What is a ref and how does it differ from state?",difficulty:"Basic",category:"React Basics & JSX",answer:"A ref (`useRef`) stores a mutable value whose changes do not trigger re-renders. Its `.current` survives renders and is commonly used for DOM nodes, timers, previous values, and imperative APIs. State is for data that affects displayed output; refs are for values React doesn't need to render.\n\n```jsx\nfunction Focus() {\n  const input = React.useRef(null);\n  return <><input ref={input} /><button onClick={() => input.current.focus()}>Focus</button></>;\n}\n```",tech:"react",displayNumber:65,sortKey:1037},{id:"react-1038",number:66,title:"What is `React.memo` and when should it be used?",question:"What is `React.memo` and when should it be used?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`React.memo` wraps a function component and skips rendering it when its props are shallowly equal to the previous props. It is a performance optimization, not a correctness feature. It does not prevent re-renders from the component's own state or consumed context, and a new object/function prop each render defeats shallow equality.\n\nUse it after profiling shows a frequently rendered, expensive child receives unchanged props. Do not wrap every component by default; the comparison itself and code complexity have costs.\n\n```jsx\nconst ListItem = React.memo(function ListItem({ item, onSelect }) {\n  return <li onClick={() => onSelect(item.id)}>{item.name}</li>;\n});\n```",tech:"react",displayNumber:66,sortKey:1038},{id:"react-1039",number:67,title:"What is the difference between `useMemo` and `React.memo`?",question:"What is the difference between `useMemo` and `React.memo`?",difficulty:"Intermediate",category:"React Basics & JSX",answer:"`React.memo(Component)` memoizes a **component's render** based on its props. `useMemo(factory, deps)` memoizes a **computed value inside a component** until dependencies change. Neither should be used to make code correct; both are optimizations and may be removed if the value is not expensive or stable identity is unnecessary.\n\n```jsx\nconst Chart = React.memo(function Chart({ points }) { return <svg />; });\nfunction Dashboard({ data }) {\n  const sorted = React.useMemo(() => [...data].sort(byDate), [data]);\n  return <Chart points={sorted} />;\n}\n```",tech:"react",displayNumber:67,sortKey:1039},{id:"react-1040",number:68,title:"How can unnecessary re-renders happen in a React tree?",question:"How can unnecessary re-renders happen in a React tree?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`A component re-renders when its state changes, its parent renders, or a consumed context value changes. Common avoidable causes include creating new object/array/function props on every parent render, putting state too high in the tree, changing keys, and broad context values that update frequently. A re-render is not automatically a bug; measure with the Profiler before optimizing.

\`\`\`jsx
function Parent({ theme }) {
  const [count, setCount] = React.useState(0);
  const options = React.useMemo(() => ({ theme }), [theme]);
  const onSave = React.useCallback(() => save(), []);
  return <ExpensiveChild options={options} onSave={onSave} />;
}
\`\`\``,tech:"react",displayNumber:68,sortKey:1040},{id:"react-1041",number:69,title:"What is `ReactDOM.createRoot` and how does it differ from `ReactDOM.render`?",question:"What is `ReactDOM.createRoot` and how does it differ from `ReactDOM.render`?",difficulty:"Basic",category:"React Basics & JSX",answer:"`createRoot` is the React 18 client entry point and enables the concurrent renderer and React 18 features such as automatic batching and transitions. `ReactDOM.render` is the legacy React 17 API; it remains for compatibility but opts the tree into legacy behavior and is deprecated for new applications. A root should normally be created once per DOM container.\n\n```jsx\nconst root = ReactDOM.createRoot(document.getElementById('root'));\nroot.render(<App />);\n// Later: root.unmount();\n```",tech:"react",displayNumber:69,sortKey:1041},{id:"react-1042",number:70,title:"What is automatic batching in React 18?",question:"What is automatic batching in React 18?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`Batching groups multiple state updates into one render. React 18 with \`createRoot\` extends automatic batching beyond React event handlers to timeouts, promises, and native events. This reduces work and avoids intermediate UI states. If an update must be committed synchronously (rare), \`flushSync\` can opt out, but it should not be used casually because it blocks scheduling.

\`\`\`jsx
function Example() {
  const [a, setA] = React.useState(0);
  const [b, setB] = React.useState(0);
  function later() {
    setTimeout(() => {
      setA(x => x + 1);
      setB(x => x + 1); // one render in React 18 createRoot
    }, 0);
  }
  return <button onClick={later}>{a + b}</button>;
}
\`\`\``,tech:"react",displayNumber:70,sortKey:1042},{id:"react-1043",number:71,title:"What is `useTransition` and how does it affect rendering priority?",question:"What is `useTransition` and how does it affect rendering priority?",difficulty:"Advanced",category:"React Basics & JSX",answer:`\`useTransition\` marks state updates as non-urgent. Urgent updates such as typing remain responsive while React works on the transition in the background. It returns \`isPending\` and \`startTransition\`. Transition updates can be interrupted and restarted; they are not a replacement for debouncing network requests or for showing a loading state.

\`\`\`jsx
function Search({ items }) {
  const [query, setQuery] = React.useState('');
  const [results, setResults] = React.useState(items);
  const [isPending, startTransition] = React.useTransition();
  function change(e) {
    const value = e.target.value;
    setQuery(value); // urgent: keep input responsive
    startTransition(() => setResults(filter(items, value))); // non-urgent
  }
  return <><input value={query} onChange={change} />{isPending && <span>Updating...</span>}</>;
}
\`\`\``,tech:"react",displayNumber:71,sortKey:1043},{id:"react-1044",number:72,title:"What is `useDeferredValue` and when might it help?",question:"What is `useDeferredValue` and when might it help?",difficulty:"Advanced",category:"React Basics & JSX",answer:"`useDeferredValue(value)` returns a deferred version of a value that may lag behind the current one while urgent work (such as typing) stays responsive. React first renders with the old deferred value, then updates the expensive consumer at lower priority. It is useful when you don't control the state update that produces the value; use `useTransition` when you do control the update.\n\n```jsx\nfunction SearchResults({ query }) {\n  const deferredQuery = React.useDeferredValue(query);\n  const results = useMemo(() => expensiveFilter(deferredQuery), [deferredQuery]);\n  return <Results items={results} />;\n}\n```",tech:"react",displayNumber:72,sortKey:1044},{id:"react-1045",number:73,title:"What is an Error Boundary and why is it relevant to component trees?",question:"What is an Error Boundary and why is it relevant to component trees?",difficulty:"Intermediate",category:"React Basics & JSX",answer:`An Error Boundary is a class component that catches JavaScript errors during rendering, lifecycle methods, and constructors in its descendant tree, then displays fallback UI and can log the error. It does not catch errors in event handlers, asynchronous callbacks, server rendering, or errors thrown by the boundary itself; handle those separately.

Boundaries isolate failures so one broken panel does not blank the whole application. Place them around meaningful independent UI regions.

\`\`\`jsx
class Boundary extends React.Component {
  state = { error: null };
  static getDerivedStateFromError(error) { return { error }; }
  componentDidCatch(error, info) { report(error, info); }
  render() { return this.state.error ? <p>Failed to load.</p> : this.props.children; }
}
\`\`\``,tech:"react",displayNumber:73,sortKey:1045},{id:"react-1046",number:74,title:"How does React handle an element type change during reconciliation?",question:"How does React handle an element type change during reconciliation?",difficulty:"Advanced",category:"React Basics & JSX",answer:"If an element's type changes at the same tree position — for example, from `<Counter />` to `<Other />`, or `<div>` to `<span>` — React treats the old subtree and new subtree as unrelated. It unmounts the old tree (running cleanup and losing state) and mounts the new one. If the type stays the same, React updates props and reconciles descendants, preserving state where identity/keys match.\n\n```jsx\nfunction Screen({ compact }) {\n  // toggling type remounts the subtree\n  return compact ? <CompactPanel /> : <FullPanel />;\n}\n```",tech:"react",displayNumber:74,sortKey:1046},{id:"react-1047",number:75,title:"What are the most important Core React and JSX principles to remember?",question:"What are the most important Core React and JSX principles to remember?",difficulty:"Advanced",category:"React Basics & JSX",answer:`A strong interview summary is:
- Describe UI declaratively; React reconciles element trees and commits minimal DOM changes.
- JSX is syntax that compiles to \`createElement\` or the automatic JSX runtime.
- Components should be pure; put side effects in event handlers/effects.
- Props flow down and are read-only; state is owned and updated through setters.
- Treat state and props as immutable so reference equality and memoization remain reliable.
- Use stable, data-derived keys for lists; indexes are unsafe for mutable lists.
- Use controlled inputs when React needs to own form state; refs/uncontrolled inputs when the DOM should own it.
- Fragments group siblings without extra DOM; explicit fragments are required for keyed fragment lists.
- React events use camelCase handlers and SyntheticEvents, with capture and bubbling behavior.
- Optimize only after measuring; Fiber, batching, transitions, and memoization help, but correctness and clear data flow come first.

\`\`\`jsx
function TodoApp({ initialTodos }) {
  const [todos, setTodos] = React.useState(initialTodos);
  const [text, setText] = React.useState('');

  function add(e) {
    e.preventDefault();
    if (!text.trim()) return;
    setTodos(prev => [...prev, { id: crypto.randomUUID(), text }]);
    setText('');
  }

  return (
    <>
      <form onSubmit={add}>
        <input value={text} onChange={e => setText(e.target.value)} />
        <button type="submit">Add</button>
      </form>
      <ul>
        {todos.map(todo => <li key={todo.id}>{todo.text}</li>)}
      </ul>
    </>
  );
}
\`\`\``,tech:"react",displayNumber:75,sortKey:1047},{id:"react-1048",number:76,title:"What is `useState` and how do you use it?",question:"What is `useState` and how do you use it?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`\`useState\` is a Hook that lets you add local state to a function component. It returns a pair: the current state value and a setter function that updates it and re-renders the component. Calling the setter with a new value (different from the current by \`Object.is\` comparison for primitives) schedules a re-render. State is isolated per component instance, so two \`<Counter />\` components never share state.

\`\`\`jsx
import { useState } from 'react';

function Counter() {
  const [count, setCount] = useState(0); // initial value 0

  return (
    <button onClick={() => setCount(count + 1)}>
      Clicked {count} times
    </button>
  );
}
// ponytail: useState returns [value, setter]; setter triggers re-render
\`\`\``,tech:"react",displayNumber:76,sortKey:1048},{id:"react-1049",number:77,title:"What is lazy initialization in `useState`?",question:"What is lazy initialization in `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`\`useState\` accepts either a direct initial value or an initializer function. The function form is only run on the first render, which avoids expensive recomputation on every render. Use it when computing the initial state is costly (e.g., reading from localStorage, parsing JSON, deriving from props). Passing the result directly runs the computation on every render even though it is discarded.

\`\`\`jsx
import { useState } from 'react';

// Direct value: runs expensiveCalc() on EVERY render
const [a] = useState(expensiveCalc());

// Lazy init: expensiveCalc() runs ONCE
const [b] = useState(() => {
  const saved = localStorage.getItem('todos');
  return saved ? JSON.parse(saved) : [];
});
\`\`\``,tech:"react",displayNumber:77,sortKey:1049},{id:"react-1050",number:78,title:"What are functional updates in `useState`?",question:"What are functional updates in `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"When the next state depends on the previous state, pass a function to the setter: `setState(prev => next)`. React guarantees `prev` is the latest committed state. This avoids stale-closure bugs, especially inside event handlers, async callbacks, intervals, or when multiple state updates are batched in the same render cycle. Using `setCount(count + 1)` twice in the same handler results in only one increment if `count` is stale.\n\n```jsx\nconst [count, setCount] = useState(0);\n\n// Bug: both use the same stale `count` => only +1\nconst bad = () => { setCount(count + 1); setCount(count + 1); };\n\n// Correct: each update sees the prior result => +2\nconst good = () => { setCount(c => c + 1); setCount(c => c + 1); };\n```",tech:"react",displayNumber:78,sortKey:1050},{id:"react-1051",number:79,title:"How does state batching work in React 18?",question:"How does state batching work in React 18?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`React batches multiple state updates into a single re-render for performance. In React 17 and earlier, batching happened only inside React event handlers, not in promises, setTimeout, or native event callbacks. React 18 extends automatic batching to those async contexts too, using the same reconciler consistently. If you truly need to flush updates synchronously, use \`flushSync\` from \`react-dom\` (use sparingly).

\`\`\`jsx
import { flushSync } from 'react-dom';

setTimeout(() => {
  // React 18: still batched into one re-render
  setA(1);
  setB(2);
}, 1000);

// Escape hatch: forces synchronous flush (rarely needed)
flushSync(() => { setA(1); });
\`\`\``,tech:"react",displayNumber:79,sortKey:1051},{id:"react-1052",number:80,title:"Can you update multiple state variables independently?",question:"Can you update multiple state variables independently?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Yes. You can call multiple `useState` hooks in one component and update them independently. Each is a separate state slot. React re-renders once after the event handler finishes, applying all queued updates. Keeping state split into the smallest reasonable pieces (rather than one giant object) is usually clearer, though for tightly coupled data a `useReducer` may be better.\n\n```jsx\nconst [name, setName] = useState('');\nconst [age, setAge] = useState(0);\n\nfunction handleSubmit() {\n  setName('Ada');\n  setAge(36); // both applied, single re-render\n}\n```",tech:"react",displayNumber:80,sortKey:1052},{id:"react-1053",number:81,title:"Why shouldn't you mutate state directly?",question:"Why shouldn't you mutate state directly?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"React compares state with `Object.is` to decide whether to re-render and to preserve referential immutability. Mutating an object in place keeps the same reference, so React may skip the update or render stale UI. Always create a new object/array with the change. For arrays use `map`, `filter`, spread; for objects use spread with overrides.\n\n```jsx\nconst [user, setUser] = useState({ name: 'Ada', age: 36 });\n\n// Wrong: mutate in place, same reference\nuser.age = 37; setUser(user);\n\n// Right: new object reference\nsetUser(prev => ({ ...prev, age: 37 }));\n```",tech:"react",displayNumber:81,sortKey:1053},{id:"react-1054",number:82,title:"What happens when state is set to the same value?",question:"What happens when state is set to the same value?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"If you set state to a value that is `Object.is`-equal to the current state, React bails out and does not re-render that component (and its children won't re-render from this update either). For objects/arrays this means a new reference with identical contents still triggers a re-render because the reference differs. This bail-out is a performance optimization; you don't need to guard updates manually.\n\n```jsx\nsetCount(0);   // if already 0 -> no re-render (primitive, equal)\nsetObj({ a: 1 }); // new reference -> re-render even if contents same\n```",tech:"react",displayNumber:82,sortKey:1054},{id:"react-1055",number:83,title:"How do you reset state when a key changes?",question:"How do you reset state when a key changes?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"The cleanest way to reset a component's internal state is to change its `key` prop. When React sees a different key, it treats it as a new instance and unmounts the old one, discarding its state, then mounts fresh. This is simpler and more reliable than calling setters in effects. It is especially useful for forms keyed by item id.\n\n```jsx\nfunction Profile({ userId }) {\n  // remounts when userId changes -> state resets automatically\n  return <UserForm key={userId} userId={userId} />;\n}\n```",tech:"react",displayNumber:83,sortKey:1055},{id:"react-1056",number:84,title:"When should you use `useReducer` instead of `useState`?",question:"When should you use `useReducer` instead of `useState`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Prefer `useReducer` when state transitions are complex, depend on previous state in non-trivial ways, involve multiple sub-values that change together, or when the next state logic is hard to express inline. It centralizes the "how state changes" into a reducer function, making updates predictable and testable, and it scales better than many `useState` calls whose setters must coordinate.\n\n```jsx\nconst [state, dispatch] = useReducer(reducer, initialState);\n// Use when: multiple related values, branching logic, or many event types\n```',tech:"react",displayNumber:84,sortKey:1056},{id:"react-1057",number:85,title:"How do you write a reducer and dispatch actions?",question:"How do you write a reducer and dispatch actions?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"A reducer is a pure function `(state, action) => newState`. It must not mutate; return a new state based on `action.type`. `dispatch(action)` sends an action object to the reducer. Actions typically have a `type` string and an optional `payload`. Because the reducer is pure, you can unit test it without rendering.\n\n```jsx\nfunction reducer(state, action) {\n  switch (action.type) {\n    case 'increment': return { count: state.count + 1 };\n    case 'set':       return { count: action.payload };\n    default:          return state;\n  }\n}\n\nconst [state, dispatch] = useReducer(reducer, { count: 0 });\ndispatch({ type: 'increment' });\ndispatch({ type: 'set', payload: 10 });\n```",tech:"react",displayNumber:85,sortKey:1057},{id:"react-1058",number:86,title:"What is lazy initialization in `useReducer`?",question:"What is lazy initialization in `useReducer`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useReducer(reducer, initialArg, init)` accepts an optional third argument `init`, an initializer function. React calls `init(initialArg)` once to compute the initial state. This lets you derive initial state from props or do expensive setup only on first mount, keeping the reducer itself pure. Without it you pass the initial state object directly.\n\n```jsx\nfunction init(initialCount) {\n  return { count: initialCount, history: [] };\n}\nconst [state, dispatch] = useReducer(reducer, 0, init); // count from props\n```",tech:"react",displayNumber:86,sortKey:1058},{id:"react-1059",number:87,title:"How does dispatch behave — is it stable and can it run async?",question:"How does dispatch behave — is it stable and can it run async?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`dispatch` from `useReducer` is stable across renders (its identity never changes), so it's safe to omit from `useEffect` dependency arrays or pass to memoized children without breaking `React.memo`. You can dispatch inside async callbacks (e.g., after a fetch resolves). The dispatch enqueues an action; React processes the queue and re-renders. Because dispatch is stable, stale-closure problems with state are avoided.\n\n```jsx\nconst [state, dispatch] = useReducer(reducer, initial);\n\nuseEffect(() => {\n  fetchData().then(data => dispatch({ type: 'loaded', data }));\n}, []); // dispatch is stable, no need to include it\n```",tech:"react",displayNumber:87,sortKey:1059},{id:"react-1060",number:88,title:"How do you handle side effects (like fetching) with useReducer?",question:"How do you handle side effects (like fetching) with useReducer?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Reducers must stay pure — no fetching, timers, or randomness inside them. Perform side effects in event handlers or `useEffect`, then `dispatch` an action with the result (or error). Common pattern: dispatch a `pending` action before the request, then `fulfilled`/`rejected` after. This keeps data-fetching logic out of the reducer while the reducer only describes state transitions.\n\n```jsx\nfunction loadUser(id) {\n  return async (dispatch) => {            // thunk-like helper\n    dispatch({ type: 'load/pending' });\n    try {\n      const u = await api.getUser(id);\n      dispatch({ type: 'load/fulfilled', user: u });\n    } catch (e) {\n      dispatch({ type: 'load/rejected', error: e.message });\n    }\n  };\n}\n```",tech:"react",displayNumber:88,sortKey:1060},{id:"react-1061",number:89,title:"What is the `children` prop and how do you use it?",question:"What is the `children` prop and how do you use it?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`\`children\` is a special prop automatically populated with whatever is passed between a component's opening and closing tags. It lets you create composable layout/container components. You can render it directly, or use functions-as-children (render props) to pass data down. It is just a regular prop — it can be a node, array, string, or function.

\`\`\`jsx
function Card({ children }) {
  return <div className="card">{children}</div>;
}

<Card>
  <h1>Title</h1>
  <p>Body content</p>
</Card>;
\`\`\``,tech:"react",displayNumber:89,sortKey:1061},{id:"react-1062",number:90,title:"How do you set default values for props?",question:"How do you set default values for props?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"In function components, set defaults by destructuring with a fallback: `function Greet({ name = 'Guest' }) {}`. The legacy `Component.defaultProps` works for class components but is discouraged for function components in modern React (and ignored for destructured defaults). Default parameters only apply when the prop is `undefined`, not when it's `null` or `false`.\n\n```jsx\nfunction Greet({ name = 'Guest', age = 18 }) {\n  return <p>{name} is {age}</p>;\n}\n// Greet({}) -> \"Guest is 18\"; Greet({ name: null }) -> \"null is 18\"\n```",tech:"react",displayNumber:90,sortKey:1062},{id:"react-1063",number:91,title:"What is prop drilling and what problems does it cause?",question:"What is prop drilling and what problems does it cause?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Prop drilling is passing data through many intermediate components that don't need it, just to reach a deeply nested child. It couples components, makes refactoring painful, and clutters intermediate signatures. It's fine for one or two levels; beyond that, prefer Context, a state library, or composition (passing elements via `children`/`slots`) to avoid threading props.\n\n```jsx\n// Drilling: App -> A -> B -> C all forward `user`\n<A user={user} />   // A doesn't use user, just forwards\n// Better: Context or render the consumer where data lives\n```",tech:"react",displayNumber:91,sortKey:1063},{id:"react-1064",number:92,title:"What is the difference between prop drilling and Context?",question:"What is the difference between prop drilling and Context?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Prop drilling passes values explicitly through the component tree (explicit, easy to trace, re-renders only where passed). Context provides values implicitly to any descendant without intermediate props (less boilerplate, but every consumer re-renders when the value changes unless split/memoized). Use props for local, explicit data; use Context for truly global or widely-shared state (theme, auth, locale).

\`\`\`jsx
// Prop drilling: explicit, traceable
<Nav user={user} />

// Context: implicit, avoids drilling but widens coupling
<AuthContext.Provider value={user}><Nav /></AuthContext.Provider>
\`\`\``,tech:"react",displayNumber:92,sortKey:1064},{id:"react-1065",number:93,title:"Can props be mutated? Why not?",question:"Can props be mutated? Why not?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:`Props are read-only from the child's perspective. React enforces a one-way data flow: a parent owns the data and passes it down; a child should never mutate received props because that would break predictability and cause subtle bugs (and in strict mode React may warn). To change data, the parent updates its own state and passes new props. Treat props as immutable inputs.

\`\`\`jsx
function Child({ user }) {
  // user.name = 'x'; // WRONG: props are read-only
  return <p>{user.name}</p>; // read only
}
\`\`\``,tech:"react",displayNumber:93,sortKey:1065},{id:"react-1066",number:94,title:"How do you type/validate props (PropTypes) in React?",question:"How do you type/validate props (PropTypes) in React?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`prop-types` (a separate package since React 15.5) lets you declare expected types; React logs console warnings in development when props mismatch. It's runtime validation, less powerful than TypeScript but useful in plain JS codebases. Common validators: `PropTypes.string`, `.number`, `.func`, `.arrayOf`, `.shape`, `.oneOf`, and `isRequired`. In TS projects, prefer static types instead.\n\n```jsx\nimport PropTypes from 'prop-types';\n\nButton.propTypes = {\n  label: PropTypes.string.isRequired,\n  onClick: PropTypes.func,\n  size: PropTypes.oneOf(['sm', 'md', 'lg']),\n};\n```",tech:"react",displayNumber:94,sortKey:1066},{id:"react-1067",number:95,title:"What is `useEffect` and when does it run?",question:"What is `useEffect` and when does it run?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`useEffect(callback, deps)` runs side effects after render is committed to the screen. With no dependency array it runs after every render; with an empty array `[]` it runs once after the initial mount; with a deps array it runs after mounts and whenever any dependency changes (and after the first render). The callback runs asynchronously after paint, so it's good for data fetching, subscriptions, and DOM measurements that don't block paint.\n\n```jsx\nuseEffect(() => {\n  document.title = `Count: ${count}`;\n}, [count]); // runs after mount and whenever count changes\n```",tech:"react",displayNumber:95,sortKey:1067},{id:"react-1068",number:96,title:"How do you run an effect only on mount?",question:"How do you run an effect only on mount?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Pass an empty dependency array `[]`. React then runs the effect once after the first commit and never re-runs it (ignoring dependency changes, because there are none). Be careful: values captured inside are from the first render; if you reference props/state, add them to deps or accept the initial snapshot intentionally.\n\n```jsx\nuseEffect(() => {\n  console.log('mounted once');\n  // one-time setup (e.g., init third-party lib)\n}, []); // mount-only\n```",tech:"react",displayNumber:96,sortKey:1068},{id:"react-1069",number:97,title:"How do you clean up in `useEffect`?",question:"How do you clean up in `useEffect`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Return a cleanup function from the effect callback. React runs it before re-running the effect (when deps change) and before unmounting. Use it to unsubscribe, clear timers, remove event listeners, or cancel requests — anything that would otherwise leak or cause errors on stale components. Forgetting cleanup is a common source of memory leaks and "setState on unmounted" warnings.

\`\`\`jsx
useEffect(() => {
  const id = setInterval(() => setNow(Date.now()), 1000);
  return () => clearInterval(id); // cleanup on unmount / dep change
}, []);
\`\`\``,tech:"react",displayNumber:97,sortKey:1069},{id:"react-1070",number:98,title:"How do you fetch data in `useEffect` safely?",question:"How do you fetch data in `useEffect` safely?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Fetch inside an effect with an empty (or param-based) deps array, and guard against setting state after unmount using an `ignore` flag or `AbortController`. Otherwise a late response can call `setState` on an unmounted component (React warns, and it's a leak). Modern approach: use `AbortController` to cancel the request on cleanup.\n\n```jsx\nuseEffect(() => {\n  const controller = new AbortController();\n  fetch(`/api/user/${id}`, { signal: controller.signal })\n    .then(r => r.json())\n    .then(setUser)\n    .catch(e => { if (e.name !== 'AbortError') setError(e); });\n  return () => controller.abort(); // cancel on unmount / id change\n}, [id]);\n```",tech:"react",displayNumber:98,sortKey:1070},{id:"react-1071",number:99,title:"What happens when dependencies are missing or wrong?",question:"What happens when dependencies are missing or wrong?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"If you omit a dependency that the effect uses, the effect closes over a stale value and won't re-run when that value changes, causing bugs. The exhaustive-deps ESLint rule flags this. Conversely, including unstable references (new objects/functions each render) causes the effect to run every render. Fix by adding real deps, or memoizing unstable values with `useMemo`/`useCallback`.\n\n```jsx\n// Bug: `id` used but not in deps -> never refetches on id change\nuseEffect(() => { fetchUser(id); }, []); // exhaustive-deps warns\n// Fix: add id, or guard intentionally and document why\nuseEffect(() => { fetchUser(id); }, [id]);\n```",tech:"react",displayNumber:99,sortKey:1071},{id:"react-1072",number:100,title:"How do you skip effect runs on initial mount?",question:"How do you skip effect runs on initial mount?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`There's no built-in "skip first render" flag, but you can use a ref to track whether it's the first run and return early. This is useful when you want to react only to updates, not the initial mount. Alternatively, initialize state from the value so the first effect run is a no-op.

\`\`\`jsx
const isFirst = useRef(true);
useEffect(() => {
  if (isFirst.current) { isFirst.current = false; return; }
  console.log('updated, not mounted');
}, [value]);
\`\`\``,tech:"react",displayNumber:100,sortKey:1072},{id:"react-1073",number:101,title:"How do multiple effects interact and in what order do they run?",question:"How do multiple effects interact and in what order do they run?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Effects run in the order they are declared, after the render is committed, top to bottom. Cleanups also run in declaration order, before the next set of effects when deps change, and on unmount. You can split concerns into multiple `useEffect` calls (one per logical side effect) rather than cramming everything into one — this is encouraged for clarity.\n\n```jsx\nuseEffect(() => { /* A: subscribe */ return () => {}; }, []);\nuseEffect(() => { /* B: sync title */ }, [count]);\n// Order: A runs, B runs; on count change: B cleanup -> B re-run\n```",tech:"react",displayNumber:101,sortKey:1073},{id:"react-1074",number:102,title:"What is the purpose of the dependency array, precisely?",question:"What is the purpose of the dependency array, precisely?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"The dependency array tells React when to re-run the effect: it compares each entry with the previous render's values using `Object.is`. Three cases: no array → every render; `[]` → only after first render; `[a, b]` → after first render and whenever `a` or `b` changes by `Object.is`. Omitting the array when you meant `[]` is a frequent mistake that causes infinite loops with state updates.\n\n```jsx\nuseEffect(fn);          // every render\nuseEffect(fn, []);      // mount only\nuseEffect(fn, [a, b]);  // when a or b change\n```",tech:"react",displayNumber:102,sortKey:1074},{id:"react-1075",number:103,title:"How do you handle subscriptions in effects?",question:"How do you handle subscriptions in effects?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Subscribe in an effect and return a cleanup that unsubscribes. If the subscription source depends on a prop/state value, include it in deps so React re-subscribes when it changes, and the cleanup prevents overlapping subscriptions. This pattern applies to WebSocket, EventTarget, store subscriptions, etc.\n\n```jsx\nuseEffect(() => {\n  const socket = new WebSocket(`/ws/${room}`);\n  socket.onmessage = e => setMessages(m => [...m, e.data]);\n  return () => socket.close(); // unsubscribe on room change/unmount\n}, [room]);\n```",tech:"react",displayNumber:103,sortKey:1075},{id:"react-1076",number:104,title:'What is the "infinite loop" trap with useEffect and objects?',question:'What is the "infinite loop" trap with useEffect and objects?',difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`If an effect sets state and the dependency is an object/array recreated every render (new reference), the effect runs every render, sets state, triggers re-render, and loops. Also, calling a state setter whose dep is the same object reference but you spread into a new one each time loops. Fix: depend on primitive fields, memoize the object, or compute inside the effect without depending on the whole object.

\`\`\`jsx
const opts = { limit: 10 }; // new ref every render
useEffect(() => { fetchData(opts); }, [opts]); // loops!
// Fix: useState/useMemo opts, or depend on opts.limit
useEffect(() => { fetchData(opts); }, [opts.limit]);
\`\`\``,tech:"react",displayNumber:104,sortKey:1076},{id:"react-1077",number:105,title:"Can you use effects to sync external stores with React state?",question:"Can you use effects to sync external stores with React state?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes — subscribe in an effect and push external changes into state via setter, with cleanup to unsubscribe. For external stores (Redux, Zustand, browser APIs), React 18 also offers `useSyncExternalStore` which is purpose-built: it reads a snapshot and subscribes safely, handling concurrent rendering and avoiding tearing. Prefer `useSyncExternalStore` over hand-rolled effect subscriptions for stores.\n\n```jsx\nimport { useSyncExternalStore } from 'react';\n\nconst snapshot = useSyncExternalStore(store.subscribe, store.getSnapshot);\n// safer than useEffect + setState for external stores\n```",tech:"react",displayNumber:105,sortKey:1077},{id:"react-1078",number:106,title:"What is the difference between `useEffect` and `useLayoutEffect`?",question:"What is the difference between `useEffect` and `useLayoutEffect`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`useEffect` runs asynchronously after the browser paints; `useLayoutEffect` runs synchronously after DOM mutations but before the browser paints. Layout effects are for reading layout (e.g., `getBoundingClientRect`) and synchronously re-styling to avoid a visible flash. Because layout effects block paint, overuse hurts performance. For most side effects (fetching, subscriptions) `useEffect` is correct.\n\n```jsx\nuseLayoutEffect(() => {\n  // measure then set position BEFORE paint (no flicker)\n  const rect = ref.current.getBoundingClientRect();\n  setPos(rect.top);\n}, []);\n```",tech:"react",displayNumber:106,sortKey:1078},{id:"react-1079",number:107,title:"When would you choose `useLayoutEffect` over `useEffect`?",question:"When would you choose `useLayoutEffect` over `useEffect`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Choose `useLayoutEffect` only when you must read computed layout and make DOM changes (or state changes that affect layout) before the user sees the painted frame — e.g., tooltip positioning, animations that start from a measured size, or avoiding a flash of wrong styles. Otherwise prefer `useEffect` to keep paint fast. On the server, `useLayoutEffect` warns; guard or use `useEffect` there.\n\n```jsx\n// Tooltip: measure target, set coordinates, all before paint\nuseLayoutEffect(() => {\n  const { top } = anchorRef.current.getBoundingClientRect();\n  tipRef.current.style.top = `${top}px`;\n}, [anchor]);\n```",tech:"react",displayNumber:107,sortKey:1079},{id:"react-1080",number:108,title:"Why does `useLayoutEffect` warn during SSR?",question:"Why does `useLayoutEffect` warn during SSR?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useLayoutEffect` cannot run on the server because there is no DOM to measure or mutate before paint. React emits a warning during SSR if it's used. The common fix is `useIsomorphicLayoutEffect`: a variable that is `useLayoutEffect` in the browser and `useEffect` on the server. This avoids the warning while keeping layout-sync behavior client-side.\n\n```jsx\nimport { useEffect, useLayoutEffect } from 'react';\n\nconst useIsomorphicLayoutEffect =\n  typeof window !== 'undefined' ? useLayoutEffect : useEffect;\n```",tech:"react",displayNumber:108,sortKey:1080},{id:"react-1081",number:109,title:"What are synthetic events in React?",question:"What are synthetic events in React?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"React wraps native browser events in a cross-browser `SyntheticEvent` object, normalizing properties (e.g., `event.target`, `event.preventDefault`, `event.stopPropagation`) across browsers. Handlers receive this synthetic event. It behaves like the native event but is pooled in legacy React (see next). In React 17+, events are attached at the root container, not document, and synthetic events are no longer pooled.\n\n```jsx\nfunction Form() {\n  return (\n    <input\n      onChange={e => console.log(e.target.value, e.type)} // SyntheticEvent\n    />\n  );\n}\n```",tech:"react",displayNumber:109,sortKey:1081},{id:"react-1082",number:110,title:"What was event pooling and is it still a thing?",question:"What was event pooling and is it still a thing?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:'In React 16 and earlier, synthetic events were "pooled": after the handler ran, the event object\'s properties were nullified and the object reused for performance. You had to call `e.persist()` to read it asynchronously. React 17 removed pooling entirely — you can freely access `e` in `setTimeout` or `async` code. Interviewers still ask; the correct modern answer is "pooling was removed in React 17."\n\n```jsx\n// React 16: e would be nulled after handler -> needed e.persist()\n// React 17+: this just works\nonClick={e => setTimeout(() => console.log(e.target), 1000)};\n```',tech:"react",displayNumber:110,sortKey:1082},{id:"react-1083",number:111,title:"How do you prevent default behavior in React events?",question:"How do you prevent default behavior in React events?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `event.preventDefault()` inside the handler — you cannot return `false` to prevent default as in some frameworks. This is essential for forms: `onSubmit` handlers should `preventDefault()` to stop the browser's full-page reload. Likewise `onClick` on links. Note React attaches listeners at the root, but `preventDefault` still works as expected.\n\n```jsx\nfunction Form() {\n  const onSubmit = e => {\n    e.preventDefault(); // stop page reload\n    console.log('handled via JS');\n  };\n  return <form onSubmit={onSubmit}><button>Go</button></form>;\n}\n```",tech:"react",displayNumber:111,sortKey:1083},{id:"react-1084",number:112,title:"How does event delegation work in React 17+?",question:"How does event delegation work in React 17+?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Before React 17, React attached a single set of listeners at the `document`. From React 17, it attaches them to the root DOM container where `ReactDOM.render` mounts, so multiple React versions can coexist and events don't leak to the document. Your `onClick` etc. are still dispatched through React's synthetic system; native listeners you add with `addEventListener` on the same element can interleave but React's delegation root differs.\n\n```jsx\n// React 17+: delegation root is the app container, not document\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n```",tech:"react",displayNumber:112,sortKey:1084},{id:"react-1085",number:113,title:"How do you pass arguments to event handlers?",question:"How do you pass arguments to event handlers?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Two common ways: wrap in an arrow function `onClick={() => handle(id)}`, or use `bind` `onClick={handle.bind(null, id)}`. The arrow form is idiomatic in modern code. Be mindful that creating a new function each render can hurt memoized children; if needed, use a data attribute or curry the handler and read `e.currentTarget.dataset` instead.\n\n```jsx\n<button onClick={() => deleteItem(id)}>Delete</button>\n// or read from dataset to avoid new closures:\n<button data-id={id} onClick={e => deleteItem(e.currentTarget.dataset.id)} />\n```",tech:"react",displayNumber:113,sortKey:1085},{id:"react-1086",number:114,title:"What is `useRef` and what is it used for?",question:"What is `useRef` and what is it used for?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"`useRef(initialValue)` returns a mutable object `{ current }` whose identity is stable across renders. Unlike state, changing `ref.current` does NOT trigger a re-render. Use it to hold DOM nodes, timer IDs, previous values, or any mutable value you don't want to cause re-renders. It persists for the component's lifetime and resets only on unmount/remount.\n\n```jsx\nconst inputRef = useRef(null);\nuseEffect(() => { inputRef.current?.focus(); }, []);\n<input ref={inputRef} />;\n```",tech:"react",displayNumber:114,sortKey:1086},{id:"react-1087",number:115,title:"How do you access a DOM node via a ref?",question:"How do you access a DOM node via a ref?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'Attach a ref object to a built-in element\'s `ref` attribute; React assigns the DOM node to `ref.current` after mount and sets it to `null` on unmount. You can then read/measure/manipulate the node in effects or handlers. This is the supported imperative escape hatch — prefer declarative rendering, but refs are fine for focus, media playback, measurements.\n\n```jsx\nconst videoRef = useRef(null);\nconst play = () => videoRef.current.play();\n<video ref={videoRef} src="a.mp4" />;\n```',tech:"react",displayNumber:115,sortKey:1087},{id:"react-1088",number:116,title:"How do you track a previous value with a ref?",question:"How do you track a previous value with a ref?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Store the value in a ref and update it after render (in an effect or during render with care). A common custom hook \`usePrevious\` saves the previous render's value. Use this when you need to compare "before vs after" without adding state that triggers extra renders.

\`\`\`jsx
function usePrevious(value) {
  const ref = useRef();
  useEffect(() => { ref.current = value; }); // runs after commit
  return ref.current;
}
const prev = usePrevious(count);
\`\`\``,tech:"react",displayNumber:116,sortKey:1088},{id:"react-1089",number:117,title:"What is `forwardRef` and when do you need it?",question:"What is `forwardRef` and when do you need it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Function components don't receive `ref` as a normal prop (it's special). `forwardRef` lets a component accept a ref from its parent and forward it to a child DOM node or component. You need it when building reusable wrappers around inputs, buttons, or libraries where the parent must directly access the underlying node. Class components accept `ref` natively, so only function components need `forwardRef`.\n\n```jsx\nconst FancyInput = forwardRef((props, ref) => (\n  <input ref={ref} className=\"fancy\" {...props} />\n));\n\nconst ref = useRef();\n<FancyInput ref={ref} />; // ref points to the <input>\n```",tech:"react",displayNumber:117,sortKey:1089},{id:"react-1090",number:118,title:"What is `useImperativeHandle`?",question:"What is `useImperativeHandle`?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"`useImperativeHandle(ref, createHandle, deps)` customizes the instance value that a parent receives via `ref` when using `forwardRef`. Instead of exposing the whole DOM node, you expose a limited API (e.g., `{ focus, reset }`). Pair it with `forwardRef`. Use sparingly — declarative props are usually better, but imperative handles are handy for focus management or animations.\n\n```jsx\nconst FancyInput = forwardRef((props, ref) => {\n  const inputRef = useRef();\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n    clear: () => { inputRef.current.value = ''; },\n  }), []);\n  return <input ref={inputRef} />;\n});\n```",tech:"react",displayNumber:118,sortKey:1090},{id:"react-1091",number:119,title:"What's the difference between a ref and state?",question:"What's the difference between a ref and state?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'State changes trigger re-renders and are immutable-ish (set via setter); refs are mutable via `.current`, do NOT trigger re-renders, and persist across renders. Use state for anything rendered in the UI; use refs for values you read/write imperatively (DOM nodes, timers, caches, "latest" values in callbacks). Don\'t use refs to store data that should be reflected in the view.\n\n```jsx\nconst [open, setOpen] = useState(false); // UI reacts\nconst timer = useRef();                  // no re-render on change\n```',tech:"react",displayNumber:119,sortKey:1091},{id:"react-1092",number:120,title:"How do you create and provide context?",question:"How do you create and provide context?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `createContext(defaultValue)` to make a context object. Wrap descendants in `<Context.Provider value={...}>`. Any component inside can read the value with `useContext`. The `defaultValue` is only used when there is no matching Provider above. Wrap the value in `useMemo` when it's an object to avoid unnecessary consumer re-renders.\n\n```jsx\nconst ThemeContext = createContext('light');\n\n<ThemeContext.Provider value=\"dark\">\n  <App />\n</ThemeContext.Provider>;\n```",tech:"react",displayNumber:120,sortKey:1092},{id:"react-1093",number:121,title:"How do you consume context with `useContext`?",question:"How do you consume context with `useContext`?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Call `useContext(MyContext)` inside a component to get the current value from the nearest Provider above it. It re-renders the component whenever the context value changes. You can consume multiple contexts. Note: `useContext` must be called at the top level of a component, not conditionally.\n\n```jsx\nfunction Toolbar() {\n  const theme = useContext(ThemeContext); // nearest Provider value\n  return <div className={theme}>...</div>;\n}\n```",tech:"react",displayNumber:121,sortKey:1093},{id:"react-1094",number:122,title:"Does a context value change re-render all consumers?",question:"Does a context value change re-render all consumers?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes — every component calling `useContext` for that context re-renders whenever the Provider's `value` changes (by `Object.is`). If you pass a new object/array each render, all consumers re-render even if their slice is unchanged. Mitigate by: memoizing the value with `useMemo`, splitting contexts by concern, or using external stores (`useSyncExternalStore`) for fine-grained updates.\n\n```jsx\n// Bad: new object each render -> all consumers re-render\n<Ctx.Provider value={{ user, theme }}>\n\n// Better: memoize\nconst value = useMemo(() => ({ user, theme }), [user, theme]);\n<Ctx.Provider value={value}>\n```",tech:"react",displayNumber:122,sortKey:1094},{id:"react-1095",number:123,title:"How do you avoid unnecessary context re-renders?",question:"How do you avoid unnecessary context re-renders?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Strategies: (1) Memoize the `value` with `useMemo` so identity is stable when contents are equal. (2) Split into multiple smaller contexts (e.g., `UserContext` vs `ThemeContext`) so a theme change doesn't re-render user consumers. (3) Separate the state setter from the value — provide the setter via a stable context and the value via another. (4) For high-frequency updates, use an external store instead of context.\n\n```jsx\nconst UserCtx = createContext(null);\nconst UserDispatch = createContext(null); // stable dispatch\n// split value (changes) from dispatch (stable) to limit re-renders\n```",tech:"react",displayNumber:123,sortKey:1095},{id:"react-1096",number:124,title:"Can you have a default value for context without a Provider?",question:"Can you have a default value for context without a Provider?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Yes. `createContext(defaultValue)` sets a fallback used when a component consumes the context but no Provider is above it in the tree. It's only a fallback — once a Provider exists, its `value` always wins, even if `undefined`. This is useful for testing components in isolation or for optional contexts.\n\n```jsx\nconst Ctx = createContext({ lang: 'en' });\n// No Provider -> component sees { lang: 'en' }\n```",tech:"react",displayNumber:124,sortKey:1096},{id:"react-1097",number:125,title:"How do you update context from a consumer?",question:"How do you update context from a consumer?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Context itself is just data; to let consumers update it, lift state to the Provider and pass the setter (or a dispatch) through context too. The provider holds `useState`/`useReducer` and supplies both value and an updater. Consumers call the updater, which changes the Provider\'s state, which re-renders all consumers. This is the standard "context + reducer" pattern.\n\n```jsx\nconst [user, setUser] = useState(null);\n<AuthContext.Provider value={{ user, setUser }}>\n// Consumer: const { setUser } = useContext(AuthContext);\n```',tech:"react",displayNumber:125,sortKey:1097},{id:"react-1098",number:126,title:"What is the difference between Context and Redux?",question:"What is the difference between Context and Redux?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Context is a built-in mechanism for passing values down without props; it re-renders all consumers on value change and has no built-in devtools, middleware, or time-travel. Redux is an external state container with a single store, reducers, middleware, selectors, and optimized subscriptions that avoid re-rendering unrelated components. For simple shared state, Context suffices; for complex, high-frequency, or multi-consumer app state, a store with fine-grained subscriptions scales better.\n\n```jsx\n// Context: re-renders consumers on any value change\n// Redux + useSelector: only re-renders when selected slice changes\n```",tech:"react",displayNumber:126,sortKey:1098},{id:"react-1099",number:127,title:"How do you provide multiple contexts cleanly?",question:"How do you provide multiple contexts cleanly?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Nest Providers, but deep nesting is ugly. You can compose them into a single \`AppProviders\` component that stacks them in a readable order, or use a context-splitting utility. Keep related contexts separate so updates don't cascade. A common pattern: a \`Providers\` wrapper component used once at the root.

\`\`\`jsx
function AppProviders({ children }) {
  return (
    <ThemeProvider>
      <AuthProvider>
        <I18nProvider>{children}</I18nProvider>
      </AuthProvider>
    </ThemeProvider>
  );
}
\`\`\``,tech:"react",displayNumber:127,sortKey:1099},{id:"react-1100",number:128,title:"Can you consume context conditionally or in loops?",question:"Can you consume context conditionally or in loops?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Hooks, including `useContext`, must be called unconditionally at the top level — never inside `if`, loops, or nested functions, or React's hook-order tracking breaks and you'll get errors. To vary behavior, always call `useContext` first, then branch on the resulting value. The \"don't call hooks in conditions\" rule is strict and non-negotiable.\n\n```jsx\nfunction C() {\n  const ctx = useContext(MyCtx); // always called\n  if (ctx.enabled) return <On />; // branch AFTER the hook\n  return <Off />;\n}\n```",tech:"react",displayNumber:128,sortKey:1100},{id:"react-1101",number:129,title:"What is `React.memo` and how does it work?",question:"What is `React.memo` and how does it work?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`React.memo(Component)` is a higher-order component that memoizes the rendered output, skipping re-renders when the component's props are shallowly equal (`Object.is` per prop) to the previous render. It's a performance optimization for components that re-render often with the same props. It does a shallow comparison; deeply nested objects still trigger re-renders unless their references are stable (via `useMemo`/`useCallback`). It does NOT help if the component uses context or internal state that changes.\n\n```jsx\nconst Row = React.memo(function Row({ item }) {\n  return <li>{item.name}</li>;\n});\n// Re-renders only when `item` reference changes\n```",tech:"react",displayNumber:129,sortKey:1101},{id:"react-1102",number:130,title:"What is `useMemo` and when should you use it?",question:"What is `useMemo` and when should you use it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useMemo(fn, deps)` memoizes the result of a computation, recomputing only when deps change. Use it for expensive calculations (sorting, filtering large arrays, heavy derivations) or to produce stable object/array references passed to memoized children / context. Don't use it for every trivial calculation — the overhead and complexity usually outweigh the gain. Its identity guarantee also helps avoid re-renders.\n\n```jsx\nconst sorted = useMemo(() => items.slice().sort(byName), [items]);\n// recomputes only when `items` changes\n```",tech:"react",displayNumber:130,sortKey:1102},{id:"react-1103",number:131,title:"What is `useCallback` and how is it different from `useMemo`?",question:"What is `useCallback` and how is it different from `useMemo`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`useCallback(fn, deps)` returns a memoized version of the function itself (stable identity across renders unless deps change). `useMemo` memoizes a value; `useCallback` memoizes a function — in fact `useCallback(fn, deps)` is roughly `useMemo(() => fn, deps)`. Use it to keep stable function identities so memoized children (via `React.memo`) don't re-render, and to keep stable deps in effects.\n\n```jsx\nconst handleClick = useCallback(() => doThing(id), [id]);\n// stable identity unless id changes -> safe for memoized children\n```",tech:"react",displayNumber:131,sortKey:1103},{id:"react-1104",number:132,title:"How do refs, memo, and callbacks interact to prevent re-renders?",question:"How do refs, memo, and callbacks interact to prevent re-renders?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"A memoized child (`React.memo`) re-renders only when its props change by shallow compare. If a parent passes a new inline function or new object each render, the child re-renders despite memoization. Wrap those functions in `useCallback` and objects in `useMemo` to keep references stable. Refs are naturally stable (no re-render on change) but don't trigger child updates. Combine: memo child + stable callbacks + stable value props = minimal re-renders.\n\n```jsx\nconst onSelect = useCallback(id => setSel(id), []);\nconst data = useMemo(() => ({ items }), [items]);\n<Row onSelect={onSelect} data={data} />; // memoized Row stays put\n```",tech:"react",displayNumber:132,sortKey:1104},{id:"react-1105",number:133,title:"Are `useMemo`/`useCallback` guarantees or optimizations?",question:"Are `useMemo`/`useCallback` guarantees or optimizations?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"They are optimizations, not semantic guarantees. React may discard memoized values (e.g., on low-memory situations or in future concurrent features) and recompute. Never rely on them for correctness — only for performance. If a computation must run exactly once or have a guaranteed identity, use `useState` lazy init or refs. Treat memoization as a hint.\n\n```jsx\n// WRONG: relying on useMemo for correctness\nconst list = useMemo(() => fetchList(), []); // may recompute\n// RIGHT: side effect in effect + state\n```",tech:"react",displayNumber:133,sortKey:1105},{id:"react-1106",number:134,title:"How do you profile and find unnecessary re-renders?",question:"How do you profile and find unnecessary re-renders?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Use the React DevTools "Profiler" to record renders and see which components re-rendered and why (it highlights commits and per-component wasted renders). The "Highlight updates" option flashes re-rendering components. For automated detection, the `why-did-you-render` library warns when a component re-renders with equal props. Always measure before optimizing — premature memoization adds complexity.\n\n```jsx\n// React DevTools Profiler -> Record -> inspect "re-rendered by"\n// why-did-you-render: patches components to log needless renders\n```',tech:"react",displayNumber:134,sortKey:1106},{id:"react-1107",number:135,title:"When should you NOT use `React.memo`?",question:"When should you NOT use `React.memo`?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Don't memoize when the component is cheap to render (the memo comparison costs more than re-rendering), when it always receives new props anyway (e.g., new inline objects), when it reads context that changes often (memo won't help), or at the very top of a tree that re-renders entirely. Memoization without a stable-prop situation just adds overhead. Measure first.\n\n```jsx\n// Pointless: parent always passes new object -> memo never skips\n<Row item={{ id, name }} />; // React.memo can't help here\n```",tech:"react",displayNumber:135,sortKey:1107},{id:"react-1108",number:136,title:"How do you create an error boundary?",question:"How do you create an error boundary?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Error boundaries are class components implementing \`static getDerivedStateFromError(error)\` (to render a fallback UI) and/or \`componentDidCatch(error, info)\` (for logging). They catch errors during rendering, in lifecycle methods, and in constructors of the whole tree below them — but NOT in event handlers, async code, or SSR. Function components cannot be error boundaries (no equivalent Hook), so you still need a class for this.

\`\`\`jsx
class ErrorBoundary extends React.Component {
  state = { hasError: false };
  static getDerivedStateFromError() { return { hasError: true }; }
  componentDidCatch(error, info) { logError(error, info); }
  render() {
    return this.state.hasError ? <Fallback /> : this.props.children;
  }
}
\`\`\``,tech:"react",displayNumber:136,sortKey:1108},{id:"react-1109",number:137,title:"What can't error boundaries catch?",question:"What can't error boundaries catch?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Error boundaries do NOT catch: errors in event handlers, asynchronous code (setTimeout, promises, async/await), server-side rendering, or errors thrown in the boundary itself. For those, use `try/catch` in event handlers/async functions, or error states. This is a common interview trap — boundaries only cover render/lifecycle/constructor phases of descendants.\n\n```jsx\n// Event handler error: handle manually, boundary won't catch\nconst onClick = () => {\n  try { risky(); } catch (e) { setError(e); }\n};\n```",tech:"react",displayNumber:137,sortKey:1109},{id:"react-1110",number:138,title:"How do you handle errors in event handlers or async code?",question:"How do you handle errors in event handlers or async code?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:'Use plain `try/catch` for synchronous/async logic and local error state to show a message. For async, catch promise rejections and set state (or dispatch an error action). If you want to "rethrow" into a boundary from async, you can store the error and re-throw during render so the nearest boundary catches it. This bridges async failures into the boundary system when desired.\n\n```jsx\nconst [err, setErr] = useState(null);\nuseEffect(() => {\n  load().catch(setErr);\n}, []);\nif (err) throw err; // optionally surface to error boundary\n```',tech:"react",displayNumber:138,sortKey:1110},{id:"react-1111",number:139,title:"What is `React.lazy` and how do you use it?",question:"What is `React.lazy` and how do you use it?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`React.lazy(() => import('./Heavy'))` lets you code-split a component: it's loaded only when first rendered, returning a Promise that resolves to a module with a default export component. Wrap it in `<Suspense fallback={...}>` to show a placeholder while loading. This reduces initial bundle size. The dynamic `import()` must resolve to a module whose default export is the component.\n\n```jsx\nconst Heavy = React.lazy(() => import('./Heavy'));\n\n<Suspense fallback={<Spinner />}>\n  <Heavy />\n</Suspense>;\n```",tech:"react",displayNumber:139,sortKey:1111},{id:"react-1112",number:140,title:"What is `Suspense` and what does `fallback` do?",question:"What is `Suspense` and what does `fallback` do?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:'`<Suspense fallback={...}>` shows the `fallback` UI while any component inside it is "suspended" — currently triggered by `React.lazy` code-splitting or data-fetching libraries that integrate with Suspense. When the suspended resource resolves, React swaps in the real content. You can nest Suspense boundaries for granular loading states. Suspense for data (not just lazy) is an evolving feature; the stable use today is with `React.lazy`.\n\n```jsx\n<Suspense fallback={<Loading />}>\n  <Profile />\n  <Posts />\n</Suspense>;\n```',tech:"react",displayNumber:140,sortKey:1112},{id:"react-1113",number:141,title:"Can you use `Suspense` for data fetching?",question:"Can you use `Suspense` for data fetching?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:"Yes, but it depends on the data library. Suspense for data is supported by libraries like Relay, React Query (with `useSuspenseQuery`), and `Suspense` integrations — your resource throws a promise that Suspense catches, then renders the fallback. The built-in `fetch` + Suspense isn't stable without a framework (Next.js App Router uses it). For plain `useEffect` fetching, Suspense does NOT apply; you'd use `React.lazy` or a suspense-enabled client.\n\n```jsx\n// With a suspense-enabled client:\nconst data = useSuspenseQuery(key); // throws promise while loading\n// Wrapped by <Suspense fallback={<Skeleton />}>\n```",tech:"react",displayNumber:141,sortKey:1113},{id:"react-1114",number:142,title:"How do error boundaries and Suspense work together?",question:"How do error boundaries and Suspense work together?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:'Place an error boundary OUTSIDE (above) the Suspense boundary so that both lazy-load failures and render errors are caught. A boundary catches errors thrown by a failed lazy import or a component that throws during render; Suspense handles the "still loading" state. Order matters: `<ErrorBoundary><Suspense><Lazy/></Suspense></ErrorBoundary>`. If the lazy import rejects (network error), the boundary shows the fallback, not Suspense.\n\n```jsx\n<ErrorBoundary fallback={<LoadFailed />}>\n  <Suspense fallback={<Spinner />}>\n    <LazyComponent />\n  </Suspense>\n</ErrorBoundary>\n```',tech:"react",displayNumber:142,sortKey:1114},{id:"react-1115",number:143,title:"What is Strict Mode and what does it do?",question:"What is Strict Mode and what does it do?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"`<React.StrictMode>` is a development-only wrapper that activates extra checks and warnings to surface unsafe patterns. It intentionally double-invokes certain functions (render, constructors, some effects' setup+cleanup+setup) to help you find impure code and missing cleanups. It renders components twice, logs deprecated APIs, and detects unsafe lifecycle usage. It has no effect in production — the double-invoke disappears.\n\n```jsx\nimport { StrictMode } from 'react';\n<StrictMode><App /></StrictMode>; // dev-only extra checks\n```",tech:"react",displayNumber:143,sortKey:1115},{id:"react-1116",number:144,title:"Why does Strict Mode double-invoke effects?",question:"Why does Strict Mode double-invoke effects?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`In development, Strict Mode mounts, unmounts, then remounts each component once to verify that effects clean up properly and can re-create state correctly. It runs setup → cleanup → setup for effects (and runs reducers/state initializers twice). This surfaces missing cleanup (leaks) and impure render logic. If your effect leaks without cleanup, Strict Mode will expose it. Production runs effects once.

\`\`\`jsx
useEffect(() => {
  const id = setInterval(tick, 1000);
  return () => clearInterval(id); // must clean up or Strict Mode leaks
}, []);
// dev: setup, cleanup, setup again
\`\`\``,tech:"react",displayNumber:144,sortKey:1116},{id:"react-1117",number:145,title:"How do you handle the double-render in Strict Mode?",question:"How do you handle the double-render in Strict Mode?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`You generally don't "handle" it — you make your code idempotent and side-effect-free in render. For effects, ensure cleanups fully undo setup so the doubled mount/unmount is harmless. Avoid module-level singletons mutated in effects without cleanup. If you must run something exactly once (e.g., analytics), guard with a ref, but first verify it's truly needed — most double-invoke issues reveal a real missing cleanup.

\`\`\`jsx
const done = useRef(false);
useEffect(() => {
  if (done.current) return; // guard one-time side effect
  done.current = true;
  initAnalytics();
}, []);
\`\`\``,tech:"react",displayNumber:145,sortKey:1117},{id:"react-1118",number:146,title:"What is the difference between development and production builds?",question:"What is the difference between development and production builds?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Development builds include prop-type checks, extra warnings, Strict Mode double-invocation, and detailed error messages — slower and larger. Production builds are minified, strip these dev-only checks, and run effects once. That's why behavior (especially Strict Mode effects) differs between `npm start` and the production bundle. Always test production-like behavior before shipping.\n\n```jsx\n// Production: NODE_ENV=production -> no dev warnings, effects run once\n// Dev: NODE_ENV=development -> Strict Mode, warnings, double effects\n```",tech:"react",displayNumber:146,sortKey:1118},{id:"react-1119",number:147,title:"How do you detect Strict Mode issues early?",question:"How do you detect Strict Mode issues early?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:`Keep Strict Mode ON in development (it's on by default in create-react-app / Vite templates). Watch the console for "double-invoke" leaking timers, duplicate subscriptions, or "state updates on unmounted component" warnings — these point to missing cleanups. Write effects that are reversible. Treat any leak exposed in dev as a real bug even if production hides the symptom.

\`\`\`jsx
// Symptom in Strict Mode: two console logs on mount -> effect not cleaned
useEffect(() => { console.log('sub'); return () => console.log('unsub'); }, []);
\`\`\``,tech:"react",displayNumber:147,sortKey:1119},{id:"react-1120",number:148,title:"Does Strict Mode affect state or context values?",question:"Does Strict Mode affect state or context values?",difficulty:"Intermediate",category:"State, Props & Lifecycle",answer:"Strict Mode double-invokes render functions and state initializers to check purity, but it does NOT change the actual committed state or context values the user sees — the second render result is what's used. It may call your `useState` initializer or `useReducer` reducer twice in dev to detect impurity, so those must be pure. The user-visible UI is still correct and single.\n\n```jsx\n// Reducer/init must be pure: double-invoked in dev\nconst [s] = useReducer(reducer, 0, init); // init may run twice (dev)\n```",tech:"react",displayNumber:148,sortKey:1120},{id:"react-1121",number:149,title:"How do you opt out of Strict Mode for a subtree?",question:"How do you opt out of Strict Mode for a subtree?",difficulty:"Basic",category:"State, Props & Lifecycle",answer:"Strict Mode is opt-in per tree. Simply don't wrap a subtree in `<React.StrictMode>`. If the whole app is wrapped, remove the wrapper or wrap only the parts you want checked. There's no per-component \"disable\" flag — it's all-or-nothing at the wrapper boundary. Most teams keep it on; you'd only remove it when a third-party lib misbehaves under double-invoke.\n\n```jsx\n// Remove outer StrictMode to opt a subtree out\n<App /> // no StrictMode wrapper = no double-invoke/dev checks\n```",tech:"react",displayNumber:149,sortKey:1121},{id:"react-1122",number:150,title:"Why might production behave differently than dev with effects?",question:"Why might production behave differently than dev with effects?",difficulty:"Advanced",category:"State, Props & Lifecycle",answer:`Two main reasons: (1) Strict Mode's double mount/cleanup only happens in dev, so a missing cleanup (leak, duplicate listener, double-subscribe) is hidden in production but exposed in dev. (2) Dev builds run extra validation. If production "works" but dev shows warnings, the dev warning usually indicates a latent bug that production merely tolerates. Always fix the root cause (proper cleanup, purity) rather than disabling Strict Mode.

\`\`\`jsx
// Bug only visible in dev Strict Mode:
useEffect(() => { window.addEventListener('resize', onResize); }, []);
// no cleanup -> duplicate listeners under Strict Mode; fix with cleanup
useEffect(() => {
  window.addEventListener('resize', onResize);
  return () => window.removeEventListener('resize', onResize);
}, []);
\`\`\``,tech:"react",displayNumber:150,sortKey:1122},{id:"react-1123",number:151,title:"Why must Hooks be called at the top level of a React component?",question:"Why must Hooks be called at the top level of a React component?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Hooks must always be called at the top level of a React function component, never inside loops, conditions, or nested functions. React relies on a linked list internally to track each Hook's state between renders. The order of Hook calls determines which state belongs to which Hook. If a Hook is called conditionally, the order changes between renders, and React assigns state to the wrong Hook, causing unpredictable bugs.

\`\`\`jsx
// VIOLATION: Hook called conditionally
function BadComponent({ isLoggedIn }) {
  if (isLoggedIn) {
    const [user, setUser] = useState(null); // WRONG: conditional hook call
  }
}

// CORRECT: Always call at top level
function GoodComponent({ isLoggedIn }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    if (isLoggedIn) {
      fetchUser().then(setUser);
    }
  }, [isLoggedIn]); // conditional logic INSIDE the hook, not around it
}
\`\`\``,tech:"react",displayNumber:151,sortKey:1123},{id:"react-1124",number:152,title:"Explain the internal mechanism React uses to track Hook calls.",question:"Explain the internal mechanism React uses to track Hook calls.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"React maintains a linked list of Hook state slots for each component fiber. When a component first renders, React allocates a slot for each Hook call in order. On subsequent renders, it walks the linked list sequentially, matching each `useState`, `useEffect`, etc. to its corresponding slot. This is why the order and count of Hook calls must remain stable. If a Hook is conditionally skipped, every Hook after it shifts up one slot, and React reads the wrong state values.\n\n```jsx\n// Pseudocode of React's internal Hook tracking\n// Render 1: useA -> useB -> useC  (slots: [A, B, C])\n// Render 2: useA ->      -> useC  (slots: [A, B, C])\n//           useA reads slot A (correct)\n//           useC reads slot B (WRONG - gets B's old value!)\n```",tech:"react",displayNumber:152,sortKey:1124},{id:"react-1125",number:153,title:"What is the relationship between custom Hooks and the Rules of Hooks?",question:"What is the relationship between custom Hooks and the Rules of Hooks?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Custom Hooks must follow the same Rules of Hooks as built-in Hooks. They must start with the word \`use\` (a naming convention so React's linter can enforce rules), and they can only call other Hooks at the top level of the custom Hook function. Custom Hooks are not special at runtime; they are plain JavaScript functions that call Hooks. The \`use\` prefix signals to developers and the linter that this function follows Hook rules.

\`\`\`jsx
function useWindowSize() {
  // This is fine because we're at the top level of the custom Hook
  const [size, setSize] = useState({ width: 0, height: 0 });

  useEffect(() => {
    const handleResize = () => setSize({
      width: window.innerWidth,
      height: window.innerHeight,
    });
    window.addEventListener('resize', handleResize);
    return () => window.removeEventListener('resize', handleResize);
  }, []);

  return size;
}
\`\`\``,tech:"react",displayNumber:153,sortKey:1125},{id:"react-1126",number:154,title:"What happens if you call Hooks conditionally and how does React ESLint plugin catch this?",question:"What happens if you call Hooks conditionally and how does React ESLint plugin catch this?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Calling Hooks conditionally causes the "Rules of Hooks" violation. The \`eslint-plugin-react-hooks\` provides the \`exhaustive-deps\` rule and the \`rules-of-hooks\` rule. The \`rules-of-hooks\` rule statically analyzes your code to detect Hook calls inside conditionals, loops, or nested functions. It reports errors at lint time before the bug manifests at runtime. This is one of the most valuable linting rules in the React ecosystem.

\`\`\`jsx
// ESLint will flag this with "React Hook 'useState' is called conditionally"
function Component({ showExtra }) {
  const [name, setName] = useState('');
  if (showExtra) {
    const [extra, setExtra] = useState(''); // Lint error!
  }
  const [age, setAge] = useState(0);
}
\`\`\``,tech:"react",displayNumber:154,sortKey:1126},{id:"react-1127",number:155,title:"Compare useState and useReducer: when would you choose one over the other?",question:"Compare useState and useReducer: when would you choose one over the other?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useState\` is ideal for simple, independent state values (strings, booleans, numbers). \`useReducer\` is better when you have complex state logic, multiple related state values, or when the next state depends on the previous one. \`useReducer\` centralizes state transitions in a reducer function, making them predictable and testable. It also avoids the problem of stale closures in setState callbacks since the reducer receives the current state as an argument.

\`\`\`jsx
// useState for simple state
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}

// useReducer for complex state with multiple sub-values
function todoReducer(state, action) {
  switch (action.type) {
    case 'add':
      return [...state, action.payload];
    case 'toggle':
      return state.map(todo =>
        todo.id === action.id ? { ...todo, done: !todo.done } : todo
      );
    case 'remove':
      return state.filter(todo => todo.id !== action.id);
    default:
      return state;
  }
}

function TodoList() {
  const [todos, dispatch] = useReducer(todoReducer, []);
  return (
    <>
      <button onClick={() => dispatch({ type: 'add', payload: { id: Date.now(), text: 'New', done: false } })}>
        Add Todo
      </button>
      {todos.map(todo => (
        <div key={todo.id} onClick={() => dispatch({ type: 'toggle', id: todo.id })}>
          {todo.text} - {todo.done ? 'Done' : 'Pending'}
        </div>
      ))}
    </>
  );
}
\`\`\``,tech:"react",displayNumber:155,sortKey:1127},{id:"react-1128",number:156,title:"Can useState receive a function as its initial value? When is this useful?",question:"Can useState receive a function as its initial value? When is this useful?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Yes. Passing a function to \`useState\` (called a "lazy initializer") causes React to call that function only on the initial render. This is useful when computing the initial state is expensive. Without the lazy initializer, the computation runs on every render even though the result is only used once. The function must be pure and take no arguments.

\`\`\`jsx
// EXPENSIVE: Computes on every render
function Component1() {
  const [state, setState] = useState(computeExpensiveValue());
}

// OPTIMAL: Computes only on first render
function Component2() {
  const [state, setState] = useState(() => computeExpensiveValue());
}

function computeExpensiveValue() {
  console.log('Computing...');
  return Array.from({ length: 1000000 }, (_, i) => i * 2);
}
\`\`\``,tech:"react",displayNumber:156,sortKey:1128},{id:"react-1129",number:157,title:"What is the difference between setState with a value vs. a function updater?",question:"What is the difference between setState with a value vs. a function updater?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When you call \`setState(newValue)\`, React schedules that exact value for the next state. When you call \`setState(prev => newValue)\`, React passes the most recent state to the updater function. The function form is essential when the new state depends on the previous state, especially inside closures like event handlers or \`useEffect\` callbacks that capture stale values. React batches multiple \`setState\` calls, so the function form ensures each update sees the latest state.

\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // WRONG: Stale closure - all three see count=0
  function handleClickThree() {
    setCount(count + 1);
    setCount(count + 1);
    setCount(count + 1);
    // Result: count = 1 (not 3!)
  }

  // CORRECT: Each updater sees the latest state
  function handleClickCorrect() {
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    // Result: count = 3
  }
}
\`\`\``,tech:"react",displayNumber:157,sortKey:1129},{id:"react-1130",number:158,title:"How does batching work with useState and what changed in React 18?",question:"How does batching work with useState and what changed in React 18?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`React batches state updates to avoid unnecessary re-renders. Before React 18, batching only occurred within React event handlers. Inside \`setTimeout\`, \`Promise.then\`, or native event handlers, each \`setState\` triggered a separate re-render. React 18 introduced "automatic batching" which batches all state updates regardless of where they originate (event handlers, timeouts, promises, native events). This is enabled by default with the new root API (\`createRoot\`).

\`\`\`jsx
// React 18: This causes only ONE re-render
function handleClick() {
  setTimeout(() => {
    setCount(c => c + 1);  // Batched
    setName('Alice');       // Batched
    setFlag(f => !f);       // Batched
    // Only 1 re-render in React 18 (was 3 in React 17)
  }, 1000);
}

// If you need a synchronous re-render in React 18:
import { flushSync } from 'react-dom';
function handleClick() {
  flushSync(() => {
    setCount(c => c + 1); // Re-render here
  });
  flushSync(() => {
    setName('Alice');      // Another re-render here
  });
}
\`\`\``,tech:"react",displayNumber:158,sortKey:1130},{id:"react-1131",number:159,title:"Explain useEffect dependency array behavior in detail.",question:"Explain useEffect dependency array behavior in detail.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The dependency array controls when \`useEffect\` runs. With no array, the effect runs after every render. With an empty array \`[]\`, it runs only once after the initial render. With specific dependencies \`[a, b]\`, it runs after any render where \`a\` or \`b\` has changed. React uses \`Object.is\` comparison to check if dependencies changed. If you omit a dependency, the effect may use stale values, leading to bugs. The \`exhaustive-deps\` ESLint rule helps catch missing dependencies.

\`\`\`jsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  // Runs only when userId changes
  useEffect(() => {
    let cancelled = false;
    fetchUser(userId).then(data => {
      if (!cancelled) setUser(data);
    });
    return () => { cancelled = true; };
  }, [userId]); // <-- dependency array

  // WARNING: Every render (no dependency array)
  useEffect(() => {
    document.title = \`User: \${user?.name}\`;
  });

  // Runs once on mount (empty dependency array)
  useEffect(() => {
    analytics.track('page_view');
  }, []);

  return <div>{user?.name}</div>;
}
\`\`\``,tech:"react",displayNumber:159,sortKey:1131},{id:"react-1132",number:160,title:"What is the purpose of cleanup functions in useEffect?",question:"What is the purpose of cleanup functions in useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Cleanup functions prevent memory leaks, remove event listeners, cancel subscriptions, and abort requests. React runs the cleanup function before the effect re-runs (on dependency change) and when the component unmounts. This lifecycle pattern ensures resources are properly released. Notable use cases include clearing timers, unsubscribing from WebSocket connections, removing DOM event listeners, and aborting fetch requests.

\`\`\`jsx
function Timer({ interval }) {
  useEffect(() => {
    const id = setInterval(() => {
      console.log('tick');
    }, interval);

    // Cleanup: removes the old interval before creating a new one
    return () => clearInterval(id);
  }, [interval]);

  useEffect(() => {
    const controller = new AbortController();

    fetch('/api/data', { signal: controller.signal })
      .then(res => res.json())
      .then(data => console.log(data));

    // Cleanup: abort in-flight request
    return () => controller.abort();
  }, []);
}
\`\`\``,tech:"react",displayNumber:160,sortKey:1132},{id:"react-1133",number:161,title:"What is the execution order of useEffect cleanup and setup?",question:"What is the execution order of useEffect cleanup and setup?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When dependencies change, React follows this order: (1) cleanup of the previous render's effect runs, then (2) the new effect runs. On unmount, only cleanup runs. On mount, only setup runs. Multiple effects in the same component run their cleanups first (in declaration order), then their setups (in declaration order). This means cleanup for all effects happens before any new effect setup.

\`\`\`jsx
useEffect(() => {
  console.log('Effect 1 setup');
  return () => console.log('Effect 1 cleanup');
}, [dep]);

useEffect(() => {
  console.log('Effect 2 setup');
  return () => console.log('Effect 2 cleanup');
}, [dep]);

// On dep change, output order:
// "Effect 1 cleanup" -> "Effect 2 cleanup" -> "Effect 1 setup" -> "Effect 2 setup"

// On unmount:
// "Effect 1 cleanup" -> "Effect 2 cleanup"
\`\`\``,tech:"react",displayNumber:161,sortKey:1133},{id:"react-1134",number:162,title:"Compare useEffect, useLayoutEffect, and useInsertionEffect.",question:"Compare useEffect, useLayoutEffect, and useInsertionEffect.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`- **useEffect**: Runs asynchronously after the browser paints. Use for most side effects (data fetching, subscriptions, logging). Does not block the visual update.
- **useLayoutEffect**: Runs synchronously after DOM mutations but before the browser paints. Use when you need to read or modify layout (measure DOM elements, position tooltips). Blocks painting, so keep it fast.
- **useInsertionEffect** (React 18): Runs synchronously after DOM mutations but before layout effects. Designed specifically for CSS-in-JS libraries to inject \`<style>\` tags without causing layout recalculations. Rarely used in application code.

\`\`\`jsx
// useInsertionEffect - for CSS-in-JS libraries
import { useInsertionEffect } from 'react';
function useStyledComponent(styles) {
  useInsertionEffect(() => {
    const style = document.createElement('style');
    style.textContent = styles;
    document.head.appendChild(style);
    return () => style.remove();
  }, [styles]);
}

// useLayoutEffect - for measuring DOM
function Tooltip({ text, targetRef }) {
  const tooltipRef = useRef(null);
  useLayoutEffect(() => {
    const tooltip = tooltipRef.current;
    const target = targetRef.current;
    if (tooltip && target) {
      const rect = target.getBoundingClientRect();
      tooltip.style.top = \`\${rect.bottom + 8}px\`;
      tooltip.style.left = \`\${rect.left}px\`;
    }
  }, [text, targetRef]);

  return <div ref={tooltipRef} className="tooltip">{text}</div>;
}

// useEffect - standard side effects
function ChatRoom({ roomId }) {
  useEffect(() => {
    const connection = createConnection(roomId);
    connection.connect();
    return () => connection.disconnect();
  }, [roomId]);
}
\`\`\``,tech:"react",displayNumber:162,sortKey:1134},{id:"react-1135",number:163,title:"When would you actually need useLayoutEffect over useEffect?",question:"When would you actually need useLayoutEffect over useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`You need \`useLayoutEffect\` when you must measure or mutate the DOM and need the result before the browser paints. Common scenarios: measuring element dimensions for tooltip positioning, preventing visual flicker when changing styles based on measurements, and synchronizing animations. If you use \`useEffect\` for these, users may see a brief flash of incorrect layout (FOUC) because the browser paints before your measurement-based style is applied.

\`\`\`jsx
function Popover({ targetRect, children }) {
  const popoverRef = useRef(null);
  const [position, setPosition] = useState({ top: 0, left: 0 });

  // Must use useLayoutEffect to avoid visual flicker
  useLayoutEffect(() => {
    const popover = popoverRef.current;
    if (!popover) return;

    const { height, width } = popover.getBoundingClientRect();
    setPosition({
      top: targetRect.bottom + 8,
      left: targetRect.left + (targetRect.width - width) / 2,
    });
  }, [targetRect]);

  return (
    <div
      ref={popoverRef}
      style={{ position: 'absolute', top: position.top, left: position.left }}
    >
      {children}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:163,sortKey:1135},{id:"react-1136",number:164,title:"What problem does useInsertionEffect solve and who should use it?",question:"What problem does useInsertionEffect solve and who should use it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useInsertionEffect\` solves a specific problem for CSS-in-JS libraries. When a library injects \`<style>\` tags at render time, it can cause layout thrashing because the browser must recalculate styles after insertion but before paint. \`useInsertionEffect\` runs after DOM mutations but before \`useLayoutEffect\`, letting libraries inject styles before layout measurements happen. Application developers rarely need this directly; it is primarily for library authors.

\`\`\`jsx
// Typical CSS-in-JS library usage (simplified)
import { useInsertionEffect, useRef } from 'react';

let counter = 0;
function useCSS(styleRules) {
  const styleRef = useRef(null);

  useInsertionEffect(() => {
    if (!styleRef.current) {
      const style = document.createElement('style');
      style.dataset.css = \`css-\${counter++}\`;
      style.textContent = styleRules;
      document.head.appendChild(style);
      styleRef.current = style;
    }
  }, [styleRules]);

  useInsertionEffect(() => {
    return () => {
      if (styleRef.current) {
        styleRef.current.remove();
        styleRef.current = null;
      }
    };
  }, []);
}
\`\`\``,tech:"react",displayNumber:164,sortKey:1136},{id:"react-1137",number:165,title:"Explain useMemo in depth and common misuse patterns.",question:"Explain useMemo in depth and common misuse patterns.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useMemo\` caches a computed value between renders. It only recalculates when its dependencies change, using \`Object.is\` comparison. Common misuse: memoizing simple calculations that are cheap (adds unnecessary overhead), memoizing primitives that React already handles efficiently, and over-memoizing everything "just in case." \`useMemo\` is valuable for expensive computations, creating stable references for child components, and preventing unnecessary re-renders of memoized children.

\`\`\`jsx
function DataGrid({ rows, filter }) {
  // GOOD: Expensive computation worth memoizing
  const filteredRows = useMemo(() => {
    return rows.filter(row =>
      row.name.toLowerCase().includes(filter.toLowerCase())
    ).sort((a, b) => a.name.localeCompare(b.name));
  }, [rows, filter]);

  // BAD: Unnecessary memoization (cheap operation)
  const greeting = useMemo(() => \`Hello, \${name}!\`, [name]); // Overkill

  // GOOD: Stable reference for child component
  const columnConfig = useMemo(() => ({
    sortable: true,
    resizable: true,
    columns: ['id', 'name', 'email'],
  }), []); // Stable across renders, prevents child re-renders

  return <Grid rows={filteredRows} config={columnConfig} />;
}
\`\`\``,tech:"react",displayNumber:165,sortKey:1137},{id:"react-1138",number:166,title:"Explain useCallback in depth and when it matters.",question:"Explain useCallback in depth and when it matters.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useCallback\` returns a memoized version of a callback function. It only creates a new function when its dependencies change. This is useful when passing callbacks to memoized child components (\`React.memo\`) to prevent unnecessary re-renders. However, \`useCallback\` adds overhead; if the child is not memoized or the callback is not passed as a prop, \`useCallback\` provides no benefit. Modern React patterns increasingly favor \`useReducer\` or state machines over \`useCallback\`.

\`\`\`jsx
// Without useCallback - child re-renders every time
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = () => console.log('clicked'); // New reference each render
  return <MemoizedChild onClick={handleClick} />; // Re-renders every time
}

// With useCallback - child skips re-renders
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []); // Stable reference
  return <MemoizedChild onClick={handleClick} />; // Only re-renders when deps change
}

const MemoizedChild = React.memo(({ onClick }) => {
  console.log('Child rendered');
  return <button onClick={onClick}>Click me</button>;
});
\`\`\``,tech:"react",displayNumber:166,sortKey:1138},{id:"react-1139",number:167,title:"When is useCallback NOT worth using?",question:"When is useCallback NOT worth using?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Skip \`useCallback\` when: (1) the child component is not wrapped in \`React.memo\` and the callback is not used as a dependency elsewhere, (2) the component re-renders rarely, (3) the component tree is shallow so re-render cost is negligible, or (4) you're memoizing a function that is recreated on every render due to unstable dependencies. Premature optimization with \`useCallback\` adds cognitive overhead and can make code harder to read without measurable performance benefit.

\`\`\`jsx
// NO NEED for useCallback here - SimpleComponent is not memoized
function Parent() {
  const [count, setCount] = useState(0);

  // This useCallback provides zero benefit
  const onClick = useCallback(() => {
    setCount(c => c + 1);
  }, []);

  return <SimpleComponent onClick={onClick} />;
}

function SimpleComponent({ onClick }) {
  return <button onClick={onClick}>Click</button>;
  // Re-renders every time Parent re-renders regardless of callback reference
}
\`\`\``,tech:"react",displayNumber:167,sortKey:1139},{id:"react-1140",number:168,title:"Compare useMemo and useCallback. How are they related internally?",question:"Compare useMemo and useCallback. How are they related internally?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"`useCallback` is essentially `useMemo` for functions. Internally, `useCallback(fn, deps)` is equivalent to `useMemo(() => fn, deps)`. Both use the same dependency comparison mechanism (`Object.is`). The key difference: `useMemo` returns the result of calling a function, while `useCallback` returns the function itself. Use `useMemo` for computed values and `useCallback` for stable function references.\n\n```jsx\n// These two are equivalent:\nconst memoizedFn = useCallback(() => doSomething(a, b), [a, b]);\nconst memoizedFn = useMemo(() => () => doSomething(a, b), [a, b]);\n\n// Practical difference:\n// useMemo - caches the RESULT of a computation\nconst expensiveResult = useMemo(() => computeExpensive(a, b), [a, b]);\n\n// useCallback - caches the FUNCTION itself\nconst handleSubmit = useCallback((data) => {\n  submitForm(data, a, b);\n}, [a, b]);\n```",tech:"react",displayNumber:168,sortKey:1140},{id:"react-1141",number:169,title:"How does React.memo interact with useMemo and useCallback?",question:"How does React.memo interact with useMemo and useCallback?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`React.memo\` performs a shallow comparison of a component's props to determine if it needs to re-render. Without \`useMemo\`/\`useCallback\`, new object references and function references created on each render cause \`React.memo\` to see "changed" props, defeating the purpose. \`useCallback\` provides stable function references, and \`useMemo\` provides stable object references, ensuring \`React.memo\` correctly skips re-renders when the actual values haven't changed.

\`\`\`jsx
function Parent() {
  const [count, setCount] = useState(0);
  const [text, setText] = useState('');

  // Without useMemo/useCallback, these create new references each render
  // defeating React.memo on ChildComponent
  const config = useMemo(() => ({ theme: 'dark' }), []);
  const handleClick = useCallback(() => {}, []);

  return (
    <>
      <ChildComponent config={config} onClick={handleClick} />
      <input value={text} onChange={e => setText(e.target.value)} />
    </>
  );
}

const ChildComponent = React.memo(({ config, onClick }) => {
  console.log('Child rendered');
  return <div>{config.theme}</div>;
});
// Without useMemo/useCallback on config/onClick, child re-renders on every Parent render
\`\`\``,tech:"react",displayNumber:169,sortKey:1141},{id:"react-1142",number:170,title:"What is useRef and how does it differ from a regular variable?",question:"What is useRef and how does it differ from a regular variable?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`\`useRef\` returns a mutable \`.current\` property that persists across renders without triggering re-renders when changed. Unlike a regular variable inside a component (which is re-created on each render), the \`.current\` value survives. Unlike \`useState\`, changing \`.current\` does not cause a re-render. This makes \`useRef\` ideal for storing DOM references, timers, previous values, and any mutable value that should persist but not drive rendering.

\`\`\`jsx
function Stopwatch() {
  const intervalRef = useRef(null);
  const [elapsed, setElapsed] = useState(0);

  const start = () => {
    intervalRef.current = setInterval(() => {
      setElapsed(prev => prev + 1);
    }, 1000);
  };

  const stop = () => {
    clearInterval(intervalRef.current); // Access persisted value
    intervalRef.current = null;
  };

  return (
    <div>
      <p>Elapsed: {elapsed}s</p>
      <button onClick={start}>Start</button>
      <button onClick={stop}>Stop</button>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:170,sortKey:1142},{id:"react-1143",number:171,title:"How do you access a DOM element using useRef?",question:"How do you access a DOM element using useRef?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Pass a \`useRef\` return value to a component's \`ref\` prop. After the component mounts, \`ref.current\` contains the underlying DOM node. This is the primary way to interact with DOM elements directly in React: measuring dimensions, focusing inputs, managing scroll positions, or integrating with non-React libraries. The ref is set after the component renders and DOM is committed.

\`\`\`jsx
function SearchBox() {
  const inputRef = useRef(null);

  useEffect(() => {
    // Auto-focus the input on mount
    inputRef.current.focus();
  }, []);

  const handleSubmit = (e) => {
    e.preventDefault();
    const value = inputRef.current.value;
    console.log('Searching for:', value);
  };

  return (
    <form onSubmit={handleSubmit}>
      <input ref={inputRef} type="search" placeholder="Search..." />
      <button type="submit">Search</button>
    </form>
  );
}
\`\`\``,tech:"react",displayNumber:171,sortKey:1143},{id:"react-1144",number:172,title:"How can useRef be used to store previous values?",question:"How can useRef be used to store previous values?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`A common pattern uses \`useRef\` to store the previous render's value by comparing it in a \`useLayoutEffect\`. Since ref updates don't trigger re-renders, it's the right tool for tracking "what was the value last time." This is useful for animations that transition between states, logging changes, or conditional logic based on previous vs. current values.

\`\`\`jsx
function usePrevious(value) {
  const ref = useRef();

  useLayoutEffect(() => {
    ref.current = value;
  }, [value]);

  return ref.current; // Returns the value from the PREVIOUS render
}

function Counter() {
  const [count, setCount] = useState(0);
  const prevCount = usePrevious(count);

  return (
    <div>
      <p>Now: {count}, Before: {prevCount}</p>
      <button onClick={() => setCount(c => c + 1)}>Increment</button>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:172,sortKey:1144},{id:"react-1145",number:173,title:"What is the difference between useRef(null) and useRef(undefined)?",question:"What is the difference between useRef(null) and useRef(undefined)?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Both create a ref with an initial value. The key difference is semantic: \`useRef(null)\` explicitly signals "no value yet" and is the conventional choice for DOM refs. \`useRef(undefined)\` works but is less idiomatic. Neither affects rendering or causes bugs. The \`.current\` property is mutable regardless of initial value. Use \`null\` for DOM refs and an appropriate initial value (or \`undefined\`) for general mutable variables.

\`\`\`jsx
// DOM ref - always use null
const divRef = useRef(null);

// Mutable variable with meaningful initial value
const timerId = useRef(0);

// Mutable variable without initial value
const previousValue = useRef(); // undefined by default

// Both work identically at runtime
useEffect(() => {
  if (divRef.current) { /* DOM node available */ }
  if (previousValue.current !== undefined) { /* has been set */ }
}, []);
\`\`\``,tech:"react",displayNumber:173,sortKey:1145},{id:"react-1146",number:174,title:"Explain useReducer's dispatch mechanism and how it differs from useState.",question:"Explain useReducer's dispatch mechanism and how it differs from useState.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useReducer\` dispatches action objects to a reducer function that returns the next state. Unlike \`useState\` where you call \`setState\` directly, \`useReducer\` separates "what happened" (action) from "how state changes" (reducer). This makes state transitions explicit, testable, and predictable. The reducer is a pure function receiving \`(state, action)\` and returning new state. \`dispatch\` is stable across renders (unlike setState, which is also stable, but the reducer pattern adds structure).

\`\`\`jsx
function formReducer(state, action) {
  switch (action.type) {
    case 'SET_FIELD':
      return { ...state, [action.field]: action.value };
    case 'RESET':
      return action.initialState;
    case 'SUBMIT_START':
      return { ...state, isSubmitting: true, error: null };
    case 'SUBMIT_SUCCESS':
      return { ...state, isSubmitting: false, isSubmitted: true };
    case 'SUBMIT_ERROR':
      return { ...state, isSubmitting: false, error: action.error };
    default:
      throw new Error(\`Unknown action: \${action.type}\`);
  }
}

function ContactForm() {
  const [state, dispatch] = useReducer(formReducer, {
    name: '', email: '', isSubmitting: false, isSubmitted: false, error: null,
  });

  return (
    <form onSubmit={async (e) => {
      e.preventDefault();
      dispatch({ type: 'SUBMIT_START' });
      try {
        await submitForm(state);
        dispatch({ type: 'SUBMIT_SUCCESS' });
      } catch (err) {
        dispatch({ type: 'SUBMIT_ERROR', error: err.message });
      }
    }}>
      <input value={state.name} onChange={e => dispatch({ type: 'SET_FIELD', field: 'name', value: e.target.value })} />
    </form>
  );
}
\`\`\``,tech:"react",displayNumber:174,sortKey:1146},{id:"react-1147",number:175,title:"When should you extract logic into a custom Hook?",question:"When should you extract logic into a custom Hook?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Extract a custom Hook when you have: (1) component logic that is duplicated across multiple components, (2) complex stateful logic you want to test or reason about independently, (3) side-effect logic that obscures the component's render output, or (4) when you want to follow the "single responsibility" principle at the component level. A custom Hook is just a function that uses other Hooks; the naming convention (\`use*\`) signals to React's tooling that it follows Hook rules.

\`\`\`jsx
// Custom Hook: encapsulates data fetching logic
function useFetch(url) {
  const [data, setData] = useState(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  useEffect(() => {
    let cancelled = false;
    setLoading(true);
    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error(\`HTTP \${res.status}\`);
        return res.json();
      })
      .then(data => { if (!cancelled) { setData(data); setLoading(false); } })
      .catch(err => { if (!cancelled) { setError(err.message); setLoading(false); } });
    return () => { cancelled = true; };
  }, [url]);

  return { data, loading, error };
}

// Usage: Component is clean and focused on rendering
function UserList() {
  const { data, loading, error } = useFetch('/api/users');

  if (loading) return <Spinner />;
  if (error) return <ErrorMessage message={error} />;
  return <ul>{data.map(u => <li key={u.id}>{u.name}</li>)}</ul>;
}
\`\`\``,tech:"react",displayNumber:175,sortKey:1147},{id:"react-1148",number:176,title:"What are the best practices for naming custom Hooks?",question:"What are the best practices for naming custom Hooks?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:"Always prefix custom Hooks with `use`. This is not just convention; it is required for React's linter to enforce the Rules of Hooks. Use descriptive, verb-based names that indicate what the Hook does: `useFetch`, `useLocalStorage`, `useDebounce`. Avoid generic names like `useHelper` or `useUtils`. A good custom Hook name describes the stateful behavior it encapsulates, not the implementation details.\n\n```jsx\n// GOOD: Clear purpose in the name\nfunction useDebounce(value, delay) { /* ... */ }\nfunction useMediaQuery(query) { /* ... */ }\nfunction useIntersectionObserver(options) { /* ... */ }\nfunction useLocalStorage(key, initialValue) { /* ... */ }\n\n// BAD: Vague names\nfunction useThing() { /* ... */ }\nfunction useHelper() { /* ... */ }\nfunction useData() { /* ... */ }\n```",tech:"react",displayNumber:176,sortKey:1148},{id:"react-1149",number:177,title:"Can custom Hooks return anything? What are the conventions?",question:"Can custom Hooks return anything? What are the conventions?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Custom Hooks can return any value: primitives, objects, arrays, functions, or combinations. Common conventions: return an array for simple state + setter pairs (mimicking \`useState\`), return an object when exposing multiple named values, and return a function when the Hook encapsulates an action. The caller decides the naming via destructuring. The Hook's return shape should be documented through consistent, predictable patterns.

\`\`\`jsx
// Array return - allows custom naming via destructuring (like useState)
function useToggle(initial = false) {
  const [value, setValue] = useState(initial);
  const toggle = useCallback(() => setValue(v => !v), []);
  return [value, toggle];
}
const [isOpen, toggleOpen] = useToggle(false);

// Object return - named properties
function useMediaQuery(query) {
  const [matches, setMatches] = useState(() => window.matchMedia(query).matches);
  useEffect(() => {
    const mql = window.matchMedia(query);
    const handler = (e) => setMatches(e.matches);
    mql.addEventListener('change', handler);
    return () => mql.removeEventListener('change', handler);
  }, [query]);
  return { matches, matchesExplicitly: matches };
}
const { matches: isMobile } = useMediaQuery('(max-width: 768px)');
\`\`\``,tech:"react",displayNumber:177,sortKey:1149},{id:"react-1150",number:178,title:"How do you handle side effects with cleanup in a custom Hook?",question:"How do you handle side effects with cleanup in a custom Hook?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks follow the same cleanup pattern as built-in Hooks. Return a cleanup function from \`useEffect\` inside the custom Hook. The cleanup runs when dependencies change or the component unmounts. Always handle cancellation, cleanup, and resource release in custom Hooks that manage subscriptions, timers, event listeners, or async operations to prevent memory leaks in consuming components.

\`\`\`jsx
function useEventListener(eventName, handler, element = window) {
  const savedHandler = useRef(handler);
  savedHandler.current = handler;

  useEffect(() => {
    const listener = (event) => savedHandler.current(event);
    element.addEventListener(eventName, listener);
    return () => element.removeEventListener(eventName, listener);
  }, [eventName, element]);
}

// Usage
function Component() {
  useEventListener('keydown', (e) => {
    if (e.key === 'Escape') closeModal();
  });
  return <div>Press Escape to close</div>;
}
\`\`\``,tech:"react",displayNumber:178,sortKey:1150},{id:"react-1151",number:179,title:"What are common pitfalls when creating custom Hooks?",question:"What are common pitfalls when creating custom Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Common pitfalls include: (1) calling Hooks conditionally inside the custom Hook, (2) not handling cleanup (memory leaks), (3) creating stable identity issues by returning new object/array references on every render, (4) tightly coupling the Hook to specific props rather than accepting flexible parameters, (5) forgetting that the Hook's state is per-component-instance (not shared), and (6) over-abstracting simple logic into a Hook when a utility function suffices.

\`\`\`jsx
// PITFALL: Returns new reference every render
function useUserData(userId) {
  const [user, setUser] = useState(null);
  // BAD: Creates new object every render
  return { user, setUser };
  // GOOD: Memoize the return value if consumers depend on reference equality
}

// PITFALL: Not handling cleanup
function useInterval(callback, delay) {
  useEffect(() => {
    // BAD: No cleanup - interval leaks
    setInterval(callback, delay);
  }, [callback, delay]);
}

// CORRECT: Cleanup handled
function useInterval(callback, delay) {
  const savedCallback = useRef(callback);
  savedCallback.current = callback;

  useEffect(() => {
    const id = setInterval(() => savedCallback.current(), delay);
    return () => clearInterval(id);
  }, [delay]);
}
\`\`\``,tech:"react",displayNumber:179,sortKey:1151},{id:"react-1152",number:180,title:"Explain useContext and its performance implications.",question:"Explain useContext and its performance implications.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useContext\` reads the nearest context value from the component tree. The problem: when a context value changes, every component that calls \`useContext\` for that context re-renders, even if the component only uses part of the value. This is the "context tearing" or "context consumer re-render" problem. Solutions include: splitting contexts by update frequency, memoizing context values in the provider, and using composition to reduce context scope.

\`\`\`jsx
// PROBLEM: All consumers re-render when ANY value changes
const AppContext = React.createContext();

function AppProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const [user, setUser] = useState(null);
  const [locale, setLocale] = useState('en');

  // Every context consumer re-renders when any value changes
  return (
    <AppContext.Provider value={{ theme, setTheme, user, setUser, locale, setLocale }}>
      {children}
    </AppContext.Provider>
  );
}

// BETTER: Split into separate contexts
const ThemeContext = React.createContext();
const UserContext = React.createContext();
const LocaleContext = React.createContext();

function AppProvider({ children }) {
  return (
    <ThemeContext.Provider value={useMemo(() => ({ theme, setTheme }), [theme])}>
      <UserContext.Provider value={useMemo(() => ({ user, setUser }), [user])}>
        <LocaleContext.Provider value={useMemo(() => ({ locale, setLocale }), [locale])}>
          {children}
        </LocaleContext.Provider>
      </UserContext.Provider>
    </ThemeContext.Provider>
  );
}
\`\`\``,tech:"react",displayNumber:180,sortKey:1152},{id:"react-1153",number:181,title:"How can you optimize context performance in React?",question:"How can you optimize context performance in React?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Several strategies: (1) Split contexts by update frequency so theme changes don't re-render user-dependent components. (2) Memoize the context value in the provider using \`useMemo\` to prevent unnecessary re-renders. (3) Move state down and use composition to limit how many components need context. (4) Use state management libraries (Zustand, Jotai) that provide fine-grained subscriptions. (5) Use the "selector" pattern with \`useSyncExternalStore\` or libraries like \`use-context-selector\`.

\`\`\`jsx
// Strategy 1: Memoized context value
function ThemeProvider({ children }) {
  const [theme, setTheme] = useState('light');
  const value = useMemo(() => ({ theme, setTheme }), [theme]);
  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

// Strategy 2: Split by update frequency
// ThemeContext (rarely changes) vs RealTimeContext (changes frequently)

// Strategy 3: Move state down
function App() {
  return (
    <Header /> {/* Does not need theme context */}
    <main>
      <ThemeProvider>
        <Dashboard /> {/* Needs theme context - scoped here */}
      </ThemeProvider>
    </main>
  );
}
\`\`\``,tech:"react",displayNumber:181,sortKey:1153},{id:"react-1154",number:182,title:"What is the Context Provider pattern and how does it relate to useContext?",question:"What is the Context Provider pattern and how does it relate to useContext?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The Provider pattern wraps a subtree of components with a context provider that supplies a value. Any descendant component can access that value via \`useContext\`. The Provider's value prop is what consumers receive. Providers can be nested, and inner providers override outer ones for the same context. This creates a natural dependency injection system in React without prop drilling.

\`\`\`jsx
const AuthContext = React.createContext(null);

function AuthProvider({ children }) {
  const [user, setUser] = useState(null);

  const login = useCallback(async (credentials) => {
    const userData = await api.login(credentials);
    setUser(userData);
  }, []);

  const logout = useCallback(() => setUser(null), []);

  const value = useMemo(() => ({ user, login, logout }), [user, login, logout]);

  return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>;
}

// Nested override example
function AdminSection() {
  return (
    <AuthProvider> {/* Overrides parent AuthContext */}
      <AdminDashboard />
    </AuthProvider>
  );
}
\`\`\``,tech:"react",displayNumber:182,sortKey:1154},{id:"react-1155",number:183,title:"Explain useImperativeHandle and its use cases.",question:"Explain useImperativeHandle and its use cases.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useImperativeHandle\` customizes the instance value exposed when a parent component uses \`ref\` to access a child component. Instead of exposing the entire DOM node, you expose only specific methods and properties. This follows the principle of minimal exposure and is useful for components that need to provide a programmatic API: form validation, focus management, animation controls. Always pair with \`forwardRef\`.

\`\`\`jsx
const FancyInput = forwardRef(function FancyInput({ placeholder }, ref) {
  const inputRef = useRef(null);
  const [value, setValue] = useState('');

  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus(),
    clear: () => { setValue(''); inputRef.current.value = ''; },
    getValue: () => value,
  }), [value]); // Recreate when value changes

  return (
    <input
      ref={inputRef}
      value={value}
      onChange={e => setValue(e.target.value)}
      placeholder={placeholder}
    />
  );
});

// Parent component
function Parent() {
  const inputRef = useRef();

  return (
    <>
      <FancyInput ref={inputRef} placeholder="Type here" />
      <button onClick={() => inputRef.current.focus()}>Focus</button>
      <button onClick={() => inputRef.current.clear()}>Clear</button>
    </>
  );
}
\`\`\``,tech:"react",displayNumber:183,sortKey:1155},{id:"react-1156",number:184,title:"How does forwardRef work and why is it needed with useImperativeHandle?",question:"How does forwardRef work and why is it needed with useImperativeHandle?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:"By default, function components don't receive `ref` as a prop. `forwardRef` creates a component that accepts a `ref` parameter and forwards it to a child element. `useImperativeHandle` needs access to the ref to customize what `.current` resolves to. Without `forwardRef`, you can't pass the ref into the component for `useImperativeHandle` to intercept. Note: React 19 eliminates the need for `forwardRef` as `ref` becomes a regular prop.\n\n```jsx\n// React 18 pattern: forwardRef required\nconst TextInput = forwardRef(function TextInput(props, ref) {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n  }));\n\n  return <input ref={inputRef} {...props} />;\n});\n\n// React 19 pattern: ref is a regular prop\nfunction TextInput({ ref, ...props }) {\n  const inputRef = useRef(null);\n\n  useImperativeHandle(ref, () => ({\n    focus: () => inputRef.current.focus(),\n  }));\n\n  return <input ref={inputRef} {...props} />;\n}\n```",tech:"react",displayNumber:184,sortKey:1156},{id:"react-1157",number:185,title:"What is useId and when should you use it?",question:"What is useId and when should you use it?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:'`useId` generates a unique, stable ID that works consistently between server and client rendering (hydration-safe). It solves the problem of generating unique IDs for accessibility attributes (`htmlFor`, `aria-labelledby`, `aria-describedby`) and form elements. Before `useId`, developers used random IDs or libraries, which caused hydration mismatches in SSR. `useId` returns the same ID on both server and client.\n\n```jsx\nfunction FormField({ label }) {\n  const id = useId(); // Unique per component instance, consistent SSR/CSR\n\n  return (\n    <div>\n      <label htmlFor={id}>{label}</label>\n      <input id={id} type="text" aria-describedby={`${id}-help`} />\n      <span id={`${id}-help`}>Enter your {label.toLowerCase()}</span>\n    </div>\n  );\n}\n\n// Generates IDs like ":r1:", ":r2:", etc.\n// Same ID on server and client for hydration safety\n```',tech:"react",displayNumber:185,sortKey:1157},{id:"react-1158",number:186,title:"What is the difference between useId and a random ID generator?",question:"What is the difference between useId and a random ID generator?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useId\` produces the same ID on server and client, which is essential for hydration. Random generators (\`Math.random()\`, \`uuid\`) produce different values on server vs. client, causing hydration mismatches that React warns about and can lead to subtle bugs. \`useId\` is also React-aware: it generates IDs that don't collide with other \`useId\` calls in the same render, even across different components.

\`\`\`jsx
// PROBLEM: Random IDs cause hydration mismatch
function BadField() {
  const id = Math.random().toString(36).substr(2, 9); // Different on server vs client
  return <input id={id} />;
}

// CORRECT: useId is hydration-safe
function GoodField() {
  const id = useId(); // Same value on server and client
  return <input id={id} />;
}

// Can generate multiple related IDs
function GoodField() {
  const id = useId();
  return (
    <>
      <input id={id} aria-describedby={\`\${id}-error\`} />
      <span id={\`\${id}-error\`}>Error message</span>
    </>
  );
}
\`\`\``,tech:"react",displayNumber:186,sortKey:1158},{id:"react-1159",number:187,title:"Can useId be used for list keys? Why or why not?",question:"Can useId be used for list keys? Why or why not?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`No. \`useId\` should not be used for list keys. Keys should identify list items based on their data (like a database ID), not their position or a generated ID. \`useId\` generates the same ID for the same component across renders, but it doesn't represent item identity. Using it as a key would cause React to reuse component instances incorrectly when list items reorder, are added, or removed.

\`\`\`jsx
// WRONG: useId for keys
function BadList({ items }) {
  return items.map(item => {
    const id = useId(); // VIOLATION: Hook called in a loop!
    return <li key={id}>{item.name}</li>;
  });
}

// CORRECT: Use data-based keys
function GoodList({ items }) {
  return items.map(item => (
    <li key={item.id}>{item.name}</li> // Use the item's own ID
  ));
}
\`\`\``,tech:"react",displayNumber:187,sortKey:1159},{id:"react-1160",number:188,title:"Explain useTransition and its benefits for UX.",question:"Explain useTransition and its benefits for UX.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useTransition\` lets you mark state updates as non-urgent transitions, keeping the UI responsive during expensive re-renders. It returns a \`startTransition\` function and an \`isPending\` boolean. Inside \`startTransition\`, React can interrupt the update to handle urgent interactions (typing, clicking). This prevents janky UI when switching between expensive views or filtering large lists.

\`\`\`jsx
function SearchResults() {
  const [query, setQuery] = useState('');
  const [results, setResults] = useState([]);
  const [isPending, startTransition] = useTransition();

  const handleSearch = (e) => {
    const value = e.target.value;
    setQuery(value); // Urgent: update input immediately

    startTransition(() => {
      setResults(filterLargeList(value)); // Non-urgent: can be interrupted
    });
  };

  return (
    <div>
      <input value={query} onChange={handleSearch} />
      {isPending && <Spinner />}
      <ResultList results={results} />
    </div>
  );
}

function filterLargeList(query) {
  // Expensive computation - won't block input
  return hugeList.filter(item => item.name.includes(query));
}
\`\`\``,tech:"react",displayNumber:188,sortKey:1160},{id:"react-1161",number:189,title:"Explain useDeferredValue and how it differs from useTransition.",question:"Explain useDeferredValue and how it differs from useTransition.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useDeferredValue\` defers updating a derived value, showing a stale version while the new value is being computed. Unlike \`useTransition\` (which wraps a state update), \`useDeferredValue\` takes a value and returns a deferred version of it. It is useful when you can't control the state update (e.g., a prop from a parent). \`useTransition\` requires a synchronous callback; \`useDeferredValue\` works with any value source.

\`\`\`jsx
// useDeferredValue: defers a derived value
function SearchPage({ query }) {
  const deferredQuery = useDeferredValue(query);
  const isStale = query !== deferredQuery;

  return (
    <div style={{ opacity: isStale ? 0.5 : 1 }}>
      <ResultList query={deferredQuery} />
    </div>
  );
}

// useTransition: wraps a state update
function TabContainer() {
  const [tab, setTab] = useState('home');
  const [isPending, startTransition] = useTransition();

  const selectTab = (nextTab) => {
    startTransition(() => {
      setTab(nextTab);
    });
  };

  return (
    <div>
      <TabBar onSelect={selectTab} />
      <div style={{ opacity: isPending ? 0.7 : 1 }}>
        <TabContent tab={tab} />
      </div>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:189,sortKey:1161},{id:"react-1162",number:190,title:"What are the prerequisites for using useTransition and useDeferredValue?",question:"What are the prerequisites for using useTransition and useDeferredValue?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Both features require React 18+ and the concurrent rendering mode, which is enabled by default with \`createRoot\` (the new client root API). If you're using the legacy \`ReactDOM.render()\`, concurrent features are not available. Both work best with components that have expensive rendering paths. They are not magic performance boosters; they only help when React can interrupt and prioritize rendering work.

\`\`\`jsx
// Old way: No concurrent features
import ReactDOM from 'react-dom';
ReactDOM.render(<App />, document.getElementById('root'));

// New way: Concurrent features enabled
import { createRoot } from 'react-dom/client';
const root = createRoot(document.getElementById('root'));
root.render(<App />);

// Now useTransition and useDeferredValue work
function App() {
  const [isPending, startTransition] = useTransition();
  const deferredValue = useDeferredValue(someValue);
  return <div>{/* ... */}</div>;
}
\`\`\``,tech:"react",displayNumber:190,sortKey:1162},{id:"react-1163",number:191,title:"How does batching work differently inside startTransition?",question:"How does batching work differently inside startTransition?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Inside \`startTransition\`, all state updates are batched into a single render, and the transition can be interrupted by urgent updates. This is different from React 18's automatic batching, which batches updates but doesn't provide interruption semantics. Transitions are deprioritized, so typing or clicking handlers can update the UI immediately while the transition processes in the background. This is what makes the UI feel responsive during expensive updates.

\`\`\`jsx
function Tabs() {
  const [tab, setTab] = useState('home');
  const [isPending, startTransition] = useTransition();

  const onTabSelect = (nextTab) => {
    startTransition(() => {
      // These updates are batched and deprioritized
      setTab(nextTab);
      setFilter(null);
      setSortBy('date');
    });
    // Urgent updates (like hover effects) still process immediately
  };

  return (
    <div>
      <TabBar onSelect={onTabSelect} />
      {isPending ? <LoadingSkeleton /> : <TabContent tab={tab} />}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:191,sortKey:1163},{id:"react-1164",number:192,title:"Can you nest multiple useTransition calls?",question:"Can you nest multiple useTransition calls?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Yes, you can call \`useTransition\` in multiple components, and each creates its own independent transition. Each component's \`isPending\` tracks only its own transition's state. However, nested transitions interact: an outer transition can be interrupted by an inner urgent update. This is fine and is by design; transitions form a priority hierarchy.

\`\`\`jsx
function Parent() {
  const [isPending: parentPending, startTransition: startParent] = useTransition();

  return (
    <div>
      {parentPending && <GlobalSpinner />}
      <ChildA />
      <ChildB />
    </div>
  );
}

function ChildA() {
  const [isPending: childPending, startTransition: startChild] = useTransition();

  return (
    <section style={{ opacity: childPending ? 0.5 : 1 }}>
      <button onClick={() => startChild(() => updateDataA())}>
        Update A
      </button>
    </section>
  );
}
\`\`\``,tech:"react",displayNumber:192,sortKey:1164},{id:"react-1165",number:193,title:"Explain the concept of Suspense-compatible Hooks.",question:"Explain the concept of Suspense-compatible Hooks.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's \`use()\` hook (React 19) reads a resource (Promise or Context) and can suspend rendering while waiting. This integrates with Suspense boundaries, enabling declarative loading states. Unlike \`useEffect\` + \`useState\` for data fetching, \`use()\` suspends the component tree at the Suspense boundary level, enabling streaming and selective hydration. This is the future of data fetching in React.

\`\`\`jsx
// React 19: use() for data fetching with Suspense
function UserProfile({ userId }) {
  const user = use(fetchUser(userId)); // Suspends until resolved
  return <div>{user.name}</div>;
}

function App() {
  return (
    <Suspense fallback={<Spinner />}>
      <UserProfile userId={1} />
    </Suspense>
  );
}

// The entire UserProfile tree is replaced with Spinner
// until fetchUser resolves - no loading states inside component
\`\`\``,tech:"react",displayNumber:193,sortKey:1165},{id:"react-1166",number:194,title:"How does useSyncExternalStore solve the external store problem?",question:"How does useSyncExternalStore solve the external store problem?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useSyncExternalStore\` subscribes to an external store and ensures the UI stays consistent with the store's state. It solves tearing issues that occurred in concurrent mode when different components read different versions of an external store during a concurrent render. The three arguments are: \`subscribe\` (function to listen for changes), \`getSnapshot\` (function to read current state), and \`getServerSnapshot\` (for SSR).

\`\`\`jsx
function useWindowSize() {
  return useSyncExternalStore(
    // Subscribe
    (callback) => {
      window.addEventListener('resize', callback);
      return () => window.removeEventListener('resize', callback);
    },
    // Get current snapshot
    () => ({ width: window.innerWidth, height: window.innerHeight }),
    // Server snapshot
    () => ({ width: 0, height: 0 }),
  );
}

// Using a store like Zustand/Redux
function useStore(selector) {
  return useSyncExternalStore(
    store.subscribe,
    () => selector(store.getState()),
  );
}
\`\`\``,tech:"react",displayNumber:194,sortKey:1166},{id:"react-1167",number:195,title:"What is the difference between useEffect and useLayoutEffect in terms of timing?",question:"What is the difference between useEffect and useLayoutEffect in terms of timing?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The timing difference is critical. \`useLayoutEffect\` fires synchronously after all DOM mutations but before the browser paints. \`useEffect\` fires asynchronously after the browser paints. This means \`useLayoutEffect\` can read/modify DOM without causing a visible flash, while \`useEffect\` might show a brief visual artifact if you're measuring or styling based on DOM state. Use \`useLayoutEffect\` sparingly since it blocks painting.

\`\`\`jsx
// useLayoutEffect: Blocks paint
function Measure() {
  const [height, setHeight] = useState(0);
  const ref = useRef(null);

  useLayoutEffect(() => {
    // This runs BEFORE the browser paints
    // User never sees height=0
    setHeight(ref.current.getBoundingClientRect().height);
  }, []);

  return <div ref={ref}>Measured content: {height}px</div>;
}

// useEffect: After paint
function MeasureAfterPaint() {
  const [height, setHeight] = useState(0);
  const ref = useRef(null);

  useEffect(() => {
    // User may briefly see height=0 before this runs
    setHeight(ref.current.getBoundingClientRect().height);
  }, []);

  return <div ref={ref}>Measured content: {height}px</div>;
}
\`\`\``,tech:"react",displayNumber:195,sortKey:1167},{id:"react-1168",number:196,title:'What is the "stale closure" problem and how do Hooks relate to it?',question:'What is the "stale closure" problem and how do Hooks relate to it?',difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A stale closure occurs when a function captures outdated variable values from a previous render. In React, event handlers and effects capture the state and props from the render when they were created. If the component re-renders with new state but the handler still references old state, you get a stale closure bug. This is common with \`setTimeout\`, \`setInterval\`, and async callbacks that reference state.

\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // STALE CLOSURE: count is always 0
  useEffect(() => {
    const id = setInterval(() => {
      console.log(count); // Always logs 0!
      setCount(count + 1); // Always sets to 1!
    }, 1000);
    return () => clearInterval(id);
  }, []); // Empty deps: effect never re-creates

  // FIXED: Use functional update
  useEffect(() => {
    const id = setInterval(() => {
      setCount(prev => prev + 1); // Always uses latest state
    }, 1000);
    return () => clearInterval(id);
  }, []);

  return <p>{count}</p>;
}
\`\`\``,tech:"react",displayNumber:196,sortKey:1168},{id:"react-1169",number:197,title:"How do you debug stale closure issues?",question:"How do you debug stale closure issues?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Debugging stale closures: (1) Add the dependency to the dependency array so the effect re-creates with fresh values, (2) Use functional state updates (\`setState(prev => ...)\`), (3) Use a ref to store the latest value (\`useRef\`), (4) Use the React DevTools Profiler to see how often effects re-run, (5) The ESLint \`exhaustive-deps\` rule catches many stale closure issues at lint time.

\`\`\`jsx
function SearchBox({ onSearch }) {
  const [query, setQuery] = useState('');

  // Solution 1: Add dependency
  useEffect(() => {
    const timer = setTimeout(() => {
      onSearch(query); // Fresh query
    }, 300);
    return () => clearTimeout(timer);
  }, [query, onSearch]); // Re-create when query or onSearch changes

  // Solution 2: Use ref for latest value
  const latestQuery = useRef(query);
  latestQuery.current = query;

  useEffect(() => {
    const timer = setTimeout(() => {
      onSearch(latestQuery.current); // Always current
    }, 300);
    return () => clearTimeout(timer);
  }, [onSearch]); // Only re-create when onSearch changes

  return <input value={query} onChange={e => setQuery(e.target.value)} />;
}
\`\`\``,tech:"react",displayNumber:197,sortKey:1169},{id:"react-1170",number:198,title:"Explain the dependency array comparison mechanism (Object.is).",question:"Explain the dependency array comparison mechanism (Object.is).",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`React uses \`Object.is\` to compare each dependency value with its previous value. \`Object.is\` behaves like \`===\` except for \`NaN\` (they are equal) and \`+0 !== -0\`. This means: primitives are compared by value, but objects and arrays are compared by reference. A new \`{}\` or \`[]\` created in the parent will always be "different," causing the effect to re-run. This is why you should not create objects or arrays in the dependency array or as inline values passed to \`useMemo\`/\`useCallback\`.

\`\`\`jsx
function Component({ config }) {
  // Object.is({ theme: 'dark' }, { theme: 'dark' }) === false
  // New object created every render = effect always re-runs

  // BAD: Inline object in deps
  useEffect(() => {
    applyTheme(config);
  }, [{ theme: config.theme }]); // New array every render!

  // GOOD: Use primitive values
  useEffect(() => {
    applyTheme(config);
  }, [config.theme]); // String comparison, stable

  // GOOD: Memoize objects in parent
  // Parent:
  const config = useMemo(() => ({ theme: theme }), [theme]);
  useEffect(() => {
    applyTheme(config);
  }, [config]); // Reference comparison, stable when theme unchanged
}
\`\`\``,tech:"react",displayNumber:198,sortKey:1170},{id:"react-1171",number:199,title:"What are the best practices for managing dependency arrays?",question:"What are the best practices for managing dependency arrays?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Best practices: (1) Always include the \`exhaustive-deps\` ESLint rule, (2) Use primitive values as dependencies when possible, (3) Memoize objects and functions that appear in dependency arrays, (4) Don't suppress lint warnings with \`// eslint-disable\` unless absolutely necessary, (5) If an effect needs to run only once, use empty \`[]\` but ensure the effect doesn't depend on mutable values, (6) Split complex effects so each has minimal, accurate dependencies.

\`\`\`jsx
// GOOD: Minimal, correct dependencies
function ChatRoom({ roomId, theme }) {
  useEffect(() => {
    const conn = createConnection(roomId);
    conn.join();
    return () => conn.leave();
  }, [roomId]); // Only roomId matters for connection

  useEffect(() => {
    document.body.className = theme;
  }, [theme]); // Only theme matters for styling
}

// BAD: Over-broad dependencies
useEffect(() => {
  doSomething(roomId, theme);
}, [roomId, theme]); // Effect re-runs on theme change even though roomId logic is unaffected
// Better: Split into two effects
\`\`\``,tech:"react",displayNumber:199,sortKey:1171},{id:"react-1172",number:200,title:"What is the useRef pattern for avoiding stale closures?",question:"What is the useRef pattern for avoiding stale closures?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`When you need the latest value of a prop or state inside an effect or callback without re-creating the effect, store it in a ref. The ref's \`.current\` always reflects the latest value because refs are mutable objects, not captured in closures. This breaks the stale closure pattern while keeping the effect's identity stable.

\`\`\`jsx
function useInterval(callback, delay) {
  const savedCallback = useRef(callback);

  // Always keep the ref up to date
  useEffect(() => {
    savedCallback.current = callback;
  });

  // Stable effect: only re-creates when delay changes
  useEffect(() => {
    const tick = () => savedCallback.current();
    const id = setInterval(tick, delay);
    return () => clearInterval(id);
  }, [delay]);
}

// Usage
function Counter() {
  const [count, setCount] = useState(0);
  useInterval(() => setCount(c => c + 1), 1000);
  return <h1>{count}</h1>;
}
\`\`\``,tech:"react",displayNumber:200,sortKey:1172},{id:"react-1173",number:201,title:"How do you create a custom Hook that works with both SSR and client rendering?",question:"How do you create a custom Hook that works with both SSR and client rendering?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`For SSR compatibility, your custom Hook must handle the case where browser APIs are unavailable. Check \`typeof window !== 'undefined'\` before accessing browser APIs, provide server-safe defaults, and use \`useSyncExternalStore\`'s \`getServerSnapshot\` parameter for stores. Hooks that depend on \`window\`, \`document\`, or \`localStorage\` need SSR guards.

\`\`\`jsx
function useMediaQuery(query) {
  const getSnapshot = () => window.matchMedia(query).matches;

  const getServerSnapshot = () => false; // Default for SSR

  const subscribe = (callback) => {
    const mql = window.matchMedia(query);
    mql.addEventListener('change', callback);
    return () => mql.removeEventListener('change', callback);
  };

  return useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);
}

function useLocalStorage(key, initialValue) {
  const [storedValue, setStoredValue] = useState(() => {
    if (typeof window === 'undefined') return initialValue; // SSR guard
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch {
      return initialValue;
    }
  });

  const setValue = (value) => {
    const valueToStore = value instanceof Function ? value(storedValue) : value;
    setStoredValue(valueToStore);
    if (typeof window !== 'undefined') {
      window.localStorage.setItem(key, JSON.stringify(valueToStore));
    }
  };

  return [storedValue, setValue];
}
\`\`\``,tech:"react",displayNumber:201,sortKey:1173},{id:"react-1174",number:202,title:"Explain how React.memo, useMemo, and useCallback work together for performance optimization.",question:"Explain how React.memo, useMemo, and useCallback work together for performance optimization.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`These three tools form the "memoization trinity" in React: \`React.memo\` prevents component re-renders when props haven't changed (shallow comparison), \`useMemo\` prevents expensive computations from re-running, and \`useCallback\` prevents function references from changing. They work together: \`useCallback\` and \`useMemo\` stabilize references that \`React.memo\` then correctly identifies as unchanged, skipping the re-render. Without the memoized references, \`React.memo\` sees new objects/functions and re-renders anyway.

\`\`\`jsx
// Complete optimization pattern
function ProductList({ products, onSort }) {
  const [sortBy, setSortBy] = useState('name');

  // Memoize expensive computation
  const sortedProducts = useMemo(
    () => [...products].sort((a, b) => a[sortBy].localeCompare(b[sortBy])),
    [products, sortBy]
  );

  // Memoize callback for child
  const handleProductClick = useCallback((productId) => {
    console.log('Clicked:', productId);
  }, []);

  return (
    <div>
      {sortedProducts.map(product => (
        // ProductCard only re-renders when its specific props change
        <ProductCard
          key={product.id}
          product={product}
          onClick={handleProductClick}
        />
      ))}
    </div>
  );
}

const ProductCard = React.memo(({ product, onClick }) => (
  <div onClick={() => onClick(product.id)}>{product.name}</div>
));
\`\`\``,tech:"react",displayNumber:202,sortKey:1174},{id:"react-1175",number:203,title:"What is the useEffect cleanup race condition and how do you prevent it?",question:"What is the useEffect cleanup race condition and how do you prevent it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A race condition occurs in \`useEffect\` when an async operation completes after a newer request has started. For example, if you fetch data for userId=1, then quickly change to userId=2, the first request might complete after the second, overwriting the correct data. Prevent it using an AbortController to cancel the previous request, or a boolean flag to ignore stale responses.

\`\`\`jsx
function UserProfile({ userId }) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    let cancelled = false;
    const controller = new AbortController();

    fetch(\`/api/users/\${userId}\`, { signal: controller.signal })
      .then(res => res.json())
      .then(data => {
        if (!cancelled) setUser(data); // Ignore if stale
      })
      .catch(err => {
        if (err.name !== 'AbortError' && !cancelled) {
          console.error(err);
        }
      });

    return () => {
      cancelled = true;     // Flag to ignore stale response
      controller.abort();   // Cancel in-flight request
    };
  }, [userId]);

  return user ? <div>{user.name}</div> : <Spinner />;
}
\`\`\``,tech:"react",displayNumber:203,sortKey:1175},{id:"react-1176",number:204,title:"How does the React Hook rules linter enforce correctness?",question:"How does the React Hook rules linter enforce correctness?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The \`eslint-plugin-react-hooks\` enforces two rules: \`rules-of-hooks\` (ensures Hooks are only called at the top level of components/custom Hooks, never inside conditions, loops, or nested functions) and \`exhaustive-deps\` (ensures dependency arrays are complete and correct). The plugin performs static analysis of your code at lint time, catching Hook violations before they cause runtime bugs. Both rules are considered essential for any React project.

\`\`\`jsx
// rules-of-hooks violation (lint error)
function Component({ flag }) {
  if (flag) {
    useEffect(() => {}, []); // ESLint: "Hook called conditionally"
  }
}

// exhaustive-deps violation (lint warning)
function Component({ userId }) {
  const [user, setUser] = useState(null);
  useEffect(() => {
    fetchUser(userId).then(setUser);
  }, []); // ESLint warning: "userId missing from dependency array"
}
\`\`\``,tech:"react",displayNumber:204,sortKey:1176},{id:"react-1177",number:205,title:"Explain the useState functional update pattern and when to use it.",question:"Explain the useState functional update pattern and when to use it.",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`The functional update pattern \`setState(prev => newValue)\` passes the most recent state to a function that computes the next state. Use it when: (1) new state depends on previous state, (2) you're updating state inside a \`setTimeout\`, \`setInterval\`, or async callback, (3) multiple \`setState\` calls need to see the latest value, or (4) you want to avoid including state in dependency arrays of callbacks.

\`\`\`jsx
function Counter() {
  const [count, setCount] = useState(0);

  // Functional update: always uses latest state
  const increment = () => setCount(prev => prev + 1);

  // Multiple updates: each sees latest
  const triple = () => {
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    setCount(prev => prev + 1);
    // Final count: prevCount + 3
  };

  // Inside async callback
  const incrementAsync = () => {
    setTimeout(() => {
      setCount(prev => prev + 1); // Correct: uses latest state
    }, 1000);
  };

  return <button onClick={triple}>{count}</button>;
}
\`\`\``,tech:"react",displayNumber:205,sortKey:1177},{id:"react-1178",number:206,title:"What is the recommended pattern for complex state with useReducer?",question:"What is the recommended pattern for complex state with useReducer?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`For complex state, use the "state machine" pattern with useReducer: define all possible states and transitions explicitly, use an enum-like object for action types, keep the reducer pure (no side effects), and separate side effects from state transitions using middleware patterns (like thunks). This makes state transitions predictable, testable, and debuggable.

\`\`\`jsx
const ACTIONS = {
  FETCH_START: 'FETCH_START',
  FETCH_SUCCESS: 'FETCH_SUCCESS',
  FETCH_ERROR: 'FETCH_ERROR',
  RETRY: 'RETRY',
};

function asyncReducer(state, action) {
  switch (action.type) {
    case ACTIONS.FETCH_START:
      return { ...state, status: 'loading', error: null };
    case ACTIONS.FETCH_SUCCESS:
      return { ...state, status: 'success', data: action.payload, error: null };
    case ACTIONS.FETCH_ERROR:
      return { ...state, status: 'error', error: action.payload };
    case ACTIONS.RETRY:
      return { ...state, status: 'idle' };
    default:
      throw new Error(\`Unhandled action: \${action.type}\`);
  }
}

function useAsync(asyncFn, deps) {
  const [state, dispatch] = useReducer(asyncReducer, {
    status: 'idle', data: null, error: null,
  });

  useEffect(() => {
    let cancelled = false;
    dispatch({ type: ACTIONS.FETCH_START });

    asyncFn()
      .then(data => { if (!cancelled) dispatch({ type: ACTIONS.FETCH_SUCCESS, payload: data }); })
      .catch(err => { if (!cancelled) dispatch({ type: ACTIONS.FETCH_ERROR, payload: err }); });

    return () => { cancelled = true; };
  }, deps);

  const retry = () => dispatch({ type: ACTIONS.RETRY });

  return { ...state, retry };
}
\`\`\``,tech:"react",displayNumber:206,sortKey:1178},{id:"react-1179",number:207,title:"How do you share state between sibling components using custom Hooks?",question:"How do you share state between sibling components using custom Hooks?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks can share state between siblings by having both components call the same custom Hook with the same arguments, but since each call creates independent state, true sharing requires lifting state up or using context. A pattern for shared state is to create a Hook that reads from a shared store (like Zustand) or use context with a custom Hook as the consumer interface.

\`\`\`jsx
// Pattern 1: Lift state to common ancestor, pass via custom Hook
function useSharedCounter() {
  // This only works if called within the same component
  const [count, setCount] = useState(0);
  return { count, increment: () => setCount(c => c + 1) };
}

// Pattern 2: Share via context + custom Hook
const CounterContext = createContext();

function CounterProvider({ children }) {
  const [count, setCount] = useState(0);
  const value = useMemo(() => ({
    count,
    increment: () => setCount(c => c + 1),
  }), [count]);
  return <CounterContext.Provider value={value}>{children}</CounterContext.Provider>;
}

function useCounter() {
  const context = useContext(CounterContext);
  if (!context) throw new Error('useCounter must be used within CounterProvider');
  return context;
}

// Both components share the same state
function Display() {
  const { count } = useCounter();
  return <span>{count}</span>;
}

function Button() {
  const { increment } = useCounter();
  return <button onClick={increment}>+1</button>;
}
\`\`\``,tech:"react",displayNumber:207,sortKey:1179},{id:"react-1180",number:208,title:"What is the useLayoutEffect measurement pattern and why does it matter?",question:"What is the useLayoutEffect measurement pattern and why does it matter?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The measurement pattern uses \`useLayoutEffect\` to read DOM measurements (height, width, position) and immediately apply corrective styles before the browser paints. This prevents layout shifts and visual flicker. It is essential for features like tooltips, popovers, dropdown menus, and responsive layouts where you need to know an element's rendered dimensions before positioning another element.

\`\`\`jsx
function AutoResizeTextarea({ value, onChange }) {
  const textareaRef = useRef(null);
  const [height, setHeight] = useState('auto');

  useLayoutEffect(() => {
    const textarea = textareaRef.current;
    // Reset to measure natural height
    textarea.style.height = 'auto';
    const newHeight = textarea.scrollHeight;
    textarea.style.height = \`\${newHeight}px\`;
    setHeight(newHeight);
  }, [value]);

  return (
    <textarea
      ref={textareaRef}
      value={value}
      onChange={onChange}
      style={{ height, overflow: 'hidden' }}
    />
  );
}
\`\`\``,tech:"react",displayNumber:208,sortKey:1180},{id:"react-1181",number:209,title:"How do you test custom Hooks?",question:"How do you test custom Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Use \`@testing-library/react-hooks\` (or \`renderHook\` from \`@testing-library/react\` v13+). \`renderHook\` wraps a Hook call in a test component, providing \`result.current\` for assertions and \`rerender\` to simulate re-renders with new props. For Hooks with side effects, use \`act()\` to flush pending state updates. Mock external dependencies (APIs, browser APIs) using \`jest.mock\` or \`msw\`.

\`\`\`jsx
import { renderHook, act } from '@testing-library/react';
import { useCounter } from './useCounter';

describe('useCounter', () => {
  it('increments count', () => {
    const { result } = renderHook(() => useCounter(0));

    act(() => result.current.increment());
    expect(result.current.count).toBe(1);

    act(() => result.current.increment());
    expect(result.current.count).toBe(2);
  });

  it('resets to initial value', () => {
    const { result } = renderHook(() => useCounter(10));

    act(() => result.current.increment());
    act(() => result.current.reset());
    expect(result.current.count).toBe(10);
  });

  it('responds to prop changes', () => {
    const { result, rerender } = renderHook(
      ({ initialValue }) => useCounter(initialValue),
      { initialProps: { initialValue: 0 } }
    );

    rerender({ initialValue: 5 });
    expect(result.current.count).toBe(5);
  });
});
\`\`\``,tech:"react",displayNumber:209,sortKey:1181},{id:"react-1182",number:210,title:"What is the useSyncExternalStore anti-pattern and how to avoid it?",question:"What is the useSyncExternalStore anti-pattern and how to avoid it?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Anti-patterns with \`useSyncExternalStore\`: (1) Creating \`subscribe\` and \`getSnapshot\` inline on every render (causes re-subscription), (2) Not handling the case where the store is not yet available (SSR), (3) Using it for local component state (just use \`useState\`), (4) Not returning a stable function reference from \`subscribe\`. Always memoize or define these functions outside the component to avoid unnecessary re-subscriptions.

\`\`\`jsx
// BAD: Creates new functions every render
function useStore(store) {
  return useSyncExternalStore(
    (callback) => store.subscribe(callback), // New function each render!
    () => store.getState(),                  // New function each render!
  );
}

// CORRECT: Stable function references
function createSubscription(store) {
  return (callback) => {
    store.subscribe(callback);
    return () => store.unsubscribe(callback);
  };
}

const subscribe = createSubscription(store);
const getSnapshot = () => store.getState();

function MyComponent() {
  const state = useSyncExternalStore(subscribe, getSnapshot);
  return <div>{state.value}</div>;
}
\`\`\``,tech:"react",displayNumber:210,sortKey:1182},{id:"react-1183",number:211,title:"How does useInsertionEffect prevent style recalculation thrashing?",question:"How does useInsertionEffect prevent style recalculation thrashing?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:"Without `useInsertionEffect`, CSS-in-JS libraries inject `<style>` tags during render (before commit) or in `useLayoutEffect` (after layout). Injecting before commit causes React to see stale styles. Injecting in `useLayoutEffect` means styles are inserted after layout measurement, causing a layout recalculation. `useInsertionEffect` runs after DOM mutation but before layout effects, allowing styles to be injected before layout is measured, eliminating the extra recalculation pass.\n\n```jsx\n// Timeline of a render with style injection:\n// 1. Render phase (virtual DOM diff)\n// 2. Commit phase (DOM mutations)\n// 3. useInsertionEffect runs <-- Style injection here\n// 4. useLayoutEffect runs <-- Layout measurement here (sees correct styles)\n// 5. Browser paints\n\n// Without useInsertionEffect (in useLayoutEffect):\n// 1-2. Same\n// 3. useLayoutEffect: measures layout (STALE styles)\n// 4. Injects styles\n// 5. Layout recalculates (WASTED WORK)\n// 6. Browser paints\n```",tech:"react",displayNumber:211,sortKey:1183},{id:"react-1184",number:212,title:'Explain the concept of "effect as synchronization" in React.',question:'Explain the concept of "effect as synchronization" in React.',difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`React's \`useEffect\` documentation describes effects as synchronizing a component with an external system. Rather than thinking of effects as lifecycle methods (componentDidMount, componentDidUpdate, componentWillUnmount), think of them as keeping the component in sync with external state (APIs, subscriptions, DOM). Each effect synchronizes with a specific external system and cleans up when that synchronization is no longer needed.

\`\`\`jsx
// NOT this (lifecycle thinking):
useEffect(() => {
  // componentDidMount
  const conn = connect(roomId);
  // componentDidUpdate (need to check if roomId changed)
  // componentWillUnmount
  return () => conn.disconnect();
}, [roomId]);

// THIS (synchronization thinking):
useEffect(() => {
  // Synchronize with the chat server for this roomId
  const conn = createConnection(roomId);
  conn.connect();
  // Stop synchronizing (cleanup)
  return () => {
    conn.disconnect();
  };
}, [roomId]); // Re-synchronize when roomId changes
\`\`\``,tech:"react",displayNumber:212,sortKey:1184},{id:"react-1185",number:213,title:"What are the common performance anti-patterns with Hooks?",question:"What are the common performance anti-patterns with Hooks?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Common anti-patterns: (1) Memoizing everything without profiling first, (2) Creating objects/arrays inline as \`useMemo\` dependencies, (3) Using \`useCallback\` when children aren't memoized, (4) Calling expensive computations inside the render body instead of \`useMemo\`, (5) Using \`useEffect\` for derived state (should compute during render), (6) Not splitting context by update frequency, (7) Using \`useState\` for values that could be derived from other state.

\`\`\`jsx
// ANTI-PATTERN: useEffect for derived state
function FilteredList({ items, filter }) {
  const [filtered, setFiltered] = useState([]);
  useEffect(() => {
    setFiltered(items.filter(i => i.name.includes(filter))); // Unnecessary
  }, [items, filter]);
  return <List items={filtered} />;
}

// CORRECT: Compute during render
function FilteredList({ items, filter }) {
  const filtered = useMemo(
    () => items.filter(i => i.name.includes(filter)),
    [items, filter]
  );
  return <List items={filtered} />;
}

// ANTI-PATTERN: Inline object as dependency
useEffect(() => {}, [{ a, b }]); // New array every render!

// CORRECT: Use primitives
useEffect(() => {}, [a, b]); // Value comparison, stable
\`\`\``,tech:"react",displayNumber:213,sortKey:1185},{id:"react-1186",number:214,title:"How do you handle form state with useReducer?",question:"How do you handle form state with useReducer?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useReducer\` excels at managing form state because forms have multiple interdependent fields, validation states, and submission states. The reducer centralizes all form logic: field changes, validation, submission, and reset. Each action type represents a specific form event, making the flow easy to follow and test. This pattern scales better than multiple \`useState\` calls for complex forms.

\`\`\`jsx
function useForm(initialValues, validate, onSubmit) {
  const [state, dispatch] = useReducer(formReducer, {
    values: initialValues,
    errors: {},
    touched: {},
    isSubmitting: false,
    isSubmitted: false,
  });

  function handleChange(field, value) {
    dispatch({ type: 'SET_FIELD', field, value });
  }

  function handleBlur(field) {
    dispatch({ type: 'SET_TOUCHED', field });
    const errors = validate(state.values);
    dispatch({ type: 'SET_ERRORS', errors });
  }

  async function handleSubmit(e) {
    e.preventDefault();
    const errors = validate(state.values);
    if (Object.keys(errors).length > 0) {
      dispatch({ type: 'SET_ERRORS', errors });
      return;
    }
    dispatch({ type: 'SUBMIT_START' });
    try {
      await onSubmit(state.values);
      dispatch({ type: 'SUBMIT_SUCCESS' });
    } catch (err) {
      dispatch({ type: 'SUBMIT_ERROR', error: err.message });
    }
  }

  function reset() {
    dispatch({ type: 'RESET', initialValues });
  }

  return {
    values: state.values,
    errors: state.errors,
    touched: state.touched,
    isSubmitting: state.isSubmitting,
    handleChange,
    handleBlur,
    handleSubmit,
    reset,
  };
}
\`\`\``,tech:"react",displayNumber:214,sortKey:1186},{id:"react-1187",number:215,title:"Explain the relationship between useEffect and component lifecycle.",question:"Explain the relationship between useEffect and component lifecycle.",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`While \`useEffect\` maps conceptually to lifecycle methods, it's more accurately described as synchronization. \`useEffect\` with \`[]\` is similar to \`componentDidMount\`, \`useEffect\` with dependencies is similar to \`componentDidUpdate\`, and the cleanup function is similar to \`componentWillUnmount\`. However, thinking in terms of synchronization is more accurate: effects synchronize with external systems and re-synchronize when dependencies change.

\`\`\`jsx
// Lifecycle mapping (for reference, not recommended mental model)
function Component({ userId }) {
  // componentDidMount equivalent
  useEffect(() => {
    logMount();
  }, []);

  // componentDidUpdate equivalent (for userId)
  useEffect(() => {
    analytics.track('user_changed', userId);
  }, [userId]);

  // componentWillUnmount equivalent
  useEffect(() => {
    const subscription = subscribe(userId);
    return () => subscription.unsubscribe(); // Cleanup
  }, [userId]);

  // All three in one: mount + update + unmount
  useEffect(() => {
    analytics.track('view', userId);
    return () => analytics.track('unview', userId);
  }, [userId]);
}
\`\`\``,tech:"react",displayNumber:215,sortKey:1187},{id:"react-1188",number:216,title:"What happens if you have multiple useEffect calls in one component?",question:"What happens if you have multiple useEffect calls in one component?",difficulty:"Basic",category:"React Hooks Deep Dive",answer:`Multiple \`useEffect\` calls are independent and run in declaration order. Each effect handles its own synchronization. This is actually a feature: it allows you to separate concerns, with each effect managing a single external system. Cleanup functions from all effects run in reverse declaration order before the component unmounts or before effects re-run.

\`\`\`jsx
function ChatRoom({ roomId, theme }) {
  // Effect 1: Connection management
  useEffect(() => {
    const conn = createConnection(roomId);
    conn.connect();
    return () => conn.disconnect();
  }, [roomId]);

  // Effect 2: Document title
  useEffect(() => {
    document.title = \`Chat - Room \${roomId}\`;
  }, [roomId]);

  // Effect 3: Theme application
  useEffect(() => {
    document.body.className = theme;
    return () => { document.body.className = ''; };
  }, [theme]);

  // All three are independent, run in order, clean up independently
}
\`\`\``,tech:"react",displayNumber:216,sortKey:1188},{id:"react-1189",number:217,title:"How do you handle errors inside useEffect?",question:"How do you handle errors inside useEffect?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useEffect\` callbacks can't use try/catch with async/await directly because the cleanup function can't be an async function. Solutions: (1) Use \`.catch()\` on promises, (2) Wrap the async logic in an async function called from the effect, (3) Use error boundaries (React 18+) with the \`onError\` callback, (4) For fetch errors, check \`response.ok\` and throw. Always handle both network errors and application-level errors.

\`\`\`jsx
function DataFetcher({ url }) {
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let cancelled = false;

    async function fetchData() {
      try {
        setLoading(true);
        const response = await fetch(url);
        if (!response.ok) throw new Error(\`HTTP \${response.status}\`);
        const json = await response.json();
        if (!cancelled) setData(json);
      } catch (err) {
        if (!cancelled) setError(err.message);
      } finally {
        if (!cancelled) setLoading(false);
      }
    }

    fetchData();
    return () => { cancelled = true; };
  }, [url]);

  if (loading) return <Spinner />;
  if (error) return <ErrorMessage message={error} />;
  return <DataView data={data} />;
}
\`\`\``,tech:"react",displayNumber:217,sortKey:1189},{id:"react-1190",number:218,title:"What is the useEffect event pattern (React canary/experimental)?",question:"What is the useEffect event pattern (React canary/experimental)?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's experimental \`useEffectEvent\` hook (not yet stable) allows you to extract "event-like" logic from effects. It creates a function that always sees the latest values of reactive dependencies without being listed as a dependency. This solves the tension between needing the latest values and not wanting the effect to re-run. It is similar to \`useRef\` for latest values but is designed specifically for effects.

\`\`\`jsx
// Experimental pattern (not yet stable in React)
import { useEffectEvent } from 'react'; // Experimental

function ChatRoom({ roomId, theme }) {
  const onVisit = useEffectEvent((visitCount) => {
    analytics.track('visit', { roomId, theme, visitCount });
  });

  useEffect(() => {
    let visits = 0;
    const conn = createConnection(roomId);
    conn.on('visit', () => {
      visits++;
      onVisit(visits); // Always sees latest roomId and theme
    });
    conn.connect();
    return () => conn.disconnect();
  }, [roomId]); // onVisit reference is stable, doesn't need to be in deps
}
\`\`\``,tech:"react",displayNumber:218,sortKey:1190},{id:"react-1191",number:219,title:"How do you optimize re-renders caused by context updates?",question:"How do you optimize re-renders caused by context updates?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`Strategies: (1) Split context by update frequency. (2) Memoize the context value with \`useMemo\`. (3) Move stateful logic into a custom Hook that uses \`useSyncExternalStore\` for fine-grained subscriptions. (4) Use the "compound component" pattern to reduce context scope. (5) Use state management libraries (Zustand, Jotai) with selector-based subscriptions that only trigger re-renders for specific slices of state.

\`\`\`jsx
// Strategy: Split context + memoize values
const AuthStateContext = createContext();   // Rarely changes
const AuthActionsContext = createContext(); // Never changes (stable refs)

function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const login = useCallback(async (creds) => {
    const u = await api.login(creds);
    setUser(u);
  }, []);
  const logout = useCallback(() => setUser(null), []);

  const stateValue = useMemo(() => ({ user }), [user]);
  const actionsValue = useMemo(() => ({ login, logout }), [login, logout]);

  return (
    <AuthStateContext.Provider value={stateValue}>
      <AuthActionsContext.Provider value={actionsValue}>
        {children}
      </AuthActionsContext.Provider>
    </AuthStateContext.Provider>
  );
}

// Components that only need actions won't re-render on user changes
function LogoutButton() {
  const { logout } = useContext(AuthActionsContext); // Stable
  return <button onClick={logout}>Logout</button>;
}
\`\`\``,tech:"react",displayNumber:219,sortKey:1191},{id:"react-1192",number:220,title:"Explain the useEffect vs useLayoutEffect timing diagram.",question:"Explain the useEffect vs useLayoutEffect timing diagram.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`The rendering timeline: (1) React renders (virtual DOM diff), (2) React commits DOM changes, (3) \`useInsertionEffect\` runs (synchronous, for CSS-in-JS), (4) \`useLayoutEffect\` runs (synchronous, for DOM measurements), (5) Browser paints pixels on screen, (6) \`useEffect\` runs (asynchronous, for non-blocking side effects). The key insight: \`useLayoutEffect\` blocks the paint, \`useEffect\` does not.

\`\`\`
Render Phase (virtual DOM) 
    |
    v
Commit Phase (DOM mutations)
    |
    v
useInsertionEffect (style injection)
    |
    v
useLayoutEffect (DOM measurement/modification)
    |
    v
Browser Paint  <-- visible to user
    |
    v
useEffect (data fetching, subscriptions, etc.)
\`\`\``,tech:"react",displayNumber:220,sortKey:1192},{id:"react-1193",number:221,title:"How do you handle multiple async operations in useEffect?",question:"How do you handle multiple async operations in useEffect?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`For multiple independent async operations, use separate \`useEffect\` calls (each handles its own cleanup). For dependent async operations (second depends on first's result), chain them within a single effect using async/await. Always handle cleanup with \`cancelled\` flags or AbortControllers, and manage loading/error states appropriately for each operation.

\`\`\`jsx
function Dashboard({ userId }) {
  const [user, setUser] = useState(null);
  const [posts, setPosts] = useState(null);

  // Independent: parallel fetching
  useEffect(() => {
    let cancelled = false;
    fetchUser(userId).then(data => { if (!cancelled) setUser(data); });
    return () => { cancelled = true; };
  }, [userId]);

  useEffect(() => {
    let cancelled = false;
    fetchPosts(userId).then(data => { if (!cancelled) setPosts(data); });
    return () => { cancelled = true; };
  }, [userId]);

  // Dependent: sequential fetching
  useEffect(() => {
    let cancelled = false;
    async function load() {
      const userData = await fetchUser(userId);
      if (cancelled) return;
      setUser(userData);
      const postData = await fetchPosts(userData.postsUrl);
      if (cancelled) return;
      setPosts(postData);
    }
    load();
    return () => { cancelled = true; };
  }, [userId]);

  if (!user || !posts) return <Spinner />;
  return <div>{user.name} - {posts.length} posts</div>;
}
\`\`\``,tech:"react",displayNumber:221,sortKey:1193},{id:"react-1194",number:222,title:"What is the difference between refs created by useRef and callback refs?",question:"What is the difference between refs created by useRef and callback refs?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`\`useRef\` returns a stable object with a \`.current\` property. A callback ref is a function that React calls with the DOM node on mount and \`null\` on unmount. Callback refs are useful when you need to perform cleanup or when the ref callback depends on dynamic values. \`useRef\` is simpler and more common; callback refs are needed for dynamic ref assignments and cleanup logic tied to the DOM element's lifecycle.

\`\`\`jsx
// useRef: Simple, stable reference
function Input() {
  const inputRef = useRef(null);
  useEffect(() => { inputRef.current.focus(); }, []);
  return <input ref={inputRef} />;
}

// Callback ref: Dynamic, with cleanup
function MeasuredDiv() {
  const [height, setHeight] = useState(0);

  const refCallback = useCallback((node) => {
    if (node !== null) {
      const observer = new ResizeObserver(([entry]) => {
        setHeight(entry.contentRect.height);
      });
      observer.observe(node);
      return () => observer.disconnect(); // Cleanup on unmount
    }
  }, []);

  return <div ref={refCallback}>Height: {height}px</div>;
}
\`\`\``,tech:"react",displayNumber:222,sortKey:1194},{id:"react-1195",number:223,title:'Explain the "use the latest ref" pattern for avoiding stale closures.',question:'Explain the "use the latest ref" pattern for avoiding stale closures.',difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`The "latest ref" pattern stores the most current value of a prop or state in a ref. Since refs are mutable and not captured by closures, the ref always provides the latest value. This is essential for callbacks stored in refs, interval functions, and event handlers that need to reference values that change between renders but shouldn't cause the effect to re-run.

\`\`\`jsx
function useLatestRef(value) {
  const ref = useRef(value);
  ref.current = value; // Update on every render
  return ref;
}

function useInterval(callback, delay) {
  const latestCallback = useLatestRef(callback);

  useEffect(() => {
    const id = setInterval(() => {
      latestCallback.current(); // Always calls the latest version
    }, delay);
    return () => clearInterval(id);
  }, [delay]); // Only re-runs when delay changes
}

// Usage: callback can change without re-creating the interval
function Timer() {
  const [count, setCount] = useState(0);
  useInterval(() => setCount(c => c + 1), 1000);
  return <p>{count}</p>;
}
\`\`\``,tech:"react",displayNumber:223,sortKey:1195},{id:"react-1196",number:224,title:"How does React ensure Hook stability across renders?",question:"How does React ensure Hook stability across renders?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React ensures Hook stability through its fiber architecture. Each component fiber has a \`memoizedState\` property pointing to the first Hook's state node. Each Hook state node has a \`next\` pointer forming a linked list. On re-renders, React walks this list in order, matching each Hook call to its state node. The \`dispatch\` function from \`useState\`/\`useReducer\` is stable across renders because it's bound to the fiber's state node, not captured in closures.

\`\`\`jsx
// dispatch is stable - safe to omit from dependencies
function Component() {
  const [count, setCount] = useState(0);

  useEffect(() => {
    // setCount reference never changes between renders
    const id = setInterval(() => setCount(c => c + 1), 1000);
    return () => clearInterval(id);
  }, []); // No dependency needed for setCount
}

// useMemo and useCallback return stable references when deps don't change
function Component({ theme }) {
  const stableFn = useCallback(() => {}, []); // Always same reference
  const stableObj = useMemo(() => ({ a: 1 }), []); // Always same reference
}
\`\`\``,tech:"react",displayNumber:224,sortKey:1196},{id:"react-1197",number:225,title:"What are the key differences between React 17 and React 18 Hooks behavior?",question:"What are the key differences between React 17 and React 18 Hooks behavior?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Key differences: (1) React 18 adds automatic batching for all state updates (not just event handlers), (2) React 18 introduces \`useTransition\`, \`useDeferredValue\`, \`useId\`, and \`useSyncExternalStore\`, (3) React 18 adds \`useInsertionEffect\`, (4) React 18's \`useId\` generates unique IDs for SSR hydration safety, (5) React 18 concurrent features allow transitions to be interrupted, (6) React 18 removes the need for \`act()\` in most cases for testing.

\`\`\`jsx
// React 17: Batching only in event handlers
function handleClick() {
  setTimeout(() => {
    setA(1); // Re-render 1
    setB(2); // Re-render 2
  }, 0);
}

// React 18: Automatic batching everywhere
function handleClick() {
  setTimeout(() => {
    setA(1); // Batched
    setB(2); // Batched - only 1 re-render
  }, 0);
}

// New Hooks in React 18
function Component() {
  const [isPending, startTransition] = useTransition();
  const deferredValue = useDeferredValue(value);
  const id = useId();
  const externalState = useSyncExternalStore(subscribe, getSnapshot);
}
\`\`\``,tech:"react",displayNumber:225,sortKey:1197},{id:"react-1198",number:226,title:"Explain the useDeferredValue pattern for search filtering.",question:"Explain the useDeferredValue pattern for search filtering.",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`\`useDeferredValue\` is ideal for search/filter UIs where typing should feel instant but the filtered results are expensive to compute. The input updates immediately (high priority), while the filtered list updates with a deferred value (low priority). React can interrupt the expensive filter computation if the user types again, keeping the UI responsive. The stale version is shown while the new computation runs.

\`\`\`jsx
function SearchableList({ items }) {
  const [query, setQuery] = useState('');
  const deferredQuery = useDeferredValue(query);
  const isStale = query !== deferredQuery;

  const filteredItems = useMemo(
    () => items.filter(item => item.name.toLowerCase().includes(deferredQuery.toLowerCase())),
    [items, deferredQuery]
  );

  return (
    <div>
      <input
        value={query}
        onChange={e => setQuery(e.target.value)}
        placeholder="Search..."
      />
      <div style={{ opacity: isStale ? 0.6 : 1, transition: 'opacity 200ms' }}>
        <ItemList items={filteredItems} />
      </div>
      {isStale && <span>Updating...</span>}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:226,sortKey:1198},{id:"react-1199",number:227,title:"How do you create a useFetch Hook with caching?",question:"How do you create a useFetch Hook with caching?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`A cached \`useFetch\` Hook stores responses in a cache (using a Map or similar structure) keyed by URL. On subsequent requests for the same URL, it returns cached data immediately while optionally revalidating in the background. This pattern (stale-while-revalidate) improves perceived performance. Use \`useRef\` for the cache to avoid re-creating it on renders.

\`\`\`jsx
const cache = new Map(); // Module-level cache persists across components

function useFetch(url) {
  const [data, setData] = useState(() => cache.get(url) || null);
  const [loading, setLoading] = useState(!cache.has(url));
  const [error, setError] = useState(null);

  useEffect(() => {
    if (cache.has(url)) {
      setData(cache.get(url));
      setLoading(false);
      return;
    }

    let cancelled = false;
    setLoading(true);

    fetch(url)
      .then(res => {
        if (!res.ok) throw new Error(\`HTTP \${res.status}\`);
        return res.json();
      })
      .then(json => {
        cache.set(url, json);
        if (!cancelled) { setData(json); setLoading(false); }
      })
      .catch(err => {
        if (!cancelled) { setError(err.message); setLoading(false); }
      });

    return () => { cancelled = true; };
  }, [url]);

  return { data, loading, error };
}
\`\`\``,tech:"react",displayNumber:227,sortKey:1199},{id:"react-1200",number:228,title:"What is the useState 18 cheat pattern for computing initial state?",question:"What is the useState 18 cheat pattern for computing initial state?",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`When using \`useState\` with a lazy initializer (function form), you can compute expensive initial state only once. The lazy initializer function runs only during the first render. This is particularly useful for expensive operations like parsing large JSON, computing complex default values, or reading from browser APIs (localStorage). Without the lazy form, the computation runs on every render even though the result is discarded.

\`\`\`jsx
// EXPENSIVE: Runs on every render
function Component1() {
  const [settings, setSettings] = useState(JSON.parse(localStorage.getItem('settings')));
}

// OPTIMAL: Runs only on first render
function Component2() {
  const [settings, setSettings] = useState(() => {
    try {
      return JSON.parse(localStorage.getItem('settings'));
    } catch {
      return defaultSettings;
    }
  });
}

// Also useful for expensive computations
function Component3() {
  const [matrix, setMatrix] = useState(() => generateLargeMatrix(1000, 1000));
}
\`\`\``,tech:"react",displayNumber:228,sortKey:1200},{id:"react-1201",number:229,title:"Explain how custom Hooks enable code reuse across your application.",question:"Explain how custom Hooks enable code reuse across your application.",difficulty:"Intermediate",category:"React Hooks Deep Dive",answer:`Custom Hooks are React's primary mechanism for logic reuse. They solve the problems that mixins, higher-order components, and render props tried to address, but without the wrapper hell or component explosion. A custom Hook encapsulates stateful logic that can be shared across components without changing the component hierarchy. Each call to the Hook gets its own independent state, so multiple components using the same Hook don't share data unless explicitly designed to.

\`\`\`jsx
// Shared logic across many components
function useClickOutside(ref, handler) {
  useEffect(() => {
    function listener(event) {
      if (!ref.current || ref.current.contains(event.target)) return;
      handler(event);
    }
    document.addEventListener('mousedown', listener);
    document.addEventListener('touchstart', listener);
    return () => {
      document.removeEventListener('mousedown', listener);
      document.removeEventListener('touchstart', listener);
    };
  }, [ref, handler]);
}

// Used in a dropdown
function Dropdown({ children }) {
  const ref = useRef(null);
  const [open, setOpen] = useState(false);
  useClickOutside(ref, () => setOpen(false));
  return <div ref={ref}>{open && children}</div>;
}

// Used in a modal
function Modal({ children, onClose }) {
  const ref = useRef(null);
  useClickOutside(ref, onClose);
  return <div ref={ref} className="modal">{children}</div>;
}
\`\`\``,tech:"react",displayNumber:229,sortKey:1201},{id:"react-1202",number:230,title:"What are the future directions for React Hooks and data fetching?",question:"What are the future directions for React Hooks and data fetching?",difficulty:"Advanced",category:"React Hooks Deep Dive",answer:`React's future direction includes: (1) The \`use()\` hook (React 19) for reading resources including promises directly in render with Suspense, (2) Server Components reducing the need for client-side data fetching hooks, (3) React Compiler (formerly React Forget) automatically memoizing components and eliminating the need for manual \`useMemo\`/\`useCallback\`, (4) Transition-based data fetching as the default pattern, (5) Potential deprecation of \`useEffect\` for data fetching in favor of \`use()\` + Suspense, (6) \`useFormStatus\` and \`useFormState\` for form actions integration.

\`\`\`jsx
// Future: use() with Suspense replaces useEffect for data fetching
function UserProfile({ userId }) {
  const user = use(fetchUser(userId)); // Suspends until resolved
  return <h1>{user.name}</h1>;
}

// Future: Server Components reduce client-side hooks
// app/users/[id]/page.tsx (Server Component)
async function UserPage({ params }) {
  const user = await db.user.findUnique({ where: { id: params.id } });
  return <UserProfile user={user} />; // No useEffect needed
}

// Future: React Compiler eliminates manual memoization
// No need for useMemo/useCallback - compiler handles it automatically
function Component({ data, onClick }) {
  // Compiler auto-memoizes expensive computations
  const processed = expensiveProcess(data);
  // Compiler auto-memoizes callbacks passed to memoized children
  return <MemoizedChild data={processed} onClick={onClick} />;
}
\`\`\``,tech:"react",displayNumber:230,sortKey:1202},{id:"react-1203",number:231,title:"What are the main reasons for unnecessary re-renders in React?",question:"What are the main reasons for unnecessary re-renders in React?",difficulty:"Basic",category:"Performance & Optimization",answer:`Unnecessary re-renders occur when a component renders even though its output wouldn't change. The main causes are:

1. **Parent re-renders** - By default, all children re-render when parent does
2. **Prop changes** - New object/array references created on every render
3. **Context changes** - All consumers re-render when context value changes
4. **State changes** - Any state update triggers re-render, even if value is the same
5. **Anonymous functions** - New function references on every render
6. **Inline objects** - New object references in JSX props

React's default behavior is to re-render children when parent re-renders because it's safer and often cheap. Performance issues arise when expensive computations or large component trees re-render unnecessarily.

\`\`\`jsx
// Problem: New reference on every render
function Parent() {
  const [count, setCount] = useState(0);

  return (
    <Child
      onClick={() => console.log('click')}  // New function each render
      style={{ color: 'red' }}              // New object each render
    />
  );
}

// Solution: Stable references
function Parent() {
  const [count, setCount] = useState(0);
  const handleClick = useCallback(() => console.log('click'), []);
  const style = useMemo(() => ({ color: 'red' }), []);

  return <Child onClick={handleClick} style={style} />;
}
\`\`\``,tech:"react",displayNumber:231,sortKey:1203},{id:"react-1204",number:232,title:"How does React.memo work and when should you use it?",question:"How does React.memo work and when should you use it?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`React.memo\` is a higher-order component that memoizes the rendered output. It performs a shallow comparison of props and skips re-rendering if props haven't changed. It's useful for:

- Components that render often with the same props
- Expensive-to-render components
- Components deep in the tree that don't need to re-render when parent changes

It should NOT be used for:
- Components that always receive different props
- Small, cheap components (memoization overhead may outweigh benefits)
- Components where props change frequently anyway

\`\`\`jsx
import React, { memo } from 'react';

// Memoized component
const UserCard = memo(function UserCard({ user, onSelect }) {
  return (
    <div onClick={() => onSelect(user.id)}>
      {user.name}
    </div>
  );
});

// Alternative syntax
const UserCard = memo(function UserCard({ user }) {
  return <div>{user.name}</div>;
});
\`\`\``,tech:"react",displayNumber:232,sortKey:1204},{id:"react-1205",number:233,title:"What is the difference between shallow comparison and custom comparator in React.memo?",question:"What is the difference between shallow comparison and custom comparator in React.memo?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`React.memo\` accepts an optional second argument: a custom comparison function \`(prevProps, nextProps) => boolean\`.

- **Shallow comparison** (default): Compares each prop with \`Object.is\`. Objects/arrays are compared by reference, not content.
- **Custom comparator**: Returns \`true\` if props are "equal" (skip render), \`false\` if different (re-render). Use it when you need deep comparison or want to ignore certain props.

**Best practice**: Avoid custom comparators unless necessary. They run on every render and can hurt performance if expensive. Prefer stable references with \`useMemo\`/\`useCallback\` instead.

\`\`\`jsx
import React, { memo } from 'react';

const ExpensiveList = memo(
  function ExpensiveList({ items }) {
    return items.map(item => <li key={item.id}>{item.text}</li>);
  },
  // Custom comparator: only re-render if length or last item changes
  (prevProps, nextProps) => {
    return (
      prevProps.items.length === nextProps.items.length &&
      prevProps.items[prevProps.items.length - 1]?.id ===
      nextProps.items[nextProps.items.length - 1]?.id
    );
  }
);
\`\`\``,tech:"react",displayNumber:233,sortKey:1205},{id:"react-1206",number:234,title:"What is code splitting and how does it improve performance?",question:"What is code splitting and how does it improve performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`Code splitting is the practice of breaking your bundle into smaller chunks that are loaded on demand rather than all at once. Benefits:

- **Faster initial load**: Users download only what they need for the current view
- **Smaller initial bundle**: Reduced Time to Interactive (TTI)
- **Better caching**: Unchanged chunks stay cached

With React, you use \`React.lazy\` for component-level splitting and dynamic \`import()\` for route/feature-based splitting. Bundle analyzers (webpack-bundle-analyzer) help identify splitting opportunities.

\`\`\`jsx
// Without splitting - everything in one bundle
import AdminPanel from './AdminPanel';

// With splitting - loaded on demand
const AdminPanel = React.lazy(() => import('./AdminPanel'));

// Route-based splitting
const routes = [
  { path: '/', component: React.lazy(() => import('./Home')) },
  { path: '/admin', component: React.lazy(() => import('./Admin')) },
];
\`\`\``,tech:"react",displayNumber:234,sortKey:1206},{id:"react-1207",number:235,title:"How do you use React.lazy and Suspense for code splitting?",question:"How do you use React.lazy and Suspense for code splitting?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`React.lazy\` lets you render a dynamic import as a regular component. \`<Suspense>\` shows a fallback while the lazy component loads.

Key points:
- \`React.lazy\` must return a Promise resolving to a module with a default export
- \`<Suspense>\` must wrap lazy components
- You can nest multiple Suspense boundaries for granular loading states
- Works in both Client and Server Components (with caveats in Next.js)

In React 18+, \`Suspense\` also handles data fetching with libraries like Relay or \`use()\` hook.

\`\`\`jsx
import React, { Suspense, lazy } from 'react';

const Chart = lazy(() => import('./Chart'));
const Modal = lazy(() => import('./Modal'));

function Dashboard() {
  const [showModal, setShowModal] = useState(false);

  return (
    <div>
      <Suspense fallback={<Spinner />}>
        <Chart />
      </Suspense>

      {showModal && (
        <Suspense fallback={<Spinner />}>
          <Modal onClose={() => setShowModal(false)} />
        </Suspense>
      )}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:235,sortKey:1207},{id:"react-1208",number:236,title:"What is virtualization (windowing) and when should you use it?",question:"What is virtualization (windowing) and when should you use it?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Virtualization (windowing) renders only the visible portion of a large list, plus a small buffer. The DOM contains only ~20-50 items regardless of list size (10,000+ items).

Use it when:
- Lists have 100+ items
- Each row is moderately expensive to render
- You need smooth scrolling performance

Libraries: \`react-window\` (lightweight, ~5kb), \`react-virtualized\` (feature-rich), \`@tanstack/react-virtual\` (modern, headless).

\`\`\`jsx
import { FixedSizeList } from 'react-window';

function VirtualList({ items }) {
  return (
    <FixedSizeList
      height={500}
      itemCount={items.length}
      itemSize={50}
      width="100%"
    >
      {({ index, style }) => (
        <div style={style}>
          {items[index].name}
        </div>
      )}
    </FixedSizeList>
  );
}
\`\`\``,tech:"react",displayNumber:236,sortKey:1208},{id:"react-1209",number:237,title:"Compare react-window and react-virtualized",question:"Compare react-window and react-virtualized",difficulty:"Advanced",category:"Performance & Optimization",answer:`Both are virtualization libraries by Brian Vaughn, but differ significantly:

| Feature | react-window | react-virtualized |
|---------|-------------|-------------------|
| Size | ~5kb | ~30kb+ |
| API | Simple (List, Grid) | Complex (many components) |
| Features | Core virtualization | AutoSizer, CellMeasurer, MultiGrid, SortableList |
| Maintenance | Actively maintained | Legacy (use react-window) |

**Recommendation**: Use \`react-window\` for standard use cases. Use \`react-virtualized\` only if you need its advanced features (variable heights, masonry, etc.). For modern apps, \`@tanstack/react-virtual\` is increasingly popular.

\`\`\`jsx
// react-window - simple and small
import { FixedSizeGrid } from 'react-window';

// react-virtualized - more features
import { Grid, AutoSizer, CellMeasurer } from 'react-virtualized';

// Modern alternative
import { useVirtualizer } from '@tanstack/react-virtual';
\`\`\``,tech:"react",displayNumber:237,sortKey:1209},{id:"react-1210",number:238,title:"How does the React DevTools Profiler work?",question:"How does the React DevTools Profiler work?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`The Profiler records component render times and reasons. Features:

1. **Record button**: Start/stop a profiling session
2. **Flamegraph**: Visualize render duration hierarchically
3. **Ranked chart**: Sort components by render time
4. **Commit timeline**: See each commit and what changed
5. **Highlight updates**: Visualize re-renders in real-time
6. **Why did this render?**: Shows changed props/state/hooks/context

To enable, ensure you're using the development build. Production builds disable profiling unless you use the special profiling build.

\`\`\`jsx
// Profiler can also be used programmatically
import { Profiler } from 'react';

function onRenderCallback(
  id,              // Component identifier
  phase,           // "mount" or "update"
  actualDuration,  // Time spent rendering
  baseDuration,    // Estimated time without memoization
  startTime,
  commitTime
) {
  console.log(\`[\${id}] \${phase}: \${actualDuration}ms\`);
}

function App() {
  return (
    <Profiler id="App" onRender={onRenderCallback}>
      <ExpensiveTree />
    </Profiler>
  );
}
\`\`\``,tech:"react",displayNumber:238,sortKey:1210},{id:"react-1211",number:239,title:"How do you identify and fix re-render performance issues?",question:"How do you identify and fix re-render performance issues?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Step-by-step approach:

1. **Profile** with React DevTools to find slow components
2. **Enable "Highlight updates"** to see what re-renders unnecessarily
3. **Check props**: Are new object/function references created each render?
4. **Wrap with \`React.memo\`** where props are stable
5. **Use \`useCallback\`/\`useMemo\`** for stable references
6. **Split context** so consumers only re-render when relevant data changes
7. **Use \`useTransition\`** for non-urgent updates

Avoid premature optimization - measure first, then fix the actual bottleneck.

\`\`\`jsx
// BEFORE: Re-renders on every parent state change
function Parent() {
  const [count, setCount] = useState(0);
  const [user] = useState({ name: 'John' });

  return (
    <>
      <button onClick={() => setCount(c => c + 1)}>{count}</button>
      <UserProfile user={user} />
    </>
  );
}

// AFTER: Memoize to prevent unnecessary re-renders
const UserProfile = memo(function UserProfile({ user }) {
  return <div>{user.name}</div>;
});
\`\`\``,tech:"react",displayNumber:239,sortKey:1211},{id:"react-1212",number:240,title:"Why are anonymous functions and inline objects problematic in JSX?",question:"Why are anonymous functions and inline objects problematic in JSX?",difficulty:"Basic",category:"Performance & Optimization",answer:`Anonymous functions and inline objects create new references on every render:

\`\`\`jsx
<Button onClick={() => doSomething()} />  // New function each render
<Button style={{ color: 'red' }} />        // New object each render
\`\`\`

When passed to memoized children, this defeats \`React.memo\` (shallow comparison sees different references) and triggers unnecessary re-renders.

Solutions:
- Extract functions with \`useCallback\`
- Extract objects with \`useMemo\`
- Define static objects/arrays outside the component

This matters most when the JSX is inside a frequently-rendering parent or passed to memoized children.

\`\`\`jsx
// BAD: New references every render
function Toolbar({ onSave }) {
  return (
    <div>
      <button onClick={() => onSave()}>Save</button>
      <button style={{ marginLeft: 8 }}>Cancel</button>
    </div>
  );
}

// GOOD: Stable references
const BUTTON_STYLE = { marginLeft: 8 };

function Toolbar({ onSave }) {
  const handleSave = useCallback(() => onSave(), [onSave]);
  return (
    <div>
      <button onClick={handleSave}>Save</button>
      <button style={BUTTON_STYLE}>Cancel</button>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:240,sortKey:1212},{id:"react-1213",number:241,title:"How do you detect and fix memory leaks in React components?",question:"How do you detect and fix memory leaks in React components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Common memory leak sources:
1. **Uncleared timers/intervals** - \`setInterval\` never cleared
2. **Uncleaned event listeners** - \`window.addEventListener\` without removal
3. **Pending async requests** - State updates after unmount
4. **Subscriptions** - WebSocket, observables not unsubscribed
5. **Global references** - Storing component instances in globals

Fix: Always clean up in \`useEffect\` return function. Use \`AbortController\` for fetch. Check \`isMounted\` ref pattern (or \`useRef\` guard).

\`\`\`jsx
useEffect(() => {
  let isMounted = true;
  const controller = new AbortController();

  fetch('/api/data', { signal: controller.signal })
    .then(res => res.json())
    .then(data => {
      if (isMounted) setData(data);  // Guard against unmount
    });

  const timer = setInterval(() => console.log('tick'), 1000);
  window.addEventListener('resize', handleResize);

  return () => {
    isMounted = false;
    controller.abort();         // Cancel fetch
    clearInterval(timer);       // Clear timer
    window.removeEventListener('resize', handleResize);
  };
}, []);
\`\`\``,tech:"react",displayNumber:241,sortKey:1213},{id:"react-1214",number:242,title:"What is the relationship between React state and memory leaks?",question:"What is the relationship between React state and memory leaks?",difficulty:"Advanced",category:"Performance & Optimization",answer:`The "Can't perform a React state update on an unmounted component" warning indicates a leak: async work completes after the component unmounts, attempting to set state on a destroyed component.

React 18 removed this warning (it's now harmless but wasteful), but the underlying issue remains - you're doing work for a component that no longer exists.

Best practice: Cancel async operations on unmount. For fetch, use \`AbortController\`. For timeouts, track and clear them. Modern approach: \`useSyncExternalStore\` handles subscription lifecycle automatically.

\`\`\`jsx
// React 18+ pattern with AbortController
function useUserData(userId) {
  const [user, setUser] = useState(null);

  useEffect(() => {
    const controller = new AbortController();
    fetch(\`/api/users/\${userId}\`, { signal: controller.signal })
      .then(res => res.json())
      .then(setUser)
      .catch(err => {
        if (err.name !== 'AbortError') throw err;
      });

    return () => controller.abort();  // Critical cleanup
  }, [userId]);

  return user;
}
\`\`\``,tech:"react",displayNumber:242,sortKey:1214},{id:"react-1215",number:243,title:"How do you optimize Context re-renders?",question:"How do you optimize Context re-renders?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context re-renders ALL consumers when the value changes. Optimizations:

1. **Split context**: Separate frequently-changing from stable values
2. **Selectors**: Use a library like \`use-context-selector\` to subscribe to slices
3. **Memoize context value**: \`useMemo\` the value object
4. **Move state down**: Keep context state minimal
5. **Combine with reducer**: Centralize but isolate updates

The core issue: every consumer re-renders when ANY part of context changes.

\`\`\`jsx
// BAD: Single context, all consumers re-render on count change
const AppContext = createContext();
function App() {
  const [user, setUser] = useState();
  const [count, setCount] = useState(0);
  return (
    <AppContext.Provider value={{ user, count, setUser, setCount }}>
      <Children />
    </AppContext.Provider>
  );
}

// GOOD: Split contexts
const UserContext = createContext();
const CountContext = createContext();

function App() {
  const [user] = useState();
  const [count, setCount] = useState(0);

  return (
    <UserContext.Provider value={user}>
      <CountContext.Provider value={{ count, setCount }}>
        <Children />
      </CountContext.Provider>
    </UserContext.Provider>
  );
}
\`\`\``,tech:"react",displayNumber:243,sortKey:1215},{id:"react-1216",number:244,title:"What are context selectors and how do they help performance?",question:"What are context selectors and how do they help performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context selectors let consumers subscribe to only the slice of context they need, preventing re-renders when unrelated parts change.

The native Context API doesn't support selectors - any context value change re-renders all consumers. Libraries like \`use-context-selector\` solve this by using a subscription model under the hood.

In React 18+, \`useSyncExternalStore\` enables selector-based subscriptions for external stores. For context specifically, \`use-context-selector\` is the popular solution.

\`\`\`jsx
import { createContext, useContextSelector } from 'use-context-selector';

const CounterContext = createContext();

function Display() {
  // Only re-renders when 'count' changes, not 'theme'
  const count = useContextSelector(CounterContext, v => v.count);
  return <div>{count}</div>;
}

function ThemeButton() {
  // Only re-renders when 'theme' changes
  const theme = useContextSelector(CounterContext, v => v.theme);
  return <button className={theme}>Themed</button>;
}
\`\`\``,tech:"react",displayNumber:244,sortKey:1216},{id:"react-1217",number:245,title:"How do you optimize images in React web applications?",question:"How do you optimize images in React web applications?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Image optimization strategies:

1. **Use modern formats**: WebP, AVIF instead of PNG/JPG (30-50% smaller)
2. **Lazy loading**: \`loading="lazy"\` attribute
3. **Responsive images**: \`srcset\` and \`sizes\` attributes
4. **Next.js Image component**: Automatic optimization, lazy loading, resizing
5. **Placeholder/blur-up**: Show low-res while loading
6. **CDN**: Serve images from optimized CDN with caching
7. **Avoid layout shift**: Always set \`width\` and \`height\`

\`\`\`jsx
// Native HTML
<img
  src="image.webp"
  loading="lazy"
  width={800}
  height={600}
  alt="Description"
  srcSet="image-400.webp 400w, image-800.webp 800w"
  sizes="(max-width: 600px) 400px, 800px"
/>

// Next.js
import Image from 'next/image';
<Image
  src="/photo.jpg"
  alt="Description"
  width={800}
  height={600}
  placeholder="blur"
  blurDataURL="data:image/..."
  priority={false}  // true for LCP images
/>
\`\`\``,tech:"react",displayNumber:245,sortKey:1217},{id:"react-1218",number:246,title:"What are Core Web Vitals and why do they matter?",question:"What are Core Web Vitals and why do they matter?",difficulty:"Basic",category:"Performance & Optimization",answer:`Core Web Vitals are Google's metrics for user experience:

1. **LCP (Largest Contentful Paint)**: Loading performance. Should be < 2.5s
2. **INP (Interaction to Next Paint)** (replaced FID in 2024): Responsiveness. Should be < 200ms
3. **CLS (Cumulative Layout Shift)**: Visual stability. Should be < 0.1

They affect SEO rankings and user experience. React apps can struggle with these due to client-side rendering delays and hydration.

Measure with Lighthouse, Chrome DevTools, or \`web-vitals\` library.

\`\`\`jsx
import { onCLS, onINP, onLCP } from 'web-vitals';

function sendToAnalytics(metric) {
  console.log(metric.name, metric.value);
}

onLCP(sendToAnalytics);  // Largest Contentful Paint
onINP(sendToAnalytics);  // Interaction to Next Paint
onCLS(sendToAnalytics);  // Cumulative Layout Shift
\`\`\``,tech:"react",displayNumber:246,sortKey:1218},{id:"react-1219",number:247,title:"How do you optimize LCP (Largest Contentful Paint) in React?",question:"How do you optimize LCP (Largest Contentful Paint) in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`LCP measures when the largest content element (image, text block) renders. Optimize:

1. **Server-side rendering (SSR)**: Render initial HTML on server
2. **Preload critical resources**: \`<link rel="preload">\` for hero images/fonts
3. **Optimize hero images**: Use WebP/AVIF, proper sizing, CDN
4. **Code splitting**: Reduce JS blocking the main thread
5. **Remove render-blocking CSS/JS**
6. **Prioritize LCP element**: Set \`priority\` prop on Next.js Image
7. **Fast fonts**: \`font-display: swap\`, preload font files

\`\`\`jsx
// Next.js: prioritize LCP image
import Image from 'next/image';

function Hero() {
  return (
    <Image
      src="/hero.jpg"
      alt="Hero"
      priority          // Preloads and prioritizes
      width={1200}
      height={600}
    />
  );
}

// Manual preload in head
<head>
  <link rel="preload" as="image" href="/hero.webp" />
  <link rel="preload" as="font" href="/font.woff2" type="font/woff2" crossOrigin="" />
</head>
\`\`\``,tech:"react",displayNumber:247,sortKey:1219},{id:"react-1220",number:248,title:"What is INP and how does it differ from FID?",question:"What is INP and how does it differ from FID?",difficulty:"Advanced",category:"Performance & Optimization",answer:`- **FID (First Input Delay)**: Measures delay for first user interaction only. Deprecated in 2024.
- **INP (Interaction to Next Paint)**: Measures responsiveness across ALL interactions throughout page lifecycle. It's the 98th percentile of interaction latency.

INP captures the full interaction lifecycle: input delay + processing time + render time.

Optimize INP:
1. **Break up long tasks** with \`useDeferredValue\`/\`useTransition\`
2. **Move work off main thread** (Web Workers)
3. **Avoid expensive re-renders** with memoization
4. **Debounce/throttle** non-urgent updates

\`\`\`jsx
import { useState, useTransition } from 'react';

function SearchResults({ query }) {
  const [isPending, startTransition] = useTransition();
  const [results, setResults] = useState([]);

  function handleChange(e) {
    const value = e.target.value;
    // Mark expensive update as non-urgent
    startTransition(() => {
      setResults(heavyFilter(value));  // Doesn't block input
    });
  }

  return (
    <div className={isPending ? 'opacity-50' : ''}>
      {/* Results */}
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:248,sortKey:1220},{id:"react-1221",number:249,title:"How do you minimize CLS (Cumulative Layout Shift) in React?",question:"How do you minimize CLS (Cumulative Layout Shift) in React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`CLS measures unexpected layout shifts. Common React causes:

1. **Images without dimensions**: Browser doesn't reserve space
2. **Async content loading**: Ads, embeds, fonts popping in
3. **Late-rendering components**: Conditional rendering without placeholders
4. **Web fonts**: FOIT/FOUT causing text reflow

Fixes:
- Always set \`width\`/\`height\` on images
- Reserve space with \`min-height\` or skeleton loaders
- Use \`font-display: swap\` with \`size-adjust\`
- Avoid inserting content above existing content

\`\`\`jsx
// BAD: No dimensions, causes shift
<img src="/banner.jpg" alt="Banner" />

// GOOD: Reserve space
<img src="/banner.jpg" alt="Banner" width={800} height={200} />

// Skeleton prevents shift for async content
function ProfileCard() {
  const [user, setUser] = useState(null);
  return user ? (
    <div>{user.name}</div>
  ) : (
    <div style={{ height: 100 }} className="skeleton" />
  );
}
\`\`\``,tech:"react",displayNumber:249,sortKey:1221},{id:"react-1222",number:250,title:"What is useMemo and when should you use it?",question:"What is useMemo and when should you use it?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`useMemo\` memoizes a computed value, recomputing only when dependencies change. It prevents expensive calculations on every render.

Use when:
- Expensive calculations (sorting, filtering large arrays)
- Referential equality needed (passing to memoized children)
- Creating objects that must be stable

Don't use for:
- Cheap calculations (overhead isn't worth it)
- As a substitute for state
- Every value "just in case" (premature optimization)

\`\`\`jsx
import { useMemo } from 'react';

function ProductList({ products, searchTerm }) {
  // Only recomputes when products or searchTerm change
  const filtered = useMemo(() => {
    return products.filter(p =>
      p.name.toLowerCase().includes(searchTerm.toLowerCase())
    );
  }, [products, searchTerm]);

  return <List items={filtered} />;
}
\`\`\``,tech:"react",displayNumber:250,sortKey:1222},{id:"react-1223",number:251,title:"What is useCallback and how does it relate to useMemo?",question:"What is useCallback and how does it relate to useMemo?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`useCallback\` memoizes a function reference. It's equivalent to \`useMemo(() => fn, deps)\`. Returns the same function instance across renders when deps are unchanged.

Primary use: Pass stable function references to memoized children (so they don't re-render) and to effects that depend on stable callbacks.

\`\`\`jsx
// These are equivalent
const fn1 = useCallback(() => doSomething(a), [a]);
const fn2 = useMemo(() => () => doSomething(a), [a]);
\`\`\`

\`\`\`jsx
import { useCallback, memo } from 'react';

const Button = memo(function Button({ onClick, children }) {
  return <button onClick={onClick}>{children}</button>;
});

function Toolbar() {
  const [count, setCount] = useState(0);

  // Without useCallback, Button re-renders every time count changes
  const handleClick = useCallback(() => {
    console.log('clicked');
  }, []);

  return (
    <>
      <Button onClick={handleClick}>Click</Button>
      <span>{count}</span>
    </>
  );
}
\`\`\``,tech:"react",displayNumber:251,sortKey:1223},{id:"react-1224",number:252,title:"What is the difference between useMemo and useCallback?",question:"What is the difference between useMemo and useCallback?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both are memoization hooks, but differ in what they return:

- **useMemo**: Returns a **memoized value** (result of computation)
- **useCallback**: Returns a **memoized function** (the function itself)

Implementation: \`useCallback(fn, deps)\` is literally \`useMemo(() => fn, deps)\`.

Use \`useMemo\` for expensive calculations and stable object/array references. Use \`useCallback\` for stable function references passed to children or effects.

\`\`\`jsx
// useMemo - memoizes the VALUE
const sortedList = useMemo(() => list.sort(), [list]);

// useCallback - memoizes the FUNCTION
const handleSort = useCallback(() => setList(list.sort()), [list]);

// Equivalent to:
const handleSort = useMemo(() => () => setList(list.sort()), [list]);
\`\`\``,tech:"react",displayNumber:252,sortKey:1224},{id:"react-1225",number:253,title:"What are the pitfalls of over-using useMemo and useCallback?",question:"What are the pitfalls of over-using useMemo and useCallback?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Common pitfalls:

1. **Premature optimization**: Memoization has overhead; for cheap operations it's slower
2. **Incorrect dependencies**: Stale closures if deps are wrong
3. **False sense of security**: Doesn't prevent re-renders from other causes
4. **Memory usage**: Cached values persist in memory
5. **Breaks when deps change**: Spurious re-renders if deps are unstable

Rule: Profile first. Only memoize when you've measured a problem. The React docs say: "You can wrap [a calculation] in useMemo... But you should only do this if the calculation is slow."

\`\`\`jsx
// BAD: Memoizing trivial work
const doubled = useMemo(() => count * 2, [count]);  // Overkill

// BAD: Wrong deps cause bugs
const handler = useCallback(() => {
  console.log(count);  // Stale! count not in deps
}, []);

// GOOD: Only memoize expensive work
const sortedUsers = useMemo(() => {
  return users.sort(complexSortFn);  // 10k+ items
}, [users]);
\`\`\``,tech:"react",displayNumber:253,sortKey:1225},{id:"react-1226",number:254,title:"How does React's concurrent rendering improve performance?",question:"How does React's concurrent rendering improve performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Concurrent rendering (React 18+) allows React to:

1. **Interrupt rendering**: Pause/resume render work based on priority
2. **Prevent blocking**: User input stays responsive during large renders
3. **Suspense integration**: Render parts of UI while waiting for data

Key APIs:
- \`useTransition\`: Mark non-urgent updates (stay responsive)
- \`useDeferredValue\`: Defer expensive value updates
- \`<Suspense>\`: Show fallbacks for async work

This means React can work on multiple states simultaneously without blocking the main thread.

\`\`\`jsx
import { useState, useDeferredValue, useTransition } from 'react';

function SearchPage() {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();

  // Defer expensive list update
  const deferredQuery = useDeferredValue(query);

  function onChange(e) {
    const value = e.target.value;
    setQuery(value);  // Urgent - updates input immediately
    startTransition(() => {
      // Non-urgent - can be interrupted
      performSearch(value);
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      <Results query={deferredQuery} pending={isPending} />
    </>
  );
}
\`\`\``,tech:"react",displayNumber:254,sortKey:1226},{id:"react-1227",number:255,title:"What is useTransition and how does it help with performance?",question:"What is useTransition and how does it help with performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useTransition\` lets you mark state updates as non-urgent ("transitions"), keeping the UI responsive during expensive re-renders.

Returns: \`[isPending, startTransition]\`.
- \`isPending\`: True while transition is in progress
- \`startTransition\`: Wrap state updates that can be deferred

Urgent updates (typing, clicking) render immediately. Non-urgent updates (filtering large lists, tab switches) can be interrupted by more urgent work.

\`\`\`jsx
import { useState, useTransition } from 'react';

function Tabs() {
  const [tab, setTab] = useState('overview');
  const [isPending, startTransition] = useTransition();

  function selectTab(nextTab) {
    startTransition(() => {
      setTab(nextTab);  // Non-urgent update
    });
  }

  return (
    <>
      <TabBar onSelect={selectTab} />
      {isPending ? <Spinner /> : <TabContent tab={tab} />}
    </>
  );
}
\`\`\``,tech:"react",displayNumber:255,sortKey:1227},{id:"react-1228",number:256,title:"What is useDeferredValue and when should you use it?",question:"What is useDeferredValue and when should you use it?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useDeferredValue\` returns a deferred copy of a value that lags behind the latest value during expensive renders. Unlike \`useTransition\` which wraps updates, \`useDeferredValue\` wraps the value itself.

Use when:
- You can't wrap the state update in \`startTransition\` (e.g., third-party state)
- You want to keep showing stale data while new data renders
- The value comes from props or external sources

It's essentially "useTransition for values" - React keeps old value visible while computing new one in background.

\`\`\`jsx
import { useState, useDeferredValue } from 'react';

function SearchResults({ query }) {
  const deferredQuery = useDeferredValue(query);
  const results = useMemo(() => {
    return search(deferredQuery);  // Uses stale value during render
  }, [deferredQuery]);

  // Input stays responsive because query updates immediately
  // Results update after a delay
  return (
    <ul>
      {results.map(r => <li key={r.id}>{r.name}</li>)}
    </ul>
  );
}
\`\`\``,tech:"react",displayNumber:256,sortKey:1228},{id:"react-1229",number:257,title:"How do you optimize a large form with many inputs?",question:"How do you optimize a large form with many inputs?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Large forms re-render the entire tree on each keystroke. Optimizations:

1. **Controlled vs uncontrolled**: Use uncontrolled inputs (\`defaultValue\` + ref) for simple cases
2. **Field-level state**: Each input manages its own state
3. **Split components**: Isolate state to sub-components
4. **memo each field**: Prevent re-render when other fields change
5. **Debounce validation**: Don't validate on every keystroke
6. **React Hook Form**: Uses uncontrolled inputs + refs (minimal re-renders)
7. **useDeferredValue**: Defer expensive validation

\`\`\`jsx
import { memo, useState } from 'react';
import { useForm } from 'react-hook-form';

// With React Hook Form (uncontrolled, minimal re-renders)
function Form() {
  const { register, handleSubmit } = useForm();
  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      <input {...register('name')} />
      <input {...register('email')} />
    </form>
  );
}

// Manual: memoize each field
const Field = memo(function Field({ label, value, onChange }) {
  return (
    <label>
      {label}
      <input value={value} onChange={onChange} />
    </label>
  );
});
\`\`\``,tech:"react",displayNumber:257,sortKey:1229},{id:"react-1230",number:258,title:"How does React's reconciliation algorithm affect performance?",question:"How does React's reconciliation algorithm affect performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`React's reconciliation (the "diffing" algorithm) determines what changed between renders. Key performance aspects:

1. **Element type check**: Different types → destroy and rebuild (expensive)
2. **Same type**: Update props in place (cheap)
3. **Keys**: Help identify list items across renders
4. **Children diffing**: O(n) with heuristics, not optimal O(n^3)

Performance implications:
- Avoid changing element types at same position (destroys subtree)
- Use stable, unique \`key\` props in lists
- Keep component identity stable (don't create components inside render)

\`\`\`jsx
// BAD: Changing element type destroys entire subtree
function Widget({ isButton }) {
  return isButton ? <button>Click</button> : <a href="#">Link</a>;
}

// GOOD: Keep element type stable
function Widget({ isButton }) {
  return isButton
    ? <button className="widget">Click</button>
    : <button className="widget link-style">Link</button>;
}

// Keys must be stable and unique
items.map(item => <li key={item.id}>{item.text}</li>);  // Good
items.map((item, i) => <li key={i}>{item.text}</li>);   // Bad - unstable
\`\`\``,tech:"react",displayNumber:258,sortKey:1230},{id:"react-1231",number:259,title:"Why is it bad to define components inside other components?",question:"Why is it bad to define components inside other components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Defining a component inside another component creates a NEW component type on every render. This causes:

1. **Full remount**: React sees a different component type, destroys and recreates it (loses state)
2. **Performance hit**: Re-mounting is expensive (DOM recreation, effect re-runs)
3. **State loss**: Internal state resets on every parent render
4. **Effect re-runs**: All effects re-execute

Fix: Define components at module level or use composition/children prop.

\`\`\`jsx
// BAD: New component type every render
function Parent() {
  function Child({ name }) {  // Re-created each render
    return <div>{name}</div>;
  }
  return <Child name="John" />;  // Fully remounts on parent re-render
}

// GOOD: Module-level component
function Child({ name }) {
  return <div>{name}</div>;
}

function Parent() {
  return <Child name="John" />;
}
\`\`\``,tech:"react",displayNumber:259,sortKey:1231},{id:"react-1232",number:260,title:"How do you handle expensive calculations in render?",question:"How do you handle expensive calculations in render?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`For expensive calculations during render:

1. **useMemo**: Cache result, recompute only when deps change
2. **useDeferredValue**: Show stale result while computing new one
3. **Web Workers**: Move heavy computation off main thread
4. **Memoize intermediate steps**: Break computation into memoized chunks
5. **Virtualize**: Only compute visible items
6. **Debounce**: Delay calculation until input settles

Measure first with Profiler to confirm the calculation is actually the bottleneck.

\`\`\`jsx
import { useMemo } from 'react';

function Analytics({ data }) {
  // Expensive: runs only when data changes
  const stats = useMemo(() => {
    return data.reduce((acc, item) => {
      // Heavy computation here
      return computeDetailedStats(acc, item);
    }, initialStats);
  }, [data]);

  return <StatsDisplay stats={stats} />;
}

// Alternative: Web Worker for very heavy work
const result = useWorker(heavyComputation, inputData);
\`\`\``,tech:"react",displayNumber:260,sortKey:1232},{id:"react-1233",number:261,title:"What is the purpose of the key prop and how does it affect performance?",question:"What is the purpose of the key prop and how does it affect performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`The \`key\` prop helps React identify which items in a list changed, were added, or removed. Without keys, React uses index-based diffing which is error-prone.

Performance impact:
- **Stable unique keys** (e.g., database IDs): Optimal reconciliation, minimal DOM operations
- **Index keys**: Causes unnecessary re-renders and bugs when list order changes
- **Missing keys**: Forces full re-render of list

Keys must be stable across renders (not random, not array index when items reorder).

\`\`\`jsx
// GOOD: Stable unique identifier
{todos.map(todo => (
  <TodoItem key={todo.id} todo={todo} />
))}

// BAD: Array index as key (breaks on reorder/insert)
{todos.map((todo, index) => (
  <TodoItem key={index} todo={todo} />
))}

// BAD: Random key (always remounts)
{todos.map(todo => (
  <TodoItem key={Math.random()} todo={todo} />
))}
\`\`\``,tech:"react",displayNumber:261,sortKey:1233},{id:"react-1234",number:262,title:"How do you optimize bundle size in a React app?",question:"How do you optimize bundle size in a React app?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Bundle size optimization techniques:

1. **Code splitting**: \`React.lazy\` + \`Suspense\` for routes/features
2. **Tree shaking**: Use ES modules, avoid side-effectful imports
3. **Bundle analyzer**: Identify large dependencies
4. **Replace heavy libs**: \`date-fns\` instead of \`moment\`, \`lodash-es\` with specific imports
5. **Compression**: Gzip/Brotli on server
6. **Modern build target**: Target modern browsers, use differential loading
7. **Remove unused code**: Dead code elimination

\`\`\`jsx
// BAD: Imports entire library
import _ from 'lodash';

// GOOD: Import only what you need
import debounce from 'lodash/debounce';

// Or use tree-shakeable lodash-es
import { debounce } from 'lodash-es';

// Analyze bundle
// npx webpack-bundle-analyzer stats.json
// or: npm install --save-dev webpack-bundle-analyzer
\`\`\``,tech:"react",displayNumber:262,sortKey:1234},{id:"react-1235",number:263,title:"What is tree shaking and how does it work with React?",question:"What is tree shaking and how does it work with React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Tree shaking is dead-code elimination that removes unused exports from the final bundle. It works with ES modules (\`import\`/\`export\`) because they're statically analyzable, but NOT with CommonJS (\`require\`).

Requirements:
- Use ES module syntax (\`import\`/\`export\`)
- Build tool must support it (webpack, Rollup, Vite all do)
- Avoid side effects in modules (\`"sideEffects": false\` in package.json)
- Don't use \`import * as\` when you need specific parts

React itself is tree-shakeable in modern versions.

\`\`\`jsx
// Tree-shakeable: only DatePicker imported
import { DatePicker } from 'antd';

// NOT tree-shakeable (default import, pulls everything)
import antd from 'antd';

// package.json
{
  "sideEffects": false  // Enables aggressive tree shaking
}

// Build config (webpack)
optimization: {
  usedExports: true,
}
\`\`\``,tech:"react",displayNumber:263,sortKey:1235},{id:"react-1236",number:264,title:"How do you measure React app performance?",question:"How do you measure React app performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`Measurement tools and techniques:

1. **React DevTools Profiler**: Component render times, re-render reasons
2. **Chrome DevTools Performance tab**: Main thread activity, long tasks
3. **Lighthouse**: Core Web Vitals, best practices
4. **web-vitals library**: Programmatic metric collection
5. **why-did-you-render**: Detect unnecessary re-renders
6. **Bundle analyzer**: Visualize bundle composition
7. **Performance API**: \`performance.now()\` for custom timing

Always measure in production build (dev has overhead) and on real devices.

\`\`\`jsx
// Install why-did-you-render for dev
// npm i -D @welldone-software/why-did-you-render

import whyDidYouRender from '@welldone-software/why-did-you-render';
whyDidYouRender(React, { trackAllPureComponents: true });

// Programmatic timing
const start = performance.now();
heavyOperation();
const duration = performance.now() - start;
console.log(\`Operation took \${duration}ms\`);
\`\`\``,tech:"react",displayNumber:264,sortKey:1236},{id:"react-1237",number:265,title:"What is the performance impact of CSS-in-JS libraries?",question:"What is the performance impact of CSS-in-JS libraries?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Runtime CSS-in-JS (styled-components, emotion) has performance costs:

1. **Runtime serialization**: Styles computed during render
2. **Re-renders**: Style recalculation on every render
3. **Bundle size**: Library code adds weight

Mitigations:
- Use \`babel-plugin-styled-components\` for static extraction
- Use compiled CSS-in-JS (vanilla-extract, linaria) - zero runtime
- Use CSS modules for static styles
- Avoid dynamic styles based on props when possible

CSS-in-JS tradeoff: developer experience vs runtime performance. For high-performance needs, prefer static CSS or build-time solutions.

\`\`\`jsx
// Runtime CSS-in-JS - recalculated each render
const Button = styled.button\`
  color: \${props => props.color};  // Dynamic, runtime cost
\`;

// Build-time (vanilla-extract) - zero runtime
import { style } from '@vanilla-extract/css';

const button = style({
  color: 'red',  // Static, extracted at build
});

// CSS Modules - also zero runtime
import styles from './Button.module.css';
<button className={styles.button} />;
\`\`\``,tech:"react",displayNumber:265,sortKey:1237},{id:"react-1238",number:266,title:"How do you optimize React apps for low-end devices?",question:"How do you optimize React apps for low-end devices?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Low-end device optimizations:

1. **Reduce JS execution**: Smaller bundles, code splitting
2. **Avoid long tasks**: Break work with \`useTransition\`/\`scheduler\`
3. **Virtualize lists**: Limit DOM nodes
4. **Debounce/throttle**: Reduce event handler frequency
5. **Lazy load heavy features**: Only load when needed
6. **Minimize re-renders**: Aggressive memoization
7. **Use passive event listeners**: \`{ passive: true }\`
8. **Optimize images**: Smaller formats, responsive sizes
9. **Avoid layout thrashing**: Batch DOM reads/writes

\`\`\`jsx
// Passive listeners for scroll
useEffect(() => {
  const handler = () => console.log('scroll');
  window.addEventListener('scroll', handler, { passive: true });
  return () => window.removeEventListener('scroll', handler);
}, []);

// Reduce work with transitions
const [isPending, startTransition] = useTransition();
startTransition(() => setHeavyState(data));

// Virtualize for low memory
<VirtualizedList items={items} />  // Renders only visible
\`\`\``,tech:"react",displayNumber:266,sortKey:1238},{id:"react-1239",number:267,title:"What is the difference between useMemo for values vs React.memo for components?",question:"What is the difference between useMemo for values vs React.memo for components?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both prevent unnecessary work, but at different levels:

- **React.memo**: Component-level. Skips re-rendering the entire component when props are shallow-equal. Changes the render decision for the component.
- **useMemo**: Calculation-level. Caches a computed value within a component across renders. The component still renders, but skips the expensive calculation.

They're complementary: \`React.memo\` prevents the render, \`useMemo\` makes the render cheaper.

\`\`\`jsx
// React.memo: prevents Child from re-rendering
const Child = memo(function Child({ data }) {
  return <ExpensiveView data={data} />;
});

// useMemo: makes calculation cheaper when Child DOES render
function ExpensiveView({ data }) {
  const processed = useMemo(() => {
    return data.map(transform).filter(predicate);
  }, [data]);

  return <List items={processed} />;
}
\`\`\``,tech:"react",displayNumber:267,sortKey:1239},{id:"react-1240",number:268,title:"How do you debounce and throttle in React for performance?",question:"How do you debounce and throttle in React for performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`**Debounce**: Delays execution until after a pause (good for search input).
**Throttle**: Limits execution to once per interval (good for scroll/resize).

In React, create stable debounced/throttled functions with \`useRef\` or \`useMemo\` to avoid recreating them each render.

Use cases:
- Search-as-you-type (debounce 300ms)
- Window resize handlers (throttle)
- Scroll position tracking (throttle)
- Auto-save (debounce)

\`\`\`jsx
import { useMemo } from 'react';
import { debounce, throttle } from 'lodash-es';

function SearchBox() {
  const [results, setResults] = useState([]);

  // Stable debounced function
  const debouncedSearch = useMemo(
    () => debounce(query => fetchResults(query), 300),
    []
  );

  function onChange(e) {
    debouncedSearch(e.target.value);
  }

  // Throttled scroll handler
  const handleScroll = useMemo(
    () => throttle(() => console.log('scroll'), 100),
    []
  );

  useEffect(() => {
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, [handleScroll]);

  return <input onChange={onChange} />;
}
\`\`\``,tech:"react",displayNumber:268,sortKey:1240},{id:"react-1241",number:269,title:"How do you optimize list rendering in React?",question:"How do you optimize list rendering in React?",difficulty:"Basic",category:"Performance & Optimization",answer:`List rendering optimization techniques:

1. **Virtualization**: Render only visible items (react-window)
2. **memo list items**: Prevent re-render when other items change
3. **Stable keys**: Use unique IDs, not array indices
4. **windowing for large data**: Process in chunks
5. **Avoid inline functions in map**: Extract callbacks
6. **Pagination**: Limit items rendered at once
7. **useMemo for derived data**: Filter/sort once

\`\`\`jsx
import { memo, useMemo } from 'react';

const ListItem = memo(function ListItem({ item, onSelect }) {
  return <li onClick={() => onSelect(item.id)}>{item.name}</li>;
});

function OptimizedList({ items, onSelect }) {
  const handleSelect = useCallback((id) => onSelect(id), [onSelect]);

  const sortedItems = useMemo(
    () => [...items].sort((a, b) => a.name.localeCompare(b.name)),
    [items]
  );

  return (
    <ul>
      {sortedItems.map(item => (
        <ListItem key={item.id} item={item} onSelect={handleSelect} />
      ))}
    </ul>
  );
}
\`\`\``,tech:"react",displayNumber:269,sortKey:1241},{id:"react-1242",number:270,title:"What is the role of React.StrictMode in performance?",question:"What is the role of React.StrictMode in performance?",difficulty:"Basic",category:"Performance & Optimization",answer:`\`React.StrictMode\` is a development-only tool that intentionally double-invokes certain functions to surface bugs:
- Function component bodies (render)
- useState/useMemo/useReducer initializers
- Effect setup/cleanup (runs twice)

It does NOT affect production. The double-invocation helps detect:
- Impure renders (side effects in render)
- Missing effect cleanup (memory leaks)
- Stale closures

Performance note: Your code should run correctly under double-invocation. If it breaks, you have a side-effect bug. Don't disable StrictMode to "fix" performance - fix the underlying issue.

\`\`\`jsx
import { StrictMode } from 'react';

// Wrap app in StrictMode for dev checks
<StrictMode>
  <App />
</StrictMode>

// Effects run twice in dev - must be idempotent
useEffect(() => {
  const subscription = subscribe();
  return () => subscription.unsubscribe();  // Cleanup prevents double-subscribe
}, []);
\`\`\``,tech:"react",displayNumber:270,sortKey:1242},{id:"react-1243",number:271,title:"How does server-side rendering (SSR) improve performance?",question:"How does server-side rendering (SSR) improve performance?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`SSR renders React components to HTML on the server, sending complete HTML to the client. Benefits:

1. **Faster FCP/LCP**: Content visible before JS loads
2. **Better SEO**: Crawlers see rendered content
3. **No blank screen**: Users see content immediately
4. **Lower TTI on slow devices**: Less client computation

Tradeoffs:
- Higher server load
- Slower TTFB (server computation)
- Complexity (hydration)

Modern alternatives: SSG (static generation), ISR (incremental static regeneration), Streaming SSR.

\`\`\`jsx
// Express + React SSR
import { renderToString } from 'react-dom/server';
import App from './App';

app.get('/', (req, res) => {
  const html = renderToString(<App />);
  res.send(\`<!DOCTYPE html><html><body>\${html}</body></html>\`);
});

// Next.js handles SSR automatically
export async function getServerSideProps() {
  const data = await fetchData();
  return { props: { data } };
}
\`\`\``,tech:"react",displayNumber:271,sortKey:1243},{id:"react-1244",number:272,title:"What is hydration and how can it be optimized?",question:"What is hydration and how can it be optimized?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Hydration is React "attaching" event listeners to server-rendered HTML, making it interactive. The problem: hydration is synchronous and blocks the main thread, delaying interactivity (hurts TTI/INP).

Optimizations:
1. **Streaming SSR**: Send HTML in chunks with \`<Suspense>\`
2. **Selective hydration** (React 18): Hydrate parts as they arrive
3. **Progressive hydration**: Hydrate on idle/visible
4. **Defer non-critical JS**: Don't load admin components initially
5. **\`hydrationMismatch\` handling**: Avoid SSR/client mismatches

\`\`\`jsx
// Next.js App Router - streaming with Suspense
import { Suspense } from 'react';

export default function Page() {
  return (
    <div>
      <h1>Static Header</h1>
      <Suspense fallback={<Skeleton />}>
        <SlowComponent />  {/* Streams in separately */}
      </Suspense>
    </div>
  );
}

// Selective hydration happens automatically in React 18
// for Suspense boundaries
\`\`\``,tech:"react",displayNumber:272,sortKey:1244},{id:"react-1245",number:273,title:"How do you use the useId hook for performance and accessibility?",question:"How do you use the useId hook for performance and accessibility?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`\`useId\` generates stable, unique IDs for accessibility attributes (aria-labelledby, htmlFor/id pairs) without causing hydration mismatches.

Before \`useId\`, developers used \`Math.random()\` or \`Date.now()\` which caused SSR/client mismatches (different IDs each render → hydration error).

It's performant because React generates IDs deterministically during render - no Math.random, no flaky hydration.

\`\`\`jsx
import { useId } from 'react';

function FormField({ label, type }) {
  const id = useId();  // Stable across SSR + hydration

  return (
    <div>
      <label htmlFor={id}>{label}</label>
      <input id={id} type={type} />
    </div>
  );
}

// Avoid this (hydration mismatch):
const id = \`field-\${Math.random()}\`;  // BAD - different on server/client
\`\`\``,tech:"react",displayNumber:273,sortKey:1245},{id:"react-1246",number:274,title:"What is the performance cost of Context vs Redux/Zustand?",question:"What is the performance cost of Context vs Redux/Zustand?",difficulty:"Advanced",category:"Performance & Optimization",answer:`**Context**: Every consumer re-renders on ANY value change. No selector support natively. Fine for low-frequency updates (theme, auth) but problematic for high-frequency state (forms, real-time data).

**Redux**: Uses reference equality + selectors. Components only re-render when their selected slice changes. More boilerplate but better performance for complex state.

**Zustand**: Like Redux but minimal. Uses selector subscriptions - components subscribe to specific state slices, re-render only on those changes. No provider needed.

Verdict: Context for static/low-frequency; Zustand/Redux for dynamic/high-frequency state.

\`\`\`jsx
// Zustand - selector-based, only re-renders on slice change
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  theme: 'dark',
  increment: () => set(s => ({ count: s.count + 1 })),
}));

// Only re-renders when count changes
function Counter() {
  const count = useStore(s => s.count);
  return <div>{count}</div>;
}

// Only re-renders when theme changes
function ThemeButton() {
  const theme = useStore(s => s.theme);
  return <button className={theme}>Theme</button>;
}
\`\`\``,tech:"react",displayNumber:274,sortKey:1246},{id:"react-1247",number:275,title:"How do you optimize React apps for SEO and performance simultaneously?",question:"How do you optimize React apps for SEO and performance simultaneously?",difficulty:"Advanced",category:"Performance & Optimization",answer:`SEO and performance align through fast-loading, crawlable content:

1. **SSR/SSG**: Render content server-side for crawlers + fast FCP
2. **Core Web Vitals**: Good LCP/INP/CLS = better rankings
3. **Semantic HTML**: Proper headings, meta tags, structured data
4. **Preload critical resources**: Faster LCP helps SEO
5. **Sitemap + robots.txt**: Help crawlers
6. **Meta tags**: Title, description, Open Graph
7. **Avoid client-only rendering** for important content

Next.js solves both with SSR, metadata API, and image optimization.

\`\`\`jsx
// Next.js Metadata API (App Router)
export const metadata = {
  title: 'Product Page',
  description: 'Best product ever',
  openGraph: {
    title: 'Product Page',
    images: ['/og-image.jpg'],
  },
};

// SSG for static content (fast + SEO-friendly)
export async function generateStaticParams() {
  const products = await getProducts();
  return products.map(p => ({ id: p.id }));
}
\`\`\``,tech:"react",displayNumber:275,sortKey:1247},{id:"react-1248",number:276,title:"What are web workers and when should you use them with React?",question:"What are web workers and when should you use them with React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Web Workers run JavaScript in a separate thread, keeping the main thread (UI) responsive. Use them for:

- Heavy computations (sorting, parsing, encryption)
- Data processing (CSV/JSON transformation)
- Image/video processing
- Real-time data transforms

React integration: Use \`useWorker\` patterns or libraries like \`comlink\`. Pass serializable data only (no functions/DOM).

The main thread stays free for rendering and user input while workers crunch data.

\`\`\`jsx
// worker.js
self.onmessage = (e) => {
  const result = heavyCompute(e.data);
  self.postMessage(result);
};

// React component
import { useEffect, useState, useRef } from 'react';

function HeavyComponent() {
  const [result, setResult] = useState(null);
  const workerRef = useRef(null);

  useEffect(() => {
    workerRef.current = new Worker('/worker.js');
    workerRef.current.onmessage = (e) => setResult(e.data);
    return () => workerRef.current.terminate();
  }, []);

  function process(data) {
    workerRef.current.postMessage(data);
  }

  return <div>{result}</div>;
}
\`\`\``,tech:"react",displayNumber:276,sortKey:1248},{id:"react-1249",number:277,title:"How do you profile and reduce long tasks?",question:"How do you profile and reduce long tasks?",difficulty:"Advanced",category:"Performance & Optimization",answer:`**Long tasks** (>50ms) block the main thread, hurting INP. To identify:

1. Chrome DevTools Performance tab - look for red "long task" markers
2. \`PerformanceObserver\` API for \`longtask\` entries
3. React Profiler - find slow components

Reduction strategies:
- **Break up work**: Process in chunks with \`requestIdleCallback\`
- **useTransition**: Mark non-urgent updates interruptible
- **Web Workers**: Move computation off main thread
- **Code splitting**: Reduce initial JS parse time
- **Yield to browser**: Split loops with \`setTimeout(0)\` or scheduler

\`\`\`jsx
// Detect long tasks
const observer = new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    console.log(\`Long task: \${entry.duration}ms\`);
  }
});
observer.observe({ entryTypes: ['longtask'] });

// Break up loop with yielding
async function processLargeArray(items) {
  const results = [];
  for (let i = 0; i < items.length; i++) {
    results.push(process(items[i]));
    if (i % 100 === 0) {
      await new Promise(resolve => setTimeout(resolve));  // Yield
    }
  }
  return results;
}
\`\`\``,tech:"react",displayNumber:277,sortKey:1249},{id:"react-1250",number:278,title:"How do you handle expensive third-party library integrations?",question:"How do you handle expensive third-party library integrations?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Third-party libs (charts, editors, maps) can be heavy and slow. Optimizations:

1. **Lazy load**: \`React.lazy\` so they load on demand
2. **Dynamic import**: Only load when feature is used
3. **Isolate in iframe/web worker**: Prevent blocking main thread
4. **Memoize instances**: Create once, reuse
5. **Virtualize**: For data-heavy widgets (charts with many points)
6. **Debounce updates**: Avoid re-initializing on every change
7. **Web Worker**: For data processing before sending to lib

\`\`\`jsx
import { lazy, Suspense, useMemo } from 'react';
import { useRef, useEffect } from 'react';

// Lazy load heavy chart library
const Chart = lazy(() => import('recharts').then(m => ({ default: m.LineChart })));

function Dashboard() {
  const chartRef = useRef(null);

  // Initialize once, update data without re-init
  useEffect(() => {
    const chart = initChart(chartRef.current);
    return () => chart.destroy();
  }, []);

  return (
    <Suspense fallback={<Spinner />}>
      <Chart ref={chartRef} data={data} />
    </Suspense>
  );
}
\`\`\``,tech:"react",displayNumber:278,sortKey:1250},{id:"react-1251",number:279,title:"How do you optimize React Native performance? (Bonus cross-platform)",question:"How do you optimize React Native performance? (Bonus cross-platform)",difficulty:"Intermediate",category:"Performance & Optimization",answer:`React Native shares many React optimization principles but with native specifics:

1. **Use \`React.memo\`/\`useCallback\`**: Same as web
2. **Virtualized lists**: \`FlatList\` instead of \`ScrollView\` for long lists
3. **Avoid inline functions/styles**: Re-created each render
4. **Use \`useNativeDriver\`**: Animate on native thread, not JS
5. **Hermes engine**: Faster startup, lower memory
6. **Avoid bridge overload**: Batch native calls
7. **New Architecture (Fabric/TurboModules)**: Direct native communication

\`\`\`jsx
import { FlatList, Animated, useNativeDriver } from 'react-native';

// Virtualized list - renders only visible items
<FlatList
  data={items}
  keyExtractor={item => item.id}
  renderItem={({ item }) => <Item item={item} />}
  initialNumToRender={10}
/>

// Animate on native thread
Animated.timing(opacity, {
  toValue: 1,
  duration: 300,
  useNativeDriver: true,  // Critical for performance
}).start();
\`\`\``,tech:"react",displayNumber:279,sortKey:1251},{id:"react-1252",number:280,title:"What is the difference between shouldComponentUpdate and React.memo?",question:"What is the difference between shouldComponentUpdate and React.memo?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Both control re-rendering but for different paradigms:

**shouldComponentUpdate (Class)**:
- Lifecycle method returning boolean
- \`this.props\`/\`this.state\` comparison
- Class components only
- Manual deep/shallow comparison logic

**React.memo (Function)**:
- Higher-order component wrapper
- Automatic shallow prop comparison
- Function components only
- Optional custom comparator

\`PureComponent\` is the class equivalent of \`React.memo\` (shallow comparison built-in).

\`\`\`jsx
// Class: shouldComponentUpdate
class UserCard extends React.Component {
  shouldComponentUpdate(nextProps) {
    return nextProps.user.id !== this.props.user.id ||
           nextProps.user.name !== this.props.user.name;
  }
  render() { return <div>{this.props.user.name}</div>; }
}

// Function: React.memo
const UserCard = memo(
  function UserCard({ user }) {
    return <div>{user.name}</div>;
  },
  (prev, next) =>
    prev.user.id === next.user.id &&
    prev.user.name === next.user.name
);
\`\`\``,tech:"react",displayNumber:280,sortKey:1252},{id:"react-1253",number:281,title:"How do you prevent unnecessary re-renders in a context-heavy app?",question:"How do you prevent unnecessary re-renders in a context-heavy app?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Context-heavy apps suffer from mass re-renders. Solutions:

1. **Split contexts** by update frequency (static vs dynamic)
2. **Use selectors** (\`use-context-selector\`, Zustand)
3. **Memoize context value** with \`useMemo\`
4. **Separate state from dispatch**: Context for state, separate for actions
5. **Move state to leaf components**: Only components that need it
6. **Use external store** (\`useSyncExternalStore\`) for fine-grained updates

The key insight: context value identity changes → all consumers re-render. Keep value stable.

\`\`\`jsx
// Pattern: Separate state and actions contexts
const StateContext = createContext();
const DispatchContext = createContext();

function Provider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  const stableDispatch = useMemo(() => dispatch, []);  // Stable ref

  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={stableDispatch}>
        {children}
      </DispatchContext.Provider>
    </StateContext.Provider>
  );
}

// Components using only dispatch never re-render on state change
function ActionButton() {
  const dispatch = useContext(DispatchContext);
  return <button onClick={() => dispatch({ type: 'INC' })}>+</button>;
}
\`\`\``,tech:"react",displayNumber:281,sortKey:1253},{id:"react-1254",number:282,title:"What is the impact of prop drilling on performance and how do you avoid it?",question:"What is the impact of prop drilling on performance and how do you avoid it?",difficulty:"Basic",category:"Performance & Optimization",answer:`Prop drilling (passing props through many intermediate components) doesn't directly hurt performance, but:

1. **Re-renders cascade**: Changing a drilled prop re-renders all intermediate components
2. **Tight coupling**: Hard to optimize isolated subtrees
3. **Maintenance cost**: Changes ripple through many files

Avoid with:
- **Context** (for low-frequency state)
- **Composition** (children/ render props)
- **State management** (Zustand/Redux for high-frequency)
- **Component extraction**: Pass JSX as children instead of props

\`\`\`jsx
// BAD: Drilling through intermediate components
<Layout user={user}>
  <Page user={user}>
    <Header user={user}>
      <Profile user={user} />  // Only this needs user
    </Header>
  </Page>
</Layout>

// GOOD: Composition - pass as children
<Layout>
  <Page>
    <Header>
      <Profile user={user} />  // Direct
    </Header>
  </Page>
</Layout>

// GOOD: Context for widely-needed state
<UserContext.Provider value={user}>
  <DeepTree />  {/* Profile consumes directly */}
</UserContext.Provider>
\`\`\``,tech:"react",displayNumber:282,sortKey:1254},{id:"react-1255",number:283,title:"How do you optimize images with the Next.js Image component?",question:"How do you optimize images with the Next.js Image component?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Next.js \`<Image>\` automatically optimizes:

1. **Lazy loading** by default (\`loading="lazy"\`)
2. **Responsive sizes** via \`srcset\` (serves correct size)
3. **Modern formats** (WebP/AVIF) automatically
4. **Prevents CLS** with reserved space (requires width/height)
5. **Priority prop** for LCP images (preloads)
6. **Placeholder** for blur-up loading
7. **CDN optimization** via Next.js image optimizer

\`\`\`jsx
import Image from 'next/image';

// LCP image - prioritize
<Image
  src="/hero.jpg"
  alt="Hero"
  width={1200}
  height={600}
  priority          // Preloads for LCP
/>

// Below-the-fold - lazy by default
<Image
  src="/gallery.jpg"
  alt="Gallery"
  width={400}
  height={300}
  placeholder="blur"
  blurDataURL="data:image/jpeg;base64,..."
/>

// Responsive
<Image
  src="/product.jpg"
  alt="Product"
  sizes="(max-width: 768px) 100vw, 33vw"
  fill              // Fill parent container
/>
\`\`\``,tech:"react",displayNumber:283,sortKey:1255},{id:"react-1256",number:284,title:"What is the performance difference between inline styles and CSS classes?",question:"What is the performance difference between inline styles and CSS classes?",difficulty:"Basic",category:"Performance & Optimization",answer:`**Inline styles** (\`style={{}}\`):
- Recalculated every render (object creation)
- No CSS cascade, specificity issues
- No pseudo-classes (\`:hover\`), media queries, keyframes
- Slower for static styles

**CSS classes**:
- Browser-optimized, cached stylesheets
- Support full CSS features
- No per-render object creation
- Better for performance and maintainability

Use inline styles only for dynamic values that can't be pre-computed. Prefer CSS modules/classes for everything else.

\`\`\`jsx
// BAD: New object every render, no CSS features
function Bad({ active }) {
  return (
    <div style={{
      color: active ? 'red' : 'blue',
      padding: '10px',
    }}>
      Content
    </div>
  );
}

// GOOD: CSS class, computed once, supports :hover
import styles from './Component.module.css';
function Good({ active }) {
  return (
    <div className={\`\${styles.box} \${active ? styles.active : ''}\`}>
      Content
    </div>
  );
}

// OK: Inline only for truly dynamic values
<div style={{ width: \`\${progress}%\` }} />
\`\`\``,tech:"react",displayNumber:284,sortKey:1256},{id:"react-1257",number:285,title:"How do you optimize a React app's initial load time?",question:"How do you optimize a React app's initial load time?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Initial load optimization:

1. **Code splitting**: Lazy load routes/features
2. **SSR/SSG**: Send rendered HTML
3. **Bundle size**: Tree shaking, remove unused deps
4. **Preload critical resources**: Fonts, hero images
5. **Compression**: Brotli/Gzip
6. **CDN**: Serve assets close to users
7. **Font optimization**: \`font-display: swap\`, preload
8. **Eliminate render-blocking JS/CSS**
9. **Service worker / caching**: Cache app shell

Measure with Lighthouse and target < 2.5s LCP.

\`\`\`jsx
// Preload in index.html
<link rel="preload" href="/critical.css" as="style" />
<link rel="preload" href="/hero.avif" as="image" />
<link rel="preconnect" href="https://api.example.com" />

// Code split routes
const Home = lazy(() => import('./Home'));
const About = lazy(() => import('./About'));

// Manifest for caching
// service-worker.js caches app shell for instant loads
\`\`\``,tech:"react",displayNumber:285,sortKey:1257},{id:"react-1258",number:286,title:"What is the useSyncExternalStore hook and how does it optimize performance?",question:"What is the useSyncExternalStore hook and how does it optimize performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`\`useSyncExternalStore\` subscribes to external stores (Redux, Zustand, browser APIs) with safe concurrent rendering. It replaces \`useSubscription\` and prevents tearing.

Benefits:
1. **Fine-grained subscriptions**: Component re-renders only when its slice changes
2. **Concurrent-safe**: No tearing during interrupts
3. **SSR support**: \`getServerSnapshot\` for server rendering
4. **No extra re-renders**: Unlike Context, only subscribers update

This is how modern state libs (Zustand, Redux Toolkit) achieve selector-based performance.

\`\`\`jsx
import { useSyncExternalStore } from 'react';

// Subscribe to window width with fine-grained updates
function useWindowWidth() {
  const subscribe = (callback) => {
    window.addEventListener('resize', callback);
    return () => window.removeEventListener('resize', callback);
  };

  const getSnapshot = () => window.innerWidth;

  return useSyncExternalStore(subscribe, getSnapshot, () => 0);
}

function ResponsiveComponent() {
  const width = useWindowWidth();  // Only re-renders on width change
  return <div>Width: {width}</div>;
}
\`\`\``,tech:"react",displayNumber:286,sortKey:1258},{id:"react-1259",number:287,title:"How do you detect and prevent layout thrashing in React?",question:"How do you detect and prevent layout thrashing in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Layout thrashing = interleaving DOM reads (forces reflow) and writes (invalidates layout) in a loop. Each read after a write forces synchronous reflow - very expensive.

Prevention:
1. **Batch reads then writes**: Read all first, then write all
2. **Use \`useLayoutEffect\` carefully**: Runs synchronously, can cause thrashing
3. **requestAnimationFrame**: Batch DOM operations per frame
4. **Avoid measuring in loops**: Cache measurements
5. **CSS transforms**: Use \`transform\` (GPU) instead of \`top/left\`

\`\`\`jsx
// BAD: Read/write interleaved = forced reflow each iteration
items.forEach(item => {
  const height = item.offsetHeight;  // READ
  item.style.height = height * 2 + 'px';  // WRITE
});

// GOOD: Batch all reads, then all writes
const heights = items.map(item => item.offsetHeight);  // All READS
items.forEach((item, i) => {
  item.style.height = heights[i] * 2 + 'px';  // All WRITES
});

// Better: useLayoutEffect for measurements
useLayoutEffect(() => {
  const heights = items.map(ref => ref.offsetHeight);
  // Batch writes in same frame
}, [items]);
\`\`\``,tech:"react",displayNumber:287,sortKey:1259},{id:"react-1260",number:288,title:"How do you optimize font loading to prevent CLS and improve LCP?",question:"How do you optimize font loading to prevent CLS and improve LCP?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Font loading affects both CLS (FOUT/FOIT reflow) and LCP (text is often LCP element).

Optimizations:
1. **\`font-display: swap\`**: Show fallback immediately, swap when loaded (prevents invisible text)
2. **Preload critical fonts**: \`<link rel="preload" as="font">\`
3. **\`size-adjust\`**: Match fallback font metrics to reduce shift
4. **Self-host fonts**: Avoid third-party requests
5. **Subset fonts**: Only include needed characters (especially for CJK)
6. **Use \`next/font\`**: Automatic optimization in Next.js

\`\`\`css
/* font-face with swap + size-adjust */
@font-face {
  font-family: 'Custom';
  src: url('/font.woff2') format('woff2');
  font-display: swap;
  size-adjust: 105%;
  ascent-override: 90%;
}

/* Preload in HTML */
<link
  rel="preload"
  href="/font.woff2"
  as="font"
  type="font/woff2"
  crossorigin
/>

/* Next.js automatic font optimization */
import { Inter } from 'next/font/google';
const inter = Inter({ subsets: ['latin'], display: 'swap' });
\`\`\``,tech:"react",displayNumber:288,sortKey:1260},{id:"react-1261",number:289,title:"What is the difference between passive and active event listeners?",question:"What is the difference between passive and active event listeners?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Event listeners affect scroll/input performance:

**Passive listeners** (\`{ passive: true }\`):
- Browser knows listener won't \`preventDefault()\`
- Allows scrolling without waiting for JS
- Required for \`touchstart\`/\`wheel\` in modern browsers
- Improves scroll performance (no blocking)

**Active listeners** (default):
- Browser waits for listener to finish (in case it calls \`preventDefault\`)
- Can block scrolling if listener is slow

Use passive for scroll/touch/wheel handlers. Next.js sets this automatically for touch events.

\`\`\`jsx
useEffect(() => {
  const handler = (e) => {
    // Reading scroll position - don't preventDefault
    console.log(window.scrollY);
  };

  // Passive: browser can scroll without waiting
  window.addEventListener('scroll', handler, { passive: true });

  return () => window.removeEventListener('scroll', handler);
}, []);

// To prevent default, must be active (not passive)
const blockScroll = (e) => e.preventDefault();
element.addEventListener('touchmove', blockScroll);  // Active
\`\`\``,tech:"react",displayNumber:289,sortKey:1261},{id:"react-1262",number:290,title:"How do you implement infinite scrolling efficiently?",question:"How do you implement infinite scrolling efficiently?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Efficient infinite scroll combines several techniques:

1. **IntersectionObserver**: Detect when sentinel enters viewport (no scroll listeners)
2. **Virtualization**: Only render loaded items (for millions of rows)
3. **Pagination**: Fetch in chunks, not all at once
4. **Debounce fetch**: Avoid duplicate requests
5. **Prefetch next page**: Load before user reaches bottom
6. **Maintain scroll position**: Use keys, avoid layout shift

Avoid scroll event listeners (use IntersectionObserver instead).

\`\`\`jsx
import { useEffect, useRef, useState } from 'react';

function InfiniteList({ fetchItems }) {
  const [items, setItems] = useState([]);
  const [page, setPage] = useState(0);
  const sentinelRef = useRef(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      (entries) => {
        if (entries[0].isIntersecting) {
          setPage(p => p + 1);  // Trigger next page load
        }
      },
      { rootMargin: '100px' }  // Prefetch before visible
    );
    observer.observe(sentinelRef.current);
    return () => observer.disconnect();
  }, []);

  useEffect(() => {
    fetchItems(page).then(newItems => setItems(prev => [...prev, ...newItems]));
  }, [page]);

  return (
    <div>
      {items.map(item => <Item key={item.id} {...item} />)}
      <div ref={sentinelRef} />
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:290,sortKey:1262},{id:"react-1263",number:291,title:"How do you optimize React app memory usage?",question:"How do you optimize React app memory usage?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Memory optimization:

1. **Clean up effects**: Clear timers, unsubscribe, abort fetches
2. **Avoid storing large data in state**: Use refs or external stores
3. **Remove event listeners**: In effect cleanup
4. **Virtualize lists**: Limit DOM nodes (10k items = 10k nodes otherwise)
5. **Debounce expensive computations**: Avoid retaining intermediate arrays
6. **Dispose Web Workers**: \`terminate()\` when done
7. **Avoid closures capturing large objects**: They prevent GC
8. **Profile with Chrome DevTools Memory tab**: Take heap snapshots

\`\`\`jsx
// BAD: Closure captures huge array, prevents GC
function ProcessButton({ hugeData }) {
  const handleClick = () => {
    const result = hugeData.filter(/* ... */);  // Captured forever
  };
  return <button onClick={handleClick}>Process</button>;
}

// GOOD: Use ref, process on demand
function ProcessButton({ hugeDataRef }) {
  const handleClick = useCallback(() => {
    const result = hugeDataRef.current.filter(/* ... */);
    // Process and discard immediately
  }, [hugeDataRef]);

  return <button onClick={handleClick}>Process</button>;
}
\`\`\``,tech:"react",displayNumber:291,sortKey:1263},{id:"react-1264",number:292,title:"What are React Server Components and how do they improve performance?",question:"What are React Server Components and how do they improve performance?",difficulty:"Advanced",category:"Performance & Optimization",answer:`React Server Components (RSC) render on the server, sending minimal JS to the client. Benefits:

1. **Zero client JS**: Component code never downloaded to browser
2. **Direct backend access**: Query databases without API layer
3. **Automatic code splitting**: Server components excluded from bundle
4. **Smaller bundles**: Only Client Components ship JS
5. **Streaming**: Progressive rendering with Suspense

Tradeoffs: Can't use hooks/state/events (those require Client Components).

\`\`\`jsx
// Server Component (no 'use client')
// Runs on server, zero JS shipped to client
async function ProductList() {
  const products = await db.products.findMany();  // Direct DB access
  return (
    <ul>
      {products.map(p => (
        <li key={p.id}>{p.name} - \${p.price}</li>
      ))}
    </ul>
  );
}

// Client Component - needs 'use client'
'use client';
function AddToCart({ productId }) {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>Add</button>;
}
\`\`\``,tech:"react",displayNumber:292,sortKey:1264},{id:"react-1265",number:293,title:"How do you optimize a dashboard with many real-time widgets?",question:"How do you optimize a dashboard with many real-time widgets?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Real-time dashboards with many widgets face re-render storms. Solutions:

1. **Isolate subscriptions**: Each widget subscribes only to its data slice
2. **Virtualize widget grid**: Render only visible widgets
3. **Throttle updates**: Batch WebSocket messages (e.g., 10/sec → 1/sec)
4. **Use external store**: Zustand with selectors (no context re-renders)
5. **memo each widget**: Prevent cross-widget re-renders
6. **Web Worker**: Parse incoming messages off main thread
7. **Skeleton placeholders**: Avoid layout shift during updates

\`\`\`jsx
import { create } from 'zustand';

const useDashboard = create((set) => ({
  cpu: 0,
  memory: 0,
  network: 0,
  update: (metric, value) => set(s => ({ ...s, [metric]: value })),
}));

// Each widget only re-renders when its metric changes
const CpuWidget = memo(function CpuWidget() {
  const cpu = useDashboard(s => s.cpu);
  return <Gauge value={cpu} label="CPU" />;
});

const MemoryWidget = memo(function MemoryWidget() {
  const memory = useDashboard(s => s.memory);
  return <Gauge value={memory} label="Memory" />;
});
\`\`\``,tech:"react",displayNumber:293,sortKey:1265},{id:"react-1266",number:294,title:"How do you handle large form state without performance issues?",question:"How do you handle large form state without performance issues?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Large forms (50+ fields) re-render entire tree on each keystroke. Solutions:

1. **React Hook Form**: Uncontrolled, uses refs, minimal re-renders
2. **Field-level state**: Each field manages own state
3. **Controlled at section level**: Not entire form
4. **Debounce validation**: Validate on blur, not each keystroke
5. **useDeferredValue**: Defer expensive derived calculations
6. **Split into sub-forms**: Isolate state per section

\`\`\`jsx
import { useForm, Controller } from 'react-hook-form';

// React Hook Form: uncontrolled, only re-renders on validation/submit
function LargeForm() {
  const { register, control, handleSubmit } = useForm({
    mode: 'onBlur',  // Validate on blur, not each keystroke
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)}>
      {Array.from({ length: 100 }).map((_, i) => (
        <input
          key={i}
          {...register(\`field\${i}\`, { required: true })}
        />
      ))}
      <button type="submit">Submit</button>
    </form>
  );
}
\`\`\``,tech:"react",displayNumber:294,sortKey:1266},{id:"react-1267",number:295,title:"What is the performance impact of importing barrel files?",question:"What is the performance impact of importing barrel files?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Barrel files (\`index.js\` that re-exports everything) cause performance issues:

1. **Slower dev startup**: Bundlers must process entire barrel even for one import
2. **Larger initial parse**: All re-exported modules loaded
3. **Circular dependency risk**: Barrels easily create cycles
4. **Tree-shaking complexity**: Harder for bundlers to eliminate unused exports

Mitigations:
- Import directly from specific files (\`import X from './X'\`)
- Use deep imports (\`import { X } from 'lib/X'\`)
- Configure bundler for better barrel handling
- Avoid barrels in hot paths

\`\`\`jsx
// BAD: Imports entire barrel (loads everything)
import { Button, Modal, Table, Chart } from '@company/ui';

// GOOD: Direct import (only loads Button)
import Button from '@company/ui/Button';

// Or named + deep path (better tree-shaking)
import { Button } from '@company/ui/Button';

// Barrel file problem
// index.ts:
export * from './Button';
export * from './Modal';  // All loaded even if unused
export * from './Table';  // All loaded even if unused
\`\`\``,tech:"react",displayNumber:295,sortKey:1267},{id:"react-1268",number:296,title:"How do you use React.Profiler API programmatically?",question:"How do you use React.Profiler API programmatically?",difficulty:"Advanced",category:"Performance & Optimization",answer:`\`<Profiler>\` wraps components and calls a callback on every commit. Useful for:

1. **Production profiling**: Collect metrics in production
2. **Automated performance tests**: Assert render times in CI
3. **Conditional profiling**: Only profile specific subtrees

Callback receives: \`id\`, \`phase\` (mount/update), \`actualDuration\`, \`baseDuration\`, \`startTime\`, \`commitTime\`.

\`actualDuration\`: time spent rendering this Profiler's subtree
\`baseDuration\`: estimated time without memoization

\`\`\`jsx
import { Profiler } from 'react';

function logProfile(
  id,
  phase,
  actualDuration,
  baseDuration,
  startTime,
  commitTime
) {
  // Send to analytics
  analytics.track('render', {
    component: id,
    phase,
    duration: actualDuration,
    baseline: baseDuration,
  });

  // Alert if slow
  if (actualDuration > 16) {
    console.warn(\`[\${id}] Slow render: \${actualDuration}ms\`);
  }
}

function App() {
  return (
    <Profiler id="Checkout" onRender={logProfile}>
      <CheckoutFlow />
    </Profiler>
  );
}
\`\`\``,tech:"react",displayNumber:296,sortKey:1268},{id:"react-1269",number:297,title:"How do you optimize animations in React?",question:"How do you optimize animations in React?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`Animation performance principles:

1. **Animate transform/opacity only**: GPU-accelerated, no layout/paint
2. **Avoid animating layout properties**: \`width\`, \`height\`, \`top\`, \`left\` trigger reflow
3. **Use \`will-change\`**: Hint browser about upcoming animations
4. **Libraries**: Framer Motion, React Spring (optimized)
5. **CSS animations**: Prefer over JS when possible
6. **useTransition for state-driven anims**: Keep UI responsive
7. **Avoid re-renders during animation**: Animate via refs/CSS

\`\`\`jsx
import { motion } from 'framer-motion';

// GOOD: Transform/opacity (GPU)
<motion.div
  animate={{ x: 100, opacity: 0.5 }}
  transition={{ duration: 0.3 }}
/>

// BAD: Layout properties (reflow every frame)
<motion.div animate={{ width: 200, marginTop: 50 }} />

// CSS-only animation (best performance)
const spinner = styled.div\`
  animation: spin 1s linear infinite;
  will-change: transform;
  @keyframes spin {
    to { transform: rotate(360deg); }
  }
\`;
\`\`\``,tech:"react",displayNumber:297,sortKey:1269},{id:"react-1270",number:298,title:"What is the difference between useMemo and computed state?",question:"What is the difference between useMemo and computed state?",difficulty:"Intermediate",category:"Performance & Optimization",answer:`**useMemo**: Memoizes a derived value, recomputes on dep change. Still runs during render.

**Computed state**: Derived via \`useState\` + \`useEffect\` or external store. Persists independently.

Key difference: \`useMemo\` recomputes synchronously during render; computed state updates asynchronously via effects.

Use \`useMemo\` for pure derivations. Use state/effects when derivation has side effects or needs to persist across unrelated re-renders.

\`\`\`jsx
// useMemo: Pure derivation during render
function FilteredList({ items, query }) {
  const filtered = useMemo(
    () => items.filter(i => i.name.includes(query)),
    [items, query]
  );
  return <List items={filtered} />;
}

// Computed state: When derivation has side effects
function SearchResults({ query }) {
  const [results, setResults] = useState([]);

  useEffect(() => {
    // Side effect: fetch from API
    fetchResults(query).then(setResults);
  }, [query]);

  return <List items={results} />;
}
\`\`\``,tech:"react",displayNumber:298,sortKey:1270},{id:"react-1271",number:299,title:"How do you implement a performant search-as-you-type feature?",question:"How do you implement a performant search-as-you-type feature?",difficulty:"Advanced",category:"Performance & Optimization",answer:`Performant search requires multiple optimizations:

1. **Debounce input** (200-300ms): Avoid filtering on every keystroke
2. **useDeferredValue**: Keep input responsive while filtering
3. **useMemo for filter**: Cache filtered results
4. **Virtualize results**: For large result sets
5. **Web Worker**: Move filtering off main thread for huge datasets
6. **Abort previous requests**: Cancel stale API searches
7. **Highlight matches via memo**

\`\`\`jsx
import { useState, useDeferredValue, useMemo, useTransition } from 'react';

function SearchPage({ dataset }) {
  const [query, setQuery] = useState('');
  const [isPending, startTransition] = useTransition();
  const deferredQuery = useDeferredValue(query);

  // Filter only when deferredQuery changes (not on every keystroke)
  const results = useMemo(() => {
    if (!deferredQuery) return [];
    return dataset.filter(item =>
      item.name.toLowerCase().includes(deferredQuery.toLowerCase())
    );
  }, [deferredQuery, dataset]);

  function onChange(e) {
    const value = e.target.value;
    setQuery(value);  // Immediate (input responsive)
    startTransition(() => {
      // Mark filtering as non-urgent
    });
  }

  return (
    <>
      <input value={query} onChange={onChange} />
      {isPending ? <Spinner /> : <ResultsList results={results} />}
    </>
  );
}
\`\`\``,tech:"react",displayNumber:299,sortKey:1271},{id:"react-1272",number:300,title:"How do you create a performance budget and monitor it in React?",question:"How do you create a performance budget and monitor it in React?",difficulty:"Advanced",category:"Performance & Optimization",answer:`A performance budget sets limits for metrics. Monitor continuously:

1. **Define budgets**: Bundle size (e.g., < 200kb JS), LCP < 2.5s, INP < 200ms, CLS < 0.1
2. **CI checks**: Fail build if bundle exceeds budget (size-limit, bundlesize)
3. **Lighthouse CI**: Automated Core Web Vitals checks on every PR
4. **RUM (Real User Monitoring)**: Collect metrics from real users (web-vitals)
5. **Bundle analyzer in CI**: Catch large dependency additions
6. **Alerting**: Notify when metrics regress

\`\`\`json
// package.json - size-limit config
{
  "size-limit": [
    {
      "path": "dist/main.js",
      "limit": "200 KB"
    },
    {
      "path": "dist/vendor.js",
      "limit": "150 KB"
    }
  ]
}
\`\`\`

\`\`\`yaml
# lighthouserc.yml - Lighthouse CI
ci:
  assertions:
    'largest-contentful-paint': ['error', { maxNumericValue: 2500 }]
    'cumulative-layout-shift': ['error', { maxNumericValue: 0.1 }]
    'total-blocking-time': ['error', { maxNumericValue: 300 }]
  budgetPath: './budget.json'
\`\`\`

\`\`\`jsx
// RUM: Send Core Web Vitals to analytics
import { onCLS, onINP, onLCP } from 'web-vitals';

function report(metric) {
  navigator.sendBeacon('/analytics', JSON.stringify(metric));
}

onLCP(report);
onINP(report);
onCLS(report);
\`\`\``,tech:"react",displayNumber:300,sortKey:1272},{id:"react-1273",number:301,title:"What is the difference between local state and global state in React?",question:"What is the difference between local state and global state in React?",difficulty:"Basic",category:"State Management & Ecosystem",answer:`Local state is managed within a single component using \`useState\` or \`useReducer\` and is only accessible to that component and its children via props. Global state is shared across multiple components in the application, typically managed through Context API or external state management libraries. Local state is ideal for UI-specific concerns like form inputs or toggle states, while global state suits user authentication, themes, or data shared across distant components.

\`\`\`jsx
// Local state - component-specific
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}

// Global state via Context
const UserContext = createContext(null);

function App() {
  const [user, setUser] = useState({ name: 'John' });
  return (
    <UserContext.Provider value={{ user, setUser }}>
      <Dashboard />
    </UserContext.Provider>
  );
}

function Profile() {
  const { user } = useContext(UserContext);
  return <h1>{user.name}</h1>;
}
\`\`\``,tech:"react",displayNumber:301,sortKey:1273},{id:"react-1274",number:302,title:"When should you use Context API versus a dedicated state management library?",question:"When should you use Context API versus a dedicated state management library?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Use Context API for low-frequency updates (theme, authentication, locale) where many components need access to rarely-changing data. Avoid Context for high-frequency updates because every consumer re-renders when the context value changes, and you cannot selectively subscribe to parts of the value. Dedicated libraries (Redux, Zustand, Jotai) are better for complex state logic, frequent updates, middleware needs, derived/atomic state, and debugging tools. Reach for Context first for simplicity, then escalate to a library when you hit re-render or scaling pain.

\`\`\`jsx
// Context: good for static-ish, global config
const ThemeContext = createContext('light');
function Toolbar() {
  const theme = useContext(ThemeContext); // re-renders on any provider value change
  return <div className={theme}>...</div>;
}

// Zustand: selective subscription avoids unnecessary re-renders
const useBearStore = create((set) => ({
  bears: 0,
  fish: 10,
  addBear: () => set((s) => ({ bears: s.bears + 1 })),
}));
function BearCount() {
  const bears = useBearStore((s) => s.bears); // only re-renders when bears changes
  return <span>{bears}</span>;
}
\`\`\``,tech:"react",displayNumber:302,sortKey:1274},{id:"react-1275",number:303,title:"What are the main limitations of Context API for state management?",question:"What are the main limitations of Context API for state management?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Context has three key limitations: (1) Re-render scope — any change to the provider's value re-renders all consuming components, with no built-in selector to subscribe to a slice. (2) No middleware or side-effect handling — you cannot intercept updates for logging, async, or devtools. (3) Performance and splitting complexity — to mitigate re-renders you must split contexts or memoize, which adds boilerplate. Context is not a replacement for a full store; it's a dependency-injection mechanism.

\`\`\`jsx
// Problem: changing count re-renders ThemeConsumer too
const Ctx = createContext();
function Provider({ children }) {
  const [count, setCount] = useState(0);
  const [theme, setTheme] = useState('light');
  return (
    <Ctx.Provider value={{ count, setCount, theme, setTheme }}>
      {children}
    </Ctx.Provider>
  );
}
// Fix: split into separate providers so consumers subscribe narrowly
\`\`\``,tech:"react",displayNumber:303,sortKey:1275},{id:"react-1276",number:304,title:"What is Redux and what problem does it solve?",question:"What is Redux and what problem does it solve?",difficulty:"Basic",category:"State Management & Ecosystem",answer:`Redux is a predictable state container for JavaScript apps based on three principles: a single source of truth (one store), state is read-only (changes only via dispatched actions), and changes are made with pure reducer functions. It solves managing complex shared state across large apps by centralizing it, making state changes traceable and debuggable (time-travel, action logging). It enforces a unidirectional data flow and is framework-agnostic but widely used with React.

\`\`\`jsx
// Three principles in action
const initialState = { count: 0 };
function counter(state = initialState, action) {
  switch (action.type) {
    case 'INCREMENT':
      return { count: state.count + 1 }; // pure, no mutation
    default:
      return state;
  }
}
// Single store = single source of truth; dispatch actions to mutate
\`\`\``,tech:"react",displayNumber:304,sortKey:1276},{id:"react-1277",number:305,title:"Explain the core Redux data flow.",question:"Explain the core Redux data flow.",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"The unidirectional flow is: View dispatches an action -> Store passes the action and current state to the root reducer -> Reducer returns a new state -> Store updates and notifies subscribers -> View re-renders with new state. Actions are plain objects describing \"what happened\" with a `type`; reducers compute the next state. This makes every change explicit and replayable.\n\n```jsx\n// 1. Action -> 2. Dispatch -> 3. Reducer -> 4. New State -> 5. Subscribers\nstore.dispatch({ type: 'ADD_TODO', payload: 'Learn Redux' });\n// -> reducer(state, action) -> newState -> components subscribed re-render\n```",tech:"react",displayNumber:305,sortKey:1277},{id:"react-1278",number:306,title:"What is the difference between Redux Core and Redux Toolkit (RTK)?",question:"What is the difference between Redux Core and Redux Toolkit (RTK)?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Redux Core requires manual boilerplate: hand-written action types, action creators, switch statements in reducers, `combineReducers`, `applyMiddleware`, and configuring the store yourself. Redux Toolkit (RTK) is the official, opinionated wrapper that reduces boilerplate via `createSlice` (generates actions + reducer), `configureStore` (bundles thunk + devtools + good defaults), `createAsyncThunk`, and `createEntityAdapter`. RTK is the recommended approach for all new Redux code; Redux Core is essentially legacy.\n\n```jsx\n// Redux Core (verbose)\nconst INCREMENT = 'INCREMENT';\nconst increment = () => ({ type: INCREMENT });\nfunction reducer(s = { n: 0 }, a) {\n  if (a.type === INCREMENT) return { n: s.n + 1 };\n  return s;\n}\n\n// RTK (concise, same result)\nconst slice = createSlice({\n  name: 'counter',\n  initialState: { n: 0 },\n  reducers: { increment: (s) => { s.n++; } },\n});\n```",tech:"react",displayNumber:306,sortKey:1278},{id:"react-1279",number:307,title:"What is `createSlice` in Redux Toolkit and how does it work?",question:"What is `createSlice` in Redux Toolkit and how does it work?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`\`createSlice\` takes a slice name, initial state, and an object of reducer functions, and automatically generates action creators and action types matching the reducer names. Internally it uses Immer, so you can "mutate" the draft state and it produces an immutable update. It returns \`{ name, reducer, actions, caseReducers }\`. This eliminates the manual action-type/creator boilerplate and keeps related logic together.

\`\`\`jsx
import { createSlice } from '@reduxjs/toolkit';

const todosSlice = createSlice({
  name: 'todos',
  initialState: [],
  reducers: {
    addTodo: (state, action) => {
      state.push(action.payload); // Immer allows "mutation"
    },
    toggle: (state, action) => {
      const todo = state.find(t => t.id === action.payload);
      if (todo) todo.done = !todo.done;
    },
  },
});

export const { addTodo, toggle } = todosSlice.actions;
export default todosSlice.reducer;
\`\`\``,tech:"react",displayNumber:307,sortKey:1279},{id:"react-1280",number:308,title:"What is Immer and how does Redux Toolkit use it?",question:"What is Immer and how does Redux Toolkit use it?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Immer is a library that lets you write "mutating" code which it transparently converts into immutable updates using structural sharing. RTK uses Immer inside \`createSlice\` reducers, so you can write \`state.push(...)\` or \`state.value++\` instead of spread operations, and Immer returns a new immutable state. This makes reducers far more concise and less error-prone while preserving immutability guarantees.

\`\`\`jsx
// Without Immer (manual immutable update)
return { ...state, user: { ...state.user, name: 'A' } };

// With Immer (inside RTK reducer)
reducers: {
  setName: (state, action) => { state.user.name = action.payload; }
}
// Immer produces a new immutable object automatically
\`\`\``,tech:"react",displayNumber:308,sortKey:1280},{id:"react-1281",number:309,title:"How do you configure the Redux store with Redux Toolkit's `configureStore`?",question:"How do you configure the Redux store with Redux Toolkit's `configureStore`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`configureStore` accepts an object with `reducer` (a single reducer or an object map that it auto-combines), optional `middleware` (defaults include thunk + serializable-check + immutable-check + devtools), `preloadedState`, and `devTools`. Unlike the classic `createStore`, it bundles sensible defaults and warns about non-serializable state, making setup safer and shorter.\n\n```jsx\nimport { configureStore } from '@reduxjs/toolkit';\nimport todos from './todosSlice';\nimport user from './userSlice';\n\nexport const store = configureStore({\n  reducer: { todos, user },\n  devTools: process.env.NODE_ENV !== 'production',\n});\n// auto-combines reducers, adds thunk + devtools\n```",tech:"react",displayNumber:309,sortKey:1281},{id:"react-1282",number:310,title:"What is `combineReducers` and when is it needed?",question:"What is `combineReducers` and when is it needed?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`combineReducers` merges multiple slice reducers into one root reducer, each managing its own slice of state keyed by name. With RTK's `configureStore` this is automatic when you pass an object map, so you rarely call `combineReducers` directly. It is needed when you want to split state management across feature modules and have each operate independently on its own sub-tree.\n\n```jsx\nimport { combineReducers } from '@reduxjs/toolkit';\nconst rootReducer = combineReducers({ todos, user, cart });\n// state = { todos: [...], user: {...}, cart: {...} }\n```",tech:"react",displayNumber:310,sortKey:1282},{id:"react-1283",number:311,title:"What is Redux middleware and what is it used for?",question:"What is Redux middleware and what is it used for?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Middleware is a layer between dispatching an action and the moment it reaches the reducer. It can intercept, modify, delay, or short-circuit actions. Common uses: handling async logic (Thunk, Saga), logging, crash reporting, analytics, and enforcing side-effect rules. Middleware is composed into a pipeline; each gets \`(store) => (next) => (action) => {...}\`.

\`\`\`jsx
// Custom logging middleware
const logger = (store) => (next) => (action) => {
  console.log('dispatching', action);
  const result = next(action);
  console.log('next state', store.getState());
  return result;
};
\`\`\``,tech:"react",displayNumber:311,sortKey:1283},{id:"react-1284",number:312,title:"Explain Redux Thunk and how it handles async logic.",question:"Explain Redux Thunk and how it handles async logic.",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Redux Thunk is middleware that lets action creators return a function (a thunk) instead of a plain object. The function receives \`dispatch\` and \`getState\`, enabling async flows: perform a request, then dispatch real actions on success/failure. Thunk is included by default in RTK. It's the simplest async pattern but can become hard to test/track for complex flows (where Saga shines).

\`\`\`jsx
// Plain thunk (RTK includes thunk by default)
export const fetchUser = (id) => async (dispatch) => {
  dispatch(fetchStart());
  try {
    const res = await api.getUser(id);
    dispatch(fetchSuccess(res.data));
  } catch (e) {
    dispatch(fetchError(e.message));
  }
};
\`\`\``,tech:"react",displayNumber:312,sortKey:1284},{id:"react-1285",number:313,title:"What is `createAsyncThunk` in Redux Toolkit?",question:"What is `createAsyncThunk` in Redux Toolkit?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`\`createAsyncThunk\` creates a thunk that dispatches \`pending\`, \`fulfilled\`, and \`rejected\` lifecycle actions automatically based on a payload creator's promise. You then handle these in \`extraReducers\` of a slice via \`builder.addCase\`. It standardizes async state (\`status\`, \`data\`, \`error\`) and removes the need to write manual loading/error actions.

\`\`\`jsx
export const fetchPosts = createAsyncThunk(
  'posts/fetch',
  async (userId, { rejectWithValue }) => {
    try {
      const res = await api.getPosts(userId);
      return res.data;
    } catch (e) {
      return rejectWithValue(e.message);
    }
  }
);

const slice = createSlice({
  name: 'posts',
  initialState: { items: [], status: 'idle' },
  extraReducers: (builder) => {
    builder
      .addCase(fetchPosts.pending, (s) => { s.status = 'loading'; })
      .addCase(fetchPosts.fulfilled, (s, a) => { s.status = 'idle'; s.items = a.payload; })
      .addCase(fetchPosts.rejected, (s, a) => { s.status = 'error'; s.error = a.payload; });
  },
});
\`\`\``,tech:"react",displayNumber:313,sortKey:1285},{id:"react-1286",number:314,title:"How do you handle loading and error states with `createAsyncThunk`?",question:"How do you handle loading and error states with `createAsyncThunk`?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Store a `status` (idle/loading/succeeded/failed) and `error` field in the slice. In `extraReducers`, set `status='loading'` on `pending`, set data and `status='succeeded'` on `fulfilled`, and set `error` and `status='failed'` on `rejected`. Components read these to render spinners and error messages. Optionally track `isLoading` per-entity, but a single status works for simple cases.\n\n```jsx\nconst { status, error, items } = useSelector(s => s.posts);\nif (status === 'loading') return <Spinner />;\nif (status === 'failed') return <Error msg={error} />;\nreturn <List items={items} />;\n```",tech:"react",displayNumber:314,sortKey:1286},{id:"react-1287",number:315,title:"What is Redux Saga and how does it differ from Thunk?",question:"What is Redux Saga and how does it differ from Thunk?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Redux Saga uses generator functions to handle complex async side effects, listening for dispatched actions and reacting with `takeLatest`, `takeEvery`, `put`, `call`, `fork`. Unlike Thunk, which co-locates async logic inside action creators as plain functions, Saga decouples side effects into a separate, testable, declarative layer. Saga excels at complex sequences, cancellation, race conditions, and retries, but adds significant boilerplate. Use Thunk/RTK Query for simple async, Saga for orchestration-heavy flows.\n\n```jsx\nimport { takeLatest, call, put } from 'redux-saga/effects';\n\nfunction* loadUser(action) {\n  try {\n    const user = yield call(api.getUser, action.payload);\n    yield put(userLoaded(user));\n  } catch (e) {\n    yield put(userError(e.message));\n  }\n}\nexport function* userSaga() {\n  yield takeLatest('user/fetch', loadUser); // decoupled from action creators\n}\n```",tech:"react",displayNumber:315,sortKey:1287},{id:"react-1288",number:316,title:"What are the most common Redux Saga effect creators?",question:"What are the most common Redux Saga effect creators?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`take`/`takeEvery`/`takeLatest` (wait for / listen to actions), `put` (dispatch action), `call` (call function, blocking), `fork`/`spawn` (non-blocking task), `select` (read state), `race` (run effects, take first to finish), `all` (run in parallel), `delay`/`cancel`. `takeLatest` cancels in-flight tasks for the same action, perfect for search-as-you-type; `all` parallelizes independent fetches.\n\n```jsx\nfunction* root() {\n  yield all([\n    takeLatest('user/fetch', loadUser),\n    takeEvery('log/event', logEvent),\n  ]);\n}\nfunction* parallel() {\n  const [a, b] = yield all([call(fetchA), call(fetchB)]); // runs concurrently\n}\n```",tech:"react",displayNumber:316,sortKey:1288},{id:"react-1289",number:317,title:"When would you choose Redux Saga over RTK Query or Thunk?",question:"When would you choose Redux Saga over RTK Query or Thunk?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Choose Saga when you need complex orchestration: long-running background tasks, cancellation (e.g., debounced search), race conditions, retries with backoff, websocket/event-stream handling, or coordinating multiple dependent side effects. For plain server-state fetching and caching, RTK Query is simpler and handles caching/invalidation for you. For straightforward request-then-dispatch, Thunk suffices. Saga's cost is boilerplate and a steeper learning curve.

\`\`\`jsx
// Debounced search with cancellation - Saga's strength
function* searchSaga() {
  yield debounce(300, 'search/run', function* (action) {
    const results = yield call(api.search, action.payload);
    yield put(searchDone(results));
  });
}
\`\`\``,tech:"react",displayNumber:317,sortKey:1289},{id:"react-1290",number:318,title:"What is RTK Query and what problem does it solve?",question:"What is RTK Query and what problem does it solve?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`RTK Query (RTKQ) is RTK's data-fetching and caching layer. It eliminates hand-written data-fetching reducers, thunks, loading/error state, and caching logic by auto-generating hooks (\`useGetPostsQuery\`). It manages caching, deduplication, background refetch, invalidation, and request lifecycle for you. It's designed for server state (data that lives on the server), not client UI state.

\`\`\`jsx
import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react';

export const api = createApi({
  reducerPath: 'api',
  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),
  endpoints: (build) => ({
    getPosts: build.query({ query: () => 'posts' }),
  }),
});
// Generates: useGetPostsQuery()
\`\`\``,tech:"react",displayNumber:318,sortKey:1290},{id:"react-1291",number:319,title:"How do you create an API slice with `createApi` in RTK Query?",question:"How do you create an API slice with `createApi` in RTK Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`createApi` requires `reducerPath`, a `baseQuery` (e.g., `fetchBaseQuery`), and `endpoints` where you define `query` (GET) and `mutation` (POST/PUT/DELETE) endpoints returning hooks. You must add the generated reducer to the store under `reducerPath` and include `api.middleware` in the middleware chain. Each endpoint auto-generates a hook like `useGetXQuery`.\n\n```jsx\nexport const api = createApi({\n  reducerPath: 'api',\n  baseQuery: fetchBaseQuery({ baseUrl: '/api' }),\n  tagTypes: ['Post'],\n  endpoints: (build) => ({\n    getPosts: build.query({ query: () => '/posts', providesTags: ['Post'] }),\n    addPost: build.mutation({\n      query: (body) => ({ url: '/posts', method: 'POST', body }),\n      invalidatesTags: ['Post'],\n    }),\n  }),\n});\nexport const { useGetPostsQuery, useAddPostMutation } = api;\n```",tech:"react",displayNumber:319,sortKey:1291},{id:"react-1292",number:320,title:"How do RTK Query auto-generated hooks work?",question:"How do RTK Query auto-generated hooks work?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"For each endpoint, RTKQ generates a React hook (e.g., `useGetPostsQuery()` or `useAddPostMutation()`). The query hook returns `{ data, isLoading, isFetching, isError, error, refetch }`. The mutation hook returns `[trigger, { data, isLoading, isSuccess }]`. Under the hood they subscribe to the cache via `useSelector` and dispatch fetch lifecycle actions. Calling a query hook triggers a fetch if data isn't cached.\n\n```jsx\nfunction Posts() {\n  const { data, isLoading, isError } = useGetPostsQuery();\n  const [addPost, { isLoading: saving }] = useAddPostMutation();\n  if (isLoading) return <Spinner />;\n  return <button onClick={() => addPost({ title: 'Hi' })}>Add</button>;\n}\n```",tech:"react",displayNumber:320,sortKey:1292},{id:"react-1293",number:321,title:"Explain caching in RTK Query.",question:"Explain caching in RTK Query.",difficulty:"Advanced",category:"State Management & Ecosystem",answer:'RTKQ caches responses in the store keyed by endpoint + serialized arguments (the "query key"). A query result is served from cache without a new request until it becomes unused (no active subscribers) for `keepUnusedDataFor` (default 60s) and until `staleTime` elapses. While mounted, the same query across components shares one cache entry and dedupes concurrent requests. Re-fetches happen on mount, arg change, `refetch`, or invalidation.\n\n```jsx\n// Two components using same args share one network request + one cache entry\nconst { data: a } = useGetPostQuery(1);\nconst { data: b } = useGetPostQuery(1); // no duplicate fetch\n```',tech:"react",displayNumber:321,sortKey:1293},{id:"react-1294",number:322,title:"What are invalidation tags in RTK Query and how do they work?",question:"What are invalidation tags in RTK Query and how do they work?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Tags (`providesTags` on queries, `invalidatesTags` on mutations) link cache entries to logical entities. When a mutation invalidates a tag, RTKQ automatically refetches all active queries that `provide` that tag — keeping server state consistent without manual refetch calls. Tags can be granular (e.g., `{ type: 'Post', id }`) so updating one post only refetches that post's query.\n\n```jsx\ngetPost: build.query({\n  query: (id) => `/posts/${id}`,\n  providesTags: (result, err, id) => [{ type: 'Post', id }],\n}),\nupdatePost: build.mutation({\n  query: ({ id, ...patch }) => ({ url: `/posts/${id}`, method: 'PATCH', body: patch }),\n  invalidatesTags: (result, err, { id }) => [{ type: 'Post', id }],\n}),\n```",tech:"react",displayNumber:322,sortKey:1294},{id:"react-1295",number:323,title:"How do you perform mutations in RTK Query?",question:"How do you perform mutations in RTK Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Define a `build.mutation` endpoint and destructure its hook (e.g., `useAddPostMutation`). Calling the trigger function returns a promise you can await for the result or error. Mutations do not cache a value but run `invalidatesTags` to refetch dependent queries. You can also use `onQueryStarted` with `updateQueryData` for optimistic updates.\n\n```jsx\nconst [addPost] = useAddPostMutation();\nconst handleClick = async () => {\n  try {\n    await addPost({ title: 'New' }).unwrap(); // unwrap throws on error\n  } catch (e) {\n    console.error(e);\n  }\n};\n```",tech:"react",displayNumber:323,sortKey:1295},{id:"react-1296",number:324,title:"How does RTK Query handle dependent queries?",question:"How does RTK Query handle dependent queries?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"A dependent (chained) query only runs when a prior query's data is available. Pass `skip` when the argument isn't ready: `useGetUserQuery(userId, { skip: !userId })`. For deeper chaining, skip the second query until the first resolves, then use its data as the argument. RTKQ also supports `selectFromResult` for derived data and combining queries.\n\n```jsx\nconst { data: user } = useGetUserQuery(id);\nconst { data: posts } = useGetPostsQuery(user?.id, {\n  skip: !user?.id, // dependent query waits for user\n});\n```",tech:"react",displayNumber:324,sortKey:1296},{id:"react-1297",number:325,title:"What is `selectFromResult` in RTK Query?",question:"What is `selectFromResult` in RTK Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`selectFromResult` lets a query hook return a derived/transformed slice of the cache instead of the whole response, and enables fine-grained re-renders. It receives the full `result` (with `data`, `isLoading`, etc.) and returns a customized object. Useful to avoid re-rendering when unrelated parts of the cache change or to precompute derived values.\n\n```jsx\nconst { activeOnly } = useGetTodosQuery(undefined, {\n  selectFromResult: ({ data }) => ({\n    activeOnly: (data ?? []).filter((t) => !t.done),\n  }),\n});\n```",tech:"react",displayNumber:325,sortKey:1297},{id:"react-1298",number:326,title:"How do you add RTK Query to an existing Redux store?",question:"How do you add RTK Query to an existing Redux store?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Add the API's reducer under its `reducerPath` and insert `api.middleware` into the store's middleware (using `getDefaultMiddleware` concatenation so RTKQ's cache lifecycle actions are handled). Then wrap the app in `<ApiProvider>` or, with React-Redux, rely on the normal `<Provider>`.\n\n```jsx\nexport const store = configureStore({\n  reducer: {\n    [api.reducerPath]: api.reducer,\n    todos,\n  },\n  middleware: (getDefault) => getDefault().concat(api.middleware),\n});\n```",tech:"react",displayNumber:326,sortKey:1298},{id:"react-1299",number:327,title:"What is the difference between server state and client state?",question:"What is the difference between server state and client state?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Server state is data owned by the backend (cached on the client): it's asynchronous, shared across users, potentially stale, and requires fetching/sync. Client state is UI state owned by the frontend: toggles, form inputs, theme, modals. Libraries like RTK Query / React Query manage server state (caching, invalidation, refetch), while Redux/Zustand/Context manage client state. Mixing them causes bugs — keep them separate.

\`\`\`jsx
// Server state -> TanStack Query / RTK Query
const { data } = useQuery(['user', id], () => fetchUser(id));
// Client state -> useState / Zustand
const [isModalOpen, setOpen] = useState(false);
\`\`\``,tech:"react",displayNumber:327,sortKey:1299},{id:"react-1300",number:328,title:"What is Zustand and why is it popular?",question:"What is Zustand and why is it popular?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Zustand is a minimal, hook-based state management library. You create a store with \`create()\` and read state via selectors inside components. It's popular because of tiny boilerplate, no providers, built-in selective subscriptions (preventing unnecessary re-renders), middleware support (persist, devtools, immer), and an approachable mental model compared to Redux. It suits both local and global state.

\`\`\`jsx
import { create } from 'zustand';

const useStore = create((set) => ({
  count: 0,
  inc: () => set((s) => ({ count: s.count + 1 })),
  reset: () => set({ count: 0 }),
}));
// No Provider needed; use anywhere
function Counter() {
  const count = useStore((s) => s.count);
  return <button onClick={() => useStore.getState().inc()}>{count}</button>;
}
\`\`\``,tech:"react",displayNumber:328,sortKey:1300},{id:"react-1301",number:329,title:"How do you create a store in Zustand?",question:"How do you create a store in Zustand?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"Call `create((set, get) => ({ ...state, ...actions }))`. `set` updates state (partial or function form), `get` reads current state. The returned hook is used to select slices. You can colocate state and actions in one object. No context provider is required — the store is a module-level singleton.\n\n```jsx\nconst useBearStore = create((set) => ({\n  bears: 0,\n  increase: () => set((s) => ({ bears: s.bears + 1 })),\n  removeAll: () => set({ bears: 0 }),\n}));\n```",tech:"react",displayNumber:329,sortKey:1301},{id:"react-1302",number:330,title:"How do selectors work in Zustand and why do they matter for performance?",question:"How do selectors work in Zustand and why do they matter for performance?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Selectors are functions passed to the store hook: `useStore((s) => s.x)`. The component re-renders only when the selected value changes (by `Object.is` comparison). This avoids the Context \"re-render everything\" problem. For derived/object selections, use `useShallow` or return primitives to prevent infinite re-renders from new object references each render.\n\n```jsx\nimport { useShallow } from 'zustand/react/shallow';\n\n// Safe object selection\nconst { bears, fish } = useBearStore(useShallow((s) => ({ bears: s.bears, fish: s.fish })));\n// Primitive selection re-renders only when value changes\nconst bears = useBearStore((s) => s.bears);\n```",tech:"react",displayNumber:330,sortKey:1302},{id:"react-1303",number:331,title:"How do you add middleware to Zustand?",question:"How do you add middleware to Zustand?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Pass a middleware-wrapped initializer to `create`: `create(persist((set) => ({...}), {name}))`. Zustand ships `persist`, `devtools`, `subscribeWithSelector`, and `immer` middlewares that can be composed with the `compose` utility. Custom middleware follows `(store) => (next) => (partial) => next(partial)`.\n\n```jsx\nimport { persist, devtools, immer } from 'zustand/middleware';\n\nconst useStore = create(\n  devtools(\n    persist(\n      immer((set) => ({ count: 0, inc: () => set((s) => { s.count++; }) })),\n      { name: 'counter' }\n    )\n  )\n);\n```",tech:"react",displayNumber:331,sortKey:1303},{id:"react-1304",number:332,title:"What does the `persist` middleware do in Zustand?",question:"What does the `persist` middleware do in Zustand?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`persist` automatically saves selected store state to `localStorage` (or a custom storage) and rehydrates on load, keeping client state across page refreshes. You configure `name` and optionally `partialize` (which keys to persist), `version`, and `migrate`. Great for themes, auth tokens, or cart contents without manual sync code.\n\n```jsx\nconst useStore = create(persist(\n  (set) => ({ token: null, setToken: (t) => set({ token: t }) }),\n  { name: 'auth', partialize: (s) => ({ token: s.token }) }\n));\n```",tech:"react",displayNumber:332,sortKey:1304},{id:"react-1305",number:333,title:"How does Zustand compare to Redux?",question:"How does Zustand compare to Redux?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Zustand is simpler and less boilerplate: no actions/types/reducers, no provider, built-in selectors and middleware. Redux (RTK) offers stricter structure, a richer ecosystem (DevTools time-travel, Saga, RTK Query), and is battle-tested for very large teams. Zustand scales well but is less opinionated. Choose Redux for complex, heavily-tooled apps; Zustand for speed and minimalism.\n\n```jsx\n// Redux: action + reducer boilerplate (even with RTK slice)\n// Zustand: one function, actions inline, no provider\n```",tech:"react",displayNumber:333,sortKey:1305},{id:"react-1306",number:334,title:"How do you avoid unnecessary re-renders in Zustand?",question:"How do you avoid unnecessary re-renders in Zustand?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Select narrowly with primitive selectors, use `useShallow` for object/array selections, avoid returning new references inline, and consider `subscribeWithSelector` for non-React subscriptions. Also avoid putting rapidly-changing large objects in one store slice; split stores if needed. The key is that each component subscribes only to the data it uses.\n\n```jsx\n// Bad: new object each render -> infinite loop risk\nconst data = useStore((s) => ({ a: s.a, b: s.b }));\n// Good\nconst a = useStore((s) => s.a);\nconst b = useStore((s) => s.b);\n// or\nconst { a, b } = useStore(useShallow((s) => ({ a: s.a, b: s.b })));\n```",tech:"react",displayNumber:334,sortKey:1306},{id:"react-1307",number:335,title:"What is Jotai and what is atomic state management?",question:"What is Jotai and what is atomic state management?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:`Jotai is a primitive, bottom-up state library built on atoms — the smallest unit of state. Instead of one store, you create independent atoms; components subscribe to only the atoms they read. Derived atoms compute from others. This "atomic" model avoids over-rendering, removes boilerplate, and scales naturally. It's a spiritual successor to Recoil with a simpler API and better maintenance status.

\`\`\`jsx
import { atom, useAtom } from 'jotai';

const countAtom = atom(0);
const doubledAtom = atom((get) => get(countAtom) * 2); // derived

function Counter() {
  const [count, setCount] = useAtom(countAtom);
  return <button onClick={() => setCount((c) => c + 1)}>{count}</button>;
}
\`\`\``,tech:"react",displayNumber:335,sortKey:1307},{id:"react-1308",number:336,title:"How do derived atoms work in Jotai?",question:"How do derived atoms work in Jotai?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"A derived (read-only) atom takes a getter: `atom((get) => get(otherAtom) + 1)`. `get` reads other atoms' values and establishes a dependency; when a dependency changes, the derived atom recomputes and its subscribers re-render. You can also create writeable derived atoms with a `set` function for computed mutations. This replaces Redux selectors/reducers with composable units.\n\n```jsx\nconst priceAtom = atom(10);\nconst qtyAtom = atom(2);\nconst totalAtom = atom((get) => get(priceAtom) * get(qtyAtom)); // recomputes on change\n```",tech:"react",displayNumber:336,sortKey:1308},{id:"react-1309",number:337,title:"What is Recoil and how does it differ from Jotai?",question:"What is Recoil and how does it differ from Jotai?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Recoil (by Meta) also used atoms and selectors with a `<RecoilRoot>` provider, and supported atom families and async selectors. It is now in maintenance/limited development. Jotai is lighter, has no provider requirement, a richer middleware/ecosystem, and active maintenance. Both are atomic; Recoil's API (atom, selector, useRecoilState) is similar but Jotai is generally recommended for new projects today.\n\n```jsx\n// Recoil (legacy)\nconst textState = atom({ key: 'text', default: '' });\nconst charCount = selector({ key: 'count', get: ({ get }) => get(textState).length });\n// Jotai equivalent is simpler and actively maintained\n```",tech:"react",displayNumber:337,sortKey:1309},{id:"react-1310",number:338,title:"What are atom families and when are they useful?",question:"What are atom families and when are they useful?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Atom families generate a parametrized atom per key/id (e.g., one atom per todo item) so each entity has isolated, independently-subscribable state. This prevents re-rendering all items when one changes and avoids storing large arrays of objects in a single atom. Supported in Jotai via `atomFamily` (or a map of atoms) and in Recoil as `atomFamily`.\n\n```jsx\nimport { atomFamily } from 'jotai/utils';\nconst todoAtomFamily = atomFamily((id) => atom({ id, text: '', done: false }));\n\nfunction Todo({ id }) {\n  const [todo, setTodo] = useAtom(todoAtomFamily(id)); // only this item re-renders\n  return <input value={todo.text} onChange={(e) => setTodo({ ...todo, text: e.target.value })} />;\n}\n```",tech:"react",displayNumber:338,sortKey:1310},{id:"react-1311",number:339,title:"How do you handle async with Jotai atoms?",question:"How do you handle async with Jotai atoms?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Jotai supports async atoms: a `read` function can return a promise, and `useAtom` suspends (with Suspense) or resolves it. Alternatively, use `atomWithQuery` (TanStack Query integration) or `loadable` / `useAtomValue` with Suspense. Async atoms let you colocate data-fetching with state while keeping client/server concerns clear.\n\n```jsx\nconst userAtom = atom(async (get) => {\n  const res = await fetch('/api/user');\n  return res.json();\n});\n\nfunction Profile() {\n  const user = useAtomValue(userAtom); // suspends until resolved\n  return <h1>{user.name}</h1>;\n}\n```",tech:"react",displayNumber:339,sortKey:1311},{id:"react-1312",number:340,title:"What is TanStack Query (React Query) and what is it used for?",question:"What is TanStack Query (React Query) and what is it used for?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"TanStack Query (formerly React Query) is a server-state management library for fetching, caching, synchronizing, and updating async data. It is not for client UI state. It provides `useQuery` (read), `useMutation` (write), automatic caching, background refetching, retries, pagination, and devtools. It dramatically reduces boilerplate for data fetching and keeps server data in sync.\n\n```jsx\nimport { useQuery } from '@tanstack/react-query';\n\nfunction User({ id }) {\n  const { data, isLoading, error } = useQuery({\n    queryKey: ['user', id],\n    queryFn: () => fetch(`/api/users/${id}`).then((r) => r.json()),\n  });\n  if (isLoading) return <Spinner />;\n  return <div>{data.name}</div>;\n}\n```",tech:"react",displayNumber:340,sortKey:1312},{id:"react-1313",number:341,title:"How do you use `useQuery`?",question:"How do you use `useQuery`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`useQuery({ queryKey, queryFn, options })` takes a unique `queryKey` (array used for caching/id) and a `queryFn` returning a promise. It returns `{ data, isLoading, isError, error, isFetching, refetch, status }`. On mount it fetches; subsequent identical keys read from cache. Options control stale time, retry, refetch intervals, and enabled flags.\n\n```jsx\nconst { data, isLoading, isError, refetch } = useQuery({\n  queryKey: ['todos'],\n  queryFn: fetchTodos,\n  staleTime: 60_000,\n});\n```",tech:"react",displayNumber:341,sortKey:1313},{id:"react-1314",number:342,title:"What are query keys and why are they important?",question:"What are query keys and why are they important?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Query keys are serializable arrays that uniquely identify a query's cache entry. They must be structured and consistent: `['todos', { status: 'done' }]`. Nested keys let you invalidate by prefix (e.g., `['todos']` invalidates all todo queries). Query keys deterministically serialize, so argument order and shape matter — keep them stable and sort object keys for consistency.\n\n```jsx\n// Good: hierarchical, deterministic\n['todos', 'list', { page: 1 }]\n['todos', 'detail', id]\n\n// Invalidating by prefix refetches all todo queries\nqueryClient.invalidateQueries({ queryKey: ['todos'] });\n```",tech:"react",displayNumber:342,sortKey:1314},{id:"react-1315",number:343,title:"How do you use `useMutation`?",question:"How do you use `useMutation`?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`useMutation({ mutationFn, onSuccess, onError, onSettled })` returns `[mutate, { data, isPending, isError, error }]`. Call `mutate(variables)` to trigger. Use it for POST/PUT/DELETE. In `onSuccess` you typically invalidate queries to refetch server state, or `setQueryData` to update the cache manually (e.g., for optimistic updates).\n\n```jsx\nconst mutation = useMutation({\n  mutationFn: (newTodo) => api.createTodo(newTodo),\n  onSuccess: () => {\n    queryClient.invalidateQueries({ queryKey: ['todos'] });\n  },\n});\n<button onClick={() => mutation.mutate({ title: 'Hi' })}>Add</button>;\n```",tech:"react",displayNumber:343,sortKey:1315},{id:"react-1316",number:344,title:"Explain cache time vs stale time in React Query.",question:"Explain cache time vs stale time in React Query.",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`staleTime` (default 0) is how long fetched data is considered fresh; while fresh, React Query will not refetch on window focus or remount. `gcTime` (garbage collection time, formerly `cacheTime`, default 5 min) is how long inactive (no subscribers) cache entries are kept before being removed. Data can be stale but still in cache; `staleTime` controls refetch behavior, `gcTime` controls memory retention. Set `staleTime` higher to reduce refetches, `gcTime` higher to keep data available offline-ish.\n\n```jsx\nuseQuery({\n  queryKey: ['user', id],\n  queryFn: fn,\n  staleTime: 5 * 60_000,   // 5 min fresh -> no auto refetch\n  gcTime: 30 * 60_000,     // 30 min retained after unmount\n});\n```",tech:"react",displayNumber:344,sortKey:1316},{id:"react-1317",number:345,title:"What are optimistic updates and how do you implement them in React Query?",question:"What are optimistic updates and how do you implement them in React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Optimistic updates reflect the expected server result in the UI immediately, then roll back if the request fails. In React Query: `onMutate` cancels outgoing queries, snapshots previous data via `queryClient.getQueryData`, optimistically `setQueryData`, then in `onError` restores the snapshot, and `onSettled` invalidates to reconcile with server truth. This makes apps feel instant.\n\n```jsx\nuseMutation({\n  mutationFn: updateTodo,\n  onMutate: async (newTodo) => {\n    await queryClient.cancelQueries({ queryKey: ['todos'] });\n    const prev = queryClient.getQueryData(['todos']);\n    queryClient.setQueryData(['todos'], (old) =>\n      old.map((t) => (t.id === newTodo.id ? { ...t, ...newTodo } : t))\n    );\n    return { prev };\n  },\n  onError: (err, newTodo, ctx) => queryClient.setQueryData(['todos'], ctx.prev),\n  onSettled: () => queryClient.invalidateQueries({ queryKey: ['todos'] }),\n});\n```",tech:"react",displayNumber:345,sortKey:1317},{id:"react-1318",number:346,title:"How does React Query handle background refetching and stale-while-revalidate?",question:"How does React Query handle background refetching and stale-while-revalidate?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"By default, React Query refetches stale data in the background on window focus, network reconnect, and remount, while showing cached data immediately (stale-while-revalidate). It only forces a loading spinner on the first fetch; subsequent background refetches set `isFetching` while `data` remains available. This keeps UIs responsive with always-fairly-fresh data. Configure via `refetchOnWindowFocus`, `refetchOnReconnect`, `refetchInterval`.\n\n```jsx\nuseQuery({\n  queryKey: ['news'],\n  queryFn: fetchNews,\n  refetchOnWindowFocus: true,   // background refresh on tab focus\n  refetchInterval: 60_000,      // poll every minute\n});\n```",tech:"react",displayNumber:346,sortKey:1318},{id:"react-1319",number:347,title:"How do you invalidate queries in React Query?",question:"How do you invalidate queries in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Use `queryClient.invalidateQueries({ queryKey })`. It marks matching queries as stale and triggers a refetch for those currently mounted/observed. You can target a specific key (`['todos', id]`) or a prefix (`['todos']`) to refetch many. After mutations, invalidation is the standard way to reconcile server state without manually updating the cache.\n\n```jsx\nconst queryClient = useQueryClient();\nqueryClient.invalidateQueries({ queryKey: ['todos'] });       // refetch all todos\nqueryClient.invalidateQueries({ queryKey: ['todos', 1] });    // only todo #1\n```",tech:"react",displayNumber:347,sortKey:1319},{id:"react-1320",number:348,title:"What is the `QueryClient` and `QueryClientProvider`?",question:"What is the `QueryClient` and `QueryClientProvider`?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`QueryClient` is the central cache/manager for all queries and mutations. `QueryClientProvider` wires it into the React tree (wrap your app once). You can configure defaults (`defaultOptions`) like `staleTime`/`retry` on the client. It also exposes imperative methods: `invalidateQueries`, `setQueryData`, `getQueryData`, `prefetchQuery`.\n\n```jsx\nconst queryClient = new QueryClient({\n  defaultOptions: { queries: { staleTime: 30_000, retry: 1 } },\n});\n\n<QueryClientProvider client={queryClient}>\n  <App />\n</QueryClientProvider>;\n```",tech:"react",displayNumber:348,sortKey:1320},{id:"react-1321",number:349,title:"How do you prefetch data with React Query?",question:"How do you prefetch data with React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `queryClient.prefetchQuery({ queryKey, queryFn })` to populate the cache before a component mounts (e.g., on hover or route transition), so the data is instantly available. With SSR you can prefetch on the server and hydrate via `HydrationBoundary` / `dehydrate`. Prefetching improves perceived performance for likely-next views.\n\n```jsx\nconst queryClient = useQueryClient();\n<Link\n  onMouseEnter={() => queryClient.prefetchQuery({\n    queryKey: ['user', id],\n    queryFn: () => fetchUser(id),\n  })}\n/>;\n```",tech:"react",displayNumber:349,sortKey:1321},{id:"react-1322",number:350,title:"How do you share query results across components without duplicate fetches?",question:"How do you share query results across components without duplicate fetches?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"React Query caches by `queryKey` at the `QueryClient` level, so any component using the same key shares one cache entry and dedupes concurrent requests. Just reuse the same `queryKey` + `queryFn` shape. No context or prop drilling needed — multiple components mounting the same query only trigger a single network call.\n\n```jsx\n// Both components mount -> only ONE network request\nfunction Header() { useQuery({ queryKey: ['user'], queryFn }); }\nfunction Sidebar() { useQuery({ queryKey: ['user'], queryFn }); }\n```",tech:"react",displayNumber:350,sortKey:1322},{id:"react-1323",number:351,title:"What is `keepPreviousData` / placeholder data in React Query?",question:"What is `keepPreviousData` / placeholder data in React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`placeholderData: keepPreviousData` keeps the previous query's `data` visible while a new query (e.g., new page/filter) loads, instead of showing a loading state — enabling smooth pagination/infinite scroll. `placeholderData` can also be a function for initial/fallback data. With it, `isPlaceholderData` is true so you can dim stale content.\n\n```jsx\nconst { data, isPlaceholderData } = useQuery({\n  queryKey: ['projects', page],\n  queryFn: () => fetchProjects(page),\n  placeholderData: keepPreviousData, // no flash of loading on page change\n});\n```",tech:"react",displayNumber:351,sortKey:1323},{id:"react-1324",number:352,title:"How do you paginate data with React Query?",question:"How do you paginate data with React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Two common approaches: (1) Manual page keys `['items', page]` with `keepPreviousData` for traditional pagination. (2) `useInfiniteQuery` with `getNextPageParam` for infinite scroll, exposing `fetchNextPage` and a flattened `data.pages`. Choose infinite for feeds, manual for paged tables.\n\n```jsx\nconst { data, fetchNextPage, hasNextPage } = useInfiniteQuery({\n  queryKey: ['feed'],\n  queryFn: ({ pageParam = 0 }) => fetchFeed(pageParam),\n  initialPageParam: 0,\n  getNextPageParam: (last) => last.nextCursor,\n});\n```",tech:"react",displayNumber:352,sortKey:1324},{id:"react-1325",number:353,title:"How does React Query integrate with Redux or Zustand?",question:"How does React Query integrate with Redux or Zustand?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Keep server state in React Query and client/UI state in Redux/Zustand — don't duplicate fetched data into Redux. If needed, write mutations/invalidation in RTK Query or React Query, and keep UI flags (modal open, selected tab) in Zustand/Redux. You can call `queryClient` from anywhere, and read React Query cache via selectors if bridging is required, but separation is the recommended architecture.\n\n```jsx\n// Client UI state in Zustand, server data in React Query\nconst isModalOpen = useStore((s) => s.isModalOpen);\nconst { data } = useQuery({ queryKey: ['todos'], queryFn: fetchTodos });\n```",tech:"react",displayNumber:353,sortKey:1325},{id:"react-1326",number:354,title:"What are React Query devtools and how do you use them?",question:"What are React Query devtools and how do you use them?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"`@tanstack/react-query-devtools` renders a panel showing every query's state (fresh/stale/fetching), cache data, observers, and timing. It helps debug caching, see why a refetch happened, and inspect query keys. Install the package, add `<ReactQueryDevtools />` inside the provider. Essential for understanding cache behavior during development.\n\n```jsx\nimport { ReactQueryDevtools } from '@tanstack/react-query-devtools';\n\n<QueryClientProvider client={queryClient}>\n  <App />\n  <ReactQueryDevtools initialIsOpen={false} />\n</QueryClientProvider>;\n```",tech:"react",displayNumber:354,sortKey:1326},{id:"react-1327",number:355,title:"How do you handle errors and retries in React Query?",question:"How do you handle errors and retries in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"By default React Query retries failed queries 3 times with exponential backoff. Configure via `retry` (false/number/function), `retryDelay`, and `staleTime`. Surface errors with `isError`/`error` from the hook or via `onError` callbacks. For mutations, handle `onError` to show toasts. Throwing in `queryFn` triggers error state; non-2xx fetch needs manual throw.\n\n```jsx\nuseQuery({\n  queryKey: ['user'],\n  queryFn: fetchUser,\n  retry: 2,\n  retryDelay: (attempt) => Math.min(1000 * 2 ** attempt, 8000),\n  onError: (err) => toast.error(err.message),\n});\n```",tech:"react",displayNumber:355,sortKey:1327},{id:"react-1328",number:356,title:"What is the difference between Redux Toolkit Query and React Query?",question:"What is the difference between Redux Toolkit Query and React Query?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Both solve server-state caching. RTK Query is tightly coupled to Redux (uses the store, reducers, middleware, devtools) and is great if you already use Redux and want one ecosystem. React Query (TanStack) is standalone, framework-agnostic-ish, with richer features (infinite queries, prefetch, devtools, suspense) and no Redux dependency. Choose based on whether you're already in the Redux ecosystem. They're largely interchangeable for fetching/caching.

\`\`\`jsx
// RTK Query: lives in Redux store, needs reducerPath + middleware
useGetPostsQuery();
// React Query: standalone QueryClientProvider, no store needed
useQuery({ queryKey: ['posts'], queryFn });
\`\`\``,tech:"react",displayNumber:356,sortKey:1328},{id:"react-1329",number:357,title:"How do you use React Query with Suspense?",question:"How do you use React Query with Suspense?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Set `useQuery({ queryKey, queryFn, suspense: true })` (or global default). With Suspense, the hook throws a promise while loading, so wrap the component in `<Suspense fallback={...}>`. Errors propagate to an error boundary. This lets you use declarative loading boundaries instead of `isLoading` flags. SSR hydration must also support suspense.\n\n```jsx\nfunction User() {\n  const { data } = useQuery({ queryKey: ['user'], queryFn, suspense: true });\n  return <h1>{data.name}</h1>; // throws promise until resolved\n}\n<Suspense fallback={<Spinner />}><User /></Suspense>;\n```",tech:"react",displayNumber:357,sortKey:1329},{id:"react-1330",number:358,title:"What is the `select` option in React Query?",question:"What is the `select` option in React Query?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`select` transforms the query data before returning it: `useQuery({ queryKey, queryFn, select: (data) => data.filter(d => !d.done) })`. It memoizes the transform and prevents re-renders when the derived result is unchanged (by reference). Useful for deriving/formatting without extra state or effects. Note: `select` runs on every render unless the input data changed.\n\n```jsx\nconst { data: active } = useQuery({\n  queryKey: ['todos'],\n  queryFn: fetchTodos,\n  select: (todos) => todos.filter((t) => !t.done),\n});\n```",tech:"react",displayNumber:358,sortKey:1330},{id:"react-1331",number:359,title:"How do you set and get query cache data imperatively?",question:"How do you set and get query cache data imperatively?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `queryClient.setQueryData(queryKey, updater)` to update a cache entry directly (e.g., append a newly created item without refetch) and `queryClient.getQueryData(queryKey)` to read it. This is common in optimistic updates and after mutations to keep the cache in sync. Keys must match exactly (same serialization) as the original query.\n\n```jsx\nqueryClient.setQueryData(['todos'], (old = []) => [...old, newTodo]);\nconst current = queryClient.getQueryData(['todos']);\n```",tech:"react",displayNumber:359,sortKey:1331},{id:"react-1332",number:360,title:"How do you structure a large Redux application with slices?",question:"How do you structure a large Redux application with slices?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Organize by feature (ducks pattern): each feature folder has its slice (`createSlice`), selectors, thunks/RTK Query APIs, and types. Combine via `configureStore`'s reducer map keyed by feature. Use `createEntityAdapter` for normalized collections, `createSelector` (reselect) for memoized derived data, and colocate async via `createAsyncThunk`. Keep cross-feature dependencies minimal and shared data in dedicated slices.\n\n```jsx\n// features/todos/todosSlice.js\nconst todosSlice = createSlice({ name: 'todos', initialState, reducers: {...} });\n// store: { reducer: { todos: todosSlice.reducer, user: userSlice.reducer } }\n```",tech:"react",displayNumber:360,sortKey:1332},{id:"react-1333",number:361,title:"What is `createEntityAdapter` and when should you use it?",question:"What is `createEntityAdapter` and when should you use it?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`createEntityAdapter` normalizes a collection into `{ ids: [], entities: {} }` for efficient lookups and updates by id, with prebuilt reducers (`setAll`, `addOne`, `upsertMany`, `removeOne`) and selectors (`selectAll`, `selectById`, `selectIds`). Use it whenever you manage a list of entities fetched from a server — it avoids manual normalization and gives O(1) access.\n\n```jsx\nconst adapter = createEntityAdapter({ selectId: (u) => u.id });\nconst slice = createSlice({\n  name: 'users',\n  initialState: adapter.getInitialState(),\n  reducers: {\n    usersReceived: adapter.setAll,\n    userAdded: adapter.addOne,\n  },\n});\nconst selectors = adapter.getSelectors((s) => s.users);\n// selectors.selectAll(state) -> array; selectors.selectById(state, id)\n```",tech:"react",displayNumber:361,sortKey:1333},{id:"react-1334",number:362,title:"What is `reselect` / `createSelector` and why is it used?",question:"What is `reselect` / `createSelector` and why is it used?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"`createSelector` builds memoized selectors: they only recompute when input selectors change (by reference), preventing expensive derivations on every render and avoiding new object references that cause re-renders. RTK re-exports `createSelector`. Use it for filtered/sorted lists or combined slices derived from state. Input selectors read raw state; the output is memoized.\n\n```jsx\nimport { createSelector } from '@reduxjs/toolkit';\nconst selectTodos = (s) => s.todos.items;\nconst selectActive = createSelector(selectTodos, (items) =>\n  items.filter((t) => !t.done)\n); // recomputes only when items reference changes\n```",tech:"react",displayNumber:362,sortKey:1334},{id:"react-1335",number:363,title:"How do you connect Redux to React components (`useSelector`, `useDispatch`)?",question:"How do you connect Redux to React components (`useSelector`, `useDispatch`)?",difficulty:"Basic",category:"State Management & Ecosystem",answer:"With React-Redux, `useSelector((state) => state.x)` reads a slice (re-renders when it changes by `Object.is`), and `useDispatch()` returns the dispatch function to send actions. Always select narrowly and return primitives or memoized data to avoid re-render issues. For actions, import them from slices and dispatch directly.\n\n```jsx\nimport { useSelector, useDispatch } from 'react-redux';\nimport { increment } from './counterSlice';\n\nfunction Counter() {\n  const count = useSelector((s) => s.counter.n);\n  const dispatch = useDispatch();\n  return <button onClick={() => dispatch(increment())}>{count}</button>;\n}\n```",tech:"react",displayNumber:363,sortKey:1335},{id:"react-1336",number:364,title:"What are common performance pitfalls with Redux and Context?",question:"What are common performance pitfalls with Redux and Context?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"(1) Returning new object/array references from `useSelector`/Context value causes infinite re-renders — use memoized selectors or `useShallow`. (2) Context value object changes re-render all consumers — split contexts or memoize. (3) Dispatching too frequently or storing rapidly-changing data in global state. (4) Connecting too many components broadly. Mitigate with selectors, memoization, and splitting state.\n\n```jsx\n// Pitfall: new array each render -> re-render loop\nconst bad = useSelector((s) => [...s.items]);\n// Fix\nconst items = useSelector((s) => s.items);\n```",tech:"react",displayNumber:364,sortKey:1336},{id:"react-1337",number:365,title:"How do you test Redux slices and async thunks?",question:"How do you test Redux slices and async thunks?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"For slices, call the reducer with initial state and an action, asserting the returned state (pure, easy). For `createAsyncThunk`, test the lifecycle reducers via `extraReducers` by dispatching `pending/fulfilled/rejected` action objects (use `thunk.fulfilled.match`). Integration tests use a real store with `configureStore` and dispatch thunks, possibly mocking the API. RTK's purity makes reducer tests trivial.\n\n```jsx\nit('adds todo', () => {\n  const state = todosReducer(undefined, addTodo('x'));\n  expect(state.items).toHaveLength(1);\n});\n// async: dispatch real thunk with mocked api, await, assert store state\n```",tech:"react",displayNumber:365,sortKey:1337},{id:"react-1338",number:366,title:"How do you test React Query hooks?",question:"How do you test React Query hooks?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Use `@testing-library/react`'s `renderHook` wrapped in `QueryClientProvider` (with `retry: false` and a `Wrapper`). Provide a `queryFn` or mock `fetch`. Assert on `result.current.data` after `waitFor`/`findBy`. For mutations, call `result.current.mutate` and await. Isolate cache per test by creating a fresh `QueryClient`. You can also use `queryClient.setQueryData` to seed state.\n\n```jsx\nconst wrapper = ({ children }) => (\n  <QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}>\n    {children}\n  </QueryClientProvider>\n);\nconst { result } = renderHook(() => useQuery({ queryKey: ['x'], queryFn: async () => 1 }), { wrapper });\nawait waitFor(() => expect(result.current.data).toBe(1));\n```",tech:"react",displayNumber:366,sortKey:1338},{id:"react-1339",number:367,title:"What is the difference between a selector returning a new object and a primitive, and why does it matter?",question:"What is the difference between a selector returning a new object and a primitive, and why does it matter?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"`useSelector((s) => ({ a: s.a, b: s.b }))` returns a new object reference every call, so Redux's default `Object.is` equality always fails → infinite re-render. Returning a primitive (`s.a`) or using `useShallow`/`createSelector` (memoized) prevents this. The rule: selectors should return stable references or be memoized. This applies equally to Zustand and Context-derived values.\n\n```jsx\n// Risky\nconst x = useSelector((s) => ({ a: s.a, b: s.b }));\n// Safe\nimport { useShallow } from 'react-redux';\nconst x = useSelector(useShallow((s) => ({ a: s.a, b: s.b })));\n```",tech:"react",displayNumber:367,sortKey:1339},{id:"react-1340",number:368,title:"How do you migrate from Redux Core to Redux Toolkit?",question:"How do you migrate from Redux Core to Redux Toolkit?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:"Incrementally: introduce RTK's `configureStore` and keep old reducers (they're still valid reducers). Convert one slice at a time to `createSlice`, replacing hand-written action types/creators with generated ones (keep action type strings stable to avoid breaking existing dispatches). Replace `redux-thunk` manual setup with RTK's built-in thunk. Finally adopt `createAsyncThunk` and `createEntityAdapter` for data slices. RTK is backward compatible with plain reducers, so migration can be gradual.\n\n```jsx\n// Old action type 'todos/add' can be reused:\ncreateSlice({ name: 'todos', initialState, reducers: { add: {...} } });\n// generates 'todos/add' automatically -> existing dispatches still work\n```",tech:"react",displayNumber:368,sortKey:1340},{id:"react-1341",number:369,title:"When would you choose Zustand over Redux Toolkit for a new project?",question:"When would you choose Zustand over Redux Toolkit for a new project?",difficulty:"Intermediate",category:"State Management & Ecosystem",answer:"Choose Zustand when you want minimal boilerplate, no provider, fast setup, built-in persist/devtools, and a small-to-medium app where the strict structure of Redux isn't needed. Choose Redux Toolkit when you need a battle-tested ecosystem, RTK Query, time-travel devtools, heavy team conventions, or complex middleware/Saga orchestration. Both handle client state well; Zustand wins on simplicity, Redux on tooling/scale.\n\n```jsx\n// Zustand: ~5 lines for a global store\nconst useStore = create((set) => ({ n: 0, inc: () => set((s) => ({ n: s.n + 1 })) }));\n// Redux: slice + store + provider + hooks\n```",tech:"react",displayNumber:369,sortKey:1341},{id:"react-1342",number:370,title:"How do you decide between Context, Redux, Zustand, Jotai, and React Query for a given problem?",question:"How do you decide between Context, Redux, Zustand, Jotai, and React Query for a given problem?",difficulty:"Advanced",category:"State Management & Ecosystem",answer:`Decision heuristic: (1) Server data (fetching/caching/sync) -> React Query or RTK Query. (2) Rarely-changing global config (theme, auth, locale) -> Context. (3) Simple global client state, low boilerplate -> Zustand. (4) Atomic/fine-grained derived state or component-level isolation -> Jotai/Recoil. (5) Large, complex, team-convention-heavy apps needing devtools/middleware/RTK Query -> Redux Toolkit. Keep server state and client state in separate tools; never put fetched data in Context/Redux unnecessarily.

\`\`\`jsx
// Server state
useQuery({ queryKey: ['user'], queryFn });
// Client UI state
const theme = useContext(ThemeContext);           // static config
const [open, setOpen] = useStore((s) => s.modal); // Zustand
// Atomic
const [n, setN] = useAtom(countAtom);             // Jotai
// Complex global logic
useSelector((s) => s.counter.n);                  // Redux Toolkit
\`\`\``,tech:"react",displayNumber:370,sortKey:1342},{id:"react-1343",number:371,title:"What is React Router and why is it necessary for SPAs?",question:"What is React Router and why is it necessary for SPAs?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`React Router is the standard routing library for React applications. It enables client-side routing in Single Page Applications (SPAs) by synchronizing the UI with the URL without making full page requests to the server. Without a router, an SPA would show the same UI regardless of the URL, making bookmarking, sharing links, and browser navigation impossible. React Router provides declarative routing through components, allowing you to map URL paths to specific React components.

\`\`\`jsx
// Without React Router - URL never changes UI
function App() {
  return <HomePage />; // Always shows HomePage regardless of URL
}

// With React Router - URL drives UI
import { BrowserRouter, Routes, Route } from 'react-router-dom';

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path='/' element={<HomePage />} />
        <Route path='/about' element={<AboutPage />} />
        <Route path='/users/:id' element={<UserProfile />} />
      </Routes>
    </BrowserRouter>
  );
}
\`\`\``,tech:"react",displayNumber:371,sortKey:1343},{id:"react-1344",number:372,title:"What is the difference between BrowserRouter, HashRouter, and MemoryRouter?",question:"What is the difference between BrowserRouter, HashRouter, and MemoryRouter?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`- BrowserRouter uses the HTML5 History API to keep UI in sync with the URL, producing clean URLs like /users/123. It requires server-side configuration so that deep links resolve to index.html.
- HashRouter stores the location in the URL hash (#/users/123). It works on any static host without server configuration and is useful for legacy browsers, but creates uglier URLs and the hash is never sent to the server.
- MemoryRouter keeps history in memory (no URL change). It is ideal for tests, React Native, and server-side rendering where there is no browser history.

\`\`\`jsx
import { BrowserRouter, HashRouter, MemoryRouter } from 'react-router-dom';

// Production web app
<BrowserRouter><App /></BrowserRouter>;

// Static host with no server rewrites
<HashRouter><App /></HashRouter>;

// Tests / non-browser environments
<MemoryRouter initialEntries={['/users/1']}><App /></MemoryRouter>;
\`\`\``,tech:"react",displayNumber:372,sortKey:1344},{id:"react-1345",number:373,title:"How do you define routes in React Router v6 with Routes and Route?",question:"How do you define routes in React Router v6 with Routes and Route?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"In v6, routes are defined using the `<Routes>` component which replaces the older `<Switch>`. You nest `<Route>` elements with a `path` and an `element`. The `Routes` component renders the first matching route. Paths are relative and you can nest routes to build layouts. There is no longer an `exact` prop, because v6 does ranking/score-based matching automatically and uses the most specific match.\n\n```jsx\nimport { Routes, Route } from 'react-router-dom';\n\nfunction App() {\n  return (\n    <Routes>\n      <Route path='/' element={<Layout />}>\n        <Route index element={<Home />} />\n        <Route path='about' element={<About />} />\n        <Route path='users/:id' element={<UserProfile />} />\n        <Route path='*' element={<NotFound />} />\n      </Route>\n    </Routes>\n  );\n}\n```",tech:"react",displayNumber:373,sortKey:1345},{id:"react-1346",number:374,title:"What is the difference between Link, NavLink, and useNavigate?",question:"What is the difference between Link, NavLink, and useNavigate?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`- \`<Link>\` renders an accessible anchor that performs client-side navigation via the History API without a full reload.
- \`<NavLink>\` is a special Link that adds styling hooks (className or style functions) when the link is active, perfect for navigation menus.
- \`useNavigate()\` is a hook returning a function used for imperative navigation inside event handlers or effects (e.g., redirect after a form submit).

\`\`\`jsx
import { Link, NavLink, useNavigate } from 'react-router-dom';

function Nav() {
  const navigate = useNavigate();
  return (
    <nav>
      <NavLink to='/' style={({ isActive }) => ({ color: isActive ? 'red' : 'black' })}>Home</NavLink>
      <Link to='/about'>About</Link>
      <button onClick={() => navigate('/login')}>Go login</button>
    </nav>
  );
}
\`\`\``,tech:"react",displayNumber:374,sortKey:1346},{id:"react-1347",number:375,title:"How do you read route parameters with useParams?",question:"How do you read route parameters with useParams?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`\`useParams()\` is a hook that returns an object of key/value pairs of the dynamic segments defined in the route path. For a route defined as /users/:id, the component rendered at that route can read the id via useParams(). Values are always strings and must be type-coerced when needed.

\`\`\`jsx
import { useParams } from 'react-router-dom';

function UserProfile() {
  const { id, tab } = useParams(); // for path /users/:id/:tab
  return <h1>User {id}, tab {tab}</h1>;
}
\`\`\``,tech:"react",displayNumber:375,sortKey:1347},{id:"react-1348",number:376,title:"How do you read and update query strings with useSearchParams?",question:"How do you read and update query strings with useSearchParams?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`\`useSearchParams()\` is a hook that returns a tuple: the current URLSearchParams and a setter function. It behaves like useState but is backed by the URL query string. You read values with \`.get('key')\`, and update them via the setter which accepts a URLSearchParams instance or a function. This makes shareable, bookmarkable filters and pagination state a natural fit for the URL.

\`\`\`jsx
import { useSearchParams } from 'react-router-dom';

function Products() {
  const [params, setParams] = useSearchParams();
  const page = params.get('page') ?? '1';
  const q = params.get('q') ?? '';

  const onSearch = (text) => {
    setParams(prev => {
      const next = new URLSearchParams(prev);
      next.set('q', text);
      next.set('page', '1');
      return next;
    });
  };

  return <input defaultValue={q} onChange={e => onSearch(e.target.value)} />;
}
\`\`\``,tech:"react",displayNumber:376,sortKey:1348},{id:"react-1349",number:377,title:"What changed from React Router v5 to v6?",question:"What changed from React Router v5 to v6?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Key v5 to v6 changes: `Switch` was replaced by `Routes`; routes are now defined with `element` prop instead of `component`/`render`; the `exact` prop was removed because v6 uses ranked matching; nested routes use relative paths and a parent layout with `<Outlet/>`; `useHistory` became `useNavigate`; redirects use `<Navigate>` or the `navigate` function; and `useRoutes` allows defining routes as JavaScript objects for better composition.\n\n```jsx\n// v5\n<Switch>\n  <Route exact path='/' component={Home} />\n  <Redirect to='/login' />\n</Switch>\n\n// v6\n<Routes>\n  <Route path='/' element={<Home />} />\n  <Route path='/login' element={<Navigate to='/dashboard' replace />} />\n</Routes>\n```",tech:"react",displayNumber:377,sortKey:1349},{id:"react-1350",number:378,title:"How do you create nested routes and use Outlet?",question:"How do you create nested routes and use Outlet?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Nested routes let a parent route render a layout while child routes render inside it via \`<Outlet />\`. You define children as nested \`<Route>\` elements. The parent component places \`<Outlet />\` where the child should appear. This avoids repeating shared UI like navbars and sidebars across pages.

\`\`\`jsx
function Dashboard() {
  return (
    <div>
      <Sidebar />
      <Outlet /> {/* child route renders here */}
    </div>
  );
}

<Routes>
  <Route path='dashboard' element={<Dashboard />}>
    <Route index element={<Overview />} />
    <Route path='stats' element={<Stats />} />
  </Route>
</Routes>
\`\`\``,tech:"react",displayNumber:378,sortKey:1350},{id:"react-1351",number:379,title:"How do you implement protected (authenticated) routes in React Router v6?",question:"How do you implement protected (authenticated) routes in React Router v6?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`A common pattern is a wrapper component, often called \`<RequireAuth>\`, that checks an auth state and either renders \`<Outlet/>\` (allowing children to render) or redirects to a login page using \`<Navigate replace />\`. The protected route is then placed in the tree with the wrapper as its element and the real pages as children. Using \`replace\` prevents the protected URL from staying in history after redirect.

\`\`\`jsx
function RequireAuth() {
  const auth = useAuth();
  const location = useLocation();
  if (!auth.user) {
    return <Navigate to='/login' replace state={{ from: location }} />;
  }
  return <Outlet />;
}

<Route element={<RequireAuth />}>
  <Route path='profile' element={<Profile />} />
  <Route path='settings' element={<Settings />} />
</Route>
\`\`\``,tech:"react",displayNumber:379,sortKey:1351},{id:"react-1352",number:380,title:"What are loaders and actions in React Router (data routers)?",question:"What are loaders and actions in React Router (data routers)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Data routers (created via `createBrowserRouter` / `RouterProvider`) let you define `loader` and `action` functions on routes. A `loader` runs before rendering to fetch data, returning a promise that the component reads with `useLoaderData()`. An `action` handles mutations (often from forms) and runs on submission. This co-locates data fetching and mutations with routes and supports pending/error states via `useNavigation()` and `errorElement`.\n\n```jsx\nconst router = createBrowserRouter([\n  {\n    path: '/users/:id',\n    element: <User />,\n    loader: async ({ params }) => fetchUser(params.id),\n    action: async ({ request, params }) => updateUser(params.id, await request.formData()),\n    errorElement: <ErrorPage />,\n  },\n]);\n```",tech:"react",displayNumber:380,sortKey:1352},{id:"react-1353",number:381,title:"How do you access loader data with useLoaderData and redirect with loaders?",question:"How do you access loader data with useLoaderData and redirect with loaders?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Inside a component rendered by a route with a loader, call \`useLoaderData()\` to get the value the loader returned. Loaders can also call the \`redirect()\` helper to send the user elsewhere (e.g., to login when unauthenticated) instead of returning data. The redirected response is handled by the router automatically.

\`\`\`jsx
import { useLoaderData, redirect } from 'react-router-dom';

export async function loader({ request }) {
  const user = await getSession(request);
  if (!user) return redirect('/login');
  return user;
}

function Account() {
  const user = useLoaderData();
  return <h1>{user.name}</h1>;
}
\`\`\``,tech:"react",displayNumber:381,sortKey:1353},{id:"react-1354",number:382,title:"What is the purpose of the Form component in React Router and how does it relate to actions?",question:"What is the purpose of the Form component in React Router and how does it relate to actions?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"React Router's `<Form>` component is a drop-in replacement for the HTML `<form>` that intercepts submission and sends the data to the route's `action` instead of causing a navigation/document reload. It uses the method (GET/POST) and action (defaults to the current route). Combined with `action` functions and `useNavigation`, you get progressive-enhancement-friendly forms with built-in pending states.\n\n```jsx\nimport { Form, useNavigation } from 'react-router-dom';\n\nfunction NewPost() {\n  const nav = useNavigation();\n  const submitting = nav.state === 'submitting';\n  return (\n    <Form method='post'>\n      <input name='title' />\n      <button disabled={submitting}>{submitting ? 'Saving…' : 'Save'}</button>\n    </Form>\n  );\n}\n```",tech:"react",displayNumber:382,sortKey:1354},{id:"react-1355",number:383,title:"How do you handle errors in routes using errorElement and useRouteError?",question:"How do you handle errors in routes using errorElement and useRouteError?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Each route can declare an `errorElement` which renders when its loader, action, or component throws. Inside that element, `useRouteError()` returns the thrown value so you can show a meaningful message. A root `errorElement` catches errors for the whole tree. This replaces try/catch sprawl with a declarative, per-route error boundary.\n\n```jsx\nimport { useRouteError, isRouteErrorResponse } from 'react-router-dom';\n\nfunction ErrorPage() {\n  const error = useRouteError();\n  if (isRouteErrorResponse(error)) {\n    return <h1>{error.status} {error.statusText}</h1>;\n  }\n  return <h1>Oops: {error.message}</h1>;\n}\n```",tech:"react",displayNumber:383,sortKey:1355},{id:"react-1356",number:384,title:"How does useNavigation help with loading and submitting states?",question:"How does useNavigation help with loading and submitting states?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`useNavigation()` returns the current navigation state of the router: `idle`, `loading`, or `submitting`. You use it to show spinners or disable buttons while loaders are running or forms are being submitted. `navigation.location` tells you the destination during a transition, which is handy for optimistic highlighting of the next route.\n\n```jsx\nfunction Layout() {\n  const nav = useNavigation();\n  return (\n    <div>\n      {nav.state === 'loading' && <GlobalSpinner />}\n      <Outlet />\n    </div>\n  );\n}\n```",tech:"react",displayNumber:384,sortKey:1356},{id:"react-1357",number:385,title:"What is the difference between useLocation, useNavigate, and useMatch?",question:"What is the difference between useLocation, useNavigate, and useMatch?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"- `useLocation()` returns the current location object ({ pathname, search, hash, state }) and is useful for reading the full URL or reacting to changes.\n- `useNavigate()` returns a function to programmatically change the route.\n- `useMatch(pattern)` returns match data if the current location matches a given path pattern, useful for conditional logic without rendering a Route.\n\n```jsx\nconst location = useLocation();\nconst navigate = useNavigate();\nconst match = useMatch('/users/:id');\nconsole.log(location.search, match?.params.id);\n```",tech:"react",displayNumber:385,sortKey:1357},{id:"react-1358",number:386,title:"How do you pass state through navigation and read it at the destination?",question:"How do you pass state through navigation and read it at the destination?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"You can attach a `state` object when navigating via `<Link to='..' state={{ from: 'home' }}>` or `navigate('/x', { state })`. At the destination, `useLocation().state` exposes it. This is ideal for transient data like \"redirect back to\" URLs or modal context without putting it in the query string.\n\n```jsx\n<Link to='/checkout' state={{ productId: 42 }}>Buy</Link>;\n\nfunction Checkout() {\n  const { state } = useLocation();\n  return <p>Product: {state?.productId}</p>;\n}\n```",tech:"react",displayNumber:386,sortKey:1358},{id:"react-1359",number:387,title:"How do you handle redirects with the Navigate component and replace?",question:"How do you handle redirects with the Navigate component and replace?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`<Navigate to='/path' />` renders and immediately navigates to the target. The `replace` prop replaces the current entry in history instead of pushing a new one, which is what you want after a login redirect so the user does not bounce back to the protected page on \"back\". You can also drive it conditionally.\n\n```jsx\nfunction OldRoute() {\n  return <Navigate to='/new-route' replace />;\n}\n```",tech:"react",displayNumber:387,sortKey:1359},{id:"react-1360",number:388,title:"What is a controlled component in React forms?",question:"What is a controlled component in React forms?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"A controlled component is a form input whose value is driven by React state. You set `value={state}` and update state in an `onChange` handler. React becomes the single source of truth, which makes validation, conditional UI, and formatting straightforward. The trade-off is more re-renders, though for typical forms this is negligible.\n\n```jsx\nfunction Controlled() {\n  const [name, setName] = useState('');\n  return <input value={name} onChange={e => setName(e.target.value)} />;\n}\n```",tech:"react",displayNumber:388,sortKey:1360},{id:"react-1361",number:389,title:"What is an uncontrolled component and when would you use one?",question:"What is an uncontrolled component and when would you use one?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"An uncontrolled component stores its own state in the DOM. You read values via a ref (`inputRef.current.value`) rather than React state. It is useful for simple forms, integrating with non-React libraries, large forms where you want to avoid many re-renders, or using native features like file inputs. The default value is set with `defaultValue`/`defaultChecked`.\n\n```jsx\nfunction Uncontrolled() {\n  const ref = useRef(null);\n  const submit = () => alert(ref.current.value);\n  return <input ref={ref} defaultValue='initial' />;\n}\n```",tech:"react",displayNumber:389,sortKey:1361},{id:"react-1362",number:390,title:"Compare controlled vs uncontrolled components with pros and cons.",question:"Compare controlled vs uncontrolled components with pros and cons.",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Controlled components keep form state in React, enabling instant validation, conditional fields, and dynamic formatting, but cause a re-render per keystroke. Uncontrolled components keep state in the DOM, reducing re-renders and easing integration with third-party widgets, but you must imperatively query values and get validation only on submit. Rule of thumb: use controlled for interactive forms, uncontrolled for performance-sensitive or simple/embedded cases.

\`\`\`jsx
// Controlled: validation as you type
const [email, setEmail] = useState('');
const invalid = !email.includes('@');

// Uncontrolled: read once at submit
const ref = useRef();
const onSubmit = () => console.log(ref.current.value);
\`\`\``,tech:"react",displayNumber:390,sortKey:1362},{id:"react-1363",number:391,title:"What is React Hook Form and why is it performant?",question:"What is React Hook Form and why is it performant?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`React Hook Form (RHF) is a library for building forms with minimal re-renders. Unlike controlled inputs that re-render on every keystroke, RHF uses uncontrolled inputs registered via \`ref\` and reads values on demand, subscribing components selectively to only the fields they care about. It uses native validation and isolates re-renders to individual inputs through its subscription model, which makes it fast even with large forms.

\`\`\`jsx
// RHF avoids re-rendering the whole form on each keystroke
const { register, handleSubmit } = useForm();
const onSubmit = (data) => console.log(data);
<form onSubmit={handleSubmit(onSubmit)}>
  <input {...register('email')} />
</form>;
\`\`\``,tech:"react",displayNumber:391,sortKey:1363},{id:"react-1364",number:392,title:"How do you build a form with useForm, register, and handleSubmit?",question:"How do you build a form with useForm, register, and handleSubmit?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Call `useForm()` to get `register`, `handleSubmit`, `formState`, etc. Spread `register('name')` onto each input to wire it up. `handleSubmit(onValid, onInvalid)` wraps your submit handler and runs validation before calling onValid with the form values. RHF collects all registered values for you, no manual state needed.\n\n```jsx\nimport { useForm } from 'react-hook-form';\n\nfunction Signup() {\n  const { register, handleSubmit, formState: { errors } } = useForm();\n  const submit = (data) => console.log(data);\n  return (\n    <form onSubmit={handleSubmit(submit)}>\n      <input {...register('email', { required: 'Email required' })} />\n      {errors.email && <span>{errors.email.message}</span>}\n      <button type='submit'>Go</button>\n    </form>\n  );\n}\n```",tech:"react",displayNumber:392,sortKey:1364},{id:"react-1365",number:393,title:"Explain formState in React Hook Form (errors, isSubmitting, isValid, dirtyFields).",question:"Explain formState in React Hook Form (errors, isSubmitting, isValid, dirtyFields).",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`formState` is a proxy object exposing form status: `errors` holds validation messages, `isSubmitting` is true during async submission, `isValid` reflects whether the form passes validation, `dirtyFields` marks which fields changed, and `isDirty`/`touchedFields` track interaction. Because formState is a proxy, you must destructure the fields you use so RHF subscribes to them and triggers re-renders correctly.\n\n```jsx\nconst { formState: { errors, isSubmitting, isValid } } = useForm({ mode: 'onChange' });\n// Destructure to subscribe; isSubmitting drives the button spinner\n```",tech:"react",displayNumber:393,sortKey:1365},{id:"react-1366",number:394,title:"How do you validate with React Hook Form rules and with a schema (zodResolver)?",question:"How do you validate with React Hook Form rules and with a schema (zodResolver)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"RHF supports inline rules via `register('field', { required, minLength, pattern })`. For complex validation, you use a resolver (e.g., `@hookform/resolvers/zod` with `zodResolver(schema)`). A Zod schema centralizes types and validation, giving you end-to-end type safety and reusable, composable rules that also validate server payloads.\n\n```jsx\nimport { z } from 'zod';\nimport { zodResolver } from '@hookform/resolvers/zod';\n\nconst schema = z.object({ email: z.string().email(), age: z.number().min(18) });\nconst { register, handleSubmit } = useForm({ resolver: zodResolver(schema) });\n```",tech:"react",displayNumber:394,sortKey:1366},{id:"react-1367",number:395,title:"What are the performance benefits of React Hook Form compared to controlled forms?",question:"What are the performance benefits of React Hook Form compared to controlled forms?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`RHF minimizes re-renders by using uncontrolled inputs and a ref-based registry, so typing in one field does not re-render the whole component. It isolates subscriptions so only components reading a given field re-render. Controlled forms re-render the parent on every keystroke. For large forms, RHF's approach yields significantly better input latency and lower memory churn.

\`\`\`jsx
// Controlled: every keystroke re-renders App
const [v, setV] = useState('');
<input value={v} onChange={e => setV(e.target.value)} />;

// RHF: input value lives in DOM ref; no re-render per keystroke
<input {...register('name')} />;
\`\`\``,tech:"react",displayNumber:395,sortKey:1367},{id:"react-1368",number:396,title:"How do you use Formik for form state and validation?",question:"How do you use Formik for form state and validation?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Formik manages form state, validation, and submission. You use `useFormik()` or the `<Formik>` component with `initialValues`, `onSubmit`, and `validate` (or a validation schema). `values`, `errors`, `touched`, and `handleChange`/`handleSubmit` come from Formik. It is a controlled approach, so it re-renders on each change, which is simpler to reason about but heavier for large forms than RHF.\n\n```jsx\nimport { useFormik } from 'formik';\n\nfunction Form() {\n  const f = useFormik({\n    initialValues: { email: '' },\n    validate: (v) => (!v.email ? { email: 'Required' } : {}),\n    onSubmit: (v) => console.log(v),\n  });\n  return (\n    <form onSubmit={f.handleSubmit}>\n      <input name='email' value={f.values.email} onChange={f.handleChange} />\n      {f.errors.email && f.touched.email && <span>{f.errors.email}</span>}\n    </form>\n  );\n}\n```",tech:"react",displayNumber:396,sortKey:1368},{id:"react-1369",number:397,title:"How do you validate with Yup and Formik?",question:"How do you validate with Yup and Formik?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:`Yup is a schema builder that pairs naturally with Formik via the \`validationSchema\` prop. Formik runs the schema on change/blur and populates \`errors\`/\`touched\`. This keeps validation declarative and reusable. The same schema can validate on the server, preventing drift between client and API.

\`\`\`jsx
import * as Yup from 'yup';

const schema = Yup.object({
  email: Yup.string().email('Invalid').required('Required'),
  password: Yup.string().min(8, 'Too short'),
});

<Formik validationSchema={schema} initialValues={{ email: '', password: '' }} onSubmit={...}>
  {/* render props give values, errors, touched */}
</Formik>;
\`\`\``,tech:"react",displayNumber:397,sortKey:1369},{id:"react-1370",number:398,title:"Zod vs Yup: when would you choose one for form validation?",question:"Zod vs Yup: when would you choose one for form validation?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Zod is TypeScript-first: its schemas infer static types automatically (`z.infer`), making it ideal in TS projects and with RHF's resolver. Yup is older, mature, and has a fluent API widely used with Formik. Choose Zod when you want type inference and composable schemas in a TS codebase; choose Yup for existing Formik setups or when you prefer its API ergonomics. Both validate at runtime.\n\n```ts\n// Zod infers the type\nconst schema = z.object({ email: z.string().email() });\ntype Form = z.infer<typeof schema>; // { email: string }\n```",tech:"react",displayNumber:398,sortKey:1370},{id:"react-1371",number:399,title:"How do you build dynamic / field-array forms with React Hook Form's useFieldArray?",question:"How do you build dynamic / field-array forms with React Hook Form's useFieldArray?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`useFieldArray({ control, name })` manages arrays of fields with `fields`, `append`, `remove`, and `insert`. It returns stable `id`s for keys. This is how you build repeatable rows (e.g., todo lists, invoice line items) without manually managing array state, and RHF tracks each sub-field's registration automatically.\n\n```jsx\nconst { control, register } = useForm({ defaultValues: { items: [{ name: '' }] } });\nconst { fields, append, remove } = useFieldArray({ control, name: 'items' });\n\nfields.map((field, i) => (\n  <div key={field.id}>\n    <input {...register(`items.${i}.name`)} />\n    <button type='button' onClick={() => remove(i)}>x</button>\n  </div>\n));\n<button type='button' onClick={() => append({ name: '' })}>Add</button>;\n```",tech:"react",displayNumber:399,sortKey:1371},{id:"react-1372",number:400,title:"How do you handle file uploads in React forms?",question:"How do you handle file uploads in React forms?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"File inputs are inherently uncontrolled because `value` cannot be set for security reasons. Read the selected file from `event.target.files[0]`. With RHF, register the input and read `data.file[0]`. Upload via FormData: append the file and POST with `multipart/form-data`. Always validate type and size before uploading, and show upload progress with XHR or a fetch wrapper.\n\n```jsx\nconst onSubmit = async (data) => {\n  const fd = new FormData();\n  fd.append('avatar', data.avatar[0]);\n  await fetch('/upload', { method: 'POST', body: fd });\n};\n<input type='file' {...register('avatar', { validate: f => f[0]?.size < 1e6 || 'Too big' })} />;\n```",tech:"react",displayNumber:400,sortKey:1372},{id:"react-1373",number:401,title:"How do you reset a form and set default values in React Hook Form?",question:"How do you reset a form and set default values in React Hook Form?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"Pass `defaultValues` to `useForm()` for initial values. Use `reset(values)` to programmatically clear or repopulate the form, e.g., after a successful submit or when loading data to edit. `reset()` with no args restores defaults. Note that RHF uses uncontrolled inputs, so defaultValues only apply on mount unless you call reset.\n\n```jsx\nconst { reset, handleSubmit } = useForm({ defaultValues: { name: '' } });\nconst submit = (data) => { console.log(data); reset(); };\n// To load existing record:\nuseEffect(() => { if (user) reset(user); }, [user, reset]);\n```",tech:"react",displayNumber:401,sortKey:1373},{id:"react-1374",number:402,title:"How do you watch form values with the watch method?",question:"How do you watch form values with the watch method?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:`\`watch(name?)\` returns the current value(s) of fields and subscribes the component to changes, causing re-renders when watched fields change (a trade-off versus register, which does not re-render). Use it when UI depends on a value (e.g., showing a password strength meter or conditional field). For non-reactive reads, use \`getValues()\`.

\`\`\`jsx
const { register, watch } = useForm();
const pw = watch('password');
return (
  <>
    <input {...register('password')} />
    <Meter strength={pw?.length ?? 0} />
  </>
);
\`\`\``,tech:"react",displayNumber:402,sortKey:1374},{id:"react-1375",number:403,title:"How do you do conditional / dependent fields in a form?",question:"How do you do conditional / dependent fields in a form?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Use `watch()` (RHF) or the controlled value (Formik) to read one field and conditionally render another. For validation dependencies, reference other fields inside resolver rules or the validate function. Avoid mounting/unmounting inputs you want to preserve; instead use conditional rendering with keys or `shouldUnregister` carefully so values do not leak.\n\n```jsx\nconst country = watch('country');\nreturn (\n  <> \n    <select {...register('country')}>...</select>\n    {country === 'US' && <input {...register('state')} placeholder='State' />}\n  </>\n);\n```",tech:"react",displayNumber:403,sortKey:1375},{id:"react-1376",number:404,title:"What are the best practices for accessible forms (labels, aria, errors)?",question:"What are the best practices for accessible forms (labels, aria, errors)?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Always associate labels with inputs via `htmlFor`/`id` or wrap them. Use `aria-invalid` and `aria-describedby` to connect error messages to inputs for screen readers. Announce errors with `role='alert'` or `aria-live`. Group related controls with `fieldset`/`legend`. Provide clear, specific error text and visible focus styles. Accessible forms improve usability for everyone and are legally required in many contexts.\n\n```jsx\n<label htmlFor='email'>Email</label>\n<input id='email' aria-invalid={!!errors.email} aria-describedby='email-err' {...register('email')} />\n{errors.email && <span id='email-err' role='alert'>{errors.email.message}</span>}\n```",tech:"react",displayNumber:404,sortKey:1376},{id:"react-1377",number:405,title:"How do you test a React component with React Testing Library and Jest?",question:"How do you test a React component with React Testing Library and Jest?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"RTL promotes testing from the user's perspective. Render the component with `render(<Comp />)`, query DOM nodes the way a user finds them (`screen.getByRole`, `getByLabelText`, `getByText`), interact with `userEvent`, and assert on the resulting DOM. Avoid testing implementation details (internal state, method calls). Jest provides the runner, matchers, and mocking utilities.\n\n```jsx\nimport { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\n\ntest('shows greeting', async () => {\n  render(<Greeting name='Ada' />);\n  expect(screen.getByText('Hello Ada')).toBeInTheDocument();\n});\n```",tech:"react",displayNumber:405,sortKey:1377},{id:"react-1378",number:406,title:"Explain the difference between getBy*, queryBy*, and findBy* queries.",question:"Explain the difference between getBy*, queryBy*, and findBy* queries.",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"- `getBy*` returns the matching node or throws immediately if not found (or if multiple match). Use for elements that should be present synchronously.\n- `queryBy*` returns the node or null, never throws. Use for asserting absence (e.g., element removed).\n- `findBy*` returns a promise that resolves when the element appears; use for async content (after fetch/timeout). There are also `getAllBy`/`queryAllBy`/`findAllBy` variants.\n\n```jsx\nscreen.getByText('Saved');          // must exist now\nexpect(screen.queryByText('Error')).toBeNull(); // must be absent\nawait screen.findByText('Loaded');  // appears async\n```",tech:"react",displayNumber:406,sortKey:1378},{id:"react-1379",number:407,title:"How do you simulate user interactions with userEvent?",question:"How do you simulate user interactions with userEvent?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`@testing-library/user-event` provides realistic interaction simulation: `userEvent.click(el)`, `userEvent.type(input, 'text')`, `userEvent.selectOptions`. Prefer it over `fireEvent` because it dispatches the full sequence of events (focus, keydown, input, change) like a real browser. In v14, create an instance `const user = userEvent.setup()` (often in `beforeEach`) and await its methods.\n\n```jsx\nconst user = userEvent.setup();\nawait user.type(screen.getByLabelText('Email'), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: 'Submit' }));\n```",tech:"react",displayNumber:407,sortKey:1379},{id:"react-1380",number:408,title:"How do you test a controlled form with React Testing Library?",question:"How do you test a controlled form with React Testing Library?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Render the form, query the input by role/label, type into it with `userEvent.type`, click submit, then assert the expected outcome (e.g., success message or that the onSubmit handler was called with the typed value). If onSubmit is a mock prop, assert it was called with the correct data. Keep assertions about what the user sees, not internal state.\n\n```jsx\nconst onSubmit = jest.fn();\nrender(<Login onSubmit={onSubmit} />);\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: /sign in/i }));\nexpect(onSubmit).toHaveBeenCalledWith(expect.objectContaining({ email: 'a@b.com' }));\n```",tech:"react",displayNumber:408,sortKey:1380},{id:"react-1381",number:409,title:"How do you test a React Hook Form component?",question:"How do you test a React Hook Form component?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Treat it like any form: register inputs with proper labels/names so they are queryable. Type into fields and submit, then assert the resulting DOM or the mocked submit handler. To test validation, submit empty required fields and assert the error message appears (`getByText`/`findByText`). Access `handleSubmit`'s resolved data via a mock passed to the component or by asserting side effects.\n\n```jsx\nrender(<Signup />);\nawait user.click(screen.getByRole('button', { name: /submit/i }));\nexpect(await screen.findByText(/email required/i)).toBeInTheDocument();\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\n// ... fill rest, submit, assert success\n```",tech:"react",displayNumber:409,sortKey:1381},{id:"react-1382",number:410,title:"How do you mock API calls in tests using MSW (Mock Service Worker)?",question:"How do you mock API calls in tests using MSW (Mock Service Worker)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"MSW intercepts network requests at the service worker (or node) level, so your component code uses real fetch/XHR unmodified. You define `handlers` with `http.get/post` returning mocked responses, set up a `server` in `beforeAll`/`afterEach`/`afterAll`, and optionally override per test with `server.use()`. This avoids mocking fetch internals and tests real request/response flows.\n\n```js\nimport { setupServer } from 'msw/node';\nimport { http, HttpResponse } from 'msw';\n\nconst server = setupServer(\n  http.get('/api/user', () => HttpResponse.json({ id: 1, name: 'Ada' }))\n);\nbeforeAll(() => server.listen());\nafterEach(() => server.resetHandlers());\nafterAll(() => server.close());\n```",tech:"react",displayNumber:410,sortKey:1382},{id:"react-1383",number:411,title:"How do you test async behavior (loading / data fetching) in components?",question:"How do you test async behavior (loading / data fetching) in components?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Render the component, assert the loading state is shown (e.g., a spinner via `getByText('Loading')`), then await the async update with `findBy*` which retries until the resolved content appears. Use `waitFor` for states not tied to a specific element. Provide mocked data (MSW or a mocked module) so the test is deterministic and fast.\n\n```jsx\nrender(<UserList />);\nexpect(screen.getByText(/loading/i)).toBeInTheDocument();\nexpect(await screen.findByText('Ada')).toBeInTheDocument();\n```",tech:"react",displayNumber:411,sortKey:1383},{id:"react-1384",number:412,title:'Why should you avoid testing implementation details, and what is the "user event" testing philosophy?',question:'Why should you avoid testing implementation details, and what is the "user event" testing philosophy?',difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Testing implementation details (internal state, private methods, props of children) makes tests brittle: refactors that preserve behavior break them. RTL's philosophy is to test as a user would, querying by accessible role/text and asserting visible outcomes. This gives confidence that the feature works for real users and keeps tests resilient to internal rewrites.

\`\`\`jsx
// Avoid: testing internal state
// expect(wrapper.state('open')).toBe(true);
// Prefer: testing what the user sees
expect(screen.getByRole('dialog')).toBeInTheDocument();
\`\`\``,tech:"react",displayNumber:412,sortKey:1384},{id:"react-1385",number:413,title:"How do you test routing and navigation with React Router in components?",question:"How do you test routing and navigation with React Router in components?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Wrap the component in a `<MemoryRouter>` with `initialEntries` to control the starting URL. Provide route definitions so links/navigation render the right destinations, then assert navigation happened (e.g., the new page's text appears). Use `initialEntries={['/users/1']}` to test a dynamic param route. For hooks like `useNavigate`, render within the router and assert location changes via a test component that reads `useLocation`.\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/']}>\n    <Routes>\n      <Route path='/' element={<Home />} />\n      <Route path='/about' element={<About />} />\n    </Routes>\n  </MemoryRouter>\n);\nawait user.click(screen.getByRole('link', { name: /about/i }));\nexpect(screen.getByText(/about page/i)).toBeInTheDocument();\n```",tech:"react",displayNumber:413,sortKey:1385},{id:"react-1386",number:414,title:"How do you render a component that uses useParams / useNavigate in tests?",question:"How do you render a component that uses useParams / useNavigate in tests?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Route hooks require a Router context. Render the component inside `<MemoryRouter initialEntries={['/users/42']}>` with a `<Routes><Route path='/users/:id' element={<YourComp/>} /></Routes>`. Then `useParams()` returns `{ id: '42' }`. For `useNavigate`, click a button that navigates and assert the destination renders, or use a spy on navigate via a custom wrapper.\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/users/42']}>\n    <Routes><Route path='/users/:id' element={<UserProfile />} /></Routes>\n  </MemoryRouter>\n);\nexpect(screen.getByText('User 42')).toBeInTheDocument();\n```",tech:"react",displayNumber:414,sortKey:1386},{id:"react-1387",number:415,title:"How do you mock modules with Jest (jest.mock) for testing?",question:"How do you mock modules with Jest (jest.mock) for testing?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`jest.mock('module')` replaces a module with a mock for the whole test file; `jest.mock('module', () => ({ ... }), { virtual: true })` lets you supply a factory. Use `jest.fn()` for functions and control return values per test. Mock components you don't want to render (e.g., heavy charts) to keep tests focused. Remember hoisting: `jest.mock` calls are hoisted to the top regardless of placement.\n\n```jsx\njest.mock('../api', () => ({ fetchUser: jest.fn() }));\nimport { fetchUser } from '../api';\nfetchUser.mockResolvedValue({ name: 'Ada' });\n```",tech:"react",displayNumber:415,sortKey:1387},{id:"react-1388",number:416,title:"What is the role of cleanup() and why does RTL auto-cleanup?",question:"What is the role of cleanup() and why does RTL auto-cleanup?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`cleanup()` unmounts React trees rendered by RTL and removes them from the DOM. Without it, multiple renders in different tests would accumulate nodes and leak state/listeners. RTL auto-configures `afterEach(cleanup)` via its Jest setup, so you usually don't call it manually. It also resets `jest` mocks if configured. Disabling it is rarely needed.\n\n```js\n// Usually automatic via jest.setup with @testing-library/react\nimport '@testing-library/react'; // exports cleanup, auto-wired by jest config\n```",tech:"react",displayNumber:416,sortKey:1388},{id:"react-1389",number:417,title:"How do you use jest.fn() spies and assertions like toHaveBeenCalledWith?",question:"How do you use jest.fn() spies and assertions like toHaveBeenCalledWith?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`jest.fn()` creates a mock function you can pass as a callback/prop. Assertions verify behavior: `toHaveBeenCalled()`, `toHaveBeenCalledWith(args)`, `toHaveBeenCalledTimes(n)`, and `expect.any(Function)`. Inspect calls with `mock.calls`. This is how you test that a child invoked a parent handler with the right payload without rendering the parent's logic.\n\n```jsx\nconst onSave = jest.fn();\nrender(<Editor onSave={onSave} />);\nawait user.click(screen.getByRole('button', { name: /save/i }));\nexpect(onSave).toHaveBeenCalledTimes(1);\nexpect(onSave).toHaveBeenCalledWith(expect.objectContaining({ id: 1 }));\n```",tech:"react",displayNumber:417,sortKey:1389},{id:"react-1390",number:418,title:"How do you test custom hooks with renderHook and act?",question:"How do you test custom hooks with renderHook and act?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"`@testing-library/react`'s `renderHook` renders a hook in a test harness and returns `{ result, rerender, unmount }`. Read the latest value via `result.current`. Wrap state-changing interactions in `act()` (userEvent and fireEvent already wrap in act) so React flushes updates before you assert. For hooks needing context (router, query client), pass a `wrapper` option.\n\n```jsx\nconst { result } = renderHook(() => useCounter());\nact(() => { result.current.increment(); });\nexpect(result.current.count).toBe(1);\n```",tech:"react",displayNumber:418,sortKey:1390},{id:"react-1391",number:419,title:"How do you test error states and error boundaries?",question:"How do you test error states and error boundaries?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"For a thrown render error, wrap the component in an Error Boundary and assert the fallback UI renders. For data-fetch errors, mock the API to reject and assert the error message appears (using `findBy*` to await it). React 18 `render` does not catch errors during render; use `react-error-boundary`'s `ErrorBoundary` or a custom class boundary as the wrapper in tests.\n\n```jsx\nserver.use(http.get('/api', () => HttpResponse.error()));\nrender(<User />);\nexpect(await screen.findByText(/failed to load/i)).toBeInTheDocument();\n```",tech:"react",displayNumber:419,sortKey:1391},{id:"react-1392",number:420,title:"What is the difference between fireEvent and userEvent?",question:"What is the difference between fireEvent and userEvent?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`fireEvent` dispatches a single synthetic event immediately (e.g., `fireEvent.change(input, { target: { value: 'x' } })`). `userEvent` simulates the full, realistic sequence of browser events (pointer down/up, focus, key events, input, change) and is async. Prefer `userEvent` for behavior fidelity; use `fireEvent` when you need a quick, synchronous, single-event dispatch or to avoid async overhead.\n\n```jsx\n// fireEvent: fast, single event\nfireEvent.change(input, { target: { value: 'a' } });\n\n// userEvent: realistic, async\nawait user.type(input, 'a');\n```",tech:"react",displayNumber:420,sortKey:1392},{id:"react-1393",number:421,title:"How do you use waitFor to assert on eventually-updated DOM?",question:"How do you use waitFor to assert on eventually-updated DOM?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"`waitFor(callback, options)` retries the callback until it stops throwing or times out (default 1000ms). Use it when an assertion depends on async side effects not tied to a single query, such as a value updated after a promise resolves. Prefer `findBy*` when waiting for an element to appear because it's more concise; reach for `waitFor` when the condition is custom (e.g., attribute toggles).\n\n```jsx\nawait waitFor(() => expect(screen.getByRole('status')).toHaveClass('done'));\n```",tech:"react",displayNumber:421,sortKey:1393},{id:"react-1394",number:422,title:"How do you query elements by role, label, text, and test id?",question:"How do you query elements by role, label, text, and test id?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"RTL queries in order of preference: `getByRole` (most accessible, matches ARIA role), `getByLabelText` (form inputs), `getByPlaceholderText`, `getByText` (non-interactive text), `getByDisplayValue` (input value), and lastly `getByTestId` (only when no accessible alternative exists). Prioritize role/label queries because they reflect what users and assistive tech actually encounter.\n\n```jsx\nscreen.getByRole('button', { name: /submit/i });\nscreen.getByLabelText(/email/i);\nscreen.getByText('Welcome');\nscreen.getByTestId('custom-widget');\n```",tech:"react",displayNumber:422,sortKey:1394},{id:"react-1395",number:423,title:"How do you test conditional rendering with queryBy?",question:"How do you test conditional rendering with queryBy?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"Use `queryBy*` to assert an element is absent (returns null instead of throwing). A common pattern: render, trigger an action that should remove a node, then `expect(screen.queryByText('Old')).not.toBeInTheDocument()`. Pair with `getBy*` to assert presence before removal. This avoids false positives from `getBy` throwing on the wrong assertion.\n\n```jsx\nexpect(screen.getByText('Banner')).toBeInTheDocument();\nawait user.click(screen.getByRole('button', { name: /dismiss/i }));\nexpect(screen.queryByText('Banner')).not.toBeInTheDocument();\n```",tech:"react",displayNumber:423,sortKey:1395},{id:"react-1396",number:424,title:"How do you test a component that uses React Router's Loader/Action?",question:"How do you test a component that uses React Router's Loader/Action?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Use `createMemoryRouter(routes, { initialEntries })` and render with `<RouterProvider router={router} />`. Mock the loader/action dependencies (API module) so they resolve deterministically. Await `findBy*` to assert loaded data or submitted results. Because the data router handles promises, wrap assertions in async and use `findBy`/`waitFor` for the resolved state.\n\n```jsx\nconst router = createMemoryRouter(routes, { initialEntries: ['/users/1'] });\nrender(<RouterProvider router={router} />);\nexpect(await screen.findByText('Ada')).toBeInTheDocument();\n```",tech:"react",displayNumber:424,sortKey:1396},{id:"react-1397",number:425,title:"How do you set up a test environment for React (jsdom, jest config, setup file)?",question:"How do you set up a test environment for React (jsdom, jest config, setup file)?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Configure Jest with `testEnvironment: 'jsdom'` so DOM APIs exist, add `setupFilesAfterEach` pointing to a setup file that imports `@testing-library/jest-dom` (for matchers like `toBeInTheDocument`) and configures automatic cleanup. Use `ts-jest` or Babel for TS/JSX. MSW and matchMedia/localStorage polyfills are added in the setup as needed.\n\n```js\n// jest.config.js\nmodule.exports = {\n  testEnvironment: 'jsdom',\n  setupFilesAfterEach: ['<rootDir>/jest.setup.js'],\n};\n// jest.setup.js\nimport '@testing-library/jest-dom';\n```",tech:"react",displayNumber:425,sortKey:1397},{id:"react-1398",number:426,title:"How do you handle timer-based code (setTimeout) in tests with fake timers?",question:"How do you handle timer-based code (setTimeout) in tests with fake timers?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Use Jest fake timers: `jest.useFakeTimers()` in setup, then `jest.advanceTimersByTime(ms)` or `jest.runOnlyPendingTimers()` inside `act()` to move time forward deterministically. This avoids real waits. Be careful combining fake timers with `userEvent` (use `jest.useFakeTimers({ advanceTimers: true })` and the modern async userEvent with `advanceTimers`).\n\n```jsx\njest.useFakeTimers();\nact(() => { render(<Toast />); });\nact(() => { jest.advanceTimersByTime(3000); });\nexpect(screen.queryByText('Saved')).not.toBeInTheDocument();\n```",tech:"react",displayNumber:426,sortKey:1398},{id:"react-1399",number:427,title:"How do you test form validation error messages?",question:"How do you test form validation error messages?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Submit the form (or blur a field) to trigger validation, then assert the error text appears. Use `findByText` because validation may be async (especially with schema resolvers). Ensure error elements have stable, queryable text and ideally `role='alert'`. For RHF/Formik, the error only shows after submit (or touched) unless you set mode to onChange.\n\n```jsx\nawait user.click(screen.getByRole('button', { name: /submit/i }));\nexpect(await screen.findByText(/email is required/i)).toBeInTheDocument();\n```",tech:"react",displayNumber:427,sortKey:1399},{id:"react-1400",number:428,title:"How do you test that a form submits the correct data?",question:"How do you test that a form submits the correct data?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Pass a `jest.fn()` as the submit handler (or mock the API), fill fields realistically with `userEvent.type`, click submit, and assert the handler received the expected payload via `toHaveBeenCalledWith`. For real network submission, mock the endpoint with MSW and assert the request body or the resulting UI state (success message).\n\n```jsx\nconst onSubmit = jest.fn();\nrender(<Form onSubmit={onSubmit} />);\nawait user.type(screen.getByLabelText(/name/i), 'Ada');\nawait user.click(screen.getByRole('button', { name: /save/i }));\nexpect(onSubmit).toHaveBeenCalledWith({ name: 'Ada' });\n```",tech:"react",displayNumber:428,sortKey:1400},{id:"react-1401",number:429,title:"How do you test React Router navigation side effects (redirect after login)?",question:"How do you test React Router navigation side effects (redirect after login)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:"Render the login flow inside a `MemoryRouter` with route definitions for both `/login` and the post-login destination (e.g., `/dashboard`). After submitting valid credentials (mock the auth API), assert the dashboard content is now rendered, proving navigation occurred. Alternatively, spy on `useNavigate` via a wrapper component to assert it was called with the expected path.\n\n```jsx\nrender(\n  <MemoryRouter initialEntries={['/login']}>\n    <Routes>\n      <Route path='/login' element={<Login />} />\n      <Route path='/dashboard' element={<h1>Dashboard</h1>} />\n    </Routes>\n  </MemoryRouter>\n);\nawait user.type(screen.getByLabelText(/email/i), 'a@b.com');\nawait user.click(screen.getByRole('button', { name: /login/i }));\nexpect(await screen.findByText('Dashboard')).toBeInTheDocument();\n```",tech:"react",displayNumber:429,sortKey:1401},{id:"react-1402",number:430,title:"How do you mock fetch or axios in a test?",question:"How do you mock fetch or axios in a test?",difficulty:"Intermediate",category:"Routing, Forms & Testing",answer:"Prefer MSW for intercepting at the network layer so your code uses real fetch/axios unchanged. Alternatively, `jest.mock('axios')` and provide resolved/rejected values per test. For global fetch, you can spy: `jest.spyOn(global, 'fetch').mockResolvedValue({ json: async () => data })`. MSW is more faithful; module mocks are simpler for unit isolation.\n\n```jsx\njest.mock('axios');\nimport axios from 'axios';\naxios.get.mockResolvedValue({ data: { name: 'Ada' } });\n```",tech:"react",displayNumber:430,sortKey:1402},{id:"react-1403",number:431,title:"What is the purpose of screen and why prefer it over container queries?",question:"What is the purpose of screen and why prefer it over container queries?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"`screen` is an object containing all the queries bound to `document.body`, so you don't need the `container` or `baseElement` returned by `render`. Using `screen.getBy*` keeps queries in the global document, matches how users see the page, and makes refactoring easier (you don't pass containers around). It's the recommended default in modern RTL.\n\n```jsx\nconst { container } = render(<App />); // older style\n// Prefer:\nrender(<App />);\nscreen.getByText('Hello');\n```",tech:"react",displayNumber:431,sortKey:1403},{id:"react-1404",number:432,title:"How do you test input change and prevent default behavior?",question:"How do you test input change and prevent default behavior?",difficulty:"Basic",category:"Routing, Forms & Testing",answer:"With `userEvent.type` into a labelled input, the component's `onChange`/`register` handles updates and React's synthetic event system calls `preventDefault` internally for controlled flows. In tests you don't call preventDefault manually; you just simulate realistic typing and submission. If testing a raw handler, `fireEvent.submit(form)` triggers the submit and React Router's Form prevents the native reload automatically.\n\n```jsx\nawait user.type(screen.getByLabelText(/search/i), 'react');\nawait user.click(screen.getByRole('button', { name: /go/i }));\n// assert search results; no manual preventDefault needed\n```",tech:"react",displayNumber:432,sortKey:1404},{id:"react-1405",number:433,title:"How do you test React Hook Form's useFieldArray (dynamic rows)?",question:"How do you test React Hook Form's useFieldArray (dynamic rows)?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Render the form, query the initial row inputs, type into them, click "Add" to append a row, assert a new row appears (e.g., one more inputs or a count), fill it, submit, and assert the handler receives an array with both rows. Use \`findAllByLabelText\` to count repeated fields. Because RHF uses uncontrolled inputs, query by name/label, not by controlled value.

\`\`\`jsx
const rows = await screen.findAllByLabelText(/item name/i);
expect(rows).toHaveLength(1);
await user.click(screen.getByRole('button', { name: /add/i }));
expect(await screen.findAllByLabelText(/item name/i)).toHaveLength(2);
\`\`\``,tech:"react",displayNumber:433,sortKey:1405},{id:"react-1406",number:434,title:"How do you write a test for a protected route redirect?",question:"How do you write a test for a protected route redirect?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Render the protected route tree inside a \`MemoryRouter\` where the auth context reports an unauthenticated user. Include both the protected page route and the \`/login\` route. After render (and any effect), assert the login page content is shown rather than the protected content, proving the \`<RequireAuth>\` redirect fired. Spy on \`useNavigate\` if you want to assert the call instead.

\`\`\`jsx
render(
  <AuthContext.Provider value={{ user: null }}>
    <MemoryRouter initialEntries={['/profile']}>
      <Routes>
        <Route element={<RequireAuth />}>
          <Route path='/profile' element={<Profile />} />
        </Route>
        <Route path='/login' element={<h1>Login</h1>} />
      </Routes>
    </MemoryRouter>
  </AuthContext.Provider>
);
expect(screen.getByText('Login')).toBeInTheDocument();
expect(screen.queryByText('Profile')).not.toBeInTheDocument();
\`\`\``,tech:"react",displayNumber:434,sortKey:1406},{id:"react-1407",number:435,title:"What are best practices for organizing and writing maintainable tests?",question:"What are best practices for organizing and writing maintainable tests?",difficulty:"Advanced",category:"Routing, Forms & Testing",answer:`Follow the "Arrange, Act, Assert" structure per test. Query by accessibility (role/label) over test ids. Test behavior, not implementation. Keep tests isolated with cleanup and per-test mocks (reset handlers). Co-locate tests with components. Use shared custom render wrappers (with providers/router) to reduce boilerplate. Mock at the boundary (network, modules), not internal logic. Aim for fast, deterministic, readable tests over 100% coverage of trivial code.

\`\`\`jsx
// custom render with providers reused across tests
const customRender = (ui, { route = '/' } = {}) =>
  render(<Providers><MemoryRouter initialEntries={[route]}>{ui}</MemoryRouter></Providers>);
export * from '@testing-library/react';
export { customRender as render };
\`\`\``,tech:"react",displayNumber:435,sortKey:1407},{id:"react-1408",number:436,title:"What is a Higher-Order Component (HOC) and when would you use one?",question:"What is a Higher-Order Component (HOC) and when would you use one?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`A Higher-Order Component is a function that takes a component and returns a new, enhanced component. HOCs are a pattern for reusing component logic without duplication. They follow composition over inheritance. Typical uses: authentication guards, data fetching, logging, theming, and error/loading wrappers. The HOC does not mutate the wrapped component; it composes a new one.

\`\`\`jsx
// A simple HOC that adds a loading indicator
function withLoading(WrappedComponent) {
  return function WithLoading({ isLoading, ...props }) {
    if (isLoading) return <div>Loading...</div>;
    return <WrappedComponent {...props} />;
  };
}

const UserListWithLoading = withLoading(UserList);
<UserListWithLoading isLoading={loading} users={users} />;
\`\`\``,tech:"react",displayNumber:436,sortKey:1408},{id:"react-1409",number:437,title:"What are the caveats and best practices when using HOCs?",question:"What are the caveats and best practices when using HOCs?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"HOCs wrap, never mutate, the input component. They must forward unknown props (`...props`) and `ref`s to the wrapped component. They can break if composed deeply (wrapper chains) and can cause prop name collisions. Always set a meaningful `displayName` for debugging. Avoid using HOCs inside the render method (creates a new component each render, remounting the tree). Prefer composing with `compose()` from a utility library.\n\n```jsx\n// Anti-pattern: creating the HOC inside render remounts on every render\nrender() {\n  const Enhanced = withLogging(MyComponent); // new type each render!\n  return <Enhanced />;\n}\n\n// Best practice: define HOCs outside the component\nconst Enhanced = compose(withLoading, withAuth)(MyComponent);\nEnhanced.displayName = 'WithAuth(WithLoading(MyComponent))';\n```",tech:"react",displayNumber:437,sortKey:1409},{id:"react-1410",number:438,title:"How do you preserve displayName and forward refs through an HOC?",question:"How do you preserve displayName and forward refs through an HOC?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Wrapped components lose their name in DevTools unless you set `displayName`. Refs passed to the enhanced component won't reach the inner component unless you use `React.forwardRef`. Combine both so the HOC is transparent and supports imperative handles.\n\n```jsx\nfunction withMouse(WrappedComponent) {\n  const DisplayName = WrappedComponent.displayName || WrappedComponent.name || 'Component';\n  const Component = React.forwardRef((props, ref) => {\n    const [pos, setPos] = React.useState({ x: 0, y: 0 });\n    return <WrappedComponent ref={ref} mouse={pos} {...props} />;\n  });\n  Component.displayName = `withMouse(${DisplayName})`;\n  return Component;\n}\n```",tech:"react",displayNumber:438,sortKey:1410},{id:"react-1411",number:439,title:"What is the Render Props pattern?",question:"What is the Render Props pattern?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`A render prop is a prop whose value is a function that returns a React element. It lets a component share its internal state or logic with its parent, which decides what to render. This is a flexible alternative to HOCs when you need fine-grained control over the rendered output and want to avoid wrapper nesting.

\`\`\`jsx
class MouseTracker extends React.Component {
  state = { x: 0, y: 0 };
  handleMove = (e) => this.setState({ x: e.clientX, y: e.clientY });
  render() {
    return (
      <div onMouseMove={this.handleMove}>
        {this.props.render(this.state)}
      </div>
    );
  }
}

<MouseTracker render={({ x, y }) => <h1>Cursor at {x},{y}</h1>} />;
\`\`\``,tech:"react",displayNumber:439,sortKey:1411},{id:"react-1412",number:440,title:"Show a practical Render Props example for data fetching.",question:"Show a practical Render Props example for data fetching.",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Render props shine for cross-cutting state like data fetching or hover. The container manages loading/error/data and calls the render function with the result. The consumer renders exactly what it wants, including custom loading and error UI.

\`\`\`jsx
function Fetch({ url, children }) {
  const [state, setState] = React.useState({ data: null, loading: true, error: null });
  React.useEffect(() => {
    fetch(url)
      .then((r) => r.json())
      .then((data) => setState({ data, loading: false, error: null }))
      .catch((error) => setState({ error, loading: false, data: null }));
  }, [url]);
  return children(state);
}

<Fetch url='/api/user'>
  {({ data, loading, error }) =>
    loading ? <Spinner /> : error ? <Error /> : <Profile data={data} />
  }
</Fetch>;
\`\`\``,tech:"react",displayNumber:440,sortKey:1412},{id:"react-1413",number:441,title:"Render Props vs the `children`-as-function pattern — what is the difference?",question:"Render Props vs the `children`-as-function pattern — what is the difference?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"They are the same idea; the only difference is which prop holds the function. Using `children` as a function reads naturally (`<Comp>{(api)=>...}</Comp>`) and is the most common form. A named prop like `render` makes the intent explicit. Both avoid HOC nesting and make the data flow visible in JSX.\n\n```jsx\n// children as function\n<Toggle>{({ on, toggle }) => <button onClick={toggle}>{on ? 'On' : 'Off'}</button>}</Toggle>\n\n// named render prop\n<Toggle render={({ on, toggle }) => <Switch on={on} onClick={toggle} />} />\n```",tech:"react",displayNumber:441,sortKey:1413},{id:"react-1414",number:442,title:"What is the Compound Components pattern?",question:"What is the Compound Components pattern?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Compound components are several components that work together and share implicit state, while letting the parent control the structure/composition. Instead of one big configurable component, you expose \`Menu\`, \`Menu.Item\`, \`Menu.Button\`. This gives a declarative, flexible API and hides coordination logic via context.

\`\`\`jsx
function Select({ children }) {
  const [open, setOpen] = React.useState(false);
  return <Ctx.Provider value={{ open, setOpen }}>{children}</Ctx.Provider>;
}
Select.Trigger = function Trigger({ children }) {
  const { open, setOpen } = React.useContext(Ctx);
  return <button onClick={() => setOpen(!open)}>{children}</button>;
};
Select.List = function List({ children }) {
  const { open } = React.useContext(Ctx);
  return open ? <ul>{children}</ul> : null;
};
\`\`\``,tech:"react",displayNumber:442,sortKey:1414},{id:"react-1415",number:443,title:"Build a working Compound Components example with context.",question:"Build a working Compound Components example with context.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Use a context to share state among sibling subcomponents so consumers can reorder or omit parts freely. The parent sets up the provider; subcomponents read/write via \`useContext\`. This keeps the public API expressive (\`<Tabs><Tabs.List/>...</Tabs>\`).

\`\`\`jsx
const TabsCtx = React.createContext(null);
function Tabs({ children }) {
  const [active, setActive] = React.useState(0);
  return <TabsCtx.Provider value={{ active, setActive }}>{children}</TabsCtx.Provider>;
}
Tabs.List = ({ children }) => <div role='tablist'>{children}</div>;
Tabs.Tab = ({ index, children }) => {
  const { active, setActive } = React.useContext(TabsCtx);
  return <button aria-selected={active === index} onClick={() => setActive(index)}>{children}</button>;
};
Tabs.Panel = ({ index, children }) => {
  const { active } = React.useContext(TabsCtx);
  return active === index ? <div role='tabpanel'>{children}</div> : null;
};
\`\`\``,tech:"react",displayNumber:443,sortKey:1415},{id:"react-1416",number:444,title:"How do you make Compound Components resilient to missing subcomponents?",question:"How do you make Compound Components resilient to missing subcomponents?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Guard against misuse: throw helpful errors when a subcomponent is rendered outside its parent, and document required context. You can also expose a hook (\`useTabs\`) for advanced consumers. Clear runtime errors beat silent failures when the pattern is misused.

\`\`\`jsx
function useTabsCtx() {
  const ctx = React.useContext(TabsCtx);
  if (!ctx) throw new Error('Tabs.Tab must be used within <Tabs>');
  return ctx;
}
// Then each subcomponent calls useTabsCtx() instead of useContext directly.
\`\`\``,tech:"react",displayNumber:444,sortKey:1416},{id:"react-1417",number:445,title:"What is the Control Props pattern?",question:"What is the Control Props pattern?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Control Props let the parent fully control a component's state from outside while still supporting an uncontrolled (internal-state) mode. The component is "controlled" if the relevant prop is provided, otherwise it manages its own state. This mirrors React's own controlled inputs and offers maximum flexibility.

\`\`\`jsx
function Toggle({ on, defaultOn = false, onChange }) {
  const [internal, setInternal] = React.useState(defaultOn);
  const isControlled = on !== undefined;
  const current = isControlled ? on : internal;
  const toggle = () => {
    const next = !current;
    if (!isControlled) setInternal(next);
    onChange?.(next);
  };
  return <button onClick={toggle}>{current ? 'On' : 'Off'}</button>;
}
\`\`\``,tech:"react",displayNumber:445,sortKey:1417},{id:"react-1418",number:446,title:"Show the Control Props pattern handling both controlled and uncontrolled usage.",question:"Show the Control Props pattern handling both controlled and uncontrolled usage.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Detect control by checking whether the prop is \`undefined\`. When controlled, ignore internal state and render the prop; when uncontrolled, keep internal state and call the optional callback. This single component supports \`<Toggle on={x}/>\` and \`<Toggle defaultOn onChange={...}/>\`.

\`\`\`jsx
function useControllableState({ value, defaultValue, onChange }) {
  const [uncontrolled, setUncontrolled] = React.useState(defaultValue);
  const isControlled = value !== undefined;
  const current = isControlled ? value : uncontrolled;
  const set = (next) => {
    if (!isControlled) setUncontrolled(next);
    onChange?.(next);
  };
  return [current, set];
}
\`\`\``,tech:"react",displayNumber:446,sortKey:1418},{id:"react-1419",number:447,title:"HOC vs Render Props vs Custom Hooks — when to use which?",question:"HOC vs Render Props vs Custom Hooks — when to use which?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Custom hooks are now the default for sharing stateful logic because they avoid extra component tree layers and are easiest to compose. Use Render Props when the consumer needs to control rendering output. Use HOCs for cross-cutting concerns that wrap a component's contract (auth, error boundary adapters). Prefer hooks; reach for render props/HOCs only when hooks can't express the need.

\`\`\`jsx
// Hook: best default
function useMouse() {
  const [pos, setPos] = React.useState({ x: 0, y: 0 });
  React.useEffect(() => {
    const h = (e) => setPos({ x: e.clientX, y: e.clientY });
    window.addEventListener('mousemove', h);
    return () => window.removeEventListener('mousemove', h);
  }, []);
  return pos;
}
\`\`\``,tech:"react",displayNumber:447,sortKey:1419},{id:"react-1420",number:448,title:"What are React Server Components (RSC)?",question:"What are React Server Components (RSC)?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`RSC are components that render on the server (or at build time) and send a lightweight serialized description to the client instead of JavaScript. They can access server-only resources (databases, filesystem, secrets) directly and ship zero client JS. They enable a smaller bundle and better initial load. They cannot use state, effects, or browser-only APIs.

\`\`\`jsx
// app/page.js  (Server Component by default)
import { db } from '@/lib/db';
export default async function Page() {
  const posts = await db.post.findMany();
  return <ul>{posts.map((p) => <li key={p.id}>{p.title}</li>)}</ul>;
}
\`\`\``,tech:"react",displayNumber:448,sortKey:1420},{id:"react-1421",number:449,title:"What does the `'use client'` directive do?",question:"What does the `'use client'` directive do?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`\`'use client'\` marks a module boundary: everything below it (and imported into it) becomes a Client Component, rendered on the server for the initial HTML but hydrated and interactive in the browser. Use it only where you need state, effects, event handlers, or browser APIs. Server Components can import Client Components, but not the reverse across the boundary (props passed must be serializable).

\`\`\`jsx
'use client';
import { useState } from 'react';
export default function Counter() {
  const [n, setN] = useState(0);
  return <button onClick={() => setN(n + 1)}>{n}</button>;
}
\`\`\``,tech:"react",displayNumber:449,sortKey:1421},{id:"react-1422",number:450,title:"What are the rules and limitations of Server vs Client Components?",question:"What are the rules and limitations of Server vs Client Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Components: no hooks/state/effects, no event handlers, can be async, may use server-only code. Client Components: support interactivity, run in the browser, but increase JS shipped. You cannot import a Server Component into a Client Component directly; instead pass it as \`children\`/props. Props crossing the boundary must be serializable (no functions, classes, or non-JSON values).

\`\`\`jsx
// app/layout.js (Server)
import ClientNav from './ClientNav'; // ok: server imports client
export default function Layout({ children }) {
  return <html><body><ClientNav />{children}</body></html>;
}
// Passing a server component into a client one via children:
<ClientShell><ServerContent /></ClientShell> // ServerContent rendered on server
\`\`\``,tech:"react",displayNumber:450,sortKey:1422},{id:"react-1423",number:451,title:"How do you pass a Server Component into a Client Component?",question:"How do you pass a Server Component into a Client Component?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`You can't import a Server Component into a Client Component, but you can pass it as a \`children\` or prop from a Server Component that renders both. The Server Component creates the element and hands it down; the Client Component receives it as a serialized React node and renders it. This is the canonical "escape hatch" for mixing the two.

\`\`\`jsx
// ServerComponent.js (Server)
import ClientCard from './ClientCard';
import ServerChart from './ServerChart'; // heavy server work
export default function Page() {
  return <ClientCard title='Report'><ServerChart /></ClientCard>;
}
// ClientCard.js ('use client')
export default function ClientCard({ title, children }) {
  const [open, setOpen] = useState(true);
  return <div><h2>{title}</h2>{open && children}</div>;
}
\`\`\``,tech:"react",displayNumber:451,sortKey:1423},{id:"react-1424",number:452,title:"Can Client Components be rendered on the server? Explain the two-pass rendering.",question:"Can Client Components be rendered on the server? Explain the two-pass rendering.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Yes. Client Components still render to HTML on the server for the initial response (so content is visible without JS), then hydrate in the browser to become interactive. The server pass produces static markup; the client pass attaches listeners. This is why \`'use client'\` ≠ "server-side rendering is skipped" — it means "also runs on the client."

\`\`\`jsx
// Both passes happen: server renders markup, client hydrates.
'use client';
export default function Badge({ label }) {
  // Renders to HTML on server, then hydrates on client
  return <span className='badge'>{label}</span>;
}
\`\`\``,tech:"react",displayNumber:452,sortKey:1424},{id:"react-1425",number:453,title:"What is the Next.js App Router and how does it differ from the Pages Router?",question:"What is the Next.js App Router and how does it differ from the Pages Router?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"The App Router (Next 13+) uses a `app/` directory with file-system routing, React Server Components by default, nested layouts, and colocated `loading`/`error`/`not-found` files. The older Pages Router (`pages/`) renders everything as Client Components by default with `getStaticProps`/`getServerSideProps`. App Router enables more granular server/client splits and streaming.\n\n```text\napp/\n  layout.js          // root layout (required)\n  page.js            // route /\n  dashboard/\n    layout.js        // nested layout\n    page.js          // route /dashboard\n    loading.js       // streaming fallback\n    error.js         // error boundary\n```",tech:"react",displayNumber:453,sortKey:1425},{id:"react-1426",number:454,title:"Explain Layouts in the App Router.",question:"Explain Layouts in the App Router.",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"A `layout.js` wraps its segment and all nested segments, persisting state and avoiding remounts on navigation between sibling routes. The root layout must define `<html>` and `<body>`. Layouts are Server Components by default and receive `children`. Nested layouts compose, so shared UI (nav, sidebar) stays mounted.\n\n```jsx\n// app/dashboard/layout.js\nexport default function DashboardLayout({ children }) {\n  return (\n    <section>\n      <Sidebar />\n      <main>{children}</main>\n    </section>\n  );\n}\n```",tech:"react",displayNumber:454,sortKey:1426},{id:"react-1427",number:455,title:"How do nested layouts preserve state across navigations?",question:"How do nested layouts preserve state across navigations?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"The App Router keeps parent layouts mounted while only the changed segment re-renders. Because the layout component instance is not discarded on navigation, any state held in a layout (or a client island within it) persists, and client-side navigation does not refetch the layout. This gives SPA-like feel with server rendering.\n\n```jsx\n// app/layout.js keeps nav mounted; navigating /a -> /b\n// only the page segment changes, layout state (e.g. open menu) survives.\n```",tech:"react",displayNumber:455,sortKey:1427},{id:"react-1428",number:456,title:"What are Pages in the App Router and how are they defined?",question:"What are Pages in the App Router and how are they defined?",difficulty:"Basic",category:"Advanced Patterns & Next.js",answer:"A `page.js` file defines the UI for a route segment and is always the leaf of a route. By default it's a Server Component and can be async. There is one `page.js` per folder; the folder structure maps to the URL. Pages receive `params` and `searchParams` (in async form in recent Next versions).\n\n```jsx\n// app/products/[id]/page.js\nexport default async function ProductPage({ params }) {\n  const { id } = await params;\n  const product = await getProduct(id);\n  return <h1>{product.name}</h1>;\n}\n```",tech:"react",displayNumber:456,sortKey:1428},{id:"react-1429",number:457,title:"How does Loading UI (`loading.js`) work with Suspense?",question:"How does Loading UI (`loading.js`) work with Suspense?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`loading.js` creates a Suspense boundary around the page. While the page's async work (data, streaming) is pending, Next shows the loading UI instantly, then swaps in the resolved content. This enables streaming SSR without manually wrapping each component in `<Suspense>`. It applies at the segment level.\n\n```jsx\n// app/dashboard/loading.js\nexport default function Loading() {\n  return <Skeleton />;\n}\n// Next wraps the page in <Suspense fallback={<Loading/>}>\n```",tech:"react",displayNumber:457,sortKey:1429},{id:"react-1430",number:458,title:"How do Error Boundaries work in the App Router (`error.js`)?",question:"How do Error Boundaries work in the App Router (`error.js`)?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`error.js` is a Client Component that catches errors thrown in its segment (and below) and renders a fallback. It must accept `error` and `reset` props. It does not catch errors in the root layout or in `error.js` itself. You can nest error boundaries for granular recovery; `reset` re-renders the segment.\n\n```jsx\n'use client';\nexport default function Error({ error, reset }) {\n  return (\n    <div>\n      <p>Something went wrong: {error.message}</p>\n      <button onClick={reset}>Try again</button>\n    </div>\n  );\n}\n```",tech:"react",displayNumber:458,sortKey:1430},{id:"react-1431",number:459,title:"What is `global-error.js` and when is it used?",question:"What is `global-error.js` and when is it used?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`global-error.js` catches errors in the root layout (which `error.js` cannot). It must render its own `<html>` and `<body>` because it replaces the whole document on failure. It's the last-resort boundary for catastrophic errors outside normal segments.\n\n```jsx\n'use client';\nexport default function GlobalError({ error, reset }) {\n  return (\n    <html><body>\n      <h1>Fatal error</h1>\n      <button onClick={reset}>Reload</button>\n    </body></html>\n  );\n}\n```",tech:"react",displayNumber:459,sortKey:1431},{id:"react-1432",number:460,title:"What is `not-found.js` and how do you trigger a 404?",question:"What is `not-found.js` and how do you trigger a 404?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`not-found.js` renders a custom 404 UI for a segment. Call the `notFound()` helper from `next/navigation` inside a Server or Client Component to trigger it and return a 404 status. Without a segment-level file, the root `not-found.js` (or Next's default) is used.\n\n```jsx\nimport { notFound } from 'next/navigation';\nexport default async function Page({ params }) {\n  const { id } = await params;\n  const user = await getUser(id);\n  if (!user) notFound();\n  return <Profile user={user} />;\n}\n```",tech:"react",displayNumber:460,sortKey:1432},{id:"react-1433",number:461,title:"How do Route Handlers work in the App Router?",question:"How do Route Handlers work in the App Router?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"Route Handlers live in `app/api/.../route.js` and let you build backend endpoints using Web `Request`/`Response`. Export named functions per HTTP method (`GET`, `POST`, `PUT`, `DELETE`, etc.). They run on the server and support caching, dynamic behavior, and streaming. They replace API routes from the Pages Router.\n\n```jsx\n// app/api/posts/route.js\nimport { NextResponse } from 'next/server';\nexport async function GET() {\n  const posts = await getPosts();\n  return NextResponse.json(posts);\n}\nexport async function POST(req) {\n  const body = await req.json();\n  const created = await createPost(body);\n  return NextResponse.json(created, { status: 201 });\n}\n```",tech:"react",displayNumber:461,sortKey:1433},{id:"react-1434",number:462,title:"How do you handle dynamic route parameters in Route Handlers?",question:"How do you handle dynamic route parameters in Route Handlers?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"Use folder segments with brackets, e.g. `app/api/posts/[id]/route.js`, and read `params` (now async in recent versions) from the second argument of the handler. Validate and parse the id before use, and return 404s for missing records.\n\n```jsx\n// app/api/posts/[id]/route.js\nexport async function GET(req, { params }) {\n  const { id } = await params;\n  const post = await getPost(id);\n  if (!post) return new Response('Not found', { status: 404 });\n  return Response.json(post);\n}\n```",tech:"react",displayNumber:462,sortKey:1434},{id:"react-1435",number:463,title:"What are Server Actions in Next.js?",question:"What are Server Actions in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Actions are async functions that run on the server and can be invoked directly from Client Components (via form actions or event handlers) without writing a manual API route. They are defined with \`'use server'\` and can mutate data, call databases, and revalidate caches. They use the same RPC mechanism as form submissions, making mutations simple and secure by default.

\`\`\`jsx
// app/actions.js
'use server';
export async function createTodo(formData) {
  const title = formData.get('title');
  await db.todo.create({ data: { title } });
  revalidatePath('/todos');
}
\`\`\``,tech:"react",displayNumber:463,sortKey:1435},{id:"react-1436",number:464,title:"How do you call a Server Action from a form?",question:"How do you call a Server Action from a form?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Pass the action to a form's `action` prop. The framework serializes the FormData and calls the server function, then can revalidate and redirect. For progressive enhancement, this works even without JavaScript. Use `useFormState`/`useActionState` to capture return values and pending state.\n\n```jsx\n'use client';\nimport { useFormStatus } from 'react-dom';\nfunction Submit() {\n  const { pending } = useFormStatus();\n  return <button disabled={pending}>{pending ? 'Saving...' : 'Save'}</button>;\n}\n// <form action={createTodo}><input name='title'/><Submit/></form>\n```",tech:"react",displayNumber:464,sortKey:1436},{id:"react-1437",number:465,title:"What is `'use server'` vs `'use client'`?",question:"What is `'use server'` vs `'use client'`?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`'use client'` marks a module as a Client Component boundary (runs/hydrates in browser). `'use server'` marks a function (or module of functions) as a Server Action callable from the client; these functions must be async, serializable in/out, and cannot return non-serializable values. Both are directives that define execution environment, but they serve opposite directions.",tech:"react",displayNumber:465,sortKey:1437},{id:"react-1438",number:466,title:"How do you revalidate cached data after a Server Action?",question:"How do you revalidate cached data after a Server Action?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use `revalidatePath(path)` to purge the cache for a specific route, or `revalidateTag(tag)` to invalidate data fetched with that tag (via `fetch(url, { next: { tags } })`). After mutation, revalidating ensures the next render reflects fresh data without a full deploy. You can also `redirect()` after success.\n\n```jsx\n'use server';\nimport { revalidateTag, revalidatePath } from 'next/cache';\nexport async function likePost(id) {\n  await db.like.create({ data: { postId: id } });\n  revalidateTag(`post-${id}`);\n  revalidatePath('/feed');\n}\n```",tech:"react",displayNumber:466,sortKey:1438},{id:"react-1439",number:467,title:"Can Server Actions accept non-form data arguments?",question:"Can Server Actions accept non-form data arguments?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Yes. When invoked programmatically (e.g., `onClick={() => action(id)}`), Server Actions can take any serializable arguments (strings, numbers, plain objects, arrays). When used as a form `action`, the argument is `FormData`. Avoid passing functions, class instances, or unsanitized client input.\n\n```jsx\n'use server';\nexport async function deleteItem(id: string) {\n  await db.item.delete({ where: { id } });\n  revalidatePath('/items');\n}\n// Client: <button onClick={() => deleteItem(item.id)}>x</button>\n```",tech:"react",displayNumber:467,sortKey:1439},{id:"react-1440",number:468,title:"Explain the difference between Server Actions and Route Handlers.",question:"Explain the difference between Server Actions and Route Handlers.",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Route Handlers are explicit HTTP endpoints (useful for external clients, webhooks, non-Next consumers). Server Actions are RPC-style functions tightly coupled to the React tree, enabling progressive-enhancement forms and automatic serialization. Use Route Handlers when you need a public API; use Server Actions for app-internal mutations from forms/UI.\n\n```text\nNeed a public REST/JSON endpoint?  -> Route Handler (app/api/...)\nNeed to mutate from a form in the tree? -> Server Action ('use server')\n```",tech:"react",displayNumber:468,sortKey:1440},{id:"react-1441",number:469,title:"What is Client-Side Rendering (CSR)?",question:"What is Client-Side Rendering (CSR)?",difficulty:"Basic",category:"Advanced Patterns & Next.js",answer:"CSR renders the app in the browser: the server sends a minimal HTML shell plus a JS bundle, and React builds the DOM on the client. It's simple and great for highly interactive dashboards behind auth, but has slower first paint and weaker SEO since content isn't in the initial HTML.\n\n```jsx\n// A pure client app (e.g. CRA): browser downloads JS, then renders\nReactDOM.createRoot(document.getElementById('root')).render(<App />);\n```",tech:"react",displayNumber:469,sortKey:1441},{id:"react-1442",number:470,title:"What is Server-Side Rendering (SSR) and how is it done in Next.js?",question:"What is Server-Side Rendering (SSR) and how is it done in Next.js?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`SSR renders each request on the server into full HTML, sent to the client, then hydrated. In the App Router, any dynamic Server Component is effectively SSR'd per request. In the Pages Router you use \`getServerSideProps\`. Benefits: fast first paint and good SEO. Cost: higher server CPU per request.

\`\`\`jsx
// App Router: dynamic, runs per request
export const dynamic = 'force-dynamic';
export default async function Page() {
  const data = await fetchData(); // executed on each request
  return <List data={data} />;
}
\`\`\``,tech:"react",displayNumber:470,sortKey:1442},{id:"react-1443",number:471,title:"What is Static Site Generation (SSG)?",question:"What is Static Site Generation (SSG)?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"SSG pre-renders pages at build time into static HTML, served from CDN. It's the fastest and most scalable option, ideal for content that doesn't change per user/request (docs, blogs, marketing). In the App Router, components without dynamic data are static by default; in Pages Router use `getStaticProps`.\n\n```jsx\n// App Router: static by default (no dynamic functions used)\nexport default async function Page() {\n  const posts = await getPosts(); // fetched once at build\n  return <Feed posts={posts} />;\n}\n```",tech:"react",displayNumber:471,sortKey:1443},{id:"react-1444",number:472,title:"What is Incremental Static Regeneration (ISR)?",question:"What is Incremental Static Regeneration (ISR)?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"ISR lets you keep SSG's speed while updating static content after deploy, without rebuilding the whole site. You set a `revalidate` interval; after it elapses, the next request regenerates the page in the background. In App Router, use `export const revalidate = 60` or `fetch(..., { next: { revalidate: 60 } })`.\n\n```jsx\n// App Router ISR\nexport const revalidate = 60; // seconds\nexport default async function Page() {\n  const data = await fetch('https://api.example.com/posts', {\n    next: { revalidate: 60 },\n  }).then((r) => r.json());\n  return <Feed data={data} />;\n}\n```",tech:"react",displayNumber:472,sortKey:1444},{id:"react-1445",number:473,title:"What is Streaming SSR and how does Suspense enable it?",question:"What is Streaming SSR and how does Suspense enable it?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Streaming SSR sends HTML in chunks as data becomes ready, instead of blocking the whole page. Wrap slow parts in \`<Suspense>\` (or \`loading.js\`); the shell streams first, then the suspended content fills in. This improves Time to First Byte and perceived performance. It's a core feature of RSC and the App Router.

\`\`\`jsx
import { Suspense } from 'react';
export default function Page() {
  return (
    <div>
      <Header />
      <Suspense fallback={<CommentsSkeleton />}>
        <Comments /> {/* async, streams in later */}
      </Suspense>
    </div>
  );
}
\`\`\``,tech:"react",displayNumber:473,sortKey:1445},{id:"react-1446",number:474,title:"How do you choose between CSR, SSR, SSG, and ISR?",question:"How do you choose between CSR, SSR, SSG, and ISR?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Pick by data freshness and audience. SSG for static, public, SEO-critical content at scale. ISR for content that changes occasionally. SSR for per-request, personalized, or frequently changing data. CSR for private, highly interactive, behind-login apps where SEO is irrelevant. Often a single app mixes them per route.

\`\`\`text
Docs/marketing  -> SSG (or ISR)
User dashboard  -> CSR or SSR (per-user)
News feed       -> SSR or ISR
Admin tool      -> CSR
\`\`\``,tech:"react",displayNumber:474,sortKey:1446},{id:"react-1447",number:475,title:"What is Partial Prerendering (PPR) in Next.js?",question:"What is Partial Prerendering (PPR) in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`PPR (experimental) combines static and dynamic rendering in one request: the static shell is prerendered at build time while dynamic holes are streamed in at request time, using Suspense boundaries to mark what's dynamic. It gives SSG speed with SSR flexibility. Enable via \`experimental.ppr = true\` (canary/experimental).

\`\`\`jsx
// app/page.js with Suspense marking dynamic content
export default function Page() {
  return (
    <>
      <StaticHeader />
      <Suspense fallback={<CartSkeleton />}>
        <DynamicCart /> {/* dynamic, streamed at request time */}
      </Suspense>
    </>
  );
}
\`\`\``,tech:"react",displayNumber:475,sortKey:1447},{id:"react-1448",number:476,title:"What is the difference between `<Suspense>` and `loading.js`?",question:"What is the difference between `<Suspense>` and `loading.js`?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`<Suspense>` is a React primitive you place manually around any async subtree for streaming. `loading.js` is a Next.js convenience that automatically wraps a route segment's page in a Suspense boundary with its own fallback. Use `<Suspense>` for fine-grained control within a page; use `loading.js` for segment-level loading states.\n\n```jsx\n// loading.js => automatic <Suspense fallback={<Loading/>}> around page\n// Manual control inside a page:\n<Suspense fallback={<ChartSkeleton/>}><ExpensiveChart/></Suspense>\n```",tech:"react",displayNumber:476,sortKey:1448},{id:"react-1449",number:477,title:"What are Micro-frontends and why use them?",question:"What are Micro-frontends and why use them?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Micro-frontends split a frontend into independently developed, deployed, and owned pieces (by team/domain), composed into one app at runtime or build time. Benefits: team autonomy, independent deploys, tech heterogeneity. Costs: complexity, duplicated dependencies, runtime coordination, and consistency challenges. Use only at organizational scale where the benefits outweigh overhead.\n\n```text\nTeam A -> Cart app (React 18)\nTeam B -> Search app (React 19)\nShell  -> composes both via Module Federation\n```",tech:"react",displayNumber:477,sortKey:1449},{id:"react-1450",number:478,title:"What is Module Federation and how does it enable Micro-frontends?",question:"What is Module Federation and how does it enable Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Module Federation (Webpack 5 / Rspack / Turbopack support evolving) lets multiple builds expose and consume modules at runtime. A "host" app loads "remote" components over the network without a build-time dependency. This is the most common Micro-frontend mechanism, enabling separate deploy cycles and shared, lazily-loaded code.

\`\`\`js
// webpack.config.js (host)
new ModuleFederationPlugin({
  name: 'host',
  remotes: { cart: 'cart@https://cart.app/mf-manifest.json' },
});
// Usage in React: const Cart = React.lazy(() => import('cart/Cart'));
\`\`\``,tech:"react",displayNumber:478,sortKey:1450},{id:"react-1451",number:479,title:"How do you load a remote Micro-frontend component in React?",question:"How do you load a remote Micro-frontend component in React?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Lazily import the remote module and render it inside \`<Suspense>\`. The remote must be reachable and expose the component via its Module Federation config. Wrap with error handling so a remote failure degrades gracefully rather than crashing the whole shell.

\`\`\`jsx
const RemoteCart = React.lazy(() => import('cart/Cart'));
function Shell() {
  return (
    <ErrorBoundary fallback={<p>Cart unavailable</p>}>
      <Suspense fallback={<Spinner />}>
        <RemoteCart />
      </Suspense>
    </ErrorBoundary>
  );
}
\`\`\``,tech:"react",displayNumber:479,sortKey:1451},{id:"react-1452",number:480,title:"What are the challenges of sharing state and dependencies in Micro-frontends?",question:"What are the challenges of sharing state and dependencies in Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Key challenges: duplicated libraries (multiple React copies → broken context/hooks), version skew, shared auth/session, global CSS conflicts, and consistent design systems. Mitigate by singletons (`shared` config), runtime shared libraries, event-bus or URL-based cross-app communication, and a federated design system. Avoid sharing mutable global state directly.\n\n```js\n// Prevent duplicate React with singletons\nshared: { react: { singleton: true, requiredVersion: '^18.0.0' } }\n```",tech:"react",displayNumber:480,sortKey:1452},{id:"react-1453",number:481,title:"How do you implement a shared design system across Micro-frontends?",question:"How do you implement a shared design system across Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Publish the design system as a standalone package (npm, internal registry) and mark it as a \`shared\` singleton in Module Federation so all remotes use the exact same instance. Alternatively, serve it from a CDN with import maps. This guarantees a single theme, components, and tokens across all apps without bundling duplicates.

\`\`\`js
// webpack.config.js (each app)
new ModuleFederationPlugin({
  shared: {
    '@company/design-system': { singleton: true, eager: true },
    react: { singleton: true },
    'react-dom': { singleton: true },
  },
});
\`\`\``,tech:"react",displayNumber:481,sortKey:1453},{id:"react-1454",number:482,title:"What are the tradeoffs of Runtime vs Build-time Micro-frontend integration?",question:"What are the tradeoffs of Runtime vs Build-time Micro-frontend integration?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Runtime (Module Federation): independent deploys, hot swaps, but runtime complexity, network latency, and duplicate risk. Build-time (monorepo, Nx, Turborepo): single build, zero duplication, atomic deploys, but all teams blocked by one pipeline and must coordinate versions. Most orgs start with build-time and adopt runtime only when deploy independence becomes a real bottleneck.\n\n```text\nBuild-time: Nx, Turborepo, Yarn workspaces - simpler, fewer footguns\nRuntime: Module Federation - true team autonomy, more complex ops\n```",tech:"react",displayNumber:482,sortKey:1454},{id:"react-1455",number:483,title:"How do you handle authentication and session sharing in Micro-frontends?",question:"How do you handle authentication and session sharing in Micro-frontends?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Keep auth state in a shared, trusted layer (HTTP-only cookies, a tiny auth MFE, or a JWT in localStorage). Don't share the whole auth object across remotes; instead, each remote validates the token or calls a `/me` endpoint. The shell app holds the session cookie and passes auth headers to backend calls. Avoid syncing auth state via events between remotes.\n\n```jsx\n// Shell sets auth cookie on login; each remote reads it or calls auth-service\n// Don't: RemoteA.onLogin(() => RemoteB.updateUser(...))\n// Do:    Cookie set by shell -> all requests include it automatically\n```",tech:"react",displayNumber:483,sortKey:1455},{id:"react-1456",number:484,title:"What is the `next/dynamic` import and when to use it?",question:"What is the `next/dynamic` import and when to use it?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/dynamic` is Next's wrapper around `React.lazy` + `Suspense` with SSR control. Use it to code-split heavy components (charts, editors) so their JS loads only when needed. The `ssr: false` option forces client-only rendering, useful for components that use `window` or are incompatible with server rendering.\n\n```jsx\nimport dynamic from 'next/dynamic';\nconst HeavyChart = dynamic(() => import('@/components/HeavyChart'), {\n  loading: () => <Skeleton />,\n  ssr: false, // skip SSR, client-only\n});\n```",tech:"react",displayNumber:484,sortKey:1456},{id:"react-1457",number:485,title:"How do you use `React.Suspense` with `next/dynamic` for code-splitting?",question:"How do you use `React.Suspense` with `next/dynamic` for code-splitting?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:`Wrap the dynamic import in \`<Suspense>\` to show a fallback while the chunk loads. The \`loading\` option in \`next/dynamic\` is a shorthand for this. The component only mounts after the chunk is downloaded and parsed, keeping initial bundle small.

\`\`\`jsx
import dynamic from 'next/dynamic';
import { Suspense } from 'react';
const Editor = dynamic(() => import('./Editor'));
export default function Page() {
  return (
    <Suspense fallback={<p>Loading editor...</p>}>
      <Editor />
    </Suspense>
  );
}
\`\`\``,tech:"react",displayNumber:485,sortKey:1457},{id:"react-1458",number:486,title:"What is the `use` hook (React 19) and how does it relate to Suspense?",question:"What is the `use` hook (React 19) and how does it relate to Suspense?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`\`use(promise)\` reads the resolved value of a promise, suspending if it's not ready. It works in Server Components (replaces \`await\` with readable composition) and Client Components (lets you kick off fetch early and read later). It integrates with Suspense boundaries and enables streaming with less boilerplate than \`await\` in async components.

\`\`\`jsx
import { use } from 'react';
// Server Component - use reads a promise, suspends if pending
function Post({ promise }) {
  const post = use(promise);
  return <article>{post.title}</article>;
}
// Client Component - fetch starts early, read suspends
const promise = fetch('/api/post/1').then((r) => r.json());
function ClientPost() {
  const post = use(promise);
  return <h1>{post.title}</h1>;
}
\`\`\``,tech:"react",displayNumber:486,sortKey:1458},{id:"react-1459",number:487,title:"How do you optimize bundle size in a Next.js App Router application?",question:"How do you optimize bundle size in a Next.js App Router application?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use RSC by default (zero client JS), split heavy UI with `dynamic` + `ssr: false`, import only needed lodash/date-fns parts, enable `optimizePackageImports` in `next.config.js`, audit with `@next/bundle-analyzer`, prefer native browser APIs, and keep Client Components small and leaf-only. Tree-shaking and granular imports are your biggest levers.\n\n```js\n// next.config.js\nmodule.exports = {\n  experimental: {\n    optimizePackageImports: ['lodash', 'date-fns', 'react-icons'],\n  },\n};\n```",tech:"react",displayNumber:487,sortKey:1459},{id:"react-1460",number:488,title:"What is `optimizePackageImports` and how does it work?",question:"What is `optimizePackageImports` and how does it work?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`optimizePackageImports` in `next.config.js` tells Next's compiler (Turbopack/SWC) to transform barrel-file imports like `import { A, B } from 'lib'` into direct imports (`import A from 'lib/A'`) for specified packages. This improves tree-shaking and reduces bundle size without changing source code. It works for popular icon/utility libraries out of the box.\n\n```js\n// next.config.js\nmodule.exports = {\n  experimental: {\n    optimizePackageImports: ['@mui/icons-material', 'lucide-react', 'date-fns'],\n  },\n};\n// import { Add, Delete } from '@mui/icons/material'\n// -> auto-rewritten to direct imports\n```",tech:"react",displayNumber:488,sortKey:1460},{id:"react-1461",number:489,title:"How do you measure and improve Core Web Vitals in Next.js?",question:"How do you measure and improve Core Web Vitals in Next.js?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use Vercel Speed Insights, web-vitals library, or Chrome DevTools. Key levers: RSC/SSR for LCP (fast HTML), `next/image` for CLS/LCP, font optimization (`next/font`), code-splitting for INP, streaming with Suspense, ISR for cache hits, and reducing third-party scripts. Set budgets in CI to catch regressions.\n\n```jsx\nimport Image from 'next/image';\nimport { Inter } from 'next/font/google';\nconst font = Inter({ subsets: ['latin'], display: 'swap' });\n// Use <Image priority /> for above-fold LCP\n```",tech:"react",displayNumber:489,sortKey:1461},{id:"react-1462",number:490,title:"What is the `next/font` optimization and why use it?",question:"What is the `next/font` optimization and why use it?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/font` self-hosts Google/Adobe fonts at build time, strips unused glyphs, inlines CSS, and prevents layout shift (`display: swap`). No external font requests = faster LCP, zero CLS from fonts, and privacy compliance (no Google Fonts requests). Works in RSC and Client Components.\n\n```jsx\nimport { Roboto } from 'next/font/google';\nconst roboto = Roboto({ subsets: ['latin'], display: 'swap', variable: '--font-roboto' });\nexport default function Layout({ children }) {\n  return <html className={roboto.variable}>{children}</html>;\n}\n```",tech:"react",displayNumber:490,sortKey:1462},{id:"react-1463",number:491,title:"How do you use `next/image` for optimal image loading?",question:"How do you use `next/image` for optimal image loading?",difficulty:"Intermediate",category:"Advanced Patterns & Next.js",answer:"`next/image` automatically optimizes, resizes, and serves modern formats (AVIF/WebP) on demand. It prevents CLS with explicit dimensions, lazy-loads below-fold, and supports `priority` for above-fold images. Configure `remotePatterns` for external sources. It replaces manual `img` tags entirely.\n\n```jsx\nimport Image from 'next/image';\n<Image src='/hero.jpg' alt='Hero' width={1200} height={600} priority />\n// For remote: next.config.js -> images: { remotePatterns: [{ hostname: 'cdn.example.com' }] }\n```",tech:"react",displayNumber:491,sortKey:1463},{id:"react-1464",number:492,title:"What is the `Middleware` in Next.js and how does it differ from Server Components?",question:"What is the `Middleware` in Next.js and how does it differ from Server Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Middleware runs before the request reaches the route, at the Edge. It can rewrite, redirect, modify headers, or respond directly. It's not a React component; it has no access to React context or Server Component data. Use it for auth checks, i18n routing, bot protection, A/B testing, or geolocation — anything that must run before rendering.

\`\`\`js
// middleware.ts
import { NextResponse } from 'next/server';
export function middleware(req) {
  const { pathname } = req.nextUrl;
  if (pathname.startsWith('/admin') && !req.cookies.get('admin')) {
    return NextResponse.redirect(new URL('/login', req.url));
  }
  return NextResponse.next();
}
export const config = { matcher: ['/admin/:path*'] };
\`\`\``,tech:"react",displayNumber:492,sortKey:1464},{id:"react-1465",number:493,title:"How do you implement internationalization (i18n) with the App Router?",question:"How do you implement internationalization (i18n) with the App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Use a `[locale]` segment (e.g., `app/[locale]/page.js`) to capture the locale in `params`. Generate static params for known locales. Use a dictionary per locale (JSON/TS modules) or a library like `next-intl`. Middleware can redirect `/` to the default locale. Avoid the old `next.config.js` i18n; the App Router handles it via file structure.\n\n```jsx\n// app/[locale]/page.js\nimport { getDict } from '@/lib/dict';\nexport async function generateStaticParams() {\n  return [{ locale: 'en' }, { locale: 'es' }];\n}\nexport default async function Page({ params }) {\n  const dict = await getDict((await params).locale);\n  return <h1>{dict.welcome}</h1>;\n}\n```",tech:"react",displayNumber:493,sortKey:1465},{id:"react-1466",number:494,title:"What are Parallel Routes in the App Router?",question:"What are Parallel Routes in the App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Parallel routes (slots) let you render multiple pages simultaneously in the same layout using named folders \`@folder\`. Each slot has its own loading/error state and can be navigated independently (e.g., modal, sidebar, tabs). Define \`default.js\` for unmatched slots. They enable complex UIs without a single page owning all content.

\`\`\`text
app/
  layout.js
  @modal/
    default.js    // shown when no modal active
    settings/
      page.js
  @sidebar/
    default.js
    page.js
// layout.js receives { children, modal, sidebar }
\`\`\``,tech:"react",displayNumber:494,sortKey:1466},{id:"react-1467",number:495,title:"What are Intercepting Routes and when would you use them?",question:"What are Intercepting Routes and when would you use them?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Intercepting routes ( `(..)` and `(...)` folders) let you show a different UI for a route while keeping the same URL — e.g., open a photo in a modal from the feed, but `/photo/1` still works as a standalone page on direct load/refresh. Use for modals, drawers, or preview overlays without breaking deep links.\n\n```text\napp/\n  feed/\n    page.js           // feed\n    (..)photo/[id]/   // intercepts /photo/:id, shows modal\n      page.js\n  photo/\n    [id]/\n      page.js         // standalone photo page\n// Click feed link -> modal; refresh -> full page\n```",tech:"react",displayNumber:495,sortKey:1467},{id:"react-1468",number:496,title:"How do you secure Server Actions against CSRF and malicious input?",question:"How do you secure Server Actions against CSRF and malicious input?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Next.js Server Actions include built-in CSRF protection (origin/header checks) and only accept same-origin requests by default. Validate all inputs with Zod/Valibot inside the action. Use `await` on `headers()`/`cookies()` to read auth. Never trust client data; treat actions like public API endpoints. Rate-limit via middleware if needed.\n\n```jsx\n'use server';\nimport { z } from 'zod';\nconst schema = z.object({ title: z.string().min(3).max(100) });\nexport async function createPost(formData) {\n  const data = schema.parse(Object.fromEntries(formData));\n  const session = await getSession();\n  if (!session) throw new Error('Unauthorized');\n  return db.post.create({ data: { ...data, authorId: session.userId } });\n}\n```",tech:"react",displayNumber:496,sortKey:1468},{id:"react-1469",number:497,title:"What are the performance implications of using many Client Components?",question:"What are the performance implications of using many Client Components?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Each `'use client'` boundary adds its component subtree to the client bundle. Deeply nested client trees ship more JS, increase hydration cost, and reduce streaming benefits. Keep Client Components as leaf nodes (buttons, forms, small widgets) and push data fetching up to Server Components. Audit with bundle analyzer; consolidate and lift client boundaries where possible.\n\n```text\nBad: <ClientLayout><ClientSidebar><ClientFeed><ClientCard/></ClientFeed></ClientSidebar></ClientLayout>\n// All client, huge bundle\nGood: <ServerLayout><ServerSidebar/><ServerFeed><ClientCard/></ServerFeed></ServerLayout>\n// Only Card is client\n```",tech:"react",displayNumber:497,sortKey:1469},{id:"react-1470",number:498,title:"How do you test Server Components and Server Actions?",question:"How do you test Server Components and Server Actions?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:`Server Components: test via Next.js testing utilities (render to HTML string, assert output) or integration tests with Playwright/Cypress against a running dev server. Server Actions: test the action logic directly (it's just an async function) by calling it with mock data and asserting DB calls, revalidations, and return values. Mock \`next/cache\` and DB.

\`\`\`jsx
// Test Server Action directly
import { createPost } from '@/actions';
jest.mock('@/lib/db', () => ({ post: { create: jest.fn() } }));
test('creates post', async () => {
  const fd = new FormData(); fd.set('title', 'Hello');
  await createPost(fd);
  expect(db.post.create).toHaveBeenCalledWith(expect.objectContaining({ title: 'Hello' }));
});
\`\`\``,tech:"react",displayNumber:498,sortKey:1470},{id:"react-1471",number:499,title:"What is the difference between `revalidatePath` and `revalidateTag`?",question:"What is the difference between `revalidatePath` and `revalidateTag`?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"`revalidatePath(path)` invalidates cached output for a specific route (page/layout). `revalidateTag(tag)` invalidates all `fetch` calls that were tagged with that tag, regardless of which route uses them. Tags enable granular, cross-route invalidation (e.g., invalidate all \"product-123\" data across the app). Paths are simpler for page-level refresh.\n\n```jsx\n// Tag-based (fine-grained, cross-route)\nfetch(url, { next: { tags: ['post-1'] } });\nrevalidateTag('post-1'); // invalidates everywhere that tag is used\n// Path-based (page-level)\nrevalidatePath('/posts/1'); // invalidates only that route's cache\n```",tech:"react",displayNumber:499,sortKey:1471},{id:"react-1472",number:500,title:"What are the key considerations when migrating from Pages Router to App Router?",question:"What are the key considerations when migrating from Pages Router to App Router?",difficulty:"Advanced",category:"Advanced Patterns & Next.js",answer:"Migrate incrementally: both routers can coexist. Convert leaf pages first. Replace `getStaticProps`/`getServerSideProps` with async RSC or `fetch` with cache options. Move data fetching into components. Replace `_app.js`/_`document.js` with `app/layout.js`. Convert API routes to Route Handlers. Audit `use client` boundaries; many Pages Router components can become Server Components. Test each route for hydration errors, dynamic usage, and streaming behavior.\n\n```text\nPages -> App migration checklist:\n1. Add app/ dir alongside pages/\n2. Migrate leaf pages to async Server Components\n3. Replace getStaticProps with fetch({ next: { revalidate } })\n4. Replace getServerSideProps with dynamic = 'force-dynamic'\n5. Convert _app/_document to root layout.js\n6. Move API routes to app/api/.../route.js\n7. Audit and minimize 'use client' usage\n8. Enable static optimization where possible\n```",tech:"react",displayNumber:500,sortKey:1472},{id:"node-1473",number:1,title:"What is Node.js?",question:"What is Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Node.js is a JavaScript runtime built on Chrome's V8 engine that runs JS outside the browser. It uses a single-threaded, event-driven, non-blocking I/O model, making it efficient for I/O-heavy and real-time applications.\n\n```js\nconsole.log('Hello from Node ' + process.version)\n```",tech:"node",displayNumber:1,sortKey:1473},{id:"node-1474",number:2,title:"Is Node.js single-threaded or multi-threaded?",question:"Is Node.js single-threaded or multi-threaded?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Your JavaScript runs on a single main thread (the event loop), but Node offloads blocking work (file I/O, DNS, crypto) to a `libuv` thread pool (default 4 threads). So it is single-threaded for JS execution, multi-threaded under the hood.\n\n```js\n// UV_THREADPOOL_SIZE controls the pool size\nprocess.env.UV_THREADPOOL_SIZE = 8\n```",tech:"node",displayNumber:2,sortKey:1474},{id:"node-1475",number:3,title:"What is V8?",question:"What is V8?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'V8 is Google\'s open-source JavaScript engine written in C++. It compiles JS directly to machine code (JIT), and Node embeds it to execute JavaScript. V8 also manages memory and garbage collection.\n\n```js\nconsole.log(process.versions.v8) // e.g. "12.4.254.21-node.20"\n```',tech:"node",displayNumber:3,sortKey:1475},{id:"node-1476",number:4,title:"What is libuv?",question:"What is libuv?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`libuv` is a C library that gives Node its asynchronous, event-driven capabilities. It provides the event loop, the thread pool, and cross-platform async I/O (file system, TCP/UDP, timers, child processes).\n\n```js\n// crypto.pbkdf2 runs on the libuv thread pool, not the main thread\nrequire('crypto').pbkdf2('pw', 'salt', 100000, 64, 'sha512', () => {})\n```",tech:"node",displayNumber:4,sortKey:1476},{id:"node-1477",number:5,title:"What is npm?",question:"What is npm?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'npm (Node Package Manager) is the default package manager for Node. It installs dependencies from the npm registry, manages versions via `package.json`, and runs scripts.\n\n```js\n// package.json\n{ "scripts": { "start": "node index.js" }, "dependencies": { "express": "^4.19.0" } }\n```',tech:"node",displayNumber:5,sortKey:1477},{id:"node-1478",number:6,title:"What is the difference between `node index.js` and running in the REPL?",question:"What is the difference between `node index.js` and running in the REPL?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`node index.js` executes a file. Running `node` with no argument starts the REPL (Read-Eval-Print-Loop), an interactive shell for evaluating expressions line by line — useful for quick experiments.\n\n```js\n$ node\n> 2 + 2\n4\n> .exit\n```",tech:"node",displayNumber:6,sortKey:1478},{id:"node-1479",number:7,title:"What are globals in Node.js?",question:"What are globals in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Globals are objects available everywhere without `require`: `global`, `process`, `console`, `Buffer`, `setTimeout`, `__dirname`, `__filename` (in CommonJS). Unlike the browser, there is no `window`; the global object is `global` (or `globalThis`).\n\n```js\nconsole.log(__dirname)  // directory of current module\nconsole.log(globalThis === global) // true\n```",tech:"node",displayNumber:7,sortKey:1479},{id:"node-1480",number:8,title:"What is `process` in Node.js?",question:"What is `process` in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process` is a global providing information and control over the current Node process: `process.argv`, `process.env`, `process.exit()`, `process.cwd()`, and events like `exit` and `uncaughtException`.\n\n```js\nconsole.log(process.argv)   // [node, script, ...args]\nconsole.log(process.env.NODE_ENV)\n```",tech:"node",displayNumber:8,sortKey:1480},{id:"node-1481",number:9,title:"What is `process.argv`?",question:"What is `process.argv`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process.argv` is an array of command-line arguments. Index 0 is the Node executable, index 1 is the script path, and the rest are user arguments.\n\n```js\n// node app.js --name Alice\nconst args = process.argv.slice(2) // ['--name', 'Alice']\n```",tech:"node",displayNumber:9,sortKey:1481},{id:"node-1482",number:10,title:"What is `process.env`?",question:"What is `process.env`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`process.env` holds environment variables as key-value strings. It is used for configuration like ports, API keys, and `NODE_ENV`, keeping secrets out of source code.\n\n```js\nconst port = process.env.PORT || 3000\n```",tech:"node",displayNumber:10,sortKey:1482},{id:"node-1483",number:11,title:"What is the difference between `__dirname` and `process.cwd()`?",question:"What is the difference between `__dirname` and `process.cwd()`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`__dirname` is the directory of the currently executing file. `process.cwd()` is the directory from which the Node process was launched. They differ when you run a script from another folder.\n\n```js\n// running: cd /home && node /app/index.js\nconsole.log(__dirname)      // /app\nconsole.log(process.cwd())  // /home\n```",tech:"node",displayNumber:11,sortKey:1483},{id:"node-1484",number:12,title:"What is `NODE_ENV` used for?",question:"What is `NODE_ENV` used for?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`NODE_ENV` is a convention (not built into Node) for signaling the environment: `development`, `production`, or `test`. Frameworks like Express enable optimizations and disable verbose errors when it is `production`.\n\n```js\nif (process.env.NODE_ENV === 'production') app.use(compression())\n```",tech:"node",displayNumber:12,sortKey:1484},{id:"node-1485",number:13,title:"How do you exit a Node process?",question:"How do you exit a Node process?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Call `process.exit(code)` — `0` means success, non-zero means failure. Avoid it in servers because it terminates immediately without flushing async work; prefer letting the event loop drain or setting `process.exitCode`.\n\n```js\nprocess.exitCode = 1 // graceful: exits when loop empties\n```",tech:"node",displayNumber:13,sortKey:1485},{id:"node-1486",number:14,title:"What is the difference between Node.js and the browser JavaScript environment?",question:"What is the difference between Node.js and the browser JavaScript environment?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Browsers provide `window`, `document`, and DOM APIs; Node provides `global`, `process`, file system, and network modules. Node has no DOM; browsers cannot access the file system directly. Both use V8 (in Chrome/Node).\n\n```js\n// Node: fs available, document undefined\nconst fs = require('fs')\n```",tech:"node",displayNumber:14,sortKey:1486},{id:"node-1487",number:15,title:"What are the different types of Node.js applications?",question:"What are the different types of Node.js applications?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Common types: REST/GraphQL APIs, real-time apps (chat, WebSockets), CLI tools, microservices, streaming servers, and build tooling. Node excels at I/O-bound and real-time workloads, less so at CPU-bound tasks.\n\n```js\n#!/usr/bin/env node\nconsole.log('A CLI tool')\n```",tech:"node",displayNumber:15,sortKey:1487},{id:"node-1488",number:16,title:"What is a callback in Node.js?",question:"What is a callback in Node.js?",difficulty:"Basic",category:"Node.js Fundamentals",answer:`A callback is a function passed as an argument to be invoked later, typically when an async operation completes. Node uses the "error-first" callback convention: \`(err, data) => {}\`.

\`\`\`js
require('fs').readFile('a.txt', (err, data) => {
  if (err) return console.error(err)
  console.log(data.toString())
})
\`\`\``,tech:"node",displayNumber:16,sortKey:1488},{id:"node-1489",number:17,title:"What is the error-first callback pattern?",question:"What is the error-first callback pattern?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"By convention, the first argument to a Node callback is an error (or `null` if none), and subsequent arguments carry results. This makes error handling consistent across the ecosystem.\n\n```js\nfunction done(err, result) {\n  if (err) throw err\n  console.log(result)\n}\n```",tech:"node",displayNumber:17,sortKey:1489},{id:"node-1490",number:18,title:"What is callback hell and how do you avoid it?",question:"What is callback hell and how do you avoid it?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Callback hell is deeply nested callbacks that become hard to read and maintain ("pyramid of doom"). Avoid it with named functions, Promises, or `async/await`.\n\n```js\n// Instead of nesting, chain promises\nreadFile(a).then(process).then(save).catch(handle)\n```',tech:"node",displayNumber:18,sortKey:1490},{id:"node-1491",number:19,title:"What is the difference between synchronous and asynchronous functions in Node?",question:"What is the difference between synchronous and asynchronous functions in Node?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Synchronous functions block the event loop until they finish (e.g., `fs.readFileSync`). Asynchronous functions return immediately and deliver results via callback/Promise, keeping the loop free. Prefer async in servers.\n\n```js\nconst data = fs.readFileSync('a.txt')      // blocks\nfs.readFile('a.txt', (e, d) => {})          // non-blocking\n```",tech:"node",displayNumber:19,sortKey:1491},{id:"node-1492",number:20,title:"What is `Buffer` in Node.js?",question:"What is `Buffer` in Node.js?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"A `Buffer` is a fixed-length chunk of raw binary data outside the V8 heap. It is used to handle bytes from files, network streams, and encoding conversions before JS strings existed with binary support.\n\n```js\nconst buf = Buffer.from('hi', 'utf8')\nconsole.log(buf)             // <Buffer 68 69>\nconsole.log(buf.toString())  // 'hi'\n```",tech:"node",displayNumber:20,sortKey:1492},{id:"node-1493",number:21,title:"What is the global object in Node vs the browser?",question:"What is the global object in Node vs the browser?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"In the browser it is `window`; in Node it is `global`. `globalThis` is the standard cross-environment reference to whichever applies. Variables declared with `var` at module scope do NOT attach to `global` in Node (modules are wrapped).\n\n```js\nglobal.myConfig = { debug: true }\nconsole.log(globalThis.myConfig.debug) // true\n```",tech:"node",displayNumber:21,sortKey:1493},{id:"node-1494",number:22,title:"Why is Node.js good for I/O-bound tasks but not CPU-bound tasks?",question:"Why is Node.js good for I/O-bound tasks but not CPU-bound tasks?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"I/O is offloaded to libuv/OS, so the single JS thread stays free to handle many concurrent requests. CPU-bound work (heavy computation) blocks that one thread, stalling all requests. Offload CPU work to worker threads or a separate service.\n\n```js\n// bad: blocks everyone\nfor (let i = 0; i < 1e10; i++) {}\n```",tech:"node",displayNumber:22,sortKey:1494},{id:"node-1495",number:23,title:"What is the `console` module?",question:"What is the `console` module?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`console` provides logging: `log`, `error`, `warn`, `info`, `table`, `time`/`timeEnd`, and `dir`. `console.log` writes to stdout, `console.error` to stderr, which lets you separate logs from errors when piping.\n\n```js\nconsole.time('t'); doWork(); console.timeEnd('t') // t: 12ms\n```",tech:"node",displayNumber:23,sortKey:1495},{id:"node-1496",number:24,title:"What is the difference between `stdout` and `stderr`?",question:"What is the difference between `stdout` and `stderr`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`process.stdout` is the standard output stream (normal program output); `process.stderr` is for errors and diagnostics. Separating them lets you redirect logs and errors independently (`node app 1>out.log 2>err.log`).\n\n```js\nprocess.stdout.write('result\\n')\nprocess.stderr.write('warning\\n')\n```",tech:"node",displayNumber:24,sortKey:1496},{id:"node-1497",number:25,title:"How do you read user input from the command line?",question:"How do you read user input from the command line?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Use the built-in `readline` module (or `process.stdin`) to read lines interactively from stdin.\n\n```js\nconst rl = require('readline').createInterface({ input: process.stdin, output: process.stdout })\nrl.question('Name? ', name => { console.log('Hi ' + name); rl.close() })\n```",tech:"node",displayNumber:25,sortKey:1497},{id:"node-1498",number:26,title:"What is the module wrapper function in Node?",question:"What is the module wrapper function in Node?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Before execution, Node wraps every CommonJS module in a function `(exports, require, module, __filename, __dirname) => {}`. This gives each module its own scope and injects those five variables, which is why they are available without importing.\n\n```js\n(function (exports, require, module, __filename, __dirname) {\n  // your module code runs here\n})\n```",tech:"node",displayNumber:26,sortKey:1498},{id:"node-1499",number:27,title:"What is `globalThis`?",question:"What is `globalThis`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`globalThis` is a standardized way (ES2020) to access the global object regardless of environment (browser `window`, Node `global`, workers `self`). It improves portability of shared code.\n\n```js\nglobalThis.crypto ??= require('crypto').webcrypto\n```",tech:"node",displayNumber:27,sortKey:1499},{id:"node-1500",number:28,title:"How does Node.js handle concurrency without multiple threads for JS?",question:"How does Node.js handle concurrency without multiple threads for JS?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Node uses an event loop plus non-blocking I/O. Instead of one thread per request, a single thread registers callbacks and processes completed I/O events as they arrive, achieving high concurrency with low overhead.\n\n```js\n// thousands of concurrent connections, one JS thread\nrequire('http').createServer((req, res) => res.end('ok')).listen(3000)\n```",tech:"node",displayNumber:28,sortKey:1500},{id:"node-1501",number:29,title:"What is the difference between `setTimeout(fn, 0)` and `setImmediate(fn)`?",question:"What is the difference between `setTimeout(fn, 0)` and `setImmediate(fn)`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`setImmediate` runs after the current poll phase (check phase); `setTimeout(fn, 0)` runs in the timers phase after at least 1ms. Inside an I/O callback, `setImmediate` always fires first; at the top level the order is nondeterministic.\n\n```js\nfs.readFile('a', () => {\n  setTimeout(() => console.log('timeout'), 0)\n  setImmediate(() => console.log('immediate')) // logs first\n})\n```",tech:"node",displayNumber:29,sortKey:1501},{id:"node-1502",number:30,title:"What is `process.nextTick()`?",question:"What is `process.nextTick()`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`process.nextTick(fn)` queues `fn` to run immediately after the current operation completes, before the event loop continues and before any timers/immediates. Overusing it can starve the loop (I/O never runs).\n\n```js\nconsole.log('start')\nprocess.nextTick(() => console.log('tick'))\nconsole.log('end') // start, end, tick\n```",tech:"node",displayNumber:30,sortKey:1502},{id:"node-1503",number:31,title:"What are the phases of the Node.js event loop?",question:"What are the phases of the Node.js event loop?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Phases in order: timers → pending callbacks → idle/prepare → poll → check → close callbacks. Between each phase (and each callback) Node drains the `nextTick` and microtask (Promise) queues.\n\n```js\n// poll = I/O callbacks; check = setImmediate; timers = setTimeout/Interval\n```",tech:"node",displayNumber:31,sortKey:1503},{id:"node-1504",number:32,title:"What is the difference between CommonJS and ES Modules?",question:"What is the difference between CommonJS and ES Modules?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"CommonJS uses `require`/`module.exports`, loads synchronously, and is the historical Node format. ES Modules use `import`/`export`, load asynchronously, support top-level `await`, and are the standard. Node picks the format via `.mjs`/`.cjs` or `\"type\"` in package.json.\n\n```js\n// CJS\nconst x = require('./x')\n// ESM\nimport x from './x.js'\n```",tech:"node",displayNumber:32,sortKey:1504},{id:"node-1505",number:33,title:"How do you enable ES Modules in Node?",question:"How do you enable ES Modules in Node?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Either use the `.mjs` extension, or set `"type": "module"` in `package.json`. Then use `import`/`export` and include file extensions in relative imports.\n\n```js\n// package.json\n{ "type": "module" }\n// index.js\nimport { readFile } from \'node:fs/promises\'\n```',tech:"node",displayNumber:33,sortKey:1505},{id:"node-1506",number:34,title:"What is the `node:` protocol in imports?",question:"What is the `node:` protocol in imports?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Prefixing a built-in module with `node:` (e.g. `node:fs`) makes it explicit that it is a core module, avoiding ambiguity with a same-named npm package and slightly speeding resolution.\n\n```js\nimport { readFile } from 'node:fs/promises'\nconst os = require('node:os')\n```",tech:"node",displayNumber:34,sortKey:1506},{id:"node-1507",number:35,title:"What is the difference between `module.exports` and `exports`?",question:"What is the difference between `module.exports` and `exports`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`exports` is a reference to `module.exports`. Adding properties to `exports` works, but reassigning `exports = ...` breaks the link — only `module.exports` is actually returned by `require`.\n\n```js\nexports.a = 1            // works\nmodule.exports = { a: 1 } // works\nexports = { a: 1 }        // does NOT export\n```",tech:"node",displayNumber:35,sortKey:1507},{id:"node-1508",number:36,title:"How does `require` resolve a module?",question:"How does `require` resolve a module?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`require` checks: core modules, then relative/absolute paths (with `.js`, `.json`, `.node` extensions and `index` files), then walks up `node_modules` folders. Resolved modules are cached so subsequent calls return the same instance.\n\n```js\nrequire('fs')        // core\nrequire('./util')    // relative -> ./util.js or ./util/index.js\nrequire('express')   // node_modules\n```",tech:"node",displayNumber:36,sortKey:1508},{id:"node-1509",number:37,title:"What is the module cache?",question:"What is the module cache?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Node caches modules after first load in `require.cache`, keyed by resolved path. Repeated `require` returns the same exported object (a singleton). Deleting the cache entry forces a reload.\n\n```js\ndelete require.cache[require.resolve('./config')]\nconst fresh = require('./config')\n```",tech:"node",displayNumber:37,sortKey:1509},{id:"node-1510",number:38,title:"What is a circular dependency and how does Node handle it?",question:"What is a circular dependency and how does Node handle it?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"A circular dependency is when module A requires B and B requires A. Node returns a partially-completed `exports` object to break the cycle, so one side may see an incomplete export. Refactor shared code into a third module to avoid subtle bugs.\n\n```js\n// a.js\nexports.done = false\nconst b = require('./b') // b sees a.done === false here\nexports.done = true\n```",tech:"node",displayNumber:38,sortKey:1510},{id:"node-1511",number:39,title:"What is the difference between `require` and `import`?",question:"What is the difference between `require` and `import`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`require` (CJS) is synchronous, dynamic, and can be called anywhere. `import` (ESM) is static, hoisted, asynchronously resolved, and normally must be top-level (dynamic `import()` returns a Promise). ESM enables tree-shaking.\n\n```js\nconst mod = await import('./mod.js') // dynamic ESM\n```",tech:"node",displayNumber:39,sortKey:1511},{id:"node-1512",number:40,title:"What are Worker Threads?",question:"What are Worker Threads?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"The `worker_threads` module runs JavaScript in parallel OS threads with their own event loop and V8 instance. They are ideal for CPU-bound work, communicating via message passing or `SharedArrayBuffer`.\n\n```js\nconst { Worker } = require('node:worker_threads')\nnew Worker('./heavy.js').on('message', console.log)\n```",tech:"node",displayNumber:40,sortKey:1512},{id:"node-1513",number:41,title:"When would you use Worker Threads vs Child Processes vs Cluster?",question:"When would you use Worker Threads vs Child Processes vs Cluster?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Worker Threads: CPU-bound JS with shared memory, low overhead. Child Processes: run separate programs/scripts with isolated memory. Cluster: fork multiple Node processes sharing a server port to use all CPU cores for I/O-bound servers.\n\n```js\n// Cluster for scaling an HTTP server across cores\nconst cluster = require('node:cluster')\nif (cluster.isPrimary) cluster.fork()\n```",tech:"node",displayNumber:41,sortKey:1513},{id:"node-1514",number:42,title:"What is the difference between blocking and non-blocking code in practice?",question:"What is the difference between blocking and non-blocking code in practice?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Blocking calls halt the thread until done, serializing all work; non-blocking calls return control immediately. In a server, one blocking call (e.g., `JSON.parse` of a huge string or `*Sync` I/O) delays every pending request.\n\n```js\n// non-blocking preferred in request handlers\nawait fs.promises.readFile('big.json')\n```",tech:"node",displayNumber:42,sortKey:1514},{id:"node-1515",number:43,title:"What is `process.hrtime()` / `performance.now()` used for?",question:"What is `process.hrtime()` / `performance.now()` used for?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"Both give high-resolution timing for benchmarking. `process.hrtime.bigint()` returns nanoseconds as a BigInt; `perf_hooks.performance.now()` returns fractional milliseconds. Use them instead of `Date.now()` for precise measurements.\n\n```js\nconst s = process.hrtime.bigint()\nwork()\nconsole.log(Number(process.hrtime.bigint() - s) / 1e6, 'ms')\n```",tech:"node",displayNumber:43,sortKey:1515},{id:"node-1516",number:44,title:"How do you handle uncaught exceptions?",question:"How do you handle uncaught exceptions?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Listen to `process.on('uncaughtException')` and `process.on('unhandledRejection')` to log and then gracefully shut down. These are last-resort handlers — the process is in an undefined state, so restart rather than continue.\n\n```js\nprocess.on('uncaughtException', err => { console.error(err); process.exit(1) })\n```",tech:"node",displayNumber:44,sortKey:1516},{id:"node-1517",number:45,title:"What is the difference between `process.on('exit')` and `process.on('beforeExit')`?",question:"What is the difference between `process.on('exit')` and `process.on('beforeExit')`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`beforeExit` fires when the event loop empties and can schedule more async work (won't fire on explicit `process.exit()`). `exit` fires just before the process terminates and only allows synchronous work.\n\n```js\nprocess.on('exit', code => console.log('exiting', code)) // sync only\n```",tech:"node",displayNumber:45,sortKey:1517},{id:"node-1518",number:46,title:"What are signals like SIGINT and SIGTERM?",question:"What are signals like SIGINT and SIGTERM?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"OS signals notify a process of events: `SIGINT` (Ctrl+C), `SIGTERM` (graceful stop, e.g. from orchestrators). Handling them enables graceful shutdown — closing servers and DB connections before exiting.\n\n```js\nprocess.on('SIGTERM', async () => { await server.close(); process.exit(0) })\n```",tech:"node",displayNumber:46,sortKey:1518},{id:"node-1519",number:47,title:"What is graceful shutdown and why does it matter?",question:"What is graceful shutdown and why does it matter?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Graceful shutdown stops accepting new requests, finishes in-flight requests, closes DB/socket connections, then exits. It prevents dropped requests and data corruption during deploys or autoscaling.\n\n```js\nprocess.on('SIGTERM', () => {\n  server.close(() => { db.end(); process.exit(0) })\n})\n```",tech:"node",displayNumber:47,sortKey:1519},{id:"node-1520",number:48,title:"How does garbage collection work in Node/V8?",question:"How does garbage collection work in Node/V8?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:'V8 uses a generational, mark-and-sweep collector. New objects live in the "young generation" (scavenged frequently); survivors are promoted to the "old generation" (collected less often). GC pauses the JS thread briefly ("stop-the-world").\n\n```js\n// run node --expose-gc to call global.gc() manually in tests\nif (global.gc) global.gc()\n```',tech:"node",displayNumber:48,sortKey:1520},{id:"node-1521",number:49,title:"What is a memory leak in Node and how do you detect it?",question:"What is a memory leak in Node and how do you detect it?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"A leak is memory that is retained but never released — common causes are unbounded caches, forgotten timers, and lingering event listeners. Detect via heap snapshots (`--inspect` + Chrome DevTools), `process.memoryUsage()`, or tools like clinic.js.\n\n```js\nconsole.log(process.memoryUsage().heapUsed / 1e6, 'MB')\n```",tech:"node",displayNumber:49,sortKey:1521},{id:"node-1522",number:50,title:"What is the maximum heap size in Node and how do you change it?",question:"What is the maximum heap size in Node and how do you change it?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"V8 caps the old-space heap (historically ~1.5–2GB on 64-bit). Raise it with `--max-old-space-size=<MB>`. If you routinely need more, reconsider the design (streaming, pagination) rather than just enlarging the heap.\n\n```js\n$ node --max-old-space-size=4096 app.js\n```",tech:"node",displayNumber:50,sortKey:1522},{id:"node-1523",number:51,title:"What is the difference between `Buffer.alloc` and `Buffer.allocUnsafe`?",question:"What is the difference between `Buffer.alloc` and `Buffer.allocUnsafe`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`Buffer.alloc(n)` returns a zero-filled buffer (safe). `Buffer.allocUnsafe(n)` is faster but may contain old memory contents, which can leak sensitive data if not fully overwritten. Prefer `alloc` unless you immediately fill the buffer.\n\n```js\nconst safe = Buffer.alloc(8)        // 00 00 00 00 00 00 00 00\nconst fast = Buffer.allocUnsafe(8)  // uninitialized bytes\n```",tech:"node",displayNumber:51,sortKey:1523},{id:"node-1524",number:52,title:"What is the difference between `path.join` and `path.resolve`?",question:"What is the difference between `path.join` and `path.resolve`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`path.join` concatenates segments and normalizes separators. `path.resolve` builds an absolute path by processing segments right-to-left until an absolute path is formed, defaulting to `cwd`. Use them instead of manual string concat for cross-platform safety.\n\n```js\npath.join('a', 'b', '..', 'c')     // 'a/c'\npath.resolve('a', '/b', 'c')        // '/b/c'\n```",tech:"node",displayNumber:52,sortKey:1524},{id:"node-1525",number:53,title:"What is the `util` module used for?",question:"What is the `util` module used for?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`util` provides helpers: `util.promisify` (callback→Promise), `util.inspect` (deep object formatting), `util.types` (type checks), and `util.format`. It bridges legacy callback APIs to Promises.\n\n```js\nconst readFile = require('util').promisify(require('fs').readFile)\nawait readFile('a.txt')\n```",tech:"node",displayNumber:53,sortKey:1525},{id:"node-1526",number:54,title:"What does `util.promisify` do?",question:"What does `util.promisify` do?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"It converts an error-first callback function into one that returns a Promise, letting you use `async/await` with legacy APIs.\n\n```js\nconst { promisify } = require('util')\nconst sleep = promisify(setTimeout)\nawait sleep(1000)\n```",tech:"node",displayNumber:54,sortKey:1526},{id:"node-1527",number:55,title:"What is the difference between `dependencies` and `devDependencies`?",question:"What is the difference between `dependencies` and `devDependencies`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`dependencies` are needed at runtime (e.g. express). `devDependencies` are only needed during development/build (e.g. jest, eslint, webpack). `npm install --production` skips devDependencies.\n\n```js\nnpm install express            // dependencies\nnpm install --save-dev jest    // devDependencies\n```",tech:"node",displayNumber:55,sortKey:1527},{id:"node-1528",number:56,title:"What is semantic versioning (semver)?",question:"What is semantic versioning (semver)?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'Semver is `MAJOR.MINOR.PATCH`: MAJOR for breaking changes, MINOR for backward-compatible features, PATCH for backward-compatible fixes. Ranges: `^1.2.3` allows minor/patch, `~1.2.3` allows patch only.\n\n```js\n"express": "^4.19.0"  // >=4.19.0 <5.0.0\n```',tech:"node",displayNumber:56,sortKey:1528},{id:"node-1529",number:57,title:"What is `package-lock.json`?",question:"What is `package-lock.json`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`package-lock.json` records the exact resolved version and integrity hash of every installed package (including transitive deps), ensuring reproducible installs across machines and CI. Commit it to source control.\n\n```js\nnpm ci  // installs strictly from the lock file\n```",tech:"node",displayNumber:57,sortKey:1529},{id:"node-1530",number:58,title:"What is the difference between `npm install` and `npm ci`?",question:"What is the difference between `npm install` and `npm ci`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`npm install` resolves versions and may update the lock file. `npm ci` does a clean, exact install from `package-lock.json` (deletes `node_modules` first), is faster, and is meant for CI/production reproducibility.\n\n```js\nnpm ci --omit=dev\n```",tech:"node",displayNumber:58,sortKey:1530},{id:"node-1531",number:59,title:"What are npm scripts?",question:"What are npm scripts?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'Scripts in `package.json` under `"scripts"` are command shortcuts run with `npm run <name>`. `start`, `test`, `stop`, and `restart` can be run without `run`. They can chain via `pre`/`post` hooks.\n\n```js\n"scripts": { "build": "tsc", "prebuild": "rimraf dist" }\n```',tech:"node",displayNumber:59,sortKey:1531},{id:"node-1532",number:60,title:"What is `npx`?",question:"What is `npx`?",difficulty:"Basic",category:"Node.js Fundamentals",answer:'`npx` runs a package binary without globally installing it, downloading it temporarily if needed. It is handy for one-off tools like scaffolders (`npx create-react-app`).\n\n```js\nnpx cowsay "Hello"\n```',tech:"node",displayNumber:60,sortKey:1532},{id:"node-1533",number:61,title:"What is the difference between local and global npm installs?",question:"What is the difference between local and global npm installs?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"Local installs (`npm install pkg`) go into the project's `node_modules` and are used by that project. Global installs (`-g`) place binaries on the system PATH for CLI use across projects but should be avoided for project dependencies.\n\n```js\nnpm install -g nodemon   // CLI available everywhere\n```",tech:"node",displayNumber:61,sortKey:1533},{id:"node-1534",number:62,title:"What is `engines` in package.json?",question:"What is `engines` in package.json?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:'The `engines` field declares which Node/npm versions your package supports. With `engine-strict`, npm errors on incompatible versions, preventing subtle runtime bugs.\n\n```js\n"engines": { "node": ">=18.0.0" }\n```',tech:"node",displayNumber:62,sortKey:1534},{id:"node-1535",number:63,title:"What are peerDependencies?",question:"What are peerDependencies?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:'`peerDependencies` declare a package that the host project must provide (e.g. a React plugin needing React). It avoids duplicate/incompatible copies of a shared library. npm 7+ auto-installs them by default.\n\n```js\n"peerDependencies": { "react": ">=17" }\n```',tech:"node",displayNumber:63,sortKey:1535},{id:"node-1536",number:64,title:"How do you create a custom global module/binary?",question:"How do you create a custom global module/binary?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:'Add a `bin` field pointing to an executable script with a shebang, then install globally or link. This is how CLI tools expose commands.\n\n```js\n// package.json\n"bin": { "mytool": "./cli.js" }\n// cli.js first line\n#!/usr/bin/env node\n```',tech:"node",displayNumber:64,sortKey:1536},{id:"node-1537",number:65,title:"How does Node handle unhandled promise rejections?",question:"How does Node handle unhandled promise rejections?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"Since Node 15, an unhandled rejection terminates the process by default (previously a warning). Always attach `.catch` or wrap `await` in try/catch, and add a global `unhandledRejection` handler as a safety net.\n\n```js\nprocess.on('unhandledRejection', reason => console.error(reason))\n```",tech:"node",displayNumber:65,sortKey:1537},{id:"node-1538",number:66,title:"What is the `os` module?",question:"What is the `os` module?",difficulty:"Basic",category:"Node.js Fundamentals",answer:"`os` exposes operating-system info: CPU count, memory, platform, hostname, network interfaces, and `os.tmpdir()`. It is often used to size worker/cluster pools.\n\n```js\nconst os = require('node:os')\nconsole.log(os.cpus().length, 'cores')\n```",tech:"node",displayNumber:66,sortKey:1538},{id:"node-1539",number:67,title:"What is the difference between `fs.readFile` and `fs.createReadStream`?",question:"What is the difference between `fs.readFile` and `fs.createReadStream`?",difficulty:"Advanced",category:"Node.js Fundamentals",answer:"`readFile` loads the entire file into memory before the callback fires — simple but memory-heavy for large files. `createReadStream` reads in chunks, letting you process data incrementally with low, constant memory.\n\n```js\nfs.createReadStream('big.log').pipe(res) // stream to client\n```",tech:"node",displayNumber:67,sortKey:1539},{id:"node-1540",number:68,title:"What is the purpose of `Buffer.concat`?",question:"What is the purpose of `Buffer.concat`?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`Buffer.concat(list)` joins an array of buffers into one, commonly used to assemble chunks collected from a stream into a complete payload.\n\n```js\nconst chunks = []\nreq.on('data', c => chunks.push(c))\nreq.on('end', () => console.log(Buffer.concat(chunks).toString()))\n```",tech:"node",displayNumber:68,sortKey:1540},{id:"node-1541",number:69,title:"What is the difference between `null` and `undefined` handling in Node config?",question:"What is the difference between `null` and `undefined` handling in Node config?",difficulty:"Intermediate",category:"Node.js Fundamentals",answer:"`process.env` values are always strings or `undefined` (never `null`). Comparisons and defaults should account for `undefined`; use `??` for nullish fallback and explicit parsing for numbers/booleans.\n\n```js\nconst port = Number(process.env.PORT ?? 3000)\nconst debug = process.env.DEBUG === 'true'\n```",tech:"node",displayNumber:69,sortKey:1541},{id:"node-1542",number:70,title:"How do you profile CPU usage of a Node application?",question:"How do you profile CPU usage of a Node application?",difficulty:"Experienced",category:"Node.js Fundamentals",answer:"Use `node --prof` to generate a V8 log then `node --prof-process`, or `--inspect` with Chrome DevTools CPU profiler, or tools like `clinic flame` / `0x` for flame graphs. Profiling reveals hot functions and blocking code.\n\n```js\n$ node --prof app.js\n$ node --prof-process isolate-*.log > profile.txt\n```",tech:"node",displayNumber:70,sortKey:1542},{id:"node-1543",number:1,title:"What is a module in Node.js?",question:"What is a module in Node.js?",difficulty:"Basic",category:"Modules & npm",answer:"A module is a self-contained unit of code (usually one file) with its own scope. It exposes functionality via exports and consumes others via `require`/`import`, enabling reuse and separation of concerns.\n\n```js\n// math.js\nmodule.exports.add = (a, b) => a + b\n// app.js\nconst { add } = require('./math')\n```",tech:"node",displayNumber:1,sortKey:1543},{id:"node-1544",number:2,title:"What are the three types of modules in Node?",question:"What are the three types of modules in Node?",difficulty:"Basic",category:"Modules & npm",answer:"Core modules (built into Node, e.g. `fs`, `http`), local modules (your own files), and third-party modules (installed from npm into `node_modules`).\n\n```js\nconst fs = require('fs')          // core\nconst util = require('./util')    // local\nconst express = require('express')// third-party\n```",tech:"node",displayNumber:2,sortKey:1544},{id:"node-1545",number:3,title:"How do you export multiple values from a module?",question:"How do you export multiple values from a module?",difficulty:"Basic",category:"Modules & npm",answer:"Attach properties to `module.exports` (CJS) or use named `export` statements (ESM). A default export represents the single primary value.\n\n```js\n// CJS\nmodule.exports = { add, subtract }\n// ESM\nexport const add = ...; export default calculator\n```",tech:"node",displayNumber:3,sortKey:1545},{id:"node-1546",number:4,title:"What happens when you `require` a JSON file?",question:"What happens when you `require` a JSON file?",difficulty:"Basic",category:"Modules & npm",answer:"Node parses the JSON and returns it as a JavaScript object. The result is cached like any module, so edits require clearing the cache to reload.\n\n```js\nconst config = require('./config.json')\nconsole.log(config.port)\n```",tech:"node",displayNumber:4,sortKey:1546},{id:"node-1547",number:5,title:"How do you import JSON in ES Modules?",question:"How do you import JSON in ES Modules?",difficulty:"Intermediate",category:"Modules & npm",answer:"Use an import attribute (`with { type: 'json' }`) in modern Node, or read/parse it manually. Older syntax used `assert { type: 'json' }`.\n\n```js\nimport config from './config.json' with { type: 'json' }\n```",tech:"node",displayNumber:5,sortKey:1547},{id:"node-1548",number:6,title:"What is the difference between named and default exports?",question:"What is the difference between named and default exports?",difficulty:"Intermediate",category:"Modules & npm",answer:"Named exports export multiple bindings imported by exact name (with `{}`); a default export is a single value imported under any name. A module can mix both.\n\n```js\nexport default App\nexport function helper() {}\nimport App, { helper } from './App.js'\n```",tech:"node",displayNumber:6,sortKey:1548},{id:"node-1549",number:7,title:"How do you re-export from another module?",question:"How do you re-export from another module?",difficulty:"Intermediate",category:"Modules & npm",answer:"Use `export ... from` (ESM) to create a barrel file that aggregates and re-exposes exports, simplifying imports for consumers.\n\n```js\n// index.js barrel\nexport { add } from './math.js'\nexport { default as User } from './User.js'\n```",tech:"node",displayNumber:7,sortKey:1549},{id:"node-1550",number:8,title:"What is a barrel file and what is its downside?",question:"What is a barrel file and what is its downside?",difficulty:"Advanced",category:"Modules & npm",answer:"A barrel file re-exports many modules from one index for cleaner imports. Downside: it can hurt tree-shaking and increase load time because importing one symbol may pull in the whole barrel.\n\n```js\n// consumers do: import { add } from './utils'\n// but bundlers may load all of ./utils\n```",tech:"node",displayNumber:8,sortKey:1550},{id:"node-1551",number:9,title:"How does Node find `node_modules`?",question:"How does Node find `node_modules`?",difficulty:"Intermediate",category:"Modules & npm",answer:"Node searches the current directory's `node_modules`, then walks up parent directories until it reaches the filesystem root. This lets nested packages share or override dependencies.\n\n```js\n// require('lodash') from /app/src looks in\n// /app/src/node_modules, /app/node_modules, /node_modules\n```",tech:"node",displayNumber:9,sortKey:1551},{id:"node-1552",number:10,title:"What is the difference between `require('./x')` and `require('x')`?",question:"What is the difference between `require('./x')` and `require('x')`?",difficulty:"Basic",category:"Modules & npm",answer:"`./x` (or `../`, `/`) is a path-based local module. A bare specifier `x` is resolved as a core module or from `node_modules`. Missing the `./` is a common cause of \"cannot find module\" errors.\n\n```js\nrequire('./config')  // local file\nrequire('config')     // npm package\n```",tech:"node",displayNumber:10,sortKey:1552},{id:"node-1553",number:11,title:"What are the fields in package.json for entry points?",question:"What are the fields in package.json for entry points?",difficulty:"Advanced",category:"Modules & npm",answer:'`main` is the CJS entry, `module` hints an ESM entry for bundlers, `exports` (modern) defines conditional/subpath entry points and can restrict access, and `types` points to TypeScript declarations.\n\n```js\n"main": "./dist/index.cjs",\n"exports": { ".": { "import": "./dist/index.mjs", "require": "./dist/index.cjs" } }\n```',tech:"node",displayNumber:11,sortKey:1553},{id:"node-1554",number:12,title:"What is the `exports` map (conditional exports)?",question:"What is the `exports` map (conditional exports)?",difficulty:"Experienced",category:"Modules & npm",answer:'The `exports` field defines which files consumers may import and provides condition-based resolution (`import`, `require`, `node`, `browser`, `default`). It encapsulates internals and enables dual CJS/ESM packages.\n\n```js\n"exports": {\n  "./feature": { "import": "./feature.mjs", "require": "./feature.cjs" }\n}\n```',tech:"node",displayNumber:12,sortKey:1554},{id:"node-1555",number:13,title:"What is a dual package (CJS + ESM)?",question:"What is a dual package (CJS + ESM)?",difficulty:"Experienced",category:"Modules & npm",answer:'A dual package ships both CommonJS and ES Module builds so it works everywhere. Risk: the "dual package hazard" — the same module loaded in both formats yields two separate instances with duplicated state.\n\n```js\n"exports": { "import": "./esm/index.js", "require": "./cjs/index.js" }\n```',tech:"node",displayNumber:13,sortKey:1555},{id:"node-1556",number:14,title:"How do you conditionally load a module?",question:"How do you conditionally load a module?",difficulty:"Intermediate",category:"Modules & npm",answer:"In CJS, call `require` inside a branch (it is dynamic). In ESM, use dynamic `import()` which returns a Promise, enabling lazy or conditional loading.\n\n```js\nif (useRedis) var store = require('./redisStore')\nconst mod = await import(`./drivers/${name}.js`)\n```",tech:"node",displayNumber:14,sortKey:1556},{id:"node-1557",number:15,title:"What is lazy loading of modules and why use it?",question:"What is lazy loading of modules and why use it?",difficulty:"Advanced",category:"Modules & npm",answer:"Lazy loading defers `require`/`import` until a module is actually needed, reducing startup time and memory when a feature may never run. Common for optional/heavy dependencies.\n\n```js\nfunction generatePdf() {\n  const PDFKit = require('pdfkit') // loaded only on first use\n}\n```",tech:"node",displayNumber:15,sortKey:1557},{id:"node-1558",number:16,title:"What is the difference between `npm update` and `npm install`?",question:"What is the difference between `npm update` and `npm install`?",difficulty:"Intermediate",category:"Modules & npm",answer:"`npm install` installs versions satisfying `package.json`, adding missing ones. `npm update` bumps installed packages to the latest allowed by their semver ranges and updates the lock file.\n\n```js\nnpm update lodash\n```",tech:"node",displayNumber:16,sortKey:1558},{id:"node-1559",number:17,title:"How do you check for outdated packages?",question:"How do you check for outdated packages?",difficulty:"Basic",category:"Modules & npm",answer:"`npm outdated` lists installed packages with their current, wanted (max satisfying range), and latest versions, helping you plan upgrades.\n\n```js\nnpm outdated\n```",tech:"node",displayNumber:17,sortKey:1559},{id:"node-1560",number:18,title:"What is `npm audit`?",question:"What is `npm audit`?",difficulty:"Intermediate",category:"Modules & npm",answer:"`npm audit` scans your dependency tree against a vulnerability database and reports issues with severity. `npm audit fix` applies compatible fixes automatically.\n\n```js\nnpm audit --audit-level=high\nnpm audit fix\n```",tech:"node",displayNumber:18,sortKey:1560},{id:"node-1561",number:19,title:"What is the difference between `~` and `^` in version ranges?",question:"What is the difference between `~` and `^` in version ranges?",difficulty:"Intermediate",category:"Modules & npm",answer:'`~1.2.3` allows patch updates (`>=1.2.3 <1.3.0`). `^1.2.3` allows minor + patch (`>=1.2.3 <2.0.0`). For `0.x`, `^` is stricter and behaves closer to `~`.\n\n```js\n"~1.2.3" // 1.2.x\n"^1.2.3" // 1.x.x\n```',tech:"node",displayNumber:19,sortKey:1561},{id:"node-1562",number:20,title:"How do you pin an exact dependency version?",question:"How do you pin an exact dependency version?",difficulty:"Basic",category:"Modules & npm",answer:'Specify the version without a range prefix, or install with `--save-exact`. The lock file already pins exact resolved versions for reproducibility.\n\n```js\nnpm install left-pad@1.3.0 --save-exact\n// "left-pad": "1.3.0"\n```',tech:"node",displayNumber:20,sortKey:1562},{id:"node-1563",number:21,title:"What are optionalDependencies?",question:"What are optionalDependencies?",difficulty:"Advanced",category:"Modules & npm",answer:"`optionalDependencies` may fail to install without failing the whole install. Your code must handle their absence at runtime (often platform-specific native modules).\n\n```js\nlet fsevents\ntry { fsevents = require('fsevents') } catch { /* not on this OS */ }\n```",tech:"node",displayNumber:21,sortKey:1563},{id:"node-1564",number:22,title:"What is `bundledDependencies`?",question:"What is `bundledDependencies`?",difficulty:"Advanced",category:"Modules & npm",answer:'`bundledDependencies` (or `bundleDependencies`) lists packages included inside your published tarball, so they ship with your package rather than being fetched from the registry. Rarely needed today.\n\n```js\n"bundledDependencies": ["my-private-fork"]\n```',tech:"node",displayNumber:22,sortKey:1564},{id:"node-1565",number:23,title:"What is an npm workspace / monorepo?",question:"What is an npm workspace / monorepo?",difficulty:"Experienced",category:"Modules & npm",answer:'npm workspaces let one repo host multiple packages sharing a single `node_modules` and lock file. Dependencies between local packages are symlinked, simplifying multi-package development.\n\n```js\n// root package.json\n"workspaces": ["packages/*"]\n```',tech:"node",displayNumber:23,sortKey:1565},{id:"node-1566",number:24,title:"How do you run a script across all workspaces?",question:"How do you run a script across all workspaces?",difficulty:"Advanced",category:"Modules & npm",answer:"Use `npm run <script> --workspaces` (or `-w <name>` for one). This executes the script in each package that defines it.\n\n```js\nnpm run build --workspaces --if-present\n```",tech:"node",displayNumber:24,sortKey:1566},{id:"node-1567",number:25,title:"What is `npm link`?",question:"What is `npm link`?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm link` symlinks a local package into the global folder, then into a consuming project, so you can develop and test a package locally before publishing. Beware duplicate peer deps.\n\n```js\ncd my-lib && npm link\ncd ../app && npm link my-lib\n```",tech:"node",displayNumber:25,sortKey:1567},{id:"node-1568",number:26,title:"How do you publish a package to npm?",question:"How do you publish a package to npm?",difficulty:"Intermediate",category:"Modules & npm",answer:"Log in (`npm login`), set `name`/`version`/`files` in package.json, then `npm publish`. Scoped packages need `--access public` to be public.\n\n```js\nnpm version patch\nnpm publish --access public\n```",tech:"node",displayNumber:26,sortKey:1568},{id:"node-1569",number:27,title:"What does the `files` field in package.json do?",question:"What does the `files` field in package.json do?",difficulty:"Intermediate",category:"Modules & npm",answer:'`files` whitelists which files are included in the published tarball. Without it, npm uses `.npmignore` (or `.gitignore`) to exclude files. Whitelisting avoids accidentally shipping source/tests.\n\n```js\n"files": ["dist", "README.md"]\n```',tech:"node",displayNumber:27,sortKey:1569},{id:"node-1570",number:28,title:"What is `.npmignore` and how does it interact with `.gitignore`?",question:"What is `.npmignore` and how does it interact with `.gitignore`?",difficulty:"Advanced",category:"Modules & npm",answer:"`.npmignore` excludes files from the published package. If it is absent, npm falls back to `.gitignore`. If both exist, `.npmignore` wins for publishing. The `files` whitelist takes precedence over both.\n\n```js\n// .npmignore\ntest/\n*.log\n```",tech:"node",displayNumber:28,sortKey:1570},{id:"node-1571",number:29,title:"What is a scoped package?",question:"What is a scoped package?",difficulty:"Intermediate",category:"Modules & npm",answer:"A scoped package is namespaced under a user/org with `@scope/name`, avoiding name collisions and enabling private packages. Scopes map to registries/organizations.\n\n```js\nnpm install @myorg/logger\n```",tech:"node",displayNumber:29,sortKey:1571},{id:"node-1572",number:30,title:"How do you use a private registry?",question:"How do you use a private registry?",difficulty:"Advanced",category:"Modules & npm",answer:"Configure the registry globally or per-scope in `.npmrc`, with an auth token. This routes installs/publishes for a scope to your private registry (e.g. GitHub Packages, Verdaccio).\n\n```js\n// .npmrc\n@myorg:registry=https://npm.pkg.github.com\n//npm.pkg.github.com/:_authToken=${TOKEN}\n```",tech:"node",displayNumber:30,sortKey:1572},{id:"node-1573",number:31,title:"What is `.npmrc` used for?",question:"What is `.npmrc` used for?",difficulty:"Intermediate",category:"Modules & npm",answer:"`.npmrc` stores npm configuration (registry, auth tokens, `save-exact`, `engine-strict`, proxy). It can be global, per-user, or per-project, with project-level overriding.\n\n```js\n// .npmrc\nsave-exact=true\nengine-strict=true\n```",tech:"node",displayNumber:31,sortKey:1573},{id:"node-1574",number:32,title:"How do you override a transitive dependency version?",question:"How do you override a transitive dependency version?",difficulty:"Experienced",category:"Modules & npm",answer:'Use the `overrides` field (npm 8+) to force a nested dependency to a specific version, useful for patching a vulnerable transitive package.\n\n```js\n"overrides": { "minimist": "1.2.8" }\n```',tech:"node",displayNumber:32,sortKey:1574},{id:"node-1575",number:33,title:"What is the difference between npm, yarn, and pnpm?",question:"What is the difference between npm, yarn, and pnpm?",difficulty:"Advanced",category:"Modules & npm",answer:"All manage packages. Yarn added lockfiles/workspaces early and offers Plug'n'Play. pnpm uses a content-addressed global store with symlinks, saving disk space and enforcing stricter dependency isolation. npm is the default and now supports most features.\n\n```js\npnpm install   // hard-links from a global store\n```",tech:"node",displayNumber:33,sortKey:1575},{id:"node-1576",number:34,title:"Why does pnpm save disk space?",question:"Why does pnpm save disk space?",difficulty:"Advanced",category:"Modules & npm",answer:"pnpm stores each package version once in a global content-addressable store and hard-links it into each project's `node_modules`, so many projects share the same physical files instead of duplicating them.\n\n```js\n// ~/.pnpm-store holds a single copy per version\n```",tech:"node",displayNumber:34,sortKey:1576},{id:"node-1577",number:35,title:"What is dependency hoisting?",question:"What is dependency hoisting?",difficulty:"Advanced",category:"Modules & npm",answer:"Hoisting moves shared transitive dependencies up to a top-level `node_modules` to deduplicate. It can cause \"phantom dependencies\" — code accidentally using a package it never declared. pnpm avoids this with a strict layout.\n\n```js\n// You import 'ms' without declaring it because express hoisted it — fragile\n```",tech:"node",displayNumber:35,sortKey:1577},{id:"node-1578",number:36,title:"What is a phantom dependency?",question:"What is a phantom dependency?",difficulty:"Experienced",category:"Modules & npm",answer:"A phantom dependency is a package your code imports but never declared in `package.json`; it works only because hoisting exposed it. It breaks when the transitive dep changes. Always declare what you import.\n\n```js\nrequire('lodash') // works today via hoisting, may vanish tomorrow\n```",tech:"node",displayNumber:36,sortKey:1578},{id:"node-1579",number:37,title:"How do you create and use a local file dependency?",question:"How do you create and use a local file dependency?",difficulty:"Intermediate",category:"Modules & npm",answer:'Reference a local path or tarball in `package.json` using `file:`. Useful for monorepos or testing before publishing.\n\n```js\n"dependencies": { "my-lib": "file:../my-lib" }\n```',tech:"node",displayNumber:37,sortKey:1579},{id:"node-1580",number:38,title:"How do you install directly from a Git repository?",question:"How do you install directly from a Git repository?",difficulty:"Intermediate",category:"Modules & npm",answer:"Provide a git URL (optionally with a commit/branch/tag) as the version. npm clones and builds it. Useful for forks or unpublished packages.\n\n```js\nnpm install github:user/repo#v2.0.0\n```",tech:"node",displayNumber:38,sortKey:1580},{id:"node-1581",number:39,title:"What are npm lifecycle hooks?",question:"What are npm lifecycle hooks?",difficulty:"Advanced",category:"Modules & npm",answer:'npm runs scripts around events: `preinstall`, `install`, `postinstall`, `prepublishOnly`, `prepare`. `pre`/`post` prefixes wrap any custom script too. Beware running untrusted postinstall scripts (supply-chain risk).\n\n```js\n"scripts": { "prepare": "husky install" }\n```',tech:"node",displayNumber:39,sortKey:1581},{id:"node-1582",number:40,title:"What security risk do postinstall scripts pose?",question:"What security risk do postinstall scripts pose?",difficulty:"Experienced",category:"Modules & npm",answer:"A malicious dependency's `postinstall` runs arbitrary code on your machine/CI, a common supply-chain attack. Mitigate with `--ignore-scripts`, lockfile review, and pinning.\n\n```js\nnpm ci --ignore-scripts\n```",tech:"node",displayNumber:40,sortKey:1582},{id:"node-1583",number:41,title:"How do you clear the npm cache?",question:"How do you clear the npm cache?",difficulty:"Basic",category:"Modules & npm",answer:"Use `npm cache clean --force`. Modern npm self-heals the cache, so this is rarely necessary; `npm cache verify` checks integrity instead.\n\n```js\nnpm cache verify\n```",tech:"node",displayNumber:41,sortKey:1583},{id:"node-1584",number:42,title:"What is `npm dedupe`?",question:"What is `npm dedupe`?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm dedupe` rearranges the dependency tree to move compatible duplicate packages to a shared higher level, reducing installed copies and bundle size.\n\n```js\nnpm dedupe\n```",tech:"node",displayNumber:42,sortKey:1584},{id:"node-1585",number:43,title:"How do you list installed dependencies as a tree?",question:"How do you list installed dependencies as a tree?",difficulty:"Basic",category:"Modules & npm",answer:"`npm ls` prints the dependency tree; add a package name to find why it is installed, or `--all` for the full depth.\n\n```js\nnpm ls express\n```",tech:"node",displayNumber:43,sortKey:1585},{id:"node-1586",number:44,title:"How do you find why a package is installed?",question:"How do you find why a package is installed?",difficulty:"Advanced",category:"Modules & npm",answer:"`npm explain <pkg>` (or `npm ls <pkg>`) shows the dependency chains that pull the package in, helping remove or override unwanted transitive deps.\n\n```js\nnpm explain minimist\n```",tech:"node",displayNumber:44,sortKey:1586},{id:"node-1587",number:45,title:"What is `npm-shrinkwrap.json`?",question:"What is `npm-shrinkwrap.json`?",difficulty:"Experienced",category:"Modules & npm",answer:"It is a publishable version of `package-lock.json`. Unlike the lock file, it IS included when you publish, letting library authors lock the entire dependency tree for consumers (rarely recommended for libraries).\n\n```js\nnpm shrinkwrap\n```",tech:"node",displayNumber:45,sortKey:1587},{id:"node-1588",number:46,title:"What is tree-shaking and how do modules affect it?",question:"What is tree-shaking and how do modules affect it?",difficulty:"Experienced",category:"Modules & npm",answer:'Tree-shaking eliminates unused exports at bundle time. It requires static ES Modules (`import`/`export`) and `"sideEffects": false` hints; dynamic CJS `require` defeats it because usage can\'t be analyzed statically.\n\n```js\n"sideEffects": false // safe to drop unused exports\n```',tech:"node",displayNumber:46,sortKey:1588},{id:"node-1589",number:47,title:'What does `"sideEffects": false` mean?',question:'What does `"sideEffects": false` mean?',difficulty:"Experienced",category:"Modules & npm",answer:'It tells bundlers that importing a module has no side effects (like polyfills or CSS registration), so unused imports can be safely removed. Mark specific files if some do have side effects.\n\n```js\n"sideEffects": ["*.css", "./src/polyfill.js"]\n```',tech:"node",displayNumber:47,sortKey:1589},{id:"node-1590",number:48,title:"How do you create a monorepo with shared code?",question:"How do you create a monorepo with shared code?",difficulty:"Experienced",category:"Modules & npm",answer:'Use workspaces (npm/pnpm/yarn) or tools like Turborepo/Nx. Shared packages live under `packages/*`, are symlinked, and imported by name; a build orchestrator caches and parallelizes tasks.\n\n```js\n"workspaces": ["apps/*", "packages/*"]\n```',tech:"node",displayNumber:48,sortKey:1590},{id:"node-1591",number:49,title:"What is the difference between `main`, `module`, and `browser` fields?",question:"What is the difference between `main`, `module`, and `browser` fields?",difficulty:"Experienced",category:"Modules & npm",answer:'`main` is the default CJS entry, `module` points bundlers to an ESM build for tree-shaking, and `browser` provides browser-specific replacements (e.g. swapping `fs` for a shim). The `exports` map now supersedes these.\n\n```js\n"main": "index.cjs", "module": "index.mjs", "browser": "index.browser.js"\n```',tech:"node",displayNumber:49,sortKey:1591},{id:"node-1592",number:50,title:"How do you make a package work in both Node and the browser?",question:"How do you make a package work in both Node and the browser?",difficulty:"Experienced",category:"Modules & npm",answer:'Avoid Node-only APIs or provide conditional entries via the `exports`/`browser` fields, and use isomorphic libraries. Bundlers pick the right build per target.\n\n```js\n"exports": { "node": "./node.js", "browser": "./browser.js", "default": "./index.js" }\n```',tech:"node",displayNumber:50,sortKey:1592},{id:"node-1593",number:51,title:"What is `import.meta.url` and why is it needed in ESM?",question:"What is `import.meta.url` and why is it needed in ESM?",difficulty:"Advanced",category:"Modules & npm",answer:"ESM has no `__dirname`/`__filename`. `import.meta.url` gives the current module's URL, from which you derive paths using `fileURLToPath`.\n\n```js\nimport { fileURLToPath } from 'node:url'\nconst __dirname = path.dirname(fileURLToPath(import.meta.url))\n```",tech:"node",displayNumber:51,sortKey:1593},{id:"node-1594",number:52,title:"How do you use `__dirname` equivalent in ESM?",question:"How do you use `__dirname` equivalent in ESM?",difficulty:"Intermediate",category:"Modules & npm",answer:"Convert `import.meta.url` with `fileURLToPath`, then take `path.dirname`. Some Node versions expose `import.meta.dirname` directly.\n\n```js\nconst dir = import.meta.dirname ?? path.dirname(fileURLToPath(import.meta.url))\n```",tech:"node",displayNumber:52,sortKey:1594},{id:"node-1595",number:53,title:"Can you use `require` inside an ES Module?",question:"Can you use `require` inside an ES Module?",difficulty:"Advanced",category:"Modules & npm",answer:"Not directly, but you can create one via `createRequire(import.meta.url)`. This helps load CJS-only packages or JSON from ESM code.\n\n```js\nimport { createRequire } from 'node:module'\nconst require = createRequire(import.meta.url)\nconst cjs = require('some-cjs-pkg')\n```",tech:"node",displayNumber:53,sortKey:1595},{id:"node-1596",number:54,title:"Can you import an ESM package from CommonJS?",question:"Can you import an ESM package from CommonJS?",difficulty:"Advanced",category:"Modules & npm",answer:"Not with synchronous `require` (ESM is async). Use dynamic `import()`, which returns a Promise, inside your CJS code.\n\n```js\n;(async () => { const mod = await import('esm-only-pkg') })()\n```",tech:"node",displayNumber:54,sortKey:1596},{id:"node-1597",number:55,title:"What is top-level await and where can you use it?",question:"What is top-level await and where can you use it?",difficulty:"Advanced",category:"Modules & npm",answer:"Top-level `await` lets you `await` at module scope without an async function — only in ES Modules. It is useful for async initialization but delays the module's readiness for importers.\n\n```js\n// config.mjs\nexport const config = await loadConfig()\n```",tech:"node",displayNumber:55,sortKey:1597},{id:"node-1598",number:56,title:"What is a module's `exports` caching behavior with mutation?",question:"What is a module's `exports` caching behavior with mutation?",difficulty:"Experienced",category:"Modules & npm",answer:"Because modules are cached singletons, mutating an exported object is visible to all importers. This can be used for shared state but risks hidden coupling and hard-to-trace bugs.\n\n```js\n// counter.js\nmodule.exports = { n: 0 }\n// any importer that does counter.n++ affects all\n```",tech:"node",displayNumber:56,sortKey:1598},{id:"node-1599",number:57,title:"What is the difference between deep and shallow module imports?",question:"What is the difference between deep and shallow module imports?",difficulty:"Advanced",category:"Modules & npm",answer:"A shallow import pulls the package entry; a deep import targets an internal file path. The `exports` map can forbid deep imports to keep internals private and refactorable.\n\n```js\nimport { debounce } from 'lodash-es'          // shallow\nimport debounce from 'lodash-es/debounce.js'  // deep (may be blocked)\n```",tech:"node",displayNumber:57,sortKey:1599},{id:"node-1600",number:58,title:"How do you handle native (C++) addons?",question:"How do you handle native (C++) addons?",difficulty:"Experienced",category:"Modules & npm",answer:"Native addons are compiled binaries (`.node`) built with node-gyp/N-API. They install via prebuilt binaries or compile on `install`, requiring build tools. They provide performance or OS-level features JS can't.\n\n```js\nconst addon = require('bcrypt') // uses a native binding under the hood\n```",tech:"node",displayNumber:58,sortKey:1600},{id:"node-1601",number:59,title:"What is N-API / Node-API?",question:"What is N-API / Node-API?",difficulty:"Experienced",category:"Modules & npm",answer:"Node-API is a stable, ABI-versioned C interface for building native addons that remain binary-compatible across Node versions, so addons don't need recompiling for each release.\n\n```js\n// addons built with Node-API load without rebuild across Node majors\n```",tech:"node",displayNumber:59,sortKey:1601},{id:"node-1602",number:60,title:"How do you mock modules in tests?",question:"How do you mock modules in tests?",difficulty:"Advanced",category:"Modules & npm",answer:"Use a test runner's mock API (`jest.mock`) or Node's built-in `mock` from `node:test`, or dependency injection. Mocks replace a module's exports so you can isolate the unit under test.\n\n```js\njest.mock('./db', () => ({ query: jest.fn() }))\n```",tech:"node",displayNumber:60,sortKey:1602},{id:"node-1603",number:61,title:"What is dependency injection and why prefer it over hard requires?",question:"What is dependency injection and why prefer it over hard requires?",difficulty:"Experienced",category:"Modules & npm",answer:"Dependency injection passes collaborators in (via constructor/args) rather than importing them internally. It decouples modules, makes testing easy (inject mocks), and avoids tangled module graphs.\n\n```js\nfunction createService(db) { return { get: id => db.find(id) } }\n```",tech:"node",displayNumber:61,sortKey:1603},{id:"node-1604",number:62,title:"How do you version and publish a breaking change responsibly?",question:"How do you version and publish a breaking change responsibly?",difficulty:"Experienced",category:"Modules & npm",answer:"Bump the MAJOR version, document the migration in a changelog, optionally publish under a `next` dist-tag first, and deprecate the old major with `npm deprecate` to warn users.\n\n```js\nnpm version major\nnpm publish --tag next\n```",tech:"node",displayNumber:62,sortKey:1604},{id:"node-1605",number:63,title:"What are npm dist-tags?",question:"What are npm dist-tags?",difficulty:"Advanced",category:"Modules & npm",answer:"Dist-tags are named pointers to versions (`latest`, `next`, `beta`). `npm install pkg` uses `latest`; users opt into others explicitly. They enable staged releases.\n\n```js\nnpm dist-tag add pkg@2.0.0-rc.1 next\nnpm install pkg@next\n```",tech:"node",displayNumber:63,sortKey:1605},{id:"node-1606",number:64,title:"How do you deprecate a package or version?",question:"How do you deprecate a package or version?",difficulty:"Advanced",category:"Modules & npm",answer:'`npm deprecate <pkg>[@range] "<message>"` prints a warning on install, guiding users to alternatives without unpublishing.\n\n```js\nnpm deprecate my-lib@"<2.0.0" "Upgrade to v2, v1 is unmaintained"\n```',tech:"node",displayNumber:64,sortKey:1606},{id:"node-1607",number:65,title:"Why is unpublishing packages restricted?",question:"Why is unpublishing packages restricted?",difficulty:"Experienced",category:"Modules & npm",answer:'After the left-pad incident, npm restricted unpublish (only within 72 hours and if nothing depends on it) to prevent breaking the ecosystem. Prefer deprecation over unpublishing.\n\n```js\nnpm deprecate my-lib "Do not use" // instead of unpublish\n```',tech:"node",displayNumber:65,sortKey:1607},{id:"node-1608",number:66,title:"How do you audit and reduce your dependency footprint?",question:"How do you audit and reduce your dependency footprint?",difficulty:"Experienced",category:"Modules & npm",answer:"Review with `npm ls`, remove unused deps (depcheck), prefer small focused libraries or the standard library, and watch install size (`npm install --dry-run`, bundlephobia). Fewer deps mean less risk and faster installs.\n\n```js\nnpx depcheck   // finds unused/missing dependencies\n```",tech:"node",displayNumber:66,sortKey:1608},{id:"node-1609",number:67,title:"What is the difference between `import x from 'y'` and `import * as x from 'y'`?",question:"What is the difference between `import x from 'y'` and `import * as x from 'y'`?",difficulty:"Intermediate",category:"Modules & npm",answer:"The first imports the default export as `x`. The second imports the entire module namespace object (all named exports) as `x`. Namespace objects are read-only.\n\n```js\nimport express from 'express'      // default\nimport * as fs from 'node:fs'      // namespace\n```",tech:"node",displayNumber:67,sortKey:1609},{id:"node-1610",number:68,title:"How do you dynamically build an import path safely?",question:"How do you dynamically build an import path safely?",difficulty:"Advanced",category:"Modules & npm",answer:"Validate/whitelist the segment before interpolating to prevent loading arbitrary files, then use dynamic `import()`. Never feed unsanitized user input into a module path.\n\n```js\nconst allowed = { pg: './pg.js', mysql: './mysql.js' }\nconst driver = await import(allowed[name])\n```",tech:"node",displayNumber:68,sortKey:1610},{id:"node-1611",number:69,title:"What is a polyfill vs a shim?",question:"What is a polyfill vs a shim?",difficulty:"Advanced",category:"Modules & npm",answer:"A polyfill implements a missing standard API using existing features (e.g. `fetch` in old Node). A shim intercepts/normalizes an existing API's behavior. Both smooth over environment differences.\n\n```js\nglobalThis.fetch ??= require('node-fetch') // polyfill for old Node\n```",tech:"node",displayNumber:69,sortKey:1611},{id:"node-1612",number:70,title:"How do you structure a large Node project's modules?",question:"How do you structure a large Node project's modules?",difficulty:"Experienced",category:"Modules & npm",answer:"Group by feature/domain (not by technical type), keep modules small and single-purpose, expose clear public interfaces via index files, avoid circular deps, and separate pure logic from I/O for testability.\n\n```js\n// src/users/{controller,service,repository,index}.js\nexport { userRoutes } from './controller.js'\n```",tech:"node",displayNumber:70,sortKey:1612},{id:"node-1613",number:1,title:"What is the event loop in Node.js?",question:"What is the event loop in Node.js?",difficulty:"Basic",category:"Event Loop & Async",answer:"The event loop is the mechanism that lets Node perform non-blocking I/O on a single thread. It continuously checks queues for completed operations and runs their callbacks, phase by phase, until there is no more work.\n\n```js\nsetTimeout(() => console.log('later'), 0)\nconsole.log('now') // 'now' then 'later'\n```",tech:"node",displayNumber:1,sortKey:1613},{id:"node-1614",number:2,title:"What are the main phases of the event loop?",question:"What are the main phases of the event loop?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Timers (setTimeout/setInterval), pending callbacks, idle/prepare (internal), poll (I/O callbacks), check (setImmediate), and close callbacks. The loop cycles through these in order.\n\n```js\n// poll -> check -> close, then back to timers on the next tick\n```",tech:"node",displayNumber:2,sortKey:1614},{id:"node-1615",number:3,title:"What is the poll phase?",question:"What is the poll phase?",difficulty:"Advanced",category:"Event Loop & Async",answer:"The poll phase retrieves new I/O events and executes their callbacks. If no timers are pending, it may block here waiting for I/O; if `setImmediate` callbacks are queued, it ends and moves to the check phase.\n\n```js\nfs.readFile('a', () => console.log('poll phase callback'))\n```",tech:"node",displayNumber:3,sortKey:1615},{id:"node-1616",number:4,title:"What is the check phase?",question:"What is the check phase?",difficulty:"Advanced",category:"Event Loop & Async",answer:"The check phase runs `setImmediate` callbacks immediately after the poll phase completes. It is designed to run code right after I/O callbacks in the same loop iteration.\n\n```js\nsetImmediate(() => console.log('check phase'))\n```",tech:"node",displayNumber:4,sortKey:1616},{id:"node-1617",number:5,title:"What is the difference between microtasks and macrotasks?",question:"What is the difference between microtasks and macrotasks?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Macrotasks are event-loop phase callbacks (timers, I/O, setImmediate). Microtasks are `process.nextTick` and resolved Promise callbacks. Microtasks run after each macrotask/operation and are fully drained before the loop proceeds — so they can starve I/O.\n\n```js\nsetTimeout(() => console.log('macro'))\nPromise.resolve().then(() => console.log('micro')) // micro first\n```",tech:"node",displayNumber:5,sortKey:1617},{id:"node-1618",number:6,title:"What is the order between `process.nextTick` and Promises?",question:"What is the order between `process.nextTick` and Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Both are microtasks, but the `nextTick` queue is drained before the Promise microtask queue after each operation. So `nextTick` callbacks run before `.then` callbacks.\n\n```js\nPromise.resolve().then(() => console.log('promise'))\nprocess.nextTick(() => console.log('nextTick')) // nextTick prints first\n```",tech:"node",displayNumber:6,sortKey:1618},{id:"node-1619",number:7,title:"Why can `process.nextTick` starve the event loop?",question:"Why can `process.nextTick` starve the event loop?",difficulty:"Experienced",category:"Event Loop & Async",answer:"The entire nextTick queue is drained before the loop advances to the next phase. If a nextTick callback keeps scheduling more nextTicks, I/O and timers never run — the loop is starved.\n\n```js\nfunction loop() { process.nextTick(loop) } // blocks all I/O forever\n```",tech:"node",displayNumber:7,sortKey:1619},{id:"node-1620",number:8,title:"What is a Promise?",question:"What is a Promise?",difficulty:"Basic",category:"Event Loop & Async",answer:"A Promise represents the eventual result of an async operation, with states pending, fulfilled, or rejected. You consume it with `.then`/`.catch`/`.finally` or `await`.\n\n```js\nconst p = new Promise((resolve, reject) => resolve(42))\np.then(v => console.log(v)) // 42\n```",tech:"node",displayNumber:8,sortKey:1620},{id:"node-1621",number:9,title:"What are the three states of a Promise?",question:"What are the three states of a Promise?",difficulty:"Basic",category:"Event Loop & Async",answer:"Pending (initial), fulfilled (resolved with a value), and rejected (failed with a reason). Once settled (fulfilled/rejected), a Promise is immutable.\n\n```js\nPromise.reject(new Error('x')).catch(e => console.log(e.message))\n```",tech:"node",displayNumber:9,sortKey:1621},{id:"node-1622",number:10,title:"What is `async/await`?",question:"What is `async/await`?",difficulty:"Basic",category:"Event Loop & Async",answer:"`async` functions return Promises; `await` pauses execution until a Promise settles, letting you write asynchronous code that reads synchronously. Errors are caught with try/catch.\n\n```js\nasync function main() {\n  const data = await fetch('/api').then(r => r.json())\n  return data\n}\n```",tech:"node",displayNumber:10,sortKey:1622},{id:"node-1623",number:11,title:"Does `await` block the event loop?",question:"Does `await` block the event loop?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"No. `await` suspends only the current async function and returns control to the event loop, which continues handling other work. It is cooperative, not blocking.\n\n```js\nasync function h() { await db.query() /* loop free meanwhile */ }\n```",tech:"node",displayNumber:11,sortKey:1623},{id:"node-1624",number:12,title:"How do you run async operations in parallel?",question:"How do you run async operations in parallel?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Start the operations first (don't await sequentially), then await them together with `Promise.all`. Awaiting one at a time serializes them unnecessarily.\n\n```js\nconst [a, b] = await Promise.all([fetchA(), fetchB()])\n```",tech:"node",displayNumber:12,sortKey:1624},{id:"node-1625",number:13,title:"What is the difference between `Promise.all` and `Promise.allSettled`?",question:"What is the difference between `Promise.all` and `Promise.allSettled`?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`Promise.all` rejects as soon as any promise rejects (fail-fast). `Promise.allSettled` waits for all and returns each result's status/value/reason, never rejecting — ideal when partial failures are acceptable.\n\n```js\nconst results = await Promise.allSettled([a(), b()])\n// [{status:'fulfilled',value}, {status:'rejected',reason}]\n```",tech:"node",displayNumber:13,sortKey:1625},{id:"node-1626",number:14,title:"What does `Promise.race` do?",question:"What does `Promise.race` do?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`Promise.race` settles as soon as the first input promise settles (fulfilled or rejected), adopting its outcome. Common for timeouts.\n\n```js\nawait Promise.race([fetchData(), timeout(5000)])\n```",tech:"node",displayNumber:14,sortKey:1626},{id:"node-1627",number:15,title:"What does `Promise.any` do?",question:"What does `Promise.any` do?",difficulty:"Advanced",category:"Event Loop & Async",answer:'`Promise.any` fulfills with the first fulfilled promise, ignoring rejections. It rejects only if all reject, throwing an `AggregateError`. Useful for "first success wins".\n\n```js\nconst fastest = await Promise.any([mirror1(), mirror2()])\n```',tech:"node",displayNumber:15,sortKey:1627},{id:"node-1628",number:16,title:"How do you implement a timeout for a Promise?",question:"How do you implement a timeout for a Promise?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Race the operation against a promise that rejects after a delay, or use `AbortController` with `AbortSignal.timeout`. The latter can actually cancel the underlying operation.\n\n```js\nconst res = await fetch(url, { signal: AbortSignal.timeout(5000) })\n```",tech:"node",displayNumber:16,sortKey:1628},{id:"node-1629",number:17,title:"What is an AbortController?",question:"What is an AbortController?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`AbortController` provides an `AbortSignal` to cancel async operations (fetch, streams, timers). Calling `abort()` triggers the signal, and supporting APIs reject with an `AbortError`.\n\n```js\nconst ac = new AbortController()\nsetTimeout(() => ac.abort(), 100)\nawait fetch(url, { signal: ac.signal })\n```",tech:"node",displayNumber:17,sortKey:1629},{id:"node-1630",number:18,title:"What is the difference between `setTimeout` and `setInterval`?",question:"What is the difference between `setTimeout` and `setInterval`?",difficulty:"Basic",category:"Event Loop & Async",answer:"`setTimeout` runs a callback once after a delay. `setInterval` runs it repeatedly at a fixed interval until cleared. Long-running work in an interval can overlap; a self-rescheduling timeout avoids that.\n\n```js\nconst id = setInterval(tick, 1000)\nclearInterval(id)\n```",tech:"node",displayNumber:18,sortKey:1630},{id:"node-1631",number:19,title:"Why might `setTimeout(fn, 1000)` fire later than 1000ms?",question:"Why might `setTimeout(fn, 1000)` fire later than 1000ms?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Timers specify a minimum delay, not exact timing. If the event loop is busy (blocking code, backed-up phases), the callback runs only when the loop reaches the timers phase after the delay elapses.\n\n```js\nsetTimeout(() => console.log('maybe >1000ms'), 1000)\nblockCpuFor2Seconds()\n```",tech:"node",displayNumber:19,sortKey:1631},{id:"node-1632",number:20,title:"What is the difference between `setImmediate` and `setTimeout(fn, 0)` again inside I/O?",question:"What is the difference between `setImmediate` and `setTimeout(fn, 0)` again inside I/O?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Within an I/O callback (poll phase), `setImmediate` always runs before `setTimeout(fn,0)` because the check phase follows poll in the same iteration, whereas the timer must wait for the next timers phase.\n\n```js\nfs.readFile('f', () => {\n  setImmediate(() => console.log('1'))\n  setTimeout(() => console.log('2'), 0)\n})\n```",tech:"node",displayNumber:20,sortKey:1632},{id:"node-1633",number:21,title:"What happens if you don't await a Promise?",question:"What happens if you don't await a Promise?",difficulty:"Intermediate",category:"Event Loop & Async",answer:'The code continues without waiting; the Promise runs in the background ("fire and forget"). If it rejects and nothing handles it, you get an unhandled rejection (which can crash the process in modern Node).\n\n```js\nsaveLog() // not awaited: errors become unhandled rejections\n```',tech:"node",displayNumber:21,sortKey:1633},{id:"node-1634",number:22,title:"How do you convert a callback-based function to a Promise?",question:"How do you convert a callback-based function to a Promise?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Wrap it in `new Promise`, resolving/rejecting in the callback, or use `util.promisify` for error-first callbacks.\n\n```js\nconst wait = ms => new Promise(res => setTimeout(res, ms))\nawait wait(500)\n```",tech:"node",displayNumber:22,sortKey:1634},{id:"node-1635",number:23,title:"What is the difference between concurrency and parallelism in Node?",question:"What is the difference between concurrency and parallelism in Node?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Concurrency is managing many tasks by interleaving them on one thread (Node's default for I/O). Parallelism is executing tasks simultaneously on multiple cores (worker threads/cluster). Node gives concurrency for free, parallelism only with extra threads/processes.\n\n```js\n// concurrency: many awaits interleaved on one thread\nawait Promise.all(urls.map(fetch))\n```",tech:"node",displayNumber:23,sortKey:1635},{id:"node-1636",number:24,title:"How do you limit concurrency of async tasks?",question:"How do you limit concurrency of async tasks?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Process items in batches or use a concurrency-limited pool (e.g. `p-limit`) so you don't launch thousands of simultaneous operations that exhaust memory, sockets, or DB connections.\n\n```js\nconst limit = pLimit(5)\nawait Promise.all(items.map(i => limit(() => process(i))))\n```",tech:"node",displayNumber:24,sortKey:1636},{id:"node-1637",number:25,title:"What is a race condition in async code and how do you prevent it?",question:"What is a race condition in async code and how do you prevent it?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A race condition occurs when overlapping async operations interleave in an unexpected order, corrupting shared state. Prevent it with locks/mutexes, atomic DB operations, queues, or by serializing critical sections.\n\n```js\n// atomic increment avoids read-modify-write race\nawait db.collection('c').updateOne({_id}, { $inc: { count: 1 } })\n```",tech:"node",displayNumber:25,sortKey:1637},{id:"node-1638",number:26,title:"What does `Promise.resolve()` do?",question:"What does `Promise.resolve()` do?",difficulty:"Basic",category:"Event Loop & Async",answer:"It returns a Promise already fulfilled with the given value (or the same promise if you pass one). Useful for normalizing sync/async values and starting a microtask chain.\n\n```js\nPromise.resolve(5).then(console.log) // 5\n```",tech:"node",displayNumber:26,sortKey:1638},{id:"node-1639",number:27,title:"What is promise chaining?",question:"What is promise chaining?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Each `.then` returns a new Promise, so you can chain sequential steps. Returning a value passes it on; returning a Promise waits for it; throwing routes to the next `.catch`.\n\n```js\nfetch(url).then(r => r.json()).then(d => d.id).catch(console.error)\n```",tech:"node",displayNumber:27,sortKey:1639},{id:"node-1640",number:28,title:"What is the difference between returning and awaiting inside a promise chain?",question:"What is the difference between returning and awaiting inside a promise chain?",difficulty:"Advanced",category:"Event Loop & Async",answer:'In `.then`, returning a Promise makes the chain wait for it; not returning it "floats" the inner promise and its errors escape the chain. Always return inner promises.\n\n```js\n.then(() => save())   // waits & propagates errors\n.then(() => { save() }) // floats: errors lost\n```',tech:"node",displayNumber:28,sortKey:1640},{id:"node-1641",number:29,title:"How does error handling work with async/await?",question:"How does error handling work with async/await?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Wrap awaited calls in try/catch; a rejected awaited Promise throws. You can also `.catch` on the async function's returned Promise. Uncaught rejections propagate up.\n\n```js\ntry { await risky() } catch (e) { handle(e) }\n```",tech:"node",displayNumber:29,sortKey:1641},{id:"node-1642",number:30,title:"What is `finally` used for in Promises?",question:"What is `finally` used for in Promises?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`.finally(fn)` (or try/finally with await) runs cleanup regardless of success/failure, without altering the result — good for releasing resources like connections or spinners.\n\n```js\ntry { await work() } finally { spinner.stop() }\n```",tech:"node",displayNumber:30,sortKey:1642},{id:"node-1643",number:31,title:"What is the difference between sequential and concurrent awaits?",question:"What is the difference between sequential and concurrent awaits?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"Sequential awaits run one after another (total = sum of times). Concurrent starts them together and awaits with `Promise.all` (total ≈ max time). Use concurrency when tasks are independent.\n\n```js\n// slow: a then b\nconst a = await getA(); const b = await getB()\n// fast: both at once\nconst [x, y] = await Promise.all([getA(), getB()])\n```",tech:"node",displayNumber:31,sortKey:1643},{id:"node-1644",number:32,title:"How do you handle errors in `Promise.all`?",question:"How do you handle errors in `Promise.all`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Promise.all` rejects on the first failure, discarding other results. Wrap each promise to catch individually, or use `Promise.allSettled` to inspect all outcomes.\n\n```js\nconst safe = ps.map(p => p.catch(e => ({ error: e })))\nconst results = await Promise.all(safe)\n```",tech:"node",displayNumber:32,sortKey:1644},{id:"node-1645",number:33,title:"What is an async iterator?",question:"What is an async iterator?",difficulty:"Advanced",category:"Event Loop & Async",answer:"An async iterator produces values over time via `Symbol.asyncIterator`, consumed with `for await...of`. It underpins async generators and streams, letting you loop over asynchronously arriving data.\n\n```js\nfor await (const chunk of readableStream) process(chunk)\n```",tech:"node",displayNumber:33,sortKey:1645},{id:"node-1646",number:34,title:"What is an async generator?",question:"What is an async generator?",difficulty:"Advanced",category:"Event Loop & Async",answer:"An `async function*` yields values asynchronously, awaiting between yields. It is ideal for streaming/paginated data with backpressure-friendly consumption via `for await`.\n\n```js\nasync function* pages(url) {\n  let next = url\n  while (next) { const p = await fetchPage(next); yield p.items; next = p.next }\n}\n```",tech:"node",displayNumber:34,sortKey:1646},{id:"node-1647",number:35,title:"How does `for await...of` work?",question:"How does `for await...of` work?",difficulty:"Advanced",category:"Event Loop & Async",answer:"It iterates an async iterable, awaiting each `next()` result before the loop body runs. It reads streams/async generators sequentially with automatic backpressure.\n\n```js\nfor await (const line of readLines(file)) console.log(line)\n```",tech:"node",displayNumber:35,sortKey:1647},{id:"node-1648",number:36,title:"What is the difference between `map` with async and `for await`?",question:"What is the difference between `map` with async and `for await`?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`array.map(async ...)` starts all operations concurrently and returns Promises (await with `Promise.all`). `for await` processes items sequentially. Choose based on whether you need concurrency or ordering/backpressure.\n\n```js\nawait Promise.all(items.map(async i => process(i))) // concurrent\nfor (const i of items) await process(i)              // sequential\n```",tech:"node",displayNumber:36,sortKey:1648},{id:"node-1649",number:37,title:"Why should you avoid `await` inside a `forEach`?",question:"Why should you avoid `await` inside a `forEach`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Array.forEach` ignores the returned Promise, so it does not wait — the loop finishes before async work completes, and errors are lost. Use `for...of` with await or `Promise.all` with `map`.\n\n```js\nitems.forEach(async i => await save(i)) // does NOT wait\nfor (const i of items) await save(i)     // correct\n```",tech:"node",displayNumber:37,sortKey:1649},{id:"node-1650",number:38,title:"What is a deferred/lazy Promise pattern?",question:"What is a deferred/lazy Promise pattern?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A deferred exposes `resolve`/`reject` outside the executor so external code can settle the Promise later — useful for bridging event-based APIs. `Promise.withResolvers()` standardizes this.\n\n```js\nconst { promise, resolve } = Promise.withResolvers()\nemitter.once('ready', resolve)\nawait promise\n```",tech:"node",displayNumber:38,sortKey:1650},{id:"node-1651",number:39,title:"What happens to a thrown error in an async function?",question:"What happens to a thrown error in an async function?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"It becomes a rejected Promise returned by that async function. Callers must catch it via try/catch on await or `.catch`; otherwise it surfaces as an unhandled rejection.\n\n```js\nasync function f() { throw new Error('x') }\nf().catch(e => console.log(e.message)) // 'x'\n```",tech:"node",displayNumber:39,sortKey:1651},{id:"node-1652",number:40,title:"What is the microtask queue drain guarantee?",question:"What is the microtask queue drain guarantee?",difficulty:"Experienced",category:"Event Loop & Async",answer:"After each macrotask (and after synchronous script completes), the engine drains the entire microtask queue before rendering/next macrotask. New microtasks scheduled during draining are also run in the same drain.\n\n```js\nPromise.resolve().then(() => Promise.resolve().then(() => console.log('still same drain')))\n```",tech:"node",displayNumber:40,sortKey:1652},{id:"node-1653",number:41,title:"How do you make a synchronous-looking retry with backoff?",question:"How do you make a synchronous-looking retry with backoff?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Loop with await, catching failures, and sleep with exponential backoff (plus jitter) between attempts up to a max. This smooths transient failures without hammering a service.\n\n```js\nfor (let i = 0; i < 5; i++) {\n  try { return await call() } catch { await sleep(2 ** i * 100) }\n}\n```",tech:"node",displayNumber:41,sortKey:1653},{id:"node-1654",number:42,title:"What is the thundering herd problem and how does jitter help?",question:"What is the thundering herd problem and how does jitter help?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Thundering herd is many clients retrying at the same instant after a failure, overwhelming the recovering service. Adding random jitter to backoff spreads retries over time, smoothing the load.\n\n```js\nconst delay = base * 2 ** attempt + Math.random() * 100 // jitter\n```",tech:"node",displayNumber:42,sortKey:1654},{id:"node-1655",number:43,title:"How do timers behave with `unref()`?",question:"How do timers behave with `unref()`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Calling `.unref()` on a timer lets the process exit even if the timer is still pending — it no longer keeps the event loop alive. `ref()` restores the keep-alive behavior.\n\n```js\nconst t = setInterval(poll, 1000)\nt.unref() // won't prevent process exit\n```",tech:"node",displayNumber:43,sortKey:1655},{id:"node-1656",number:44,title:"What keeps the Node process alive?",question:"What keeps the Node process alive?",difficulty:"Advanced",category:"Event Loop & Async",answer:'Pending "refs": active timers, open sockets/servers, pending I/O, and open handles keep the event loop running. When none remain, the loop empties and the process exits.\n\n```js\nsetTimeout(() => {}, 100000) // keeps process alive for 100s\n```',tech:"node",displayNumber:44,sortKey:1656},{id:"node-1657",number:45,title:"What is `queueMicrotask`?",question:"What is `queueMicrotask`?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`queueMicrotask(fn)` schedules a microtask directly, running after the current task before the next macrotask — like `Promise.resolve().then` but clearer and without creating a Promise.\n\n```js\nqueueMicrotask(() => console.log('runs before timers'))\n```",tech:"node",displayNumber:45,sortKey:1657},{id:"node-1658",number:46,title:"How do you debounce an async function?",question:"How do you debounce an async function?",difficulty:"Advanced",category:"Event Loop & Async",answer:`Delay execution until calls stop for a quiet period by clearing and resetting a timer each call. For async, also cancel or ignore stale in-flight results.

\`\`\`js
function debounce(fn, ms) {
  let t
  return (...a) => { clearTimeout(t); t = setTimeout(() => fn(...a), ms) }
}
\`\`\``,tech:"node",displayNumber:46,sortKey:1658},{id:"node-1659",number:47,title:"How do you throttle async calls?",question:"How do you throttle async calls?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Throttling limits execution to at most once per interval regardless of call frequency, using a timestamp or a leading/trailing timer. Good for rate-limiting expensive operations.\n\n```js\nlet last = 0\nconst throttled = fn => (...a) => { const n = Date.now(); if (n - last > 1000) { last = n; fn(...a) } }\n```",tech:"node",displayNumber:47,sortKey:1659},{id:"node-1660",number:48,title:"What is memoization of async functions?",question:"What is memoization of async functions?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Cache the returned Promise (not just the value) keyed by args, so concurrent identical calls share one in-flight request (request coalescing). Evict on error or TTL.\n\n```js\nconst cache = new Map()\nconst get = k => cache.get(k) ?? cache.set(k, fetchData(k)).get(k)\n```",tech:"node",displayNumber:48,sortKey:1660},{id:"node-1661",number:49,title:"What is the difference between eager and lazy evaluation of Promises?",question:"What is the difference between eager and lazy evaluation of Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Promises are eager: the executor runs immediately when created, even before `await`. To defer work, wrap it in a function (thunk) and call it when needed.\n\n```js\nconst eager = fetch(url)        // request already sent\nconst lazy = () => fetch(url)   // sent only when called\n```",tech:"node",displayNumber:49,sortKey:1661},{id:"node-1662",number:50,title:"How do you detect and fix event loop lag?",question:"How do you detect and fix event loop lag?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Measure lag with `perf_hooks.monitorEventLoopDelay` or a timer-drift check. High lag means blocking (CPU-bound sync code); fix by offloading to worker threads, chunking work, or streaming.\n\n```js\nconst h = require('perf_hooks').monitorEventLoopDelay(); h.enable()\nsetInterval(() => console.log(h.mean / 1e6, 'ms lag'), 1000)\n```",tech:"node",displayNumber:50,sortKey:1662},{id:"node-1663",number:51,title:"What is backpressure in async data flow?",question:"What is backpressure in async data flow?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Backpressure is signaling a fast producer to slow down when a slow consumer can't keep up, preventing unbounded memory growth. Streams and async iterators implement it; `for await` respects it naturally.\n\n```js\nfor await (const chunk of source) await slowConsumer(chunk) // paced\n```",tech:"node",displayNumber:51,sortKey:1663},{id:"node-1664",number:52,title:"Why is mixing callbacks and Promises for the same operation dangerous?",question:"Why is mixing callbacks and Promises for the same operation dangerous?",difficulty:"Advanced",category:"Event Loop & Async",answer:"A function should not both call a callback and return a Promise, or it may resolve twice / double-run logic. Choose one contract to avoid subtle double-execution bugs.\n\n```js\n// bad: resolves via callback AND promise\nfunction f(cb) { return new Promise(res => { cb(); res() }) }\n```",tech:"node",displayNumber:52,sortKey:1664},{id:"node-1665",number:53,title:"What is a semaphore and when do you use it in Node?",question:"What is a semaphore and when do you use it in Node?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A semaphore limits how many async operations run concurrently by handing out a fixed number of permits. Use it to cap DB connections, file handles, or outbound requests.\n\n```js\nconst sem = new Semaphore(10)\nawait sem.acquire(); try { await doWork() } finally { sem.release() }\n```",tech:"node",displayNumber:53,sortKey:1665},{id:"node-1666",number:54,title:"How do you cancel an in-flight async operation?",question:"How do you cancel an in-flight async operation?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Pass an `AbortSignal` to APIs that support it and call `controller.abort()`. For custom code, check `signal.aborted` at checkpoints and reject with an `AbortError`.\n\n```js\nif (signal.aborted) throw new DOMException('Aborted', 'AbortError')\n```",tech:"node",displayNumber:54,sortKey:1666},{id:"node-1667",number:55,title:"What is the difference between `process.nextTick` recursion and `setImmediate` recursion?",question:"What is the difference between `process.nextTick` recursion and `setImmediate` recursion?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Recursive `nextTick` blocks the loop (microtasks never yield to I/O). Recursive `setImmediate` yields between iterations, letting I/O and timers run — safer for chunking long work.\n\n```js\nfunction chunk() { doPart(); if (more) setImmediate(chunk) } // non-blocking\n```",tech:"node",displayNumber:55,sortKey:1667},{id:"node-1668",number:56,title:"How do you break a long CPU task without blocking the loop?",question:"How do you break a long CPU task without blocking the loop?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Split the work into slices and yield to the loop between slices via `setImmediate`/`await new Promise(setImmediate)`, or move it entirely to a worker thread for true parallelism.\n\n```js\nfor (let i = 0; i < n; i += 1000) { processSlice(i); await new Promise(setImmediate) }\n```",tech:"node",displayNumber:56,sortKey:1668},{id:"node-1669",number:57,title:`What is a Promise's "resolution" vs "fulfillment"?`,question:`What is a Promise's "resolution" vs "fulfillment"?`,difficulty:"Experienced",category:"Event Loop & Async",answer:"Resolving a Promise locks in its fate — which may be fulfillment with a value or, if resolved with another thenable, following that thenable (possibly ending in rejection). Fulfillment specifically means settled with a value.\n\n```js\nnew Promise(res => res(Promise.reject('x'))).catch(console.log) // 'x'\n```",tech:"node",displayNumber:57,sortKey:1669},{id:"node-1670",number:58,title:"What happens if you `await` a non-Promise value?",question:"What happens if you `await` a non-Promise value?",difficulty:"Intermediate",category:"Event Loop & Async",answer:"`await` wraps non-thenables in `Promise.resolve`, so it returns the value on the next microtask. It still yields to the microtask queue even for plain values.\n\n```js\nconst x = await 5 // x === 5, but resolves asynchronously\n```",tech:"node",displayNumber:58,sortKey:1670},{id:"node-1671",number:59,title:"How do you implement a simple async queue?",question:"How do you implement a simple async queue?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Keep a promise chain; each enqueued task appends `.then(task)` so tasks run strictly one after another, serializing access to a shared resource.\n\n```js\nlet tail = Promise.resolve()\nconst enqueue = task => (tail = tail.then(task))\n```",tech:"node",displayNumber:59,sortKey:1671},{id:"node-1672",number:60,title:"What is the difference between `Promise.all` fail-fast and gathering all errors?",question:"What is the difference between `Promise.all` fail-fast and gathering all errors?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Fail-fast (`Promise.all`) stops at the first rejection — good when any failure invalidates the batch. Gathering (`allSettled`) collects every outcome — good for reporting which items succeeded/failed.\n\n```js\nconst outcomes = await Promise.allSettled(jobs)\nconst failed = outcomes.filter(o => o.status === 'rejected')\n```",tech:"node",displayNumber:60,sortKey:1672},{id:"node-1673",number:61,title:"How does async context propagate across awaits?",question:"How does async context propagate across awaits?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Local variables persist across `await` because the function's state is preserved. For cross-call context (like request IDs), use `AsyncLocalStorage`, which maintains context through async chains.\n\n```js\nals.run({ reqId }, async () => { await handler() /* als.getStore().reqId available */ })\n```",tech:"node",displayNumber:61,sortKey:1673},{id:"node-1674",number:62,title:"What is `AsyncLocalStorage`?",question:"What is `AsyncLocalStorage`?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`AsyncLocalStorage` (from `async_hooks`) stores data that stays associated with an async execution chain, like thread-local storage. It is commonly used for per-request logging context without passing it everywhere.\n\n```js\nconst als = new AsyncLocalStorage()\nals.run(store, () => { /* store available in all awaited callees */ })\n```",tech:"node",displayNumber:62,sortKey:1674},{id:"node-1675",number:63,title:"What are async hooks?",question:"What are async hooks?",difficulty:"Experienced",category:"Event Loop & Async",answer:"`async_hooks` lets you track the lifecycle of async resources (init, before, after, destroy). It powers context propagation and diagnostics but has performance overhead, so use sparingly in production.\n\n```js\nconst hook = require('async_hooks').createHook({ init() {} })\nhook.enable()\n```",tech:"node",displayNumber:63,sortKey:1675},{id:"node-1676",number:64,title:"Why is `Promise.all` order preserved even if tasks finish out of order?",question:"Why is `Promise.all` order preserved even if tasks finish out of order?",difficulty:"Advanced",category:"Event Loop & Async",answer:"`Promise.all` returns results in input order regardless of completion order, because it stores each result at its index. Only timing differs, not the result array's ordering.\n\n```js\nawait Promise.all([slow(), fast()]) // [slowResult, fastResult]\n```",tech:"node",displayNumber:64,sortKey:1676},{id:"node-1677",number:65,title:"What is a leaked Promise and how does it happen?",question:"What is a leaked Promise and how does it happen?",difficulty:"Experienced",category:"Event Loop & Async",answer:"A leaked Promise is one that never settles (e.g. awaiting an event that never fires), leaving the awaiting code stuck and holding resources. Add timeouts/AbortSignals to guarantee settlement.\n\n```js\nawait new Promise(() => {}) // never resolves -> leak\n```",tech:"node",displayNumber:65,sortKey:1677},{id:"node-1678",number:66,title:"How do you test asynchronous code?",question:"How do you test asynchronous code?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Return or await the Promise in the test so the runner waits for completion, use fake timers to control delays, and assert on resolved values or rejections.\n\n```js\ntest('resolves', async () => { await expect(fetchUser(1)).resolves.toHaveProperty('id') })\n```",tech:"node",displayNumber:66,sortKey:1678},{id:"node-1679",number:67,title:"What is the difference between `setTimeout` in Node and the browser?",question:"What is the difference between `setTimeout` in Node and the browser?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Node's `setTimeout` returns a `Timeout` object (with `ref`/`unref`), not a number, and integrates with libuv's timers phase. The browser returns a numeric id and clamps nested timeouts to 4ms.\n\n```js\nconst t = setTimeout(fn, 100) // Timeout object in Node\nt.unref()\n```",tech:"node",displayNumber:67,sortKey:1679},{id:"node-1680",number:68,title:"How do you flatten nested async results cleanly?",question:"How do you flatten nested async results cleanly?",difficulty:"Advanced",category:"Event Loop & Async",answer:"Use await to avoid nested `.then`, and `Promise.all` with `flat`/`flatMap` to combine arrays of async results into one list.\n\n```js\nconst nested = await Promise.all(groups.map(g => fetchItems(g)))\nconst all = nested.flat()\n```",tech:"node",displayNumber:68,sortKey:1680},{id:"node-1681",number:69,title:"What is the cost of creating many Promises?",question:"What is the cost of creating many Promises?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Each Promise allocates memory and schedules microtasks; creating millions at once causes GC pressure and memory spikes. Batch/stream work and limit concurrency instead of materializing everything up front.\n\n```js\n// avoid: urls.map(fetch) for 1M urls; use a bounded pool\n```",tech:"node",displayNumber:69,sortKey:1681},{id:"node-1682",number:70,title:"How do you ensure ordered processing of a stream of async events?",question:"How do you ensure ordered processing of a stream of async events?",difficulty:"Experienced",category:"Event Loop & Async",answer:"Serialize with an async queue or `for await`, or tag events with sequence numbers and reorder. Concurrency without ordering guarantees can interleave results unpredictably.\n\n```js\nfor await (const event of eventStream) await handleInOrder(event)\n```",tech:"node",displayNumber:70,sortKey:1682},{id:"node-1683",number:1,title:"What is a stream in Node.js?",question:"What is a stream in Node.js?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"A stream is an abstraction for reading or writing data piece by piece (chunks) instead of all at once. It enables processing large data with constant, low memory and supports composition via piping.\n\n```js\nfs.createReadStream('big.txt').pipe(process.stdout)\n```",tech:"node",displayNumber:1,sortKey:1683},{id:"node-1684",number:2,title:"What are the four types of streams?",question:"What are the four types of streams?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"Readable (source, e.g. file read), Writable (sink, e.g. file write), Duplex (both, e.g. TCP socket), and Transform (duplex that modifies data, e.g. gzip).\n\n```js\nconst gzip = require('zlib').createGzip() // Transform stream\n```",tech:"node",displayNumber:2,sortKey:1684},{id:"node-1685",number:3,title:"What is the advantage of streams over reading a whole file?",question:"What is the advantage of streams over reading a whole file?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"Streams use constant memory regardless of file size and start producing output immediately (lower latency), whereas buffering the whole file consumes memory proportional to its size and delays processing until fully loaded.\n\n```js\nfs.createReadStream('10gb.log').pipe(res) // won't blow up memory\n```",tech:"node",displayNumber:3,sortKey:1685},{id:"node-1686",number:4,title:"What is `pipe()`?",question:"What is `pipe()`?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`pipe()` connects a readable stream to a writable one, automatically forwarding data and handling backpressure. It returns the destination so pipes can be chained.\n\n```js\nreadable.pipe(transform).pipe(writable)\n```",tech:"node",displayNumber:4,sortKey:1686},{id:"node-1687",number:5,title:"What is backpressure in streams?",question:"What is backpressure in streams?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Backpressure occurs when a writable can't consume data as fast as a readable produces it. `write()` returns `false` when its buffer is full; the readable should pause until the `drain` event. `pipe` handles this automatically.\n\n```js\nif (!dest.write(chunk)) src.pause()\ndest.on('drain', () => src.resume())\n```",tech:"node",displayNumber:5,sortKey:1687},{id:"node-1688",number:6,title:"What are flowing and paused modes of readable streams?",question:"What are flowing and paused modes of readable streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"In flowing mode data is pushed via `data` events as fast as possible; in paused mode you pull via `read()`. Adding a `data` listener or calling `pipe`/`resume` switches to flowing; `pause()` switches back.\n\n```js\nstream.on('data', chunk => {}) // flowing mode\n```",tech:"node",displayNumber:6,sortKey:1688},{id:"node-1689",number:7,title:"What is `highWaterMark`?",question:"What is `highWaterMark`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`highWaterMark` is the internal buffer size threshold (bytes for binary, count for object mode). It controls when a stream signals backpressure and how much it buffers before pausing the source.\n\n```js\nfs.createReadStream('f', { highWaterMark: 64 * 1024 }) // 64KB chunks\n```",tech:"node",displayNumber:7,sortKey:1689},{id:"node-1690",number:8,title:"What is object mode in streams?",question:"What is object mode in streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"By default streams handle strings/Buffers. In object mode (`objectMode: true`) they can push/consume arbitrary JS objects, useful for record pipelines (e.g. DB rows, parsed CSV lines).\n\n```js\nnew Readable({ objectMode: true, read() { this.push({ id: 1 }) } })\n```",tech:"node",displayNumber:8,sortKey:1690},{id:"node-1691",number:9,title:"How do you create a custom Readable stream?",question:"How do you create a custom Readable stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Extend `Readable` (or pass options to its constructor) and implement `_read()`, calling `this.push(chunk)` to emit data and `this.push(null)` to signal EOF.\n\n```js\nconst { Readable } = require('stream')\nconst r = new Readable({ read() { this.push('hi'); this.push(null) } })\n```",tech:"node",displayNumber:9,sortKey:1691},{id:"node-1692",number:10,title:"How do you create a custom Writable stream?",question:"How do you create a custom Writable stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Implement `_write(chunk, encoding, callback)`, doing the work and calling `callback()` when ready for more (or with an error). The callback drives backpressure.\n\n```js\nconst { Writable } = require('stream')\nnew Writable({ write(chunk, enc, cb) { console.log(chunk.toString()); cb() } })\n```",tech:"node",displayNumber:10,sortKey:1692},{id:"node-1693",number:11,title:"How do you create a Transform stream?",question:"How do you create a Transform stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Implement `_transform(chunk, enc, callback)` to process input and `push` output, calling `callback` when done. Optionally `_flush` for trailing data.\n\n```js\nconst { Transform } = require('stream')\nnew Transform({ transform(c, e, cb) { cb(null, c.toString().toUpperCase()) } })\n```",tech:"node",displayNumber:11,sortKey:1693},{id:"node-1694",number:12,title:"What is `stream.pipeline()` and why prefer it over `pipe`?",question:"What is `stream.pipeline()` and why prefer it over `pipe`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`pipeline` chains streams and propagates errors and cleanup automatically; plain `pipe` does not forward errors or destroy streams on failure, risking leaks. Always use `pipeline` for multi-stage pipes.\n\n```js\nconst { pipeline } = require('stream/promises')\nawait pipeline(fs.createReadStream('a'), gzip, fs.createWriteStream('a.gz'))\n```",tech:"node",displayNumber:12,sortKey:1694},{id:"node-1695",number:13,title:"Why is error handling important with `pipe`?",question:"Why is error handling important with `pipe`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`pipe` doesn't destroy the source when the destination errors (or vice versa), leaking file descriptors/memory. You must attach `error` handlers on every stream, or use `pipeline` which handles it.\n\n```js\nsrc.on('error', onErr); dest.on('error', onErr) // or use pipeline\n```",tech:"node",displayNumber:13,sortKey:1695},{id:"node-1696",number:14,title:"How do you convert a stream to a string/buffer?",question:"How do you convert a stream to a string/buffer?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Collect chunks and concatenate, or use the helper `stream/consumers` (`text`, `buffer`, `json`) in modern Node.\n\n```js\nconst { text } = require('stream/consumers')\nconst body = await text(req)\n```",tech:"node",displayNumber:14,sortKey:1696},{id:"node-1697",number:15,title:"What is the difference between `stream.Readable.from()` and manual streams?",question:"What is the difference between `stream.Readable.from()` and manual streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`Readable.from(iterable)` builds a readable stream from any (async) iterable/generator without implementing `_read`, greatly simplifying custom sources.\n\n```js\nconst r = Readable.from(async function* () { yield 'a'; yield 'b' }())\n```",tech:"node",displayNumber:15,sortKey:1697},{id:"node-1698",number:16,title:"What is a Buffer and how is it different from an array?",question:"What is a Buffer and how is it different from an array?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"A Buffer is a fixed-size raw memory region for bytes, stored outside the V8 heap. Unlike arrays it can't grow, holds only 0–255 byte values, and is optimized for binary/encoding operations.\n\n```js\nconst b = Buffer.from([104, 105])\nconsole.log(b.toString()) // 'hi'\n```",tech:"node",displayNumber:16,sortKey:1698},{id:"node-1699",number:17,title:"How do you convert between Buffers and strings with encodings?",question:"How do you convert between Buffers and strings with encodings?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`Buffer.from(str, encoding)` encodes; `buf.toString(encoding)` decodes. Supported encodings include `utf8`, `hex`, `base64`, `ascii`, `latin1`.\n\n```js\nBuffer.from('hi').toString('base64') // 'aGk='\nBuffer.from('aGk=', 'base64').toString() // 'hi'\n```",tech:"node",displayNumber:17,sortKey:1699},{id:"node-1700",number:18,title:"What is the risk of `Buffer.allocUnsafe`?",question:"What is the risk of `Buffer.allocUnsafe`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"It returns uninitialized memory that may contain old data (including secrets from prior allocations). It's faster but must be fully overwritten before use or exposure. Prefer `Buffer.alloc` when in doubt.\n\n```js\nconst b = Buffer.allocUnsafe(16); b.fill(0) // sanitize before use\n```",tech:"node",displayNumber:18,sortKey:1700},{id:"node-1701",number:19,title:"How do you handle a chunk split across multi-byte characters?",question:"How do you handle a chunk split across multi-byte characters?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"A UTF-8 character may straddle two chunks; decoding each chunk independently corrupts it. Use `string_decoder.StringDecoder`, which buffers incomplete multi-byte sequences until complete.\n\n```js\nconst dec = new (require('string_decoder').StringDecoder)('utf8')\nstream.on('data', c => output += dec.write(c))\n```",tech:"node",displayNumber:19,sortKey:1701},{id:"node-1702",number:20,title:"What is `Buffer.byteLength` vs `String.length`?",question:"What is `Buffer.byteLength` vs `String.length`?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`String.length` counts UTF-16 code units; `Buffer.byteLength(str)` counts actual bytes in a given encoding. They differ for non-ASCII text — important for Content-Length headers.\n\n```js\n'é'.length              // 1\nBuffer.byteLength('é')  // 2 (UTF-8)\n```",tech:"node",displayNumber:20,sortKey:1702},{id:"node-1703",number:21,title:"What is the EventEmitter?",question:"What is the EventEmitter?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`EventEmitter` is the core class for the observer pattern in Node. Objects emit named events and listeners subscribe via `on`. Streams, servers, and many core APIs are EventEmitters.\n\n```js\nconst e = new (require('events'))()\ne.on('greet', name => console.log('Hi ' + name))\ne.emit('greet', 'Ada')\n```",tech:"node",displayNumber:21,sortKey:1703},{id:"node-1704",number:22,title:"What is the difference between `on` and `once`?",question:"What is the difference between `on` and `once`?",difficulty:"Basic",category:"Streams, Buffers & Events",answer:"`on` registers a listener that fires every time the event is emitted; `once` fires only the first time then auto-removes itself.\n\n```js\nserver.once('listening', () => console.log('ready')) // one-time\n```",tech:"node",displayNumber:22,sortKey:1704},{id:"node-1705",number:23,title:"How do you remove event listeners?",question:"How do you remove event listeners?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Use `removeListener(event, fn)`/`off` with the same function reference, or `removeAllListeners(event)`. You must keep a reference to the original function to remove it.\n\n```js\nconst fn = () => {}\ne.on('x', fn); e.off('x', fn)\n```",tech:"node",displayNumber:23,sortKey:1705},{id:"node-1706",number:24,title:"What is the max listeners warning?",question:"What is the max listeners warning?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"By default an emitter warns after 10 listeners for one event (a leak heuristic). Genuine cases can raise it with `setMaxListeners(n)`; unexpected growth usually signals a listener leak.\n\n```js\nemitter.setMaxListeners(20)\n```",tech:"node",displayNumber:24,sortKey:1706},{id:"node-1707",number:25,title:"What is an EventEmitter memory leak?",question:"What is an EventEmitter memory leak?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Repeatedly adding listeners without removing them (e.g. per request) accumulates them, retaining closures/memory and eventually degrading performance. Fix by using `once`, removing listeners, or `AbortSignal`.\n\n```js\nemitter.on('data', handler, { signal: controller.signal }) // auto-cleanup\n```",tech:"node",displayNumber:25,sortKey:1707},{id:"node-1708",number:26,title:"How do you emit an error event correctly?",question:"How do you emit an error event correctly?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Emit `'error'` with an `Error` object. If no listener is registered for `'error'`, Node throws and crashes the process — so always attach an error listener on emitters/streams.\n\n```js\nemitter.on('error', e => log(e))\nemitter.emit('error', new Error('boom'))\n```",tech:"node",displayNumber:26,sortKey:1708},{id:"node-1709",number:27,title:"What is the order of listener invocation?",question:"What is the order of listener invocation?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"Listeners for an event run synchronously in registration order. `prependListener` adds to the front. Emitting is synchronous — all listeners run before `emit` returns.\n\n```js\ne.on('x', () => console.log(1)); e.prependListener('x', () => console.log(0))\ne.emit('x') // 0 then 1\n```",tech:"node",displayNumber:27,sortKey:1709},{id:"node-1710",number:28,title:"Is `emit` synchronous or asynchronous?",question:"Is `emit` synchronous or asynchronous?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`emit` is synchronous: it calls listeners immediately and in order. To defer, wrap listener bodies in `setImmediate`/`queueMicrotask`. This sync nature can surprise developers expecting async behavior.\n\n```js\ne.emit('x'); console.log('after') // all 'x' listeners ran before 'after'\n```",tech:"node",displayNumber:28,sortKey:1710},{id:"node-1711",number:29,title:"How do you wait for an event as a Promise?",question:"How do you wait for an event as a Promise?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Use `events.once(emitter, name)` which returns a Promise resolving with the event args, or `events.on` for an async iterator of events.\n\n```js\nconst { once } = require('events')\nconst [data] = await once(stream, 'data')\n```",tech:"node",displayNumber:29,sortKey:1711},{id:"node-1712",number:30,title:"How do you iterate events with `for await`?",question:"How do you iterate events with `for await`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`events.on(emitter, name)` returns an async iterator yielding event arguments, letting you consume a stream of events sequentially with backpressure.\n\n```js\nfor await (const [msg] of events.on(socket, 'message')) handle(msg)\n```",tech:"node",displayNumber:30,sortKey:1712},{id:"node-1713",number:31,title:"What is the `close` event on a stream?",question:"What is the `close` event on a stream?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`close` fires when a stream and its underlying resource (file descriptor) are fully closed. `end` (readable) or `finish` (writable) fire earlier when data is done but before resources are released.\n\n```js\nws.on('finish', () => console.log('data flushed'))\nws.on('close', () => console.log('fd closed'))\n```",tech:"node",displayNumber:31,sortKey:1713},{id:"node-1714",number:32,title:"What is the difference between `end` and `finish` events?",question:"What is the difference between `end` and `finish` events?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`end` fires on a readable when there is no more data to consume. `finish` fires on a writable after `end()` is called and all data is flushed to the underlying system.\n\n```js\nreadable.on('end', () => {})\nwritable.on('finish', () => {})\n```",tech:"node",displayNumber:32,sortKey:1714},{id:"node-1715",number:33,title:"How do you handle large file uploads with streams?",question:"How do you handle large file uploads with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pipe the incoming request stream directly to disk or storage instead of buffering it in memory, using `pipeline` for error handling and enforcing size limits to avoid abuse.\n\n```js\nawait pipeline(req, fs.createWriteStream('/uploads/file'))\n```",tech:"node",displayNumber:33,sortKey:1715},{id:"node-1716",number:34,title:"How do you compress data on the fly with streams?",question:"How do you compress data on the fly with streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Insert a `zlib` transform (gzip/brotli) between source and destination in a pipeline, compressing chunk by chunk without loading the whole payload.\n\n```js\nawait pipeline(fs.createReadStream('a'), zlib.createBrotliCompress(), fs.createWriteStream('a.br'))\n```",tech:"node",displayNumber:34,sortKey:1716},{id:"node-1717",number:35,title:"What is `stream.finished()`?",question:"What is `stream.finished()`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`stream.finished(stream, cb)` invokes the callback when a stream is no longer readable/writable or errors, giving a reliable single completion signal across stream types.\n\n```js\nconst { finished } = require('stream/promises')\nawait finished(readable)\n```",tech:"node",displayNumber:35,sortKey:1717},{id:"node-1718",number:36,title:"How do you tee/duplicate a stream to two destinations?",question:"How do you tee/duplicate a stream to two destinations?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use `stream.PassThrough` or pipe the source to multiple writables (via `pipe` twice or a fan-out transform). Note both destinations share backpressure — the slowest paces the source.\n\n```js\nconst pass = new PassThrough()\nsrc.pipe(pass); pass.pipe(fileA); pass.pipe(fileB)\n```",tech:"node",displayNumber:36,sortKey:1718},{id:"node-1719",number:37,title:"What is a PassThrough stream?",question:"What is a PassThrough stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`PassThrough` is a trivial Transform that forwards input unchanged. It is handy as a placeholder, for measuring throughput, or for connecting/adapting pipelines.\n\n```js\nconst pt = new (require('stream').PassThrough)()\n```",tech:"node",displayNumber:37,sortKey:1719},{id:"node-1720",number:38,title:"How do you measure throughput of a stream?",question:"How do you measure throughput of a stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Insert a Transform (or PassThrough with a `data` listener) that counts bytes over time to compute bytes/sec, then forwards chunks unchanged.\n\n```js\nlet bytes = 0\nstream.on('data', c => bytes += c.length)\n```",tech:"node",displayNumber:38,sortKey:1720},{id:"node-1721",number:39,title:"What is the difference between `write()` returning true/false?",question:"What is the difference between `write()` returning true/false?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`write()` returns `true` if the chunk fit in the buffer (keep writing) and `false` if the buffer exceeded `highWaterMark` (stop and wait for `drain`). Ignoring `false` causes unbounded memory growth.\n\n```js\nif (!ws.write(chunk)) await once(ws, 'drain')\n```",tech:"node",displayNumber:39,sortKey:1721},{id:"node-1722",number:40,title:"How do you convert an async generator into a readable stream?",question:"How do you convert an async generator into a readable stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pass the async generator to `Readable.from`, which pulls values on demand and applies backpressure automatically.\n\n```js\nconst stream = Readable.from(rowsGenerator())\nstream.pipe(res)\n```",tech:"node",displayNumber:40,sortKey:1722},{id:"node-1723",number:41,title:"What is the `data`, `end`, `error` lifecycle of a readable?",question:"What is the `data`, `end`, `error` lifecycle of a readable?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`data` fires for each chunk (flowing mode), `end` when all data is consumed, and `error` on failure. Always handle `error`; missing it can crash the process.\n\n```js\nr.on('data', d => {}).on('end', () => {}).on('error', e => {})\n```",tech:"node",displayNumber:41,sortKey:1723},{id:"node-1724",number:42,title:"How do you enforce a max size when buffering a stream?",question:"How do you enforce a max size when buffering a stream?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Track accumulated bytes as chunks arrive and destroy the stream with an error once a limit is exceeded, preventing memory-exhaustion attacks (e.g. huge request bodies).\n\n```js\nlet size = 0\nreq.on('data', c => { size += c.length; if (size > MAX) req.destroy(new Error('too large')) })\n```",tech:"node",displayNumber:42,sortKey:1724},{id:"node-1725",number:43,title:"What is the difference between Duplex and Transform streams?",question:"What is the difference between Duplex and Transform streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"A Duplex has independent readable and writable sides (e.g. a socket: what you read isn't what you write). A Transform's output is a function of its input (e.g. compression) — the two sides are linked.\n\n```js\n// TCP socket = Duplex; zlib.createGzip() = Transform\n```",tech:"node",displayNumber:43,sortKey:1725},{id:"node-1726",number:44,title:"How do you gracefully destroy a stream?",question:"How do you gracefully destroy a stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Call `stream.destroy(err?)` to close it and release resources, optionally emitting an error. It stops further reads/writes and triggers cleanup. Prefer this over leaving streams open.\n\n```js\nreadable.destroy() // release fd, stop emitting\n```",tech:"node",displayNumber:44,sortKey:1726},{id:"node-1727",number:45,title:"What is the `readableEnded` / `writableFinished` property?",question:"What is the `readableEnded` / `writableFinished` property?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"These booleans report whether the stream has finished reading/writing, useful for state checks without relying solely on events.\n\n```js\nif (ws.writableFinished) console.log('all written')\n```",tech:"node",displayNumber:45,sortKey:1727},{id:"node-1728",number:46,title:"How do you pipe with transformation and error handling in one call?",question:"How do you pipe with transformation and error handling in one call?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use the promise-based `pipeline` with all stages; it awaits completion and rejects on any stage error, cleaning up every stream.\n\n```js\nawait pipeline(source, decompress, parse, writeToDb)\n```",tech:"node",displayNumber:46,sortKey:1728},{id:"node-1729",number:47,title:"What is chunked transfer and how do streams relate?",question:"What is chunked transfer and how do streams relate?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"HTTP chunked transfer sends a response in pieces without a known total length. Node streams map naturally onto this: piping a readable to the response emits chunks with `Transfer-Encoding: chunked`.\n\n```js\nfs.createReadStream('video.mp4').pipe(res) // chunked automatically\n```",tech:"node",displayNumber:47,sortKey:1729},{id:"node-1730",number:48,title:"How do you implement range requests (partial content) with streams?",question:"How do you implement range requests (partial content) with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Parse the `Range` header, respond with 206 and `Content-Range`, and stream only the requested byte range via `createReadStream(path, { start, end })`.\n\n```js\nfs.createReadStream(file, { start, end }).pipe(res.writeHead(206, headers))\n```",tech:"node",displayNumber:48,sortKey:1730},{id:"node-1731",number:49,title:"What is the `cork`/`uncork` mechanism?",question:"What is the `cork`/`uncork` mechanism?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`cork()` buffers multiple `write()` calls and flushes them together on `uncork()`, reducing syscalls when writing many small chunks — a batching optimization.\n\n```js\nsocket.cork(); socket.write('a'); socket.write('b'); socket.uncork()\n```",tech:"node",displayNumber:49,sortKey:1731},{id:"node-1732",number:50,title:"How do you compose reusable stream transforms?",question:"How do you compose reusable stream transforms?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Build small Transform factories and combine them in a `pipeline`, or use `stream.compose()` (modern Node) to merge several into one reusable stream.\n\n```js\nconst combined = compose(parseJson, validate, enrich)\nawait pipeline(source, combined, sink)\n```",tech:"node",displayNumber:50,sortKey:1732},{id:"node-1733",number:51,title:"What is the difference between `Buffer.copy` and `Buffer.slice`?",question:"What is the difference between `Buffer.copy` and `Buffer.slice`?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"`slice`/`subarray` returns a view sharing the same memory (mutations affect the original). `copy` writes bytes into a separate target buffer (independent memory). Choose based on whether you need isolation.\n\n```js\nconst view = buf.subarray(0, 4) // shares memory\nbuf.copy(target)                 // independent copy\n```",tech:"node",displayNumber:51,sortKey:1733},{id:"node-1734",number:52,title:"How do you read a binary file structure with Buffers?",question:"How do you read a binary file structure with Buffers?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use typed read methods (`readUInt32BE`, `readInt16LE`, etc.) at byte offsets to parse headers/fields, respecting endianness.\n\n```js\nconst magic = buf.readUInt32BE(0)\nconst width = buf.readUInt16LE(4)\n```",tech:"node",displayNumber:52,sortKey:1734},{id:"node-1735",number:53,title:"What is endianness and why does it matter for Buffers?",question:"What is endianness and why does it matter for Buffers?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Endianness is byte order for multi-byte numbers: big-endian (most significant first) vs little-endian. Reading with the wrong order yields garbage, so binary protocols specify it and you use the matching `BE`/`LE` methods.\n\n```js\nbuf.readUInt16BE(0) // big-endian\nbuf.readUInt16LE(0) // little-endian\n```",tech:"node",displayNumber:53,sortKey:1735},{id:"node-1736",number:54,title:"How do you handle newline-delimited data from a stream?",question:"How do you handle newline-delimited data from a stream?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Use `readline.createInterface` over the stream, or a Transform that buffers until it sees `\\n`, emitting complete lines. Naive chunk splitting breaks lines that span chunks.\n\n```js\nconst rl = readline.createInterface({ input: fs.createReadStream('log') })\nfor await (const line of rl) process(line)\n```",tech:"node",displayNumber:54,sortKey:1736},{id:"node-1737",number:55,title:"What is `readline` used for besides input?",question:"What is `readline` used for besides input?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`readline` reads any readable line by line (files, sockets), supports async iteration, and offers interactive prompts/history for CLIs.\n\n```js\nfor await (const line of readline.createInterface({ input: stream })) {}\n```",tech:"node",displayNumber:55,sortKey:1737},{id:"node-1738",number:56,title:"How do you signal end-of-stream from a custom readable?",question:"How do you signal end-of-stream from a custom readable?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Call `this.push(null)` to indicate no more data; consumers then receive the `end` event. Failing to push null leaves consumers hanging.\n\n```js\nread() { if (done) this.push(null); else this.push(nextChunk()) }\n```",tech:"node",displayNumber:56,sortKey:1738},{id:"node-1739",number:57,title:'What causes "write after end" errors?',question:'What causes "write after end" errors?',difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Calling `write()` on a writable after `end()` has been called throws/emits an error. It usually indicates a logic bug where writes race with stream termination.\n\n```js\nws.end('last'); ws.write('oops') // Error: write after end\n```",tech:"node",displayNumber:57,sortKey:1739},{id:"node-1740",number:58,title:"How do you throttle a stream's rate?",question:"How do you throttle a stream's rate?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Insert a Transform that delays chunk passthrough (using timers) to cap bytes/sec, applying backpressure to the source. Useful for bandwidth limiting.\n\n```js\nnew Transform({ transform(c, e, cb) { setTimeout(() => cb(null, c), delay) } })\n```",tech:"node",displayNumber:58,sortKey:1740},{id:"node-1741",number:59,title:"What is the relationship between HTTP req/res and streams?",question:"What is the relationship between HTTP req/res and streams?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`req` (IncomingMessage) is a Readable stream of the request body; `res` (ServerResponse) is a Writable stream for the response. This is why you can pipe files directly to `res`.\n\n```js\nreq.pipe(fs.createWriteStream('body.txt'))\n```",tech:"node",displayNumber:59,sortKey:1741},{id:"node-1742",number:60,title:"How do you handle stream errors in an Express route?",question:"How do you handle stream errors in an Express route?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Attach an `error` handler (or use `pipeline`) and forward to Express's error middleware via `next(err)`; also ensure the response is ended/destroyed to avoid hanging connections.\n\n```js\npipeline(readStream, res, err => { if (err) next(err) })\n```",tech:"node",displayNumber:60,sortKey:1742},{id:"node-1743",number:61,title:"What is `Symbol.asyncIterator` on streams?",question:"What is `Symbol.asyncIterator` on streams?",difficulty:"Advanced",category:"Streams, Buffers & Events",answer:"Readable streams implement `Symbol.asyncIterator`, so `for await (const chunk of stream)` works out of the box, consuming chunks with backpressure and stopping on `end`/`error`.\n\n```js\nfor await (const chunk of fs.createReadStream('f')) total += chunk.length\n```",tech:"node",displayNumber:61,sortKey:1743},{id:"node-1744",number:62,title:"How do Web Streams differ from Node streams?",question:"How do Web Streams differ from Node streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Web Streams (`ReadableStream`/`WritableStream`) are the WHATWG standard also available in Node for cross-platform code (e.g. `fetch` bodies). Node provides adapters (`Readable.fromWeb`/`toWeb`) to interoperate.\n\n```js\nconst nodeStream = require('stream').Readable.fromWeb(webReadable)\n```",tech:"node",displayNumber:62,sortKey:1744},{id:"node-1745",number:63,title:"How do you avoid loading a whole JSON array into memory?",question:"How do you avoid loading a whole JSON array into memory?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Use a streaming JSON parser (e.g. a Transform that emits each array element) so you process records incrementally instead of `JSON.parse` on the entire file.\n\n```js\npipeline(fs.createReadStream('big.json'), streamArrayParser(), handleEach)\n```",tech:"node",displayNumber:63,sortKey:1745},{id:"node-1746",number:64,title:"What is the difference between `pause()`/`resume()` and `cork()`/`uncork()`?",question:"What is the difference between `pause()`/`resume()` and `cork()`/`uncork()`?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"`pause`/`resume` control a readable's flow (whether it emits data). `cork`/`uncork` batch writes on a writable to reduce syscalls. Different sides, different purposes.\n\n```js\nreadable.pause() // stop reading\nwritable.cork()  // batch writes\n```",tech:"node",displayNumber:64,sortKey:1746},{id:"node-1747",number:65,title:"How do you propagate an abort into a stream pipeline?",question:"How do you propagate an abort into a stream pipeline?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Pass an `AbortSignal` to `pipeline`'s options; aborting destroys all streams and rejects the pipeline, enabling cancellable transfers.\n\n```js\nawait pipeline(src, dst, { signal: controller.signal })\n```",tech:"node",displayNumber:65,sortKey:1747},{id:"node-1748",number:66,title:"What are common causes of stream memory leaks?",question:"What are common causes of stream memory leaks?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Ignoring backpressure, not destroying streams on error, unbounded internal buffering (object mode without highWaterMark), and accumulating chunks in arrays. Use `pipeline` and honor `write()`'s return value.\n\n```js\n// leak: chunks.push(c) forever without consuming\n```",tech:"node",displayNumber:66,sortKey:1748},{id:"node-1749",number:67,title:"How do you implement a rate-limited log writer with streams?",question:"How do you implement a rate-limited log writer with streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Write to a writable file stream and rely on backpressure; batch with `cork`/`uncork` and rotate files by size/time to bound disk usage.\n\n```js\nlogStream.cork(); for (const l of batch) logStream.write(l + '\\n'); logStream.uncork()\n```",tech:"node",displayNumber:67,sortKey:1749},{id:"node-1750",number:68,title:"What is the difference between `Buffer.from(string)` and `Buffer.from(array)`?",question:"What is the difference between `Buffer.from(string)` and `Buffer.from(array)`?",difficulty:"Intermediate",category:"Streams, Buffers & Events",answer:"`Buffer.from(string, enc)` encodes text into bytes. `Buffer.from(array)` treats each element as a byte value (0–255). They produce different bytes for the same-looking input.\n\n```js\nBuffer.from('123')          // <Buffer 31 32 33>\nBuffer.from([1, 2, 3])      // <Buffer 01 02 03>\n```",tech:"node",displayNumber:68,sortKey:1750},{id:"node-1751",number:69,title:"How do you detect the end of an EventEmitter-based protocol?",question:"How do you detect the end of an EventEmitter-based protocol?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"Listen for the protocol's terminal event (`end`, `close`, or a custom `done`), and guard against emitting after termination. Combine with timeouts to detect stalled peers.\n\n```js\nsocket.on('end', cleanup); socket.on('close', cleanup)\n```",tech:"node",displayNumber:69,sortKey:1751},{id:"node-1752",number:70,title:"When should you NOT use streams?",question:"When should you NOT use streams?",difficulty:"Experienced",category:"Streams, Buffers & Events",answer:"For small payloads that fit comfortably in memory, streams add complexity and overhead for little benefit — just buffer and process directly. Use streams when data is large, unbounded, or benefits from incremental/low-latency processing.\n\n```js\nconst cfg = JSON.parse(await fs.promises.readFile('config.json')) // fine, small\n```",tech:"node",displayNumber:70,sortKey:1752},{id:"node-1753",number:1,title:"What is the `fs` module?",question:"What is the `fs` module?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs` provides file system operations: reading, writing, deleting, watching files and directories. It offers callback, synchronous (`*Sync`), and Promise (`fs/promises`) variants.\n\n```js\nconst fs = require('node:fs/promises')\nconst data = await fs.readFile('a.txt', 'utf8')\n```",tech:"node",displayNumber:1,sortKey:1753},{id:"node-1754",number:2,title:"What are the three API styles of `fs`?",question:"What are the three API styles of `fs`?",difficulty:"Basic",category:"File System, Process & OS",answer:"Callback-based (`fs.readFile(path, cb)`), synchronous (`fs.readFileSync`), and Promise-based (`require('fs/promises')`). Prefer Promises in modern async code; avoid Sync in servers.\n\n```js\nimport { readFile } from 'node:fs/promises'\n```",tech:"node",displayNumber:2,sortKey:1754},{id:"node-1755",number:3,title:"Why avoid `fs.readFileSync` in a web server?",question:"Why avoid `fs.readFileSync` in a web server?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Sync methods block the single event-loop thread until I/O completes, freezing all concurrent requests. Sync I/O is acceptable only at startup (loading config) or in CLI scripts.\n\n```js\n// startup only\nconst config = JSON.parse(fs.readFileSync('config.json', 'utf8'))\n```",tech:"node",displayNumber:3,sortKey:1755},{id:"node-1756",number:4,title:"How do you read a file's contents?",question:"How do you read a file's contents?",difficulty:"Basic",category:"File System, Process & OS",answer:"Use `fs.readFile(path, encoding)`. Without an encoding you get a Buffer; with `'utf8'` you get a string. For large files, prefer streaming.\n\n```js\nconst text = await fs.readFile('notes.md', 'utf8')\n```",tech:"node",displayNumber:4,sortKey:1756},{id:"node-1757",number:5,title:"How do you write and append to a file?",question:"How do you write and append to a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.writeFile` overwrites (or creates) a file; `fs.appendFile` adds to the end. Both accept strings or Buffers.\n\n```js\nawait fs.writeFile('log.txt', 'start\\n')\nawait fs.appendFile('log.txt', 'more\\n')\n```",tech:"node",displayNumber:5,sortKey:1757},{id:"node-1758",number:6,title:"How do you check if a file exists?",question:"How do you check if a file exists?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Prefer trying the operation and handling `ENOENT`, or use `fs.access`. Avoid the old `fs.exists` (deprecated) and avoid check-then-act races (TOCTOU).\n\n```js\ntry { await fs.access('f.txt') } catch { /* missing */ }\n```",tech:"node",displayNumber:6,sortKey:1758},{id:"node-1759",number:7,title:"What is a TOCTOU race in file operations?",question:"What is a TOCTOU race in file operations?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Time-Of-Check to Time-Of-Use: a file's state can change between checking (exists/permissions) and using it, enabling bugs or attacks. Prefer atomic operations that check-and-act in one call, and handle errors instead of pre-checking.\n\n```js\n// atomic: fails if it already exists, no separate check\nawait fs.writeFile('f', data, { flag: 'wx' })\n```",tech:"node",displayNumber:7,sortKey:1759},{id:"node-1760",number:8,title:"How do you create a directory (including parents)?",question:"How do you create a directory (including parents)?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.mkdir(path, { recursive: true })` creates the directory and any missing parents, and won't error if it already exists.\n\n```js\nawait fs.mkdir('a/b/c', { recursive: true })\n```",tech:"node",displayNumber:8,sortKey:1760},{id:"node-1761",number:9,title:"How do you read the contents of a directory?",question:"How do you read the contents of a directory?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.readdir(path)` returns file/dir names. With `{ withFileTypes: true }` it returns `Dirent` objects so you can distinguish files from directories without extra `stat` calls.\n\n```js\nfor (const d of await fs.readdir('.', { withFileTypes: true }))\n  if (d.isDirectory()) console.log(d.name)\n```",tech:"node",displayNumber:9,sortKey:1761},{id:"node-1762",number:10,title:"How do you delete a file or directory?",question:"How do you delete a file or directory?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.unlink` removes a file; `fs.rm(path, { recursive: true, force: true })` removes files or directory trees, replacing the deprecated `fs.rmdir` recursive option.\n\n```js\nawait fs.rm('temp', { recursive: true, force: true })\n```",tech:"node",displayNumber:10,sortKey:1762},{id:"node-1763",number:11,title:"What does `fs.stat` return?",question:"What does `fs.stat` return?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"A `Stats` object with metadata: size, timestamps (`mtime`, `ctime`, `atime`), and type predicates (`isFile()`, `isDirectory()`, `isSymbolicLink()`).\n\n```js\nconst s = await fs.stat('a.txt')\nconsole.log(s.size, s.isFile())\n```",tech:"node",displayNumber:11,sortKey:1763},{id:"node-1764",number:12,title:"What is the difference between `fs.stat` and `fs.lstat`?",question:"What is the difference between `fs.stat` and `fs.lstat`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`stat` follows symbolic links and reports the target's info; `lstat` reports the link itself. Use `lstat` when you need to detect symlinks.\n\n```js\nconst s = await fs.lstat('link'); s.isSymbolicLink() // true for the link\n```",tech:"node",displayNumber:12,sortKey:1764},{id:"node-1765",number:13,title:"How do you rename or move a file?",question:"How do you rename or move a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.rename(old, new)` renames or moves within the same filesystem atomically. Across filesystems it may fail with `EXDEV`, requiring a copy-then-delete.\n\n```js\nawait fs.rename('a.txt', 'archive/a.txt')\n```",tech:"node",displayNumber:13,sortKey:1765},{id:"node-1766",number:14,title:"How do you copy a file?",question:"How do you copy a file?",difficulty:"Basic",category:"File System, Process & OS",answer:"`fs.copyFile(src, dest)` copies a single file; `fs.cp(src, dest, { recursive: true })` copies directory trees.\n\n```js\nawait fs.cp('src', 'backup', { recursive: true })\n```",tech:"node",displayNumber:14,sortKey:1766},{id:"node-1767",number:15,title:"How do you watch a file or directory for changes?",question:"How do you watch a file or directory for changes?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`fs.watch` emits events on changes (efficient, OS-backed, but can miss/duplicate events and vary by platform). `fs.watchFile` polls stats (reliable but heavier). Libraries like chokidar smooth over platform quirks.\n\n```js\nfs.watch('config.json', (event, filename) => reload())\n```",tech:"node",displayNumber:15,sortKey:1767},{id:"node-1768",number:16,title:"What is a file descriptor?",question:"What is a file descriptor?",difficulty:"Advanced",category:"File System, Process & OS",answer:"A file descriptor (fd) is an integer handle to an open file/socket from the OS. `fs.open` returns one; you must `close` it to avoid leaking descriptors (there's an OS limit).\n\n```js\nconst fh = await fs.open('a.txt', 'r')\ntry { /* read */ } finally { await fh.close() }\n```",tech:"node",displayNumber:16,sortKey:1768},{id:"node-1769",number:17,title:"What are file open flags like `r`, `w`, `a`, `wx`?",question:"What are file open flags like `r`, `w`, `a`, `wx`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`r` read, `w` write/truncate, `a` append, `x` fail if exists. Combinations like `wx` create exclusively (atomic \"create if not exists\"), and `r+`/`w+` allow read+write.\n\n```js\nawait fs.writeFile('lock', '', { flag: 'wx' }) // errors if lock exists\n```",tech:"node",displayNumber:17,sortKey:1769},{id:"node-1770",number:18,title:"How do you read a large file efficiently?",question:"How do you read a large file efficiently?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.createReadStream` to process in chunks with constant memory, optionally through `readline` for line-based data, rather than loading it all with `readFile`.\n\n```js\nfor await (const line of readline.createInterface({ input: fs.createReadStream('big.log') })) {}\n```",tech:"node",displayNumber:18,sortKey:1770},{id:"node-1771",number:19,title:"How do you handle file paths cross-platform?",question:"How do you handle file paths cross-platform?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use the `path` module (`join`, `resolve`, `sep`, `basename`, `extname`) instead of hardcoding `/` or `\\`, so code works on Windows and POSIX.\n\n```js\nconst p = path.join(dir, 'sub', 'file.txt')\n```",tech:"node",displayNumber:19,sortKey:1771},{id:"node-1772",number:20,title:"What is `path.normalize`?",question:"What is `path.normalize`?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`path.normalize` resolves `.` and `..` segments and collapses duplicate separators into a canonical path, useful before comparing or using user-supplied paths.\n\n```js\npath.normalize('a//b/../c') // 'a/c'\n```",tech:"node",displayNumber:20,sortKey:1772},{id:"node-1773",number:21,title:"How do you prevent path traversal attacks?",question:"How do you prevent path traversal attacks?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Resolve the requested path against a base directory and verify the result still starts with that base, rejecting `../` escapes. Never concatenate user input directly into file paths.\n\n```js\nconst full = path.resolve(base, userPath)\nif (!full.startsWith(base + path.sep)) throw new Error('traversal')\n```",tech:"node",displayNumber:21,sortKey:1773},{id:"node-1774",number:22,title:"What is the difference between `path.extname` and manual splitting?",question:"What is the difference between `path.extname` and manual splitting?",difficulty:"Basic",category:"File System, Process & OS",answer:"`path.extname` reliably returns the extension including the dot (handling edge cases like dotfiles and multiple dots), avoiding brittle manual `split('.')` logic.\n\n```js\npath.extname('archive.tar.gz') // '.gz'\npath.extname('.env')            // ''\n```",tech:"node",displayNumber:22,sortKey:1774},{id:"node-1775",number:23,title:"How do you create a temporary file/directory?",question:"How do you create a temporary file/directory?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.mkdtemp(prefix)` for a unique temp directory (typically under `os.tmpdir()`), then write files inside it. This avoids name collisions.\n\n```js\nconst dir = await fs.mkdtemp(path.join(os.tmpdir(), 'app-'))\n```",tech:"node",displayNumber:23,sortKey:1775},{id:"node-1776",number:24,title:"What is `os.tmpdir()`?",question:"What is `os.tmpdir()`?",difficulty:"Basic",category:"File System, Process & OS",answer:"It returns the operating system's default directory for temporary files (e.g. `/tmp` on Linux), which the OS may clean periodically.\n\n```js\nconsole.log(os.tmpdir()) // '/tmp'\n```",tech:"node",displayNumber:24,sortKey:1776},{id:"node-1777",number:25,title:"How do you set file permissions?",question:"How do you set file permissions?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.chmod(path, mode)` with an octal mode (e.g. `0o600`) on POSIX systems. On Windows, permission bits are largely ignored.\n\n```js\nawait fs.chmod('secret.key', 0o600) // owner read/write only\n```",tech:"node",displayNumber:25,sortKey:1777},{id:"node-1778",number:26,title:"What does `process.cwd()` return and can it change?",question:"What does `process.cwd()` return and can it change?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"It returns the process's current working directory. `process.chdir(dir)` changes it, affecting relative path resolution. Prefer absolute paths (via `__dirname`) to avoid surprises.\n\n```js\nprocess.chdir('/app'); console.log(process.cwd()) // '/app'\n```",tech:"node",displayNumber:26,sortKey:1778},{id:"node-1779",number:27,title:"How do you spawn a child process?",question:"How do you spawn a child process?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`child_process.spawn(cmd, args)` launches a process and streams its stdio (good for large output). `exec` buffers output, `execFile` runs a binary directly, `fork` spawns a Node child with an IPC channel.\n\n```js\nconst { spawn } = require('node:child_process')\nspawn('ls', ['-la']).stdout.pipe(process.stdout)\n```",tech:"node",displayNumber:27,sortKey:1779},{id:"node-1780",number:28,title:"What is the difference between `spawn` and `exec`?",question:"What is the difference between `spawn` and `exec`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`spawn` streams stdout/stderr (constant memory, good for long/large output). `exec` runs a shell command and buffers all output in memory (convenient but risky for large output and shell injection).\n\n```js\nexec('git log', (err, stdout) => {}) // buffered\nspawn('git', ['log'])                 // streamed\n```",tech:"node",displayNumber:28,sortKey:1780},{id:"node-1781",number:29,title:"Why prefer `execFile`/`spawn` with an args array over `exec` with a string?",question:"Why prefer `execFile`/`spawn` with an args array over `exec` with a string?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`exec` runs through a shell, so interpolated user input enables command injection. `execFile`/`spawn` pass args directly to the binary without shell parsing, preventing injection.\n\n```js\nexecFile('convert', [userFile, 'out.png']) // safe, no shell\n```",tech:"node",displayNumber:29,sortKey:1781},{id:"node-1782",number:30,title:"What is `child_process.fork`?",question:"What is `child_process.fork`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`fork` spawns a new Node process running a module, with a built-in IPC channel for `send`/`message`. It is used to run separate Node scripts and communicate via message passing.\n\n```js\nconst child = fork('worker.js')\nchild.send({ task: 1 }); child.on('message', console.log)\n```",tech:"node",displayNumber:30,sortKey:1782},{id:"node-1783",number:31,title:"How do child processes communicate (IPC)?",question:"How do child processes communicate (IPC)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Forked children use `process.send`/`on('message')` over an IPC channel with structured cloning of messages. Spawned processes communicate via stdio streams or sockets.\n\n```js\n// in child\nprocess.on('message', m => process.send({ echo: m }))\n```",tech:"node",displayNumber:31,sortKey:1783},{id:"node-1784",number:32,title:"What is the `cluster` module?",question:"What is the `cluster` module?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`cluster` forks multiple worker processes that share a server port, letting a Node app use all CPU cores. The primary process distributes incoming connections among workers.\n\n```js\nif (cluster.isPrimary) for (let i = 0; i < os.cpus().length; i++) cluster.fork()\nelse http.createServer(handler).listen(3000)\n```",tech:"node",displayNumber:32,sortKey:1784},{id:"node-1785",number:33,title:"How does the cluster module share a port across workers?",question:"How does the cluster module share a port across workers?",difficulty:"Experienced",category:"File System, Process & OS",answer:"The primary creates the listening socket and hands connections to workers (round-robin by default on non-Windows), or workers share the descriptor. The OS/primary balances the load.\n\n```js\ncluster.schedulingPolicy = cluster.SCHED_RR\n```",tech:"node",displayNumber:33,sortKey:1785},{id:"node-1786",number:34,title:"What is the difference between cluster and worker threads?",question:"What is the difference between cluster and worker threads?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Cluster forks full processes (isolated memory, good for scaling I/O-bound servers across cores). Worker threads share memory within one process (lower overhead, good for CPU-bound tasks). Choose by workload and isolation needs.\n\n```js\n// I/O scaling -> cluster; CPU crunching -> worker_threads\n```",tech:"node",displayNumber:34,sortKey:1786},{id:"node-1787",number:35,title:"How do you pass data between worker threads?",question:"How do you pass data between worker threads?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Via `postMessage`/`on('message')` (structured clone), `workerData` at creation, or shared memory with `SharedArrayBuffer` + `Atomics` for zero-copy communication.\n\n```js\nnew Worker('./w.js', { workerData: { rows } })\n```",tech:"node",displayNumber:35,sortKey:1787},{id:"node-1788",number:36,title:"What is `SharedArrayBuffer` and `Atomics`?",question:"What is `SharedArrayBuffer` and `Atomics`?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`SharedArrayBuffer` is memory shared between threads without copying; `Atomics` provides atomic read/write/wait operations to coordinate access safely and avoid data races.\n\n```js\nconst shared = new Int32Array(new SharedArrayBuffer(4))\nAtomics.add(shared, 0, 1)\n```",tech:"node",displayNumber:36,sortKey:1788},{id:"node-1789",number:37,title:"How do you get system information?",question:"How do you get system information?",difficulty:"Basic",category:"File System, Process & OS",answer:"The `os` module exposes `platform()`, `arch()`, `cpus()`, `totalmem()`, `freemem()`, `hostname()`, `uptime()`, and `networkInterfaces()`.\n\n```js\nconsole.log(os.platform(), os.cpus().length, os.freemem())\n```",tech:"node",displayNumber:37,sortKey:1789},{id:"node-1790",number:38,title:"How do you read memory usage of the Node process?",question:"How do you read memory usage of the Node process?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`process.memoryUsage()` returns `rss`, `heapTotal`, `heapUsed`, `external`, and `arrayBuffers` in bytes — useful for monitoring and leak detection.\n\n```js\nconsole.log(process.memoryUsage().rss / 1e6, 'MB RSS')\n```",tech:"node",displayNumber:38,sortKey:1790},{id:"node-1791",number:39,title:"What is RSS vs heap in memory usage?",question:"What is RSS vs heap in memory usage?",difficulty:"Experienced",category:"File System, Process & OS",answer:"RSS (Resident Set Size) is total physical memory the process holds (code, stack, heap, buffers). Heap is the V8-managed JS object memory. External/arrayBuffers cover off-heap allocations like Buffers.\n\n```js\nconst { rss, heapUsed, external } = process.memoryUsage()\n```",tech:"node",displayNumber:39,sortKey:1791},{id:"node-1792",number:40,title:"How do you handle process signals for graceful shutdown?",question:"How do you handle process signals for graceful shutdown?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Listen for `SIGINT`/`SIGTERM`, stop accepting connections, drain in-flight work, close resources, then exit. Add a timeout to force-exit if cleanup hangs.\n\n```js\nprocess.on('SIGTERM', async () => { await server.close(); process.exit(0) })\n```",tech:"node",displayNumber:40,sortKey:1792},{id:"node-1793",number:41,title:"What is `process.pid` and `process.ppid`?",question:"What is `process.pid` and `process.ppid`?",difficulty:"Basic",category:"File System, Process & OS",answer:"`process.pid` is the current process ID; `process.ppid` is the parent's. Useful for logging, process management, and IPC coordination.\n\n```js\nconsole.log(`pid ${process.pid}, parent ${process.ppid}`)\n```",tech:"node",displayNumber:41,sortKey:1793},{id:"node-1794",number:42,title:"How do you measure CPU usage of the current process?",question:"How do you measure CPU usage of the current process?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`process.cpuUsage()` returns user/system microseconds; call it before and after work and diff to get consumption. `process.resourceUsage()` gives more detail.\n\n```js\nconst start = process.cpuUsage(); work(); console.log(process.cpuUsage(start))\n```",tech:"node",displayNumber:42,sortKey:1794},{id:"node-1795",number:43,title:"How do you set and read environment variables at runtime?",question:"How do you set and read environment variables at runtime?",difficulty:"Basic",category:"File System, Process & OS",answer:"Read/write `process.env`. Values are strings; changes affect the current process (and children spawned afterward) but not the parent shell.\n\n```js\nprocess.env.FEATURE = 'on'\n```",tech:"node",displayNumber:43,sortKey:1795},{id:"node-1796",number:44,title:"How do you load a `.env` file?",question:"How do you load a `.env` file?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use a loader like `dotenv`, or Node's built-in `--env-file` flag (recent versions), which parses key=value pairs into `process.env` at startup. Never commit secret `.env` files.\n\n```js\n$ node --env-file=.env app.js\n```",tech:"node",displayNumber:44,sortKey:1796},{id:"node-1797",number:45,title:"What is the standard way to structure config for multiple environments?",question:"What is the standard way to structure config for multiple environments?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Read all config from environment variables (12-factor), with sensible defaults and validation at startup. Avoid committing environment-specific secrets; use per-environment env files or a secrets manager.\n\n```js\nconst config = { port: Number(process.env.PORT ?? 3000), db: process.env.DB_URL }\n```",tech:"node",displayNumber:45,sortKey:1797},{id:"node-1798",number:46,title:"How do you run a shell command and capture its output as a Promise?",question:"How do you run a shell command and capture its output as a Promise?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Promisify `exec` (or use `execFile`), awaiting `{ stdout, stderr }`. Prefer `execFile` with args to avoid shell injection.\n\n```js\nconst { stdout } = await promisify(execFile)('node', ['-v'])\n```",tech:"node",displayNumber:46,sortKey:1798},{id:"node-1799",number:47,title:"How do you handle a child process that hangs?",question:"How do you handle a child process that hangs?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Set a `timeout` (spawn/exec option) or an external timer that calls `child.kill('SIGKILL')`. Also handle `error`, `exit`, and non-zero exit codes.\n\n```js\nconst child = spawn('slow', [], { timeout: 5000, killSignal: 'SIGKILL' })\n```",tech:"node",displayNumber:47,sortKey:1799},{id:"node-1800",number:48,title:"What are exit codes and how do you set them?",question:"What are exit codes and how do you set them?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Exit codes signal success (0) or failure (non-zero) to the OS/parent. Set via `process.exit(code)` or `process.exitCode = code` (graceful). CLIs use them so scripts can detect failure.\n\n```js\nif (invalid) process.exitCode = 2\n```",tech:"node",displayNumber:48,sortKey:1800},{id:"node-1801",number:49,title:"How do you detect the platform and adjust behavior?",question:"How do you detect the platform and adjust behavior?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Check `process.platform` (`win32`, `linux`, `darwin`) or `os.platform()`/`os.type()` to branch on OS-specific paths, commands, or line endings.\n\n```js\nconst npmCmd = process.platform === 'win32' ? 'npm.cmd' : 'npm'\n```",tech:"node",displayNumber:49,sortKey:1801},{id:"node-1802",number:50,title:"What is `os.EOL`?",question:"What is `os.EOL`?",difficulty:"Basic",category:"File System, Process & OS",answer:"`os.EOL` is the platform's line terminator (`\\n` on POSIX, `\\r\\n` on Windows). Use it when generating text files meant to match native conventions.\n\n```js\nfs.writeFile('out.txt', lines.join(os.EOL))\n```",tech:"node",displayNumber:50,sortKey:1802},{id:"node-1803",number:51,title:"How do you get the number of CPU cores for scaling?",question:"How do you get the number of CPU cores for scaling?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`os.cpus().length` (or `os.availableParallelism()` in newer Node) gives core count, used to size cluster workers or thread pools.\n\n```js\nconst workers = os.availableParallelism()\n```",tech:"node",displayNumber:51,sortKey:1803},{id:"node-1804",number:52,title:"What is `process.stdin` used for?",question:"What is `process.stdin` used for?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"`process.stdin` is a readable stream of input piped/typed into the process. Read it for CLI input or piped data; combine with `readline` for line-based reading.\n\n```js\nprocess.stdin.on('data', d => console.log('got', d.toString()))\n```",tech:"node",displayNumber:52,sortKey:1804},{id:"node-1805",number:53,title:"How do you read piped input vs interactive input?",question:"How do you read piped input vs interactive input?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Check `process.stdin.isTTY`: true means an interactive terminal (prompt the user); false means piped/redirected data (read the stream to end).\n\n```js\nif (process.stdin.isTTY) prompt(); else readPipedData()\n```",tech:"node",displayNumber:53,sortKey:1805},{id:"node-1806",number:54,title:"How do you handle uncaught exceptions in a worker/cluster setup?",question:"How do you handle uncaught exceptions in a worker/cluster setup?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Log the error and let the worker exit; the primary listens for `exit` and forks a replacement. This keeps the service available while replacing crashed workers.\n\n```js\ncluster.on('exit', () => cluster.fork()) // respawn\n```",tech:"node",displayNumber:54,sortKey:1806},{id:"node-1807",number:55,title:"What is a zombie/orphan process and how do you avoid it?",question:"What is a zombie/orphan process and how do you avoid it?",difficulty:"Experienced",category:"File System, Process & OS",answer:"A zombie is a finished child not yet reaped by its parent; an orphan is a child whose parent died. Avoid by handling `exit` events, killing children on shutdown, and using process managers/`detached` correctly.\n\n```js\nprocess.on('exit', () => child.kill())\n```",tech:"node",displayNumber:55,sortKey:1807},{id:"node-1808",number:56,title:"How do you make a long-running background process survive terminal close?",question:"How do you make a long-running background process survive terminal close?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Spawn with `{ detached: true, stdio: 'ignore' }` and call `child.unref()`, or use a process manager (pm2, systemd). This decouples the child from the parent's lifecycle.\n\n```js\nconst c = spawn('node', ['job.js'], { detached: true, stdio: 'ignore' }); c.unref()\n```",tech:"node",displayNumber:56,sortKey:1808},{id:"node-1809",number:57,title:"What is `process.umask()`?",question:"What is `process.umask()`?",difficulty:"Experienced",category:"File System, Process & OS",answer:"The umask controls default permission bits stripped from newly created files/dirs. `process.umask()` reads it; setting it affects security of created files (e.g. preventing world-readable secrets).\n\n```js\nconsole.log(process.umask().toString(8)) // e.g. '22'\n```",tech:"node",displayNumber:57,sortKey:1809},{id:"node-1810",number:58,title:"How do you atomically write a file to avoid corruption?",question:"How do you atomically write a file to avoid corruption?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Write to a temp file in the same directory, then `fs.rename` it over the target (rename is atomic on the same filesystem). A crash mid-write leaves the original intact.\n\n```js\nawait fs.writeFile(tmp, data); await fs.rename(tmp, 'config.json')\n```",tech:"node",displayNumber:58,sortKey:1810},{id:"node-1811",number:59,title:"How do you tail a growing log file?",question:"How do you tail a growing log file?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Track the last read position, watch the file for changes, and read only the new bytes with a read stream starting at that offset, handling truncation/rotation.\n\n```js\nfs.watch('app.log', () => readFrom(lastPos))\n```",tech:"node",displayNumber:59,sortKey:1811},{id:"node-1812",number:60,title:"How do you handle file locking in Node?",question:"How do you handle file locking in Node?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Node has no built-in advisory locks; use exclusive create (`flag: 'wx'`) as a lock file, an OS lock via a native module (`proper-lockfile`), or coordinate through a database.\n\n```js\nawait fs.writeFile('app.lock', String(process.pid), { flag: 'wx' })\n```",tech:"node",displayNumber:60,sortKey:1812},{id:"node-1813",number:61,title:"What is the `glob` pattern and how do you match files?",question:"What is the `glob` pattern and how do you match files?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Globs (`**/*.js`) match file paths by pattern. Node's `fs` now offers `fs.glob`, or use libraries like `fast-glob` for recursive matching with ignore rules.\n\n```js\nfor await (const f of fs.glob('src/**/*.test.js')) console.log(f)\n```",tech:"node",displayNumber:61,sortKey:1813},{id:"node-1814",number:62,title:"How do you recursively walk a directory tree?",question:"How do you recursively walk a directory tree?",difficulty:"Advanced",category:"File System, Process & OS",answer:"Use `fs.readdir(dir, { withFileTypes: true, recursive: true })` (modern) or write a recursive function that descends into subdirectories, guarding against symlink loops.\n\n```js\nconst files = await fs.readdir('src', { recursive: true })\n```",tech:"node",displayNumber:62,sortKey:1814},{id:"node-1815",number:63,title:"How do you handle EMFILE (too many open files)?",question:"How do you handle EMFILE (too many open files)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Limit concurrent file operations (a queue/pool), always close descriptors, and optionally raise the OS `ulimit`. EMFILE means you exhausted the descriptor limit by opening files faster than closing.\n\n```js\nconst limit = pLimit(50)\nawait Promise.all(files.map(f => limit(() => fs.readFile(f))))\n```",tech:"node",displayNumber:63,sortKey:1815},{id:"node-1816",number:64,title:"What is the difference between hard links and symbolic links?",question:"What is the difference between hard links and symbolic links?",difficulty:"Advanced",category:"File System, Process & OS",answer:"A hard link is another name for the same inode/data (both must be deleted to free it). A symlink is a pointer to a path that can dangle if the target is removed. `fs.link` vs `fs.symlink` create them.\n\n```js\nawait fs.symlink('target.txt', 'alias.txt')\n```",tech:"node",displayNumber:64,sortKey:1816},{id:"node-1817",number:65,title:"How do you stream-copy with progress reporting?",question:"How do you stream-copy with progress reporting?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Pipe a read stream through a counting Transform to a write stream, emitting progress from bytes transferred vs total file size.\n\n```js\nread.on('data', c => { copied += c.length; report(copied / total) })\n```",tech:"node",displayNumber:65,sortKey:1817},{id:"node-1818",number:66,title:"What are `process.stdout.columns` / `rows` used for?",question:"What are `process.stdout.columns` / `rows` used for?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"They report terminal dimensions (when stdout is a TTY), letting CLIs wrap text, draw progress bars, or lay out tables. They're undefined when output is piped.\n\n```js\nconst width = process.stdout.columns ?? 80\n```",tech:"node",displayNumber:66,sortKey:1818},{id:"node-1819",number:67,title:"How do you gracefully restart workers on deploy (zero-downtime)?",question:"How do you gracefully restart workers on deploy (zero-downtime)?",difficulty:"Experienced",category:"File System, Process & OS",answer:"Fork new workers, wait until they're listening, then signal old workers to stop accepting new connections and exit after draining. Process managers (pm2 reload) automate this rolling restart.\n\n```js\npm2 reload app // zero-downtime rolling restart\n```",tech:"node",displayNumber:67,sortKey:1819},{id:"node-1820",number:68,title:"How do you handle `SIGKILL` vs `SIGTERM`?",question:"How do you handle `SIGKILL` vs `SIGTERM`?",difficulty:"Advanced",category:"File System, Process & OS",answer:"`SIGTERM` is catchable and requests graceful shutdown; `SIGKILL` (and `SIGSTOP`) cannot be caught or handled — the OS terminates immediately. Design cleanup around SIGTERM; treat SIGKILL as a hard stop.\n\n```js\nprocess.on('SIGTERM', shutdown) // SIGKILL can't be intercepted\n```",tech:"node",displayNumber:68,sortKey:1820},{id:"node-1821",number:69,title:"How do you detect if code runs in the primary or a worker?",question:"How do you detect if code runs in the primary or a worker?",difficulty:"Intermediate",category:"File System, Process & OS",answer:"Use `cluster.isPrimary` (formerly `isMaster`) / `cluster.isWorker`, or in worker threads check `worker_threads.isMainThread`.\n\n```js\nif (cluster.isPrimary) setupWorkers(); else runServer()\n```",tech:"node",displayNumber:69,sortKey:1821},{id:"node-1822",number:70,title:"What is `process.report` used for?",question:"What is `process.report` used for?",difficulty:"Experienced",category:"File System, Process & OS",answer:"`process.report` generates a diagnostic report (JSON) of the process state — stack traces, heap stats, resource usage, OS info — on demand or on crashes/signals, aiding post-mortem debugging in production.\n\n```js\nprocess.report.writeReport('./diagnostic.json')\n```",tech:"node",displayNumber:70,sortKey:1822},{id:"node-1823",number:1,title:"How do you create an HTTP server in Node?",question:"How do you create an HTTP server in Node?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use the `http` module's `createServer`, passing a handler `(req, res)`, then call `listen(port)`. `req` is a readable stream, `res` is a writable stream.\n\n```js\nrequire('http').createServer((req, res) => res.end('Hello')).listen(3000)\n```",tech:"node",displayNumber:1,sortKey:1823},{id:"node-1824",number:2,title:"What are `req` and `res` in an HTTP handler?",question:"What are `req` and `res` in an HTTP handler?",difficulty:"Basic",category:"HTTP & Networking",answer:"`req` is an `IncomingMessage` (readable stream) with method, url, and headers. `res` is a `ServerResponse` (writable stream) used to set status/headers and send the body.\n\n```js\n(req, res) => { console.log(req.method, req.url); res.end() }\n```",tech:"node",displayNumber:2,sortKey:1824},{id:"node-1825",number:3,title:"How do you read the request body?",question:"How do you read the request body?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The body arrives as stream chunks. Collect `data` events into buffers and concatenate on `end`, then parse. Frameworks/`body-parser` do this for you with size limits.\n\n```js\nconst chunks = []\nreq.on('data', c => chunks.push(c)).on('end', () => {\n  const body = Buffer.concat(chunks).toString()\n})\n```",tech:"node",displayNumber:3,sortKey:1825},{id:"node-1826",number:4,title:"How do you send a JSON response?",question:"How do you send a JSON response?",difficulty:"Basic",category:"HTTP & Networking",answer:"Set `Content-Type: application/json`, then write `JSON.stringify(data)`. Set an appropriate status code first.\n\n```js\nres.writeHead(200, { 'Content-Type': 'application/json' })\nres.end(JSON.stringify({ ok: true }))\n```",tech:"node",displayNumber:4,sortKey:1826},{id:"node-1827",number:5,title:"What are HTTP status codes and their categories?",question:"What are HTTP status codes and their categories?",difficulty:"Basic",category:"HTTP & Networking",answer:"1xx informational, 2xx success (200 OK, 201 Created), 3xx redirect (301, 304), 4xx client error (400, 401, 403, 404), 5xx server error (500, 503). Use them accurately so clients/caches behave correctly.\n\n```js\nres.statusCode = 404; res.end('Not Found')\n```",tech:"node",displayNumber:5,sortKey:1827},{id:"node-1828",number:6,title:"What is the difference between 401 and 403?",question:"What is the difference between 401 and 403?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"401 Unauthorized means authentication is missing/invalid (who are you?). 403 Forbidden means you're authenticated but lack permission (you can't do this).\n\n```js\nif (!user) return res.status(401).end()\nif (!user.isAdmin) return res.status(403).end()\n```",tech:"node",displayNumber:6,sortKey:1828},{id:"node-1829",number:7,title:"How do you set response headers?",question:"How do you set response headers?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use `res.setHeader(name, value)` before sending the body, or pass a headers object to `res.writeHead`. Headers can't change after the first byte of the body is written.\n\n```js\nres.setHeader('Cache-Control', 'no-store')\n```",tech:"node",displayNumber:7,sortKey:1829},{id:"node-1830",number:8,title:"How do you make an outbound HTTP request in Node?",question:"How do you make an outbound HTTP request in Node?",difficulty:"Basic",category:"HTTP & Networking",answer:"Use the global `fetch` (built into modern Node), or the lower-level `http`/`https` modules, or libraries like `axios`/`undici`.\n\n```js\nconst data = await fetch('https://api.example.com').then(r => r.json())\n```",tech:"node",displayNumber:8,sortKey:1830},{id:"node-1831",number:9,title:"What is `undici`?",question:"What is `undici`?",difficulty:"Advanced",category:"HTTP & Networking",answer:"`undici` is Node's modern, high-performance HTTP/1.1 client (the engine behind global `fetch`). It offers connection pooling, pipelining, and low overhead compared to the legacy `http` client.\n\n```js\nimport { request } from 'undici'\nconst { body } = await request('https://api.example.com')\n```",tech:"node",displayNumber:9,sortKey:1831},{id:"node-1832",number:10,title:"How do you handle query parameters?",question:"How do you handle query parameters?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Parse the URL with the `URL`/`URLSearchParams` API. `req.url` is just a path+query string; construct a full URL to read `searchParams`.\n\n```js\nconst url = new URL(req.url, `http://${req.headers.host}`)\nconst page = url.searchParams.get('page')\n```",tech:"node",displayNumber:10,sortKey:1832},{id:"node-1833",number:11,title:"What is the `URL` and `URLSearchParams` API?",question:"What is the `URL` and `URLSearchParams` API?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The WHATWG `URL` class parses and builds URLs (protocol, host, pathname, search). `URLSearchParams` parses/serializes query strings and handles encoding, replacing the legacy `url.parse`/`querystring`.\n\n```js\nconst u = new URL('https://x.com/a?b=1&b=2')\nu.searchParams.getAll('b') // ['1','2']\n```",tech:"node",displayNumber:11,sortKey:1833},{id:"node-1834",number:12,title:"How do you handle different HTTP methods without a framework?",question:"How do you handle different HTTP methods without a framework?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Branch on `req.method` and `req.url` in the handler. This is verbose, which is why routers/frameworks (Express) exist.\n\n```js\nif (req.method === 'POST' && req.url === '/users') createUser(req, res)\n```",tech:"node",displayNumber:12,sortKey:1834},{id:"node-1835",number:13,title:"What is the difference between HTTP/1.1 and HTTP/2?",question:"What is the difference between HTTP/1.1 and HTTP/2?",difficulty:"Advanced",category:"HTTP & Networking",answer:"HTTP/2 multiplexes many requests over one connection (no head-of-line blocking at HTTP level), compresses headers (HPACK), and supports server push. HTTP/1.1 uses one request per connection (or limited pipelining). Node's `http2` module supports it.\n\n```js\nrequire('http2').createSecureServer(opts, handler).listen(443)\n```",tech:"node",displayNumber:13,sortKey:1835},{id:"node-1836",number:14,title:"What is keep-alive and why does it matter?",question:"What is keep-alive and why does it matter?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Keep-alive reuses a TCP connection for multiple requests, avoiding repeated handshake overhead and improving throughput/latency. Node clients should enable an agent with keep-alive for outbound calls.\n\n```js\nconst agent = new http.Agent({ keepAlive: true })\n```",tech:"node",displayNumber:14,sortKey:1836},{id:"node-1837",number:15,title:"What is an HTTP Agent and connection pooling?",question:"What is an HTTP Agent and connection pooling?",difficulty:"Experienced",category:"HTTP & Networking",answer:"An Agent manages a pool of sockets for outbound requests, reusing connections (keep-alive) and limiting concurrency (`maxSockets`). Proper pooling prevents socket exhaustion under load.\n\n```js\nnew http.Agent({ keepAlive: true, maxSockets: 100 })\n```",tech:"node",displayNumber:15,sortKey:1837},{id:"node-1838",number:16,title:"How do you handle timeouts on HTTP requests?",question:"How do you handle timeouts on HTTP requests?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Set a request/socket timeout so slow or dead peers don't hang forever. With `fetch`, use `AbortSignal.timeout`; with `http`, set `req.setTimeout` and handle the `timeout` event.\n\n```js\nawait fetch(url, { signal: AbortSignal.timeout(5000) })\n```",tech:"node",displayNumber:16,sortKey:1838},{id:"node-1839",number:17,title:"How do you set server-side timeouts to protect against slow clients?",question:"How do you set server-side timeouts to protect against slow clients?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Configure `server.requestTimeout`, `server.headersTimeout`, and `server.keepAliveTimeout` to bound how long clients can hold connections, mitigating slowloris-style attacks.\n\n```js\nserver.requestTimeout = 30000\nserver.headersTimeout = 10000\n```",tech:"node",displayNumber:17,sortKey:1839},{id:"node-1840",number:18,title:"What is a slowloris attack?",question:"What is a slowloris attack?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Slowloris opens many connections and sends headers/body extremely slowly, tying up server resources until it can't serve legitimate clients. Mitigate with header/request timeouts, connection limits, and a reverse proxy.\n\n```js\nserver.headersTimeout = 5000 // drop slow header senders\n```",tech:"node",displayNumber:18,sortKey:1840},{id:"node-1841",number:19,title:"How do you implement graceful shutdown of an HTTP server?",question:"How do you implement graceful shutdown of an HTTP server?",difficulty:"Experienced",category:"HTTP & Networking",answer:"On SIGTERM, call `server.close()` to stop accepting new connections and wait for in-flight requests to finish, then close DB connections and exit. Add a timeout to force-close lingering keep-alive sockets.\n\n```js\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```",tech:"node",displayNumber:19,sortKey:1841},{id:"node-1842",number:20,title:"What is CORS and how do you enable it?",question:"What is CORS and how do you enable it?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"CORS (Cross-Origin Resource Sharing) lets a browser call an API on a different origin, controlled by `Access-Control-*` response headers. The server whitelists origins/methods/headers; preflight `OPTIONS` requests check permission first.\n\n```js\nres.setHeader('Access-Control-Allow-Origin', 'https://app.com')\n```",tech:"node",displayNumber:20,sortKey:1842},{id:"node-1843",number:21,title:"What is a CORS preflight request?",question:"What is a CORS preflight request?",difficulty:"Advanced",category:"HTTP & Networking",answer:"For \"non-simple\" requests (custom headers, methods like PUT/DELETE), the browser first sends an `OPTIONS` preflight asking whether the actual request is allowed. The server must respond with the appropriate `Access-Control-Allow-*` headers.\n\n```js\nif (req.method === 'OPTIONS') { res.writeHead(204, corsHeaders).end() }\n```",tech:"node",displayNumber:21,sortKey:1843},{id:"node-1844",number:22,title:"How do you serve static files?",question:"How do you serve static files?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Map the URL to a safe file path (guard against traversal), stream the file to `res` with the right `Content-Type`, and set caching headers. Express uses `express.static`.\n\n```js\napp.use(express.static('public', { maxAge: '1d' }))\n```",tech:"node",displayNumber:22,sortKey:1844},{id:"node-1845",number:23,title:"How do you handle file downloads with correct headers?",question:"How do you handle file downloads with correct headers?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Set `Content-Disposition: attachment; filename=...`, the correct `Content-Type`, and stream the file. `Content-Length` (if known) enables progress bars.\n\n```js\nres.setHeader('Content-Disposition', 'attachment; filename=\"report.pdf\"')\nfs.createReadStream(path).pipe(res)\n```",tech:"node",displayNumber:23,sortKey:1845},{id:"node-1846",number:24,title:"What is chunked transfer encoding?",question:"What is chunked transfer encoding?",difficulty:"Advanced",category:"HTTP & Networking",answer:"When the total body size is unknown, the server sends it in chunks with `Transfer-Encoding: chunked` and no `Content-Length`. Piping a stream to `res` uses this automatically.\n\n```js\nstream.pipe(res) // Transfer-Encoding: chunked\n```",tech:"node",displayNumber:24,sortKey:1846},{id:"node-1847",number:25,title:"How do you implement Server-Sent Events (SSE)?",question:"How do you implement Server-Sent Events (SSE)?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Respond with `Content-Type: text/event-stream`, keep the connection open, and write `data: ...\\n\\n` messages. SSE is a simple one-way server→client push over HTTP.\n\n```js\nres.writeHead(200, { 'Content-Type': 'text/event-stream', Connection: 'keep-alive' })\nsetInterval(() => res.write(`data: ${Date.now()}\\n\\n`), 1000)\n```",tech:"node",displayNumber:25,sortKey:1847},{id:"node-1848",number:26,title:"What is the difference between SSE and WebSockets?",question:"What is the difference between SSE and WebSockets?",difficulty:"Advanced",category:"HTTP & Networking",answer:"SSE is one-way (server→client), text-only, over plain HTTP with auto-reconnect. WebSockets are full-duplex, binary-capable, over a dedicated upgraded connection. Use SSE for simple feeds, WebSockets for interactive/bidirectional apps.\n\n```js\n// SSE: EventSource; WebSocket: bidirectional ws://\n```",tech:"node",displayNumber:26,sortKey:1848},{id:"node-1849",number:27,title:"How do WebSockets work in Node?",question:"How do WebSockets work in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"A WebSocket starts as an HTTP `Upgrade` request, then switches to a persistent full-duplex TCP channel. Libraries like `ws` or `socket.io` handle the handshake and framing.\n\n```js\nconst wss = new (require('ws').Server)({ port: 8080 })\nwss.on('connection', ws => ws.on('message', m => ws.send('echo ' + m)))\n```",tech:"node",displayNumber:27,sortKey:1849},{id:"node-1850",number:28,title:"What is the WebSocket handshake?",question:"What is the WebSocket handshake?",difficulty:"Experienced",category:"HTTP & Networking",answer:"The client sends an HTTP request with `Upgrade: websocket` and a `Sec-WebSocket-Key`; the server responds 101 Switching Protocols with a derived `Sec-WebSocket-Accept`. After that, both sides exchange WebSocket frames.\n\n```js\nserver.on('upgrade', (req, socket, head) => wss.handleUpgrade(req, socket, head, cb))\n```",tech:"node",displayNumber:28,sortKey:1850},{id:"node-1851",number:29,title:"How do you create a raw TCP server?",question:"How do you create a raw TCP server?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use the `net` module's `createServer`, which gives a Duplex socket per connection for reading/writing raw bytes — the foundation beneath HTTP.\n\n```js\nrequire('net').createServer(sock => sock.pipe(sock)).listen(9000) // echo\n```",tech:"node",displayNumber:29,sortKey:1851},{id:"node-1852",number:30,title:"What is the difference between TCP and UDP in Node?",question:"What is the difference between TCP and UDP in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"`net` provides reliable, ordered TCP streams. `dgram` provides UDP: connectionless, unordered, no delivery guarantee, but low latency — good for telemetry, gaming, DNS.\n\n```js\nconst udp = require('dgram').createSocket('udp4')\nudp.send('ping', 41234, 'host')\n```",tech:"node",displayNumber:30,sortKey:1852},{id:"node-1853",number:31,title:"How do you handle DNS lookups?",question:"How do you handle DNS lookups?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"The `dns` module resolves hostnames. `dns.lookup` uses the OS resolver (thread pool), while `dns.resolve*` query DNS servers directly. Caching lookups reduces latency.\n\n```js\nconst { address } = await require('dns').promises.lookup('example.com')\n```",tech:"node",displayNumber:31,sortKey:1853},{id:"node-1854",number:32,title:"Why can `dns.lookup` block the thread pool?",question:"Why can `dns.lookup` block the thread pool?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`dns.lookup` calls the synchronous OS resolver (`getaddrinfo`) on a libuv thread-pool thread. Many concurrent lookups can saturate the default 4-thread pool, delaying other pool work (fs, crypto). Increase `UV_THREADPOOL_SIZE` or cache DNS.\n\n```js\nprocess.env.UV_THREADPOOL_SIZE = 16\n```",tech:"node",displayNumber:32,sortKey:1854},{id:"node-1855",number:33,title:"How do you implement HTTPS in Node?",question:"How do you implement HTTPS in Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use `https.createServer` with a TLS key and certificate. In production, TLS is often terminated at a reverse proxy/load balancer instead of in Node.\n\n```js\nhttps.createServer({ key, cert }, handler).listen(443)\n```",tech:"node",displayNumber:33,sortKey:1855},{id:"node-1856",number:34,title:"What is TLS termination and where should it happen?",question:"What is TLS termination and where should it happen?",difficulty:"Experienced",category:"HTTP & Networking",answer:"TLS termination is decrypting HTTPS traffic. It commonly happens at a load balancer/reverse proxy (nginx, ALB) which forwards plain HTTP to Node, simplifying cert management and offloading crypto from the app.\n\n```js\n// nginx terminates TLS -> proxies to http://localhost:3000\n```",tech:"node",displayNumber:34,sortKey:1856},{id:"node-1857",number:35,title:"How do you trust the `X-Forwarded-For` header safely?",question:"How do you trust the `X-Forwarded-For` header safely?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Behind a proxy, the client IP is in `X-Forwarded-For`, but it's spoofable if you trust it blindly. Configure trusted proxies (Express `trust proxy`) so only your infra's forwarded values are honored.\n\n```js\napp.set('trust proxy', 'loopback, 10.0.0.0/8')\n```",tech:"node",displayNumber:35,sortKey:1857},{id:"node-1858",number:36,title:"How do you handle large request bodies safely?",question:"How do you handle large request bodies safely?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Enforce a max body size and reject/destroy oversized requests to prevent memory exhaustion. Body parsers accept a `limit` option; for streaming uploads, count bytes and abort past the limit.\n\n```js\napp.use(express.json({ limit: '1mb' }))\n```",tech:"node",displayNumber:36,sortKey:1858},{id:"node-1859",number:37,title:"What is HTTP pipelining?",question:"What is HTTP pipelining?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Pipelining sends multiple HTTP/1.1 requests without waiting for responses, which must return in order (causing head-of-line blocking). Rarely used; HTTP/2 multiplexing solved the underlying need.\n\n```js\n// HTTP/2 multiplexing supersedes HTTP/1.1 pipelining\n```",tech:"node",displayNumber:37,sortKey:1859},{id:"node-1860",number:38,title:"How do you proxy a request through Node?",question:"How do you proxy a request through Node?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Forward the incoming request to an upstream and pipe the upstream response back, copying relevant headers. Libraries like `http-proxy` handle edge cases (websockets, headers, errors).\n\n```js\nconst upstream = http.request(target, up => up.pipe(res))\nreq.pipe(upstream)\n```",tech:"node",displayNumber:38,sortKey:1860},{id:"node-1861",number:39,title:"What are hop-by-hop vs end-to-end headers?",question:"What are hop-by-hop vs end-to-end headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Hop-by-hop headers (`Connection`, `Keep-Alive`, `Transfer-Encoding`) apply to a single connection and must not be forwarded by proxies. End-to-end headers are passed through unchanged. Proxies must strip hop-by-hop headers.\n\n```js\ndelete headers['connection']; delete headers['transfer-encoding']\n```",tech:"node",displayNumber:39,sortKey:1861},{id:"node-1862",number:40,title:"How do you implement rate limiting?",question:"How do you implement rate limiting?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Track request counts per client (IP/key) in a window using a counter store (in-memory for one instance, Redis for many), and return 429 when the limit is exceeded. Algorithms: fixed window, sliding window, token bucket.\n\n```js\napp.use(rateLimit({ windowMs: 60000, max: 100 }))\n```",tech:"node",displayNumber:40,sortKey:1862},{id:"node-1863",number:41,title:"What is the token bucket algorithm?",question:"What is the token bucket algorithm?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Tokens refill at a fixed rate up to a capacity; each request consumes one. Requests proceed if a token is available, else are throttled. It permits bursts up to the bucket size while enforcing an average rate.\n\n```js\ntokens = Math.min(cap, tokens + elapsed * rate); if (tokens >= 1) { tokens--; allow() }\n```",tech:"node",displayNumber:41,sortKey:1863},{id:"node-1864",number:42,title:"How do you implement retries with idempotency?",question:"How do you implement retries with idempotency?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Retry only idempotent operations (GET/PUT/DELETE) or use an idempotency key so the server dedupes repeated POSTs. Retrying non-idempotent calls blindly can duplicate side effects.\n\n```js\nfetch(url, { method: 'POST', headers: { 'Idempotency-Key': uuid } })\n```",tech:"node",displayNumber:42,sortKey:1864},{id:"node-1865",number:43,title:"What is a circuit breaker?",question:"What is a circuit breaker?",difficulty:"Experienced",category:"HTTP & Networking",answer:"A circuit breaker stops calling a failing dependency after a threshold of errors (open state), failing fast to protect resources, then periodically probes (half-open) before resuming (closed). It prevents cascading failures.\n\n```js\nif (breaker.isOpen()) throw new Error('circuit open')\n```",tech:"node",displayNumber:43,sortKey:1865},{id:"node-1866",number:44,title:"How do you handle streaming responses from an upstream API?",question:"How do you handle streaming responses from an upstream API?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Pipe the upstream response stream directly to your client response so you don't buffer the whole payload, preserving low memory and latency.\n\n```js\nconst upstream = await fetch(url)\nReadable.fromWeb(upstream.body).pipe(res)\n```",tech:"node",displayNumber:44,sortKey:1866},{id:"node-1867",number:45,title:"What is `Content-Type` negotiation?",question:"What is `Content-Type` negotiation?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Content negotiation picks a response format based on the client's `Accept` header (JSON, XML, HTML). The server inspects `Accept` and responds with the best supported representation and matching `Content-Type`.\n\n```js\nres.format({ json: () => res.json(data), html: () => res.render('view', data) })\n```",tech:"node",displayNumber:45,sortKey:1867},{id:"node-1868",number:46,title:"How do you set cookies from a Node server?",question:"How do you set cookies from a Node server?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"Send a `Set-Cookie` header with attributes like `HttpOnly`, `Secure`, `SameSite`, `Max-Age`, and `Path`. `HttpOnly`+`Secure`+`SameSite` protect against XSS/CSRF theft.\n\n```js\nres.setHeader('Set-Cookie', 'sid=abc; HttpOnly; Secure; SameSite=Strict')\n```",tech:"node",displayNumber:46,sortKey:1868},{id:"node-1869",number:47,title:"What does the `SameSite` cookie attribute do?",question:"What does the `SameSite` cookie attribute do?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`SameSite` controls whether cookies are sent on cross-site requests: `Strict` (never), `Lax` (top-level navigations only), `None` (always, requires `Secure`). It mitigates CSRF.\n\n```js\n'session=x; SameSite=Lax; Secure'\n```",tech:"node",displayNumber:47,sortKey:1869},{id:"node-1870",number:48,title:"How do you compress HTTP responses?",question:"How do you compress HTTP responses?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Check the client's `Accept-Encoding`, pipe the body through gzip/brotli, and set `Content-Encoding`. Middleware (`compression`) automates this; often done at the proxy instead.\n\n```js\napp.use(require('compression')())\n```",tech:"node",displayNumber:48,sortKey:1870},{id:"node-1871",number:49,title:"What is ETag and conditional requests?",question:"What is ETag and conditional requests?",difficulty:"Experienced",category:"HTTP & Networking",answer:"An `ETag` is a content fingerprint. Clients send `If-None-Match`; if unchanged, the server returns `304 Not Modified` with no body, saving bandwidth. `Last-Modified`/`If-Modified-Since` work similarly by date.\n\n```js\nif (req.headers['if-none-match'] === etag) return res.writeHead(304).end()\n```",tech:"node",displayNumber:49,sortKey:1871},{id:"node-1872",number:50,title:"How do you implement HTTP caching headers?",question:"How do you implement HTTP caching headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Use `Cache-Control` (`max-age`, `no-store`, `private`, `immutable`) to instruct browsers/CDNs, plus validators (`ETag`, `Last-Modified`) for revalidation. Correct caching cuts load and latency.\n\n```js\nres.setHeader('Cache-Control', 'public, max-age=31536000, immutable')\n```",tech:"node",displayNumber:50,sortKey:1872},{id:"node-1873",number:51,title:"What is a reverse proxy and why use one in front of Node?",question:"What is a reverse proxy and why use one in front of Node?",difficulty:"Experienced",category:"HTTP & Networking",answer:"A reverse proxy (nginx, HAProxy) sits in front of Node to terminate TLS, serve static files, load-balance across instances, buffer slow clients, and add caching/compression — offloading concerns from the app.\n\n```js\n// nginx: proxy_pass http://localhost:3000\n```",tech:"node",displayNumber:51,sortKey:1873},{id:"node-1874",number:52,title:"How do you handle sticky sessions behind a load balancer?",question:"How do you handle sticky sessions behind a load balancer?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Sticky sessions pin a client to one instance (via cookie/IP hash) so in-memory session state works. Better: make servers stateless with a shared session store (Redis) so any instance can serve any request.\n\n```js\napp.use(session({ store: new RedisStore({ client }) })) // no stickiness needed\n```",tech:"node",displayNumber:52,sortKey:1874},{id:"node-1875",number:53,title:"What is HTTP long polling?",question:"What is HTTP long polling?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Long polling holds a request open until the server has data (or a timeout), then the client immediately re-requests. It simulates push over plain HTTP but is less efficient than SSE/WebSockets.\n\n```js\n// server holds res until an event, then res.json(update)\n```",tech:"node",displayNumber:53,sortKey:1875},{id:"node-1876",number:54,title:"How do you validate and sanitize incoming request data?",question:"How do you validate and sanitize incoming request data?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Validate structure/types against a schema (zod, Joi) at the boundary, reject invalid input with 400, and sanitize to prevent injection. Never trust client data.\n\n```js\nconst body = schema.parse(req.body) // throws on invalid input\n```",tech:"node",displayNumber:54,sortKey:1876},{id:"node-1877",number:55,title:"What is the difference between `res.end` and `res.write`?",question:"What is the difference between `res.end` and `res.write`?",difficulty:"Intermediate",category:"HTTP & Networking",answer:"`res.write` sends a body chunk and keeps the response open; `res.end` optionally sends a final chunk and closes the response. You can call `write` many times then `end` once.\n\n```js\nres.write('part1'); res.write('part2'); res.end()\n```",tech:"node",displayNumber:55,sortKey:1877},{id:"node-1878",number:56,title:'How do you avoid "headers already sent" errors?',question:'How do you avoid "headers already sent" errors?',difficulty:"Advanced",category:"HTTP & Networking",answer:"Once the body/headers are sent, don't set headers or send another response. Guard with `res.headersSent`, and always `return` after sending to avoid falling through to more response code.\n\n```js\nif (err) return res.status(500).end() // return prevents double-send\n```",tech:"node",displayNumber:56,sortKey:1878},{id:"node-1879",number:57,title:"How do you stream a video with range support?",question:"How do you stream a video with range support?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Parse the `Range` header, respond with 206 Partial Content, `Content-Range`, and `Accept-Ranges: bytes`, then stream the requested byte slice. This enables seeking in media players.\n\n```js\nres.writeHead(206, { 'Content-Range': `bytes ${start}-${end}/${size}` })\nfs.createReadStream(file, { start, end }).pipe(res)\n```",tech:"node",displayNumber:57,sortKey:1879},{id:"node-1880",number:58,title:"What is HSTS?",question:"What is HSTS?",difficulty:"Experienced",category:"HTTP & Networking",answer:"HTTP Strict Transport Security (`Strict-Transport-Security` header) tells browsers to only use HTTPS for your domain for a duration, preventing protocol-downgrade and SSL-strip attacks.\n\n```js\nres.setHeader('Strict-Transport-Security', 'max-age=31536000; includeSubDomains')\n```",tech:"node",displayNumber:58,sortKey:1880},{id:"node-1881",number:59,title:"How do you handle multipart/form-data uploads?",question:"How do you handle multipart/form-data uploads?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Parse the multipart body with a streaming parser (`busboy`, `multer`) that separates fields and files, streaming files to storage rather than buffering. Enforce size/type limits.\n\n```js\napp.post('/upload', multer({ dest: 'uploads/' }).single('file'), handler)\n```",tech:"node",displayNumber:59,sortKey:1881},{id:"node-1882",number:60,title:"What is `keepAliveTimeout` and why tune it behind a load balancer?",question:"What is `keepAliveTimeout` and why tune it behind a load balancer?",difficulty:"Experienced",category:"HTTP & Networking",answer:"`keepAliveTimeout` is how long Node keeps an idle connection open. It should be slightly longer than the upstream load balancer's idle timeout to avoid races where the LB reuses a socket Node just closed (causing 502s).\n\n```js\nserver.keepAliveTimeout = 65000 // > ALB's 60s\n```",tech:"node",displayNumber:60,sortKey:1882},{id:"node-1883",number:61,title:"How do you detect and handle a dropped client connection?",question:"How do you detect and handle a dropped client connection?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Listen for `req.on('close')` / `res.on('close')` (or an AbortSignal) to know the client disconnected, then stop expensive work and release resources for that request.\n\n```js\nreq.on('close', () => controller.abort()) // cancel work if client left\n```",tech:"node",displayNumber:61,sortKey:1883},{id:"node-1884",number:62,title:"What is HTTP/3 and QUIC?",question:"What is HTTP/3 and QUIC?",difficulty:"Experienced",category:"HTTP & Networking",answer:"HTTP/3 runs over QUIC (a UDP-based transport) instead of TCP, eliminating TCP head-of-line blocking, enabling faster connection setup (0-RTT), and better mobility. Node support is experimental/via proxies.\n\n```js\n// typically terminated at a CDN/edge; Node speaks HTTP/1.1 or /2 upstream\n```",tech:"node",displayNumber:62,sortKey:1884},{id:"node-1885",number:63,title:"How do you implement request tracing/correlation IDs?",question:"How do you implement request tracing/correlation IDs?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Generate or propagate a correlation ID (from `X-Request-Id`/`traceparent`) per request, store it in `AsyncLocalStorage`, and include it in all logs and downstream calls to trace a request across services.\n\n```js\nconst id = req.headers['x-request-id'] ?? crypto.randomUUID()\nals.run({ id }, () => next())\n```",tech:"node",displayNumber:63,sortKey:1885},{id:"node-1886",number:64,title:"How do you secure HTTP headers?",question:"How do you secure HTTP headers?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Set defensive headers: `Content-Security-Policy`, `X-Content-Type-Options: nosniff`, `X-Frame-Options`, `Referrer-Policy`, HSTS. Middleware like `helmet` applies sensible defaults.\n\n```js\napp.use(require('helmet')())\n```",tech:"node",displayNumber:64,sortKey:1886},{id:"node-1887",number:65,title:"What is `Content-Security-Policy`?",question:"What is `Content-Security-Policy`?",difficulty:"Experienced",category:"HTTP & Networking",answer:"CSP restricts which sources scripts/styles/images can load from, mitigating XSS and data injection. It's declared via a header listing allowed origins per resource type.\n\n```js\nres.setHeader('Content-Security-Policy', \"default-src 'self'\")\n```",tech:"node",displayNumber:65,sortKey:1887},{id:"node-1888",number:66,title:"How do you handle concurrent connections limit?",question:"How do you handle concurrent connections limit?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Set `server.maxConnections`, use a reverse proxy for buffering, scale horizontally with cluster/instances, and cap outbound `maxSockets`. Monitor `server.getConnections()` to observe load.\n\n```js\nserver.maxConnections = 10000\n```",tech:"node",displayNumber:66,sortKey:1888},{id:"node-1889",number:67,title:"What is the difference between `http` and `https` Agent?",question:"What is the difference between `http` and `https` Agent?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Both pool sockets, but the `https.Agent` also manages TLS session parameters and can reuse TLS sessions. Use the matching agent for the protocol; mixing them breaks connection reuse.\n\n```js\nconst agent = new https.Agent({ keepAlive: true })\n```",tech:"node",displayNumber:67,sortKey:1889},{id:"node-1890",number:68,title:"How do you make outbound requests resilient?",question:"How do you make outbound requests resilient?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Combine timeouts, retries with backoff+jitter (idempotent only), circuit breakers, connection pooling, and bulkheads (concurrency limits). This prevents a slow dependency from taking down your service.\n\n```js\nawait retry(() => fetch(url, { signal: AbortSignal.timeout(3000) }), { retries: 3 })\n```",tech:"node",displayNumber:68,sortKey:1890},{id:"node-1891",number:69,title:"How do you test HTTP endpoints?",question:"How do you test HTTP endpoints?",difficulty:"Advanced",category:"HTTP & Networking",answer:"Use `supertest` (or Node's `fetch` against an ephemeral port) to send requests to the app instance and assert on status, headers, and body — without a real network or fixed port.\n\n```js\nawait request(app).get('/health').expect(200)\n```",tech:"node",displayNumber:69,sortKey:1891},{id:"node-1892",number:70,title:"How do you detect and prevent SSRF?",question:"How do you detect and prevent SSRF?",difficulty:"Experienced",category:"HTTP & Networking",answer:"Server-Side Request Forgery tricks your server into fetching internal/unintended URLs. Prevent by validating/whitelisting outbound hosts, blocking private IP ranges and redirects to them, and never fetching raw user-supplied URLs.\n\n```js\nif (isPrivateIp(new URL(userUrl).hostname)) throw new Error('blocked')\n```",tech:"node",displayNumber:70,sortKey:1892},{id:"node-1893",number:1,title:"What is Express.js?",question:"What is Express.js?",difficulty:"Basic",category:"Express & REST APIs",answer:"Express is a minimal, unopinionated web framework for Node that provides routing, middleware, and helpers over the raw `http` module, simplifying building APIs and web apps.\n\n```js\nconst app = require('express')()\napp.get('/', (req, res) => res.send('Hello'))\napp.listen(3000)\n```",tech:"node",displayNumber:1,sortKey:1893},{id:"node-1894",number:2,title:"What is middleware in Express?",question:"What is middleware in Express?",difficulty:"Basic",category:"Express & REST APIs",answer:"Middleware are functions `(req, res, next)` that run in order on the request pipeline. They can modify req/res, end the response, or call `next()` to pass control. Used for logging, auth, parsing, etc.\n\n```js\napp.use((req, res, next) => { req.time = Date.now(); next() })\n```",tech:"node",displayNumber:2,sortKey:1894},{id:"node-1895",number:3,title:"What is the role of `next()`?",question:"What is the role of `next()`?",difficulty:"Basic",category:"Express & REST APIs",answer:"`next()` passes control to the next middleware/route handler. `next(err)` skips to error-handling middleware. Forgetting to call `next()` or send a response leaves the request hanging.\n\n```js\napp.use((req, res, next) => auth(req) ? next() : res.status(401).end())\n```",tech:"node",displayNumber:3,sortKey:1895},{id:"node-1896",number:4,title:"What are the types of middleware?",question:"What are the types of middleware?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Application-level (`app.use`), router-level (`router.use`), built-in (`express.json`, `express.static`), third-party (`cors`, `helmet`), and error-handling (four-arg `(err, req, res, next)`).\n\n```js\napp.use(express.json())            // built-in\napp.use('/api', apiRouter)         // router-level\n```",tech:"node",displayNumber:4,sortKey:1896},{id:"node-1897",number:5,title:"How does Express match routes?",question:"How does Express match routes?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Express matches by HTTP method and path pattern in the order routes/middleware are registered. The first matching handler that responds (without `next`) wins. Order matters.\n\n```js\napp.get('/users/:id', handler) // matches GET /users/42\n```",tech:"node",displayNumber:5,sortKey:1897},{id:"node-1898",number:6,title:"What are route parameters?",question:"What are route parameters?",difficulty:"Basic",category:"Express & REST APIs",answer:"Route params are named URL segments (`:id`) captured into `req.params`. They identify resources in RESTful paths.\n\n```js\napp.get('/users/:id', (req, res) => res.send(req.params.id))\n```",tech:"node",displayNumber:6,sortKey:1898},{id:"node-1899",number:7,title:"What is the difference between `req.params`, `req.query`, and `req.body`?",question:"What is the difference between `req.params`, `req.query`, and `req.body`?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`req.params` holds URL path params (`/users/:id`), `req.query` holds the query string (`?page=2`), and `req.body` holds the parsed request body (needs a body-parsing middleware).\n\n```js\n// GET /users/5?full=true  body {name}\nreq.params.id // '5'\nreq.query.full // 'true'\n```",tech:"node",displayNumber:7,sortKey:1899},{id:"node-1900",number:8,title:"How do you parse JSON request bodies?",question:"How do you parse JSON request bodies?",difficulty:"Basic",category:"Express & REST APIs",answer:"Add `express.json()` middleware, which reads and parses `application/json` bodies into `req.body`. Set a `limit` to bound size.\n\n```js\napp.use(express.json({ limit: '1mb' }))\n```",tech:"node",displayNumber:8,sortKey:1900},{id:"node-1901",number:9,title:"What is a Router in Express?",question:"What is a Router in Express?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`express.Router()` is a mini-app for grouping related routes and middleware into a modular, mountable unit, keeping large apps organized.\n\n```js\nconst r = express.Router()\nr.get('/', list); app.use('/products', r)\n```",tech:"node",displayNumber:9,sortKey:1901},{id:"node-1902",number:10,title:"How do you handle errors in Express?",question:"How do you handle errors in Express?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Define error-handling middleware with four arguments `(err, req, res, next)` after all routes. Pass errors via `next(err)`; Express routes them to this handler.\n\n```js\napp.use((err, req, res, next) => res.status(err.status || 500).json({ error: err.message }))\n```",tech:"node",displayNumber:10,sortKey:1902},{id:"node-1903",number:11,title:"How do you handle errors in async route handlers?",question:"How do you handle errors in async route handlers?",difficulty:"Advanced",category:"Express & REST APIs",answer:"In Express 4, thrown errors in async handlers aren't caught automatically — wrap handlers to forward rejections to `next`, or use a wrapper/`express-async-errors`. Express 5 handles rejected promises natively.\n\n```js\nconst wrap = fn => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next)\napp.get('/x', wrap(async (req, res) => { await work(); res.end() }))\n```",tech:"node",displayNumber:11,sortKey:1903},{id:"node-1904",number:12,title:"What are the principles of REST?",question:"What are the principles of REST?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"REST is stateless, resource-oriented (nouns as URIs), uses standard HTTP methods/status codes, supports a uniform interface, and is cacheable. Each request carries all needed context; the server holds no client session state.\n\n```js\n// GET /orders/1  (resource), not GET /getOrder?id=1 (RPC)\n```",tech:"node",displayNumber:12,sortKey:1904},{id:"node-1905",number:13,title:"How do HTTP methods map to CRUD?",question:"How do HTTP methods map to CRUD?",difficulty:"Basic",category:"Express & REST APIs",answer:"GET=read, POST=create, PUT=replace/update, PATCH=partial update, DELETE=remove. Using them semantically enables caching, idempotency, and predictable APIs.\n\n```js\napp.post('/users', create)\napp.patch('/users/:id', update)\n```",tech:"node",displayNumber:13,sortKey:1905},{id:"node-1906",number:14,title:"What is idempotency and which methods are idempotent?",question:"What is idempotency and which methods are idempotent?",difficulty:"Advanced",category:"Express & REST APIs",answer:"An idempotent method produces the same result whether called once or many times. GET, PUT, DELETE, HEAD are idempotent; POST is not (each call creates a new resource). This matters for safe retries.\n\n```js\n// PUT /users/5 with same body -> same final state every time\n```",tech:"node",displayNumber:14,sortKey:1906},{id:"node-1907",number:15,title:"What is the difference between PUT and PATCH?",question:"What is the difference between PUT and PATCH?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"PUT replaces the entire resource (idempotent full update); PATCH applies a partial modification. Sending only changed fields to PUT can wipe omitted fields.\n\n```js\napp.put('/users/:id', replaceUser)   // full\napp.patch('/users/:id', patchUser)    // partial\n```",tech:"node",displayNumber:15,sortKey:1907},{id:"node-1908",number:16,title:"How do you version a REST API?",question:"How do you version a REST API?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Common approaches: URL prefix (`/v1/...`), a custom header, or media-type versioning (`Accept: application/vnd.api.v2+json`). Versioning lets you evolve without breaking existing clients.\n\n```js\napp.use('/v1', v1Router); app.use('/v2', v2Router)\n```",tech:"node",displayNumber:16,sortKey:1908},{id:"node-1909",number:17,title:"What status code should a successful POST return?",question:"What status code should a successful POST return?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"201 Created with a `Location` header pointing to the new resource (and often the created body). 200 is acceptable if no resource is created; 204 for success with no content.\n\n```js\nres.status(201).location(`/users/${id}`).json(user)\n```",tech:"node",displayNumber:17,sortKey:1909},{id:"node-1910",number:18,title:"How do you implement pagination?",question:"How do you implement pagination?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Offset-based (`?page&limit`) is simple but slow/inconsistent on large/changing data. Cursor-based (`?after=<id>`) is stable and efficient for large sets. Return metadata (next cursor/total).\n\n```js\nconst rows = await db.find({ _id: { $gt: cursor } }).limit(limit)\n```",tech:"node",displayNumber:18,sortKey:1910},{id:"node-1911",number:19,title:"Why is cursor pagination better than offset for large datasets?",question:"Why is cursor pagination better than offset for large datasets?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Offset requires the DB to scan and skip N rows (slow as N grows) and can skip/duplicate items when data changes between pages. Cursors seek directly via an indexed key, giving stable, O(log n) navigation.\n\n```js\n// WHERE id > :cursor ORDER BY id LIMIT :n  (uses index, no skip)\n```",tech:"node",displayNumber:19,sortKey:1911},{id:"node-1912",number:20,title:"How do you validate request input in Express?",question:"How do you validate request input in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Validate against a schema (zod/Joi/express-validator) in middleware, rejecting invalid requests with 400 and clear error details before they reach business logic.\n\n```js\napp.post('/u', (req, res, next) => { req.body = schema.parse(req.body); next() })\n```",tech:"node",displayNumber:20,sortKey:1912},{id:"node-1913",number:21,title:"How do you structure a scalable Express app?",question:"How do you structure a scalable Express app?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Layer by responsibility: routes/controllers (HTTP), services (business logic), repositories (data access), and organize by feature. Keep handlers thin, put logic in services, and centralize error/validation handling.\n\n```js\n// routes -> controller -> service -> repository -> db\n```",tech:"node",displayNumber:21,sortKey:1913},{id:"node-1914",number:22,title:"What is the difference between `app.use` and `app.get`?",question:"What is the difference between `app.use` and `app.get`?",difficulty:"Basic",category:"Express & REST APIs",answer:"`app.use` mounts middleware for all methods (and matches path prefixes). `app.get` (and other verbs) registers a handler for a specific method and exact-ish path.\n\n```js\napp.use('/api', logger)     // all methods under /api\napp.get('/api/users', list) // only GET\n```",tech:"node",displayNumber:22,sortKey:1914},{id:"node-1915",number:23,title:"How do you serve a REST API and a SPA together?",question:"How do you serve a REST API and a SPA together?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Mount API routes first, serve static assets, then add a catch-all that returns `index.html` for client-side routing (excluding API paths).\n\n```js\napp.use('/api', api)\napp.use(express.static('dist'))\napp.get('*', (req, res) => res.sendFile('index.html'))\n```",tech:"node",displayNumber:23,sortKey:1915},{id:"node-1916",number:24,title:"What is HATEOAS?",question:"What is HATEOAS?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Hypermedia As The Engine Of Application State: responses include links to related actions/resources, so clients navigate the API dynamically rather than hardcoding URLs. It is the highest REST maturity level, rarely fully adopted.\n\n```js\nres.json({ id, _links: { self: `/orders/${id}`, cancel: `/orders/${id}/cancel` } })\n```",tech:"node",displayNumber:24,sortKey:1916},{id:"node-1917",number:25,title:"How do you handle 404 for unknown routes?",question:"How do you handle 404 for unknown routes?",difficulty:"Basic",category:"Express & REST APIs",answer:"Add a final catch-all middleware after all routes that responds with 404, since a request reaching it matched nothing earlier.\n\n```js\napp.use((req, res) => res.status(404).json({ error: 'Not found' }))\n```",tech:"node",displayNumber:25,sortKey:1917},{id:"node-1918",number:26,title:"What is CORS middleware and how do you configure it?",question:"What is CORS middleware and how do you configure it?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"The `cors` middleware sets `Access-Control-*` headers and handles preflight. Configure allowed origins, methods, and credentials explicitly rather than using a wildcard with credentials.\n\n```js\napp.use(cors({ origin: 'https://app.com', credentials: true }))\n```",tech:"node",displayNumber:26,sortKey:1918},{id:"node-1919",number:27,title:"How do you implement authentication middleware?",question:"How do you implement authentication middleware?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Write middleware that reads a token/cookie, verifies it, attaches the user to `req`, and calls `next()` or returns 401. Apply it to protected routers.\n\n```js\nfunction auth(req, res, next) {\n  try { req.user = verify(req.headers.authorization); next() }\n  catch { res.status(401).end() }\n}\n```",tech:"node",displayNumber:27,sortKey:1919},{id:"node-1920",number:28,title:"How do you implement role-based authorization?",question:"How do you implement role-based authorization?",difficulty:"Advanced",category:"Express & REST APIs",answer:"After authentication, add middleware that checks the user's role/permissions against the route's requirement, returning 403 if insufficient.\n\n```js\nconst requireRole = role => (req, res, next) =>\n  req.user.role === role ? next() : res.status(403).end()\napp.delete('/users/:id', auth, requireRole('admin'), remove)\n```",tech:"node",displayNumber:28,sortKey:1920},{id:"node-1921",number:29,title:"What is the difference between authentication and authorization?",question:"What is the difference between authentication and authorization?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Authentication verifies identity (who you are); authorization determines access (what you may do). You authenticate first, then authorize per resource/action.\n\n```js\napp.use(authenticate) // who\napp.use('/admin', authorizeAdmin) // what\n```",tech:"node",displayNumber:29,sortKey:1921},{id:"node-1922",number:30,title:"How do you handle request logging?",question:"How do you handle request logging?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Use `morgan` for HTTP access logs or a structured logger (`pino`) in middleware capturing method, path, status, and latency. Add a correlation ID for tracing.\n\n```js\napp.use(require('morgan')('combined'))\n```",tech:"node",displayNumber:30,sortKey:1922},{id:"node-1923",number:31,title:"How do you set response compression?",question:"How do you set response compression?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Add the `compression` middleware to gzip/deflate responses based on `Accept-Encoding`. Often skipped in favor of proxy-level compression.\n\n```js\napp.use(require('compression')())\n```",tech:"node",displayNumber:31,sortKey:1923},{id:"node-1924",number:32,title:"What is the order of middleware execution and why does it matter?",question:"What is the order of middleware execution and why does it matter?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Middleware runs top-to-bottom as registered. Placement matters: body parsers before handlers that need `req.body`, auth before protected routes, error handler last. Wrong order causes bugs (e.g. missing body).\n\n```js\napp.use(express.json()) // must come before routes using req.body\n```",tech:"node",displayNumber:32,sortKey:1924},{id:"node-1925",number:33,title:"How do you mount sub-applications?",question:"How do you mount sub-applications?",difficulty:"Advanced",category:"Express & REST APIs",answer:"An Express app or Router can be mounted at a path with `app.use('/path', subApp)`, giving modular composition and independent middleware per mount.\n\n```js\napp.use('/admin', adminApp)\n```",tech:"node",displayNumber:33,sortKey:1925},{id:"node-1926",number:34,title:"How do you implement API rate limiting in Express?",question:"How do you implement API rate limiting in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use `express-rate-limit` with a shared store (Redis) for multi-instance correctness, returning 429 with `Retry-After` when limits are hit.\n\n```js\napp.use(rateLimit({ windowMs: 60000, max: 100, store: redisStore }))\n```",tech:"node",displayNumber:34,sortKey:1926},{id:"node-1927",number:35,title:"What is the `trust proxy` setting?",question:"What is the `trust proxy` setting?",difficulty:"Experienced",category:"Express & REST APIs",answer:"`app.set('trust proxy', ...)` tells Express to trust `X-Forwarded-*` headers from your proxy so `req.ip`, protocol, and secure-cookie logic reflect the real client. Misconfiguring it lets clients spoof their IP.\n\n```js\napp.set('trust proxy', 1) // trust first proxy hop\n```",tech:"node",displayNumber:35,sortKey:1927},{id:"node-1928",number:36,title:"How do you handle file uploads in Express?",question:"How do you handle file uploads in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Use `multer` (multipart parser) to stream files to disk/memory/cloud with size and type limits, exposing files on `req.file`/`req.files`.\n\n```js\napp.post('/avatar', multer({ limits: { fileSize: 2e6 } }).single('img'), save)\n```",tech:"node",displayNumber:36,sortKey:1928},{id:"node-1929",number:37,title:"How do you send different content types?",question:"How do you send different content types?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`res.json()` sends JSON, `res.send()` infers type, `res.sendFile()` streams files, `res.render()` renders templates, and `res.type()` sets the `Content-Type` explicitly.\n\n```js\nres.type('text/csv').send(csvString)\n```",tech:"node",displayNumber:37,sortKey:1929},{id:"node-1930",number:38,title:"What is content negotiation in Express?",question:"What is content negotiation in Express?",difficulty:"Advanced",category:"Express & REST APIs",answer:"`res.format()` picks a handler based on the client's `Accept` header, serving JSON, HTML, or other representations of the same resource.\n\n```js\nres.format({ 'application/json': () => res.json(x), 'text/html': () => res.render('x', x) })\n```",tech:"node",displayNumber:38,sortKey:1930},{id:"node-1931",number:39,title:"How do you implement idempotency keys for POST requests?",question:"How do you implement idempotency keys for POST requests?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Store the result keyed by a client-supplied `Idempotency-Key`; on repeat, return the cached response instead of reprocessing. Prevents duplicate charges/orders on retries.\n\n```js\nconst cached = await store.get(req.headers['idempotency-key'])\nif (cached) return res.json(cached)\n```",tech:"node",displayNumber:39,sortKey:1931},{id:"node-1932",number:40,title:"How do you structure error responses consistently?",question:"How do you structure error responses consistently?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use a standard error shape (code, message, details) and centralize it in error middleware. Consider RFC 7807 \"problem+json\". Avoid leaking stack traces in production.\n\n```js\nres.status(422).json({ type: 'validation', errors: [{ field: 'email', msg: 'invalid' }] })\n```",tech:"node",displayNumber:40,sortKey:1932},{id:"node-1933",number:41,title:"What is the difference between operational and programmer errors?",question:"What is the difference between operational and programmer errors?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Operational errors are expected runtime conditions (invalid input, 404, timeouts) you handle and recover from. Programmer errors are bugs (undefined access) that should crash and be fixed, not caught-and-ignored.\n\n```js\nif (!user) throw new NotFoundError() // operational -> 404\n```",tech:"node",displayNumber:41,sortKey:1933},{id:"node-1934",number:42,title:"How do you handle graceful shutdown in Express?",question:"How do you handle graceful shutdown in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Capture the `http.Server` from `app.listen`, and on SIGTERM call `server.close()` to drain connections, close DB pools, then exit. Use a timeout to force-close if needed.\n\n```js\nconst server = app.listen(3000)\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```",tech:"node",displayNumber:42,sortKey:1934},{id:"node-1935",number:43,title:"What is `express.static` and how do you cache it?",question:"What is `express.static` and how do you cache it?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`express.static` serves files from a directory with proper `Content-Type` and caching. Set `maxAge`/`immutable` for hashed assets, and `etag` for revalidation.\n\n```js\napp.use(express.static('public', { maxAge: '1y', immutable: true }))\n```",tech:"node",displayNumber:43,sortKey:1935},{id:"node-1936",number:44,title:"How do you protect against common web vulnerabilities in Express?",question:"How do you protect against common web vulnerabilities in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Use `helmet` for secure headers, validate/sanitize input, parameterized DB queries, `cors` with a strict origin list, rate limiting, CSRF protection for cookie auth, and never trust client data.\n\n```js\napp.use(helmet()); app.use(express.json({ limit: '100kb' }))\n```",tech:"node",displayNumber:44,sortKey:1936},{id:"node-1937",number:45,title:"What is CSRF and how do you prevent it?",question:"What is CSRF and how do you prevent it?",difficulty:"Experienced",category:"Express & REST APIs",answer:"CSRF tricks an authenticated browser into making unwanted requests using its cookies. Prevent with `SameSite` cookies, anti-CSRF tokens (double-submit/synchronizer), and requiring a custom header for state-changing requests. Token/header auth (not cookies) is inherently less exposed.\n\n```js\napp.use(csrf()); // and send token to client for form submissions\n```",tech:"node",displayNumber:45,sortKey:1937},{id:"node-1938",number:46,title:"How do you handle long-running requests without blocking?",question:"How do you handle long-running requests without blocking?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Return 202 Accepted immediately, enqueue the job to a background worker/queue, and let clients poll a status endpoint or receive a webhook. Don't hold the HTTP connection for minutes.\n\n```js\napp.post('/reports', (req, res) => { queue.add(req.body); res.status(202).json({ jobId }) })\n```",tech:"node",displayNumber:46,sortKey:1938},{id:"node-1939",number:47,title:"What is the difference between `res.send`, `res.json`, and `res.end`?",question:"What is the difference between `res.send`, `res.json`, and `res.end`?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"`res.json` serializes to JSON and sets the header. `res.send` handles strings/buffers/objects, inferring type. `res.end` (from core) sends raw data without Express conveniences. Prefer `json` for APIs.\n\n```js\nres.json({ ok: true }) // sets application/json + stringifies\n```",tech:"node",displayNumber:47,sortKey:1939},{id:"node-1940",number:48,title:"How do you implement API documentation?",question:"How do you implement API documentation?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Describe endpoints with an OpenAPI/Swagger spec and serve interactive docs (`swagger-ui-express`). Generating the spec from code/annotations keeps docs in sync.\n\n```js\napp.use('/docs', swaggerUi.serve, swaggerUi.setup(openapiSpec))\n```",tech:"node",displayNumber:48,sortKey:1940},{id:"node-1941",number:49,title:"What is the difference between REST and GraphQL?",question:"What is the difference between REST and GraphQL?",difficulty:"Advanced",category:"Express & REST APIs",answer:"REST exposes fixed resource endpoints; clients may over/under-fetch. GraphQL exposes one endpoint where clients request exactly the fields they need, avoiding round-trips but adding query-complexity and caching challenges.\n\n```js\n// GraphQL: query { user(id:1){ name orders{ total } } }\n```",tech:"node",displayNumber:49,sortKey:1941},{id:"node-1942",number:50,title:"How do you handle nested/related resources in REST?",question:"How do you handle nested/related resources in REST?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Model relationships in the URL hierarchy (`/users/:id/orders`), keep nesting shallow (usually one level), and offer filtering/embedding via query params to avoid many round-trips.\n\n```js\napp.get('/users/:id/orders', listUserOrders)\n```",tech:"node",displayNumber:50,sortKey:1942},{id:"node-1943",number:51,title:"What is the N+1 problem in APIs and how do you fix it?",question:"What is the N+1 problem in APIs and how do you fix it?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Fetching a list then querying related data per item causes N+1 queries. Fix by batching (one `IN` query), joins, or a DataLoader that coalesces per-request lookups.\n\n```js\nconst orders = await db.orders.find({ userId: { $in: ids } }) // one query\n```",tech:"node",displayNumber:51,sortKey:1943},{id:"node-1944",number:52,title:"How do you implement filtering, sorting, and field selection?",question:"How do you implement filtering, sorting, and field selection?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Parse query params (`?status=open&sort=-createdAt&fields=id,name`), whitelist allowed fields to prevent injection, and translate them to safe DB queries.\n\n```js\nconst sort = allowedSort[req.query.sort] ?? { createdAt: -1 }\n```",tech:"node",displayNumber:52,sortKey:1944},{id:"node-1945",number:53,title:"What are webhooks and how do you handle them securely?",question:"What are webhooks and how do you handle them securely?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Webhooks are server-to-server HTTP callbacks on events. Secure them by verifying a signature (HMAC of the raw body with a shared secret), checking timestamps to prevent replay, and responding fast (queue processing).\n\n```js\nconst sig = hmac(secret, req.rawBody)\nif (sig !== req.headers['x-signature']) return res.status(400).end()\n```",tech:"node",displayNumber:53,sortKey:1945},{id:"node-1946",number:54,title:"Why do you need the raw body for webhook signature verification?",question:"Why do you need the raw body for webhook signature verification?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Signatures are computed over exact raw bytes; JSON parsing/reserialization changes whitespace/key order and breaks verification. Capture `req.rawBody` (via a `verify` callback) before parsing.\n\n```js\nexpress.json({ verify: (req, res, buf) => { req.rawBody = buf } })\n```",tech:"node",displayNumber:54,sortKey:1946},{id:"node-1947",number:55,title:"How do you handle concurrent updates (optimistic locking)?",question:"How do you handle concurrent updates (optimistic locking)?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Include a version/`ETag`; on update, require it via `If-Match` and reject with 409/412 if it no longer matches, preventing lost updates from concurrent writers.\n\n```js\nconst r = await db.update({ id, version }, { $set: data, $inc: { version: 1 } })\nif (!r.matchedCount) return res.status(409).end()\n```",tech:"node",displayNumber:55,sortKey:1947},{id:"node-1948",number:56,title:"What is the difference between synchronous and event-driven API design?",question:"What is the difference between synchronous and event-driven API design?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Synchronous request/response returns results immediately, coupling caller and service. Event-driven (queues/pub-sub) decouples them: producers emit events, consumers process asynchronously, improving resilience and scalability at the cost of eventual consistency.\n\n```js\nawait queue.publish('order.created', order) // async, decoupled\n```",tech:"node",displayNumber:56,sortKey:1948},{id:"node-1949",number:57,title:"How do you implement health check endpoints?",question:"How do you implement health check endpoints?",difficulty:"Intermediate",category:"Express & REST APIs",answer:"Expose `/health` (liveness: process up) and `/ready` (readiness: dependencies like DB reachable). Orchestrators use these to route traffic and restart unhealthy instances.\n\n```js\napp.get('/health', (req, res) => res.json({ status: 'ok' }))\n```",tech:"node",displayNumber:57,sortKey:1949},{id:"node-1950",number:58,title:"What is the difference between liveness and readiness probes?",question:"What is the difference between liveness and readiness probes?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Liveness checks if the process is alive (restart if not). Readiness checks if it can serve traffic now (e.g. DB connected, warmed up). Failing readiness removes the pod from load balancing without restarting it.\n\n```js\napp.get('/ready', async (req, res) => (await db.ping()) ? res.end() : res.status(503).end())\n```",tech:"node",displayNumber:58,sortKey:1950},{id:"node-1951",number:59,title:"How do you handle timeouts on Express routes?",question:"How do you handle timeouts on Express routes?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Set server-level timeouts and per-request guards (e.g. `connect-timeout` or an AbortSignal), responding 503/504 and aborting downstream work when exceeded so slow handlers don't pile up.\n\n```js\nreq.setTimeout(10000, () => res.status(503).end())\n```",tech:"node",displayNumber:59,sortKey:1951},{id:"node-1952",number:60,title:"How do you test Express routes?",question:"How do you test Express routes?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Export the `app` (without calling `listen`) and drive it with `supertest`, asserting status/body. Mock external services and use a test database.\n\n```js\nawait request(app).post('/users').send({ name: 'A' }).expect(201)\n```",tech:"node",displayNumber:60,sortKey:1952},{id:"node-1953",number:61,title:"What is dependency injection in Express services?",question:"What is dependency injection in Express services?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Pass dependencies (DB, config, clients) into services/route factories instead of importing them internally. This decouples layers, eases testing with mocks, and avoids hidden global state.\n\n```js\nconst makeUserRoutes = ({ db }) => { const r = Router(); r.get('/', () => db.users()); return r }\n```",tech:"node",displayNumber:61,sortKey:1953},{id:"node-1954",number:62,title:"How do you handle multi-tenancy in an API?",question:"How do you handle multi-tenancy in an API?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Identify the tenant (subdomain/header/token claim) in middleware, then scope every query/resource to that tenant. Enforce isolation at the data layer so tenants can never read each other's data.\n\n```js\napp.use((req, res, next) => { req.tenantId = req.user.tenantId; next() })\n```",tech:"node",displayNumber:62,sortKey:1954},{id:"node-1955",number:63,title:"What is API gateway pattern?",question:"What is API gateway pattern?",difficulty:"Experienced",category:"Express & REST APIs",answer:"An API gateway is a single entry point that routes to backend services and handles cross-cutting concerns (auth, rate limiting, aggregation, TLS). It simplifies clients and centralizes policy in a microservices system.\n\n```js\n// gateway -> /users -> user-service, /orders -> order-service\n```",tech:"node",displayNumber:63,sortKey:1955},{id:"node-1956",number:64,title:"How do you implement request validation errors with clear messages?",question:"How do you implement request validation errors with clear messages?",difficulty:"Advanced",category:"Express & REST APIs",answer:"Validate with a schema, catch validation errors in middleware, and map them to a 400/422 response listing each failing field and reason, aiding client debugging.\n\n```js\ncatch (e) { if (e instanceof ZodError) res.status(422).json({ errors: e.issues }) }\n```",tech:"node",displayNumber:64,sortKey:1956},{id:"node-1957",number:65,title:"What is the difference between `app.listen` and using an http server directly?",question:"What is the difference between `app.listen` and using an http server directly?",difficulty:"Advanced",category:"Express & REST APIs",answer:"`app.listen` is a shortcut that creates an `http.Server` internally. Creating the server yourself (`http.createServer(app)`) is needed when you also attach WebSockets or HTTP/2, or want the server reference for graceful shutdown.\n\n```js\nconst server = http.createServer(app); wss.attach(server); server.listen(3000)\n```",tech:"node",displayNumber:65,sortKey:1957},{id:"node-1958",number:66,title:"How do you handle streaming responses in Express?",question:"How do you handle streaming responses in Express?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Pipe a readable stream to `res` (which is writable), setting appropriate headers. Use `pipeline` for error handling so failures clean up and reach the error handler.\n\n```js\npipeline(fs.createReadStream('big.csv'), res, err => err && next(err))\n```",tech:"node",displayNumber:66,sortKey:1958},{id:"node-1959",number:67,title:"What are the security implications of `express.json()` limits?",question:"What are the security implications of `express.json()` limits?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Without a `limit`, a huge JSON body can exhaust memory (DoS). Setting a conservative `limit` rejects oversized payloads early with 413. Also beware deeply nested JSON causing CPU spikes on parse.\n\n```js\napp.use(express.json({ limit: '100kb' }))\n```",tech:"node",displayNumber:67,sortKey:1959},{id:"node-1960",number:68,title:"How do you migrate from Express 4 to 5?",question:"How do you migrate from Express 4 to 5?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Express 5 auto-handles rejected promises in handlers, changes some path-matching (`*` syntax), and removes deprecated APIs. Review async error handling, update wildcard routes, and test middleware order.\n\n```js\n// Express 5: async errors propagate to error middleware automatically\napp.get('/x', async (req, res) => { await work(); res.end() })\n```",tech:"node",displayNumber:68,sortKey:1960},{id:"node-1961",number:69,title:"What are alternatives to Express and why choose them?",question:"What are alternatives to Express and why choose them?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Fastify (faster, schema-based validation/serialization), Koa (async middleware, minimal), NestJS (opinionated, DI, TypeScript), Hapi (config-centric). Choose based on performance, structure, and TypeScript needs.\n\n```js\n// Fastify: built-in JSON schema validation & fast serialization\nfastify.post('/u', { schema }, handler)\n```",tech:"node",displayNumber:69,sortKey:1961},{id:"node-1962",number:70,title:"How do you achieve zero-downtime deploys for an Express API?",question:"How do you achieve zero-downtime deploys for an Express API?",difficulty:"Experienced",category:"Express & REST APIs",answer:"Run multiple instances behind a load balancer, roll out new instances while draining old ones (readiness probes + graceful shutdown), and keep DB migrations backward-compatible so old and new code coexist during rollout.\n\n```js\n// LB removes instance on /ready 503, then SIGTERM -> server.close() drains\n```",tech:"node",displayNumber:70,sortKey:1962},{id:"node-1963",number:1,title:"How do you connect to a database in Node?",question:"How do you connect to a database in Node?",difficulty:"Basic",category:"Databases & ORMs",answer:"Use a driver (`pg`, `mysql2`, `mongodb`) or an ORM (Prisma, Sequelize, TypeORM). Create a connection or pool once at startup and reuse it across requests.\n\n```js\nconst { Pool } = require('pg')\nconst pool = new Pool({ connectionString: process.env.DB_URL })\n```",tech:"node",displayNumber:1,sortKey:1963},{id:"node-1964",number:2,title:"What is a connection pool and why use one?",question:"What is a connection pool and why use one?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A pool maintains a set of reusable open connections, avoiding the cost of opening/closing per query and capping total concurrent connections to protect the database. Requests borrow and return connections.\n\n```js\nconst pool = new Pool({ max: 20 })\nconst { rows } = await pool.query('SELECT 1')\n```",tech:"node",displayNumber:2,sortKey:1964},{id:"node-1965",number:3,title:"Why not open a new DB connection per request?",question:"Why not open a new DB connection per request?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Connecting is expensive (handshake/auth) and databases limit total connections. Per-request connections cause latency and can exhaust the DB's connection limit under load. Pools solve both.\n\n```js\n// reuse pool.query, never new Client() per request\n```",tech:"node",displayNumber:3,sortKey:1965},{id:"node-1966",number:4,title:"What is SQL injection and how do you prevent it?",question:"What is SQL injection and how do you prevent it?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"SQL injection inserts malicious SQL via unsanitized input. Prevent it with parameterized queries/prepared statements, which send data separately from the query so it's never executed as SQL.\n\n```js\npool.query('SELECT * FROM users WHERE id = $1', [id]) // safe\n```",tech:"node",displayNumber:4,sortKey:1966},{id:"node-1967",number:5,title:"What is the difference between parameterized queries and string concatenation?",question:"What is the difference between parameterized queries and string concatenation?",difficulty:"Advanced",category:"Databases & ORMs",answer:"String concatenation mixes untrusted data into the SQL text (injectable). Parameterized queries send the query structure and values separately; the driver/DB binds values as data, immune to injection and often cached/prepared.\n\n```js\n// NEVER: `WHERE name = '${name}'`\ndb.query('WHERE name = $1', [name])\n```",tech:"node",displayNumber:5,sortKey:1967},{id:"node-1968",number:6,title:"What is an ORM and what are its trade-offs?",question:"What is an ORM and what are its trade-offs?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An ORM maps DB tables to objects, generating SQL and handling relations/migrations. Pros: productivity, safety, portability. Cons: abstraction leaks, N+1 pitfalls, less control over complex/optimized queries.\n\n```js\nconst user = await prisma.user.findUnique({ where: { id } })\n```",tech:"node",displayNumber:6,sortKey:1968},{id:"node-1969",number:7,title:"What is the difference between SQL and NoSQL databases?",question:"What is the difference between SQL and NoSQL databases?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"SQL (relational) uses fixed schemas, joins, and strong consistency/transactions. NoSQL (document/key-value/graph) offers flexible schemas and horizontal scaling, often with eventual consistency. Choose by data shape and consistency needs.\n\n```js\n// SQL: JOIN across tables; MongoDB: embedded documents\n```",tech:"node",displayNumber:7,sortKey:1969},{id:"node-1970",number:8,title:"What is the N+1 query problem?",question:"What is the N+1 query problem?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Loading a list then querying related rows one-by-one issues 1 + N queries. Fix by eager loading (join/include), batching with `IN`, or a DataLoader that coalesces lookups.\n\n```js\nconst users = await prisma.user.findMany({ include: { orders: true } }) // one round trip\n```",tech:"node",displayNumber:8,sortKey:1970},{id:"node-1971",number:9,title:"What is a database transaction?",question:"What is a database transaction?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A transaction groups operations so they all succeed (commit) or all fail (rollback), preserving consistency. Essential for multi-step changes like transferring funds.\n\n```js\nawait prisma.$transaction([debit, credit]) // both or neither\n```",tech:"node",displayNumber:9,sortKey:1971},{id:"node-1972",number:10,title:"What does ACID stand for?",question:"What does ACID stand for?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Atomicity (all-or-nothing), Consistency (valid state transitions), Isolation (concurrent transactions don't interfere), Durability (committed data survives crashes). Relational DBs provide ACID guarantees.\n\n```js\nBEGIN; UPDATE ...; UPDATE ...; COMMIT; -- atomic + durable\n```",tech:"node",displayNumber:10,sortKey:1972},{id:"node-1973",number:11,title:"What are transaction isolation levels?",question:"What are transaction isolation levels?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Read Uncommitted, Read Committed, Repeatable Read, Serializable — increasing isolation and cost. They control anomalies (dirty/non-repeatable reads, phantoms). Higher isolation reduces concurrency; pick per use case.\n\n```js\nawait client.query('SET TRANSACTION ISOLATION LEVEL SERIALIZABLE')\n```",tech:"node",displayNumber:11,sortKey:1973},{id:"node-1974",number:12,title:"What is a dirty read?",question:"What is a dirty read?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A dirty read reads data written by another transaction that hasn't committed yet and might roll back, so you may act on data that never truly existed. Read Committed and above prevent it.\n\n```js\n// avoided at READ COMMITTED and higher\n```",tech:"node",displayNumber:12,sortKey:1974},{id:"node-1975",number:13,title:"How do you run a transaction with a driver like `pg`?",question:"How do you run a transaction with a driver like `pg`?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Check out a single client from the pool, issue `BEGIN`, run queries, then `COMMIT` (or `ROLLBACK` on error), and always release the client.\n\n```js\nconst c = await pool.connect()\ntry { await c.query('BEGIN'); await c.query(sql); await c.query('COMMIT') }\ncatch (e) { await c.query('ROLLBACK'); throw e } finally { c.release() }\n```",tech:"node",displayNumber:13,sortKey:1975},{id:"node-1976",number:14,title:"What is optimistic vs pessimistic locking?",question:"What is optimistic vs pessimistic locking?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Optimistic locking assumes low contention: it checks a version column at update time and retries on conflict. Pessimistic locking locks rows (`SELECT ... FOR UPDATE`) upfront, blocking others. Optimistic scales better; pessimistic suits hot contention.\n\n```js\nUPDATE t SET x=$1, version=version+1 WHERE id=$2 AND version=$3\n```",tech:"node",displayNumber:14,sortKey:1976},{id:"node-1977",number:15,title:"What is `SELECT ... FOR UPDATE`?",question:"What is `SELECT ... FOR UPDATE`?",difficulty:"Experienced",category:"Databases & ORMs",answer:"It locks the selected rows within a transaction so other transactions can't modify them until commit, implementing pessimistic locking to prevent concurrent update conflicts.\n\n```js\nawait c.query('SELECT balance FROM acct WHERE id=$1 FOR UPDATE', [id])\n```",tech:"node",displayNumber:15,sortKey:1977},{id:"node-1978",number:16,title:"What is database indexing?",question:"What is database indexing?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An index is a data structure (usually B-tree) that speeds up lookups/sorts on indexed columns at the cost of extra storage and slower writes. Index columns used in `WHERE`, `JOIN`, and `ORDER BY`.\n\n```js\nCREATE INDEX idx_users_email ON users(email);\n```",tech:"node",displayNumber:16,sortKey:1978},{id:"node-1979",number:17,title:"When can an index hurt performance?",question:"When can an index hurt performance?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Indexes slow down INSERT/UPDATE/DELETE (must be maintained) and consume storage. Too many or unused indexes waste resources; low-cardinality columns benefit little. Index selectively based on query patterns.\n\n```js\n// avoid indexing a boolean column with 50/50 distribution\n```",tech:"node",displayNumber:17,sortKey:1979},{id:"node-1980",number:18,title:"What is a composite index and column order?",question:"What is a composite index and column order?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A composite index covers multiple columns; order matters. It serves queries filtering on a left-prefix of its columns. `(a,b)` helps `WHERE a` and `WHERE a AND b`, but not `WHERE b` alone.\n\n```js\nCREATE INDEX idx ON t(status, created_at); -- helps WHERE status ORDER BY created_at\n```",tech:"node",displayNumber:18,sortKey:1980},{id:"node-1981",number:19,title:"How do you analyze a slow query?",question:"How do you analyze a slow query?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Use `EXPLAIN ANALYZE` to see the query plan — whether it uses indexes or does sequential scans, join methods, and row estimates vs actuals. Then add indexes or rewrite the query.\n\n```js\nEXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 5;\n```",tech:"node",displayNumber:19,sortKey:1981},{id:"node-1982",number:20,title:"What is a database migration?",question:"What is a database migration?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"A migration is a versioned, incremental schema change (create table, add column) tracked in code so environments stay in sync and changes are reproducible and reversible.\n\n```js\nnpx prisma migrate dev --name add_orders\n```",tech:"node",displayNumber:20,sortKey:1982},{id:"node-1983",number:21,title:"How do you perform zero-downtime schema migrations?",question:"How do you perform zero-downtime schema migrations?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Make changes backward-compatible and multi-step: add nullable column → backfill → deploy code using it → enforce constraint → remove old column later. Never rename/drop in a single deploy while old code runs.\n\n```js\n// step1: ADD COLUMN email_new; step2: backfill; step3: switch reads/writes\n```",tech:"node",displayNumber:21,sortKey:1983},{id:"node-1984",number:22,title:"What is the difference between `findOne` and `findMany`?",question:"What is the difference between `findOne` and `findMany`?",difficulty:"Basic",category:"Databases & ORMs",answer:"`findOne`/`findUnique` returns a single record (or null); `findMany` returns an array. Use unique lookups for single entities and filtered/paged queries for lists.\n\n```js\nconst u = await prisma.user.findUnique({ where: { email } })\nconst many = await prisma.user.findMany({ where: { active: true } })\n```",tech:"node",displayNumber:22,sortKey:1984},{id:"node-1985",number:23,title:"What is eager vs lazy loading of relations?",question:"What is eager vs lazy loading of relations?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Eager loading fetches related data with the main query (`include`/`join`), avoiding extra round trips. Lazy loading fetches relations on access, which is convenient but can trigger N+1 queries.\n\n```js\nprisma.post.findMany({ include: { author: true } }) // eager\n```",tech:"node",displayNumber:23,sortKey:1985},{id:"node-1986",number:24,title:"What is a DataLoader and how does it help?",question:"What is a DataLoader and how does it help?",difficulty:"Experienced",category:"Databases & ORMs",answer:"DataLoader batches individual key lookups made within a tick into a single query and caches per request, eliminating N+1 in GraphQL/REST resolvers.\n\n```js\nconst loader = new DataLoader(ids => db.users.findByIds(ids))\nawait loader.load(1) // batched with other loads\n```",tech:"node",displayNumber:24,sortKey:1986},{id:"node-1987",number:25,title:"How do you model relationships in MongoDB?",question:"How do you model relationships in MongoDB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Embed related data for one-to-few, tightly-coupled data read together; reference (store IDs) for one-to-many/many-to-many or independently-updated data. Balance read efficiency against duplication and document size limits.\n\n```js\n// embed comments in a post, or reference authorId\n{ title, comments: [{ text }], authorId: ObjectId('...') }\n```",tech:"node",displayNumber:25,sortKey:1987},{id:"node-1988",number:26,title:"What is the aggregation pipeline in MongoDB?",question:"What is the aggregation pipeline in MongoDB?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A sequence of stages (`$match`, `$group`, `$sort`, `$lookup`, `$project`) that transform documents, enabling complex analytics/joins server-side without pulling data into the app.\n\n```js\ndb.orders.aggregate([{ $match: { status: 'paid' } }, { $group: { _id: '$userId', total: { $sum: '$amount' } } }])\n```",tech:"node",displayNumber:26,sortKey:1988},{id:"node-1989",number:27,title:"What is `$lookup` in MongoDB?",question:"What is `$lookup` in MongoDB?",difficulty:"Experienced",category:"Databases & ORMs",answer:"`$lookup` performs a left outer join between collections in an aggregation, joining documents by matching fields — MongoDB's answer to SQL joins.\n\n```js\n{ $lookup: { from: 'users', localField: 'userId', foreignField: '_id', as: 'user' } }\n```",tech:"node",displayNumber:27,sortKey:1989},{id:"node-1990",number:28,title:"How do you handle schema validation in MongoDB?",question:"How do you handle schema validation in MongoDB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Use an ODM like Mongoose with schemas, or MongoDB's built-in JSON Schema validators on collections. Validation enforces structure despite MongoDB's flexible documents.\n\n```js\nconst User = mongoose.model('User', new Schema({ email: { type: String, required: true } }))\n```",tech:"node",displayNumber:28,sortKey:1990},{id:"node-1991",number:29,title:"What is Mongoose?",question:"What is Mongoose?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Mongoose is an ODM for MongoDB providing schemas, validation, middleware (hooks), population (references), and typed models over the raw driver.\n\n```js\nconst u = await User.findById(id).populate('orders')\n```",tech:"node",displayNumber:29,sortKey:1991},{id:"node-1992",number:30,title:"What is the difference between Prisma, Sequelize, and TypeORM?",question:"What is the difference between Prisma, Sequelize, and TypeORM?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Prisma uses a schema file + generated type-safe client (great DX/TS). Sequelize is a mature ORM with a JS API. TypeORM uses decorators/entities (Active Record or Data Mapper). Choose by type-safety, patterns, and ecosystem fit.\n\n```js\nconst users = await prisma.user.findMany() // fully typed\n```",tech:"node",displayNumber:30,sortKey:1992},{id:"node-1993",number:31,title:"What is a prepared statement?",question:"What is a prepared statement?",difficulty:"Advanced",category:"Databases & ORMs",answer:"A prepared statement is a parameterized query the DB parses/plans once and reuses with different values, improving performance and preventing injection. Drivers often prepare parameterized queries automatically.\n\n```js\nconst stmt = db.prepare('INSERT INTO t(x) VALUES(?)'); stmt.run(1); stmt.run(2)\n```",tech:"node",displayNumber:31,sortKey:1993},{id:"node-1994",number:32,title:"How do you paginate database results efficiently?",question:"How do you paginate database results efficiently?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Prefer keyset/cursor pagination (`WHERE id > :last ORDER BY id LIMIT n`) using an indexed column over `OFFSET`, which scans and skips rows and degrades on large offsets.\n\n```js\nSELECT * FROM t WHERE id > $1 ORDER BY id LIMIT 20\n```",tech:"node",displayNumber:32,sortKey:1994},{id:"node-1995",number:33,title:"What is connection pool exhaustion and how do you avoid it?",question:"What is connection pool exhaustion and how do you avoid it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"It happens when all pooled connections are busy (leaked, held by long transactions, or too-small pool), causing requests to queue/timeout. Avoid by releasing clients promptly, sizing the pool, and setting query timeouts.\n\n```js\ntry { await c.query(sql) } finally { c.release() } // always release\n```",tech:"node",displayNumber:33,sortKey:1995},{id:"node-1996",number:34,title:"How do you size a connection pool?",question:"How do you size a connection pool?",difficulty:"Experienced",category:"Databases & ORMs",answer:'Base it on the DB\'s max connections divided across app instances, workload concurrency, and query latency — not "bigger is better". Too large overwhelms the DB; too small starves the app. Measure and tune.\n\n```js\nnew Pool({ max: 20 }) // per instance; sum across instances < DB limit\n```',tech:"node",displayNumber:34,sortKey:1996},{id:"node-1997",number:35,title:"What is a read replica and when do you use it?",question:"What is a read replica and when do you use it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A read replica is a copy that serves read queries, offloading the primary. Use it to scale reads, but account for replication lag (eventual consistency) — route critical read-after-write to the primary.\n\n```js\nconst rows = await replicaPool.query('SELECT ...') // reads off replica\n```",tech:"node",displayNumber:35,sortKey:1997},{id:"node-1998",number:36,title:"What is replication lag and how do you handle it?",question:"What is replication lag and how do you handle it?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Replication lag is the delay before a write appears on replicas, so a read right after a write may return stale data. Handle by reading from the primary for read-after-write, or waiting for the replica to catch up.\n\n```js\n// after write, read from primary to avoid stale replica data\n```",tech:"node",displayNumber:36,sortKey:1998},{id:"node-1999",number:37,title:"What is sharding?",question:"What is sharding?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Sharding partitions data horizontally across multiple servers by a shard key, so each holds a subset. It scales writes/storage beyond one machine but complicates cross-shard queries and transactions.\n\n```js\n// shard by user_id % N -> route query to the right shard\n```",tech:"node",displayNumber:37,sortKey:1999},{id:"node-2000",number:38,title:"What is a caching layer and cache invalidation?",question:"What is a caching layer and cache invalidation?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A cache (Redis) stores frequently-read data to reduce DB load/latency. Invalidation — expiring or updating stale entries on writes — is the hard part; strategies include TTLs, write-through, and cache-aside.\n\n```js\nlet u = await redis.get(k) ?? await db.user(id).then(v => (redis.set(k, v, 'EX', 60), v))\n```",tech:"node",displayNumber:38,sortKey:2e3},{id:"node-2001",number:39,title:"What is the cache-aside pattern?",question:"What is the cache-aside pattern?",difficulty:"Advanced",category:"Databases & ORMs",answer:"The app checks the cache first; on a miss it loads from the DB and populates the cache. Writes update the DB and invalidate the cached entry. It's simple and common but risks brief staleness.\n\n```js\nconst hit = await cache.get(k); if (hit) return hit\nconst v = await db.get(k); await cache.set(k, v); return v\n```",tech:"node",displayNumber:39,sortKey:2001},{id:"node-2002",number:40,title:"What is Redis and what is it used for in Node apps?",question:"What is Redis and what is it used for in Node apps?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Redis is an in-memory data store used for caching, session storage, rate limiting, pub/sub, queues, and leaderboards. Its speed and data structures make it a versatile companion to a primary DB.\n\n```js\nawait redis.set('session:abc', JSON.stringify(user), 'EX', 3600)\n```",tech:"node",displayNumber:40,sortKey:2002},{id:"node-2003",number:41,title:"How do you use Redis for sessions across multiple instances?",question:"How do you use Redis for sessions across multiple instances?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Store session data in Redis (shared) instead of process memory, so any instance can serve any request without sticky sessions. Set TTLs matching session expiry.\n\n```js\napp.use(session({ store: new RedisStore({ client: redis }), secret }))\n```",tech:"node",displayNumber:41,sortKey:2003},{id:"node-2004",number:42,title:"What is a database deadlock?",question:"What is a database deadlock?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A deadlock occurs when two transactions each hold a lock the other needs, waiting forever. The DB detects it and aborts one (a deadlock error). Avoid by acquiring locks in a consistent order and keeping transactions short.\n\n```js\n// always update tables/rows in the same order to avoid cycles\n```",tech:"node",displayNumber:42,sortKey:2004},{id:"node-2005",number:43,title:"How do you handle transient DB errors and retries?",question:"How do you handle transient DB errors and retries?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Retry only retryable errors (deadlock, serialization failure, connection reset) with backoff, and make the operation idempotent or wrap it in a transaction. Don't blindly retry non-idempotent writes.\n\n```js\nfor (let i = 0; i < 3; i++) { try { return await tx() } catch (e) { if (!isRetryable(e)) throw e } }\n```",tech:"node",displayNumber:43,sortKey:2005},{id:"node-2006",number:44,title:"What is the difference between `LEFT JOIN` and `INNER JOIN`?",question:"What is the difference between `LEFT JOIN` and `INNER JOIN`?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"INNER JOIN returns only matching rows in both tables. LEFT JOIN returns all rows from the left table plus matches (NULLs where none). Choose based on whether unmatched left rows should appear.\n\n```js\nSELECT u.*, o.id FROM users u LEFT JOIN orders o ON o.user_id = u.id;\n```",tech:"node",displayNumber:44,sortKey:2006},{id:"node-2007",number:45,title:"How do you prevent over-fetching data from the DB?",question:"How do you prevent over-fetching data from the DB?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Select only needed columns (not `SELECT *`), paginate, filter at the DB, and avoid loading unused relations. Less data transferred means lower latency and memory.\n\n```js\nprisma.user.findMany({ select: { id: true, name: true } })\n```",tech:"node",displayNumber:45,sortKey:2007},{id:"node-2008",number:46,title:"What are database constraints and why enforce them at the DB level?",question:"What are database constraints and why enforce them at the DB level?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Constraints (NOT NULL, UNIQUE, FOREIGN KEY, CHECK) enforce data integrity regardless of app bugs or multiple writers. The DB is the last line of defense; app-only validation can be bypassed.\n\n```js\nCREATE TABLE users (email TEXT UNIQUE NOT NULL);\n```",tech:"node",displayNumber:46,sortKey:2008},{id:"node-2009",number:47,title:"How do you handle unique constraint violations gracefully?",question:"How do you handle unique constraint violations gracefully?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Attempt the insert and catch the unique-violation error code, returning a 409 Conflict, or use an upsert (`ON CONFLICT`). Checking-then-inserting has a race window.\n\n```js\nINSERT INTO users(email) VALUES($1) ON CONFLICT (email) DO NOTHING;\n```",tech:"node",displayNumber:47,sortKey:2009},{id:"node-2010",number:48,title:"What is an upsert?",question:"What is an upsert?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"An upsert inserts a row or updates it if it already exists, atomically. It avoids race conditions from separate check/insert/update logic.\n\n```js\nprisma.user.upsert({ where: { email }, create: { email }, update: { seen: new Date() } })\n```",tech:"node",displayNumber:48,sortKey:2010},{id:"node-2011",number:49,title:"What is the difference between soft delete and hard delete?",question:"What is the difference between soft delete and hard delete?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Hard delete removes the row permanently. Soft delete sets a `deleted_at`/`is_deleted` flag, preserving data for audit/recovery while filtering it from normal queries. Soft delete needs consistent query filters and indexes.\n\n```js\nUPDATE users SET deleted_at = now() WHERE id = $1;\n```",tech:"node",displayNumber:49,sortKey:2011},{id:"node-2012",number:50,title:"How do you store timestamps and time zones correctly?",question:"How do you store timestamps and time zones correctly?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Store timestamps in UTC (`timestamptz`), convert to local time only for display. Storing local times without offsets causes bugs across zones and DST changes.\n\n```js\nINSERT INTO events(at) VALUES (now() AT TIME ZONE 'UTC');\n```",tech:"node",displayNumber:50,sortKey:2012},{id:"node-2013",number:51,title:"What is database normalization?",question:"What is database normalization?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Normalization organizes data to reduce redundancy via related tables (1NF–3NF), improving integrity and update efficiency. It can require more joins for reads, which denormalization selectively trades away.\n\n```js\n// separate users and addresses tables instead of repeating address columns\n```",tech:"node",displayNumber:51,sortKey:2013},{id:"node-2014",number:52,title:"When would you denormalize?",question:"When would you denormalize?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Denormalize (duplicate data) to speed up read-heavy paths by avoiding expensive joins/aggregations, accepting extra write complexity and update anomalies. Common in analytics, caches, and NoSQL.\n\n```js\n// store order_count on user to avoid COUNT(*) per request\n```",tech:"node",displayNumber:52,sortKey:2014},{id:"node-2015",number:53,title:"How do you seed and manage test data?",question:"How do you seed and manage test data?",difficulty:"Intermediate",category:"Databases & ORMs",answer:"Use seed scripts/migrations to create deterministic data, wrap tests in transactions that roll back, or reset a test database between runs to keep tests isolated and repeatable.\n\n```js\nbeforeEach(async () => { await db.query('BEGIN') })\nafterEach(async () => { await db.query('ROLLBACK') })\n```",tech:"node",displayNumber:53,sortKey:2015},{id:"node-2016",number:54,title:"What is an ORM query builder vs raw SQL?",question:"What is an ORM query builder vs raw SQL?",difficulty:"Advanced",category:"Databases & ORMs",answer:"Query builders (Knex) compose SQL programmatically with safety and portability; raw SQL gives full control for complex/optimized queries. Most apps mix both — builder/ORM for CRUD, raw for hotspots.\n\n```js\nknex('users').where('active', true).select('id', 'name')\n```",tech:"node",displayNumber:54,sortKey:2016},{id:"node-2017",number:55,title:"How do you handle large bulk inserts efficiently?",question:"How do you handle large bulk inserts efficiently?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Batch rows into multi-row inserts or use `COPY`/bulk APIs, wrap in a transaction, and disable per-row overhead. Inserting one-by-one is slow due to round trips.\n\n```js\nINSERT INTO t(a,b) VALUES ($1,$2),($3,$4),($5,$6); -- batched\n```",tech:"node",displayNumber:55,sortKey:2017},{id:"node-2018",number:56,title:"What is a stored procedure and when use one?",question:"What is a stored procedure and when use one?",difficulty:"Experienced",category:"Databases & ORMs",answer:"A stored procedure is logic stored/executed in the DB. Use it for data-intensive operations to reduce round trips and enforce logic centrally, at the cost of portability and harder version control.\n\n```js\nawait db.query('CALL transfer_funds($1, $2, $3)', [from, to, amt])\n```",tech:"node",displayNumber:56,sortKey:2018},{id:"node-2019",number:57,title:"How do you monitor database performance from Node?",question:"How do you monitor database performance from Node?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Log slow queries with durations, track pool metrics (active/idle/waiting), expose DB latency in APM/metrics, and use the DB's own slow-query log and `pg_stat_statements` to find hotspots.\n\n```js\nconst t = Date.now(); await pool.query(sql); metrics.timing('db', Date.now() - t)\n```",tech:"node",displayNumber:57,sortKey:2019},{id:"node-2020",number:58,title:"What is the difference between `count(*)` performance in SQL and estimates?",question:"What is the difference between `count(*)` performance in SQL and estimates?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Exact `COUNT(*)` on large tables scans many rows and can be slow. For approximate totals (e.g. pagination UI), use planner estimates (`pg_class.reltuples`) or maintained counters instead.\n\n```js\nSELECT reltuples::bigint FROM pg_class WHERE relname = 'orders'; -- estimate\n```",tech:"node",displayNumber:58,sortKey:2020},{id:"node-2021",number:59,title:"How do you implement full-text search?",question:"How do you implement full-text search?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Use the DB's full-text features (Postgres `tsvector`/GIN index, Mongo text index) for basic search, or a dedicated engine (Elasticsearch/OpenSearch) for advanced relevance, facets, and scale.\n\n```js\nSELECT * FROM docs WHERE to_tsvector(body) @@ to_tsquery('node & stream');\n```",tech:"node",displayNumber:59,sortKey:2021},{id:"node-2022",number:60,title:"What is eventual consistency?",question:"What is eventual consistency?",difficulty:"Experienced",category:"Databases & ORMs",answer:"In eventually-consistent systems, replicas converge to the same state over time but may return stale reads briefly. It trades immediate consistency for availability/scale (CAP), acceptable for many read-heavy features.\n\n```js\n// a just-written value may not appear on a replica read immediately\n```",tech:"node",displayNumber:60,sortKey:2022},{id:"node-2023",number:61,title:"What is the CAP theorem?",question:"What is the CAP theorem?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Under a network partition, a distributed system can guarantee at most two of Consistency, Availability, Partition tolerance. Since partitions happen, you effectively choose CP (reject to stay consistent) or AP (serve possibly-stale data).\n\n```js\n// CP: refuse writes during partition; AP: accept and reconcile later\n```",tech:"node",displayNumber:61,sortKey:2023},{id:"node-2024",number:62,title:"How do you avoid leaking DB credentials?",question:"How do you avoid leaking DB credentials?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Load credentials from environment/secret managers, never hardcode or commit them, use least-privilege DB users, rotate secrets, and require TLS to the database.\n\n```js\nnew Pool({ connectionString: process.env.DB_URL, ssl: { rejectUnauthorized: true } })\n```",tech:"node",displayNumber:62,sortKey:2024},{id:"node-2025",number:63,title:"How do you implement audit logging of data changes?",question:"How do you implement audit logging of data changes?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Record who/what/when for changes via triggers, an audit table, or app-level change events (outbox). Keep audit records immutable and separate from operational data.\n\n```js\nINSERT INTO audit(table, row_id, action, actor, at) VALUES(...);\n```",tech:"node",displayNumber:63,sortKey:2025},{id:"node-2026",number:64,title:"What is the transactional outbox pattern?",question:"What is the transactional outbox pattern?",difficulty:"Experienced",category:"Databases & ORMs",answer:'Write domain changes and an event row in the same DB transaction (the "outbox"); a separate process publishes those events to a message broker. It guarantees the event is sent iff the data change committed, avoiding dual-write inconsistency.\n\n```js\nawait tx([saveOrder, insertOutboxEvent]) // atomic; publisher reads outbox later\n```',tech:"node",displayNumber:64,sortKey:2026},{id:"node-2027",number:65,title:"How do you handle migrations in a team/CI setting?",question:"How do you handle migrations in a team/CI setting?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Keep migrations in version control, apply them automatically in CI/CD before deploy, make them forward-only and idempotent where possible, and coordinate to avoid conflicting concurrent migrations.\n\n```js\nnpx prisma migrate deploy // applies pending migrations in CI/prod\n```",tech:"node",displayNumber:65,sortKey:2027},{id:"node-2028",number:66,title:"What is connection string SSL and why require it?",question:"What is connection string SSL and why require it?",difficulty:"Advanced",category:"Databases & ORMs",answer:"SSL/TLS encrypts traffic between app and DB, protecting credentials/data in transit from eavesdropping, especially across networks/cloud. Verify the server certificate to prevent MITM.\n\n```js\nssl: { rejectUnauthorized: true, ca: fs.readFileSync('rds-ca.pem') }\n```",tech:"node",displayNumber:66,sortKey:2028},{id:"node-2029",number:67,title:"How do you handle schema drift between environments?",question:"How do you handle schema drift between environments?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Make migrations the single source of truth, run the same migrations everywhere, and detect drift with tools that diff the live schema against expected. Avoid manual production changes.\n\n```js\nnpx prisma migrate status // reports drift/pending migrations\n```",tech:"node",displayNumber:67,sortKey:2029},{id:"node-2030",number:68,title:"What is the difference between `TRUNCATE` and `DELETE`?",question:"What is the difference between `TRUNCATE` and `DELETE`?",difficulty:"Advanced",category:"Databases & ORMs",answer:"`DELETE` removes rows one by one (logged, respects triggers, can be filtered/rolled back). `TRUNCATE` quickly empties a whole table by deallocating pages (faster, resets sequences, fewer logs) but can't be filtered.\n\n```js\nTRUNCATE TABLE temp_import; -- fast full clear\n```",tech:"node",displayNumber:68,sortKey:2030},{id:"node-2031",number:69,title:"How do you avoid loading huge result sets into memory?",question:"How do you avoid loading huge result sets into memory?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Stream/cursor through results in chunks instead of buffering all rows. Drivers offer server-side cursors or query streams that emit rows incrementally.\n\n```js\nconst stream = pool.query(new QueryStream('SELECT * FROM big'))\nfor await (const row of stream) process(row)\n```",tech:"node",displayNumber:69,sortKey:2031},{id:"node-2032",number:70,title:"How do you choose between SQL and NoSQL for a project?",question:"How do you choose between SQL and NoSQL for a project?",difficulty:"Experienced",category:"Databases & ORMs",answer:"Choose SQL for relational data, strong consistency, complex queries, and transactions. Choose NoSQL for flexible/evolving schemas, massive scale, or specific access patterns (document/key-value/graph). Many systems use both (polyglot persistence).\n\n```js\n// orders/payments -> Postgres; session/cache -> Redis; catalog -> Mongo\n```",tech:"node",displayNumber:70,sortKey:2032},{id:"node-2033",number:1,title:"How do you securely store passwords?",question:"How do you securely store passwords?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Never store plaintext. Hash with a slow, salted algorithm designed for passwords: bcrypt, scrypt, or Argon2. Salts prevent rainbow-table attacks; slowness resists brute force.\n\n```js\nconst hash = await bcrypt.hash(password, 12)\nconst ok = await bcrypt.compare(password, hash)\n```",tech:"node",displayNumber:1,sortKey:2033},{id:"node-2034",number:2,title:"Why not use SHA-256 for passwords?",question:"Why not use SHA-256 for passwords?",difficulty:"Advanced",category:"Security & Authentication",answer:"SHA-256 is fast, so attackers can compute billions of guesses per second. Password hashing needs deliberate slowness and memory-hardness (bcrypt/Argon2) to make brute forcing impractical.\n\n```js\nawait argon2.hash(password) // memory-hard, slow by design\n```",tech:"node",displayNumber:2,sortKey:2034},{id:"node-2035",number:3,title:"What is a salt and why is it needed?",question:"What is a salt and why is it needed?",difficulty:"Intermediate",category:"Security & Authentication",answer:"A salt is unique random data added per password before hashing, so identical passwords produce different hashes and precomputed rainbow tables are useless. bcrypt/Argon2 embed the salt in the output.\n\n```js\n// bcrypt output contains cost + salt + hash: $2b$12$salt...hash\n```",tech:"node",displayNumber:3,sortKey:2035},{id:"node-2036",number:4,title:"What is a pepper?",question:"What is a pepper?",difficulty:"Experienced",category:"Security & Authentication",answer:"A pepper is a secret added to passwords before hashing, stored separately from the DB (e.g. in an HSM/env), so a database leak alone doesn't enable offline cracking. It complements per-user salts.\n\n```js\nconst hash = await bcrypt.hash(password + process.env.PEPPER, 12)\n```",tech:"node",displayNumber:4,sortKey:2036},{id:"node-2037",number:5,title:"What is JWT?",question:"What is JWT?",difficulty:"Intermediate",category:"Security & Authentication",answer:"A JSON Web Token is a signed, base64url token with header.payload.signature. The signature lets the server verify claims without server-side session storage, enabling stateless auth.\n\n```js\nconst token = jwt.sign({ sub: user.id }, secret, { expiresIn: '15m' })\njwt.verify(token, secret)\n```",tech:"node",displayNumber:5,sortKey:2037},{id:"node-2038",number:6,title:"What are the three parts of a JWT?",question:"What are the three parts of a JWT?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Header (algorithm/type), Payload (claims like `sub`, `exp`), Signature (HMAC or RSA over header+payload). The payload is only base64-encoded, not encrypted — never put secrets in it.\n\n```js\n// eyJhbGci...  .  eyJzdWIi...  .  signature\n```",tech:"node",displayNumber:6,sortKey:2038},{id:"node-2039",number:7,title:"Is JWT payload encrypted?",question:"Is JWT payload encrypted?",difficulty:"Advanced",category:"Security & Authentication",answer:"No. A standard (signed) JWT is only base64url-encoded and fully readable. Anyone can decode the payload; the signature only guarantees integrity/authenticity. For confidentiality use JWE (encrypted) or don't store sensitive data in the token.\n\n```js\nJSON.parse(Buffer.from(token.split('.')[1], 'base64')) // readable claims\n```",tech:"node",displayNumber:7,sortKey:2039},{id:"node-2040",number:8,title:"What is the difference between JWT and session-based auth?",question:"What is the difference between JWT and session-based auth?",difficulty:"Advanced",category:"Security & Authentication",answer:"Sessions store state server-side (a session ID cookie references it) and are easy to revoke. JWTs are stateless (self-contained, scale across servers) but hard to revoke before expiry. Choose based on revocation and scaling needs.\n\n```js\n// session: cookie 'sid' -> store lookup; JWT: verify signature, no lookup\n```",tech:"node",displayNumber:8,sortKey:2040},{id:"node-2041",number:9,title:"How do you revoke a JWT?",question:"How do you revoke a JWT?",difficulty:"Experienced",category:"Security & Authentication",answer:"Since JWTs are stateless, use short expiry + refresh tokens, and maintain a denylist/allowlist (by jti/user) in a fast store to invalidate tokens early. True revocation reintroduces some server state.\n\n```js\nif (await redis.sismember('revoked', payload.jti)) throw new Error('revoked')\n```",tech:"node",displayNumber:9,sortKey:2041},{id:"node-2042",number:10,title:"What is the access token / refresh token pattern?",question:"What is the access token / refresh token pattern?",difficulty:"Experienced",category:"Security & Authentication",answer:"A short-lived access token authorizes API calls; a long-lived refresh token (stored securely, e.g. HttpOnly cookie) obtains new access tokens. This limits exposure if an access token leaks.\n\n```js\nconst access = jwt.sign(claims, s, { expiresIn: '15m' })\nconst refresh = jwt.sign({ sub }, r, { expiresIn: '7d' })\n```",tech:"node",displayNumber:10,sortKey:2042},{id:"node-2043",number:11,title:"Where should you store tokens on the client?",question:"Where should you store tokens on the client?",difficulty:"Experienced",category:"Security & Authentication",answer:"Prefer HttpOnly, Secure, SameSite cookies (inaccessible to JS, mitigating XSS theft). `localStorage` is vulnerable to XSS. Cookies need CSRF protection; tokens in headers avoid CSRF but are exposed to XSS.\n\n```js\nres.cookie('token', jwt, { httpOnly: true, secure: true, sameSite: 'strict' })\n```",tech:"node",displayNumber:11,sortKey:2043},{id:"node-2044",number:12,title:"What is the `alg: none` JWT vulnerability?",question:"What is the `alg: none` JWT vulnerability?",difficulty:"Experienced",category:"Security & Authentication",answer:"Some libraries accepted tokens with `alg: none` (no signature), letting attackers forge tokens. Always pin the expected algorithm on verify and reject `none`.\n\n```js\njwt.verify(token, key, { algorithms: ['HS256'] }) // pin algorithm\n```",tech:"node",displayNumber:12,sortKey:2044},{id:"node-2045",number:13,title:"What is the algorithm confusion attack (RS256 vs HS256)?",question:"What is the algorithm confusion attack (RS256 vs HS256)?",difficulty:"Experienced",category:"Security & Authentication",answer:"If a server accepts either RS256 or HS256, an attacker can sign a token with HS256 using the public RSA key as the HMAC secret. Prevent it by pinning the exact algorithm and separating key usage.\n\n```js\njwt.verify(token, publicKey, { algorithms: ['RS256'] }) // no HS fallback\n```",tech:"node",displayNumber:13,sortKey:2045},{id:"node-2046",number:14,title:"What is OAuth 2.0?",question:"What is OAuth 2.0?",difficulty:"Advanced",category:"Security & Authentication",answer:"OAuth 2.0 is a delegated authorization framework: a user grants a third-party app limited access to their resources without sharing credentials, via tokens issued by an authorization server.\n\n```js\n// redirect to provider -> user consents -> callback with code -> exchange for token\n```",tech:"node",displayNumber:14,sortKey:2046},{id:"node-2047",number:15,title:"What is the OAuth authorization code flow with PKCE?",question:"What is the OAuth authorization code flow with PKCE?",difficulty:"Experienced",category:"Security & Authentication",answer:"The app redirects to the provider with a code challenge, receives an auth code on callback, then exchanges it (plus the code verifier) for tokens. PKCE prevents code interception attacks, essential for public/mobile clients.\n\n```js\n// challenge = SHA256(verifier); exchange code + verifier for tokens\n```",tech:"node",displayNumber:15,sortKey:2047},{id:"node-2048",number:16,title:"What is the difference between OAuth and OpenID Connect?",question:"What is the difference between OAuth and OpenID Connect?",difficulty:"Advanced",category:"Security & Authentication",answer:"OAuth 2.0 is for authorization (access to resources). OpenID Connect (OIDC) is an identity layer on top that adds an ID token (JWT) proving who the user is — i.e. authentication.\n\n```js\n// OIDC adds id_token with user identity claims\n```",tech:"node",displayNumber:16,sortKey:2048},{id:"node-2049",number:17,title:"What is XSS and how do you prevent it?",question:"What is XSS and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Cross-Site Scripting injects malicious scripts into pages viewed by others. Prevent by escaping/encoding output, using frameworks that auto-escape, a strict Content-Security-Policy, and never inserting untrusted HTML.\n\n```js\nres.setHeader('Content-Security-Policy', \"default-src 'self'\")\n```",tech:"node",displayNumber:17,sortKey:2049},{id:"node-2050",number:18,title:"What is CSRF and how do you prevent it?",question:"What is CSRF and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Cross-Site Request Forgery makes a logged-in user's browser send unwanted requests using their cookies. Prevent with `SameSite` cookies, anti-CSRF tokens, and requiring a non-simple header. Header-based tokens (not cookies) sidestep it.\n\n```js\nres.cookie('sid', v, { sameSite: 'strict' }) // + CSRF token for forms\n```",tech:"node",displayNumber:18,sortKey:2050},{id:"node-2051",number:19,title:"What is SQL injection prevention in one line?",question:"What is SQL injection prevention in one line?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Always use parameterized queries/prepared statements; never build SQL by concatenating untrusted input.\n\n```js\ndb.query('SELECT * FROM u WHERE email = $1', [email])\n```",tech:"node",displayNumber:19,sortKey:2051},{id:"node-2052",number:20,title:"What is command injection and how do you avoid it?",question:"What is command injection and how do you avoid it?",difficulty:"Experienced",category:"Security & Authentication",answer:"Command injection runs attacker-controlled shell commands via `exec` with interpolated input. Avoid it by using `execFile`/`spawn` with argument arrays (no shell) and validating/whitelisting inputs.\n\n```js\nexecFile('convert', [userPath, out]) // no shell, no injection\n```",tech:"node",displayNumber:20,sortKey:2052},{id:"node-2053",number:21,title:"What is the principle of least privilege?",question:"What is the principle of least privilege?",difficulty:"Advanced",category:"Security & Authentication",answer:"Grant each component/user the minimum permissions needed. Limit DB roles, API scopes, file permissions, and cloud IAM so a compromise has minimal blast radius.\n\n```js\n// app DB user: SELECT/INSERT/UPDATE only, no DROP/GRANT\n```",tech:"node",displayNumber:21,sortKey:2053},{id:"node-2054",number:22,title:"How do you generate cryptographically secure random values?",question:"How do you generate cryptographically secure random values?",difficulty:"Advanced",category:"Security & Authentication",answer:"Use `crypto.randomBytes`/`crypto.randomUUID`/`crypto.webcrypto`, never `Math.random()` (predictable). Use them for tokens, salts, session IDs, and password-reset codes.\n\n```js\nconst token = crypto.randomBytes(32).toString('hex')\n```",tech:"node",displayNumber:22,sortKey:2054},{id:"node-2055",number:23,title:"Why is `Math.random()` insecure for tokens?",question:"Why is `Math.random()` insecure for tokens?",difficulty:"Advanced",category:"Security & Authentication",answer:"`Math.random()` is a non-cryptographic PRNG whose output can be predicted from observed values, letting attackers guess tokens/IDs. Security-sensitive randomness must come from a CSPRNG.\n\n```js\n// insecure: Math.random().toString(36)\ncrypto.randomUUID() // secure\n```",tech:"node",displayNumber:23,sortKey:2055},{id:"node-2056",number:24,title:"What is a timing attack and how do you prevent it?",question:"What is a timing attack and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"A timing attack infers secrets from how long comparisons take (e.g. early-exit string compare). Use constant-time comparison (`crypto.timingSafeEqual`) for tokens/HMACs.\n\n```js\ncrypto.timingSafeEqual(Buffer.from(a), Buffer.from(b))\n```",tech:"node",displayNumber:24,sortKey:2056},{id:"node-2057",number:25,title:"How do you sign and verify data with HMAC?",question:"How do you sign and verify data with HMAC?",difficulty:"Advanced",category:"Security & Authentication",answer:"HMAC combines a secret key with a hash to produce a tag that proves integrity/authenticity. Recompute and compare in constant time to verify (e.g. webhook signatures).\n\n```js\nconst mac = crypto.createHmac('sha256', secret).update(body).digest('hex')\n```",tech:"node",displayNumber:25,sortKey:2057},{id:"node-2058",number:26,title:"What is the difference between hashing, encryption, and encoding?",question:"What is the difference between hashing, encryption, and encoding?",difficulty:"Intermediate",category:"Security & Authentication",answer:"Hashing is one-way (integrity, passwords). Encryption is two-way with a key (confidentiality). Encoding (base64/hex) is reversible with no key and provides no security — just representation.\n\n```js\nBuffer.from('hi').toString('base64') // encoding, NOT security\n```",tech:"node",displayNumber:26,sortKey:2058},{id:"node-2059",number:27,title:"How do you encrypt sensitive data at rest?",question:"How do you encrypt sensitive data at rest?",difficulty:"Experienced",category:"Security & Authentication",answer:"Use authenticated symmetric encryption (AES-256-GCM) with a securely managed key (KMS), storing the IV and auth tag. GCM provides confidentiality and integrity.\n\n```js\nconst c = crypto.createCipheriv('aes-256-gcm', key, iv)\nconst enc = Buffer.concat([c.update(data), c.final()]); const tag = c.getAuthTag()\n```",tech:"node",displayNumber:27,sortKey:2059},{id:"node-2060",number:28,title:"Why use AES-GCM over AES-CBC?",question:"Why use AES-GCM over AES-CBC?",difficulty:"Experienced",category:"Security & Authentication",answer:"GCM is authenticated encryption (AEAD): it detects tampering via an auth tag. CBC provides only confidentiality and needs a separate MAC; misuse leads to padding-oracle attacks. Prefer GCM.\n\n```js\ncrypto.createCipheriv('aes-256-gcm', key, iv) // authenticated\n```",tech:"node",displayNumber:28,sortKey:2060},{id:"node-2061",number:29,title:"What is key rotation and why does it matter?",question:"What is key rotation and why does it matter?",difficulty:"Experienced",category:"Security & Authentication",answer:"Key rotation periodically replaces cryptographic keys to limit exposure if a key leaks. Support multiple active keys (by key id) so old data can still be decrypted/verified during transition.\n\n```js\nconst key = keyring[token.kid] // pick key by id, enabling rotation\n```",tech:"node",displayNumber:29,sortKey:2061},{id:"node-2062",number:30,title:"How do you securely handle environment secrets?",question:"How do you securely handle environment secrets?",difficulty:"Advanced",category:"Security & Authentication",answer:"Load secrets from a secret manager (Vault, AWS Secrets Manager) or injected env vars, never commit them, restrict access, rotate regularly, and avoid logging them.\n\n```js\nconst dbPass = await secrets.get('db/password') // not in code/repo\n```",tech:"node",displayNumber:30,sortKey:2062},{id:"node-2063",number:31,title:"What is rate limiting's role in security?",question:"What is rate limiting's role in security?",difficulty:"Advanced",category:"Security & Authentication",answer:"Rate limiting throttles requests per client to slow brute-force credential attacks, prevent abuse/DoS, and protect expensive endpoints. Combine per-IP and per-account limits.\n\n```js\nloginLimiter({ windowMs: 15 * 60000, max: 5 }) // 5 login tries / 15 min\n```",tech:"node",displayNumber:31,sortKey:2063},{id:"node-2064",number:32,title:"How do you protect against brute-force login attacks?",question:"How do you protect against brute-force login attacks?",difficulty:"Experienced",category:"Security & Authentication",answer:"Rate-limit login attempts, add exponential backoff/lockouts per account, use CAPTCHA after failures, require strong passwords/MFA, and use slow password hashing so each guess is costly.\n\n```js\nif (attempts > 5) await sleep(2 ** attempts * 100) // backoff\n```",tech:"node",displayNumber:32,sortKey:2064},{id:"node-2065",number:33,title:"What is multi-factor authentication (MFA)?",question:"What is multi-factor authentication (MFA)?",difficulty:"Advanced",category:"Security & Authentication",answer:"MFA requires two+ factors: something you know (password), have (TOTP app/hardware key), or are (biometric). It protects accounts even if the password is compromised.\n\n```js\nconst valid = speakeasy.totp.verify({ secret, token: userCode })\n```",tech:"node",displayNumber:33,sortKey:2065},{id:"node-2066",number:34,title:"How does TOTP work?",question:"How does TOTP work?",difficulty:"Experienced",category:"Security & Authentication",answer:"Time-based One-Time Password derives a short code from a shared secret and the current time window (usually 30s) via HMAC. Both server and authenticator app compute the same code independently.\n\n```js\nspeakeasy.totp({ secret, encoding: 'base32' }) // 6-digit code\n```",tech:"node",displayNumber:34,sortKey:2066},{id:"node-2067",number:35,title:"What are security headers and which are essential?",question:"What are security headers and which are essential?",difficulty:"Experienced",category:"Security & Authentication",answer:"Essential headers: `Content-Security-Policy`, `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `X-Frame-Options` (clickjacking), and `Referrer-Policy`. `helmet` sets sane defaults.\n\n```js\napp.use(helmet())\n```",tech:"node",displayNumber:35,sortKey:2067},{id:"node-2068",number:36,title:"What is clickjacking and how do you prevent it?",question:"What is clickjacking and how do you prevent it?",difficulty:"Advanced",category:"Security & Authentication",answer:"Clickjacking tricks users into clicking hidden framed content. Prevent by disallowing framing with `X-Frame-Options: DENY` or CSP `frame-ancestors 'none'`.\n\n```js\nres.setHeader('Content-Security-Policy', \"frame-ancestors 'none'\")\n```",tech:"node",displayNumber:36,sortKey:2068},{id:"node-2069",number:37,title:"How do you prevent NoSQL injection?",question:"How do you prevent NoSQL injection?",difficulty:"Experienced",category:"Security & Authentication",answer:"Attackers can inject query operators (e.g. `{ $ne: null }`) via JSON. Validate/cast types, reject object values where scalars are expected, and use schema validation to strip operators.\n\n```js\nif (typeof req.body.email !== 'string') return res.status(400).end()\n```",tech:"node",displayNumber:37,sortKey:2069},{id:"node-2070",number:38,title:"What is the danger of `eval` and dynamic code?",question:"What is the danger of `eval` and dynamic code?",difficulty:"Advanced",category:"Security & Authentication",answer:"`eval`/`new Function` executes arbitrary strings as code; with untrusted input this is remote code execution. Avoid them; parse data with `JSON.parse` and use safe alternatives.\n\n```js\nJSON.parse(input) // not eval('(' + input + ')')\n```",tech:"node",displayNumber:38,sortKey:2070},{id:"node-2071",number:39,title:"What is prototype pollution?",question:"What is prototype pollution?",difficulty:"Experienced",category:"Security & Authentication",answer:"Prototype pollution injects `__proto__`/`constructor.prototype` keys via merged untrusted objects, altering all objects' behavior and enabling DoS/RCE. Guard by rejecting those keys, using `Object.create(null)`, or `Map`.\n\n```js\nif (key === '__proto__' || key === 'constructor') continue\n```",tech:"node",displayNumber:39,sortKey:2071},{id:"node-2072",number:40,title:"How do you validate and sanitize user input?",question:"How do you validate and sanitize user input?",difficulty:"Advanced",category:"Security & Authentication",answer:"Validate structure/type/range against a schema at the boundary, reject invalid input, and sanitize/escape when outputting (HTML, SQL, shell). Treat all external input as hostile.\n\n```js\nconst data = schema.parse(req.body) // whitelist-validate\n```",tech:"node",displayNumber:40,sortKey:2072},{id:"node-2073",number:41,title:"What is the OWASP Top 10?",question:"What is the OWASP Top 10?",difficulty:"Advanced",category:"Security & Authentication",answer:"A widely-used list of the most critical web app security risks (e.g. injection, broken access control, cryptographic failures, SSRF). It guides prioritizing defenses.\n\n```js\n// checklist: authz, injection, crypto, SSRF, misconfig, ...\n```",tech:"node",displayNumber:41,sortKey:2073},{id:"node-2074",number:42,title:"What is broken access control and how do you prevent it?",question:"What is broken access control and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"It's failing to enforce what users may do — e.g. IDOR where changing an ID accesses others' data. Prevent by checking authorization on every request against the resource owner, never trusting client-side checks.\n\n```js\nif (order.userId !== req.user.id) return res.status(403).end()\n```",tech:"node",displayNumber:42,sortKey:2074},{id:"node-2075",number:43,title:"What is IDOR (Insecure Direct Object Reference)?",question:"What is IDOR (Insecure Direct Object Reference)?",difficulty:"Experienced",category:"Security & Authentication",answer:"IDOR occurs when an object reference (id) in a request isn't checked against the requester's permissions, letting them access others' resources by guessing/changing ids. Always verify ownership/scope server-side.\n\n```js\nconst doc = await db.docs.findOne({ _id: id, ownerId: req.user.id })\n```",tech:"node",displayNumber:43,sortKey:2075},{id:"node-2076",number:44,title:"How do you securely reset passwords?",question:"How do you securely reset passwords?",difficulty:"Experienced",category:"Security & Authentication",answer:"Email a single-use, expiring, cryptographically-random token (store only its hash), verify it, then update the password and invalidate existing sessions. Don't reveal whether an email exists.\n\n```js\nconst token = crypto.randomBytes(32).toString('hex')\nawait db.saveReset(userId, sha256(token), Date.now() + 3600e3)\n```",tech:"node",displayNumber:44,sortKey:2076},{id:"node-2077",number:45,title:"Why avoid leaking whether a username/email exists?",question:"Why avoid leaking whether a username/email exists?",difficulty:"Advanced",category:"Security & Authentication",answer:"Different responses for existing vs non-existing accounts enable user enumeration, aiding targeted attacks. Return generic messages and consistent timing for login/reset flows.\n\n```js\nreturn res.json({ message: 'If the account exists, an email was sent' })\n```",tech:"node",displayNumber:45,sortKey:2077},{id:"node-2078",number:46,title:"What is HTTPS and why is it mandatory?",question:"What is HTTPS and why is it mandatory?",difficulty:"Intermediate",category:"Security & Authentication",answer:"HTTPS encrypts traffic with TLS, protecting credentials/data from eavesdropping and tampering and authenticating the server. Without it, cookies/tokens can be stolen on the network.\n\n```js\nres.setHeader('Strict-Transport-Security', 'max-age=31536000')\n```",tech:"node",displayNumber:46,sortKey:2078},{id:"node-2079",number:47,title:"What is certificate pinning?",question:"What is certificate pinning?",difficulty:"Experienced",category:"Security & Authentication",answer:"Pinning restricts accepted TLS certificates to a known set (or CA), so a mis-issued/rogue certificate is rejected, defeating some MITM attacks. It adds operational risk when certs rotate.\n\n```js\nconst agent = new https.Agent({ ca: fs.readFileSync('pinned-ca.pem') })\n```",tech:"node",displayNumber:47,sortKey:2079},{id:"node-2080",number:48,title:"How do you handle dependency vulnerabilities?",question:"How do you handle dependency vulnerabilities?",difficulty:"Advanced",category:"Security & Authentication",answer:"Scan with `npm audit`/Snyk/Dependabot, keep dependencies updated, pin via lockfiles, minimize the dependency surface, and review install scripts to reduce supply-chain risk.\n\n```js\nnpm audit --audit-level=high\n```",tech:"node",displayNumber:48,sortKey:2080},{id:"node-2081",number:49,title:"What is a supply-chain attack?",question:"What is a supply-chain attack?",difficulty:"Experienced",category:"Security & Authentication",answer:"Attackers compromise a dependency (typosquatting, hijacked maintainer, malicious update) so your build/runtime executes their code. Mitigate with lockfiles, `--ignore-scripts`, provenance checks, and minimal trusted deps.\n\n```js\nnpm ci --ignore-scripts\n```",tech:"node",displayNumber:49,sortKey:2081},{id:"node-2082",number:50,title:"How do you prevent sensitive data exposure in logs?",question:"How do you prevent sensitive data exposure in logs?",difficulty:"Advanced",category:"Security & Authentication",answer:"Redact passwords, tokens, PII, and secrets before logging; use structured loggers with redaction rules, and never log full request bodies/headers containing credentials.\n\n```js\nlogger.info({ user: id }, 'login') // no password/token fields\n```",tech:"node",displayNumber:50,sortKey:2082},{id:"node-2083",number:51,title:"What is the secure cookie flag set and its purpose?",question:"What is the secure cookie flag set and its purpose?",difficulty:"Advanced",category:"Security & Authentication",answer:"`HttpOnly` blocks JS access (XSS theft), `Secure` sends only over HTTPS, `SameSite` limits cross-site sending (CSRF), and `__Host-` prefix enforces path/host binding. Combine them for session cookies.\n\n```js\nres.cookie('sid', v, { httpOnly: true, secure: true, sameSite: 'strict' })\n```",tech:"node",displayNumber:51,sortKey:2083},{id:"node-2084",number:52,title:"What is session fixation and how do you prevent it?",question:"What is session fixation and how do you prevent it?",difficulty:"Experienced",category:"Security & Authentication",answer:"An attacker sets a known session id before login, then hijacks the authenticated session. Prevent by regenerating the session id on privilege change (login) so the pre-set id becomes useless.\n\n```js\nreq.session.regenerate(() => { req.session.userId = user.id })\n```",tech:"node",displayNumber:52,sortKey:2084},{id:"node-2085",number:53,title:"How do you protect an API with API keys?",question:"How do you protect an API with API keys?",difficulty:"Advanced",category:"Security & Authentication",answer:"Issue random keys, store only their hashes, transmit over HTTPS in a header, scope/rate-limit per key, and support rotation/revocation. Keys identify apps, not end users.\n\n```js\nconst hash = sha256(req.headers['x-api-key'])\nconst key = await db.keys.findOne({ hash, active: true })\n```",tech:"node",displayNumber:53,sortKey:2085},{id:"node-2086",number:54,title:"What is the difference between authentication tokens in header vs cookie?",question:"What is the difference between authentication tokens in header vs cookie?",difficulty:"Experienced",category:"Security & Authentication",answer:"Header (Bearer) tokens aren't sent automatically, so they resist CSRF but are exposed to XSS if stored in JS-accessible storage. HttpOnly cookies resist XSS theft but need CSRF defenses. Pick based on your threat model.\n\n```js\nAuthorization: Bearer <token> // not auto-sent -> CSRF-resistant\n```",tech:"node",displayNumber:54,sortKey:2086},{id:"node-2087",number:55,title:"How do you implement logout with JWTs?",question:"How do you implement logout with JWTs?",difficulty:"Experienced",category:"Security & Authentication",answer:"Delete the client token/cookie and, for real invalidation, add the token's `jti` (or user token version) to a denylist until it expires, since JWTs can't be un-signed.\n\n```js\nawait redis.set(`revoked:${jti}`, '1', 'EX', ttl); res.clearCookie('token')\n```",tech:"node",displayNumber:55,sortKey:2087},{id:"node-2088",number:56,title:"What is CORS misconfiguration risk?",question:"What is CORS misconfiguration risk?",difficulty:"Experienced",category:"Security & Authentication",answer:"Reflecting the request `Origin` with `Access-Control-Allow-Credentials: true` lets any site make authenticated cross-origin requests. Whitelist specific trusted origins; never combine wildcard origin with credentials.\n\n```js\nif (allowlist.has(origin)) res.setHeader('Access-Control-Allow-Origin', origin)\n```",tech:"node",displayNumber:56,sortKey:2088},{id:"node-2089",number:57,title:"How do you securely handle file uploads?",question:"How do you securely handle file uploads?",difficulty:"Experienced",category:"Security & Authentication",answer:"Validate type/size, store outside the web root with random names, scan for malware, never trust the client filename/MIME, and serve with correct `Content-Type` and `Content-Disposition` to prevent execution.\n\n```js\nconst name = crypto.randomUUID() + path.extname(original)\n```",tech:"node",displayNumber:57,sortKey:2089},{id:"node-2090",number:58,title:"What is SSRF and how do you defend against it?",question:"What is SSRF and how do you defend against it?",difficulty:"Experienced",category:"Security & Authentication",answer:"Server-Side Request Forgery abuses server-side fetching to reach internal services/metadata endpoints. Defend by whitelisting destinations, blocking private/link-local IPs, disabling redirects to them, and validating URLs.\n\n```js\nif (isPrivate(dns.lookup(host))) throw new Error('blocked host')\n```",tech:"node",displayNumber:58,sortKey:2090},{id:"node-2091",number:59,title:"How do you prevent open redirects?",question:"How do you prevent open redirects?",difficulty:"Advanced",category:"Security & Authentication",answer:"Don't redirect to arbitrary user-supplied URLs (used in phishing). Whitelist allowed paths/hosts or use mapping keys instead of raw URLs.\n\n```js\nconst dest = allowed[req.query.next] ?? '/'\nres.redirect(dest)\n```",tech:"node",displayNumber:59,sortKey:2091},{id:"node-2092",number:60,title:"What is content-type sniffing and `nosniff`?",question:"What is content-type sniffing and `nosniff`?",difficulty:"Advanced",category:"Security & Authentication",answer:"Browsers may guess a response's type, potentially executing uploaded files as scripts. `X-Content-Type-Options: nosniff` forces the declared `Content-Type`, preventing MIME-sniffing attacks.\n\n```js\nres.setHeader('X-Content-Type-Options', 'nosniff')\n```",tech:"node",displayNumber:60,sortKey:2092},{id:"node-2093",number:61,title:"How do you secure inter-service communication?",question:"How do you secure inter-service communication?",difficulty:"Experienced",category:"Security & Authentication",answer:"Use mutual TLS (mTLS) or signed service tokens (JWT with short expiry), authorize by service identity, encrypt in transit, and apply least-privilege network policies between services.\n\n```js\nhttps.request({ cert: clientCert, key: clientKey, ... }) // mTLS\n```",tech:"node",displayNumber:61,sortKey:2093},{id:"node-2094",number:62,title:"What is the difference between symmetric and asymmetric encryption?",question:"What is the difference between symmetric and asymmetric encryption?",difficulty:"Advanced",category:"Security & Authentication",answer:"Symmetric uses one shared secret key (fast, for bulk data, e.g. AES). Asymmetric uses a public/private key pair (slower, for key exchange and signatures, e.g. RSA/ECDSA). TLS combines both.\n\n```js\n// AES for data, RSA/ECDH to exchange the AES key\n```",tech:"node",displayNumber:62,sortKey:2094},{id:"node-2095",number:63,title:"How do you sign JWTs asymmetrically (RS256) and why?",question:"How do you sign JWTs asymmetrically (RS256) and why?",difficulty:"Experienced",category:"Security & Authentication",answer:"Sign with a private key; anyone can verify with the public key. This lets multiple services verify tokens without sharing a signing secret, ideal for distributed systems and third-party verification.\n\n```js\njwt.sign(claims, privateKey, { algorithm: 'RS256' })\njwt.verify(token, publicKey, { algorithms: ['RS256'] })\n```",tech:"node",displayNumber:63,sortKey:2095},{id:"node-2096",number:64,title:"What is a nonce and where is it used?",question:"What is a nonce and where is it used?",difficulty:"Advanced",category:"Security & Authentication",answer:"A nonce is a number used once to prevent replay and ensure uniqueness — in CSP script hashes, OAuth/OIDC requests, and encryption IVs. Reusing a nonce (especially in GCM) breaks security.\n\n```js\nconst nonce = crypto.randomBytes(16).toString('base64')\nres.setHeader('Content-Security-Policy', `script-src 'nonce-${nonce}'`)\n```",tech:"node",displayNumber:64,sortKey:2096},{id:"node-2097",number:65,title:"How do you protect against replay attacks?",question:"How do you protect against replay attacks?",difficulty:"Experienced",category:"Security & Authentication",answer:"Include a timestamp and nonce in signed requests, reject old/duplicate ones (track seen nonces within a window), and use short token lifetimes. Common for webhooks and API signing.\n\n```js\nif (Date.now() - ts > 300000 || seen.has(nonce)) reject()\n```",tech:"node",displayNumber:65,sortKey:2097},{id:"node-2098",number:66,title:"What is defense in depth?",question:"What is defense in depth?",difficulty:"Advanced",category:"Security & Authentication",answer:"Layering multiple independent controls (validation, authz, WAF, least privilege, encryption, monitoring) so if one fails, others still protect the system. No single control is trusted alone.\n\n```js\n// validate + parameterize + authorize + rate-limit + log\n```",tech:"node",displayNumber:66,sortKey:2098},{id:"node-2099",number:67,title:"How do you handle secrets in Docker/CI?",question:"How do you handle secrets in Docker/CI?",difficulty:"Experienced",category:"Security & Authentication",answer:"Inject secrets at runtime via environment/secret stores, not baked into images or committed to CI configs. Use masked CI secrets, avoid `ARG`/`ENV` for secrets in Dockerfiles, and scan images.\n\n```js\ndocker run --env-file secrets.env app # not COPY secrets into image\n```",tech:"node",displayNumber:67,sortKey:2099},{id:"node-2100",number:68,title:"What is subresource integrity (SRI)?",question:"What is subresource integrity (SRI)?",difficulty:"Experienced",category:"Security & Authentication",answer:'SRI adds an `integrity` hash to script/link tags so the browser verifies a CDN-loaded file wasn\'t tampered with, failing if the hash doesn\'t match.\n\n```js\n// <script src="cdn/lib.js" integrity="sha384-..." crossorigin="anonymous">\n```',tech:"node",displayNumber:68,sortKey:2100},{id:"node-2101",number:69,title:"How do you audit and monitor security events?",question:"How do you audit and monitor security events?",difficulty:"Experienced",category:"Security & Authentication",answer:"Log authentication attempts, authorization failures, and sensitive actions with context; centralize logs, alert on anomalies (spikes in 401/403, new geos), and retain audit trails immutably for investigation.\n\n```js\nlogger.warn({ userId, ip }, 'authz_denied') // feed to SIEM/alerts\n```",tech:"node",displayNumber:69,sortKey:2101},{id:"node-2102",number:70,title:"What is a security misconfiguration and common examples?",question:"What is a security misconfiguration and common examples?",difficulty:"Advanced",category:"Security & Authentication",answer:"Insecure defaults or incomplete setup: verbose error stacks in prod, default credentials, open cloud buckets, disabled TLS, permissive CORS, exposed admin endpoints. Harden defaults, minimize surface, and review configs.\n\n```js\nif (process.env.NODE_ENV === 'production') app.set('json spaces', 0) // no stack leaks\n```",tech:"node",displayNumber:70,sortKey:2102},{id:"node-2103",number:1,title:"How do you profile a Node application?",question:"How do you profile a Node application?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use `node --prof` + `--prof-process`, the `--inspect` DevTools CPU profiler, or flame-graph tools (`clinic flame`, `0x`) to find hot functions and blocking code.\n\n```js\n$ node --prof app.js && node --prof-process isolate-*.log\n```",tech:"node",displayNumber:1,sortKey:2103},{id:"node-2104",number:2,title:"What is a flame graph?",question:"What is a flame graph?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"A flame graph visualizes stack samples: width shows time spent, stacking shows call hierarchy. Wide bars at the top are hotspots to optimize.\n\n```js\n$ npx 0x app.js\n```",tech:"node",displayNumber:2,sortKey:2104},{id:"node-2105",number:3,title:"How do you detect event loop blocking?",question:"How do you detect event loop blocking?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Measure event loop delay with `perf_hooks.monitorEventLoopDelay` or a drift timer. High delay means synchronous CPU work is blocking; offload it to worker threads or chunk it.\n\n```js\nconst h = require('perf_hooks').monitorEventLoopDelay(); h.enable()\n```",tech:"node",displayNumber:3,sortKey:2105},{id:"node-2106",number:4,title:"What are common causes of high memory usage?",question:"What are common causes of high memory usage?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Unbounded caches, buffering large payloads instead of streaming, retained closures/listeners, global accumulation, and large in-memory data structures. Profile heap snapshots to locate retainers.\n\n```js\nconsole.log(process.memoryUsage().heapUsed)\n```",tech:"node",displayNumber:4,sortKey:2106},{id:"node-2107",number:5,title:"How do you take and analyze a heap snapshot?",question:"How do you take and analyze a heap snapshot?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Start with `--inspect`, capture a heap snapshot in Chrome DevTools (or `v8.writeHeapSnapshot()`), and compare snapshots over time to find objects that grow — the leak's retainers.\n\n```js\nrequire('v8').writeHeapSnapshot('./heap.heapsnapshot')\n```",tech:"node",displayNumber:5,sortKey:2107},{id:"node-2108",number:6,title:"How do you scale a Node app across CPU cores?",question:"How do you scale a Node app across CPU cores?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Run one process per core with the `cluster` module or a process manager (pm2), or run multiple container replicas behind a load balancer. Node's single thread can't use multiple cores alone.\n\n```js\npm2 start app.js -i max # one instance per core\n```",tech:"node",displayNumber:6,sortKey:2108},{id:"node-2109",number:7,title:"What is horizontal vs vertical scaling?",question:"What is horizontal vs vertical scaling?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Vertical scaling adds resources (CPU/RAM) to one machine (simple but capped). Horizontal scaling adds more machines/instances behind a balancer (elastic, requires statelessness). Node apps typically scale horizontally.\n\n```js\n// stateless app + LB + N replicas -> horizontal scale\n```",tech:"node",displayNumber:7,sortKey:2109},{id:"node-2110",number:8,title:"How do you offload CPU-bound work?",question:"How do you offload CPU-bound work?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Move heavy computation to worker threads, a separate service, or a job queue, keeping the main event loop responsive. Never run long synchronous loops in request handlers.\n\n```js\nnew Worker('./cpu-task.js', { workerData }).on('message', done)\n```",tech:"node",displayNumber:8,sortKey:2110},{id:"node-2111",number:9,title:"What is a job queue and when do you use one?",question:"What is a job queue and when do you use one?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A job queue (BullMQ/Redis, SQS) defers work to background workers, decoupling request handling from slow tasks (emails, image processing, reports) and enabling retries, rate control, and scaling workers independently.\n\n```js\nawait queue.add('email', { to, body })\n```",tech:"node",displayNumber:9,sortKey:2111},{id:"node-2112",number:10,title:"How do you implement caching to improve performance?",question:"How do you implement caching to improve performance?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Cache expensive results in memory (`lru-cache`) or Redis with TTLs, use HTTP caching headers/CDN for responses, and memoize pure computations. The hard part is invalidation on data change.\n\n```js\nconst cache = new LRUCache({ max: 1000, ttl: 60000 })\n```",tech:"node",displayNumber:10,sortKey:2112},{id:"node-2113",number:11,title:"What is the difference between in-memory and distributed cache?",question:"What is the difference between in-memory and distributed cache?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"In-memory cache is per-process (fast, but not shared and lost on restart). Distributed cache (Redis) is shared across instances (consistent, survives restarts) at network-call cost. Use in-memory for hot local data, Redis for shared state.\n\n```js\n// L1: process LRU (fast) -> L2: Redis (shared)\n```",tech:"node",displayNumber:11,sortKey:2113},{id:"node-2114",number:12,title:"How do you reduce startup time / cold starts?",question:"How do you reduce startup time / cold starts?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Lazy-load heavy modules, minimize top-level work, reduce dependencies, use snapshots/bundling, and keep instances warm (provisioned concurrency in serverless). Defer non-critical initialization.\n\n```js\nlet pdf; function makePdf() { pdf ??= require('pdfkit') } // lazy\n```",tech:"node",displayNumber:12,sortKey:2114},{id:"node-2115",number:13,title:"What is `--max-old-space-size`?",question:"What is `--max-old-space-size`?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"It sets V8's old-generation heap limit in MB. Raise it for memory-heavy apps, but prefer streaming/pagination over just enlarging the heap, and match container memory limits.\n\n```js\nnode --max-old-space-size=2048 app.js\n```",tech:"node",displayNumber:13,sortKey:2115},{id:"node-2116",number:14,title:"How do you benchmark code performance?",question:"How do you benchmark code performance?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use high-resolution timers (`perf_hooks.performance.now()`), run many iterations, warm up the JIT, and use tools like `benchmark.js` or `autocannon` for HTTP load. Beware micro-benchmark pitfalls.\n\n```js\n$ npx autocannon -c 100 -d 10 http://localhost:3000\n```",tech:"node",displayNumber:14,sortKey:2116},{id:"node-2117",number:15,title:"What is `autocannon` / load testing?",question:"What is `autocannon` / load testing?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Load testing sends concurrent traffic to measure throughput, latency percentiles, and error rates under stress. It reveals bottlenecks and capacity limits before production traffic does.\n\n```js\nautocannon -c 200 -d 30 http://localhost:3000/api\n```",tech:"node",displayNumber:15,sortKey:2117},{id:"node-2118",number:16,title:"Why look at latency percentiles (p95/p99) not averages?",question:"Why look at latency percentiles (p95/p99) not averages?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Averages hide tail latency; a few slow requests hurt user experience and cascade. p95/p99 show what the worst-served users experience, guiding SLOs and revealing GC pauses or contention.\n\n```js\n// optimize the p99, not just the mean\n```",tech:"node",displayNumber:16,sortKey:2118},{id:"node-2119",number:17,title:"What is the difference between throughput and latency?",question:"What is the difference between throughput and latency?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Latency is the time for one request; throughput is requests handled per unit time. They interact — high concurrency can raise throughput while worsening tail latency. Optimize the one your SLO targets.\n\n```js\n// 1000 req/s throughput, p99 latency 200ms\n```",tech:"node",displayNumber:17,sortKey:2119},{id:"node-2120",number:18,title:"How do you reduce JSON serialization overhead?",question:"How do you reduce JSON serialization overhead?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Serialize only needed fields, use schema-based fast serializers (`fast-json-stringify`), stream large responses, and avoid re-stringifying. `JSON.stringify` on huge objects blocks the loop.\n\n```js\nconst stringify = fastJson(schema) // precompiled, ~2x faster\n```",tech:"node",displayNumber:18,sortKey:2120},{id:"node-2121",number:19,title:"What is the unit testing pyramid?",question:"What is the unit testing pyramid?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Many fast unit tests at the base, fewer integration tests in the middle, and few slow end-to-end tests at the top. It balances coverage, speed, and confidence.\n\n```js\n// lots of unit, some integration, few e2e\n```",tech:"node",displayNumber:19,sortKey:2121},{id:"node-2122",number:20,title:"What testing frameworks are used in Node?",question:"What testing frameworks are used in Node?",difficulty:"Basic",category:"Performance, Testing & Deployment",answer:"Jest (all-in-one), Vitest (fast, ESM/TS), Mocha+Chai, and the built-in `node:test` runner. They provide assertions, mocking, and coverage.\n\n```js\nimport { test } from 'node:test'\nimport assert from 'node:assert'\ntest('adds', () => assert.equal(1 + 1, 2))\n```",tech:"node",displayNumber:20,sortKey:2122},{id:"node-2123",number:21,title:"What is the difference between unit, integration, and e2e tests?",question:"What is the difference between unit, integration, and e2e tests?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Unit tests isolate one function (mock deps). Integration tests verify components together (e.g. route + DB). E2E tests exercise the whole system as a user would. Each catches different bug classes.\n\n```js\n// unit: service.calc(); integration: POST /orders + db; e2e: browser flow\n```",tech:"node",displayNumber:21,sortKey:2123},{id:"node-2124",number:22,title:"What is mocking and when do you use it?",question:"What is mocking and when do you use it?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Mocking replaces real dependencies (DB, HTTP, time) with controllable fakes to isolate the unit, make tests deterministic/fast, and simulate errors. Over-mocking, however, can make tests brittle and unrealistic.\n\n```js\njest.spyOn(mailer, 'send').mockResolvedValue(true)\n```",tech:"node",displayNumber:22,sortKey:2124},{id:"node-2125",number:23,title:"What is the difference between a mock, stub, and spy?",question:"What is the difference between a mock, stub, and spy?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A stub returns canned values; a spy records how a real/replaced function was called; a mock is a preprogrammed fake with expectations you assert against. Terms overlap across libraries.\n\n```js\nconst spy = jest.fn(); handler(spy); expect(spy).toHaveBeenCalledWith(1)\n```",tech:"node",displayNumber:23,sortKey:2125},{id:"node-2126",number:24,title:"How do you test async code?",question:"How do you test async code?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Return/await the promise so the runner waits, assert on resolved values or rejections, and use fake timers to control delays without real waiting.\n\n```js\nawait expect(fetchUser(1)).resolves.toMatchObject({ id: 1 })\n```",tech:"node",displayNumber:24,sortKey:2126},{id:"node-2127",number:25,title:"What are fake timers?",question:"What are fake timers?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Fake timers replace `setTimeout`/`Date` so tests can advance time instantly, testing debounce/retry/scheduling logic without real delays.\n\n```js\njest.useFakeTimers(); run(); jest.advanceTimersByTime(1000)\n```",tech:"node",displayNumber:25,sortKey:2127},{id:"node-2128",number:26,title:"What is code coverage and its limits?",question:"What is code coverage and its limits?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"Coverage measures which lines/branches tests execute. High coverage doesn't guarantee correctness (you can execute code without asserting behavior). Use it to find untested areas, not as the sole quality metric.\n\n```js\n$ node --test --experimental-test-coverage\n```",tech:"node",displayNumber:26,sortKey:2128},{id:"node-2129",number:27,title:"How do you test HTTP endpoints without a real server port?",question:"How do you test HTTP endpoints without a real server port?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Export the app and use `supertest`, which spins up the server on an ephemeral port per test, sends requests, and asserts responses — isolated and parallel-safe.\n\n```js\nawait request(app).get('/health').expect(200)\n```",tech:"node",displayNumber:27,sortKey:2129},{id:"node-2130",number:28,title:"How do you isolate database tests?",question:"How do you isolate database tests?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use a dedicated test DB (containerized), wrap each test in a transaction that rolls back, or truncate between tests. Avoid shared state so tests are deterministic and parallelizable.\n\n```js\nafterEach(() => db.query('ROLLBACK'))\n```",tech:"node",displayNumber:28,sortKey:2130},{id:"node-2131",number:29,title:"What is test-driven development (TDD)?",question:"What is test-driven development (TDD)?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"TDD writes a failing test first, then minimal code to pass, then refactors (red-green-refactor). It drives design, ensures coverage, and provides a safety net for change.\n\n```js\n// 1. write failing test 2. make it pass 3. refactor\n```",tech:"node",displayNumber:29,sortKey:2131},{id:"node-2132",number:30,title:"What is a flaky test and how do you fix it?",question:"What is a flaky test and how do you fix it?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"A flaky test passes/fails nondeterministically, usually from timing, shared state, ordering, or real network. Fix by removing nondeterminism: fake timers, isolate state, mock external calls, and await properly.\n\n```js\n// replace sleep-based waits with deterministic awaits/fake timers\n```",tech:"node",displayNumber:30,sortKey:2132},{id:"node-2133",number:31,title:"How do you structure a CI pipeline for Node?",question:"How do you structure a CI pipeline for Node?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"On each push: install with `npm ci`, lint, run tests with coverage, build, and (on main) deploy. Cache dependencies, run stages in parallel, and fail fast on errors.\n\n```yaml\n- run: npm ci\n- run: npm test\n- run: npm run build\n```",tech:"node",displayNumber:31,sortKey:2133},{id:"node-2134",number:32,title:"Why use `npm ci` in CI instead of `npm install`?",question:"Why use `npm ci` in CI instead of `npm install`?",difficulty:"Intermediate",category:"Performance, Testing & Deployment",answer:"`npm ci` installs exactly from the lockfile (deleting `node_modules` first), giving reproducible, faster installs and failing if `package.json` and the lockfile disagree.\n\n```js\nnpm ci --omit=dev\n```",tech:"node",displayNumber:32,sortKey:2134},{id:"node-2135",number:33,title:"How do you containerize a Node app with Docker?",question:"How do you containerize a Node app with Docker?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:'Use an official Node base image, copy `package*.json` and install first (layer caching), copy source, set `NODE_ENV=production`, run as a non-root user, and use a multi-stage build to shrink the image.\n\n```dockerfile\nFROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./ \nRUN npm ci --omit=dev\nCOPY . .\nUSER node\nCMD ["node", "index.js"]\n```',tech:"node",displayNumber:33,sortKey:2135},{id:"node-2136",number:34,title:"What is a multi-stage Docker build?",question:"What is a multi-stage Docker build?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:`It uses one stage to build/compile (with dev deps and toolchains) and a slim final stage that copies only the artifacts, producing a smaller, more secure runtime image.

\`\`\`dockerfile
FROM node:20 AS build
RUN npm ci && npm run build
FROM node:20-alpine
COPY --from=build /app/dist ./dist
\`\`\``,tech:"node",displayNumber:34,sortKey:2136},{id:"node-2137",number:35,title:"Why run the container as a non-root user?",question:"Why run the container as a non-root user?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Running as root increases blast radius if the app is compromised (container escape, file tampering). Use the built-in `node` user or create one, following least privilege.\n\n```dockerfile\nUSER node\n```",tech:"node",displayNumber:35,sortKey:2137},{id:"node-2138",number:36,title:"How do you handle graceful shutdown in Kubernetes?",question:"How do you handle graceful shutdown in Kubernetes?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"K8s sends SIGTERM before killing a pod; handle it to stop accepting traffic (fail readiness), drain in-flight requests, close connections, and exit within `terminationGracePeriodSeconds`.\n\n```js\nprocess.on('SIGTERM', () => server.close(() => process.exit(0)))\n```",tech:"node",displayNumber:36,sortKey:2138},{id:"node-2139",number:37,title:"What are liveness and readiness probes?",question:"What are liveness and readiness probes?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Liveness checks the process is healthy (restart if not); readiness checks it can serve traffic now (remove from load balancing if not, without restarting). Implement lightweight HTTP endpoints for each.\n\n```js\napp.get('/live', (_, res) => res.end())\napp.get('/ready', async (_, res) => (await db.ping()) ? res.end() : res.status(503).end())\n```",tech:"node",displayNumber:37,sortKey:2139},{id:"node-2140",number:38,title:"How do you achieve zero-downtime deployment?",question:"How do you achieve zero-downtime deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use rolling/blue-green/canary deploys behind a load balancer with readiness gating and graceful shutdown, plus backward-compatible DB migrations so old and new versions coexist during rollout.\n\n```js\n// rolling update: new pods pass readiness before old pods drain\n```",tech:"node",displayNumber:38,sortKey:2140},{id:"node-2141",number:39,title:"What is blue-green deployment?",question:"What is blue-green deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Two identical environments (blue live, green idle). Deploy to green, test it, then switch traffic instantly. Rollback is switching back. It reduces risk but doubles infrastructure briefly.\n\n```js\n// switch LB target from blue -> green after health checks\n```",tech:"node",displayNumber:39,sortKey:2141},{id:"node-2142",number:40,title:"What is canary deployment?",question:"What is canary deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Route a small percentage of traffic to the new version, monitor errors/latency, then gradually increase if healthy or roll back if not. It limits the blast radius of bad releases.\n\n```js\n// 5% traffic -> new version, watch metrics, then ramp to 100%\n```",tech:"node",displayNumber:40,sortKey:2142},{id:"node-2143",number:41,title:"How do you manage configuration across environments?",question:"How do you manage configuration across environments?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Follow 12-factor: read all config from environment variables, validate at startup, keep secrets in a secret manager, and never bake env-specific values into images.\n\n```js\nconst cfg = envSchema.parse(process.env) // validated config\n```",tech:"node",displayNumber:41,sortKey:2143},{id:"node-2144",number:42,title:"What is a process manager like pm2?",question:"What is a process manager like pm2?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"pm2 keeps Node processes alive (auto-restart on crash), clusters across cores, does zero-downtime reloads, and centralizes logs/metrics — useful on VMs (containers often use the orchestrator instead).\n\n```js\npm2 start app.js -i max && pm2 save\n```",tech:"node",displayNumber:42,sortKey:2144},{id:"node-2145",number:43,title:"How do you handle logging in production?",question:"How do you handle logging in production?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use a structured JSON logger (`pino`) writing to stdout, include correlation IDs and levels, avoid logging secrets, and ship logs to a centralized system (ELK/Loki/Datadog) for search and alerting.\n\n```js\nlogger.info({ reqId, userId, ms }, 'request completed')\n```",tech:"node",displayNumber:43,sortKey:2145},{id:"node-2146",number:44,title:"Why log to stdout instead of files in containers?",question:"Why log to stdout instead of files in containers?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Containers are ephemeral; files vanish on restart. Writing structured logs to stdout lets the platform collect, aggregate, and route them (12-factor), decoupling apps from log storage.\n\n```js\nlogger.info(obj) // -> stdout -> platform log pipeline\n```",tech:"node",displayNumber:44,sortKey:2146},{id:"node-2147",number:45,title:"What is observability (logs, metrics, traces)?",question:"What is observability (logs, metrics, traces)?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"The three pillars: logs (discrete events), metrics (aggregated numbers like latency/throughput), and traces (request paths across services). Together they let you understand and debug production behavior.\n\n```js\nmetrics.increment('http.requests'); span.end() // metrics + trace\n```",tech:"node",displayNumber:45,sortKey:2147},{id:"node-2148",number:46,title:"What is distributed tracing?",question:"What is distributed tracing?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Tracing propagates a trace/span context (e.g. `traceparent`) across services so you can see a single request's full path and where latency accrues. OpenTelemetry is the standard instrumentation.\n\n```js\nconst span = tracer.startSpan('db.query'); await q(); span.end()\n```",tech:"node",displayNumber:46,sortKey:2148},{id:"node-2149",number:47,title:"How do you collect application metrics?",question:"How do you collect application metrics?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Expose counters/gauges/histograms (via `prom-client`) at a `/metrics` endpoint scraped by Prometheus, tracking request rate, error rate, latency, and resource usage for dashboards/alerts.\n\n```js\napp.get('/metrics', async (_, res) => res.end(await register.metrics()))\n```",tech:"node",displayNumber:47,sortKey:2149},{id:"node-2150",number:48,title:"What are the RED and USE metrics methods?",question:"What are the RED and USE metrics methods?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"RED (Rate, Errors, Duration) monitors request-driven services. USE (Utilization, Saturation, Errors) monitors resources (CPU, memory, disk). Together they cover service health and capacity.\n\n```js\n// RED: req/s, error%, p99 latency\n```",tech:"node",displayNumber:48,sortKey:2150},{id:"node-2151",number:49,title:"How do you set up health-based auto-restart?",question:"How do you set up health-based auto-restart?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Use orchestrator liveness probes or a process manager to restart on crash/hang, plus alerting. Ensure the app crashes on programmer errors (don't swallow) so it can be restarted clean.\n\n```js\nprocess.on('uncaughtException', e => { log(e); process.exit(1) })\n```",tech:"node",displayNumber:49,sortKey:2151},{id:"node-2152",number:50,title:"What is a memory leak's symptom in production?",question:"What is a memory leak's symptom in production?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Steadily climbing RSS/heap that never returns to baseline, rising GC frequency/pauses, growing latency, and eventual OOM restarts. Confirm with heap snapshots and monitor `memoryUsage` trends.\n\n```js\nsetInterval(() => metric('heap', process.memoryUsage().heapUsed), 10000)\n```",tech:"node",displayNumber:50,sortKey:2152},{id:"node-2153",number:51,title:"How do you handle secrets in deployment?",question:"How do you handle secrets in deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Inject secrets at runtime from a secret manager or orchestrator secrets, mount as env/files, restrict access, rotate, and keep them out of images, repos, and logs.\n\n```js\n// K8s: mount Secret as env; app reads process.env at boot\n```",tech:"node",displayNumber:51,sortKey:2153},{id:"node-2154",number:52,title:"What is the difference between `NODE_ENV=production` effects?",question:"What is the difference between `NODE_ENV=production` effects?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"`production` makes frameworks (Express, React) enable optimizations, disable verbose errors/debug, and cache views. Some libraries change behavior. Always set it in production for performance and safety.\n\n```js\n// Express caches templates; hides stack traces when production\n```",tech:"node",displayNumber:52,sortKey:2154},{id:"node-2155",number:53,title:"How do you minimize Docker image size?",question:"How do you minimize Docker image size?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use slim/alpine bases, multi-stage builds, `npm ci --omit=dev`, `.dockerignore` to exclude junk, and combine/clean layers. Smaller images deploy faster and reduce attack surface.\n\n```js\n// .dockerignore: node_modules, .git, tests\n```",tech:"node",displayNumber:53,sortKey:2155},{id:"node-2156",number:54,title:"How do you handle database migrations during deploy?",question:"How do you handle database migrations during deploy?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Run migrations as a separate, idempotent step before/at rollout, keep them backward-compatible for rolling deploys, and avoid destructive changes until old code is fully retired.\n\n```js\nnpx prisma migrate deploy # in CI/CD before app rollout\n```",tech:"node",displayNumber:54,sortKey:2156},{id:"node-2157",number:55,title:"What is feature flagging?",question:"What is feature flagging?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"Feature flags gate functionality at runtime so you can deploy code dark, roll out gradually, A/B test, and kill features instantly without redeploying. Decouples deploy from release.\n\n```js\nif (flags.isEnabled('new-checkout', user)) return newCheckout()\n```",tech:"node",displayNumber:55,sortKey:2157},{id:"node-2158",number:56,title:"How do you implement graceful degradation?",question:"How do you implement graceful degradation?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"When a dependency fails, serve reduced functionality (cached/stale data, defaults, skipped non-critical features) instead of a full outage, using timeouts, circuit breakers, and fallbacks.\n\n```js\nconst recs = await getRecs().catch(() => []) // degrade, don't fail page\n```",tech:"node",displayNumber:56,sortKey:2158},{id:"node-2159",number:57,title:"What is a circuit breaker in deployment resilience?",question:"What is a circuit breaker in deployment resilience?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"It trips open after repeated failures to a dependency, failing fast and giving it time to recover, then half-opens to test before closing. It prevents cascading failures and resource exhaustion.\n\n```js\nbreaker.fire(request).catch(() => fallback())\n```",tech:"node",displayNumber:57,sortKey:2159},{id:"node-2160",number:58,title:"How do you handle configuration reload without restart?",question:"How do you handle configuration reload without restart?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Watch a config source (file/consul) or receive a signal (`SIGHUP`) to reload settings into memory, applying changes atomically. Keep it optional; some changes still need a restart.\n\n```js\nprocess.on('SIGHUP', () => reloadConfig())\n```",tech:"node",displayNumber:58,sortKey:2160},{id:"node-2161",number:59,title:"What are the benefits of serverless for Node?",question:"What are the benefits of serverless for Node?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Serverless (Lambda/Cloud Functions) auto-scales, bills per invocation, and removes server management — great for spiky/event-driven workloads. Trade-offs: cold starts, execution limits, and statelessness.\n\n```js\nexport const handler = async (event) => ({ statusCode: 200, body: 'ok' })\n```",tech:"node",displayNumber:59,sortKey:2161},{id:"node-2162",number:60,title:"What causes cold starts and how do you reduce them?",question:"What causes cold starts and how do you reduce them?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Cold starts are the latency of initializing a new function instance (runtime + deps + init code). Reduce by shrinking bundle/deps, lazy-loading, provisioned concurrency, and keeping functions warm.\n\n```js\n// move heavy require() inside handler only if rarely used\n```",tech:"node",displayNumber:60,sortKey:2162},{id:"node-2163",number:61,title:"How do you handle stateful connections in serverless?",question:"How do you handle stateful connections in serverless?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Reuse connections across invocations by declaring them outside the handler, use connection proxies (RDS Proxy) to avoid exhausting DB connections from many concurrent instances, and prefer HTTP/serverless-friendly data stores.\n\n```js\nconst db = connect() // module scope: reused on warm invocations\nexport const handler = async () => db.query(...)\n```",tech:"node",displayNumber:61,sortKey:2163},{id:"node-2164",number:62,title:"What is the twelve-factor app methodology?",question:"What is the twelve-factor app methodology?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"A set of practices for scalable, portable services: config in env, stateless processes, backing services as attached resources, logs to stdout, dev/prod parity, explicit dependencies, and disposability.\n\n```js\n// config via env, stateless, logs to stdout -> 12-factor\n```",tech:"node",displayNumber:62,sortKey:2164},{id:"node-2165",number:63,title:"How do you keep processes stateless for scaling?",question:"How do you keep processes stateless for scaling?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Store session/state in external stores (Redis/DB), not process memory, so any instance can serve any request and instances can be added/removed freely without sticky sessions.\n\n```js\napp.use(session({ store: new RedisStore({ client }) }))\n```",tech:"node",displayNumber:63,sortKey:2165},{id:"node-2166",number:64,title:"How do you run smoke tests after deployment?",question:"How do you run smoke tests after deployment?",difficulty:"Advanced",category:"Performance, Testing & Deployment",answer:"After deploy, hit critical endpoints (health, key user flows) against the live environment to confirm it works before shifting full traffic; roll back automatically if smoke tests fail.\n\n```js\nawait request('https://staging').get('/health').expect(200)\n```",tech:"node",displayNumber:64,sortKey:2166},{id:"node-2167",number:65,title:"How do you profile production safely?",question:"How do you profile production safely?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use low-overhead continuous profilers (Pyroscope, cloud profilers) or sampled `--inspect` on one instance, capture on-demand heap/CPU snapshots, and prefer metrics/traces to avoid impacting all traffic.\n\n```js\nprocess.on('SIGUSR2', () => require('v8').writeHeapSnapshot())\n```",tech:"node",displayNumber:65,sortKey:2167},{id:"node-2168",number:66,title:"What is the difference between `dependencies` size and runtime performance?",question:"What is the difference between `dependencies` size and runtime performance?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"More/larger dependencies slow installs, cold starts, and increase attack surface, but don't necessarily slow steady-state runtime. Trim unused deps for faster deploys and security, independent of hot-path speed.\n\n```js\nnpx depcheck # remove unused deps\n```",tech:"node",displayNumber:66,sortKey:2168},{id:"node-2169",number:67,title:"How do you set up alerting?",question:"How do you set up alerting?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Alert on symptoms users feel (error rate, latency SLO breaches, saturation) rather than every metric, with thresholds and routing to on-call. Avoid noisy alerts that cause fatigue.\n\n```js\n// alert if p99 latency > 500ms for 5m OR error rate > 1%\n```",tech:"node",displayNumber:67,sortKey:2169},{id:"node-2170",number:68,title:"What is an SLO/SLA/SLI?",question:"What is an SLO/SLA/SLI?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"An SLI is a measured indicator (e.g. success rate). An SLO is the internal target (e.g. 99.9%). An SLA is the external contractual promise with penalties. Error budgets derive from SLOs to balance reliability vs velocity.\n\n```js\n// SLI: successful requests / total; SLO: 99.9% monthly\n```",tech:"node",displayNumber:68,sortKey:2170},{id:"node-2171",number:69,title:"How do you roll back a bad deployment?",question:"How do you roll back a bad deployment?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Redeploy the previous known-good image/version (kept immutable and tagged), verify with smoke tests, and ensure DB migrations were backward-compatible so rollback doesn't corrupt data.\n\n```js\nkubectl rollout undo deployment/app\n```",tech:"node",displayNumber:69,sortKey:2171},{id:"node-2172",number:70,title:"How do you ensure database changes are rollback-safe?",question:"How do you ensure database changes are rollback-safe?",difficulty:"Experienced",category:"Performance, Testing & Deployment",answer:"Use expand-contract: add new columns/tables (expand), deploy code using both old and new, backfill, then remove old (contract) only after the new version is stable. Never drop/rename in the same release as the code change.\n\n```js\n// release1: add column; release2: use it; release3: drop old column\n```",tech:"node",displayNumber:70,sortKey:2172},{id:"microservices-2173",number:1,title:"What is the difference between Synchronous and Asynchronous communication in microservices?",question:"What is the difference between Synchronous and Asynchronous communication in microservices?",difficulty:"Basic",category:"Communication",answer:`- **Synchronous**: the caller sends a request and blocks until it receives a response (e.g. REST over HTTP, gRPC). Simple to reason about, but the caller's availability is now coupled to the callee's — if the downstream is slow or down, the caller suffers latency or failure. Chains of sync calls multiply latency and create cascading-failure risk.
- **Asynchronous**: the caller emits a message/event and does not wait (e.g. Kafka, RabbitMQ). Services are temporally decoupled — the consumer can process later. Improves resilience and scalability, but adds eventual consistency, ordering, and debugging complexity.

Rule of thumb: use sync for queries needing an immediate answer; use async for commands/events where the result can be processed in the background.

\`\`\`java
// SYNCHRONOUS — caller blocks for the response
OrderResponse resp = restClient.post()
    .uri("http://payment-service/charge")
    .body(chargeRequest)
    .retrieve()
    .body(OrderResponse.class);   // blocks here

// ASYNCHRONOUS — fire an event and move on
kafkaTemplate.send("order.placed", new OrderPlacedEvent(orderId, amount));
// payment-service consumes it whenever it can
\`\`\``,tech:"microservices",displayNumber:1,sortKey:2173},{id:"microservices-2174",number:2,title:"How is REST used in microservices and what are its trade-offs?",question:"How is REST used in microservices and what are its trade-offs?",difficulty:"Basic",category:"Communication",answer:`REST over HTTP/JSON is the default for synchronous service-to-service and client-to-service calls. It is ubiquitous, language-agnostic, cacheable, and human-readable. Trade-offs: JSON is verbose and slower to (de)serialize than binary formats, there is no built-in contract enforcement (needs OpenAPI), over-/under-fetching is common, and streaming is awkward. Good REST in microservices uses proper status codes, versioned URIs (\`/v1/orders\`), idempotency keys for retries, and pagination.

\`\`\`java
@RestController
@RequestMapping("/v1/orders")
class OrderController {
    @PostMapping
    ResponseEntity<Order> create(@RequestBody @Valid OrderRequest req,
                                 @RequestHeader("Idempotency-Key") String key) {
        Order o = service.createIdempotent(key, req);
        return ResponseEntity.status(HttpStatus.CREATED).body(o); // 201
    }

    @GetMapping("/{id}")
    ResponseEntity<Order> get(@PathVariable String id) {
        return service.find(id)
            .map(ResponseEntity::ok)
            .orElse(ResponseEntity.notFound().build());          // 404
    }
}
\`\`\``,tech:"microservices",displayNumber:2,sortKey:2174},{id:"microservices-2175",number:3,title:"What are gRPC and Protocol Buffers, and when would you use them over REST?",question:"What are gRPC and Protocol Buffers, and when would you use them over REST?",difficulty:"Intermediate",category:"Communication",answer:`**gRPC** is a high-performance RPC framework using HTTP/2 (multiplexing, streaming) and **Protocol Buffers** (Protobuf) — a compact binary, schema-first serialization format. You define services and messages in a \`.proto\` file; codegen produces strongly-typed client/server stubs in many languages. Benefits: smaller payloads, faster (de)serialization, bidirectional streaming, and an enforced contract. Use gRPC for internal, high-throughput, low-latency service-to-service calls. Keep REST/JSON at the public edge (browser support for gRPC is limited without gRPC-Web).

\`\`\`protobuf
// payment.proto — the contract, shared by both services
syntax = "proto3";
service PaymentService {
  rpc Charge (ChargeRequest) returns (ChargeReply);
}
message ChargeRequest { string order_id = 1; int64 amount_cents = 2; }
message ChargeReply   { bool success = 1; string txn_id = 2; }
\`\`\`
\`\`\`java
// Generated stub — strongly typed, no manual JSON parsing
ChargeReply reply = paymentStub.charge(
    ChargeRequest.newBuilder().setOrderId("A1").setAmountCents(4200).build());
\`\`\``,tech:"microservices",displayNumber:3,sortKey:2175},{id:"microservices-2176",number:4,title:"What problem does GraphQL and schema federation solve in a microservices system?",question:"What problem does GraphQL and schema federation solve in a microservices system?",difficulty:"Intermediate",category:"Communication",answer:'GraphQL gives clients a single endpoint to request exactly the fields they need, eliminating over-/under-fetching across many services. **Schema federation** (e.g. Apollo Federation) lets each service own a slice of one unified graph: the `users` service defines the `User` type, the `orders` service extends `User` with an `orders` field. A **gateway** composes these subgraphs and resolves cross-service references, so clients see one schema while ownership stays decentralized.\n\n```graphql\n# users subgraph\ntype User @key(fields: "id") { id: ID!  name: String! }\n\n# orders subgraph — extends a type it does not own\nextend type User @key(fields: "id") {\n  id: ID! @external\n  orders: [Order!]!            # resolved by the orders service\n}\ntype Order { id: ID!  total: Float! }\n```\nThe gateway fetches the user from one service and its orders from another, stitching them into a single response.',tech:"microservices",displayNumber:4,sortKey:2176},{id:"microservices-2177",number:5,title:"What role do Message Queues play in microservices?",question:"What role do Message Queues play in microservices?",difficulty:"Basic",category:"Messaging",answer:`A message queue (RabbitMQ, SQS, ActiveMQ) decouples producers from consumers by buffering messages. It enables async processing, load leveling (absorbing spikes), and reliable delivery via acknowledgements and retries. Point-to-point queues deliver each message to exactly one consumer (competing consumers for horizontal scaling); a dead-letter queue captures messages that repeatedly fail. Unlike a log (Kafka), a classic queue typically deletes a message once acked and does not retain history for replay.

\`\`\`java
// Producer
rabbitTemplate.convertAndSend("orders.exchange", "order.created",
        new OrderCreated(orderId));

// Consumer — auto-ack after successful processing; failures re-queue / DLQ
@RabbitListener(queues = "orders.queue")
public void handle(OrderCreated evt) {
    inventory.reserve(evt.orderId());   // throws -> nack -> retry/DLQ
}
\`\`\``,tech:"microservices",displayNumber:5,sortKey:2177},{id:"microservices-2178",number:6,title:"How does Event Streaming with Kafka differ from a traditional message queue?",question:"How does Event Streaming with Kafka differ from a traditional message queue?",difficulty:"Intermediate",category:"Messaging",answer:`Kafka is a distributed, append-only **commit log**, not just a queue. Key differences:
- **Retention & replay**: messages are kept for a configured time/size, so consumers can re-read from any offset — a queue usually deletes on ack.
- **Consumer groups**: multiple groups each get every message (pub/sub) while partitions are load-balanced within a group.
- **Ordering**: guaranteed per partition (choose a partition key, e.g. \`orderId\`, to keep related events ordered).
- **Throughput**: sequential disk writes + partitioning scale to millions of events/sec.

Use Kafka for event sourcing, stream processing, and multiple independent consumers of the same event.

\`\`\`java
// Partition key keeps all events for one order in order
kafkaTemplate.send("orders", order.getId(), new OrderPlaced(order));

@KafkaListener(topics = "orders", groupId = "billing")
public void bill(OrderPlaced e) { /* billing group sees every event */ }

@KafkaListener(topics = "orders", groupId = "analytics")
public void track(OrderPlaced e) { /* analytics group also sees every event */ }
\`\`\``,tech:"microservices",displayNumber:6,sortKey:2178},{id:"microservices-2179",number:7,title:"What are the core patterns of Event-Driven Architecture (EDA)?",question:"What are the core patterns of Event-Driven Architecture (EDA)?",difficulty:"Intermediate",category:"Architecture",answer:`- **Event Notification**: a thin event says "something happened" (\`OrderPlaced{id}\`); consumers call back for details. Low coupling, more chatter.
- **Event-Carried State Transfer**: the event carries all needed data, so consumers keep local read models and avoid callbacks.
- **Event Sourcing**: the event log *is* the source of truth; state is rebuilt by replaying events.
- **CQRS**: separate write and read models, often fed by events.

Events should be immutable, named in past tense, versioned, and self-describing.

\`\`\`java
// Event notification (thin)
record OrderPlaced(String orderId) {}

// Event-carried state transfer (fat — consumer needs no callback)
record OrderPlaced(String orderId, String customerId,
                   List<LineItem> items, long totalCents, Instant placedAt) {}
\`\`\``,tech:"microservices",displayNumber:7,sortKey:2179},{id:"microservices-2180",number:8,title:"What is the difference between Choreography and Orchestration?",question:"What is the difference between Choreography and Orchestration?",difficulty:"Intermediate",category:"Architecture",answer:`- **Choreography**: no central coordinator; each service reacts to events and emits its own. Decoupled and scalable, but the end-to-end flow is implicit and hard to trace/monitor.
- **Orchestration**: a central orchestrator explicitly tells each service what to do and tracks progress. Easier to visualize, monitor, and change flow logic, but the orchestrator is a coupling point and potential bottleneck.

Choose choreography for simple, loosely-coupled reactions; orchestration for complex, multi-step business processes needing visibility (often implemented as an orchestrated Saga).

\`\`\`java
// CHOREOGRAPHY — services react to each other's events
// order-service:   emit OrderPlaced
// payment-service: on OrderPlaced -> charge -> emit PaymentCompleted
// shipping-service:on PaymentCompleted -> ship

// ORCHESTRATION — one coordinator drives the steps
class OrderOrchestrator {
    void place(Order o) {
        payment.charge(o);      // step 1
        inventory.reserve(o);   // step 2
        shipping.schedule(o);   // step 3  (with compensation on failure)
    }
}
\`\`\``,tech:"microservices",displayNumber:8,sortKey:2180},{id:"microservices-2181",number:9,title:"What is the Database-Per-Service pattern and why is it important?",question:"What is the Database-Per-Service pattern and why is it important?",difficulty:"Basic",category:"Data Management",answer:`Each microservice owns its own database and no other service may access it directly — only through the owning service's API/events. This enforces loose coupling, lets each service choose the best datastore, and allows independent schema evolution and scaling. The cost: no cross-service JOINs or ACID transactions, so you need API composition or read models for queries, and Sagas for consistency. A shared database is an anti-pattern because it re-couples services at the schema level.

\`\`\`yaml
# Each service has isolated credentials/schema — no cross-service table access
order-service:
  datasource: { url: jdbc:postgresql://order-db:5432/orders }
payment-service:
  datasource: { url: jdbc:postgresql://payment-db:5432/payments }
# order-service NEVER runs: SELECT * FROM payments.transactions
# it calls payment-service's API or consumes its events instead
\`\`\``,tech:"microservices",displayNumber:9,sortKey:2181},{id:"microservices-2182",number:10,title:"What is Polyglot Persistence?",question:"What is Polyglot Persistence?",difficulty:"Basic",category:"Data Management",answer:`Polyglot persistence means using different database technologies for different services based on each one's data access patterns — a natural consequence of database-per-service. E.g. a catalog service uses a document store, a session service uses Redis, a fraud service uses a graph DB, and an orders service uses PostgreSQL. The benefit is fit-for-purpose storage; the cost is more operational surface area (backups, expertise, monitoring per engine), so teams standardize on a small approved set.

\`\`\`text
catalog-service    -> MongoDB      (flexible product documents)
cart-service       -> Redis        (fast, ephemeral, TTL)
orders-service     -> PostgreSQL   (transactions, integrity)
recommendation svc -> Neo4j        (graph traversals)
search-service     -> Elasticsearch(full-text, relevance)
\`\`\``,tech:"microservices",displayNumber:10,sortKey:2182},{id:"microservices-2183",number:11,title:"What is the Distributed Transaction Problem?",question:"What is the Distributed Transaction Problem?",difficulty:"Intermediate",category:"Data Management",answer:`With database-per-service, a business operation spanning multiple services (place order → charge payment → reserve stock) cannot use a single ACID transaction, because each service has its own DB. Classic two-phase commit (2PC/XA) provides atomicity but is slow, locks resources, and reduces availability (blocking if the coordinator fails) — it does not fit highly available microservices. The pragmatic answer is to give up immediate consistency and use **Sagas** with compensating actions plus idempotency, accepting eventual consistency.

\`\`\`java
// This is IMPOSSIBLE across separate service databases:
@Transactional
void placeOrder() {
    orderDb.save(order);        // order-service DB
    paymentDb.charge(payment);  // payment-service DB  <-- different DB, not in this tx
    inventoryDb.reserve(items); // inventory-service DB <-- different DB
}
// Solution: Saga (Q12) — a sequence of local transactions + compensations.
\`\`\``,tech:"microservices",displayNumber:11,sortKey:2183},{id:"microservices-2184",number:12,title:"Explain the Saga Pattern and its two coordination styles.",question:"Explain the Saga Pattern and its two coordination styles.",difficulty:"Hard",category:"Data Management",answer:`A Saga models a distributed transaction as a sequence of **local transactions**, each in one service. After each local commit the service publishes an event/command triggering the next step. If a step fails, the Saga runs **compensating transactions** to undo prior steps (semantic rollback — you can't roll back a committed local tx, so you issue a counter-action like "refund"). Two styles: **choreography** (services react to events) and **orchestration** (a central Saga orchestrator drives steps and compensations). Sagas provide atomicity-of-outcome and eventual consistency, not isolation, so guard against anomalies with idempotency and semantic locks.

\`\`\`java
// Orchestrated Saga with compensation
class CreateOrderSaga {
    void execute(Order o) {
        try {
            payment.charge(o);            // step 1
            try {
                inventory.reserve(o);     // step 2
                shipping.schedule(o);     // step 3
            } catch (Exception e) {
                payment.refund(o);        // compensate step 1
                inventory.release(o);     // compensate step 2
                throw e;
            }
        } catch (Exception e) {
            order.markFailed(o);
        }
    }
}
\`\`\``,tech:"microservices",displayNumber:12,sortKey:2184},{id:"microservices-2185",number:13,title:"Exercise — Saga Pattern: design a booking that reserves a flight and a hotel atomically.",question:"Exercise — Saga Pattern: design a booking that reserves a flight and a hotel atomically.",difficulty:"Hard",category:"Data Management",answer:`Model each reservation as a local transaction with a compensation. If the hotel step fails after the flight is booked, cancel the flight. Make every step idempotent (retry-safe) and record Saga state so it can resume after a crash. Below is an orchestrated implementation with explicit compensation and idempotency keys.

\`\`\`java
class TripBookingSaga {
    void book(TripRequest req) {
        String sagaId = req.id();
        String flightRef = null;
        try {
            flightRef = flight.reserve(sagaId, req.flight());   // idempotent by sagaId
            String hotelRef = hotel.reserve(sagaId, req.hotel());
            store.markCompleted(sagaId, flightRef, hotelRef);
        } catch (Exception ex) {
            if (flightRef != null) flight.cancel(sagaId, flightRef); // compensate
            store.markAborted(sagaId, ex.getMessage());
            throw new BookingFailedException(sagaId, ex);
        }
    }
}
// Idempotency: flight.reserve(sagaId,...) returns the SAME ref if called twice.
\`\`\``,tech:"microservices",displayNumber:13,sortKey:2185},{id:"microservices-2186",number:14,title:"What is Event Sourcing?",question:"What is Event Sourcing?",difficulty:"Hard",category:"Data Management",answer:`Instead of storing current state, Event Sourcing stores the full, immutable, ordered sequence of state-changing **events**. Current state is derived by replaying events. Benefits: a complete audit log, time-travel/debugging, natural fit for EDA, and the ability to build new projections retroactively. Costs: querying current state requires folding events (mitigated by snapshots and CQRS read models), schema/versioning of old events, and eventual consistency. The event store is append-only; events are never mutated or deleted.

\`\`\`java
// State is a fold over past events, not a stored row
class Account {
    long balance;
    void apply(Event e) {
        if (e instanceof Deposited d)  balance += d.amount();
        if (e instanceof Withdrawn w)  balance -= w.amount();
    }
    static Account rehydrate(List<Event> history) {
        Account a = new Account();
        history.forEach(a::apply);   // replay to rebuild current balance
        return a;
    }
}
\`\`\``,tech:"microservices",displayNumber:14,sortKey:2186},{id:"microservices-2187",number:15,title:"Exercise — Event Sourcing: rebuild an account balance from an event stream, with snapshots.",question:"Exercise — Event Sourcing: rebuild an account balance from an event stream, with snapshots.",difficulty:"Hard",category:"Data Management",answer:`Replaying millions of events per read is expensive, so periodically persist a **snapshot** of state at a known version, then replay only events after that version. On load: fetch latest snapshot, apply subsequent events. This bounds replay cost while keeping the event log authoritative.

\`\`\`java
Account load(String id) {
    Snapshot snap = snapshots.latest(id);                 // e.g. version 1000
    Account acc = snap != null ? Account.from(snap) : new Account();
    long fromVersion = snap != null ? snap.version() : 0;
    for (Event e : store.eventsAfter(id, fromVersion))    // replay only the tail
        acc.apply(e);
    return acc;
}
void maybeSnapshot(String id, Account acc, long version) {
    if (version % 1000 == 0) snapshots.save(id, acc.toSnapshot(version));
}
\`\`\``,tech:"microservices",displayNumber:15,sortKey:2187},{id:"microservices-2188",number:16,title:"What is CQRS and when should you use it?",question:"What is CQRS and when should you use it?",difficulty:"Hard",category:"Data Management",answer:`CQRS (Command Query Responsibility Segregation) separates the **write model** (commands that change state, optimized for validation/consistency) from the **read model** (queries, optimized for fast reads, often denormalized). The two models can use different schemas or even different databases, kept in sync via events. Benefits: independent scaling of reads vs writes, tailored read models per use case, and a clean pairing with event sourcing. Costs: added complexity and eventual consistency between write and read sides — use it only where read/write needs genuinely diverge, not everywhere.

\`\`\`java
// WRITE side — normalized, validated
class PlaceOrderCommandHandler {
    void handle(PlaceOrder cmd) {
        Order o = Order.create(cmd);          // enforce invariants
        repo.save(o);
        events.publish(new OrderPlaced(o));   // feeds the read side
    }
}
// READ side — denormalized projection updated from events
@KafkaListener(topics = "orders")
void project(OrderPlaced e) {
    readDb.upsertOrderSummary(e.id(), e.customerName(), e.total());
}
\`\`\``,tech:"microservices",displayNumber:16,sortKey:2188},{id:"microservices-2189",number:17,title:"Exercise — CQRS: build a read projection kept in sync with the write side.",question:"Exercise — CQRS: build a read projection kept in sync with the write side.",difficulty:"Hard",category:"Data Management",answer:`Commands mutate the write DB and emit events; a projector consumes those events to (re)build a denormalized read table for fast queries. Make the projector idempotent (handle duplicate events) and track the last processed offset so it can resume. Queries hit only the read table.

\`\`\`java
@Component
class OrderSummaryProjector {
    @KafkaListener(topics = "orders", groupId = "order-summary")
    void on(OrderEvent e) {
        switch (e) {
            case OrderPlaced p   -> readDb.insertIfAbsent(p.id(), p.total(), "PLACED");
            case OrderShipped s  -> readDb.updateStatus(s.id(), "SHIPPED");
            case OrderCancelled c-> readDb.updateStatus(c.id(), "CANCELLED");
            default -> {}
        }
    }
}
// Query path is a trivial single-table read:
List<OrderSummary> recent() { return readDb.findTop50ByOrderByCreatedDesc(); }
\`\`\``,tech:"microservices",displayNumber:17,sortKey:2189},{id:"microservices-2190",number:18,title:"What is the Outbox Pattern and how does CDC support it?",question:"What is the Outbox Pattern and how does CDC support it?",difficulty:"Hard",category:"Data Management",answer:`The **dual-write problem**: updating the DB and publishing to a broker in two separate steps can leave them inconsistent if one fails. The **Transactional Outbox** solves it by writing the business row and an \`outbox\` event row in the *same local transaction*. A separate relay then publishes outbox rows to the broker. **Change Data Capture (CDC)** (e.g. Debezium tailing the DB transaction log) reads committed outbox inserts and streams them to Kafka — no polling, low latency, and the write is atomic. This gives at-least-once delivery, so consumers must be idempotent.

\`\`\`java
@Transactional
public void placeOrder(Order o) {
    orderRepo.save(o);                                   // business row
    outboxRepo.save(new OutboxEvent(                     // SAME transaction
        "Order", o.getId(), "OrderPlaced", toJson(o)));
    // No broker call here — CDC/Debezium tails the outbox table
    // and publishes committed rows to Kafka.
}
\`\`\``,tech:"microservices",displayNumber:18,sortKey:2190},{id:"microservices-2191",number:19,title:"Exercise — Outbox Pattern: guarantee an event is published exactly when the row is committed.",question:"Exercise — Outbox Pattern: guarantee an event is published exactly when the row is committed.",difficulty:"Hard",category:"Data Management",answer:`Write to \`outbox\` inside the business transaction. A poller (or CDC) reads unpublished rows, publishes them, and marks them sent — retrying on failure. Because publishing and marking-sent aren't atomic, delivery is at-least-once; consumers dedupe on the event id.

\`\`\`java
// Relay: runs on a schedule, publishes committed outbox rows
@Scheduled(fixedDelay = 500)
void relay() {
    for (OutboxEvent e : outboxRepo.findTop100ByPublishedFalseOrderById()) {
        kafka.send(e.getTopic(), e.getAggregateId(), e.getPayload());
        e.setPublished(true);           // if this fails, row re-published next tick
        outboxRepo.save(e);
    }
}
// Consumer dedupes:
if (processedRepo.existsById(e.eventId())) return;   // already handled
\`\`\``,tech:"microservices",displayNumber:19,sortKey:2191},{id:"microservices-2192",number:20,title:"How do you handle Eventual Consistency in a microservices system?",question:"How do you handle Eventual Consistency in a microservices system?",difficulty:"Intermediate",category:"Data Management",answer:`Accept that read models and downstream services lag the source of truth briefly, and design for it: make consumers **idempotent** (safe on duplicates), tolerate **out-of-order** events (use versions/timestamps), expose the consistency to users honestly (e.g. "payment processing"), use **compensations** to fix divergence, and add reconciliation jobs to detect drift. Never assume a cross-service read reflects a write that just happened; instead confirm via events or poll with a status.

\`\`\`java
// Version guard: ignore stale/duplicate events, apply only newer state
void onProfileUpdated(ProfileUpdated e) {
    ReadRow row = readDb.find(e.userId());
    if (row != null && e.version() <= row.version()) return; // stale/dup -> skip
    readDb.upsert(e.userId(), e.data(), e.version());
}
\`\`\``,tech:"microservices",displayNumber:20,sortKey:2192},{id:"microservices-2193",number:21,title:"Exercise — Eventual Consistency: make a consumer idempotent and order-tolerant.",question:"Exercise — Eventual Consistency: make a consumer idempotent and order-tolerant.",difficulty:"Intermediate",category:"Data Management",answer:`Combine a processed-id set (idempotency) with a version check (ordering). Duplicates are dropped by id; out-of-order updates are dropped by version. Together they make the projection converge to the latest state regardless of delivery quirks.

\`\`\`java
@KafkaListener(topics = "inventory")
void handle(StockChanged e) {
    if (!processed.add(e.eventId())) return;               // idempotent: dup dropped
    Integer current = readDb.versionOf(e.sku());
    if (current != null && e.version() <= current) return; // order-tolerant
    readDb.setStock(e.sku(), e.qty(), e.version());
}
\`\`\``,tech:"microservices",displayNumber:21,sortKey:2193},{id:"microservices-2194",number:22,title:"What are the common Failure Modes in distributed systems?",question:"What are the common Failure Modes in distributed systems?",difficulty:"Intermediate",category:"Resilience",answer:`- **Partial failure**: some nodes/services fail while others work — the defining trait of distributed systems.
- **Network**: latency spikes, packet loss, partitions (split brain), and the "network is unreliable" fallacy.
- **Slow (grey) failures**: a service responds but slowly, exhausting caller threads — often worse than a clean crash.
- **Cascading failures**: one overloaded service drags down its callers.
- **Clock skew** and **message duplication/reordering**.

Design assuming any dependency can be slow or unavailable at any time; add timeouts, retries with backoff, circuit breakers, and bulkheads.

\`\`\`java
// The #1 mistake: no timeout -> a slow dependency hangs your threads forever
var client = HttpClient.newBuilder()
    .connectTimeout(Duration.ofSeconds(2))     // bound the connect
    .build();
var req = HttpRequest.newBuilder(uri)
    .timeout(Duration.ofSeconds(3))            // bound the response
    .build();
\`\`\``,tech:"microservices",displayNumber:22,sortKey:2194},{id:"microservices-2195",number:23,title:"Explain Timeouts, Retries, and Exponential Backoff.",question:"Explain Timeouts, Retries, and Exponential Backoff.",difficulty:"Intermediate",category:"Resilience",answer:`- **Timeout**: cap how long you wait for a dependency so a slow call fails fast instead of pinning a thread.
- **Retry**: re-attempt transient failures — but only **idempotent** operations, or you risk double side-effects.
- **Exponential backoff**: increase the delay between retries (1s, 2s, 4s…) to avoid hammering a struggling service.
- **Jitter**: add randomness so many clients don't retry in lockstep (the "thundering herd").

Always bound total attempts and total time; combine with a circuit breaker so retries don't prolong an outage.

\`\`\`java
// Resilience4j: retry with exponential backoff + jitter
RetryConfig cfg = RetryConfig.custom()
    .maxAttempts(4)
    .intervalFunction(IntervalFunction.ofExponentialRandomBackoff(
        Duration.ofSeconds(1), 2.0, 0.5))   // 1s,2s,4s (+/-50% jitter)
    .retryOnException(ex -> ex instanceof IOException)
    .build();
Retry retry = Retry.of("payment", cfg);
Supplier<Reply> call = Retry.decorateSupplier(retry, () -> paymentClient.charge(req));
\`\`\``,tech:"microservices",displayNumber:23,sortKey:2195},{id:"microservices-2196",number:24,title:"Exercise — Timeouts, Retries, and Backoff: wrap a flaky call safely.",question:"Exercise — Timeouts, Retries, and Backoff: wrap a flaky call safely.",difficulty:"Intermediate",category:"Resilience",answer:`Only retry idempotent calls, cap attempts and per-attempt time, back off exponentially with jitter, and give up to a fallback. Below combines a timeout with a bounded, jittered retry.

\`\`\`java
TimeLimiter limiter = TimeLimiter.of(Duration.ofSeconds(3));
Retry retry = Retry.of("inventory", RetryConfig.custom()
    .maxAttempts(3)
    .intervalFunction(IntervalFunction.ofExponentialRandomBackoff(
        Duration.ofMillis(200), 2.0, 0.5))
    .build());

Supplier<Integer> guarded = Retry.decorateSupplier(retry,
    () -> {
        try { return limiter.executeFutureSupplier(
                 () -> CompletableFuture.supplyAsync(() -> client.stock(sku))); }
        catch (Exception e) { throw new RuntimeException(e); }
    });
int stock = Try.ofSupplier(guarded).recover(ex -> 0).get();  // fallback: 0
\`\`\``,tech:"microservices",displayNumber:24,sortKey:2196},{id:"microservices-2197",number:25,title:"What is the Circuit Breaker pattern?",question:"What is the Circuit Breaker pattern?",difficulty:"Intermediate",category:"Resilience",answer:`A circuit breaker stops calling a failing dependency to let it recover and to fail fast. States:
- **Closed**: calls flow; failures are counted.
- **Open**: once a failure threshold is crossed, calls are rejected immediately (return a fallback) for a cooldown window.
- **Half-Open**: after cooldown, a few trial calls are allowed; success closes the circuit, failure re-opens it.

This prevents wasted work and cascading failures, and gives the downstream breathing room.

\`\`\`java
CircuitBreakerConfig cfg = CircuitBreakerConfig.custom()
    .failureRateThreshold(50)                     // open at 50% failures
    .slowCallRateThreshold(80)
    .slowCallDurationThreshold(Duration.ofSeconds(2))
    .waitDurationInOpenState(Duration.ofSeconds(10))
    .permittedNumberOfCallsInHalfOpenState(3)
    .slidingWindowSize(20)
    .build();
CircuitBreaker cb = CircuitBreaker.of("payment", cfg);

Supplier<Reply> decorated = CircuitBreaker.decorateSupplier(cb,
    () -> paymentClient.charge(req));
Reply r = Try.ofSupplier(decorated).recover(ex -> Reply.declined()).get();
\`\`\``,tech:"microservices",displayNumber:25,sortKey:2197},{id:"microservices-2198",number:26,title:"Exercise — Circuit Breaker: add fallback behavior when a dependency is down.",question:"Exercise — Circuit Breaker: add fallback behavior when a dependency is down.",difficulty:"Intermediate",category:"Resilience",answer:`Wrap the call in a breaker and provide a fallback (cached value, default, or degraded response) invoked when the breaker is open or the call fails. In Spring, \`@CircuitBreaker\` with a \`fallbackMethod\` keeps it declarative.

\`\`\`java
@Service
class RecommendationService {
    @CircuitBreaker(name = "recs", fallbackMethod = "popularFallback")
    public List<Item> personalized(String userId) {
        return recsClient.forUser(userId);       // remote call
    }
    // Same signature + Throwable; runs when circuit open or call fails
    List<Item> popularFallback(String userId, Throwable t) {
        return cache.popularItems();             // graceful degradation
    }
}
\`\`\``,tech:"microservices",displayNumber:26,sortKey:2198},{id:"microservices-2199",number:27,title:"What is the Bulkhead pattern?",question:"What is the Bulkhead pattern?",difficulty:"Intermediate",category:"Resilience",answer:`Named after ship compartments, the bulkhead pattern isolates resources (thread pools, connection pools, semaphores) per dependency so a failure in one cannot consume all resources and sink the whole service. If calls to a slow \`report-service\` are capped at their own pool, they can't starve threads needed for \`payment-service\`. It contains partial failures and preserves capacity for healthy paths.

\`\`\`java
// Separate, bounded pools per dependency — one can't exhaust the other
Bulkhead paymentBh = Bulkhead.of("payment",
    BulkheadConfig.custom().maxConcurrentCalls(20).maxWaitDuration(Duration.ofMillis(50)).build());
Bulkhead reportBh = Bulkhead.of("report",
    BulkheadConfig.custom().maxConcurrentCalls(5).build());   // slow, so cap tight

Supplier<Reply> call = Bulkhead.decorateSupplier(paymentBh, () -> paymentClient.charge(req));
\`\`\``,tech:"microservices",displayNumber:27,sortKey:2199},{id:"microservices-2200",number:28,title:"Exercise — Bulkhead Pattern: isolate a slow dependency behind its own pool.",question:"Exercise — Bulkhead Pattern: isolate a slow dependency behind its own pool.",difficulty:"Intermediate",category:"Resilience",answer:`Give the slow dependency a dedicated, small thread pool (thread-pool bulkhead) so its saturation rejects only its own calls while the main request threads stay free. Combine with a fallback when the bulkhead is full.

\`\`\`java
@Service
class ReportService {
    @Bulkhead(name = "report", type = Bulkhead.Type.THREADPOOL,
              fallbackMethod = "busy")
    public CompletableFuture<Report> generate(String id) {
        return CompletableFuture.completedFuture(reportClient.build(id));
    }
    CompletableFuture<Report> busy(String id, Throwable t) {
        return CompletableFuture.completedFuture(Report.tryLater()); // rejected when full
    }
}
\`\`\``,tech:"microservices",displayNumber:28,sortKey:2200},{id:"microservices-2201",number:29,title:"What are Cascading Failures and how do you prevent them?",question:"What are Cascading Failures and how do you prevent them?",difficulty:"Hard",category:"Resilience",answer:`A cascading failure is a chain reaction: service A slows, its callers' threads pile up waiting on A, those callers become unresponsive, *their* callers fail, and the outage spreads upstream. Common triggers: no timeouts, unbounded retries, shared thread pools, and no backpressure. Prevention: aggressive timeouts, circuit breakers (fail fast when A is unhealthy), bulkheads (isolate A's blast radius), load shedding/rate limiting, and retry budgets with jitter. The goal is to convert a slow dependency into a fast, contained failure.

\`\`\`java
// Layered defense converts a slow dependency into a fast, contained failure
Supplier<Reply> guarded =
  Decorators.ofSupplier(() -> downstream.call(req))
    .withThreadPoolBulkhead(bulkhead)   // isolate blast radius
    .withTimeLimiter(timeLimiter, scheduler) // fail fast on slowness
    .withCircuitBreaker(circuitBreaker) // stop calling when unhealthy
    .withFallback(List.of(Exception.class), ex -> Reply.degraded())
    .decorate()::get;
\`\`\``,tech:"microservices",displayNumber:29,sortKey:2201},{id:"microservices-2202",number:30,title:"What are Graceful Degradation and Fallbacks?",question:"What are Graceful Degradation and Fallbacks?",difficulty:"Intermediate",category:"Resilience",answer:`Graceful degradation keeps the core experience working when a non-critical dependency fails, by returning a reduced-but-useful response instead of an error. Examples: show cached/popular recommendations when the recs engine is down, hide the reviews section if the reviews service fails, or accept an order and settle payment later. A **fallback** is the concrete alternative value/action invoked on failure (via circuit breaker/try-recover). Distinguish critical paths (must succeed) from optional ones (safe to degrade).

\`\`\`java
ProductPage page = new ProductPage();
page.setProduct(catalog.get(id));                     // critical — must succeed
page.setReviews(Try.ofSupplier(() -> reviews.get(id)) // optional — degrade
                   .recover(ex -> List.of()).get());
page.setRecommendations(Try.ofSupplier(() -> recs.get(id))
                   .recover(ex -> cache.popular()).get());
return page;   // page renders even if reviews/recs are down
\`\`\``,tech:"microservices",displayNumber:30,sortKey:2202},{id:"microservices-2203",number:31,title:"How does Service-to-Service Authentication work in microservices?",question:"How does Service-to-Service Authentication work in microservices?",difficulty:"Hard",category:"Security",answer:`Internal calls must authenticate the *caller service*, not just end users. Common approaches: **mTLS** (each service presents a certificate; identity is the cert, often automated by a service mesh), **OAuth2 client-credentials** (a service gets a token from an auth server and presents it as a Bearer JWT), and **signed JWTs** propagating the end-user context downstream. Verify tokens' signature, issuer, audience, and expiry on every hop; apply least-privilege scopes. Never trust the network alone (zero-trust).

\`\`\`java
// Caller obtains a client-credentials token, forwards it as Bearer
String token = oauth.clientCredentialsToken("order-service", scopes("payment.charge"));
paymentClient.post().uri("/charge")
    .header("Authorization", "Bearer " + token)
    .body(req).retrieve();

// Callee validates issuer + audience + scope on the JWT (Spring Security)
http.oauth2ResourceServer(o -> o.jwt(j -> j.jwtAuthenticationConverter(scopeConverter)));
\`\`\``,tech:"microservices",displayNumber:31,sortKey:2203},{id:"microservices-2204",number:32,title:"How do you manage Secrets Across Services?",question:"How do you manage Secrets Across Services?",difficulty:"Intermediate",category:"Security",answer:`Never hardcode secrets or bake them into images/repos. Use a dedicated secrets manager (HashiCorp Vault, AWS Secrets Manager, Kubernetes Secrets with encryption-at-rest) that provides: centralized storage, access control per service identity, rotation, dynamic short-lived credentials, and audit logs. Inject secrets at runtime (env/mounted files/sidecar), scope them least-privilege, and rotate regularly. Encrypt in transit and at rest.

\`\`\`yaml
# Spring Cloud Vault — secrets fetched at startup by service identity, never in git
spring:
  cloud:
    vault:
      uri: https://vault:8200
      authentication: KUBERNETES        # service's k8s identity authenticates
      kv: { enabled: true, backend: secret }
# Usage: @Value("\${db.password}") resolved from Vault, rotated centrally
\`\`\``,tech:"microservices",displayNumber:32,sortKey:2204},{id:"microservices-2205",number:33,title:"What are the main Deployment Strategies for microservices?",question:"What are the main Deployment Strategies for microservices?",difficulty:"Intermediate",category:"Deployment",answer:`- **Rolling**: replace instances gradually; no downtime, but mixed versions run briefly.
- **Blue-Green**: run the new version (green) alongside old (blue), switch traffic at once; instant rollback, but doubles resources.
- **Canary**: route a small % of traffic to the new version, watch metrics, then ramp up; limits blast radius.
- **Shadow/dark launch**: send mirrored traffic to the new version without serving its responses, to test under real load.

Pair with automated health checks and rollback. Backward-compatible APIs/schemas are required so old and new can coexist.

\`\`\`yaml
# Canary: 10% to v2, 90% to v1 (e.g. Istio VirtualService)
http:
- route:
  - destination: { host: orders, subset: v1 }
    weight: 90
  - destination: { host: orders, subset: v2 }
    weight: 10        # ramp to 100 as metrics stay green
\`\`\``,tech:"microservices",displayNumber:33,sortKey:2205},{id:"microservices-2206",number:34,title:"How do you handle Configuration Management and Feature Flags?",question:"How do you handle Configuration Management and Feature Flags?",difficulty:"Intermediate",category:"Deployment",answer:`Externalize configuration from code (12-factor) so the same artifact runs in every environment. Use a central config service (Spring Cloud Config, Consul, k8s ConfigMaps) with per-environment overrides and, ideally, dynamic refresh without redeploy. **Feature flags** decouple deploy from release: ship code dark, then toggle features per environment/user cohort at runtime — enabling canary releases, A/B tests, and instant kill-switches. Keep secrets out of plain config (Q32) and audit flag changes.

\`\`\`java
// Feature flag gates behavior at runtime — no redeploy to turn on/off
if (features.isEnabled("new-checkout", userId)) {
    return newCheckoutFlow(cart);      // gradually rolled out / A-B tested
}
return legacyCheckoutFlow(cart);       // instant kill-switch back to this
\`\`\``,tech:"microservices",displayNumber:34,sortKey:2206},{id:"microservices-2207",number:35,title:"Exercise — Configuration and Feature Flags: roll out a feature to 5% of users.",question:"Exercise — Configuration and Feature Flags: roll out a feature to 5% of users.",difficulty:"Intermediate",category:"Deployment",answer:`Use a deterministic hash of the user id to bucket users into a stable percentage, so the same user consistently sees the same variant and you can ramp the percentage centrally. Read the rollout percentage from config so it changes without redeploy.

\`\`\`java
class PercentageRollout {
    boolean enabled(String flag, String userId, int percent) {
        int bucket = Math.floorMod(
            (flag + ":" + userId).hashCode(), 100);   // stable 0..99 per user
        return bucket < percent;                       // percent read from config
    }
}
// percent=5 -> ~5% of users; bump to 25, 50, 100 centrally to ramp the rollout.
\`\`\``,tech:"microservices",displayNumber:35,sortKey:2207},{id:"microservices-2208",number:36,title:"What is Service Discovery and why is it needed?",question:"What is Service Discovery and why is it needed?",difficulty:"Intermediate",category:"Infrastructure",answer:`In dynamic environments instances come and go and get ephemeral IPs, so hardcoding addresses breaks. **Service discovery** lets services find each other by logical name. Two models:
- **Client-side**: the client queries a registry (Eureka, Consul) for healthy instances and load-balances itself.
- **Server-side**: the client hits a stable virtual address (load balancer / k8s Service / DNS) that routes to healthy instances.

A registry tracks registrations and health so traffic only goes to live instances.

\`\`\`java
// Client-side discovery: call by service name, not IP:port
@FeignClient(name = "payment-service")   // resolved via registry (Eureka/Consul)
interface PaymentClient {
    @PostMapping("/charge") ChargeReply charge(@RequestBody ChargeRequest r);
}
// In Kubernetes, server-side discovery is just DNS:
//   http://payment-service.default.svc.cluster.local/charge
\`\`\``,tech:"microservices",displayNumber:36,sortKey:2208},{id:"microservices-2209",number:37,title:"Exercise — Service Discovery: register a service and resolve a healthy instance.",question:"Exercise — Service Discovery: register a service and resolve a healthy instance.",difficulty:"Intermediate",category:"Infrastructure",answer:`On startup the instance registers (name, host, port, health URL) with the registry and sends heartbeats; the registry evicts instances that stop reporting healthy. Clients resolve the name to the current healthy set and load-balance. Below registers with Eureka and calls another service by name with client-side load balancing.

\`\`\`yaml
# application.yml — self-register + heartbeat
eureka:
  client: { service-url: { defaultZone: http://eureka:8761/eureka } }
  instance: { lease-renewal-interval-in-seconds: 10 }   # heartbeat
\`\`\`
\`\`\`java
@Bean @LoadBalanced                       // client-side LB over discovered instances
RestClient.Builder rc() { return RestClient.builder(); }
// resolves "inventory-service" to a healthy instance each call:
rc().build().get().uri("http://inventory-service/stock/{sku}", sku).retrieve();
\`\`\``,tech:"microservices",displayNumber:37,sortKey:2209},{id:"microservices-2210",number:38,title:"What is the API Gateway pattern?",question:"What is the API Gateway pattern?",difficulty:"Intermediate",category:"Infrastructure",answer:`An API Gateway is the single entry point between clients and the microservices behind it. It handles cross-cutting concerns so services don't each reimplement them: routing/composition, authentication/authorization, rate limiting, TLS termination, request/response transformation, caching, and observability. It shields clients from internal topology and lets services evolve independently. Risk: it can become a bottleneck or a "god" component, so keep business logic out of it.

Use a token-bucket limiter keyed by client identity (API key/user/IP). Tokens refill at the allowed rate up to a burst cap; a request that finds no token gets HTTP 429. Redis backs the counter so the limit holds across all gateway instances.

\`\`\`yaml
# Spring Cloud Gateway — route + auth + rate limit at the edge
spring.cloud.gateway.routes:
- id: orders
  uri: lb://order-service
  predicates: [ Path=/api/orders/** ]
  filters:
    - StripPrefix=1
    - name: RequestRateLimiter
      args: { redis-rate-limiter.replenishRate: 100, redis-rate-limiter.burstCapacity: 200 }
\`\`\`

\`\`\`java
// Key the limiter by API key so each client gets its own bucket
@Bean
KeyResolver apiKeyResolver() {
    return exchange -> Mono.just(
        exchange.getRequest().getHeaders().getFirst("X-API-Key"));
}
// replenishRate=10, burstCapacity=20 -> ~10 req/s, bursts to 20; excess -> 429
\`\`\``,tech:"microservices",displayNumber:38,sortKey:2210},{id:"microservices-2211",number:39,title:"What is the Backend for Frontend (BFF) pattern?",question:"What is the Backend for Frontend (BFF) pattern?",difficulty:"Intermediate",category:"Infrastructure",answer:`A BFF is a dedicated gateway/backend per client type (web, iOS, Android), instead of one general API. Each BFF aggregates and shapes data specifically for its frontend — trimming fields, batching calls, adapting to the device's constraints — so clients make one tailored call instead of many. Benefits: frontend teams own their BFF, faster iteration, and no bloated shared API. Cost: some duplicated aggregation logic across BFFs.

\`\`\`java
// mobile-bff: one call returns exactly what the mobile screen needs
@GetMapping("/mobile/home")
HomeDto home(Principal user) {
    var profile = userClient.get(user.getName());
    var orders  = orderClient.recent(user.getName(), 3);   // only 3 for small screen
    var promos  = promoClient.forUser(user.getName());
    return HomeDto.compactForMobile(profile, orders, promos); // trimmed payload
}
// web-bff would return richer data for the desktop layout.
\`\`\``,tech:"microservices",displayNumber:39,sortKey:2211},{id:"microservices-2212",number:40,title:"What is a Service Mesh and what problems does it solve?",question:"What is a Service Mesh and what problems does it solve?",difficulty:"Hard",category:"Infrastructure",answer:`A service mesh (Istio, Linkerd) is an infrastructure layer that handles service-to-service communication via **sidecar proxies** deployed next to each service. It moves cross-cutting concerns — mTLS, retries, timeouts, circuit breaking, load balancing, traffic shifting (canary), and telemetry — out of application code and into the platform, configured declaratively. The **data plane** (sidecars) carries traffic; the **control plane** configures them. Benefit: consistent, language-agnostic resilience/security/observability without touching app code; cost: added complexity and per-hop latency.

\`\`\`yaml
# Istio: enforce mTLS + retries + timeout WITHOUT changing service code
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
spec:
  hosts: [ payment ]
  http:
  - route: [ { destination: { host: payment } } ]
    timeout: 3s
    retries: { attempts: 3, perTryTimeout: 1s, retryOn: 5xx }
\`\`\``,tech:"microservices",displayNumber:40,sortKey:2212},{id:"microservices-2213",number:41,title:"What are common Load Balancing Strategies?",question:"What are common Load Balancing Strategies?",difficulty:"Intermediate",category:"Infrastructure",answer:`- **Round Robin**: rotate evenly; simple, ignores load.
- **Least Connections**: send to the instance with fewest active requests; good for uneven request durations.
- **Weighted**: bias toward more capable instances (or canary weights).
- **Least Response Time / EWMA**: favor the fastest-responding instances.
- **Consistent Hashing**: map a key (user/session) to the same instance for cache locality/sticky sessions with minimal reshuffling when instances change.

Layer 4 (TCP) vs Layer 7 (HTTP-aware, can route by path/header). Pair with health checks so only healthy instances receive traffic.

\`\`\`text
Round Robin       -> A,B,C,A,B,C            (even, load-blind)
Least Connections -> pick instance with min in-flight requests
Weighted          -> A:5  B:3  C:2          (capacity/canary bias)
Consistent Hash   -> hash(userId) -> same instance (cache/session affinity)
\`\`\``,tech:"microservices",displayNumber:41,sortKey:2213},{id:"microservices-2214",number:42,title:"Exercise — Load Balancing Strategies: implement consistent hashing for sticky routing.",question:"Exercise — Load Balancing Strategies: implement consistent hashing for sticky routing.",difficulty:"Hard",category:"Infrastructure",answer:`Place nodes on a hash ring (with virtual nodes for even spread); route a key to the first node clockwise from the key's hash. Adding/removing a node only remaps keys near it, preserving most affinity — unlike \`hash % N\`, which remaps almost everything.

\`\`\`java
class ConsistentHashRing {
    private final TreeMap<Integer, String> ring = new TreeMap<>();
    ConsistentHashRing(List<String> nodes, int vnodes) {
        for (String n : nodes)
            for (int i = 0; i < vnodes; i++)
                ring.put((n + "#" + i).hashCode(), n);   // virtual nodes
    }
    String route(String key) {
        var e = ring.ceilingEntry(key.hashCode());
        return (e != null ? e : ring.firstEntry()).getValue();  // wrap around
    }
}
\`\`\``,tech:"microservices",displayNumber:42,sortKey:2214},{id:"microservices-2215",number:43,title:"What is Distributed Tracing and why is it essential?",question:"What is Distributed Tracing and why is it essential?",difficulty:"Intermediate",category:"Observability",answer:`A single user request may traverse many services; distributed tracing stitches those hops into one **trace** so you can see the end-to-end path, per-service latency, and where failures occur. A **trace id** is generated at the edge and propagated on every call (via headers like W3C \`traceparent\`); each hop records a **span** (with parent-child links). Tools: OpenTelemetry (instrumentation) + Jaeger/Zipkin/Tempo (storage/UI). It's essential for debugging latency and failures across service boundaries.

\`\`\`java
// Context propagation: the trace id flows across services via headers.
// Spring Boot 3 + Micrometer Tracing auto-injects W3C traceparent:
//   traceparent: 00-<trace-id>-<span-id>-01
// All logs/spans for one request share the trace-id, so you can filter by it.
@Observed(name = "checkout")     // creates a span around this method
public Receipt checkout(Cart cart) { ... }
\`\`\``,tech:"microservices",displayNumber:43,sortKey:2215},{id:"microservices-2216",number:44,title:"Exercise — Distributed Tracing: propagate a correlation id across two services.",question:"Exercise — Distributed Tracing: propagate a correlation id across two services.",difficulty:"Intermediate",category:"Observability",answer:`Generate a correlation/trace id at the entry point if absent, store it in the logging MDC, and forward it on outbound calls. The downstream reads the same header and reuses it, so logs from both services can be joined by one id.

\`\`\`java
// Inbound filter: adopt or create the id, put it in MDC (shows in every log line)
String cid = Optional.ofNullable(req.getHeader("X-Correlation-Id"))
                     .orElse(UUID.randomUUID().toString());
MDC.put("correlationId", cid);

// Outbound: forward it so the next service continues the same trace
restClient.get().uri("http://inventory/stock/{s}", sku)
    .header("X-Correlation-Id", MDC.get("correlationId"))
    .retrieve();
\`\`\``,tech:"microservices",displayNumber:44,sortKey:2216},{id:"microservices-2217",number:45,title:"What are effective Centralized Logging strategies?",question:"What are effective Centralized Logging strategies?",difficulty:"Intermediate",category:"Observability",answer:`With many ephemeral instances, \`ssh\`-into-a-box logging is impossible. Ship all logs to a central store (ELK/EFK, Loki, Splunk). Best practices: **structured JSON logs** (queryable fields, not free text), a **correlation/trace id** on every line, consistent levels, no secrets/PII, and context fields (service, version, env). Logs, metrics, and traces are the "three pillars" — link them by trace id. Sample or rate-limit noisy logs to control cost.

\`\`\`java
// Structured log with correlation id -> searchable in Kibana/Loki by traceId
log.atInfo()
   .addKeyValue("event", "order.placed")
   .addKeyValue("orderId", order.getId())
   .addKeyValue("traceId", MDC.get("correlationId"))
   .addKeyValue("amountCents", order.getTotalCents())
   .log("Order placed");
// -> {"level":"INFO","event":"order.placed","orderId":"A1","traceId":"...", ...}
\`\`\``,tech:"microservices",displayNumber:45,sortKey:2217},{id:"microservices-2218",number:46,title:"How do you approach Metrics, Health Checks, and Monitoring?",question:"How do you approach Metrics, Health Checks, and Monitoring?",difficulty:"Intermediate",category:"Observability",answer:`- **Metrics**: emit time-series (request rate, error rate, latency percentiles, saturation) — the RED (Rate/Errors/Duration) and USE methods — via Prometheus/Micrometer, visualized in Grafana, with alerting on SLOs.
- **Health checks**: expose **liveness** (is the process healthy? restart if not) and **readiness** (can it serve traffic? pull from LB if not) endpoints so orchestrators route correctly.
- **Monitoring/alerting**: alert on symptoms users feel (error rate, latency), not just resources.

\`\`\`java
// Micrometer metrics + Spring Boot Actuator health probes
meterRegistry.counter("orders.placed", "channel", "web").increment();
Timer.builder("checkout.latency").publishPercentiles(0.5, 0.95, 0.99)
     .register(meterRegistry).record(() -> checkout(cart));
// Actuator exposes /actuator/health/liveness and /actuator/health/readiness
// which Kubernetes probes hit to restart or de-route the pod.
\`\`\``,tech:"microservices",displayNumber:46,sortKey:2218},{id:"microservices-2219",number:47,title:"Exercise — Metrics and Monitoring: expose a custom business metric and alert on it.",question:"Exercise — Metrics and Monitoring: expose a custom business metric and alert on it.",difficulty:"Intermediate",category:"Observability",answer:'Instrument the code with a counter/gauge, expose it on `/actuator/prometheus`, scrape with Prometheus, and write an alert rule on a rate/threshold. Below tracks failed payments and alerts when the failure rate is high.\n\n```java\nmeterRegistry.counter("payments.total", "result", result).increment();\n```\n```yaml\n# Prometheus alert: fire when >5% of payments fail over 5 min\n- alert: HighPaymentFailureRate\n  expr: |\n    sum(rate(payments_total{result="failed"}[5m]))\n      / sum(rate(payments_total[5m])) > 0.05\n  for: 5m\n  labels: { severity: page }\n```',tech:"microservices",displayNumber:47,sortKey:2219},{id:"microservices-2220",number:48,title:"How do you Test Microservices effectively?",question:"How do you Test Microservices effectively?",difficulty:"Hard",category:"Testing",answer:`Use the **test pyramid** adapted for distribution:
- **Unit**: fast, isolated logic.
- **Integration**: a service with its real DB/broker via **Testcontainers**.
- **Contract tests** (Pact/Spring Cloud Contract): verify provider and consumer agree on the API without full end-to-end runs — catch breaking changes early.
- **Component**: one service in isolation with dependencies stubbed.
- **End-to-end**: a few critical happy paths only (slow, brittle).

Also test resilience (chaos/fault injection) and idempotency. Favor contract + integration over sprawling E2E.

\`\`\`java
// Integration test with a REAL database via Testcontainers
@Testcontainers @SpringBootTest
class OrderRepoTest {
    @Container static PostgreSQLContainer<?> db =
        new PostgreSQLContainer<>("postgres:16");
    @DynamicPropertySource
    static void props(DynamicPropertyRegistry r) {
        r.add("spring.datasource.url", db::getJdbcUrl);
    }
    @Test void savesAndReadsOrder() { /* real SQL, no mocks */ }
}
\`\`\``,tech:"microservices",displayNumber:48,sortKey:2220},{id:"microservices-2221",number:49,title:"Describe Netflix's microservices architecture and its key lessons.",question:"Describe Netflix's microservices architecture and its key lessons.",difficulty:"Intermediate",category:"Real-World",answer:`Netflix pioneered large-scale microservices on AWS after a monolith DB corruption outage. Key ideas: hundreds of services behind an edge gateway (Zuul), client-side discovery (Eureka), resilience via Hystrix (circuit breakers/bulkheads — the origin of these patterns at scale), and **Chaos Engineering** (Chaos Monkey randomly kills instances in production to prove resilience). Lessons: design for failure as normal, isolate faults, automate everything, and embrace eventual consistency. Many tools became open source (later superseded by Resilience4j, Spring Cloud, service meshes).

\`\`\`text
Client -> Zuul (edge gateway) -> Eureka (discovery) -> 100s of services
Resilience: Hystrix circuit breakers + bulkheads + fallbacks
Proof:      Chaos Monkey kills prod instances to validate resilience
Takeaway:   assume failure is constant; automate recovery, not heroics
\`\`\``,tech:"microservices",displayNumber:49,sortKey:2221},{id:"microservices-2222",number:50,title:"What is Uber's Domain-Oriented Microservices Architecture (DOMA)?",question:"What is Uber's Domain-Oriented Microservices Architecture (DOMA)?",difficulty:"Hard",category:"Real-World",answer:`After thousands of microservices caused sprawl and cognitive overload, Uber grouped related services into **domains** with clear boundaries, and fronted each domain with a **gateway** so consumers depend on the domain interface, not individual services. Layered dependencies flow one direction (no cycles), and extension points allow customization without forking. DOMA is essentially applying DDD bounded contexts and layering to tame microservice proliferation — balancing team autonomy with system comprehensibility.

\`\`\`text
Consumers ─▶ Domain Gateway ─▶ [ services within one domain ]
Principles:
  - Domains = bounded contexts (grouped related services)
  - Gateway per domain hides internal services
  - One-directional layered dependencies (no cycles)
  - Extension points instead of forking
Goal: keep microservice benefits without the "too many services" chaos
\`\`\``,tech:"microservices",displayNumber:50,sortKey:2222},{id:"microservices-2223",number:51,title:`What do Amazon's "Two-Pizza Teams" and service ownership teach about microservices?`,question:`What do Amazon's "Two-Pizza Teams" and service ownership teach about microservices?`,difficulty:"Intermediate",category:"Real-World",answer:`Amazon organizes around small autonomous teams (small enough to feed with two pizzas) that **own a service end-to-end** — "you build it, you run it." Each team owns design, deploy, on-call, and iteration for its service, aligning team boundaries with service boundaries (Conway's Law used deliberately). This maximizes autonomy and speed and creates strong accountability (the team feels its own operational pain). The lesson: microservices are as much an organizational strategy as a technical one.

\`\`\`text
Conway's Law: system design mirrors org communication structure.
Amazon:  small team  ==  one service  ==  full ownership (build+run+on-call)
Effect:  loose coupling between teams -> independent, fast deployment
Caution: microservices without matching team ownership = distributed monolith
\`\`\``,tech:"microservices",displayNumber:51,sortKey:2223},{id:"microservices-2224",number:52,title:"How do you approach a Monolith-to-Microservices migration?",question:"How do you approach a Monolith-to-Microservices migration?",difficulty:"Hard",category:"Migration",answer:`Migrate incrementally, never big-bang. The **Strangler Fig** pattern routes traffic through a facade/proxy and peels off capabilities one at a time into new services until the monolith is "strangled." Steps: identify bounded contexts, extract the highest-value/lowest-risk capability first, split its data (the hardest part — decouple the shared DB with anti-corruption layers and events), run old and new in parallel, verify, then cut over. Keep the monolith the source of truth until each slice is proven. Don't migrate if the monolith isn't actually the bottleneck.

\`\`\`java
// Strangler Fig: facade routes each capability to old or new implementation
@GetMapping("/orders/**")
Object orders(HttpServletRequest req) {
    return featureFlags.isEnabled("orders-microservice")
        ? newOrderService.handle(req)     // extracted slice
        : legacyMonolith.forward(req);    // not yet migrated
}
// Flip the flag per capability once the new service is verified.
\`\`\``,tech:"microservices",displayNumber:52,sortKey:2224},{id:"microservices-2225",number:53,title:"What are the most common Microservices interview questions to expect?",question:"What are the most common Microservices interview questions to expect?",difficulty:"Basic",category:"Interview Prep",answer:`Be ready for: When do microservices make sense vs a monolith? How do you handle data consistency without distributed transactions (Saga/outbox)? How do services communicate (sync vs async)? How do you prevent cascading failures (timeout/retry/circuit breaker/bulkhead)? How do you find services (discovery)? How do you secure service-to-service calls? How do you trace/debug across services? How do you version and deploy independently? How do you decompose a monolith? What are the downsides/anti-patterns? Always tie answers to trade-offs and a concrete example.

\`\`\`text
Cheat-sheet mapping problem -> pattern:
  cross-service consistency  -> Saga + Transactional Outbox
  dual-write                 -> Outbox + CDC
  slow/failing dependency    -> Timeout + Circuit Breaker + Bulkhead + Fallback
  find instances             -> Service Discovery / k8s DNS
  cross-service query        -> API Composition / CQRS read model
  debug across services      -> Distributed Tracing (trace id)
  decompose monolith         -> Strangler Fig + bounded contexts
\`\`\``,tech:"microservices",displayNumber:53,sortKey:2225},{id:"microservices-2226",number:54,title:"What are the major Microservices Anti-Patterns?",question:"What are the major Microservices Anti-Patterns?",difficulty:"Hard",category:"Anti-Patterns",answer:`- **Distributed Monolith**: services so tightly coupled they must deploy together — worst of both worlds.
- **Shared Database**: multiple services on one schema, re-coupling them.
- **Nano-services / too fine-grained**: excessive chatter and ops overhead.
- **Chatty communication**: many fine-grained sync calls per request (latency, cascading failure).
- **No API versioning**: breaking consumers on change.
- **Synchronous everywhere**: coupling availability; no async where it fits.
- **Ignoring the data problem**: assuming distributed ACID exists.
- **Microservices without the org/automation** to run them (premature adoption).

\`\`\`text
Smell -> Fix
distributed monolith   -> define bounded contexts; async events; independent deploy
shared database        -> database-per-service; integrate via API/events
chatty sync calls      -> aggregate (BFF/API composition); go async where possible
no versioning          -> /v1 URIs, backward-compatible schema evolution
sync everywhere        -> events/queues for commands; sync only for needed queries
\`\`\``,tech:"microservices",displayNumber:54,sortKey:2226},{id:"microservices-2227",number:55,title:"How do you decide the right size/boundaries for a microservice?",question:"How do you decide the right size/boundaries for a microservice?",difficulty:"Hard",category:"Design",answer:`Size by **business capability / bounded context** (DDD), not by lines of code. A good service owns one cohesive capability and its data, changes for one reason (high cohesion), and can be developed/deployed by one team independently (loose coupling). Signs a boundary is wrong: two services always change together (merge them), or one service does unrelated things (split it), or they need chatty sync calls / shared tables (boundary cuts through a transaction). Start coarser and split when real pain (scaling, team friction, deploy coupling) appears — premature decomposition is costly.

\`\`\`text
Good boundary checklist:
  [ ] owns one business capability + its own data
  [ ] one team can build & deploy it independently
  [ ] changes for a single reason (high cohesion)
  [ ] no shared tables / no chatty cross-service transactions
Heuristic: if two services must deploy together, they're really one service.
\`\`\``,tech:"microservices",displayNumber:55,sortKey:2227},{id:"microservices-2228",number:56,title:"How do you build a microservice with Spring Boot and Spring Cloud?",question:"How do you build a microservice with Spring Boot and Spring Cloud?",difficulty:"Intermediate",category:"Java Side",answer:`Spring Boot gives you an embedded server, auto-configuration, and Actuator (health/metrics) so each service is an independently deployable JAR. **Spring Cloud** adds the distributed-systems glue: config server (externalized config), discovery client (Eureka/Consul), declarative HTTP clients (OpenFeign), client-side load balancing, gateway, and Resilience4j integration. A typical service: expose REST controllers, register with discovery, read config centrally, and protect outbound calls with circuit breakers.

\`\`\`java
@SpringBootApplication
@EnableDiscoveryClient                 // register with Eureka/Consul
public class OrderServiceApp {
    public static void main(String[] a) { SpringApplication.run(OrderServiceApp.class, a); }
}
// build.gradle: spring-boot-starter-web, -actuator,
//   spring-cloud-starter-netflix-eureka-client, -openfeign, resilience4j-spring-boot3
\`\`\``,tech:"microservices",displayNumber:56,sortKey:2228},{id:"microservices-2229",number:57,title:"How do you call another service in Java using OpenFeign with resilience?",question:"How do you call another service in Java using OpenFeign with resilience?",difficulty:"Intermediate",category:"Java Side",answer:`OpenFeign turns an interface into an HTTP client resolved via service discovery. Wrap it with Resilience4j for timeouts, retries, and a circuit breaker with a fallback so a failing downstream degrades gracefully instead of cascading.

\`\`\`java
@FeignClient(name = "payment-service", fallback = PaymentFallback.class)
interface PaymentClient {
    @PostMapping("/charge") ChargeReply charge(@RequestBody ChargeRequest r);
}
@Component
class PaymentFallback implements PaymentClient {           // used when circuit open
    public ChargeReply charge(ChargeRequest r) { return ChargeReply.declined(); }
}
\`\`\`
\`\`\`yaml
resilience4j.circuitbreaker.instances.payment-service:
  failureRateThreshold: 50
  waitDurationInOpenState: 10s
feign.circuitbreaker.enabled: true
\`\`\``,tech:"microservices",displayNumber:57,sortKey:2229},{id:"microservices-2230",number:58,title:"How do you implement asynchronous messaging between Java services with Kafka?",question:"How do you implement asynchronous messaging between Java services with Kafka?",difficulty:"Intermediate",category:"Java Side",answer:`Use \`spring-kafka\`: a \`KafkaTemplate\` produces events (partition-keyed for ordering) and \`@KafkaListener\` consumes them within a consumer group. Serialize events as JSON/Avro with a schema registry for evolution. Make consumers idempotent (at-least-once delivery) and configure manual ack + retry/DLQ for poison messages.

\`\`\`java
// Producer — key by orderId keeps a given order's events ordered
kafkaTemplate.send("order.events", order.getId(), new OrderPlaced(order));

// Consumer — idempotent, manual ack, DLQ on repeated failure
@KafkaListener(topics = "order.events", groupId = "inventory")
public void on(OrderPlaced e, Acknowledgment ack) {
    if (processed.add(e.eventId())) inventory.reserve(e);  // dedupe
    ack.acknowledge();
}
// @RetryableTopic(...) auto-creates retry + DLT topics for failures
\`\`\``,tech:"microservices",displayNumber:58,sortKey:2230},{id:"microservices-2231",number:59,title:"How do you externalize configuration for Java microservices across environments?",question:"How do you externalize configuration for Java microservices across environments?",difficulty:"Basic",category:"Java Side",answer:'Use Spring Cloud Config (or k8s ConfigMaps/Secrets) so one build runs everywhere with environment-specific values injected at runtime. Services fetch config on startup (and can hot-refresh with `@RefreshScope` + Actuator `/refresh`). Keep secrets in Vault, not plain config. Profiles (`dev`, `prod`) select the right property set.\n\n```yaml\n# bootstrap.yml — pull config from the central config server by app name + profile\nspring:\n  application: { name: order-service }\n  cloud:\n    config: { uri: http://config-server:8888, profile: ${ENV:dev} }\n```\n```java\n@RefreshScope                      // picks up config changes without restart\n@Component class RatesConfig { @Value("${fx.rate}") BigDecimal rate; }\n```',tech:"microservices",displayNumber:59,sortKey:2231},{id:"microservices-2232",number:60,title:"How do you build a microservice in Node.js and call another service?",question:"How do you build a microservice in Node.js and call another service?",difficulty:"Intermediate",category:"Node Side",answer:`A Node microservice is typically an Express/Fastify app exposing REST/JSON, run as its own process/container. For inter-service calls use \`fetch\`/axios/\`undici\` with an **AbortController timeout**, retries with backoff for idempotent calls, and a circuit breaker (e.g. \`opossum\`) with a fallback. Resolve peers by DNS/service name (k8s) rather than hardcoded IPs.

\`\`\`js
import CircuitBreaker from 'opossum'

async function charge(order) {
  const ac = new AbortController()
  const t = setTimeout(() => ac.abort(), 3000)          // bound the call
  try {
    const res = await fetch('http://payment-service/charge', {
      method: 'POST', signal: ac.signal,
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify(order),
    })
    if (!res.ok) throw new Error(\`payment \${res.status}\`)
    return res.json()
  } finally { clearTimeout(t) }
}
const breaker = new CircuitBreaker(charge, { timeout: 3000, errorThresholdPercentage: 50 })
breaker.fallback(() => ({ status: 'declined' }))        // graceful degradation
\`\`\``,tech:"microservices",displayNumber:60,sortKey:2232},{id:"microservices-2233",number:61,title:"How do you implement async processing in Node with a message queue?",question:"How do you implement async processing in Node with a message queue?",difficulty:"Intermediate",category:"Node Side",answer:`Offload slow/side-effect work to a broker so the request returns fast. With Redis-backed **BullMQ** you enqueue a job and a separate worker process consumes it with retries, backoff, and a dead-letter equivalent (failed set). For event streaming use **KafkaJS** with consumer groups. Keep handlers idempotent since delivery is at-least-once.

\`\`\`js
import { Queue, Worker } from 'bullmq'
const emails = new Queue('emails', { connection })

// producer: return immediately, process later
await emails.add('welcome', { userId }, {
  attempts: 5, backoff: { type: 'exponential', delay: 1000 },
})

// worker: separate process, retried with backoff, idempotent
new Worker('emails', async job => {
  if (await alreadySent(job.data.userId)) return          // dedupe
  await sendWelcome(job.data.userId)
}, { connection })
\`\`\``,tech:"microservices",displayNumber:61,sortKey:2233},{id:"microservices-2234",number:62,title:"How do you handle authentication and correlation IDs across Node microservices?",question:"How do you handle authentication and correlation IDs across Node microservices?",difficulty:"Intermediate",category:"Node Side",answer:`Validate a JWT at the gateway/service (verify signature, issuer, audience, expiry) and forward the user context downstream. Generate or adopt a correlation id per request, attach it to logs (structured, e.g. \`pino\`), and forward it on outbound calls so a request can be traced across services.

\`\`\`js
// middleware: adopt/create correlation id + verify JWT, forward both downstream
app.use((req, res, next) => {
  req.cid = req.headers['x-correlation-id'] ?? crypto.randomUUID()
  res.setHeader('x-correlation-id', req.cid)
  req.user = jwt.verify(bearer(req), publicKey, { issuer, audience })
  next()
})
// outbound call reuses them:
fetch(url, { headers: { 'x-correlation-id': req.cid, authorization: req.headers.authorization } })
\`\`\``,tech:"microservices",displayNumber:62,sortKey:2234},{id:"microservices-2235",number:63,title:"How do you design databases for microservices (schema-per-service) in SQL?",question:"How do you design databases for microservices (schema-per-service) in SQL?",difficulty:"Intermediate",category:"SQL Side",answer:`Give each service its own schema/database with isolated credentials; no service reads another's tables directly. This means you lose cross-service JOINs and foreign keys across boundaries — instead reference other aggregates by **id only**, and build read models via events (CQRS) when you need to display joined data. Each service migrates its own schema independently (Flyway/Liquibase).

\`\`\`sql
-- order-service DB: reference customer by ID only, no FK to another service's table
CREATE TABLE orders (
  id           UUID PRIMARY KEY,
  customer_id  UUID NOT NULL,        -- owned by customer-service; no cross-DB FK
  total_cents  BIGINT NOT NULL,
  status       TEXT   NOT NULL,
  created_at   TIMESTAMPTZ DEFAULT now()
);
-- To show "orders with customer name", the order-service keeps a local
-- read model updated from customer-service events (no cross-service JOIN).
\`\`\``,tech:"microservices",displayNumber:63,sortKey:2235},{id:"microservices-2236",number:64,title:"How do you implement the Transactional Outbox table in SQL?",question:"How do you implement the Transactional Outbox table in SQL?",difficulty:"Hard",category:"SQL Side",answer:`Add an \`outbox\` table written in the *same transaction* as the business change. A relay/CDC process reads unpublished rows, emits them to the broker, and marks them published. This makes the DB write and the intent-to-publish atomic, solving the dual-write problem. Index on \`published\` for the poller; retention job purges old published rows.

\`\`\`sql
CREATE TABLE outbox (
  id            BIGSERIAL PRIMARY KEY,
  aggregate_id  UUID        NOT NULL,
  type          TEXT        NOT NULL,      -- e.g. 'OrderPlaced'
  payload       JSONB       NOT NULL,
  published     BOOLEAN     NOT NULL DEFAULT FALSE,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX idx_outbox_unpublished ON outbox (id) WHERE published = FALSE;

BEGIN;
  INSERT INTO orders (id, customer_id, total_cents, status) VALUES (...);
  INSERT INTO outbox (aggregate_id, type, payload)
    VALUES ('...', 'OrderPlaced', '{"orderId":"..."}');   -- same tx
COMMIT;   -- CDC/relay publishes the outbox row after commit
\`\`\``,tech:"microservices",displayNumber:64,sortKey:2236},{id:"microservices-2237",number:65,title:"How do you enforce idempotency at the database level in SQL?",question:"How do you enforce idempotency at the database level in SQL?",difficulty:"Intermediate",category:"SQL Side",answer:`Store an **idempotency key** with a unique constraint so a retried request can't create duplicate side-effects. On the first call you insert the key (and result); a retry hits the unique violation and you return the stored result instead of re-executing. This makes at-least-once delivery / client retries safe for non-idempotent operations like payments.

\`\`\`sql
CREATE TABLE processed_requests (
  idempotency_key TEXT PRIMARY KEY,       -- client-supplied, unique
  response        JSONB NOT NULL,
  created_at      TIMESTAMPTZ DEFAULT now()
);
-- First call inserts; a retry conflicts and returns the SAME response.
INSERT INTO processed_requests (idempotency_key, response)
VALUES ($1, $2)
ON CONFLICT (idempotency_key) DO NOTHING
RETURNING response;   -- empty rowset => already processed, fetch stored response
\`\`\``,tech:"microservices",displayNumber:65,sortKey:2237},{id:"microservices-2238",number:66,title:"How does a React frontend consume a microservices backend without coupling to every service?",question:"How does a React frontend consume a microservices backend without coupling to every service?",difficulty:"Intermediate",category:"React Side",answer:`The frontend should talk to **one aggregation layer** — an API Gateway or a BFF — not directly to dozens of services, so it isn't coupled to internal topology, CORS sprawl, or N round-trips. The BFF composes/trims data for the screen. In React, centralize the base URL and auth in one API client and use a data-fetching library (React Query/SWR) for caching, retries, and loading/error states.

\`\`\`jsx
// One client points at the gateway/BFF, not individual services
const api = axios.create({ baseURL: import.meta.env.VITE_BFF_URL })
api.interceptors.request.use(c => {
  c.headers.Authorization = \`Bearer \${getToken()}\`
  return c
})
// React Query handles caching/retries/loading so components stay simple
function useHome() {
  return useQuery({ queryKey: ['home'], queryFn: () => api.get('/mobile/home').then(r => r.data) })
}
\`\`\``,tech:"microservices",displayNumber:66,sortKey:2238},{id:"microservices-2239",number:67,title:"How do you handle partial failures and loading states in React with a microservices backend?",question:"How do you handle partial failures and loading states in React with a microservices backend?",difficulty:"Intermediate",category:"React Side",answer:`Because downstream services fail independently, a page may have some sections load and others error. Design the UI for **graceful degradation**: render each region independently, show per-section skeletons/errors (not one all-or-nothing spinner), and use error boundaries plus React Query's \`retry\`/\`isError\` to recover or show fallbacks. This mirrors backend resilience on the client.

\`\`\`jsx
function ProductPage({ id }) {
  const product = useQuery({ queryKey: ['product', id], queryFn: () => getProduct(id) })
  const reviews = useQuery({ queryKey: ['reviews', id], queryFn: () => getReviews(id), retry: 2 })

  if (product.isLoading) return <Skeleton />
  if (product.isError)  return <Error retry={product.refetch} />   // critical section
  return (
    <>
      <ProductInfo data={product.data} />
      {reviews.isError                       // optional section degrades, page still renders
        ? <Notice>Reviews unavailable right now</Notice>
        : <Reviews data={reviews.data} />}
    </>
  )
}
\`\`\``,tech:"microservices",displayNumber:67,sortKey:2239},{id:"microservices-2240",number:68,title:"What are Micro-Frontends and how do they relate to microservices?",question:"What are Micro-Frontends and how do they relate to microservices?",difficulty:"Hard",category:"React Side",answer:`Micro-frontends extend the microservices idea to the UI: split a large frontend into independently developed/deployed pieces, each owned by the team that owns the corresponding backend service (vertical, full-stack ownership). Composition can be at build time, run time (Webpack **Module Federation**), or via web components. Benefits: independent deploys and team autonomy. Costs: shared-dependency duplication, styling/consistency challenges, and orchestration complexity — only worth it at large org scale.

\`\`\`js
// Module Federation: the shell loads a remote micro-frontend at runtime
new ModuleFederationPlugin({
  name: 'shell',
  remotes: { checkout: 'checkout@https://checkout.example.com/remoteEntry.js' },
  shared: { react: { singleton: true }, 'react-dom': { singleton: true } },
})
// const Checkout = React.lazy(() => import('checkout/Cart'))  // owned by another team
\`\`\``,tech:"microservices",displayNumber:68,sortKey:2240},{id:"microservices-2241",number:69,title:"How do you keep API contracts stable between a React frontend and evolving services?",question:"How do you keep API contracts stable between a React frontend and evolving services?",difficulty:"Intermediate",category:"React Side",answer:`Version the API (\`/v1\`) and evolve backward-compatibly (add fields, don't remove/rename). Generate a typed client from the OpenAPI/GraphQL schema so breaking changes surface at compile time, and use **consumer-driven contract tests** (Pact) so the frontend's expectations are verified against providers in CI. The BFF also absorbs shape changes so screens don't break when an internal service changes.

\`\`\`ts
// Types generated from the backend OpenAPI schema — breaking changes fail the build
import type { paths } from './generated/api'
type Home = paths['/mobile/home']['get']['responses']['200']['content']['application/json']

async function getHome(): Promise<Home> {
  const r = await api.get('/mobile/home'); return r.data   // shape guaranteed by types
}
\`\`\``,tech:"microservices",displayNumber:69,sortKey:2241},{id:"microservices-2242",number:70,title:"When should you NOT use microservices?",question:"When should you NOT use microservices?",difficulty:"Intermediate",category:"Design",answer:`Avoid microservices when: the domain/team is small (a modular monolith is faster and simpler), boundaries aren't yet understood (premature splits are expensive to undo), you lack the operational maturity (CI/CD, monitoring, on-call, infra automation) to run many services, or the app isn't hitting scaling/team-friction limits. Microservices trade in-process simplicity for network complexity, eventual consistency, and ops overhead — only worthwhile when their benefits (independent scaling/deploy, team autonomy) outweigh that. Start monolith-first, extract when pain is real.

\`\`\`text
Prefer a (modular) monolith when:
  - small team / early product, boundaries still shifting
  - no mature CI/CD, monitoring, tracing, on-call
  - no scaling or deploy-coupling pain yet
Rule: don't pay distributed-systems tax until a real problem demands it.
Modular monolith first -> extract services along proven seams later.
\`\`\``,tech:"microservices",displayNumber:70,sortKey:2242},{id:"microservices-2243",number:71,title:"What is Short Polling and what are its drawbacks?",question:"What is Short Polling and what are its drawbacks?",difficulty:"Basic",category:"Real-Time Communication",answer:`Short polling is the client repeatedly sending requests at a fixed interval (e.g. every 3s) asking "any new data?". The server responds immediately with data or an empty result. It's trivial to implement over plain HTTP and needs no special server support. Drawbacks: most requests return nothing (wasted bandwidth/CPU), there's inherent latency up to the polling interval, and it scales poorly (N clients × frequency = constant load even when idle). Use it only for low-frequency, latency-tolerant updates.

\`\`\`js
// Client asks on a fixed interval regardless of whether data changed
setInterval(async () => {
  const res = await fetch('/api/notifications')
  const data = await res.json()
  if (data.length) render(data)        // most polls return [] -> wasted calls
}, 3000)                               // latency up to 3s, constant load
\`\`\``,tech:"microservices",displayNumber:71,sortKey:2243},{id:"microservices-2244",number:72,title:"What is Long Polling and how does it improve on short polling?",question:"What is Long Polling and how does it improve on short polling?",difficulty:"Intermediate",category:"Real-Time Communication",answer:`In long polling the client sends a request and the server **holds it open** until data is available (or a timeout), then responds; the client immediately re-requests. This gives near-real-time delivery without constant empty responses — the connection only returns when there's something to send. It works over standard HTTP (firewall/proxy friendly). Drawbacks: a held request consumes a server connection/thread (use async/non-blocking servers), reconnection overhead per message, and it's still request/response (half-duplex), not true push.

\`\`\`js
// Client: re-issues the request as soon as the server responds
async function poll() {
  try {
    const res = await fetch('/api/updates')   // server holds this until data/timeout
    if (res.ok) handle(await res.json())
  } finally {
    poll()                                     // immediately wait for the next event
  }
}
poll()
\`\`\`
\`\`\`java
// Server: hold the request asynchronously, complete it when an event arrives
@GetMapping("/api/updates")
DeferredResult<Update> updates() {
    DeferredResult<Update> out = new DeferredResult<>(30_000L); // timeout
    subscribers.add(out);                       // completed later by publisher
    return out;
}
\`\`\``,tech:"microservices",displayNumber:72,sortKey:2244},{id:"microservices-2245",number:73,title:"What is Server-Sent Events (SSE) and when is it a good fit?",question:"What is Server-Sent Events (SSE) and when is it a good fit?",difficulty:"Intermediate",category:"Real-Time Communication",answer:"SSE is a standard for **server-to-client streaming** over a single long-lived HTTP connection using the `text/event-stream` content type. The server pushes messages as they occur; the browser's `EventSource` auto-reconnects and can resume via `Last-Event-ID`. It's one-directional (server→client only), text-based, and simpler than WebSocket (plain HTTP, works with existing infra). Ideal for feeds, notifications, live scores, or progress updates where the client only receives. For bidirectional/binary needs, use WebSocket.\n\n```js\n// Client — built-in reconnection, no library needed\nconst es = new EventSource('/api/stream')\nes.onmessage = e => render(JSON.parse(e.data))\nes.addEventListener('price', e => updatePrice(JSON.parse(e.data)))\n```\n```java\n// Server (Spring) — push events over one connection\n@GetMapping(value = \"/api/stream\", produces = MediaType.TEXT_EVENT_STREAM_VALUE)\nFlux<ServerSentEvent<Price>> stream() {\n    return prices.map(p -> ServerSentEvent.builder(p).event(\"price\").id(p.id()).build());\n}\n```",tech:"microservices",displayNumber:73,sortKey:2245},{id:"microservices-2246",number:74,title:"What are WebSockets and how do they differ from HTTP?",question:"What are WebSockets and how do they differ from HTTP?",difficulty:"Intermediate",category:"Real-Time Communication",answer:"WebSocket is a protocol providing a **full-duplex, persistent** TCP connection between client and server. It starts as an HTTP request with an `Upgrade: websocket` header (the handshake), then switches protocols; afterward both sides can send messages any time with low overhead (small frames, no repeated headers). Unlike HTTP's request/response, either side can push. It supports text and binary. Ideal for chat, multiplayer games, collaborative editing, and live trading. Costs: stateful connections complicate load balancing/scaling and need heartbeats + reconnection logic.\n\n```js\n// After the HTTP Upgrade handshake, both sides push freely\nconst ws = new WebSocket('wss://example.com/chat')\nws.onopen = () => ws.send(JSON.stringify({ type: 'join', room: 'general' }))\nws.onmessage = e => appendMessage(JSON.parse(e.data))   // server can push anytime\n// Client can also send anytime — full-duplex, unlike request/response HTTP\n```",tech:"microservices",displayNumber:74,sortKey:2246},{id:"microservices-2247",number:75,title:"What is WebRTC and when would you use it?",question:"What is WebRTC and when would you use it?",difficulty:"Hard",category:"Real-Time Communication",answer:`WebRTC enables **peer-to-peer**, low-latency audio/video/data directly between browsers (no media relay through your server in the common case). Key pieces: **getUserMedia** (capture), **RTCPeerConnection** (the P2P media/data channel over UDP/SRTP), **ICE/STUN/TURN** (NAT traversal — STUN discovers public IPs, TURN relays when P2P fails), and a **signaling** channel (you build this, often via WebSocket) to exchange SDP offers/answers and ICE candidates. Use it for video calls, live streaming, screen sharing, and P2P file transfer. It's complex and needs STUN/TURN infrastructure.

\`\`\`js
const pc = new RTCPeerConnection({ iceServers: [{ urls: 'stun:stun.l.google.com:19302' }] })
// 1. Signaling (via YOUR channel, e.g. WebSocket) exchanges these:
const offer = await pc.createOffer(); await pc.setLocalDescription(offer)
signal.send({ sdp: offer })                       // -> peer
pc.onicecandidate = e => e.candidate && signal.send({ ice: e.candidate })
// 2. Media/data then flows peer-to-peer, not through your server:
pc.ontrack = e => remoteVideo.srcObject = e.streams[0]
\`\`\``,tech:"microservices",displayNumber:75,sortKey:2247},{id:"microservices-2248",number:76,title:"Compare Short Polling, Long Polling, SSE, and WebSocket.",question:"Compare Short Polling, Long Polling, SSE, and WebSocket.",difficulty:"Intermediate",category:"Real-Time Communication",answer:`- **Short polling**: client pulls on an interval. Simple; high latency + waste. Low-frequency updates.
- **Long polling**: server holds request until data. Near real-time over HTTP; connection churn. Fallback when WebSocket unavailable.
- **SSE**: server→client stream over HTTP; auto-reconnect; text only, one-way. Feeds/notifications.
- **WebSocket**: full-duplex persistent connection; binary+text; lowest overhead per message; stateful, harder to scale. Chat/games/collab.

Rule: one-way server push → SSE; two-way interactive → WebSocket; must work through restrictive proxies with minimal setup → long polling; trivial/rare updates → short polling; P2P media → WebRTC.

\`\`\`text
                  Direction     Transport   Latency   Complexity  Best for
Short polling     C->S pull     HTTP        high      very low    rare updates
Long polling      C<->S (held)  HTTP        low-med   low         WS fallback
SSE               S->C stream   HTTP        low       low         feeds/notifs
WebSocket         C<->S duplex  TCP(ws)     lowest    medium      chat/games
WebRTC            P2P           UDP/SRTP    lowest    high        video/voice
\`\`\``,tech:"microservices",displayNumber:76,sortKey:2248},{id:"microservices-2249",number:77,title:"How do you scale WebSocket connections across many servers?",question:"How do you scale WebSocket connections across many servers?",difficulty:"Hard",category:"Real-Time Communication",answer:`WebSockets are **stateful**: a connection lives on one server, so a message from user A (on server 1) to user B (on server 2) needs cross-server routing. Solutions: a **pub/sub backplane** (Redis Pub/Sub, Kafka, NATS) so any server can broadcast to connections held elsewhere; **sticky sessions** at the load balancer so a client stays on its server; store connection→server mappings for targeted delivery; and scale horizontally with connection limits per node. Add heartbeats (ping/pong) to detect dead connections and clean up.

\`\`\`js
// Backplane: publish to Redis; every app server subscribes and pushes to its local sockets
redisPub.publish('room:general', JSON.stringify(msg))       // from server 1

redisSub.subscribe('room:general')
redisSub.on('message', (_ch, payload) => {
  const msg = JSON.parse(payload)
  for (const ws of localSocketsInRoom('general')) ws.send(payload) // on every server
})
// LB uses sticky sessions so a client's frames land on the server holding its socket.
\`\`\``,tech:"microservices",displayNumber:77,sortKey:2249},{id:"microservices-2250",number:78,title:"How do you handle reconnection and heartbeats for persistent connections?",question:"How do you handle reconnection and heartbeats for persistent connections?",difficulty:"Intermediate",category:"Real-Time Communication",answer:`Networks drop; persistent connections must detect death and recover. Use **heartbeats** (periodic ping/pong) to detect a half-open connection faster than TCP timeouts. On disconnect, **reconnect with exponential backoff + jitter** to avoid thundering herds. Preserve continuity: resume from a last-seen id/offset (SSE \`Last-Event-ID\`, or an app-level sequence) so no messages are lost or duplicated. Make message handling idempotent since reconnection can replay.

\`\`\`js
let delay = 1000
function connect() {
  const ws = new WebSocket(\`wss://x/stream?since=\${lastSeenId}\`) // resume point
  const hb = setInterval(() => ws.readyState === 1 && ws.send('ping'), 15000)
  ws.onmessage = e => { if (e.data !== 'pong') { lastSeenId = handle(e.data); } }
  ws.onclose = () => {
    clearInterval(hb)
    setTimeout(connect, Math.min(delay *= 2, 30000) + Math.random() * 1000) // backoff+jitter
  }
  ws.onopen = () => { delay = 1000 }
}
connect()
\`\`\``,tech:"microservices",displayNumber:78,sortKey:2250},{id:"microservices-2251",number:79,title:"What is HTTP/2 Server Push and gRPC streaming, versus WebSocket?",question:"What is HTTP/2 Server Push and gRPC streaming, versus WebSocket?",difficulty:"Hard",category:"Real-Time Communication",answer:`- **HTTP/2 server push** lets a server proactively send resources it knows the client will need (mostly for assets; largely deprecated in browsers). It is *not* a general real-time messaging channel.
- **gRPC streaming** (over HTTP/2) offers server-streaming, client-streaming, and bidirectional streaming with typed messages — great for service-to-service real-time and mobile, but limited in browsers without gRPC-Web.
- **WebSocket** is the browser-native choice for arbitrary bidirectional app messaging.

Use gRPC streaming internally between services; WebSocket/SSE at the browser edge.

\`\`\`protobuf
// gRPC bidirectional streaming — typed, multiplexed over HTTP/2
service Chat {
  rpc Connect (stream ClientMsg) returns (stream ServerMsg);  // both directions stream
}
\`\`\`
\`\`\`java
// Server-streaming example
StreamObserver<ServerMsg> obs = ...;
prices.forEach(p -> obs.onNext(ServerMsg.newBuilder().setPrice(p).build()));
obs.onCompleted();
\`\`\``,tech:"microservices",displayNumber:79,sortKey:2251},{id:"microservices-2252",number:80,title:"Exercise — Real-Time Communication: choose the right technique for a live system.",question:"Exercise — Real-Time Communication: choose the right technique for a live system.",difficulty:"Intermediate",category:"Real-Time Communication",answer:`Match the requirement to the mechanism. Examples: a stock **ticker** (server→client only) → SSE; a **chat app** (two-way, text/binary) → WebSocket + Redis backplane; a **video call** → WebRTC with a WebSocket signaling channel; a legacy dashboard behind strict proxies needing occasional updates → long polling; a rarely-changing status badge → short polling. Justify by direction, latency, scale, and infra constraints.

\`\`\`text
Requirement                     -> Choice
live prices (one-way)           -> SSE (auto-reconnect, HTTP-simple)
chat / collaboration (two-way)  -> WebSocket + pub/sub backplane
video / voice call              -> WebRTC (P2P) + WebSocket signaling
progress bar of a long job      -> SSE or long polling
occasional status refresh       -> short polling
service-to-service streaming    -> gRPC bidirectional streaming
\`\`\``,tech:"microservices",displayNumber:80,sortKey:2252},{id:"kafka-2253",number:81,title:"Why is Kafka required? What problem does it solve?",question:"Why is Kafka required? What problem does it solve?",difficulty:"Basic",category:"Kafka Fundamentals",answer:`As systems grow, one service synchronously calling many others creates tight coupling, cascading failures, and load spikes the downstream can't absorb. Kafka decouples producers from consumers with a durable, high-throughput **event log** in the middle: producers write events and move on; consumers read at their own pace, replay history, and scale independently. It absorbs traffic bursts (buffering), enables multiple independent consumers of the same data, and provides durability/ordering that a simple HTTP call or in-memory queue can't. It's the backbone for event-driven architectures and real-time data pipelines.

\`\`\`text
Without Kafka:  order-svc --HTTP--> payment, inventory, email, analytics
   -> tight coupling, one slow/downstream failure blocks the order

With Kafka:     order-svc --> [ "orders" log ] --> payment
                                                --> inventory
                                                --> email
                                                --> analytics
   -> producer unaffected by consumers; each consumes independently & can replay
\`\`\``,tech:"kafka",displayNumber:81,sortKey:2253},{id:"kafka-2254",number:82,title:"What is the difference between Synchronous and Asynchronous processing in system design?",question:"What is the difference between Synchronous and Asynchronous processing in system design?",difficulty:"Basic",category:"Kafka Fundamentals",answer:`**Synchronous**: the caller waits for the operation to finish (request/response). Simple and immediate, but the caller's latency and availability are coupled to the callee. **Asynchronous**: the caller hands off work and continues; the result is processed later (via queue/log/callback). It improves throughput, resilience, and load leveling but introduces eventual consistency and more moving parts. Kafka is an async mechanism: producing an event returns quickly while consumers process independently.

\`\`\`java
// Synchronous: block until payment finishes (coupled latency/availability)
PaymentResult r = paymentClient.charge(order);   // waits

// Asynchronous with Kafka: return immediately; payment happens later
kafka.send("orders", order.getId(), new OrderPlaced(order));  // fire and continue
\`\`\``,tech:"kafka",displayNumber:82,sortKey:2254},{id:"kafka-2255",number:83,title:"What are the ways to do asynchronous processing in system design?",question:"What are the ways to do asynchronous processing in system design?",difficulty:"Intermediate",category:"Kafka Fundamentals",answer:`Common approaches: **background threads/executors** (in-process, lost on crash), **database-backed job tables** (durable, pollable), **message queues** (RabbitMQ/SQS — task distribution, delete on ack), **event streaming logs** (Kafka — retained, replayable, multi-consumer), and **scheduler/cron** for deferred work. Choice depends on durability, ordering, replay, fan-out, and throughput needs. Kafka wins when you need durability + high throughput + multiple independent consumers + replay.

\`\`\`text
In-process executor  -> fast, NOT durable (lost on crash)
DB job table         -> durable, simple, limited throughput
Message queue        -> load distribution, delete-on-ack, single consumer group
Event streaming(Kafka)-> durable log, replay, many consumer groups, high throughput
\`\`\``,tech:"kafka",displayNumber:83,sortKey:2255},{id:"kafka-2256",number:84,title:"What is a Message Queue?",question:"What is a Message Queue?",difficulty:"Basic",category:"Messaging Concepts",answer:`A message queue is a broker that stores messages produced by senders until consumers process them, decoupling the two in time. Classic queues are **point-to-point**: each message is delivered to exactly one consumer among competing consumers, and is typically **removed after acknowledgement**. This is ideal for distributing tasks/work items (a job should run once). Examples: RabbitMQ, ActiveMQ, Amazon SQS. It does not usually retain history for replay once consumed.

\`\`\`text
Producer -> [ Queue: m1, m2, m3 ] -> Consumer A gets m1
                                   -> Consumer B gets m2   (competing consumers)
                                   -> Consumer A gets m3
Each message processed ONCE, then deleted on ack. Good for task distribution.
\`\`\``,tech:"kafka",displayNumber:84,sortKey:2256},{id:"kafka-2257",number:85,title:"What is Event Streaming?",question:"What is Event Streaming?",difficulty:"Intermediate",category:"Messaging Concepts",answer:`Event streaming stores an **ordered, immutable, replayable log** of events that happened. Unlike a queue, events are **retained** (by time/size) after being read, so multiple independent consumers can each read the full stream and re-read from any position. It models a continuous flow of facts ("OrderPlaced", "PaymentMade") rather than one-off tasks, enabling stream processing, analytics, event sourcing, and many consumers of the same data. Kafka is the canonical event streaming platform.

\`\`\`text
Append-only log (retained):  [e0 e1 e2 e3 e4 e5 ...]
   Consumer group A reads from offset 0 ...
   Consumer group B reads the SAME events independently ...
   Either can rewind and replay. Events NOT deleted on read.
\`\`\``,tech:"kafka",displayNumber:85,sortKey:2257},{id:"kafka-2258",number:86,title:"What is the difference between a Message Queue and Event Streaming?",question:"What is the difference between a Message Queue and Event Streaming?",difficulty:"Intermediate",category:"Messaging Concepts",answer:`- **Consumption**: queue deletes on ack (one consumer wins); stream retains, so many consumer groups each read everything.
- **Replay**: queue can't re-read consumed messages; stream can rewind to any offset.
- **Model**: queue = task distribution (do this once); stream = event log / data pipeline (record of facts).
- **Ordering & scale**: streams order per partition and scale via partitioning; queues distribute across competing consumers.

Use a queue for commands/tasks; use streaming for events consumed by multiple systems and for replay/analytics.

\`\`\`text
                 Message Queue            Event Streaming (Kafka)
Retention        delete on ack            retained (time/size)
Consumers        one gets each msg        many groups get all msgs
Replay           no                       yes (seek to offset)
Best for         task distribution        event log, pipelines, analytics
\`\`\``,tech:"kafka",displayNumber:86,sortKey:2258},{id:"kafka-2259",number:87,title:"Is Kafka a Message Queue or an Event Stream?",question:"Is Kafka a Message Queue or an Event Stream?",difficulty:"Intermediate",category:"Kafka Fundamentals",answer:`Kafka is fundamentally an **event streaming platform** (a distributed commit log), but it can *behave like* a message queue. With a single consumer group, partitions are load-balanced among consumers (queue-like, work distribution). With multiple consumer groups, every group gets all messages (pub/sub). The key difference from a classic queue is **retention and replay** — Kafka doesn't delete on consume. So: Kafka is a log that can emulate both queue and pub/sub semantics.

\`\`\`text
Queue-like:  one consumer group "workers" -> partitions split across its consumers
Pub/Sub:     groups "billing" + "analytics" -> each independently gets ALL events
Difference:  Kafka retains + allows replay; a classic MQ deletes on ack.
\`\`\``,tech:"kafka",displayNumber:87,sortKey:2259},{id:"kafka-2260",number:88,title:"What is Kafka?",question:"What is Kafka?",difficulty:"Basic",category:"Kafka Fundamentals",answer:"Apache Kafka is a distributed, fault-tolerant, high-throughput **event streaming platform**. At its core it's an append-only, partitioned **commit log**: producers append events to topics, brokers store them durably and replicated across the cluster, and consumers read them by offset at their own pace. It scales horizontally via partitioning, guarantees ordering per partition, retains data for replay, and handles millions of messages/second. It's used for messaging, activity tracking, log aggregation, stream processing, and as the integration backbone of event-driven systems.\n\n```text\nProducers --append--> [ Broker cluster: topics -> partitions (replicated logs) ] --read by offset--> Consumers\nTraits: distributed, durable (disk + replication), ordered per partition,\n        retained/replayable, horizontally scalable, very high throughput.\n```",tech:"kafka",displayNumber:88,sortKey:2260},{id:"kafka-2261",number:89,title:"What is a Topic in Kafka?",question:"What is a Topic in Kafka?",difficulty:"Basic",category:"Kafka Fundamentals",answer:"A topic is a named **category/stream** of events — the logical channel producers write to and consumers read from (e.g. `orders`, `payments`). Topics are multi-subscriber (many consumer groups). Physically a topic is split into one or more **partitions** for parallelism and scale, and each partition is replicated for fault tolerance. Topics are append-only and retained by a configurable policy.\n\n```bash\n# Create a topic split into 6 partitions, replicated 3x\nkafka-topics.sh --create --topic orders \\\n  --partitions 6 --replication-factor 3 --bootstrap-server broker:9092\n```",tech:"kafka",displayNumber:89,sortKey:2261},{id:"kafka-2262",number:90,title:"Is a Topic the same as an Event?",question:"Is a Topic the same as an Event?",difficulty:"Basic",category:"Kafka Fundamentals",answer:'No. An **event** (record/message) is a single immutable fact — a key, value, timestamp, and headers (e.g. "order A1 placed for $42"). A **topic** is the named log/stream that holds many such events over time. Think of the topic as a table/file and events as the rows/lines appended to it. Producers publish events *to* a topic; consumers read events *from* it.\n\n```text\nTopic "orders"  =  the log/stream (a container)\nEvents in it    =  e0{key:A1,val:placed}  e1{key:A2,val:placed}  e2{key:A1,val:paid}\nOne topic holds many events; an event is a single record within the topic.\n```',tech:"kafka",displayNumber:90,sortKey:2262},{id:"kafka-2263",number:91,title:"What are Partitions and Brokers in Kafka?",question:"What are Partitions and Brokers in Kafka?",difficulty:"Intermediate",category:"Kafka Architecture",answer:`A **partition** is an ordered, immutable, append-only sequence of events within a topic; it's the unit of parallelism and ordering. Each event in a partition has a monotonically increasing **offset**. A **broker** is a Kafka server that stores partitions on disk and serves reads/writes; a cluster has many brokers. Partitions of a topic are distributed across brokers so load and storage spread out, and each partition is replicated to other brokers for fault tolerance. More partitions → more parallel consumers → more throughput.

\`\`\`text
Topic "orders" (6 partitions) spread across 3 brokers:
  Broker1: P0, P3      Broker2: P1, P4      Broker3: P2, P5
Each partition = ordered log with offsets: P0 -> [o0 o1 o2 o3 ...]
Parallelism = number of partitions (max active consumers per group).
\`\`\``,tech:"kafka",displayNumber:91,sortKey:2263},{id:"kafka-2264",number:92,title:"How is ordering guaranteed within a partition?",question:"How is ordering guaranteed within a partition?",difficulty:"Intermediate",category:"Kafka Architecture",answer:`Kafka guarantees **strict order within a single partition**: events are appended sequentially and assigned increasing offsets, and a consumer reads them in that offset order. Ordering is **not** guaranteed across partitions (they're consumed in parallel). To keep related events ordered, produce them with the **same key** (e.g. \`orderId\`) so Kafka's default partitioner hashes them to the same partition. Thus "all events for order A1" stay ordered, while different orders spread across partitions for scale.

\`\`\`java
// Same key -> same partition -> guaranteed order for that key
kafka.send("orders", order.getId(), new OrderPlaced(order));  // A1 -> P2
kafka.send("orders", order.getId(), new OrderPaid(order));    // A1 -> P2 (after placed)
// Different orders (A2, A3) may land on other partitions and be processed in parallel.
\`\`\``,tech:"kafka",displayNumber:92,sortKey:2264},{id:"kafka-2265",number:93,title:"What is a Consumer Group and how does rebalancing work?",question:"What is a Consumer Group and how does rebalancing work?",difficulty:"Intermediate",category:"Kafka Consumers",answer:'A **consumer group** is a set of consumers sharing a `group.id` that cooperatively read a topic: each partition is assigned to exactly **one** consumer in the group, so work is split (parallelism) without duplication. Different groups each get the full stream (pub/sub). **Rebalancing** reassigns partitions when consumers join/leave/crash or partitions change — briefly pausing consumption while ownership is redistributed. Max useful consumers in a group = number of partitions (extra consumers sit idle).\n\n```text\nTopic "orders" (4 partitions P0-P3), group "billing" with 2 consumers:\n  Consumer1 <- P0, P1        Consumer2 <- P2, P3\nAdd Consumer3 -> rebalance -> C1<-P0,P3  C2<-P1  C3<-P2\nAdd a 5th consumer -> it\'s IDLE (more consumers than partitions).\n```',tech:"kafka",displayNumber:93,sortKey:2265},{id:"kafka-2266",number:94,title:"What is the difference between a Topic and a Partition?",question:"What is the difference between a Topic and a Partition?",difficulty:"Basic",category:"Kafka Architecture",answer:`A **topic** is the logical stream/name; a **partition** is a physical sub-log of that topic. A topic is divided into N partitions to enable parallelism, scaling, and ordering-per-key. Producers pick a partition (by key hash or round-robin); consumers in a group divide partitions among themselves. Ordering is per-partition, not per-topic. So: topic = the whole channel, partition = one ordered shard of it.

\`\`\`text
Topic "payments"  ── split into ──►  P0: [e0 e1 e2]
                                     P1: [e0 e1]
                                     P2: [e0 e1 e2 e3]
Topic = logical name & retention/config; Partition = ordered unit of storage & parallelism.
\`\`\``,tech:"kafka",displayNumber:94,sortKey:2266},{id:"kafka-2267",number:95,title:"How do you add a topic in Kafka and choose its configuration?",question:"How do you add a topic in Kafka and choose its configuration?",difficulty:"Basic",category:"Kafka Operations",answer:`Create a topic with a name, partition count, replication factor, and retention settings (or enable auto-creation, discouraged in prod). Partition count sets max parallelism (hard to reduce later); replication factor sets fault tolerance (commonly 3). You can also set retention, cleanup policy (delete vs compact), and min in-sync replicas. Prefer explicit creation with capacity planning.

\`\`\`bash
kafka-topics.sh --create --topic payments \\
  --partitions 12 --replication-factor 3 \\
  --config retention.ms=604800000 \\        # keep 7 days
  --config min.insync.replicas=2 \\
  --bootstrap-server broker:9092
\`\`\``,tech:"kafka",displayNumber:95,sortKey:2267},{id:"kafka-2268",number:96,title:"What is an Offset and how do multiple consumers use it?",question:"What is an Offset and how do multiple consumers use it?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`An **offset** is the sequential id of a record within a partition (0,1,2,…). Each consumer group tracks its **committed offset** per partition — the position up to which it has processed. Different groups have independent offsets, so they read the same partition at different positions. A consumer can also seek to an arbitrary offset to replay. Offsets are what let Kafka retain data yet give each consumer group its own progress pointer.

\`\`\`text
Partition P0: [ o0 o1 o2 o3 o4 o5 o6 ]
   group "billing"   committed offset = 5  (will read o5 next)
   group "analytics" committed offset = 2  (will read o2 next)  -- independent
Replay: consumer.seek(P0, 0) re-reads from the beginning.
\`\`\``,tech:"kafka",displayNumber:96,sortKey:2268},{id:"kafka-2269",number:97,title:"Where are offsets stored and who stores them?",question:"Where are offsets stored and who stores them?",difficulty:"Intermediate",category:"Kafka Consumers",answer:'Consumer group offsets are stored **in Kafka itself**, in an internal compacted topic named `__consumer_offsets` (keyed by group/topic/partition). The consumer commits offsets there — automatically (`enable.auto.commit`) or manually after processing. Storing offsets in Kafka (rather than the old ZooKeeper approach) makes them durable, replicated, and survivable across consumer restarts and rebalances. You may also manage offsets externally (e.g. in your DB) for exactly-once with your sink.\n\n```java\nprops.put("enable.auto.commit", "false");    // commit manually after processing\nrecords.forEach(r -> process(r));\nconsumer.commitSync();                        // writes to __consumer_offsets in Kafka\n// On restart/rebalance, the group resumes from its committed offset.\n```',tech:"kafka",displayNumber:97,sortKey:2269},{id:"kafka-2270",number:98,title:"How do consumers actually consume events from Kafka (pull vs push)?",question:"How do consumers actually consume events from Kafka (pull vs push)?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`Kafka consumers **pull** (poll) — they request batches of records from the broker, rather than the broker pushing. Pull lets consumers control their own rate (natural backpressure), batch efficiently, and replay by offset. The consumer runs a poll loop: fetch records, process them, commit offsets, repeat. Brokers don't track per-consumer delivery state (consumers own their offsets), which keeps brokers simple and scalable.

\`\`\`java
while (running) {
    ConsumerRecords<String, Order> recs = consumer.poll(Duration.ofMillis(200)); // PULL
    for (ConsumerRecord<String, Order> r : recs) {
        process(r.value());              // consumer controls its own pace
    }
    consumer.commitSync();               // advance committed offset
}
\`\`\``,tech:"kafka",displayNumber:98,sortKey:2270},{id:"kafka-2271",number:99,title:"What is a Broker and what does it store?",question:"What is a Broker and what does it store?",difficulty:"Basic",category:"Kafka Architecture",answer:"A **broker** is a single Kafka server in the cluster. It stores partition data as **segment files on disk** (append-only logs plus indexes), serves produce/fetch requests, and hosts a subset of the cluster's partitions and their replicas. What's stored: the actual event records (key, value, timestamp, headers) in ordered segments, plus offset indexes and the internal `__consumer_offsets`/metadata. Brokers rely on the OS page cache and sequential disk I/O for speed. One broker is the **leader** for a partition; others hold **follower** replicas.\n\n```text\nBroker disk layout (per partition):\n  /orders-0/00000000000000000000.log   <- events appended sequentially\n           /00000000000000000000.index <- offset -> byte position\nBroker serves: producer appends, consumer fetches, replication to followers.\n```",tech:"kafka",displayNumber:99,sortKey:2271},{id:"kafka-2272",number:100,title:"What is Partition Replication in Kafka?",question:"What is Partition Replication in Kafka?",difficulty:"Intermediate",category:"Kafka Reliability",answer:`Each partition is copied to multiple brokers (the **replication factor**, e.g. 3). One replica is the **leader** (handles all reads/writes); the others are **followers** that continuously fetch and stay in sync. Replicas that are caught up form the **ISR (In-Sync Replica)** set. If the leader broker fails, a follower from the ISR is promoted to leader, so no data is lost and the partition stays available. Replication is what makes Kafka fault-tolerant.

\`\`\`text
Partition orders-0, replication-factor 3:
  Broker1: LEADER (orders-0)   <- producers/consumers talk here
  Broker2: follower (in-sync)  <- fetches from leader
  Broker3: follower (in-sync)
ISR = {B1,B2,B3}. If B1 dies -> B2 or B3 promoted to leader (no data loss).
\`\`\``,tech:"kafka",displayNumber:100,sortKey:2272},{id:"kafka-2273",number:101,title:"What are Leader and Follower brokers?",question:"What are Leader and Follower brokers?",difficulty:"Intermediate",category:"Kafka Reliability",answer:`For each partition, exactly one replica is the **leader** and the rest are **followers**. All produce and consume traffic for that partition goes through its leader; followers passively replicate the leader's log to stay in the ISR. Leadership is per-partition, so a single broker can be leader for some partitions and follower for others — spreading load. On leader failure, the controller elects a new leader from the ISR. This design balances load and provides high availability.

\`\`\`text
Broker1: LEADER of P0, FOLLOWER of P1, P2
Broker2: LEADER of P1, FOLLOWER of P0, P2
Broker3: LEADER of P2, FOLLOWER of P0, P1
-> read/write load for each partition spread across different leaders.
\`\`\``,tech:"kafka",displayNumber:101,sortKey:2273},{id:"kafka-2274",number:102,title:"What is Kafka metadata and how is the cluster coordinated?",question:"What is Kafka metadata and how is the cluster coordinated?",difficulty:"Hard",category:"Kafka Architecture",answer:`Cluster **metadata** describes topics, partitions, replica assignments, leaders, and ISR. A **controller** manages this — historically stored in **ZooKeeper**, but modern Kafka uses **KRaft** (Kafka Raft), where a quorum of controllers stores metadata in an internal Raft log, removing the ZooKeeper dependency. Clients fetch metadata to learn which broker leads each partition, then talk directly to that leader. The controller handles leader election and reacts to broker failures.

\`\`\`text
Producer/Consumer -> ask any broker for METADATA
   -> "P2 leader = Broker3" -> client connects directly to Broker3 for P2
Controller (KRaft quorum) tracks: topics, partitions, leaders, ISR,
   handles leader election on broker failure. (KRaft replaces ZooKeeper.)
\`\`\``,tech:"kafka",displayNumber:102,sortKey:2274},{id:"kafka-2275",number:103,title:"Walk through the complete end-to-end Kafka flow.",question:"Walk through the complete end-to-end Kafka flow.",difficulty:"Hard",category:"Kafka Architecture",answer:`1. Producer serializes an event, picks a partition (key hash or round-robin), and sends it to that partition's **leader** broker.
2. Leader appends it to its log, assigns an **offset**, and followers in the ISR replicate it.
3. Once \`acks\` conditions are met, the leader acknowledges the producer.
4. The event is retained on disk per the retention policy.
5. Consumers in each group **poll** the leader, read from their committed offset, process, and commit the new offset to \`__consumer_offsets\`.
6. On broker/consumer failure, leader election / rebalancing keeps things running from the last committed state.

\`\`\`text
Produce: app -> partition(key) -> LEADER append -> offset -> ISR replicate -> ack
Store:   segment files on disk, retained by policy
Consume: group polls leader from committed offset -> process -> commit offset
Recover: leader dies -> ISR promotion;  consumer dies -> rebalance & resume
\`\`\``,tech:"kafka",displayNumber:103,sortKey:2275},{id:"kafka-2276",number:104,title:"Why is ordering guaranteed within a partition but not across partitions?",question:"Why is ordering guaranteed within a partition but not across partitions?",difficulty:"Intermediate",category:"Kafka Architecture",answer:`A partition is a single append-only log written and read sequentially, so its offsets define a total order that consumers follow exactly. Across partitions there is no shared clock or coordination — they're written and consumed **in parallel** on possibly different brokers/consumers, so their relative timing is undefined. That parallelism is exactly what gives Kafka its throughput. If you need global ordering you must use a single partition (losing parallelism) or order by key (same key → same partition → ordered).

\`\`\`text
Within P0: o0 < o1 < o2      (strict, single sequential log)
P0 and P1 consumed in parallel -> no defined order between P0.o1 and P1.o0
Need order for an entity? key by it -> all its events land in ONE partition.
Need TOTAL order? one partition only (throughput bottleneck).
\`\`\``,tech:"kafka",displayNumber:104,sortKey:2276},{id:"kafka-2277",number:105,title:"Can multiple consumers read from the same partition?",question:"Can multiple consumers read from the same partition?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`Within **one consumer group**, a partition is assigned to exactly **one** consumer at a time — so two consumers in the same group cannot both read the same partition (this preserves ordering and avoids double-processing). Across **different consumer groups**, yes: each group independently reads the same partition with its own offset. So the answer is "same group: no; different groups: yes." This is how Kafka does both work-splitting and fan-out.

\`\`\`text
Partition P0:
  group "billing"   -> only Consumer-B1 reads P0 (never two in same group)
  group "analytics" -> Consumer-A1 also reads P0 independently (own offset)
Same group => one consumer per partition; different groups => parallel independent reads.
\`\`\``,tech:"kafka",displayNumber:105,sortKey:2277},{id:"kafka-2278",number:106,title:"How many partitions should you choose when defining a topic?",question:"How many partitions should you choose when defining a topic?",difficulty:"Hard",category:"Kafka Operations",answer:"Partitions set max consumer parallelism and throughput, but too many add overhead (more files, longer leader elections, more end-to-end latency, more memory). Estimate from target throughput: `partitions ≈ max(desired_throughput / per_partition_throughput, desired_consumer_parallelism)`. Plan for growth since increasing partitions later changes key→partition mapping (breaking per-key ordering) and you can't easily decrease. Common practice: start with a modest number (e.g. tens) sized to peak consumer count, with headroom.\n\n```text\npartitions = max( T_target / T_per_partition , consumers_needed )\ne.g. need 600 MB/s, one partition sustains ~50 MB/s -> >= 12 partitions\nAlso >= max consumers you want in a group.\nCaution: adding partitions later re-hashes keys -> breaks existing per-key ordering.\n```",tech:"kafka",displayNumber:106,sortKey:2278},{id:"kafka-2279",number:107,title:"When are events deleted from Kafka?",question:"When are events deleted from Kafka?",difficulty:"Intermediate",category:"Kafka Operations",answer:"Not on consumption. Kafka deletes by **retention policy**: time-based (`retention.ms`, e.g. 7 days) and/or size-based (`retention.bytes`) — old **segments** are removed once past the limit. Alternatively, with **log compaction** (`cleanup.policy=compact`), Kafka retains at least the latest value per key indefinitely (great for changelogs/state). So events live until retention expires or compaction supersedes them — independent of whether consumers have read them.\n\n```bash\n# Time/size retention (delete old data)\n--config cleanup.policy=delete --config retention.ms=604800000\n\n# Compaction (keep latest value per key forever) — for changelog/state topics\n--config cleanup.policy=compact\n```",tech:"kafka",displayNumber:107,sortKey:2279},{id:"kafka-2280",number:108,title:"What are offset commit strategies and the delivery guarantees (at-most-once, at-least-once, exactly-once)?",question:"What are offset commit strategies and the delivery guarantees (at-most-once, at-least-once, exactly-once)?",difficulty:"Hard",category:"Kafka Reliability",answer:`The guarantee depends on **when** you commit the offset relative to processing:
- **At-most-once**: commit **before** processing → if you crash mid-process, the message is lost but never reprocessed.
- **At-least-once**: commit **after** processing → on crash you reprocess (duplicates possible) — the common default; make consumers idempotent.
- **Exactly-once**: no message lost or duplicated — achieved with Kafka **transactions** (idempotent producer + transactional read-process-write) or by atomically storing offset with the result in your sink.

\`\`\`java
// AT-LEAST-ONCE: process first, then commit (safe default; dedupe downstream)
process(record); consumer.commitSync();

// AT-MOST-ONCE: commit first, then process (loss on crash, no dupes)
consumer.commitSync(); process(record);

// EXACTLY-ONCE: transactional produce+offset commit
producer.initTransactions();
producer.beginTransaction();
producer.send(outRecord);
producer.sendOffsetsToTransaction(offsets, groupMetadata);
producer.commitTransaction();
\`\`\``,tech:"kafka",displayNumber:108,sortKey:2280},{id:"kafka-2281",number:109,title:"Exercise — Delivery Guarantees: make an at-least-once consumer idempotent.",question:"Exercise — Delivery Guarantees: make an at-least-once consumer idempotent.",difficulty:"Hard",category:"Kafka Reliability",answer:`At-least-once means duplicates on retry, so the consumer must dedupe. Track processed event ids (or use the natural business key with an upsert / unique constraint) so reprocessing the same event has no additional effect. Commit offsets only after the idempotent write succeeds.

\`\`\`java
@KafkaListener(topics = "payments", groupId = "ledger")
void handle(PaymentEvent e, Acknowledgment ack) {
    // Unique constraint on event_id makes the insert idempotent
    int rows = jdbc.update(
        "INSERT INTO ledger(event_id, account, amount) VALUES (?,?,?) " +
        "ON CONFLICT (event_id) DO NOTHING", e.id(), e.account(), e.amount());
    // duplicate -> rows == 0, no double credit
    ack.acknowledge();   // commit only after successful (idempotent) write
}
\`\`\``,tech:"kafka",displayNumber:109,sortKey:2281},{id:"kafka-2282",number:110,title:"How do consumers know an event has arrived, and how does Kafka handle a consumer crash?",question:"How do consumers know an event has arrived, and how does Kafka handle a consumer crash?",difficulty:"Intermediate",category:"Kafka Consumers",answer:'Consumers **poll** in a loop; the broker returns any records past their fetch offset (they don\'t get "notified" — they ask). Liveness is tracked via **heartbeats** and `max.poll.interval.ms`: if a consumer stops heartbeating or takes too long between polls (crash/hang), the group coordinator declares it dead and triggers a **rebalance**, reassigning its partitions to surviving consumers. Those consumers resume from the last **committed offset**, so no committed progress is lost (uncommitted work is reprocessed → at-least-once).\n\n```text\nPoll loop fetches new records past the committed offset.\nHeartbeat thread proves liveness; session.timeout.ms / max.poll.interval.ms bound it.\nConsumer crashes -> coordinator times it out -> REBALANCE\n   -> its partitions reassigned -> peers resume from last committed offset.\n```',tech:"kafka",displayNumber:110,sortKey:2282},{id:"kafka-2283",number:111,title:"Does every event have an offset, and who assigns it?",question:"Does every event have an offset, and who assigns it?",difficulty:"Basic",category:"Kafka Architecture",answer:`Yes — every record in a partition gets a unique, monotonically increasing **offset**, assigned by the **partition leader broker** at append time (not by the producer). Offsets are per-partition (P0 and P1 both start at 0 independently). This offset is the record's permanent address within that partition and what consumers use to track position and replay.

\`\`\`text
Producer sends record (no offset yet)
  -> LEADER appends to partition log
  -> LEADER assigns next offset (e.g. 42) and returns it in the ack
Offsets are per-partition & assigned server-side by the leader.
\`\`\``,tech:"kafka",displayNumber:111,sortKey:2283},{id:"kafka-2284",number:112,title:"What is Backpressure and how does Kafka handle it?",question:"What is Backpressure and how does Kafka handle it?",difficulty:"Hard",category:"Kafka Reliability",answer:"Backpressure is when producers generate data faster than consumers can process it. Kafka absorbs it naturally because it's a **durable buffer**: events pile up on disk (bounded by retention), and consumers **pull** at their own pace without being overwhelmed or dropping data (unlike push systems). If consumers fall behind, **consumer lag** grows — the signal to scale out consumers (up to the partition count), optimize processing, or add partitions. The producer side can also apply backpressure via buffer limits (`buffer.memory`, `max.block.ms`).\n\n```text\nProducer fast, consumer slow -> events buffered on disk, consumer LAG rises.\nKafka handles it via: durable retention + pull-based consumers (no drop, no overload).\nFix rising lag: add consumers (<= #partitions), speed up processing, add partitions.\nProducer backpressure: buffer.memory full -> send() blocks up to max.block.ms.\n```",tech:"kafka",displayNumber:112,sortKey:2284},{id:"kafka-2285",number:113,title:"What does Kafka do when a topic is created?",question:"What does Kafka do when a topic is created?",difficulty:"Intermediate",category:"Kafka Operations",answer:`On topic creation the controller: validates config, decides **partition placement** and **replica assignment** across brokers (spreading leaders/followers for balance), elects an initial **leader** per partition, updates cluster **metadata**, and instructs brokers to create the on-disk log directories/segment files for their assigned partitions. Once metadata propagates, producers/consumers can discover leaders and start using the topic.

\`\`\`text
create topic -> controller assigns partitions+replicas to brokers
             -> elects leader per partition
             -> brokers create /topic-N log dirs (empty segment files)
             -> metadata updated & propagated -> clients can produce/consume
\`\`\``,tech:"kafka",displayNumber:113,sortKey:2285},{id:"kafka-2286",number:114,title:"How do you add a consumer group to an existing topic?",question:"How do you add a consumer group to an existing topic?",difficulty:"Basic",category:"Kafka Operations",answer:'You don\'t pre-create consumer groups — just start consumers with a **new `group.id`** subscribed to the topic. The new group registers with the coordinator and, per `auto.offset.reset`, begins from the **earliest** (replay all retained events) or **latest** (only new events) offset. Because Kafka retains events and groups have independent offsets, adding a group never affects existing ones — a core benefit over queues.\n\n```java\nprops.put("group.id", "new-analytics");            // brand-new group\nprops.put("auto.offset.reset", "earliest");        // replay full history\n                                                   // ("latest" = only new events)\nconsumer.subscribe(List.of("orders"));\n// Existing groups (billing, etc.) are completely unaffected.\n```',tech:"kafka",displayNumber:114,sortKey:2286},{id:"kafka-2287",number:115,title:"How does Kafka handle a huge number of write operations?",question:"How does Kafka handle a huge number of write operations?",difficulty:"Hard",category:"Kafka Performance",answer:`Kafka achieves very high write throughput via: **sequential disk writes** (append-only logs are fast even on spinning disks), **partitioning** (writes spread across many partitions/brokers in parallel), **OS page cache** (writes buffered in memory, flushed by the OS), **zero-copy** reads (\`sendfile\`) for consumers, **batching + compression** on the producer, and minimal per-message broker bookkeeping (consumers own offsets). Together these let a cluster sustain millions of messages/second.

\`\`\`text
High write throughput comes from:
  - append-only SEQUENTIAL disk I/O (no random seeks)
  - PARTITIONING -> parallel writes across brokers
  - producer BATCHING + COMPRESSION (fewer, bigger requests)
  - OS PAGE CACHE + zero-copy (sendfile) on read path
  - brokers store little per-message state (consumers track offsets)
\`\`\``,tech:"kafka",displayNumber:115,sortKey:2287},{id:"kafka-2288",number:116,title:"What is the Pub-Sub model and how does Kafka relate to it?",question:"What is the Pub-Sub model and how does Kafka relate to it?",difficulty:"Intermediate",category:"Messaging Concepts",answer:`Pub-Sub (publish-subscribe) decouples publishers from subscribers via topics: publishers send to a topic, and **all** subscribers to that topic receive the message. Classic pub/sub systems (e.g. Google Pub/Sub, JMS topics) typically push to subscribers and don't retain messages after delivery. Kafka implements pub/sub semantics via **consumer groups** (each group = a subscriber that gets all events) but adds a persistent, replayable log — so it's pub/sub plus durability, ordering, and work-partitioning.

\`\`\`text
Pub/Sub: publisher -> topic -> every subscriber gets a copy
Kafka:   producer  -> topic -> every consumer GROUP gets all events (pub/sub)
                              within a group, partitions split work (queue-like)
Kafka adds: retention + replay + per-partition ordering + horizontal scale.
\`\`\``,tech:"kafka",displayNumber:116,sortKey:2288},{id:"kafka-2289",number:117,title:'Why is Kafka not "just" Pub-Sub?',question:'Why is Kafka not "just" Pub-Sub?',difficulty:"Intermediate",category:"Messaging Concepts",answer:`Because Kafka is a **persistent, ordered log**, not just a message dispatcher. Beyond pub/sub fan-out it provides: **retention & replay** (re-read history, bootstrap new consumers), **per-partition ordering**, **partitioned parallelism** (queue-like work splitting within a group), **offset-based independent progress** per group, and durability/replication. Traditional pub/sub usually pushes and forgets. Kafka's log model also enables stream processing (Kafka Streams), event sourcing, and CDC pipelines — use cases plain pub/sub can't serve.

\`\`\`text
Plain Pub/Sub          Kafka
push & forget          durable log, replayable
no ordering guarantee  ordered per partition
one delivery, no replay seek to any offset, re-read
subscriber = 1 stream  consumer group = fan-out + internal work split
                       + stream processing, event sourcing, CDC
\`\`\``,tech:"kafka",displayNumber:117,sortKey:2289},{id:"kafka-2290",number:118,title:"When should you use Kafka and when a simpler Pub/Sub (or queue)?",question:"When should you use Kafka and when a simpler Pub/Sub (or queue)?",difficulty:"Intermediate",category:"Messaging Concepts",answer:`Use **Kafka** when you need high throughput, durable/replayable event history, multiple independent consumers, ordering, or stream processing (analytics pipelines, event sourcing, CDC, activity streams). Use a **managed Pub/Sub or queue** (SQS, Google Pub/Sub, RabbitMQ) when you want simple task distribution, request/reply, per-message TTL/priority, or minimal ops overhead and don't need replay/ordering/high fan-out. Don't run Kafka's operational complexity for a low-volume job queue.

\`\`\`text
Choose Kafka when:            Choose simple Pub/Sub/Queue when:
  high throughput / streams      low-volume task distribution
  replay & retention needed      no replay needed
  many independent consumers     one worker pool
  ordering matters               per-message priority/TTL/delay
  event sourcing / CDC           minimal ops, fully managed
\`\`\``,tech:"kafka",displayNumber:118,sortKey:2290},{id:"kafka-2291",number:119,title:"Kafka vs RabbitMQ — what are the key differences?",question:"Kafka vs RabbitMQ — what are the key differences?",difficulty:"Hard",category:"Messaging Concepts",answer:`- **Model**: Kafka is a durable, partitioned **log** (pull, retained, replayable); RabbitMQ is a **smart broker / message queue** (push, routes via exchanges, deletes on ack).
- **Throughput**: Kafka excels at very high throughput and streaming; RabbitMQ favors flexible routing and lower-latency task delivery.
- **Routing**: RabbitMQ has rich routing (direct/topic/fanout/headers exchanges); Kafka routing is by topic/partition/key.
- **Ordering/replay**: Kafka retains & replays with per-partition order; RabbitMQ generally doesn't replay.
- **Use**: Kafka for event streaming/pipelines; RabbitMQ for complex routing, RPC, and traditional task queues.

\`\`\`text
                Kafka                         RabbitMQ
Paradigm        distributed log (pull)        message broker/queue (push)
Retention       retained, replayable          deleted on ack
Throughput      very high (streaming)          high (task-oriented)
Routing         topic/partition/key           exchanges (direct/topic/fanout/headers)
Ordering        per partition                 per queue (best-effort)
Best for        streams, pipelines, sourcing  routing, RPC, task queues
\`\`\``,tech:"kafka",displayNumber:119,sortKey:2291},{id:"kafka-2292",number:120,title:"Can a partition contain events of multiple topics?",question:"Can a partition contain events of multiple topics?",difficulty:"Basic",category:"Kafka Architecture",answer:`No. A partition belongs to exactly **one** topic and only holds that topic's events. Topics are isolated units of storage and configuration; each has its own set of partitions. A broker hosts partitions from many topics, but any single partition's log contains records for just one topic.

\`\`\`text
Topic "orders":   P0, P1, P2   (only order events)
Topic "payments": P0, P1       (only payment events)
A partition = one topic's shard. Never mixed. Broker can host both topics' partitions.
\`\`\``,tech:"kafka",displayNumber:120,sortKey:2292},{id:"kafka-2293",number:121,title:"Do you create new partitions for every new topic?",question:"Do you create new partitions for every new topic?",difficulty:"Basic",category:"Kafka Operations",answer:"Yes — each topic has its **own** partitions, created when the topic is created (you specify the count). Partitions are not shared between topics. Different topics can have different partition counts based on their throughput/parallelism needs (e.g. `orders`=12, `audit-log`=1). Adding a topic means allocating and placing its partitions/replicas across brokers.\n\n```bash\nkafka-topics.sh --create --topic orders   --partitions 12 --replication-factor 3 ...\nkafka-topics.sh --create --topic audit    --partitions 1  --replication-factor 3 ...\n# Each topic gets its own independent set of partitions, sized per its needs.\n```",tech:"kafka",displayNumber:121,sortKey:2293},{id:"kafka-2294",number:122,title:"Can a consumer consume from multiple topics?",question:"Can a consumer consume from multiple topics?",difficulty:"Basic",category:"Kafka Consumers",answer:`Yes. A single consumer (and consumer group) can **subscribe to multiple topics** at once (by list or regex pattern). The group coordinator assigns partitions across all subscribed topics among the group's consumers. This is handy for a service reacting to several event streams. Ordering is still only guaranteed per partition, not across topics.

\`\`\`java
consumer.subscribe(List.of("orders", "payments", "shipments"));   // multiple topics
// or by pattern:
consumer.subscribe(Pattern.compile("audit\\\\..*"));
// The group's consumers share all partitions of all subscribed topics.
\`\`\``,tech:"kafka",displayNumber:122,sortKey:2294},{id:"kafka-2295",number:123,title:"How do large systems like Uber, Amazon, YouTube, and WhatsApp use Kafka?",question:"How do large systems like Uber, Amazon, YouTube, and WhatsApp use Kafka?",difficulty:"Intermediate",category:"Kafka Real-World",answer:`- **Uber**: Kafka is the real-time backbone — trip events, driver/rider location streams, surge pricing, fraud detection, and analytics pipelines feeding stream processors.
- **Amazon-scale commerce**: order events, inventory updates, clickstream, and decoupling microservices; MSK (managed Kafka) for event-driven services.
- **YouTube/streaming-scale**: activity/view events, recommendations, and log/metric aggregation pipelines.
- **WhatsApp/messaging-scale**: high-throughput message routing, delivery/read receipts, and async fan-out to devices.

Common thread: decoupling producers/consumers, absorbing massive event volume, replayable pipelines, and feeding real-time analytics/ML.

\`\`\`text
Uber:    location + trip events -> surge pricing, ETA, fraud, analytics
Amazon:  order/inventory/clickstream events -> decoupled microservices, ML
YouTube: views/activity events -> recommendations, metrics aggregation
WhatsApp:message/receipt events -> high-throughput routing & async fan-out
Pattern: durable, replayable event backbone feeding real-time processing.
\`\`\``,tech:"kafka",displayNumber:123,sortKey:2295},{id:"kafka-2296",number:124,title:"When should you (and shouldn't you) use Kafka in a system design interview?",question:"When should you (and shouldn't you) use Kafka in a system design interview?",difficulty:"Intermediate",category:"Kafka Real-World",answer:`**Reach for Kafka** when the design needs: async decoupling at scale, high write/ingest throughput, multiple consumers of the same events, replay/retention, ordering per key, or a streaming/analytics/CDC pipeline. **Avoid it** for simple request/reply, low-volume tasks (a queue or DB job table suffices), when you need strict cross-entity global ordering, or when the added operational complexity isn't justified. In interviews, name the specific property (durability, fan-out, replay, throughput) that makes Kafka the right tool — don't add it reflexively.

\`\`\`text
Use Kafka in a design when you can point to:
  [ ] high-throughput event ingestion   [ ] many independent consumers
  [ ] replay / retention needed         [ ] per-key ordering
  [ ] stream processing / CDC / sourcing
Skip it for: simple RPC, small task queues, strict global ordering, tiny scale.
Interview tip: justify with the PROPERTY, not the buzzword.
\`\`\``,tech:"kafka",displayNumber:124,sortKey:2296},{id:"kafka-2297",number:125,title:"Exercise — Kafka Design: build an order-processing pipeline with ordering and no data loss.",question:"Exercise — Kafka Design: build an order-processing pipeline with ordering and no data loss.",difficulty:"Hard",category:"Kafka Real-World",answer:`Key the \`orders\` topic by \`orderId\` so all events for an order keep order in one partition. Produce with \`acks=all\` + idempotent producer for no loss/duplication on the write side. Consumers use at-least-once with idempotent writes (dedupe by event id) and commit offsets after processing. Size partitions to peak consumer parallelism, replication factor 3 with \`min.insync.replicas=2\` for durability.

\`\`\`java
// Producer: durable + no dup on retry, ordered per order
props.put("acks", "all");
props.put("enable.idempotence", "true");
props.put("min.insync.replicas", "2");            // topic-side config
kafka.send("orders", order.getId(), event);        // key = orderId -> one partition

// Consumer: at-least-once + idempotent sink, commit after processing
@KafkaListener(topics = "orders", groupId = "fulfillment")
void on(OrderEvent e, Acknowledgment ack) {
    if (dedupe.insertIfAbsent(e.eventId()))        // idempotent
        fulfillment.apply(e);
    ack.acknowledge();                             // commit after success
}
\`\`\``,tech:"kafka",displayNumber:125,sortKey:2297},{id:"kafka-2298",number:126,title:"What is KafkaProducer and is it thread-safe?",question:"What is KafkaProducer and is it thread-safe?",difficulty:"Basic",category:"Kafka Producers",answer:`\`KafkaProducer\` is the client applications use to publish records to Kafka. It **is thread-safe** and is designed to be shared across threads — creating one producer per application (not per request/thread) is the recommended pattern, since it maintains connection pools, batching buffers, and metadata that are expensive to recreate. Internally it batches records per partition and sends them asynchronously via an I/O thread.

\`\`\`java
// One shared, thread-safe producer for the whole app
Properties p = new Properties();
p.put("bootstrap.servers", "broker:9092");
p.put("key.serializer", "org.apache.kafka.common.serialization.StringSerializer");
p.put("value.serializer", "org.apache.kafka.common.serialization.StringSerializer");
KafkaProducer<String,String> producer = new KafkaProducer<>(p);  // share across threads
\`\`\``,tech:"kafka",displayNumber:126,sortKey:2298},{id:"kafka-2299",number:127,title:"Is `send()` synchronous or asynchronous, and how do you wait for the result?",question:"Is `send()` synchronous or asynchronous, and how do you wait for the result?",difficulty:"Basic",category:"Kafka Producers",answer:'`send()` is **asynchronous**: it adds the record to the in-memory accumulator (batched per partition) and returns a `Future<RecordMetadata>` immediately; the actual network send happens on a background I/O thread. You can make it effectively synchronous by calling `.get()` on the future (blocks until ack), or pass a **callback** for non-blocking completion handling. Blocking per-record kills throughput — prefer callbacks or `flush()` at checkpoints.\n\n```java\n// Async with callback (preferred — high throughput)\nproducer.send(new ProducerRecord<>("orders", orderId, json), (meta, ex) -> {\n    if (ex != null) log.error("send failed", ex);\n    else log.info("sent to {}-{}@{}", meta.topic(), meta.partition(), meta.offset());\n});\n\n// Synchronous (blocks — use sparingly)\nRecordMetadata meta = producer.send(record).get();  // waits for ack\n```',tech:"kafka",displayNumber:127,sortKey:2299},{id:"kafka-2300",number:128,title:"Explain the `acks` setting (0, 1, all) and which is strongest.",question:"Explain the `acks` setting (0, 1, all) and which is strongest.",difficulty:"Intermediate",category:"Kafka Producers",answer:'`acks` controls how many acknowledgements the producer waits for before considering a write successful:\n- **acks=0**: fire-and-forget; no ack. Lowest latency, but records can be lost silently.\n- **acks=1**: the **leader** acks after writing to its local log; if the leader dies before followers replicate, the record can be lost.\n- **acks=all** (-1): the leader waits until all **in-sync replicas** (per `min.insync.replicas`) have the record. Strongest durability, highest latency.\n\nFor no data loss use **acks=all** with `min.insync.replicas=2` and replication factor 3.\n\n```java\np.put("acks", "all");                 // strongest: wait for in-sync replicas\np.put("enable.idempotence", "true");  // no duplicates on retry\n// Topic side: min.insync.replicas=2, replication.factor=3\n// acks=0 -> may lose data; acks=1 -> lose if leader fails pre-replication\n```',tech:"kafka",displayNumber:128,sortKey:2300},{id:"kafka-2301",number:129,title:"What is `min.insync.replicas` and what happens if the ISR falls below it?",question:"What is `min.insync.replicas` and what happens if the ISR falls below it?",difficulty:"Intermediate",category:"Kafka Producers",answer:"`min.insync.replicas` is the minimum number of in-sync replicas that must acknowledge an **acks=all** write for it to succeed. It's a durability guardrail: with RF=3 and `min.insync.replicas=2`, a write needs the leader + at least one follower in sync. If the ISR shrinks below this (brokers down/lagging), acks=all writes **fail** with `NotEnoughReplicas`/`NotEnoughReplicasAfterAppend` — Kafka refuses to accept data it can't durably replicate (choosing consistency over availability for that partition).\n\n```text\nRF=3, min.insync.replicas=2, acks=all:\n  ISR = {leader, f1, f2}  -> writes succeed\n  ISR = {leader, f1}      -> writes still succeed (2 >= 2)\n  ISR = {leader}          -> writes FAIL (NotEnoughReplicas) until a follower re-syncs\nGuardrail: never ack data that isn't replicated enough to survive a broker loss.\n```",tech:"kafka",displayNumber:129,sortKey:2301},{id:"kafka-2302",number:130,title:"Explain producer batching: `batch.size` and `linger.ms`.",question:"Explain producer batching: `batch.size` and `linger.ms`.",difficulty:"Intermediate",category:"Kafka Producers",answer:'The producer groups records destined for the same partition into **batches** to amortize network/protocol overhead. `batch.size` is the max bytes per batch (per partition) before it\'s sent; `linger.ms` is how long the producer waits for more records to fill a batch before sending, even if `batch.size` isn\'t reached. Increasing `linger.ms` (e.g. 5–20ms) trades a little latency for much higher throughput and better compression. `batch.size=0` disables batching.\n\n```java\np.put("batch.size", 32 * 1024);   // 32KB target batch per partition\np.put("linger.ms", 10);           // wait up to 10ms to fill the batch\n// Bigger batches + small linger -> fewer, larger requests -> higher throughput.\n// linger.ms=0 (default) sends ASAP -> lowest latency, smaller batches.\n```',tech:"kafka",displayNumber:130,sortKey:2302},{id:"kafka-2303",number:131,title:"What is producer compression and what are the trade-offs?",question:"What is producer compression and what are the trade-offs?",difficulty:"Intermediate",category:"Kafka Producers",answer:'`compression.type` (none, gzip, snappy, lz4, zstd) compresses record **batches** before sending. It reduces network bandwidth and disk storage (broker stores them compressed) and often *increases* throughput, at the cost of CPU. Compression works best with batching (bigger batches compress better). **lz4** and **zstd** are usually the best balance (zstd = best ratio, lz4 = fastest); gzip has high ratio but more CPU.\n\n```java\np.put("compression.type", "zstd");   // great ratio; lz4 = fastest\np.put("linger.ms", 10);              // batching amplifies compression benefit\n// Trade-off: less network/disk, more CPU. Broker stores batches compressed,\n// and consumers decompress — end-to-end savings.\n```',tech:"kafka",displayNumber:131,sortKey:2303},{id:"kafka-2304",number:132,title:"What is the idempotent producer and why is it important?",question:"What is the idempotent producer and why is it important?",difficulty:"Hard",category:"Kafka Producers",answer:'`enable.idempotence=true` makes the producer **exactly-once per partition within a session**: the broker deduplicates retried records so a network retry can\'t create duplicates. It works by assigning each producer a **PID (producer id)** and a monotonic **sequence number** per partition; the broker rejects duplicates/out-of-order sequences. This fixes the classic "retry after a lost ack creates a duplicate" problem. Enabling it also enforces safe settings (`acks=all`, bounded in-flight). It\'s the default in recent Kafka and a prerequisite for transactions.\n\n```java\np.put("enable.idempotence", "true");   // PID + per-partition sequence numbers\n// Implies acks=all, retries>0, max.in.flight<=5 -> no duplicates from retries,\n// and preserves ordering even with retries. Basis for EOS/transactions.\n```',tech:"kafka",displayNumber:132,sortKey:2304},{id:"kafka-2305",number:133,title:"What is `max.in.flight.requests.per.connection` and how does it affect ordering?",question:"What is `max.in.flight.requests.per.connection` and how does it affect ordering?",difficulty:"Hard",category:"Kafka Producers",answer:'It caps the number of unacknowledged produce requests per broker connection. Higher values improve throughput (more parallel in-flight requests) but, **without idempotence**, a retry of an earlier failed request can land *after* a later successful one — reordering records within a partition. With `enable.idempotence=true`, Kafka preserves ordering even with up to 5 in-flight requests (it tracks sequence numbers). Without idempotence, set it to 1 if strict ordering matters.\n\n```java\n// Safe ordering + throughput:\np.put("enable.idempotence", "true");\np.put("max.in.flight.requests.per.connection", "5");   // ordering still preserved\n\n// Without idempotence, only in.flight=1 guarantees no reordering on retry.\n```',tech:"kafka",displayNumber:133,sortKey:2305},{id:"kafka-2306",number:134,title:"Explain the producer timeout/retry settings: `delivery.timeout.ms`, `retries`, `retry.backoff.ms`, `max.block.ms`.",question:"Explain the producer timeout/retry settings: `delivery.timeout.ms`, `retries`, `retry.backoff.ms`, `max.block.ms`.",difficulty:"Intermediate",category:"Kafka Producers",answer:'- **delivery.timeout.ms**: the total upper bound for a record from `send()` to success/failure, including batching, network, and all retries. This is the primary knob to bound delivery.\n- **retries**: how many times transient errors are retried (default high; usually leave it and bound via `delivery.timeout.ms`).\n- **retry.backoff.ms**: delay between retries (avoids hammering).\n- **max.block.ms**: how long `send()`/`partitionsFor()` may block waiting for metadata or free buffer space before throwing.\n\n```java\np.put("delivery.timeout.ms", 120000);  // total budget incl. retries (2 min)\np.put("retry.backoff.ms", 100);        // wait between retries\np.put("max.block.ms", 60000);          // send() blocks max 60s for buffer/metadata\n// retries is bounded in practice by delivery.timeout.ms.\n```',tech:"kafka",displayNumber:134,sortKey:2306},{id:"kafka-2307",number:135,title:"What is `buffer.memory` and what happens when the buffer fills?",question:"What is `buffer.memory` and what happens when the buffer fills?",difficulty:"Intermediate",category:"Kafka Producers",answer:'`buffer.memory` is the total memory the producer uses to buffer unsent records waiting to be batched/sent. If the app produces faster than the broker can accept (or the network is slow), the buffer fills; then `send()` **blocks** up to `max.block.ms` waiting for space, and throws `TimeoutException` if it can\'t get any — a form of producer-side backpressure. Size it for your throughput × latency; monitor `buffer-available-bytes`.\n\n```java\np.put("buffer.memory", 64 * 1024 * 1024);  // 64MB of send buffer\np.put("max.block.ms", 5000);               // block up to 5s when buffer is full\n// Buffer full + slow broker -> send() blocks (backpressure) -> then throws.\n```',tech:"kafka",displayNumber:135,sortKey:2307},{id:"kafka-2308",number:136,title:"How does the partitioner decide which partition a record goes to?",question:"How does the partitioner decide which partition a record goes to?",difficulty:"Intermediate",category:"Kafka Producers",answer:`The **partitioner** picks the target partition. If the record has a **key**, the default partitioner hashes it (murmur2) modulo partition count → same key always maps to the same partition (preserving per-key order). If the **key is null**, modern Kafka uses a **sticky partitioner**: it fills one partition's batch, then switches, spreading load while keeping batches efficient. You can also supply an explicit partition or a custom \`Partitioner\`.

\`\`\`java
// Keyed -> deterministic partition (order preserved per key)
producer.send(new ProducerRecord<>("orders", order.getId(), json)); // hash(key)%N

// Null key -> sticky partitioner spreads records across partitions in batches
producer.send(new ProducerRecord<>("logs", null, line));

// Custom: p.put("partitioner.class", "com.acme.MyPartitioner");
\`\`\``,tech:"kafka",displayNumber:136,sortKey:2308},{id:"kafka-2309",number:137,title:"How does changing the partition count affect keyed records?",question:"How does changing the partition count affect keyed records?",difficulty:"Intermediate",category:"Kafka Producers",answer:'Keyed partitioning uses `hash(key) % partitionCount`, so **increasing the partition count changes the mapping** — a key that went to partition 2 may now go to partition 5. This breaks the "all events for key K are in one partition, in order" guarantee across the change: old events for K sit in the old partition, new ones in a different partition, so a consumer can see them out of order relative to each other. Plan partition counts ahead; if you must grow, be aware ordering-sensitive keys are affected.\n\n```text\nBefore (4 partitions): hash("A1") % 4 = 2  -> partition 2\nAfter  (6 partitions): hash("A1") % 6 = 5  -> partition 5\n=> "A1" history split across P2 (old) and P5 (new) -> per-key ordering broken.\nMitigation: size partitions up front; or use a custom partitioner stable to growth.\n```',tech:"kafka",displayNumber:137,sortKey:2309},{id:"kafka-2310",number:138,title:"What is a producer interceptor and when would you use one?",question:"What is a producer interceptor and when would you use one?",difficulty:"Intermediate",category:"Kafka Producers",answer:`A \`ProducerInterceptor\` is a client-side hook that can inspect or modify records **before they're sent** (\`onSend\`) and observe the result **after acknowledgement** (\`onAcknowledgement\`). Uses: injecting tracing/correlation headers, standardized metrics, auditing, or lightweight transformation — cross-cutting concerns applied centrally without changing business code. Keep them fast and side-effect-light (they run on the calling thread / I/O path).

\`\`\`java
public class TracingInterceptor implements ProducerInterceptor<String,String> {
    public ProducerRecord<String,String> onSend(ProducerRecord<String,String> r) {
        r.headers().add("trace-id", currentTraceId().getBytes());   // inject header
        return r;
    }
    public void onAcknowledgement(RecordMetadata m, Exception e) { /* metrics */ }
    public void configure(Map<String,?> c) {} public void close() {}
}
// p.put("interceptor.classes", "com.acme.TracingInterceptor");
\`\`\``,tech:"kafka",displayNumber:138,sortKey:2310},{id:"kafka-2311",number:139,title:"How do you serialize keys and values, and how does Avro/Schema Registry fit in?",question:"How do you serialize keys and values, and how does Avro/Schema Registry fit in?",difficulty:"Intermediate",category:"Kafka Producers",answer:'Producers convert objects to bytes via a **Serializer** (`key.serializer`, `value.serializer`). Built-ins: String, ByteArray, Integer, etc. For evolving structured events, use a schema-based serializer (Avro/Protobuf/JSON Schema) backed by a **Schema Registry**, which stores schemas and enforces compatibility — the message carries a small schema id, not the full schema, keeping payloads compact and contracts safe.\n\n```java\np.put("key.serializer", "org.apache.kafka.common.serialization.StringSerializer");\np.put("value.serializer", "io.confluent.kafka.serializers.KafkaAvroSerializer");\np.put("schema.registry.url", "http://schema-registry:8081");\n// Payload = [magic byte][schema id][avro bytes] -> compact + registry-validated.\n```',tech:"kafka",displayNumber:139,sortKey:2311},{id:"kafka-2312",number:140,title:"How do you improve producer throughput vs latency?",question:"How do you improve producer throughput vs latency?",difficulty:"Intermediate",category:"Kafka Producers",answer:"- **Throughput**: larger `batch.size`, some `linger.ms` (5–20ms), compression (lz4/zstd), enough `buffer.memory`, efficient serialization, and adequate partitions for parallelism.\n- **Latency**: small/zero `linger.ms`, smaller batches, avoid synchronous `.get()` per record, keep brokers unsaturated, and don't over-set `acks` beyond your durability need.\n\nThese pull in opposite directions — tune to your SLA. Benchmark with `kafka-producer-perf-test`.\n\n```text\nThroughput-tuned          Latency-tuned\nbatch.size high (32-64KB) batch.size small\nlinger.ms 10-20           linger.ms 0\ncompression zstd/lz4      compression off/lz4\nacks=all (durable)        acks=1 (if loss-tolerant)\nmore partitions           fewer hops, unsaturated brokers\n```",tech:"kafka",displayNumber:140,sortKey:2312},{id:"kafka-2313",number:141,title:"What is `transactional.id` and what does it enable?",question:"What is `transactional.id` and what does it enable?",difficulty:"Hard",category:"Kafka Producers",answer:`\`transactional.id\` is a **stable, unique producer identity** that enables Kafka **transactions** (atomic multi-partition/topic writes + consumed-offset commits) and **producer fencing**. When a producer with a given transactional.id starts, it "fences" any older instance with the same id (bumps the epoch), so a zombie/restarted producer can't corrupt the stream. It's required for exactly-once semantics (EOS) that span produce + offset-commit.

\`\`\`java
p.put("transactional.id", "order-processor-1");  // stable per logical producer
p.put("enable.idempotence", "true");
KafkaProducer<String,String> producer = new KafkaProducer<>(p);
producer.initTransactions();     // fences older instances with the same id
// See transactions section for beginTransaction/commit flow.
\`\`\``,tech:"kafka",displayNumber:141,sortKey:2313},{id:"kafka-2314",number:142,title:"How do you write an atomic transactional produce?",question:"How do you write an atomic transactional produce?",difficulty:"Hard",category:"Kafka Producers",answer:'With a transactional producer, wrap sends (and optionally consumed-offset commits) in `beginTransaction()` … `commitTransaction()`. All records become visible atomically to `read_committed` consumers; on error you `abortTransaction()` and nothing is exposed. This gives all-or-nothing writes across multiple partitions/topics.\n\n```java\nproducer.initTransactions();\ntry {\n    producer.beginTransaction();\n    producer.send(new ProducerRecord<>("orders", id, order));\n    producer.send(new ProducerRecord<>("audit", id, audit));   // multi-topic atomic\n    producer.commitTransaction();     // both visible together, or neither\n} catch (KafkaException e) {\n    producer.abortTransaction();      // read_committed consumers never see these\n}\n```',tech:"kafka",displayNumber:142,sortKey:2314},{id:"kafka-2315",number:143,title:"What is the difference between `flush()` and `close()` on a producer?",question:"What is the difference between `flush()` and `close()` on a producer?",difficulty:"Basic",category:"Kafka Producers",answer:"`flush()` blocks until all buffered records have been sent and acknowledged (or failed) — useful at checkpoints to ensure delivery without shutting down. `close()` flushes remaining records and then releases resources (connections, I/O thread); after close the producer is unusable. Always `close()` on shutdown (try-with-resources) to avoid losing buffered records.\n\n```java\ntry (KafkaProducer<String,String> producer = new KafkaProducer<>(p)) {\n    producer.send(record);\n    producer.flush();     // ensure sent up to this point (producer still usable)\n} // close() here: flushes remaining + frees resources\n```",tech:"kafka",displayNumber:143,sortKey:2315},{id:"kafka-2316",number:144,title:"How do producers discover which broker leads a partition?",question:"How do producers discover which broker leads a partition?",difficulty:"Intermediate",category:"Kafka Producers",answer:"On startup the producer connects to a `bootstrap.servers` broker and fetches **cluster metadata** (topics, partitions, and each partition's current leader). It caches this and sends each record directly to the **leader** of its target partition. Metadata is refreshed periodically (`metadata.max.age.ms`) and immediately on errors like `NotLeaderForPartition` (e.g. after a leader election), so the producer re-routes to the new leader automatically.\n\n```text\nproducer -> bootstrap broker -> METADATA {orders: P0 leader=B1, P1 leader=B2, ...}\nsend(record for P1) -> connect directly to B2 (P1's leader)\nLeader changes -> NotLeaderForPartition -> refresh metadata -> route to new leader.\n```",tech:"kafka",displayNumber:144,sortKey:2316},{id:"kafka-2317",number:145,title:"Exercise — Producer: configure a producer for zero data loss and no duplicates.",question:"Exercise — Producer: configure a producer for zero data loss and no duplicates.",difficulty:"Hard",category:"Kafka Producers",answer:'Combine strong acks, idempotence, and topic-side ISR requirements. `acks=all` + `min.insync.replicas=2` (RF=3) guarantees durability; `enable.idempotence=true` guarantees no duplicates from retries; a generous `delivery.timeout.ms` survives transient issues; key by the entity for ordering.\n\n```java\nProperties p = new Properties();\np.put("bootstrap.servers", "b1:9092,b2:9092,b3:9092");\np.put("acks", "all");                    // durability\np.put("enable.idempotence", "true");     // no duplicates + ordering on retry\np.put("retries", Integer.MAX_VALUE);\np.put("delivery.timeout.ms", 120000);\np.put("compression.type", "zstd");\n// Topic: replication.factor=3, min.insync.replicas=2\nproducer.send(new ProducerRecord<>("orders", order.getId(), json));  // ordered per order\n```',tech:"kafka",displayNumber:145,sortKey:2317},{id:"kafka-2318",number:146,title:"What is `client.id` and why set it?",question:"What is `client.id` and why set it?",difficulty:"Basic",category:"Kafka Producers",answer:'`client.id` is a logical name the client sends with every request. It doesn\'t affect delivery, but it\'s invaluable for **observability and control**: broker-side metrics, request logs, and quotas can be attributed per `client.id`, so you can identify which application/instance is producing load or hitting limits. Set a meaningful, stable value per service/instance.\n\n```java\np.put("client.id", "checkout-service-pod-7");\n// Shows up in broker metrics/logs and enables per-client quotas & troubleshooting.\n```',tech:"kafka",displayNumber:146,sortKey:2318},{id:"kafka-2319",number:147,title:"How do you handle producer errors (retriable vs non-retriable)?",question:"How do you handle producer errors (retriable vs non-retriable)?",difficulty:"Hard",category:"Kafka Producers",answer:'Kafka distinguishes **retriable** exceptions (transient — `NotEnoughReplicas`, `LeaderNotAvailable`, timeouts) which the client retries automatically within `delivery.timeout.ms`, from **non-retriable** ones (permanent — `RecordTooLargeException`, `SerializationException`, `AuthorizationException`) which fail immediately and must be handled by the app. In the callback, check the exception type: log/deadletter non-retriable failures; rely on built-in retries for transient ones, and alert if delivery ultimately times out.\n\n```java\nproducer.send(record, (meta, ex) -> {\n    if (ex == null) return;\n    if (ex instanceof RetriableException) {\n        // Kafka already retries these up to delivery.timeout.ms; just log/metric\n        log.warn("retriable produce error", ex);\n    } else {\n        // Permanent: too large / serialization / authz -> dead-letter or alert\n        deadLetter(record, ex);\n    }\n});\n```',tech:"kafka",displayNumber:147,sortKey:2319},{id:"kafka-2320",number:148,title:"What is KafkaConsumer and why does it use `poll()`?",question:"What is KafkaConsumer and why does it use `poll()`?",difficulty:"Basic",category:"Kafka Consumers",answer:'`KafkaConsumer` subscribes to topics and reads records. It is **not thread-safe** (one consumer per thread). It\'s built around a `poll()` loop: `poll()` fetches batches of records **and** drives group coordination — sending heartbeats, handling rebalances, and updating offsets. Not calling `poll()` regularly (long processing) makes the group think the consumer died. So `poll()` is both "get data" and "prove I\'m alive."\n\n```java\nconsumer.subscribe(List.of("orders"));\nwhile (running) {\n    ConsumerRecords<String,String> recs = consumer.poll(Duration.ofMillis(500));\n    for (ConsumerRecord<String,String> r : recs) process(r);\n    consumer.commitSync();\n}\n// poll() = fetch records + heartbeat + rebalance handling (liveness). Keep calling it.\n```',tech:"kafka",displayNumber:148,sortKey:2320},{id:"kafka-2321",number:149,title:"What is the difference between auto commit and manual commit?",question:"What is the difference between auto commit and manual commit?",difficulty:"Intermediate",category:"Kafka Consumers",answer:'- **Auto commit** (`enable.auto.commit=true`): the consumer periodically (`auto.commit.interval.ms`) commits the latest polled offsets in the background. Simple, but risky — offsets can be committed before your processing finishes, so a crash can **lose** records (they\'re marked done but weren\'t processed).\n- **Manual commit** (`false`): the app commits **after** it has successfully processed, tying offset progress to business success — the standard for at-least-once. More control, slightly more code.\n\n```java\np.put("enable.auto.commit", "false");   // control offset progress explicitly\nfor (var r : recs) process(r);          // process FIRST\nconsumer.commitSync();                   // then commit -> at-least-once\n// Auto-commit can advance offsets before processing completes -> possible loss.\n```',tech:"kafka",displayNumber:149,sortKey:2321},{id:"kafka-2322",number:150,title:"What is the difference between `commitSync()` and `commitAsync()`?",question:"What is the difference between `commitSync()` and `commitAsync()`?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`- **commitSync()**: blocks until the broker confirms the commit, retries automatically on retriable errors — safe but adds latency.
- **commitAsync()**: fires the commit and returns immediately with an optional callback — higher throughput but no automatic retry (a later commit supersedes a failed earlier one).

Common pattern: \`commitAsync()\` in the loop for speed, and a final \`commitSync()\` in a \`finally\`/on shutdown to guarantee the last offsets are durably committed.

\`\`\`java
try {
    while (running) {
        var recs = consumer.poll(Duration.ofMillis(500));
        recs.forEach(this::process);
        consumer.commitAsync();          // fast, no retry
    }
} finally {
    consumer.commitSync();               // ensure final offsets are committed
    consumer.close();
}
\`\`\``,tech:"kafka",displayNumber:150,sortKey:2322},{id:"kafka-2323",number:151,title:"What is `auto.offset.reset` (earliest / latest / none)?",question:"What is `auto.offset.reset` (earliest / latest / none)?",difficulty:"Basic",category:"Kafka Consumers",answer:`It decides where a consumer starts when there's **no valid committed offset** (new group, or the committed offset expired):
- **earliest**: start from the oldest retained record (replay all available history).
- **latest** (default): start from the end — only new records after the consumer joins.
- **none**: throw an exception, forcing the app to decide.

It only applies when no committed offset exists; otherwise the consumer resumes from its commit.

\`\`\`java
p.put("group.id", "analytics");
p.put("auto.offset.reset", "earliest");   // new group replays full history
// "latest" -> only new events; "none" -> error if no committed offset.
// Ignored once the group has a committed offset (resumes from there).
\`\`\``,tech:"kafka",displayNumber:151,sortKey:2323},{id:"kafka-2324",number:152,title:"What are the offset-commit failure scenarios (before vs after processing)?",question:"What are the offset-commit failure scenarios (before vs after processing)?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`- **Commit before processing**: if the consumer crashes after committing but before finishing, on restart it resumes past those records → they're **skipped** (at-most-once, possible data loss).
- **Commit after processing**: if it crashes after processing but before committing, on restart it **re-reads** them → possible **duplicates** (at-least-once).

There's no free lunch: choose at-least-once + idempotent processing (common), or transactions for exactly-once.

\`\`\`java
// AT-LEAST-ONCE (recommended default): process then commit -> dup-safe via idempotency
process(r); consumer.commitSync();

// AT-MOST-ONCE: commit then process -> loss on crash, never duplicates
consumer.commitSync(); process(r);
\`\`\``,tech:"kafka",displayNumber:152,sortKey:2324},{id:"kafka-2325",number:153,title:"What is consumer lag and why does it matter?",question:"What is consumer lag and why does it matter?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`Consumer lag = (latest offset in a partition) − (consumer group's committed offset) — i.e. how many records the group hasn't processed yet. It's the key health metric: steady/low lag means consumers keep up; growing lag means they're falling behind (slow processing, too few consumers, a hot partition, or downstream bottlenecks). Alert on lag trend, not just absolute value.

\`\`\`bash
kafka-consumer-groups.sh --bootstrap-server b:9092 \\
  --describe --group billing
# TOPIC  PARTITION  CURRENT-OFFSET  LOG-END-OFFSET  LAG
# orders 0          10450           10480           30   <- 30 records behind
\`\`\``,tech:"kafka",displayNumber:153,sortKey:2325},{id:"kafka-2326",number:154,title:"How do you reduce consumer lag?",question:"How do you reduce consumer lag?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`- **Add consumers** to the group — but effective parallelism is capped at the **partition count** (extra consumers idle).
- **Add partitions** if you've hit that cap (mind key-ordering effects).
- **Speed up processing**: batch downstream writes, async I/O, remove per-record network calls.
- **Tune fetching**: larger \`max.poll.records\`, \`fetch.min.bytes\` for throughput.
- **Offload heavy work** to a worker pool while keeping \`poll()\` responsive.

\`\`\`text
Lag rising -> in order of impact:
  1) more consumers (up to #partitions)
  2) faster processing (batch writes, async, dedupe network calls)
  3) more partitions (if at consumer cap)
  4) tune max.poll.records / fetch.min.bytes
  5) offload processing to worker threads (keep poll() alive)
\`\`\``,tech:"kafka",displayNumber:154,sortKey:2326},{id:"kafka-2327",number:155,title:"Explain the fetch-tuning settings: `fetch.min.bytes`, `fetch.max.wait.ms`, `max.partition.fetch.bytes`, `max.poll.records`.",question:"Explain the fetch-tuning settings: `fetch.min.bytes`, `fetch.max.wait.ms`, `max.partition.fetch.bytes`, `max.poll.records`.",difficulty:"Intermediate",category:"Kafka Consumers",answer:'- **fetch.min.bytes**: broker waits until it has at least this many bytes before responding — bigger = fewer, larger fetches (throughput) at some latency cost.\n- **fetch.max.wait.ms**: max time the broker waits to satisfy `fetch.min.bytes` before replying anyway (bounds latency).\n- **max.partition.fetch.bytes**: max data returned per partition per fetch (memory bound).\n- **max.poll.records**: max records `poll()` returns per call — controls processing batch size and helps avoid `max.poll.interval.ms` violations.\n\n```java\np.put("fetch.min.bytes", 64 * 1024);        // wait for 64KB -> fewer round-trips\np.put("fetch.max.wait.ms", 500);            // but no longer than 500ms\np.put("max.partition.fetch.bytes", 1048576);// 1MB/partition cap\np.put("max.poll.records", 500);             // process 500 per poll -> keep poll() timely\n```',tech:"kafka",displayNumber:155,sortKey:2327},{id:"kafka-2328",number:156,title:"What is a poison message and how do you handle it?",question:"What is a poison message and how do you handle it?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`A poison message is a record that **repeatedly fails** processing (bad schema, corrupt data, a bug), blocking the partition if you keep retrying it forever. Handle it with: **bounded retries** (with backoff), then route it to a **dead-letter topic (DLT)** so the partition can proceed, plus alerting and a replay/remediation path. Never infinite-retry inline — one poison record can halt an entire partition and grow lag.

\`\`\`java
try {
    process(r);
} catch (Exception e) {
    if (r.headers().lastHeader("retries") == null || retriesLeft(r) > 0) {
        sendToRetryTopic(r);            // bounded retries with delay
    } else {
        sendToDeadLetterTopic(r, e);    // give up -> DLT, alert, don't block partition
    }
}
consumer.commitSync();  // move past it so the partition keeps flowing
\`\`\``,tech:"kafka",displayNumber:156,sortKey:2328},{id:"kafka-2329",number:157,title:"What is a Dead-Letter Topic (DLT) and a replay strategy?",question:"What is a Dead-Letter Topic (DLT) and a replay strategy?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`A DLT is a dedicated topic where records that can't be processed after retries are parked for investigation, so the main flow isn't blocked. Include failure metadata (original topic/partition/offset, exception, timestamp) in headers. **Replay**: diagnose and fix the root cause (bug/data), validate the record, then republish selected records back to the original (or a controlled recovery) topic — with safeguards (dedup, rate limiting) so replay doesn't cause new problems.

\`\`\`java
// Producing to DLT with context
ProducerRecord<String,String> dlt = new ProducerRecord<>("orders.DLT", r.key(), r.value());
dlt.headers().add("orig-topic", r.topic().getBytes());
dlt.headers().add("orig-offset", Long.toString(r.offset()).getBytes());
dlt.headers().add("error", e.getMessage().getBytes());
producer.send(dlt);
// Replay: fix cause -> validate -> republish to source topic with dedup safeguards.
\`\`\``,tech:"kafka",displayNumber:157,sortKey:2329},{id:"kafka-2330",number:158,title:"How do you commit specific offsets manually (per partition)?",question:"How do you commit specific offsets manually (per partition)?",difficulty:"Hard",category:"Kafka Consumers",answer:`\`commitSync(Map<TopicPartition, OffsetAndMetadata>)\` lets you commit exact offsets rather than "everything polled." You commit **next offset to read** = last processed offset + 1. This is useful for fine-grained control (e.g. commit per partition as each finishes, or commit mid-batch after a checkpoint).

\`\`\`java
Map<TopicPartition, OffsetAndMetadata> offsets = new HashMap<>();
for (ConsumerRecord<String,String> r : recs) {
    process(r);
    offsets.put(new TopicPartition(r.topic(), r.partition()),
                new OffsetAndMetadata(r.offset() + 1));   // commit NEXT offset
}
consumer.commitSync(offsets);   // precise, per-partition commit
\`\`\``,tech:"kafka",displayNumber:158,sortKey:2330},{id:"kafka-2331",number:159,title:"How do you seek to a specific offset or timestamp (replay)?",question:"How do you seek to a specific offset or timestamp (replay)?",difficulty:"Hard",category:"Kafka Consumers",answer:'Use `seek()` to reposition a consumer: `seekToBeginning()`/`seekToEnd()` for extremes, `seek(partition, offset)` for an exact offset, or `offsetsForTimes()` to translate a timestamp into offsets and seek there — enabling time-based replay ("reprocess everything since 2am"). Seeking must happen after partitions are assigned (after a `poll()` or in a rebalance callback).\n\n```java\n// Replay from a point in time\nlong since = Instant.now().minus(Duration.ofHours(2)).toEpochMilli();\nvar tp = new TopicPartition("orders", 0);\nMap<TopicPartition,Long> query = Map.of(tp, since);\nOffsetAndTimestamp ot = consumer.offsetsForTimes(query).get(tp);\nif (ot != null) consumer.seek(tp, ot.offset());   // reprocess from 2h ago\n```',tech:"kafka",displayNumber:159,sortKey:2331},{id:"kafka-2332",number:160,title:"What is the difference between `subscribe()` and `assign()`?",question:"What is the difference between `subscribe()` and `assign()`?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`- **subscribe(topics)**: joins a **consumer group**; Kafka dynamically assigns partitions and rebalances as members change. Use for scalable, auto-balanced consumption.
- **assign(partitions)**: **manually** pins specific partitions to this consumer — no group management, no rebalancing, no automatic offset ownership by group coordination. Use for precise control (e.g. reading one partition, custom frameworks). You can't mix them on one consumer.

\`\`\`java
// Group-managed, auto-balanced:
consumer.subscribe(List.of("orders"));

// Manual, fixed assignment (no rebalancing):
consumer.assign(List.of(new TopicPartition("orders", 3)));
\`\`\``,tech:"kafka",displayNumber:160,sortKey:2332},{id:"kafka-2333",number:161,title:"Can one consumer read multiple partitions? Can one partition go to multiple consumers in a group?",question:"Can one consumer read multiple partitions? Can one partition go to multiple consumers in a group?",difficulty:"Basic",category:"Kafka Consumers",answer:'Yes — a single consumer can be assigned **many** partitions (it round-robins across them in `poll()`). But within **one consumer group**, a partition is owned by exactly **one** consumer at a time — never two simultaneously — which preserves per-partition ordering and avoids double processing. Across different groups, the same partition is read independently by each group.\n\n```text\nGroup "billing", topic with P0-P3, 2 consumers:\n  C1 <- P0, P1     C2 <- P2, P3      (one consumer, many partitions: OK)\nNever: P0 -> C1 AND C2 in the same group (one owner per partition per group).\n```',tech:"kafka",displayNumber:161,sortKey:2333},{id:"kafka-2334",number:162,title:"How do you pause and resume partitions for backpressure?",question:"How do you pause and resume partitions for backpressure?",difficulty:"Hard",category:"Kafka Consumers",answer:"`consumer.pause(partitions)` stops fetching records for those partitions while still calling `poll()` (so heartbeats continue and the consumer isn't kicked from the group); `resume()` re-enables them. This is the correct way to apply **backpressure** when a downstream is overwhelmed — you keep the consumer alive without pulling more data than you can handle, avoiding `max.poll.interval.ms` violations.\n\n```java\nif (downstreamQueue.isFull()) {\n    consumer.pause(consumer.assignment());   // stop fetching, keep heartbeating\n}\nvar recs = consumer.poll(Duration.ofMillis(200));  // still poll -> stays in group\nif (downstreamQueue.hasCapacity()) {\n    consumer.resume(consumer.assignment());  // start fetching again\n}\n```",tech:"kafka",displayNumber:162,sortKey:2334},{id:"kafka-2335",number:163,title:"What is `read_committed` vs `read_uncommitted` isolation level?",question:"What is `read_committed` vs `read_uncommitted` isolation level?",difficulty:"Hard",category:"Kafka Consumers",answer:`For consumers reading topics written by **transactional** producers:
- **read_uncommitted** (default): returns all records, including those from open or **aborted** transactions.
- **read_committed**: returns only records from **committed** transactions; it buffers and hides aborted/in-flight transactional records, and won't advance past the last stable offset. Required for exactly-once reads.

\`\`\`java
p.put("isolation.level", "read_committed");
// Consumer sees ONLY committed transactional records; aborted ones are invisible.
// Needed downstream of transactional/EOS producers.
\`\`\``,tech:"kafka",displayNumber:163,sortKey:2335},{id:"kafka-2336",number:164,title:"What happens when a consumer crashes before committing?",question:"What happens when a consumer crashes before committing?",difficulty:"Basic",category:"Kafka Consumers",answer:"The group coordinator detects the failure (missed heartbeats / `session.timeout.ms` or a `max.poll.interval.ms` violation), triggers a **rebalance**, and reassigns the dead consumer's partitions to surviving members. Those consumers **resume from the last committed offset**, so any records processed-but-not-committed are re-read (duplicates possible → at-least-once). No committed progress is lost.\n\n```text\nConsumer crashes -> misses heartbeats -> coordinator times it out -> REBALANCE\n-> its partitions reassigned -> peers resume from last COMMITTED offset\n-> uncommitted-but-processed records are reprocessed (idempotency handles dupes).\n```",tech:"kafka",displayNumber:164,sortKey:2336},{id:"kafka-2337",number:165,title:"How do you gracefully shut down a consumer?",question:"How do you gracefully shut down a consumer?",difficulty:"Intermediate",category:"Kafka Consumers",answer:`Signal the poll loop to stop (a flag), call \`consumer.wakeup()\` from another thread to interrupt a blocking \`poll()\` (throws \`WakeupException\`), then commit final offsets and \`close()\` (which also leaves the group promptly, triggering a faster rebalance than a timeout). Graceful shutdown avoids reprocessing and reduces rebalance disruption.

\`\`\`java
// Shutdown hook / other thread:
running = false;
consumer.wakeup();                      // interrupts a blocked poll()

// In the loop:
try {
    while (running) { var r = consumer.poll(Duration.ofMillis(500)); /* ... */ }
} catch (WakeupException ignore) {      // expected on shutdown
} finally {
    consumer.commitSync();              // final offsets
    consumer.close();                   // leaves group -> fast rebalance
}
\`\`\``,tech:"kafka",displayNumber:165,sortKey:2337},{id:"kafka-2338",number:166,title:"What is the difference between committed offset, current position, and high watermark?",question:"What is the difference between committed offset, current position, and high watermark?",difficulty:"Hard",category:"Kafka Consumers",answer:`- **Position**: the offset of the next record this consumer will fetch (advances as you poll).
- **Committed offset**: the last offset durably saved for the group in \`__consumer_offsets\` — the resume point after a crash/rebalance.
- **High watermark**: the highest offset replicated to enough ISRs; consumers can't read beyond it (in read_committed, the last stable offset applies).

Position ≥ committed (you may have polled ahead of what you committed); high watermark bounds what's readable.

\`\`\`text
Partition log: [ ... committed=100 ... position=150 ... highWatermark=180 ... LEO=185 ]
committed(100)  : resume point after restart (group offset)
position(150)   : next fetch for this running consumer
highWatermark(180): max readable (replicated to ISR)
LEO(185)        : leader's log end (not yet fully replicated)
\`\`\``,tech:"kafka",displayNumber:166,sortKey:2338},{id:"kafka-2339",number:167,title:"How do you process records concurrently while keeping offsets correct?",question:"How do you process records concurrently while keeping offsets correct?",difficulty:"Hard",category:"Kafka Consumers",answer:`Since \`KafkaConsumer\` is single-threaded, to parallelize heavy processing you hand records to a worker pool — but then you must only commit an offset once **all records up to it** are done, or you risk committing past unfinished work (loss on crash). Track completion per partition and commit the highest **contiguous** completed offset. Pause partitions if workers fall behind. Frameworks like Spring Kafka's concurrent listener or a per-key executor simplify this.

\`\`\`java
// Commit only the highest CONTIGUOUS completed offset per partition
var completed = new ConcurrentSkipListSet<Long>();
for (var r : recs) pool.submit(() -> { process(r); completed.add(r.offset()); });
// after draining/checkpoint: find max contiguous offset from base, commit offset+1
long commitUpTo = highestContiguous(completed, baseOffset);
consumer.commitSync(Map.of(tp, new OffsetAndMetadata(commitUpTo + 1)));
// Pause the partition if the worker queue backs up (backpressure).
\`\`\``,tech:"kafka",displayNumber:167,sortKey:2339},{id:"kafka-2340",number:168,title:"What is `group.instance.id` (static membership) and why use it?",question:"What is `group.instance.id` (static membership) and why use it?",difficulty:"Hard",category:"Kafka Consumers",answer:'Setting a stable `group.instance.id` makes a consumer a **static member**. When it restarts (e.g. a rolling deploy) within `session.timeout.ms`, the coordinator recognizes it and **skips the rebalance**, giving back the same partitions — avoiding the churn (and lag spikes) that dynamic membership causes on every restart. Ideal for stateful consumers and frequent deployments.\n\n```java\np.put("group.instance.id", "consumer-pod-3");   // stable identity -> static member\np.put("session.timeout.ms", "45000");\n// Restart within the session -> no rebalance, same partitions reassigned.\n```',tech:"kafka",displayNumber:168,sortKey:2340},{id:"kafka-2341",number:169,title:"How do you consume from multiple topics with one consumer?",question:"How do you consume from multiple topics with one consumer?",difficulty:"Basic",category:"Kafka Consumers",answer:'Subscribe to a list of topics or a regex pattern; the group coordinator assigns partitions across all subscribed topics among the group\'s members. One `poll()` returns records from any of them — inspect `record.topic()` to route. Ordering remains per-partition only, never across topics.\n\n```java\nconsumer.subscribe(List.of("orders", "payments"));          // explicit list\n// or by pattern:\nconsumer.subscribe(Pattern.compile("events\\\\..*"));         // all events.* topics\n\nfor (var r : consumer.poll(Duration.ofMillis(500)))\n    switch (r.topic()) { case "orders" -> onOrder(r); case "payments" -> onPay(r); }\n```',tech:"kafka",displayNumber:169,sortKey:2341},{id:"kafka-2342",number:170,title:"Exercise — Consumer: build a robust at-least-once consumer with retries and DLT.",question:"Exercise — Consumer: build a robust at-least-once consumer with retries and DLT.",difficulty:"Hard",category:"Kafka Consumers",answer:`Disable auto-commit, process then commit (at-least-once), make processing idempotent (dedupe by event id), bound retries with backoff, route permanent failures to a DLT so the partition isn't blocked, and commit after handling each batch. Use \`wakeup()\` for graceful shutdown.

\`\`\`java
p.put("enable.auto.commit", "false");
consumer.subscribe(List.of("orders"));
while (running) {
    var recs = consumer.poll(Duration.ofMillis(500));
    for (var r : recs) {
        try {
            if (dedupe.add(eventId(r))) process(r);      // idempotent
        } catch (Exception e) {
            if (retriesLeft(r) > 0) sendToRetryTopic(r); // bounded backoff retries
            else                    sendToDlt(r, e);     // permanent -> DLT, don't block
        }
    }
    consumer.commitSync();                                // commit after the batch
}
\`\`\``,tech:"kafka",displayNumber:170,sortKey:2342},{id:"kafka-2343",number:171,title:"What is a rebalance and why does it happen?",question:"What is a rebalance and why does it happen?",difficulty:"Basic",category:"Rebalancing",answer:"A rebalance is the redistribution of a topic's partitions among the consumers in a group. It's triggered when group membership or metadata changes: a consumer **joins** or **leaves**, a consumer **fails** (missed heartbeats / poll timeout), the **subscription** changes, or the **partition count** changes. During a rebalance, partition ownership is recalculated so every partition has exactly one owner.\n\n```text\nTriggers: consumer join/leave, consumer crash (timeout), subscription change,\n          partition count change.\nEffect:   partitions reassigned among current members (one owner each).\n```",tech:"kafka",displayNumber:171,sortKey:2343},{id:"kafka-2344",number:172,title:"What happens during a rebalance and why can it hurt?",question:"What happens during a rebalance and why can it hurt?",difficulty:"Intermediate",category:"Rebalancing",answer:`In the traditional **eager** protocol, all consumers **revoke all** their partitions, then the coordinator computes new assignments and hands them out — a "stop-the-world" pause where **no processing happens** and lag spikes. In-flight work may be reprocessed if offsets weren't committed before revocation. Frequent or slow rebalances are a top cause of latency and duplicate processing, which is why cooperative rebalancing and static membership exist to minimize them.

\`\`\`text
Eager rebalance timeline:
  all consumers revoke ALL partitions  -> processing STOPS (lag rises)
  coordinator computes assignment      -> hands out new partitions
  consumers resume from committed offsets (uncommitted work reprocessed)
Minimize with: commit-on-revoke, cooperative assignor, static membership.
\`\`\``,tech:"kafka",displayNumber:172,sortKey:2344},{id:"kafka-2345",number:173,title:"What is the group coordinator?",question:"What is the group coordinator?",difficulty:"Intermediate",category:"Rebalancing",answer:"The group coordinator is a **broker** that manages a consumer group's membership and committed offsets. It's the broker that owns the `__consumer_offsets` partition for that group (chosen by hashing the group id). It receives heartbeats, detects failures, drives the rebalance protocol (assigning a group leader consumer to compute the assignment), and stores committed offsets. Each group has exactly one coordinator at a time.\n\n```text\ngroup.id -> hash -> __consumer_offsets partition -> that partition's leader broker\n           = the group COORDINATOR\nCoordinator duties: membership, heartbeats, failure detection, offset commits,\n                    orchestrating rebalances.\n```",tech:"kafka",displayNumber:173,sortKey:2345},{id:"kafka-2346",number:174,title:"Compare RangeAssignor, RoundRobinAssignor, StickyAssignor, and CooperativeStickyAssignor.",question:"Compare RangeAssignor, RoundRobinAssignor, StickyAssignor, and CooperativeStickyAssignor.",difficulty:"Hard",category:"Rebalancing",answer:`- **RangeAssignor** (default legacy): assigns contiguous ranges per topic — can imbalance when partition counts differ across topics.
- **RoundRobinAssignor**: distributes all partitions round-robin across consumers — more even, but reshuffles a lot on change.
- **StickyAssignor**: balances **and** tries to keep prior assignments stable across rebalances (less movement).
- **CooperativeStickyAssignor** (recommended): sticky **and** cooperative — revokes only the partitions that must move, without stopping everything (incremental rebalancing).

\`\`\`java
p.put("partition.assignment.strategy",
      "org.apache.kafka.clients.consumer.CooperativeStickyAssignor");
// Range: per-topic ranges (simple, can imbalance)
// RoundRobin: even spread, more churn
// Sticky: minimal movement
// CooperativeSticky: minimal movement + no stop-the-world (best default)
\`\`\``,tech:"kafka",displayNumber:174,sortKey:2346},{id:"kafka-2347",number:175,title:"What is the difference between eager and cooperative (incremental) rebalancing?",question:"What is the difference between eager and cooperative (incremental) rebalancing?",difficulty:"Hard",category:"Rebalancing",answer:`- **Eager**: every consumer revokes **all** partitions at the start of the rebalance, then gets new ones — full stop-the-world pause.
- **Cooperative (incremental)**: only the partitions that actually need to move are revoked; consumers keep processing the partitions they retain. It takes an extra rebalance round but avoids the global pause, drastically reducing disruption during scaling and rolling deploys.

Cooperative is enabled via \`CooperativeStickyAssignor\`.

\`\`\`text
Eager:       revoke ALL -> reassign ALL           (everyone pauses)
Cooperative: revoke only moving partitions        (retained partitions keep flowing)
             -> shorter effective pause, smoother scaling/deploys
\`\`\``,tech:"kafka",displayNumber:175,sortKey:2347},{id:"kafka-2348",number:176,title:"What is static membership and how does it reduce rebalances?",question:"What is static membership and how does it reduce rebalances?",difficulty:"Hard",category:"Rebalancing",answer:'By assigning a stable `group.instance.id`, a consumer becomes a **static member**. On restart within `session.timeout.ms` (e.g. a rolling deployment or a brief crash), the coordinator recognizes the returning member and **reassigns its previous partitions without a rebalance**. This eliminates the two rebalances (leave + rejoin) that dynamic members trigger on every restart — critical for stateful consumers and frequent deploys.\n\n```java\np.put("group.instance.id", "worker-3");   // static identity\np.put("session.timeout.ms", "45000");     // grace window for restart\n// Restart within 45s -> coordinator skips rebalance, returns same partitions.\n```',tech:"kafka",displayNumber:176,sortKey:2348},{id:"kafka-2349",number:177,title:"What are the rebalance listener callbacks (onPartitionsRevoked / Assigned / Lost)?",question:"What are the rebalance listener callbacks (onPartitionsRevoked / Assigned / Lost)?",difficulty:"Intermediate",category:"Rebalancing",answer:`A \`ConsumerRebalanceListener\` lets you react around ownership changes:
- **onPartitionsRevoked**: called **before** losing partitions — commit offsets and flush state here so nothing is reprocessed/lost.
- **onPartitionsAssigned**: called after getting partitions — initialize state, seek to custom offsets.
- **onPartitionsLost**: ownership lost **without** a clean revocation opportunity (e.g. fell out of the group) — clean up without assuming you can commit.

\`\`\`java
consumer.subscribe(List.of("orders"), new ConsumerRebalanceListener() {
    public void onPartitionsRevoked(Collection<TopicPartition> p) {
        consumer.commitSync();          // commit BEFORE giving up partitions
    }
    public void onPartitionsAssigned(Collection<TopicPartition> p) {
        // init state / seek to custom offsets
    }
    public void onPartitionsLost(Collection<TopicPartition> p) {
        cleanupWithoutCommit();         // lost ungracefully
    }
});
\`\`\``,tech:"kafka",displayNumber:177,sortKey:2349},{id:"kafka-2350",number:178,title:"Why can long processing cause rebalances, and how do you handle it?",question:"Why can long processing cause rebalances, and how do you handle it?",difficulty:"Intermediate",category:"Rebalancing",answer:'If your processing between `poll()` calls exceeds `max.poll.interval.ms`, the coordinator assumes the consumer is dead and rebalances its partitions away — then your commit fails and work is reprocessed. Fixes: lower `max.poll.records` (smaller batches), raise `max.poll.interval.ms` if work is legitimately long, or offload processing to worker threads while continuing to `poll()` (and pause partitions for backpressure).\n\n```java\np.put("max.poll.records", 100);          // smaller batches -> poll() returns sooner\np.put("max.poll.interval.ms", 300000);   // allow longer processing if needed\n// Or: hand records to a worker pool, keep calling poll(), pause/resume for backpressure.\n```',tech:"kafka",displayNumber:178,sortKey:2350},{id:"kafka-2351",number:179,title:"What is a rebalance storm and how do you diagnose it?",question:"What is a rebalance storm and how do you diagnose it?",difficulty:"Hard",category:"Rebalancing",answer:'A rebalance storm is repeated, rapid rebalances that stall the group — usually caused by unstable consumers (crashing/restarting), `max.poll.interval.ms`/`session.timeout.ms` violations from slow processing or GC pauses, aggressive autoscaling, or misconfiguration. Diagnose via consumer/coordinator logs (frequent "Revoking/Rejoining"), group generation churning, poll-interval violations, and deployment timing. Fix with static membership, cooperative assignor, tuned timeouts, and stable processing.\n\n```text\nSymptoms: constant "(Re-)joining group", generation number climbing, lag oscillating.\nCauses:   crash loops, GC/processing > max.poll.interval.ms, flapping autoscaling.\nFixes:    static membership, CooperativeStickyAssignor, raise timeouts, fix GC/processing,\n          stabilize deployments (avoid rapid pod churn).\n```',tech:"kafka",displayNumber:179,sortKey:2351},{id:"kafka-2352",number:180,title:"What is group generation and how does it prevent stale commits?",question:"What is group generation and how does it prevent stale commits?",difficulty:"Hard",category:"Rebalancing",answer:"Each successful rebalance increments the group's **generation** (a version number). Consumers carry their generation when committing offsets; the coordinator **rejects commits from an outdated generation** (`CommitFailedException`), preventing a slow/zombie consumer that already lost its partitions from overwriting the current owner's offsets. This is a key correctness guard during rebalances.\n\n```text\nRebalance -> generation 5 -> 6\nZombie consumer (still on gen 5) tries commitSync() -> REJECTED (CommitFailedException)\n=> stale member can't clobber the new owner's committed offsets.\n```",tech:"kafka",displayNumber:180,sortKey:2352},{id:"kafka-2353",number:181,title:"Can two groups have different offsets for the same partition?",question:"Can two groups have different offsets for the same partition?",difficulty:"Basic",category:"Rebalancing",answer:`Yes — offsets are tracked **per consumer group per partition**, independently. Group A can be at offset 100 while group B is at offset 500 on the same partition, each replaying/consuming at its own pace. This is what enables multiple independent applications (billing, analytics, search indexing) to consume the same topic without interfering.

\`\`\`text
Partition orders-0:
  group "billing"   committed offset = 100
  group "analytics" committed offset = 500   (independent)
Same data, separate progress -> fan-out to many apps.
\`\`\``,tech:"kafka",displayNumber:181,sortKey:2353},{id:"kafka-2354",number:182,title:"How do you scale consumers and what limits parallelism?",question:"How do you scale consumers and what limits parallelism?",difficulty:"Intermediate",category:"Rebalancing",answer:`Scale by adding consumers to the group; Kafka rebalances partitions across them. But **effective parallelism is capped by the partition count** — with 6 partitions, a 7th consumer sits idle. So plan partition count for peak parallelism up front. Beyond that, options are: add partitions (mind key ordering), or parallelize within a consumer via worker threads while preserving offset correctness.

\`\`\`text
Topic with 6 partitions:
  up to 6 active consumers (1 partition each)
  7th+ consumer = IDLE (no partition to own)
=> max group parallelism = #partitions. Size partitions for peak scale.
\`\`\``,tech:"kafka",displayNumber:182,sortKey:2354},{id:"kafka-2355",number:183,title:"What is the difference between `session.timeout.ms` and `max.poll.interval.ms`?",question:"What is the difference between `session.timeout.ms` and `max.poll.interval.ms`?",difficulty:"Hard",category:"Rebalancing",answer:"- **session.timeout.ms**: if the coordinator receives no **heartbeat** within this window, the consumer is considered dead. Heartbeats are sent by a background thread (`heartbeat.interval.ms`), independent of processing.\n- **max.poll.interval.ms**: the max time allowed **between `poll()` calls**; exceeding it means processing is stuck, so the consumer leaves the group even if heartbeats were fine.\n\nSession timeout detects a crashed **process**; poll interval detects **stuck processing**.\n\n```text\nsession.timeout.ms   -> no HEARTBEAT (process/network dead) -> removed\nmax.poll.interval.ms -> no POLL (processing too slow/hung)  -> removed\nheartbeat runs in background; poll cadence reflects processing speed.\n```",tech:"kafka",displayNumber:183,sortKey:2355},{id:"kafka-2356",number:184,title:"How do you monitor a consumer group's health?",question:"How do you monitor a consumer group's health?",difficulty:"Intermediate",category:"Rebalancing",answer:'Watch: **consumer lag** per partition (are we keeping up?), **rebalance frequency/duration** (stability), **partition assignment balance** (hot consumers?), **commit failures** (`CommitFailedException`), and **liveness** (heartbeats, poll interval). Use `kafka-consumer-groups.sh --describe`, JMX metrics, or Burrow/Kafka-lag-exporter feeding Prometheus/Grafana with alerts on lag trend and rebalance rate.\n\n```bash\nkafka-consumer-groups.sh --bootstrap-server b:9092 --describe --group billing\n# Watch: LAG per partition, CONSUMER-ID assignment, whether group state is "Stable"\n# Alert on: rising lag, frequent rebalances, CommitFailedException rate.\n```',tech:"kafka",displayNumber:184,sortKey:2356},{id:"kafka-2357",number:185,title:"Can a single process consume for multiple groups?",question:"Can a single process consume for multiple groups?",difficulty:"Intermediate",category:"Rebalancing",answer:'A single `KafkaConsumer` instance belongs to exactly **one** group (its `group.id`). But one **process** can create multiple `KafkaConsumer` instances, each with a different `group.id`, to participate in several groups simultaneously (each on its own thread, since consumers aren\'t thread-safe). This is common when one service needs independent offset tracks for different purposes.\n\n```java\n// Two independent consumers in one process, different groups:\nvar c1 = new KafkaConsumer<>(propsWithGroup("billing"));    // own offsets\nvar c2 = new KafkaConsumer<>(propsWithGroup("audit"));      // own offsets\n// Run each in its own thread (KafkaConsumer is not thread-safe).\n```',tech:"kafka",displayNumber:185,sortKey:2357},{id:"kafka-2358",number:186,title:"What is partition assignment fairness and how do hot consumers arise?",question:"What is partition assignment fairness and how do hot consumers arise?",difficulty:"Hard",category:"Rebalancing",answer:`The assignor spreads partition **count** evenly, but not partition **load** — if some partitions carry far more traffic (a hot key skews data into one partition), the consumer owning it becomes a hot consumer while others idle. The fix is upstream: better keying/partitioning to spread load, or increasing partitions, since the assignor can't rebalance by throughput. RoundRobin/CooperativeSticky help with count fairness, not data skew.

\`\`\`text
Even partition COUNT ≠ even LOAD.
Hot key -> one partition gets most records -> its consumer is overloaded.
Fix at the source: improve key cardinality/partitioning; add partitions;
                   split/salt hot keys. Assignor can't fix data skew.
\`\`\``,tech:"kafka",displayNumber:186,sortKey:2358},{id:"kafka-2359",number:187,title:"Exercise — Rebalancing: configure a group for smooth rolling deployments.",question:"Exercise — Rebalancing: configure a group for smooth rolling deployments.",difficulty:"Hard",category:"Rebalancing",answer:`Combine **static membership** (skip rebalance on restart), the **cooperative sticky assignor** (incremental, no stop-the-world), sensible timeouts, and **commit-on-revoke** in a rebalance listener. Together these make rolling restarts nearly rebalance-free and prevent reprocessing.

\`\`\`java
p.put("group.instance.id", System.getenv("POD_NAME"));   // static member
p.put("partition.assignment.strategy",
      "org.apache.kafka.clients.consumer.CooperativeStickyAssignor");
p.put("session.timeout.ms", "45000");
p.put("max.poll.interval.ms", "300000");
consumer.subscribe(List.of("orders"), new ConsumerRebalanceListener() {
    public void onPartitionsRevoked(Collection<TopicPartition> tp){ consumer.commitSync(); }
    public void onPartitionsAssigned(Collection<TopicPartition> tp){}
});
// Rolling restart within session timeout -> minimal/no rebalance, no reprocessing.
\`\`\``,tech:"kafka",displayNumber:187,sortKey:2359},{id:"kafka-2360",number:188,title:"What is leader election and when does it happen?",question:"What is leader election and when does it happen?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"Leader election chooses which replica becomes the **leader** of a partition when the current leader becomes unavailable (broker failure, restart, or maintenance). The controller picks an eligible replica — normally one that's **in-sync (ISR)** so no committed data is lost — and updates metadata; producers/consumers then refresh and talk to the new leader. Elections also happen for load balancing (preferred-leader election).\n\n```text\nLeader broker fails -> controller detects -> picks an ISR replica as new leader\n-> metadata updated -> clients refresh -> traffic resumes on new leader.\n```",tech:"kafka",displayNumber:188,sortKey:2360},{id:"kafka-2361",number:189,title:"What is a preferred leader and preferred-leader election?",question:"What is a preferred leader and preferred-leader election?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"The **preferred leader** is the **first replica** listed in a partition's replica assignment. Kafka tries to keep it as the leader so leadership is spread evenly across brokers. After failures/restarts, leadership can drift and concentrate on a few brokers; **preferred-leader election** (auto or via `kafka-leader-election.sh`) restores the balanced distribution, avoiding hot brokers.\n\n```bash\n# Rebalance leadership back to preferred replicas\nkafka-leader-election.sh --bootstrap-server b:9092 \\\n  --election-type PREFERRED --all-topic-partitions\n# auto.leader.rebalance.enable=true does this periodically.\n```",tech:"kafka",displayNumber:189,sortKey:2361},{id:"kafka-2362",number:190,title:"What is unclean leader election and why is it dangerous?",question:"What is unclean leader election and why is it dangerous?",difficulty:"Hard",category:"Replication & Reliability",answer:"Unclean leader election allows an **out-of-sync** replica to become leader when no in-sync replica is available. It keeps the partition **available**, but the new leader lacks the latest committed records that were only on the failed leader → **data loss** and log divergence. It's an availability-vs-durability trade-off; most durable setups keep `unclean.leader.election.enable=false` and accept temporary unavailability rather than silent loss.\n\n```text\nunclean.leader.election.enable=false (durable default):\n  no ISR available -> partition stays OFFLINE until an ISR returns (no loss)\n=true (availability-first):\n  out-of-sync replica becomes leader -> partition available BUT data loss possible.\n```",tech:"kafka",displayNumber:190,sortKey:2362},{id:"kafka-2363",number:191,title:"What is the high watermark and why can't consumers read past it?",question:"What is the high watermark and why can't consumers read past it?",difficulty:"Hard",category:"Replication & Reliability",answer:`The **high watermark (HW)** is the highest offset that has been replicated to all required in-sync replicas — i.e. the boundary of **committed** data. Normal consumers can only read **up to** the HW, because records beyond it exist only on the leader and aren't yet durably replicated; exposing them would risk showing data that could be lost in a leader failover. The **Log End Offset (LEO)** is the leader's actual end (≥ HW).

\`\`\`text
Leader log:  [........ HW=180 ..... LEO=185 ]
              committed(<=180) : readable by consumers (replicated to ISR)
              181..185         : on leader only, NOT yet readable (unreplicated)
HW advances as followers catch up. read_committed also respects the LSO.
\`\`\``,tech:"kafka",displayNumber:191,sortKey:2363},{id:"kafka-2364",number:192,title:"What is the leader epoch and what problem does it solve?",question:"What is the leader epoch and what problem does it solve?",difficulty:"Hard",category:"Replication & Reliability",answer:`A **leader epoch** is a monotonically increasing number identifying each leadership term of a partition. It's stamped on the log so replicas and consumers can detect **stale leaders** and reconcile logs correctly after failovers. Before leader epochs, a follower recovering after a leader change could truncate/keep the wrong records (data loss/divergence); epochs let a replica ask "what was the log end at epoch N?" and truncate precisely, ensuring consistent recovery.

\`\`\`text
Each leadership term -> new epoch (0,1,2,...). Records carry the epoch.
On failover, a follower uses OffsetsForLeaderEpoch to find the exact divergence point
-> truncates only what's invalid -> no over/under truncation -> consistent logs.
\`\`\``,tech:"kafka",displayNumber:192,sortKey:2364},{id:"kafka-2365",number:193,title:"What is log truncation and when does it occur?",question:"What is log truncation and when does it occur?",difficulty:"Hard",category:"Replication & Reliability",answer:"Log truncation removes records from a **replica** when its log diverges from the current leader's valid history — typically after a leader change where a former leader had unreplicated records that the new leader never received. Using leader epochs, the replica finds the divergence offset and truncates everything after it, then re-fetches the correct records from the new leader. This keeps all replicas consistent (at the cost of dropping never-committed records).\n\n```text\nOld leader had offsets up to 185 (HW was 180). It fails; new leader ends at 180.\nOld broker returns as follower -> detects divergence at 181 via leader epoch\n-> truncates 181..185 -> re-replicates from the new leader. Consistent again.\n```",tech:"kafka",displayNumber:193,sortKey:2365},{id:"kafka-2366",number:194,title:"What causes replicas to fall out of the ISR?",question:"What causes replicas to fall out of the ISR?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"A follower leaves the ISR when it can't keep up with the leader within `replica.lag.time.max.ms` — usually due to **slow disks**, **network issues**, an **overloaded broker** (CPU/GC), or insufficient replica-fetch throughput. Shrinking ISR reduces fault tolerance and can block acks=all writes if it drops below `min.insync.replicas`. Monitor `UnderReplicatedPartitions` and `IsrShrinksPerSec`.\n\n```text\nFollower can't catch up within replica.lag.time.max.ms -> removed from ISR.\nCommon causes: slow/failing disk, network saturation, broker CPU/GC, fetch backlog.\nImpact: less fault tolerance; acks=all may fail if ISR < min.insync.replicas.\n```",tech:"kafka",displayNumber:194,sortKey:2366},{id:"kafka-2367",number:195,title:"What is an under-replicated partition and why does it matter?",question:"What is an under-replicated partition and why does it matter?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"An under-replicated partition has fewer in-sync replicas than its replication factor — some replicas are lagging or down. It signals **degraded fault tolerance** (you're closer to data loss/unavailability) and often precedes bigger problems. `UnderReplicatedPartitions > 0` is a standard alerting metric; investigate broker health, disk, and network. It's not immediate data loss, but a warning that redundancy is compromised.\n\n```bash\nkafka-topics.sh --bootstrap-server b:9092 --describe --under-replicated-partitions\n# Any output = replicas lagging/down -> reduced redundancy. Alert on this metric.\n```",tech:"kafka",displayNumber:195,sortKey:2367},{id:"kafka-2368",number:196,title:"What is an offline partition and why is it serious?",question:"What is an offline partition and why is it serious?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"An offline partition has **no available leader** — all its replicas are down or none is eligible. Clients can't produce to or consume from it, so that slice of the topic is completely unavailable (and with `unclean.leader.election.enable=false`, it stays offline until an ISR returns). `OfflinePartitionsCount > 0` is a critical alert indicating data unavailability, usually from multiple broker failures or disk loss.\n\n```text\nOfflinePartitionsCount > 0  =>  partition has NO leader  =>  no reads/writes.\nCauses: all replicas down, or no ISR eligible with unclean election disabled.\nCritical alert -> restore brokers / replicas to bring the partition back online.\n```",tech:"kafka",displayNumber:196,sortKey:2368},{id:"kafka-2369",number:197,title:"What is rack awareness and why use it?",question:"What is rack awareness and why use it?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"Rack awareness (`broker.rack`) makes Kafka place a partition's replicas across **different failure domains** (racks / availability zones) rather than all in one. So a single rack/AZ outage can't take down all replicas of any partition — the partition stays available via replicas in surviving zones. Essential for cloud multi-AZ durability. Consumers can also fetch from a same-rack replica to save cross-AZ bandwidth (follower fetching).\n\n```properties\n# broker config\nbroker.rack=us-east-1a\n# Kafka spreads RF=3 replicas across a,b,c zones -> one AZ down != data loss.\n# Consumers can use rack-aware follower fetching to reduce cross-AZ traffic.\n```",tech:"kafka",displayNumber:197,sortKey:2369},{id:"kafka-2370",number:198,title:"What is the difference between replication factor and min.insync.replicas?",question:"What is the difference between replication factor and min.insync.replicas?",difficulty:"Intermediate",category:"Replication & Reliability",answer:`- **replication.factor** (topic): how many **copies** of each partition exist (durability ceiling). RF=3 → 3 replicas.
- **min.insync.replicas** (topic/broker): how many replicas must be **in sync** for an acks=all write to succeed (durability floor at write time).

RF sets total redundancy; \`min.insync.replicas\` sets the minimum you require to be caught up when acknowledging. Typical durable combo: RF=3, min.insync.replicas=2 (tolerates one broker loss while still accepting writes).

\`\`\`text
RF=3               -> 3 replicas exist (can lose up to 2 and not lose data)
min.insync=2       -> acks=all needs >=2 in-sync to accept a write
Combo RF=3/min=2   -> survive 1 broker down AND still accept writes; block if 2 down.
\`\`\``,tech:"kafka",displayNumber:198,sortKey:2370},{id:"kafka-2371",number:199,title:"What is a replica fetcher and how do followers stay in sync?",question:"What is a replica fetcher and how do followers stay in sync?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"Each follower runs **replica fetcher** threads that continuously pull records from the partition **leader** (just like a consumer), appending them to their local log and reporting their fetch position. The leader tracks each follower's progress; followers within `replica.lag.time.max.ms` stay in the ISR. Fetcher throughput (threads, network, disk) determines how quickly followers catch up and whether they stay in sync under load.\n\n```text\nFollower's ReplicaFetcherThread --fetch--> Leader --records--> follower log\nLeader tracks follower offsets -> those caught up within replica.lag.time.max.ms = ISR.\nTune num.replica.fetchers / network for replication throughput under load.\n```",tech:"kafka",displayNumber:199,sortKey:2371},{id:"kafka-2372",number:200,title:"What is controller failover and the KRaft quorum?",question:"What is controller failover and the KRaft quorum?",difficulty:"Hard",category:"Replication & Reliability",answer:"The **controller** manages cluster metadata and partition leadership. In **KRaft** mode, a set of controller nodes forms a **Raft quorum** that stores metadata in an internal replicated log (no ZooKeeper). One controller is active; if it fails, the quorum elects another and the new controller has the full metadata log — fast, consistent **controller failover**. This scales metadata handling and removes the old ZooKeeper dependency and its failure modes.\n\n```text\nKRaft: N controllers -> Raft quorum -> replicated metadata log (topics, leaders, ISR).\nActive controller fails -> quorum elects a new active controller (has full log)\n-> no ZooKeeper, faster metadata, consistent failover.\n```",tech:"kafka",displayNumber:200,sortKey:2372},{id:"kafka-2373",number:201,title:"What is partition reassignment and why do it?",question:"What is partition reassignment and why do it?",difficulty:"Hard",category:"Replication & Reliability",answer:"Partition reassignment moves partition **replicas between brokers** to rebalance storage and load — e.g. after adding new brokers, decommissioning old ones, or when some brokers are hot. It's done with `kafka-reassign-partitions.sh` (generate a plan, execute, verify), and Kafka copies data to the new replicas before switching. Throttle it (`--throttle`) so replication traffic doesn't overwhelm the cluster during the move.\n\n```bash\n# Move replicas to balance a new broker in (throttled)\nkafka-reassign-partitions.sh --bootstrap-server b:9092 \\\n  --reassignment-json-file plan.json --execute --throttle 50000000\nkafka-reassign-partitions.sh --bootstrap-server b:9092 \\\n  --reassignment-json-file plan.json --verify\n```",tech:"kafka",displayNumber:201,sortKey:2373},{id:"kafka-2374",number:202,title:"How does Kafka handle a broker failure end-to-end?",question:"How does Kafka handle a broker failure end-to-end?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"When a broker fails: the controller detects it (session loss), triggers **leader election** for all partitions the broker led (promoting ISR replicas), and updates metadata. Producers/consumers get `NotLeaderForPartition`, refresh metadata, and reconnect to the new leaders — continuing seamlessly if enough replicas remain. Partitions where the failed broker held the **only** in-sync replica may go offline until it recovers (with unclean election disabled). Under-replicated partitions re-sync when the broker returns.\n\n```text\nBroker down -> controller detects -> elect new leaders from ISR -> update metadata\nClients get NotLeaderForPartition -> refresh -> reconnect to new leaders -> continue.\nIf failed broker had sole ISR for a partition -> offline until recovery (no unclean loss).\n```",tech:"kafka",displayNumber:202,sortKey:2374},{id:"kafka-2375",number:203,title:"What is the difference between LEO, HW, and LSO?",question:"What is the difference between LEO, HW, and LSO?",difficulty:"Hard",category:"Replication & Reliability",answer:`- **LEO (Log End Offset)**: the offset after the last record in a replica's log (the leader's LEO is where new writes land).
- **HW (High Watermark)**: the highest offset replicated to the ISR — the max normal consumers can read.
- **LSO (Last Stable Offset)**: for transactions — the offset below which all transactional records are decided (committed/aborted); \`read_committed\` consumers can't read past it.

HW ≤ LEO; LSO ≤ HW when open transactions exist.

\`\`\`text
LEO : leader's next-write position (log end)
HW  : replicated-to-ISR boundary (readable limit for consumers)
LSO : transaction-decided boundary (readable limit for read_committed)
Relationship: LSO <= HW <= LEO
\`\`\``,tech:"kafka",displayNumber:203,sortKey:2375},{id:"kafka-2376",number:204,title:"How do you improve Kafka availability and durability together?",question:"How do you improve Kafka availability and durability together?",difficulty:"Hard",category:"Replication & Reliability",answer:`Layer the guarantees: **RF ≥ 3** across **rack/AZ-aware** brokers; **min.insync.replicas=2** with **acks=all** (durability without single-broker fragility); **unclean.leader.election.enable=false** (no silent loss); enough brokers to tolerate failures with capacity to spare; **monitoring** of under-replicated/offline partitions and ISR changes; and **tested failover/DR** procedures. Balance: too-strict settings hurt availability, too-loose risk loss — tune to your RPO/RTO.

\`\`\`text
Durable + available baseline:
  replication.factor=3, spread across AZs (broker.rack)
  min.insync.replicas=2, producer acks=all
  unclean.leader.election.enable=false
  monitor: UnderReplicated/Offline partitions, IsrShrinks, controller changes
  practice failover + DR (RPO/RTO defined and tested).
\`\`\``,tech:"kafka",displayNumber:204,sortKey:2376},{id:"kafka-2377",number:205,title:"Why can't all replicas be on the same broker?",question:"Why can't all replicas be on the same broker?",difficulty:"Basic",category:"Replication & Reliability",answer:"Replication exists to survive **broker failure**, so Kafka deliberately places each partition's replicas on **different brokers** — putting them all on one broker would defeat the purpose (that broker dying loses every copy). The controller's replica-placement algorithm enforces this (and rack awareness spreads them across zones too). You can't have replication factor higher than the number of brokers.\n\n```text\nRF=3 requires >= 3 brokers; each replica on a DISTINCT broker.\nAll-on-one-broker would mean one failure = total loss -> Kafka forbids it.\nRack awareness extends this to distinct AZs/racks.\n```",tech:"kafka",displayNumber:205,sortKey:2377},{id:"kafka-2378",number:206,title:"What is `replica.lag.time.max.ms`?",question:"What is `replica.lag.time.max.ms`?",difficulty:"Intermediate",category:"Replication & Reliability",answer:"It's the maximum time a follower can go without fully catching up to the leader before it's removed from the ISR. Rather than lagging by a message count (the old `replica.lag.max.messages`, which misbehaved under bursts), Kafka uses time: a follower that hasn't caught up to the leader's log end within this window is considered out of sync. Tuning it trades ISR stability against how quickly you detect a genuinely slow replica.\n\n```properties\nreplica.lag.time.max.ms=30000   # follower must catch up within 30s or leave ISR\n# Time-based (not message-count) -> tolerant of bursts, catches truly slow replicas.\n```",tech:"kafka",displayNumber:206,sortKey:2378},{id:"kafka-2379",number:207,title:"Exercise — Reliability: configure a topic to survive one AZ failure with no data loss.",question:"Exercise — Reliability: configure a topic to survive one AZ failure with no data loss.",difficulty:"Hard",category:"Replication & Reliability",answer:"Use RF=3 spread across three AZs (rack awareness), `min.insync.replicas=2`, producers with `acks=all` and idempotence, and disable unclean leader election. Losing one AZ leaves 2 replicas (still ≥ min.insync), so writes continue and no committed data is lost.\n\n```bash\nkafka-topics.sh --create --topic orders \\\n  --partitions 12 --replication-factor 3 \\\n  --config min.insync.replicas=2 \\\n  --config unclean.leader.election.enable=false \\\n  --bootstrap-server b:9092\n# Brokers in 3 AZs via broker.rack; producers: acks=all, enable.idempotence=true.\n# One AZ down -> 2 ISR remain (>= min.insync) -> writes continue, zero loss.\n```",tech:"kafka",displayNumber:207,sortKey:2379},{id:"kafka-2380",number:208,title:"What are the three delivery semantics (at-most-once, at-least-once, exactly-once)?",question:"What are the three delivery semantics (at-most-once, at-least-once, exactly-once)?",difficulty:"Intermediate",category:"Delivery Semantics",answer:`- **At-most-once**: each record is processed 0 or 1 times — no duplicates, but possible **loss** (commit offset before processing).
- **At-least-once**: each record is processed 1+ times — no loss, but possible **duplicates** (commit after processing). The common default; pair with idempotency.
- **Exactly-once**: each record's effect is applied exactly once within a transactional boundary — no loss, no duplicates, but more complexity/overhead (Kafka transactions).

\`\`\`text
                 Loss?   Duplicate?   How
at-most-once      yes      no         commit offset BEFORE processing
at-least-once     no       yes        commit AFTER processing (+ idempotency)
exactly-once      no       no         Kafka transactions (EOS) / idempotent sink
\`\`\``,tech:"kafka",displayNumber:208,sortKey:2380},{id:"kafka-2381",number:209,title:"Does Kafka provide exactly-once automatically for every app?",question:"Does Kafka provide exactly-once automatically for every app?",difficulty:"Intermediate",category:"Delivery Semantics",answer:"No. Kafka **can** provide exactly-once **semantics (EOS)**, but only for the specific pattern of **read-process-write within Kafka**, and only when configured for it: idempotent + transactional producer, `read_committed` consumer, and transactional offset commits (or Kafka Streams with `processing.guarantee=exactly_once_v2`). It does **not** automatically make arbitrary side effects (external DB/API calls) exactly-once — those need idempotency keys or the outbox pattern.\n\n```text\nEOS applies to: consume -> transform -> produce (+ commit offsets) WITHIN Kafka.\nRequires: enable.idempotence, transactional.id, sendOffsetsToTransaction,\n          consumer isolation.level=read_committed  (or Kafka Streams EOS).\nExternal side effects (DB/HTTP) still need idempotency keys / outbox.\n```",tech:"kafka",displayNumber:209,sortKey:2381},{id:"kafka-2382",number:210,title:"What is a Kafka transaction and what does it guarantee?",question:"What is a Kafka transaction and what does it guarantee?",difficulty:"Hard",category:"Transactions",answer:`A Kafka transaction atomically groups **multiple produces** (across partitions/topics) **and** the **commit of consumed offsets** into one all-or-nothing unit. On commit, all produced records and the offset advance become visible together to \`read_committed\` consumers; on abort, none do. This solves the "wrote output but didn't commit input offset (or vice versa)" failure, enabling exactly-once read-process-write pipelines.

\`\`\`java
producer.initTransactions();
producer.beginTransaction();
producer.send(new ProducerRecord<>("output", key, transformed));
producer.sendOffsetsToTransaction(consumedOffsets, consumer.groupMetadata());
producer.commitTransaction();   // output + input-offset advance become atomic
// On error: producer.abortTransaction();  -> neither is visible.
\`\`\``,tech:"kafka",displayNumber:210,sortKey:2382},{id:"kafka-2383",number:211,title:"What is `transactional.id` and producer fencing?",question:"What is `transactional.id` and producer fencing?",difficulty:"Hard",category:"Transactions",answer:'`transactional.id` is a stable identity for a transactional producer. When a new producer instance with the same id calls `initTransactions()`, the transaction coordinator **bumps the epoch**, **fencing** any older instance — an old/zombie producer with a stale epoch gets `ProducerFencedException` and can no longer write. This prevents a hung-then-resumed producer from corrupting the stream after a new instance took over (critical during failover/redeploy).\n\n```java\np.put("transactional.id", "order-processor");   // stable per logical producer\nproducer.initTransactions();  // bumps epoch -> older instance is FENCED\n// Zombie old producer -> ProducerFencedException on send/commit -> must stop.\n```',tech:"kafka",displayNumber:211,sortKey:2383},{id:"kafka-2384",number:212,title:"What is `sendOffsetsToTransaction` and why combine offsets with output?",question:"What is `sendOffsetsToTransaction` and why combine offsets with output?",difficulty:"Hard",category:"Transactions",answer:`\`sendOffsetsToTransaction(offsets, groupMetadata)\` includes the consumer's **input offsets** in the same transaction as the produced **output**. Committing them together guarantees that output is published **iff** the corresponding input offset advances — eliminating the two classic failure windows (output written but offset not committed → duplicates on restart; offset committed but output lost → data loss). This is the core of Kafka's read-process-write exactly-once.

\`\`\`java
Map<TopicPartition, OffsetAndMetadata> offsets = new HashMap<>();
for (var r : recs) {
    producer.send(new ProducerRecord<>("out", transform(r.value())));
    offsets.put(new TopicPartition(r.topic(), r.partition()),
                new OffsetAndMetadata(r.offset() + 1));
}
producer.sendOffsetsToTransaction(offsets, consumer.groupMetadata());
producer.commitTransaction();  // output + offsets atomic -> no dup / no loss
\`\`\``,tech:"kafka",displayNumber:212,sortKey:2384},{id:"kafka-2385",number:213,title:"What is `isolation.level` and how do read_committed consumers work?",question:"What is `isolation.level` and how do read_committed consumers work?",difficulty:"Hard",category:"Transactions",answer:'`isolation.level=read_committed` makes a consumer read only records from **committed** transactions; it buffers transactional records and only delivers them once it sees the commit marker, hiding aborted ones, and never reads past the **Last Stable Offset (LSO)**. `read_uncommitted` (default) delivers everything, including aborted-transaction records. Use `read_committed` downstream of any transactional producer to get exactly-once reads.\n\n```java\np.put("isolation.level", "read_committed");\n// Sees only committed transactional records; aborted ones filtered out;\n// won\'t advance past LSO while a transaction is still open.\n// read_uncommitted -> may deliver records that later get aborted.\n```',tech:"kafka",displayNumber:213,sortKey:2385},{id:"kafka-2386",number:214,title:"What is an aborted transaction and what happens to its records?",question:"What is an aborted transaction and what happens to its records?",difficulty:"Intermediate",category:"Transactions",answer:"An aborted transaction is one that didn't commit — the app called `abortTransaction()`, the producer crashed, or the transaction timed out. Its records were physically written to the log (they occupy offsets) but are marked aborted via a control marker; **`read_committed` consumers skip them entirely**. `read_uncommitted` consumers would still see them. Aborting is how you cleanly discard partial work in a transaction.\n\n```text\nbeginTransaction -> send A, B -> error -> abortTransaction()\nLog physically has A,B + ABORT marker.\nread_committed consumers: never see A,B.\nread_uncommitted consumers: see A,B (unsafe for EOS).\n```",tech:"kafka",displayNumber:214,sortKey:2386},{id:"kafka-2387",number:215,title:"What is the transaction coordinator and transaction log?",question:"What is the transaction coordinator and transaction log?",difficulty:"Hard",category:"Transactions",answer:"The **transaction coordinator** is a broker (chosen by hashing `transactional.id`) that manages a transactional producer's state — assigning epochs, tracking which partitions are in a transaction, and driving commit/abort (writing control markers to the data partitions). It persists state in the internal **transaction log** (`__transaction_state`), a replicated compacted topic, so transactions can be recovered and completed consistently after coordinator failover.\n\n```text\ntransactional.id -> hash -> __transaction_state partition -> coordinator broker\nCoordinator: assigns PID/epoch, tracks partitions in the txn, commits/aborts,\n             writes markers, persists state in __transaction_state (durable).\nCoordinator failover -> recover in-flight txns from the transaction log.\n```",tech:"kafka",displayNumber:215,sortKey:2387},{id:"kafka-2388",number:216,title:"What is idempotent processing and why does at-least-once need it?",question:"What is idempotent processing and why does at-least-once need it?",difficulty:"Intermediate",category:"Delivery Semantics",answer:`Idempotent processing means applying the same record multiple times yields the **same final result** as applying it once. Since at-least-once delivery can redeliver records (crash between processing and commit), non-idempotent side effects (e.g. "add $10") would double-apply. Make operations idempotent via **unique event IDs + a dedup store**, **database unique constraints/upserts**, or naturally idempotent operations (set-to-value rather than increment).

\`\`\`java
// Dedup by event id -> reprocessing has no extra effect
int rows = jdbc.update(
  "INSERT INTO applied(event_id) VALUES (?) ON CONFLICT DO NOTHING", r_eventId);
if (rows == 1) applyBusinessEffect(r);   // only first time
// Or upsert to a target state instead of incrementing.
\`\`\``,tech:"kafka",displayNumber:216,sortKey:2388},{id:"kafka-2389",number:217,title:"What is the dual-write problem and how does the outbox pattern solve it?",question:"What is the dual-write problem and how does the outbox pattern solve it?",difficulty:"Hard",category:"Transactions",answer:`The **dual-write problem**: updating your database and publishing to Kafka are two separate operations, so a crash between them leaves them inconsistent (DB updated but event lost, or vice versa) — Kafka transactions can't span an external DB. The **Transactional Outbox** writes the business row **and** an \`outbox\` event row in the **same DB transaction**; a relay or **CDC** (Debezium) then publishes committed outbox rows to Kafka. The write is atomic; delivery is at-least-once (consumers dedupe).

\`\`\`java
@Transactional
void placeOrder(Order o) {
    orderRepo.save(o);                                    // business row
    outboxRepo.save(new Outbox("Order", o.id(), "OrderPlaced", json(o))); // same tx
}
// CDC/Debezium tails the outbox table -> publishes to Kafka after commit.
// No dual write -> no lost/orphaned events. Consumers dedupe (at-least-once).
\`\`\``,tech:"kafka",displayNumber:217,sortKey:2389},{id:"kafka-2390",number:218,title:"Can a Kafka transaction span an external database?",question:"Can a Kafka transaction span an external database?",difficulty:"Hard",category:"Transactions",answer:`No — Kafka transactions are **only** atomic across Kafka partitions/topics and Kafka offset commits. They can't make an external DB write atomic with a Kafka publish. To coordinate Kafka with a database you use application-level patterns: the **outbox pattern** (DB + event in one DB tx, then CDC to Kafka), **idempotent consumers** (dedup on the DB side), or, rarely, a distributed-transaction/2PC approach (usually avoided for performance/availability).

\`\`\`text
Kafka txn scope = Kafka topics/partitions + consumer offsets ONLY.
Kafka + external DB atomicity -> use:
  - Outbox pattern (DB tx writes event; CDC publishes)
  - Idempotent consumer (dedup on DB unique key)
  - (rarely) 2PC/Saga for cross-system coordination
\`\`\``,tech:"kafka",displayNumber:218,sortKey:2390},{id:"kafka-2391",number:219,title:"What is an idempotency key and how do you use it?",question:"What is an idempotency key and how do you use it?",difficulty:"Intermediate",category:"Delivery Semantics",answer:`An idempotency key is a unique business identifier (order id, request id, event id) used to detect and safely ignore **duplicate** operations. Store processed keys with a **unique constraint**; on a duplicate, the insert fails/no-ops and you return the prior result instead of re-executing. It's the practical way to make at-least-once delivery safe for non-idempotent actions like payments.

\`\`\`sql
CREATE TABLE processed (idempotency_key TEXT PRIMARY KEY, result JSONB);
-- First call inserts and runs; a retry conflicts -> return stored result, no re-charge.
INSERT INTO processed(idempotency_key, result) VALUES ($1,$2)
ON CONFLICT (idempotency_key) DO NOTHING;
\`\`\``,tech:"kafka",displayNumber:219,sortKey:2391},{id:"kafka-2392",number:220,title:"What is a producer sequence number and how does it prevent duplicates?",question:"What is a producer sequence number and how does it prevent duplicates?",difficulty:"Hard",category:"Transactions",answer:`With idempotence enabled, each producer gets a **PID** and assigns a monotonically increasing **sequence number** per partition to every record. The broker tracks the last sequence it accepted per (PID, partition); it **rejects duplicates** (a re-sent sequence it already has) and **out-of-order** writes. So if an ack is lost and the producer retries, the broker recognizes the duplicate sequence and doesn't append it again — no duplicate record from retries.

\`\`\`text
Producer (PID=42) -> partition 0: seq 0,1,2,3...
Broker remembers last accepted seq per (PID, partition).
Retry re-sends seq 2 -> broker sees duplicate -> ignores (returns success), no dup.
Out-of-order seq -> OutOfOrderSequenceException.
\`\`\``,tech:"kafka",displayNumber:220,sortKey:2392},{id:"kafka-2393",number:221,title:"What is EOS (Exactly-Once Semantics) `v2` and how is it enabled?",question:"What is EOS (Exactly-Once Semantics) `v2` and how is it enabled?",difficulty:"Hard",category:"Transactions",answer:'EOS is Kafka\'s transactional mechanism for exactly-once **read-process-write**. `v2` (default in modern Kafka) is more efficient than v1 — it scales to many partitions per transaction with less coordinator overhead. In raw clients you enable it with idempotent + transactional producer and `sendOffsetsToTransaction`; in **Kafka Streams** you simply set `processing.guarantee=exactly_once_v2` and the library handles transactions, offsets, and state-store consistency for you.\n\n```java\n// Kafka Streams — one line gives EOS across input offsets, output, and state stores\nprops.put(StreamsConfig.PROCESSING_GUARANTEE_CONFIG, "exactly_once_v2");\n// Raw clients: enable.idempotence + transactional.id + sendOffsetsToTransaction.\n```',tech:"kafka",displayNumber:221,sortKey:2393},{id:"kafka-2394",number:222,title:"What happens if a transactional producer crashes mid-transaction?",question:"What happens if a transactional producer crashes mid-transaction?",difficulty:"Hard",category:"Transactions",answer:"The in-flight transaction is not committed, so its records stay **invisible** to `read_committed` consumers. The coordinator will **abort** it (either when the new producer instance fences the old one via `initTransactions()`, or when `transaction.timeout.ms` expires). Because commit is atomic, there's no partial visibility — downstream never sees half a transaction. The restarted producer starts clean with a bumped epoch.\n\n```text\nCrash during txn -> not committed -> read_committed consumers see nothing.\nCoordinator aborts it: on new producer initTransactions() (fencing) OR on\ntransaction.timeout.ms. Restarted producer resumes with a new epoch, clean state.\n```",tech:"kafka",displayNumber:222,sortKey:2394},{id:"kafka-2395",number:223,title:"What is `transaction.timeout.ms`?",question:"What is `transaction.timeout.ms`?",difficulty:"Intermediate",category:"Transactions",answer:"It's the maximum time a transaction may stay open before the coordinator **aborts** it automatically. This prevents a hung producer from holding a transaction (and blocking `read_committed` consumers past the LSO) forever. It must be ≤ the broker's `transaction.max.timeout.ms`. Set it long enough for your processing batch but short enough that a stuck producer is cleaned up promptly.\n\n```java\np.put(\"transaction.timeout.ms\", 60000);   // abort if not committed within 60s\n// Guards against stuck transactions blocking read_committed consumers.\n// Must be <= broker transaction.max.timeout.ms.\n```",tech:"kafka",displayNumber:223,sortKey:2395},{id:"kafka-2396",number:224,title:"When should you use Kafka transactions, and what's the cost?",question:"When should you use Kafka transactions, and what's the cost?",difficulty:"Hard",category:"Transactions",answer:"Use transactions when you genuinely need **atomic multi-partition/topic writes** and **exactly-once read-process-write within Kafka** (e.g. streaming aggregations, financial event pipelines). Costs: added latency (commit round-trips + markers), throughput overhead, operational complexity, and the LSO delay for read_committed consumers. If at-least-once + idempotent consumers suffice (often they do), prefer that simpler path; reserve transactions for true exactly-once requirements.\n\n```text\nUse transactions when: atomic multi-topic writes OR EOS read-process-write in Kafka.\nCosts: extra latency (commit markers), lower throughput, complexity, LSO read delay.\nIf idempotent at-least-once is enough -> use it (simpler, faster). Don't over-adopt EOS.\n```",tech:"kafka",displayNumber:224,sortKey:2396},{id:"kafka-2397",number:225,title:"How do you make a consumer idempotent without transactions?",question:"How do you make a consumer idempotent without transactions?",difficulty:"Intermediate",category:"Delivery Semantics",answer:`Store a unique event id per record with a DB **unique constraint** (or key-set); process only if the insert succeeds, so reprocessing after a crash is a no-op. Alternatively use **upserts** (write final state, not deltas) so applying twice is harmless, or check current state before acting. This achieves effective exactly-once **effects** with plain at-least-once delivery and no transactional overhead — the most common production approach.

\`\`\`java
@KafkaListener(topics = "payments", groupId = "ledger")
void handle(Payment p) {
    boolean fresh = ledger.recordIfNew(p.eventId());   // unique constraint
    if (fresh) ledger.credit(p.account(), p.amount()); // only once
    // duplicate delivery -> recordIfNew returns false -> skipped. No transactions needed.
}
\`\`\``,tech:"kafka",displayNumber:225,sortKey:2397},{id:"kafka-2398",number:226,title:"What is the difference between idempotent producer and transactional producer?",question:"What is the difference between idempotent producer and transactional producer?",difficulty:"Hard",category:"Transactions",answer:"- **Idempotent producer** (`enable.idempotence=true`): prevents **duplicates from retries** and preserves ordering — but only within a single producer session, per partition. No cross-partition atomicity.\n- **Transactional producer** (adds `transactional.id` + the transaction API): builds on idempotence to give **atomic multi-partition/topic writes**, **offset+output atomicity**, and **fencing** across sessions — i.e. full EOS.\n\nIdempotence = no dup retries; transactions = atomic units + exactly-once pipelines.\n\n```text\nIdempotent producer:  no duplicate on retry, ordered, single-session, per-partition.\nTransactional producer: idempotence + atomic across partitions/topics + offsets\n                        + cross-session fencing = exactly-once read-process-write.\n```",tech:"kafka",displayNumber:226,sortKey:2398},{id:"kafka-2399",number:227,title:"Exercise — Transactions: implement an exactly-once read-process-write loop.",question:"Exercise — Transactions: implement an exactly-once read-process-write loop.",difficulty:"Hard",category:"Transactions",answer:`Configure a transactional producer and a \`read_committed\` consumer (auto-commit off). In each batch: begin a transaction, produce transformed outputs, add the consumed offsets to the transaction, and commit — so input consumption and output production are atomic. Abort on any error.

\`\`\`java
producer.initTransactions();
while (running) {
    var recs = consumer.poll(Duration.ofMillis(500));
    if (recs.isEmpty()) continue;
    try {
        producer.beginTransaction();
        var offsets = new HashMap<TopicPartition, OffsetAndMetadata>();
        for (var r : recs) {
            producer.send(new ProducerRecord<>("out", transform(r.value())));
            offsets.put(new TopicPartition(r.topic(), r.partition()),
                        new OffsetAndMetadata(r.offset() + 1));
        }
        producer.sendOffsetsToTransaction(offsets, consumer.groupMetadata());
        producer.commitTransaction();          // atomic: output + input offsets
    } catch (Exception e) {
        producer.abortTransaction();           // nothing visible downstream
    }
}
// consumer: isolation.level=read_committed, enable.auto.commit=false
\`\`\``,tech:"kafka",displayNumber:227,sortKey:2399},{id:"kafka-2400",number:228,title:"How does Kafka store records on disk?",question:"How does Kafka store records on disk?",difficulty:"Intermediate",category:"Internals & Storage",answer:"Kafka stores each **partition** as an **append-only commit log** on disk — a directory containing ordered **segment** files. New records are appended sequentially to the active segment (fast sequential I/O); records are never modified in place. Alongside the `.log` files, Kafka keeps offset and timestamp **indexes** for fast lookups. This log-structured design is why Kafka is fast and why it retains/replays data rather than deleting on consume.\n\n```text\n/var/kafka/orders-0/\n  00000000000000000000.log     <- records appended sequentially\n  00000000000000000000.index   <- offset -> file position (sparse)\n  00000000000000000000.timeindex<- timestamp -> offset\n  00000000000000010000.log     <- next segment (after roll)\n```",tech:"kafka",displayNumber:228,sortKey:2400},{id:"kafka-2401",number:229,title:"What is a log segment and why does Kafka use segments?",question:"What is a log segment and why does Kafka use segments?",difficulty:"Intermediate",category:"Internals & Storage",answer:"A partition log is split into fixed-size/time **segments** rather than one giant file. Only the newest (active) segment is written; older ones are read-only. Segments make retention and deletion efficient (drop whole old segment files instead of rewriting a huge file), enable per-segment indexing, and bound recovery/compaction work. `segment.bytes` and `segment.ms` control when the active segment **rolls** to a new one.\n\n```text\nSegment benefits:\n  - delete old data = remove whole segment files (cheap)\n  - each segment has its own index (fast lookup)\n  - bounded compaction/recovery per segment\nsegment.bytes (size) / segment.ms (time) trigger rolling to a new active segment.\n```",tech:"kafka",displayNumber:229,sortKey:2401},{id:"kafka-2402",number:230,title:"What is log rolling?",question:"What is log rolling?",difficulty:"Basic",category:"Internals & Storage",answer:"Log rolling is closing the current **active segment** and starting a new one when a threshold is hit — the segment reaches `segment.bytes` in size or `segment.ms` in age. Rolling bounds segment size (so retention/deletion and indexing stay efficient) and lets time-based retention delete older segments. A very large `segment.ms` with low traffic can delay deletion, since retention acts on **closed** segments.\n\n```properties\nsegment.bytes=1073741824   # roll at 1GB\nsegment.ms=604800000       # or roll after 7 days\n# Retention deletes CLOSED segments -> tune so old data actually ages out.\n```",tech:"kafka",displayNumber:230,sortKey:2402},{id:"kafka-2403",number:231,title:"How do Kafka indexes and the sparse offset index work?",question:"How do Kafka indexes and the sparse offset index work?",difficulty:"Hard",category:"Internals & Storage",answer:"Each segment has a **sparse index** mapping *some* offsets to byte positions in the `.log` file (an entry roughly every `index.interval.bytes`, not every record). To find offset N, Kafka binary-searches the index for the nearest lower entry, then scans forward in the log — O(log n) + a small scan, without indexing every record (saving memory). A separate **time index** maps timestamps to offsets for time-based lookups (`offsetsForTimes`).\n\n```text\nSparse offset index (every ~4KB, not every record):\n  offset 0    -> byte 0\n  offset 512  -> byte 40960\n  offset 1024 -> byte 81920\nLookup offset 700: binary-search index -> start at 512@40960 -> scan forward to 700.\nTime index: timestamp -> offset (powers offsetsForTimes / time-based seek).\n```",tech:"kafka",displayNumber:231,sortKey:2403},{id:"kafka-2404",number:232,title:"What is the page cache and why does Kafka rely on it?",question:"What is the page cache and why does Kafka rely on it?",difficulty:"Hard",category:"Internals & Storage",answer:"The **page cache** is the OS filesystem cache in RAM. Kafka writes to the log via the page cache (the OS flushes to disk asynchronously) and serves reads from it — recently produced records are usually still in cache, so consumers reading near the tail hit RAM, not disk. Kafka deliberately keeps the JVM heap small and lets the OS manage caching, avoiding duplicate app-level caches and GC pressure. This is central to Kafka's throughput.\n\n```text\nProduce -> append to page cache (RAM) -> OS flushes to disk asynchronously\nConsume tail -> served from page cache (no disk read)\n=> keep JVM heap modest; give the OS lots of RAM for page cache.\n```",tech:"kafka",displayNumber:232,sortKey:2404},{id:"kafka-2405",number:233,title:"What is zero-copy and how does Kafka use it?",question:"What is zero-copy and how does Kafka use it?",difficulty:"Hard",category:"Internals & Storage",answer:"**Zero-copy** avoids copying data through user space when sending it over the network. Normally serving a file to a socket copies bytes disk→kernel→app→kernel→socket. Kafka uses the OS **`sendfile`** syscall to transfer log data directly from the page cache to the network socket, skipping the app-space copies and context switches. This makes the consumer read path extremely efficient at high throughput.\n\n```text\nWithout zero-copy: page cache -> app buffer -> socket buffer -> NIC (4 copies, 2+ switches)\nWith sendfile:     page cache -----------------------> socket/NIC (kernel-only)\n=> Kafka serves consumers directly from page cache with minimal CPU.\n(TLS/encryption can reduce zero-copy applicability.)\n```",tech:"kafka",displayNumber:233,sortKey:2405},{id:"kafka-2406",number:234,title:"Why is Kafka so fast?",question:"Why is Kafka so fast?",difficulty:"Intermediate",category:"Internals & Storage",answer:"Several design choices compound: **sequential disk I/O** (append-only log, no random seeks), **page cache** (reads/writes hit RAM, OS-managed), **zero-copy** (`sendfile` on the read path), **batching** (producer/broker amortize per-record overhead), **compression** (less network/disk), **partition parallelism** (scale across cores/brokers), and minimal per-message broker state (consumers own offsets). Together these let commodity hardware sustain very high throughput.\n\n```text\nSpeed = sequential I/O + page cache + zero-copy + batching + compression\n        + partition parallelism + lean broker bookkeeping.\nNo random disk seeks, no per-message ack tracking on the broker -> high throughput.\n```",tech:"kafka",displayNumber:234,sortKey:2406},{id:"kafka-2407",number:235,title:"What is log compaction and when should you use it?",question:"What is log compaction and when should you use it?",difficulty:"Hard",category:"Internals & Storage",answer:'Log compaction (`cleanup.policy=compact`) retains at least the **latest value per key** and eventually removes older superseded values — so the topic becomes a compacted **changelog/snapshot** of current state rather than a time-bounded stream. Use it for **state/changelog** topics: latest config per key, latest account balance, Kafka Streams state, or database change streams where you only need the current value. It runs asynchronously in the background (old values aren\'t removed instantly).\n\n```text\nBefore compaction (key -> value):  A=1  B=1  A=2  C=1  A=3  B=2\nAfter compaction (latest per key): A=3  C=1  B=2   (old A=1,A=2,B=1 removed)\nUse for: config topics, KTable/Streams state, CDC "current value" streams.\nConfig: cleanup.policy=compact\n```',tech:"kafka",displayNumber:235,sortKey:2407},{id:"kafka-2408",number:236,title:"What is a tombstone record?",question:"What is a tombstone record?",difficulty:"Intermediate",category:"Internals & Storage",answer:'In a compacted topic, a **tombstone** is a record with a key and a **null value**. It signals **deletion** of that key: after compaction (and a `delete.retention.ms` grace period so consumers can observe it), the key and its prior values are removed from the log. Tombstones are how you delete keys from a compacted (changelog) topic — e.g. reflecting a row deletion in a CDC stream.\n\n```java\n// Delete key "A1" from a compacted topic -> send a null-value tombstone\nproducer.send(new ProducerRecord<>("state", "A1", null));   // tombstone\n// After compaction + delete.retention.ms, key "A1" is removed from the log.\n```',tech:"kafka",displayNumber:236,sortKey:2408},{id:"kafka-2409",number:237,title:"What is the log cleaner and dirty ratio?",question:"What is the log cleaner and dirty ratio?",difficulty:"Hard",category:"Internals & Storage",answer:'The **log cleaner** is the background component that performs compaction. It works on the "dirty" portion of the log (records after the last clean point) and rewrites segments keeping only the latest value per key. The **dirty ratio** (`min.cleanable.dirty.ratio`) controls when cleaning kicks in — cleaning only when enough dirty data has accumulated to make the work worthwhile (default ~0.5). Tuning it trades compaction frequency/CPU against how promptly stale values are removed.\n\n```properties\nmin.cleanable.dirty.ratio=0.5   # clean when >=50% of the log is "dirty"\n# Lower -> compact more aggressively (fresher state, more CPU)\n# Higher -> compact less often (staler tail, less CPU)\n# log.cleaner.threads controls cleaner parallelism.\n```',tech:"kafka",displayNumber:237,sortKey:2409},{id:"kafka-2410",number:238,title:"Can a topic use both retention (delete) and compaction?",question:"Can a topic use both retention (delete) and compaction?",difficulty:"Intermediate",category:"Internals & Storage",answer:"Yes — `cleanup.policy=compact,delete` combines both: keep the latest value per key **and** also delete records older than the time/size retention. This is useful when you want a current-state changelog but don't need to keep very old keys forever (bounded compacted topic). With just `compact`, keys are retained indefinitely (until tombstoned); adding `delete` also ages out old data by `retention.ms`/`retention.bytes`.\n\n```properties\ncleanup.policy=compact,delete\nretention.ms=2592000000     # also drop data older than 30 days\n# = latest value per key, but nothing older than 30 days survives.\n```",tech:"kafka",displayNumber:238,sortKey:2410},{id:"kafka-2411",number:239,title:"What are `retention.ms` and `retention.bytes`?",question:"What are `retention.ms` and `retention.bytes`?",difficulty:"Basic",category:"Internals & Storage",answer:"For `cleanup.policy=delete`, these control how long/large a partition's log is kept before old **segments** are deleted:\n- **retention.ms**: max age of records (e.g. 7 days) — closed segments older than this are eligible for deletion.\n- **retention.bytes**: max size **per partition**; when exceeded, the oldest segments are deleted.\n\nEither (or both) can trigger deletion; the total topic disk use is roughly `retention.bytes × partitions × replication.factor`.\n\n```properties\nretention.ms=604800000       # keep 7 days\nretention.bytes=10737418240  # or cap each partition at 10GB (whichever hits first)\n# Deletion acts on closed segments; disk ≈ retention.bytes × partitions × RF.\n```",tech:"kafka",displayNumber:239,sortKey:2411},{id:"kafka-2412",number:240,title:"What is `segment.bytes` and how does it interact with retention?",question:"What is `segment.bytes` and how does it interact with retention?",difficulty:"Intermediate",category:"Internals & Storage",answer:"`segment.bytes` is the max size of a single log segment before it rolls. Retention/compaction operate at **segment granularity** — Kafka can only delete a **closed** segment, so segment size affects how finely retention applies. Very large segments mean data ages out in big chunks (and old data lingers until the segment closes); very small segments increase file/handle overhead. Tune it relative to throughput and retention needs.\n\n```text\nsegment.bytes too large -> old data can't be deleted until the big segment closes\nsegment.bytes too small -> many files, more open handles / index overhead\nRetention deletes whole CLOSED segments -> size affects deletion granularity.\n```",tech:"kafka",displayNumber:240,sortKey:2412},{id:"kafka-2413",number:241,title:"What happens when a record value is null in a compacted vs non-compacted topic?",question:"What happens when a record value is null in a compacted vs non-compacted topic?",difficulty:"Intermediate",category:"Internals & Storage",answer:`- **Compacted topic**: a null value is a **tombstone** — it marks the key for deletion during compaction.
- **Non-compacted (delete) topic**: a null value is just a normal record with no payload; it carries no special deletion meaning and is retained/deleted purely by time/size retention.

So the same null value has very different semantics depending on \`cleanup.policy\`.

\`\`\`text
cleanup.policy=compact : key + null value = TOMBSTONE (delete the key)
cleanup.policy=delete  : key + null value = ordinary empty-payload record
Meaning depends on the topic's cleanup policy.
\`\`\``,tech:"kafka",displayNumber:241,sortKey:2413},{id:"kafka-2414",number:242,title:"How does partition count impact storage and cluster overhead?",question:"How does partition count impact storage and cluster overhead?",difficulty:"Intermediate",category:"Internals & Storage",answer:`Each partition (times replication factor) is a set of on-disk log/index files plus in-memory metadata, open file handles, and replication/fetcher connections. More partitions = more parallelism, but also more **memory, file descriptors, controller metadata, longer recovery/leader-election times, and heavier rebalances**. Thousands of unnecessary partitions degrade stability and startup/failover time. Size partitions for real parallelism/throughput needs plus headroom — not "as many as possible."

\`\`\`text
Per partition-replica cost: log+index files, file handles, memory, fetcher connections.
Too many partitions -> more metadata, slower controller/recovery, heavier rebalances,
                       higher memory & FD usage.
Right-size for throughput/parallelism + growth headroom, not maximal count.
\`\`\``,tech:"kafka",displayNumber:242,sortKey:2414},{id:"kafka-2415",number:243,title:"What is disk throughput's role and why monitor disk utilization?",question:"What is disk throughput's role and why monitor disk utilization?",difficulty:"Intermediate",category:"Internals & Storage",answer:"Brokers write incoming records, replicate to followers, and serve fetches — all disk-bound at volume. If disks are slow or saturated, you get replication lag (followers fall out of ISR), rising produce latency, and consumer lag. A **full disk** causes write failures and broker instability. Monitor disk **utilization, latency, and free space**, use fast disks (SSD/NVMe) sized with headroom, and manage retention/reassignment proactively so partitions don't fill a broker.\n\n```text\nSlow/full disk -> ISR shrinks, produce latency up, consumer lag up, write failures.\nMonitor: disk %util, I/O latency, free space; alert before full.\nMitigate: SSD/NVMe, size for headroom, tune retention, reassign partitions off hot brokers.\n```",tech:"kafka",displayNumber:243,sortKey:2415},{id:"kafka-2416",number:244,title:"What is the difference between offset and timestamp indexes?",question:"What is the difference between offset and timestamp indexes?",difficulty:"Hard",category:"Internals & Storage",answer:'- **Offset index** (`.index`): maps a sparse set of **offsets → byte positions** so Kafka can locate a record by offset quickly (used for normal fetches and seeks).\n- **Timestamp index** (`.timeindex`): maps **timestamps → offsets** so Kafka can find the first offset at/after a given time — powering `offsetsForTimes()`, time-based retention, and time-based seeking/replay.\n\nBoth are per-segment and sparse.\n\n```text\n.index      : offset    -> byte position (find record by offset)\n.timeindex  : timestamp -> offset        (find record by time; offsetsForTimes)\nBoth sparse, per segment. Timestamp index enables "replay since T".\n```',tech:"kafka",displayNumber:244,sortKey:2416},{id:"kafka-2417",number:245,title:"What is tiered storage in Kafka?",question:"What is tiered storage in Kafka?",difficulty:"Hard",category:"Internals & Storage",answer:`Tiered storage (KIP-405, GA in recent Kafka) lets a broker keep only **recent** log segments on local disk and offload **older** segments to cheaper remote object storage (e.g. S3), while consumers can still read them transparently. This decouples storage from compute — you can retain data for a long time (or "infinitely") without huge local disks, and brokers recover faster (less local data). It's ideal for long-retention/replay use cases at lower cost.

\`\`\`text
Local disk: hot/recent segments (fast reads)
Remote (S3/object store): older segments (cheap, long retention)
Consumers read both transparently. Benefits: cheaper long retention, faster broker
recovery (smaller local footprint). Enable per-topic (remote.storage.enable).
\`\`\``,tech:"kafka",displayNumber:245,sortKey:2417},{id:"kafka-2418",number:246,title:"Why avoid very large Kafka messages, and how do you handle them?",question:"Why avoid very large Kafka messages, and how do you handle them?",difficulty:"Intermediate",category:"Internals & Storage",answer:'Large messages inflate memory (producer/broker/consumer buffers), network and replication cost, latency, and recovery time, and can trip `max.message.bytes`/`max.partition.fetch.bytes` limits. Prefer keeping payloads small: store the large blob externally (S3/blob store) and put a **reference/URL** in the Kafka record (claim-check pattern), or chunk/compress. Raising size limits works but shifts cost onto the whole cluster.\n\n```java\n// Claim-check pattern: store the blob externally, publish a small reference\nString url = blobStore.put(largePayload);              // S3 etc.\nproducer.send(new ProducerRecord<>("docs", id,\n    "{\\"ref\\":\\"" + url + "\\",\\"size\\":" + largePayload.length + "}"));\n// Keeps Kafka records small -> better throughput, memory, replication, recovery.\n```',tech:"kafka",displayNumber:246,sortKey:2418},{id:"kafka-2419",number:247,title:"Exercise — Storage: configure a changelog (state) topic correctly.",question:"Exercise — Storage: configure a changelog (state) topic correctly.",difficulty:"Hard",category:"Internals & Storage",answer:"A state/changelog topic should be **compacted** (keep latest value per key), with a moderate segment size so compaction runs, `min.insync.replicas=2`/RF=3 for durability, and a `delete.retention.ms` long enough for consumers to observe tombstones before removal. Optionally `compact,delete` to bound very old keys.\n\n```bash\nkafka-topics.sh --create --topic account-state \\\n  --partitions 12 --replication-factor 3 \\\n  --config cleanup.policy=compact \\\n  --config min.insync.replicas=2 \\\n  --config segment.bytes=104857600 \\\n  --config min.cleanable.dirty.ratio=0.5 \\\n  --config delete.retention.ms=86400000 \\\n  --bootstrap-server b:9092\n# Latest value per key retained; tombstones remove deleted keys after 1 day.\n```",tech:"kafka",displayNumber:247,sortKey:2419},{id:"kafka-2420",number:248,title:"How do you improve producer throughput and reduce latency (trade-offs)?",question:"How do you improve producer throughput and reduce latency (trade-offs)?",difficulty:"Intermediate",category:"Performance & Tuning",answer:'**Throughput**: larger `batch.size`, some `linger.ms` (5–20ms), compression (lz4/zstd), sufficient `buffer.memory`, efficient serialization, and enough partitions. **Latency**: small/zero `linger.ms`, smaller batches, avoid per-record `.get()`, keep brokers unsaturated, and don\'t over-tighten `acks` beyond need. They trade against each other — a small `linger.ms` often improves throughput at negligible latency cost, which is a common sweet spot.\n\n```java\n// Balanced throughput-leaning producer\np.put("batch.size", 32768);\np.put("linger.ms", 10);\np.put("compression.type", "lz4");\np.put("buffer.memory", 67108864);\n// Latency-critical: linger.ms=0, smaller batches, unsaturated brokers.\n```',tech:"kafka",displayNumber:248,sortKey:2420},{id:"kafka-2421",number:249,title:"How do you improve consumer throughput?",question:"How do you improve consumer throughput?",difficulty:"Intermediate",category:"Performance & Tuning",answer:"Increase parallelism (more consumers up to partition count), fetch larger batches (`fetch.min.bytes`, `max.partition.fetch.bytes`, `max.poll.records`), optimize processing (batch downstream writes, async I/O, avoid per-record network calls), use efficient deserialization, and offload heavy work off the poll thread. Ensure partition count supports the parallelism you need; a hot partition caps a single consumer regardless of tuning.\n\n```text\nConsumer throughput levers:\n  - more consumers (<= #partitions)\n  - bigger fetches: fetch.min.bytes, max.partition.fetch.bytes, max.poll.records\n  - faster processing: batch DB writes, async, dedupe network calls\n  - efficient deserialization\n  - offload processing to workers (keep poll() responsive)\n```",tech:"kafka",displayNumber:249,sortKey:2421},{id:"kafka-2422",number:250,title:"What is backpressure and how do consumers apply it?",question:"What is backpressure and how do consumers apply it?",difficulty:"Hard",category:"Performance & Tuning",answer:"Backpressure is throttling intake when downstream processing can't keep up, to avoid unbounded memory growth or overload. Kafka's **pull** model gives natural backpressure (consumers fetch at their pace). To apply it explicitly: bound in-flight work queues, **pause** partitions when the downstream is saturated (and resume later), limit concurrent processing, and control poll/commit cadence. Crucially, keep calling `poll()` while paused so heartbeats continue and you don't get kicked from the group.\n\n```java\nif (workQueue.size() > HIGH_WATERMARK)\n    consumer.pause(consumer.assignment());     // stop fetching, keep heartbeating\nconsumer.poll(Duration.ofMillis(200));         // still poll -> stay in group\nif (workQueue.size() < LOW_WATERMARK)\n    consumer.resume(consumer.assignment());\n```",tech:"kafka",displayNumber:250,sortKey:2422},{id:"kafka-2423",number:251,title:"What causes high producer latency?",question:"What causes high producer latency?",difficulty:"Intermediate",category:"Performance & Tuning",answer:"Common causes: **broker overload** (CPU/disk/network saturation), **network latency**, **insufficient replication capacity** (acks=all waiting on slow followers), **too-large `linger.ms`**/over-batching, **retries** from transient errors, **slow disks** (fsync/page-cache pressure), and **GC pauses** on broker or client. Diagnose with producer metrics (`request-latency-avg`, `record-queue-time`) and broker request-latency/queue-time metrics.\n\n```text\nHigh produce latency -> check:\n  broker CPU/disk/network saturation, request queue time\n  acks=all waiting on lagging followers (ISR health)\n  linger.ms too high / batches too big\n  retries (transient errors), GC pauses (client & broker)\nMetrics: request-latency-avg, record-queue-time-avg, RequestQueueTimeMs.\n```",tech:"kafka",displayNumber:251,sortKey:2423},{id:"kafka-2424",number:252,title:"What causes high consumer lag and how do you troubleshoot it?",question:"What causes high consumer lag and how do you troubleshoot it?",difficulty:"Intermediate",category:"Performance & Tuning",answer:`Causes: too few consumers, slow per-record processing, downstream bottlenecks (DB/API), frequent rebalances, large messages, hot partitions, or broker/network issues. Troubleshoot by checking **per-partition** lag (is it one partition = hot key?), consumer processing time, rebalance/generation churn, broker health, and downstream latency. Fix the specific bottleneck: add consumers/partitions, speed processing, stabilize rebalances, or rebalance the hot key.

\`\`\`text
Lag rising -> localize:
  one partition hot? -> keying/partition problem
  all partitions?   -> too few consumers / slow processing / downstream slow
  oscillating?      -> rebalance storm
Check: per-partition LAG, processing time, rebalances, broker health, downstream.
\`\`\``,tech:"kafka",displayNumber:252,sortKey:2424},{id:"kafka-2425",number:253,title:"What is a hot partition and how do you fix it?",question:"What is a hot partition and how do you fix it?",difficulty:"Hard",category:"Performance & Tuning",answer:`A hot partition receives disproportionately more traffic than others — usually because many records share the **same key** (or a low-cardinality key), and keyed partitioning routes them all to one partition. Its consumer becomes a bottleneck while others idle. Fixes: choose a **higher-cardinality key**, **salt** hot keys (append a suffix to spread them, at the cost of per-key ordering), use a custom partitioner, or increase partitions. The root cause is key distribution, not consumer count.

\`\`\`java
// Salting a hot key to spread load (sacrifices strict per-key ordering)
String saltedKey = hotKey + "#" + (counter++ % SALT_BUCKETS);
producer.send(new ProducerRecord<>("events", saltedKey, value));
// Better keys / more cardinality / custom partitioner also fix hot partitions.
// Adding consumers alone does NOT help a single hot partition.
\`\`\``,tech:"kafka",displayNumber:253,sortKey:2425},{id:"kafka-2426",number:254,title:"Can one key be spread across partitions automatically?",question:"Can one key be spread across partitions automatically?",difficulty:"Intermediate",category:"Performance & Tuning",answer:'No — the whole point of keying is that a given key maps deterministically to **one** partition (to preserve order). Kafka won\'t split one key across partitions while keeping that guarantee. If a single key is too hot, you must do **application-level sharding**: salt the key into sub-keys (e.g. `user#0`, `user#1`), accepting that ordering is now only preserved within each sub-key, and your consumer merges/handles them accordingly.\n\n```text\nSame key -> same partition (ordering guarantee) -> can\'t auto-split.\nTo spread a hot key: app-level sharding via salting:\n  "user42" -> "user42#0".."user42#N"  (N partitions possible)\nCost: ordering preserved only within each salted sub-key, not across them.\n```',tech:"kafka",displayNumber:254,sortKey:2426},{id:"kafka-2427",number:255,title:"What are broker network threads and request handler threads?",question:"What are broker network threads and request handler threads?",difficulty:"Hard",category:"Performance & Tuning",answer:"A broker separates I/O concerns:\n- **Network threads** (`num.network.threads`): accept connections, read requests off sockets, and write responses — they don't do the actual work, they hand requests to a queue.\n- **Request handler threads** (`num.io.threads`): pull from the request queue and **process** produce/fetch/etc., touching the log/page cache.\n\nIf either pool is undersized, requests queue up (high **request queue time**), raising latency. Tune to CPU/core count and load.\n\n```properties\nnum.network.threads=3   # accept/read/write sockets, enqueue requests\nnum.io.threads=8        # process requests (produce/fetch) -> touch log/page cache\n# High RequestQueueTimeMs -> handlers saturated (raise num.io.threads / add brokers).\n```",tech:"kafka",displayNumber:255,sortKey:2427},{id:"kafka-2428",number:256,title:"Why monitor request queue time and what does high queue time indicate?",question:"Why monitor request queue time and what does high queue time indicate?",difficulty:"Intermediate",category:"Performance & Tuning",answer:"**Request queue time** is how long a request waits between being read by a network thread and being picked up by a handler thread. High queue time means the handler pool (`num.io.threads`) is **saturated** — the broker is CPU/IO-bound and can't keep up — which shows up as elevated end-to-end produce/fetch latency. It's an early saturation signal; respond by adding I/O threads, reducing load, or adding brokers/partitions to spread work.\n\n```text\nRequest lifecycle: socket -> network thread -> REQUEST QUEUE -> handler thread -> log\nHigh RequestQueueTimeMs = handlers saturated -> broker overloaded.\nFix: increase num.io.threads, reduce load, scale out brokers/partitions.\n```",tech:"kafka",displayNumber:256,sortKey:2428},{id:"kafka-2429",number:257,title:"What key metrics should Kafka operators monitor?",question:"What key metrics should Kafka operators monitor?",difficulty:"Intermediate",category:"Performance & Tuning",answer:"Track: **throughput** (bytes/records in/out), **request latency** and **queue time**, **consumer lag** (per group/partition), **under-replicated partitions** and **offline partitions**, **ISR shrink/expand rate**, **controller changes**, **disk %util/latency/free**, **CPU/GC**, **network**, and **error rates** (failed produce/fetch, `NotEnoughReplicas`). Alert on symptoms users feel (lag, latency, offline partitions) plus leading indicators (under-replicated, disk filling).\n\n```text\nGolden signals:\n  consumer LAG, UnderReplicatedPartitions, OfflinePartitionsCount\n  request latency + queue time, ISR shrink/expand, controller changes\n  disk %util/free, CPU/GC, network, error rates (NotEnoughReplicas, failed produce)\nAlert on user-facing (lag/latency/offline) + leading (under-replicated/disk).\n```",tech:"kafka",displayNumber:257,sortKey:2429},{id:"kafka-2430",number:258,title:"What is end-to-end latency and how do you diagnose slow consumers?",question:"What is end-to-end latency and how do you diagnose slow consumers?",difficulty:"Hard",category:"Performance & Tuning",answer:"**End-to-end latency** is the time from a record being produced to a consumer successfully processing it (produce + broker/replication + fetch + processing). To diagnose slow consumers, break it down: measure consumer **processing time per record**, poll behavior (are you hitting `max.poll.interval.ms`?), fetch metrics, **lag** trend, downstream dependency latency, plus GC/CPU on the consumer and broker health. Isolate whether the delay is in fetching, processing, or downstream.\n\n```text\nE2E latency = produce -> broker+replicate -> fetch -> process.\nSlow consumer? measure:\n  per-record processing time, downstream call latency, GC pauses, CPU\n  poll interval violations, fetch throughput, lag trend, broker health\nIsolate the stage (fetch vs process vs downstream) before tuning.\n```",tech:"kafka",displayNumber:258,sortKey:2430},{id:"kafka-2431",number:259,title:"What are message size limits and what happens with oversized messages?",question:"What are message size limits and what happens with oversized messages?",difficulty:"Intermediate",category:"Performance & Tuning",answer:"Size is bounded at multiple layers: broker `message.max.bytes` (and topic `max.message.bytes`), producer `max.request.size`, and consumer `max.partition.fetch.bytes`/`fetch.max.bytes`. An oversized record is **rejected** by whichever limit it exceeds — the producer throws `RecordTooLargeException`, or the broker rejects it, or a consumer can't fetch a record larger than its fetch limit (stall). Keep all three consistent, and prefer the claim-check pattern for genuinely large payloads.\n\n```text\nProducer max.request.size  -----\\\nBroker/topic max.message.bytes ---> all must agree; exceed one -> reject/stall\nConsumer max.partition.fetch.bytes -/\nOversized -> RecordTooLargeException / broker reject / consumer can't fetch.\nPrefer external blob + reference (claim-check) over huge messages.\n```",tech:"kafka",displayNumber:259,sortKey:2431},{id:"kafka-2432",number:260,title:"Which compression algorithm should you choose?",question:"Which compression algorithm should you choose?",difficulty:"Intermediate",category:"Performance & Tuning",answer:`- **lz4**: very fast, moderate ratio — great default for latency-sensitive, high-throughput.
- **zstd**: best compression ratio with good speed (tunable level) — great for saving network/disk, slightly more CPU.
- **snappy**: fast, moderate ratio (older common choice).
- **gzip**: highest ratio but most CPU/slowest — use when bandwidth is the tight constraint.

Choose by workload: CPU-abundant + bandwidth-tight → zstd/gzip; latency-critical → lz4. Bigger batches compress better.

\`\`\`text
lz4   : fast, moderate ratio  -> low-latency default
zstd  : best ratio, good speed -> save network/disk (a bit more CPU)
snappy: fast, moderate         -> legacy common
gzip  : best ratio (old), high CPU -> bandwidth-constrained only
Batch more (linger.ms) -> better compression regardless of codec.
\`\`\``,tech:"kafka",displayNumber:260,sortKey:2432},{id:"kafka-2433",number:261,title:"How do you tune request timeouts and socket buffers?",question:"How do you tune request timeouts and socket buffers?",difficulty:"Hard",category:"Performance & Tuning",answer:`- **request.timeout.ms**: how long a client waits for a broker response before retrying/failing; too low causes spurious retries under load, too high delays failure detection.
- **socket.send/receive.buffer.bytes** (client and broker): TCP buffer sizes; larger buffers improve throughput on high-bandwidth/high-latency (high BDP) links (e.g. cross-region replication). Set them to match the bandwidth-delay product.

Tune together with \`delivery.timeout.ms\` (producer) and replication settings for WAN links.

\`\`\`properties
request.timeout.ms=30000                 # wait for broker response before retry
socket.send.buffer.bytes=1048576         # bigger TCP buffers for high-BDP links
socket.receive.buffer.bytes=1048576      # (e.g. cross-region replication)
# Match buffers to bandwidth-delay product; don't set request.timeout too low.
\`\`\``,tech:"kafka",displayNumber:261,sortKey:2433},{id:"kafka-2434",number:262,title:"Exercise — Tuning: configure producer + topic for a high-throughput pipeline.",question:"Exercise — Tuning: configure producer + topic for a high-throughput pipeline.",difficulty:"Hard",category:"Performance & Tuning",answer:`Maximize batching and parallelism while keeping durability: sizable batches with a small linger, zstd compression, ample buffer, acks=all with idempotence, and a topic with enough partitions and RF=3/min.insync=2. Benchmark with \`kafka-producer-perf-test\` before production.

\`\`\`java
p.put("batch.size", 65536);           // 64KB batches
p.put("linger.ms", 15);               // fill batches
p.put("compression.type", "zstd");    // save network/disk
p.put("buffer.memory", 134217728);    // 128MB
p.put("acks", "all");
p.put("enable.idempotence", "true");
// Topic: --partitions 24 --replication-factor 3 --config min.insync.replicas=2
// Validate: kafka-producer-perf-test.sh --num-records ... --throughput -1
\`\`\``,tech:"kafka",displayNumber:262,sortKey:2434},{id:"kafka-2435",number:263,title:"What is Kafka Connect?",question:"What is Kafka Connect?",difficulty:"Intermediate",category:"Kafka Connect",answer:`Kafka Connect is a framework for reliably streaming data **between Kafka and external systems** without writing custom producer/consumer code. **Source connectors** ingest from external systems (databases, files, queues) into Kafka; **sink connectors** export from Kafka to external systems (databases, search, object stores). It handles scaling, parallelism (tasks), offset tracking, retries, and fault tolerance declaratively via configuration, so integrations are config, not code.

\`\`\`json
// Sink connector: stream a Kafka topic into a JDBC database (config, not code)
{
  "name": "orders-jdbc-sink",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "topics": "orders",
    "connection.url": "jdbc:postgresql://db:5432/warehouse",
    "tasks.max": "4",
    "insert.mode": "upsert", "pk.mode": "record_key"
  }
}
\`\`\``,tech:"kafka",displayNumber:263,sortKey:2435},{id:"kafka-2436",number:264,title:"What is the difference between a source and a sink connector?",question:"What is the difference between a source and a sink connector?",difficulty:"Basic",category:"Kafka Connect",answer:`- **Source connector**: reads from an external system and **publishes** the data into Kafka topics (e.g. Debezium capturing DB changes → Kafka).
- **Sink connector**: **consumes** from Kafka topics and writes the data to an external system (e.g. Kafka → Elasticsearch/S3/JDBC).

Source = into Kafka; sink = out of Kafka. Both run on the Connect runtime and split work into tasks.

\`\`\`text
Source: external system --> [source connector] --> Kafka topic
Sink:   Kafka topic --> [sink connector] --> external system
Debezium (CDC) = source; JDBC/Elasticsearch/S3 sinks = out of Kafka.
\`\`\``,tech:"kafka",displayNumber:264,sortKey:2436},{id:"kafka-2437",number:265,title:"What is a connector task and how does parallelism work in Connect?",question:"What is a connector task and how does parallelism work in Connect?",difficulty:"Intermediate",category:"Kafka Connect",answer:"A connector's work is divided into **tasks** — the unit of parallelism. `tasks.max` sets the upper bound; the connector decides how to split work (e.g. a source assigns table/partition ranges to tasks; a sink's tasks map to topic partitions like consumers in a group). Tasks are distributed across Connect **workers** in distributed mode, so scaling = more tasks across more workers, bounded by the data's natural parallelism (e.g. partition count for sinks).\n\n```text\nConnector -> splits into tasks (tasks.max) -> distributed across workers.\nSink tasks behave like a consumer group: parallelism <= topic partition count.\nSource tasks: connector-defined split (tables, shards, partitions).\n```",tech:"kafka",displayNumber:265,sortKey:2437},{id:"kafka-2438",number:266,title:"What is the difference between standalone and distributed mode in Kafka Connect?",question:"What is the difference between standalone and distributed mode in Kafka Connect?",difficulty:"Intermediate",category:"Kafka Connect",answer:`- **Standalone**: a single worker process, config in local files, offsets in a local file. Simple — good for development, testing, or a single-machine pipeline. No fault tolerance.
- **Distributed**: multiple workers form a cluster, coordinating via Kafka (config, offsets, and status stored in internal Kafka topics). Connectors/tasks are balanced across workers and **rebalance on failure** — fault-tolerant and scalable. Use distributed for production.

\`\`\`text
Standalone: 1 worker, file-based config/offsets -> dev/test, no HA.
Distributed: worker cluster, config/offset/status in Kafka topics -> HA + scale,
             manage connectors via REST API, auto rebalancing on worker failure.
\`\`\``,tech:"kafka",displayNumber:266,sortKey:2438},{id:"kafka-2439",number:267,title:"What is a converter in Kafka Connect?",question:"What is a converter in Kafka Connect?",difficulty:"Intermediate",category:"Kafka Connect",answer:"A **converter** translates between Connect's internal data representation and the **serialized bytes** stored in Kafka. `key.converter`/`value.converter` decide the wire format — e.g. `AvroConverter`, `JsonConverter`, `ProtobufConverter`, `StringConverter`. It's separate from the connector logic, so the same connector can produce/consume Avro or JSON just by changing the converter. Schema-aware converters integrate with Schema Registry for compatibility.\n\n```properties\nvalue.converter=io.confluent.connect.avro.AvroConverter\nvalue.converter.schema.registry.url=http://schema-registry:8081\n# Converter = serialization format (Avro/JSON/Protobuf/String), independent of connector.\n```",tech:"kafka",displayNumber:267,sortKey:2439},{id:"kafka-2440",number:268,title:"What is a Single Message Transform (SMT) in Kafka Connect?",question:"What is a Single Message Transform (SMT) in Kafka Connect?",difficulty:"Hard",category:"Kafka Connect",answer:`SMTs are lightweight, per-record transformations applied **inline** in the Connect pipeline (source before writing to Kafka, or sink before writing out) — without a separate stream-processing app. Common SMTs: rename/mask/drop fields, add headers, route to topics, extract a field as the key, or convert timestamps. They're for simple transformations; heavier logic belongs in Kafka Streams/ksqlDB.

\`\`\`properties
# Mask a PII field and route by topic prefix, inline in the connector
transforms=mask,route
transforms.mask.type=org.apache.kafka.connect.transforms.MaskField$Value
transforms.mask.fields=ssn
transforms.route.type=org.apache.kafka.connect.transforms.RegexRouter
transforms.route.regex=(.*)
transforms.route.replacement=warehouse_$1
\`\`\``,tech:"kafka",displayNumber:268,sortKey:2440},{id:"kafka-2441",number:269,title:"What is the Schema Registry and why use schemas?",question:"What is the Schema Registry and why use schemas?",difficulty:"Intermediate",category:"Schema Registry",answer:"A **Schema Registry** centrally stores and versions the schemas (Avro/Protobuf/JSON Schema) for topic data and enforces **compatibility** rules on new versions. Producers register/validate schemas and send only a compact **schema id** in each message; consumers fetch the schema by id to deserialize. Benefits: explicit, enforceable **data contracts**, safe schema evolution, smaller payloads, and prevention of breaking changes between independently-deployed producers and consumers.\n\n```text\nProducer -> register/validate schema -> message = [magic][schema-id][payload]\nConsumer -> read schema-id -> fetch schema from registry -> deserialize\nRegistry enforces compatibility -> producers/consumers evolve safely, payloads stay small.\n```",tech:"kafka",displayNumber:269,sortKey:2441},{id:"kafka-2442",number:270,title:"What are Avro, Protobuf, and JSON Schema, and how do they compare?",question:"What are Avro, Protobuf, and JSON Schema, and how do they compare?",difficulty:"Intermediate",category:"Schema Registry",answer:`All are schema-based formats used with the Schema Registry:
- **Avro**: compact binary, schema travels by id, excellent for evolution; the traditional Kafka default.
- **Protobuf**: compact binary, strong cross-language tooling and typed stubs, good for gRPC-adjacent stacks.
- **JSON Schema**: human-readable JSON with a validation schema; larger payloads but easy to inspect/debug.

Choose Avro/Protobuf for efficiency at scale, JSON Schema for readability/interoperability.

\`\`\`text
Avro        : compact binary, great evolution, classic Kafka choice
Protobuf    : compact binary, strong typed codegen, cross-language
JSON Schema : readable JSON, larger, easy to debug/interoperate
All integrate with Schema Registry + compatibility rules.
\`\`\``,tech:"kafka",displayNumber:270,sortKey:2442},{id:"kafka-2443",number:271,title:"What is schema compatibility (backward, forward, full)?",question:"What is schema compatibility (backward, forward, full)?",difficulty:"Hard",category:"Schema Registry",answer:`Compatibility rules govern whether a new schema version can coexist with existing producers/consumers:
- **Backward**: new schema can read data written with the **old** schema (safe to upgrade **consumers** first) — e.g. add optional field with default, remove a field.
- **Forward**: old schema can read data written with the **new** schema (safe to upgrade **producers** first) — e.g. add a field, remove optional.
- **Full**: both backward and forward.

The registry rejects incompatible changes, protecting the pipeline.

\`\`\`text
Backward: new consumer reads old data  -> upgrade consumers first (add field w/ default)
Forward:  old consumer reads new data  -> upgrade producers first
Full:     both directions hold
Registry enforces the chosen mode -> incompatible schema change = rejected.
\`\`\``,tech:"kafka",displayNumber:271,sortKey:2443},{id:"kafka-2444",number:272,title:"What is Kafka Streams and when would you choose it?",question:"What is Kafka Streams and when would you choose it?",difficulty:"Intermediate",category:"Kafka Streams",answer:`Kafka Streams is a **Java library** (not a separate cluster) for building stream-processing apps directly on Kafka topics — filtering, mapping, aggregations, joins, and windowing — with fault tolerance, state stores, and exactly-once. You deploy it as a normal application (it scales by running more instances, which share partitions like a consumer group). Choose it for Kafka-native transformations when you don't want to operate a separate framework (Flink/Spark); choose those for very large, cross-source, or complex processing.

\`\`\`java
StreamsBuilder b = new StreamsBuilder();
b.stream("orders", Consumed.with(Serdes.String(), orderSerde))
 .filter((k, o) -> o.total() > 100)
 .mapValues(Order::summary)
 .to("big-orders");
new KafkaStreams(b.build(), props).start();   // just a library, deploy as an app
\`\`\``,tech:"kafka",displayNumber:272,sortKey:2444},{id:"kafka-2445",number:273,title:"What is the difference between KStream, KTable, and GlobalKTable?",question:"What is the difference between KStream, KTable, and GlobalKTable?",difficulty:"Hard",category:"Kafka Streams",answer:`- **KStream**: an unbounded **stream of records** (event/append semantics) — each record is an independent fact.
- **KTable**: a **changelog table** — keys map to their **latest value** (upsert/update semantics), backed by a compacted changelog; partitioned like the source.
- **GlobalKTable**: a KTable **fully replicated to every instance**, so any instance can do lookups against the whole table (good for small reference/dimension data and non-co-partitioned joins).

\`\`\`text
KStream      : insert-only event stream        (each record = an event)
KTable        : latest value per key (upsert)   (changelog-backed, partitioned)
GlobalKTable  : full copy on every instance     (small lookup/reference data)
Stream = facts; Table = current state; GlobalKTable = broadcast state for joins.
\`\`\``,tech:"kafka",displayNumber:273,sortKey:2445},{id:"kafka-2446",number:274,title:"What is a state store in Kafka Streams and how is it made fault-tolerant?",question:"What is a state store in Kafka Streams and how is it made fault-tolerant?",difficulty:"Hard",category:"Kafka Streams",answer:"A **state store** is local, durable storage (RocksDB by default, or in-memory) used for stateful operations — aggregations, joins, windowed counts. To survive failures, each store is backed by a **changelog topic** in Kafka: every state update is also written there (compacted). If an instance dies and its partitions move, the new instance **restores** the state by replaying the changelog. **Standby replicas** can pre-warm state to speed failover.\n\n```text\nStateful op -> update local RocksDB store -> also append to CHANGELOG topic (compacted)\nInstance fails -> partition moves -> new instance replays changelog to rebuild state.\nnum.standby.replicas > 0 -> warm standby -> faster failover (less restore time).\n```",tech:"kafka",displayNumber:274,sortKey:2446},{id:"kafka-2447",number:275,title:"What is a repartition topic and a changelog topic in Kafka Streams?",question:"What is a repartition topic and a changelog topic in Kafka Streams?",difficulty:"Hard",category:"Kafka Streams",answer:`- **Repartition topic**: an internal topic Streams creates when an operation (e.g. a key change before an aggregation/join) requires records to be **re-distributed by a new key** so downstream operations are correctly co-partitioned.
- **Changelog topic**: an internal compacted topic that **persists state-store updates** for fault-tolerant recovery (see Q274).

Both are managed automatically; they consume Kafka storage, so be mindful of them at scale.

\`\`\`text
Repartition topic: created on selectKey/groupBy re-keying -> ensures co-partitioning
Changelog topic  : persists state store -> enables recovery of aggregations/joins
Both internal & auto-managed (named <app-id>-<store/op>-...). Watch their storage.
\`\`\``,tech:"kafka",displayNumber:275,sortKey:2447},{id:"kafka-2448",number:276,title:"What is windowing (tumbling, hopping, session)?",question:"What is windowing (tumbling, hopping, session)?",difficulty:"Hard",category:"Kafka Streams",answer:`Windowing groups stream events into bounded time buckets for aggregation:
- **Tumbling**: fixed-size, **non-overlapping** windows (e.g. every 1 min). Each event in exactly one window.
- **Hopping**: fixed-size windows that **advance by a hop** smaller than the size, so they **overlap** (e.g. 5-min window every 1 min).
- **Session**: **dynamic** windows defined by activity separated by an **inactivity gap** — great for user sessions.

Handle late events with a grace period.

\`\`\`java
// Tumbling: count per 1-minute non-overlapping window
stream.groupByKey()
      .windowedBy(TimeWindows.ofSizeAndGrace(Duration.ofMinutes(1), Duration.ofSeconds(10)))
      .count();
// Hopping: .advanceBy(Duration.ofMinutes(1)) on a 5-min window -> overlapping
// Session: SessionWindows.ofInactivityGapWithNoGrace(Duration.ofMinutes(5))
\`\`\``,tech:"kafka",displayNumber:276,sortKey:2448},{id:"kafka-2449",number:277,title:"What is a stream-table join?",question:"What is a stream-table join?",difficulty:"Hard",category:"Kafka Streams",answer:'A **stream-table join** enriches each streaming record (KStream) with the current value from a table (KTable/GlobalKTable) for the same key — e.g. join an `orders` stream with a `customers` table to attach customer details. For KStream-KTable joins the inputs must be **co-partitioned** (same key, same partition count); GlobalKTable joins avoid that requirement (the table is fully replicated) and can join on a derived key. It\'s a lookup/enrichment at event time.\n\n```java\nKStream<String, Order> orders = builder.stream("orders");\nKTable<String, Customer> customers = builder.table("customers");   // co-partitioned\norders.join(customers, (order, cust) -> enrich(order, cust))       // enrich per event\n      .to("orders-enriched");\n// GlobalKTable variant: join on a derived key, no co-partitioning required.\n```',tech:"kafka",displayNumber:277,sortKey:2449},{id:"kafka-2450",number:278,title:"How does exactly-once work in Kafka Streams?",question:"How does exactly-once work in Kafka Streams?",difficulty:"Hard",category:"Kafka Streams",answer:"Set `processing.guarantee=exactly_once_v2` and Kafka Streams uses transactions under the hood to atomically coordinate: consuming input offsets, updating **state stores** (via changelog), and producing output — all commit together or not at all. So a failure/restart never double-counts an aggregation or emits duplicate output. It builds on the transactional producer + read_committed consumer, but the library manages it, making EOS essentially a one-line configuration.\n\n```java\nprops.put(StreamsConfig.PROCESSING_GUARANTEE_CONFIG,\n          StreamsConfig.EXACTLY_ONCE_V2);\n// Atomic across input offsets + state changelog + output -> no double aggregation,\n// no duplicate output, even across failures. Library handles the transactions.\n```",tech:"kafka",displayNumber:278,sortKey:2450},{id:"kafka-2451",number:279,title:"What is ksqlDB and how does it relate to Kafka Streams?",question:"What is ksqlDB and how does it relate to Kafka Streams?",difficulty:"Intermediate",category:"Kafka Streams",answer:`**ksqlDB** is a streaming SQL layer built on top of Kafka Streams: you define streams/tables and processing (filters, joins, aggregations, windows) with **SQL** instead of Java, running as a server you query. It's ideal for teams that want stream processing without writing/deploying JVM apps; under the hood it compiles to Kafka Streams topologies. Choose Kafka Streams for full programmatic control, ksqlDB for rapid, declarative pipelines.

\`\`\`sql
-- Stream processing as SQL (compiles to Kafka Streams under the hood)
CREATE STREAM big_orders AS
  SELECT customer_id, total
  FROM orders
  WHERE total > 100
  EMIT CHANGES;
\`\`\``,tech:"kafka",displayNumber:279,sortKey:2451},{id:"kafka-2452",number:280,title:"What is the difference between Kafka Streams and a stream processor like Flink/Spark?",question:"What is the difference between Kafka Streams and a stream processor like Flink/Spark?",difficulty:"Hard",category:"Kafka Streams",answer:`- **Kafka Streams**: a **library** embedded in your app, Kafka-to-Kafka, no separate cluster to run, scales by running more app instances. Simplest for Kafka-centric processing.
- **Flink/Spark Streaming**: standalone **distributed frameworks** with their own clusters/schedulers, richer for very large-scale, complex event-time processing, multi-source/multi-sink pipelines, and advanced state/windowing.

Use Streams for Kafka-native, operationally simple jobs; Flink/Spark for heavy, cross-system, or extremely large processing.

\`\`\`text
Kafka Streams: library, no cluster, Kafka<->Kafka, scale = more app instances.
Flink/Spark : dedicated cluster, multi-source/sink, advanced event-time/state,
              better for very large or complex pipelines.
Pick Streams for simplicity; Flink/Spark for scale/complexity beyond Kafka-only.
\`\`\``,tech:"kafka",displayNumber:280,sortKey:2452},{id:"kafka-2453",number:281,title:"How do you handle schema evolution safely in production?",question:"How do you handle schema evolution safely in production?",difficulty:"Hard",category:"Schema Registry",answer:`Use a Schema Registry with an enforced **compatibility mode** (commonly BACKWARD or FULL), make changes **additive** (add optional fields with defaults; avoid renames/removals/type changes), and version deliberately. Deploy in the order the compatibility mode allows (backward → consumers first; forward → producers first). Keep consumers tolerant of unknown/extra fields. Test schema changes in CI against the registry before rollout.

\`\`\`text
Rules for safe evolution:
  - enforce compatibility (BACKWARD/FULL) in the registry
  - additive changes only (new optional fields w/ defaults; no rename/remove/retype)
  - deploy order per mode (backward: consumers first; forward: producers first)
  - consumers tolerate unknown fields
  - validate schema changes in CI before rollout
\`\`\``,tech:"kafka",displayNumber:281,sortKey:2453},{id:"kafka-2454",number:282,title:"What is the Confluent Schema Registry's role in serialization?",question:"What is the Confluent Schema Registry's role in serialization?",difficulty:"Intermediate",category:"Schema Registry",answer:"The schema-aware serializer registers the writer's schema with the registry (getting a numeric **schema id**) and prepends `[magic byte][schema id]` to the payload — so the full schema isn't in every message. The deserializer reads the id, fetches (and caches) the schema, and decodes. This keeps messages compact, guarantees producer/consumer agreement, and lets the registry enforce compatibility centrally.\n\n```text\nSerialize:   payload = [0x00][4-byte schema id][avro/proto bytes]\nDeserialize: read id -> fetch+cache schema from registry -> decode\nBenefits: compact messages, guaranteed contract, central compatibility enforcement.\n```",tech:"kafka",displayNumber:282,sortKey:2454},{id:"kafka-2455",number:283,title:"What is a source connector offset and how does Connect track progress?",question:"What is a source connector offset and how does Connect track progress?",difficulty:"Hard",category:"Kafka Connect",answer:"Source connectors track their position in the **external** system (e.g. a DB binlog position or a file offset) using **source offsets** — stored by Connect in an internal `connect-offsets` topic (distributed mode). On restart/rebalance the task resumes from the last committed source offset, giving at-least-once ingestion. Sink connectors instead use normal Kafka **consumer group offsets** to track their position in Kafka topics.\n\n```text\nSource connector: position in external system (binlog/file/table cursor)\n                  -> stored in connect-offsets topic -> resume after restart.\nSink connector:   Kafka consumer-group offsets -> resume in the Kafka topic.\nBoth at-least-once by default -> design sinks to be idempotent (upsert/PK).\n```",tech:"kafka",displayNumber:283,sortKey:2455},{id:"kafka-2456",number:284,title:"How do you make a sink connector idempotent / exactly-once?",question:"How do you make a sink connector idempotent / exactly-once?",difficulty:"Hard",category:"Kafka Connect",answer:`Since Connect is at-least-once by default, make the **sink target** idempotent: use **upsert** with a primary key derived from the record key/fields (so replays overwrite rather than duplicate), or rely on the target's unique constraints. Some sinks support exactly-once via transactional writes or offset storage in the target. The JDBC sink's \`insert.mode=upsert\` + \`pk.mode\` is the classic idempotent pattern.

\`\`\`json
{
  "insert.mode": "upsert",
  "pk.mode": "record_key",        // dedupe by Kafka record key
  "pk.fields": "id"
}
// Replays overwrite the same PK -> no duplicate rows (idempotent sink).
\`\`\``,tech:"kafka",displayNumber:284,sortKey:2456},{id:"kafka-2457",number:285,title:"What is Kafka Streams' interactive queries feature?",question:"What is Kafka Streams' interactive queries feature?",difficulty:"Hard",category:"Kafka Streams",answer:`Interactive Queries let you **read the local state stores** of a running Kafka Streams app directly (e.g. serve a REST lookup of the latest aggregate for a key) instead of writing state back out to a topic and re-consuming it. Since state is partitioned across instances, you query the local store for keys this instance owns and use metadata to **route** requests to the instance that owns other keys. It turns a Streams app into a queryable, real-time materialized view.

\`\`\`java
// Query the materialized state store directly (real-time view)
ReadOnlyKeyValueStore<String, Long> store =
    streams.store(StoreQueryParameters.fromNameAndType("counts",
        QueryableStoreTypes.keyValueStore()));
Long count = store.get("user-42");
// Use streams.queryMetadataForKey(...) to route to the instance owning other keys.
\`\`\``,tech:"kafka",displayNumber:285,sortKey:2457},{id:"kafka-2458",number:286,title:"When would you use Kafka Connect vs writing a custom consumer/producer?",question:"When would you use Kafka Connect vs writing a custom consumer/producer?",difficulty:"Intermediate",category:"Kafka Connect",answer:"Use **Kafka Connect** for standard **integration** (move data between Kafka and common systems — DBs, S3, Elasticsearch, CDC) where a connector already exists: you get scaling, offset management, retries, and fault tolerance for free, as config. Write a **custom consumer/producer** when you need bespoke business logic, non-standard systems without a connector, or tight control over processing semantics. Prefer Connect for plumbing, custom code for logic.\n\n```text\nKafka Connect: standard data movement (DB<->Kafka, CDC, S3, ES) -> config, HA, offsets free.\nCustom client: bespoke business logic, unusual systems, precise custom semantics.\nRule: plumbing/integration -> Connect;  application logic -> custom code / Streams.\n```",tech:"kafka",displayNumber:286,sortKey:2458},{id:"kafka-2459",number:287,title:"Exercise — Streams: build a windowed count with exactly-once and expose it via interactive queries.",question:"Exercise — Streams: build a windowed count with exactly-once and expose it via interactive queries.",difficulty:"Hard",category:"Kafka Streams",answer:`Count events per key in tumbling windows with EOS enabled and materialize the result into a named state store, then serve the current counts via interactive queries. EOS ensures no double-counting on failure; the materialized store powers real-time lookups.

\`\`\`java
props.put(StreamsConfig.PROCESSING_GUARANTEE_CONFIG, StreamsConfig.EXACTLY_ONCE_V2);
StreamsBuilder b = new StreamsBuilder();
b.stream("clicks", Consumed.with(Serdes.String(), Serdes.String()))
 .groupByKey()
 .windowedBy(TimeWindows.ofSizeWithNoGrace(Duration.ofMinutes(1)))
 .count(Materialized.as("clicks-per-min"));      // named store for queries
KafkaStreams streams = new KafkaStreams(b.build(), props);
streams.start();
// Serve counts via interactive queries against the "clicks-per-min" store.
\`\`\``,tech:"kafka",displayNumber:287,sortKey:2459},{id:"kafka-2460",number:288,title:"How would you design an order-event system with Kafka?",question:"How would you design an order-event system with Kafka?",difficulty:"Hard",category:"System Design",answer:"Create an `orders` topic partitioned by **order/customer id** (so per-order events stay ordered), RF=3 with `min.insync.replicas=2`, and **idempotent producers** (`acks=all`). Each downstream capability (payment, inventory, shipping, analytics) is its own **consumer group**, so they consume independently and can replay. Add **retry topics + DLT** for failures, **idempotent consumers** for at-least-once safety, **schema governance** (Schema Registry) for contracts, and full **monitoring** (lag, under-replicated). Use the outbox pattern if orders originate in a database.\n\n```text\nTopic: orders (key=orderId, RF=3, min.insync=2), producer acks=all + idempotent\nConsumers (independent groups): payment | inventory | shipping | analytics\nReliability: retry topics + DLT, idempotent consumers, Schema Registry contracts\nOrigin in DB? -> outbox pattern + CDC. Monitor: lag, ISR, offline partitions.\n```",tech:"kafka",displayNumber:288,sortKey:2460},{id:"kafka-2461",number:289,title:"How would you guarantee ordering of events for a single order?",question:"How would you guarantee ordering of events for a single order?",difficulty:"Intermediate",category:"System Design",answer:`Use the **orderId as the Kafka key**, so all events for that order hash to the **same partition**, where Kafka guarantees offset order. A single consumer in the group owns that partition and processes its events sequentially. Keep the producer **idempotent** (so retries don't reorder), and avoid re-partitioning the topic later (which would break the key→partition mapping). Different orders spread across partitions for parallelism.

\`\`\`java
// All events for one order -> same partition -> ordered
kafka.send(new ProducerRecord<>("orders", order.getId(), new OrderPlaced(order)));
kafka.send(new ProducerRecord<>("orders", order.getId(), new OrderPaid(order)));
// enable.idempotence=true preserves order on retry; don't re-partition later.
\`\`\``,tech:"kafka",displayNumber:289,sortKey:2461},{id:"kafka-2462",number:290,title:"How would you prevent duplicate payment processing?",question:"How would you prevent duplicate payment processing?",difficulty:"Hard",category:"System Design",answer:`Since delivery is at-least-once, dedupe on an **idempotency key** (payment/event id) with a durable **uniqueness check**: attempt to insert the key; if it already exists, skip the charge and return the prior result. Alternatively use a transactional/EOS pipeline for Kafka-internal steps, but the external payment side still needs an idempotency key. Combine with \`acks=all\` + idempotent producer upstream so you don't create duplicate events in the first place.

\`\`\`java
@KafkaListener(topics = "payments", groupId = "payment-processor")
void handle(PaymentCommand cmd) {
    boolean first = payments.recordIfNew(cmd.idempotencyKey());  // unique constraint
    if (first) gateway.charge(cmd);                              // charge once only
    // duplicate delivery -> recordIfNew=false -> no second charge.
}
\`\`\``,tech:"kafka",displayNumber:290,sortKey:2462},{id:"kafka-2463",number:291,title:"How would you handle a failed consumer in production?",question:"How would you handle a failed consumer in production?",difficulty:"Intermediate",category:"System Design",answer:"Rely on **consumer group coordination**: when a consumer dies, the coordinator times it out and **reassigns** its partitions to healthy members, which resume from the last **committed offset**. Design for this: at-least-once + idempotent processing (so reprocessing is safe), bounded retries + DLT for poison records, static membership/cooperative rebalancing to minimize disruption, health checks/auto-restart (k8s), and alerting on lag and rebalance rate.\n\n```text\nConsumer fails -> coordinator reassigns partitions -> peers resume from committed offset.\nMake it safe: idempotent processing, retries + DLT, static membership + cooperative assignor,\nk8s liveness/restart, alert on lag & rebalance frequency.\n```",tech:"kafka",displayNumber:291,sortKey:2463},{id:"kafka-2464",number:292,title:"How would you design retry topics with backoff?",question:"How would you design retry topics with backoff?",difficulty:"Hard",category:"System Design",answer:'Use **tiered retry topics** with increasing delays (e.g. `orders.retry.5s`, `orders.retry.1m`, `orders.retry.10m`): a failed record is republished to the next retry tier; a consumer for that tier waits the delay (or uses a scheduler) before reprocessing. After exhausting tiers, route to the **DLT**. Track attempt count in headers. Spring Kafka\'s `@RetryableTopic` automates this (non-blocking retries) so a poison record doesn\'t block the main partition.\n\n```java\n// Spring Kafka non-blocking retries -> auto-creates retry topics + DLT\n@RetryableTopic(attempts = "4",\n    backoff = @Backoff(delay = 5000, multiplier = 3.0),   // 5s, 15s, 45s\n    dltStrategy = DltStrategy.FAIL_ON_ERROR)\n@KafkaListener(topics = "orders", groupId = "fulfillment")\nvoid handle(Order o) { process(o); }   // failures flow to retry topics then DLT\n```',tech:"kafka",displayNumber:292,sortKey:2464},{id:"kafka-2465",number:293,title:"What is a DLT replay strategy?",question:"What is a DLT replay strategy?",difficulty:"Hard",category:"System Design",answer:`A DLT replay is the controlled reprocessing of dead-lettered records after the root cause is fixed. Steps: **inspect** the failures (headers carry original topic/offset/error), **fix** the underlying bug/data or a downstream dependency, **validate** the records, then **republish** selected records to the original (or a dedicated recovery) topic with **safeguards** — deduplication (idempotency), rate limiting (don't flood), and monitoring. Never blindly replay everything; curate and throttle.

\`\`\`text
1) Inspect DLT records (orig-topic/offset/error headers)
2) Fix root cause (bug/data/downstream)
3) Validate records
4) Republish selected records to source/recovery topic
   with: idempotency (dedupe), rate limiting, monitoring, rollback plan.
\`\`\``,tech:"kafka",displayNumber:293,sortKey:2465},{id:"kafka-2466",number:294,title:"How do you choose the partition count for a topic?",question:"How do you choose the partition count for a topic?",difficulty:"Hard",category:"System Design",answer:`Estimate from: target **throughput** ÷ per-partition throughput, required **consumer parallelism** (partitions ≥ max consumers), **broker capacity** (partitions per broker limits), expected **growth**, **recovery/rebalance** cost (more partitions = slower), and **ordering** needs (keys spread across partitions). Pick the max of the throughput- and parallelism-driven numbers, add headroom, but avoid excessive counts. Remember you can increase but not easily decrease, and increasing changes key mapping.

\`\`\`text
partitions = max( throughput_target / per_partition_throughput , consumers_needed )
             + growth headroom
Constraints: broker partition limits, recovery/rebalance time, key-ordering effects.
Increase possible (breaks key mapping); decrease NOT supported -> size with care.
\`\`\``,tech:"kafka",displayNumber:294,sortKey:2466},{id:"kafka-2467",number:295,title:"Can partition count be increased or decreased, and what are the effects?",question:"Can partition count be increased or decreased, and what are the effects?",difficulty:"Intermediate",category:"System Design",answer:"- **Increase**: supported (`kafka-topics.sh --alter --partitions N`), but it **changes `hash(key) % partitions`**, so existing keyed records stay in old partitions while new ones map differently — **breaking per-key ordering** across the change and re-skewing distribution.\n- **Decrease**: **not supported** in place; you must create a new topic with fewer partitions and **migrate/replicate** data, then switch producers/consumers.\n\nPlan partitions up front to avoid both problems.\n\n```bash\n# Increase (careful: re-hashes keys, breaks per-key ordering going forward)\nkafka-topics.sh --alter --topic orders --partitions 24 --bootstrap-server b:9092\n# Decrease: NOT supported -> create new topic + migrate data + cut over.\n```",tech:"kafka",displayNumber:295,sortKey:2467},{id:"kafka-2468",number:296,title:"How would you design Kafka for 1 million events per second?",question:"How would you design Kafka for 1 million events per second?",difficulty:"Hard",category:"System Design",answer:"Partition heavily for parallelism, spread across many brokers on fast disks/NICs, and tune the pipeline: producer **batching + compression** (lz4/zstd), large `buffer.memory`, adequate `linger.ms`; consumers scaled to partition count with big fetches; RF=3 with enough replication throughput; sufficient `num.io.threads`/network threads; and OS/page-cache-friendly config. Size CPU/network/disk to the sustained rate with headroom, and **benchmark** (`kafka-producer/consumer-perf-test`) before production. Consider tiered storage for long retention.\n\n```text\n1M events/s blueprint:\n  - many partitions across many brokers (parallelism)\n  - producers: batch + zstd/lz4 compression, big buffer, linger.ms tuned\n  - consumers scaled to #partitions, large fetches\n  - RF=3, enough replica-fetch + io/network threads\n  - fast NVMe, big page cache, sized network\n  - BENCHMARK before prod; tiered storage for long retention.\n```",tech:"kafka",displayNumber:296,sortKey:2468},{id:"kafka-2469",number:297,title:"How do you secure Kafka (encryption, authentication, authorization)?",question:"How do you secure Kafka (encryption, authentication, authorization)?",difficulty:"Hard",category:"Security",answer:`Three layers:
- **Encryption in transit**: **TLS** on broker listeners (and inter-broker) so data on the wire is encrypted.
- **Authentication**: **SASL** (SCRAM, GSSAPI/Kerberos, OAUTHBEARER) or **mTLS** to verify client/broker identity.
- **Authorization**: **ACLs** granting specific principals rights on specific resources (topics, groups) with least privilege.

Add network isolation, secrets management, quotas, and audit logging. Never run production Kafka open/unauthenticated.

\`\`\`properties
# Broker: TLS + SASL/SCRAM
listeners=SASL_SSL://:9093
security.inter.broker.protocol=SASL_SSL
sasl.enabled.mechanisms=SCRAM-SHA-512
ssl.keystore.location=/certs/broker.keystore.jks
authorizer.class.name=org.apache.kafka.metadata.authorizer.StandardAuthorizer
# + ACLs per principal, least privilege.
\`\`\``,tech:"kafka",displayNumber:297,sortKey:2469},{id:"kafka-2470",number:298,title:"What is a Kafka ACL and how do you apply least privilege?",question:"What is a Kafka ACL and how do you apply least privilege?",difficulty:"Intermediate",category:"Security",answer:"An **ACL (Access Control List)** rule defines that a **principal** (authenticated identity) is Allowed/Denied a specific **operation** (Read, Write, Describe, Create…) on a **resource** (topic, consumer group, cluster), optionally scoped by host. Least privilege means granting each service only what it needs — e.g. the order service can **Write** `orders` and **Read** its own group, nothing else. Manage with `kafka-acls.sh`.\n\n```bash\n# Allow only the payment service to READ 'orders' via its group\nkafka-acls.sh --bootstrap-server b:9093 --add \\\n  --allow-principal User:payment-svc \\\n  --operation Read --topic orders \\\n  --group payment-processor\n# Grant only what each principal needs (Write/Read/Describe on specific resources).\n```",tech:"kafka",displayNumber:298,sortKey:2470},{id:"kafka-2471",number:299,title:"What is the difference between SASL, TLS, and mTLS in Kafka?",question:"What is the difference between SASL, TLS, and mTLS in Kafka?",difficulty:"Hard",category:"Security",answer:`- **TLS**: encrypts the connection and authenticates the **server** to the client (broker identity + confidentiality/integrity).
- **SASL**: an **authentication** framework for the **client** (mechanisms: SCRAM = username/password hashed, GSSAPI = Kerberos, OAUTHBEARER = tokens); usually layered over TLS (\`SASL_SSL\`).
- **mTLS (mutual TLS)**: both client **and** server present certificates, so TLS itself authenticates the client — an alternative to SASL for client identity.

Common setups: \`SASL_SSL\` (SASL auth + TLS encryption) or mTLS.

\`\`\`text
TLS   : encrypt + authenticate the SERVER (broker)
SASL  : authenticate the CLIENT (SCRAM/Kerberos/OAuth), usually over TLS = SASL_SSL
mTLS  : both sides present certs -> TLS authenticates the client too (no SASL needed)
Pick SASL_SSL or mTLS depending on your identity/PKI strategy.
\`\`\``,tech:"kafka",displayNumber:299,sortKey:2471},{id:"kafka-2472",number:300,title:"How would you migrate from RabbitMQ to Kafka?",question:"How would you migrate from RabbitMQ to Kafka?",difficulty:"Hard",category:"Migration",answer:`Map RabbitMQ **queues/exchanges** to Kafka **topics/partitions**, redesigning routing (RabbitMQ's rich exchange routing → topic + key + consumer-side filtering). Establish **consumer groups**, preserve **ordering** requirements via keys, and handle at-least-once + idempotency differences. Migrate incrementally: **dual-run or bridge** (a connector/relay copies messages both ways), validate semantics and throughput, then **cut over** producers and consumers gradually with rollback capability. Don't forget DLQ→DLT and monitoring parity.

\`\`\`text
RabbitMQ -> Kafka mapping:
  queue/exchange -> topic (+ partitions/keys); routing keys -> topic/key + filtering
  consumers -> consumer groups; DLQ -> DLT
Strategy: bridge/dual-run -> validate ordering/throughput/semantics -> gradual cutover
          -> keep rollback path. Add idempotency for at-least-once.
\`\`\``,tech:"kafka",displayNumber:300,sortKey:2472},{id:"kafka-2473",number:301,title:"How would you migrate Kafka topics safely (repartition / cluster move)?",question:"How would you migrate Kafka topics safely (repartition / cluster move)?",difficulty:"Hard",category:"Migration",answer:`Create the **target topic** (new partition count/config or new cluster), **replicate/copy** data (MirrorMaker 2 for cross-cluster, or a stream job), **validate** offsets, schema compatibility, and ordering requirements, then **switch producers and consumers gradually** — often producing to both during a transition — while retaining a **rollback** path. For partition-count changes, remember key mapping changes, so plan for consumers to handle the transition. Verify lag and data completeness before decommissioning the source.

\`\`\`text
1) create target topic/cluster (new config)
2) replicate data (MirrorMaker 2 / stream copy)
3) validate offsets, schema, ordering, completeness
4) switch producers/consumers gradually (dual-produce during transition)
5) keep rollback; decommission source only after verification.
\`\`\``,tech:"kafka",displayNumber:301,sortKey:2473},{id:"kafka-2474",number:302,title:"What is MirrorMaker 2 and when do you use it?",question:"What is MirrorMaker 2 and when do you use it?",difficulty:"Hard",category:"Migration",answer:`**MirrorMaker 2** (built on Kafka Connect) replicates **topics and consumer-group offsets** between Kafka clusters, with topic renaming/prefixing and offset translation. Use it for **cross-datacenter/region replication**, **disaster recovery** (active-passive or active-active), **cluster migration**, or aggregating data from many clusters into one. It handles the plumbing (offset sync so consumers can fail over to the target cluster near their last position).

\`\`\`text
MirrorMaker 2 (on Kafka Connect):
  replicates topics + consumer-group offsets across clusters, with offset translation.
Use for: DR (active-passive/active-active), cross-region, migration, aggregation.
Renames topics (e.g. source-cluster.orders) to avoid collisions.
\`\`\``,tech:"kafka",displayNumber:302,sortKey:2474},{id:"kafka-2475",number:303,title:"Why run multiple Kafka clusters?",question:"Why run multiple Kafka clusters?",difficulty:"Intermediate",category:"System Design",answer:`Reasons: **disaster recovery** (a standby cluster in another region), **geographic distribution** (low-latency local clusters + replication), **isolation** (separate workloads/teams/tenants, or prod vs analytics to protect SLAs), **compliance/data residency**, and **independent scaling/upgrades**. The trade-off is operational complexity and cross-cluster replication (MirrorMaker 2) with its own lag/consistency considerations. Prefer one cluster until a concrete driver justifies more.

\`\`\`text
Multiple clusters for: DR, geo-distribution, workload/tenant isolation,
                       compliance/data residency, independent scaling/upgrades.
Cost: cross-cluster replication (MM2), operational overhead.
Default to one cluster until a real driver appears.
\`\`\``,tech:"kafka",displayNumber:303,sortKey:2475},{id:"kafka-2476",number:304,title:"What is disaster recovery in Kafka (RPO/RTO)?",question:"What is disaster recovery in Kafka (RPO/RTO)?",difficulty:"Hard",category:"System Design",answer:`DR is a tested strategy to restore Kafka service/data after a cluster or region failure, built on cross-region **replication** (MirrorMaker 2), backups, and infrastructure automation. It's defined by two objectives:
- **RPO (Recovery Point Objective)**: max acceptable **data loss** (in time/events) — driven by replication lag.
- **RTO (Recovery Time Objective)**: max acceptable **downtime** to restore service — driven by failover automation.

Design (active-passive vs active-active), replicate offsets so consumers resume near their position, and **regularly test** failover.

\`\`\`text
DR = replicate (MM2) + backups + automation + tested failover.
RPO: how much data you can lose (bounded by replication lag) -> smaller = more replication.
RTO: how fast you recover (bounded by failover automation).
Replicate offsets so consumers resume near their last position. Test DR regularly.
\`\`\``,tech:"kafka",displayNumber:304,sortKey:2476},{id:"kafka-2477",number:305,title:"What is RPO vs RTO?",question:"What is RPO vs RTO?",difficulty:"Intermediate",category:"System Design",answer:`- **RPO (Recovery Point Objective)**: the maximum acceptable amount of **data loss**, measured in time or events — "we can afford to lose at most 5 minutes of data." Lower RPO needs more frequent/synchronous replication.
- **RTO (Recovery Time Objective)**: the maximum acceptable **time to restore** service after a failure — "we must be back within 15 minutes." Lower RTO needs more automation and warm standby.

RPO is about data; RTO is about time. Both drive DR design and cost.

\`\`\`text
RPO = data loss tolerance (time/events)  -> replication frequency/lag
RTO = downtime tolerance (time)          -> failover automation / warm standby
Tighter RPO/RTO = more cost. Define both explicitly per system criticality.
\`\`\``,tech:"kafka",displayNumber:305,sortKey:2477},{id:"kafka-2478",number:306,title:"How do you handle a Kafka broker running out of disk?",question:"How do you handle a Kafka broker running out of disk?",difficulty:"Hard",category:"System Design",answer:`A full disk causes **write failures**, replication stalls, and broker instability — a serious incident. Prevent it: **alert early** on disk %/free space, right-size **retention** (time/size) so data ages out, spread partitions (avoid hot brokers via reassignment), and provision headroom. To recover: reduce retention temporarily, delete/reassign partitions to other brokers, add capacity, or (last resort) remove old segments carefully. Consider **tiered storage** to offload old data and shrink local footprint.

\`\`\`text
Prevent: alert on disk free%, tune retention.ms/bytes, balance partitions, headroom.
Recover: lower retention, reassign partitions off the hot broker, add disk/brokers.
Long-term: tiered storage offloads old segments to object storage.
Full disk -> write failures + instability -> treat as a real incident.
\`\`\``,tech:"kafka",displayNumber:306,sortKey:2478},{id:"kafka-2479",number:307,title:"How do you secure Kafka topics with least privilege in a multi-team cluster?",question:"How do you secure Kafka topics with least privilege in a multi-team cluster?",difficulty:"Hard",category:"Security",answer:`Give each team/service a distinct **authenticated principal** (SASL/mTLS), grant **topic-scoped ACLs** (Write to topics they own, Read to topics they consume, Describe as needed) and **group-scoped** ACLs for their consumer groups, and deny by default. Use **prefixed topic naming** + ACL patterns for whole domains, enforce **schema governance** for shared topics, apply **quotas** to prevent noisy neighbors, and audit access. This isolates teams and limits blast radius.

\`\`\`bash
# Team owns 'team-a.*' topics: write to own, read via own groups; default deny
kafka-acls.sh --add --allow-principal User:team-a \\
  --operation Write --operation Describe \\
  --resource-pattern-type prefixed --topic team-a.
kafka-acls.sh --add --allow-principal User:team-a \\
  --operation Read --resource-pattern-type prefixed --group team-a.
# + quotas per principal, schema governance for shared topics, audit logging.
\`\`\``,tech:"kafka",displayNumber:307,sortKey:2479},{id:"kafka-2480",number:308,title:"How would you handle poison messages in a production pipeline?",question:"How would you handle poison messages in a production pipeline?",difficulty:"Intermediate",category:"System Design",answer:`Detect repeated failures on a record, apply **bounded retries** with backoff (via retry topics or a retry framework), then **isolate** it to a **dead-letter topic** so it doesn't block the partition (avoid infinite inline retries that stall consumption and grow lag). Attach failure context, **alert**, and provide a **replay/remediation** path once the cause is fixed. Combine with idempotent processing so retries/replays are safe.

\`\`\`text
Poison record -> bounded retries (backoff via retry topics)
             -> exhausted? route to DLT (don't block the partition)
             -> alert + attach context (offset/error) + replay after fix.
Never infinite inline retry (stalls partition, grows lag). Keep processing idempotent.
\`\`\``,tech:"kafka",displayNumber:308,sortKey:2480},{id:"kafka-2481",number:309,title:"How do you monitor and alert on a production Kafka cluster?",question:"How do you monitor and alert on a production Kafka cluster?",difficulty:"Intermediate",category:"System Design",answer:`Export broker/client **JMX metrics** to Prometheus (via JMX exporter) and visualize in Grafana. Alert on: **consumer lag** trend, **under-replicated** and **offline** partitions, **ISR shrink** rate, **controller** changes, **request latency/queue time**, **disk** free/latency, **CPU/GC**, and **error rates**. Add consumer-lag exporters (Burrow/kafka-lag-exporter) and broker health checks. Alert on user-facing symptoms plus leading indicators, with runbooks per alert.

\`\`\`text
Pipeline: broker/client JMX -> Prometheus (JMX exporter) -> Grafana + Alertmanager.
Alert on: lag trend, UnderReplicated/Offline partitions, ISR shrinks, controller changes,
          request latency/queue time, disk free, CPU/GC, error rates.
Add: kafka-lag-exporter/Burrow, health checks, per-alert runbooks.
\`\`\``,tech:"kafka",displayNumber:309,sortKey:2481},{id:"kafka-2482",number:310,title:"How do you perform a rolling upgrade of a Kafka cluster with zero downtime?",question:"How do you perform a rolling upgrade of a Kafka cluster with zero downtime?",difficulty:"Hard",category:"System Design",answer:"Upgrade **one broker at a time**: ensure all partitions are fully replicated (no under-replicated partitions) and leadership can move off the broker, then stop, upgrade, restart, wait for it to rejoin the ISR and re-balance leadership before moving to the next. Follow version-specific steps for `inter.broker.protocol.version` / `log.message.format.version` (bump only after all brokers are upgraded). RF≥3 and `min.insync.replicas=2` keep partitions available throughout. Monitor URP/lag between steps.\n\n```text\nPer broker, one at a time:\n  1) confirm 0 under-replicated partitions\n  2) (leadership moves off automatically) stop -> upgrade -> start\n  3) wait: rejoin ISR + leadership rebalance -> then next broker\nBump inter.broker.protocol.version AFTER all brokers upgraded.\nRF>=3 + min.insync=2 keep it available. Watch URP/lag between steps.\n```",tech:"kafka",displayNumber:310,sortKey:2482},{id:"kafka-2483",number:311,title:"How do you troubleshoot under-replicated partitions?",question:"How do you troubleshoot under-replicated partitions?",difficulty:"Hard",category:"System Design",answer:"Under-replicated partitions mean followers can't keep up. Check the affected **broker(s)** health: **disk latency/space**, **network** saturation, **CPU/GC** pauses, **replication traffic** (throttles too low, too few `num.replica.fetchers`), failed disks, and recent broker events (restarts, reassignments). Correlate with `IsrShrinksPerSec` and controller logs. Fix the bottleneck (faster disk/network, more fetchers, remove throttle, replace failed disk) so followers rejoin the ISR.\n\n```text\nURP > 0 -> follower(s) lagging. Check on the affected brokers:\n  disk latency/space, network saturation, CPU/GC, replication throttle,\n  num.replica.fetchers, failed disks, recent restarts/reassignments.\nCorrelate: IsrShrinksPerSec, controller logs. Fix bottleneck -> ISR recovers.\n```",tech:"kafka",displayNumber:311,sortKey:2483},{id:"kafka-2484",number:312,title:"How would you explain Kafka end-to-end in an SDE-2 / system-design interview?",question:"How would you explain Kafka end-to-end in an SDE-2 / system-design interview?",difficulty:"Intermediate",category:"System Design",answer:`Give the flow and the trade-offs: producers write keyed records to a **topic's partitions** (leader broker), which are **replicated** to followers (ISR) for durability; **consumer groups** read partitions (one owner per partition) tracking **offsets**, enabling independent, replayable consumption. Cover **ordering** (per partition), **delivery semantics** (at-least-once + idempotency, or EOS/transactions), **failure handling** (leader election, rebalancing, DLT), and **production trade-offs** (partition count, RF/min.insync, retention, monitoring, security). Anchor with a concrete example (order pipeline).

\`\`\`text
Story arc for the interview:
  producer -> keyed record -> partition leader -> replicate to ISR (durability)
  consumer group -> one owner/partition -> offsets -> independent, replayable reads
  ordering per partition; delivery = at-least-once+idempotency or EOS
  failures: leader election, rebalancing, retry+DLT
  trade-offs: partitions, RF/min.insync, retention, monitoring, security
  ground it in an example (order-event pipeline).
\`\`\``,tech:"kafka",displayNumber:312,sortKey:2484},{id:"design-patterns-2485",number:1,title:"What is the Singleton pattern and how do you implement it correctly?",question:"What is the Singleton pattern and how do you implement it correctly?",difficulty:"Basic",category:"Creational Patterns",answer:`Singleton ensures a class has **exactly one instance** and provides a global access point to it — used for shared resources like configuration, logging, connection pools, or caches. The correct, thread-safe, lazy, reflection- and serialization-safe implementation in Java is an **enum** (Effective Java). If you need a class, use the **static holder idiom** (lazy + thread-safe without synchronization) or a double-checked-locking \`volatile\` field. Avoid naive lazy init (not thread-safe) and beware that singletons are effectively global state (hard to test/mock).

\`\`\`java
// Best: enum singleton — thread-safe, serialization/reflection-proof
enum Config {
    INSTANCE;
    private final Properties props = load();
    public String get(String k) { return props.getProperty(k); }
    private Properties load() { /* ... */ return new Properties(); }
}
// Usage: Config.INSTANCE.get("db.url");

// Alternative: lazy holder idiom (thread-safe, no locking)
class Logger {
    private Logger() {}
    private static class Holder { static final Logger I = new Logger(); }
    public static Logger get() { return Holder.I; }
}
\`\`\``,tech:"design-patterns",displayNumber:1,sortKey:2485},{id:"design-patterns-2486",number:2,title:"Explain the Factory Method pattern.",question:"Explain the Factory Method pattern.",difficulty:"Basic",category:"Creational Patterns",answer:`Factory Method defines an interface for creating an object but lets subclasses (or a factory method) decide **which concrete class** to instantiate. It decouples client code from concrete types — the client depends on an abstraction and asks a factory for an instance, so adding a new product doesn't change client code (Open/Closed). Use it when a class can't anticipate the object type it must create, or to centralize/standardize object creation.

\`\`\`java
interface Notification { void send(String msg); }
class Email implements Notification { public void send(String m){ /*...*/ } }
class Sms   implements Notification { public void send(String m){ /*...*/ } }
class Push  implements Notification { public void send(String m){ /*...*/ } }

class NotificationFactory {
    static Notification create(String type) {           // one place decides the type
        return switch (type) {
            case "email" -> new Email();
            case "sms"   -> new Sms();
            case "push"  -> new Push();
            default -> throw new IllegalArgumentException(type);
        };
    }
}
// Client depends only on the interface:
Notification n = NotificationFactory.create("sms"); n.send("hi");
\`\`\``,tech:"design-patterns",displayNumber:2,sortKey:2486},{id:"design-patterns-2487",number:3,title:"What is the Abstract Factory pattern and how does it differ from Factory Method?",question:"What is the Abstract Factory pattern and how does it differ from Factory Method?",difficulty:"Intermediate",category:"Creational Patterns",answer:`Abstract Factory provides an interface to create **families of related objects** without specifying their concrete classes — e.g. a \`WindowsFactory\` produces a matching \`WindowsButton\` + \`WindowsCheckbox\`, a \`MacFactory\` produces Mac variants. It guarantees the products used together are compatible. Difference: **Factory Method** creates one product via inheritance (a method); **Abstract Factory** creates several related products via composition (an object with multiple create methods). Use Abstract Factory when your system must be independent of how a whole family of products is created.

\`\`\`java
interface Button   { void render(); }
interface Checkbox { void render(); }

interface GUIFactory {            // creates a FAMILY of related widgets
    Button createButton();
    Checkbox createCheckbox();
}
class WindowsFactory implements GUIFactory {
    public Button createButton()     { return new WindowsButton(); }
    public Checkbox createCheckbox() { return new WindowsCheckbox(); }
}
class MacFactory implements GUIFactory {
    public Button createButton()     { return new MacButton(); }
    public Checkbox createCheckbox() { return new MacCheckbox(); }
}
// Client gets a consistent family; no mixing Win button with Mac checkbox.
GUIFactory f = isWindows ? new WindowsFactory() : new MacFactory();
f.createButton().render(); f.createCheckbox().render();
\`\`\``,tech:"design-patterns",displayNumber:3,sortKey:2487},{id:"design-patterns-2488",number:4,title:"Explain the Builder pattern and when to use it.",question:"Explain the Builder pattern and when to use it.",difficulty:"Intermediate",category:"Creational Patterns",answer:`Builder constructs a complex object step by step, separating construction from representation. It's ideal when a class has **many (especially optional) parameters** — avoiding the "telescoping constructor" anti-pattern and confusing positional args. The builder exposes fluent, named setters and a \`build()\` that returns an immutable object, and can validate invariants before construction. Use it for objects with many optional fields or that must be immutable.

\`\`\`java
class Pizza {
    private final int size; private final boolean cheese, pepperoni, mushroom;
    private Pizza(Builder b){ size=b.size; cheese=b.cheese; pepperoni=b.pepperoni; mushroom=b.mushroom; }

    static class Builder {
        private final int size;                 // required
        private boolean cheese, pepperoni, mushroom;  // optional
        Builder(int size){ this.size = size; }
        Builder cheese(boolean v){ this.cheese=v; return this; }      // fluent
        Builder pepperoni(boolean v){ this.pepperoni=v; return this; }
        Builder mushroom(boolean v){ this.mushroom=v; return this; }
        Pizza build(){
            if (size <= 0) throw new IllegalStateException("bad size"); // validate
            return new Pizza(this);
        }
    }
}
Pizza p = new Pizza.Builder(12).cheese(true).mushroom(true).build();
\`\`\``,tech:"design-patterns",displayNumber:4,sortKey:2488},{id:"design-patterns-2489",number:5,title:"What is the Prototype pattern?",question:"What is the Prototype pattern?",difficulty:"Intermediate",category:"Creational Patterns",answer:`Prototype creates new objects by **cloning an existing instance** (the prototype) instead of instantiating from scratch. It's useful when object creation is expensive (heavy initialization, DB/network load) or when you need many similar objects — you configure one and copy it. Key concern: **deep vs shallow copy** (nested mutable references must be deep-copied to avoid shared-state bugs). In Java you can implement \`Cleanable\`/\`clone()\` or a copy constructor.

\`\`\`java
interface Prototype<T> { T copy(); }

class Document implements Prototype<Document> {
    private String title;
    private List<String> sections;                 // mutable -> must deep copy
    Document(String t, List<String> s){ title=t; sections=s; }
    public Document copy() {
        return new Document(title, new ArrayList<>(sections)); // DEEP copy
    }
}
Document template = new Document("Report", List.of("Intro"));
Document copy = template.copy();   // cheap clone instead of rebuilding from scratch
\`\`\``,tech:"design-patterns",displayNumber:5,sortKey:2489},{id:"design-patterns-2490",number:6,title:"Explain the difference between the main Creational patterns (when to pick which).",question:"Explain the difference between the main Creational patterns (when to pick which).",difficulty:"Intermediate",category:"Creational Patterns",answer:`- **Singleton**: exactly one shared instance.
- **Factory Method**: pick one concrete product behind an interface (subclass/method decides).
- **Abstract Factory**: create a whole **family** of compatible products.
- **Builder**: assemble a complex object step-by-step (many optional fields, immutability).
- **Prototype**: clone an existing configured instance (expensive creation).

Rule: many params → Builder; one-of-many types → Factory Method; families → Abstract Factory; expensive-to-create → Prototype; single global instance → Singleton.

\`\`\`text
Need                                   -> Pattern
one shared instance                    -> Singleton
choose a concrete type via interface   -> Factory Method
a family of related objects            -> Abstract Factory
complex object, many optional fields   -> Builder
copy a costly-to-build instance        -> Prototype
\`\`\``,tech:"design-patterns",displayNumber:6,sortKey:2490},{id:"design-patterns-2491",number:7,title:"Explain the Adapter pattern.",question:"Explain the Adapter pattern.",difficulty:"Basic",category:"Structural Patterns",answer:`Adapter converts the interface of a class into another interface the client expects, letting incompatible interfaces work together — like a plug adapter. Use it to integrate a third-party/legacy class whose API you can't change into your codebase's expected interface. It wraps the adaptee and translates calls. (In DDD, an anti-corruption layer is an adapter at a service boundary.)

\`\`\`java
// Target interface our app expects
interface PaymentGateway { boolean pay(long cents); }

// Incompatible third-party class we can't modify
class StripeApi { void charge(double dollars, String cur) { /* ... */ } }

// Adapter translates our interface -> the third-party API
class StripeAdapter implements PaymentGateway {
    private final StripeApi stripe = new StripeApi();
    public boolean pay(long cents) {
        stripe.charge(cents / 100.0, "USD");   // translate call/units
        return true;
    }
}
PaymentGateway gw = new StripeAdapter();   // app code stays gateway-agnostic
\`\`\``,tech:"design-patterns",displayNumber:7,sortKey:2491},{id:"design-patterns-2492",number:8,title:"What is the Bridge pattern?",question:"What is the Bridge pattern?",difficulty:"Hard",category:"Structural Patterns",answer:`Bridge decouples an **abstraction** from its **implementation** so the two can vary independently, replacing a combinatorial explosion of subclasses with composition. Instead of \`RedCircle, BlueCircle, RedSquare, BlueSquare…\`, you have a \`Shape\` abstraction holding a \`Color\` implementor. Adding a shape or a color is then linear, not multiplicative. Use it when both dimensions change independently.

\`\`\`java
interface Color { String fill(); }                 // implementation side
class Red  implements Color { public String fill(){ return "red"; } }
class Blue implements Color { public String fill(){ return "blue"; } }

abstract class Shape {                              // abstraction side
    protected final Color color;                    // bridge (composition)
    Shape(Color c){ this.color = c; }
    abstract String draw();
}
class Circle extends Shape {
    Circle(Color c){ super(c); }
    String draw(){ return "Circle in " + color.fill(); }
}
// New shape OR new color added independently — no NxM subclasses.
Shape s = new Circle(new Blue());
\`\`\``,tech:"design-patterns",displayNumber:8,sortKey:2492},{id:"design-patterns-2493",number:9,title:"Explain the Composite pattern.",question:"Explain the Composite pattern.",difficulty:"Intermediate",category:"Structural Patterns",answer:`Composite lets you treat individual objects (leaves) and compositions of objects (nodes) **uniformly** through a common interface, forming a tree structure. Clients call the same method on a single item or a whole subtree without knowing the difference. Ideal for hierarchies: file systems (files/folders), UI component trees, org charts, menus.

\`\`\`java
interface FileNode { int size(); }                 // common interface

class File implements FileNode {                    // leaf
    private final int size;
    File(int s){ size=s; }
    public int size(){ return size; }
}
class Folder implements FileNode {                  // composite
    private final List<FileNode> children = new ArrayList<>();
    void add(FileNode n){ children.add(n); }
    public int size(){ return children.stream().mapToInt(FileNode::size).sum(); }
}
Folder root = new Folder();
root.add(new File(10)); root.add(new File(20));
root.size();   // 30 — same call works on file or folder subtree
\`\`\``,tech:"design-patterns",displayNumber:9,sortKey:2493},{id:"design-patterns-2494",number:10,title:"Explain the Decorator pattern.",question:"Explain the Decorator pattern.",difficulty:"Intermediate",category:"Structural Patterns",answer:`Decorator dynamically **adds responsibilities** to an object by wrapping it in a decorator that implements the same interface and delegates to the wrapped object, adding behavior before/after. It's a flexible alternative to subclassing for extending behavior — you can stack decorators in any combination at runtime (Open/Closed). Java I/O (\`BufferedInputStream\` wrapping \`FileInputStream\`) is the classic example.

\`\`\`java
interface Coffee { double cost(); String desc(); }
class Espresso implements Coffee {
    public double cost(){ return 2.0; } public String desc(){ return "espresso"; }
}
abstract class CoffeeDecorator implements Coffee {
    protected final Coffee inner;                   // same interface, wraps one
    CoffeeDecorator(Coffee c){ inner = c; }
}
class Milk extends CoffeeDecorator {
    Milk(Coffee c){ super(c); }
    public double cost(){ return inner.cost() + 0.5; }        // add behavior
    public String desc(){ return inner.desc() + "+milk"; }
}
class Sugar extends CoffeeDecorator {
    Sugar(Coffee c){ super(c); }
    public double cost(){ return inner.cost() + 0.2; }
    public String desc(){ return inner.desc() + "+sugar"; }
}
// Stack decorators at runtime:
Coffee c = new Sugar(new Milk(new Espresso()));   // cost 2.7, "espresso+milk+sugar"
\`\`\``,tech:"design-patterns",displayNumber:10,sortKey:2494},{id:"design-patterns-2495",number:11,title:"Explain the Facade pattern.",question:"Explain the Facade pattern.",difficulty:"Basic",category:"Structural Patterns",answer:`Facade provides a **simple, unified interface** to a complex subsystem, hiding its internal classes and interactions behind one entry point. Clients use the easy facade instead of orchestrating many low-level objects, reducing coupling and cognitive load. It doesn't add functionality — it simplifies access. Example: an \`OrderFacade.placeOrder()\` that internally coordinates inventory, payment, and shipping services.

\`\`\`java
// Complex subsystem
class Inventory { void reserve(String id){ /*...*/ } }
class Payment   { void charge(String id){ /*...*/ } }
class Shipping  { void schedule(String id){ /*...*/ } }

// Facade: one simple method hides the orchestration
class OrderFacade {
    private final Inventory inv = new Inventory();
    private final Payment pay = new Payment();
    private final Shipping ship = new Shipping();
    public void placeOrder(String id) {           // client calls just this
        inv.reserve(id); pay.charge(id); ship.schedule(id);
    }
}
new OrderFacade().placeOrder("A1");   // client unaware of subsystem details
\`\`\``,tech:"design-patterns",displayNumber:11,sortKey:2495},{id:"design-patterns-2496",number:12,title:"Explain the Flyweight pattern.",question:"Explain the Flyweight pattern.",difficulty:"Hard",category:"Structural Patterns",answer:`Flyweight minimizes memory by **sharing** common, immutable parts of many similar objects (intrinsic state) and passing the varying parts (extrinsic state) in from outside. Instead of millions of objects each holding duplicate data, you cache and reuse shared instances. Classic uses: characters/glyphs in a text editor, particles in a game, or Java's \`Integer.valueOf\` cache. Requires clearly separating shared (intrinsic) from context-specific (extrinsic) state.

\`\`\`java
// Intrinsic (shared) state — cached and reused
record TreeType(String name, String texture) {}   // immutable, shared

class TreeFactory {
    private static final Map<String, TreeType> cache = new HashMap<>();
    static TreeType of(String name, String texture) {
        return cache.computeIfAbsent(name + texture, k -> new TreeType(name, texture));
    }
}
// Extrinsic (varying) state passed in per instance:
void draw(TreeType type, int x, int y) { /* render type at x,y */ }
// 1,000,000 trees share a handful of TreeType objects.
\`\`\``,tech:"design-patterns",displayNumber:12,sortKey:2496},{id:"design-patterns-2497",number:13,title:"Explain the Proxy pattern and its common variants.",question:"Explain the Proxy pattern and its common variants.",difficulty:"Intermediate",category:"Structural Patterns",answer:`Proxy provides a **surrogate** for another object to control access to it, implementing the same interface. Variants: **Virtual** (lazy-load an expensive object on first use), **Protection** (access control/authorization), **Remote** (represent an object in another address space — RPC stubs), and **Caching/Logging** (add cross-cutting behavior). Unlike Decorator (adds behavior), Proxy primarily controls access. Spring AOP proxies are a real-world example.

\`\`\`java
interface Image { void display(); }
class RealImage implements Image {                 // expensive to create
    RealImage(String f){ loadFromDisk(f); }
    public void display(){ /* show */ }
    private void loadFromDisk(String f){ /* heavy */ }
}
class ImageProxy implements Image {                // virtual proxy: lazy load
    private final String file; private RealImage real;
    ImageProxy(String f){ file = f; }
    public void display() {
        if (real == null) real = new RealImage(file);  // created only on first use
        real.display();
    }
}
Image img = new ImageProxy("big.png");   // no disk load until display() is called
\`\`\``,tech:"design-patterns",displayNumber:13,sortKey:2497},{id:"design-patterns-2498",number:14,title:'Compare Adapter, Decorator, Facade, and Proxy — they all "wrap".',question:'Compare Adapter, Decorator, Facade, and Proxy — they all "wrap".',difficulty:"Intermediate",category:"Structural Patterns",answer:`All wrap an object but with different intent:
- **Adapter**: changes the interface so incompatible types work together.
- **Decorator**: keeps the interface, **adds behavior/responsibilities** (stackable).
- **Facade**: introduces a **new simpler interface** over a complex subsystem.
- **Proxy**: keeps the interface, **controls access** (lazy, security, remote, caching).

Intent is the differentiator: convert (Adapter), enhance (Decorator), simplify (Facade), control (Proxy).

\`\`\`text
Pattern    Same interface?   Intent
Adapter    NO (converts)     make incompatible interfaces work together
Decorator  YES               add responsibilities dynamically (stack them)
Facade     NEW simpler one   hide a complex subsystem behind one entry point
Proxy      YES               control access (lazy load / auth / remote / cache)
\`\`\``,tech:"design-patterns",displayNumber:14,sortKey:2498},{id:"design-patterns-2499",number:15,title:"Explain the Strategy pattern.",question:"Explain the Strategy pattern.",difficulty:"Basic",category:"Behavioral Patterns",answer:`Strategy defines a family of interchangeable algorithms, encapsulates each behind a common interface, and lets the client select one at runtime. It replaces sprawling \`if/else\`/\`switch\` on behavior with polymorphism, and follows Open/Closed — add a new strategy without touching existing code. Classic uses: payment methods, sorting/compression algorithms, pricing/discount rules. In Java, a strategy is often just a lambda / functional interface.

\`\`\`java
interface DiscountStrategy { double apply(double total); }

class NoDiscount     implements DiscountStrategy { public double apply(double t){ return t; } }
class PercentOff     implements DiscountStrategy {
    private final double pct; PercentOff(double p){ pct=p; }
    public double apply(double t){ return t * (1 - pct); }
}
class Cart {
    private DiscountStrategy strategy = new NoDiscount();
    void setStrategy(DiscountStrategy s){ strategy = s; }   // swap at runtime
    double checkout(double total){ return strategy.apply(total); }
}
Cart cart = new Cart();
cart.setStrategy(new PercentOff(0.10));   // or a lambda: t -> t*0.9
cart.checkout(100);                        // 90
\`\`\``,tech:"design-patterns",displayNumber:15,sortKey:2499},{id:"design-patterns-2500",number:16,title:"Explain the Observer pattern.",question:"Explain the Observer pattern.",difficulty:"Basic",category:"Behavioral Patterns",answer:`Observer defines a **one-to-many** dependency: when a subject's state changes, all registered observers are notified automatically. It decouples the subject from its observers (it knows only the interface), enabling event/notification systems, UI data-binding, and pub/sub. Observers subscribe/unsubscribe dynamically. Watch for memory leaks (unregister observers) and update storms. It's the foundation of reactive/event-driven UIs.

\`\`\`java
interface Observer { void update(String news); }

class NewsAgency {                                  // subject
    private final List<Observer> observers = new ArrayList<>();
    void subscribe(Observer o){ observers.add(o); }
    void unsubscribe(Observer o){ observers.remove(o); }
    void publish(String news){ observers.forEach(o -> o.update(news)); } // notify all
}
class EmailSubscriber implements Observer {
    public void update(String news){ /* send email with news */ }
}
NewsAgency agency = new NewsAgency();
agency.subscribe(new EmailSubscriber());
agency.publish("Kafka 4.0 released");   // every subscriber notified
\`\`\``,tech:"design-patterns",displayNumber:16,sortKey:2500},{id:"design-patterns-2501",number:17,title:"Explain the Command pattern.",question:"Explain the Command pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`Command encapsulates a request as an **object**, decoupling the invoker from the receiver. This lets you parameterize objects with operations, queue or log requests, and support **undo/redo** (each command knows how to execute and reverse itself). Uses: menu/button actions, task queues, transactional operations, macro recording. The invoker just calls \`execute()\` without knowing the concrete action.

\`\`\`java
interface Command { void execute(); void undo(); }

class Light { void on(){ /*...*/ } void off(){ /*...*/ } }
class LightOnCommand implements Command {
    private final Light light; LightOnCommand(Light l){ light=l; }
    public void execute(){ light.on(); }
    public void undo(){ light.off(); }              // reversible
}
class Remote {                                      // invoker
    private final Deque<Command> history = new ArrayDeque<>();
    void press(Command c){ c.execute(); history.push(c); }
    void undoLast(){ if(!history.isEmpty()) history.pop().undo(); }
}
Remote r = new Remote();
r.press(new LightOnCommand(new Light()));
r.undoLast();   // undo/redo enabled by encapsulating the action
\`\`\``,tech:"design-patterns",displayNumber:17,sortKey:2501},{id:"design-patterns-2502",number:18,title:"Explain the Template Method pattern.",question:"Explain the Template Method pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`Template Method defines the **skeleton of an algorithm** in a base class method, deferring specific steps to subclasses via abstract (or hook) methods. The overall structure and step order are fixed; subclasses customize the variable steps. It promotes reuse and enforces a consistent flow (the "Hollywood principle": the base class calls the subclass, not vice versa). Uses: framework lifecycle methods, data-processing pipelines, \`HttpServlet.service()\`.

\`\`\`java
abstract class DataProcessor {
    // template method: fixed skeleton, steps vary
    public final void process() {
        read();
        transform();      // subclass-specific
        write();
    }
    protected void read(){ /* common */ }
    protected abstract void transform();            // subclasses fill this in
    protected void write(){ /* common */ }
}
class CsvProcessor extends DataProcessor {
    protected void transform(){ /* CSV-specific parsing */ }
}
new CsvProcessor().process();   // structure enforced, only transform() customized
\`\`\``,tech:"design-patterns",displayNumber:18,sortKey:2502},{id:"design-patterns-2503",number:19,title:"Explain the Chain of Responsibility pattern.",question:"Explain the Chain of Responsibility pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`Chain of Responsibility passes a request along a chain of handlers; each handler either processes it or forwards it to the next. It decouples sender from receiver and lets you add/reorder handlers freely. Uses: middleware/filter pipelines (auth → logging → validation), event bubbling, approval workflows, exception handling. Each handler focuses on one concern (Single Responsibility).

\`\`\`java
abstract class Handler {
    protected Handler next;
    Handler setNext(Handler n){ this.next = n; return n; }
    abstract void handle(Request r);
    protected void forward(Request r){ if (next != null) next.handle(r); }
}
class AuthHandler extends Handler {
    void handle(Request r){ if (r.authed()) forward(r); else r.reject("401"); }
}
class RateLimitHandler extends Handler {
    void handle(Request r){ if (r.underLimit()) forward(r); else r.reject("429"); }
}
class BusinessHandler extends Handler {
    void handle(Request r){ r.serve(); }
}
Handler chain = new AuthHandler();
chain.setNext(new RateLimitHandler()).setNext(new BusinessHandler());
chain.handle(request);   // flows through the pipeline
\`\`\``,tech:"design-patterns",displayNumber:19,sortKey:2503},{id:"design-patterns-2504",number:20,title:"Explain the State pattern.",question:"Explain the State pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`State lets an object alter its behavior when its internal state changes — it appears to change class. Each state is a separate object encapsulating the behavior valid in that state and the transitions to other states, replacing large conditional blocks on a "status" field. Uses: order lifecycle (placed→paid→shipped), vending machines, document workflows, TCP connection states. It makes valid transitions explicit and adding states clean.

\`\`\`java
interface OrderState { OrderState next(); String name(); }

class Placed  implements OrderState { public OrderState next(){ return new Paid(); }    public String name(){ return "PLACED"; } }
class Paid    implements OrderState { public OrderState next(){ return new Shipped(); } public String name(){ return "PAID"; } }
class Shipped implements OrderState { public OrderState next(){ return this; }          public String name(){ return "SHIPPED"; } }

class Order {
    private OrderState state = new Placed();
    void advance(){ state = state.next(); }   // behavior depends on current state
    String status(){ return state.name(); }
}
Order o = new Order(); o.advance(); o.advance();   // PLACED -> PAID -> SHIPPED
\`\`\``,tech:"design-patterns",displayNumber:20,sortKey:2504},{id:"design-patterns-2505",number:21,title:"Explain the Iterator pattern.",question:"Explain the Iterator pattern.",difficulty:"Basic",category:"Behavioral Patterns",answer:`Iterator provides a way to access elements of a collection sequentially **without exposing its internal representation**. The collection returns an iterator object that knows how to traverse it (\`hasNext()\`/\`next()\`), so client code is decoupled from whether it's an array, list, or tree. Java's \`Iterator\`/\`Iterable\` and the for-each loop are built on this. It also allows multiple simultaneous traversals.

\`\`\`java
class Playlist implements Iterable<String> {
    private final String[] songs;
    Playlist(String... s){ songs = s; }
    public Iterator<String> iterator() {           // hides internal array
        return new Iterator<>() {
            int i = 0;
            public boolean hasNext(){ return i < songs.length; }
            public String next(){ return songs[i++]; }
        };
    }
}
for (String song : new Playlist("a","b","c")) { /* traverse without knowing internals */ }
\`\`\``,tech:"design-patterns",displayNumber:21,sortKey:2505},{id:"design-patterns-2506",number:22,title:"Explain the Mediator pattern.",question:"Explain the Mediator pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`Mediator centralizes complex communication between objects into a mediator object, so components refer only to the mediator instead of to each other — turning a many-to-many web of dependencies into a hub-and-spoke. It reduces coupling and makes interactions easier to change. Uses: chat rooms (users talk via the room), UI dialogs coordinating widgets, air-traffic control. Caution: the mediator can become a god object if overloaded.

\`\`\`java
class ChatRoom {                                    // mediator
    private final List<User> users = new ArrayList<>();
    void register(User u){ users.add(u); u.setRoom(this); }
    void send(String msg, User from) {              // routes between users
        users.stream().filter(u -> u != from).forEach(u -> u.receive(msg));
    }
}
class User {
    private final String name; private ChatRoom room;
    User(String n){ name=n; }
    void setRoom(ChatRoom r){ room = r; }
    void send(String m){ room.send(name+": "+m, this); }  // no direct user refs
    void receive(String m){ /* show */ }
}
\`\`\``,tech:"design-patterns",displayNumber:22,sortKey:2506},{id:"design-patterns-2507",number:23,title:"Explain the Memento pattern.",question:"Explain the Memento pattern.",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`Memento captures and externalizes an object's internal state so it can be **restored later**, without violating encapsulation (the state is stored in an opaque memento object). It powers undo/redo, checkpoints, and snapshots. Three roles: **Originator** (creates/restores mementos), **Memento** (immutable state snapshot), **Caretaker** (stores mementos, e.g. an undo stack) without inspecting them.

\`\`\`java
class Editor {                                      // originator
    private String content = "";
    void type(String t){ content += t; }
    Memento save(){ return new Memento(content); }              // snapshot
    void restore(Memento m){ content = m.state(); }             // rollback
    record Memento(String state) {}                             // opaque snapshot
}
Editor e = new Editor();
Deque<Editor.Memento> undo = new ArrayDeque<>();  // caretaker
e.type("Hello"); undo.push(e.save());
e.type(" World"); e.restore(undo.pop());          // back to "Hello"
\`\`\``,tech:"design-patterns",displayNumber:23,sortKey:2507},{id:"design-patterns-2508",number:24,title:"Explain the Visitor pattern.",question:"Explain the Visitor pattern.",difficulty:"Hard",category:"Behavioral Patterns",answer:`Visitor lets you add new operations to an object structure **without modifying the element classes**. Elements accept a visitor and dispatch to the visitor's type-specific method (double dispatch). It's useful when you have a stable class hierarchy but frequently add new operations (e.g. AST traversal: type-check, generate code, pretty-print). Trade-off: adding a new element type forces changing every visitor, so it favors stable structures with changing operations.

\`\`\`java
interface Visitor { void visit(Book b); void visit(Fruit f); }
interface Item { void accept(Visitor v); }

class Book  implements Item { double price=20; public void accept(Visitor v){ v.visit(this);} }
class Fruit implements Item { double kg=2;    public void accept(Visitor v){ v.visit(this);} }

class PriceVisitor implements Visitor {             // new operation, no element edits
    double total = 0;
    public void visit(Book b){ total += b.price; }
    public void visit(Fruit f){ total += f.kg * 3; }
}
List<Item> cart = List.of(new Book(), new Fruit());
PriceVisitor pv = new PriceVisitor();
cart.forEach(i -> i.accept(pv));   // double dispatch computes total
\`\`\``,tech:"design-patterns",displayNumber:24,sortKey:2508},{id:"design-patterns-2509",number:25,title:"Explain the Interpreter pattern.",question:"Explain the Interpreter pattern.",difficulty:"Hard",category:"Behavioral Patterns",answer:`Interpreter defines a grammar for a simple language and an interpreter that evaluates sentences in it, representing each grammar rule as a class in an expression tree. Use it for small, well-defined languages: rule engines, query/filter expressions, calculators, regex-like matchers. It's clean for simple grammars but becomes unwieldy for complex ones (use a real parser generator instead).

\`\`\`java
interface Expr { int eval(); }
class Num  implements Expr { int v; Num(int v){this.v=v;} public int eval(){ return v; } }
class Add  implements Expr {
    Expr l, r; Add(Expr l, Expr r){ this.l=l; this.r=r; }
    public int eval(){ return l.eval() + r.eval(); }    // grammar rule as a class
}
class Mul  implements Expr {
    Expr l, r; Mul(Expr l, Expr r){ this.l=l; this.r=r; }
    public int eval(){ return l.eval() * r.eval(); }
}
// (2 + 3) * 4  as an expression tree:
Expr e = new Mul(new Add(new Num(2), new Num(3)), new Num(4));
e.eval();   // 20
\`\`\``,tech:"design-patterns",displayNumber:25,sortKey:2509},{id:"design-patterns-2510",number:26,title:"What is the difference between Strategy and State patterns (they look identical)?",question:"What is the difference between Strategy and State patterns (they look identical)?",difficulty:"Hard",category:"Behavioral Patterns",answer:`Structurally both delegate to an interchangeable object, but intent differs:
- **Strategy**: the client chooses **which algorithm** to use; strategies are independent and unaware of each other; the choice usually doesn't change on its own.
- **State**: the object's **behavior changes with its internal state**, and states typically **trigger transitions** to other states; the client doesn't pick the state, the state machine drives it.

Strategy = "how to do something" (pluggable algorithm); State = "what to do based on current status" (self-transitioning lifecycle).

\`\`\`text
Strategy                              State
client picks the algorithm            object's state picks behavior
strategies don't know each other      states transition to one another
stable during an operation            changes as the object's status changes
e.g. sort/compress/pricing algorithm  e.g. order placed->paid->shipped
\`\`\``,tech:"design-patterns",displayNumber:26,sortKey:2510},{id:"design-patterns-2511",number:27,title:"What are the three GoF categories and how do you choose a behavioral pattern?",question:"What are the three GoF categories and how do you choose a behavioral pattern?",difficulty:"Intermediate",category:"Behavioral Patterns",answer:`GoF groups patterns into **Creational** (object creation), **Structural** (object composition), and **Behavioral** (object interaction/responsibility). For behavioral selection: pluggable algorithm → **Strategy**; notify many on change → **Observer**; encapsulate a request/undo → **Command**; fixed algorithm with varying steps → **Template Method**; pass request through handlers → **Chain of Responsibility**; behavior by status → **State**; traverse without exposing internals → **Iterator**; centralize N-to-N comms → **Mediator**; snapshot/restore → **Memento**; new ops over a stable hierarchy → **Visitor**; evaluate a mini-language → **Interpreter**.

\`\`\`text
Need                                   -> Behavioral pattern
swap algorithm at runtime              -> Strategy
notify many observers of a change      -> Observer
encapsulate action / undo-redo         -> Command
fixed skeleton, custom steps           -> Template Method
request through a handler pipeline      -> Chain of Responsibility
behavior depends on status + transitions-> State
sequential access, hide internals      -> Iterator
centralize object communication        -> Mediator
capture/restore state                  -> Memento
add operations to stable classes       -> Visitor
evaluate a simple grammar              -> Interpreter
\`\`\``,tech:"design-patterns",displayNumber:27,sortKey:2511},{id:"design-patterns-2512",number:28,title:"What are the SOLID principles?",question:"What are the SOLID principles?",difficulty:"Intermediate",category:"SOLID Principles",answer:`SOLID is five OOP design principles for maintainable code:
- **S**ingle Responsibility: a class should have one reason to change.
- **O**pen/Closed: open for extension, closed for modification (add behavior via new code, not edits).
- **L**iskov Substitution: subtypes must be usable anywhere their base type is expected, without breaking behavior.
- **I**nterface Segregation: prefer many small, specific interfaces over one fat one.
- **D**ependency Inversion: depend on abstractions, not concretions.

Together they reduce coupling, ease testing, and localize change.

\`\`\`java
// Dependency Inversion + Single Responsibility:
interface Repository { void save(Order o); }        // abstraction
class OrderService {
    private final Repository repo;                   // depends on abstraction
    OrderService(Repository repo){ this.repo = repo; }  // injected, not \`new\`
    void place(Order o){ repo.save(o); }             // one responsibility
}
// Open/Closed: add a new Repository impl without touching OrderService.
\`\`\``,tech:"design-patterns",displayNumber:28,sortKey:2512},{id:"design-patterns-2513",number:29,title:"Explain the Single Responsibility Principle with a refactor example.",question:"Explain the Single Responsibility Principle with a refactor example.",difficulty:"Basic",category:"SOLID Principles",answer:`A class should have exactly one responsibility / one reason to change. Mixing concerns (e.g. business logic + persistence + formatting) makes a class fragile: a change to one concern risks the others, and it's hard to test/reuse. Split responsibilities into focused classes that collaborate. This improves cohesion and testability.

\`\`\`java
// BAD: one class does calculation, persistence, and emailing
class Invoice { double total(){/*...*/} void saveToDb(){/*...*/} void email(){/*...*/} }

// GOOD: one responsibility each
class Invoice        { double total(){ /* calc only */ return 0; } }
class InvoiceRepository { void save(Invoice i){ /* persistence */ } }
class InvoiceMailer  { void send(Invoice i){ /* emailing */ } }
// A change to emailing no longer risks the calculation logic.
\`\`\``,tech:"design-patterns",displayNumber:29,sortKey:2513},{id:"design-patterns-2514",number:30,title:"Explain the Open/Closed and Liskov Substitution principles.",question:"Explain the Open/Closed and Liskov Substitution principles.",difficulty:"Intermediate",category:"SOLID Principles",answer:`- **Open/Closed**: you should extend behavior by adding new code (new subclasses/strategies), not by editing existing, tested code — often achieved with polymorphism/Strategy.
- **Liskov Substitution**: any subtype must honor the base type's contract, so substituting it doesn't break correctness (no strengthening preconditions or weakening postconditions). The classic violation: \`Square extends Rectangle\` breaking \`setWidth/setHeight\` expectations.

\`\`\`java
// Open/Closed via polymorphism — add shapes without editing area()
interface Shape { double area(); }
class Circle implements Shape { double r; public double area(){ return Math.PI*r*r; } }
class Square implements Shape { double s; public double area(){ return s*s; } }
double totalArea(List<Shape> shapes){ return shapes.stream().mapToDouble(Shape::area).sum(); }
// Adding Triangle requires NO change to totalArea (Open/Closed).
// LSV: every Shape returns a valid area() — substitutable everywhere a Shape is used.
\`\`\``,tech:"design-patterns",displayNumber:30,sortKey:2514},{id:"design-patterns-2515",number:31,title:"Explain the Interface Segregation and Dependency Inversion principles.",question:"Explain the Interface Segregation and Dependency Inversion principles.",difficulty:"Intermediate",category:"SOLID Principles",answer:`- **Interface Segregation**: clients shouldn't depend on methods they don't use. Split fat interfaces so implementers aren't forced to stub irrelevant methods (e.g. don't make a \`SimplePrinter\` implement \`scan()\`/\`fax()\`).
- **Dependency Inversion**: high-level modules and low-level modules should both depend on **abstractions**; details depend on abstractions, not vice versa. This decouples policy from implementation and enables injection/mocking.

\`\`\`java
// Interface Segregation: small focused interfaces
interface Printer { void print(Doc d); }
interface Scanner { void scan(Doc d); }
class SimplePrinter implements Printer { public void print(Doc d){ /*...*/ } } // no scan() stub

// Dependency Inversion: high-level depends on abstraction, impl injected
interface MessageSender { void send(String to, String body); }
class OrderNotifier {
    private final MessageSender sender;              // abstraction
    OrderNotifier(MessageSender s){ sender = s; }    // inject email/SMS/push impl
}
\`\`\``,tech:"design-patterns",displayNumber:31,sortKey:2515},{id:"design-patterns-2516",number:32,title:"What is Dependency Injection and how does it relate to IoC?",question:"What is Dependency Injection and how does it relate to IoC?",difficulty:"Intermediate",category:"Enterprise Patterns",answer:`Dependency Injection (DI) supplies a class's dependencies from the outside (constructor/setter) instead of the class creating them with \`new\`. It's the primary way to implement **Inversion of Control** — the framework/container, not the class, controls object creation and wiring. Benefits: loose coupling, easy testing (inject mocks), and swappable implementations. Prefer **constructor injection** for required, immutable dependencies. Spring's container is a DI framework.

\`\`\`java
// Constructor injection — dependency provided, not constructed internally
class PaymentService {
    private final Gateway gateway;
    PaymentService(Gateway gateway){ this.gateway = gateway; }  // injected
}
// Production wiring: new PaymentService(new StripeGateway());
// Test wiring:       new PaymentService(mockGateway);   // trivially mockable
\`\`\``,tech:"design-patterns",displayNumber:32,sortKey:2516},{id:"design-patterns-2517",number:33,title:"Explain the MVC architectural pattern.",question:"Explain the MVC architectural pattern.",difficulty:"Basic",category:"Enterprise Patterns",answer:`MVC separates an application into **Model** (data + business rules), **View** (presentation/UI), and **Controller** (handles input, coordinates model and view). This separation of concerns improves testability, parallel development, and reuse (multiple views over one model). The controller receives a request, updates/reads the model, and selects a view to render. Variants: MVP, MVVM (used in frontend frameworks). Spring MVC is a canonical example.

\`\`\`java
// Model
record Order(String id, double total) {}
// Controller — handles input, coordinates model + view
@RestController
class OrderController {
    private final OrderService service;             // model/business layer
    OrderController(OrderService s){ service = s; }
    @GetMapping("/orders/{id}")
    Order get(@PathVariable String id){ return service.find(id); }  // returns to view
}
// View = the JSON/HTML rendering of the returned model.
\`\`\``,tech:"design-patterns",displayNumber:33,sortKey:2517},{id:"design-patterns-2518",number:34,title:"Explain the Repository pattern.",question:"Explain the Repository pattern.",difficulty:"Intermediate",category:"Enterprise Patterns",answer:`Repository mediates between the domain and data-mapping layers, providing a **collection-like interface** for accessing aggregates ("save this order", "find orders by customer") while hiding the persistence mechanism (SQL, JPA, NoSQL). It centralizes query logic, decouples business code from the database, and makes swapping/mock­ing storage easy. Spring Data repositories are a direct implementation.

\`\`\`java
// Domain-facing abstraction (collection-like)
interface OrderRepository {
    Optional<Order> findById(String id);
    List<Order> findByCustomer(String customerId);
    Order save(Order o);
}
// Business code depends only on the interface, not on JDBC/JPA:
class OrderService {
    private final OrderRepository repo;
    OrderService(OrderRepository r){ repo = r; }
    Order place(Order o){ return repo.save(o); }
}
// Spring Data: interface OrderRepository extends JpaRepository<Order,String> {}
\`\`\``,tech:"design-patterns",displayNumber:34,sortKey:2518},{id:"design-patterns-2519",number:35,title:"What is the difference between DAO and Repository patterns?",question:"What is the difference between DAO and Repository patterns?",difficulty:"Intermediate",category:"Enterprise Patterns",answer:`Both abstract data access, but at different levels:
- **DAO (Data Access Object)**: lower-level, typically **one per table/entity**, exposing CRUD operations close to the database structure.
- **Repository**: higher-level, domain-oriented, works with **aggregates** and speaks the ubiquitous language of the domain; it may use one or more DAOs internally and can encapsulate richer domain queries.

In practice they overlap; DAO is persistence-centric, Repository is domain-centric (DDD).

\`\`\`java
// DAO — table/CRUD oriented, persistence-centric
interface OrderDao {
    void insert(OrderRow row); OrderRow selectById(String id); void update(OrderRow row);
}
// Repository — domain/aggregate oriented, speaks business language
interface OrderRepository {
    Order findById(String id);            // returns a domain aggregate
    List<Order> findUnshippedFor(String customerId);   // domain query
}
// Repository may delegate to DAO(s) under the hood.
\`\`\``,tech:"design-patterns",displayNumber:35,sortKey:2519},{id:"design-patterns-2520",number:36,title:"Explain the DTO pattern and why it's used.",question:"Explain the DTO pattern and why it's used.",difficulty:"Basic",category:"Enterprise Patterns",answer:`A DTO (Data Transfer Object) is a simple, behavior-less object that carries data across boundaries (API responses, service-to-service, layers). It decouples your API/wire contract from internal domain/entity models — so you can expose only needed fields, avoid leaking persistence details (lazy-loading, sensitive columns), aggregate data from multiple sources, and evolve internals without breaking clients. Map between entity and DTO explicitly (or via MapStruct).

\`\`\`java
// Internal entity (persistence) stays hidden
class OrderEntity { String id; String customerId; String internalNotes; double total; }

// DTO exposes only what the API should return (no internalNotes)
record OrderDto(String id, double total) {}

OrderDto toDto(OrderEntity e){ return new OrderDto(e.id, e.total); }
// API returns OrderDto -> internal fields never leak; contract stays stable.
\`\`\``,tech:"design-patterns",displayNumber:36,sortKey:2520},{id:"design-patterns-2521",number:37,title:"What is the Unit of Work pattern?",question:"What is the Unit of Work pattern?",difficulty:"Hard",category:"Enterprise Patterns",answer:`Unit of Work tracks all changes (inserts/updates/deletes) made during a business transaction and commits them as **one atomic operation**, coordinating a single database transaction and often reducing round-trips. It ensures consistency (all-or-nothing) and manages the identity/dirty-tracking of objects. JPA's \`EntityManager\`/persistence context and Spring's \`@Transactional\` are implementations — changes within a transaction are flushed together on commit.

\`\`\`java
// @Transactional = Unit of Work: all changes commit atomically or roll back
@Transactional
public void transfer(String from, String to, long cents) {
    Account a = repo.find(from);
    Account b = repo.find(to);
    a.debit(cents);                 // tracked
    b.credit(cents);                // tracked
    // Both persisted together on commit; any exception rolls back BOTH.
}
\`\`\``,tech:"design-patterns",displayNumber:37,sortKey:2521},{id:"design-patterns-2522",number:38,title:"What is the difference between design patterns and architectural patterns?",question:"What is the difference between design patterns and architectural patterns?",difficulty:"Intermediate",category:"Enterprise Patterns",answer:`- **Design patterns (LLD)**: class/object-level solutions to recurring code design problems (GoF: Strategy, Observer, Factory…). Scope: within a component/module.
- **Architectural patterns (HLD)**: system-level structures organizing the whole application or multiple services (Layered, MVC, Microservices, Event-Driven, CQRS, Sidecar, Client-Server). Scope: the system.

Design patterns shape code; architectural patterns shape systems. Both aim for maintainability, scalability, and separation of concerns at their respective altitudes.

\`\`\`text
Design patterns (LLD)          Architectural patterns (HLD)
class/object interactions      system/component organization
Strategy, Observer, Factory    Layered, MVC, Microservices, EDA, CQRS, Sidecar
scope: a module                scope: the whole system
\`\`\``,tech:"design-patterns",displayNumber:38,sortKey:2522},{id:"design-patterns-2523",number:39,title:"What is the Sidecar pattern?",question:"What is the Sidecar pattern?",difficulty:"Intermediate",category:"Architecture Patterns",answer:`The Sidecar pattern deploys a helper component in a **separate process/container alongside** the main application (in the same pod/host), sharing its lifecycle and network. It offloads cross-cutting concerns — TLS/mTLS, retries, service discovery, logging, metrics, config — out of the app and into the sidecar, so the app stays focused on business logic and the behavior is language-agnostic. It's the foundation of a **service mesh** (Envoy sidecar per service). Cost: extra resource/latency per hop.

\`\`\`yaml
# Kubernetes pod: app container + sidecar proxy share network & lifecycle
spec:
  containers:
    - name: order-service          # your business app
      image: order-service:1.0
    - name: envoy-sidecar          # handles mTLS, retries, metrics, tracing
      image: envoyproxy/envoy:latest
# App calls localhost; the sidecar intercepts and secures/observes all traffic.
\`\`\``,tech:"design-patterns",displayNumber:39,sortKey:2523},{id:"design-patterns-2524",number:40,title:"What is the Ambassador pattern and how does it differ from Sidecar?",question:"What is the Ambassador pattern and how does it differ from Sidecar?",difficulty:"Hard",category:"Architecture Patterns",answer:`The Ambassador pattern places a helper proxy that handles **outbound** network requests on behalf of the app — connection management, retries, circuit breaking, routing, monitoring — so the app just calls a local endpoint. It's a specialized sidecar focused on client-side connectivity to remote services, useful for adding resilience to legacy apps without changing their code. **Sidecar** is the general concept (any co-located helper); **Ambassador** specifically brokers the app's outbound calls to external/remote services.

\`\`\`text
App --> localhost:9000 (Ambassador proxy) --> remote-service
The Ambassador adds: retries, timeouts, circuit breaking, TLS, service discovery.
Sidecar   = general co-located helper (in/out, cross-cutting concerns)
Ambassador= sidecar specialized for the app's OUTBOUND connectivity
\`\`\``,tech:"design-patterns",displayNumber:40,sortKey:2524},{id:"design-patterns-2525",number:41,title:"Explain the Circuit Breaker pattern at the architecture level.",question:"Explain the Circuit Breaker pattern at the architecture level.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Circuit Breaker prevents an app from repeatedly calling a failing remote dependency, giving it time to recover and failing fast to avoid resource exhaustion and cascading failures. States: **Closed** (calls pass, failures counted), **Open** (calls short-circuit to a fallback for a cooldown), **Half-Open** (a few trial calls test recovery). It's essential in distributed systems where any dependency can be slow/down. Often provided by a library (Resilience4j) or the service mesh.

\`\`\`java
CircuitBreaker cb = CircuitBreaker.of("inventory", CircuitBreakerConfig.custom()
    .failureRateThreshold(50)                       // open at 50% failures
    .waitDurationInOpenState(Duration.ofSeconds(10))
    .permittedNumberOfCallsInHalfOpenState(3)
    .slidingWindowSize(20).build());

Supplier<Stock> call = CircuitBreaker.decorateSupplier(cb, () -> inventoryClient.get(sku));
Stock s = Try.ofSupplier(call).recover(ex -> Stock.unknown()).get();  // fast fallback
\`\`\``,tech:"design-patterns",displayNumber:41,sortKey:2525},{id:"design-patterns-2526",number:42,title:"What is the Master-Slave (Primary-Replica) architecture?",question:"What is the Master-Slave (Primary-Replica) architecture?",difficulty:"Intermediate",category:"Architecture Patterns",answer:`In Master-Slave (now usually called **Primary-Replica** or **Leader-Follower**), one **master** node handles writes and one or more **slave/replica** nodes replicate the master's data and serve reads. Benefits: read scalability (fan reads across replicas), high availability (promote a replica if the master fails), and backups/analytics off replicas. Trade-offs: **replication lag** (replicas may be slightly stale → eventual consistency for reads), and writes remain a single-master bottleneck. Widely used in databases (MySQL/Postgres replication), Redis, and Kafka (partition leader/followers).

\`\`\`text
                 writes
   Clients ─────────────► [ MASTER / Primary ] ──replicate──► [ Replica 1 ] ─┐
                             ▲                              └► [ Replica 2 ] ─┤ reads
                             └──────── reads may go to replicas ─────────────┘
Master fails -> promote a replica to new master (failover).
Caveat: replication lag -> replica reads can be slightly stale.
\`\`\``,tech:"design-patterns",displayNumber:42,sortKey:2526},{id:"design-patterns-2527",number:43,title:"How do you route reads to replicas and writes to the master?",question:"How do you route reads to replicas and writes to the master?",difficulty:"Hard",category:"Architecture Patterns",answer:`Use a routing layer (application-level datasource router, proxy like ProxySQL, or driver read/write split) that sends write/transactional queries to the master and read-only queries to replicas (load-balanced). Handle **replication lag**: for read-your-own-writes consistency, route a user's reads to the master briefly after they write, or use a version/timestamp check. Below is a Spring \`AbstractRoutingDataSource\` that switches datasources by a thread-local flag.

\`\`\`java
class RoutingDataSource extends AbstractRoutingDataSource {
    protected Object determineCurrentLookupKey() {
        return TransactionSynchronizationManager.isCurrentTransactionReadOnly()
            ? "replica" : "master";                 // reads -> replica, writes -> master
    }
}
// @Transactional(readOnly = true) methods hit a replica; writes hit the master.
// For read-your-writes: pin the user to master for a short window after a write.
\`\`\``,tech:"design-patterns",displayNumber:43,sortKey:2527},{id:"design-patterns-2528",number:44,title:"What is Leader Election in distributed systems?",question:"What is Leader Election in distributed systems?",difficulty:"Hard",category:"Architecture Patterns",answer:`Leader Election designates one node among many as the coordinator (leader) to make decisions or own a resource, so work isn't duplicated (e.g. only one node runs a scheduled job or is the write master). If the leader fails, the group elects a new one. It requires consensus to avoid **split-brain** (two leaders). Implemented via consensus algorithms (**Raft**, Paxos) or coordination services (ZooKeeper, etcd, Consul) using distributed locks/leases with fencing tokens.

\`\`\`java
// Lease-based leader election (e.g. via etcd/ZooKeeper/Redis lock)
if (coordinator.tryAcquireLeadership("scheduler-leader", Duration.ofSeconds(15))) {
    runScheduledJobsAsLeader();                    // only the leader runs these
    coordinator.renewLeaseHeartbeat();             // keep leadership alive
}
// Leader crashes -> lease expires -> another node acquires it (new leader).
// Fencing token prevents a paused old leader from acting after losing the lease.
\`\`\``,tech:"design-patterns",displayNumber:44,sortKey:2528},{id:"design-patterns-2529",number:45,title:"Explain the Load Balancer pattern and its strategies.",question:"Explain the Load Balancer pattern and its strategies.",difficulty:"Basic",category:"Architecture Patterns",answer:`A load balancer distributes incoming traffic across multiple backend instances to improve throughput, availability, and to avoid overloading any one node. It performs **health checks** and removes unhealthy instances. **Layer 4** balances by TCP/IP; **Layer 7** is HTTP-aware (route by path/header/cookie). Strategies: round robin, least connections, weighted, IP/consistent hash (sticky). It's a cornerstone of horizontal scaling and zero-downtime deploys.

\`\`\`text
            ┌─► instance A (healthy)
Clients ─► LB ─► instance B (healthy)      strategy: round robin / least-conn / weighted
            └─► instance C (unhealthy -> removed by health check)
L4: TCP-level, fast.   L7: HTTP-aware (path/header routing, TLS termination).
\`\`\``,tech:"design-patterns",displayNumber:45,sortKey:2529},{id:"design-patterns-2530",number:46,title:"What is the Reverse Proxy pattern (vs Forward Proxy)?",question:"What is the Reverse Proxy pattern (vs Forward Proxy)?",difficulty:"Intermediate",category:"Architecture Patterns",answer:`A **reverse proxy** sits in front of servers and handles incoming client requests on their behalf — doing TLS termination, load balancing, caching, compression, and routing (Nginx, Envoy, API gateways). Clients think they're talking to one server. A **forward proxy** sits in front of clients and forwards their outbound requests to the internet (egress control, anonymity, corporate filtering). Reverse = protects/serves the servers; forward = represents the clients.

\`\`\`nginx
# Reverse proxy: one public entry point -> internal services, with caching + TLS
server {
  listen 443 ssl;
  location /api/ { proxy_pass http://backend_pool; }   # route + load balance
  location /static/ { proxy_cache assets; proxy_pass http://cdn_origin; }
}
# Forward proxy = client-side egress (e.g. browser -> corporate proxy -> internet)
\`\`\``,tech:"design-patterns",displayNumber:46,sortKey:2530},{id:"design-patterns-2531",number:47,title:"Explain Database Sharding (partitioning) as a pattern.",question:"Explain Database Sharding (partitioning) as a pattern.",difficulty:"Hard",category:"Architecture Patterns",answer:`Sharding horizontally partitions data across multiple databases/nodes, each holding a subset, so no single node holds everything — scaling writes and storage beyond one machine. A **shard key** determines placement: **range** (by value range), **hash** (even distribution, avoids hotspots), or **directory/lookup**. Trade-offs: cross-shard queries/joins and transactions are hard, rebalancing is complex (consistent hashing helps), and a bad shard key causes hotspots. Choose a high-cardinality, evenly-accessed key.

\`\`\`java
// Hash sharding: route by shard key to one of N shards
int shard = Math.floorMod(userId.hashCode(), shards.size());
DataSource ds = shards.get(shard);                  // that user's data lives here
// Range sharding: users A-M -> shard0, N-Z -> shard1
// Pitfall: low-cardinality/skewed keys -> hotspots. Cross-shard JOINs are costly.
\`\`\``,tech:"design-patterns",displayNumber:47,sortKey:2531},{id:"design-patterns-2532",number:48,title:"Explain the Rate Limiter / Throttling pattern.",question:"Explain the Rate Limiter / Throttling pattern.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Rate limiting caps how many requests a client can make in a time window to protect services from overload, abuse, and to enforce fair use/quotas. Algorithms: **Token Bucket** (tokens refill at a rate, allow bursts up to capacity), **Leaky Bucket** (smooths to a constant rate), **Fixed/Sliding Window** counters. In distributed systems, back the counter with Redis so the limit holds across instances. Exceeding the limit returns HTTP 429 with \`Retry-After\`.

\`\`\`java
// Token bucket: refill at \`rate\`/sec, allow bursts up to \`capacity\`
class TokenBucket {
    private double tokens; private final double capacity, ratePerSec;
    private long last = System.nanoTime();
    TokenBucket(double cap, double rate){ capacity=cap; ratePerSec=rate; tokens=cap; }
    synchronized boolean allow() {
        long now = System.nanoTime();
        tokens = Math.min(capacity, tokens + (now - last)/1e9 * ratePerSec);
        last = now;
        if (tokens >= 1) { tokens -= 1; return true; }
        return false;                                // -> HTTP 429
    }
}
\`\`\``,tech:"design-patterns",displayNumber:48,sortKey:2532},{id:"design-patterns-2533",number:49,title:"Explain the Retry pattern with backoff and idempotency.",question:"Explain the Retry pattern with backoff and idempotency.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Retry re-attempts a failed operation that may be **transient** (network blip, timeout, 503). To be safe it must: only retry **idempotent** operations (or use idempotency keys), use **exponential backoff + jitter** to avoid hammering/thundering herd, cap total attempts/time, and only retry retryable errors (not a 400/validation error). Pair with a circuit breaker so retries don't prolong a real outage.

\`\`\`java
Retry retry = Retry.of("api", RetryConfig.custom()
    .maxAttempts(4)
    .intervalFunction(IntervalFunction.ofExponentialRandomBackoff(
        Duration.ofMillis(200), 2.0, 0.5))          // 0.2s,0.4s,0.8s +/- jitter
    .retryOnException(ex -> ex instanceof IOException) // only transient errors
    .build());
Supplier<Resp> call = Retry.decorateSupplier(retry, () -> client.getIdempotent(id));
\`\`\``,tech:"design-patterns",displayNumber:49,sortKey:2533},{id:"design-patterns-2534",number:50,title:"Explain the Publish-Subscribe (Pub/Sub) pattern.",question:"Explain the Publish-Subscribe (Pub/Sub) pattern.",difficulty:"Basic",category:"Architecture Patterns",answer:`Pub/Sub decouples senders (publishers) from receivers (subscribers) via a message broker/topic: publishers emit messages to a topic without knowing who consumes them, and all subscribers to that topic receive them. It enables one-to-many fan-out, async processing, and loose coupling — publishers and subscribers can scale and evolve independently. Implemented with Kafka, RabbitMQ, Redis Pub/Sub, Google Pub/Sub, or an in-process event bus.

\`\`\`java
// Publisher doesn't know or care who subscribes
eventBus.publish("user.registered", new UserRegistered(userId));

// Independent subscribers each react
@EventListener void sendWelcomeEmail(UserRegistered e){ /* ... */ }
@EventListener void createDefaultSettings(UserRegistered e){ /* ... */ }
@EventListener void trackAnalytics(UserRegistered e){ /* ... */ }
// Add a new subscriber without touching the publisher (Open/Closed at system level).
\`\`\``,tech:"design-patterns",displayNumber:50,sortKey:2534},{id:"design-patterns-2535",number:51,title:"What is the Service Registry / Discovery pattern?",question:"What is the Service Registry / Discovery pattern?",difficulty:"Intermediate",category:"Architecture Patterns",answer:`A Service Registry is a database of available service instances (name → healthy host:port). Instances **register** on startup and heartbeat; clients or a router **discover** healthy instances by logical name instead of hardcoding addresses — essential in dynamic/auto-scaling environments. **Client-side discovery**: client queries the registry and load-balances. **Server-side discovery**: a load balancer/router (or k8s Service DNS) resolves it. Tools: Eureka, Consul, etcd, Kubernetes.

\`\`\`text
Startup:  instance -> REGISTER {name:"orders", host:10.0.0.5:8080, health:/health}
Heartbeat: instance -> renew lease; registry evicts if it stops
Discovery: client -> "give me healthy 'orders' instances" -> [10.0.0.5, 10.0.0.7] -> LB
Kubernetes does this via DNS: http://orders.default.svc.cluster.local
\`\`\``,tech:"design-patterns",displayNumber:51,sortKey:2535},{id:"design-patterns-2536",number:52,title:"Explain the Cache-Aside (Lazy Loading) pattern.",question:"Explain the Cache-Aside (Lazy Loading) pattern.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`In Cache-Aside the application manages the cache: on read, check the cache; on a **miss**, load from the database, populate the cache, and return. On write, update the DB and **invalidate** (or update) the cache entry. It keeps only requested data cached (efficient) and the cache stays optional (a cache outage just means DB hits). Concerns: stale data (use TTLs), and the thundering-herd on popular misses (use locks/singleflight). Contrast with read-through/write-through where the cache library handles loading.

\`\`\`java
Product getProduct(String id) {
    Product cached = cache.get(id);
    if (cached != null) return cached;              // hit
    Product p = db.findById(id);                    // miss -> load from DB
    cache.set(id, p, Duration.ofMinutes(10));       // populate with TTL
    return p;
}
void updateProduct(Product p) {
    db.save(p);
    cache.evict(p.id());                            // invalidate on write
}
\`\`\``,tech:"design-patterns",displayNumber:52,sortKey:2536},{id:"design-patterns-2537",number:53,title:"What is the Layered (N-Tier) architecture pattern?",question:"What is the Layered (N-Tier) architecture pattern?",difficulty:"Basic",category:"Architecture Patterns",answer:`Layered architecture organizes code into horizontal layers each with a defined responsibility — commonly **Presentation → Application/Service → Domain/Business → Persistence/Data** — where each layer depends only on the one below. It gives clear separation of concerns, testability, and replaceable layers, at the cost of possible rigidity and pass-through boilerplate. It's the default for many monoliths and each microservice's internal structure.

\`\`\`text
[ Presentation ]  Controllers / REST / UI
        │  (depends on)
[ Application  ]  Services / use-cases / orchestration
        │
[ Domain      ]  Entities / business rules
        │
[ Persistence ]  Repositories / DAO / DB
Rule: dependencies point DOWNWARD only; upper layers never leak into lower ones.
\`\`\``,tech:"design-patterns",displayNumber:53,sortKey:2537},{id:"design-patterns-2538",number:54,title:"Explain the Strangler Fig pattern for modernization.",question:"Explain the Strangler Fig pattern for modernization.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Strangler Fig incrementally replaces a legacy system by routing requests through a facade/proxy and peeling off one capability at a time into new services, until the old system is fully "strangled" and retired. It avoids a risky big-bang rewrite: old and new run in parallel, you migrate slice by slice, and you can roll back per capability. Requires a routing layer and careful data decoupling.

\`\`\`java
// Facade routes each capability to legacy or the new service; flip per feature
@RequestMapping("/**")
Object route(HttpServletRequest req) {
    String path = req.getRequestURI();
    return migrated.contains(capabilityOf(path))
        ? newService.forward(req)      // already extracted
        : legacyMonolith.forward(req); // not yet migrated
}
// Migrate one capability -> verify -> add to \`migrated\` -> repeat until legacy is gone.
\`\`\``,tech:"design-patterns",displayNumber:54,sortKey:2538},{id:"design-patterns-2539",number:55,title:"Explain the Bulkhead pattern at the architecture level.",question:"Explain the Bulkhead pattern at the architecture level.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Bulkhead isolates resources (thread pools, connection pools, or even separate service instances/clusters) per dependency or tenant, so a failure or overload in one partition can't consume all resources and take down the whole system — like watertight compartments in a ship. Example: separate thread pools per downstream service, or a dedicated instance pool for premium customers. It contains blast radius and preserves capacity for healthy paths.

\`\`\`java
// Separate bounded pools per dependency -> one can't starve the others
Bulkhead paymentBh = Bulkhead.of("payment",
    BulkheadConfig.custom().maxConcurrentCalls(30).build());
Bulkhead reportBh  = Bulkhead.of("report",
    BulkheadConfig.custom().maxConcurrentCalls(5).build());   // slow dep, capped tight
// A flood of slow report calls exhausts only reportBh, not payment capacity.
\`\`\``,tech:"design-patterns",displayNumber:55,sortKey:2539},{id:"design-patterns-2540",number:56,title:"Explain the Event-Driven Architecture (EDA) pattern.",question:"Explain the Event-Driven Architecture (EDA) pattern.",difficulty:"Intermediate",category:"Architecture Patterns",answer:`EDA structures a system around the production, detection, and reaction to **events**. Components communicate by emitting/consuming events (via a broker) rather than direct calls, giving loose coupling, scalability, and real-time responsiveness. Patterns: event notification, event-carried state transfer, event sourcing, CQRS. Trade-offs: eventual consistency, harder debugging/ordering, and the need for idempotent consumers. Great for reactive, high-throughput, integration-heavy systems.

\`\`\`text
order-service --emit OrderPlaced--► [ broker ] ─► payment-service (charge)
                                                ─► inventory-service (reserve)
                                                ─► notification-service (email)
Loose coupling: producer doesn't know consumers; add consumers freely.
Cost: eventual consistency + idempotency + ordering concerns.
\`\`\``,tech:"design-patterns",displayNumber:56,sortKey:2540},{id:"design-patterns-2541",number:57,title:"What is the Client-Server vs Peer-to-Peer architecture?",question:"What is the Client-Server vs Peer-to-Peer architecture?",difficulty:"Basic",category:"Architecture Patterns",answer:`- **Client-Server**: clients request, a central server provides resources/services. Simple, centralized control/security, but the server is a scaling bottleneck / single point of failure (mitigated by clustering + load balancers). Most web apps.
- **Peer-to-Peer (P2P)**: nodes act as both client and server, sharing resources directly with no central authority. Highly scalable/resilient (no single point), but harder to secure, coordinate, and ensure consistency. Uses: BitTorrent, blockchains, WebRTC media.

\`\`\`text
Client-Server:   [clients] ──► [ central server(s) ]        (centralized, simple)
Peer-to-Peer:    node ⇄ node ⇄ node   (each is client+server, decentralized)
Pick C-S for control/consistency; P2P for decentralization/resilience.
\`\`\``,tech:"design-patterns",displayNumber:57,sortKey:2541},{id:"design-patterns-2542",number:58,title:"How do you choose the right architecture/distributed pattern (summary)?",question:"How do you choose the right architecture/distributed pattern (summary)?",difficulty:"Intermediate",category:"Architecture Patterns",answer:`Map the problem to the pattern: distribute traffic → **Load Balancer**; front many services → **Reverse Proxy / API Gateway**; offload cross-cutting concerns per instance → **Sidecar/Service Mesh**; protect against failing dependencies → **Circuit Breaker + Retry + Bulkhead**; scale reads / HA → **Master-Replica**; scale writes/storage → **Sharding**; single coordinator → **Leader Election**; decouple producers/consumers → **Pub/Sub / EDA**; speed reads → **Cache-Aside**; find instances → **Service Registry**; modernize a monolith → **Strangler Fig**; cap usage → **Rate Limiter**. Always justify by the specific quality attribute (scalability, availability, resilience) you're solving for.

\`\`\`text
Problem                         -> Pattern
distribute incoming traffic     -> Load Balancer
single entry over many services -> Reverse Proxy / API Gateway
per-instance cross-cutting      -> Sidecar / Service Mesh
failing/slow dependency         -> Circuit Breaker + Retry + Bulkhead
scale reads / high availability -> Master-Replica (Leader-Follower)
scale writes & storage          -> Sharding
one coordinator among nodes     -> Leader Election
decouple producer/consumer      -> Pub/Sub / Event-Driven
faster reads                    -> Cache-Aside
locate service instances        -> Service Registry / Discovery
modernize legacy incrementally  -> Strangler Fig
protect from overload/abuse     -> Rate Limiter / Throttling
\`\`\``,tech:"design-patterns",displayNumber:58,sortKey:2542},{id:"sql-2543",number:1,title:"Select all columns from a table",question:"Select all columns from a table",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`SELECT *` returns every column. In production prefer listing columns explicitly to avoid fetching unneeeded data and to stay stable if the schema changes.\n\n```sql\nSELECT * FROM employees;\n```",tech:"sql",displayNumber:1,sortKey:2543},{id:"sql-2544",number:2,title:"Select specific columns",question:"Select specific columns",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"List the column names after `SELECT`. This reduces I/O and makes intent clear.\n\n```sql\nSELECT id, name, salary FROM employees;\n```",tech:"sql",displayNumber:2,sortKey:2544},{id:"sql-2545",number:3,title:"Filter rows with WHERE",question:"Filter rows with WHERE",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`WHERE` restricts rows to those matching a condition, evaluated before grouping and selection.\n\n```sql\nSELECT name, salary FROM employees WHERE salary > 50000;\n```",tech:"sql",displayNumber:3,sortKey:2545},{id:"sql-2546",number:4,title:"Sort results with ORDER BY",question:"Sort results with ORDER BY",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`ORDER BY column [ASC|DESC]` sorts the result. Default is ascending; use `DESC` for descending and list multiple columns for tie-breaking.\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC, name ASC;\n```",tech:"sql",displayNumber:4,sortKey:2546},{id:"sql-2547",number:5,title:"Return only unique values with DISTINCT",question:"Return only unique values with DISTINCT",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`DISTINCT` removes duplicate rows from the result set based on the selected columns.\n\n```sql\nSELECT DISTINCT department FROM employees;\n```",tech:"sql",displayNumber:5,sortKey:2547},{id:"sql-2548",number:6,title:"Limit the number of rows returned",question:"Limit the number of rows returned",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Use `LIMIT n` (MySQL/Postgres) or `FETCH FIRST n ROWS ONLY` / `TOP n` (SQL Server) to cap rows, typically after `ORDER BY`.\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC LIMIT 5;\n```",tech:"sql",displayNumber:6,sortKey:2548},{id:"sql-2549",number:7,title:"Skip rows with OFFSET (pagination)",question:"Skip rows with OFFSET (pagination)",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`OFFSET n` skips the first n rows; combined with `LIMIT` it implements page-based pagination. Large offsets are slow because the DB still scans skipped rows.\n\n```sql\nSELECT name FROM employees ORDER BY id LIMIT 10 OFFSET 20; -- page 3, size 10\n```",tech:"sql",displayNumber:7,sortKey:2549},{id:"sql-2550",number:8,title:"Filter with multiple conditions (AND / OR)",question:"Filter with multiple conditions (AND / OR)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Combine conditions with `AND`/`OR`; use parentheses to control precedence since `AND` binds tighter than `OR`.\n\n```sql\nSELECT * FROM employees\nWHERE department = 'Sales' AND (salary > 60000 OR manager_id IS NULL);\n```",tech:"sql",displayNumber:8,sortKey:2550},{id:"sql-2551",number:9,title:"Filter a range with BETWEEN",question:"Filter a range with BETWEEN",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`BETWEEN a AND b` is inclusive of both bounds. It's shorthand for `col >= a AND col <= b`.\n\n```sql\nSELECT name, salary FROM employees WHERE salary BETWEEN 40000 AND 60000;\n```",tech:"sql",displayNumber:9,sortKey:2551},{id:"sql-2552",number:10,title:"Filter a list with IN",question:"Filter a list with IN",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`IN (...)` matches any value in the list, a cleaner alternative to multiple `OR`s.\n\n```sql\nSELECT * FROM employees WHERE department IN ('Sales', 'HR', 'Finance');\n```",tech:"sql",displayNumber:10,sortKey:2552},{id:"sql-2553",number:11,title:"Pattern matching with LIKE",question:"Pattern matching with LIKE",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`LIKE` matches patterns: `%` = any sequence of characters, `_` = a single character. Use `ILIKE` (Postgres) for case-insensitive matching.\n\n```sql\nSELECT name FROM employees WHERE name LIKE 'A%';   -- starts with A\nSELECT name FROM employees WHERE name LIKE '_a%';  -- 'a' as 2nd char\n```",tech:"sql",displayNumber:11,sortKey:2553},{id:"sql-2554",number:12,title:"Handle NULL values (IS NULL / IS NOT NULL)",question:"Handle NULL values (IS NULL / IS NOT NULL)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:'NULL means "unknown"; comparisons with `=` don\'t work. Use `IS NULL` / `IS NOT NULL`. `NULL = NULL` is not true.\n\n```sql\nSELECT name FROM employees WHERE manager_id IS NULL;\n```',tech:"sql",displayNumber:12,sortKey:2554},{id:"sql-2555",number:13,title:"Replace NULLs with a default (COALESCE)",question:"Replace NULLs with a default (COALESCE)",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`COALESCE(a, b, ...)` returns the first non-NULL argument, useful for substituting defaults.\n\n```sql\nSELECT name, COALESCE(commission, 0) AS commission FROM employees;\n```",tech:"sql",displayNumber:13,sortKey:2555},{id:"sql-2556",number:14,title:"Alias columns and tables (AS)",question:"Alias columns and tables (AS)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`AS` renames columns/tables in the output or for shorter references. The keyword is optional for table aliases.\n\n```sql\nSELECT e.name AS employee_name, e.salary * 12 AS annual_salary\nFROM employees e;\n```",tech:"sql",displayNumber:14,sortKey:2556},{id:"sql-2557",number:15,title:"Concatenate strings",question:"Concatenate strings",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Use `||` (standard/Postgres/Oracle) or `CONCAT(...)` (MySQL/SQL Server) to join strings.\n\n```sql\nSELECT CONCAT(first_name, ' ', last_name) AS full_name FROM employees;\n```",tech:"sql",displayNumber:15,sortKey:2557},{id:"sql-2558",number:16,title:"Compute values in SELECT (arithmetic)",question:"Compute values in SELECT (arithmetic)",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"You can include expressions in `SELECT`; they're evaluated per row.\n\n```sql\nSELECT name, salary, salary * 0.10 AS bonus FROM employees;\n```",tech:"sql",displayNumber:16,sortKey:2558},{id:"sql-2559",number:17,title:"Conditional logic with CASE",question:"Conditional logic with CASE",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`CASE WHEN ... THEN ... ELSE ... END` returns different values per row based on conditions — SQL's if/else.\n\n```sql\nSELECT name,\n  CASE WHEN salary >= 80000 THEN 'High'\n       WHEN salary >= 50000 THEN 'Medium'\n       ELSE 'Low' END AS band\nFROM employees;\n```",tech:"sql",displayNumber:17,sortKey:2559},{id:"sql-2560",number:18,title:"Convert case and trim strings",question:"Convert case and trim strings",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`UPPER`/`LOWER` change case; `TRIM`/`LTRIM`/`RTRIM` remove whitespace. Handy for normalizing text before comparison.\n\n```sql\nSELECT UPPER(name) AS upper_name, TRIM(department) AS dept FROM employees;\n```",tech:"sql",displayNumber:18,sortKey:2560},{id:"sql-2561",number:19,title:"Extract a substring",question:"Extract a substring",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`SUBSTRING(str FROM start FOR length)` (or `SUBSTR`) extracts part of a string; positions are usually 1-based.\n\n```sql\nSELECT SUBSTRING(name FROM 1 FOR 3) AS prefix FROM employees;\n```",tech:"sql",displayNumber:19,sortKey:2561},{id:"sql-2562",number:20,title:"Get the length of a string",question:"Get the length of a string",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`LENGTH(str)` (or `LEN` in SQL Server) returns the number of characters.\n\n```sql\nSELECT name, LENGTH(name) AS name_len FROM employees;\n```",tech:"sql",displayNumber:20,sortKey:2562},{id:"sql-2563",number:21,title:"Round and format numbers",question:"Round and format numbers",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`ROUND(value, decimals)` rounds; `CEIL`/`FLOOR` go up/down. Use for currency and display formatting.\n\n```sql\nSELECT name, ROUND(salary / 12.0, 2) AS monthly FROM employees;\n```",tech:"sql",displayNumber:21,sortKey:2563},{id:"sql-2564",number:22,title:"Get the current date/time",question:"Get the current date/time",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`CURRENT_DATE`, `CURRENT_TIMESTAMP` (or `NOW()`) return the current date/time from the database server.\n\n```sql\nSELECT name, hire_date, CURRENT_DATE - hire_date AS days_employed FROM employees;\n```",tech:"sql",displayNumber:22,sortKey:2564},{id:"sql-2565",number:23,title:"Extract parts of a date",question:"Extract parts of a date",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`EXTRACT(YEAR FROM date)` (or `YEAR()`/`MONTH()`) pulls date components, useful for grouping by year/month.\n\n```sql\nSELECT name, EXTRACT(YEAR FROM hire_date) AS hire_year FROM employees;\n```",tech:"sql",displayNumber:23,sortKey:2565},{id:"sql-2566",number:24,title:"Filter by date range",question:"Filter by date range",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Compare date columns with literals or use `BETWEEN`. Prefer half-open ranges (`>= start AND < next_day`) to correctly include timestamps.\n\n```sql\nSELECT * FROM orders WHERE order_date >= '2024-01-01' AND order_date < '2024-02-01';\n```",tech:"sql",displayNumber:24,sortKey:2566},{id:"sql-2567",number:25,title:"Count rows",question:"Count rows",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`COUNT(*)` counts all rows; `COUNT(col)` counts non-NULL values of a column; `COUNT(DISTINCT col)` counts unique non-NULL values.\n\n```sql\nSELECT COUNT(*) AS total, COUNT(DISTINCT department) AS depts FROM employees;\n```",tech:"sql",displayNumber:25,sortKey:2567},{id:"sql-2568",number:26,title:"Order NULLs first or last",question:"Order NULLs first or last",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`ORDER BY col NULLS LAST` (Postgres/Oracle) controls NULL placement. In MySQL, sort on `col IS NULL` first to emulate it.\n\n```sql\nSELECT name, commission FROM employees ORDER BY commission DESC NULLS LAST;\n```",tech:"sql",displayNumber:26,sortKey:2568},{id:"sql-2569",number:27,title:"Filter with NOT",question:"Filter with NOT",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"`NOT` negates a condition; combine with `IN`, `LIKE`, `BETWEEN`. Beware `NOT IN` with NULLs, which can return no rows unexpectedly.\n\n```sql\nSELECT * FROM employees WHERE department NOT IN ('HR', 'Legal');\n```",tech:"sql",displayNumber:27,sortKey:2569},{id:"sql-2570",number:28,title:"Use column aliases in ORDER BY",question:"Use column aliases in ORDER BY",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`ORDER BY` can reference a `SELECT` alias (it's evaluated after projection), unlike `WHERE`, which cannot use aliases.\n\n```sql\nSELECT name, salary * 12 AS annual FROM employees ORDER BY annual DESC;\n```",tech:"sql",displayNumber:28,sortKey:2570},{id:"sql-2571",number:29,title:"Return a constant/computed column",question:"Return a constant/computed column",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"You can select literals or expressions without a table reference for testing, or add a constant tag column to results.\n\n```sql\nSELECT name, 'active' AS status FROM employees;\n```",tech:"sql",displayNumber:29,sortKey:2571},{id:"sql-2572",number:30,title:"Filter with comparison operators",question:"Filter with comparison operators",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Standard operators: `=`, `<>`/`!=`, `<`, `>`, `<=`, `>=`. Use them in `WHERE`/`HAVING`.\n\n```sql\nSELECT name FROM employees WHERE salary <> 50000;\n```",tech:"sql",displayNumber:30,sortKey:2572},{id:"sql-2573",number:31,title:"Combine result sets with UNION",question:"Combine result sets with UNION",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`UNION` stacks two result sets (same columns/types) and removes duplicates; `UNION ALL` keeps duplicates and is faster.\n\n```sql\nSELECT name FROM employees\nUNION\nSELECT name FROM contractors;\n```",tech:"sql",displayNumber:31,sortKey:2573},{id:"sql-2574",number:32,title:"Difference between UNION and UNION ALL",question:"Difference between UNION and UNION ALL",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`UNION` deduplicates (extra sort/hash cost); `UNION ALL` returns everything including duplicates and is more performant when you know rows are distinct or duplicates are acceptable.\n\n```sql\nSELECT city FROM customers\nUNION ALL\nSELECT city FROM suppliers; -- keeps duplicates\n```",tech:"sql",displayNumber:32,sortKey:2574},{id:"sql-2575",number:33,title:"INTERSECT and EXCEPT",question:"INTERSECT and EXCEPT",difficulty:"Advanced",category:"SQL Basics & SELECT",answer:"`INTERSECT` returns rows common to both queries; `EXCEPT` (`MINUS` in Oracle) returns rows in the first not in the second. Both deduplicate.\n\n```sql\nSELECT id FROM active_users\nEXCEPT\nSELECT id FROM banned_users;\n```",tech:"sql",displayNumber:33,sortKey:2575},{id:"sql-2576",number:34,title:"Cast/convert data types",question:"Cast/convert data types",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`CAST(expr AS type)` (or `::type` in Postgres) converts between types, e.g. text to number or date.\n\n```sql\nSELECT CAST(salary AS VARCHAR) AS salary_text, order_date::DATE FROM orders;\n```",tech:"sql",displayNumber:34,sortKey:2576},{id:"sql-2577",number:35,title:"Filter using a computed condition",question:"Filter using a computed condition",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"You can put expressions in `WHERE`, but applying functions to a column can prevent index use. Prefer sargable predicates (compare the raw column to a computed constant).\n\n```sql\n-- non-sargable: WHERE YEAR(order_date) = 2024\nSELECT * FROM orders WHERE order_date >= '2024-01-01' AND order_date < '2025-01-01';\n```",tech:"sql",displayNumber:35,sortKey:2577},{id:"sql-2578",number:36,title:"Return top N per simple sort",question:"Return top N per simple sort",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"Combine `ORDER BY` with `LIMIT`/`FETCH FIRST`/`TOP`. Ensure a deterministic order (add a tie-breaker) for stable results.\n\n```sql\nSELECT name, salary FROM employees ORDER BY salary DESC, id LIMIT 3;\n```",tech:"sql",displayNumber:36,sortKey:2578},{id:"sql-2579",number:37,title:"Use aliases with expressions and functions",question:"Use aliases with expressions and functions",difficulty:"Basic",category:"SQL Basics & SELECT",answer:"Give computed columns clear aliases so the result set has meaningful names for downstream code.\n\n```sql\nSELECT name, ROUND(salary * 1.1, 2) AS raised_salary FROM employees;\n```",tech:"sql",displayNumber:37,sortKey:2579},{id:"sql-2580",number:38,title:"Filter rows by string length",question:"Filter rows by string length",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"Use `LENGTH()` in the `WHERE` clause. Note applying a function to the column may bypass indexes.\n\n```sql\nSELECT name FROM employees WHERE LENGTH(name) > 10;\n```",tech:"sql",displayNumber:38,sortKey:2580},{id:"sql-2581",number:39,title:"Replace substring values",question:"Replace substring values",difficulty:"Intermediate",category:"SQL Basics & SELECT",answer:"`REPLACE(str, from, to)` substitutes all occurrences of a substring — useful for cleaning/formatting text.\n\n```sql\nSELECT REPLACE(phone, '-', '') AS digits FROM customers;\n```",tech:"sql",displayNumber:39,sortKey:2581},{id:"sql-2582",number:40,title:"Order of SQL clause evaluation",question:"Order of SQL clause evaluation",difficulty:"Advanced",category:"SQL Basics & SELECT",answer:"Logical order: `FROM` → `WHERE` → `GROUP BY` → `HAVING` → `SELECT` → `DISTINCT` → `ORDER BY` → `LIMIT`. This is why `WHERE` can't use `SELECT` aliases but `ORDER BY` can.\n\n```sql\n-- WHERE runs before SELECT, so it can't reference the alias 'annual'\nSELECT salary * 12 AS annual FROM employees WHERE salary > 40000 ORDER BY annual;\n```",tech:"sql",displayNumber:40,sortKey:2582},{id:"sql-2583",number:1,title:"What is a JOIN?",question:"What is a JOIN?",difficulty:"Basic",category:"Joins",answer:"A JOIN combines rows from two or more tables based on a related column, letting you query normalized data as a single result set.\n\n```sql\nSELECT e.name, d.name AS dept\nFROM employees e\nJOIN departments d ON e.department_id = d.id;\n```",tech:"sql",displayNumber:1,sortKey:2583},{id:"sql-2584",number:2,title:"What is an INNER JOIN?",question:"What is an INNER JOIN?",difficulty:"Basic",category:"Joins",answer:"An INNER JOIN returns only rows that have matching values in both tables. Rows without a match on either side are excluded.\n\n```sql\nSELECT o.id, c.name\nFROM orders o\nINNER JOIN customers c ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:2,sortKey:2584},{id:"sql-2585",number:3,title:"What is a LEFT JOIN?",question:"What is a LEFT JOIN?",difficulty:"Basic",category:"Joins",answer:"A LEFT (OUTER) JOIN returns all rows from the left table plus matching rows from the right; unmatched right columns are NULL.\n\n```sql\nSELECT c.name, o.id\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:3,sortKey:2585},{id:"sql-2586",number:4,title:"What is a RIGHT JOIN?",question:"What is a RIGHT JOIN?",difficulty:"Basic",category:"Joins",answer:"A RIGHT JOIN returns all rows from the right table plus matches from the left; unmatched left columns are NULL. It's a mirror of LEFT JOIN.\n\n```sql\nSELECT e.name, d.name AS dept\nFROM employees e\nRIGHT JOIN departments d ON e.department_id = d.id;\n```",tech:"sql",displayNumber:4,sortKey:2586},{id:"sql-2587",number:5,title:"What is a FULL OUTER JOIN?",question:"What is a FULL OUTER JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A FULL OUTER JOIN returns all rows from both tables, matching where possible and filling NULLs where there's no match on either side.\n\n```sql\nSELECT c.name, o.id\nFROM customers c\nFULL OUTER JOIN orders o ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:5,sortKey:2587},{id:"sql-2588",number:6,title:"What is a CROSS JOIN?",question:"What is a CROSS JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A CROSS JOIN produces the Cartesian product — every row of the first table paired with every row of the second. Useful for generating combinations; dangerous if unintentional.\n\n```sql\nSELECT s.size, c.color FROM sizes s CROSS JOIN colors c;\n```",tech:"sql",displayNumber:6,sortKey:2588},{id:"sql-2589",number:7,title:"What is a SELF JOIN?",question:"What is a SELF JOIN?",difficulty:"Intermediate",category:"Joins",answer:"A self join joins a table to itself using aliases, useful for hierarchical/related-row queries like employees and their managers.\n\n```sql\nSELECT e.name AS employee, m.name AS manager\nFROM employees e\nLEFT JOIN employees m ON e.manager_id = m.id;\n```",tech:"sql",displayNumber:7,sortKey:2589},{id:"sql-2590",number:8,title:"Difference between INNER JOIN and LEFT JOIN",question:"Difference between INNER JOIN and LEFT JOIN",difficulty:"Basic",category:"Joins",answer:"INNER JOIN returns only matched rows; LEFT JOIN returns all left rows including unmatched ones (with NULLs). Use LEFT JOIN when you must keep rows that may lack a related record.\n\n```sql\n-- customers with no orders appear only with LEFT JOIN\nSELECT c.name, o.id FROM customers c LEFT JOIN orders o ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:8,sortKey:2590},{id:"sql-2591",number:9,title:"Find rows in one table with no match in another",question:"Find rows in one table with no match in another",difficulty:"Intermediate",category:"Joins",answer:"Use a LEFT JOIN and filter where the right key `IS NULL` (anti-join), returning only unmatched left rows.\n\n```sql\nSELECT c.name\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nWHERE o.id IS NULL; -- customers who never ordered\n```",tech:"sql",displayNumber:9,sortKey:2591},{id:"sql-2592",number:10,title:"Join three or more tables",question:"Join three or more tables",difficulty:"Intermediate",category:"Joins",answer:`Chain JOIN clauses; each adds a table with its own ON condition. Order doesn't change results for inner joins but can affect readability and the optimizer's plan.

\`\`\`sql
SELECT o.id, c.name, p.title
FROM orders o
JOIN customers c ON o.customer_id = c.id
JOIN products p ON o.product_id = p.id;
\`\`\``,tech:"sql",displayNumber:10,sortKey:2592},{id:"sql-2593",number:11,title:"Join on multiple columns",question:"Join on multiple columns",difficulty:"Intermediate",category:"Joins",answer:"Combine conditions in `ON` with `AND` when the relationship spans multiple columns (composite keys).\n\n```sql\nSELECT *\nFROM sales s\nJOIN targets t ON s.region = t.region AND s.year = t.year;\n```",tech:"sql",displayNumber:11,sortKey:2593},{id:"sql-2594",number:12,title:"Difference between ON and WHERE in a join",question:"Difference between ON and WHERE in a join",difficulty:"Advanced",category:"Joins",answer:"For INNER JOIN they're equivalent. For OUTER JOINs, a condition in `ON` filters the match (preserving unmatched rows), while the same condition in `WHERE` runs after the join and can turn a LEFT JOIN into an effective INNER JOIN by removing NULL rows.\n\n```sql\n-- keeps customers with no 2024 orders:\nSELECT c.name, o.id FROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id AND o.order_date >= '2024-01-01';\n```",tech:"sql",displayNumber:12,sortKey:2594},{id:"sql-2595",number:13,title:"Join a table to itself for hierarchy",question:"Join a table to itself for hierarchy",difficulty:"Intermediate",category:"Joins",answer:"Self-join on the parent-key column to relate a row to its parent, e.g. employee → manager or category → parent category.\n\n```sql\nSELECT child.name AS category, parent.name AS parent_category\nFROM categories child\nLEFT JOIN categories parent ON child.parent_id = parent.id;\n```",tech:"sql",displayNumber:13,sortKey:2595},{id:"sql-2596",number:14,title:"Count related rows per parent",question:"Count related rows per parent",difficulty:"Intermediate",category:"Joins",answer:"LEFT JOIN then `GROUP BY` the parent and `COUNT` the child key (not `*`) so parents with zero children count as 0.\n\n```sql\nSELECT c.name, COUNT(o.id) AS order_count\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```",tech:"sql",displayNumber:14,sortKey:2596},{id:"sql-2597",number:15,title:"Why COUNT(child.id) instead of COUNT(*) in a LEFT JOIN?",question:"Why COUNT(child.id) instead of COUNT(*) in a LEFT JOIN?",difficulty:"Advanced",category:"Joins",answer:"`COUNT(*)` counts the joined rows, so an unmatched parent (one row with NULLs) counts as 1. `COUNT(child.id)` counts non-NULL child keys, correctly yielding 0 for parents with no children.\n\n```sql\nSELECT c.name, COUNT(o.id) AS orders -- 0 for customers with none\nFROM customers c LEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```",tech:"sql",displayNumber:15,sortKey:2597},{id:"sql-2598",number:16,title:"Join and aggregate (sum per group)",question:"Join and aggregate (sum per group)",difficulty:"Intermediate",category:"Joins",answer:"Join related tables, then `GROUP BY` a dimension and aggregate a measure, e.g. total order amount per customer.\n\n```sql\nSELECT c.name, SUM(o.amount) AS total_spent\nFROM customers c\nJOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```",tech:"sql",displayNumber:16,sortKey:2598},{id:"sql-2599",number:17,title:"Avoid duplicate rows from one-to-many joins",question:"Avoid duplicate rows from one-to-many joins",difficulty:"Advanced",category:"Joins",answer:"Joining a one-to-many relationship multiplies parent rows. Deduplicate with aggregation (`GROUP BY`), `DISTINCT`, or by pre-aggregating the child table in a subquery before joining.\n\n```sql\nSELECT c.name, sub.total\nFROM customers c\nJOIN (SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id) sub\n  ON sub.customer_id = c.id;\n```",tech:"sql",displayNumber:17,sortKey:2599},{id:"sql-2600",number:18,title:"Join with a filtered (derived) table",question:"Join with a filtered (derived) table",difficulty:"Advanced",category:"Joins",answer:`Join to a subquery (derived table) that pre-filters/aggregates data, reducing rows and clarifying intent.

\`\`\`sql
SELECT e.name, big.total
FROM employees e
JOIN (SELECT employee_id, SUM(amount) total FROM sales GROUP BY employee_id HAVING SUM(amount) > 100000) big
  ON big.employee_id = e.id;
\`\`\``,tech:"sql",displayNumber:18,sortKey:2600},{id:"sql-2601",number:19,title:"Semi-join with EXISTS vs JOIN",question:"Semi-join with EXISTS vs JOIN",difficulty:"Advanced",category:"Joins",answer:"To check existence of related rows without duplicating parents, use `EXISTS` (a semi-join) instead of a JOIN. It stops at the first match and doesn't multiply rows.\n\n```sql\nSELECT c.name FROM customers c\nWHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```",tech:"sql",displayNumber:19,sortKey:2601},{id:"sql-2602",number:20,title:"Anti-join with NOT EXISTS",question:"Anti-join with NOT EXISTS",difficulty:"Advanced",category:"Joins",answer:"`NOT EXISTS` returns parents with no matching child rows and, unlike `NOT IN`, behaves correctly when the subquery can produce NULLs.\n\n```sql\nSELECT c.name FROM customers c\nWHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```",tech:"sql",displayNumber:20,sortKey:2602},{id:"sql-2603",number:21,title:"Difference between NOT IN and NOT EXISTS with NULLs",question:"Difference between NOT IN and NOT EXISTS with NULLs",difficulty:"Experienced",category:"Joins",answer:"If the `NOT IN` subquery returns any NULL, the whole predicate becomes UNKNOWN and no rows are returned. `NOT EXISTS` is NULL-safe and generally preferred for anti-joins.\n\n```sql\n-- risky if orders.customer_id has NULLs:\nSELECT * FROM customers WHERE id NOT IN (SELECT customer_id FROM orders);\n```",tech:"sql",displayNumber:21,sortKey:2603},{id:"sql-2604",number:22,title:"Join using USING clause",question:"Join using USING clause",difficulty:"Intermediate",category:"Joins",answer:"`USING(col)` is shorthand for `ON a.col = b.col` when the join columns share a name, and it collapses them into a single output column.\n\n```sql\nSELECT * FROM orders JOIN customers USING (customer_id);\n```",tech:"sql",displayNumber:22,sortKey:2604},{id:"sql-2605",number:23,title:"Natural join and why to avoid it",question:"Natural join and why to avoid it",difficulty:"Advanced",category:"Joins",answer:"`NATURAL JOIN` auto-joins on all same-named columns. It's risky because schema changes can silently alter the join condition; prefer explicit `ON`/`USING`.\n\n```sql\nSELECT * FROM orders NATURAL JOIN customers; -- fragile, avoid in production\n```",tech:"sql",displayNumber:23,sortKey:2605},{id:"sql-2606",number:24,title:"Join to find matching pairs (e.g. same city)",question:"Join to find matching pairs (e.g. same city)",difficulty:"Advanced",category:"Joins",answer:"Self-join on the shared attribute with an inequality on ids to avoid pairing a row with itself and to avoid mirror duplicates.\n\n```sql\nSELECT a.name, b.name, a.city\nFROM customers a\nJOIN customers b ON a.city = b.city AND a.id < b.id;\n```",tech:"sql",displayNumber:24,sortKey:2606},{id:"sql-2607",number:25,title:"Left join with default values",question:"Left join with default values",difficulty:"Intermediate",category:"Joins",answer:"Wrap nullable right-side columns in `COALESCE` to supply defaults for unmatched rows.\n\n```sql\nSELECT c.name, COALESCE(SUM(o.amount), 0) AS total\nFROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```",tech:"sql",displayNumber:25,sortKey:2607},{id:"sql-2608",number:26,title:"Join and filter on the right table correctly",question:"Join and filter on the right table correctly",difficulty:"Advanced",category:"Joins",answer:"To keep left rows while filtering the right table, put the right-table condition in `ON`, not `WHERE`; a `WHERE` filter on the right side removes unmatched (NULL) rows.\n\n```sql\nSELECT c.name, o.id FROM customers c\nLEFT JOIN orders o ON o.customer_id = c.id AND o.status = 'paid';\n```",tech:"sql",displayNumber:26,sortKey:2608},{id:"sql-2609",number:27,title:"Join two tables and pick the latest related row",question:"Join two tables and pick the latest related row",difficulty:"Experienced",category:"Joins",answer:`Use a lateral/correlated subquery or window function to fetch only the most recent child per parent, avoiding fan-out from all children.

\`\`\`sql
SELECT c.name, last_o.amount
FROM customers c
JOIN LATERAL (
  SELECT amount FROM orders o WHERE o.customer_id = c.id
  ORDER BY o.order_date DESC LIMIT 1
) last_o ON true;
\`\`\``,tech:"sql",displayNumber:27,sortKey:2609},{id:"sql-2610",number:28,title:"Join with aggregation and HAVING",question:"Join with aggregation and HAVING",difficulty:"Advanced",category:"Joins",answer:"Filter aggregated join results with `HAVING`, e.g. customers whose total spend exceeds a threshold.\n\n```sql\nSELECT c.name, SUM(o.amount) AS total\nFROM customers c JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name\nHAVING SUM(o.amount) > 10000;\n```",tech:"sql",displayNumber:28,sortKey:2610},{id:"sql-2611",number:29,title:"Multiple LEFT JOINs",question:"Multiple LEFT JOINs",difficulty:"Advanced",category:"Joins",answer:`Chaining multiple LEFT JOINs keeps all base rows while attaching optional related data from several tables. Watch for row multiplication when several joined tables are one-to-many.

\`\`\`sql
SELECT e.name, d.name AS dept, p.name AS project
FROM employees e
LEFT JOIN departments d ON e.department_id = d.id
LEFT JOIN projects p ON p.lead_id = e.id;
\`\`\``,tech:"sql",displayNumber:29,sortKey:2611},{id:"sql-2612",number:30,title:"Emulate FULL OUTER JOIN in MySQL",question:"Emulate FULL OUTER JOIN in MySQL",difficulty:"Experienced",category:"Joins",answer:"MySQL lacks FULL OUTER JOIN; emulate it by `UNION`-ing a LEFT JOIN with a RIGHT JOIN (or a LEFT JOIN of the swapped tables with an anti-join filter).\n\n```sql\nSELECT c.name, o.id FROM customers c LEFT JOIN orders o ON o.customer_id = c.id\nUNION\nSELECT c.name, o.id FROM customers c RIGHT JOIN orders o ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:30,sortKey:2612},{id:"sql-2613",number:31,title:"Join performance: indexing join keys",question:"Join performance: indexing join keys",difficulty:"Experienced",category:"Joins",answer:"Index the columns used in `ON` (usually the foreign key on the child side; primary keys are already indexed). Without indexes the optimizer may resort to slow nested-loop or full scans.\n\n```sql\nCREATE INDEX idx_orders_customer ON orders(customer_id);\n```",tech:"sql",displayNumber:31,sortKey:2613},{id:"sql-2614",number:32,title:"Nested loop vs hash vs merge join",question:"Nested loop vs hash vs merge join",difficulty:"Experienced",category:"Joins",answer:"Nested loop is best for small/indexed inputs; hash join suits large unsorted sets joined on equality; merge join is efficient for large pre-sorted/indexed inputs. The optimizer chooses based on statistics; `EXPLAIN` reveals which it picks.\n\n```sql\nEXPLAIN SELECT * FROM orders o JOIN customers c ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:32,sortKey:2614},{id:"sql-2615",number:33,title:"Join with a range condition",question:"Join with a range condition",difficulty:"Advanced",category:"Joins",answer:"Joins can use inequalities (non-equi joins), e.g. matching a value to a band/tier table where it falls between bounds.\n\n```sql\nSELECT e.name, t.grade\nFROM employees e\nJOIN salary_tiers t ON e.salary BETWEEN t.min_salary AND t.max_salary;\n```",tech:"sql",displayNumber:33,sortKey:2615},{id:"sql-2616",number:34,title:"Deduplicate a join result",question:"Deduplicate a join result",difficulty:"Intermediate",category:"Joins",answer:"Use `DISTINCT` or aggregate to collapse duplicate rows produced by one-to-many joins when you only need unique parent rows.\n\n```sql\nSELECT DISTINCT c.id, c.name\nFROM customers c JOIN orders o ON o.customer_id = c.id;\n```",tech:"sql",displayNumber:34,sortKey:2616},{id:"sql-2617",number:35,title:"Join to a numbers/calendar table",question:"Join to a numbers/calendar table",difficulty:"Experienced",category:"Joins",answer:`Join to a generated calendar/numbers table to include periods (e.g. days/months) that have no data, producing gap-free reports with zeros.

\`\`\`sql
SELECT d.day, COALESCE(COUNT(o.id), 0) AS orders
FROM calendar d
LEFT JOIN orders o ON o.order_date = d.day
GROUP BY d.day ORDER BY d.day;
\`\`\``,tech:"sql",displayNumber:35,sortKey:2617},{id:"sql-2618",number:36,title:"Combine data with a UNION vs JOIN — which to use?",question:"Combine data with a UNION vs JOIN — which to use?",difficulty:"Intermediate",category:"Joins",answer:"JOIN combines columns from related tables (widening rows); UNION stacks rows from similar-shaped queries (lengthening the result). Use JOIN for relationships, UNION for concatenating sets.\n\n```sql\n-- JOIN adds columns; UNION adds rows\nSELECT name FROM employees UNION SELECT name FROM contractors;\n```",tech:"sql",displayNumber:36,sortKey:2618},{id:"sql-2619",number:37,title:"Find common rows between two tables",question:"Find common rows between two tables",difficulty:"Intermediate",category:"Joins",answer:"An INNER JOIN on the key (or `INTERSECT`) returns rows present in both tables.\n\n```sql\nSELECT a.id FROM table_a a JOIN table_b b ON a.id = b.id;\n```",tech:"sql",displayNumber:37,sortKey:2619},{id:"sql-2620",number:38,title:"Join with conditional aggregation",question:"Join with conditional aggregation",difficulty:"Experienced",category:"Joins",answer:"Combine a join with `SUM(CASE WHEN ...)` to compute multiple conditional measures in one pass (pivot-like).\n\n```sql\nSELECT c.name,\n  SUM(CASE WHEN o.status='paid' THEN o.amount ELSE 0 END) AS paid,\n  SUM(CASE WHEN o.status='refunded' THEN o.amount ELSE 0 END) AS refunded\nFROM customers c JOIN orders o ON o.customer_id = c.id\nGROUP BY c.name;\n```",tech:"sql",displayNumber:38,sortKey:2620},{id:"sql-2621",number:39,title:"Join order and the optimizer",question:"Join order and the optimizer",difficulty:"Experienced",category:"Joins",answer:"For inner joins, the written order doesn't dictate execution — the cost-based optimizer reorders based on statistics and indexes. Keeping statistics fresh and indexing keys matters more than manual ordering.\n\n```sql\nANALYZE orders; -- refresh statistics so the optimizer picks a good join order\n```",tech:"sql",displayNumber:39,sortKey:2621},{id:"sql-2622",number:40,title:"Join to explode an array/JSON (lateral)",question:"Join to explode an array/JSON (lateral)",difficulty:"Experienced",category:"Joins",answer:"Use a lateral join (`CROSS JOIN LATERAL` / `unnest` / `json_array_elements`) to expand array or JSON values into rows joined back to the parent.\n\n```sql\nSELECT p.id, tag\nFROM posts p\nCROSS JOIN LATERAL unnest(p.tags) AS tag;\n```",tech:"sql",displayNumber:40,sortKey:2622},{id:"sql-2623",number:1,title:"What are aggregate functions?",question:"What are aggregate functions?",difficulty:"Basic",category:"Aggregation & Grouping",answer:"Aggregate functions compute a single value over a set of rows: `COUNT`, `SUM`, `AVG`, `MIN`, `MAX`. They're used with or without `GROUP BY`.\n\n```sql\nSELECT COUNT(*), AVG(salary), MAX(salary) FROM employees;\n```",tech:"sql",displayNumber:1,sortKey:2623},{id:"sql-2624",number:2,title:"What is GROUP BY?",question:"What is GROUP BY?",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`GROUP BY` partitions rows into groups by one or more columns, and aggregates are computed per group.\n\n```sql\nSELECT department, COUNT(*) FROM employees GROUP BY department;\n```",tech:"sql",displayNumber:2,sortKey:2624},{id:"sql-2625",number:3,title:"Difference between WHERE and HAVING",question:"Difference between WHERE and HAVING",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`WHERE` filters rows before grouping; `HAVING` filters groups after aggregation. Aggregate conditions must go in `HAVING`.\n\n```sql\nSELECT department, AVG(salary) avg_sal\nFROM employees\nWHERE status = 'active'          -- row filter\nGROUP BY department\nHAVING AVG(salary) > 60000;      -- group filter\n```",tech:"sql",displayNumber:3,sortKey:2625},{id:"sql-2626",number:4,title:"Sum a column per group",question:"Sum a column per group",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`SUM(col)` totals values within each group defined by `GROUP BY`.\n\n```sql\nSELECT department, SUM(salary) AS total_payroll\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:4,sortKey:2626},{id:"sql-2627",number:5,title:"Average with rounding",question:"Average with rounding",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`AVG` ignores NULLs. Wrap in `ROUND` for clean output.\n\n```sql\nSELECT department, ROUND(AVG(salary), 2) AS avg_salary\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:5,sortKey:2627},{id:"sql-2628",number:6,title:"Count distinct values per group",question:"Count distinct values per group",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`COUNT(DISTINCT col)` counts unique non-NULL values within each group.\n\n```sql\nSELECT department, COUNT(DISTINCT job_title) AS distinct_roles\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:6,sortKey:2628},{id:"sql-2629",number:7,title:"How do aggregates handle NULLs?",question:"How do aggregates handle NULLs?",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Aggregates (except `COUNT(*)`) ignore NULLs. `COUNT(col)` skips NULLs; `AVG`/`SUM` compute over non-NULLs only; `COUNT(*)` counts every row.\n\n```sql\n-- AVG divides by count of non-NULL commissions, not total rows\nSELECT AVG(commission) FROM employees;\n```",tech:"sql",displayNumber:7,sortKey:2629},{id:"sql-2630",number:8,title:"Group by multiple columns",question:"Group by multiple columns",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"List several columns in `GROUP BY` to form groups per unique combination.\n\n```sql\nSELECT department, gender, COUNT(*) \nFROM employees GROUP BY department, gender;\n```",tech:"sql",displayNumber:8,sortKey:2630},{id:"sql-2631",number:9,title:"Filter groups with HAVING and COUNT",question:"Filter groups with HAVING and COUNT",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Use `HAVING COUNT(*) > n` to keep only groups meeting a size threshold, e.g. departments with more than 5 employees.\n\n```sql\nSELECT department, COUNT(*) AS headcount\nFROM employees GROUP BY department HAVING COUNT(*) > 5;\n```",tech:"sql",displayNumber:9,sortKey:2631},{id:"sql-2632",number:10,title:"Find duplicate values",question:"Find duplicate values",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Group by the column(s) and keep groups with `COUNT(*) > 1` to identify duplicates.\n\n```sql\nSELECT email, COUNT(*) AS cnt\nFROM users GROUP BY email HAVING COUNT(*) > 1;\n```",tech:"sql",displayNumber:10,sortKey:2632},{id:"sql-2633",number:11,title:"Delete duplicate rows keeping one",question:"Delete duplicate rows keeping one",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Use a window function (`ROW_NUMBER` per duplicate group) and delete rows where the row number > 1, or delete rows whose id isn't the min per group.\n\n```sql\nDELETE FROM users\nWHERE id NOT IN (SELECT MIN(id) FROM users GROUP BY email);\n```",tech:"sql",displayNumber:11,sortKey:2633},{id:"sql-2634",number:12,title:"Can you use aggregate functions in WHERE?",question:"Can you use aggregate functions in WHERE?",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"No. `WHERE` runs before aggregation, so aggregates belong in `HAVING` (or a subquery). Putting `SUM()`/`COUNT()` in `WHERE` is a syntax error.\n\n```sql\n-- wrong: WHERE COUNT(*) > 5\nSELECT department FROM employees GROUP BY department HAVING COUNT(*) > 5;\n```",tech:"sql",displayNumber:12,sortKey:2634},{id:"sql-2635",number:13,title:"Conditional aggregation (pivot with CASE)",question:"Conditional aggregation (pivot with CASE)",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Use `SUM(CASE WHEN ... THEN 1 ELSE 0 END)` or `COUNT(CASE ...)` to compute multiple conditional counts/sums in one query — a manual pivot.\n\n```sql\nSELECT department,\n  SUM(CASE WHEN gender='Male' THEN 1 ELSE 0 END) AS males,\n  SUM(CASE WHEN gender='Female' THEN 1 ELSE 0 END) AS females\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:13,sortKey:2635},{id:"sql-2636",number:14,title:"Group and order by an aggregate",question:"Group and order by an aggregate",difficulty:"Basic",category:"Aggregation & Grouping",answer:"You can `ORDER BY` an aggregate or its alias to rank groups, e.g. departments by total salary descending.\n\n```sql\nSELECT department, SUM(salary) AS total\nFROM employees GROUP BY department ORDER BY total DESC;\n```",tech:"sql",displayNumber:14,sortKey:2636},{id:"sql-2637",number:15,title:"Every non-aggregated column must be in GROUP BY",question:"Every non-aggregated column must be in GROUP BY",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"In standard SQL, columns in `SELECT` that aren't aggregated must appear in `GROUP BY`. MySQL historically relaxed this (ONLY_FULL_GROUP_BY off), which can return arbitrary values — avoid relying on it.\n\n```sql\n-- name isn't functionally dependent on department -> must aggregate or group it\nSELECT department, MAX(name) FROM employees GROUP BY department;\n```",tech:"sql",displayNumber:15,sortKey:2637},{id:"sql-2638",number:16,title:"Compute min and max per group",question:"Compute min and max per group",difficulty:"Basic",category:"Aggregation & Grouping",answer:"`MIN`/`MAX` return the smallest/largest value per group.\n\n```sql\nSELECT department, MIN(salary) AS lowest, MAX(salary) AS highest\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:16,sortKey:2638},{id:"sql-2639",number:17,title:"Percentage of total per group",question:"Percentage of total per group",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Divide a group's aggregate by the grand total using a window function (`SUM(...) OVER ()`) or a subquery for the total.\n\n```sql\nSELECT department, SUM(salary) AS total,\n  ROUND(100.0 * SUM(salary) / SUM(SUM(salary)) OVER (), 2) AS pct\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:17,sortKey:2639},{id:"sql-2640",number:18,title:"GROUP BY with ROLLUP (subtotals)",question:"GROUP BY with ROLLUP (subtotals)",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUP BY ROLLUP(a, b)` adds subtotal and grand-total rows (with NULL group keys) to the result, useful for hierarchical reporting.\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY ROLLUP(department, gender);\n```",tech:"sql",displayNumber:18,sortKey:2640},{id:"sql-2641",number:19,title:"GROUP BY CUBE",question:"GROUP BY CUBE",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`CUBE(a, b)` produces aggregates for all combinations of the grouping columns (all subtotals across every dimension), unlike ROLLUP's hierarchical subtotals.\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY CUBE(department, gender);\n```",tech:"sql",displayNumber:19,sortKey:2641},{id:"sql-2642",number:20,title:"GROUPING SETS",question:"GROUPING SETS",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUPING SETS` lets you specify exactly which groupings to compute in one query, combining multiple `GROUP BY` results without multiple passes.\n\n```sql\nSELECT department, gender, SUM(salary)\nFROM employees GROUP BY GROUPING SETS ((department), (gender), ());\n```",tech:"sql",displayNumber:20,sortKey:2642},{id:"sql-2643",number:21,title:"Count rows matching a condition without WHERE",question:"Count rows matching a condition without WHERE",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Use `COUNT(CASE WHEN cond THEN 1 END)` or `SUM(cond::int)` to count matches while still returning other aggregates over all rows.\n\n```sql\nSELECT COUNT(*) AS total,\n       COUNT(CASE WHEN salary > 80000 THEN 1 END) AS high_earners\nFROM employees;\n```",tech:"sql",displayNumber:21,sortKey:2643},{id:"sql-2644",number:22,title:"Average excluding outliers",question:"Average excluding outliers",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Filter outliers in `WHERE` before aggregating, or use percentiles to trim. `AVG` is sensitive to extremes, so consider `MEDIAN`/`PERCENTILE_CONT`.\n\n```sql\nSELECT AVG(salary) FROM employees\nWHERE salary BETWEEN 20000 AND 200000;\n```",tech:"sql",displayNumber:22,sortKey:2644},{id:"sql-2645",number:23,title:"Median with PERCENTILE_CONT",question:"Median with PERCENTILE_CONT",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY col)` computes the median (an ordered-set aggregate) since SQL has no built-in MEDIAN.\n\n```sql\nSELECT PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median_salary\nFROM employees;\n```",tech:"sql",displayNumber:23,sortKey:2645},{id:"sql-2646",number:24,title:"String aggregation (concatenate group values)",question:"String aggregation (concatenate group values)",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`STRING_AGG(col, ', ')` (Postgres/SQL Server) or `GROUP_CONCAT` (MySQL) concatenates group values into one string, optionally ordered.\n\n```sql\nSELECT department, STRING_AGG(name, ', ' ORDER BY name) AS members\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:24,sortKey:2646},{id:"sql-2647",number:25,title:"Array aggregation",question:"Array aggregation",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`ARRAY_AGG(col)` (Postgres) collects group values into an array, useful for nesting related values without a separate query.\n\n```sql\nSELECT department, ARRAY_AGG(name) AS employee_names\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:25,sortKey:2647},{id:"sql-2648",number:26,title:"Filter aggregates with FILTER clause",question:"Filter aggregates with FILTER clause",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"The SQL-standard `FILTER (WHERE ...)` clause restricts which rows an aggregate sees, a cleaner alternative to `CASE` inside the aggregate (Postgres).\n\n```sql\nSELECT department,\n  COUNT(*) FILTER (WHERE salary > 80000) AS high_earners\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:26,sortKey:2648},{id:"sql-2649",number:27,title:"Count groups (number of distinct groups)",question:"Count groups (number of distinct groups)",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"Wrap the grouped query in a subquery and `COUNT(*)`, or use `COUNT(DISTINCT col)` for a single grouping column.\n\n```sql\nSELECT COUNT(DISTINCT department) AS num_departments FROM employees;\n```",tech:"sql",displayNumber:27,sortKey:2649},{id:"sql-2650",number:28,title:"Find groups where all rows meet a condition",question:"Find groups where all rows meet a condition",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Compare total count to conditional count: a group where every row matches has `COUNT(*) = COUNT(CASE WHEN cond THEN 1 END)`.\n\n```sql\nSELECT department FROM employees\nGROUP BY department\nHAVING COUNT(*) = COUNT(CASE WHEN salary > 40000 THEN 1 END);\n```",tech:"sql",displayNumber:28,sortKey:2650},{id:"sql-2651",number:29,title:"Find groups where no row meets a condition",question:"Find groups where no row meets a condition",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Use `HAVING COUNT(CASE WHEN cond THEN 1 END) = 0` to keep groups with zero matching rows.\n\n```sql\nSELECT department FROM employees\nGROUP BY department\nHAVING COUNT(CASE WHEN salary > 150000 THEN 1 END) = 0;\n```",tech:"sql",displayNumber:29,sortKey:2651},{id:"sql-2652",number:30,title:"Aggregate over a date/time bucket",question:"Aggregate over a date/time bucket",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Truncate the timestamp to the desired granularity (`DATE_TRUNC('month', ts)`) and group by it for time-series aggregation.\n\n```sql\nSELECT DATE_TRUNC('month', order_date) AS month, SUM(amount) AS revenue\nFROM orders GROUP BY 1 ORDER BY 1;\n```",tech:"sql",displayNumber:30,sortKey:2652},{id:"sql-2653",number:31,title:"Group by an expression",question:"Group by an expression",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`GROUP BY` can use expressions (or ordinal positions / aliases in some DBs), e.g. grouping by year extracted from a date.\n\n```sql\nSELECT EXTRACT(YEAR FROM hire_date) AS yr, COUNT(*) \nFROM employees GROUP BY EXTRACT(YEAR FROM hire_date);\n```",tech:"sql",displayNumber:31,sortKey:2653},{id:"sql-2654",number:32,title:"HAVING without GROUP BY",question:"HAVING without GROUP BY",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`HAVING` can be used without `GROUP BY` when the whole table is one implicit group, filtering based on a table-wide aggregate.\n\n```sql\nSELECT COUNT(*) FROM orders HAVING COUNT(*) > 1000;\n```",tech:"sql",displayNumber:32,sortKey:2654},{id:"sql-2655",number:33,title:"Running total with SUM OVER",question:"Running total with SUM OVER",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"A windowed `SUM(...) OVER (ORDER BY ...)` computes a cumulative running total without collapsing rows — an aggregate used as a window function.\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (ORDER BY order_date) AS running_total\nFROM orders;\n```",tech:"sql",displayNumber:33,sortKey:2655},{id:"sql-2656",number:34,title:"Average per group alongside detail rows",question:"Average per group alongside detail rows",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Use `AVG(...) OVER (PARTITION BY group)` to attach a group average to every detail row without a self-join or `GROUP BY`.\n\n```sql\nSELECT name, department, salary,\n  AVG(salary) OVER (PARTITION BY department) AS dept_avg\nFROM employees;\n```",tech:"sql",displayNumber:34,sortKey:2656},{id:"sql-2657",number:35,title:"Count NULLs in a column",question:"Count NULLs in a column",difficulty:"Intermediate",category:"Aggregation & Grouping",answer:"`COUNT(*) - COUNT(col)` gives the NULL count, since `COUNT(col)` skips NULLs. Or `SUM(CASE WHEN col IS NULL THEN 1 ELSE 0 END)`.\n\n```sql\nSELECT COUNT(*) - COUNT(commission) AS null_commissions FROM employees;\n```",tech:"sql",displayNumber:35,sortKey:2657},{id:"sql-2658",number:36,title:"Group data into ranges/buckets",question:"Group data into ranges/buckets",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"Derive a bucket with `CASE` or `WIDTH_BUCKET`/`FLOOR`, then group by it to build histograms.\n\n```sql\nSELECT FLOOR(salary / 20000) * 20000 AS band, COUNT(*)\nFROM employees GROUP BY FLOOR(salary / 20000) GROUP BY 1;\n```",tech:"sql",displayNumber:36,sortKey:2658},{id:"sql-2659",number:37,title:"Total with grand total row using UNION",question:"Total with grand total row using UNION",difficulty:"Advanced",category:"Aggregation & Grouping",answer:"`UNION ALL` a grand-total query onto the grouped result (or use `ROLLUP`) to append an overall total.\n\n```sql\nSELECT department, SUM(salary) FROM employees GROUP BY department\nUNION ALL\nSELECT 'ALL', SUM(salary) FROM employees;\n```",tech:"sql",displayNumber:37,sortKey:2659},{id:"sql-2660",number:38,title:"Weighted average",question:"Weighted average",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"Compute `SUM(value * weight) / SUM(weight)` rather than a plain `AVG`, when rows contribute unequally.\n\n```sql\nSELECT product_id, SUM(price * quantity) / SUM(quantity) AS avg_price\nFROM order_items GROUP BY product_id;\n```",tech:"sql",displayNumber:38,sortKey:2660},{id:"sql-2661",number:39,title:"Aggregate then join back to detail",question:"Aggregate then join back to detail",difficulty:"Experienced",category:"Aggregation & Grouping",answer:`Compute group aggregates in a subquery/CTE, then join back to detail rows to compare each row against its group stat (e.g. above-average earners).

\`\`\`sql
WITH dept_avg AS (
  SELECT department, AVG(salary) avg_sal FROM employees GROUP BY department)
SELECT e.name, e.salary
FROM employees e JOIN dept_avg d ON e.department = d.department
WHERE e.salary > d.avg_sal;
\`\`\``,tech:"sql",displayNumber:39,sortKey:2661},{id:"sql-2662",number:40,title:"Why is GROUP BY slow and how to speed it up?",question:"Why is GROUP BY slow and how to speed it up?",difficulty:"Experienced",category:"Aggregation & Grouping",answer:"`GROUP BY` may sort/hash large inputs. Speed it up with indexes on grouping columns (enabling index-ordered grouping), filtering rows early in `WHERE`, pre-aggregating, or materialized views for repeated heavy aggregations.\n\n```sql\nCREATE INDEX idx_emp_dept ON employees(department);\nEXPLAIN SELECT department, COUNT(*) FROM employees GROUP BY department;\n```",tech:"sql",displayNumber:40,sortKey:2662},{id:"sql-2663",number:1,title:"What is a subquery?",question:"What is a subquery?",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A subquery is a query nested inside another query (in `SELECT`, `FROM`, `WHERE`, or `HAVING`). It can return a scalar, a row, or a set used by the outer query.\n\n```sql\nSELECT name FROM employees\nWHERE salary > (SELECT AVG(salary) FROM employees);\n```",tech:"sql",displayNumber:1,sortKey:2663},{id:"sql-2664",number:2,title:"Scalar subquery",question:"Scalar subquery",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A scalar subquery returns exactly one value and can be used anywhere a single value is expected.\n\n```sql\nSELECT name, salary, (SELECT MAX(salary) FROM employees) AS top_salary\nFROM employees;\n```",tech:"sql",displayNumber:2,sortKey:2664},{id:"sql-2665",number:3,title:"Subquery in WHERE with IN",question:"Subquery in WHERE with IN",difficulty:"Basic",category:"Subqueries, CTEs & Window Functions",answer:"A subquery returning a column of values can feed `IN` to filter the outer query.\n\n```sql\nSELECT name FROM employees\nWHERE department_id IN (SELECT id FROM departments WHERE location = 'NY');\n```",tech:"sql",displayNumber:3,sortKey:2665},{id:"sql-2666",number:4,title:"Correlated subquery",question:"Correlated subquery",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A correlated subquery references columns from the outer query, so it's re-evaluated per outer row. It's powerful but can be slow if not indexed.\n\n```sql\nSELECT e.name FROM employees e\nWHERE e.salary > (SELECT AVG(salary) FROM employees WHERE department = e.department);\n```",tech:"sql",displayNumber:4,sortKey:2666},{id:"sql-2667",number:5,title:"Difference between correlated and non-correlated subquery",question:"Difference between correlated and non-correlated subquery",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A non-correlated subquery is independent and runs once. A correlated subquery depends on the outer row and conceptually runs per row. Optimizers often rewrite correlated subqueries as joins.\n\n```sql\n-- non-correlated: runs once\nSELECT * FROM orders WHERE amount > (SELECT AVG(amount) FROM orders);\n```",tech:"sql",displayNumber:5,sortKey:2667},{id:"sql-2668",number:6,title:"EXISTS vs IN",question:"EXISTS vs IN",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`EXISTS` checks for existence and short-circuits at the first match (good for correlated checks and large subsets). `IN` compares against a materialized list. `EXISTS` is NULL-safe; `NOT IN` is not.\n\n```sql\nSELECT name FROM customers c\nWHERE EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```",tech:"sql",displayNumber:6,sortKey:2668},{id:"sql-2669",number:7,title:"Subquery in FROM (derived table)",question:"Subquery in FROM (derived table)",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"A subquery in `FROM` acts as an inline (derived) table that the outer query selects from. It must be aliased.\n\n```sql\nSELECT dept, avg_sal FROM\n  (SELECT department AS dept, AVG(salary) AS avg_sal FROM employees GROUP BY department) t\nWHERE avg_sal > 50000;\n```",tech:"sql",displayNumber:7,sortKey:2669},{id:"sql-2670",number:8,title:"What is a CTE (Common Table Expression)?",question:"What is a CTE (Common Table Expression)?",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"A CTE is a named temporary result defined with `WITH`, usable later in the same query. It improves readability and lets you reference the same subquery multiple times.\n\n```sql\nWITH high_earners AS (\n  SELECT * FROM employees WHERE salary > 80000)\nSELECT department, COUNT(*) FROM high_earners GROUP BY department;\n```",tech:"sql",displayNumber:8,sortKey:2670},{id:"sql-2671",number:9,title:"CTE vs subquery vs temp table",question:"CTE vs subquery vs temp table",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"A subquery is inline and single-use. A CTE names a subquery for readability/reuse within one statement (usually not materialized). A temp table persists across statements and can be indexed but adds overhead.\n\n```sql\nWITH t AS (SELECT ...) SELECT * FROM t JOIN t t2 ON ...; -- reuse within one query\n```",tech:"sql",displayNumber:9,sortKey:2671},{id:"sql-2672",number:10,title:"Recursive CTE",question:"Recursive CTE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:`A recursive CTE references itself to traverse hierarchies/graphs (org charts, category trees). It has an anchor member and a recursive member joined via \`UNION ALL\`.

\`\`\`sql
WITH RECURSIVE org AS (
  SELECT id, name, manager_id, 1 AS lvl FROM employees WHERE manager_id IS NULL
  UNION ALL
  SELECT e.id, e.name, e.manager_id, o.lvl + 1
  FROM employees e JOIN org o ON e.manager_id = o.id)
SELECT * FROM org ORDER BY lvl;
\`\`\``,tech:"sql",displayNumber:10,sortKey:2672},{id:"sql-2673",number:11,title:"What are window functions?",question:"What are window functions?",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:'Window functions compute values across a set of rows (a "window") related to the current row, without collapsing them like `GROUP BY`. They use `OVER (PARTITION BY ... ORDER BY ...)`.\n\n```sql\nSELECT name, department, salary,\n  RANK() OVER (PARTITION BY department ORDER BY salary DESC) AS dept_rank\nFROM employees;\n```',tech:"sql",displayNumber:11,sortKey:2673},{id:"sql-2674",number:12,title:"ROW_NUMBER vs RANK vs DENSE_RANK",question:"ROW_NUMBER vs RANK vs DENSE_RANK",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROW_NUMBER` gives unique sequential numbers. `RANK` gives ties the same rank but skips subsequent numbers (1,1,3). `DENSE_RANK` gives ties the same rank without gaps (1,1,2).\n\n```sql\nSELECT name, salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) rn,\n  RANK()       OVER (ORDER BY salary DESC) rnk,\n  DENSE_RANK() OVER (ORDER BY salary DESC) drnk\nFROM employees;\n```",tech:"sql",displayNumber:12,sortKey:2674},{id:"sql-2675",number:13,title:"Find the Nth highest salary with window functions",question:"Find the Nth highest salary with window functions",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Rank rows with `DENSE_RANK` (to handle ties) and filter for the desired rank in an outer query, since you can't filter window results in `WHERE`.\n\n```sql\nSELECT name, salary FROM (\n  SELECT name, salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS rnk\n  FROM employees) t\nWHERE rnk = 3; -- 3rd highest\n```",tech:"sql",displayNumber:13,sortKey:2675},{id:"sql-2676",number:14,title:"Top N per group",question:"Top N per group",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:'Partition by the group, order within it, and filter by `ROW_NUMBER`/`RANK` ≤ N — a classic "top-N-per-category" pattern.\n\n```sql\nSELECT * FROM (\n  SELECT name, department, salary,\n    ROW_NUMBER() OVER (PARTITION BY department ORDER BY salary DESC) rn\n  FROM employees) t\nWHERE rn <= 3;\n```',tech:"sql",displayNumber:14,sortKey:2676},{id:"sql-2677",number:15,title:"PARTITION BY explained",question:"PARTITION BY explained",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`PARTITION BY` splits rows into groups for the window function, resetting the calculation per partition — like `GROUP BY` but without collapsing rows.\n\n```sql\nSELECT name, department,\n  COUNT(*) OVER (PARTITION BY department) AS dept_size\nFROM employees;\n```",tech:"sql",displayNumber:15,sortKey:2677},{id:"sql-2678",number:16,title:"LAG and LEAD",question:"LAG and LEAD",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`LAG(col, n)` accesses a previous row's value; `LEAD(col, n)` a following row's — great for period-over-period comparisons.\n\n```sql\nSELECT order_date, amount,\n  amount - LAG(amount) OVER (ORDER BY order_date) AS change\nFROM daily_sales;\n```",tech:"sql",displayNumber:16,sortKey:2678},{id:"sql-2679",number:17,title:"Running total (cumulative sum)",question:"Running total (cumulative sum)",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`SUM(col) OVER (ORDER BY ...)` accumulates values row by row. Add `PARTITION BY` to reset the running total per group.\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (ORDER BY order_date\n       ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS running_total\nFROM orders;\n```",tech:"sql",displayNumber:17,sortKey:2679},{id:"sql-2680",number:18,title:"Moving average",question:"Moving average",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Use a windowed `AVG` with a frame (`ROWS BETWEEN n PRECEDING AND CURRENT ROW`) to smooth a series over a sliding window.\n\n```sql\nSELECT day, value,\n  AVG(value) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW) AS ma7\nFROM metrics;\n```",tech:"sql",displayNumber:18,sortKey:2680},{id:"sql-2681",number:19,title:"FIRST_VALUE and LAST_VALUE",question:"FIRST_VALUE and LAST_VALUE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`FIRST_VALUE`/`LAST_VALUE` return the first/last value in the window frame. For a correct `LAST_VALUE`, set the frame to `ROWS BETWEEN UNBOUNDED PRECEDING AND UNBOUNDED FOLLOWING`.\n\n```sql\nSELECT name, department, salary,\n  FIRST_VALUE(name) OVER (PARTITION BY department ORDER BY salary DESC) AS top_earner\nFROM employees;\n```",tech:"sql",displayNumber:19,sortKey:2681},{id:"sql-2682",number:20,title:"NTILE for bucketing",question:"NTILE for bucketing",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`NTILE(n)` distributes ordered rows into n roughly equal buckets — useful for quartiles/percentile bands.\n\n```sql\nSELECT name, salary, NTILE(4) OVER (ORDER BY salary) AS quartile\nFROM employees;\n```",tech:"sql",displayNumber:20,sortKey:2682},{id:"sql-2683",number:21,title:"Why can't you use window functions in WHERE?",question:"Why can't you use window functions in WHERE?",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Window functions are computed after `WHERE`/`GROUP BY`/`HAVING` (during `SELECT`), so you must wrap them in a subquery or CTE to filter on their results.\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (ORDER BY salary DESC) rn FROM employees) t\nWHERE rn = 1;\n```",tech:"sql",displayNumber:21,sortKey:2683},{id:"sql-2684",number:22,title:"Difference between GROUP BY and window functions",question:"Difference between GROUP BY and window functions",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"`GROUP BY` collapses rows into one per group. Window functions add aggregate/analytic columns while preserving all detail rows, so you keep row-level data plus group context.\n\n```sql\nSELECT name, salary, AVG(salary) OVER (PARTITION BY department) AS dept_avg\nFROM employees; -- every employee row kept\n```",tech:"sql",displayNumber:22,sortKey:2684},{id:"sql-2685",number:23,title:"Second highest salary without window functions",question:"Second highest salary without window functions",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Use a subquery: the max salary less than the overall max, which correctly skips ties.\n\n```sql\nSELECT MAX(salary) AS second_highest\nFROM employees\nWHERE salary < (SELECT MAX(salary) FROM employees);\n```",tech:"sql",displayNumber:23,sortKey:2685},{id:"sql-2686",number:24,title:"Nth highest salary with LIMIT/OFFSET",question:"Nth highest salary with LIMIT/OFFSET",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Order distinct salaries descending and use `OFFSET n-1 LIMIT 1`. Use `DISTINCT` so ties don't shift the position.\n\n```sql\nSELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 2; -- 3rd\n```",tech:"sql",displayNumber:24,sortKey:2686},{id:"sql-2687",number:25,title:"Correlated subquery to rank rows",question:"Correlated subquery to rank rows",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Before window functions, ranks were computed by counting how many rows have a greater value in a correlated subquery.\n\n```sql\nSELECT e.name, e.salary,\n  (SELECT COUNT(DISTINCT salary) FROM employees WHERE salary >= e.salary) AS rank\nFROM employees e;\n```",tech:"sql",displayNumber:25,sortKey:2687},{id:"sql-2688",number:26,title:"Using a CTE for readability",question:"Using a CTE for readability",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Break a complex query into named CTE steps, each transforming the previous, making logic easier to follow and test.\n\n```sql\nWITH paid AS (SELECT * FROM orders WHERE status='paid'),\n     per_cust AS (SELECT customer_id, SUM(amount) t FROM paid GROUP BY customer_id)\nSELECT * FROM per_cust WHERE t > 1000;\n```",tech:"sql",displayNumber:26,sortKey:2688},{id:"sql-2689",number:27,title:"Multiple CTEs in one query",question:"Multiple CTEs in one query",difficulty:"Intermediate",category:"Subqueries, CTEs & Window Functions",answer:"Define several CTEs separated by commas after a single `WITH`; later CTEs can reference earlier ones.\n\n```sql\nWITH a AS (SELECT ...),\n     b AS (SELECT ... FROM a ...)\nSELECT * FROM b;\n```",tech:"sql",displayNumber:27,sortKey:2689},{id:"sql-2690",number:28,title:"Recursive CTE to generate a number/date series",question:"Recursive CTE to generate a number/date series",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"A recursive CTE can generate sequences (numbers, dates) when the DB lacks `generate_series`, useful for gap-filling reports.\n\n```sql\nWITH RECURSIVE seq(n) AS (\n  SELECT 1 UNION ALL SELECT n + 1 FROM seq WHERE n < 10)\nSELECT n FROM seq;\n```",tech:"sql",displayNumber:28,sortKey:2690},{id:"sql-2691",number:29,title:"Detect gaps in a sequence",question:"Detect gaps in a sequence",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Compare each value to the next via `LEAD`; a gap exists where `next - current > 1`.\n\n```sql\nSELECT id AS gap_start, next_id AS gap_end FROM (\n  SELECT id, LEAD(id) OVER (ORDER BY id) AS next_id FROM tickets) t\nWHERE next_id - id > 1;\n```",tech:"sql",displayNumber:29,sortKey:2691},{id:"sql-2692",number:30,title:"Islands and gaps problem",question:"Islands and gaps problem",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:`Group consecutive rows ("islands") by subtracting \`ROW_NUMBER()\` from the sequential value; equal differences belong to the same run.

\`\`\`sql
SELECT MIN(day) AS start, MAX(day) AS end
FROM (SELECT day, day - ROW_NUMBER() OVER (ORDER BY day) * INTERVAL '1 day' AS grp
      FROM active_days) t
GROUP BY grp;
\`\`\``,tech:"sql",displayNumber:30,sortKey:2692},{id:"sql-2693",number:31,title:"Deduplicate keeping the latest row",question:"Deduplicate keeping the latest row",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROW_NUMBER()` partitioned by the dedup key, ordered by recency, then keep `rn = 1`.\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY email ORDER BY updated_at DESC) rn\n  FROM users) t\nWHERE rn = 1;\n```",tech:"sql",displayNumber:31,sortKey:2693},{id:"sql-2694",number:32,title:"Compare each row to the group max",question:"Compare each row to the group max",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Use `MAX(col) OVER (PARTITION BY group)` to attach the group max to each row, then compute a difference or flag.\n\n```sql\nSELECT name, department, salary,\n  MAX(salary) OVER (PARTITION BY department) - salary AS below_top\nFROM employees;\n```",tech:"sql",displayNumber:32,sortKey:2694},{id:"sql-2695",number:33,title:"Percent of total using window SUM",question:"Percent of total using window SUM",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Divide each value by `SUM(col) OVER ()` (whole set) or `OVER (PARTITION BY group)` for share within group.\n\n```sql\nSELECT department, salary,\n  ROUND(100.0 * salary / SUM(salary) OVER (PARTITION BY department), 2) AS pct_of_dept\nFROM employees;\n```",tech:"sql",displayNumber:33,sortKey:2695},{id:"sql-2696",number:34,title:"Cumulative distribution and PERCENT_RANK",question:"Cumulative distribution and PERCENT_RANK",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`CUME_DIST` gives the fraction of rows ≤ current; `PERCENT_RANK` gives relative rank (0..1). Useful for percentiles/scoring.\n\n```sql\nSELECT name, salary,\n  ROUND(PERCENT_RANK() OVER (ORDER BY salary)::numeric, 2) AS pct_rank\nFROM employees;\n```",tech:"sql",displayNumber:34,sortKey:2696},{id:"sql-2697",number:35,title:"Window frame: ROWS vs RANGE",question:"Window frame: ROWS vs RANGE",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"`ROWS` counts physical rows in the frame; `RANGE` groups rows with the same `ORDER BY` value (peers) together. They differ when ordering values tie.\n\n```sql\nSUM(x) OVER (ORDER BY d ROWS BETWEEN 1 PRECEDING AND CURRENT ROW)   -- physical\nSUM(x) OVER (ORDER BY d RANGE BETWEEN 1 PRECEDING AND CURRENT ROW)  -- value-based\n```",tech:"sql",displayNumber:35,sortKey:2697},{id:"sql-2698",number:36,title:"Difference between two consecutive rows",question:"Difference between two consecutive rows",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Subtract `LAG(col)` from the current value to get the row-to-row delta (e.g. daily growth).\n\n```sql\nSELECT day, revenue,\n  revenue - LAG(revenue) OVER (ORDER BY day) AS daily_change\nFROM sales;\n```",tech:"sql",displayNumber:36,sortKey:2698},{id:"sql-2699",number:37,title:"Rank with ties broken by a second column",question:"Rank with ties broken by a second column",difficulty:"Advanced",category:"Subqueries, CTEs & Window Functions",answer:"Add tie-breaker columns to the window `ORDER BY` so `ROW_NUMBER`/`RANK` produce a deterministic order.\n\n```sql\nSELECT name,\n  ROW_NUMBER() OVER (ORDER BY salary DESC, hire_date ASC) AS rn\nFROM employees;\n```",tech:"sql",displayNumber:37,sortKey:2699},{id:"sql-2700",number:38,title:"Reuse a window with WINDOW clause",question:"Reuse a window with WINDOW clause",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Define a named window once in a `WINDOW` clause and reference it from multiple functions to avoid repetition.\n\n```sql\nSELECT name, salary,\n  RANK() OVER w, DENSE_RANK() OVER w\nFROM employees\nWINDOW w AS (PARTITION BY department ORDER BY salary DESC);\n```",tech:"sql",displayNumber:38,sortKey:2700},{id:"sql-2701",number:39,title:"Correlated subquery vs window function performance",question:"Correlated subquery vs window function performance",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"A per-row correlated subquery can be O(n²); a window function computes over sorted partitions in roughly one pass, so window functions are usually faster and clearer for ranking/running totals.\n\n```sql\n-- prefer this over a COUNT(*) correlated subquery\nSELECT name, RANK() OVER (ORDER BY salary DESC) FROM employees;\n```",tech:"sql",displayNumber:39,sortKey:2701},{id:"sql-2702",number:40,title:"Pivot rows to columns",question:"Pivot rows to columns",difficulty:"Experienced",category:"Subqueries, CTEs & Window Functions",answer:"Pivot with conditional aggregation (`SUM(CASE WHEN ...)`) or a dedicated `PIVOT` operator (SQL Server/Oracle), turning distinct row values into columns.\n\n```sql\nSELECT department,\n  SUM(CASE WHEN gender='Male' THEN 1 ELSE 0 END) AS male,\n  SUM(CASE WHEN gender='Female' THEN 1 ELSE 0 END) AS female\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:40,sortKey:2702},{id:"sql-2703",number:1,title:"Find the second highest salary",question:"Find the second highest salary",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Take the max salary below the overall max (handles ties), or use `DENSE_RANK`. This is one of the most-asked SQL interview questions.\n\n```sql\nSELECT MAX(salary) AS second_highest\nFROM employees WHERE salary < (SELECT MAX(salary) FROM employees);\n```",tech:"sql",displayNumber:1,sortKey:2703},{id:"sql-2704",number:2,title:"Find employees earning more than their department average",question:"Find employees earning more than their department average",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:`Compare each employee's salary to their department average via a correlated subquery or a window function.

\`\`\`sql
SELECT name, department, salary FROM (
  SELECT name, department, salary,
    AVG(salary) OVER (PARTITION BY department) AS avg_sal
  FROM employees) t
WHERE salary > avg_sal;
\`\`\``,tech:"sql",displayNumber:2,sortKey:2704},{id:"sql-2705",number:3,title:"Find the highest-paid employee in each department",question:"Find the highest-paid employee in each department",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Rank within each department and keep rank 1. Use `RANK` if you want to include ties, `ROW_NUMBER` for exactly one.\n\n```sql\nSELECT name, department, salary FROM (\n  SELECT *, RANK() OVER (PARTITION BY department ORDER BY salary DESC) rk\n  FROM employees) t\nWHERE rk = 1;\n```",tech:"sql",displayNumber:3,sortKey:2705},{id:"sql-2706",number:4,title:"Find duplicate records",question:"Find duplicate records",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Group by the columns that define a duplicate and keep groups with `COUNT(*) > 1`.\n\n```sql\nSELECT email, COUNT(*) FROM users GROUP BY email HAVING COUNT(*) > 1;\n```",tech:"sql",displayNumber:4,sortKey:2706},{id:"sql-2707",number:5,title:"Delete duplicate rows, keep the earliest",question:"Delete duplicate rows, keep the earliest",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Rank duplicates by a tiebreaker and delete all but the first, or delete rows whose id isn't the minimum per duplicate group.\n\n```sql\nDELETE FROM users a\nUSING users b\nWHERE a.email = b.email AND a.id > b.id;\n```",tech:"sql",displayNumber:5,sortKey:2707},{id:"sql-2708",number:6,title:"Find customers who never placed an order",question:"Find customers who never placed an order",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Anti-join with LEFT JOIN + `IS NULL`, or `NOT EXISTS` (NULL-safe).\n\n```sql\nSELECT c.name FROM customers c\nWHERE NOT EXISTS (SELECT 1 FROM orders o WHERE o.customer_id = c.id);\n```",tech:"sql",displayNumber:6,sortKey:2708},{id:"sql-2709",number:7,title:"Find the most recent order per customer",question:"Find the most recent order per customer",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Partition by customer, order by date descending, keep `ROW_NUMBER = 1`.\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date DESC) rn\n  FROM orders) t\nWHERE rn = 1;\n```",tech:"sql",displayNumber:7,sortKey:2709},{id:"sql-2710",number:8,title:"Running total of daily sales",question:"Running total of daily sales",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Windowed cumulative `SUM` ordered by date.\n\n```sql\nSELECT day, amount,\n  SUM(amount) OVER (ORDER BY day) AS cumulative\nFROM daily_sales;\n```",tech:"sql",displayNumber:8,sortKey:2710},{id:"sql-2711",number:9,title:"Month-over-month growth",question:"Month-over-month growth",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Aggregate by month, then use \`LAG\` to compare to the previous month and compute percentage change.

\`\`\`sql
WITH m AS (
  SELECT DATE_TRUNC('month', order_date) mo, SUM(amount) rev
  FROM orders GROUP BY 1)
SELECT mo, rev,
  ROUND(100.0 * (rev - LAG(rev) OVER (ORDER BY mo)) / LAG(rev) OVER (ORDER BY mo), 2) AS growth_pct
FROM m;
\`\`\``,tech:"sql",displayNumber:9,sortKey:2711},{id:"sql-2712",number:10,title:"Find the top 3 products by revenue",question:"Find the top 3 products by revenue",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Aggregate revenue per product, order descending, and limit to 3 (or rank and filter for ties).\n\n```sql\nSELECT product_id, SUM(amount) AS revenue\nFROM order_items GROUP BY product_id\nORDER BY revenue DESC LIMIT 3;\n```",tech:"sql",displayNumber:10,sortKey:2712},{id:"sql-2713",number:11,title:"Find gaps between consecutive dates per user",question:"Find gaps between consecutive dates per user",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `LAG` over the date ordered per user and compute the interval difference.\n\n```sql\nSELECT user_id, login_date,\n  login_date - LAG(login_date) OVER (PARTITION BY user_id ORDER BY login_date) AS gap\nFROM logins;\n```",tech:"sql",displayNumber:11,sortKey:2713},{id:"sql-2714",number:12,title:"Find consecutive login streaks",question:"Find consecutive login streaks",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Subtract `ROW_NUMBER()` from the date to form a constant group per consecutive run, then aggregate min/max and count per group.\n\n```sql\nSELECT user_id, MIN(d) AS streak_start, COUNT(*) AS length\nFROM (SELECT user_id, d,\n        d - (ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY d)) * INTERVAL '1 day' AS grp\n      FROM login_days) t\nGROUP BY user_id, grp;\n```",tech:"sql",displayNumber:12,sortKey:2714},{id:"sql-2715",number:13,title:"Pivot monthly sales into columns",question:"Pivot monthly sales into columns",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Conditional aggregation with one `SUM(CASE ...)` per target column.\n\n```sql\nSELECT product_id,\n  SUM(CASE WHEN EXTRACT(MONTH FROM d)=1 THEN amt END) AS jan,\n  SUM(CASE WHEN EXTRACT(MONTH FROM d)=2 THEN amt END) AS feb\nFROM sales GROUP BY product_id;\n```",tech:"sql",displayNumber:13,sortKey:2715},{id:"sql-2716",number:14,title:"Unpivot columns into rows",question:"Unpivot columns into rows",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`UNION ALL` one query per column, or use `UNPIVOT`/`LATERAL` to turn wide columns into (key, value) rows.\n\n```sql\nSELECT id, 'q1' AS quarter, q1 AS amt FROM sales\nUNION ALL SELECT id, 'q2', q2 FROM sales;\n```",tech:"sql",displayNumber:14,sortKey:2716},{id:"sql-2717",number:15,title:"Find median salary",question:"Find median salary",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary)`, or emulate with row numbering picking the middle row(s).\n\n```sql\nSELECT PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY salary) AS median FROM employees;\n```",tech:"sql",displayNumber:15,sortKey:2717},{id:"sql-2718",number:16,title:"Find employees with the same salary",question:"Find employees with the same salary",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Group by salary and keep groups with more than one employee, or self-join on equal salary and different id.\n\n```sql\nSELECT salary, STRING_AGG(name, ', ') AS people\nFROM employees GROUP BY salary HAVING COUNT(*) > 1;\n```",tech:"sql",displayNumber:16,sortKey:2718},{id:"sql-2719",number:17,title:"Find the manager with the most reports",question:"Find the manager with the most reports",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Self-join or group by `manager_id`, count reports, order descending.\n\n```sql\nSELECT m.name, COUNT(*) AS reports\nFROM employees e JOIN employees m ON e.manager_id = m.id\nGROUP BY m.name ORDER BY reports DESC LIMIT 1;\n```",tech:"sql",displayNumber:17,sortKey:2719},{id:"sql-2720",number:18,title:"Traverse an org hierarchy",question:"Traverse an org hierarchy",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`A recursive CTE walks from a root down the reporting chain, tracking depth/path.

\`\`\`sql
WITH RECURSIVE tree AS (
  SELECT id, name, manager_id, 1 lvl FROM employees WHERE id = 1
  UNION ALL
  SELECT e.id, e.name, e.manager_id, t.lvl+1
  FROM employees e JOIN tree t ON e.manager_id = t.id)
SELECT * FROM tree;
\`\`\``,tech:"sql",displayNumber:18,sortKey:2720},{id:"sql-2721",number:19,title:"Calculate retention (users active in consecutive periods)",question:"Calculate retention (users active in consecutive periods)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Self-join or use `EXISTS` to check activity in period N and N+1 per user, then aggregate.\n\n```sql\nSELECT COUNT(DISTINCT a.user_id) AS retained\nFROM monthly_active a\nJOIN monthly_active b ON a.user_id = b.user_id AND b.month = a.month + 1;\n```",tech:"sql",displayNumber:19,sortKey:2721},{id:"sql-2722",number:20,title:"Find the first purchase date per customer",question:"Find the first purchase date per customer",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`MIN(order_date)` grouped by customer, or `FIRST_VALUE` window if you need it alongside other order fields.\n\n```sql\nSELECT customer_id, MIN(order_date) AS first_purchase\nFROM orders GROUP BY customer_id;\n```",tech:"sql",displayNumber:20,sortKey:2722},{id:"sql-2723",number:21,title:"Cohort analysis by signup month",question:"Cohort analysis by signup month",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Assign each user a cohort (signup month), then join activity and count actives per cohort per subsequent month.

\`\`\`sql
WITH cohort AS (
  SELECT id, DATE_TRUNC('month', signup_date) AS cohort_month FROM users)
SELECT c.cohort_month, DATE_TRUNC('month', a.event_date) AS active_month, COUNT(DISTINCT a.user_id)
FROM cohort c JOIN events a ON a.user_id = c.id
GROUP BY 1, 2;
\`\`\``,tech:"sql",displayNumber:21,sortKey:2723},{id:"sql-2724",number:22,title:"Find rows where a value changed",question:"Find rows where a value changed",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Compare each row to the previous with `LAG` and keep rows where the value differs — change-detection over an ordered series.\n\n```sql\nSELECT * FROM (\n  SELECT *, LAG(status) OVER (PARTITION BY order_id ORDER BY ts) AS prev\n  FROM order_events) t\nWHERE status IS DISTINCT FROM prev;\n```",tech:"sql",displayNumber:22,sortKey:2724},{id:"sql-2725",number:23,title:"Deduplicate keeping the row with max value",question:"Deduplicate keeping the row with max value",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`ROW_NUMBER()` partitioned by the key ordered by the value descending, keep `rn = 1`.\n\n```sql\nSELECT * FROM (\n  SELECT *, ROW_NUMBER() OVER (PARTITION BY product_id ORDER BY price DESC) rn\n  FROM price_history) t\nWHERE rn = 1;\n```",tech:"sql",displayNumber:23,sortKey:2725},{id:"sql-2726",number:24,title:"Find products never sold",question:"Find products never sold",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"Anti-join products against order items with `NOT EXISTS`.\n\n```sql\nSELECT p.name FROM products p\nWHERE NOT EXISTS (SELECT 1 FROM order_items oi WHERE oi.product_id = p.id);\n```",tech:"sql",displayNumber:24,sortKey:2726},{id:"sql-2727",number:25,title:"Compute year-to-date totals",question:"Compute year-to-date totals",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Windowed running sum partitioned by year, ordered by date.\n\n```sql\nSELECT order_date, amount,\n  SUM(amount) OVER (PARTITION BY EXTRACT(YEAR FROM order_date) ORDER BY order_date) AS ytd\nFROM orders;\n```",tech:"sql",displayNumber:25,sortKey:2727},{id:"sql-2728",number:26,title:"Rank customers into quartiles by spend",question:"Rank customers into quartiles by spend",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Aggregate spend per customer, then `NTILE(4)` over spend to segment them.\n\n```sql\nSELECT customer_id, total,\n  NTILE(4) OVER (ORDER BY total DESC) AS quartile\nFROM (SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id) s;\n```",tech:"sql",displayNumber:26,sortKey:2728},{id:"sql-2729",number:27,title:"Find overlapping date ranges",question:"Find overlapping date ranges",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Self-join and check `a.start <= b.end AND b.start <= a.end` (with an id inequality to avoid self/duplicate pairs).\n\n```sql\nSELECT a.id, b.id FROM bookings a\nJOIN bookings b ON a.room_id = b.room_id AND a.id < b.id\nWHERE a.start_time < b.end_time AND b.start_time < a.end_time;\n```",tech:"sql",displayNumber:27,sortKey:2729},{id:"sql-2730",number:28,title:"Fill missing dates with zero (gap filling)",question:"Fill missing dates with zero (gap filling)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Generate a full date series (`generate_series`/calendar table) and LEFT JOIN the data, coalescing missing values to 0.\n\n```sql\nSELECT d::date, COALESCE(SUM(amount), 0) AS revenue\nFROM generate_series('2024-01-01','2024-01-31',INTERVAL '1 day') d\nLEFT JOIN orders o ON o.order_date = d::date\nGROUP BY d ORDER BY d;\n```",tech:"sql",displayNumber:28,sortKey:2730},{id:"sql-2731",number:29,title:"Get the difference between max and min per group",question:"Get the difference between max and min per group",difficulty:"Intermediate",category:"Advanced Query Scenarios",answer:"`MAX(col) - MIN(col)` per group gives the range/spread.\n\n```sql\nSELECT department, MAX(salary) - MIN(salary) AS salary_range\nFROM employees GROUP BY department;\n```",tech:"sql",displayNumber:29,sortKey:2731},{id:"sql-2732",number:30,title:"Find top spender per city",question:"Find top spender per city",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:`Aggregate spend by city+customer, rank within city, keep rank 1.

\`\`\`sql
SELECT * FROM (
  SELECT city, customer_id, SUM(amount) total,
    RANK() OVER (PARTITION BY city ORDER BY SUM(amount) DESC) rk
  FROM orders GROUP BY city, customer_id) t
WHERE rk = 1;
\`\`\``,tech:"sql",displayNumber:30,sortKey:2732},{id:"sql-2733",number:31,title:"Calculate conversion rate",question:"Calculate conversion rate",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Divide converted counts by total counts using conditional aggregation, casting to a decimal to avoid integer division.\n\n```sql\nSELECT ROUND(100.0 * COUNT(*) FILTER (WHERE converted) / COUNT(*), 2) AS conversion_pct\nFROM sessions;\n```",tech:"sql",displayNumber:31,sortKey:2733},{id:"sql-2734",number:32,title:"Avoid integer division pitfalls",question:"Avoid integer division pitfalls",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"Integer / integer truncates. Multiply by `1.0` or cast one operand to numeric/float to get a decimal result.\n\n```sql\nSELECT 3 / 2 AS wrong, 3 * 1.0 / 2 AS right; -- 1 vs 1.5\n```",tech:"sql",displayNumber:32,sortKey:2734},{id:"sql-2735",number:33,title:"Update rows based on another table (correlated update)",question:"Update rows based on another table (correlated update)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Use `UPDATE ... FROM`/subquery to set values from a related table, matching on the key.\n\n```sql\nUPDATE employees e\nSET department_name = d.name\nFROM departments d\nWHERE e.department_id = d.id;\n```",tech:"sql",displayNumber:33,sortKey:2735},{id:"sql-2736",number:34,title:"Upsert (insert or update on conflict)",question:"Upsert (insert or update on conflict)",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`INSERT ... ON CONFLICT DO UPDATE` (Postgres) or `INSERT ... ON DUPLICATE KEY UPDATE` (MySQL) / `MERGE` inserts new rows and updates existing ones atomically.\n\n```sql\nINSERT INTO inventory(sku, qty) VALUES ('A1', 5)\nON CONFLICT (sku) DO UPDATE SET qty = inventory.qty + EXCLUDED.qty;\n```",tech:"sql",displayNumber:34,sortKey:2736},{id:"sql-2737",number:35,title:"Bulk delete with a join condition",question:"Bulk delete with a join condition",difficulty:"Advanced",category:"Advanced Query Scenarios",answer:"`DELETE ... USING`/subquery removes rows matched against another table, e.g. orders of inactive customers.\n\n```sql\nDELETE FROM orders o\nUSING customers c\nWHERE o.customer_id = c.id AND c.status = 'inactive';\n```",tech:"sql",displayNumber:35,sortKey:2737},{id:"sql-2738",number:36,title:"Find the mode (most frequent value)",question:"Find the mode (most frequent value)",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Group by the value, order by count descending, limit 1 — or `MODE() WITHIN GROUP (ORDER BY col)` in Postgres.\n\n```sql\nSELECT MODE() WITHIN GROUP (ORDER BY department) AS most_common_dept FROM employees;\n```",tech:"sql",displayNumber:36,sortKey:2738},{id:"sql-2739",number:37,title:"Compare current vs previous period side by side",question:"Compare current vs previous period side by side",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Aggregate per period, then self-join (or `LAG`) to align this period with the prior one for comparison.\n\n```sql\nWITH m AS (SELECT DATE_TRUNC('month',d) mo, SUM(amt) rev FROM sales GROUP BY 1)\nSELECT mo, rev, LAG(rev) OVER (ORDER BY mo) AS prev_rev FROM m;\n```",tech:"sql",displayNumber:37,sortKey:2739},{id:"sql-2740",number:38,title:"Explain query performance",question:"Explain query performance",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"`EXPLAIN ANALYZE` shows the execution plan with actual timings and row counts, revealing sequential scans, join methods, and where indexes are (or aren't) used.\n\n```sql\nEXPLAIN ANALYZE\nSELECT * FROM orders WHERE customer_id = 42;\n```",tech:"sql",displayNumber:38,sortKey:2740},{id:"sql-2741",number:39,title:"When does an index NOT get used?",question:"When does an index NOT get used?",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"When you apply a function to the indexed column, use leading wildcards (`LIKE '%x'`), have low selectivity, mismatched types, or stale statistics. Keep predicates sargable and statistics fresh, or add a functional/partial index.\n\n```sql\n-- non-sargable: index on created_at unused\nWHERE DATE(created_at) = '2024-01-01'\n-- sargable rewrite:\nWHERE created_at >= '2024-01-01' AND created_at < '2024-01-02'\n```",tech:"sql",displayNumber:39,sortKey:2741},{id:"sql-2742",number:40,title:"Optimize a slow aggregation query",question:"Optimize a slow aggregation query",difficulty:"Experienced",category:"Advanced Query Scenarios",answer:"Index filter/group/join columns, filter early, avoid `SELECT *`, pre-aggregate in a CTE, consider a covering index or materialized view, and check the plan with `EXPLAIN ANALYZE` to confirm the fix.\n\n```sql\nCREATE INDEX idx_orders_cust_date ON orders(customer_id, order_date);\nCREATE MATERIALIZED VIEW cust_totals AS\n  SELECT customer_id, SUM(amount) total FROM orders GROUP BY customer_id;\n```",tech:"sql",displayNumber:40,sortKey:2742}];function Kp({text:e}){const t=String(e||"").split(/```(\w+)?\n([\s\S]*?)```/g);return d.jsx("div",{className:"markdown prose prose-slate dark:prose-invert max-w-none",children:t.map((n,a)=>{if(a%3===2)return d.jsx("pre",{className:"bg-slate-900 rounded-lg p-4 overflow-x-auto border border-slate-700/50",children:d.jsx("code",{className:"text-slate-100 text-sm leading-relaxed font-mono",children:Wp(n.trim())})},a);if(a%3===1)return null;const r=n.split(`
`),s=[];for(let i=0;i<r.length;i++){const o=r[i],c=`${a}-${i}`,p=o.match(/!\[([^\]]*)\]\(([^)"\s]+)(?:\s+"([^"]*)")?\)/);if(p){const[,h,v,w]=p;s.push(d.jsxs("div",{className:"my-4",children:[d.jsx("img",{src:v,alt:h,title:w,className:"max-w-full h-auto rounded-lg border border-slate-200 dark:border-slate-700",loading:"lazy"}),h&&d.jsx("p",{className:"text-xs text-slate-500 dark:text-slate-400 text-center mt-1",children:h})]},c));continue}const f=h=>h&&h.includes("|")&&h.split("|").map(v=>v.trim()).filter(Boolean).length>1,y=h=>h&&/^\s*\|?[\s:|-]*-[\s:|-]*\|?\s*$/.test(h)&&h.includes("-");if(f(o)){const h=[];let v=i;for(;v<r.length&&(f(r[v])||y(r[v]));)y(r[v])||h.push(r[v].split("|").map(j=>j.trim()).filter((j,u,l)=>!(j===""&&(u===0||u===l.length-1)))),v++;const[w,...k]=h;s.push(d.jsx("div",{className:"overflow-x-auto my-4 rounded-lg border border-slate-200 dark:border-slate-700",children:d.jsxs("table",{className:"min-w-full text-sm border-collapse",children:[d.jsx("thead",{children:d.jsx("tr",{className:"bg-slate-100 dark:bg-slate-800",children:w.map((j,u)=>d.jsx("th",{className:"px-4 py-2.5 text-left font-semibold text-slate-900 dark:text-slate-100 border-b border-slate-200 dark:border-slate-700",children:vn(j)},u))})}),d.jsx("tbody",{children:k.map((j,u)=>d.jsx("tr",{className:"even:bg-slate-50 dark:even:bg-slate-800/40",children:j.map((l,m)=>d.jsx("td",{className:"px-4 py-2.5 align-top text-slate-700 dark:text-slate-300 border-b border-slate-100 dark:border-slate-800",children:vn(l)},m))},u))})]})},c)),i=v-1;continue}if(o.trim()!=="```"){if(!o.trim()){s.push(d.jsx("br",{},c));continue}if(o.startsWith("### ")){s.push(d.jsx("h3",{className:"text-lg font-semibold text-slate-900 dark:text-slate-100 mt-4 mb-2",children:o.slice(4)},c));continue}if(o.startsWith("## ")){s.push(d.jsx("h2",{className:"text-xl font-bold text-slate-900 dark:text-slate-100 mt-6 mb-3",children:o.slice(3)},c));continue}if(o.startsWith("# ")){s.push(d.jsx("h1",{className:"text-2xl font-bold text-slate-900 dark:text-slate-100 mt-6 mb-4",children:o.slice(2)},c));continue}if(o.startsWith("> ")){s.push(d.jsx("blockquote",{className:"border-l-4 border-blue-500 pl-4 italic text-slate-700 dark:text-slate-300 my-3",children:o.slice(2)},c));continue}if(o.startsWith("- ")||o.startsWith("* ")){s.push(d.jsx("li",{className:"ml-6 list-disc text-slate-700 dark:text-slate-300 leading-relaxed",children:vn(o.slice(2))},c));continue}if(o.match(/^\d+\.\s/)){s.push(d.jsx("li",{className:"ml-6 list-decimal text-slate-700 dark:text-slate-300 leading-relaxed",children:vn(o.replace(/^\d+\.\s/,""))},c));continue}s.push(d.jsx("p",{className:"text-slate-700 dark:text-slate-300 leading-relaxed my-2",children:vn(o)},c))}}return s})})}const Mp=new Set("abstract assert boolean break byte case catch char class const continue default do double else enum extends final finally float for goto if implements import instanceof int interface long native new package private protected public return short static strictfp super switch synchronized this throw throws transient try void volatile while var let function async await yield typeof instanceof of in delete null true false undefined NaN".split(" "));function Wp(e){const t=/(\/\/[^\n]*|\/\*[\s\S]*?\*\/)|("(?:\\.|[^"\\])*"|'(?:\\.|[^'\\])*'|`(?:\\.|[^`\\])*`)|(@\w+)|(\b\d[\d.xXa-fA-F_]*\b)|(\b[A-Za-z_$][\w$]*\b)/g,n=[];let a=0,r,s=0;for(;r=t.exec(e);){r.index>a&&n.push(e.slice(a,r.index));const i=s++;if(r[1])n.push(d.jsx("span",{className:"text-slate-500 italic",children:r[1]},i));else if(r[2])n.push(d.jsx("span",{className:"text-emerald-400",children:r[2]},i));else if(r[3])n.push(d.jsx("span",{className:"text-amber-400",children:r[3]},i));else if(r[4])n.push(d.jsx("span",{className:"text-orange-400",children:r[4]},i));else if(r[5]){const o=r[5];Mp.has(o)?n.push(d.jsx("span",{className:"text-purple-400 font-medium",children:o},i)):/^[A-Z]/.test(o)?n.push(d.jsx("span",{className:"text-cyan-300",children:o},i)):e[t.lastIndex]==="("?n.push(d.jsx("span",{className:"text-blue-400",children:o},i)):n.push(o)}a=t.lastIndex}return a<e.length&&n.push(e.slice(a)),n}function vn(e){return e.split(/(`[^`]+`|\*\*[^*]+\*\*|\*[^*]+\*|~~[^~]+~~)/g).map((n,a)=>n.startsWith("`")&&n.endsWith("`")?d.jsx("code",{className:"bg-slate-100 dark:bg-slate-800 px-1.5 py-0.5 rounded text-sm font-mono text-slate-900 dark:text-slate-100",children:n.slice(1,-1)},a):n.startsWith("**")&&n.endsWith("**")?d.jsx("strong",{className:"font-semibold text-slate-900 dark:text-slate-100",children:n.slice(2,-2)},a):n.startsWith("*")&&n.endsWith("*")?d.jsx("em",{className:"italic text-slate-700 dark:text-slate-300",children:n.slice(1,-1)},a):n.startsWith("~~")&&n.endsWith("~~")?d.jsx("del",{className:"line-through text-slate-500 dark:text-slate-400",children:n.slice(2,-2)},a):d.jsx("span",{children:n},a))}function Hp({question:e,isActive:t,onClick:n}){const a={hld:"bg-indigo-500/10 text-indigo-600 dark:bg-indigo-500/20 dark:text-indigo-400 border-indigo-500/20",java:"bg-orange-500/10 text-orange-600 dark:bg-orange-500/20 dark:text-orange-400 border-orange-500/20",react:"bg-cyan-500/10 text-cyan-600 dark:bg-cyan-500/20 dark:text-cyan-400 border-cyan-500/20",node:"bg-green-500/10 text-green-600 dark:bg-green-500/20 dark:text-green-400 border-green-500/20",sql:"bg-purple-500/10 text-purple-600 dark:bg-purple-500/20 dark:text-purple-400 border-purple-500/20",microservices:"bg-rose-500/10 text-rose-600 dark:bg-rose-500/20 dark:text-rose-400 border-rose-500/20","design-patterns":"bg-amber-500/10 text-amber-600 dark:bg-amber-500/20 dark:text-amber-400 border-amber-500/20",kafka:"bg-slate-500/10 text-slate-600 dark:bg-slate-500/20 dark:text-slate-400 border-slate-500/20",default:"bg-blue-500/10 text-blue-600 dark:bg-blue-500/20 dark:text-blue-400 border-blue-500/20"},r={Basic:"bg-green-500/10 text-green-600 dark:bg-green-500/20 dark:text-green-400 border-green-500/20",Intermediate:"bg-blue-500/10 text-blue-600 dark:bg-blue-500/20 dark:text-blue-400 border-blue-500/20",Advanced:"bg-orange-500/10 text-orange-600 dark:bg-orange-500/20 dark:text-orange-400 border-orange-500/20",Experienced:"bg-red-500/10 text-red-600 dark:bg-red-500/20 dark:text-red-400 border-red-500/20",default:"bg-slate-500/10 text-slate-600 dark:bg-slate-500/20 dark:text-slate-400 border-slate-500/20"},s=e.tech==="design-patterns"?"DESIGN PATTERN":e.tech==="hld"?"HLD":e.tech.toUpperCase();return d.jsxs("button",{className:`question-link w-full text-left px-3 py-2.5 rounded-lg transition-all duration-150 ${t?"bg-blue-50 dark:bg-blue-900/30 text-slate-900 dark:text-slate-100 border-l-3 border-blue-500 shadow-sm":"text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800/50"}`,onClick:n,children:[d.jsxs("div",{className:"flex items-start gap-2.5",children:[d.jsxs("span",{className:"qnum flex-shrink-0 text-[11px] font-mono font-semibold text-slate-400 dark:text-slate-500 mt-0.5 w-10 text-right",children:["Q",e.displayNumber]}),d.jsx("span",{className:"text-sm font-medium leading-snug truncate text-slate-900 dark:text-slate-100",children:e.question})]}),d.jsxs("div",{className:"flex items-center gap-2 mt-2 pt-2 border-t border-slate-100 dark:border-slate-800",children:[d.jsx("span",{className:`badge px-2.5 py-1 text-[10px] font-medium rounded-full border ${a[e.tech]||a.default}`,children:s}),d.jsx("span",{className:`badge px-2.5 py-1 text-[10px] font-medium rounded-full border ${r[e.difficulty]||r.default}`,children:e.difficulty})]})]})}function Bp({value:e,onChange:t,placeholder:n}){return d.jsxs("div",{className:"relative",children:[d.jsx("svg",{className:"absolute left-3 top-1/2 -translate-y-1/2 w-5 h-5 text-slate-400",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"})}),d.jsx("input",{type:"text",className:"search w-full pl-10 pr-10 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 placeholder-slate-500 dark:placeholder-slate-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all",placeholder:n,value:e,onChange:a=>t(a.target.value),"aria-label":"Search questions"}),e&&d.jsx("button",{onClick:()=>t(""),className:"absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-700 dark:hover:text-slate-300 transition-colors","aria-label":"Clear search",children:d.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M6 18L18 6M6 6l12 12"})})})]})}function Go({value:e,onChange:t}){const n=[{value:"all",label:"All",active:"bg-blue-500 text-white shadow-sm shadow-blue-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z"})})},{value:"hld",label:"HLD",active:"bg-indigo-500 text-white shadow-sm shadow-indigo-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"})})},{value:"java",label:"Java",active:"bg-orange-500 text-white shadow-sm shadow-orange-500/30",icon:d.jsxs("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:[d.jsx("path",{d:"M14.9 2.1L15.5 1c.3-.2.7-.2 1 0l1.2 1.6 1.4 2.5c.1.2.4.4.7.4h.1c.3 0 .6-.2.7-.4l.1-.1 1.4-2.5c.3-.2.7-.2 1 0l.6 1.1c.5.9.3 2-.5 2.7L17.7 11H19c.6 0 1 .4 1 1v8c0 .6-.4 1-1 1H5c-.6 0-1-.4-1-1v-8c0-.6.4-1 1-1h1.3l1.7-5.1c-.8-.7-1-1.8-.5-2.7zm-7.4 0l.2.4H6c-.6 0-1 .4-1 1v8c0 .6.4 1 1 1h14c.6 0 1-.4 1-1v-8c0-.6-.4-1-1-1h-1.5l.2-.4c0-.2-.1-.3-.2-.4l-.2-.4c-.3-.2-.7-.2-1 0L13 3H11l-.8 1.8c-.3.2-.7.2-1 0z"}),d.jsx("path",{d:"M8 11h8v8H8z"})]})},{value:"react",label:"React",active:"bg-cyan-500 text-white shadow-sm shadow-cyan-500/30",icon:d.jsxs("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:[d.jsx("circle",{cx:"12",cy:"12",r:"3"}),d.jsx("path",{d:"M12 2C6.5 2 2 6.5 2 12s4.5 10 10 10 10-4.5 10-10S17.5 2 12 2zm0 18c-4.4 0-8-3.6-8-8s3.6-8 8-8 8 3.6 8 8-3.6 8-8 8z"}),d.jsx("ellipse",{cx:"12",cy:"12",rx:"9",ry:"3",fill:"none",stroke:"currentColor",strokeWidth:"1.5"}),d.jsx("ellipse",{cx:"12",cy:"12",rx:"9",ry:"3",fill:"none",stroke:"currentColor",strokeWidth:"1.5",transform:"rotate(60 12 12)"}),d.jsx("ellipse",{cx:"12",cy:"12",rx:"9",ry:"3",fill:"none",stroke:"currentColor",strokeWidth:"1.5",transform:"rotate(120 12 12)"})]})},{value:"node",label:"Node",active:"bg-green-500 text-white shadow-sm shadow-green-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{d:"M12 2L2 7v10l10 5 10-5V7L12 2zm0 2.18l7.5 3.75v8.64L12 21.82 4.5 18.07v-8.64L12 4.18zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"})})},{value:"sql",label:"SQL",active:"bg-purple-500 text-white shadow-sm shadow-purple-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{d:"M12 2c-4.42 0-8 1.34-8 3v14c0 1.66 3.58 3 8 3s8-1.34 8-3V5c0-1.66-3.58-3-8-3zm6 17c0 .3-2.13 1.5-6 1.5S6 19.3 6 19v-2.23c1.5.77 3.72 1.23 6 1.23s4.5-.46 6-1.23V19zm0-4.5c0 .3-2.13 1.5-6 1.5s-6-1.2-6-1.5v-2.23c1.5.77 3.72 1.23 6 1.23s4.5-.46 6-1.23v2.23zM12 8.5C8.13 8.5 6 7.3 6 7s2.13-1.5 6-1.5S18 6.7 18 7s-2.13 1.5-6 1.5z"})})},{value:"microservices",label:"Microservices",active:"bg-rose-500 text-white shadow-sm shadow-rose-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{d:"M12 2a3 3 0 00-1 5.83V10H7a3 3 0 00-3 3v1.17a3 3 0 101.99.01V13a1 1 0 011-1h10a1 1 0 011 1v1.18A3 3 0 1020 14.17V13a3 3 0 00-3-3h-4V7.83A3 3 0 0012 2z"})})},{value:"kafka",label:"Kafka",active:"bg-slate-700 text-white shadow-sm shadow-slate-700/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{d:"M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 18c-4.41 0-8-3.59-8-8s3.59-8 8-8 8 3.59 8 8-3.59 8-8 8zm-1-13h2v6h-2zm0 8h2v2h-2z"})})},{value:"design-patterns",label:"Design Pattern",active:"bg-amber-500 text-white shadow-sm shadow-amber-500/30",icon:d.jsx("svg",{className:"w-4 h-4",fill:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{d:"M20.5 11H19V7a2 2 0 00-2-2h-4V3.5a2.5 2.5 0 00-5 0V5H4a2 2 0 00-2 2v3.8h1.5a2.2 2.2 0 010 4.4H2V19a2 2 0 002 2h3.8v-1.5a2.2 2.2 0 014.4 0V21H17a2 2 0 002-2v-4h1.5a2.5 2.5 0 000-5z"})})}];return d.jsx("div",{className:"segmented grid grid-cols-3 gap-1 bg-slate-100 dark:bg-slate-800/50 p-1 rounded-lg",role:"group","aria-label":"Filter by technology",children:n.map(a=>d.jsxs("button",{className:`w-full min-w-0 flex items-center justify-center gap-1.5 px-2 py-1.5 rounded-md text-xs font-medium whitespace-nowrap truncate transition-all duration-150 ${e===a.value?a.active:"text-slate-700 dark:text-slate-300 hover:text-slate-900 dark:hover:text-slate-100"}`,onClick:()=>t(a.value),"aria-pressed":e===a.value,title:a.label,children:[a.icon,d.jsx("span",{children:a.label})]},a.value))})}function Vo({value:e,onChange:t,options:n,disabled:a}){return a||n.length===0?null:d.jsxs("div",{className:"relative",children:[d.jsx("svg",{className:"absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M3 7h18M3 12h18M3 17h18"})}),d.jsxs("select",{className:"select w-full pl-10 pr-10 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all cursor-pointer appearance-none",value:e,onChange:r=>t(r.target.value),"aria-label":"Filter by category",children:[d.jsx("option",{value:"all",children:"All categories"}),n.map(r=>d.jsx("option",{value:r,children:r},r))]}),d.jsx("svg",{className:"absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none",fill:"none",stroke:"currentColor",viewBox:"0 0 20 20","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M6 8l4 4 4-4"})})]})}function _o({value:e,onChange:t,questions:n,tech:a}){const r=["Basic","Intermediate","Advanced","Experienced"],s=G.useMemo(()=>{const i=n.filter(c=>a==="all"||c.tech===a),o=new Set(i.map(c=>c.difficulty));return r.filter(c=>o.has(c))},[n,a]);return d.jsxs("div",{className:"relative",children:[d.jsx("svg",{className:"absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M13 10V3L4 14h7v7l9-11h-7z"})}),d.jsxs("select",{className:"select w-full pl-10 pr-10 py-2.5 bg-slate-50 dark:bg-slate-800/50 border border-slate-200 dark:border-slate-700 rounded-lg text-sm text-slate-900 dark:text-slate-100 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all cursor-pointer appearance-none",value:e,onChange:i=>t(i.target.value),"aria-label":"Filter by difficulty",children:[d.jsx("option",{value:"all",children:"All difficulties"}),s.map(i=>d.jsx("option",{value:i,children:i},i))]}),d.jsx("svg",{className:"absolute right-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400 pointer-events-none",fill:"none",stroke:"currentColor",viewBox:"0 0 20 20","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M6 8l4 4 4-4"})})]})}function Qo({count:e,total:t}){return d.jsx("div",{className:"count text-xs text-slate-500 dark:text-slate-400 flex items-center justify-between",children:d.jsxs("span",{children:[e," of ",t," questions"]})})}function cd({questions:e,filtered:t,selectedId:n,query:a,setQuery:r,tech:s,setTech:i,category:o,setCategory:c,difficulty:p,setDifficulty:f,onSelect:y,onToggleDark:h,isDark:v,className:w="",isMobile:k=!1,sidebarWidth:j=360}){const u=G.useMemo(()=>[...new Set(e.filter(g=>s==="all"||g.tech===s).map(g=>g.category))].sort(),[s,e]),[l,m]=G.useState(!k);return d.jsxs("aside",{className:`sidebar sidebar-responsive h-full flex flex-col bg-white dark:bg-slate-950 border-r border-slate-200 dark:border-slate-800 ${k?"":"hidden lg:flex"} ${w}`,style:{width:`${j}px`,minWidth:"280px",maxWidth:"600px",flexShrink:0},children:[d.jsx("div",{className:"brand p-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:d.jsxs("div",{className:"flex items-center justify-between gap-3",children:[d.jsxs("div",{className:"flex items-center gap-3 flex-1 min-w-0",children:[d.jsx("div",{className:"logo w-10 h-10 rounded-xl bg-gradient-to-br from-blue-500 to-indigo-600 flex items-center justify-center text-white font-bold text-lg shadow-lg flex-shrink-0",children:"JR"}),d.jsxs("div",{className:"min-w-0",children:[d.jsx("h1",{className:"text-xl font-bold text-slate-900 dark:text-slate-100 truncate",children:"Interview Reader"}),d.jsxs("p",{className:"text-xs text-slate-500 dark:text-slate-400 truncate",children:[e.length," Java, React, Node & SQL questions"]})]})]}),d.jsx("button",{onClick:h,className:"flex-shrink-0 p-2 rounded-lg bg-slate-100 dark:bg-slate-800 text-slate-700 dark:text-slate-300 hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors","aria-label":"Toggle dark mode","aria-pressed":v,children:v?d.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"})}):d.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"})})})]})}),d.jsx("div",{className:"p-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:d.jsx(Bp,{value:a,onChange:r,placeholder:"Search questions..."})}),k?d.jsxs("div",{className:"border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:[d.jsxs("button",{onClick:()=>m(!l),className:"w-full px-4 py-3 flex items-center justify-between text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-50 dark:hover:bg-slate-800/50 transition-colors","aria-expanded":l,"aria-controls":"mobile-filters",children:[d.jsxs("span",{className:"flex items-center gap-2",children:[d.jsx("svg",{className:"w-5 h-5",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z"})}),"Filters"]}),d.jsx("svg",{className:`w-4 h-4 text-slate-500 transition-transform ${l?"rotate-180":""}`,fill:"none",stroke:"currentColor",viewBox:"0 0 24 24","aria-hidden":"true",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M19 9l-7 7-7-7"})})]}),d.jsx("div",{id:"mobile-filters",className:`${l?"block":"hidden"}`,children:d.jsxs("div",{className:"p-4 space-y-4",children:[d.jsx(Go,{value:s,onChange:i}),d.jsx(Vo,{value:o,onChange:c,options:u,disabled:s==="all"&&u.length>50}),d.jsx(_o,{value:p,onChange:f,questions:e,tech:s}),d.jsx(Qo,{count:t.length,total:e.length})]})})]}):d.jsxs("div",{className:"p-4 space-y-4 border-b border-slate-200 dark:border-slate-800 flex-shrink-0",children:[d.jsx(Go,{value:s,onChange:i}),d.jsx(Vo,{value:o,onChange:c,options:u,disabled:s==="all"&&u.length>50}),d.jsx(_o,{value:p,onChange:f,questions:e,tech:s}),d.jsx(Qo,{count:t.length,total:e.length})]}),d.jsx("nav",{className:"question-list flex-1 overflow-y-auto p-3 space-y-1","aria-label":"Question list",children:t.length===0?d.jsxs("div",{className:"text-center py-8 text-slate-500 dark:text-slate-400",children:[d.jsx("svg",{className:"w-12 h-12 mx-auto mb-3 opacity-50",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"})}),d.jsx("p",{children:"No questions match your filters"})]}):t.map(g=>d.jsx(Hp,{question:g,isActive:n===g.id,onClick:()=>y(g.id)},g.id))})]})}function Fp({question:e,questions:t,onNavigate:n}){if(!e)return d.jsx("main",{className:"reader flex-1 flex items-center justify-center bg-slate-50 dark:bg-slate-950 min-w-0",children:d.jsxs("div",{className:"text-center p-4 sm:p-8 w-full max-w-3xl mx-auto",children:[d.jsx("svg",{className:"w-16 h-16 mx-auto mb-4 text-slate-300 dark:text-slate-700",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:1.5,d:"M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"})}),d.jsx("h2",{className:"text-xl font-semibold text-slate-900 dark:text-slate-100 mb-2",children:"Select a question"}),d.jsx("p",{className:"text-slate-500 dark:text-slate-400",children:"Choose from the sidebar to start reading"})]})});const a=t.findIndex(i=>i.id===e.id),r=t[a-1],s=t[a+1];return d.jsxs("main",{className:"reader flex-1 h-screen overflow-hidden bg-slate-50 dark:bg-slate-950 flex flex-col min-w-0",children:[d.jsxs("article",{className:"paper flex-1 overflow-y-auto p-4 sm:p-6 lg:p-8 max-w-3xl mx-auto w-full min-w-0",children:[d.jsxs("div",{className:"lg:hidden mb-4 flex items-center justify-between px-2",children:[d.jsxs("button",{disabled:!r,onClick:()=>n(r.id),className:"px-3 py-1.5 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-1.5","aria-label":"Previous question",children:[d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M15 19l-7-7 7-7"})}),d.jsx("span",{className:"hidden sm:inline",children:"Previous"})]}),d.jsxs("div",{className:"text-xs text-slate-500 dark:text-slate-400 flex-1 text-center px-2",children:[a+1," / ",t.length]}),d.jsxs("button",{disabled:!s,onClick:()=>n(s.id),className:"px-3 py-1.5 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-1.5","aria-label":"Next question",children:[d.jsx("span",{className:"hidden sm:inline",children:"Next"}),d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M9 5l7 7-7 7"})})]})]}),d.jsxs("header",{className:"mb-6 pb-4 border-b border-slate-200 dark:border-slate-800",children:[d.jsxs("div",{className:"badges flex flex-wrap gap-2 mb-4",children:[d.jsx("span",{className:`badge px-3 py-1 text-xs font-medium rounded-full ${e.tech==="hld"?"bg-indigo-100 text-indigo-700 dark:bg-indigo-900/30 dark:text-indigo-300":e.tech==="java"?"bg-orange-100 text-orange-700 dark:bg-orange-900/30 dark:text-orange-300":e.tech==="node"?"bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300":e.tech==="sql"?"bg-purple-100 text-purple-700 dark:bg-purple-900/30 dark:text-purple-300":e.tech==="microservices"?"bg-rose-100 text-rose-700 dark:bg-rose-900/30 dark:text-rose-300":e.tech==="design-patterns"?"bg-amber-100 text-amber-700 dark:bg-amber-900/30 dark:text-amber-300":e.tech==="kafka"?"bg-slate-200 text-slate-800 dark:bg-slate-700/50 dark:text-slate-200":"bg-cyan-100 text-cyan-700 dark:bg-cyan-900/30 dark:text-cyan-300"}`,children:e.tech==="design-patterns"?"DESIGN PATTERN":e.tech==="hld"?"HLD":e.tech.toUpperCase()}),d.jsx("span",{className:"badge px-3 py-1 text-xs font-medium rounded-full bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-300",children:e.category}),d.jsx("span",{className:`badge px-3 py-1 text-xs font-medium rounded-full ${e.difficulty==="Basic"?"bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-300":e.difficulty==="Intermediate"?"bg-yellow-100 text-yellow-700 dark:bg-yellow-900/30 dark:text-yellow-300":"bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-300"}`,children:e.difficulty})]}),d.jsxs("h2",{className:"question-title text-xl sm:text-2xl lg:text-3xl font-bold text-slate-900 dark:text-slate-100 leading-tight",children:["Q",e.displayNumber,". ",e.question]})]}),d.jsx("div",{className:"answer-content",children:d.jsx(Kp,{text:e.answer})})]}),d.jsx("footer",{className:"hidden lg:block pager flex-shrink-0 border-t border-slate-200 dark:border-slate-800 bg-slate-50 dark:bg-slate-950",children:d.jsxs("div",{className:"max-w-3xl mx-auto w-full px-6 lg:px-8 py-4 flex items-center justify-between",children:[d.jsxs("button",{disabled:!r,onClick:()=>n(r.id),className:"px-4 py-2 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-2",children:[d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M15 19l-7-7 7-7"})}),"Previous"]}),d.jsxs("div",{className:"text-sm text-slate-500 dark:text-slate-400",children:["Question ",a+1," of ",t.length]}),d.jsxs("button",{disabled:!s,onClick:()=>n(s.id),className:"px-4 py-2 text-sm font-medium text-slate-700 dark:text-slate-300 hover:bg-slate-100 dark:hover:bg-slate-800 rounded-lg disabled:opacity-30 disabled:cursor-not-allowed transition-colors flex items-center gap-2",children:["Next",d.jsx("svg",{className:"w-4 h-4",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M9 5l7 7-7 7"})})]})]})})]})}function Up({isOpen:e,onToggle:t}){return d.jsx("button",{className:"lg:hidden fixed bottom-4 right-4 z-50 w-12 h-12 rounded-full bg-blue-600 text-white shadow-lg flex items-center justify-center hover:bg-blue-700 transition-colors touch-target",onClick:t,"aria-label":e?"Close menu":"Open menu","aria-expanded":e,children:e?d.jsx("svg",{className:"w-6 h-6",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M6 18L18 6M6 6l12 12"})}):d.jsx("svg",{className:"w-6 h-6",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M4 6h16M4 12h16M4 18h16"})})})}function zp({isOpen:e,onClose:t,...n}){return e?d.jsxs(d.Fragment,{children:[d.jsx("div",{className:"fixed inset-0 bg-black/50 z-40 lg:hidden",onClick:t,"aria-hidden":"true"}),d.jsx("aside",{className:"fixed inset-y-0 left-0 z-50 w-80 max-w-[85vw] bg-white dark:bg-slate-950 shadow-xl lg:hidden transform transition-transform duration-300 ease-out translate-x-0",children:d.jsx(cd,{...n,isMobile:!0})})]}):null}function Jp(){var R;const[e,t]=G.useState((R=bn[0])==null?void 0:R.id),[n,a]=G.useState(""),[r,s]=G.useState("all"),[i,o]=G.useState("all"),[c,p]=G.useState("all"),[f,y]=G.useState(!1),[h,v]=G.useState(()=>{if(typeof window<"u"){const S=localStorage.getItem("theme");return S?S==="dark":window.matchMedia("(prefers-color-scheme: dark)").matches}return!1}),[w,k]=G.useState(360),j=G.useMemo(()=>{const S=n.trim().toLowerCase();return bn.filter(N=>{const A=r==="all"||N.tech===r,ne=i==="all"||N.category===i,De=c==="all"||N.difficulty===c,Be=`q${N.displayNumber} ${N.question} ${N.answer} ${N.category}`.toLowerCase();return A&&ne&&De&&(!S||Be.includes(S))}).sort((N,A)=>(N.sortKey||N.displayNumber||0)-(A.sortKey||A.displayNumber||0))},[n,r,i,c]),u=j.find(S=>S.id===e)||j[0]||bn[0],l=G.useCallback(()=>{v(S=>{const N=!S;return localStorage.setItem("theme",N?"dark":"light"),document.documentElement.classList.toggle("dark",N),N})},[]),m=G.useCallback(S=>{t(S),y(!1),window.innerWidth<1024&&window.scrollTo({top:0,behavior:"smooth"})},[]),g=G.useCallback(S=>{t(S),y(!1),window.innerWidth<1024&&window.scrollTo({top:0,behavior:"smooth"})},[]),C=G.useCallback(S=>{s(S),o("all")},[]),T=G.useCallback(S=>{S.preventDefault();const N=S.clientX,A=w,ne=Be=>{const ta=Be.clientX-N,vr=Math.min(Math.max(A+ta,280),600);k(vr)},De=()=>{document.removeEventListener("mousemove",ne),document.removeEventListener("mouseup",De),document.body.style.cursor="",document.body.style.userSelect=""};document.addEventListener("mousemove",ne),document.addEventListener("mouseup",De),document.body.style.cursor="col-resize",document.body.style.userSelect="none"},[w]);return d.jsxs("div",{className:"app-shell h-screen overflow-hidden bg-slate-50 dark:bg-slate-950 flex",children:[d.jsx(cd,{questions:bn,filtered:j,selectedId:e,query:n,setQuery:a,tech:r,setTech:C,category:i,setCategory:o,difficulty:c,setDifficulty:p,onSelect:m,onToggleDark:l,isDark:h,sidebarWidth:w}),f&&d.jsx("div",{className:"lg:hidden w-80 max-w-[85vw]","aria-hidden":"true"}),d.jsx(zp,{isOpen:f,onClose:()=>y(!1),questions:bn,filtered:j,selectedId:e,query:n,setQuery:a,tech:r,setTech:C,category:i,setCategory:o,difficulty:c,setDifficulty:p,onSelect:m,onToggleDark:l,isDark:h}),d.jsx(Up,{isOpen:f,onToggle:()=>y(!f)}),d.jsx("div",{className:"hidden lg:block w-1 cursor-col-resize bg-slate-200 dark:bg-slate-700 hover:bg-slate-300 dark:hover:bg-slate-600 transition-colors flex-shrink-0 select-none",onMouseDown:T,role:"separator","aria-label":"Resize sidebar","aria-orientation":"vertical",tabIndex:0,onKeyDown:S=>{S.key==="ArrowLeft"?k(N=>Math.max(N-20,280)):S.key==="ArrowRight"&&k(N=>Math.min(N+20,600))},style:{background:"linear-gradient(180deg, transparent 20%, currentColor 20%, currentColor 80%, transparent 80%)",backgroundSize:"1px 20px"},children:d.jsx("div",{className:"absolute inset-y-0 left-1/2 -translate-x-1/2 w-4 h-4 flex items-center justify-center",style:{pointerEvents:"none"},children:d.jsx("svg",{className:"w-3 h-3 text-slate-300 dark:text-slate-600",fill:"none",stroke:"currentColor",viewBox:"0 0 24 24",children:d.jsx("path",{strokeLinecap:"round",strokeLinejoin:"round",strokeWidth:2,d:"M5 12h14M5 12a2 2 0 010-4h14a2 2 0 010 4M5 12a2 2 0 000 4h14a2 2 0 000-4"})})})}),d.jsx(Fp,{question:u,questions:j,onNavigate:g})]})}Qr.createRoot(document.getElementById("root")).render(d.jsx(Ed.StrictMode,{children:d.jsx(Jp,{})}));
